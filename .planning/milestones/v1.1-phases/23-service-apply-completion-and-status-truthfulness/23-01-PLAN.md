---
phase: 23-service-apply-completion-and-status-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/service/launchd.rs
  - packages/open-bitcoin-cli/src/operator/service/systemd.rs
  - packages/open-bitcoin-cli/src/operator/service/tests.rs
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-04-28T17-18-36
generated_at: 2026-04-28T17:18:36.921Z

must_haves:
  truths:
    - "`open-bitcoin service install --apply` runs the same launchd registration steps that dry-run previews instead of stopping after plist write."
    - "`open-bitcoin service install --apply` runs the same systemd reload and enable steps that dry-run previews instead of stopping after unit write."
    - "Dry-run previews stay truthful because command preview helpers and apply-mode execution stay aligned."
    - "Service verification remains hermetic through parser or dry-run tests, not real launchd or systemd mutation."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/service/launchd.rs"
      provides: "launchd install preview and apply sequencing"
    - path: "packages/open-bitcoin-cli/src/operator/service/systemd.rs"
      provides: "systemd install preview and apply sequencing"
    - path: "packages/open-bitcoin-cli/src/operator/service/tests.rs"
      provides: "dry-run preview and parser coverage"
  key_links:
    - from: "service/launchd.rs"
      to: "service/tests.rs"
      via: "dry-run preview command assertions and parser coverage"
    - from: "service/systemd.rs"
      to: "service/tests.rs"
      via: "dry-run preview command assertions and parser coverage"
---

# Phase 23 Plan 01: Complete Apply Semantics

## Objective

Repair the launchd and systemd install apply paths so they finish the manager
registration sequence immediately after writing the generated service file, and
prove the new sequencing with hermetic tests.

## Scope

- `LaunchdAdapter::install()` preview and apply sequencing
- `SystemdAdapter::install()` preview and apply sequencing
- Parser or preview tests that keep the change hermetic

## Tasks

1. Add small helper functions that define the preview command sequence for
   launchd and systemd installs.
2. Update apply mode to execute those same commands after the service file is
   written.
3. Add focused service tests that assert the previewed command list matches the
   expected launchd and systemd registration steps.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service::tests -- --nocapture`
