---
phase: 32-benchmark-wrapper-list-mode-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-benchmarks.sh
  - scripts/verify.sh
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-04-29T22-28-38
generated_at: 2026-04-29T22:28:38.658Z

must_haves:
  truths:
    - "`bash scripts/run-benchmarks.sh --list` no longer aborts before the Cargo invocation is initialized."
    - "Existing `--smoke` and `--full` wrapper behavior remains unchanged."
    - "The repo-native verification contract now exercises the wrapper `--list` path as a cheap regression guard."
  artifacts:
    - path: "scripts/run-benchmarks.sh"
      provides: "fixed list-mode control flow and shared Cargo invocation"
    - path: "scripts/verify.sh"
      provides: "durable list-mode regression coverage"
  key_links:
    - from: "scripts/run-benchmarks.sh"
      to: "packages/open-bitcoin-bench/src/main.rs"
      via: "shared list/smoke/full benchmark binary entrypoint"
    - from: "scripts/verify.sh"
      to: "scripts/run-benchmarks.sh"
      via: "repo-native list-mode regression check"
---

# Phase 32 Plan 01: Repair List Mode And Add A Durable Guard

## Objective

Fix the benchmark wrapper's `--list` fast path and add a cheap repo-native
regression guard that exercises the same contributor-facing command surface.

## Scope

- Shell wrapper control flow in `scripts/run-benchmarks.sh`
- Bounded benchmark-related verification wiring in `scripts/verify.sh`

## Tasks

1. Build the shared Cargo invocation before the `--list` fast path so list mode
   can reuse the benchmark binary entrypoint without selecting smoke or full.
2. Preserve the current `--list` exclusivity guard and existing smoke/full
   argument behavior.
3. Add a `bash scripts/run-benchmarks.sh --list` check to `scripts/verify.sh`
   as the durable regression guard for this optional cleanup.

## Verification

- `bash scripts/run-benchmarks.sh --list`
- `bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports`
- `bash scripts/verify.sh`
