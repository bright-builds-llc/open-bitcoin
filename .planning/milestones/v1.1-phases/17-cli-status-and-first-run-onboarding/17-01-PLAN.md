---
phase: 17-cli-status-and-first-run-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/config.rs
  - packages/open-bitcoin-cli/src/operator/config/tests.rs
  - packages/open-bitcoin-cli/src/operator/detect.rs
  - packages/open-bitcoin-cli/src/operator/detect/tests.rs
  - packages/open-bitcoin-cli/src/operator/onboarding.rs
  - packages/open-bitcoin-cli/src/operator/onboarding/tests.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [CLI-03, CLI-05, CLI-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-04-26T23-56-00
generated_at: 2026-04-27T00:10:29Z
user_setup: []
must_haves:
  truths:
    - "The operator command tree has typed contracts for config, detection, status, onboarding, and command runtime execution."
    - "The existing open-bitcoin-cli compatibility route remains unchanged while open-bitcoin operator contracts compile."
    - "Every new first-party Rust source/test file under breadcrumb scope is registered in docs/parity/source-breadcrumbs.json."
  artifacts:
    - path: packages/open-bitcoin-cli/src/operator.rs
      provides: "Clap operator root and child module declarations"
      contains: "pub mod config;"
    - path: packages/open-bitcoin-cli/src/operator/config.rs
      provides: "Operator config source, path, and precedence contract types"
      exports: ["OperatorConfigResolution", "OperatorConfigSource"]
    - path: packages/open-bitcoin-cli/src/operator/detect.rs
      provides: "Read-only Core/Knots detection contract types"
      exports: ["DetectedInstallation", "DetectionRoots"]
    - path: packages/open-bitcoin-cli/src/operator/onboarding.rs
      provides: "Onboarding request, plan, and write-decision contract types"
      exports: ["OnboardingRequest", "OnboardingPlan"]
    - path: packages/open-bitcoin-cli/src/operator/status.rs
      provides: "Status request, collector input, and render-mode contract types"
      exports: ["StatusRequest", "StatusRenderMode"]
    - path: packages/open-bitcoin-cli/src/operator/runtime.rs
      provides: "Operator command outcome and runtime error contracts"
      exports: ["OperatorCommandOutcome", "OperatorRuntimeError"]
  key_links:
    - from: packages/open-bitcoin-cli/src/operator.rs
      to: packages/open-bitcoin-cli/src/operator/config.rs
      via: "Rust child module declaration"
      pattern: "pub mod config;"
    - from: docs/parity/source-breadcrumbs.json
      to: packages/open-bitcoin-cli/src/operator
      via: "breadcrumb manifest entries for new Rust files"
      pattern: "operator/config.rs"
---

<objective>
Create the typed operator module contracts that the rest of Phase 17 implements.

Purpose: Keep the phase from devolving into renderer-local structs or ad hoc command shells, while preserving the Phase 13 `open-bitcoin` versus `open-bitcoin-cli` command boundary.
Output: Operator child modules, shared contracts, compile-time module wiring, and parity breadcrumb registrations for new Rust files.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-cli-status-and-first-run-onboarding/17-CONTEXT.md
@.planning/phases/17-cli-status-and-first-run-onboarding/17-RESEARCH.md
@.planning/phases/13-operator-runtime-foundations/13-04-SUMMARY.md
@.planning/phases/13-operator-runtime-foundations/13-05-SUMMARY.md
@docs/architecture/cli-command-architecture.md
@docs/architecture/config-precedence.md
@packages/open-bitcoin-cli/src/operator.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
From packages/open-bitcoin-cli/src/operator.rs:
```rust
pub struct OperatorCli {
    pub maybe_config_path: Option<PathBuf>,
    pub maybe_data_dir: Option<PathBuf>,
    pub maybe_network: Option<NetworkSelection>,
    pub format: OperatorOutputFormat,
    pub no_color: bool,
    pub command: OperatorCommand,
}

pub enum OperatorCommand {
    Status(StatusArgs),
    Config(ConfigArgs),
    Service(ServiceArgs),
    Dashboard(DashboardArgs),
    Onboard(OnboardArgs),
}

pub fn route_cli_invocation(binary_name: &str, args: &[OsString]) -> Result<CliRoute, CliError>;
```

From packages/open-bitcoin-rpc/src/config/open_bitcoin.rs:
```rust
pub struct OpenBitcoinConfig {
    pub schema_version: u32,
    pub onboarding: OnboardingConfig,
    pub metrics: MetricsConfig,
    pub logs: LogsConfig,
    pub service: ServiceConfig,
    pub dashboard: DashboardConfig,
    pub migration: MigrationConfig,
    pub storage: StorageConfig,
    pub sync: SyncConfig,
}

pub struct ConfigPrecedence;
impl ConfigPrecedence {
    pub const fn ordered_sources() -> [ConfigSource; 6];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config and detection contracts</name>
  <files>packages/open-bitcoin-cli/src/operator.rs, packages/open-bitcoin-cli/src/operator/config.rs, packages/open-bitcoin-cli/src/operator/config/tests.rs, packages/open-bitcoin-cli/src/operator/detect.rs, packages/open-bitcoin-cli/src/operator/detect/tests.rs</files>
  <read_first>
    - .planning/phases/17-cli-status-and-first-run-onboarding/17-CONTEXT.md
    - docs/architecture/cli-command-architecture.md
    - docs/architecture/config-precedence.md
    - packages/open-bitcoin-cli/src/operator.rs
    - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  </read_first>
  <behavior>
    - Test 1: `route_cli_invocation("open-bitcoin-cli", args)` still returns `CliRoute::BitcoinCliCompat(args)` exactly.
    - Test 2: `route_cli_invocation("open-bitcoin", ["status"])` still returns `CliRoute::Operator` with `OperatorCommand::Status`.
    - Test 3: config and detection contract types serialize or compare deterministically where required by downstream tests.
  </behavior>
  <action>Add child module declarations to `operator.rs` for `pub mod config;` and `pub mod detect;`. Create `operator/config.rs` with contract types only for `OperatorConfigSource`, `OperatorConfigResolution`, and path-report structs using D-09 source names. Create `operator/detect.rs` with contract types only for `DetectionRoots`, `DetectedInstallation`, source path, confidence, uncertainty, service-candidate, wallet-candidate, and product-family enums per D-12 and D-13. Create sibling test files `config/tests.rs` and `detect/tests.rs` with compile-time contract tests for type construction and stable source names. This split deliberately keeps config/detection contracts separate from status/onboarding/runtime contracts so each task stays reviewable.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/operator.rs` contains `pub mod config;` and `pub mod detect;`.
    - `packages/open-bitcoin-cli/src/operator/config.rs` contains the exact source names `cli_flags`, `environment`, `open_bitcoin_jsonc`, `bitcoin_conf`, `cookies`, and `defaults`.
    - `packages/open-bitcoin-cli/src/operator/config/tests.rs` and `detect/tests.rs` exist and are referenced by their parent modules with `#[cfg(test)] mod tests;`.
    - `packages/open-bitcoin-cli/src/operator/detect.rs` contains no calls matching `fs::write`, `remove_file`, `remove_dir`, `rename`, `copy`, `OpenOptions::new().write`, `launchctl`, or `systemctl`.
    - Existing operator routing tests still pass without changing `open-bitcoin-cli` compatibility behavior.
  </acceptance_criteria>
  <done>The config and detection contracts compile, route boundaries remain compatible, and no Core/Knots mutation path is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create status, onboarding, and runtime contracts</name>
  <files>packages/open-bitcoin-cli/src/operator.rs, packages/open-bitcoin-cli/src/operator/onboarding.rs, packages/open-bitcoin-cli/src/operator/onboarding/tests.rs, packages/open-bitcoin-cli/src/operator/runtime.rs, packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <read_first>
    - .planning/phases/17-cli-status-and-first-run-onboarding/17-CONTEXT.md
    - packages/open-bitcoin-cli/src/operator.rs
    - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  </read_first>
  <behavior>
    - Test 1: status contract types define request/render mode and adapter input contracts without introducing a renderer-local DTO or importing shared status types before the production dependency exists.
    - Test 2: onboarding contract types model interactive answers, non-interactive requests, proposed writes, and approved writes distinctly.
    - Test 3: runtime contract types preserve typed stdout/stderr/exit outcomes for the future `open-bitcoin` binary.
  </behavior>
  <action>Add child module declarations to `operator.rs` for `pub mod onboarding;`, `pub mod runtime;`, and `pub mod status;`. Create `operator/onboarding.rs` with contract types only for `OnboardingRequest`, injected prompt answers, `OnboardingPlan`, write decision variants for no write/proposed write/approved write, and user-facing messages per D-05 through D-08. Create `operator/runtime.rs` with `OperatorCommandOutcome` and `OperatorRuntimeError`. Create `operator/status.rs` with `StatusRequest`, `StatusRenderMode`, live RPC adapter input contracts, detection evidence inputs, and status collector input contracts, but do not import `OpenBitcoinStatusSnapshot` in this plan because `open-bitcoin-node` is not yet a production dependency of `open-bitcoin-cli`. Plan 17-04 owns adding the dependency and binding these status contracts to the shared snapshot per D-01 through D-04. Create sibling test files `onboarding/tests.rs` and `status/tests.rs` with compile-time contract tests.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/operator.rs` contains `pub mod onboarding;`, `pub mod runtime;`, and `pub mod status;`.
    - `packages/open-bitcoin-cli/src/operator/onboarding.rs` contains an explicit write decision type with variants for no write, proposed write, and approved write.
    - `packages/open-bitcoin-cli/src/operator/status.rs` contains no custom final JSON DTO named `StatusJson`, `StatusDto`, or `CliStatusSnapshot`.
    - `packages/open-bitcoin-cli/src/operator/status.rs` does not import or reference `OpenBitcoinStatusSnapshot`; that shared snapshot binding is deferred to Plan 17-04 after `open-bitcoin-node` is added as a production dependency.
    - `packages/open-bitcoin-cli/src/operator/onboarding/tests.rs` and `status/tests.rs` exist and are referenced by their parent modules with `#[cfg(test)] mod tests;`.
    - Existing operator routing tests still pass without changing `open-bitcoin-cli` compatibility behavior.
  </acceptance_criteria>
  <done>The status, onboarding, and runtime contracts compile with explicit prompt/write/status adapter surfaces and no deferred service/dashboard/migration behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register breadcrumbs and contract tests</name>
  <files>packages/open-bitcoin-cli/src/operator/config.rs, packages/open-bitcoin-cli/src/operator/config/tests.rs, packages/open-bitcoin-cli/src/operator/detect.rs, packages/open-bitcoin-cli/src/operator/detect/tests.rs, packages/open-bitcoin-cli/src/operator/onboarding.rs, packages/open-bitcoin-cli/src/operator/onboarding/tests.rs, packages/open-bitcoin-cli/src/operator/runtime.rs, packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/src/operator/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - docs/parity/README.md
    - docs/parity/source-breadcrumbs.json
    - packages/open-bitcoin-cli/src/operator/tests.rs
  </read_first>
  <behavior>
    - Test 1: breadcrumb verification passes after adding every new `packages/open-bitcoin-cli/src/operator/*.rs` file.
    - Test 2: operator contract tests prove the new source names and route boundaries are stable.
  </behavior>
  <action>Update `docs/parity/source-breadcrumbs.json` for every new first-party Rust source/test file created in this plan, including `operator/config.rs`, `operator/config/tests.rs`, `operator/detect.rs`, `operator/detect/tests.rs`, `operator/onboarding.rs`, `operator/onboarding/tests.rs`, `operator/runtime.rs`, `operator/status.rs`, and `operator/status/tests.rs`. Use Knots anchors for command/config-adjacent files when defensible (`bitcoin-cli.cpp`, `common/args.cpp`, `common/config.cpp`, `interface_bitcoin_cli.py`) and use the explicit repo-approved `none` breadcrumb for Open Bitcoin-only support contracts with no direct Knots source anchor. After updating the manifest, run `bun run scripts/check-parity-breadcrumbs.ts --write` so every new Rust source/test file receives the required top-of-file `// Parity breadcrumbs:` block matching the manifest, then run the check mode. Extend `packages/open-bitcoin-cli/src/operator/tests.rs` with focused contract tests for source ordering, route preservation, write-decision coverage, and no status-specific JSON DTO type.</action>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --write</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/source-breadcrumbs.json` lists all new `packages/open-bitcoin-cli/src/operator/*.rs` and `packages/open-bitcoin-cli/src/operator/*/tests.rs` files.
    - Every new Rust file created in Plan 17-01 contains exactly one top-of-file `// Parity breadcrumbs:` block generated from `docs/parity/source-breadcrumbs.json`.
    - The breadcrumb check reports success.
    - Operator tests contain assertions for `open-bitcoin-cli` compatibility routing and `open-bitcoin` operator routing.
    - No test asserts service install/enable/disable behavior, dashboard rendering, or migration mutation.
  </acceptance_criteria>
  <done>New operator contract files are covered by the parity breadcrumb manifest and targeted contract tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args/env -> operator contracts | Untrusted flags and environment values become typed operator inputs. |
| Planning contracts -> command execution | Contract gaps can accidentally allow service/dashboard/migration work into Phase 17. |
| Source files -> parity breadcrumb manifest | New Rust files under breadcrumb scope must remain auditable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | `operator/detect.rs` contract | mitigate | Make detection contract read-only by type and acceptance scan: no write/remove/copy/rename/system manager calls. |
| T-17-01-02 | Information Disclosure | status/config contract names | mitigate | Contract types may represent credential source/path availability only; they must not define fields named `password`, `authorization`, `cookie_value`, or `secret`. |
| T-17-01-03 | Spoofing | command routing | mitigate | Preserve `open-bitcoin-cli` argv0 routing tests so baseline-compatible RPC invocations are not parsed as operator commands. |
| T-17-01-04 | Repudiation | new Rust files | mitigate | Require `docs/parity/source-breadcrumbs.json` coverage and `bun run scripts/check-parity-breadcrumbs.ts --check`. |
</threat_model>

<verification>
Targeted checks:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::`
- `bun run scripts/check-parity-breadcrumbs.ts --check`

Repo-native final expectation after the full phase:
- `bash scripts/verify.sh`
</verification>

<success_criteria>
Plan 17-01 is complete when the operator module contracts compile, source ordering and route-boundary tests pass, and every new Rust file is covered by parity breadcrumbs.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cli-status-and-first-run-onboarding/17-01-SUMMARY.md`
</output>
