---
phase: 19-ratatui-node-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/mod.rs
  - packages/open-bitcoin-cli/Cargo.toml
  - docs/parity/source-breadcrumbs.json

autonomous: true
requirements: [DASH-01, SYNC-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-27T09-02-20
generated_at: "2026-04-27T04:18:00Z"
must_haves:
  truths:

    - `open-bitcoin dashboard` executes through runtime dispatch instead of the deferred placeholder.
    - Dashboard execution uses a single snapshot from `collect_status_snapshot` per refresh cycle.
    - TTY-incompatible or non-interactive invocations return deterministic plain-text fallback.
    - Unavailable sections render with explicit reasons using status availability semantics.
  artifacts:

    - "path: packages/open-bitcoin-cli/src/operator/runtime.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"
    - "path: packages/open-bitcoin-cli/Cargo.toml"
    - "path: docs/parity/source-breadcrumbs.json"
  key_links:

    - "from: packages/open-bitcoin-cli/src/operator/runtime.rs"
    - "from: packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"
    - "from: packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"

---

<objective>
Create the command activation path and non-interactive bootstrap for the dashboard so Phase 19 starts from a snapshot-first, terminal-capability-aware foundation.

Purpose: Make `open-bitcoin dashboard` executable now, while reusing existing status/service contracts and keeping non-interactive safety.
Output: runtime wiring + dashboard module shell + dependency + parity metadata updates.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-ratatui-node-dashboard/19-CONTEXT.md
@.planning/phases/19-ratatui-node-dashboard/19-RESEARCH.md
@.planning/phases/19-ratatui-node-dashboard/19-UI-SPEC.md
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/src/operator/status.rs
</context>

<interfaces>
From packages/open-bitcoin-cli/src/operator.rs:

```rust
pub enum OperatorCommand {
    Status(StatusArgs),
    Service(ServiceArgs),
    Dashboard(DashboardArgs),
    Onboarding(OnboardingArgs),
}

pub struct DashboardArgs {
    pub maybe_format: Option<OutputFormat>,
    pub no_color: bool,
    pub tick_ms: u64,
}
```

From packages/open-bitcoin-cli/src/operator/status.rs:

```rust
pub fn collect_status_snapshot(
    input: &StatusCollectorInput,
) -> OpenBitcoinStatusSnapshot;
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace deferred dashboard branch with runtime shell dispatch</name>
  <files>packages/open-bitcoin-cli/src/operator.rs, packages/open-bitcoin-cli/src/operator/runtime.rs</files>
  <behavior>

    - `OperatorCommand::Dashboard` is no longer mapped to the static deferred string.
    - Dispatch route receives runtime context and forwards to dashboard shell only through one code path.
    - `--no-color` and output format flags are passed through to snapshot mode unchanged.
  </behavior>
  <action>

1. In `packages/open-bitcoin-cli/src/operator.rs`, ensure dashboard module export and `OperatorCommand::Dashboard` contract are used as-is (no new command flags or contract drift).
2. In `packages/open-bitcoin-cli/src/operator/runtime.rs`, replace the deferred branch with:
   - `run_dashboard(dashboard_args, runtime_ctx)` invocation
   - a safe outcome mapping that preserves existing failure semantics.
3. Wire this branch with existing service manager and config detection context already used by status path.

Decision coverage: D-01, D-02, D-04.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- tests::dashboard_command_routing</automated>
  </verify>
  <done>`open-bitcoin dashboard` reaches executable runtime path rather than `dashboard command is deferred to Phase 19`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dashboard shell fallback path and baseline projection</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/mod.rs, packages/open-bitcoin-cli/Cargo.toml, docs/parity/source-breadcrumbs.json</files>
  <behavior>

    - Non-interactive mode is deterministic and stable.
    - Snapshot is projected once per invocation and reused by output formatters.
    - Missing data is always shown as explicit availability reasons.
  </behavior>
  <action>

1. Create `packages/open-bitcoin-cli/src/operator/dashboard/mod.rs` with:
   - `DashboardMode::{Interactive, Snapshot}`
   - `run_dashboard(args: DashboardArgs, runtime_ctx: &RuntimeContext) -> OperatorCommandOutcome`
   - `collect_dashboard_snapshot(...)` that calls `collect_status_snapshot` once.
   - `render_dashboard_text_snapshot(...)` producing ordered sections for node/sync/peers/wallet/service/metrics/logs.
2. Add terminal-safe fallback gate:
   - if no tty or output format/automation requires text, use snapshot renderer only.
3. Update `packages/open-bitcoin-cli/Cargo.toml`:
   - add `ratatui = "0.30"` and `crossterm = "0.29"` as compile dependencies.
   - do not introduce extra terminal rendering crates.
4. Add `docs/parity/source-breadcrumbs.json` entry for dashboard shell file.

Decision coverage: D-01, D-02, D-03, D-08, D-11.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- lib operator::dashboard</automated>
  </verify>
  <done>Dashboard fallback emits deterministic sectioned text from one snapshot and no longer depends on tty-only behavior for command exit.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → dashboard runtime | User flags control mode and output style |
| status snapshot → fallback renderer | Unavailable states and partial data should remain bounded |
| runtime outcome → process output | Command exit must remain typed and non-panicking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | dispatch selection | mitigate | Single-arm dispatch, single runtime entrypoint |
| T-19-02 | Availability | non-interactive sessions | mitigate | Forced fallback snapshot path when tty unavailable |
| T-19-03 | Information Disclosure | output composition | accept | Use existing snapshot contracts only; no credential-like fields added |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --package open-bitcoin-cli --all-targets --all-features -- -D warnings
cargo build --package open-bitcoin-cli --all-features
</verification>

<success_criteria>

- Dashboard command no longer returns the static deferred message.
- Snapshot mode works without a terminal and is deterministic.
- `collect_status_snapshot` is the only dashboard data input.
- Terminal-only assumptions are removed from the non-interactive path.

</success_criteria>

<output>
After completion, create `.planning/phases/19-ratatui-node-dashboard/19-01-SUMMARY.md`.
</output>
