---
phase: 19-ratatui-node-dashboard
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:

  - packages/open-bitcoin-cli/src/operator/dashboard/mod.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/action.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/app.rs

autonomous: true
requirements:

  - DASH-01
  - DASH-02
  - DASH-03
  - SYNC-06

generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-27T09-02-20
generated_at: "2026-04-27T04:28:00Z"
must_haves:
  truths:

    - Interactive mode shows compact sections with bounded historical charts for required metrics.
    - Refresh cadence is 1 second by default and supports manual refresh.
    - Action list distinguishes safe and destructive/service-affecting actions.
    - Destructive/service actions require explicit confirmation before execution.
  artifacts:

    - "path: packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/action.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/app.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"
  key_links:

    - "from: packages/open-bitcoin-cli/src/operator/dashboard/app.rs"
    - "from: packages/open-bitcoin-cli/src/operator/dashboard/app.rs"
    - "from: packages/open-bitcoin-cli/src/operator/dashboard/action.rs"

---

<objective>
Build the interactive Ratatui dashboard: projection-based chart rendering and safe action dispatch with explicit confirmation.

Purpose: satisfy DASH-02/DASH-03 behavior by turning fallback snapshot state into a terminal UI with guarded actions.
Output: interactive shell with metric graphs and confirmation-gated service actions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-ratatui-node-dashboard/19-CONTEXT.md
@.planning/phases/19-ratatui-node-dashboard/19-RESEARCH.md
@packages/open-bitcoin-cli/src/operator/dashboard/mod.rs
@packages/open-bitcoin-cli/src/operator/service.rs
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/status.rs
</context>

<interfaces>
From packages/open-bitcoin-node/src/metrics.rs:

```rust
pub enum MetricKind {
    SyncHeight,
    PeerCount,
    MempoolTransactions,
    DiskUsageBytes,
    RpcHealth,
}
```

From packages/open-bitcoin-cli/src/operator/service.rs:

```rust
pub fn execute_service_command(
    manager: &dyn ServiceManager,
    request: ServiceCommandRequest,
) -> OperatorCommandOutcome;
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement snapshot projection and bounded metric helpers</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/mod.rs</files>
  <behavior>

    - Projection is pure and deterministic from one snapshot.
    - Required metrics produce bounded vectors suitable for chart widgets.
    - Unavailable values are explicit and section headings remain stable.
  </behavior>
  <action>

1. Create `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`:
   - Define `DashboardState` and section structs for node, sync, peers/mempool, service, wallet, logs, metrics.
   - Map only `MetricKind::{SyncHeight, PeerCount, MempoolTransactions, DiskUsageBytes, RpcHealth}` for chart inputs.
   - Add bounded normalization helper `derive_metric_points(points, width)` returning stable output.
2. Add section availability mapping to keep explicit unavailable reasons and avoid hidden fields.
3. Extend `packages/open-bitcoin-cli/src/operator/dashboard/mod.rs` to consume `DashboardState` in both snapshot and interactive flows.

Decision coverage: D-02, D-03, D-05, D-06, D-08, SYNC-06.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- tests::dashboard_projection</automated>
  </verify>
  <done>DashboardState includes all required sections and bounded metric point vectors without side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Ratatui app loop, charts, and confirmation-gated actions</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/action.rs, packages/open-bitcoin-cli/src/operator/dashboard/app.rs, packages/open-bitcoin-cli/src/operator/dashboard/mod.rs</files>
  <behavior>

    - `q/esc/ctrl+c` exit cleanly; `r` performs manual refresh.
    - Destructive actions require `Pending -> Confirmed -> Execute`.
    - Action keys for safe queries execute immediately with no side effects.
  </behavior>
  <action>

1. Implement `packages/open-bitcoin-cli/src/operator/dashboard/action.rs`:
   - `DashboardAction::{Refresh, Exit, ShowStatus, InstallService, UninstallService, EnableService, DisableService, Help, None}`
   - `ActionDecision::{Pending, Confirmed, Cancelled}`
   - `action_confirm_text(action, apply_mode)` returns scope/effect summary.
2. Implement `packages/open-bitcoin-cli/src/operator/dashboard/app.rs`:
   - crossterm + ratatui event loop and draw/update pipeline.
   - default tick = 1 second (`DashboardArgs::tick_ms` as initial default source).
   - render compact panels and chart widgets from `DashboardState`.
   - on confirmed service action, call existing service execution path, never process APIs directly.
3. In `packages/open-bitcoin-cli/src/operator/dashboard/mod.rs`, connect interactive selection to `DashboardApp`.

Decision coverage: D-06, D-07, D-09, D-10, D-11.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- tests::dashboard_actions</automated>
  </verify>
  <done>Interactive dashboard shows bounded charts and executes service actions only through confirmed action states.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input → action engine | Only explicit keys map to action transitions |
| Key-triggered action → service manager | Side-effecting actions can affect daemon lifecycle |
| Tick loop → rendering | Refresh cadence can impact responsiveness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | action mapping | mitigate | Ignore unknown keys and invalid transitions |
| T-19-05 | Elevation of Privilege | service action execution | mitigate | Confirmation state machine + existing service command API |
| T-19-06 | Denial of Service | tick/render saturation | mitigate | bounded points and fixed refresh interval |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --package open-bitcoin-cli --all-targets --all-features -- -D warnings
cargo test --package open-bitcoin-cli --all-features
</verification>

<success_criteria>

- Sync/peer/mempool/disk/RPC charts render from bounded metric history in interactive mode.
- 1-second default refresh and manual refresh key are implemented.
- Destructive/service actions remain blocked until confirmed.
- Non-color path does not degrade chart/layout readability.

</success_criteria>

<output>
After completion, create `.planning/phases/19-ratatui-node-dashboard/19-02-SUMMARY.md`.
</output>
