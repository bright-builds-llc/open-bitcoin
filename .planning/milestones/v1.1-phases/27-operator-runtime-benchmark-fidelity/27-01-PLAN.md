---
phase: 27-operator-runtime-benchmark-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-bench/src/cases/operator_runtime.rs
  - packages/open-bitcoin-bench/src/runtime_fixtures.rs
autonomous: true
requirements:
  - VER-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-04-28T20-48-00
generated_at: 2026-04-28T21:02:00.000Z

must_haves:
  truths:
    - "The status benchmark case collects a snapshot through `collect_status_snapshot()` instead of using only `sample_status_snapshot()`."
    - "The benchmark stays deterministic through fake live RPC inputs, fake service state, and tempdir-backed local metrics data."
    - "The operator-runtime benchmark still renders stable human and JSON status output after the fidelity upgrade."
  artifacts:
    - path: "packages/open-bitcoin-bench/src/cases/operator_runtime.rs"
      provides: "runtime-collected status benchmark path"
    - path: "packages/open-bitcoin-bench/src/runtime_fixtures.rs"
      provides: "seeded local metrics data for deterministic operator-runtime collection"
  key_links:
    - from: "packages/open-bitcoin-bench/src/cases/operator_runtime.rs"
      to: "packages/open-bitcoin-cli/src/operator/status.rs"
      via: "`collect_status_snapshot()`"
    - from: "packages/open-bitcoin-bench/src/cases/operator_runtime.rs"
      to: "packages/open-bitcoin-bench/src/runtime_fixtures.rs"
      via: "temp-store and metrics seeding helpers"
---

# Phase 27 Plan 01: Runtime-Collected Status Benchmark

## Objective

Upgrade the operator-runtime status benchmark case so it exercises the real
shared status collector with deterministic fake runtime inputs instead of a
synthetic prebuilt snapshot.

## Scope

- `operator-runtime.status-render`
- shared benchmark fixture support in existing runtime-fixture helpers

## Tasks

1. Build a deterministic operator-runtime benchmark fixture with tempdir-backed
   config and metrics paths.
2. Seed local metrics history and fake service state for the shared collector.
3. Add a tiny fake `StatusRpcClient` that returns stable running-node, mempool,
   and wallet responses.
4. Route `operator-runtime.status-render` through `collect_status_snapshot()`
   before rendering human and JSON output.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture`
