---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-04-28T01-24-15
generated_at: 2026-04-28T01:24:15Z
phase: "22"
plan: "03"
title: "Parity ledger, benchmark verification, and release-readiness closeout"
status: planned
requirements:
  - MIG-05
  - VER-05
  - VER-06
  - VER-08
depends_on:
  - 22-01
  - 22-02
verification:
  - bash scripts/verify.sh
artifacts:
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/release-readiness.md
---

# Plan 22-03: Parity ledger, benchmark verification, and release-readiness closeout

## Why

Phase 22 only closes cleanly if the benchmark and documentation work becomes auditable release evidence. The machine-readable parity ledger, checklist view, and release-readiness docs need to reflect the shipped v1.1 runtime surface while preserving explicit deferred and out-of-scope boundaries.

## Must Be True

1. `docs/parity/index.json` models the Phase 22 runtime-hardening surfaces explicitly enough for audit and review.
2. Repo-native verification proves the new benchmark smoke output is present and structurally correct.
3. Release-readiness docs point reviewers at the Phase 22 evidence instead of the older headless-v1 handoff language.

## Tasks

### Task 1: Harden benchmark verification and evidence flow

**Files:**
- `scripts/verify.sh`
- `scripts/run-benchmarks.sh`
- new benchmark-report validation helper under `scripts/`

**Actions:**
1. Add a repo-owned validation step that checks the generated benchmark smoke report for the required Phase 22 runtime-hardening groups or metadata.
2. Keep the check deterministic and compatible with the existing smoke output path.
3. Avoid introducing release-threshold timing failures.

**Verify:**
- `bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports`
- `bash scripts/verify.sh`

### Task 2: Update parity and release-readiness sources of truth

**Files:**
- `docs/parity/index.json`
- `docs/parity/checklist.md`
- `docs/parity/release-readiness.md`
- `docs/parity/deviations-and-unknowns.md`
- `docs/parity/catalog/README.md`
- a new Phase 22 catalog entry under `docs/parity/catalog/`

**Actions:**
1. Add explicit Phase 22 operator-runtime or release-hardening ledger surfaces and distinguish shipped claims from deferred or out-of-scope work.
2. Update release-readiness to point at the new operator guide, benchmark contract, and verification evidence.
3. Keep migration differences and other deliberate non-claims visible in both the human docs and machine-readable ledger.

**Verify:**
- `bash scripts/verify.sh`
