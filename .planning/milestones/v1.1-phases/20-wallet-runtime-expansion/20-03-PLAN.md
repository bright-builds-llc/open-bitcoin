---
phase: 20-wallet-runtime-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/http.rs
  - packages/open-bitcoin-rpc/src/http/tests.rs
  - packages/open-bitcoin-cli/src/args.rs
  - packages/open-bitcoin-cli/src/args/tests.rs
  - packages/open-bitcoin-cli/src/client.rs
  - packages/open-bitcoin-cli/src/client/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - WAL-04
  - WAL-05
  - WAL-06
  - WAL-07
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-27T09-35-46
generated_at: "2026-04-27T15:15:00Z"
must_haves:
  truths:
    - "`-rpcwallet` selects wallet scope through transport metadata instead of method JSON per D-06 and D-07."
    - "Wallet-scoped requests route through `/wallet/<name>` and root-scoped methods remain on `/`."
    - "The current wallet RPC subset now includes `sendtoaddress`, `getnewaddress`, `getrawchangeaddress`, `listdescriptors`, expanded `getwalletinfo`, and resumable `rescanblockchain`."
    - "Mutating send commits reuse the shared send-intent/build-sign path from Plan 20-01 per D-01 through D-04."
  artifacts:
    - "path: packages/open-bitcoin-rpc/src/http.rs"
    - "path: packages/open-bitcoin-rpc/src/context.rs"
    - "path: packages/open-bitcoin-rpc/src/method.rs"
    - "path: packages/open-bitcoin-rpc/src/dispatch.rs"
    - "path: packages/open-bitcoin-cli/src/args.rs"
    - "path: packages/open-bitcoin-cli/src/client.rs"
  key_links:
    - "from: packages/open-bitcoin-cli/src/args.rs to: packages/open-bitcoin-cli/src/client.rs via: parsed -rpcwallet startup arg -> wallet-scoped endpoint URL"
    - "from: packages/open-bitcoin-cli/src/client.rs to: packages/open-bitcoin-rpc/src/http.rs via: /wallet/<name> POST target"
    - "from: packages/open-bitcoin-rpc/src/http.rs to: packages/open-bitcoin-rpc/src/dispatch.rs via: root-vs-wallet request scope"
---

<objective>
Implement wallet-scoped transport routing and the minimal practical wallet RPC surface required for Phase 20.

Purpose: Deliver D-01 through D-07 and the routed RPC half of WAL-04 through WAL-07 on top of the durable registry created in Plan 20-02.
Output: `-rpcwallet` transport support, `/wallet/<name>` routing, and the new wallet method subset with hermetic tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
@.planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
@AGENTS.md
@docs/architecture/cli-command-architecture.md
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-rpc/src/dispatch.rs
@packages/open-bitcoin-rpc/src/http.rs
@packages/open-bitcoin-cli/src/args.rs
@packages/open-bitcoin-cli/src/args/tests.rs
@packages/open-bitcoin-cli/src/client.rs
</context>

<interfaces>
From packages/open-bitcoin-rpc/src/config.rs:

```rust
pub enum WalletRuntimeScope {
    LocalOperatorSingleWallet,
}

pub struct RuntimeConfig {
    pub chain: AddressNetwork,
    pub maybe_data_dir: Option<PathBuf>,
    pub rpc_server: RpcServerConfig,
    pub rpc_client: RpcClientConfig,
    pub wallet: WalletRuntimeConfig,
}
```

From packages/open-bitcoin-cli/src/args.rs:

```rust
pub struct CliStartupArgs {
    pub maybe_conf_path: Option<PathBuf>,
    pub maybe_data_dir: Option<PathBuf>,
    pub maybe_rpc_connect: Option<String>,
    pub maybe_rpc_port: Option<u16>,
    pub maybe_rpc_user: Option<String>,
    pub maybe_rpc_password: Option<String>,
    pub maybe_rpc_cookie_file: Option<PathBuf>,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Un-defer wallet-scoped transport routing through `-rpcwallet` and `/wallet/&lt;name&gt;`</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/config.rs
    - packages/open-bitcoin-rpc/src/context.rs
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/http/tests.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-cli/src/args.rs
    - packages/open-bitcoin-cli/src/args/tests.rs
    - packages/open-bitcoin-cli/src/client.rs
    - packages/open-bitcoin-cli/src/client/tests.rs
    - .planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/config.rs,
    packages/open-bitcoin-rpc/src/context.rs,
    packages/open-bitcoin-rpc/src/http.rs,
    packages/open-bitcoin-rpc/src/http/tests.rs,
    packages/open-bitcoin-rpc/src/dispatch/tests.rs,
    packages/open-bitcoin-cli/src/args.rs,
    packages/open-bitcoin-cli/src/args/tests.rs,
    packages/open-bitcoin-cli/src/client.rs,
    packages/open-bitcoin-cli/src/client/tests.rs
  </files>
  <behavior>
    - Test 1: `parse_cli_args(...)` accepts `-rpcwallet=<name>` and preserves it as startup transport metadata rather than rejecting it.
    - Test 2: the CLI client posts wallet-scoped calls to `/wallet/<name>` and root calls to `/`.
    - Test 3: the RPC HTTP router extracts wallet scope from `/wallet/<name>` and rejects root-path use of wallet-only methods when no wallet is selected.
    - Test 4: multiple managed wallets require explicit wallet selection for wallet-scoped calls; node-scoped methods continue to work at `/`.
  </behavior>
  <action>
    1. Extend `packages/open-bitcoin-cli/src/args.rs` and `client.rs` so `-rpcwallet` becomes a first-class startup field and endpoint builder input per D-06. Keep wallet selection out of request JSON.
    2. Extend `packages/open-bitcoin-rpc/src/config.rs`, `context.rs`, and `http.rs` to represent wallet scope explicitly:
       - support root `/` and wallet-scoped `/wallet/<name>` routes
       - select the named wallet from the durable registry from Plan 20-02
       - keep node-scoped methods rooted and wallet-scoped methods gated by allowlists per D-07
    3. Write or update hermetic transport tests first. Mirror the Knots URI-selection model, but do not implement deferred `loadwallet`, `unloadwallet`, or `listwallets`.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli args:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli client:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc http:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc dispatch:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `-rpcwallet` is parsed and preserved to transport selection.
    - Wallet scope reaches the RPC server through the request path, not a JSON parameter.
    - Root-scoped methods stay available without wallet routing while wallet-only methods fail clearly without a selected wallet.
  </acceptance_criteria>
  <done>Wallet selection now survives CLI transport and RPC routing in a Knots-shaped, root-vs-wallet-disciplined way.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the minimal wallet RPC method subset on the new registry and ranged-descriptor model</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/context.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/open-bitcoin-rpc/src/method/tests.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - packages/open-bitcoin-node/src/wallet_registry.rs
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/context.rs,
    packages/open-bitcoin-rpc/src/method.rs,
    packages/open-bitcoin-rpc/src/method/tests.rs,
    packages/open-bitcoin-rpc/src/dispatch.rs,
    packages/open-bitcoin-rpc/src/dispatch/tests.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <action>
    1. Expand `packages/open-bitcoin-rpc/src/method.rs` to normalize and serialize the current Phase 20 wallet method subset:
       - `sendtoaddress`
       - `getnewaddress`
       - `getrawchangeaddress`
       - `listdescriptors`
       - expanded `getwalletinfo`
       - range-aware `rescanblockchain`
    2. Implement those methods in `packages/open-bitcoin-rpc/src/dispatch.rs` and `context.rs` using the new node registry and the pure-core helpers from Plan 20-01:
       - `sendtoaddress` must reuse the shared send-intent/build-sign path per D-02 and D-04
       - `getnewaddress` and `getrawchangeaddress` must advance persisted wallet-local cursor state per D-10
       - `listdescriptors` must expose active external/internal ranged descriptors with `next_index`
       - `getwalletinfo` must report wallet identity and scan/freshness fields needed by later status work
       - `rescanblockchain` must operate on persisted per-wallet rescan jobs instead of the old full-snapshot-only restriction per D-11
    3. Implement accepted `conf_target` and `estimate_mode` handling through the estimator contract resolved in research, and keep only deferred wallet lifecycle methods explicit in method/dispatch tests. Do not widen into `send`, `sendall`, PSBT, bumpfee, or multiwallet lifecycle parity.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc method:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc dispatch:: -- --nocapture && bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 20 wallet RPC subset is available and covered by hermetic dispatch/method tests.
    - `sendtoaddress` commits through the shared send-intent/build-sign flow rather than a second spending pipeline.
    - `getnewaddress` and `getrawchangeaddress` allocate distinct persisted addresses on repeat calls.
    - Deferred wallet lifecycle surfaces still return deterministic unsupported or not-found behavior, while accepted estimate inputs resolve through the planned estimator contract.
    - D-17 ownership is explicit here: repo-owned RPC and CLI tests cover wallet selection routing plus the practical send path before operator wrappers build on top.
  </acceptance_criteria>
  <done>Wallet-scoped RPC behavior is practical, restart-safe, and limited to the explicitly planned Phase 20 surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI startup flags -> transport endpoint | User-controlled wallet names influence request path selection |
| HTTP path -> RPC dispatch | Request URI determines whether root or wallet scope is active |
| RPC params -> wallet mutations | Mutating wallet methods consume untrusted fee, address, and rescan inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | http.rs wallet path routing | mitigate | Parse wallet name from `/wallet/<name>`, keep wallet scope separate from JSON params, and test root-vs-wallet allowlists |
| T-20-08 | Elevation of Privilege | dispatch.rs method ownership | mitigate | Reject wallet-scoped methods on the root path when selection is missing or ambiguous |
| T-20-09 | Integrity | sendtoaddress / rescanblockchain dispatch | mitigate | Reuse typed send-intent validation and persisted rescan job state instead of ad hoc adapter-local mutation |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc -p open-bitcoin-cli --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-rpc -p open-bitcoin-cli --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc -p open-bitcoin-cli --all-features
bash scripts/verify.sh
</verification>

<success_criteria>
  - `-rpcwallet` and `/wallet/<name>` route wallet scope end to end.
  - The new wallet RPC subset works against the durable registry and ranged-descriptor model.
  - Root-vs-wallet method ownership is explicit in tests and deterministic in failures.
</success_criteria>

<output>
After completion, create `.planning/phases/20-wallet-runtime-expansion/20-03-SUMMARY.md`
</output>
