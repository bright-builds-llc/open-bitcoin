---
phase: 20-wallet-runtime-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-wallet/src/descriptor.rs
  - packages/open-bitcoin-wallet/src/error.rs
  - packages/open-bitcoin-wallet/src/wallet.rs
  - packages/open-bitcoin-wallet/src/wallet/build.rs
  - packages/open-bitcoin-wallet/src/wallet/scan.rs
  - packages/open-bitcoin-wallet/src/wallet/sign.rs
  - packages/open-bitcoin-wallet/src/wallet/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - WAL-04
  - WAL-06
  - WAL-07
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-27T09-35-46
generated_at: "2026-04-27T15:15:00Z"
must_haves:
  truths:
    - "Wallet core accepts the narrow ranged single-key descriptor slice locked in D-08 and D-09."
    - "Receive and change address allocation advances durable wallet-local cursor state instead of adapter-local counters per D-10."
    - "One shared pure-core send-intent model exists for preview and commit paths per D-02."
    - "Wallet rescan state can represent fresh, partial, and in-progress progress semantics before node orchestration persists jobs per D-11 and D-12."
  artifacts:
    - "path: packages/open-bitcoin-wallet/src/descriptor.rs"
    - "path: packages/open-bitcoin-wallet/src/wallet.rs"
    - "path: packages/open-bitcoin-wallet/src/wallet/build.rs"
    - "path: packages/open-bitcoin-wallet/src/wallet/scan.rs"
    - "path: packages/open-bitcoin-wallet/src/wallet/tests.rs"
  key_links:
    - "from: packages/open-bitcoin-wallet/src/descriptor.rs to: packages/open-bitcoin-wallet/src/wallet.rs via: ranged descriptor record + next_index fields"
    - "from: packages/open-bitcoin-wallet/src/wallet.rs to: packages/open-bitcoin-wallet/src/wallet/build.rs via: shared send-intent/build request mapping"
    - "from: packages/open-bitcoin-wallet/src/wallet.rs to: packages/open-bitcoin-wallet/src/wallet/scan.rs via: rescan progress/freshness projection"
---

<objective>
Expand the pure wallet core so later node, RPC, and operator shells can build on stable ranged-descriptor, send-intent, and rescan-progress contracts.

Purpose: Satisfy D-02, D-08, D-09, D-10, D-11, D-12, and D-15 before any shell crate persists or routes the new wallet behavior.
Output: new wallet-domain contracts, tests, and implementations for ranged single-key descriptors, address allocation, send intent, and rescan progress.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
@.planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@docs/architecture/storage-decision.md
@docs/parity/catalog/wallet.md
@packages/open-bitcoin-wallet/src/descriptor.rs
@packages/open-bitcoin-wallet/src/error.rs
@packages/open-bitcoin-wallet/src/wallet.rs
@packages/open-bitcoin-wallet/src/wallet/build.rs
@packages/open-bitcoin-wallet/src/wallet/scan.rs
@packages/open-bitcoin-wallet/src/wallet/sign.rs
</context>

<interfaces>
From packages/open-bitcoin-wallet/src/descriptor.rs:

```rust
pub enum DescriptorRole {
    External,
    Internal,
}

pub struct DescriptorRecord {
    pub id: u32,
    pub label: String,
    pub role: DescriptorRole,
    pub original_text: String,
    pub descriptor: SingleKeyDescriptor,
}
```

From packages/open-bitcoin-wallet/src/wallet.rs:

```rust
pub struct WalletSnapshot {
    pub network: AddressNetwork,
    pub descriptors: Vec<DescriptorRecord>,
    pub utxos: Vec<WalletUtxo>,
    pub next_descriptor_id: u32,
    pub maybe_tip_height: Option<u32>,
    pub maybe_tip_median_time_past: Option<i64>,
}

pub struct BuildRequest {
    pub recipients: Vec<Recipient>,
    pub fee_rate: FeeRate,
    pub maybe_change_descriptor_id: Option<u32>,
    pub maybe_lock_time: Option<u32>,
    pub enable_rbf: bool,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing ranged-descriptor, send-intent, and rescan-progress contracts</name>
  <read_first>
    - packages/open-bitcoin-wallet/src/descriptor.rs
    - packages/open-bitcoin-wallet/src/error.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet/build.rs
    - packages/open-bitcoin-wallet/src/wallet/scan.rs
    - packages/open-bitcoin-wallet/src/wallet/tests.rs
    - .planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
    - .planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
  </read_first>
  <files>
    packages/open-bitcoin-wallet/src/descriptor.rs,
    packages/open-bitcoin-wallet/src/error.rs,
    packages/open-bitcoin-wallet/src/wallet.rs,
    packages/open-bitcoin-wallet/src/wallet/tests.rs
  </files>
  <behavior>
    - Test 1: ranged `wpkh(...)`, `sh(wpkh(...))`, and `tr(...)` descriptor forms with the locked single-key xpub/xprv path subset normalize into one external and one internal active descriptor model, while multipath/miniscript/multisig forms still fail explicitly.
    - Test 2: allocating a receive or change address increments `next_index` in wallet state exactly once and returns different addresses on successive calls.
    - Test 3: a shared send-intent request captures recipient, explicit fee-rate or estimate input, replaceability, fee ceiling, and change policy with deterministic validation errors only for invalid or unresolvable combinations.
    - Test 4: wallet rescan progress distinguishes `fresh`, `partial`, and `scanning` states without any filesystem or network effects.
  </behavior>
  <action>
    1. Add the public contract surface in `packages/open-bitcoin-wallet/src/wallet.rs` for the Phase 20 pure-core slice:
       - ranged descriptor metadata persisted on each descriptor record per D-08 and D-10
       - wallet-local address allocation APIs for external and internal roles
       - a shared send-intent/request model reused by preview and commit paths per D-02
       - rescan-progress/freshness domain types per D-11 and D-12
    2. Extend `packages/open-bitcoin-wallet/src/error.rs` with typed errors for invalid descriptor range, cursor exhaustion, invalid estimate request, estimator-unavailable runtime resolution, fee ceiling violation, and unsupported address-role or change-policy mismatches. Keep deterministic error text; do not add shell-owned confirmation or prompt errors here per D-15.
    3. Write failing tests in `packages/open-bitcoin-wallet/src/wallet/tests.rs` first. Keep the supported descriptor slice narrow and explicit; do not add deferred Core surfaces from the phase deferred list.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-wallet wallet:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The pure wallet API exposes explicit types for ranged descriptors, address allocation, send intent, and rescan freshness.
    - Tests fail before implementation because the new contracts are not yet satisfied.
    - No shell-owned persistence, HTTP, CLI, prompt, or filesystem behavior is added to `open-bitcoin-wallet`.
  </acceptance_criteria>
  <done>Wallet-core contracts and failing tests define the exact Phase 20 pure-domain surface for later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic ranged descriptors, address allocation, and send/rescan domain logic</name>
  <read_first>
    - packages/open-bitcoin-wallet/src/descriptor.rs
    - packages/open-bitcoin-wallet/src/error.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet/build.rs
    - packages/open-bitcoin-wallet/src/wallet/scan.rs
    - packages/open-bitcoin-wallet/src/wallet/sign.rs
    - packages/open-bitcoin-wallet/src/wallet/tests.rs
  </read_first>
  <files>
    packages/open-bitcoin-wallet/src/descriptor.rs,
    packages/open-bitcoin-wallet/src/error.rs,
    packages/open-bitcoin-wallet/src/wallet.rs,
    packages/open-bitcoin-wallet/src/wallet/build.rs,
    packages/open-bitcoin-wallet/src/wallet/scan.rs,
    packages/open-bitcoin-wallet/src/wallet/sign.rs,
    packages/open-bitcoin-wallet/src/wallet/tests.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <action>
    1. Implement the narrow ranged single-key descriptor parser and derivation logic in `packages/open-bitcoin-wallet/src/descriptor.rs`:
       - allow only the xpub/xprv plus derivation forms needed by D-09
       - track `range_start`, `range_end`, and `next_index` per active descriptor per D-08
       - keep multipath, miniscript, multisig, PSBT, and unrelated Core descriptor families rejected explicitly
    2. Implement restart-safe address allocation and send-intent mapping in `packages/open-bitcoin-wallet/src/wallet.rs` and `wallet/build.rs`:
       - add wallet-local receive/change allocation helpers that advance cursor state exactly once per successful allocation per D-10
       - reuse the existing build/sign pipeline rather than creating a second send-specific builder per D-02 and the research "don't hand-roll" rule
       - support explicit fee-rate inputs plus typed fee-selection intent and error surfaces that downstream node/RPC shells use when resolving `conf_target`/`estimate_mode` into a concrete `FeeRate`; do not perform runtime estimator resolution inside `open-bitcoin-wallet`
    3. Implement pure rescan-progress/freshness helpers in `packages/open-bitcoin-wallet/src/wallet/scan.rs` so the node shell can checkpoint resumable jobs later without re-encoding freshness logic in adapters.
    4. Update parity breadcrumb coverage if any new wallet-core source file is added.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-wallet && bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - Wallet-core tests pass for the supported ranged descriptor slice and still fail deferred descriptor families explicitly.
    - Successive receive/change allocations return distinct addresses and advance cursor state in the wallet snapshot.
    - Send-intent validation is deterministic and uses the existing build/sign path rather than a duplicated spend pipeline.
    - Rescan-progress helpers expose fresh/partial/scanning semantics without shell dependencies.
    - D-17 ownership is explicit here: repo-owned wallet-core tests cover descriptor cursor persistence plus pure rescan freshness semantics before shell plans build on them.
  </acceptance_criteria>
  <done>Pure wallet-domain logic fully covers the Phase 20 foundational contracts needed by downstream persistence and routing plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| descriptor text -> wallet core | Untrusted descriptor strings cross into pure parsing and validation |
| send intent -> build/sign logic | Operator-provided fee and change inputs influence transaction funding |
| rescan progress -> status semantics | Partial progress must not be misreported as fresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | wallet/build.rs send-intent validation | mitigate | Reject invalid or unresolvable estimate requests, fee-ceiling violations, and wrong change policies with typed wallet errors per D-04 |
| T-20-02 | Information Disclosure | wallet.rs address allocation | mitigate | Persist cursor state in wallet-domain snapshot contracts so later shells cannot accidentally reuse addresses after restart |
| T-20-03 | Integrity | wallet/scan.rs freshness math | mitigate | Encode explicit fresh/partial/scanning state in pure types so shells do not infer freshness from balance alone |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-wallet --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-wallet --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-wallet --all-features
bash scripts/verify.sh
</verification>

<success_criteria>
  - Ranged single-key descriptor state, address allocation, send intent, and rescan freshness live in `open-bitcoin-wallet`.
  - The supported Phase 20 descriptor subset is explicit and all deferred families still fail deterministically.
  - Downstream plans can persist and route the new wallet surface without inventing extra core logic.
</success_criteria>

<output>
After completion, create `.planning/phases/20-wallet-runtime-expansion/20-01-SUMMARY.md`
</output>
