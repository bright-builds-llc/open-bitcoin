---
phase: 33-operator-surface-truth-and-coverage-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/tests.rs
  - packages/open-bitcoin-cli/src/operator/service.rs
  - packages/open-bitcoin-cli/src/operator/service/launchd.rs
  - packages/open-bitcoin-cli/src/operator/service/systemd.rs
  - packages/open-bitcoin-cli/src/operator/service/tests.rs
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-04-30T05-02-20
generated_at: 2026-04-30T05:02:20.722Z

must_haves:
  truths:
    - "The public `open-bitcoin status` clap contract no longer advertises an unused `--watch` flag."
    - "Unmanaged service errors and manager-backed diagnostics now point operators at the real preview flow instead of a nonexistent `--dry-run` flag."
    - "Shared service preview semantics stay preview-by-default, with `--apply` remaining the explicit mutate switch."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator.rs"
      provides: "truthful status CLI contract"
    - path: "packages/open-bitcoin-cli/src/operator/service.rs"
      provides: "normalized shared service preview guidance"
    - path: "packages/open-bitcoin-cli/src/operator/service/launchd.rs"
      provides: "launchd unmanaged diagnostics aligned to preview-by-default semantics"
    - path: "packages/open-bitcoin-cli/src/operator/service/systemd.rs"
      provides: "systemd unmanaged diagnostics aligned to preview-by-default semantics"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator.rs"
      to: "packages/open-bitcoin-cli/src/operator/runtime.rs"
      via: "status runtime remains one-shot after removing the dead watch flag"
    - from: "packages/open-bitcoin-cli/src/operator/service.rs"
      to: "docs/operator/runtime-guide.md"
      via: "shared preview guidance matches the documented service install contract"
---

# Phase 33 Plan 01: Remove Dead Status Watch Surface And Normalize Service Preview Hints

## Objective

Remove the dead `status --watch` operator surface and align all shared
unmanaged-service guidance with the existing preview-by-default service
contract.

## Scope

- Clap-owned status argument contract and nearby operator tests
- Shared service typed errors and unmanaged diagnostics
- Focused hermetic service tests proving the repaired preview guidance

## Tasks

1. Remove the unused `watch` field from `StatusArgs` and update nearby operator
   tests so the public CLI/help surface matches the one-shot runtime behavior.
2. Replace stale `open-bitcoin service install --dry-run` wording in
   `ServiceError::NotInstalled` and the unmanaged launchd/systemd diagnostics
   with language that points operators at `open-bitcoin service install` for
   preview and `--apply` for mutation.
3. Add or update focused tests in `packages/open-bitcoin-cli/src/operator/service/tests.rs`
   to prove the repaired hint text and shared preview contract stay truthful.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::service::tests`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::tests`
