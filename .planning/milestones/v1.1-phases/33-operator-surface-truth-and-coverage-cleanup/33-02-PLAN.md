---
phase: 33-operator-surface-truth-and-coverage-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - packages/open-bitcoin-cli/src/operator/dashboard/app.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-04-30T05-02-20
generated_at: 2026-04-30T05:02:20.722Z

must_haves:
  truths:
    - "Interactive dashboard service-action coverage now exercises the higher-level confirmation loop instead of relying only on `dashboard/action.rs` unit tests."
    - "The fake-RPC operator-binary status path is deterministic enough that the normal verification contract does not need lucky reruns."
    - "Dashboard service actions still reuse the shared service command path and remain confirmation-gated."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/app.rs"
      provides: "higher-level hermetic coverage for interactive dashboard action handling"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "stabilized fake-RPC binary coverage"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/dashboard/app.rs"
      to: "packages/open-bitcoin-cli/src/operator/dashboard/action.rs"
      via: "app-level confirmation flow still routes through the shared service execution path"
    - from: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      to: "packages/open-bitcoin-cli/src/operator/runtime.rs"
      via: "binary status tests exercise the shipped one-shot runtime against a fake RPC server"
---

# Phase 33 Plan 02: Add Dashboard Interaction Coverage And Harden Fake RPC Status Tests

## Objective

Add higher-level dashboard action coverage around the interactive confirmation
loop and harden the fake-RPC operator-binary status fixture so the test path is
deterministic.

## Scope

- Hermetic app-level dashboard tests in `dashboard/app.rs`
- Shared fake-RPC operator-binary fixture and the nearby running-status test

## Tasks

1. Add app-level dashboard tests that drive `handle_action()` through pending,
   confirmed, and cancelled service-action flows with a real
   `DashboardRuntimeContext` and fake service manager.
2. Keep the higher-level dashboard assertions focused on confirmation behavior,
   shared service-command reuse, and truthful operator messaging rather than on
   terminal-paint details.
3. Harden `FakeRpcServer` or the nearby running-status test setup so
   `open_bitcoin_status_json_uses_fake_running_rpc` stops flaking due to timing
   or request-handling races.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard::app`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary open_bitcoin_status_json_uses_fake_running_rpc`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary open_bitcoin_dashboard_human_non_tty_uses_snapshot_sections`
