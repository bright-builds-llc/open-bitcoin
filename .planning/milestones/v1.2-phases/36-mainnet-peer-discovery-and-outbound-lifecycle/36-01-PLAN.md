---
phase: 36
phase_name: "Mainnet Peer Discovery and Outbound Lifecycle"
plan_id: "36-01"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: "36-2026-05-01T22-57-33"
generated_at: "2026-05-01T23:05:08.840Z"
status: pending
---

# Plan 36-01: Sync Peer Config And Resolver Boundary

## Objective

Add a typed Phase 36 sync peer contract that can express manual peers, DNS seeds, bounded pool policy, and injectable resolution without leaking DNS or socket concerns into pure-core crates.

## Requirements

- PEERMAIN-01: Mainnet DNS seed and manual-peer resolution must be configurable and testable through injected resolvers.
- PEERMAIN-02: Outbound lifecycle policy inputs must be typed and bounded.
- PEERMAIN-04: Peer source metadata must survive into later telemetry.
- RESUME-02: Resolver/config errors must stay typed and deterministic.
- VERMAIN-01: Default verification must remain hermetic.

## Files

- `packages/open-bitcoin-node/src/sync/types.rs`
- `packages/open-bitcoin-node/src/sync/resolver.rs`
- `packages/open-bitcoin-node/src/sync/tcp.rs`
- `packages/open-bitcoin-node/src/lib.rs`
- `packages/open-bitcoin-rpc/src/config/open_bitcoin.rs`
- `packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs`
- `packages/open-bitcoin-rpc/src/config/tests.rs`

## Tasks

<task id="T36-01-01" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-node/src/sync/types.rs</file>
    <file>packages/open-bitcoin-node/src/sync/tcp.rs</file>
    <file>packages/open-bitcoin-rpc/src/config/open_bitcoin.rs</file>
  </read_first>
  <action>Extend the sync shell domain with typed peer candidate, resolved endpoint, resolver policy, and bounded outbound lifecycle config records. Keep manual peers and DNS seeds distinct in the domain model, preserve source labels, and expose defaults that match the current mainnet seed set without changing default disabled behavior.</action>
  <acceptance_criteria>
    <item>New config types encode target outbound peer count, retry budget, and timeout/backoff knobs without using raw free-form maps.</item>
    <item>Manual peers and DNS seeds remain distinguishable after parsing.</item>
    <item>No production path uses `unwrap()` or `expect()`.</item>
  </acceptance_criteria>
</task>

<task id="T36-01-02" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-node/src/sync/tcp.rs</file>
    <file>packages/open-bitcoin-node/src/sync.rs</file>
  </read_first>
  <action>Split DNS resolution out of the real TCP transport into an injected `SyncPeerResolver` shell abstraction plus a real system-backed resolver implementation. Keep `TcpPeerSession` focused on socket/session I/O so tests can replace resolution without contacting public DNS.</action>
  <acceptance_criteria>
    <item>The real resolver owns `to_socket_addrs()` or equivalent system resolution.</item>
    <item>Fake resolvers can deterministically return endpoints or failures in tests.</item>
    <item>Pure-core crates remain free of DNS and socket imports.</item>
  </acceptance_criteria>
</task>

<task id="T36-01-03" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-rpc/src/config/open_bitcoin.rs</file>
    <file>packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs</file>
    <file>packages/open-bitcoin-rpc/src/config/tests.rs</file>
  </read_first>
  <action>Expose the Phase 36 peer configuration through `open-bitcoin.jsonc` and daemon loader code. Support operator-configured manual peers and optional DNS seed overrides while preserving the Phase 35 `bitcoin.conf` compatibility boundary and deterministic validation errors.</action>
  <acceptance_criteria>
    <item>JSONC can configure manual peers and DNS seed overrides without weakening `sync.network_enabled` and `sync.mode` safety gates.</item>
    <item>Invalid peer config returns clear typed config errors.</item>
    <item>`bitcoin.conf` still rejects Open Bitcoin-only sync keys.</item>
  </acceptance_criteria>
</task>

<task id="T36-01-04" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-rpc/src/config/tests.rs</file>
  </read_first>
  <action>Add deterministic config and resolver-boundary tests covering defaults, manual-peer parsing, DNS seed overrides, fake resolver endpoint normalization, duplicate endpoint handling, and resolver failure projection without public-network access.</action>
  <acceptance_criteria>
    <item>Tests cover JSONC and daemon loader behavior.</item>
    <item>Tests do not require live DNS or sockets.</item>
  </acceptance_criteria>
</task>

## Threat Model

| Threat | Severity | Mitigation |
| --- | --- | --- |
| DNS or socket logic leaks into pure-core crates | High | Keep resolver and real transport inside `open-bitcoin-node` shell modules only. |
| Config sprawl silently enables public-network behavior | High | Preserve explicit `network_enabled` + `mode` gating and deterministic config validation. |
| Non-deterministic tests depend on public DNS | High | Use fake resolvers and typed endpoint records in tests. |

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc config::tests`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync::`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
