---
phase: 40
phase_name: "Live Mainnet Smoke, Docs, and Parity Closeout"
plan_id: "40-01"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: "40-2026-05-02T13-22-45"
generated_at: "2026-05-02T13:22:45.481Z"
status: completed
---

# Plan 40-01: Opt-In Live Mainnet Evidence And Closeout Truth

## Objective

Add an explicit opt-in live mainnet smoke workflow that verifies real `open-bitcoind` progress, emits local audit reports, fails clearly when prerequisites are missing, and then refresh README, operator, and parity surfaces so the v1.2 shipped claim matches reality without widening the default hermetic verification contract.

## Requirements

- SYNCMAIN-04: Keep mainnet-sync claims bounded and explicitly non-production.
- CHAINMAIN-05: Surface resource and prerequisite guidance when local runtime conditions are insufficient.
- RESUME-03: Explain reindex, repair, or resync guidance through the closeout and troubleshooting docs.
- OBSMAIN-04: Let operators collect support evidence without parsing internal store files manually.
- VERMAIN-01: Keep repo-native verification hermetic by default.
- VERMAIN-03: Add an optional live mainnet smoke command that proves real progress and writes reproducible reports.
- VERMAIN-04: Update operator docs for prerequisites, startup, status, stop/resume, troubleshooting, and known limitations.
- VERMAIN-05: Refresh parity docs and machine-readable indexes so shipped versus deferred behavior stays auditable.

## Files

- `scripts/run-live-mainnet-smoke.ts`
- `scripts/test-run-live-mainnet-smoke.sh`
- `README.md`
- `docs/operator/runtime-guide.md`
- `docs/parity/index.json`
- `docs/parity/checklist.md`
- `docs/parity/deviations-and-unknowns.md`
- `docs/parity/release-readiness.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

## Tasks

<task id="T40-01-01" owner="inline">
  <action>Implement a repo-owned Bun live-mainnet smoke runner that launches the existing daemon path, polls the existing sync-status surface, writes JSON and Markdown evidence reports, and fails early with actionable prerequisite guidance while staying outside `scripts/verify.sh`.</action>
</task>

<task id="T40-01-02" owner="inline">
  <action>Add focused script-level regression coverage for the smoke runner so clear preflight failures and successful report generation are protected without requiring public-network access.</action>
</task>

<task id="T40-01-03" owner="inline">
  <action>Refresh README, operator runtime docs, parity checklist, deviations, release-readiness notes, and machine-readable parity metadata so Phase 40 closes the stale “future work” wording and records the shipped v1.2 limits truthfully.</action>
</task>

<task id="T40-01-04" owner="inline">
  <action>Update roadmap and state closeout metadata, then verify the phase with repo-native checks plus the new script regression so the wrapper can safely commit and push only after a passed verification report.</action>
</task>

## Verification

- `bash scripts/test-run-live-mainnet-smoke.sh`
- `cargo fmt --manifest-path packages/Cargo.toml --all`
- `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
- `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
- `cargo test --manifest-path packages/Cargo.toml --all-features`
- `bash scripts/verify.sh`
- `git diff --check`
