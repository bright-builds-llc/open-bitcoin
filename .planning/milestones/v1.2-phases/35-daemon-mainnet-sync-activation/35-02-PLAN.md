---
phase: 35
phase_name: "Daemon Mainnet Sync Activation"
plan_id: "35-02"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: "35-2026-05-01T21-26-04"
generated_at: "2026-05-01T21:29:26.254Z"
status: completed
---

# Plan 35-02: Daemon Sync Bootstrap Preflight

## Objective

Wire enabled sync activation into `open-bitcoind` startup by validating safety gates, opening the durable node store, constructing `DurableSyncRuntime`, and reporting a pre-sync summary without starting network transport.

## Requirements

- SYNCMAIN-01: Daemon activation must be available from the operator runtime surface.
- SYNCMAIN-03: Startup must validate durable sync runtime wiring before serving RPC.
- RESUME-01: The runtime must reopen existing durable sync state safely.
- RESUME-04: Startup errors must be actionable for operators.
- VERMAIN-01: Verification must avoid live mainnet network dependence.

## Files

- `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs`
- `packages/open-bitcoin-rpc/src/config.rs`

## Tasks

<task id="T35-02-01" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</file>
    <file>packages/open-bitcoin-node/src/sync.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store.rs</file>
  </read_first>
  <action>Add a daemon bootstrap helper that returns early when sync is disabled, and when enabled opens `FjallNodeStore`, constructs `DurableSyncRuntime`, and returns a pre-sync `SyncRunSummary`.</action>
  <acceptance_criteria>
    <item>Disabled mode preserves current RPC startup behavior.</item>
    <item>Enabled mode does not call `sync_once`, `sync_until_idle`, or `TcpPeerTransport`.</item>
    <item>Missing datadir returns a clear error before RPC bind.</item>
  </acceptance_criteria>
</task>

<task id="T35-02-02" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</file>
  </read_first>
  <action>Print a concise daemon startup line for enabled preflight that identifies the mode, datadir, and current durable best header/block heights.</action>
  <acceptance_criteria>
    <item>Message says preflight/bootstrap, not full sync completion.</item>
    <item>Message avoids production-readiness claims.</item>
  </acceptance_criteria>
</task>

<task id="T35-02-03" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</file>
  </read_first>
  <action>Add unit tests for disabled bootstrap and enabled bootstrap against a temporary durable store.</action>
  <acceptance_criteria>
    <item>Tests pass without binding sockets.</item>
    <item>Tests assert the bootstrap summary is empty and durable-state based.</item>
  </acceptance_criteria>
</task>

## Threat Model

| Threat | Severity | Mitigation |
| --- | --- | --- |
| Daemon listens after failed sync preflight | High | Run bootstrap before `TcpListener::bind` and propagate errors. |
| Phase 35 accidentally starts live network I/O | High | Preflight constructs runtime only; tests do not use `TcpPeerTransport`. |
| Operator mistakes preflight for completed full sync | Medium | Startup copy and docs explicitly say bootstrap/preflight. |

## Verification

- `cargo test --all-features -p open-bitcoin-rpc --bin open-bitcoind`
- `cargo build --all-targets --all-features`
