# Quick Task 260620-qs1: Implement verify.sh profiling and fast local mode

**Date:** 2026-06-21
**Status:** Completed

## Objective

Add lightweight timing instrumentation and an explicit fast local verification
mode while preserving the default strict `bash scripts/verify.sh` contract and
the repo-managed pre-commit hook behavior.

## Tasks

1. [x] Refactor `scripts/verify.sh` around explicit modes and timed steps.
   - Files: `scripts/verify.sh`
   - Action: Add `--full`, `--profile`, `--fast`, and `--timings`, plus a safe
     `run_step` helper that records stage durations without `eval`.
   - Verify: Existing checker command strings and order remain visible in the
     script for release-boundary checkers.
   - Done: Full mode stays strict, profile mode prints timings, and fast mode
     skips benchmark smoke, Bazel smoke, and `cargo llvm-cov`.

2. [x] Extend verifier regression coverage.
   - Files: `scripts/test-check-file-lengths.sh`
   - Action: Update the fake tool fixture and add invalid-flag, fast-mode, and
     profile/timing assertions.
   - Verify: `bash scripts/test-check-file-lengths.sh`.
   - Done: Fixture tests prove expensive commands are skipped in fast mode and
     timing summaries are available.

3. [x] Document local verification modes.
   - Files: `docs/operator/runtime-guide.md`, `AGENTS.md`
   - Action: Document `--fast`, `--profile`, and strict default/pre-commit
     behavior without weakening the repo-native verification contract.
   - Verify: targeted checker tests plus verifier mode runs.
   - Done: Operators and contributors can choose the right local command.

## Completion Review

Implemented `--full`, `--profile`, `--fast`, and `--timings` in
`scripts/verify.sh`. The strict default verifier remains the release and
pre-commit contract, while fast mode skips benchmark smoke, Bazel smoke, and
coverage only.

Verification completed:

- `bash -n scripts/verify.sh scripts/test-check-file-lengths.sh`
- `bash scripts/test-check-file-lengths.sh`
- `bun test scripts/check-phase73-uat-verification.test.ts scripts/check-phase75-soak-runner.test.ts scripts/check-phase76-resource-bounds.test.ts scripts/check-phase77-corruption-lock-recovery.test.ts scripts/check-phase78-progress-guarantees.test.ts scripts/check-phase79-diagnostics-support-bundle.test.ts scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts`
- `bun run scripts/check-phase70-reorg-recovery.ts && bun run scripts/check-phase71-resource-restart.ts && bun run scripts/check-phase72-observability-evidence.ts`
- `bash scripts/verify.sh --fast --timings`
- `bash scripts/verify.sh --profile`

Profiling notes:

- Fast mode passed in 58m 54.002s; `cargo test` took 54m 6.293s.
- Full profile mode passed in 51m 58.168s; `cargo test` took 45m 29.170s.
- Full-only stages were comparatively small in the profiled run: benchmark
  smoke 4.445s, Bazel build 11.063s, and coverage 7.472s.

Residual risk: the fastest local shortcut requested here still keeps the full
workspace `cargo test` contract, so the largest remaining runtime reduction
would require a separate decision about splitting or narrowing local test
surfaces.
