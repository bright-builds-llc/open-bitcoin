# Quick Task 260424-7ow: Improve GitHub CI Caching For Rust And Bazel Verification

**Date:** 2026-04-24
**Status:** Ready for execution

## Objective

Improve the existing GitHub Actions `verify` job speed without changing the
repo-native verification contract.

## Tasks

1. Add Rust-aware caching to `.github/workflows/ci.yml` after Rust toolchain
   installation using `Swatinem/rust-cache@v2`.
2. Configure the existing `bazel-contrib/setup-bazel@0.19.0` step to restore
   Bazelisk, repository, and disk caches.
3. Keep cache writes restricted to pushes on `main`, keep PRs restore-only, and
   continue running `bash scripts/verify.sh` unchanged.

## Verification

- `actionlint .github/workflows/ci.yml` when `actionlint` is available.
- `bash scripts/verify.sh`
- `git diff --check`
