# Quick Task 260425-e1c: Fix CI Bun provisioning

**Date:** 2026-04-25
**Status:** Planned

## Objective

Provision the pinned Bun runtime in CI so `bash scripts/verify.sh` can run
without `error: bun is required`, while preserving Bun as the canonical runtime
for repo-owned higher-level automation scripts.

## Tasks

1. Add a pinned Bun runtime source of truth and install it in CI.
   - Files: `.bun-version`, `.github/workflows/ci.yml`
   - Action: Pin Bun to `1.3.9`, install it with `oven-sh/setup-bun@v2`, and
     print `bun --version` before repo verification.
   - Verify: CI workflow has Bun setup before `bash scripts/verify.sh`.
   - Done: GitHub Actions runners have `bun` on `PATH` for verification.

2. Document the local prerequisite and refresh generated metrics if needed.
   - Files: `CONTRIBUTING.md`, `docs/metrics/lines-of-code.md`
   - Action: Document Bun as a required local verification tool and regenerate
     the LOC report after workflow changes.
   - Verify: `bun --version`, `git diff --check`, and `bash scripts/verify.sh`.
   - Done: Contributor docs and generated metrics match the new CI/tooling
     contract.
