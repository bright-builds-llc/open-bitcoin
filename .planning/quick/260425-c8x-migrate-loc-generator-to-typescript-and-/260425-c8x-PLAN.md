---
quick_id: 260425-c8x
description: Migrate LOC generator to TypeScript and Bun
created: 2026-04-25
status: completed
---

# Quick Task 260425-c8x Plan

## Goal

Convert the LOC report generator from Node-run JavaScript to Bun-run TypeScript, make Bun the canonical runtime for repo-owned higher-level automation scripts, and remove repo-owned Node runtime usage from shell verification paths.

## Tasks

### 1. Convert the LOC generator

- Files: `scripts/generate-loc-report.mjs`, `scripts/generate-loc-report.ts`, `docs/metrics/lines-of-code.md`
- Action: Rename the generator to TypeScript, add lightweight types for CLI options and metrics structures, switch the shebang and report metadata to Bun, and keep the current deterministic CLI behavior.
- Verify: Run Bun worktree/index freshness checks and regenerate the report from the staged index.
- Done: The report is produced by `bun run scripts/generate-loc-report.ts` and records Bun/TypeScript metadata.

### 2. Replace repo-owned Node runtime usage

- Files: `.githooks/pre-commit`, `scripts/verify.sh`, `scripts/check-pure-core-deps.sh`, `AGENTS.md`
- Action: Use Bun for LOC generation, verification timing, and Cargo metadata parsing; update repo-local guidance to prefer TypeScript with Bun for higher-level scripts.
- Verify: Run the pure-core dependency check and shell lint/format checks.
- Done: Runtime usage of Node is removed from repo-owned automation scripts while domain references to node remain untouched.

### 3. Update tests

- Files: `scripts/test-generate-loc-report.sh`, `scripts/test-check-file-lengths.sh`
- Action: Run the LOC tests through Bun and update verify integration fakes from Node to Bun, including a stub for LOC freshness in temp repos.
- Verify: Run both shell regression test scripts.
- Done: Script behavior and verify ordering remain covered under the Bun runtime contract.

## Final Verification

- `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check`
- `bun run scripts/generate-loc-report.ts --source=index --output=docs/metrics/lines-of-code.md --check`
- `bash scripts/test-generate-loc-report.sh`
- `bash scripts/test-check-file-lengths.sh`
- `bash scripts/check-pure-core-deps.sh`
- `shfmt -i 2 -ci -d .githooks/pre-commit scripts/verify.sh scripts/check-pure-core-deps.sh scripts/test-generate-loc-report.sh scripts/test-check-file-lengths.sh`
- `shellcheck .githooks/pre-commit scripts/verify.sh scripts/check-pure-core-deps.sh scripts/test-generate-loc-report.sh scripts/test-check-file-lengths.sh`
- `mdformat --check AGENTS.md docs/metrics/lines-of-code.md`
- `bash scripts/verify.sh`
