---
phase: 89-release-readiness-guardrail-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - scripts/check-phase88-deterministic-claim-guardrails.ts
  - scripts/check-phase88-deterministic-claim-guardrails.test.ts
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/upgrade-and-rollback-policy.md
  - docs/parity/operator-runbooks.md
  - docs/parity/service-operation-expectations.md
autonomous: true
requirements:
  - REL-02
  - REL-03
  - REL-04
requirements_addressed:
  - REL-02
  - REL-03
  - REL-04
must_haves:
  truths:
    - "Deferred-surface promotion in the upgrade policy, operator runbooks, or service expectations docs fails deterministically."
    - "Scoped no-claim, deferred, unsupported, opt-in UAT, outside-default-verification, historical, and future-gate wording still passes."
    - "The Phase 88 corpus remains curated and does not scan planning archives or all historical docs."
  artifacts:
    - path: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      provides: "Curated corpus including canonical v1.8 upgrade, runbook, and service policy docs"
    - path: "scripts/check-phase88-deterministic-claim-guardrails.test.ts"
      provides: "Fixture coverage for newly covered policy docs"
    - path: "docs/parity/index.json"
      provides: "Machine-readable Phase 88 evidence roots including newly scanned policy docs"
    - path: "docs/parity/checklist.md"
      provides: "Human checklist evidence row for expanded deterministic guardrail coverage"
  key_links:
    - from: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      to: "docs/parity/upgrade-and-rollback-policy.md"
      via: "TARGET_FILES and POINTER_FILES"
      pattern: "upgrade-and-rollback-policy\\.md"
    - from: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      to: "docs/parity/operator-runbooks.md"
      via: "TARGET_FILES and POINTER_FILES"
      pattern: "operator-runbooks\\.md"
    - from: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      to: "docs/parity/service-operation-expectations.md"
      via: "TARGET_FILES and POINTER_FILES"
      pattern: "service-operation-expectations\\.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 89-2026-06-24T20-03-26
generated_at: 2026-06-24T20:20:01Z
---

<objective>
Expand the Phase 88 deterministic claim-guardrail corpus to scan every canonical v1.8 policy document that can promote deferred production-adjacent surfaces.

Purpose: Close GAP-02 and the deterministic claim-guardrail flow gap while preserving the curated corpus boundary chosen in Phase 88.
Output: Updated Phase 88 checker corpus, fixture tests, parity roots, and compact policy-doc pointers for the newly covered canonical docs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/v1.8-MILESTONE-AUDIT.md
@.planning/phases/89-release-readiness-guardrail-closure/89-CONTEXT.md
@.planning/phases/89-release-readiness-guardrail-closure/89-RESEARCH.md
@.planning/phases/88-deterministic-claim-guardrails/88-01-SUMMARY.md
@.planning/phases/88-deterministic-claim-guardrails/88-02-SUMMARY.md
@scripts/check-phase88-deterministic-claim-guardrails.ts
@scripts/check-phase88-deterministic-claim-guardrails.test.ts
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/upgrade-and-rollback-policy.md
@docs/parity/operator-runbooks.md
@docs/parity/service-operation-expectations.md

<interfaces>
Existing Phase 88 checker contract to preserve:

```typescript
export function checkPhase88DeterministicClaimGuardrails(
  maybeRepoRoot = process.env[REPO_ROOT_OVERRIDE_ENV],
): string[] {
```

New canonical policy docs required by D-05:

```text
docs/parity/upgrade-and-rollback-policy.md
docs/parity/operator-runbooks.md
docs/parity/service-operation-expectations.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scan new canonical policy docs in Phase 88</name>
  <read_first>
    - `scripts/check-phase88-deterministic-claim-guardrails.ts`
    - `scripts/check-phase88-deterministic-claim-guardrails.test.ts`
    - `.planning/phases/89-release-readiness-guardrail-closure/89-CONTEXT.md`
    - `.planning/v1.8-MILESTONE-AUDIT.md`
  </read_first>
  <files>scripts/check-phase88-deterministic-claim-guardrails.ts, scripts/check-phase88-deterministic-claim-guardrails.test.ts</files>
  <behavior>
    - Test: Appending `Migration apply mode is production-ready for existing datadirs.` to `docs/parity/upgrade-and-rollback-policy.md` fails with `deferred surface promotion`.
    - Test: Appending `Inbound serving is fully supported by the operator runbook.` to `docs/parity/operator-runbooks.md` fails with `deferred surface promotion`.
    - Test: Appending `Windows service integration is production-grade for default releases.` to `docs/parity/service-operation-expectations.md` fails with `deferred surface promotion`.
    - Test: Appending scoped text that says these surfaces are `deferred`, `unsupported`, `opt-in UAT`, `outside default verification`, or behind a `future gate` passes.
  </behavior>
  <action>
    Implement D-05, D-06, D-07, D-08, and D-09 in the existing Phase 88 checker. Do not replace the curated corpus with a whole-repo scan, and do not add `.planning/`, milestone archives, generated live-smoke reports, generated support bundles, or recursive docs traversal.

    In `scripts/check-phase88-deterministic-claim-guardrails.ts`:
    - Add `docs/parity/upgrade-and-rollback-policy.md`, `docs/parity/operator-runbooks.md`, and `docs/parity/service-operation-expectations.md` to `TARGET_FILES` immediately after `docs/parity/support-matrix.md` and before `docs/parity/release-readiness.md`.
    - Add the same three paths to `POINTER_FILES` immediately after `docs/parity/support-matrix.md`.
    - Add the same three paths to `REQUIRED_EVIDENCE` immediately after `docs/parity/support-matrix.md` and before `docs/parity/release-readiness.md`.
    - Keep `DEFERRED_SURFACES`, `PROMOTION_PREDICATES`, and `ALLOWED_SCOPE_TERMS` as the policy vocabulary unless a failing canonical doc requires one extra scoped no-claim term. If one extra term is needed, add only the exact term observed in the canonical docs and cover it with a passing fixture.

    In `scripts/check-phase88-deterministic-claim-guardrails.test.ts`:
    - Mirror the same three path additions in `TARGET_FILES` and `REQUIRED_EVIDENCE`.
    - Add a test named `fails_when_new_policy_docs_promote_deferred_surfaces` that creates three fixtures with the exact bad sentences listed in the behavior block and asserts each failure output includes `deferred surface promotion`.
    - Add or extend a scoped-passing test so each newly covered policy doc can include one allowed scoped sentence without failures:
      - `Migration apply mode remains deferred until a future gate.`
      - `Inbound serving remains outside default verification.`
      - `Windows service integration is unsupported for this milestone.`
    - Keep Arrange, Act, Assert comments in new tests.
  </action>
  <verify>
    <automated>bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts</automated>
    <automated>bun run scripts/check-phase88-deterministic-claim-guardrails.ts</automated>
  </verify>
  <acceptance_criteria>
    - The checker scans all three newly required canonical policy docs.
    - Bad promotion prose in each newly covered doc fails deterministically.
    - Scoped no-claim wording in each newly covered doc passes.
    - The corpus remains curated and excludes `.planning/` and historical milestone archives.
  </acceptance_criteria>
  <done>Phase 88 checker and tests prove deferred-surface promotion fails in the three newly covered policy docs while scoped no-claim wording passes.</done>
</task>

<task type="auto">
  <name>Task 2: Register expanded Phase 88 evidence roots</name>
  <read_first>
    - `docs/parity/index.json`
    - `docs/parity/checklist.md`
    - `docs/parity/upgrade-and-rollback-policy.md`
    - `docs/parity/operator-runbooks.md`
    - `docs/parity/service-operation-expectations.md`
    - `scripts/check-phase88-deterministic-claim-guardrails.ts`
  </read_first>
  <files>docs/parity/index.json, docs/parity/checklist.md, docs/parity/upgrade-and-rollback-policy.md, docs/parity/operator-runbooks.md, docs/parity/service-operation-expectations.md</files>
  <action>
    Update the human and machine evidence roots to match the expanded Phase 88 corpus from Task 1.

    In `docs/parity/index.json`:
    - For `checklist.surfaces` entry `v1-8-deterministic-claim-guardrails`, add these evidence paths: `docs/parity/upgrade-and-rollback-policy.md`, `docs/parity/operator-runbooks.md`, and `docs/parity/service-operation-expectations.md`.
    - For `audit.v1_8_deterministic_claim_guardrails`, add the same three evidence paths.
    - Keep requirements exactly `["REL-02", "REL-03", "REL-04"]`; do not add future requirement IDs.

    In `docs/parity/checklist.md`:
    - Update the `v1-8-deterministic-claim-guardrails` row Evidence cell to include links to `upgrade-and-rollback-policy.md`, `operator-runbooks.md`, and `service-operation-expectations.md`.
    - Keep Known Gaps and Suspected Unknowns explicit that these guardrails are deterministic local docs/checker coverage only and do not claim production full-node readiness.

    In each newly covered policy doc, add one compact pointer near the existing release-readiness or Phase 87 pointer section:
    - `docs/parity/upgrade-and-rollback-policy.md`
    - `docs/parity/operator-runbooks.md`
    - `docs/parity/service-operation-expectations.md`

    Use this exact sentence in each file so the checker pointer requirement is satisfied without broadening claims:
    `The v1.8 deterministic claim guardrails scan this document for overbroad production-readiness and deferred-surface promotion; they define gates only and do not claim production full-node readiness.`
  </action>
  <verify>
    <automated>bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts</automated>
    <automated>bun run scripts/check-phase88-deterministic-claim-guardrails.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` and `docs/parity/checklist.md` list the three newly scanned policy docs as Phase 88 evidence.
    - Each newly scanned policy doc contains the exact `v1.8 deterministic claim guardrails` pointer sentence.
    - The real repository passes the Phase 88 checker after docs and parity-root updates.
  </acceptance_criteria>
  <done>The expanded Phase 88 corpus is registered in code, fixtures, machine roots, human roots, and policy-doc pointers.</done>
</task>

</tasks>

<must_haves>
- Truth: The Phase 88 checker scans `upgrade-and-rollback-policy.md`, `operator-runbooks.md`, and `service-operation-expectations.md`.
- Artifact: `scripts/check-phase88-deterministic-claim-guardrails.test.ts` proves bad promotion text fails in each new corpus file.
- Key link: `docs/parity/index.json` Phase 88 evidence matches the checker `REQUIRED_EVIDENCE` list for the new policy docs.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Policy docs -> release claim scanner | Canonical policy prose can promote deferred surfaces if omitted from the scanner corpus. |
| Fixture repository -> checker behavior | Test fixtures must prove failures in newly scanned docs, not only in older release docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-89-04 | Tampering | `scripts/check-phase88-deterministic-claim-guardrails.ts` | mitigate | Add the three canonical policy docs to `TARGET_FILES`, `POINTER_FILES`, and `REQUIRED_EVIDENCE`. |
| T-89-05 | Spoofing | `docs/parity/*policy*.md` | mitigate | Add scoped guardrail pointer text and fixture failures for unscoped deferred-surface promotion. |
| T-89-06 | Denial of service | Phase 88 checker corpus | accept | Corpus remains a small curated static list; no recursive scan or network dependency is added. |
</threat_model>

<verification>
Run focused commands before moving to Plan 03:

```bash
bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts
bun run scripts/check-phase88-deterministic-claim-guardrails.ts
```
</verification>

<success_criteria>
- GAP-02 deterministic claim-guardrail corpus coverage is closed.
- D-05 through D-09 are fully implemented.
- Default verification remains deterministic and local; no public-network, real service-manager, package-manager service, support-upload, destructive-repair, or multi-day checks are added.
</success_criteria>

<output>
After completion, create `.planning/phases/89-release-readiness-guardrail-closure/89-02-SUMMARY.md`.
</output>
