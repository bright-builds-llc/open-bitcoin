---
phase: 111-full-block-serving-request-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer/inventory_state.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
autonomous: true
requirements: [BSRV-04, GOV-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 111-2026-07-04T14-58-18
generated_at: 2026-07-04T15:53:30Z
must_haves:
  truths:
    - "Full block, witness block, and compact block getdata requests pass through peer-manager request-pressure checks before any node-shell serving action can exist."
    - "Over-cap getdata bursts return existing resource-limit disconnect evidence and do not emit PeerAction::ServeInventory."
    - "Received block, notfound, and peer removal cleanup keep block in-flight state bounded without compact-block or renderer-local cleanup state."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/inventory_state.rs"
      provides: "Peer-manager getdata pressure gate and ServeInventory routing before node-shell lookup"
      exports: ["PeerManager::handle_getdata", "request_pressure_input", "resource_limit_disconnect_actions_from_decision"]
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Phase 111 regression coverage for bounded block, witness block, and compact block getdata handling"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer/inventory_state.rs"
      to: "packages/open-bitcoin-node/src/network/inventory.rs"
      via: "PeerAction::ServeInventory after request_pressure_input"
      pattern: "request_pressure_input[\\s\\S]*PeerAction::ServeInventory"
    - from: "packages/open-bitcoin-network/src/peer/inventory_state.rs"
      to: "packages/open-bitcoin-network/src/resource.rs"
      via: "ResourceGovernancePolicy::decide_request"
      pattern: "resource_limit_disconnect_actions\\("
---

<objective>
Lock the peer-manager side of the Phase 111 request path so block, witness block, and compact block `getdata` inventory remains bounded by existing request caps and cleanup paths before any node-shell storage lookup.

Purpose: Satisfy the BSRV-04 and GOV-01 request-pressure portion while preserving D-01, D-03, D-04, D-09, D-10, and D-11.
Output: Focused `open-bitcoin-network` regressions and any minimal peer-manager adjustment needed to keep `getdata` pressure, in-flight cleanup, and compact-block deferral deterministic.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md
@.planning/phases/111-full-block-serving-request-path/111-RESEARCH.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-03-SUMMARY.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@packages/open-bitcoin-network/src/block_serving.rs
@packages/open-bitcoin-network/src/peer/inventory_state.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-network/src/resource.rs
@packages/open-bitcoin-primitives/src/network.rs

<interfaces>
From `packages/open-bitcoin-network/src/peer/inventory_state.rs`:
```rust
pub(super) fn handle_getdata(
    &self,
    peer_id: PeerId,
    inventory: InventoryList,
) -> Result<Vec<PeerAction>, NetworkError>;

fn request_pressure_input(
    peer: &PeerState,
    inventory_items: usize,
    getdata_items: usize,
    header_locator_hashes: usize,
    block_in_flight_count: usize,
    txid_in_flight_count: usize,
    wtxid_in_flight_count: usize,
) -> RequestPressureInput;

pub(super) fn resource_limit_disconnect_actions_from_decision(
    decision: ResourceGovernanceDecision,
) -> Option<Vec<PeerAction>>;
```

From `packages/open-bitcoin-network/src/peer.rs`:
```rust
pub enum PeerAction {
    Send(WireNetworkMessage),
    ServeInventory(Vec<InventoryVector>),
    ReceivedBlock(Block),
    ReceivedTransaction(Transaction),
    TransactionRelay(TxDownloadAction),
}
```

From `packages/open-bitcoin-primitives/src/network.rs`:
```rust
pub enum InventoryType {
    Block,
    CompactBlock,
    WitnessBlock,
    Transaction,
    WitnessTransaction,
    Unknown(u32),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove getdata pressure gates block, witness block, and compact block inventory</name>
  <files>packages/open-bitcoin-network/src/peer/inventory_state.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer/inventory_state.rs` for `PeerManager::handle_getdata`, `request_pressure_input`, and `typed_serve_inventory_vector`.
    - `packages/open-bitcoin-network/src/peer/tests.rs` for existing Phase 110 request-cap and cleanup regression style.
    - `packages/open-bitcoin-network/src/resource.rs` for `ResourceGovernancePolicy::decide_request` and `RequestPressureInput` fields.
    - `packages/open-bitcoin-network/src/block_serving.rs` for Phase 110 outcome labels `block_request_cap_reached` and `block_serving_suppressed`.
    - `.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md` decisions D-01, D-03, D-04, D-09, and D-11.
  </read_first>
  <behavior>
    - Test 1: a mixed `getdata` containing `InventoryType::Block`, `InventoryType::WitnessBlock`, `InventoryType::CompactBlock`, `InventoryType::Transaction`, and `InventoryType::WitnessTransaction` emits exactly one `PeerAction::ServeInventory` after the request cap check accepts it.
    - Test 2: an over-cap `getdata` containing block, witness block, and compact block inventory emits a resource-limit disconnect action and emits no `PeerAction::ServeInventory`.
    - Test 3: compact block inventory in `getdata` does not enter `peer.requested_blocks`, does not emit `WireNetworkMessage::Block`, and remains node-shell classified inventory only.
  </behavior>
  <action>Add tests named exactly `phase111_getdata_block_witness_and_compact_inventory_stays_after_request_pressure_gate`, `phase111_over_cap_block_witness_compact_getdata_disconnects_before_serve_inventory`, and `phase111_compact_block_getdata_does_not_enter_requested_blocks`. If the RED tests expose a production gap, modify only `PeerManager::handle_getdata`, `request_pressure_input`, or `typed_serve_inventory_vector` so `request_pressure_input(peer, 0, inventory.inventory.len(), 0, peer.requested_blocks.len(), self.tx_download.peer_snapshot(peer_id).in_flight_count, 0)` runs before `PeerAction::ServeInventory`. Preserve transaction inventory normalization through `TxRelayId::{Txid,Wtxid}.to_inventory_vector()`. Preserve compact block inventory as `InventoryType::CompactBlock` in `ServeInventory`; do not transform it into `InventoryType::Block`, `WireNetworkMessage::Block`, `cmpctblock`, `getblocktxn`, or `blocktxn`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib phase111_getdata -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase111_getdata_block_witness_and_compact_inventory_stays_after_request_pressure_gate|phase111_over_cap_block_witness_compact_getdata_disconnects_before_serve_inventory|phase111_compact_block_getdata_does_not_enter_requested_blocks" packages/open-bitcoin-network/src/peer/tests.rs` finds all three tests.
    - `rg -n "InventoryType::Block|InventoryType::WitnessBlock|InventoryType::CompactBlock" packages/open-bitcoin-network/src/peer/tests.rs` finds all three inventory types in Phase 111 tests.
    - `rg -n "request_pressure_input\\(\\s*peer,\\s*0,\\s*inventory\\.inventory\\.len\\(\\)" packages/open-bitcoin-network/src/peer/inventory_state.rs` finds the getdata pressure input before serving.
    - `rg -n "PeerAction::ServeInventory" packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/tests.rs` finds the serving action, and the over-cap test asserts it is absent.
    - `! rg -n "WireNetworkMessage::Block\\(|cmpctblock|getblocktxn|blocktxn" packages/open-bitcoin-network/src/peer/inventory_state.rs` passes for new Phase 111 peer-manager code.
  </acceptance_criteria>
  <done>Peer-manager `getdata` routing proves D-01, D-03, D-04, D-09, and D-11: bounded inventory is accepted only after request-pressure checks, over-cap bursts disconnect/suppress before serving, and compact block requests stay classified without payload serving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove block request cleanup stays on existing in-flight paths</name>
  <files>packages/open-bitcoin-network/src/peer/inventory_state.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer/inventory_state.rs` for `handle_notfound`, `handle_block`, `request_missing_blocks`, and `remove_peer_with_transaction_cleanup`.
    - `packages/open-bitcoin-network/src/peer/tests.rs` for existing tests `phase110_block_notfound_releases_requested_block`, `phase110_received_block_releases_requested_block`, and `phase110_block_peer_removal_drops_requested_blocks_and_preserves_tx_cleanup`.
    - `packages/open-bitcoin-network/src/block_serving.rs` for `classify_block_inflight_cleanup` labels that must remain the shared evidence vocabulary.
    - `.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md` decision D-10.
  </read_first>
  <behavior>
    - Test 1: `WireNetworkMessage::NotFound` for `InventoryType::Block` and `InventoryType::WitnessBlock` removes those hashes from `peer.requested_blocks`.
    - Test 2: `WireNetworkMessage::Block` removes the received block hash from `peer.requested_blocks`.
    - Test 3: peer removal drops block request state while preserving transaction cleanup behavior.
    - Test 4: `InventoryType::CompactBlock` in `NotFound` does not mutate `peer.requested_blocks` because Phase 111 has no compact-block in-flight state.
  </behavior>
  <action>Add tests named exactly `phase111_notfound_releases_block_and_witness_block_requested_state`, `phase111_received_block_releases_requested_block_state`, `phase111_peer_removal_drops_block_request_state_without_compact_state`, and `phase111_compact_notfound_does_not_create_or_release_block_inflight_state`. If production adjustment is needed, modify only the existing cleanup branches in `handle_notfound`, `handle_block`, or peer removal; keep cleanup mutation on `peer.requested_blocks` and transaction relay cleanup on `tx_download`. Do not add a compact-block in-flight map, renderer-local cleanup labels, socket-side cleanup labels, or any `getblocktxn`/`blocktxn` state.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib phase111_ -- --nocapture</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase111_notfound_releases_block_and_witness_block_requested_state|phase111_received_block_releases_requested_block_state|phase111_peer_removal_drops_block_request_state_without_compact_state|phase111_compact_notfound_does_not_create_or_release_block_inflight_state" packages/open-bitcoin-network/src/peer/tests.rs` finds all four tests.
    - `rg -n "requested_blocks\\.remove|requested_blocks\\.insert|InventoryType::WitnessBlock|InventoryType::CompactBlock" packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/tests.rs` finds request and cleanup coverage.
    - `! rg -n "compact.*in_flight|Compact.*InFlight|getblocktxn|blocktxn|BlockServing.*Renderer|cleanup_label_" packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/tests.rs` passes for Phase 111 peer-manager changes.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` exits 0 with the touched Rust test file registered.
  </acceptance_criteria>
  <done>Block and witness block request state releases through existing `notfound`, received block, and peer removal paths, while compact block requests remain bounded classified requests with no Phase 111 in-flight payload state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| remote peer -> peer-manager getdata | Untrusted inventory vectors enter request-pressure accounting. |
| peer-manager -> node-shell ServeInventory | Accepted inventory crosses from pure peer policy into effectful serving adapters. |
| cleanup messages -> in-flight state | Peer-provided `notfound`, received block payloads, disconnects, and cleanup events mutate request state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-111-01-01 | Denial of Service | `PeerManager::handle_getdata` | mitigate | Preserve `request_pressure_input` before `PeerAction::ServeInventory`; add over-cap block/witness/compact getdata tests proving resource-limit disconnect and no serving action. |
| T-111-01-02 | Elevation of Privilege | permissioned/protected peers | mitigate | Tests prove permission-bearing inventory still uses existing request caps; no permission effect bypasses `ResourceGovernancePolicy::decide_request`. |
| T-111-01-03 | Tampering | compact block inventory | mitigate | Compact block inventory remains `InventoryType::CompactBlock` and cannot become `WireNetworkMessage::Block`, `cmpctblock`, `getblocktxn`, or `blocktxn` in peer-manager code. |
| T-111-01-04 | Repudiation | cleanup evidence | mitigate | Cleanup continues through existing `requested_blocks` release paths and Phase 110 `classify_block_inflight_cleanup` labels; no renderer-local cleanup vocabulary is introduced. |
</threat_model>

<verification>
Run after all tasks:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib phase111_ -- --nocapture
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings
bun run scripts/check-parity-breadcrumbs.ts --check
```
</verification>

<success_criteria>
- BSRV-04 getdata routing for block, witness block, and compact block inventory is bounded before node-shell serving.
- GOV-01 request caps and block in-flight cleanup remain on existing `ResourceGovernancePolicy` and `requested_blocks` paths.
- Compact block inventory is classified and bounded but cannot produce compact-block payloads or full-block fallback responses in this phase.
- No package/filter relay, public serving default, archive-node, public-network CI, production readiness, schema push, `getblocktxn`, or `blocktxn` work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/111-full-block-serving-request-path/111-01-SUMMARY.md`.
</output>
