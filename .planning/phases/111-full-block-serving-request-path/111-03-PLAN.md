---
phase: 111-full-block-serving-request-path
plan: 03
type: execute
wave: 2
depends_on: [111-01, 111-02]
files_modified:
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network/block_serving.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/architecture/status-snapshot.md
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/checklist.md
  - docs/parity/index.json
  - scripts/check-phase111-full-block-serving-request-path.ts
  - scripts/check-phase111-full-block-serving-request-path.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [BSRV-04, GOV-01, GOV-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 111-2026-07-04T14-58-18
generated_at: 2026-07-04T15:53:30Z
must_haves:
  truths:
    - "Historical, pruned, stale, side-chain, unavailable, compact block, and ineligible block-serving requests are bounded by stable outcomes and do not imply archive-node behavior."
    - "Adversarial full/witness/compact getdata bursts keep using existing queue, request, and in-flight limits under ordinary, permissioned, and protected peer cases."
    - "Verifier output preserves the Phase 111 claim: bounded eligible full/witness block serving only, with compact relay payloads and archive-node behavior deferred."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/tests.rs"
      provides: "Managed-network historical, pruned, side-chain, unavailable, permission, witness, compact, and request-pressure matrix"
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Peer-manager adversarial getdata and cleanup matrix for full/witness/compact inventory"
    - path: "scripts/check-phase111-full-block-serving-request-path.ts"
      provides: "Deterministic Phase 111 no-claim and evidence checker"
    - path: "docs/parity/index.json"
      provides: "Machine-readable bounded Phase 111 parity surface with BSRV-04, GOV-01, and GOV-05"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase111-full-block-serving-request-path.ts"
      via: "bun test and bun run verifier steps"
      pattern: "check-phase111-full-block-serving-request-path"
    - from: "docs/parity/index.json"
      to: "docs/parity/catalog/p2p.md"
      via: "surface id v2-1-full-block-serving-request-path"
      pattern: "v2-1-full-block-serving-request-path"
    - from: "packages/open-bitcoin-node/src/network/tests.rs"
      to: "packages/open-bitcoin-node/src/network/block_serving.rs"
      via: "stable outcome labels for pruned/unavailable/suppressed/cap cases"
      pattern: "block_status_pruned|block_status_unavailable|block_request_cap_reached"
---

<objective>
Complete the Phase 111 verification matrix and guardrails for historical/pruned boundaries, adversarial request pressure, and bounded no-archive/no-compact-relay claims.

Purpose: Satisfy BSRV-04, GOV-01, and GOV-05 across the high-risk negative cases from D-09 through D-16 without adding BIP152 payloads, archive-node behavior, public-serving defaults, or production-readiness claims.
Output: Focused peer-manager and managed-network tests, bounded docs/parity evidence, a Phase 111 checker with mutation tests, verifier wiring, and refreshed generated LOC metrics if verification regenerates them.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md
@.planning/phases/111-full-block-serving-request-path/111-RESEARCH.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-03-SUMMARY.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@packages/open-bitcoin-network/src/block_serving.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-node/src/network/block_serving.rs
@packages/open-bitcoin-node/src/network/tests.rs
@scripts/check-phase110-block-serving-boundary.ts
@scripts/check-phase110-block-serving-boundary.test.ts
@scripts/verify.sh

<interfaces>
Expected from Plan 111-02:
```rust
pub(super) struct ManagedBlockServeDecision {
    pub label: BlockServingOutcomeLabel,
    pub maybe_block: Option<Block>,
    pub missing_inventory: bool,
}

pub(super) fn serve_managed_block_request(input: ManagedBlockServeInput) -> ManagedBlockServeDecision;
```

From `packages/open-bitcoin-network/src/block_serving.rs`:
```rust
pub enum BlockServingOutcomeLabel {
    BlockServingDisabled,
    BlockServingEligible,
    BlockServingSuppressed,
    BlockStatusUnavailable,
    BlockStatusPruned,
    BlockStatusUnvalidated,
    BlockRequestCapReached,
    BlockInFlightCleanupReleased,
    BlockInFlightCleanupPeerRemoved,
    BlockInFlightCleanupTimeout,
    BlockInFlightCleanupRestart,
    BlockInFlightLimitStillReached,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add historical, pruned, recent-valid, stale, side-chain, and no-archive serving matrix</name>
  <files>packages/open-bitcoin-node/src/network/block_serving.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/network/block_serving.rs` from Plan 111-02 for adapter decision labels and any crate-private outcome helpers.
    - `packages/open-bitcoin-node/src/network/tests.rs` for `build_block`, `connect_stored_block` tests, test stores, and inbound permission helpers.
    - `packages/open-bitcoin-node/src/network.rs` for `connect_local_block`, `connect_stored_block`, `BlockConnectDisposition`, `reorg_to_branch`, and `blocks_by_hash`.
    - `packages/open-bitcoin-network/src/block_serving.rs` for labels `block_status_pruned`, `block_status_unavailable`, and `block_serving_suppressed`.
    - `.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md` decisions D-05, D-06, D-12, and D-13.
  </read_first>
  <behavior>
    - Test 1: a cached non-extending or disconnected block from `connect_stored_block` is not served and returns `WireNetworkMessage::NotFound`.
    - Test 2: an active-chain non-tip hash with no local block bytes returns `WireNetworkMessage::NotFound` and stable label `block_status_pruned`.
    - Test 3: an active-chain tip hash with no local block bytes returns `WireNetworkMessage::NotFound` and stable label `block_status_unavailable`.
    - Test 4: an explicit recent-valid, validated, locally available adapter fact serves a full block only after lazy payload lookup.
    - Test 5: an explicit stale adapter fact returns `WireNetworkMessage::NotFound` and stable label `block_status_unavailable` without invoking payload lookup.
    - Test 6: an old cached block outside the active chain is not served as an archive-node response and has no `WireNetworkMessage::Block`.
  </behavior>
  <action>Add managed-network and adapter tests named exactly `phase111_side_chain_cached_block_is_not_served`, `phase111_active_chain_non_tip_missing_local_block_returns_pruned_notfound`, `phase111_active_tip_missing_local_block_returns_unavailable_notfound`, `phase111_recent_valid_available_block_is_served_after_policy_gate`, `phase111_stale_block_fact_returns_unavailable_notfound_without_lookup`, and `phase111_cached_old_block_outside_active_chain_is_not_archive_served`. Build missing-local-block cases with a test store implementing `ChainstateStore` that returns a `ChainstateSnapshot` containing explicit `ChainPosition` values while `blocks_by_hash` is empty. Cover `BlockServingChainPosition::RecentValid` and `BlockServingChainPosition::Stale` directly at the adapter boundary if managed chainstate has no first-class recent-valid/stale metadata yet; the recent-valid case must be validated and locally available, and the stale case must prove the lazy payload lookup is not invoked. If the test needs to inspect adapter labels, expose a crate-private or `pub(super)` fixed-label helper in `packages/open-bitcoin-node/src/network/block_serving.rs` returning only `BlockServingOutcomeLabel::as_str()` values. Assert only stable labels or message variants: `WireNetworkMessage::NotFound`, `block_status_pruned`, `block_status_unavailable`, `block_serving_suppressed`, and absence of `WireNetworkMessage::Block`. Do not assert raw peer ids, endpoints, permission strings, prune heights, block payload bytes, transaction payloads, credentials, dynamic labels, or archive-node language.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase111_active -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase111_cached -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase111_side_chain_cached_block_is_not_served|phase111_active_chain_non_tip_missing_local_block_returns_pruned_notfound|phase111_active_tip_missing_local_block_returns_unavailable_notfound|phase111_recent_valid_available_block_is_served_after_policy_gate|phase111_stale_block_fact_returns_unavailable_notfound_without_lookup|phase111_cached_old_block_outside_active_chain_is_not_archive_served" packages/open-bitcoin-node/src/network/tests.rs packages/open-bitcoin-node/src/network/block_serving.rs` finds all six tests.
    - `rg -n "BlockServingChainPosition::RecentValid|BlockServingChainPosition::Stale|block_status_pruned|block_status_unavailable|block_serving_suppressed|WireNetworkMessage::NotFound" packages/open-bitcoin-node/src/network/tests.rs packages/open-bitcoin-node/src/network/block_serving.rs` finds status branch coverage.
    - `! rg -n "prune_height|raw_peer|permission_string|credential|transaction_payload|block_payload|archive node|archive-node" packages/open-bitcoin-node/src/network/tests.rs packages/open-bitcoin-node/src/network/block_serving.rs` passes for new Phase 111 assertions and helpers.
  </acceptance_criteria>
  <done>Historical, pruned, recent-valid, stale, side-chain, unavailable, and old-cache probes are tested as bounded outcomes with stable labels and no raw peer/prune/payload leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adversarial request-pressure and cleanup matrix</name>
  <files>packages/open-bitcoin-network/src/peer/tests.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer/tests.rs` for Phase 110 `requested_blocks`, `notfound`, peer removal, and request cap regressions.
    - `packages/open-bitcoin-node/src/network/tests.rs` for managed-network send/receive helpers and request-cap assertions.
    - `packages/open-bitcoin-network/src/peer/inventory_state.rs` for `handle_getdata`, `handle_notfound`, `handle_block`, and `remove_peer_with_transaction_cleanup`.
    - `packages/open-bitcoin-network/src/block_serving.rs` for cleanup labels and `block_request_cap_reached`.
    - `.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md` decisions D-01, D-09, D-10, D-11, and D-14.
  </read_first>
  <behavior>
    - Test 1: managed-network over-cap `getdata` with block, witness block, and compact block inventory disconnects or suppresses before any `WireNetworkMessage::Block`.
    - Test 2: permissioned/protected peers with `download` permission still hit `block_request_cap_reached` and do not bypass request caps.
    - Test 3: `notfound`, received block, peer disconnect/removal, and timeout/restart cleanup evidence continues to use Phase 110 labels, not Phase 111 renderer-local labels.
    - Test 4: compact-block bursts remain bounded and non-served without creating compact partial state.
  </behavior>
  <action>Add or extend focused tests named exactly `phase111_managed_getdata_over_request_cap_disconnects_without_block_payload`, `phase111_permissioned_block_getdata_still_hits_request_cap`, `phase111_full_witness_block_cleanup_matrix_uses_phase110_labels`, and `phase111_compact_block_burst_remains_bounded_without_partial_state`. Keep peer-manager tests in `packages/open-bitcoin-network/src/peer/tests.rs` and node-shell integration tests in `packages/open-bitcoin-node/src/network/tests.rs`. Use existing `ResourceGovernancePolicy::default()` limits and existing `requested_blocks` paths; do not add new queues, new cap counters, compact partial state, `getblocktxn`, `blocktxn`, wall-clock sleeps, public-network probes, or service-manager behavior. When asserting labels, use exactly `block_request_cap_reached`, `block_inflight_cleanup_released`, `block_inflight_cleanup_peer_removed`, `block_inflight_cleanup_timeout`, `block_inflight_cleanup_restart`, and `block_inflight_limit_still_reached`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib phase111_ -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase111_ -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase111_managed_getdata_over_request_cap_disconnects_without_block_payload|phase111_permissioned_block_getdata_still_hits_request_cap|phase111_full_witness_block_cleanup_matrix_uses_phase110_labels|phase111_compact_block_burst_remains_bounded_without_partial_state" packages/open-bitcoin-network/src/peer/tests.rs packages/open-bitcoin-node/src/network/tests.rs` finds all four tests.
    - `rg -n "block_request_cap_reached|block_inflight_cleanup_released|block_inflight_cleanup_peer_removed|block_inflight_cleanup_timeout|block_inflight_cleanup_restart|block_inflight_limit_still_reached" packages/open-bitcoin-network/src/peer/tests.rs packages/open-bitcoin-node/src/network/tests.rs` finds the required fixed labels.
    - `! rg -n "compact.*partial|getblocktxn|blocktxn|sleep\\(|public network|service manager|new queue|archive node|archive-node" packages/open-bitcoin-network/src/peer/tests.rs packages/open-bitcoin-node/src/network/tests.rs` passes for new Phase 111 tests.
    - `bash scripts/check-file-lengths.sh` exits 0 after adding the matrix.
  </acceptance_criteria>
  <done>Full block serving, witness block serving, compact-block classification, permissioned/protected peers, and cleanup paths remain under existing request, queue, and in-flight resource governance with deterministic local tests only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 111 docs, parity evidence, and no-claim checker</name>
  <files>docs/architecture/status-snapshot.md, docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md, docs/parity/checklist.md, docs/parity/index.json, scripts/check-phase111-full-block-serving-request-path.ts, scripts/check-phase111-full-block-serving-request-path.test.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `scripts/check-phase110-block-serving-boundary.ts` and `scripts/check-phase110-block-serving-boundary.test.ts` for fixed-file checker and mutation-test patterns.
    - `scripts/verify.sh` for Phase 110 checker wiring location.
    - `docs/architecture/status-snapshot.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/p2p.md`, `docs/parity/checklist.md`, and `docs/parity/index.json` before editing.
    - `.planning/phases/111-full-block-serving-request-path/111-CONTEXT.md` decisions D-12, D-13, D-14, D-15, D-16, and deferred section.
    - `.planning/phases/111-full-block-serving-request-path/111-RESEARCH.md` "Known Threat Patterns for Block Serving" and "Pitfall 6: Checker False Positives From Legacy Docs".
  </read_first>
  <behavior>
    - RED checker test: fails if BSRV-04, GOV-01, or GOV-05 is missing from Phase 111 parity evidence.
    - RED checker test: fails if evidence omits `InventoryType::Block`, `InventoryType::WitnessBlock`, `InventoryType::CompactBlock`, `WireNetworkMessage::Block`, `WireNetworkMessage::NotFound`, `block_status_pruned`, `block_status_unavailable`, or `block_request_cap_reached`.
    - RED checker test: fails on positive claims for BIP152 compact block payload serving, `getblocktxn`, `blocktxn`, archive-node behavior, public-serving default, package relay, bloom/filter serving, compact filter serving, public-network CI, production full-node readiness, production service operation, or production-funds wallet use.
    - RED checker test: allows explicit no-claim/deferred wording for those out-of-scope surfaces.
  </behavior>
  <action>Update docs and parity roots with a bounded Phase 111 surface id exactly `v2-1-full-block-serving-request-path`. The docs must say Phase 111 proves eligible full and witness block `getdata` responses from validated local data only, compact block requests are bounded but not served, historical/pruned behavior is bounded, and archive-node/public-serving behavior remains deferred. In `docs/operator/runtime-guide.md`, keep UAT guidance local/deterministic and include both repo-local command forms exactly: `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...` and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...`. Create `scripts/check-phase111-full-block-serving-request-path.ts` and `.test.ts` with fixed file lists and mutation tests for required evidence, forbidden overclaims, allowed no-claim wording, and verifier omission. Wire `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` and `bun run scripts/check-phase111-full-block-serving-request-path.ts` into `scripts/verify.sh` immediately after Phase 110 checker execution. Do not add schema push, ORM, database migration, public-network, wall-clock soak, service-manager, production deployment, compact-block payload, `getblocktxn`, or `blocktxn` verification gates.</action>
  <verify>
    <automated>bun test scripts/check-phase111-full-block-serving-request-path.test.ts</automated>
    <automated>bun run scripts/check-phase111-full-block-serving-request-path.ts</automated>
    <automated>rg -n "check-phase111-full-block-serving-request-path.test.ts|check-phase111-full-block-serving-request-path.ts" scripts/verify.sh</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "v2-1-full-block-serving-request-path|BSRV-04|GOV-01|GOV-05|InventoryType::Block|InventoryType::WitnessBlock|InventoryType::CompactBlock|WireNetworkMessage::Block|WireNetworkMessage::NotFound|block_status_pruned|block_status_unavailable|block_request_cap_reached" docs/architecture/status-snapshot.md docs/operator/runtime-guide.md docs/parity/catalog/p2p.md docs/parity/checklist.md docs/parity/index.json scripts/check-phase111-full-block-serving-request-path.ts scripts/check-phase111-full-block-serving-request-path.test.ts` finds required evidence terms.
    - `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` passes and includes mutation tests for missing evidence, forbidden compact/BIP152 claims, forbidden archive/public/default/production claims, and verifier omission.
    - `bun run scripts/check-phase111-full-block-serving-request-path.ts` passes.
    - `scripts/verify.sh` contains both `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` and `bun run scripts/check-phase111-full-block-serving-request-path.ts`.
    - `! rg -n "schema push|prisma|typeorm|public-network CI|required public network|archive-node support|production full-node readiness|production service operation|production-funds|package relay support|bloom filter serving|compact filter serving|getblocktxn support|blocktxn support|cmpctblock support" docs/architecture/status-snapshot.md docs/operator/runtime-guide.md docs/parity/catalog/p2p.md docs/parity/checklist.md docs/parity/index.json scripts/check-phase111-full-block-serving-request-path.ts` passes except explicit no-claim/deferred wording allowed by the checker tests.
  </acceptance_criteria>
  <done>Phase 111 has deterministic docs/parity/verifier evidence for bounded full/witness block serving, request-pressure governance, historical/pruned safeguards, and all no-claim boundaries required by D-13 through D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| adversarial peer -> request-pressure matrix | Repeated block/witness/compact getdata bursts probe request caps and cleanup paths. |
| local chain/cache state -> evidence and docs | Tests and docs can accidentally turn cache hits into archive-node claims. |
| docs/parity/checker -> verifier | Static claims become release evidence and must reject overclaims deterministically. |
| operator UAT guidance -> user expectations | Human-facing commands must stay local/deterministic and avoid public-network default claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-111-03-01 | Denial of Service | adversarial getdata matrix | mitigate | Tests exercise over-cap full/witness/compact bursts and permissioned/protected cases with existing request and in-flight limits. |
| T-111-03-02 | Information Disclosure | historical/pruned probes | mitigate | Matrix asserts fixed labels and `NotFound` only; acceptance rejects prune heights, raw peer data, permission strings, credentials, and payload details. |
| T-111-03-03 | Spoofing | archive-node/public-serving claims | mitigate | Phase 111 checker rejects archive-node, public-serving default, production-readiness, and production-service claims while allowing explicit deferred/no-claim wording. |
| T-111-03-04 | Elevation of Privilege | compact-block request scope | mitigate | Checker and tests reject `cmpctblock`, `getblocktxn`, `blocktxn`, compact reconstruction, and full-block fallback claims for compact inventory. |
| T-111-03-05 | Repudiation | parity/release evidence | mitigate | Parity surface id `v2-1-full-block-serving-request-path` records BSRV-04, GOV-01, and GOV-05 with deterministic `scripts/verify.sh` wiring. |
</threat_model>

<verification>
Run after all tasks:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib phase111_ -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase111_ -- --nocapture
cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings
bun test scripts/check-phase111-full-block-serving-request-path.test.ts
bun run scripts/check-phase111-full-block-serving-request-path.ts
bun run scripts/check-parity-breadcrumbs.ts --check
bash scripts/check-file-lengths.sh
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Historical, pruned, recent-valid, stale, side-chain, unavailable, compact block, and ineligible block-serving requests are covered by deterministic local tests with low-cardinality outcomes.
- Adversarial `getdata` bursts and permissioned/protected peer cases remain bounded by existing request, queue, and in-flight governance.
- Phase 111 docs and parity evidence claim only bounded eligible full/witness block serving from validated local data.
- Deterministic checker and `scripts/verify.sh` reject compact-block payload serving, `getblocktxn`, `blocktxn`, archive-node behavior, public-serving defaults, package/filter relay, public-network CI, production readiness, production service operation, production-funds wallet use, and schema/ORM work.
</success_criteria>

<output>
After completion, create `.planning/phases/111-full-block-serving-request-path/111-03-SUMMARY.md`.
</output>
