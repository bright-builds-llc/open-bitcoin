---
phase: 111-full-block-serving-request-path
plan: 04
type: execute
wave: 3
depends_on: [111-03]
gap_closure: true
files_modified:
  - scripts/check-phase111-full-block-serving-request-path.ts
  - scripts/check-phase111-full-block-serving-request-path.test.ts
autonomous: true
requirements: [BSRV-04, GOV-01, GOV-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 111-2026-07-04T14-58-18
generated_at: 2026-07-04T19:02:53.197Z
must_haves:
  truths:
    - "The Phase 111 checker reads every evidence root it declares as required for the bounded full/witness block-serving request path."
    - "Forbidden positive compact/archive/public/default/production claims cannot be bypassed by broad no-claim marker wording such as 'only supports'."
  artifacts:
    - path: "scripts/check-phase111-full-block-serving-request-path.ts"
      provides: "Expanded Phase 111 checker corpus and narrowed no-claim marker logic"
    - path: "scripts/check-phase111-full-block-serving-request-path.test.ts"
      provides: "Mutation tests for declared evidence roots and the 'only supports' forbidden-claim bypass"
  key_links:
    - from: "scripts/check-phase111-full-block-serving-request-path.ts"
      to: "packages/open-bitcoin-network/src/peer/inventory_state.rs"
      via: "TARGET_FILES evidence corpus"
      pattern: "inventory_state.rs"
    - from: "scripts/check-phase111-full-block-serving-request-path.ts"
      to: "packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs"
      via: "TARGET_FILES evidence corpus"
      pattern: "relay_serving_cases.rs"
---

<objective>
Close the Phase 111 verification gaps by making the static checker enforce the same evidence roots and no-claim boundary that its verification report requires.

Purpose: Preserve the Phase 111 scope claim: bounded eligible full/witness block serving only, with compact relay payloads and archive-node behavior deferred.
Output: Checker corpus fixes, mutation coverage for omitted evidence roots, mutation coverage for the `only supports ...` forbidden-claim bypass, and a refreshed verification report.
</objective>

<tasks>
1. Add the omitted evidence roots to the checker corpus.
   - Update `TARGET_FILES` in `scripts/check-phase111-full-block-serving-request-path.ts` to include `packages/open-bitcoin-network/src/peer/inventory_state.rs` and `packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs`.
   - Update the test fixture's `TARGET_FILES` and `fixtureFiles()` map with representative content for both roots.
   - Acceptance: removing Phase 111 evidence from either newly included root causes the checker to fail.

2. Narrow forbidden-claim suppression.
   - Remove or narrow the standalone `only` no-claim marker so it cannot suppress positive support claims.
   - Add a mutation test proving `Phase 111 only supports BIP152 compact block payload serving.` is rejected.
   - Acceptance: explicit negative/no-claim wording remains allowed.

3. Re-run targeted verification.
   - Run `bun test scripts/check-phase111-full-block-serving-request-path.test.ts`.
   - Run `bun run scripts/check-phase111-full-block-serving-request-path.ts`.
   - Re-run Phase 111 goal verification and require `status: passed` before any finalization.
</tasks>

<verification>
- `bun test scripts/check-phase111-full-block-serving-request-path.test.ts`
- `bun run scripts/check-phase111-full-block-serving-request-path.ts`
- Phase 111 `gsd-verifier` re-verification writes `111-VERIFICATION.md` with `status: passed`.
</verification>

<success_criteria>
- The checker reads every path named in the gap report's missing evidence roots.
- The checker rejects forbidden positive claims even when they contain `only`.
- Existing explicit no-claim wording stays accepted.
- Phase 111 verification passes cleanly after the fix.
</success_criteria>
