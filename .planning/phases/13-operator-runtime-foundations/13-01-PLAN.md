---
phase: 13-operator-runtime-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/storage-decision.md
  - packages/open-bitcoin-node/src/storage.rs
  - packages/open-bitcoin-node/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - DB-01
must_haves:
  truths:
    - "Contributors can inspect one database ADR that compares fjall, redb, and rocksdb against chainstate, headers, block index, wallet, metrics, recovery, Bazel, and dependency constraints."
    - "The selected storage direction is explicit before durable adapters or real sync work depend on it."
    - "The node crate exposes storage contract types without adding filesystem or database side effects to pure core crates."
  artifacts:
    - path: docs/architecture/storage-decision.md
      provides: "DB-01 database decision ADR"
      contains: "Decision: fjall"
    - path: packages/open-bitcoin-node/src/storage.rs
      provides: "Storage contract and recovery semantics for later adapters"
      contains: "StorageRecoveryAction"
    - path: docs/parity/source-breadcrumbs.json
      provides: "Parity breadcrumb manifest coverage for the new Rust source"
      contains: "node-storage-contract"
  key_links:
    - from: packages/open-bitcoin-node/src/lib.rs
      to: packages/open-bitcoin-node/src/storage.rs
      via: "public module export"
      pattern: "pub mod storage"
    - from: docs/architecture/storage-decision.md
      to: packages/open-bitcoin-node/src/storage.rs
      via: "ADR names the contract symbols Phase 14 must implement"
      pattern: "StorageNamespace|StorageRecoveryAction|StorageError"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-04-26T18-50-22
generated_at: 2026-04-26T18:50:22.441Z
---

<objective>
Create the storage decision and adapter-facing storage contract that later durable storage work must follow.

Purpose: Satisfy DB-01 without adding a durable database adapter, network sync persistence, or storage side effects to pure core crates.
Output: A documented database decision plus typed storage namespace, schema, persistence, and recovery contracts in the node shell crate.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-operator-runtime-foundations/13-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/open-bitcoin-node/src/lib.rs
@packages/open-bitcoin-node/BUILD.bazel
@packages/open-bitcoin-node/Cargo.toml
@docs/parity/source-breadcrumbs.json

<interfaces>
Existing node crate root:
```rust
pub mod chainstate;
pub mod mempool;
pub mod network;
pub mod wallet;

pub use chainstate::{ChainstateStore, ManagedChainstate, MemoryChainstateStore};
pub use mempool::ManagedMempool;
pub use network::{ManagedNetworkError, ManagedPeerNetwork};
pub use open_bitcoin_core as core;
pub use wallet::{ManagedWallet, MemoryWalletStore, WalletStore};
```

Existing breadcrumb manifest pattern for Open Bitcoin-only node support files:
```json
{
  "label": "node-support",
  "files": [
    "packages/open-bitcoin-node/src/lib.rs"
  ],
  "breadcrumbs": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the database decision ADR</name>
  <read_first>
    - docs/architecture/storage-decision.md
    - .planning/phases/13-operator-runtime-foundations/13-RESEARCH.md
    - .planning/research/STACK.md
    - .planning/research/PITFALLS.md
    - AGENTS.md
  </read_first>
  <files>
    docs/architecture/storage-decision.md
  </files>
  <acceptance_criteria>
    - `docs/architecture/storage-decision.md` contains `Decision: fjall`.
    - The ADR contains comparison rows or sections for `fjall`, `redb`, and `rocksdb`.
    - The ADR contains the exact storage constraint terms `chainstate`, `headers`, `block index`, `wallet`, `metrics`, `recovery`, `Bazel`, and `dependency`.
    - The ADR contains `Phase 14 obligations` with `schema version`, `corruption`, `restart`, `interrupted write`, `reindex`, and `repair`.
    - The ADR states that RocksDB remains a fallback only if Rust-native options fail measured storage and recovery checks.
  </acceptance_criteria>
  <action>Create `docs/architecture/storage-decision.md` as an ADR for DB-01. Select `fjall` as the milestone default decision target because it is Rust-native, LSM-shaped, keyspace-oriented, and avoids the native C++ build cost of `rocksdb`. Compare `fjall`, `redb`, and `rocksdb` against the required axes: chainstate, headers, block index, wallet, metrics, recovery, Bazel, dependency footprint, schema versioning, corruption handling, interrupted writes, reindex, and repair. Include a `Phase 14 obligations` section that explicitly requires adapter tests for schema version mismatch, corruption classification, restart persistence, interrupted-write recovery, reindex, and repair before real sync persistence depends on the adapter. Do not add any storage dependency or concrete database adapter in this task.</action>
  <verify>
    <automated>rg -n "Decision: fjall|fjall|redb|rocksdb|Phase 14 obligations|schema version|corruption|interrupted write|reindex|repair" docs/architecture/storage-decision.md</automated>
  </verify>
  <done>The repository has a single inspectable database decision record satisfying DB-01 and naming the storage evidence required before durable adapter implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define storage contract types in the node shell crate</name>
  <read_first>
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/lib.rs
    - docs/architecture/storage-decision.md
    - ../coding-and-architecture-requirements/standards/core/architecture.md
    - ../coding-and-architecture-requirements/standards/languages/rust.md
  </read_first>
  <files>
    packages/open-bitcoin-node/src/storage.rs
  </files>
  <behavior>
    - Test 1: `StorageNamespace::as_str()` returns stable lowercase names for `headers`, `block_index`, `chainstate`, `wallet`, `metrics`, `runtime`, and `schema`.
    - Test 2: `StorageRecoveryAction::operator_message()` includes actionable text for `Restart`, `Reindex`, `Repair`, and `RestoreFromBackup`.
    - Test 3: `StorageError::schema_mismatch(expected, actual)` formats without panicking and exposes both versions.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/storage.rs` contains `pub enum StorageNamespace`.
    - `packages/open-bitcoin-node/src/storage.rs` contains `pub struct SchemaVersion`.
    - `packages/open-bitcoin-node/src/storage.rs` contains `pub enum PersistMode`.
    - `packages/open-bitcoin-node/src/storage.rs` contains `pub enum StorageRecoveryAction`.
    - `packages/open-bitcoin-node/src/storage.rs` contains `pub enum StorageError`.
    - `packages/open-bitcoin-node/src/storage.rs` does not contain `std::fs`, `fjall::`, `redb::`, or `rocksdb::`.
  </acceptance_criteria>
  <action>Create `packages/open-bitcoin-node/src/storage.rs` with typed contracts only. Define `StorageNamespace` variants `Headers`, `BlockIndex`, `Chainstate`, `Wallet`, `Metrics`, `Runtime`, and `Schema`; `SchemaVersion` as a checked nonzero u32 newtype; `PersistMode` variants `Buffered`, `Flush`, and `Sync`; `StorageRecoveryAction` variants `Restart`, `Reindex`, `Repair`, and `RestoreFromBackup`; and `StorageError` variants for schema mismatch, corruption, unavailable namespace, interrupted write, and backend failure. Add focused unit tests inside this file using Arrange, Act, Assert comments. Keep this module free of filesystem calls and concrete database crate imports so it remains an adapter contract, not an implementation.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage::</automated>
  </verify>
  <done>The node crate has typed storage and recovery contracts that compile and are unit-tested without opening or writing a database.</done>
</task>

<task type="auto">
  <name>Task 3: Wire storage contracts and parity breadcrumbs</name>
  <read_first>
    - packages/open-bitcoin-node/src/lib.rs
    - packages/open-bitcoin-node/src/storage.rs
    - docs/parity/source-breadcrumbs.json
    - scripts/check-parity-breadcrumbs.ts
    - scripts/verify.sh
  </read_first>
  <files>
    packages/open-bitcoin-node/src/lib.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/lib.rs` contains `pub mod storage;`.
    - `packages/open-bitcoin-node/src/lib.rs` re-exports `StorageNamespace`, `StorageRecoveryAction`, `SchemaVersion`, `PersistMode`, and `StorageError`.
    - `docs/parity/source-breadcrumbs.json` contains label `node-storage-contract`.
    - `docs/parity/source-breadcrumbs.json` lists `packages/open-bitcoin-node/src/storage.rs`.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
  </acceptance_criteria>
  <action>Export the storage contract module from `packages/open-bitcoin-node/src/lib.rs`, and add a `node-storage-contract` entry to `docs/parity/source-breadcrumbs.json` with the `packages/open-bitcoin-node/src/storage.rs` file. Use an empty breadcrumbs array for this Open Bitcoin-only adapter contract and rely on the manifest `noneReason`; do not claim a direct Knots source anchor for the contract itself. Run the breadcrumb checker and targeted Rust tests before the full repo verifier.</action>
  <verify>
    <automated>rg -n "pub mod storage|StorageNamespace|StorageRecoveryAction|SchemaVersion|PersistMode|StorageError" packages/open-bitcoin-node/src/lib.rs
rg -n "node-storage-contract|packages/open-bitcoin-node/src/storage.rs" docs/parity/source-breadcrumbs.json
bun run scripts/check-parity-breadcrumbs.ts --check
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage::
bash scripts/verify.sh</automated>
  </verify>
  <done>The storage contract is reachable from the node crate, breadcrumb-covered, and verified through the repo-native gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributors -> storage ADR | Contributors use the ADR to make dependency and recovery decisions that can affect all persisted node state. |
| storage adapter implementers -> node storage contracts | Later adapter code will map untrusted on-disk state into typed storage and recovery outcomes. |
| storage contracts -> pure core crates | The node shell must not push database or filesystem effects into pure consensus, chainstate, mempool, wallet, or protocol crates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | T | `docs/architecture/storage-decision.md` | mitigate | Require the ADR to compare `fjall`, `redb`, and `rocksdb` across recovery, Bazel, and dependency constraints, verified with `rg` for the required decision and obligation strings. |
| T-13-01-02 | D | `packages/open-bitcoin-node/src/storage.rs` | mitigate | Model schema mismatch, corruption, interrupted writes, reindex, repair, and backup restoration as typed errors/actions so later adapters cannot collapse recovery into panics. |
| T-13-01-03 | E | pure core crates | mitigate | Keep storage contracts in `open-bitcoin-node` only and verify the module contains no `std::fs`, `fjall::`, `redb::`, or `rocksdb::` imports. |
| T-13-01-04 | D | storage dependency selection | mitigate | Record RocksDB as a fallback only after measured Rust-native failure so native build cost cannot enter the workspace without explicit evidence. |
</threat_model>

<verification>
- `rg` proves the ADR contains the storage decision, option comparison, and recovery obligations.
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage::` proves the contract behavior.
- `bun run scripts/check-parity-breadcrumbs.ts --check` proves new Rust source is covered.
- `bash scripts/verify.sh` proves the repo-native verification contract still passes.
</verification>

<success_criteria>
- DB-01 is satisfied by an inspectable ADR comparing Rust-native and RocksDB-style options.
- The node shell crate exposes storage recovery contracts without adding durable adapter behavior.
- Pure core crates remain free of storage dependencies and side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/13-operator-runtime-foundations/13-01-SUMMARY.md`
</output>
