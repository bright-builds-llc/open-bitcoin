---
phase: 34-migration-detection-ownership-model-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - packages/open-bitcoin-cli/src/operator/migration.rs
  - packages/open-bitcoin-cli/src/operator/migration/planning.rs
  - packages/open-bitcoin-cli/src/operator/migration/service_evidence.rs
  - packages/open-bitcoin-cli/src/operator/migration/tests.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 34-2026-04-30T07-38-33
generated_at: 2026-04-30T07:38:33.000Z

must_haves:
  truths:
    - "Migration planning keeps the current selected-source service-review truth after the shared ownership model changes."
    - "Ambiguous service ownership still degrades to explicit manual review instead of showing unrelated service paths."
    - "Explicit `--source-datadir` migration flows keep working with the tightened scan-level evidence."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/migration/planning.rs"
      provides: "migration planning that consumes scan-level service evidence explicitly"
    - path: "packages/open-bitcoin-cli/src/operator/migration/service_evidence.rs"
      provides: "service-ownership association against the shared scan-level model"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "operator-binary regression coverage for the tightened migration evidence model"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/migration/planning.rs"
      to: "packages/open-bitcoin-cli/src/operator/migration/service_evidence.rs"
      via: "selected-source service review is rebuilt from explicit scan-level service evidence"
    - from: "packages/open-bitcoin-cli/src/operator/migration.rs"
      to: "packages/open-bitcoin-cli/src/operator/runtime.rs"
      via: "migration command now accepts the tightened shared detection report"
---

# Phase 34 Plan 02: Adopt The Tightened Ownership Model In Migration

## Objective

Preserve the current truthful migration planner behavior while switching it from
the old cloned per-install service list to the new scan-level service evidence.

## Scope

- Migration command input shape
- Selected-source service association and summaries
- Focused planner and operator-binary regressions

## Tasks

1. Update migration command entrypoints and pure planner helpers to consume the
   tightened detection report instead of a bare vector of installations.
2. Change service association helpers to match a selected installation against
   the scan-level service list explicitly.
3. Preserve manual-review fallbacks and relevant-deviation truth when service
   ownership remains ambiguous after the refactor.
4. Refresh focused migration unit tests and operator-binary coverage so the
   selected-source and ambiguous-service cases still prove the truthful flow.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli migration::tests`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary -- --nocapture open_bitcoin_migrate_plan_selects_explicit_custom_source_outside_default_roots`
