---
phase: 90-inbound-listener-and-admission-policy
plan: 08
type: execute
wave: 4
depends_on:
  - 90-05
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements:
  - INB-05
requirements_addressed:
  - INB-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Support bundle JSON includes bounded shared inbound evidence."
    - "Support bundle Markdown has an inbound serving section with diagnostic labels and next actions."
    - "Support evidence does not copy raw unbounded peer tables or sensitive endpoint detail."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "Bounded inbound support Markdown rendering"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "Redaction and bounded evidence regressions"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "embedded OpenBitcoinStatusSnapshot"
      pattern: "status"
---

<objective>
Add bounded inbound listener/admission evidence to support bundles.

Purpose: Operators need useful diagnostics for listener/preflight/admission outcomes, but support bundles must avoid raw peer tables and sensitive local endpoint leakage.
Output: Support JSON/Markdown evidence with redaction and bounded rendering tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-05-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render bounded inbound support evidence</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/src/operator/support/render/inbound.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test setup expectation: support JSON continues embedding `OpenBitcoinStatusSnapshot` so inbound evidence appears under `status.peers.inbound`.
    - Markdown expectation: `## Inbound Serving` renders listener state, preflight reason, admitted/rejected counts, handshake counts, duplicate/self/cap/reserved-slot rejects, and latest event.
    - Redaction expectation: endpoint evidence is bounded and rendered as redacted or compact labels when needed.
  </behavior>
  <action>Create `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs` and call it from `render.rs`. If adding the module call would trip `scripts/check-file-lengths.sh`, first move an existing support rendering section into a child module to create headroom. Keep support JSON based on the embedded shared status snapshot; do not add a parallel support-only inbound DTO unless it is a compact projection with a clearly named source field. Render reserved-slot/protected-admission rejects as Phase 90 admission evidence without introducing Phase 91 permission-class wording. Add redaction summary items such as `inbound peer endpoints bounded/redacted` when endpoint details are omitted. Do not include raw peer tables, raw logs, cookies, RPC credentials, wallet material, or unbounded endpoint lists.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture</automated>
    <automated>bash scripts/check-file-lengths.sh</automated>
  </verify>
  <acceptance_criteria>
    - Support Markdown contains an inbound serving section.
    - Support JSON uses shared status evidence.
    - File-length checks pass.
  </acceptance_criteria>
  <done>Support bundles include inbound diagnostics without duplicating the status model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove support redaction and no raw peer tables</name>
  <files>packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/support/tests.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  </read_first>
  <behavior>
    - Test: support JSON and Markdown contain inbound listener/admission labels when status evidence is available.
    - Test: support JSON and Markdown preserve unavailable inbound reason when evidence is unavailable.
    - Test: support JSON and Markdown do not contain raw sample peer endpoint table values such as multiple unredacted public IP:port rows.
    - Test: support redaction summary includes the inbound endpoint handling safeguard.
  </behavior>
  <action>Add focused support tests with explicit Arrange, Act, Assert comments. Use representative loopback and documentation-reserved public endpoint strings in fixtures, then assert only bounded/redacted forms appear. Keep tests deterministic and local; do not spawn daemons or network listeners in support rendering tests.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture</automated>
    <automated>rg -n "Inbound Serving|listener_state|preflight_reason|duplicate|self_connection|cap_reject|reserved_slot|redact" packages/open-bitcoin-cli/src/operator/support/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - Support redaction behavior is regression-tested.
    - Inbound unavailable evidence is preserved.
    - Raw unbounded peer table data is absent.
  </acceptance_criteria>
  <done>Support evidence is useful for diagnosis and bounded for safe local sharing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local peer evidence to support bundle | Local endpoint and peer data can be shared outside the machine. |
| Shared status to Markdown projection | Renderer text can omit redaction or invent unsupported conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-08-01 | Information Disclosure | support JSON/Markdown | mitigate | Render bounded/redacted inbound endpoint evidence and add tests with sensitive-looking endpoint strings. |
| T-90-08-02 | Repudiation | unavailable inbound evidence | mitigate | Preserve unavailable reason in support JSON and Markdown. |
| T-90-08-03 | Tampering | support-specific DTO drift | mitigate | Use embedded `OpenBitcoinStatusSnapshot` as the source and keep projection logic narrow. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture
```
</verification>

<success_criteria>
- INB-05 support evidence is present, bounded, and redacted where needed.
- Support output does not contain raw unbounded peer tables.
- Support text does not imply relay, public default, or production readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-08-SUMMARY.md`.
</output>
