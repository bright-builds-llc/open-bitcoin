---
phase: 90-inbound-listener-and-admission-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/inbound.rs
  - packages/open-bitcoin-network/src/inbound/tests.rs
  - packages/open-bitcoin-network/src/lib.rs
autonomous: true
requirements:
  - INB-02
  - INB-03
  - INB-04
requirements_addressed:
  - INB-02
  - INB-03
  - INB-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Listener configuration can be classified without socket side effects."
    - "Admission decisions are pure data-in/data-out decisions before peer insertion."
    - "Inbound caps, reserved slots, duplicate endpoint, duplicate peer id, and self-connection outcomes have stable labels."
    - "Phase 90 reserved-slot/protected-admission handling is modeled without introducing Phase 91 permission classes."
  artifacts:
    - path: "packages/open-bitcoin-network/src/inbound.rs"
      provides: "Pure inbound listener preflight and admission policy contracts"
      exports:
        - "InboundListenerConfig"
        - "InboundPreflightDiagnostic"
        - "InboundAdmissionPolicy"
        - "InboundAdmissionDecision"
        - "InboundPeerRecord"
        - "InboundAdmissionSlotClass"
    - path: "packages/open-bitcoin-network/src/inbound/tests.rs"
      provides: "Pure preflight and admission unit coverage"
  key_links:
    - from: "packages/open-bitcoin-network/src/lib.rs"
      to: "packages/open-bitcoin-network/src/inbound.rs"
      via: "module export"
      pattern: "pub use inbound::"
---

<objective>
Create the pure inbound listener and admission policy contracts.

Purpose: Phase 90 needs deterministic listener and admission decisions before any Tokio socket work. This plan gives later config, peer, runtime, status, and support work one typed source of truth.
Output: `open-bitcoin-network` inbound contracts, pure policy implementation, and focused tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@packages/open-bitcoin-network/src/lib.rs
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/message.rs
@packages/bitcoin-knots/src/net.cpp
@packages/bitcoin-knots/src/net_processing.cpp
@packages/bitcoin-knots/test/functional/p2p_handshake.py

<interfaces>
Create these contracts in `packages/open-bitcoin-network/src/inbound.rs`:

```rust
pub struct InboundListenerConfig {
    pub enabled: bool,
    pub listen_addresses: Vec<String>,
    pub max_peers: usize,
    pub reserved_slots: usize,
    pub allow_public: bool,
}

pub enum InboundPreflightReason {
    Disabled,
    NoListenAddresses,
    InvalidEndpoint,
    UnsafeEndpoint,
    BindUnavailable,
    AlreadyBound,
    Ready,
}

pub struct InboundPreflightDiagnostic {
    pub reason: InboundPreflightReason,
    pub maybe_endpoint: Option<String>,
    pub field: &'static str,
    pub message: String,
    pub next_action: String,
}

pub struct InboundAdmissionPolicy {
    pub max_inbound_peers: usize,
    pub reserved_slots: usize,
}

pub enum InboundAdmissionSlotClass {
    Ordinary,
    Reserved,
}

pub enum InboundAdmissionDecision {
    Admit(InboundPeerRecord),
    Reject(InboundAdmissionRejection),
}
```

Use `Option` fields with `maybe_` names. If exact names need to differ, keep the same semantics and update downstream summaries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define inbound policy contracts</name>
  <files>packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>
    - packages/open-bitcoin-network/src/lib.rs
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-network/src/message.rs
    - .planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
  </read_first>
  <behavior>
    - Contract: preflight reasons include exactly `disabled`, `no_listen_addresses`, `invalid_endpoint`, `unsafe_endpoint`, `bind_unavailable`, `already_bound`, and `ready` as stable serialized labels per D-03.
    - Contract: admission rejection reasons include stable labels for cap reached, reserved slot unavailable, duplicate endpoint, duplicate peer id, self connection, and shutdown per D-09, D-12, and D-14.
    - Contract: admission requests distinguish ordinary admission from reserved-slot/protected admission with a Phase 90-only slot class; do not encode Knots permission names or Phase 91 connection classes.
    - Contract: reserved slots are represented only as a Phase 90 admission primitive, not as Knots permission classes per D-13.
  </behavior>
  <action>Create `packages/open-bitcoin-network/src/inbound.rs` with a top-of-file parity breadcrumb block citing `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, and `packages/bitcoin-knots/test/functional/p2p_handshake.py`. Add typed structs/enums for listener config, parsed endpoints, preflight diagnostics, activation diagnostics for OS-observed bind results, admission requests, admission decisions, admission rejections, inbound peer records, handshake state, and bounded inbound counters. Keep these types pure: no Tokio, no file system, no sockets, no clocks. Add `mod inbound;` and `pub use inbound::{...};` in `lib.rs`. Do not add permission names such as `noban`, `forcerelay`, `mempool`, `addr`, `download`, `whitebind`, or `whitelist` because Phase 91 owns those surfaces.</action>
  <verify>
    <automated>rg -n "pub struct InboundListenerConfig|pub enum InboundPreflightReason|pub struct InboundAdmissionPolicy|pub enum InboundAdmissionDecision|pub struct InboundPeerRecord|pub use inbound" packages/open-bitcoin-network/src/inbound.rs packages/open-bitcoin-network/src/lib.rs</automated>
    <automated>! rg -n "noban|forcerelay|mempool|whitebind|whitelist|NetPermission" packages/open-bitcoin-network/src/inbound.rs</automated>
  </verify>
  <acceptance_criteria>
    - New inbound contracts are exported from `open-bitcoin-network`.
    - The module contains Knots parity breadcrumbs and no runtime side effects.
    - The contract vocabulary matches D-03, D-09, D-12, and D-13.
  </acceptance_criteria>
  <done>Downstream plans can import typed inbound policy contracts from `open-bitcoin-network` without inventing local DTOs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure preflight and admission decisions</name>
  <files>packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/inbound/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-network/src/inbound.rs
    - packages/open-bitcoin-network/src/peer/tests.rs
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <behavior>
    - Test: disabled config returns `disabled` and no bind-attempt intent per D-01.
    - Test: enabled config with no addresses returns `no_listen_addresses` with field `inbound.listen_addresses`.
    - Test: malformed endpoints return `invalid_endpoint` with endpoint and next action.
    - Test: wildcard or non-loopback endpoints return `unsafe_endpoint` unless `allow_public` is true per D-04.
    - Test: loopback endpoints return `ready` with normalized endpoints.
    - Test: admission rejects cap reached without changing admitted counts per D-12.
    - Test: ordinary peers cannot consume reserved slots when ordinary capacity is full, while reserved-slot candidates can consume available reserved capacity without introducing permission classes per D-13.
    - Test: reserved-slot candidates are rejected with `reserved_slot_unavailable` when the reserved pool is full.
    - Test: duplicate endpoint, duplicate peer id, and local nonce self-connection reject before admitted counts increase per D-09.
    - Test: inbound/outbound counts are separate policy inputs per D-11.
  </behavior>
  <action>Implement pure functions such as `classify_inbound_preflight(config: &InboundListenerConfig) -> InboundPreflightPlan` and `InboundAdmissionPolicy::decide(request: InboundAdmissionRequest) -> InboundAdmissionDecision`. OS-observed `bind_unavailable` and `already_bound` must be represented as typed activation diagnostics but not produced by the pure config classifier. Every diagnostic must include a stable reason, `maybe_endpoint`, field name, human message, and next action per D-05. Tests must use explicit Arrange, Act, Assert comments. Keep Phase 94 resource-governance behavior out of this module; this policy covers caps and admission only.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture</automated>
    <automated>rg -n "disabled|no_listen_addresses|invalid_endpoint|unsafe_endpoint|bind_unavailable|already_bound|ready|Arrange|Act|Assert" packages/open-bitcoin-network/src/inbound/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - Focused network tests pass.
    - Pure preflight and admission behavior covers D-01, D-03, D-04, D-05, D-09, D-11, D-12, and D-13.
    - No socket, async runtime, public-network probe, ban, eviction, address relay, transaction relay, or compact-block behavior is introduced.
  </acceptance_criteria>
  <done>Pure inbound policy behavior is implemented and tested before runtime adapters consume it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Config text to inbound policy | User-controlled config and CLI strings become typed listener endpoints and admission limits. |
| Remote peer identity to admission | Untrusted endpoint, peer id, and nonce observations decide whether a connection becomes admitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-01-01 | Tampering | `InboundListenerConfig` parsing contract | mitigate | Parse endpoints into typed diagnostics and keep invalid values out of runtime bind calls. |
| T-90-01-02 | Information Disclosure | public endpoint preflight | mitigate | Reject public or wildcard endpoints unless `allow_public` is true and keep default tests loopback-only. |
| T-90-01-03 | Denial of Service | inbound admission caps | mitigate | Enforce pure cap and reserved-slot decisions before peer insertion. |
| T-90-01-04 | Spoofing | self-connection nonce | mitigate | Reject a remote nonce matching local nonce before admission evidence counts it as accepted. |
| T-90-01-05 | Elevation of Privilege | permission-class creep | mitigate | Do not model Knots permission classes or relay privileges in Phase 90 policy. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture
```

Full repo verification is intentionally deferred until the final Phase 90 verifier plan wires breadcrumbs and docs.
</verification>

<success_criteria>
- `open-bitcoin-network` exports pure inbound listener and admission contracts.
- D-01 through D-05, D-09, D-11 through D-13, D-19, and D-20 have focused test coverage where applicable.
- Phase 91+ permission, address, eviction, ban, DoS, relay, compact-block, mempool, public-default, and production-readiness surfaces are not implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-01-SUMMARY.md`.
</output>
