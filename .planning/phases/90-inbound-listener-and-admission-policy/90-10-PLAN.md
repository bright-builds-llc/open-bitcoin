---
phase: 90-inbound-listener-and-admission-policy
plan: 10
type: execute
wave: 7
depends_on:
  - 90-09
files_modified:
  - scripts/check-phase90-inbound-listener-admission.test.ts
  - scripts/check-phase90-inbound-listener-admission.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - INB-01
  - INB-02
  - INB-03
  - INB-04
  - INB-05
requirements_addressed:
  - INB-01
  - INB-02
  - INB-03
  - INB-04
  - INB-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Deterministic verification proves Phase 90 docs, parity roots, UAT commands, breadcrumbs, and no-claim boundaries."
    - "The checker proves inbound status, metrics, structured-log, RPC, support, and reserved-slot evidence are documented."
    - "`bash scripts/verify.sh` runs the Phase 90 checker without public-network, service-manager, or long-running gates."
    - "Full repo verification passes after Phase 90."
  artifacts:
    - path: "scripts/check-phase90-inbound-listener-admission.ts"
      provides: "Deterministic Phase 90 checker"
      exports:
        - "checkPhase90InboundListenerAdmission"
    - path: "scripts/check-phase90-inbound-listener-admission.test.ts"
      provides: "Checker fixture tests"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase90-inbound-listener-admission.ts"
      via: "run_step"
      pattern: "check Phase 90 inbound listener admission"
---

<objective>
Add deterministic Phase 90 verification and wire it into the repo-native verifier.

Purpose: Phase 90 expands network participation, so default verification must prove the local deterministic evidence and reject scope drift without adding public-network exposure.
Output: Phase 90 checker, fixture tests, verifier wiring, and final full verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-09-SUMMARY.md
@scripts/verify.sh
@scripts/check-phase88-deterministic-claim-guardrails.ts
@docs/operator/runtime-guide.md
@docs/architecture/config-precedence.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/parity/catalog/p2p.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/source-breadcrumbs.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 90 checker fixtures</name>
  <files>scripts/check-phase90-inbound-listener-admission.test.ts</files>
  <read_first>
    - scripts/check-phase88-deterministic-claim-guardrails.test.ts
    - scripts/verify.sh
    - .planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
    - .planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
  </read_first>
  <behavior>
    - Test: valid fixture with INB-01 through INB-05 roots, UAT Cargo/Bazel daemon/RPC/status/support commands, `openbitcoinnetworkstatus`, structured-log labels, reserved-slot evidence, exact source breadcrumbs, and verifier wiring returns `[]`.
    - Test: missing any INB requirement in parity roots fails.
    - Test: missing Cargo or Bazel daemon/status/support command forms fails.
    - Test: missing any required new Rust source breadcrumb path fails.
    - Test: missing inbound log labels or reserved-slot evidence labels fails.
    - Test: default verifier text containing public-network listener probes, `0.0.0.0`, `::`, `systemctl`, `launchctl`, long sleeps, transaction relay, compact blocks, mempool propagation, permission classes, address relay, eviction, ban, or DoS governance fails.
    - Test: docs claiming public inbound by default or production full-node readiness fails.
  </behavior>
  <action>Create `scripts/check-phase90-inbound-listener-admission.test.ts` with `#!/usr/bin/env bun`, `bun:test`, temp fixture helpers, and explicit Arrange/Act/Assert comments. Fixture files must include only the curated Phase 90 docs/checker corpus, not `.planning/` scans or milestone archives. Include constants for `SURFACE_ID = "v1-9-inbound-listener-admission-policy"`, `PHASE90_REQUIREMENTS = ["INB-01", "INB-02", "INB-03", "INB-04", "INB-05"] as const`, `REQUIRED_BREADCRUMB_PATHS` listing the eight new Rust files from Plan 09, and command families for Cargo/Bazel daemon startup, `open-bitcoin-cli getnetworkinfo`, `open-bitcoin --format json status`, and `open-bitcoin support bundle --output-dir=/tmp/open-bitcoin-inbound-support`. Include commands `bun test scripts/check-phase90-inbound-listener-admission.test.ts`, `bun run scripts/check-phase90-inbound-listener-admission.ts`, and `bash scripts/verify.sh`.</action>
  <verify>
    <automated>rg -n "v1-9-inbound-listener-admission-policy|INB-01|INB-02|INB-03|INB-04|INB-05|REQUIRED_BREADCRUMB_PATHS|support bundle --output-dir=/tmp/open-bitcoin-inbound-support|inbound_listener_state|reserved_slot|Arrange|Act|Assert|public inbound by default|production full-node readiness" scripts/check-phase90-inbound-listener-admission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fixture tests describe positive and negative checker behavior before implementation.
    - Test fixtures cover UAT commands, verifier boundary, parity roots, exact breadcrumbs, log/status/support evidence, reserved-slot evidence, and no-claim wording.
  </acceptance_criteria>
  <done>Phase 90 checker behavior is specified by deterministic fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checker and verifier wiring</name>
  <files>scripts/check-phase90-inbound-listener-admission.ts, scripts/verify.sh</files>
  <read_first>
    - scripts/check-phase90-inbound-listener-admission.test.ts
    - scripts/check-phase88-deterministic-claim-guardrails.ts
    - scripts/verify.sh
    - docs/operator/runtime-guide.md
    - docs/parity/catalog/p2p.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/source-breadcrumbs.json
  </read_first>
  <behavior>
    - Checker returns failures for missing docs, missing requirements, missing command forms, missing breadcrumb mappings, missing structured-log/status/support/reserved-slot evidence, and forbidden default verifier drift.
    - Checker strips legacy command-order heredocs and validates executable `run_step` text.
    - `scripts/verify.sh` runs Phase 90 checker test then checker after Phase 88 checks and before pure-core/file-length/Rust checks.
  </behavior>
  <action>Create `scripts/check-phase90-inbound-listener-admission.ts` exporting `checkPhase90InboundListenerAdmission(maybeRepoRoot = process.env.OPEN_BITCOIN_PHASE90_REPO_ROOT): string[]`. Use fixed-file reads only. Validate `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/catalog/p2p.md`, `docs/operator/runtime-guide.md`, architecture docs, `docs/parity/source-breadcrumbs.json`, and `scripts/verify.sh`. Fail if required command forms are missing, including Cargo/Bazel daemon startup, Cargo/Bazel `open-bitcoin-cli getnetworkinfo`, Cargo/Bazel `open-bitcoin --format json status`, and Cargo/Bazel `open-bitcoin support bundle --output-dir=/tmp/open-bitcoin-inbound-support`. Fail if `openbitcoinnetworkstatus`, `openbitcoininbound`, `openbitcoinlisten`, loopback/public acknowledgement wording, `inbound_listener_state`, `inbound_preflight_reason`, `admission_reject_reason`, reserved-slot evidence labels, INB requirement ids, or the exact new source breadcrumb mappings from Plan 09 are missing. Fail if executable verifier text includes public-network listener probes, service-manager calls, multi-day sleeps, wildcard listener commands, transaction relay, compact block relay, mempool propagation, Knots permission classes, address relay, eviction, ban, or broad resource-governance claims. Wire the test/checker in `scripts/verify.sh` using `run_step`; do not add public-network, service-manager, long-running, or manual UAT commands to default verification.</action>
  <verify>
    <automated>bun test scripts/check-phase90-inbound-listener-admission.test.ts</automated>
    <automated>bun run scripts/check-phase90-inbound-listener-admission.ts</automated>
    <automated>rg -n "check Phase 90 inbound listener admission|check-phase90-inbound-listener-admission" scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - Checker and tests pass against the real repository.
    - `scripts/verify.sh` runs Phase 90 checker test and checker.
    - Default verification remains deterministic, local, short-running, public-network-free, and service-manager-free.
  </acceptance_criteria>
  <done>Phase 90 deterministic verification is wired into the repo-native verifier.</done>
</task>

<task type="auto">
  <name>Task 3: Run final repo-native verification</name>
  <files>docs/metrics/lines-of-code.md</files>
  <read_first>
    - AGENTS.md
    - scripts/verify.sh
  </read_first>
  <action>Run the repo-native verification contract. If `docs/metrics/lines-of-code.md` changes because the verifier regenerates the tracked LOC artifact, keep that generated freshness update. Fix any failures introduced by Phase 90 before marking the plan complete. Do not weaken checks or append `|| true` style failure masking.</action>
  <verify>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify.sh` exits 0.
    - Any LOC report changes are reviewed as tracked generated freshness updates.
    - No default verifier step contacts the public network or service manager.
  </acceptance_criteria>
  <done>Full Phase 90 verification passes under the repo-native contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Docs/verifier text to release boundary | Text changes can overstate inbound serving or add unsafe default verification. |
| Checker fixtures to real repository | Fixture-only success can miss missing real evidence roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-10-01 | Repudiation | Phase 90 checker | mitigate | Require INB-01 through INB-05 in parity roots and docs. |
| T-90-10-02 | Elevation of Privilege | docs and verifier wording | mitigate | Fail public-default, relay, permission, address relay, eviction/ban, DoS-governance, and production-readiness promotions. |
| T-90-10-03 | Denial of Service | `scripts/verify.sh` | mitigate | Reject public-network, service-manager, wildcard bind, and long-running default verifier drift. |
| T-90-10-04 | Tampering | verifier wiring | mitigate | Validate executable `run_step` order rather than heredoc-only command text. |
</threat_model>

<verification>
Final verification:

```bash
bun test scripts/check-phase90-inbound-listener-admission.test.ts
bun run scripts/check-phase90-inbound-listener-admission.ts
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Phase 90 deterministic checker is implemented and wired.
- Full repo verification passes.
- All INB requirements have traceable docs/checker/parity/status/RPC/CLI/support coverage.
- Phase 91+ surfaces remain out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-10-SUMMARY.md`.
</output>
