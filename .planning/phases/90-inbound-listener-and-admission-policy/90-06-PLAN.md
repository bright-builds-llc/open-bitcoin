---
phase: 90-inbound-listener-and-admission-policy
plan: 06
type: execute
wave: 4
depends_on:
  - 90-04
  - 90-05
files_modified:
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements:
  - INB-03
  - INB-05
requirements_addressed:
  - INB-03
  - INB-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Baseline `getnetworkinfo` keeps accurate `connections`, `connections_in`, and `connections_out`."
    - "Open Bitcoin-specific listener/admission evidence is available through an Open Bitcoin-owned RPC method."
    - "Runtime listener/admission evidence is normalized into the shared inbound status contract before RPC serialization."
    - "The RPC method list marks the new status method as an Open Bitcoin node extension."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/method.rs"
      provides: "`openbitcoinnetworkstatus` method normalization"
    - path: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      provides: "RPC response from shared inbound status contract"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "response serialization"
      pattern: "InboundPeerServingStatus"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "runtime evidence normalization"
      pattern: "current_inbound_status"
---

<objective>
Expose inbound listener/admission evidence through RPC without changing baseline `getnetworkinfo` shape.

Purpose: Phase 90 requires RPC-facing status, but Knots-shaped `getnetworkinfo` should remain predictable. Use an Open Bitcoin-owned extension for detailed inbound evidence.
Output: `openbitcoinnetworkstatus` RPC method and dispatch tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-04-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-05-SUMMARY.md
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-rpc/src/dispatch.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/method/node.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/bitcoin-knots/src/rpc/net.cpp

<interfaces>
Add an Open Bitcoin extension method:

```rust
#[serde(rename = "openbitcoinnetworkstatus")]
OpenBitcoinNetworkStatus,

pub struct OpenBitcoinNetworkStatusRequest {}

pub struct OpenBitcoinNetworkStatusResponse {
    pub inbound: FieldAvailability<InboundPeerServingStatus>,
}
```

The response can include peer counts if it reads them from existing `ManagedNetworkInfo`, but detailed listener/admission evidence must come from the shared status contract created in Plan 05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add method normalization for network status extension</name>
  <files>packages/open-bitcoin-rpc/src/method.rs, packages/open-bitcoin-rpc/src/method/node.rs, packages/open-bitcoin-rpc/src/method/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/open-bitcoin-rpc/src/method/tests.rs
    - packages/open-bitcoin-rpc/src/method/node.rs
  </read_first>
  <behavior>
    - Test: `openbitcoinnetworkstatus` appears in `SupportedMethod::all()`.
    - Test: the method is `MethodOrigin::OpenBitcoinExtension` and `MethodScope::Node`.
    - Test: positional or named parameters are rejected for the status method.
  </behavior>
  <action>Add `OpenBitcoinNetworkStatus` to `SupportedMethod`, `MethodCall`, normalization, method tests, and node request/response types. Keep the method name lower-case exactly `openbitcoinnetworkstatus`. Do not mark it as baseline parity. Do not add listener/admission fields to `GetNetworkInfoResponse` beyond existing accurate count fields.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc network_status -- --nocapture</automated>
    <automated>rg -n "openbitcoinnetworkstatus|OpenBitcoinNetworkStatus|OpenBitcoinExtension" packages/open-bitcoin-rpc/src/method.rs packages/open-bitcoin-rpc/src/method/node.rs packages/open-bitcoin-rpc/src/method/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - New RPC method normalizes and rejects parameters deterministically.
    - Method origin/scope are correct.
  </acceptance_criteria>
  <done>RPC method contracts are ready for dispatch wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch shared inbound status evidence</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/dispatch.rs, packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-rpc/src/context/network.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test: `openbitcoinnetworkstatus` returns listener state, preflight reason, handshake counts, duplicate/self/cap rejects, reserved-slot rejects, and latest admission event from shared status.
    - Test: unavailable inbound status preserves the unavailable reason.
    - Test: `getnetworkinfo` still serializes only baseline-shaped count fields and does not expose detailed listener/admission field names per D-16.
  </behavior>
  <action>Add a `ManagedRpcContext` accessor that normalizes Plan 04 listener/admission evidence into the Plan 05 `FieldAvailability<InboundPeerServingStatus>` contract, including unavailable reasons when no listener evidence has been recorded. Wire `MethodCall::OpenBitcoinNetworkStatus` in dispatch and implement `dispatch::node::open_bitcoin_network_status`. Add dispatch tests for available and unavailable status and a regression that serializing `getnetworkinfo` does not include `listener_state`, `preflight_reason`, `admission`, `duplicate_rejects`, `self_connection_rejects`, `reserved_slot_rejects`, or `cap_rejects`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_network_info -- --nocapture</automated>
    <automated>rg -n "current_inbound_status|InboundPeerServingStatus|FieldAvailability|reserved_slot" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/dispatch/node.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - Detailed inbound evidence is RPC-facing through the Open Bitcoin extension method.
    - Runtime listener/admission evidence is projected through the shared status contract, not through a dispatch-local DTO.
    - Baseline `getnetworkinfo` keeps accurate counts without detailed Open Bitcoin fields.
  </acceptance_criteria>
  <done>RPC consumers can inspect Phase 90 inbound listener/admission state without baseline response drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime status to RPC response | Internal listener/admission evidence becomes machine-readable RPC output. |
| Baseline RPC compatibility to Open Bitcoin extensions | New fields can accidentally alter baseline-shaped responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-06-01 | Information Disclosure | `openbitcoinnetworkstatus` | mitigate | Return bounded shared status evidence and rely on support redaction for bundle rendering. |
| T-90-06-02 | Tampering | `getnetworkinfo` shape | mitigate | Add regression proving detailed listener/admission fields are absent from baseline response. |
| T-90-06-03 | Repudiation | RPC method origin | mitigate | Mark method as `OpenBitcoinExtension` and test method scope/origin. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture
```
</verification>

<success_criteria>
- INB-05 RPC-facing detailed status exists through `openbitcoinnetworkstatus`.
- INB-03 baseline counts remain accurate in `getnetworkinfo`.
- No baseline response shape is broadened unexpectedly.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-06-SUMMARY.md`.
</output>
