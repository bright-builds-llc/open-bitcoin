---
phase: 90-inbound-listener-and-admission-policy
plan: 07
type: execute
wave: 5
depends_on:
  - 90-06
files_modified:
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/http.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
autonomous: true
requirements:
  - INB-05
requirements_addressed:
  - INB-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Operator status JSON includes shared inbound listener/admission evidence when live RPC is available."
    - "Human status separates inbound serving from outbound sync."
    - "Missing or older-daemon inbound status renders `Unavailable: <reason>`."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      provides: "Inbound status human rendering helper"
    - path: "packages/open-bitcoin-cli/src/operator/status/http.rs"
      provides: "Live RPC adapter call for `openbitcoinnetworkstatus`"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/http.rs"
      to: "openbitcoinnetworkstatus"
      via: "JSON-RPC method call"
      pattern: "openbitcoinnetworkstatus"
---

<objective>
Render inbound listener/admission evidence in `open-bitcoin status`.

Purpose: Operators need to distinguish inbound serving from outbound sync in both JSON and human status output, using the shared status contract and Open Bitcoin RPC method.
Output: Status collection, HTTP adapter, human renderer, and tests for inbound evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-05-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-06-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/http.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/status/tests.rs
@docs/architecture/status-snapshot.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collect inbound RPC status into the shared snapshot</name>
  <files>packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/http.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/status.rs
    - packages/open-bitcoin-cli/src/operator/status/http.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-rpc/src/method/node.rs
  </read_first>
  <behavior>
    - Test: fake live RPC with inbound evidence maps that evidence into `OpenBitcoinStatusSnapshot.peers.inbound`.
    - Test: fake live RPC missing `openbitcoinnetworkstatus` keeps peer counts from `getnetworkinfo` but marks inbound evidence unavailable with a reason.
    - Test: credentials, cookies, and endpoint secrets are not rendered or stored in inbound status.
  </behavior>
  <action>Extend `StatusRpcClient` and `HttpStatusRpcClient` with `get_open_bitcoin_network_status`, calling `openbitcoinnetworkstatus`. In `collect_status_snapshot`, combine existing `getnetworkinfo` counts with the new shared inbound evidence. Treat RPC method-not-found or older-daemon failures as an unavailable inbound field, not as total status failure. Preserve `getnetworkinfo.connections_in/out` mapping for baseline counts per D-16.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture</automated>
    <automated>rg -n "openbitcoinnetworkstatus|peers\\.inbound|Unavailable" packages/open-bitcoin-cli/src/operator/status.rs packages/open-bitcoin-cli/src/operator/status/http.rs packages/open-bitcoin-cli/src/operator/status/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - Live status collection uses the Open Bitcoin network status method when available.
    - Older-daemon/missing method behavior is explicit and non-fatal.
    - Peer counts continue to come from `getnetworkinfo`.
  </acceptance_criteria>
  <done>`open-bitcoin status --format json` can carry detailed inbound evidence through the shared snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render inbound serving separately from outbound sync</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/status/render.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test: human status contains labels for inbound listener state, bound endpoints, preflight reason, admitted/rejected inbound peers, handshake counts, duplicate/self/cap/reserved-slot rejects, and latest admission event per D-17.
    - Test: human status still shows existing `Peers: in=<n> out=<n>` count text.
    - Test: unavailable inbound evidence renders `Inbound serving: Unavailable: <reason>`.
  </behavior>
  <action>Create `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs` and call it from `render.rs`, extracting helper code if needed so `render.rs` stays below the file-length gate. Render compact operator labels that separate inbound serving from outbound sync, including reserved-slot/protected-admission rejects as Phase 90 admission evidence without naming Phase 91 permission classes. Do not add marketing or production readiness language. JSON rendering should remain the serde projection of `OpenBitcoinStatusSnapshot`, not a CLI-specific DTO.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture</automated>
    <automated>bash scripts/check-file-lengths.sh</automated>
  </verify>
  <acceptance_criteria>
    - Human and JSON status both expose inbound evidence.
    - Renderer uses the shared snapshot contract.
    - File-length checks pass.
  </acceptance_criteria>
  <done>Operator status clearly distinguishes inbound serving evidence from outbound sync evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Daemon RPC to CLI status | Live RPC data can be missing, stale, or from an older daemon. |
| Inbound endpoint evidence to terminal output | Status output can reveal bound endpoints or overstate capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-07-01 | Repudiation | status unavailable path | mitigate | Preserve `Unavailable: <reason>` when detailed inbound RPC status is absent. |
| T-90-07-02 | Information Disclosure | human status renderer | mitigate | Render bounded endpoint lists from shared status only; no raw peer table or credentials. |
| T-90-07-03 | Tampering | renderer-local summaries | mitigate | Keep JSON as shared snapshot serde output and human rendering as projection only. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture
```
</verification>

<success_criteria>
- INB-05 operator status evidence is visible in JSON and human modes.
- Inbound serving labels are separated from outbound sync.
- Status output does not claim public-default, relay, or production readiness behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-07-SUMMARY.md`.
</output>
