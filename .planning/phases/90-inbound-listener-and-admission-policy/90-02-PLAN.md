---
phase: 90-inbound-listener-and-admission-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 90-01
files_modified:
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  - packages/open-bitcoin-rpc/src/config/loader.rs
  - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
autonomous: true
requirements:
  - INB-01
  - INB-02
  - INB-04
requirements_addressed:
  - INB-01
  - INB-02
  - INB-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Inbound serving is disabled by default in runtime config."
    - "Operators can enable inbound serving only through Open Bitcoin JSONC or Open Bitcoin-prefixed daemon CLI flags."
    - "Baseline `bitcoin.conf` listener, bind, and whitelist keys do not silently activate Phase 90 inbound serving."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/config/open_bitcoin.rs"
      provides: "JSONC `inbound` section with disabled defaults"
    - path: "packages/open-bitcoin-rpc/src/config/loader.rs"
      provides: "Open Bitcoin-prefixed daemon CLI parsing for inbound overrides"
    - path: "packages/open-bitcoin-rpc/src/config/tests.rs"
      provides: "Config precedence and validation regressions"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs"
      to: "packages/open-bitcoin-network/src/inbound.rs"
      via: "runtime config resolution"
      pattern: "InboundListenerConfig"
---

<objective>
Add Open Bitcoin-owned inbound config and daemon CLI controls.

Purpose: Operators need explicit controls for inbound serving, and disabled config must not bind listeners or imply Knots `-listen`/`-bind` compatibility.
Output: Runtime config carries a resolved `InboundListenerConfig` from JSONC/CLI/defaults with deterministic validation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-01-SUMMARY.md
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
@packages/open-bitcoin-rpc/src/config/loader.rs
@packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
@packages/open-bitcoin-rpc/src/config/tests.rs
@docs/architecture/config-precedence.md

<interfaces>
Use the Plan 01 `InboundListenerConfig` contract. Add JSONC input fields under `inbound`:

```jsonc
{
  "inbound": {
    "enabled": false,
    "listen_addresses": ["127.0.0.1:18444"],
    "max_peers": 8,
    "reserved_slots": 0,
    "allow_public": false
  }
}
```

Daemon CLI overrides must use Open Bitcoin-owned names: `-openbitcoininbound=1`, `-openbitcoinlisten=<host:port>`, `-openbitcoinmaxinbound=<n>`, `-openbitcoinreservedslots=<n>`, and `-openbitcoinallowpublic=1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JSONC inbound config contract</name>
  <files>packages/open-bitcoin-rpc/src/config.rs, packages/open-bitcoin-rpc/src/config/open_bitcoin.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
    - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
    - packages/open-bitcoin-rpc/src/config/tests.rs
    - packages/open-bitcoin-network/src/inbound.rs
  </read_first>
  <behavior>
    - Test: default `OpenBitcoinConfig::default()` resolves inbound serving as disabled per D-01.
    - Test: JSONC `inbound.enabled = true` with loopback `listen_addresses` resolves into `RuntimeConfig.inbound` per D-02 and D-04.
    - Test: unknown inbound fields are rejected because `deny_unknown_fields` is preserved.
    - Test: `max_peers = 0` or `reserved_slots > max_peers` fails with field-specific messages per D-05.
  </behavior>
  <action>Add an `InboundConfig` sibling to `SyncConfig` in `OpenBitcoinConfig` with `#[serde(default, deny_unknown_fields)]`. Map it to the Plan 01 `InboundListenerConfig` in `open_bitcoin_runtime.rs`, preserving CLI > JSONC > defaults precedence. `RuntimeConfig` must include a resolved inbound field and default to disabled. Error messages must name exact fields such as `inbound.max_peers`, `inbound.reserved_slots`, and `inbound.listen_addresses`. Do not put Open Bitcoin-only inbound keys in `bitcoin.conf` parsing.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_jsonc_accepts_inbound_listener_contract -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - JSONC inbound config defaults disabled.
    - Loopback-enabled JSONC maps into the shared network config contract.
    - Invalid inbound values fail deterministically with field names.
  </acceptance_criteria>
  <done>Open Bitcoin JSONC can carry inbound listener settings without changing baseline config semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add daemon CLI overrides and baseline key rejection</name>
  <files>packages/open-bitcoin-rpc/src/config/loader.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-rpc/src/config/loader.rs
    - packages/open-bitcoin-rpc/src/config/tests.rs
    - docs/architecture/config-precedence.md
  </read_first>
  <behavior>
    - Test: `-openbitcoininbound=1` enables inbound serving over disabled JSONC.
    - Test: repeated `-openbitcoinlisten=<host:port>` values override JSONC listen addresses in order.
    - Test: `-openbitcoininbound=0` disables inbound serving even if JSONC enables it.
    - Test: `bitcoin.conf` keys `listen`, `bind`, `whitebind`, and `whitelist` are rejected or ignored with explicit errors and never activate Phase 90 inbound.
    - Test: CLI public endpoint without `-openbitcoinallowpublic=1` remains unsafe at preflight rather than becoming accepted config.
  </behavior>
  <action>Extend CLI parsing for Open Bitcoin-prefixed inbound flags. Reuse the existing boolean parser style used by daemon sync. Keep field validation in the runtime resolver so JSONC and CLI share error handling. Add config tests that prove CLI has precedence over JSONC and defaults, and that baseline listener/permission-looking keys do not activate inbound serving. Do not accept Knots `-listen`, `-bind`, `-whitebind`, `-whitelist`, or permission values as Phase 90 controls per D-02 and D-10.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc daemon_inbound -- --nocapture</automated>
    <automated>rg -n "openbitcoininbound|openbitcoinlisten|openbitcoinmaxinbound|openbitcoinreservedslots|openbitcoinallowpublic" packages/open-bitcoin-rpc/src/config/loader.rs packages/open-bitcoin-rpc/src/config/tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - CLI override behavior is covered by focused tests.
    - Baseline `listen`/`bind`/`whitebind`/`whitelist` do not imply Phase 90 compatibility.
    - Config still follows the documented precedence model.
  </acceptance_criteria>
  <done>Operators have explicit Open Bitcoin-prefixed daemon controls for inbound listener activation and limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI/JSONC to runtime config | User-controlled strings become runtime listener settings. |
| Baseline config to Open Bitcoin-owned behavior | Bitcoin-compatible config files must not silently activate new Open Bitcoin-only inbound behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-02-01 | Elevation of Privilege | `RuntimeConfig.inbound` | mitigate | Default to disabled and require Open Bitcoin-owned enablement controls. |
| T-90-02-02 | Tampering | inbound numeric limits | mitigate | Reject zero max peers and reserved slots greater than max peers with field-specific errors. |
| T-90-02-03 | Information Disclosure | public listener config | mitigate | Preserve `allow_public = false` default and classify unsafe endpoints before bind. |
| T-90-02-04 | Repudiation | config precedence | mitigate | Add tests proving CLI over JSONC over defaults and explicit errors for invalid fields. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound -- --nocapture
```
</verification>

<success_criteria>
- INB-01 config activation is explicit and disabled by default.
- INB-02 preflight inputs are fully typed and deterministic.
- INB-04 cap/reserved-slot fields are validated before runtime use.
- No Phase 91+ permission or relay controls are accepted.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-02-SUMMARY.md`.
</output>
