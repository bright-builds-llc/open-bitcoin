---
phase: 90-inbound-listener-and-admission-policy
plan: 03
type: execute
wave: 2
depends_on:
  - 90-01
files_modified:
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/lib.rs
autonomous: true
requirements:
  - INB-03
  - INB-04
requirements_addressed:
  - INB-03
  - INB-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "Inbound peers are typed records with handshake state, not anonymous count increments."
    - "Duplicate and self-connection rejects are visible but not counted as admitted inbound peers."
    - "Inbound caps do not reduce outbound sync target accounting."
    - "Reserved-slot/protected admission consumes only Phase 90 reserved capacity and never changes outbound targets."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "PeerManager inbound record insertion and nonce-based self-connection rejection"
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "ManagedPeerNetwork inbound admission and separate inbound/outbound count projection"
  key_links:
    - from: "packages/open-bitcoin-node/src/network.rs"
      to: "packages/open-bitcoin-network/src/inbound.rs"
      via: "admission policy"
      pattern: "InboundAdmissionPolicy"
---

<objective>
Integrate typed inbound admission with the existing peer manager and managed network counts.

Purpose: Open Bitcoin already has inbound peer roles and message-driven handshake logic. Phase 90 must extend that path without building a second peer store or starving outbound sync.
Output: Inbound peer records, duplicate/self protections, cap-aware admission, and count projection tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-01-SUMMARY.md
@packages/open-bitcoin-network/src/inbound.rs
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/network/tests.rs
@packages/bitcoin-knots/src/net_processing.cpp
@packages/bitcoin-knots/test/functional/p2p_handshake.py

<interfaces>
Existing contracts to preserve:

```rust
pub fn add_inbound_peer(&mut self, peer_id: PeerId) -> Result<(), NetworkError>;
pub fn handle_message(&mut self, peer_id: PeerId, message: WireNetworkMessage, timestamp: i64) -> Result<Vec<PeerAction>, NetworkError>;
pub struct ManagedNetworkInfo {
    pub connected_peers: usize,
    pub inbound_peers: usize,
    pub outbound_peers: usize,
}
```

Add richer APIs without removing the existing simple helper:

```rust
pub fn add_inbound_peer_record(&mut self, record: InboundPeerRecord) -> Result<(), NetworkError>;
pub fn admit_inbound_peer(&mut self, request: InboundAdmissionRequest) -> InboundAdmissionDecision;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PeerManager inbound state and handshake checks</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-network/src/inbound.rs
    - packages/open-bitcoin-network/src/message.rs
    - packages/open-bitcoin-network/src/peer/tests.rs
  </read_first>
  <behavior>
    - Test: `add_inbound_peer_record` stores an inbound peer with endpoint key and starts in a handshaking state.
    - Test: existing `add_inbound_peer` still creates a compatible inbound record for current callers.
    - Test: an inbound `version` message with remote nonce equal to `LocalPeerConfig.nonce` disconnects/rejects as self connection per D-09.
    - Test: inbound handshake emits local `version`, `wtxidrelay`, `verack`, and `sendheaders` only through existing `PeerAction::Send` flow per D-08.
  </behavior>
  <action>Extend `PeerState` with bounded inbound metadata from Plan 01, using `maybe_` names for optional endpoint/nonce fields. Add `PeerManager::add_inbound_peer_record` and keep `add_inbound_peer` as a compatibility wrapper. Update `handle_version` so self-connection nonce matches produce a stable disconnect/rejection action and do not mark the peer established. Do not add transaction relay, compact block relay, mempool propagation, permission classes, eviction, ban, or address relay behavior per D-10.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network self_connection -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Inbound peer state is typed and test-covered.
    - Existing message-driven handshake remains the only handshake engine.
    - Self-connection rejects do not become admitted established peers.
  </acceptance_criteria>
  <done>`PeerManager` can represent Phase 90 inbound peers without replacing existing peer lifecycle code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add managed admission and separate count projection</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/lib.rs</files>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-network/src/inbound.rs
    - packages/open-bitcoin-network/src/peer.rs
  </read_first>
  <behavior>
    - Test: admitted inbound peer increments `connected_peers` and `inbound_peers`.
    - Test: cap-rejected inbound peer increments rejection evidence but does not change `connected_peers` or `inbound_peers`.
    - Test: ordinary inbound admission is rejected when ordinary slots are full even if reserved slots remain.
    - Test: reserved-slot/protected admission succeeds while reserved capacity remains and then fails with the stable reserved-slot rejection reason once reserved capacity is exhausted.
    - Test: one outbound peer plus one inbound peer leaves outbound count and configured outbound target evidence separate per D-11.
    - Test: duplicate endpoint or duplicate peer id is rejected before admitted counts change.
  </behavior>
  <action>Add managed-network APIs that run the Plan 01 admission policy before inserting inbound peers into `PeerManager`. Carry the Phase 90-only ordinary/reserved slot class through admission requests so reserved/protected admission is testable without adding Knots permission classes. Extend `ManagedNetworkInfo` or a child evidence type with bounded inbound admission counters needed by status plans, including reserved-slot admits/rejects if the Plan 01 contract exposes them. Preserve `connections`, `connections_in`, and `connections_out` semantics for baseline RPC. Re-export only the node-level types required by downstream runtime/status work. Do not share inbound caps with `target_outbound_peers` or durable sync peer-attempt limits.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node network_info -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Inbound/outbound count projection is deterministic.
    - Admission rejections remain visible to later status work.
    - Outbound sync accounting is not reduced by inbound peers.
  </acceptance_criteria>
  <done>`ManagedPeerNetwork` exposes cap-aware inbound admission while preserving outbound sync safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Untrusted remote handshake to peer state | Remote `version` payloads update local peer lifecycle state. |
| Admission policy to baseline count projection | Rejected inbound peers must not be counted as active baseline connections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-03-01 | Spoofing | `PeerManager::handle_version` | mitigate | Reject remote nonce equal to local nonce and test admitted counts remain unchanged. |
| T-90-03-02 | Denial of Service | `ManagedPeerNetwork` caps | mitigate | Apply pure cap decisions before insertion and preserve outbound target accounting separately. |
| T-90-03-03 | Tampering | duplicate peer id/endpoint | mitigate | Reject duplicate peer id and endpoint keys before network info projection. |
| T-90-03-04 | Elevation of Privilege | peer message handling | mitigate | Keep relay, permissions, address relay, ban, and eviction behavior out of Phase 90 changes. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound -- --nocapture
```
</verification>

<success_criteria>
- INB-03 typed inbound peer lifecycle is implemented through existing peer manager paths.
- INB-04 cap behavior is enforced without outbound sync starvation.
- Baseline `getnetworkinfo` count semantics remain available for later RPC evidence work.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-03-SUMMARY.md`.
</output>
