---
phase: 90-inbound-listener-and-admission-policy
plan: 05
type: execute
wave: 3
depends_on:
  - 90-03
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/lib.rs
autonomous: true
requirements:
  - INB-05
requirements_addressed:
  - INB-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 90-2026-06-25T04-23-47
generated_at: 2026-06-25T05:33:28Z
must_haves:
  truths:
    - "The shared status snapshot contains inbound listener and admission evidence."
    - "Unavailable inbound evidence preserves a reason instead of disappearing."
    - "Inbound metrics are low-cardinality numeric counters only."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Shared inbound listener/admission status contract"
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Low-cardinality inbound metric kinds"
  key_links:
    - from: "packages/open-bitcoin-node/src/status.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "PeerStatus child field and re-export"
      pattern: "InboundPeerServingStatus"
---

<objective>
Add shared inbound listener and admission evidence to node status and metrics.

Purpose: Status, RPC, CLI, support, metrics, and logs need one shared contract for Phase 90 evidence. Renderer-local summaries would drift.
Output: Serializable inbound status types under `OpenBitcoinStatusSnapshot.peers` and bounded metric kinds.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-CONTEXT.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-RESEARCH.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-03-SUMMARY.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/status/tests.rs
@packages/open-bitcoin-node/src/metrics.rs
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md

<interfaces>
Add a child status contract instead of expanding renderers first:

```rust
pub struct InboundPeerServingStatus {
    pub listener_state: String,
    pub bound_endpoints: Vec<String>,
    pub preflight_reason: String,
    pub admitted_inbound_peers: u32,
    pub rejected_inbound_peers: u32,
    pub handshake: InboundHandshakeStatusCounts,
    pub duplicate_rejects: u32,
    pub self_connection_rejects: u32,
    pub cap_rejects: u32,
    pub reserved_slot_rejects: u32,
    pub latest_admission_event: FieldAvailability<InboundAdmissionEvent>,
}

pub struct PeerStatus {
    pub peer_counts: FieldAvailability<PeerCounts>,
    pub recent_peers: FieldAvailability<Vec<PeerTelemetry>>,
    pub inbound: FieldAvailability<InboundPeerServingStatus>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inbound status child contract</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-node/src/lib.rs</files>
  <read_first>
    - packages/open-bitcoin-node/src/status.rs
    - packages/open-bitcoin-node/src/status/tests.rs
    - docs/architecture/status-snapshot.md
  </read_first>
  <behavior>
    - Test: default/stopped snapshots serialize `peers.inbound.state = "unavailable"` with a reason.
    - Test: available inbound status serializes stable labels for listener state, preflight reason, handshake counts, duplicate/self/cap/reserved-slot rejects, and latest admission event per D-17.
    - Test: adding the field is serde-compatible for existing fixture constructors through `#[serde(default)]` or explicit constructor updates.
  </behavior>
  <action>Create `packages/open-bitcoin-node/src/status/inbound.rs` and child tests to keep `status.rs` below the file-length gate. Add `FieldAvailability<InboundPeerServingStatus>` to `PeerStatus` with a conservative unavailable default. Re-export inbound status types from `status.rs`/`lib.rs` for RPC and CLI consumers. Include reserved-slot/protected-admission counters from Plan 01/03 as Phase 90 admission evidence without adding Phase 91 permission classes. Keep endpoint evidence bounded as strings; do not store raw peer tables. Use stable snake_case serialized labels for machine-facing states.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_status -- --nocapture</automated>
    <automated>bash scripts/check-file-lengths.sh</automated>
  </verify>
  <acceptance_criteria>
    - Shared status has one inbound evidence contract under `peers`.
    - Missing data remains explicitly unavailable.
    - File-length checks pass after adding the child module.
  </acceptance_criteria>
  <done>`OpenBitcoinStatusSnapshot` can carry Phase 90 inbound evidence for all downstream surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add low-cardinality inbound metrics</name>
  <files>packages/open-bitcoin-node/src/metrics.rs</files>
  <read_first>
    - packages/open-bitcoin-node/src/metrics.rs
    - docs/architecture/operator-observability.md
  </read_first>
  <behavior>
    - Test: metric labels exist for admitted inbound peers, rejected inbound peers, cap rejects, reserved-slot rejects, duplicate rejects, and self-connection rejects.
    - Test: metric serialization remains low-cardinality and does not include endpoint values.
  </behavior>
  <action>Add only bounded numeric `MetricKind` variants needed for Phase 90 listener/admission evidence, such as `InboundAdmittedPeerCount`, `InboundRejectedPeerCount`, `InboundCapRejectCount`, `InboundReservedSlotRejectCount`, `InboundDuplicateRejectCount`, and `InboundSelfConnectionRejectCount`. Do not add endpoint labels, peer ids, raw messages, or high-cardinality dimensions. Update existing metric-kind tests and string label mapping.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture</automated>
    <automated>! rg -n "endpoint_label|peer_id_label|remote_addr|remote_endpoint" packages/open-bitcoin-node/src/metrics.rs</automated>
  </verify>
  <acceptance_criteria>
    - Inbound metrics are bounded numeric counters.
    - No high-cardinality endpoint or peer identifiers are introduced.
  </acceptance_criteria>
  <done>Metrics can expose Phase 90 inbound admission outcomes without leaking endpoints or growing cardinality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime admission events to status snapshot | Runtime facts become operator-visible support and automation data. |
| Peer endpoints to metrics/status | Endpoint evidence can leak local network details if copied unbounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-90-05-01 | Information Disclosure | `InboundPeerServingStatus` | mitigate | Bound endpoint arrays and keep support redaction for detailed rendering in later plans. |
| T-90-05-02 | Denial of Service | `MetricKind` labels | mitigate | Add numeric low-cardinality metrics only; no endpoint or peer labels. |
| T-90-05-03 | Repudiation | unavailable status fields | mitigate | Preserve `FieldAvailability` unavailable reasons for missing listener evidence. |
</threat_model>

<verification>
Focused verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_status -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture
```
</verification>

<success_criteria>
- INB-05 has a shared status/metrics contract.
- The contract is ready for RPC, CLI, support, and docs without renderer-local truth.
- File-length and low-cardinality constraints remain satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/90-inbound-listener-and-admission-policy/90-05-SUMMARY.md`.
</output>
