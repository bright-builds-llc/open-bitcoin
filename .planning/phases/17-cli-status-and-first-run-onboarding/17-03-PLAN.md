---
phase: 17-cli-status-and-first-run-onboarding
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/detect.rs
  - packages/open-bitcoin-cli/src/operator/detect/tests.rs
autonomous: true
requirements: [CLI-07, MIG-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-04-26T23-56-00
generated_at: 2026-04-27T00:10:29Z
user_setup: []
must_haves:
  truths:
    - "CLI support code can detect existing Core/Knots datadirs, configs, cookies, service files, and wallet candidates without mutating them."
    - "Detection output reports uncertainty and source paths instead of pretending to make migration decisions."
    - "Detection runs in tests against isolated temp roots and does not inspect real developer service state."
  artifacts:
    - path: packages/open-bitcoin-cli/src/operator/detect.rs
      provides: "Read-only Core/Knots installation detection"
      exports: ["detect_existing_installations", "DetectedInstallation"]
    - path: packages/open-bitcoin-cli/src/operator/detect/tests.rs
      provides: "Hermetic detection tests for macOS/Linux candidate paths"
      contains: "detects_wallet_config_cookie_and_service_candidates_read_only"
  key_links:
    - from: packages/open-bitcoin-cli/src/operator/detect.rs
      to: packages/bitcoin-knots/doc/files.md
      via: "documented datadir/config/cookie path conventions"
      pattern: "bitcoin.conf"
    - from: packages/open-bitcoin-cli/src/operator/detect.rs
      to: packages/bitcoin-knots/doc/init.md
      via: "documented service file candidates"
      pattern: "bitcoind.service"
---

<objective>
Implement read-only Core/Knots detection for status and onboarding.

Purpose: Satisfy CLI-07 and MIG-02 without crossing into Phase 21 migration or Phase 18 service lifecycle mutation.
Output: Detection models, pure classification helpers, filesystem collection over injected roots, and hermetic tests proving no mutation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-cli-status-and-first-run-onboarding/17-CONTEXT.md
@.planning/phases/17-cli-status-and-first-run-onboarding/17-RESEARCH.md
@packages/open-bitcoin-cli/src/operator/detect.rs
@packages/bitcoin-knots/doc/files.md
@packages/bitcoin-knots/doc/init.md
@packages/bitcoin-knots/doc/managing-wallets.md
@packages/bitcoin-knots/contrib/init/README.md
@docs/parity/source-breadcrumbs.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify Core/Knots evidence without filesystem mutation</name>
  <files>packages/open-bitcoin-cli/src/operator/detect.rs, packages/open-bitcoin-cli/src/operator/detect/tests.rs</files>
  <read_first>
    - .planning/phases/17-cli-status-and-first-run-onboarding/17-CONTEXT.md
    - packages/open-bitcoin-cli/src/operator/detect.rs
    - packages/bitcoin-knots/doc/files.md
    - packages/bitcoin-knots/doc/init.md
    - packages/bitcoin-knots/doc/managing-wallets.md
  </read_first>
  <behavior>
    - Test 1: a fake Linux home with `.bitcoin/bitcoin.conf`, `.bitcoin/.cookie`, `.bitcoin/wallets/main/wallet.dat`, and a fake user systemd unit reports all candidate paths.
    - Test 2: a fake macOS home with `Library/Application Support/Bitcoin/bitcoin.conf`, `.cookie`, wallet candidates, and LaunchAgents plist reports all candidate paths.
    - Test 3: detection output marks product identity as uncertain when evidence cannot distinguish Core from Knots.
  </behavior>
  <action>Implement pure evidence classification and read-only path collection in `operator/detect.rs`. The public function should be `detect_existing_installations(roots: &DetectionRoots) -> Vec<DetectedInstallation>` or the equivalent contract from Plan 17-01. It must inspect only injected roots, not global `/` or the developer's real `$HOME` unless the caller explicitly passes those paths. Detect datadirs, `bitcoin.conf`, `.cookie`, service files, and wallet candidates for macOS and Linux. Candidate service paths must include `~/Library/LaunchAgents/org.bitcoin.bitcoind.plist`, `/Library/LaunchDaemons/org.bitcoin.bitcoind.plist`, `/etc/systemd/system/bitcoind.service`, `/usr/lib/systemd/system/bitcoind.service`, and `~/.config/systemd/user/bitcoind.service` through injected roots. Wallet candidates must include `wallet.dat`, `wallets/*`, and chain-scoped `regtest/wallets/*`, `signet/wallets/*`, and `testnet3/wallets/*`. Return source-path strings and confidence/uncertainty notes; do not copy, move, rewrite, delete, open wallet databases for write, invoke migration RPCs, or call `launchctl`/`systemctl`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::detect::</automated>
  </verify>
  <acceptance_criteria>
    - Tests include `detects_linux_core_knots_candidates_read_only` and `detects_macos_core_knots_candidates_read_only`.
    - A fake datadir with `bitcoin.conf`, `.cookie`, `wallet.dat`, and `wallets/main/wallet.dat` produces source paths containing those exact filenames.
    - Detection result notes include either `uncertain` or a typed uncertainty enum when Core versus Knots cannot be distinguished.
    - `rg -n "fs::write|remove_file|remove_dir|rename|copy|OpenOptions::new\\(\\).*write|launchctl|systemctl" packages/open-bitcoin-cli/src/operator/detect.rs` returns no matches.
  </acceptance_criteria>
  <done>Core/Knots detection reports all Phase 17 candidate surfaces read-only with explicit uncertainty and source paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove detection is hermetic</name>
  <files>packages/open-bitcoin-cli/src/operator/detect/tests.rs</files>
  <read_first>
    - AGENTS.md
    - packages/open-bitcoin-cli/src/operator/detect/tests.rs
    - scripts/check-parity-breadcrumbs.ts
  </read_first>
  <behavior>
    - Test 1: detection tests run entirely under temp directories and do not require real Core, Knots, launchd, systemd, bitcoind, or bitcoin-cli.
    - Test 2: breadcrumb verification still passes with the detection test registration from Plan 17-01.
  </behavior>
  <action>Add hermetic temp-root tests that verify detection never changes file contents or modification timestamps for candidate files. Do not edit `docs/parity/source-breadcrumbs.json` in this plan; Plan 17-01 owns the breadcrumb entry for `operator/detect/tests.rs`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::detect::</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert candidate file contents before and after detection are byte-for-byte equal.
    - Tests do not branch on the host OS service manager being installed.
    - Breadcrumb verification passes.
    - No test copies or opens a real wallet database from the developer machine.
  </acceptance_criteria>
  <done>Detection is covered by hermetic read-only tests and required parity breadcrumbs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem -> detection report | High-value datadir, wallet, service, config, and cookie paths become displayable evidence. |
| Detection report -> onboarding/status | Detection must inform the user without becoming migration or mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | Core/Knots detection | mitigate | Detection functions are read-only, test mtime/content preservation, and acceptance scan rejects write/remove/copy/rename/system-manager calls. |
| T-17-03-02 | Information Disclosure | cookie/wallet paths | mitigate | Report cookie and wallet paths only; never read or render `.cookie` contents or wallet database contents. |
| T-17-03-03 | Spoofing | product identity | mitigate | Use explicit uncertainty/confidence fields when evidence cannot distinguish Bitcoin Core from Bitcoin Knots. |
| T-17-03-04 | Elevation of Privilege | service candidates | mitigate | Do not invoke `launchctl`, `systemctl`, or privileged service paths; service lifecycle remains Phase 18. |
</threat_model>

<verification>
Targeted checks:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::detect::`
- `bun run scripts/check-parity-breadcrumbs.ts --check`

Repo-native final expectation after the full phase:
- `bash scripts/verify.sh`
</verification>

<success_criteria>
Plan 17-03 is complete when status/onboarding can consume read-only detection results for datadir, config, cookie, service, and wallet candidates with explicit uncertainty and no mutation path.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cli-status-and-first-run-onboarding/17-03-SUMMARY.md`
</output>
