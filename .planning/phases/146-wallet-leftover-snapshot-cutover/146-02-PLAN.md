---
phase: 146-wallet-leftover-snapshot-cutover
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/context/rescan.rs
  - packages/open-bitcoin-rpc/src/context/tests/construction.rs
  - docs/parity/source-breadcrumbs.json
requirements:
  - SNAP-01
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 146-2026-09-21T20-57-36
generated_at: 2026-09-21T22:20:00.000Z
user_setup: []
must_haves:
  truths:
    - "Durable ManagedRpcContext seeds MemoryChainstateStore from wallet_scan_chainstate_snapshot, not leftover load_chainstate_snapshot_with_confirmation_migration and not hydrate_chainstate_for_open leftover-meta fallback."
    - "Missing chain_meta on a nonempty coins datadir fails closed at durable RPC open — leftover active_chain must not supply tip."
    - "Operator-facing durable named-wallet rescanblockchain fails closed when a height in [start, stop] lacks payload bytes (has_block), and never succeeds via leftover UTXO fallback."
    - "Operator-facing rescanblockchain against a datadir with coins truth plus a disagreeing leftover snapshot rebuilds wallet tip/UTXOs from coins truth."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "durable init uses wallet_scan_chainstate_snapshot"
      contains: "wallet_scan_chainstate_snapshot"
    - path: "packages/open-bitcoin-rpc/src/context/rescan.rs"
      provides: "durable named-wallet rescan has_block fail-closed gate"
      contains: "has_block"
    - path: "packages/open-bitcoin-rpc/src/context/tests/construction.rs"
      provides: "disagreeing leftover durable rescan regression"
      contains: "durable_rescan_ignores_disagreeing_leftover_snapshot"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store/coins.rs"
      via: "from_runtime_config_with_store durable_chainstate seed"
      pattern: "wallet_scan_chainstate_snapshot"
    - from: "packages/open-bitcoin-rpc/src/context/rescan.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs"
      via: "has_block gate before durable rescan_chainstate"
      pattern: "has_block\\("
    - from: "packages/open-bitcoin-rpc/src/context/rescan.rs"
      to: "packages/open-bitcoin-rpc/src/context/network.rs"
      via: "blockchain_snapshot from memory store seeded at open"
      pattern: "blockchain_snapshot"
---

# Phase 146 Plan 02: Durable RPC Rescan Leftover-Seed Cutover

<objective>
Stop durable RPC wallet rescan from seeding chain truth from leftover `"snapshot"` bytes so SNAP-01 holds on the operator `rescanblockchain` path (research Pitfall 1 / D-01; Open Questions RESOLVED).

Purpose: `WalletRescanRuntime` alone is insufficient — `ManagedRpcContext` still calls `load_chainstate_snapshot_with_confirmation_migration` at open and `rescan_wallet_range` reads that memory snapshot without a payload-present gate.
Output: coins-backed fail-closed durable init via Plan 01's `wallet_scan_chainstate_snapshot`, identical `has_block` fail-closed admission on durable named-wallet rescan, plus a construction/rescan regression test. No prune operator fields (D-12).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-RESEARCH.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-01-PLAN.md
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/context/rescan.rs
@packages/open-bitcoin-rpc/src/context/tests/construction.rs
@packages/open-bitcoin-rpc/src/dispatch/tests/storage_fixtures.rs

<interfaces>
Current durable seed in `packages/open-bitcoin-rpc/src/context/network.rs` inside `from_runtime_config_with_store` (must change):

```rust
let durable_chainstate = match effective_store.as_ref() {
    Some(store) => store.load_chainstate_snapshot_with_confirmation_migration()?,
    None => None,
};
let chainstate_store = durable_chainstate.map_or_else(
    MemoryChainstateStore::default,
    MemoryChainstateStore::from_snapshot,
);
```

Required replacement (Plan 01 helper — fail closed, no leftover meta tip) per D-01 and research Open Question 2 RESOLVED:

```rust
let durable_chainstate = match effective_store.as_ref() {
    Some(store) => store.wallet_scan_chainstate_snapshot()?,
    None => None,
};
```

Do **not** use `hydrate_chainstate_for_open` here: that path can still take tip from leftover via `load_hydrate_chain_meta` when `chain_meta` is missing, undoing Plan 01's fail-closed tip rule.

`rescan.rs` keeps calling `self.blockchain_snapshot()` for the coins-backed memory snapshot after this seed change. For `WalletState::DurableNamedRegistry`, before any successful `wallet.rescan_chainstate`, apply the same payload-present gate as Plan 01's `WalletRescanRuntime` (D-04/D-08): every `active_chain` position with `height` in `[start_height, stop_height]` inclusive must pass `store.has_block(position.block_hash)? == true`. On false, return `RpcFailure` whose message/display contains the exact substring `missing block payload` — do not call leftover load and do not skip the height. Prefer calling `store.has_block` directly on the durable Fjall store (same method Plan 01 uses); routing the entire RPC rescan through `WalletRescanRuntime` is acceptable only if it preserves existing RPC job/freshness response shape without inventing prune surfaces.

Plan 01 already shipped `wallet_scan_chainstate_snapshot`. This plan must consume that helper, not reimplement hydrate-with-leftover-fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed durable RPC from wallet_scan_chainstate_snapshot and gate durable rescan with has_block</name>
  <files>
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/context/rescan.rs
  </files>
  <read_first>
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-RESEARCH.md
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-01-PLAN.md
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/context/rescan.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
  </read_first>
  <behavior>
    - In `ManagedRpcContext::from_runtime_config_with_store`, when `effective_store` is `Some(store)`, durable chainstate comes from `store.wallet_scan_chainstate_snapshot()?` (Plan 01 helper).
    - Missing `chain_meta` / empty `active_chain` with nonempty coins propagates `Err` from that helper — durable open must not catch and fall back to leftover tip (D-01; Open Question 2 RESOLVED).
    - `network.rs` production durable-init path no longer calls `load_chainstate_snapshot_with_confirmation_migration` or `hydrate_chainstate_for_open` for that seed (schema migration remains inside store open / node hydrate as already implemented).
    - Local (non-durable) contexts still default to empty `MemoryChainstateStore` when no store is present.
    - In `rescan_wallet_range` for `WalletState::DurableNamedRegistry`, before `wallet.rescan_chainstate`, every `partial_snapshot` / snapshot `active_chain` position with `height` in `[start_height, stop_height]` inclusive must pass `store.has_block(position.block_hash)? == true`; on false, return error containing `missing block payload` and do not mark the job Complete / do not save a successful wallet rescan (D-04, D-08, D-09).
    - No new prune RPC method names, no `have_pruned` fields, no dashboard prune copy (D-12).
  </behavior>
  <action>
    1. In `packages/open-bitcoin-rpc/src/context/network.rs`, locate the durable seed block that currently calls `load_chainstate_snapshot_with_confirmation_migration` (approximately the `durable_chainstate` match on `effective_store`). Replace that call with `wallet_scan_chainstate_snapshot()` per D-01 and research Open Questions RESOLVED (Q1–Q2). Keep `MemoryChainstateStore::from_snapshot` / `default` mapping unchanged. Do not call `hydrate_chainstate_for_open` on this path — that helper can still supply leftover `active_chain` via `load_hydrate_chain_meta` when `chain_meta` is missing.

    2. Search the same file for any other production wallet/chain truth seed that calls `load_chainstate_snapshot`, `load_chainstate_snapshot_with_confirmation_migration`, or `hydrate_chainstate_for_open` to populate `ManagedPeerNetwork` / `MemoryChainstateStore` at context construction. If a second twin exists in `from_runtime_config_with_network_handle` (or similar), apply the same `wallet_scan_chainstate_snapshot` cutover there so both construction paths stay consistent. Do not change mempool recovery APIs unless they are the same leftover seed for wallet balances.

    3. In `packages/open-bitcoin-rpc/src/context/rescan.rs`, inside `rescan_wallet_range` for `WalletState::DurableNamedRegistry { store, .. }`, after computing `start_height` / `stop_height` and the `partial_snapshot` (or the full snapshot range used for the rescan) and **before** any successful `wallet.rescan_chainstate` / job Complete persistence, gate payload presence equivalently to Plan 01:
       ```rust
       for position in snapshot.active_chain.iter().filter(|position| {
           position.height >= start_height && position.height <= stop_height
       }) {
           if !store.has_block(position.block_hash).map_err(/* map StorageError to RpcFailure */)? {
               return Err(/* RpcFailure whose Display/message contains "missing block payload"
                            and height/hash detail; map UnavailableNamespace::BlockIndex or
                            equivalent existing unavailable mapping — do not invent Pruned */);
           }
       }
       ```
       Use the snapshot that defines the heights being rescanned (`snapshot` or `partial_snapshot` — whichever contains the `active_chain` positions for `[start_height, stop_height]`). Prefer failing before mutating wallet balances when any height in range lacks payload. Local (non-durable) wallet path does not need Fjall `has_block` (no durable store); durable named-wallet path must not succeed when a chunk height lacks payload bytes (D-04/D-08).

       Alternatively, route durable named-wallet rescan through `WalletRescanRuntime` so the Plan 01 gate runs automatically — only if RPC response fields (`WalletRescanExecution` / freshness) remain operator-compatible and no prune surfaces are added.

    4. Do not delete leftover blobs (D-05). Do not restore `save_chainstate_snapshot` in node `persist_progress` (D-06). Do not add prune operator surfaces (D-11/D-12). Do not relabel Unavailable as Pruned (D-09). Presence gate only — no block-body history scan (Open Question 3 RESOLVED).

    5. Keep `open-bitcoin-wallet` I/O-free — no Fjall imports in the wallet crate (D-02).
  </action>
  <verify>
    <automated>rg -n "wallet_scan_chainstate_snapshot|hydrate_chainstate_for_open|load_chainstate_snapshot_with_confirmation_migration" packages/open-bitcoin-rpc/src/context/network.rs; rg -n "has_block|missing block payload" packages/open-bitcoin-rpc/src/context/rescan.rs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "wallet_scan_chainstate_snapshot" packages/open-bitcoin-rpc/src/context/network.rs` prints at least one match inside `from_runtime_config_with_store` (or the durable construction twin that seeds `MemoryChainstateStore`)
    - `rg -n "hydrate_chainstate_for_open" packages/open-bitcoin-rpc/src/context/network.rs` prints no matches in the durable `MemoryChainstateStore` seed block
    - `rg -n "load_chainstate_snapshot_with_confirmation_migration" packages/open-bitcoin-rpc/src/context/network.rs` prints no matches in the durable `MemoryChainstateStore` seed block; if the symbol remains elsewhere in the file, each remaining match must not assign into `durable_chainstate` / `MemoryChainstateStore::from_snapshot`
    - `rg -n "has_block\\(" packages/open-bitcoin-rpc/src/context/rescan.rs` prints at least one match on the durable named-wallet rescan path (or `WalletRescanRuntime` is invoked from that path and Plan 01's gate applies)
    - `rg -n "missing block payload" packages/open-bitcoin-rpc/src/context/rescan.rs` prints at least one match (unless durable rescan is fully delegated to `WalletRescanRuntime`, in which case `rg -n "WalletRescanRuntime" packages/open-bitcoin-rpc/src/context/rescan.rs` must print at least one match and Plan 01's gate remains the authority)
    - `rg -n "have_pruned|pruneheight|NODE_NETWORK_LIMITED|block_status_pruned" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/context/rescan.rs` prints no matches
  </acceptance_criteria>
  <done>Durable RPC context construction seeds chainstate from wallet_scan_chainstate_snapshot (fail closed, no leftover tip), and durable named-wallet rescanblockchain cannot succeed when a height in range lacks payload bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression — durable rescanblockchain ignores disagreeing leftover</name>
  <files>
    packages/open-bitcoin-rpc/src/context/tests/construction.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    packages/open-bitcoin-rpc/src/context/tests/construction.rs
    packages/open-bitcoin-rpc/src/dispatch/tests/storage_fixtures.rs
    packages/open-bitcoin-rpc/src/dispatch/tests/wallet_methods.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/context/rescan.rs
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
  </read_first>
  <behavior>
    - Test name exactly: `durable_rescan_ignores_disagreeing_leftover_snapshot`.
    - Arrange: open a temp Fjall datadir; create a durable named wallet with a ranged receive descriptor; plant `coins_truth` via `seed_coins_from_snapshot` with tip height >= 1 and at least one wallet-matching UTXO of exactly `25_000` sats; save block payloads for every `active_chain` hash; overwrite leftover with tip height `99` and a single UTXO valued `999_999` sats that does not match the wallet script; drop the store.
    - Act: construct `ManagedRpcContext::from_runtime_config` with `maybe_data_dir: Some(path)`, set request wallet name, call `rescan_wallet_range(Some(0), None)` (or dispatch `RescanBlockchain` with start 0).
    - Assert: wallet tip height equals coins_truth tip height (not 99); wallet UTXO values sum to `25_000` (not `999_999`); `FjallNodeStore::open(&path)?.load_chainstate_snapshot()?.is_some()` so leftover remains (D-05).
    - Optional but preferred in the same file or a sibling: `durable_rescan_missing_block_payload_fails_closed` plants coins + meta for heights covering the rescan range with one height missing `save_block`, then asserts `rescan_wallet_range` returns `Err` whose display contains `missing block payload` (D-04/D-08).
  </behavior>
  <action>
    1. Add the test to `packages/open-bitcoin-rpc/src/context/tests/construction.rs` (preferred) or a new sibling module already wired by `context/tests.rs`. Reuse temp-dir helpers from existing construction tests / `storage_fixtures` patterns. Prefer `context.rescan_wallet_range` if that API is reachable on `ManagedRpcContext` without full dispatch; otherwise use the same `dispatch(... MethodCall::RescanBlockchain ...)` pattern as `wallet_methods.rs`.

    2. Plant coins with `seed_coins_from_snapshot` so `chain_meta` exists and leftover meta fallback is never needed. Save payloads with `save_block` for each coins_truth hash so Plan 01 / Task 1 payload rules remain coherent.

    3. Poison leftover with `save_chainstate_snapshot` only — do not delete it afterward (D-05).

    4. If adding the missing-payload RPC test, omit exactly one in-range payload, call rescan, and assert fail-closed with `missing block payload` — do not skip heights and do not fall back to leftover UTXOs.

    5. Register any new first-party Rust test path in `docs/parity/source-breadcrumbs.json` under the existing RPC context/tests group; use existing Knots anchors or explicit `none` only if no defensible anchor exists (D-13). Do not add Phase 151 prune roots.

    6. Do not add prune RPC fields, CLI flags, or dashboard prune labels (D-12).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase146-02-rpc-durable-rescan -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib durable_rescan_ignores_disagreeing_leftover_snapshot -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn durable_rescan_ignores_disagreeing_leftover_snapshot" packages/open-bitcoin-rpc/src/context/tests/construction.rs` prints exactly one match (or the sibling file where the test was added — then that path must be listed in this plan's SUMMARY)
    - `rg -n "999_999|25_000|seed_coins_from_snapshot|save_chainstate_snapshot" packages/open-bitcoin-rpc/src/context/tests/construction.rs` prints matches covering poison leftover and coins truth
    - `rg -n "have_pruned|pruneheight|NODE_NETWORK_LIMITED" packages/open-bitcoin-rpc/src/context/tests/construction.rs` prints no matches
    - The cargo test command in `<verify>` exits 0
  </acceptance_criteria>
  <done>Durable rescanblockchain rebuilds wallet state from coins truth while leftover snapshot bytes remain non-authoritative; missing payloads cannot yield a successful durable RPC rescan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable datadir leftover snapshot → RPC MemoryChainstateStore | Stale blob must not seed operator rescan tip/UTXOs |
| Missing chain_meta with nonempty coins → durable open | Must fail closed; leftover active_chain must not become tip |
| Operator RPC rescanblockchain → wallet balances | Must reflect coins-backed chain truth only; missing payload fails closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-06 | Tampering | `from_runtime_config_with_store` durable seed | mitigate | Replace leftover load with `wallet_scan_chainstate_snapshot` (not `hydrate_chainstate_for_open`) |
| T-146-06b | Tampering | leftover `chain_meta` / `active_chain` tip fallback | mitigate | Plan 01 helper fails closed when meta missing or tip disagrees with coins B; durable open propagates Err |
| T-146-07 | Tampering | `rescan_wallet_range` via `blockchain_snapshot` | mitigate | Regression asserts tip/UTXOs match coins, not leftover 999_999 / height 99 |
| T-146-07b | Tampering | durable named-wallet missing payload skip | mitigate | `has_block` gate (or WalletRescanRuntime) before successful `rescan_chainstate`; message contains `missing block payload` |
| T-146-08 | Spoofing | prune-looking operator fields | mitigate | Explicit non-introduction of `have_pruned` / pruneheight / NODE_NETWORK_LIMITED on this path (D-12) |
| T-146-09 | Information disclosure | silent leftover authority | mitigate | Keep leftover on disk but unused; test asserts blob still present |
</threat_model>

<verification>
Focused cargo test named in Task 2. Full `bash scripts/verify.sh` remains Plan 03.
</verification>

<success_criteria>
- Durable RPC init uses `wallet_scan_chainstate_snapshot` (not leftover load, not hydrate leftover-meta fallback)
- Durable named-wallet `rescanblockchain` fails closed on missing payload bytes
- `durable_rescan_ignores_disagreeing_leftover_snapshot` passes
</success_criteria>

<output>
After completion, create `.planning/phases/146-wallet-leftover-snapshot-cutover/146-02-SUMMARY.md`
</output>
