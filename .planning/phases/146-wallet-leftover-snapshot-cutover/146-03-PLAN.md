---
phase: 146-wallet-leftover-snapshot-cutover
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - docs/parity/source-breadcrumbs.json
  - packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
requirements:
  - SNAP-01
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 146-2026-09-21T20-57-36
generated_at: 2026-09-21T21:55:00.000Z
user_setup: []
must_haves:
  truths:
    - "Touched Phase 146 first-party Rust paths have parity breadcrumbs."
    - "Cutover does not invent have-pruned, Pruned production emission, or prune operator surfaces."
    - "persist_progress still does not write leftover snapshot bytes."
  artifacts:
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "breadcrumb coverage for Phase 146 touched Rust paths"
      contains: "wallet_rescan"
    - path: "packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs"
      provides: "no-prune / leftover-retained source assertions"
      contains: "phase146_cutover_does_not_invent_have_pruned"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs"
      to: "packages/open-bitcoin-node/src/sync/wallet_rescan.rs"
      via: "source-string assertions for banned prune symbols"
      pattern: "have_pruned"
    - from: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store.rs"
      via: "persist_progress still omits save_chainstate_snapshot"
      pattern: "save_chainstate_snapshot"
---

# Phase 146 Plan 03: Parity Breadcrumbs and No-Prune Guardrails

<objective>
Lock SNAP-01 closeout evidence: parity breadcrumbs for touched files, explicit non-introduction of prune/have-pruned surfaces, and confirmation that leftover writes stay cut (D-06, D-10–D-14).

Purpose: Success criterion 3 requires contributors to observe that cutover alone did not delete payloads or invent have-pruned. Phase 151 owns full prune parity roots — this plan only covers touched-file breadcrumbs and negative assertions.
Output: breadcrumb completeness, guardrail tests, and verify-oriented evidence. No prune product behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-01-SUMMARY.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-02-SUMMARY.md
@docs/parity/source-breadcrumbs.json
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs

<interfaces>
Existing guardrail pattern from Phase 143 inventory tests (`packages/open-bitcoin-node/src/network/tests/block_serving.rs`):

```rust
let injects_pruned = inventory_source.contains("BlockServingDataAvailability::Pruned");
assert!(
    !injects_pruned,
    "production inventory assembly must not inject BlockServingDataAvailability::Pruned"
);
```

Existing leftover-write guard from `packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs`:

```rust
fn persist_progress_source_does_not_write_leftover_snapshot() {
    // ...
    assert!(!runtime_state.contains("save_chainstate_snapshot"));
}
```

Reuse that pattern: read source files as strings and assert absences. Do not invent a Phase 151 no-claim checker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete parity breadcrumbs for Phase 146 touched Rust paths</name>
  <files>
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
    docs/parity/source-breadcrumbs.json
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/context/tests/construction.rs
  </read_first>
  <action>
    1. Ensure every first-party Rust path created or modified in Plans 01–02 under `packages/open-bitcoin-*/src` or `packages/open-bitcoin-*/tests` appears in `docs/parity/source-breadcrumbs.json` (D-13). Minimum checklist:
       - `packages/open-bitcoin-node/src/sync/wallet_rescan.rs` → `node-sync-runtime` (already listed; keep)
       - `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` → existing durable-coins / fjall coins group (keep)
       - `packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` → `node-sync-tests` (add if missing)
       - `packages/open-bitcoin-rpc/src/context/network.rs` → existing RPC context group (keep/add)
       - Any new RPC test file from Plan 02 → matching RPC tests group

    2. Do not create a new v2.4 prune parity surface in `docs/parity/index.json` (Phase 151 / GRD-01). Do not flip SNAP-01 to Complete in `.planning/REQUIREMENTS.md` here — leave Pending until phase VERIFICATION.

    3. Breadcrumb comments at the top of touched Rust files must match their group anchors. If no defensible Knots wallet-rescan-vs-leftover anchor exists beyond existing sync/wallet anchors, keep the existing group breadcrumbs; use explicit `none` only for a brand-new file that truly has no Knots anchor.

    4. Run `bun scripts/check-parity-breadcrumbs.ts` (or the repo's equivalent invoked by verify) after edits and fix any missing-path failures for Phase 146 files only.
  </action>
  <verify>
    <automated>bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun scripts/check-parity-breadcrumbs.ts` exits 0
    - `rg -n "packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs" docs/parity/source-breadcrumbs.json` prints at least one match
    - `rg -n "packages/open-bitcoin-node/src/sync/wallet_rescan.rs" docs/parity/source-breadcrumbs.json` prints at least one match
    - `rg -n "v2-4-prune|GRD-01|have_pruned" docs/parity/index.json` is not newly introduced by this plan as a done surface (no new `v2-4` surface object required; `rg -n '"name": "v2-4' docs/parity/index.json` may still print nothing)
  </acceptance_criteria>
  <done>All Phase 146 touched first-party Rust paths are covered by parity breadcrumbs without inventing Phase 151 prune roots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guardrail tests — no have-pruned invention; persist_progress still skips leftover writes</name>
  <files>
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
  </files>
  <read_first>
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
    packages/open-bitcoin-node/src/network/tests/block_serving.rs
  </read_first>
  <behavior>
    - Test `phase146_cutover_does_not_invent_have_pruned`: read `packages/open-bitcoin-node/src/sync/wallet_rescan.rs` and `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` source via `include_str!` or `std::fs::read_to_string` from `CARGO_MANIFEST_DIR`; assert the combined text does not contain `have_pruned`, does not contain `block_status_pruned`, and does not contain `NODE_NETWORK_LIMITED`; assert `wallet_rescan.rs` contains `wallet_scan_chainstate_snapshot` or `has_block` (cutover landed); assert `wallet_rescan.rs` does not contain `load_chainstate_snapshot`.
    - Test `phase146_persist_progress_still_skips_leftover_snapshot_writes`: read `packages/open-bitcoin-node/src/sync/runtime_state.rs` source and assert it does not contain `save_chainstate_snapshot` (D-06). Do not modify `persist_progress` unless the assertion unexpectedly fails because someone reintroduced the call — in that case delete the reintroduced call only (do not invent new leftover writes).
  </behavior>
  <action>
    1. Add both tests to `packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` (or a tiny sibling under `sync/tests/` already wired by `sync/tests.rs`). Follow Arrange/Act/Assert. Use path joins from `env!("CARGO_MANIFEST_DIR")` to reach `src/sync/wallet_rescan.rs`, `src/storage/fjall_store/coins.rs`, and `src/sync/runtime_state.rs`.

    2. Confirm `runtime_state.rs` still has no `save_chainstate_snapshot` token (D-06). Do not restore leftover writes. Do not delete leftover blobs in any production path (D-05).

    3. Do not add prune RPC/CLI/dashboard fields anywhere in this plan (D-12). Do not implement Phase 147–151 behaviors (D-11).

    4. After tests pass, run the focused suite:
       `bun run scripts/command-timings.ts run --key phase146-03-guardrails -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib sync::tests::wallet_rescan_runtime -- --nocapture`
       and
       `bun run scripts/command-timings.ts run --key phase146-03-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib durable_rescan_ignores_disagreeing_leftover_snapshot -- --nocapture`

    5. Verification contract reminder (D-14): pre-commit / release still use `bash scripts/verify.sh`. Executors should run verify if hooks will run on commit; do not weaken public-network-free defaults. Historical `.planning/phases/` directories stay tracked — do not delete them.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase146-03-guardrails -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase146_cutover_does_not_invent_have_pruned phase146_persist_progress_still_skips_leftover_snapshot_writes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn phase146_cutover_does_not_invent_have_pruned" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints exactly one match
    - `rg -n "fn phase146_persist_progress_still_skips_leftover_snapshot_writes" packages/open-bitcoin-node/src/sync/tests` prints exactly one match
    - `rg -n "save_chainstate_snapshot" packages/open-bitcoin-node/src/sync/runtime_state.rs` prints no matches
    - `rg -n "have_pruned|NODE_NETWORK_LIMITED|block_status_pruned" packages/open-bitcoin-node/src/sync/wallet_rescan.rs` prints no matches
    - The cargo test command in `<verify>` exits 0
    - `rg -n "load_chainstate_snapshot" packages/open-bitcoin-node/src/sync/wallet_rescan.rs` prints no matches
    - `rg -n "wallet_scan_chainstate_snapshot" packages/open-bitcoin-rpc/src/context/network.rs` prints at least one match
    - `rg -n "has_block\\(|WalletRescanRuntime" packages/open-bitcoin-rpc/src/context/rescan.rs` prints at least one match on the durable rescan path
  </acceptance_criteria>
  <done>Contributors can observe that Phase 146 cutover did not invent have-pruned, did not restore leftover snapshot writes, and kept wallet/RPC paths on coins-backed authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning/docs claims → production symbols | Docs must not claim prune/have-pruned that code did not earn |
| Source-string guardrails → future regressions | Catch accidental reintroduction of leftover wallet authority or prune labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-10 | Spoofing | `have_pruned` / `Pruned` / `NODE_NETWORK_LIMITED` | mitigate | Source-string tests forbid those tokens on wallet cutover files (D-10) |
| T-146-11 | Tampering | `persist_progress` leftover write resurrection | mitigate | Assert `runtime_state.rs` lacks `save_chainstate_snapshot` (D-06) |
| T-146-12 | Repudiation | missing parity breadcrumbs | mitigate | `check-parity-breadcrumbs.ts` must pass for touched paths (D-13) |
| T-146-13 | Elevation | premature prune operator surfaces | accept | Deferred to Phase 150; negative greps only in this phase (D-12) |
</threat_model>

<verification>
Focused cargo tests above plus `bun scripts/check-parity-breadcrumbs.ts`. Repo-native contract: `bash scripts/verify.sh` (D-14).
</verification>

<success_criteria>
- Breadcrumb checker passes for Phase 146 paths
- Guardrail tests prove no have-pruned invention and no leftover write restoration
- Wallet and RPC cutovers from Plans 01–02 remain intact under grep acceptance criteria
</success_criteria>

<output>
After completion, create `.planning/phases/146-wallet-leftover-snapshot-cutover/146-03-SUMMARY.md`
</output>
