---
phase: 146-wallet-leftover-snapshot-cutover
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
  - packages/open-bitcoin-node/src/sync/wallet_rescan.rs
  - packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
  - docs/parity/source-breadcrumbs.json
requirements:
  - SNAP-01
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 146-2026-09-21T20-57-36
generated_at: 2026-09-21T21:55:00.000Z
user_setup: []
must_haves:
  truths:
    - "WalletRescanRuntime builds scan input from durable coins and chain_meta, never from StorageNamespace::Chainstate key snapshot."
    - "A chunk whose height lacks payload bytes fails closed without leftover fallback."
    - "Same-datadir reopen with a disagreeing leftover snapshot leaves wallet tip and UTXOs matching coins truth, while the leftover key remains on disk."
  artifacts:
    - path: "packages/open-bitcoin-node/src/storage/fjall_store/coins.rs"
      provides: "wallet_scan_chainstate_snapshot that never calls load_chainstate_snapshot"
      contains: "wallet_scan_chainstate_snapshot"
    - path: "packages/open-bitcoin-node/src/sync/wallet_rescan.rs"
      provides: "coins-backed required snapshot plus has_block chunk gate"
      contains: "has_block"
    - path: "packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs"
      provides: "disagreeing leftover reopen and missing-payload fail-closed coverage"
      contains: "disagreeing_leftover_snapshot_does_not_change_wallet_balances"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/wallet_rescan.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store/coins.rs"
      via: "required_chainstate_snapshot -> wallet_scan_chainstate_snapshot"
      pattern: "wallet_scan_chainstate_snapshot"
    - from: "packages/open-bitcoin-node/src/sync/wallet_rescan.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs"
      via: "has_block gate before rescan_chainstate"
      pattern: "has_block\\("
---

# Phase 146 Plan 01: WalletRescanRuntime Coins-Backed Cutover

<objective>
Cut `WalletRescanRuntime` off leftover `"snapshot"` bytes so wallet rescan authority is durable coins + `chain_meta` + payload-present blocks (SNAP-01 / D-01–D-10).

Purpose: Leftover snapshot is the last wallet-path consumer that can resurrect snapshot UTXOs before Phase 148 unlink. Closing this seam is the phase goal.
Output: `wallet_scan_chainstate_snapshot`, updated `WalletRescanRuntime`, and reopen/missing-payload tests. Wallet crate stays I/O-free.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
@.planning/phases/146-wallet-leftover-snapshot-cutover/146-RESEARCH.md
@packages/open-bitcoin-node/src/sync/wallet_rescan.rs
@packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
@packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
@packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
@packages/open-bitcoin-wallet/src/wallet/scan.rs

<interfaces>
From `packages/open-bitcoin-node/src/sync/wallet_rescan.rs` (current anti-pattern to replace):

```rust
fn required_chainstate_snapshot(
    &self,
) -> Result<open_bitcoin_core::chainstate::ChainstateSnapshot, WalletRegistryError> {
    self.store.load_chainstate_snapshot()?.ok_or({
        WalletRegistryError::Storage(StorageError::UnavailableNamespace {
            namespace: StorageNamespace::Chainstate,
        })
    })
}
```

From `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` (reuse, do not call leftover load on wallet path):

```rust
pub fn hydrate_chainstate_for_open(&self) -> Result<Option<ChainstateSnapshot>, StorageError>;
pub fn seed_coins_from_snapshot(&self, snapshot: &ChainstateSnapshot) -> Result<(), StorageError>;
pub(crate) fn load_chain_meta_for_open(&self) -> Result<HydratedChainMeta, StorageError>;
fn scan_coin_records(&self) -> Result<HashMap<OutPoint, Coin>, StorageError>;
fn load_all_undo_records(&self) -> Result<HashMap<BlockHash, BlockUndo>, StorageError>;
```

From `packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs`:

```rust
pub fn has_block(&self, block_hash: BlockHash) -> Result<bool, StorageError>;
```

From `packages/open-bitcoin-wallet/src/wallet/scan.rs` (keep signature; shell assembles input — D-02/D-07 discretion):

```rust
pub fn rescan_chainstate(&mut self, snapshot: &ChainstateSnapshot) -> Result<(), WalletError>;
```

Discretion locked in this plan: keep feeding assembled `ChainstateSnapshot` into `rescan_chainstate`; do not invent a narrower wallet scan-input type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wallet_scan_chainstate_snapshot and cut WalletRescanRuntime off leftover load</name>
  <files>
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-RESEARCH.md
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
    packages/open-bitcoin-wallet/src/wallet/scan.rs
  </read_first>
  <behavior>
    - `FjallNodeStore::wallet_scan_chainstate_snapshot()` returns `Ok(Some(snapshot))` whose `utxos` equal `scan_coin_records()` and whose `active_chain` comes from `load_chain_meta_for_open`, never from leftover `"snapshot"`.
    - When coins keyspace is empty, returns `Ok(None)`.
    - When coins are nonempty but `load_chain_meta_for_open` yields an empty `active_chain`, returns `Err` (fail closed — no leftover meta fallback) per research Open Question 2 / D-01.
    - When coins best-block `B` is `Some(hash)` and `active_chain.last().block_hash != hash`, returns `Err` (fail closed).
    - `WalletRescanRuntime::required_chainstate_snapshot` calls `wallet_scan_chainstate_snapshot` only; the function body of `wallet_rescan.rs` must contain zero occurrences of `load_chainstate_snapshot`.
    - Before `wallet.rescan_chainstate` in `advance_wallet_rescan`, every `active_chain` position with `height` in `[job.next_height, chunk_end_height]` inclusive must pass `store.has_block(position.block_hash)? == true`; on false, call `job.mark_failed` with a message containing the exact substring `missing block payload`, persist the job, and return `Err` — do not call leftover load and do not skip the height (D-04, D-08, D-09).
  </behavior>
  <action>
    1. In `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs`, add `pub fn wallet_scan_chainstate_snapshot(&self) -> Result<Option<ChainstateSnapshot>, StorageError>` near `hydrate_chainstate_for_open` (per D-01, D-02, D-07). Implementation must:
       - Return `Ok(None)` when `coins_keyspace_is_empty()?` is true.
       - Build UTXOs via existing `scan_coin_records()?` and undo via existing `load_all_undo_records()?`.
       - Load meta via existing `load_chain_meta_for_open()?` only — never call `load_chainstate_snapshot`, `load_chainstate_snapshot_with_confirmation_migration`, or `load_hydrate_chain_meta` (those can fall back to leftover `active_chain`).
       - If `active_chain` is empty after meta load, return `Err(StorageError::UnavailableNamespace { namespace: StorageNamespace::Chainstate })` or an existing corruption/unavailable variant already used in this file — prefer `UnavailableNamespace { namespace: StorageNamespace::Chainstate }`.
       - Compare tip to coins best-block: `let view = FjallCoinsView::from_store(self);` then `view.best_block().map_err(map_heads_error)?`. If `Some(best)` and `active_chain.last().map(|p| p.block_hash) != Some(best)`, return the same unavailable/corruption error.
       - Construct `ChainstateSnapshot::new(active_chain, utxos, undo_by_block)` and set `maybe_confirmed_txid_counts` from meta.
       - Do not delete the leftover `"snapshot"` key (D-05). Do not call `save_chainstate_snapshot` (D-06).

    2. In `packages/open-bitcoin-node/src/sync/wallet_rescan.rs`, replace `required_chainstate_snapshot` body so it uses `self.store.wallet_scan_chainstate_snapshot()?` with the same `UnavailableNamespace::Chainstate` mapping as today when `None`. Delete any `load_chainstate_snapshot` call from this file.

    3. In `advance_wallet_rescan`, after computing `chunk_end_height` and before `partial_chainstate_snapshot` / `rescan_chainstate`, gate payload presence:
       ```rust
       for position in chainstate.active_chain.iter().filter(|position| {
           position.height >= job.next_height && position.height <= chunk_end_height
       }) {
           if !self.store.has_block(position.block_hash)? {
               job.mark_failed(format!(
                   "missing block payload at height {} hash {}",
                   position.height, position.block_hash
               ));
               registry.save_rescan_job(&self.store, job.clone(), self.persist_mode)?;
               return Err(WalletRegistryError::Storage(StorageError::UnavailableNamespace {
                   namespace: StorageNamespace::BlockIndex,
               }));
           }
       }
       ```
       Keep job protocol (`chunk_end_height`, `mark_chunk_progress`, resume) unchanged (D-03). Do not emit `Pruned`, `have_pruned`, or `block_status_pruned` (D-09, D-10). Do not add prune RPC/CLI/dashboard fields (D-12).

    4. If `coins.rs` or `wallet_rescan.rs` already have parity breadcrumb comments, keep them. Ensure any newly created first-party Rust path under `packages/open-bitcoin-*/src` is listed in `docs/parity/source-breadcrumbs.json` under an existing group (`node-sync-runtime` for `wallet_rescan.rs`; the coins adapter group that already owns `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` for the coins method). Do not invent Phase 151 prune parity roots (D-13).

    5. Do not modify `packages/open-bitcoin-wallet/**` except if a compile error proves otherwise — wallet must stay I/O-free and must not import Fjall (D-02).
  </action>
  <verify>
    <automated>rg -n "load_chainstate_snapshot" packages/open-bitcoin-node/src/sync/wallet_rescan.rs; rg -n "wallet_scan_chainstate_snapshot|has_block|missing block payload" packages/open-bitcoin-node/src/sync/wallet_rescan.rs packages/open-bitcoin-node/src/storage/fjall_store/coins.rs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "load_chainstate_snapshot" packages/open-bitcoin-node/src/sync/wallet_rescan.rs` prints no matches
    - `rg -n "fn wallet_scan_chainstate_snapshot" packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` prints exactly one match
    - `rg -n "load_chainstate_snapshot" packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` still matches only schema-migration / leftover helpers, and the body of `wallet_scan_chainstate_snapshot` (function text between its `fn` line and the next `pub fn`/`fn` at the same indent) contains zero `load_chainstate_snapshot` tokens
    - `rg -n "has_block\\(" packages/open-bitcoin-node/src/sync/wallet_rescan.rs` prints at least one match
    - `rg -n "missing block payload" packages/open-bitcoin-node/src/sync/wallet_rescan.rs` prints at least one match
    - `rg -n "have_pruned|block_status_pruned" packages/open-bitcoin-node/src/sync/wallet_rescan.rs packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` prints no matches
    - `rg -n "fjall|FjallNodeStore|StorageNamespace::Chainstate" packages/open-bitcoin-wallet/src/wallet/scan.rs` prints no matches
  </acceptance_criteria>
  <done>WalletRescanRuntime scan authority is coins + chain_meta with a has_block fail-closed chunk gate and no leftover load on that path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove disagreeing leftover reopen and missing-payload fail-closed</name>
  <files>
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/146-wallet-leftover-snapshot-cutover/146-CONTEXT.md
    packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
    packages/open-bitcoin-node/src/sync/wallet_rescan.rs
    packages/open-bitcoin-node/src/sync/tests/support_blocks.rs
  </read_first>
  <behavior>
    - Test `disagreeing_leftover_snapshot_does_not_change_wallet_balances`: plant coins_truth (tip height 3, two wallet UTXOs totaling 60_000 sats) via `seed_coins_from_snapshot`; save a block payload for every `active_chain` hash via `save_block`; overwrite leftover with a snapshot whose tip height is 99 and whose single UTXO value is 999_999 sats; reopen store; run `WalletRescanRuntime::enqueue_rescan`; assert wallet `maybe_tip_height == Some(3)`, `utxos.len() == 2`, total value 60_000; assert `load_chainstate_snapshot()?.is_some()`; assert every coins_truth hash still `has_block == true`.
    - Test `missing_block_payload_fails_chunk_closed`: plant coins + chain_meta for heights 0..=2 with only height 0 and 1 payloads saved; enqueue with chunk_size covering height 2; assert rescan returns `Err`, job state is failed (or freshness not Fresh/Complete), and job error/display contains `missing block payload`; assert leftover load is not required for the failure path.
    - Update `restart_resume_advances_pending_rescan_in_bounded_chunks` so it plants coins via `seed_coins_from_snapshot(&funded_chainstate(...))` (or save leftover then seed, then still save_block for every active_chain hash). After cutover, leftover-only without payloads must not be the sole authority.
  </behavior>
  <action>
    1. Extend `packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` with helpers that:
       - Build `coins_truth` using the existing `funded_chainstate` / `tip` helpers (keep 25_000 + 35_000 = 60_000 sats and tip height 3).
       - For each `ChainPosition` in `coins_truth.active_chain`, construct a minimal `Block` whose header matches that position's header (so `save_block` stores under `position.block_hash`) and call `store.save_block(&block, PersistMode::Sync)`.
       - Call `store.seed_coins_from_snapshot(&coins_truth)` so `C`/`B`/`chain_meta` exist without treating leftover as live truth.
       - Build `poison_leftover` as a `ChainstateSnapshot` with `active_chain: vec![tip(99)]` and one UTXO of `999_999` sats at a distinct outpoint, then `store.save_chainstate_snapshot(&poison_leftover, PersistMode::Sync)` (D-05: leave blob on disk; do not delete).

    2. Add `#[test] fn disagreeing_leftover_snapshot_does_not_change_wallet_balances` implementing the Arrange/Act/Assert in `<behavior>` (D-03, D-05, D-10). After reopen, create `WalletRescanRuntime::open_with_chunk_size(..., 2)`, `enqueue_rescan("alpha")`, then assert wallet balances/tip from coins_truth. Assert `store.load_chainstate_snapshot().expect("load").is_some()`. Assert no production path introduced `have_pruned` by keeping assertions free of that symbol and by asserting payloads remain via `has_block`.

    3. Add `#[test] fn missing_block_payload_fails_chunk_closed` per `<behavior>` (D-04, D-08, D-09). Do not skip heights; do not fall back to leftover UTXOs.

    4. Update `restart_resume_advances_pending_rescan_in_bounded_chunks` so every height in the funded chain has a saved block payload before enqueue/resume. Prefer `seed_coins_from_snapshot` as the coins plant; leftover save is optional and must not be the sole UTXO source.

    5. Add the test file path to `docs/parity/source-breadcrumbs.json` under `node-sync-tests` (or the group that already lists `packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` if present). If the file is already listed, leave the group breadcrumbs unchanged. Use existing Knots anchors; do not invent prune parity roots (D-13).

    6. Out of scope: deleting leftover blobs, prune policy, Fjall unlink, `NODE_NETWORK_LIMITED`, prune operator surfaces (D-11 deferred).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase146-01-wallet-rescan -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib sync::tests::wallet_rescan_runtime -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn disagreeing_leftover_snapshot_does_not_change_wallet_balances" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints exactly one match
    - `rg -n "fn missing_block_payload_fails_chunk_closed" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints exactly one match
    - `rg -n "999_999|tip\\(99\\)|save_block" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints matches for the poison leftover and payload plants
    - `rg -n "missing block payload" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints at least one match
    - `rg -n "have_pruned|NODE_NETWORK_LIMITED|block_status_pruned" packages/open-bitcoin-node/src/sync/tests/wallet_rescan_runtime.rs` prints no matches
    - The cargo test command in `<verify>` exits 0
  </acceptance_criteria>
  <done>Reopen with disagreeing leftover preserves coins-backed wallet tip/UTXOs; missing payloads fail the chunk closed; leftover blob and payloads remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fjall leftover `"snapshot"` → wallet rescan | Untrusted/stale durable bytes must not become balances |
| Missing block payload → rescan chunk | Absence must fail closed, not invent history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Tampering | `required_chainstate_snapshot` | mitigate | Call only `wallet_scan_chainstate_snapshot`; ban `load_chainstate_snapshot` in `wallet_rescan.rs` |
| T-146-02 | Tampering | `load_hydrate_chain_meta` leftover fallback | mitigate | Wallet path uses `load_chain_meta_for_open` only; fail closed if meta/tip disagree with coins B |
| T-146-03 | Information disclosure | missing payload skip | mitigate | `has_block` gate + `mark_failed` with `missing block payload`; no leftover UTXO fallback |
| T-146-04 | Spoofing | `Pruned` / `have_pruned` labels | mitigate | Do not introduce those symbols on this path; missing stays Unavailable (D-09/D-10) |
| T-146-05 | Elevation | prune operator surfaces | accept | Out of scope for Phase 146; deferred to 147–150 (D-11/D-12) |
</threat_model>

<verification>
Focused: `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib sync::tests::wallet_rescan_runtime`.
Repo contract remains `bash scripts/verify.sh` (D-14); full verify runs in Plan 03.
</verification>

<success_criteria>
- `wallet_rescan.rs` has zero `load_chainstate_snapshot` calls
- Disagreeing leftover reopen test passes with coins tip/UTXOs
- Missing payload chunk fails closed with `missing block payload`
</success_criteria>

<output>
After completion, create `.planning/phases/146-wallet-leftover-snapshot-cutover/146-01-SUMMARY.md`
</output>
