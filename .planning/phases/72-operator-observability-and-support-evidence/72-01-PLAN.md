---
phase: 72-operator-observability-and-support-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [OBS-01, OBS-03]
user_setup: []
must_haves:
  truths:
    - "CLI human status renders validated active-chain height/hash/work separately from header, downloaded, and connected progress."
    - "Dashboard status rows expose the same best-known tip, stay-current, no-progress, reorg, reconcile, resource-pressure, and validated-work facts as the shared status snapshot."
    - "Open Bitcoin-specific RPC sync status preserves durable Phase 68-71 fields while baseline `getblockchaininfo` remains Knots-compatible and does not expose support-only fields."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      provides: "Human CLI projection from OpenBitcoinStatusSnapshot/SyncStatus"
      contains: "Sync best-known tip"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Dashboard projection from OpenBitcoinStatusSnapshot/SyncStatus"
      contains: "Best-known tip"
    - path: "packages/open-bitcoin-rpc/src/dispatch/tests.rs"
      provides: "RPC durable sync status and baseline compatibility regression tests"
      contains: "open_bitcoin_sync_status_returns_phase72_durable_truth_contract"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "render helpers consume SyncStatus fields directly"
      pattern: "snapshot.sync.best_known_tip"
    - from: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "dashboard rows consume SyncStatus fields directly"
      pattern: "snapshot.sync.stay_current"
    - from: "packages/open-bitcoin-rpc/src/dispatch/tests.rs"
      to: "packages/open-bitcoin-rpc/src/method/node.rs"
      via: "OpenBitcoinSyncControlResponse serializes RuntimeMetadata; getblockchaininfo shape remains scoped"
      pattern: "OpenBitcoinSyncControlResponse"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 72-2026-06-13T16-25-04
generated_at: 2026-06-13T16:55:09Z
---

<objective>
Align human CLI, terminal dashboard, and Open Bitcoin-specific RPC status around `OpenBitcoinStatusSnapshot` and nested `SyncStatus`.

Purpose: OBS-01 and OBS-03 require operators to inspect and compare the same full-sync truth contract across status surfaces without renderer-local sync classification or baseline RPC compatibility drift.
Output: CLI, dashboard, and RPC regression coverage for shared Phase 68-71 status fields.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
@.planning/phases/72-operator-observability-and-support-evidence/72-RESEARCH.md
@.planning/phases/72-operator-observability-and-support-evidence/72-UI-SPEC.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-05-SUMMARY.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-05-SUMMARY.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs

<interfaces>
From packages/open-bitcoin-node/src/status.rs:
```rust
pub struct SyncProgress {
    pub header_height: u64,
    pub block_height: u64,
    pub downloaded_block_height: u64,
    pub connected_block_height: u64,
    pub validated_active_chain_height: u64,
    pub maybe_downloaded_block_hash: Option<String>,
    pub maybe_connected_block_hash: Option<String>,
    pub maybe_validated_active_chain_hash: Option<String>,
    pub maybe_validated_active_chain_work: Option<String>,
    pub progress_ratio: f64,
    pub messages_processed: u64,
    pub headers_received: u64,
    pub blocks_received: u64,
}

pub struct BestKnownTipStatus {
    pub source: BestKnownTipSource,
    pub height: u64,
    pub block_hash: String,
    pub work: String,
    pub block_time_unix_seconds: u64,
    pub observed_at_unix_seconds: u64,
    pub freshness: TipFreshnessStatus,
    pub peer_agreement: Vec<PeerTipAgreement>,
}

pub enum StayCurrentStatus {
    InitialCatchUp,
    CurrentAtBestKnownTip,
    StaleTip,
    Recovering,
    NoProgress,
}

pub struct SyncStatus {
    pub sync_progress: FieldAvailability<SyncProgress>,
    pub resource_pressure: FieldAvailability<SyncResourcePressure>,
    pub best_known_tip: FieldAvailability<BestKnownTipStatus>,
    pub stay_current: FieldAvailability<StayCurrentStatus>,
    pub stay_current_next_action: FieldAvailability<String>,
    pub no_progress_diagnosis: FieldAvailability<NoProgressDiagnosis>,
    pub no_progress_next_action: FieldAvailability<String>,
    pub latest_reorg: FieldAvailability<SyncReorgEvidence>,
    pub reconcile_progress: FieldAvailability<SyncReconcileProgressStatus>,
}
```

From packages/open-bitcoin-rpc/src/method/node.rs:
```rust
pub struct OpenBitcoinSyncControlResponse {
    pub metadata: RuntimeMetadata,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render full-sync truth fields in CLI human status</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-UI-SPEC.md
    - packages/open-bitcoin-cli/src/operator/status/render.rs
    - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
    - packages/open-bitcoin-node/src/status.rs
    - .planning/phases/69-tip-tracking-and-stay-current-operation/69-05-SUMMARY.md
    - .planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-05-SUMMARY.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-04-SUMMARY.md
  </read_first>
  <behavior>
    - Test `phase72_cli_status_renders_full_sync_truth_contract`: a populated `OpenBitcoinStatusSnapshot` renders lines beginning `Sync best-known tip:`, `Sync stay-current:`, `Sync stay-current action:`, `Sync latest reorg:`, `Sync reconcile:`, `Sync no-progress diagnosis:`, `Sync no-progress action:`, `Sync pressure:`, and `Sync:`.
    - The same test asserts the `Sync:` line contains `headers=840100`, `downloaded_blocks=840006`, `connected_blocks=840004`, `validated_active_chain_height=840004`, `validated_active_chain_hash=1111111111111111111111111111111111111111111111111111111111111111`, and `validated_active_chain_work=840005`.
    - The same test asserts unavailable fields render exactly `Unavailable: {reason}` when `best_known_tip`, `stay_current`, `latest_reorg`, and `reconcile_progress` are unavailable.
  </behavior>
  <action>Update CLI human status rendering per D-01, D-02, D-03, D-04, D-05, and D-16. Extend `sync_progress_availability` so the available form includes exactly `validated_active_chain_height={}`, `validated_active_chain_hash={}`, and `validated_active_chain_work={}` after `connected_blocks={}`; render missing hash/work as `Unavailable: validated active-chain hash unavailable` and `Unavailable: validated active-chain work unavailable`. Add helper renderers for `BestKnownTipStatus`, `StayCurrentStatus`, `SyncReorgEvidence`, and `SyncReconcileProgressStatus`; use serde snake_case labels for enum/status values and keep unavailable output as `Unavailable: {reason}`. Add human lines named exactly `Sync best-known tip:`, `Sync stay-current:`, `Sync stay-current action:`, `Sync latest reorg:`, and `Sync reconcile:`. Add the named focused unit test with Arrange/Act/Assert comments when setup is non-trivial. Do not parse human output or infer current-at-tip from header/downloaded progress.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_cli_status_renders_full_sync_truth_contract --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase72_cli_status_renders_full_sync_truth_contract|Sync best-known tip:|Sync stay-current:|Sync latest reorg:|validated_active_chain_work=840005" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs` finds all five strings.
    - `rg -n "validated_active_chain_height=\\{\\}|validated_active_chain_hash=\\{\\}|validated_active_chain_work=\\{\\}" packages/open-bitcoin-cli/src/operator/status/render.rs` finds the progress format fields.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_cli_status_renders_full_sync_truth_contract --all-features` passes.
  </acceptance_criteria>
  <done>CLI human status renders the canonical full-sync truth fields from `SyncStatus` without local reclassification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project full-sync truth fields into dashboard rows</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-UI-SPEC.md
    - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <behavior>
    - Test `phase72_dashboard_projects_full_sync_truth_contract`: the `Sync and Peers` section contains row labels exactly `Best-known tip`, `Stay-current`, `Stay-current action`, `No-progress diagnosis`, `No-progress action`, `Latest reorg`, `Reconcile`, `Pressure`, and `Progress`.
    - The same test asserts the `Progress` row contains `validated_active_chain_height=840004`, `validated_active_chain_hash=1111111111111111111111111111111111111111111111111111111111111111`, and `validated_active_chain_work=840005`.
    - The same test asserts the unavailable `Best-known tip`, `Stay-current`, `Latest reorg`, and `Reconcile` rows preserve `Unavailable: best-known tip evidence unavailable`, `Unavailable: stay-current state unavailable`, `Unavailable: no reorg evidence recorded`, and `Unavailable: reconcile progress unavailable`.
  </behavior>
  <action>Update `dashboard_sections` per D-01, D-02, D-03, D-04, D-05, and D-16. Add rows to the existing `Sync and Peers` section; do not add a new terminal section unless row overflow forces it. Reuse the same field semantics as CLI human rendering: best-known tip includes source, height, hash, work, freshness, observed time, and peer agreement count; stay-current renders the serde snake_case state; latest reorg and reconcile render bounded machine labels/details; progress preserves header/downloaded/connected/validated distinctions. Add the named focused unit test with Arrange/Act/Assert comments when setup is non-trivial. Keep dashboard action keys and destructive confirmation flow unchanged.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_dashboard_projects_full_sync_truth_contract --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase72_dashboard_projects_full_sync_truth_contract|Best-known tip|Stay-current action|Latest reorg|validated_active_chain_work=840005" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs` finds all five strings.
    - `rg -n "start service|stop service|restart service|install service|uninstall service|enable service|disable service" packages/open-bitcoin-cli/src/operator/dashboard/model.rs` still finds the destructive dashboard actions.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_dashboard_projects_full_sync_truth_contract --all-features` passes.
  </acceptance_criteria>
  <done>Dashboard rows expose the same shared full-sync truth fields and unavailable reasons as CLI human status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Open Bitcoin RPC status preserves durable evidence without widening baseline RPC</name>
  <files>packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - packages/open-bitcoin-rpc/src/method/node.rs
    - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <behavior>
    - Test `open_bitcoin_sync_status_returns_phase72_durable_truth_contract`: the Open Bitcoin-specific sync status response serializes `metadata.maybe_sync_state.sync.sync_progress.value.validated_active_chain_height`, `maybe_validated_active_chain_hash`, `maybe_validated_active_chain_work`, `best_known_tip`, `stay_current`, `no_progress_diagnosis`, `latest_reorg`, `reconcile_progress`, and `resource_pressure`.
    - Test `get_blockchain_info_does_not_expose_phase72_support_fields`: serialized baseline `getblockchaininfo` does not contain `best_known_tip`, `stay_current`, `latest_reorg`, `reconcile_progress`, `resource_pressure`, `support_evidence`, `evidence_verdict`, or `validated_active_chain_work`.
  </behavior>
  <action>Add the two regression tests per D-06 and D-16. Reuse existing durable sync test fixtures in `packages/open-bitcoin-rpc/src/dispatch/tests.rs`; if the existing fixture has Phase 69-71 fields unavailable, populate it directly with `FieldAvailability::available(...)` values. Do not change `GetBlockchainInfoResponse`; baseline-compatible RPC remains scoped to chain, blocks, headers, bestblockhash, mediantime, verificationprogress, initialblockdownload, and warnings. Open Bitcoin-specific RPC status may continue returning `OpenBitcoinSyncControlResponse { metadata }` as long as the test proves the durable metadata includes the canonical evidence.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_sync_status_returns_phase72_durable_truth_contract --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info_does_not_expose_phase72_support_fields --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "open_bitcoin_sync_status_returns_phase72_durable_truth_contract|get_blockchain_info_does_not_expose_phase72_support_fields|validated_active_chain_work|best_known_tip|evidence_verdict" packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds all five patterns.
    - `rg -n "best_known_tip|stay_current|latest_reorg|support_evidence|evidence_verdict" packages/open-bitcoin-rpc/src/method/node.rs` returns no matches.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_sync_status_returns_phase72_durable_truth_contract --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info_does_not_expose_phase72_support_fields --all-features` passes.
  </acceptance_criteria>
  <done>Open Bitcoin-specific RPC exposes durable full-sync evidence and baseline RPC stays compatibility-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared status -> human renderers | Trusted typed status crosses into user-visible CLI/dashboard text where renderer-local classification could drift. |
| durable metadata -> RPC response | Durable sync metadata crosses into local RPC responses with different compatibility contracts. |
| baseline RPC compatibility -> Open Bitcoin extensions | Baseline-compatible methods must not expose support-only or Open Bitcoin-only evidence fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | T/R | CLI and dashboard renderers | mitigate | Render `OpenBitcoinStatusSnapshot`/`SyncStatus` fields directly and add exact renderer tests for Phase 68-71 fields. |
| T-72-01-02 | R | status progress wording | mitigate | Preserve header/downloaded/connected/validated active-chain distinctions in exact output and tests. |
| T-72-01-03 | I/T | baseline `getblockchaininfo` | mitigate | Add a regression proving baseline RPC output omits support evidence, verdict, best-known tip, stay-current, reorg, reconcile, and resource-pressure fields. |
| T-72-01-04 | S/R | sync-to-tip evidence interpretation | mitigate | Do not infer current-at-tip in renderers; only display typed `stay_current` and `best_known_tip` evidence from shared status. |
</threat_model>

<verification>
Run focused tests for the three task test names, then run `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_ --all-features` and `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc phase72 --all-features` after implementation.
</verification>

<success_criteria>
- OBS-01 status surfaces consume or summarize `OpenBitcoinStatusSnapshot`/`SyncStatus`.
- OBS-03 comparison fields are visible in CLI, dashboard, and Open Bitcoin RPC status with matching unavailable reasons.
- Baseline RPC compatibility shape is unchanged for support-only fields.
</success_criteria>

<output>
After completion, create `.planning/phases/72-operator-observability-and-support-evidence/72-01-SUMMARY.md`.
</output>
