---
phase: 72-operator-observability-and-support-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: [OBS-02, OBS-04]
user_setup: []
must_haves:
  truths:
    - "Support bundles contain allowlisted full-sync evidence, explicit connected/validated active-chain height/hash/work, and a typed evidence-derived verdict."
    - "Support evidence summarizes live-smoke reports without copying raw reports, raw logs, peer transcripts, credentials, wallet material, or unbounded endpoint tables."
    - "Support Markdown explains whether evidence proves sync-to-tip, stay-current behavior, a diagnosed blocker, or remains inconclusive."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support.rs"
      provides: "Support bundle full-sync evidence, connected/validated active-chain evidence, and typed verdict derivation"
      contains: "SupportEvidenceVerdict"
    - path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      provides: "Allowlisted live-smoke summary expansion for Phase 69-71 fields"
      contains: "validatedActiveChainHeight"
    - path: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      provides: "Markdown rendering for full-sync evidence and verdicts"
      contains: "Evidence verdict:"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "derive_full_sync_evidence reads OpenBitcoinStatusSnapshot/SyncStatus"
      pattern: "derive_full_sync_evidence"
    - from: "packages/open-bitcoin-cli/src/operator/support.rs"
      to: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      via: "collect_live_smoke_evidence stores summary-only report data"
      pattern: "live_smoke::summary"
    - from: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      to: "packages/open-bitcoin-cli/src/operator/support.rs"
      via: "render_support_markdown displays bundle.full_sync_evidence.verdict"
      pattern: "full_sync_evidence"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 72-2026-06-13T16-25-04
generated_at: 2026-06-13T16:55:09Z
---

<objective>
Add compact, redacted support-bundle full-sync evidence and typed final verdicts.

Purpose: OBS-02 and OBS-04 require a shareable evidence object that explains what was proven without overcollecting raw logs, secrets, wallet material, or raw live-smoke reports.
Output: Support bundle JSON/Markdown fields, typed verdict derivation, allowlisted live-smoke summaries, and regression tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
@.planning/phases/72-operator-observability-and-support-evidence/72-RESEARCH.md
@.planning/phases/72-operator-observability-and-support-evidence/72-UI-SPEC.md
@.planning/phases/65-support-bundle-and-operator-review-docs/65-01-SUMMARY.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs

<interfaces>
From packages/open-bitcoin-cli/src/operator/support.rs:
```rust
struct SupportEvidenceBundle {
    generated_at_unix_seconds: u64,
    generated_by: String,
    output: SupportEvidenceOutput,
    redaction: RedactionSummary,
    config: ConfigEvidence,
    status: OpenBitcoinStatusSnapshot,
    store_health: StoreHealthEvidence,
    live_smoke: LiveSmokeEvidence,
}
```

From packages/open-bitcoin-cli/src/operator/support/live_smoke.rs:
```rust
pub(super) fn summary(value: &Value) -> Option<Value>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add support-bundle full-sync evidence and typed verdict derivation</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-RESEARCH.md
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-node/src/status.rs
    - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
  </read_first>
  <behavior>
    - Test `phase72_support_verdict_sync_to_tip_requires_validated_tip_match`: available fresh `best_known_tip` plus available `sync_progress` with `validated_active_chain_height`, `maybe_validated_active_chain_hash`, and `maybe_validated_active_chain_work` matching the best-known tip yields verdict `sync_to_tip_proven` and justification `validated_active_chain_matches_best_known_tip`.
    - The same test asserts serialized `full_sync_evidence.connected_active_chain.height = 840004`, `hash = "1111111111111111111111111111111111111111111111111111111111111111"`, `work = "840005"`, plus the same `validated_active_chain.height`, `hash`, and `work` values.
    - Test `phase72_support_verdict_stay_current_requires_current_at_best_known_tip`: the same evidence plus `stay_current = CurrentAtBestKnownTip` yields verdict `stay_current_proven` and justification `stay_current_current_at_best_known_tip`.
    - Test `phase72_support_verdict_diagnosed_blocker_uses_shared_diagnosis`: available blocking `no_progress_diagnosis = StorageOrResourceBlocked` and `recovery_category = ResourceExhaustion` yields verdict `diagnosed_blocker` and justification `blocking_diagnosis_available`.
    - Test `phase72_support_verdict_normal_resource_pressure_alone_is_inconclusive`: available normal/bounded `resource_pressure` without blocking no-progress diagnosis, resource-exhaustion recovery category/action, non-normal pressure, or blocker-indicating reorg/reconcile evidence yields verdict `inconclusive`.
    - Test `phase72_support_verdict_inconclusive_lists_missing_evidence`: missing best-known tip, connected active-chain hash/work, or validated active-chain hash/work yields verdict `inconclusive`, justification `missing_required_sync_to_tip_evidence`, and JSON unavailable-reason fields for the missing connected/validated fact.
  </behavior>
  <action>Add a `full_sync_evidence: FullSyncEvidence` field to `SupportEvidenceBundle` per D-09, D-10, D-11, and D-16. Add `#[derive(Debug, Clone, PartialEq, Eq, Serialize)] enum SupportEvidenceVerdict { SyncToTipProven, StayCurrentProven, DiagnosedBlocker, Inconclusive }` with `#[serde(rename_all = "snake_case")]`. Add `FullSyncEvidence` with fields named exactly `initial_tip`, `final_tip`, `connected_active_chain`, `validated_active_chain`, `restart_resume_checkpoints`, `stay_current_window`, `peer_contribution`, `no_progress_or_reorg_events`, `resource_pressure`, `recovery`, and `verdict`. Add a reusable active-chain evidence struct serialized with fields `height`, `hash`, `work`, and `maybe_unavailable_reason`; `connected_active_chain` must use `sync_progress.connected_block_height`, `maybe_connected_block_hash`, and the canonical active-chain cumulative work when connected and validated evidence agree, otherwise `maybe_unavailable_reason = "connected active-chain work unavailable"`. `validated_active_chain` must use `validated_active_chain_height`, `maybe_validated_active_chain_hash`, and `maybe_validated_active_chain_work`, with unavailable reasons for missing hash/work. Add `EvidenceVerdictSummary { label: SupportEvidenceVerdict, justifications: Vec<String> }`. Implement `derive_full_sync_evidence(status: &OpenBitcoinStatusSnapshot, live_smoke: &LiveSmokeEvidence) -> FullSyncEvidence` as a pure helper. Verdict rules: `stay_current_proven` requires `sync_to_tip_proven` evidence plus `StayCurrentStatus::CurrentAtBestKnownTip`; `sync_to_tip_proven` requires fresh best-known tip evidence and connected plus validated active-chain height/hash/work matching that tip; `diagnosed_blocker` requires actual blocking evidence: a blocking no-progress diagnosis, resource-exhaustion recovery category/action, non-normal resource pressure, or latest reorg/reconcile evidence that explicitly indicates a blocker; normal bounded resource pressure alone must remain `inconclusive`; otherwise `inconclusive`. Keep exact fixture values and field strings in tests because the Plan 72-04 checker compares them across surfaces, not just checks field presence. Add the named focused unit tests with Arrange/Act/Assert comments when setup is non-trivial. Do not include raw daemon logs, raw peer transcripts, RPC cookies/passwords, config secrets, wallet material, raw live-smoke report contents, or unbounded endpoint tables.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_support_verdict_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "enum SupportEvidenceVerdict|SyncToTipProven|StayCurrentProven|DiagnosedBlocker|Inconclusive|derive_full_sync_evidence|validated_active_chain_matches_best_known_tip|missing_required_sync_to_tip_evidence|phase72_support_verdict_normal_resource_pressure_alone_is_inconclusive" packages/open-bitcoin-cli/src/operator/support.rs` finds all nine patterns.
    - `rg -n "full_sync_evidence: FullSyncEvidence|initial_tip|final_tip|connected_active_chain|validated_active_chain|restart_resume_checkpoints|stay_current_window|peer_contribution|no_progress_or_reorg_events|resource_pressure|recovery" packages/open-bitcoin-cli/src/operator/support.rs` finds all eleven field names.
    - `rg -n "connected active-chain work unavailable|validated active-chain hash unavailable|validated active-chain work unavailable|840004|840005|1111111111111111111111111111111111111111111111111111111111111111" packages/open-bitcoin-cli/src/operator/support.rs` finds unavailable-reason and deterministic comparison fixture values.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_support_verdict_ --all-features` passes.
  </acceptance_criteria>
  <done>Support bundles derive a typed verdict from shared status and compact live-smoke evidence, with illegal proof states guarded by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render full-sync evidence and verdicts in support output</name>
  <files>packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-UI-SPEC.md
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
    - packages/open-bitcoin-cli/src/operator/status/render.rs
  </read_first>
  <behavior>
    - Test `open_bitcoin_support_bundle_includes_phase72_full_sync_evidence_and_typed_verdict`: generated `support-evidence.json` contains `full_sync_evidence.verdict.label = "sync_to_tip_proven"` for a fixture whose validated active-chain evidence matches a fresh best-known tip.
    - The same test asserts JSON contains `full_sync_evidence.connected_active_chain.height = 840004`, `hash = "1111111111111111111111111111111111111111111111111111111111111111"`, `work = "840005"`, and matching `full_sync_evidence.validated_active_chain.height/hash/work`.
    - The same test asserts Markdown contains `## Full Sync Evidence`, `Evidence verdict: sync_to_tip_proven`, `validated_active_chain_matches_best_known_tip`, `Connected active chain: height=840004 hash=1111111111111111111111111111111111111111111111111111111111111111 work=840005`, `Validated active chain: height=840004 hash=1111111111111111111111111111111111111111111111111111111111111111 work=840005`, `Stay-current window:`, `Peer contribution:`, `No-progress or reorg events:`, `Resource pressure:`, and `Recovery:`.
    - The same test asserts unavailable connected/validated active-chain paths render as `Unavailable: {reason}` in Markdown and preserve the same reason in JSON `maybe_unavailable_reason`.
    - The same test asserts JSON, Markdown, and command stdout do not contain `super-secret-password`, `super-secret-cookie`, `stdoutTail`, `stderrTail`, `rawPeerTable`, `rawLogTail`, `seed phrase`, or `walletMaterial`.
  </behavior>
  <action>Update `render_support_markdown` per D-09, D-10, D-11, D-13, and D-16. Add a Markdown section named exactly `## Full Sync Evidence`. Render the verdict line exactly as `Evidence verdict: {snake_case_label}`. Render one compact line each for connected active chain, validated active chain, restart/resume checkpoints, stay-current window, peer contribution, no-progress or reorg events, resource pressure, recovery, and verdict justifications. Connected and validated active-chain lines must include explicit `height=`, `hash=`, and `work=` components when available, and `Unavailable: {reason}` when the corresponding JSON evidence carries `maybe_unavailable_reason`. Use values from `bundle.full_sync_evidence`; do not rederive verdicts in the renderer. Extend the existing operator binary support bundle test fixture or add the named test, keeping credential and wallet-secret absence checks and exact cross-surface checker fixture values.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_includes_phase72_full_sync_evidence_and_typed_verdict --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "## Full Sync Evidence|Evidence verdict:|Connected active chain:|Validated active chain:|Stay-current window:|Peer contribution:|No-progress or reorg events:|Unavailable: \\{reason\\}" packages/open-bitcoin-cli/src/operator/support/render.rs` finds all eight strings.
    - `rg -n "open_bitcoin_support_bundle_includes_phase72_full_sync_evidence_and_typed_verdict|sync_to_tip_proven|validated_active_chain_matches_best_known_tip|connected_active_chain|validated_active_chain|840004|840005|rawPeerTable|walletMaterial" packages/open-bitcoin-cli/tests/operator_binary.rs` finds all nine strings.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_includes_phase72_full_sync_evidence_and_typed_verdict --all-features` passes.
  </acceptance_criteria>
  <done>Support JSON and Markdown expose compact typed evidence and preserve redaction boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand live-smoke support allowlist for Phase 69-71 evidence</name>
  <files>packages/open-bitcoin-cli/src/operator/support/live_smoke.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
  </read_first>
  <behavior>
    - Test `phase72_live_smoke_summary_preserves_full_sync_evidence_without_raw_report`: schema version 2 summary keeps final-status keys `validatedActiveChainHeight`, `maybeValidatedActiveChainHash`, `maybeValidatedActiveChainWork`, `bestKnownTip`, `stayCurrent`, `stayCurrentNextAction`, `noProgressDiagnosis`, `noProgressNextAction`, `latestReorg`, `reconcileProgress`, `resourcePressure`, and `peerContribution`.
    - The same test asserts summary text does not contain `rawPeerTable`, `daemonStdout`, `daemonStderr`, `rawLogTail`, `rpcpassword`, `rpcauth`, `__cookie__`, `walletMaterial`, or the raw secret value `phase72-live-smoke-secret`.
  </behavior>
  <action>Extend `FINAL_STATUS_KEYS` and nested summary helpers in `support/live_smoke.rs` per D-09, D-12, and D-16. Add allowlisted summary objects for `bestKnownTip`, `latestReorg`, `reconcileProgress`, and `peerContribution`; keep each helper copying only scalar bounded fields and summary labels. Redact suspicious strings through the existing redaction path so `rpcpassword`, `rpcauth`, and `__cookie__` become `[redacted]` when they appear in allowlisted text fields. Add the named focused unit test with Arrange/Act/Assert comments when setup is non-trivial. Do not copy the raw report, daemon stdout/stderr, raw log tails, raw peer tables, wallet material, endpoint tables beyond bounded peer contribution summaries, or arbitrary nested objects.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_live_smoke_summary_preserves_full_sync_evidence_without_raw_report --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase72_live_smoke_summary_preserves_full_sync_evidence_without_raw_report|validatedActiveChainHeight|maybeValidatedActiveChainHash|maybeValidatedActiveChainWork|bestKnownTip|stayCurrent|latestReorg|reconcileProgress|peerContribution" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` finds all nine patterns.
    - `rg -n "rawPeerTable|daemonStdout|daemonStderr|rawLogTail|rpcpassword|rpcauth|__cookie__|walletMaterial|phase72-live-smoke-secret" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` finds forbidden-field regression fixture strings.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_live_smoke_summary_preserves_full_sync_evidence_without_raw_report --all-features` passes.
  </acceptance_criteria>
  <done>Support bundle live-smoke ingestion remains summary-only while preserving Phase 69-71 proof fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local status/live-smoke input -> support evidence | Local report/status data may contain secrets, raw logs, peer transcripts, wallet material, or misleading evidence. |
| support evidence -> external sharing | Operators may share bundle JSON/Markdown outside their machine. |
| evidence fields -> final verdict | Support verdict labels can overclaim sync-to-tip or stay-current proof if not tied to canonical evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | I | `support.rs` and `support/live_smoke.rs` | mitigate | Keep support evidence allowlisted and test absence of RPC cookies/passwords, raw logs, raw live-smoke reports, raw peer tables, wallet material, and secret-like values. |
| T-72-02-02 | R/S | `SupportEvidenceVerdict` | mitigate | Derive verdicts from validated active-chain, best-known tip, stay-current, blocking no-progress, resource-exhaustion recovery, non-normal resource pressure, blocker-indicating reorg/reconcile evidence, and unavailable evidence; include justification strings and a normal-resource-pressure negative test. |
| T-72-02-03 | I | support Markdown renderer | mitigate | Render compact summaries only from `FullSyncEvidence`; never print raw report fields or arbitrary JSON. |
| T-72-02-04 | T | live-smoke report ingestion | mitigate | Copy only named schema version 2 keys and nested scalar summaries; redact credential-like strings in allowlisted fields. |
</threat_model>

<verification>
Run `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_support_ --all-features`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase72_live_smoke_ --all-features`, and the named operator binary test before summary.
</verification>

<success_criteria>
- OBS-02 support evidence contains initial/final tip, connected and validated active-chain evidence, restart/resume, stay-current, peer contribution, no-progress/reorg, resource pressure, recovery, and typed verdict fields.
- OBS-04 support output uses bounded verdict language and preserves deferred production scope.
- Raw report/log/credential/wallet material is not copied into support evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/72-operator-observability-and-support-evidence/72-02-SUMMARY.md`.
</output>
