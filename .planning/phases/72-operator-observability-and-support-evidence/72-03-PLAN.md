---
phase: 72-operator-observability-and-support-evidence
plan: 03
type: execute
wave: 2
depends_on: [72-02]
files_modified:
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
user_setup: []
must_haves:
  truths:
    - "Metrics and structured logs retain bounded numeric/compact forms while exposing enough shared dimensions to correlate with status and support evidence."
    - "Live-smoke final status carries validated active-chain, best-known tip, stay-current, no-progress, reorg, reconcile, peer contribution, resource pressure, and latest stop evidence."
    - "Live-smoke schema version 2 remains deterministic and public-network-free in fixture verification."
  artifacts:
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Bounded numeric metric kind for validated active-chain height"
      contains: "ValidatedActiveChainHeight"
    - path: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      provides: "Metrics and structured log projection from sync summary"
      contains: "phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions"
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "Opt-in live-smoke final status full-sync evidence"
      contains: "validatedActiveChainHeight"
    - path: "scripts/test-run-live-mainnet-smoke.sh"
      provides: "Deterministic live-smoke fixture assertions"
      contains: "phase72 live-smoke final status evidence missing"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      to: "packages/open-bitcoin-node/src/metrics.rs"
      via: "SyncRunSummary::metric_samples emits MetricKind::ValidatedActiveChainHeight"
      pattern: "MetricKind::ValidatedActiveChainHeight"
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      via: "schema version 2 final_status keys are allowlisted by Plan 72-02"
      pattern: "bestKnownTip"
    - from: "scripts/test-run-live-mainnet-smoke.sh"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "deterministic fixture checks generated JSON and Markdown"
      pattern: "validatedActiveChainHeight"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 72-2026-06-13T16-25-04
generated_at: 2026-06-13T16:55:09Z
---

<objective>
Extend bounded metric/log and live-smoke projections with Phase 72 full-sync evidence fields.

Purpose: OBS-01 through OBS-03 require metrics, logs, and live-smoke summaries to correlate with the same full-sync truth contract without becoming raw report stores or default public-network checks.
Output: Numeric metric/log projection updates, live-smoke final-status fields, and deterministic fixture assertions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
@.planning/phases/72-operator-observability-and-support-evidence/72-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-03-SUMMARY.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-04-SUMMARY.md
@.planning/phases/72-operator-observability-and-support-evidence/72-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/logging.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh

<interfaces>
From packages/open-bitcoin-node/src/metrics.rs:
```rust
pub enum MetricKind {
    SyncHeight,
    HeaderHeight,
    DownloadedBlockHeight,
    ConnectedBlockHeight,
    PeerCount,
    MempoolTransactions,
    WalletTrustedBalanceSats,
    DiskUsageBytes,
    RpcHealth,
    ServiceRestarts,
}
```

From packages/open-bitcoin-node/src/logging.rs:
```rust
pub struct StructuredLogRecord {
    pub level: StructuredLogLevel,
    pub source: String,
    pub message: String,
    pub timestamp_unix_seconds: u64,
}
```

From scripts/run-live-mainnet-smoke.ts:
```ts
type FinalStatusSummary = {
  headerHeight: number | null;
  downloadedBlockHeight: number | null;
  connectedBlockHeight: number | null;
  resourcePressure: ResourcePressureSummary | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded metric and structured-log full-sync dimensions</name>
  <files>packages/open-bitcoin-node/src/metrics.rs, packages/open-bitcoin-node/src/sync/types/summary.rs</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - packages/open-bitcoin-node/src/metrics.rs
    - packages/open-bitcoin-node/src/logging.rs
    - packages/open-bitcoin-node/src/sync/types/summary.rs
    - packages/open-bitcoin-node/src/sync/tests.rs
  </read_first>
  <behavior>
    - Test `phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions`: `SyncRunSummary::metric_samples(1717000000)` includes `MetricKind::ValidatedActiveChainHeight` with value `best_block_height as f64`.
    - The same test asserts `structured_log_records(1717000000)` contains compact message labels `validated_active_chain_height=`, `validated_active_chain_work=`, `resource_pressure_blocks_in_flight=`, `resource_pressure_target_outbound_peers=`, `peer_contribution_connected=`, `peer_contribution_failed=`, `latest_stop_reason=`, and `recovery_category=`.
    - Existing `metric_kind_names_are_stable` includes `(MetricKind::ValidatedActiveChainHeight, "validated_active_chain_height")`.
  </behavior>
  <action>Add `ValidatedActiveChainHeight` to `MetricKind`, `MetricKind::ALL`, and `MetricKind::as_str()` per D-02, D-07, D-16, and D-18. Keep metrics numeric and bounded; do not add freeform labels to `MetricSample`. Modify only existing Rust files in this plan, so no new parity breadcrumb entry is required. In `SyncRunSummary::metric_samples`, emit `MetricSample::new(MetricKind::ValidatedActiveChainHeight, self.best_block_height as f64, timestamp_unix_seconds)`. In `structured_log_records`, extend compact sync info records so one record includes exact labels `validated_active_chain_height={}`, `validated_active_chain_work={}`, `resource_pressure_blocks_in_flight=0`, `resource_pressure_target_outbound_peers={}`, `peer_contribution_connected={}`, `peer_contribution_failed={}`, `latest_stop_reason={}`, and `recovery_category={}`. Use `self.maybe_validated_active_chain_work.as_deref().unwrap_or("unavailable")` for missing work. Add the named focused unit test with Arrange/Act/Assert comments when setup is non-trivial. Do not log raw peer transcripts, raw block data, credentials, config secrets, wallet material, or unbounded endpoint tables.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metric_kind_names_are_stable --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ValidatedActiveChainHeight|validated_active_chain_height|phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions|validated_active_chain_work=|resource_pressure_target_outbound_peers=|peer_contribution_connected=" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/sync/types/summary.rs` finds all six patterns.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metric_kind_names_are_stable --all-features` passes.
  </acceptance_criteria>
  <done>Metrics and structured logs expose bounded, correlatable full-sync dimensions without raw evidence overcollection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend live-smoke final status with full-sync truth fields</name>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <read_first>
    - .planning/phases/72-operator-observability-and-support-evidence/72-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-RESEARCH.md
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
    - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
    - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <behavior>
    - Deterministic fixture assertions in `scripts/test-run-live-mainnet-smoke.sh` fail with message `phase72 live-smoke final status evidence missing` if generated JSON lacks `final_status.validatedActiveChainHeight`, `maybeValidatedActiveChainHash`, `maybeValidatedActiveChainWork`, `bestKnownTip`, `stayCurrent`, `stayCurrentNextAction`, `noProgressDiagnosis`, `noProgressNextAction`, `latestReorg`, `reconcileProgress`, and `peerContribution`.
    - Generated Markdown contains rows beginning `- Validated active-chain height:`, `- Validated active-chain hash:`, `- Validated active-chain work:`, `- Best-known tip:`, `- Stay-current:`, `- No-progress diagnosis:`, `- Latest reorg:`, and `- Reconcile progress:`.
    - `schema_version` remains `2`.
  </behavior>
  <action>Extend `FinalStatusSummary` in `scripts/run-live-mainnet-smoke.ts` per D-01, D-02, D-03, D-04, D-12, D-14, and D-16. Add fields named exactly `validatedActiveChainHeight`, `maybeValidatedActiveChainHash`, `maybeValidatedActiveChainWork`, `bestKnownTip`, `maybeBestKnownTipUnavailableReason`, `stayCurrent`, `maybeStayCurrentUnavailableReason`, `stayCurrentNextAction`, `maybeStayCurrentNextActionUnavailableReason`, `noProgressDiagnosis`, `maybeNoProgressDiagnosisUnavailableReason`, `noProgressNextAction`, `maybeNoProgressNextActionUnavailableReason`, `latestReorg`, `maybeLatestReorgUnavailableReason`, `reconcileProgress`, `maybeReconcileProgressUnavailableReason`, and `peerContribution`. Map them only from typed durable status JSON (`metadata.maybe_sync_state.sync` and `metadata.maybe_sync_state.peers`); do not parse CLI human text or Markdown. Add compact Markdown lines for these fields using existing `fieldText`/summary helpers. Keep report `schema_version: 2` because fields are additive. Extend `scripts/test-run-live-mainnet-smoke.sh` deterministic fixtures and generated JSON/Markdown assertions; do not add public-network execution to default verification.</action>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validatedActiveChainHeight|maybeValidatedActiveChainHash|maybeValidatedActiveChainWork|bestKnownTip|stayCurrentNextAction|noProgressDiagnosis|latestReorg|reconcileProgress|peerContribution" scripts/run-live-mainnet-smoke.ts` finds all nine patterns.
    - `rg -n "phase72 live-smoke final status evidence missing|Validated active-chain height:|Best-known tip:|Stay-current:|No-progress diagnosis:|Latest reorg:|Reconcile progress:" scripts/test-run-live-mainnet-smoke.sh scripts/run-live-mainnet-smoke.ts` finds all seven patterns.
    - `rg -n "schema_version: 2|schema_version\": 2" scripts/run-live-mainnet-smoke.ts scripts/test-run-live-mainnet-smoke.sh` finds schema version 2 assertions or assignments.
    - `bash scripts/test-run-live-mainnet-smoke.sh` passes.
  </acceptance_criteria>
  <done>Live-smoke schema version 2 final status carries compact full-sync truth fields and deterministic fixture coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync runtime summary -> metrics/logs | Runtime evidence is projected into bounded telemetry that may be used for support correlation. |
| durable status JSON -> live-smoke report | Local status JSON is parsed by TypeScript and written into shareable opt-in reports. |
| live-smoke fixtures -> default verification | Deterministic fixture checks must not invoke public network, service managers, or timing-sensitive sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-01 | I | structured logs | mitigate | Add only compact labels and bounded counters; prohibit raw peer transcripts, raw block data, credentials, config secrets, and wallet material. |
| T-72-03-02 | R | metrics/status correlation | mitigate | Add `validated_active_chain_height` numeric metric so metrics do not conflate downloaded/connected and validated progress. |
| T-72-03-03 | T | live-smoke status mapping | mitigate | Parse typed durable status JSON fields only; do not parse human CLI or Markdown text. |
| T-72-03-04 | D | deterministic verification | mitigate | Keep `scripts/test-run-live-mainnet-smoke.sh` fixture-only and do not wire public live-smoke execution into `scripts/verify.sh`. |
</threat_model>

<verification>
Run `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase72_summary_metrics_and_logs_carry_full_sync_truth_dimensions --all-features`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metric_kind_names_are_stable --all-features`, and `bash scripts/test-run-live-mainnet-smoke.sh`.
</verification>

<success_criteria>
- OBS-01 metrics/log/live-smoke projections consume or summarize shared status fields.
- OBS-02 live-smoke report evidence remains compact and support-ingestible.
- OBS-03 comparison fields exist in deterministic telemetry/report fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/72-operator-observability-and-support-evidence/72-03-SUMMARY.md`.
</output>
