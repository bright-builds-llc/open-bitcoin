---
phase: 86-service-operation-expectations
plan: 03
type: execute
subsystem: automation
wave: 3
depends_on:
  - 86-01
  - 86-02
files_modified:
  - scripts/check-phase86-service-operation-expectations.test.ts
  - scripts/check-phase86-service-operation-expectations.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - SVC-01
  - SVC-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 86-2026-06-22T19-33-52
generated_at: 2026-06-22T19:50:39Z
must_haves:
  truths:
    - "Contributors can run deterministic Bun fixture tests for Phase 86 service expectation drift."
    - "The checker validates service document sections, exact support terms, command forms, lifecycle labels, restart/resume fields, field-based evidence rules, sensitive support boundaries, parity roots, required links, and verifier wiring."
    - "Default verification runs the Phase 86 checker after Phase 85 without public-network, real service-manager, long-wall-clock, package-manager service, Windows service, automatic upload, or production-service ownership actions."
  artifacts:
    - path: "scripts/check-phase86-service-operation-expectations.test.ts"
      provides: "Bun fixture tests for Phase 86 checker behavior"
      contains: "passes_when_phase86_fixture_contains_service_roots_and_verify_order"
    - path: "scripts/check-phase86-service-operation-expectations.ts"
      provides: "Deterministic Phase 86 checker"
      exports:
        - "checkPhase86ServiceOperationExpectations"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring for Phase 86"
      contains: "bun run scripts/check-phase86-service-operation-expectations.ts"
  key_links:
    - from: "scripts/check-phase86-service-operation-expectations.ts"
      to: "docs/parity/service-operation-expectations.md"
      via: "fixed target-file read"
      pattern: "docs/parity/service-operation-expectations\\.md"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase86-service-operation-expectations.ts"
      via: "run_step after Phase 85"
      pattern: "check Phase 86 service operation expectations"
---

<objective>
Add a narrow deterministic Bun checker for Phase 86 and wire it into the repo-native verifier.

Purpose: Guard SVC-01 and SVC-02 documentation drift without becoming the broad Phase 88 production-claim scanner.
Output: Phase 86 checker tests, checker implementation, and `scripts/verify.sh` test/check commands after Phase 85.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-service-operation-expectations/86-CONTEXT.md
@.planning/phases/86-service-operation-expectations/86-RESEARCH.md
@.planning/phases/86-service-operation-expectations/86-01-SUMMARY.md
@.planning/phases/86-service-operation-expectations/86-02-SUMMARY.md
@scripts/check-phase85-operator-runbooks.ts
@scripts/check-phase85-operator-runbooks.test.ts
@scripts/verify.sh
@docs/parity/service-operation-expectations.md
@docs/parity/index.json
@docs/parity/checklist.md

<interfaces>
Create this exported checker contract:
```typescript
export function checkPhase86ServiceOperationExpectations(
  maybeRepoRoot = process.env.OPEN_BITCOIN_PHASE86_REPO_ROOT,
): string[];
```

Use these constants:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE86_REPO_ROOT";
const SURFACE_ID = "v1-8-service-operation-expectations";
const AUDIT_KEY = "v1_8_service_operation_expectations";
const PHASE86_REQUIREMENTS = ["SVC-01", "SVC-02"] as const;
const SERVICE_DOC_PATH = "docs/parity/service-operation-expectations.md";
const PHASE85_CHECKER_COMMAND =
  "bun run scripts/check-phase85-operator-runbooks.ts";
const PHASE86_TEST_COMMAND =
  "bun test scripts/check-phase86-service-operation-expectations.test.ts";
const PHASE86_CHECKER_COMMAND =
  "bun run scripts/check-phase86-service-operation-expectations.ts";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 86 checker fixture tests</name>
  <files>scripts/check-phase86-service-operation-expectations.test.ts</files>
  <read_first>
    <file>scripts/check-phase85-operator-runbooks.test.ts</file>
    <file>scripts/check-phase85-operator-runbooks.ts</file>
    <file>docs/parity/service-operation-expectations.md</file>
    <file>docs/parity/index.json</file>
    <file>docs/parity/checklist.md</file>
    <file>scripts/verify.sh</file>
  </read_first>
  <behavior>
    - Test valid fixture roots and executed verifier order pass.
    - Test missing support terms or service lifecycle labels fail.
    - Test missing required command group or missing Cargo/Bazel build path fails.
    - Test treating artifact existence, service file existence, daemon startup, elapsed time, raw log tail, public peer reachability, or support bundle path as proof fails.
    - Test missing restart/resume field fails.
    - Test default verifier mutation drift fails.
    - Test sensitive support artifacts or automatic upload permission fails.
    - Test missing parity roots, SVC requirements, canonical evidence, checker evidence, or verifier evidence fails.
    - Test pointer docs duplicating the service classification table fail.
    - Test Phase 86 commands only in the `VERIFY_COMMAND_ORDER` heredoc fail.
    - Test executable verifier text containing forbidden service-manager, public-network, long-wall-clock, package-manager, Windows service, automatic upload, production ownership, or broad readiness strings fails.
  </behavior>
  <action>Following the Phase 85 test pattern, create `scripts/check-phase86-service-operation-expectations.test.ts`. Use Bun's `test`, `expect`, `afterEach`, temporary fixture roots, and Arrange/Act/Assert comments in every test. Define fixture text containing the final Phase 86 headings, support terms, service surfaces, command forms, lifecycle labels, restart/resume fields, parity roots, human pointers, sensitive evidence boundaries, and verifier order. Include test names exactly: `passes_when_phase86_fixture_contains_service_roots_and_verify_order`, `fails_when_service_doc_omits_support_term_or_lifecycle_label`, `fails_when_service_doc_omits_required_command_group_or_build_path`, `fails_when_service_doc_treats_artifact_existence_as_evidence`, `fails_when_service_doc_omits_restart_resume_field`, `fails_when_service_doc_allows_mutating_default_verification`, `fails_when_service_doc_allows_sensitive_support_artifacts_or_upload`, `fails_when_parity_roots_omit_service_requirement_or_evidence`, `fails_when_pointer_docs_duplicate_service_table`, `fails_when_phase86_verify_commands_exist_only_in_legacy_heredoc`, and `fails_when_default_verify_contains_service_manager_network_or_production_drift`. Per D-17 and D-19, tests must stay fixed-file and deterministic; do not execute operator commands, public-network commands, service-manager commands, package-manager service commands, Windows service commands, support upload commands, or sleeps.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "OPEN_BITCOIN_PHASE86_REPO_ROOT|passes_when_phase86_fixture_contains_service_roots_and_verify_order|fails_when_service_doc_omits_support_term_or_lifecycle_label|fails_when_service_doc_omits_required_command_group_or_build_path|fails_when_service_doc_treats_artifact_existence_as_evidence|fails_when_service_doc_omits_restart_resume_field|fails_when_service_doc_allows_mutating_default_verification|fails_when_service_doc_allows_sensitive_support_artifacts_or_upload|fails_when_parity_roots_omit_service_requirement_or_evidence|fails_when_pointer_docs_duplicate_service_table|fails_when_phase86_verify_commands_exist_only_in_legacy_heredoc|fails_when_default_verify_contains_service_manager_network_or_production_drift|supported|preview|opt-in UAT|unsupported|deferred|unmanaged|installed-stopped|running|failed|disabled|unavailable-manager|service.restart_resume|same_datadir|prior_shutdown|durable_progress|stale_inflight|recovery_category|next_action|// Arrange|// Act|// Assert" scripts/check-phase86-service-operation-expectations.test.ts; set +e; bun test scripts/check-phase86-service-operation-expectations.test.ts; status="$?"; set -e; if [[ "$status" -eq 0 ]]; then echo "expected RED failure before checker implementation" >&2; exit 1; fi; git diff --check -- scripts/check-phase86-service-operation-expectations.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase86-service-operation-expectations.test.ts` exists.
    - The file imports `checkPhase86ServiceOperationExpectations` from `./check-phase86-service-operation-expectations`.
    - The file contains all eleven exact test names listed in the action.
    - The file contains `// Arrange`, `// Act`, and `// Assert`.
    - The initial RED run fails only because the checker module does not exist or is not implemented yet; after Task 2 and Task 3, the same command passes.
  </acceptance_criteria>
  <done>Phase 86 checker behavior is specified with focused Bun fixture tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement narrow Phase 86 checker</name>
  <files>scripts/check-phase86-service-operation-expectations.ts</files>
  <read_first>
    <file>scripts/check-phase86-service-operation-expectations.test.ts</file>
    <file>scripts/check-phase85-operator-runbooks.ts</file>
    <file>docs/parity/service-operation-expectations.md</file>
    <file>docs/parity/index.json</file>
    <file>docs/parity/checklist.md</file>
    <file>docs/parity/README.md</file>
    <file>README.md</file>
    <file>docs/operator/runtime-guide.md</file>
    <file>docs/parity/catalog/operator-runtime-release-hardening.md</file>
  </read_first>
  <behavior>
    - `checkPhase86ServiceOperationExpectations()` returns `[]` when all required Phase 86 docs, roots, links, fields, and verifier commands exist after Task 3 wires `scripts/verify.sh`.
    - The checker returns clear failure strings for missing service document headings, support terms, service surfaces, command groups, command prefixes, lifecycle labels, restart/resume fields, field-based evidence rules, sensitive support boundaries, parity roots, human pointers, and verifier order.
    - The checker rejects forbidden Phase 86 default-verifier drift without scanning unrelated repository files.
  </behavior>
  <action>Create `scripts/check-phase86-service-operation-expectations.ts` following the Phase 85 checker structure: fixed target-file list, `readText`, pure validation helpers, exported `checkPhase86ServiceOperationExpectations`, CLI failure reporting, and `import.meta.main`. Per D-17, validate only Phase 86 targets: `docs/parity/service-operation-expectations.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`, `docs/parity/operator-runbooks.md`, `docs/parity/upgrade-and-rollback-policy.md`, `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/operator-runtime-release-hardening.md`, and `scripts/verify.sh`. Validate required headings, exact support terms, exact service lifecycle labels, exact restart/resume fields, exact SVC requirements, `v1-8-service-operation-expectations`, `v1_8_service_operation_expectations`, required evidence roots, human pointer links, non-duplication of the service table outside `docs/parity/service-operation-expectations.md`, and the required command prefixes from the `<interfaces>` block. Validate command groups contain the exact Cargo/Bazel direct daemon, status, sync status, service preview, service install, service install `--apply`, service start, service status, service restart, service stop, service disable, service uninstall, service uninstall `--apply`, and support bundle command forms from Plan 86-01. Validate field-based evidence rejects artifact-existence proof by checking for the required insufficient-proof terms: `service file existence`, `daemon startup`, `elapsed time`, `raw log tail`, `public peer reachability`, `support bundle path`, `expected fields and unavailable reasons`, `do not prove durable resume`, and `Unavailable: &lt;reason&gt;`. Validate sensitive support boundaries include `wallet private material`, `raw wallet files`, `RPC cookies`, `rpcpassword`, `rpcauth`, `raw datadirs`, `unredacted logs`, `raw unbounded logs`, `automatic support-bundle upload`, and `production service ownership`. Include verifier-order validation that strips the `VERIFY_COMMAND_ORDER` heredoc before confirming executed Phase 86 test and checker commands occur after `bun run scripts/check-phase85-operator-runbooks.ts`; Task 2 fixture tests may validate this behavior before Task 3 wires the real verifier, but the real-repository CLI run belongs to Task 3. Per D-19, reject these exact forbidden strings in executable verifier text: `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, `--restart-after-progress`, `brew services`, `Windows service`, `automatic support-bundle upload`, `production service ownership`, and `broad production-node readiness`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; bun test scripts/check-phase86-service-operation-expectations.test.ts; bun --check scripts/check-phase86-service-operation-expectations.ts; rg -n "OPEN_BITCOIN_PHASE86_REPO_ROOT|v1-8-service-operation-expectations|v1_8_service_operation_expectations|SVC-01|SVC-02|docs/parity/service-operation-expectations.md|checkPhase86ServiceOperationExpectations|VERIFY_COMMAND_ORDER|service.restart_resume|same_datadir|prior_shutdown|durable_progress|stale_inflight|recovery_category|next_action|run-live-mainnet-smoke|systemctl|launchctl|sleep 259200|--restart-after-progress|brew services|Windows service|automatic support-bundle upload|production service ownership|broad production-node readiness" scripts/check-phase86-service-operation-expectations.ts; git diff --check -- scripts/check-phase86-service-operation-expectations.ts scripts/check-phase86-service-operation-expectations.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase86-service-operation-expectations.ts` exists and exports `checkPhase86ServiceOperationExpectations`.
    - `bun test scripts/check-phase86-service-operation-expectations.test.ts` passes.
    - `bun --check scripts/check-phase86-service-operation-expectations.ts` passes.
    - `bun run scripts/check-phase86-service-operation-expectations.ts` is deferred to Task 3 after `scripts/verify.sh` has real Phase 86 wiring.
    - The checker target-file list does not include broad repository globs.
    - The checker does not execute any operator command from documentation.
  </acceptance_criteria>
  <done>Phase 86 deterministic checker passes fixture drift tests and validates the planned document set once verifier wiring exists.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 86 checker into scripts/verify.sh</name>
  <files>scripts/verify.sh</files>
  <read_first>
    <file>scripts/verify.sh</file>
    <file>scripts/check-phase86-service-operation-expectations.ts</file>
    <file>scripts/check-phase86-service-operation-expectations.test.ts</file>
    <file>scripts/check-phase85-operator-runbooks.ts</file>
  </read_first>
  <action>Per D-18, wire the Phase 86 test and checker immediately after Phase 85 in both the visible `VERIFY_COMMAND_ORDER` block and the executed `run_step` sequence. Add exactly these command lines to the visible order after `bun run scripts/check-phase85-operator-runbooks.ts`: `bun test scripts/check-phase86-service-operation-expectations.test.ts` and `bun run scripts/check-phase86-service-operation-expectations.ts`. Add exactly these executed steps after `run_step "check Phase 85 operator runbooks" bun run scripts/check-phase85-operator-runbooks.ts`: `run_step "test Phase 86 service operation expectations checker" bun test scripts/check-phase86-service-operation-expectations.test.ts` and `run_step "check Phase 86 service operation expectations" bun run scripts/check-phase86-service-operation-expectations.ts`. Do not add public-network, real service-manager, package-manager service, Windows service, support-upload, multi-day, or operator runtime command execution to `scripts/verify.sh`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "bun test scripts/check-phase86-service-operation-expectations.test.ts|bun run scripts/check-phase86-service-operation-expectations.ts|test Phase 86 service operation expectations checker|check Phase 86 service operation expectations" scripts/verify.sh; bun test scripts/check-phase86-service-operation-expectations.test.ts; bun run scripts/check-phase86-service-operation-expectations.ts; git diff --check -- scripts/verify.sh scripts/check-phase86-service-operation-expectations.ts scripts/check-phase86-service-operation-expectations.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` visible command-order block contains the Phase 86 test and checker after Phase 85.
    - `scripts/verify.sh` executed `run_step` sequence contains the Phase 86 test and checker after Phase 85.
    - `bun run scripts/check-phase86-service-operation-expectations.ts` validates executed order, not only the heredoc order.
    - `scripts/verify.sh` does not add executable `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, `--restart-after-progress`, `brew services`, `Windows service`, `automatic support-bundle upload`, `production service ownership`, or `broad production-node readiness`.
  </acceptance_criteria>
  <done>Default verification includes the narrow Phase 86 checker while preserving deterministic verifier boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files to checker | Markdown, JSON, and shell text are parsed as fixed local inputs. |
| Checker to verifier | Checker failures control whether `bash scripts/verify.sh` passes. |
| Verifier to local environment | Default verifier must not mutate service managers, contact public peers, sleep for multi-day windows, or upload support artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-11 | Tampering | `docs/parity/index.json` and human roots | mitigate | Checker validates exact surface id, audit key, SVC requirements, canonical evidence roots, checker evidence, and human pointer links. |
| T-86-12 | Repudiation | Service evidence sufficiency | mitigate | Checker requires field-level evidence, lifecycle labels, restart/resume fields, unavailable reasons, and insufficient-proof language. |
| T-86-13 | Information Disclosure | Support evidence boundaries | mitigate | Checker requires sensitive artifact exclusions and automatic-upload non-claim language in Phase 86 target docs. |
| T-86-14 | Denial of Service | `scripts/verify.sh` | mitigate | Checker validates executed verifier order and rejects public-network, real-service-manager, package-manager service, Windows service, support-upload, and multi-day default command strings. |
| T-86-15 | Elevation of Privilege | Checker scope | mitigate | Fixed target-file list avoids a broad Phase 88 scanner and avoids executing operator commands from documentation. |
</threat_model>

<verification>
Run focused verification:

```bash
bun test scripts/check-phase86-service-operation-expectations.test.ts
bun --check scripts/check-phase86-service-operation-expectations.ts
bun run scripts/check-phase86-service-operation-expectations.ts
git diff --check -- scripts/check-phase86-service-operation-expectations.ts scripts/check-phase86-service-operation-expectations.test.ts scripts/verify.sh
```
</verification>

<success_criteria>
- Phase 86 checker tests pass.
- Phase 86 checker passes against the real repository state.
- `scripts/verify.sh` executes Phase 86 after Phase 85.
- The checker remains narrow to Phase 86 and does not become the Phase 88 broad claim scanner.
- Default verification remains deterministic, public-network-free, real-service-manager-free, package-manager-service-free, Windows-service-free, support-upload-free, and multi-day-free.
</success_criteria>

<output>
After completion, create `.planning/phases/86-service-operation-expectations/86-03-SUMMARY.md`.
</output>
