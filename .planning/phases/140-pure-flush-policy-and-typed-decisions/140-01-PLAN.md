---
phase: 140-pure-flush-policy-and-typed-decisions
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-chainstate/src/coins.rs
  - packages/open-bitcoin-chainstate/src/coins/flush.rs
  - packages/open-bitcoin-chainstate/src/coins/tests.rs
  - packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FLUSH-01
  - MGR-03
user_setup: []
must_haves:
  truths:
    - "Contributors can construct FlushPolicyInput from injected cache bytes, limits, leftover, FlushPolicyTime now/next_write, memory_pressure, disk-free bytes, and entry count without Instant::now() or disk I/O."
    - "None-mode decide_flush classifies OK at the 90% threshold on a 50 MiB budget, LARGE one byte over that threshold, and CRITICAL when cache_bytes exceed total space."
    - "None-mode decide_flush classifies OK at 180 MiB and at 190 MiB on a 200 MiB budget, and LARGE only one byte over total-minus-10-MiB headroom."
    - "None mode still classifies cache size and never returns RefuseDiskSpace, even when disk_free_bytes is 0."
    - "open-bitcoin-chainstate coins/flush.rs stays I/O-free: no Fjall, std::fs, Tokio, Instant, or SystemTime."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/coins/flush.rs
      provides: "FlushMode, FlushPolicyInput, FlushDecision, classify_cache_size, decide_flush skeleton"
      contains: "pub fn decide_flush"
    - path: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      provides: "None-mode classification and purity unit tests"
      contains: "none_mode_classifies_ok_at_180_mib_on_200_mib_budget"
    - path: docs/parity/source-breadcrumbs.json
      provides: "chainstate-engine mapping for flush sources"
      contains: "packages/open-bitcoin-chainstate/src/coins/flush.rs"
  key_links:
    - from: packages/open-bitcoin-chainstate/src/coins.rs
      to: packages/open-bitcoin-chainstate/src/coins/flush.rs
      via: "mod flush plus public re-exports"
      pattern: "mod flush;"
    - from: packages/open-bitcoin-chainstate/src/coins/flush.rs
      to: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      via: "None-mode decide_flush returns classified cache_size"
      pattern: "fn decide_flush"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 140-2026-09-02T02-14-08
generated_at: 2026-09-02T03:19:02.000Z
---

# Phase 140 Plan 01: Flush Types, Cache-Size Classification, and decide_flush Skeleton

<objective>
Create the I/O-free flush-policy vocabulary in `coins/flush.rs` and prove Knots `GetCoinsCacheSizeState` math through `FlushMode::None` `decide_flush` outcomes.

Purpose: FLUSH-01 and MGR-03 start with typed injected facts. Plan 02 cannot lock Flush vs Sync until OK / LARGE / CRITICAL and the 90% vs 10 MiB `max` are unit-tested (D-01 through D-10, D-23).

Output: `coins/flush.rs` types, `pub(crate) classify_cache_size`, a `decide_flush` skeleton that always returns `FlushDecision::None` with classified facts, flush unit tests, and parity breadcrumbs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
@packages/open-bitcoin-chainstate/src/coins.rs
@packages/open-bitcoin-chainstate/src/lib.rs
@packages/open-bitcoin-chainstate/src/error.rs
@docs/parity/source-breadcrumbs.json
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-chainstate/src/coins.rs — add `mod flush;` next to `mod cache;` / `mod memory;`. Do not change `CoinsView`, `CoinsCache`, or `CoinsCacheEntry`.

From packages/open-bitcoin-chainstate/src/error.rs — do not add disk-refusal as `MissingCoin` or any new apply error. Refusal is a `FlushDecision` variant in Plan 02.

From packages/open-bitcoin-mempool/src/context.rs — copy the *shape* only; do not import mempool:
```rust
pub struct PolicyTime(i64);
impl PolicyTime {
    pub const fn new(unix_seconds: i64) -> Self;
    pub const fn from_unix_seconds(unix_seconds: i64) -> Self;
    pub const fn unix_seconds(self) -> i64;
}
```

Target contracts this plan must create (executor writes these exact names per D-01..D-10, D-23 and RESEARCH discretion):
```rust
pub const LARGE_CACHE_NUMERATOR: u64 = 9;
pub const LARGE_CACHE_DENOMINATOR: u64 = 10;
pub const LARGE_CACHE_HEADROOM_BYTES: u64 = 10 * 1024 * 1024;
pub const COIN_WRITE_GUARD_BYTES_PER_ENTRY: u64 = 48 * 2 * 2; // 192

pub struct FlushPolicyTime(u64); // unix seconds; no Instant::now()

pub enum FlushMode { None, IfNeeded, Periodic, Always }

pub enum CoinsCacheSizeState { Ok = 0, Large = 1, Critical = 2 }

pub enum LastFlushReason { None, Needed, Periodic, Always, FailedDisk }

pub struct FlushDecisionFacts {
    pub cache_size: CoinsCacheSizeState,
    pub reason: LastFlushReason,
}

pub enum FlushDecision {
    None(FlushDecisionFacts),
    Flush(FlushDecisionFacts),
    Sync(FlushDecisionFacts),
    RefuseDiskSpace(FlushDecisionFacts),
}

pub struct FlushPolicyInput {
    pub mode: FlushMode,
    pub cache_bytes: u64,
    pub cache_byte_limit: u64,
    pub mempool_leftover_bytes: u64,
    pub now: FlushPolicyTime,
    pub next_write: FlushPolicyTime,
    pub memory_pressure: bool,
    pub disk_free_bytes: u64,
    pub cache_entry_count: u64,
}

pub fn decide_flush(input: FlushPolicyInput) -> FlushDecision;
pub(crate) fn classify_cache_size(
    cache_bytes: u64,
    cache_byte_limit: u64,
    mempool_leftover_bytes: u64,
) -> CoinsCacheSizeState;
```

Plan 01 `decide_flush` skeleton (replace in Plan 02): classify, then always return `FlushDecision::None(FlushDecisionFacts { cache_size, reason: LastFlushReason::None })`. Do not implement IfNeeded / Periodic / Always write-kind or disk refusal here.

Do not create `RecoveryDecision` or `decide_recovery` in this plan (Plan 03).
Do not add `empty_cache: bool` or `disk_ok: bool` (D-02, D-05).
Do not pin `450 * 1024 * 1024`, `4 * 1024 * 1024`, or `8 * 1024 * 1024` (D-10).
Do not add `50 * 1024 * 1024` / `52428800` (known Knots `CheckDiskSpace` floor difference; D-09 is Plan 02).
</interfaces>
</context>

<feature>
  <name>Injected-fact cache-size classification</name>
  <files>packages/open-bitcoin-chainstate/src/coins/flush.rs, packages/open-bitcoin-chainstate/src/coins/tests/flush.rs</files>
  <behavior>
    Shell injects occupancy bytes. Core computes total = cache_limit + leftover, CRITICAL when cache_bytes &gt; total, LARGE when cache_bytes &gt; max((9 * total) / 10, total - 10 MiB) using strict `&gt;`, otherwise OK. FlushMode::None decide_flush returns FlushDecision::None with that classification and LastFlushReason::None.
  </behavior>
  <implementation>
    Shared-payload FlushDecision enum. classify_cache_size is pub(crate). decide_flush skeleton always returns None so Plan 02 can TDD the Knots write-kind matrix against a compiling API.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing None-mode classification and purity tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/coins/tests.rs
    packages/open-bitcoin-chainstate/src/lib.rs
    docs/parity/source-breadcrumbs.json
    standards/core/testing.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    - Test `none_mode_classifies_ok_at_small_budget_ninety_percent_threshold`: `FlushPolicyInput` with `mode=FlushMode::None`, `cache_byte_limit=50 * 1024 * 1024`, `mempool_leftover_bytes=0`, `cache_bytes=45 * 1024 * 1024`, `disk_free_bytes=0`, `cache_entry_count=10`. `decide_flush` returns `FlushDecision::None`; `cache_size() == CoinsCacheSizeState::Ok`; `reason() == LastFlushReason::None`.
    - Test `none_mode_classifies_large_one_byte_over_small_budget_ninety_percent`: same budget, `cache_bytes=45 * 1024 * 1024 + 1` → `CoinsCacheSizeState::Large` on `FlushDecision::None`.
    - Test `none_mode_classifies_critical_when_cache_bytes_exceed_total`: same budget, `cache_bytes=50 * 1024 * 1024 + 1` → `CoinsCacheSizeState::Critical` on `FlushDecision::None`.
    - Test `none_mode_classifies_ok_at_180_mib_on_200_mib_budget`: `cache_byte_limit=200 * 1024 * 1024`, `cache_bytes=180 * 1024 * 1024` → `Ok` (90% must not win; headroom is 190 MiB).
    - Test `none_mode_classifies_ok_at_exactly_total_minus_10_mib_on_200_mib_budget`: `cache_bytes=190 * 1024 * 1024` on 200 MiB budget → `Ok` (strict `>`).
    - Test `none_mode_classifies_large_one_byte_over_headroom_on_200_mib_budget`: `cache_bytes=190 * 1024 * 1024 + 1` → `Large`.
    - Test `none_mode_adds_mempool_leftover_to_total_space`: `cache_byte_limit=40 * 1024 * 1024`, `mempool_leftover_bytes=10 * 1024 * 1024`, `cache_bytes=45 * 1024 * 1024 + 1` → `Large` (total is 50 MiB).
    - Test `none_mode_never_refuses_disk_even_when_free_bytes_are_zero`: `FlushMode::None`, `CoinsCacheSizeState::Critical` occupancy, `disk_free_bytes=0`, `cache_entry_count=10` → `FlushDecision::None`, never `RefuseDiskSpace` (D-17).
    - Test `classify_does_not_panic_when_limit_and_leftover_are_u64_max`: `cache_byte_limit=u64::MAX`, `mempool_leftover_bytes=u64::MAX`, `cache_bytes=u64::MAX`, `FlushMode::None` → returns some `FlushDecision` without panic (saturating / `u128` 90% math).
    - Test `flush_policy_constants_are_9_10_10_mib_and_192`: `LARGE_CACHE_NUMERATOR == 9`, `LARGE_CACHE_DENOMINATOR == 10`, `LARGE_CACHE_HEADROOM_BYTES == 10 * 1024 * 1024`, `COIN_WRITE_GUARD_BYTES_PER_ENTRY == 48 * 2 * 2`.
    - Test `flush_policy_module_source_has_no_clock_fs_or_fjall`: `include_str!("../flush.rs")` must not contain `Instant`, `SystemTime`, `std::fs`, `fjall`, or `tokio`.
  </behavior>
  <action>
    Create the module skeleton so tests compile and fail on behavior (RED):

    1. Add `mod flush;` to `packages/open-bitcoin-chainstate/src/coins.rs` immediately after `mod memory;`. Re-export from `coins.rs` (not `lib.rs` — Plan 03 owns crate-root exports): `FlushDecision`, `FlushDecisionFacts`, `FlushMode`, `FlushPolicyInput`, `FlushPolicyTime`, `LastFlushReason`, `CoinsCacheSizeState`, `decide_flush`, `LARGE_CACHE_NUMERATOR`, `LARGE_CACHE_DENOMINATOR`, `LARGE_CACHE_HEADROOM_BYTES`, `COIN_WRITE_GUARD_BYTES_PER_ENTRY`.
    2. Create `packages/open-bitcoin-chainstate/src/coins/flush.rs` with the breadcrumb block already used on `coins.rs`:
    ```
    // Parity breadcrumbs:
    // - packages/bitcoin-knots/src/coins.h
    // - packages/bitcoin-knots/src/coins.cpp
    // - packages/bitcoin-knots/src/validation.cpp
    // - packages/bitcoin-knots/src/node/blockstorage.cpp
    // - packages/bitcoin-knots/src/node/chainstate.cpp
    ```
    3. Add `mod flush;` to `packages/open-bitcoin-chainstate/src/coins/tests.rs` next to `mod algebra;`. Create `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` with the same breadcrumb block. Tests use Arrange / Act / Assert comments and call `crate::coins::decide_flush`.
    4. Append these two paths to the existing `chainstate-engine` `files` array in `docs/parity/source-breadcrumbs.json` immediately after `packages/open-bitcoin-chainstate/src/coins/tests/algebra.rs`. Do not create a new breadcrumb group. Do not add `txdb.cpp`. Then run `bun run scripts/check-parity-breadcrumbs.ts --write` if the checker requires generated comment refresh.
    5. Declare the types, constants, `FlushPolicyTime::from_unix_seconds(u64)` / `unix_seconds()`, `FlushPolicyInput::periodic_due()` as `self.now >= self.next_write`, and `FlushDecision::cache_size()` / `reason()` accessors that read the shared `FlushDecisionFacts` on every variant (D-01, D-03). Derive `PartialOrd, Ord` on `CoinsCacheSizeState` with `Ok = 0, Large = 1, Critical = 2`.
    6. Stub `classify_cache_size` / `decide_flush` so tests compile and fail: return `CoinsCacheSizeState::Ok` / `FlushDecision::None` with `Ok` for every input, or return a dedicated placeholder that is not the expected LARGE/CRITICAL. Do not use `todo!()` / `unwrap()` in production (crate `deny(clippy::unimplemented)`).
    7. Do not implement IfNeeded / Periodic / Always write-kind, `disk_guard_fails`, `RecoveryDecision`, `ReplayBlocks`, `FlushForPrune`, or `PruneAndFlush` (D-19, D-20). Do not edit `error.rs`, `coins/cache.rs` `flush()`/`sync()`, `engine.rs`, `packages/open-bitcoin-node/src/chainstate.rs`, or `packages/open-bitcoin-node/src/sync/runtime_state.rs` (D-22). Do not add Cargo dependencies. Do not import `open_bitcoin_mempool`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-01-flush-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-chainstate/src/coins.rs` contains `mod flush;` and `pub use flush::`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `pub enum FlushMode`, `pub enum FlushDecision`, `pub struct FlushPolicyInput`, `pub fn decide_flush`, `pub(crate) fn classify_cache_size`, `LARGE_CACHE_HEADROOM_BYTES`, and `COIN_WRITE_GUARD_BYTES_PER_ENTRY`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` does not contain `empty_cache`, `disk_ok`, `Instant`, `SystemTime`, `std::fs`, `fjall`, `tokio`, `FlushForPrune`, `450 * 1024 * 1024`, or `52428800`
    - `packages/open-bitcoin-chainstate/src/coins/tests.rs` contains `mod flush;`
    - `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` contains `none_mode_classifies_ok_at_180_mib_on_200_mib_budget` and `none_mode_never_refuses_disk_even_when_free_bytes_are_zero`
    - `docs/parity/source-breadcrumbs.json` `chainstate-engine` files list includes `packages/open-bitcoin-chainstate/src/coins/flush.rs` and `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs`
    - `packages/open-bitcoin-chainstate/src/lib.rs` still has no `FlushDecision` / `decide_flush` in its `pub use coins::` list (Plan 03)
    - Task 1 cargo test for `coins::tests::flush` is RED (failures, not compile errors) before Task 2
  </acceptance_criteria>
  <done>Failing None-mode classification tests exist against the named public types; breadcrumbs are registered; crate-root exports and recovery types are not added yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement classify_cache_size and the None-returning decide_flush skeleton</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
  </files>
  <read_first>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
    standards/core/architecture.md
    standards/core/code-shape.md
  </read_first>
  <behavior>
    Same tests as Task 1 must pass. `classify_cache_size` uses `total = cache_byte_limit.saturating_add(mempool_leftover_bytes)`, CRITICAL iff `cache_bytes > total`, LARGE iff `cache_bytes > ninety.max(headroom)` where `ninety = u64::try_from(u128::from(total) * 9 / 10).unwrap_or(u64::MAX)` and `headroom = total.saturating_sub(LARGE_CACHE_HEADROOM_BYTES)`.
  </behavior>
  <action>
    Implement (GREEN) per D-04, D-06, D-07, D-08, D-10, D-23:

    **`classify_cache_size` (D-04, D-06):** `pub(crate)`, not re-exported from `coins.rs`. Pin constants exactly: `LARGE_CACHE_NUMERATOR = 9`, `LARGE_CACHE_DENOMINATOR = 10`, `LARGE_CACHE_HEADROOM_BYTES = 10 * 1024 * 1024`, `COIN_WRITE_GUARD_BYTES_PER_ENTRY = 48 * 2 * 2`. Do not multiply `9 * total` on `u64`. Leftover is `u64` so it is already `>= 0`; do not inject `max_mempool` / usage. Do not read an allocator.

    **`FlushPolicyTime` (D-07):** newtype `u64` unix seconds with `from_unix_seconds` / `unix_seconds`. Core must not call `Instant::now()` or `SystemTime::now()`. `periodic_due` is `now >= next_write` on `FlushPolicyInput`; do not store last-flush plus 50–70 minute bounds.

    **`decide_flush` skeleton:** call `classify_cache_size(input.cache_bytes, input.cache_byte_limit, input.mempool_leftover_bytes)` then return only `FlushDecision::None(FlushDecisionFacts { cache_size, reason: LastFlushReason::None })`. Ignore `mode`, `memory_pressure`, `disk_free_bytes`, and `cache_entry_count` in this plan so Plan 02 can TDD the Knots boolean split. Do not add `disk_guard_fails` yet.

    **Illegal-state rules:** `FlushDecision` variants are exclusive (D-02). No `empty_cache: bool` (D-05). No `FlushForPrune` (D-19). No `unwrap()` in production; `u128` → `u64` uses `try_from(...).unwrap_or(u64::MAX)` only.

    Keep each new production file under 628 physical lines. After GREEN: `rg -n "Instant|SystemTime|std::fs|fjall|tokio|empty_cache|disk_ok|FlushForPrune|52428800|450 \\* 1024" packages/open-bitcoin-chainstate/src/coins/flush.rs` prints nothing.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-01-flush-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush` exits 0
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `saturating_add` and `u128::from(total)` and does not contain `9 * total` as a `u64` product
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `pub(crate) fn classify_cache_size` and does not contain `pub fn classify_cache_size`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` `fn decide_flush` body returns only `FlushDecision::None` (no `FlushDecision::Flush`, `FlushDecision::Sync`, or `RefuseDiskSpace` construction yet)
    - `rg -n "Instant|SystemTime|std::fs|fjall|tokio" packages/open-bitcoin-chainstate/src/coins/flush.rs` prints nothing
    - `bash scripts/check-pure-core-deps.sh` exits 0
    - `bun run scripts/check-parity-breadcrumbs.ts --check` exits 0
  </acceptance_criteria>
  <done>None-mode classification is green: 50 MiB 90% bind, 200 MiB headroom bind, leftover adds to total, low disk never refuses under None, no clock/I/O in flush.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell → `decide_flush` | Occupancy, time, disk-free, and pressure facts are untrusted primitives that must be parsed into `FlushPolicyTime` / `u64` / enums. |
| Pure-core crate → I/O | Fjall, filesystem, Tokio, or clocks in `open-bitcoin-chainstate` would break the functional-core boundary. |
| Classification → later write-kind | Overflow or `>=` instead of `>` can turn CRITICAL/LARGE into OK and skip a required Flush in Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Tampering | `classify_cache_size` | mitigate | Use `u128` 90% math and saturating add; unit tests include `u64::MAX` leftover and strict `>` thresholds (D-06). |
| T-140-02 | Information Disclosure | `decide_flush` | mitigate | Inject `FlushPolicyTime`; forbid `Instant` / `SystemTime` / `std::fs` / `fjall` in `flush.rs` (D-07, D-23). Test `flush_policy_module_source_has_no_clock_fs_or_fjall`. |
| T-140-03 | Elevation of Privilege | `open-bitcoin-chainstate` flush module | mitigate | No Fjall/`std::fs`/Tokio imports. `check-pure-core-deps.sh` after edits. |
| T-140-04 | Denial of Service | cache-size math | mitigate | Saturating arithmetic so huge injected limits cannot panic the crate (`classify_does_not_panic_when_limit_and_leftover_are_u64_max`). |
| T-140-05 | Spoofing | leftover persist | accept | Plan 01 does not touch `ManagedChainstate::persist`; Plan 03 owns the no-wiring guard (D-22). |
</threat_model>

<verification>
`bun run scripts/command-timings.ts run --key phase140-01-flush-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush`
`bash scripts/check-pure-core-deps.sh`
`bun run scripts/check-parity-breadcrumbs.ts --check`
Do not start a second Cargo job against the same target directory.
</verification>

<success_criteria>
- `FlushMode`, `FlushPolicyInput`, `FlushDecision`, and `classify_cache_size` exist in `coins/flush.rs`.
- None-mode `decide_flush` reports OK / LARGE / CRITICAL from injected bytes using Knots `max(90%, total - 10 MiB)` with strict `>`.
- No clock, Fjall, filesystem, `empty_cache`, or prune flush in the new module.
- `lib.rs` crate-root exports, recovery types, and leftover persist files are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/140-pure-flush-policy-and-typed-decisions/140-01-SUMMARY.md`
</output>
