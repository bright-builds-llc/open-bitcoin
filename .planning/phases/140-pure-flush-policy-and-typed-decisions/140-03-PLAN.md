---
phase: 140-pure-flush-policy-and-typed-decisions
plan: "03"
type: tdd
wave: 3
depends_on:
  - "02"
files_modified:
  - packages/open-bitcoin-chainstate/src/coins.rs
  - packages/open-bitcoin-chainstate/src/coins/flush.rs
  - packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
  - packages/open-bitcoin-chainstate/src/lib.rs
autonomous: true
requirements:
  - MGR-03
user_setup: []
must_haves:
  truths:
    - "decide_recovery maps head-marker count 0/1/2/other to ConsistentEmptyHeads / OneHead / InterruptedTwoHeads / InconsistentOtherCount without hashes or I/O."
    - "Crate-root open-bitcoin-chainstate re-exports FlushDecision, decide_flush, RecoveryDecision, and decide_recovery next to CoinsCache."
    - "ManagedChainstate::persist still calls save_snapshot and does not call decide_flush."
    - "DurableSyncRuntime::persist_progress still writes save_chainstate_snapshot and does not call decide_flush."
    - "flush.rs still has no ReplayBlocks, BlockHash, Instant, std::fs, Fjall, or FlushForPrune."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/coins/flush.rs
      provides: "Count-only RecoveryDecision sketch"
      contains: "pub fn decide_recovery"
    - path: packages/open-bitcoin-chainstate/src/lib.rs
      provides: "Crate-root flush and recovery re-exports"
      contains: "decide_recovery"
    - path: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      provides: "Recovery cardinality and leftover-persist wiring tests"
      contains: "decide_recovery_one_head_is_first_class"
  key_links:
    - from: packages/open-bitcoin-chainstate/src/lib.rs
      to: packages/open-bitcoin-chainstate/src/coins/flush.rs
      via: "pub use coins::{..., RecoveryDecision, decide_flush, decide_recovery}"
      pattern: "pub use coins::"
    - from: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      to: packages/open-bitcoin-node/src/chainstate.rs
      via: "include_str persist still save_snapshot"
      pattern: "managed_chainstate_persist_still_writes_snapshot_and_does_not_call_decide_flush"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 140-2026-09-02T02-14-08
generated_at: 2026-09-02T03:19:02.000Z
---

# Phase 140 Plan 03: RecoveryDecision Sketch, Crate Exports, and Leftover-Persist Guard

<objective>
Add the I/O-free `RecoveryDecision` count sketch, export the flush/recovery machine at the crate root, and prove leftover snapshot persist is not retargeted.

Purpose: MGR-03 is a typed machine, not flush-only. Phase 141 owns `head_blocks` encoding; Phase 142 owns replay versus fail-closed. This plan must not pretend persist is flush-policy durability (D-20, D-21, D-22).

Output: `decide_recovery`, crate-root re-exports, recovery + persist-guard tests. No `ReplayBlocks`, no Fjall coins view, no node persist edits.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
@packages/open-bitcoin-chainstate/src/lib.rs
@packages/open-bitcoin-chainstate/src/coins.rs
@packages/open-bitcoin-chainstate/src/coins/flush.rs
@packages/open-bitcoin-node/src/chainstate.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@standards/core/architecture.md
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-chainstate/src/lib.rs after Plan 01 (do not drop existing coins exports):
```rust
pub use coins::{
    CoinsBatch, CoinsCache, CoinsCacheEntry, CoinsCacheFlags, CoinsView, MemoryCoinsView,
};
```

This plan extends that `pub use` with exactly:
`COIN_WRITE_GUARD_BYTES_PER_ENTRY`, `LARGE_CACHE_DENOMINATOR`, `LARGE_CACHE_HEADROOM_BYTES`, `LARGE_CACHE_NUMERATOR`, `CoinsCacheSizeState`, `FlushDecision`, `FlushDecisionFacts`, `FlushMode`, `FlushPolicyInput`, `FlushPolicyTime`, `LastFlushReason`, `RecoveryDecision`, `decide_flush`, `decide_recovery`.

Target recovery contracts (D-20; one-element is first-class, not lumped into other):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum RecoveryDecision {
    ConsistentEmptyHeads,
    OneHead,
    InterruptedTwoHeads,
    InconsistentOtherCount { count: usize },
}

pub fn decide_recovery(head_marker_count: usize) -> RecoveryDecision {
    match head_marker_count {
        0 => RecoveryDecision::ConsistentEmptyHeads,
        1 => RecoveryDecision::OneHead,
        2 => RecoveryDecision::InterruptedTwoHeads,
        count => RecoveryDecision::InconsistentOtherCount { count },
    }
}
```

Do not take `BlockHash`, undo bytes, or a replay plan. Do not resolve replay versus fail-closed (D-21).

Read-only leftover persist seams (D-22) — **do not edit these files**:
```249:251:packages/open-bitcoin-node/src/chainstate.rs
    fn persist(&mut self) {
        self.store.save_snapshot(self.chainstate.snapshot());
    }
```
```88:101:packages/open-bitcoin-node/src/sync/runtime_state.rs
    pub(super) fn persist_progress(&self) -> Result<(), SyncRuntimeError> {
        // ...
        self.store
            .save_chainstate_snapshot(&chainstate_snapshot, self.config.persist_mode)?;
        // ...
    }
```
</interfaces>
</context>

<feature>
  <name>Count-only recovery sketch and persist-wiring guard</name>
  <files>packages/open-bitcoin-chainstate/src/coins/flush.rs, packages/open-bitcoin-chainstate/src/lib.rs, packages/open-bitcoin-chainstate/src/coins/tests/flush.rs</files>
  <behavior>
    decide_recovery is a pure count match. Crate root re-exports the flush/recovery types. Node persist functions still write snapshot blobs and do not mention decide_flush.
  </behavior>
  <implementation>
    Add RecoveryDecision next to FlushDecision in flush.rs. Guard leftover persist with include_str! tests. Do not implement ReplayBlocks.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing recovery-count and leftover-persist guard tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/lib.rs
  </files>
  <read_first>
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/lib.rs
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `decide_recovery_zero_heads_is_consistent_empty`: `decide_recovery(0) == RecoveryDecision::ConsistentEmptyHeads`.
    - Test `decide_recovery_one_head_is_first_class`: `decide_recovery(1) == RecoveryDecision::OneHead` (D-20; must not be `InconsistentOtherCount { count: 1 }`).
    - Test `decide_recovery_two_heads_is_interrupted`: `decide_recovery(2) == RecoveryDecision::InterruptedTwoHeads`.
    - Test `decide_recovery_three_heads_is_inconsistent_other_count`: `decide_recovery(3) == RecoveryDecision::InconsistentOtherCount { count: 3 }`.
    - Test `decide_recovery_does_not_mention_block_hash_or_replay_in_source`: `include_str!("../flush.rs")` must not contain `ReplayBlocks`, `BlockHash`, `PruneAndFlush`, or `FlushForPrune`.
    - Test `crate_root_reexports_flush_and_recovery_types`: construct `crate::FlushPolicyTime::from_unix_seconds(0)`, `crate::decide_flush` with `crate::FlushMode::None`, and `crate::decide_recovery(0)` without going through `crate::coins::`.
    - Test `managed_chainstate_persist_still_writes_snapshot_and_does_not_call_decide_flush`: `include_str!("../../../../open-bitcoin-node/src/chainstate.rs")` contains `fn persist(` and `save_snapshot` and does not contain `decide_flush` or `RefuseDiskSpace`.
    - Test `durable_sync_persist_progress_still_writes_snapshot_and_does_not_call_decide_flush`: `include_str!("../../../../open-bitcoin-node/src/sync/runtime_state.rs")` contains `fn persist_progress` and `save_chainstate_snapshot` and does not contain `decide_flush` or `FlushMode`.
  </behavior>
  <action>
    1. Declare `RecoveryDecision` and a compiling `decide_recovery` stub in `coins/flush.rs` that returns `ConsistentEmptyHeads` for every count so tests compile and the 1/2/3 cases fail (RED). Do not take a `BlockHash` argument. Do not implement undo replay or fail-closed I/O (D-20, D-21).
    2. Re-export `RecoveryDecision` and `decide_recovery` from `coins.rs` (`pub use flush::...`). Do **not** add crate-root `lib.rs` exports in this task so `crate_root_reexports_flush_and_recovery_types` fails until Task 2.
    3. Append the named tests to `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` with Arrange / Act / Assert comments.
    4. Do not edit `packages/open-bitcoin-node/src/chainstate.rs` or `packages/open-bitcoin-node/src/sync/runtime_state.rs` (D-22). Read them only. Do not add `FjallCoinsView`, `head_blocks` encoding, or operator evidence.
    5. Do not use `unwrap()` in production. Do not add Cargo dependencies.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-03-recovery-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `pub enum RecoveryDecision` with `ConsistentEmptyHeads`, `OneHead`, `InterruptedTwoHeads`, and `InconsistentOtherCount`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `pub fn decide_recovery`
    - `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` contains `decide_recovery_one_head_is_first_class` and `managed_chainstate_persist_still_writes_snapshot_and_does_not_call_decide_flush`
    - `packages/open-bitcoin-chainstate/src/lib.rs` `pub use coins::` list still omits `RecoveryDecision` and `decide_recovery` before Task 2
    - `packages/open-bitcoin-node/src/chainstate.rs` still contains `self.store.save_snapshot(self.chainstate.snapshot());` and does not contain `decide_flush`
    - Task 1 cargo test for `coins::tests::flush` is RED (recovery count + crate-root export failures, not compile errors)
  </acceptance_criteria>
  <done>Failing recovery-count, crate-root export, and leftover-persist guard tests exist; node persist files are unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decide_recovery and crate-root re-exports</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/lib.rs
  </files>
  <read_first>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    packages/open-bitcoin-chainstate/src/lib.rs
    packages/open-bitcoin-chainstate/src/coins.rs
    standards/core/architecture.md
  </read_first>
  <behavior>
    Same tests as Task 1 must pass. decide_recovery is the four-arm count match. lib.rs re-exports flush and recovery types next to CoinsCache.
  </behavior>
  <action>
    Implement (GREEN) per D-20, D-21, D-22, D-23:

    1. Replace the `decide_recovery` stub with the exact match: `0 → ConsistentEmptyHeads`, `1 → OneHead`, `2 → InterruptedTwoHeads`, `_ → InconsistentOtherCount { count }`. No hashes, no undo, no I/O, no replay-versus-fail-closed comment that claims a chosen rule beyond naming the variants (D-21).
    2. Extend `packages/open-bitcoin-chainstate/src/lib.rs` `pub use coins::{...}` to include `COIN_WRITE_GUARD_BYTES_PER_ENTRY`, `LARGE_CACHE_DENOMINATOR`, `LARGE_CACHE_HEADROOM_BYTES`, `LARGE_CACHE_NUMERATOR`, `CoinsCacheSizeState`, `FlushDecision`, `FlushDecisionFacts`, `FlushMode`, `FlushPolicyInput`, `FlushPolicyTime`, `LastFlushReason`, `RecoveryDecision`, `decide_flush`, `decide_recovery` while keeping the existing `CoinsBatch` / `CoinsCache` / `CoinsView` / `MemoryCoinsView` exports.
    3. Keep `classify_cache_size` crate-private (not in `lib.rs`).
    4. Do not change `decide_flush` mapping from Plan 02. Do not edit node persist files. Do not add `ReplayBlocks`, `FjallCoinsView`, or `std::fs`.
    5. After GREEN run these read-only greps (must match / must be empty as stated):
       - `rg -n "fn persist\\(" -A 2 packages/open-bitcoin-node/src/chainstate.rs` shows `save_snapshot`
       - `rg -n "decide_flush|RefuseDiskSpace|FlushMode" packages/open-bitcoin-node/src/chainstate.rs packages/open-bitcoin-node/src/sync/runtime_state.rs` prints nothing
       - `rg -n "ReplayBlocks|BlockHash|FlushForPrune|Instant|SystemTime|std::fs|fjall|tokio" packages/open-bitcoin-chainstate/src/coins/flush.rs` prints nothing

    Keep `flush.rs` under 628 physical lines. No `unwrap()` in production.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-03-recovery-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush` exits 0
    - `packages/open-bitcoin-chainstate/src/lib.rs` contains `RecoveryDecision`, `decide_flush`, and `decide_recovery` inside `pub use coins::`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` `fn decide_recovery` matches `0`, `1`, `2`, and `count`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` does not contain `ReplayBlocks`, `BlockHash`, `FlushForPrune`, `Instant`, `std::fs`, or `fjall`
    - `packages/open-bitcoin-node/src/chainstate.rs` still contains `self.store.save_snapshot(self.chainstate.snapshot());`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs` still contains `save_chainstate_snapshot`
    - `rg -n "decide_flush" packages/open-bitcoin-node/src/chainstate.rs packages/open-bitcoin-node/src/sync/runtime_state.rs` prints nothing
    - `bash scripts/check-pure-core-deps.sh` exits 0
  </acceptance_criteria>
  <done>RecoveryDecision is a count-only sketch at the crate root; leftover persist still writes snapshots and is not a flush-policy proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injected marker count → `RecoveryDecision` | Count is an untrusted fact; the sketch must not invent hashes, undo, or a consistent tip. |
| Decision crate → leftover persist | Treating `save_snapshot` as flush execution would spoof durability. |
| Recovery sketch → replay I/O | Implementing `ReplayBlocks` here would pull fail-closed I/O into core before `head_blocks` exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-12 | Spoofing | `ManagedChainstate::persist` | mitigate | Do not retarget persist (D-22). Test `managed_chainstate_persist_still_writes_snapshot_and_does_not_call_decide_flush` plus `rg` empty for `decide_flush` in node persist files. |
| T-140-13 | Tampering | `decide_recovery` | mitigate | Count-only variants; one-element is first-class (D-20). No `BlockHash` / `ReplayBlocks` in `flush.rs`. |
| T-140-14 | Repudiation | replay vs fail-closed | accept | Rule stays unresolved until Phase 142 (D-21). Sketch names facts only. |
| T-140-15 | Tampering | prune / unlink | mitigate | Keep `FlushForPrune` absent; test source grep. |
| T-140-16 | Information Disclosure | `open-bitcoin-chainstate` | mitigate | No Fjall/`std::fs`/clock in `flush.rs`; `check-pure-core-deps.sh`. |
| T-140-17 | Elevation of Privilege | cache occupancy as durability | mitigate | Persist guard documents that a snapshot write is not a flush-policy proof (D-22). |
</threat_model>

<verification>
`bun run scripts/command-timings.ts run --key phase140-03-recovery-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush`
`rg -n "decide_flush" packages/open-bitcoin-node/src/chainstate.rs packages/open-bitcoin-node/src/sync/runtime_state.rs`
`bash scripts/check-pure-core-deps.sh`
Do not start a second Cargo job against the same target directory.
Do not modify leftover persist files to make tests pass.
</verification>

<success_criteria>
- `decide_recovery` distinguishes empty / one / two / other marker counts.
- Crate root exports flush and recovery types.
- Node persist functions still write leftover snapshots and do not call `decide_flush`.
- No ReplayBlocks, Fjall coins view, or prune flush.
</success_criteria>

<output>
After completion, create `.planning/phases/140-pure-flush-policy-and-typed-decisions/140-03-SUMMARY.md`
</output>
