---
phase: 140-pure-flush-policy-and-typed-decisions
plan: "02"
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-chainstate/src/coins/flush.rs
  - packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
autonomous: true
requirements:
  - FLUSH-01
user_setup: []
must_haves:
  truths:
    - "Always mode returns Flush with LastFlushReason::Always, or RefuseDiskSpace with FailedDisk when free_bytes is below 192 * entry_count."
    - "Periodic + LARGE or CRITICAL returns Flush even when periodic_due is false; Periodic + OK + due returns Sync; Periodic + OK + not due returns None."
    - "IfNeeded + CRITICAL, or IfNeeded + memory_pressure, returns Flush with LastFlushReason::Needed."
    - "IfNeeded + LARGE without CRITICAL or pressure returns None even when disk_free_bytes is 0."
    - "Disk-space refusal is checked only after a write would otherwise happen and replaces Flush or Sync; it never coexists with a write."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/coins/flush.rs
      provides: "Knots FlushStateToDisk mapping and coins-write disk guard"
      contains: "fn disk_guard_fails"
    - path: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      provides: "Mode × cache × pressure × due × disk matrix"
      contains: "ifneeded_large_without_pressure_returns_none"
  key_links:
    - from: packages/open-bitcoin-chainstate/src/coins/flush.rs
      to: packages/open-bitcoin-chainstate/src/coins/flush.rs
      via: "decide_flush classifies, picks intended write, then maybe RefuseDiskSpace"
      pattern: "RefuseDiskSpace"
    - from: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      to: packages/open-bitcoin-chainstate/src/coins/flush.rs
      via: "IfNeeded LARGE is not a write trigger"
      pattern: "ifneeded_large_without_pressure_returns_none"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 140-2026-09-02T02-14-08
generated_at: 2026-09-02T03:19:02.000Z
---

# Phase 140 Plan 02: Knots Flush, Sync, and Disk-Refusal Matrix

<objective>
Replace the Plan 01 `decide_flush` skeleton with the pinned Knots `FlushStateToDisk` boolean split and first-class `RefuseDiskSpace`.

Purpose: FLUSH-01 is the decision surface only. Phase 142 must execute Flush vs Sync without re-choosing write-kind. The planner-critical trap is treating LARGE as a universal write trigger (D-11 through D-19).

Output: GREEN matrix tests plus `maybe_intended_write` / `disk_guard_fails` inside `coins/flush.rs`. No persist wiring, no prune flush, no 50 MiB `CheckDiskSpace` floor.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
@.planning/phases/140-pure-flush-policy-and-typed-decisions/140-01-PLAN.md
@packages/open-bitcoin-chainstate/src/coins/flush.rs
@packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 01 already exported these from `coins.rs`. Do not rename them.

Knots `FlushStateToDisk` with `fFlushForPrune` forced false (D-11, D-19):
```
fCacheLarge    = mode == Periodic && cache_size >= Large   // LARGE and CRITICAL
fCacheCritical = mode == IfNeeded && (Critical || memory_pressure)
fPeriodicWrite = mode == Periodic && now >= next_write
should_write   = Always || fCacheLarge || fCacheCritical || fPeriodicWrite
empty_cache    = Always || fCacheLarge || fCacheCritical
write          = empty_cache ? Flush : Sync
```
Do not add an `empty_cache: bool` field. Write-kind is the enum variant (D-05).

Disk guard (D-09, not full Knots `CheckDiskSpace`):
```rust
fn disk_guard_fails(disk_free_bytes: u64, cache_entry_count: u64) -> bool {
    let required = COIN_WRITE_GUARD_BYTES_PER_ENTRY.saturating_mul(cache_entry_count);
    disk_free_bytes < required
}
```
Known Knots difference: do **not** add `50 * 1024 * 1024` / `52428800`. Refuse iff `free_bytes < 192 * entry_count`.

Locked outcomes (reason on the shared `FlushDecisionFacts`):

| Mode | Cache | Pressure | periodic_due | Disk | Outcome | Reason |
|------|-------|----------|--------------|------|---------|--------|
| None | any | any | any | any | `None` | `None` |
| Always | any | any | any | ok | `Flush` | `Always` |
| Always | any | any | any | fail | `RefuseDiskSpace` | `FailedDisk` |
| Periodic | Large or Critical | any | false | ok | `Flush` | `Periodic` |
| Periodic | Ok | any | true | ok | `Sync` | `Periodic` |
| Periodic | Ok | any | false | any | `None` | `None` |
| Periodic | Ok | any | true | fail | `RefuseDiskSpace` | `FailedDisk` |
| IfNeeded | Critical | any | any | ok | `Flush` | `Needed` |
| IfNeeded | Ok or Large | true | any | ok | `Flush` | `Needed` |
| IfNeeded | Large | false | any | any | `None` | `None` |
| IfNeeded | Ok | false | any | any | `None` | `None` |

`decide_flush` shape after this plan (D-01, D-02, D-18):
```rust
pub fn decide_flush(input: FlushPolicyInput) -> FlushDecision {
    let cache_size = classify_cache_size(...);
    let Some((kind, reason)) = maybe_intended_write(...) else {
        return FlushDecision::None(FlushDecisionFacts { cache_size, reason: LastFlushReason::None });
    };
    if disk_guard_fails(input.disk_free_bytes, input.cache_entry_count) {
        return FlushDecision::RefuseDiskSpace(FlushDecisionFacts {
            cache_size,
            reason: LastFlushReason::FailedDisk,
        });
    }
    match kind {
        FlushWriteKind::Flush => FlushDecision::Flush(FlushDecisionFacts { cache_size, reason }),
        FlushWriteKind::Sync => FlushDecision::Sync(FlushDecisionFacts { cache_size, reason }),
    }
}
```
`FlushWriteKind` is private. `RefuseDiskSpace` cannot carry a write (D-02).
</interfaces>
</context>

<feature>
  <name>Knots FlushStateToDisk decision matrix</name>
  <files>packages/open-bitcoin-chainstate/src/coins/flush.rs, packages/open-bitcoin-chainstate/src/coins/tests/flush.rs</files>
  <behavior>
    decide_flush returns one FlushDecision. Periodic LARGE/CRITICAL flushes even when not due. IfNeeded LARGE without pressure is None. Disk refusal replaces an intended Flush or Sync and is skipped when no write is intended.
  </behavior>
  <implementation>
    Private maybe_intended_write plus disk_guard_fails after should-write. Keep Plan 01 None-mode classification tests passing.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Flush, Sync, and RefuseDiskSpace matrix tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
  </files>
  <read_first>
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-CONTEXT.md
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    Shared fixture helper in the test module: `cache_byte_limit = 50 * 1024 * 1024`, `mempool_leftover_bytes = 0`, `now` / `next_write` via `FlushPolicyTime::from_unix_seconds`. LARGE occupancy = `45 * 1024 * 1024 + 1`. CRITICAL occupancy = `50 * 1024 * 1024 + 1`. OK occupancy = `0`. Plenty of disk = `u64::MAX`. Guard-fail disk for `cache_entry_count = 10` = `192 * 10 - 1` (`1919`). Guard-pass disk for 10 entries = `1920`.
    - Test `always_mode_returns_flush_with_reason_always`: `FlushMode::Always`, OK cache, `memory_pressure=false`, `now=0`, `next_write=1`, disk plenty → `FlushDecision::Flush`, `reason() == LastFlushReason::Always`, `cache_size() == Ok`.
    - Test `always_mode_refuses_disk_when_free_below_192_times_entries`: Always, OK cache, `cache_entry_count=10`, `disk_free_bytes=1919` → `FlushDecision::RefuseDiskSpace`, `reason() == LastFlushReason::FailedDisk`. `disk_free_bytes=1920` in a sibling assertion or dedicated test `always_mode_writes_flush_when_free_equals_192_times_entries` → `Flush` (strict `<`).
    - Test `periodic_large_returns_flush_even_when_not_due`: `FlushMode::Periodic`, LARGE cache, `now=0`, `next_write=1` (`periodic_due=false`), disk plenty → `Flush`, `LastFlushReason::Periodic` (D-13, `fCacheLarge` Periodic-only).
    - Test `periodic_critical_returns_flush_even_when_not_due`: Periodic, CRITICAL cache, not due, disk plenty → `Flush`, `Periodic`.
    - Test `periodic_ok_due_returns_sync`: Periodic, OK cache, `now=100`, `next_write=100` → `FlushDecision::Sync`, `LastFlushReason::Periodic` (D-14).
    - Test `periodic_ok_not_due_returns_none`: Periodic, OK cache, `now=99`, `next_write=100` → `FlushDecision::None`, `LastFlushReason::None`.
    - Test `periodic_ok_due_refuses_disk_instead_of_sync`: Periodic, OK, due, `cache_entry_count=10`, `disk_free_bytes=1919` → `RefuseDiskSpace`, `FailedDisk` (D-18).
    - Test `ifneeded_critical_returns_flush_with_reason_needed`: `FlushMode::IfNeeded`, CRITICAL, `memory_pressure=false`, disk plenty → `Flush`, `LastFlushReason::Needed` (D-15).
    - Test `ifneeded_ok_with_memory_pressure_returns_flush`: IfNeeded, OK cache, `memory_pressure=true` → `Flush`, `Needed` (D-08, D-15).
    - Test `ifneeded_large_without_pressure_returns_none`: IfNeeded, LARGE, `memory_pressure=false`, disk plenty → `FlushDecision::None`, `LastFlushReason::None` (D-16). This test must exist; do not name anything `large_always_flushes`.
    - Test `ifneeded_large_with_low_disk_still_returns_none`: IfNeeded, LARGE, no pressure, `disk_free_bytes=0`, `cache_entry_count=10` → `None`, never `RefuseDiskSpace` (D-09 order).
    - Test `none_mode_critical_with_pressure_and_low_disk_still_returns_none`: `FlushMode::None`, CRITICAL, `memory_pressure=true`, `disk_free_bytes=0` → `None` (D-17). Keep Plan 01 classification tests unchanged.
  </behavior>
  <action>
    Append the named tests to `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` with Arrange / Act / Assert comments. Use `crate::coins::{decide_flush, FlushDecision, FlushMode, FlushPolicyInput, FlushPolicyTime, LastFlushReason, CoinsCacheSizeState, COIN_WRITE_GUARD_BYTES_PER_ENTRY}`.

    Do not implement `maybe_intended_write` or change `decide_flush` in this task. Plan 01 skeleton still returns `FlushDecision::None` for every mode, so these new tests must compile and fail (RED).

    Do not add `ReplayBlocks`, `RecoveryDecision`, `FlushForPrune`, `50 * 1024 * 1024`, or node persist edits. Do not change `coins/cache.rs` `flush()` / `sync()` algebra.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-02-matrix-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` contains `ifneeded_large_without_pressure_returns_none`, `periodic_large_returns_flush_even_when_not_due`, `periodic_ok_due_returns_sync`, `always_mode_refuses_disk_when_free_below_192_times_entries`, and `ifneeded_large_with_low_disk_still_returns_none`
    - `packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` does not contain `large_always_flushes`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` still constructs only `FlushDecision::None` inside `decide_flush` before Task 2
    - Task 1 cargo test for `coins::tests::flush` is RED (new matrix tests fail; Plan 01 None-mode tests still pass)
  </acceptance_criteria>
  <done>Failing Knots mode-matrix tests exist against the Plan 01 API; decide_flush is still the None-only skeleton.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement maybe_intended_write and post-should-write disk refusal</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
  </files>
  <read_first>
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    .planning/phases/140-pure-flush-policy-and-typed-decisions/140-RESEARCH.md
    standards/core/code-shape.md
  </read_first>
  <behavior>
    All Plan 01 and Plan 02 flush tests pass. decide_flush never returns a write variant together with FailedDisk. None mode never refuses.
  </behavior>
  <action>
    Implement (GREEN) per D-01, D-02, D-05, D-09, D-11 through D-19:

    1. Add private `enum FlushWriteKind { Flush, Sync }`.
    2. Add private `maybe_intended_write(mode, cache_size, periodic_due, memory_pressure) -> Option<(FlushWriteKind, LastFlushReason)>` that encodes the Knots split verbatim:
       - `FlushMode::None` → `None`
       - `FlushMode::Always` → `Some((Flush, Always))` (D-12)
       - `FlushMode::Periodic` if `cache_size >= CoinsCacheSizeState::Large` → `Some((Flush, Periodic))` (D-13)
       - `FlushMode::Periodic` else if `periodic_due` → `Some((Sync, Periodic))` (D-14)
       - `FlushMode::IfNeeded` if `cache_size == Critical || memory_pressure` → `Some((Flush, Needed))` (D-15)
       - otherwise `None` (D-16)
    3. Add `fn disk_guard_fails(disk_free_bytes: u64, cache_entry_count: u64) -> bool` using `COIN_WRITE_GUARD_BYTES_PER_ENTRY.saturating_mul(cache_entry_count)` and `disk_free_bytes < required` (D-09). No `50 MiB` addend.
    4. Replace the Plan 01 `decide_flush` body with: classify → `let Some((kind, reason)) = maybe_intended_write(...) else { return FlushDecision::None(...) }` → if `disk_guard_fails` return `RefuseDiskSpace` with `LastFlushReason::FailedDisk` → else `Flush` or `Sync` (D-01, D-02, D-18). Use `let...else` for the no-write path. Use `input.periodic_due()` (already `now >= next_write`).
    5. Keep `classify_cache_size` `pub(crate)`. Keep accessors `cache_size()` / `reason()` on `FlushDecision`.
    6. Do not implement `FlushForPrune`, `PruneAndFlush`, file unlink, or a prune bool (D-19). Do not call `CoinsCache::flush` / `sync`. Do not edit `error.rs` to map refusal. Do not edit node `persist` / `persist_progress` (D-22). Do not use `unwrap()`.

    After GREEN: `rg -n "52428800|50 \\* 1024 \\* 1024|FlushForPrune|empty_cache|disk_ok" packages/open-bitcoin-chainstate/src/coins/flush.rs` prints nothing.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase140-02-matrix-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush` exits 0
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` contains `fn disk_guard_fails`, `FlushWriteKind`, `RefuseDiskSpace`, and `LastFlushReason::FailedDisk`
    - `packages/open-bitcoin-chainstate/src/coins/flush.rs` does not contain `52428800`, `50 * 1024 * 1024`, `FlushForPrune`, `empty_cache`, `disk_ok`, `Instant`, `fjall`, or `std::fs`
    - `rg -n "ifneeded_large_without_pressure_returns_none" packages/open-bitcoin-chainstate/src/coins/tests/flush.rs` prints a match
    - `bash scripts/check-pure-core-deps.sh` exits 0
  </acceptance_criteria>
  <done>decide_flush implements the Knots boolean split: Periodic-only LARGE flush, IfNeeded LARGE is None, disk refusal replaces an intended write only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intended write → disk guard | A low-disk fact must not refuse when no write is intended, and must not allow Flush/Sync when the guard fails. |
| Mode → write-kind | Adapters must not recompose Flush vs Sync; a wrong LARGE rule would flush after every nearly-full IfNeeded connect. |
| Policy → prune/unlink | Knots `fFlushForPrune` is adjacent in `FlushStateToDisk` and must stay unimplemented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-06 | Tampering | `maybe_intended_write` IfNeeded+LARGE | mitigate | `fCacheLarge` is Periodic-only. Test `ifneeded_large_without_pressure_returns_none` (D-16). |
| T-140-07 | Denial of Service | `disk_guard_fails` | mitigate | First-class `RefuseDiskSpace` after should-write (D-09, D-18). Tests cover Always and Periodic+due refusal, and IfNeeded+LARGE / None-mode non-refusal. |
| T-140-08 | Tampering | `FlushDecision` | mitigate | Exclusive enum: write and refuse cannot coexist; no `disk_ok` flag (D-02). |
| T-140-09 | Tampering | prune path | mitigate | Do not implement `FlushForPrune` / file unlink (D-19). Grep `FlushForPrune` empty in `flush.rs`. |
| T-140-10 | Spoofing | leftover persist | accept | This plan does not call `decide_flush` from `ManagedChainstate::persist` (D-22; Plan 03 guard). |
| T-140-11 | Information Disclosure | disk/clock in core | mitigate | Continue injecting `disk_free_bytes`; no `std::fs` (D-23). |
</threat_model>

<verification>
`bun run scripts/command-timings.ts run --key phase140-02-matrix-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::tests::flush`
`bash scripts/check-pure-core-deps.sh`
Do not start a second Cargo job against the same target directory.
</verification>

<success_criteria>
- Always → Flush or RefuseDiskSpace.
- Periodic LARGE/CRITICAL → Flush even when not due; Periodic OK due → Sync.
- IfNeeded LARGE without pressure → None; IfNeeded CRITICAL or pressure → Flush.
- Disk refusal runs only after should-write and uses `192 * entry_count` with no 50 MiB floor.
- FlushForPrune is absent. CoinsCache algebra and leftover persist are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/140-pure-flush-policy-and-typed-decisions/140-02-SUMMARY.md`
</output>
