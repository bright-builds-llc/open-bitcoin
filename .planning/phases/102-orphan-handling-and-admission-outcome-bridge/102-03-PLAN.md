---
phase: 102-orphan-handling-and-admission-outcome-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - 102-01
  - 102-02
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - DL-03
  - DL-04
  - DL-05
  - MEM-01
  - MEM-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 102-2026-06-30T14-54-50
generated_at: 2026-06-30T23:04:47Z
user_setup: []
must_haves:
  truths:
    - "Peer transactions pass through `PeerAction::ReceivedTransaction` and the Phase 101 download boundary before any mempool admission occurs."
    - "Managed runtime mutation is the only place peer transactions are submitted to mempool or staged as orphans."
    - "Peer submissions and the new `ManagedPeerNetwork::submit_local_transaction_outcome` method both receive the shared `MempoolOutcome` contract."
    - "Existing RPC callers keep using `ManagedPeerNetwork::submit_local_transaction` with its `AdmissionResult` return type."
    - "A missing-parent peer transaction is staged as an orphan and triggers scheduler-governed parent `getdata` actions."
    - "Accepted parents trigger bounded child reconsideration and stable accepted, still-missing, rejected, expired, and evicted evidence."
    - "Managed peer disconnect cleanup removes peer-owned orphanage state with `self.orphanage.cleanup_peer(peer_id)` while preserving Phase 101 transaction request cleanup."
    - "Rejected, duplicate, replaced, evicted, and expired outcomes update only the in-memory bridge/index state needed by Phase 102 and do not implement relay serving/fanout."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Managed runtime admission/orphan bridge"
      contains: "process_peer_transaction_admission"
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "Bridge field, `PeerAction::ReceivedTransaction` routing, and compatible local submission APIs"
      contains: "submit_local_transaction_outcome"
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "Managed disconnect orphan cleanup"
      contains: "disconnect_peer_at"
    - path: "packages/open-bitcoin-node/src/network/inventory.rs"
      provides: "Stored transaction removal helper for replaced/evicted outcomes"
      contains: "remove_stored_transactions"
    - path: "packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs"
      provides: "In-memory managed bridge tests for peer/local outcomes"
      contains: "managed_admission_bridge"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Existing RPC local transaction caller that remains compatible with `AdmissionResult`"
      contains: "submit_local_transaction"
  key_links:
    - from: "packages/open-bitcoin-node/src/network.rs"
      to: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      via: "`PeerAction::ReceivedTransaction` handler"
      pattern: "process_peer_transaction_admission"
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-mempool/src/outcome.rs"
      via: "shared `MempoolOutcome` matching"
      pattern: "MempoolOutcome::"
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      via: "orphan staging and reconsideration"
      pattern: "TxOrphanage"
    - from: "packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs"
      to: "packages/open-bitcoin-node/src/network.rs"
      via: "local outcome tests call the new compatibility-safe method"
      pattern: "submit_local_transaction_outcome"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/network.rs"
      via: "unchanged RPC-compatible `submit_local_transaction` path"
      pattern: "submit_local_transaction"
    - from: "packages/open-bitcoin-node/src/network/action_translation.rs"
      to: "packages/open-bitcoin-network/src/peer.rs"
      via: "parent requests translated through existing transaction relay action path"
      pattern: "process_transaction_relay_action"
    - from: "packages/open-bitcoin-node/src/network.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      via: "managed peer disconnect cleanup"
      pattern: "cleanup_peer"
---

<objective>
Wire the managed runtime bridge from transaction download to mempool outcomes and bounded orphan handling.

Purpose: Phase 102 must connect peer/local transaction submissions to mempool admission while preserving the functional-core/imperative-shell boundary: pure network decisions stay in `open-bitcoin-network`, pure mempool outcomes stay in `open-bitcoin-mempool`, and runtime mutation stays in `open-bitcoin-node`.
Output: managed admission bridge, local outcome API, peer orphan staging, scheduler-governed parent requests, child reconsideration, in-memory integration tests, and breadcrumbs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-CONTEXT.md
@.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-RESEARCH.md
@.planning/phases/101-transaction-inventory-identity-and-download-scheduling/101-02-SUMMARY.md
@.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-01-SUMMARY.md
@.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-02-SUMMARY.md
@packages/open-bitcoin-mempool/src/outcome.rs
@packages/open-bitcoin-mempool/src/pool.rs
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/transaction_relay.rs
@packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs
@packages/open-bitcoin-node/src/mempool.rs
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/network/action_translation.rs
@packages/open-bitcoin-node/src/network/inventory.rs
@packages/open-bitcoin-node/src/network/tests.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/open-bitcoin-rpc/src/dispatch/wallet.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
Expected outcome contract from Plan 01:

```rust
pub enum MempoolOutcome {
    Accepted { txid: Txid, wtxid: Wtxid, evicted: Vec<Txid> },
    Rejected { txid: Txid, wtxid: Wtxid, category: MempoolRejectionCategory },
    Duplicate { txid: Txid },
    Replaced { txid: Txid, wtxid: Wtxid, replaced: Vec<Txid>, evicted: Vec<Txid> },
    Orphaned { txid: Txid, wtxid: Wtxid, missing_parents: Vec<Txid> },
    Evicted { txid: Txid, wtxid: Wtxid },
    Expired { txid: Txid, wtxid: Wtxid },
}
```

Expected orphan contract from Plan 02:

```rust
pub struct TxOrphanage;
impl TxOrphanage {
    pub fn stage_missing_parent(&mut self, input: OrphanStageInput) -> Vec<OrphanAction>;
    pub fn reconsider_after_parent(&mut self, accepted_parent: TxRelayId, now_unix_seconds: i64) -> Vec<OrphanAction>;
    pub fn record_reconsideration_outcome(&mut self, wtxid: Wtxid, status: OrphanReconsiderationStatus) -> Vec<OrphanAction>;
    pub fn expire(&mut self, now_unix_seconds: i64) -> Vec<OrphanAction>;
    pub fn cleanup_peer(&mut self, peer_id: PeerId) -> Vec<OrphanAction>;
}
```

Current local transaction compatibility path to preserve:

```rust
pub fn submit_local_transaction(
    &mut self,
    transaction: Transaction,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<AdmissionResult, ManagedNetworkError>;
```

`open-bitcoin-rpc` currently calls this method and expects `AdmissionResult`; Plan 03 must add an outcome-returning method without changing that existing caller contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add managed admission bridge and local outcome submission</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/inventory.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    AGENTS.md
    standards/core/architecture.md
    standards/languages/rust.md
    .planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-CONTEXT.md
    .planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-RESEARCH.md
    packages/open-bitcoin-mempool/src/outcome.rs
    packages/open-bitcoin-network/src/peer.rs
    packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs
    packages/open-bitcoin-node/src/mempool.rs
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/wallet.rs
    docs/parity/source-breadcrumbs.json
  </read_first>
  <action>
    Add `mod admission_bridge;` to `packages/open-bitcoin-node/src/network.rs` and add `orphanage: TxOrphanage` to `ManagedPeerNetwork<S>`, initialized with `TxOrphanage::new(OrphanPolicy::default())` in both constructors.

    In the managed peer disconnect cleanup path, wire orphan cleanup directly on the shell-owned orphanage. Update `ManagedPeerNetwork::disconnect_peer_at(peer_id, timestamp)` and any helper it delegates to for `disconnect_peer` so the same path that clears Phase 101 transaction request state also calls `self.orphanage.cleanup_peer(peer_id)`. Place the call near the existing transaction cleanup behavior for disconnects, not in `PeerManager` or socket-facing code. The implementation must keep `PeerManager` and socket-facing code free of mempool/orphanage mutation, must not call mempool APIs from peer code, and must not translate cleanup into new socket writes. If `cleanup_peer` returns `OrphanAction` evidence, retain only the in-memory cleanup/evidence needed by Phase 102 and do not issue parent requests for a disconnected peer.

    Create `packages/open-bitcoin-node/src/network/admission_bridge.rs` with this exact parity breadcrumb block:

    ```rust
    // Parity breadcrumbs:
    // - packages/bitcoin-knots/src/net_processing.cpp
    // - packages/bitcoin-knots/src/node/txdownloadman_impl.cpp
    // - packages/bitcoin-knots/src/txorphanage.cpp
    // - packages/bitcoin-knots/src/validation.cpp
    // - packages/bitcoin-knots/test/functional/p2p_orphan_handling.py
    // - packages/bitcoin-knots/test/functional/mempool_accept.py
    ```

    Implement methods on `ManagedPeerNetwork<S>` inside the child module:
    - `pub(super) fn process_peer_transaction_admission(&mut self, peer_id: PeerId, transaction: Transaction, timestamp: i64, verify_flags: ScriptVerifyFlags, consensus_params: ConsensusParams) -> Result<ManagedAdmissionBridgeResult, ManagedNetworkError>`.
    - `pub(super) fn reconsider_orphans_after_acceptance(&mut self, accepted_parent: Txid, timestamp: i64, verify_flags: ScriptVerifyFlags, consensus_params: ConsensusParams) -> Result<ManagedAdmissionBridgeResult, ManagedNetworkError>`.
    - `pub fn expire_orphan_transactions(&mut self, now_unix_seconds: i64) -> Vec<MempoolOutcome>`.
    - `pub fn orphan_count(&self) -> usize`.

    Define `pub(super) struct ManagedAdmissionBridgeResult { pub outcome: MempoolOutcome, pub targeted_outbound: Vec<(PeerId, WireNetworkMessage)>, pub reconsidered: Vec<MempoolOutcome> }`.

    `process_peer_transaction_admission` must call `self.mempool.submit_transaction_outcome(...)` exactly once for the received transaction. It must:
    - store the transaction only for `MempoolOutcome::Accepted` and `MempoolOutcome::Replaced`;
    - remove replaced and evicted txids from in-memory transaction indexes before/after storing the accepted transaction;
    - for `MempoolOutcome::Orphaned`, call `self.orphanage.stage_missing_parent(...)`, then for every `OrphanAction::RequestParent` call `self.peer_manager.request_orphan_parent(peer_id, parent_txid, timestamp)` and translate returned `PeerAction::TransactionRelay` values through `process_transaction_relay_action`;
    - for `Rejected`, `Duplicate`, and `Evicted`, avoid storing the transaction and call `peer_manager.note_recent_reject(TxRelayId::Txid(txid))`; if wtxid exists, also call `note_recent_reject(TxRelayId::Wtxid(wtxid))`;
    - for `Accepted` and `Replaced`, call `reconsider_orphans_after_acceptance(txid, timestamp, verify_flags, consensus_params)` once and append the reconsidered outcomes and parent-request messages it returns. Do not recursively reconsider grandchildren in the same call beyond the orphanage work cap.

    `reconsider_orphans_after_acceptance` must call `self.orphanage.reconsider_after_parent(TxRelayId::Txid(accepted_parent), timestamp)`, admit each returned candidate once, record each child's `OrphanReconsiderationStatus`, store accepted/replaced children, remove rejected/evicted/expired children, and keep still-missing children staged. It must not call itself recursively from a child acceptance.

    In `packages/open-bitcoin-node/src/network/inventory.rs`, add `pub(super) fn remove_stored_transactions(&mut self, txids: &[Txid]) -> Result<(), ManagedNetworkError>` that removes matching `transactions_by_txid` and corresponding `transactions_by_wtxid` entries. Clean only the managed in-memory transaction indexes required for Phase 102 duplicate/mempool-known behavior when replaced or evicted outcomes occur. Do not implement relay-serving cache lifecycle; Phase 104 owns serving/fanout.

    Preserve the existing `ManagedPeerNetwork::submit_local_transaction(...) -> Result<AdmissionResult, ManagedNetworkError>` signature for current `open-bitcoin-rpc` callers. Do not edit RPC callers in this plan. Update the existing method body only as needed for Phase 102 index coherence: after a successful `AdmissionResult`, call `remove_stored_transactions` for `result.replaced` and `result.evicted`, store the accepted transaction, and return the original `AdmissionResult`. Missing-input compatibility stays as `ManagedNetworkError::Mempool(MempoolError::MissingInput { .. })` on this compatibility path.

    Add `pub fn submit_local_transaction_outcome(...) -> Result<MempoolOutcome, ManagedNetworkError>` on `ManagedPeerNetwork<S>`. It must use `submit_transaction_outcome`, store only accepted/replaced transactions, remove replaced/evicted managed in-memory indexes needed for Phase 102 duplicate/mempool-known behavior, and return the same outcome variants as peer submissions. Keep no orphan staging for local submissions in this phase unless the outcome is returned as `MempoolOutcome::Orphaned`; local RPC/operator mapping is Phase 105. Phase 102 bridge tests must call `submit_local_transaction_outcome` when they need the stable outcome contract.

    Update `docs/parity/source-breadcrumbs.json` so `packages/open-bitcoin-node/src/network/admission_bridge.rs` and `packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` are in the `node-network-adapter` group with the `txorphanage`, `validation.cpp`, `p2p_orphan_handling.py`, and `mempool_accept.py` anchors.

    Decision trace: implements D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-17, D-18, D-19, D-20, D-21, D-24.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features --no-run</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --no-run</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mod admission_bridge|orphanage: TxOrphanage|TxOrphanage::new|process_peer_transaction_admission|MempoolOutcome" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/admission_bridge.rs` finds bridge wiring.
    - `rg -n "pub fn submit_local_transaction\\(|AdmissionResult|pub fn submit_local_transaction_outcome\\(|MempoolOutcome|submit_transaction_outcome|remove_stored_transactions" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/admission_bridge.rs packages/open-bitcoin-node/src/network/inventory.rs` finds local compatibility, local outcome, and index cleanup.
    - `rg -n "submit_local_transaction|AdmissionResult" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/dispatch/node.rs packages/open-bitcoin-rpc/src/dispatch/wallet.rs` finds existing RPC callers still using the compatibility path.
    - `rg -n "RequestParent|request_orphan_parent|process_transaction_relay_action|note_recent_reject|reconsider_after_parent|record_reconsideration_outcome" packages/open-bitcoin-node/src/network/admission_bridge.rs` finds parent-request and reconsideration integration.
    - `rg -n "disconnect_peer_at|cleanup_peer\\(peer_id\\)|orphanage\\.cleanup_peer" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/admission_bridge.rs` finds managed disconnect orphan cleanup wired in the same shell-owned cleanup surface as transaction request cleanup.
    - `rg -n "relay fanout|rebroadcast|support bundle|RPC|public-network|service-manager|durable mempool" packages/open-bitcoin-node/src/network/admission_bridge.rs` returns no matches.
  </acceptance_criteria>
  <done>The managed runtime owns peer mempool mutation, local outcome submission through `submit_local_transaction_outcome`, RPC-compatible local submission through `submit_local_transaction`, orphan staging, parent request translation, and bounded reconsideration through stable outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove managed bridge behavior with in-memory integration tests</name>
  <files>packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-CONTEXT.md
    .planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-RESEARCH.md
    packages/open-bitcoin-mempool/src/outcome.rs
    packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/admission_bridge.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-rpc/src/context/network.rs
  </read_first>
  <action>
    Add `mod admission_bridge_cases;` to `packages/open-bitcoin-node/src/network/tests.rs` and create `packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` using existing helper patterns from `network/tests.rs`. Each non-trivial test must include `// Arrange`, `// Act`, and `// Assert` comments.

    Add exact test names:
    - `managed_admission_bridge_peer_tx_uses_download_boundary_before_mempool`: deliver `inv -> getdata -> tx`; assert the peer tx is requested through `GetData` before `Tx` admission and that no test calls mempool directly from peer/socket code.
    - `managed_admission_bridge_peer_missing_parent_stages_orphan_and_requests_parent`: peer sends child before parent; assert outcome `orphaned`, `orphan_count() == 1`, and targeted or outbound parent `getdata` uses `InventoryType::Transaction`.
    - `managed_admission_bridge_parent_acceptance_reconsiders_child`: stage child, accept parent, assert child is reconsidered and accepted when all parents are present.
    - `managed_admission_bridge_still_missing_parent_child_remains_staged`: child with two missing parents remains staged after only one parent is accepted.
    - `managed_admission_bridge_rejected_child_is_removed_from_orphanage`: staged child reconsidered after parent acceptance can return `rejected` and orphan count decreases.
    - `managed_admission_bridge_orphan_expiry_returns_expired_outcome_without_sleep`: call `expire_orphan_transactions(now + PHASE102_ORPHAN_TTL_SECONDS)` and assert `MempoolOutcome::Expired`.
    - `managed_admission_bridge_orphan_cap_eviction_returns_evicted_outcome`: use a tiny test policy if constructor injection exists, or stage enough deterministic orphans through a test-only bridge helper; assert `orphan_evicted`/`evicted` outcome without sleeping.
    - `managed_admission_bridge_duplicate_and_rejected_peer_txs_do_not_store_transaction`: duplicate and rejected peer submissions return typed outcomes and do not grow `transactions_by_txid`.
    - `managed_admission_bridge_replacement_removes_replaced_indexes`: successful replacement returns `replaced` and the replaced txid/wtxid are no longer served from the in-memory transaction store.
    - `managed_admission_bridge_disconnect_cleans_peer_orphans_and_request_state`: stage peer-owned orphans, create/observe Phase 101 transaction request state for that peer, call `ManagedPeerNetwork::disconnect_peer_at`, then assert the peer's staged orphans are removed by `self.orphanage.cleanup_peer(peer_id)` and existing transaction request cleanup still leaves no stale in-flight/request state.
    - `managed_admission_bridge_local_submission_uses_same_outcome_contract`: `submit_local_transaction_outcome` returns `accepted`, `duplicate`, `rejected`, and `orphaned` outcomes without exposing raw error display strings.
    - `managed_admission_bridge_existing_local_submission_preserves_admission_result_compatibility`: the existing `submit_local_transaction` method still returns `AdmissionResult` for accepted local submissions and remains source-compatible with the RPC context wrapper.
    - `managed_admission_bridge_resource_caps_preserved_under_orphan_burst`: repeated orphan parent requests hit scheduler request caps and produce `request_cap_reached` suppression/resource evidence rather than direct socket writes.

    If a test-only constructor is needed, add `#[cfg(test)] pub(super) fn with_orphan_policy(..., orphan_policy: OrphanPolicy)` in `admission_bridge.rs` rather than making production config larger. Do not add durable persistence, block connect/disconnect mempool lifecycle, relay serving/fanout, rebroadcast, RPC/operator/support surfaces, compact blocks, package relay, filters, public relay defaults, public-network CI, service-manager gates, sleeps, or production-funds wallet behavior.

    Decision trace: implements D-02, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-17, D-18, D-19, D-20, D-21, D-22.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>timeout 180s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_admission_bridge -- --nocapture</automated>
    <automated>timeout 120s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_network_transaction_relay -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mod admission_bridge_cases" packages/open-bitcoin-node/src/network/tests.rs` finds the child test module.
    - `rg -n "managed_admission_bridge_peer_tx_uses_download_boundary_before_mempool|managed_admission_bridge_peer_missing_parent_stages_orphan_and_requests_parent|managed_admission_bridge_parent_acceptance_reconsiders_child|managed_admission_bridge_local_submission_uses_same_outcome_contract|managed_admission_bridge_existing_local_submission_preserves_admission_result_compatibility" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` finds core bridge and local compatibility tests.
    - `rg -n "managed_admission_bridge_still_missing_parent_child_remains_staged|managed_admission_bridge_rejected_child_is_removed_from_orphanage|managed_admission_bridge_orphan_expiry_returns_expired_outcome_without_sleep|managed_admission_bridge_orphan_cap_eviction_returns_evicted_outcome|managed_admission_bridge_resource_caps_preserved_under_orphan_burst" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` finds orphan and resource tests.
    - `rg -n "managed_admission_bridge_disconnect_cleans_peer_orphans_and_request_state|disconnect_peer_at|cleanup_peer|in-flight|request state" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` finds the managed disconnect cleanup test proving peer-owned orphans and transaction request state are both cleaned up.
    - `rg -n "submit_local_transaction_outcome|submit_local_transaction|AdmissionResult" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs packages/open-bitcoin-rpc/src/context/network.rs` finds both the outcome test path and the unchanged compatibility path.
    - `rg -n "// Arrange|// Act|// Assert" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` finds AAA comments.
    - `rg -n "thread::sleep|SystemTime|public-network|service-manager|rebroadcast|relay fanout|compact block|package relay|support bundle" packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs` returns no matches.
  </acceptance_criteria>
  <done>Managed in-memory tests prove peer/local submissions flow through the stable outcome bridge, orphan staging, parent requests, reconsideration, expiry, eviction, managed disconnect orphan/request cleanup, and resource caps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| peer socket/PeerManager to managed bridge | Untrusted peer transactions cross from typed peer actions into shell-owned admission. |
| managed bridge to mempool core | Runtime submits transactions to pure mempool and receives `MempoolOutcome`. |
| managed bridge to network scheduler | Orphan parent requests cross back as typed scheduler actions and wire translations. |
| in-memory transaction store to future relay serving | Accepted/replaced/evicted indexes may affect duplicate suppression now and relay serving later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-102-03-01 | Elevation of Privilege | `PeerAction::ReceivedTransaction` handling | mitigate | Route peer txs only through `process_peer_transaction_admission` in `open-bitcoin-node`; do not add mempool calls to peer/socket code. |
| T-102-03-02 | Denial of Service | orphan burst bridge | mitigate | Use bounded `TxOrphanage` and scheduler-governed parent requests; test request caps under bursts. |
| T-102-03-03 | Information Disclosure | bridge outcomes/actions | mitigate | Keep evidence to fixed outcome/orphan labels; do not add raw tx hex, txid/wtxid, endpoints, permissions, credentials, logs, support, metrics, or RPC surfaces in this phase. |
| T-102-03-04 | Tampering | replacement/eviction index cleanup | mitigate | Remove replaced/evicted txids from in-memory transaction indexes and test stale store cleanup. |
| T-102-03-05 | Repudiation | local vs peer outcome consistency | mitigate | Preserve `submit_local_transaction` as the `AdmissionResult` compatibility path for RPC callers while adding `submit_local_transaction_outcome` for Phase 102 bridge tests and stable local outcome classification. |
| T-102-03-06 | Tampering | managed disconnect cleanup | mitigate | Call `self.orphanage.cleanup_peer(peer_id)` from the `ManagedPeerNetwork::disconnect_peer_at` cleanup path and test that peer-owned orphans and Phase 101 request state are both removed. |
</threat_model>

<verification>
Run focused checks during tasks, then run the full repo-native contract after this plan if executing standalone:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all --check
timeout 180s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_admission_bridge -- --nocapture
timeout 120s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_network_transaction_relay -- --nocapture
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-targets --all-features -- -D warnings
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --no-run
bun run scripts/check-parity-breadcrumbs.ts --check
bash scripts/verify.sh
```
</verification>

<success_criteria>
DL-03, DL-04, DL-05, MEM-01, and MEM-02 are connected end to end at the managed runtime seam: peer downloads reach mempool through typed outcomes, missing parents stage bounded orphans and request parents, accepted parents reconsider children, the new local outcome method uses the same contract, the existing local submission method stays compatible with RPC callers, and no peer/socket code mutates mempool directly.
</success_criteria>

<output>
After completion, create `.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-03-SUMMARY.md`.
</output>
