---
phase: 84-upgrade-and-rollback-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/upgrade-and-rollback-policy.md
autonomous: true
requirements:
  - UPG-01
  - UPG-02
  - UPG-03
user_setup: []
must_haves:
  truths:
    - "Operator can follow one source-built pre-upgrade checklist before changing binaries or runtime state."
    - "Operator can classify state and schema outcomes with the existing recovery vocabulary and action classes."
    - "Operator can follow failed-upgrade and rollback guidance without hidden source datadir, wallet, service, or config mutation."
  artifacts:
    - path: "docs/parity/upgrade-and-rollback-policy.md"
      provides: "Canonical Phase 84 upgrade and rollback policy"
      contains: "Surface id: `v1-8-upgrade-rollback-policy`"
  key_links:
    - from: "docs/parity/upgrade-and-rollback-policy.md"
      to: "docs/operator/runtime-guide.md"
      via: "operator command and support evidence pointers"
      pattern: "runtime-guide.md"
    - from: "docs/parity/upgrade-and-rollback-policy.md"
      to: "docs/architecture/status-snapshot.md"
      via: "state/schema compatibility vocabulary"
      pattern: "status-snapshot.md"
    - from: "docs/parity/upgrade-and-rollback-policy.md"
      to: "docs/architecture/storage-decision.md"
      via: "non-mutating recovery action-class boundary"
      pattern: "storage-decision.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 84-2026-06-21T21-33-46
generated_at: 2026-06-21T23:16:59.811Z
---

<objective>
Create the canonical Phase 84 upgrade and rollback policy under `docs/parity/`.

Purpose: Operators need one source-built policy for pre-upgrade, compatibility, failed-upgrade, rollback, backup, and mutation-boundary decisions.
Output: `docs/parity/upgrade-and-rollback-policy.md` with UPG-01, UPG-02, and UPG-03 content, using the locked Phase 84 decisions D-01 through D-12.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
@.planning/phases/84-upgrade-and-rollback-policy/84-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-01-SUMMARY.md
@.planning/phases/83-support-matrix-and-issue-evidence/83-01-SUMMARY.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-06-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@docs/parity/production-claim-boundary.md
@docs/parity/support-matrix.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/architecture/storage-decision.md
@docs/architecture/cli-command-architecture.md

<interfaces>
Existing recovery vocabulary to reuse exactly:

```text
sync.recovery_category labels:
- clean_shutdown
- unclean_shutdown
- incompatible_schema
- store_corruption
- storage_lock_contention

Recovery action classes:
- safe_retry
- read_only_inspection
- backup_then_rebuild
- stop_and_escalate

Recovery causes:
- schema_mismatch
- corruption_marker
- corrupt_record
- partial_write
- unreadable_namespace
- backend_open_failure
```

Required repo-local operator command prefixes:

```bash
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --
bazel run //packages/open-bitcoin-cli:open_bitcoin --
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create policy shell and pre-upgrade checklist</name>
  <files>docs/parity/upgrade-and-rollback-policy.md</files>
  <read_first>
    - .planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
    - .planning/phases/84-upgrade-and-rollback-policy/84-RESEARCH.md
    - docs/parity/production-claim-boundary.md
    - docs/parity/support-matrix.md
    - docs/operator/runtime-guide.md
    - docs/architecture/status-snapshot.md
    - docs/architecture/storage-decision.md
    - docs/architecture/cli-command-architecture.md
    - docs/parity/upgrade-and-rollback-policy.md (if it already exists)
  </read_first>
  <action>Create `docs/parity/upgrade-and-rollback-policy.md` per D-01 with title `# Upgrade And Rollback Policy`, the exact line `Surface id: \`v1-8-upgrade-rollback-policy\``, and an opening paragraph stating that v1.8 defines source-built upgrade boundaries and does not claim production full-node readiness. Add `## Scope And Non-Claims` with the exact Phase 82 support terms `supported`, `preview`, `opt-in UAT`, `unsupported`, and `deferred`. Add `## Pre-Upgrade Checklist` per D-02 through D-04 as a Markdown table with columns `Evidence to record`, `How to collect it`, `Mutation status`, and `Why it matters`. The table must include these exact evidence rows: `current source revision or commit`, `repo-local verification status`, `binary provenance from Cargo or Bazel`, `Open Bitcoin JSONC config path`, `bitcoin.conf path`, `selected datadir`, `datadir ownership and free-space review`, `current sync/status evidence`, `support-bundle evidence when available`, `service state`, `wallet scope`, and `backup location`. Include the exact commands `git rev-parse HEAD`, `bash scripts/verify.sh`, `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; status --format json`, `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; status --format json`, `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support`, and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support`. Mark status, support bundle, config summary, service state, source revision, and backup-location recording as `review-only evidence`; state that source datadir, wallet, service, and config mutation requires a future scoped plan per D-04.</action>
  <verify>
    <automated>rg -n "Surface id: `v1-8-upgrade-rollback-policy`|## Pre-Upgrade Checklist|current source revision or commit|repo-local verification status|binary provenance from Cargo or Bazel|Open Bitcoin JSONC config path|bitcoin.conf path|selected datadir|datadir ownership and free-space review|current sync/status evidence|support-bundle evidence when available|service state|wallet scope|backup location" docs/parity/upgrade-and-rollback-policy.md && rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; status --format json|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; status --format json|bash scripts/verify.sh|review-only evidence" docs/parity/upgrade-and-rollback-policy.md && git diff --check -- docs/parity/upgrade-and-rollback-policy.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/upgrade-and-rollback-policy.md` contains `Surface id: \`v1-8-upgrade-rollback-policy\``.
    - `docs/parity/upgrade-and-rollback-policy.md` contains all 12 D-02 checklist row strings exactly: `current source revision or commit`, `repo-local verification status`, `binary provenance from Cargo or Bazel`, `Open Bitcoin JSONC config path`, `bitcoin.conf path`, `selected datadir`, `datadir ownership and free-space review`, `current sync/status evidence`, `support-bundle evidence when available`, `service state`, `wallet scope`, `backup location`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains both repo-local command prefixes: `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --` and `bazel run //packages/open-bitcoin-cli:open_bitcoin --`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains `review-only evidence`.
  </acceptance_criteria>
  <done>The canonical policy exists, uses the Phase 82 vocabulary, includes every pre-upgrade checklist item and repo-local command form, and separates review-only evidence from mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add state and schema compatibility decision tables</name>
  <files>docs/parity/upgrade-and-rollback-policy.md</files>
  <read_first>
    - docs/parity/upgrade-and-rollback-policy.md
    - .planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
    - docs/architecture/status-snapshot.md
    - docs/architecture/storage-decision.md
    - docs/architecture/operator-observability.md
    - docs/operator/runtime-guide.md
  </read_first>
  <action>Extend `docs/parity/upgrade-and-rollback-policy.md` per D-05 through D-08 with `## State And Schema Compatibility Decision Table`. Use a Markdown table with columns `Evidence observed`, `Compatibility category`, `Action class`, `Allowed next action`, `Forbidden hidden mutation`, and `Required evidence`. Add rows that map these exact labels: `clean_shutdown` -> `safe_retry`; `unclean_shutdown` -> `safe_retry`; `storage_lock_contention` -> `read_only_inspection`; `incompatible_schema` and `schema_mismatch` -> `stop_and_escalate`; `store_corruption`, `corruption_marker`, `corrupt_record`, `partial_write`, and `unreadable_namespace` -> `backup_then_rebuild`; `backend_open_failure` -> `read_only_inspection` with escalation when field evidence cannot classify the cause. Add a second table or subsection `## Evidence That Is Not Sufficient` containing the exact list `daemon startup`, `elapsed time`, `peer reachability`, `raw logs`, and `report existence alone`; state that compatibility decisions require field-level evidence and `Unavailable: &lt;reason&gt;` for missing fields per D-07. Add `## Open Bitcoin Store Versus External State` and state that Open Bitcoin-owned durable store state is separate from external Core/Knots source datadirs and wallets; external Core/Knots datadirs and wallets are high-value input and must not be rewritten by rollback guidance per D-08.</action>
  <verify>
    <automated>rg -n "## State And Schema Compatibility Decision Table|clean_shutdown|unclean_shutdown|storage_lock_contention|incompatible_schema|schema_mismatch|store_corruption|corruption_marker|corrupt_record|partial_write|unreadable_namespace|backend_open_failure|safe_retry|read_only_inspection|backup_then_rebuild|stop_and_escalate" docs/parity/upgrade-and-rollback-policy.md && rg -n "## Evidence That Is Not Sufficient|daemon startup|elapsed time|peer reachability|raw logs|report existence alone|Unavailable: &lt;reason&gt;|Open Bitcoin-owned durable store state|external Core/Knots source datadirs and wallets" docs/parity/upgrade-and-rollback-policy.md && git diff --check -- docs/parity/upgrade-and-rollback-policy.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/upgrade-and-rollback-policy.md` contains `## State And Schema Compatibility Decision Table`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains all D-05 labels: `clean_shutdown`, `unclean_shutdown`, `incompatible_schema`, `store_corruption`, `storage_lock_contention`, `schema_mismatch`, `corruption_marker`, `corrupt_record`, `partial_write`, `unreadable_namespace`, `backend_open_failure`, `safe_retry`, `read_only_inspection`, `backup_then_rebuild`, `stop_and_escalate`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains `daemon startup`, `elapsed time`, `peer reachability`, `raw logs`, and `report existence alone`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains `Open Bitcoin-owned durable store state` and `external Core/Knots source datadirs and wallets`.
  </acceptance_criteria>
  <done>Compatibility outcomes are table-driven, use existing recovery vocabulary, require field-level evidence, and separate Open Bitcoin store state from external source state.</done>
</task>

<task type="auto">
  <name>Task 3: Add failed-upgrade, rollback, and deferred mutation boundaries</name>
  <files>docs/parity/upgrade-and-rollback-policy.md</files>
  <read_first>
    - docs/parity/upgrade-and-rollback-policy.md
    - .planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
    - docs/parity/support-matrix.md
    - docs/parity/deviations-and-unknowns.md
    - docs/parity/catalog/drop-in-audit-and-migration.md
    - docs/parity/catalog/wallet.md
    - docs/operator/runtime-guide.md
  </read_first>
  <action>Extend `docs/parity/upgrade-and-rollback-policy.md` per D-09 through D-12 with `## Failed Upgrade Guidance`, `## Rollback Guidance`, and `## Boundary And Deferred Work`. In failed-upgrade guidance, require these exact steps: `stop the attempted upgraded process`, `record exact command and commit`, `collect redacted local evidence`, `preserve backups`, and `avoid repeated mutation until the compatibility class is understood`. In rollback guidance, require source-built local rollback only: `return to the previous checked-out source revision or known binary`, `use the same explicit datadir and config paths`, `verify with repo-local commands`, and `record rollback evidence`. Explicitly say the policy does not imply `package-manager rollback`, `signed release channels`, or `automatic update behavior` per D-10. Add one forbidden-mutation paragraph with the exact sentence `Phase 84 does not recommend hidden mutation of source datadirs, external wallets, service files, launchd/systemd state, bitcoin.conf, or Open Bitcoin JSONC config.` Add the exact sentence `Destructive repair remains deferred.` Add the exact sentence `backup_then_rebuild is evidence and operator-decision guidance, not permission for automated destructive rebuild or repair.`</action>
  <verify>
    <automated>rg -n "## Failed Upgrade Guidance|stop the attempted upgraded process|record exact command and commit|collect redacted local evidence|preserve backups|avoid repeated mutation until the compatibility class is understood|## Rollback Guidance|return to the previous checked-out source revision or known binary|use the same explicit datadir and config paths|verify with repo-local commands|record rollback evidence" docs/parity/upgrade-and-rollback-policy.md && rg -n "package-manager rollback|signed release channels|automatic update behavior|Phase 84 does not recommend hidden mutation of source datadirs, external wallets, service files, launchd/systemd state, bitcoin.conf, or Open Bitcoin JSONC config.|Destructive repair remains deferred.|backup_then_rebuild is evidence and operator-decision guidance, not permission for automated destructive rebuild or repair." docs/parity/upgrade-and-rollback-policy.md && git diff --check -- docs/parity/upgrade-and-rollback-policy.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/upgrade-and-rollback-policy.md` contains `## Failed Upgrade Guidance`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains `## Rollback Guidance`.
    - `docs/parity/upgrade-and-rollback-policy.md` contains `Phase 84 does not recommend hidden mutation of source datadirs, external wallets, service files, launchd/systemd state, bitcoin.conf, or Open Bitcoin JSONC config.`
    - `docs/parity/upgrade-and-rollback-policy.md` contains `Destructive repair remains deferred.`
    - `docs/parity/upgrade-and-rollback-policy.md` contains `backup_then_rebuild is evidence and operator-decision guidance, not permission for automated destructive rebuild or repair.`
  </acceptance_criteria>
  <done>Failed-upgrade and rollback guidance preserves evidence, stays source-built/local-first, and explicitly forbids hidden mutation or destructive repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs -> local datadir/wallet/service/config | Operator may follow policy text against high-value local state. |
| support evidence -> issue report | Redacted local evidence may cross from operator machine into a public or maintainer-visible issue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | docs/parity/upgrade-and-rollback-policy.md | mitigate | Require exact no-hidden-mutation and destructive-repair-deferred sentences in Task 3 acceptance criteria. |
| T-84-02 | Repudiation | pre-upgrade checklist | mitigate | Require source revision, command, verification status, and binary provenance rows in Task 1. |
| T-84-03 | Information Disclosure | support evidence guidance | mitigate | Require redacted local evidence language and link to support-matrix issue-evidence boundaries. |
| T-84-04 | Elevation of Privilege | rollback/service guidance | mitigate | Forbid hidden mutation of service files and launchd/systemd state; any mutation remains future scoped. |
| T-84-05 | Denial of Service | compatibility table | mitigate | Require field-level evidence and action classes so operators do not repeatedly mutate or restart against unknown store state. |
</threat_model>

<verification>
Run all task-level commands, then:

```bash
rg -n "v1-8-upgrade-rollback-policy|UPG-01|UPG-02|UPG-03|UPG-04|Phase 84 does not recommend hidden mutation" docs/parity/upgrade-and-rollback-policy.md
git diff --check -- docs/parity/upgrade-and-rollback-policy.md
```
</verification>

<success_criteria>
- `docs/parity/upgrade-and-rollback-policy.md` exists and contains `Surface id: \`v1-8-upgrade-rollback-policy\``.
- UPG-01 pre-upgrade checklist includes every D-02 evidence row and the required Cargo and Bazel command forms.
- UPG-02 compatibility guidance maps the required recovery labels to `safe_retry`, `read_only_inspection`, `backup_then_rebuild`, and `stop_and_escalate`.
- UPG-03 rollback and failed-upgrade guidance forbids hidden mutation and destructive repair.
</success_criteria>

<output>
After completion, create `.planning/phases/84-upgrade-and-rollback-policy/84-01-SUMMARY.md`.
</output>
