---
phase: 84-upgrade-and-rollback-policy
plan: 04
type: execute
wave: 3
depends_on:
  - 84-01
  - 84-02
  - 84-03
files_modified:
  - scripts/check-phase84-upgrade-rollback-policy.ts
  - scripts/check-phase84-upgrade-rollback-policy.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - UPG-04
user_setup: []
must_haves:
  truths:
    - "Contributor can run a deterministic Phase 84 checker locally."
    - "Default `bash scripts/verify.sh` runs the Phase 84 checker after Phase 83."
    - "The checker fails when upgrade policy, rollback boundaries, backup expectations, repo-local commands, canonical links, or verifier wiring drift."
  artifacts:
    - path: "scripts/check-phase84-upgrade-rollback-policy.ts"
      provides: "Narrow Phase 84 deterministic checker"
      exports: ["checkPhase84UpgradeRollbackPolicy"]
    - path: "scripts/check-phase84-upgrade-rollback-policy.test.ts"
      provides: "Fixture tests for Phase 84 checker drift cases"
      contains: "passes_when_phase84_fixture_contains_policy_roots_and_verify_order"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring"
      contains: "check Phase 84 upgrade rollback policy"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase84-upgrade-rollback-policy.ts"
      via: "run_step after Phase 83 checker"
      pattern: "run_step \"check Phase 84 upgrade rollback policy\""
    - from: "scripts/check-phase84-upgrade-rollback-policy.ts"
      to: "docs/parity/upgrade-and-rollback-policy.md"
      via: "TARGET_FILES"
      pattern: "docs/parity/upgrade-and-rollback-policy.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 84-2026-06-21T21-33-46
generated_at: 2026-06-21T23:16:59.811Z
---

<objective>
Add deterministic Phase 84 checker coverage and wire it into the repo-native verifier.

Purpose: UPG-04 requires local checks that fail when upgrade policy docs, rollback boundaries, backup expectations, or verifier wiring drift.
Output: Bun checker, fixture tests, `scripts/verify.sh` wiring, refreshed LOC report, and full `bash scripts/verify.sh` evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
@.planning/phases/84-upgrade-and-rollback-policy/84-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-04-SUMMARY.md
@.planning/phases/83-support-matrix-and-issue-evidence/83-04-SUMMARY.md
@docs/parity/upgrade-and-rollback-policy.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@docs/parity/release-readiness.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/production-claim-boundary.md
@docs/parity/support-matrix.md
@README.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/operator-runtime-release-hardening.md
@docs/parity/catalog/drop-in-audit-and-migration.md
@docs/parity/catalog/wallet.md
@docs/parity/catalog/chainstate.md
@scripts/check-phase82-production-claim-boundary.ts
@scripts/check-phase82-production-claim-boundary.test.ts
@scripts/check-phase83-support-matrix-issue-evidence.ts
@scripts/check-phase83-support-matrix-issue-evidence.test.ts
@scripts/verify.sh
@docs/metrics/lines-of-code.md

<interfaces>
Use the Phase 83 checker pattern:

```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE84_REPO_ROOT";
const DEFAULT_REPO_ROOT = path.resolve(import.meta.dir, "..");
const SURFACE_ID = "v1-8-upgrade-rollback-policy";
const PHASE84_REQUIREMENTS = ["UPG-01", "UPG-02", "UPG-03", "UPG-04"] as const;

export function checkPhase84UpgradeRollbackPolicy(
  maybeRepoRoot = process.env[REPO_ROOT_OVERRIDE_ENV],
): string[] {
  // pure validation over repository file text; no operator commands are executed
}
```

Verifier commands to add after Phase 83:

```bash
bun test scripts/check-phase84-upgrade-rollback-policy.test.ts
bun run scripts/check-phase84-upgrade-rollback-policy.ts
run_step "test Phase 84 upgrade rollback policy checker" bun test scripts/check-phase84-upgrade-rollback-policy.test.ts
run_step "check Phase 84 upgrade rollback policy" bun run scripts/check-phase84-upgrade-rollback-policy.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fixture tests for Phase 84 checker</name>
  <files>scripts/check-phase84-upgrade-rollback-policy.test.ts</files>
  <read_first>
    - scripts/check-phase83-support-matrix-issue-evidence.test.ts
    - scripts/check-phase83-support-matrix-issue-evidence.ts
    - docs/parity/upgrade-and-rollback-policy.md
    - docs/parity/index.json
    - scripts/verify.sh
    - .planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
    - standards/core/testing.md
    - standards/languages/typescript-javascript.md
  </read_first>
  <behavior>
    - Test 1: valid fixture with policy, parity roots, human links, and verifier order returns `[]`.
    - Test 2: omitting any required pre-upgrade checklist item fails with `pre-upgrade checklist`.
    - Test 3: omitting any D-05 recovery label or action class fails with `compatibility decision table`.
    - Test 4: replacing field-level evidence with daemon startup, elapsed time, peer reachability, raw logs, or report existence as proof fails with `insufficient compatibility proof`.
    - Test 5: hidden datadir, wallet, service, config mutation or destructive repair permission fails with `hidden mutation boundary`.
    - Test 6: parity roots missing `UPG-04` or `docs/parity/upgrade-and-rollback-policy.md` fail with `parity root`.
    - Test 7: Phase 84 verifier commands that exist only inside `VERIFY_COMMAND_ORDER` heredoc fail with `verifier-order`.
    - Test 8: default verifier text that adds public-network, real service-manager, or multi-day commands fails with `default verifier boundary`.
  </behavior>
  <action>Create `scripts/check-phase84-upgrade-rollback-policy.test.ts` using Bun test and the Phase 83 fixture-root pattern: import `afterEach`, `expect`, and `test` from `bun:test`; use `mkdtemp`, `mkdir`, `rm`, and `writeFile` from `node:fs/promises`; track temporary roots in `tempRoots`; set and clear `OPEN_BITCOIN_PHASE84_REPO_ROOT`; define `SURFACE_ID = "v1-8-upgrade-rollback-policy"` and `PHASE84_REQUIREMENTS = ["UPG-01", "UPG-02", "UPG-03", "UPG-04"] as const`. Add the exact test names `passes_when_phase84_fixture_contains_policy_roots_and_verify_order`, `fails_when_pre_upgrade_checklist_omits_required_evidence`, `fails_when_compatibility_decision_table_missing_recovery_label`, `fails_when_policy_claims_startup_or_report_existence_as_proof`, `fails_when_policy_allows_hidden_mutation_or_destructive_repair`, `fails_when_parity_roots_omit_upgrade_policy_requirement_or_evidence`, `fails_when_phase84_verify_commands_exist_only_in_legacy_heredoc`, and `fails_when_default_verify_contains_public_network_service_manager_or_multiday_drift`. Use explicit `// Arrange`, `// Act`, and `// Assert` comments in each test. The fixture must include representative text files for all Phase 84 target files and a verify script fixture that has both the visible heredoc commands and executed `run_step` commands.</action>
  <verify>
    <automated>rg -n "OPEN_BITCOIN_PHASE84_REPO_ROOT|passes_when_phase84_fixture_contains_policy_roots_and_verify_order|fails_when_pre_upgrade_checklist_omits_required_evidence|fails_when_compatibility_decision_table_missing_recovery_label|fails_when_policy_claims_startup_or_report_existence_as_proof|fails_when_policy_allows_hidden_mutation_or_destructive_repair|fails_when_parity_roots_omit_upgrade_policy_requirement_or_evidence|fails_when_phase84_verify_commands_exist_only_in_legacy_heredoc|fails_when_default_verify_contains_public_network_service_manager_or_multiday_drift|// Arrange|// Act|// Assert" scripts/check-phase84-upgrade-rollback-policy.test.ts && git diff --check -- scripts/check-phase84-upgrade-rollback-policy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase84-upgrade-rollback-policy.test.ts` contains `OPEN_BITCOIN_PHASE84_REPO_ROOT`.
    - `scripts/check-phase84-upgrade-rollback-policy.test.ts` contains `passes_when_phase84_fixture_contains_policy_roots_and_verify_order`.
    - `scripts/check-phase84-upgrade-rollback-policy.test.ts` contains all seven failure test names listed in the action.
    - `scripts/check-phase84-upgrade-rollback-policy.test.ts` contains `// Arrange`, `// Act`, and `// Assert`.
    - Task 2 owns the first passing `bun test scripts/check-phase84-upgrade-rollback-policy.test.ts` run; during RED work the test suite may fail for missing Phase 84 checker behavior but the test file must contain the exact drift cases listed above.
  </acceptance_criteria>
  <done>Fixture tests define the expected Phase 84 checker behavior with focused Arrange/Act/Assert cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checker, verifier wiring, and closeout verification</name>
  <files>scripts/check-phase84-upgrade-rollback-policy.ts, scripts/check-phase84-upgrade-rollback-policy.test.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - scripts/check-phase84-upgrade-rollback-policy.test.ts
    - scripts/check-phase83-support-matrix-issue-evidence.ts
    - scripts/check-phase83-support-matrix-issue-evidence.test.ts
    - scripts/check-phase82-production-claim-boundary.ts
    - scripts/verify.sh
    - docs/parity/upgrade-and-rollback-policy.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/README.md
    - docs/parity/release-readiness.md
    - docs/parity/deviations-and-unknowns.md
    - docs/parity/production-claim-boundary.md
    - docs/parity/support-matrix.md
    - README.md
    - docs/operator/runtime-guide.md
    - docs/parity/catalog/operator-runtime-release-hardening.md
    - docs/parity/catalog/drop-in-audit-and-migration.md
    - docs/parity/catalog/wallet.md
    - docs/parity/catalog/chainstate.md
    - docs/metrics/lines-of-code.md
    - .planning/phases/84-upgrade-and-rollback-policy/84-CONTEXT.md
  </read_first>
  <behavior>
    - The checker reads a fixed `TARGET_FILES` list and returns string failures; it does not execute operator commands from docs.
    - The checker validates exact policy sections, required checklist items, D-05 recovery labels, D-06 action classes, D-07 insufficient-proof strings, D-08 external-state separation, D-09 failed-upgrade evidence preservation, D-10 source-built rollback boundaries, D-11 hidden-mutation ban, D-12 destructive-repair deferral, D-13 canonical links, D-14 checker scope, D-15 deterministic verifier boundaries, and D-16 verifier wiring.
    - `scripts/verify.sh` runs Phase 84 test and checker immediately after the Phase 83 checker in both the visible `VERIFY_COMMAND_ORDER` heredoc and executed `run_step` sequence.
  </behavior>
  <action>Implement `scripts/check-phase84-upgrade-rollback-policy.ts` with `#!/usr/bin/env bun`, imports from `node:fs` and `node:path`, exported function `checkPhase84UpgradeRollbackPolicy`, and CLI behavior that prints each failure and exits non-zero when failures exist. Use constants `REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE84_REPO_ROOT"`, `SURFACE_ID = "v1-8-upgrade-rollback-policy"`, `PHASE84_REQUIREMENTS = ["UPG-01", "UPG-02", "UPG-03", "UPG-04"] as const`, `PHASE83_CHECKER_COMMAND = "bun run scripts/check-phase83-support-matrix-issue-evidence.ts"`, `PHASE84_TEST_COMMAND = "bun test scripts/check-phase84-upgrade-rollback-policy.test.ts"`, and `PHASE84_CHECKER_COMMAND = "bun run scripts/check-phase84-upgrade-rollback-policy.ts"`. Validate these `TARGET_FILES`: `docs/parity/upgrade-and-rollback-policy.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`, `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/operator-runtime-release-hardening.md`, `docs/parity/catalog/drop-in-audit-and-migration.md`, `docs/parity/catalog/wallet.md`, `docs/parity/catalog/chainstate.md`, and `scripts/verify.sh`. Validate the policy contains exact headings from Plan 01, all D-02 checklist rows, both repo-local command prefixes, all D-05 recovery labels/action classes, `daemon startup`, `elapsed time`, `peer reachability`, `raw logs`, `report existence alone`, `Unavailable: &lt;reason&gt;`, `Open Bitcoin-owned durable store state`, `external Core/Knots source datadirs and wallets`, `stop the attempted upgraded process`, `record exact command and commit`, `preserve backups`, `package-manager rollback`, `signed release channels`, `automatic update behavior`, the exact D-11 sentence, `Destructive repair remains deferred.`, and the exact `backup_then_rebuild` sentence. Validate `docs/parity/index.json` top-level, checklist, and `audit.v1_8_upgrade_rollback_policy` roots with exact requirements and evidence. Validate every human target file contains `upgrade-and-rollback-policy.md`; validate all files except the policy do not contain the full table header `Evidence to record | How to collect it | Mutation status | Why it matters`. Strip the `VERIFY_COMMAND_ORDER` heredoc before checking executed verifier order, then require Phase 84 test and checker after Phase 83 checker. Reject default verifier drift strings using concatenated literals for `run-live-mainnet-` + `smoke`, `system` + `ctl`, `launch` + `ctl`, and `sleep ` + `259200`. Update `scripts/verify.sh` visible heredoc and executed `run_step` calls with the exact Phase 84 commands from `<interfaces>`. Refresh `docs/metrics/lines-of-code.md` with the repo generator in check-compatible mode after the new tracked script files are present.</action>
  <verify>
    <automated>bun test scripts/check-phase84-upgrade-rollback-policy.test.ts && bun --check scripts/check-phase84-upgrade-rollback-policy.ts && bun run scripts/check-phase84-upgrade-rollback-policy.ts && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase84-upgrade-rollback-policy.ts` contains `export function checkPhase84UpgradeRollbackPolicy`.
    - `scripts/check-phase84-upgrade-rollback-policy.ts` contains `OPEN_BITCOIN_PHASE84_REPO_ROOT`.
    - `scripts/check-phase84-upgrade-rollback-policy.ts` contains `PHASE84_REQUIREMENTS = ["UPG-01", "UPG-02", "UPG-03", "UPG-04"] as const`.
    - `scripts/check-phase84-upgrade-rollback-policy.ts` contains `v1_8_upgrade_rollback_policy`.
    - `scripts/verify.sh` contains `bun test scripts/check-phase84-upgrade-rollback-policy.test.ts`.
    - `scripts/verify.sh` contains `run_step "check Phase 84 upgrade rollback policy" bun run scripts/check-phase84-upgrade-rollback-policy.ts`.
    - `bun test scripts/check-phase84-upgrade-rollback-policy.test.ts` exits 0.
    - `bun --check scripts/check-phase84-upgrade-rollback-policy.ts` exits 0.
    - `bun run scripts/check-phase84-upgrade-rollback-policy.ts` exits 0.
    - `bash scripts/verify.sh` exits 0 per D-16.
  </acceptance_criteria>
  <done>Phase 84 has deterministic checker/test coverage, verifier wiring after Phase 83, refreshed LOC output, and full repo-native verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file system -> Bun checker | Checker reads repository docs and scripts as text. |
| docs -> verifier | Default verifier may accidentally gain public-network, service-manager, or multi-day commands. |
| parity roots -> release review | Checker-enforced roots influence release-readiness decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-16 | Tampering | scripts/check-phase84-upgrade-rollback-policy.ts | mitigate | Export pure data-in/data-out checker and fixture tests for missing roots, labels, commands, and links. |
| T-84-17 | Denial of Service | scripts/verify.sh | mitigate | Reject public-network, real service-manager, and multi-day verifier drift; wire Phase 84 after Phase 83. |
| T-84-18 | Repudiation | parity root validation | mitigate | Require exact UPG-01 through UPG-04 arrays and evidence lists in checklist and audit roots. |
| T-84-19 | Information Disclosure | checker target files | mitigate | Checker only reads fixed repo files and never executes operator commands or reads local datadirs. |
| T-84-20 | Spoofing | verifier-order check | mitigate | Strip `VERIFY_COMMAND_ORDER` heredoc before validating executed `run_step` ordering. |
</threat_model>

<verification>
Run:

```bash
bun test scripts/check-phase84-upgrade-rollback-policy.test.ts
bun --check scripts/check-phase84-upgrade-rollback-policy.ts
bun run scripts/check-phase84-upgrade-rollback-policy.ts
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Phase 84 checker and tests exist and pass.
- `scripts/verify.sh` executes Phase 84 test/checker after Phase 83.
- Full repo-native `bash scripts/verify.sh` passes and is cited in the summary.
- `docs/metrics/lines-of-code.md` is refreshed if verification requires it.
</success_criteria>

<output>
After completion, create `.planning/phases/84-upgrade-and-rollback-policy/84-04-SUMMARY.md`.
</output>
