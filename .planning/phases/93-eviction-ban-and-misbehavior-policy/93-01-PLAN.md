---
phase: 93-eviction-ban-and-misbehavior-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer_policy.rs
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [EVICT-01, EVICT-03, EVICT-04]
must_haves:
  truths:
    - "Eviction decisions are pure and deterministic from typed peer-policy inputs."
    - "Ban/discourage state is scoped, expiry-aware, and manually reversible."
    - "Misbehavior responses respect protected peer classes and still expose protected no-action evidence."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer_policy.rs"
      provides: "Pure eviction, ban, unban, and misbehavior policy"
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "Thin PeerManager hooks for eviction and misbehavior decisions"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Breadcrumb registration for the Phase 93 policy module"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 93-2026-06-26T13-15-10
generated_at: 2026-06-26T13:30:03.284Z
---

<objective>
Create the pure Phase 93 peer-policy core for eviction scoring, ban/discourage state, manual unban, and misbehavior responses.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add peer-policy domain model and tests</name>
  <files>packages/open-bitcoin-network/src/peer_policy.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/inbound/permissions.rs`
    - `docs/parity/source-breadcrumbs.json`
    - `.planning/phases/93-eviction-ban-and-misbehavior-policy/93-CONTEXT.md`
    - `.planning/phases/93-eviction-ban-and-misbehavior-policy/93-RESEARCH.md`
  </read_first>
  <action>
    Add `packages/open-bitcoin-network/src/peer_policy.rs` with parity breadcrumbs for `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/banman.h`, `packages/bitcoin-knots/src/banman.cpp`, and `packages/bitcoin-knots/src/net_permissions.cpp`. Define stable-label enums and structs for eviction scoring, ban scope/state, manual unban, and misbehavior: `EvictionReason`, `EvictionScoreComponent`, `EvictionCandidateInput`, `EvictionCandidate`, `EvictionDecision`, `BanScope`, `PeerBanEntry`, `PeerBanBook`, `BanDecision`, `UnbanDecision`, `BanReason`, `MisbehaviorKind`, `MisbehaviorPolicy`, `MisbehaviorObservation`, `MisbehaviorDecision`, and `MisbehaviorResponse`. Export them from `packages/open-bitcoin-network/src/lib.rs`. Add unit tests in the same module using Arrange/Act/Assert.
  </action>
  <acceptance_criteria>
    - `rg -n "pub enum EvictionReason|pub struct EvictionCandidateInput|pub struct PeerBanBook|pub enum MisbehaviorResponse" packages/open-bitcoin-network/src/peer_policy.rs`
    - `rg -n "cap_pressure|handshake_stalled|ban_active|ban_expired|unbanned|protected_no_action|misbehavior_threshold_reached" packages/open-bitcoin-network/src/peer_policy.rs`
    - `rg -n "peer-policy|packages/open-bitcoin-network/src/peer_policy.rs|packages/bitcoin-knots/src/banman.cpp" docs/parity/source-breadcrumbs.json`
    - `rg -n "pub use peer_policy::|mod peer_policy" packages/open-bitcoin-network/src/lib.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy --no-fail-fast</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add thin PeerManager decision hooks</name>
  <files>packages/open-bitcoin-network/src/peer.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer_policy.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
  </read_first>
  <action>
    Add thin `PeerManager` APIs that feed existing peer state into the pure policy module without adding runtime side effects: `eviction_decision(&self) -> EvictionDecision` and `misbehavior_decision(&self, peer_id: PeerId, kind: MisbehaviorKind, points: u32, now_unix_seconds: i64) -> Result<MisbehaviorDecision, NetworkError>`. Use `PeerState` fields, inbound records, handshake state, requested inventory counts, and permission effects to build `EvictionCandidateInput`. Do not remove peers or mutate runtime sockets inside these methods.
  </action>
  <acceptance_criteria>
    - `rg -n "eviction_decision|misbehavior_decision|EvictionCandidateInput|MisbehaviorKind" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/tests.rs`
    - `rg -n "protected_no_action|EvictionPolicyProtected|MisbehaviorPolicyProtected" packages/open-bitcoin-network/src/peer/tests.rs`
    - `! rg -n "remove_peer\\(|TcpStream|spawn|sleep" packages/open-bitcoin-network/src/peer_policy.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast</automated>
  </verify>
</task>
</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-93-01-01 | Tampering | eviction scoring | mitigate | Use typed immutable inputs and stable score components instead of runtime-local heuristics. |
| T-93-01-02 | Denial of Service | ban state | mitigate | Bound state operations to deterministic maps and injected timestamps; avoid sleeps or runtime sockets. |
| T-93-01-03 | Elevation of Privilege | protected peers | mitigate | Return `protected_no_action` for protected peers while preserving violation evidence. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
</verification>

<success_criteria>
EVICT-01, EVICT-03, and EVICT-04 have a tested pure-core implementation with typed decisions and protected-peer handling.
</success_criteria>
