---
phase: 93-eviction-ban-and-misbehavior-policy
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - packages/open-bitcoin-node/src/network/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
autonomous: true
requirements: [EVICT-02, EVICT-03, EVICT-04]
must_haves:
  truths:
    - "Inbound status exposes bounded eviction, ban, unban, and misbehavior evidence."
    - "Status/support evidence uses low-cardinality labels and omits raw peer identifiers, raw endpoint tables, and raw permission config strings."
    - "Metrics are fixed MetricKind variants, not dynamic labels."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Shared serializable peer-policy evidence"
    - path: "packages/open-bitcoin-node/src/network/inbound.rs"
      provides: "Managed projection of peer-policy decisions"
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "Redacted support-bundle projection"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 93-2026-06-26T13-15-10
generated_at: 2026-06-26T13:30:03.284Z
---

<objective>
Project Phase 93 peer-policy evidence through shared node status, metrics, CLI status, and support renderers.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend shared inbound status and managed projection</name>
  <files>packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-node/src/network/inbound.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/status/inbound.rs`
    - `packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `packages/open-bitcoin-node/src/network/inbound.rs`
    - `packages/open-bitcoin-network/src/peer_policy.rs`
  </read_first>
  <action>
    Add `InboundPeerPolicyEvent` and serde-defaulted fields on `InboundPeerServingStatus`: `eviction_candidates_evaluated`, `disconnects_requested`, `discouraged_peers`, `active_bans`, `expired_bans`, `manual_unbans`, `misbehavior_observations`, `protected_no_actions`, and `latest_peer_policy_decision`. Add a managed projection type in `network/inbound.rs` that converts policy decisions into these status fields with stable labels such as `eviction_candidate_selected`, `ban_active`, `ban_expired`, `unbanned`, `misbehavior_observed`, and `protected_no_action`.
  </action>
  <acceptance_criteria>
    - `rg -n "InboundPeerPolicyEvent|eviction_candidates_evaluated|disconnects_requested|discouraged_peers|active_bans|expired_bans|manual_unbans|misbehavior_observations|protected_no_actions|latest_peer_policy_decision" packages/open-bitcoin-node/src/status/inbound.rs`
    - `rg -n "eviction_candidate_selected|ban_active|ban_expired|unbanned|misbehavior_observed|protected_no_action" packages/open-bitcoin-node/src/network/inbound.rs packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `! rg -n "raw_endpoint|peer_id|address_bytes|raw_permission|class_name" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-node/src/network/inbound.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound --no-fail-fast</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render status/support evidence and fixed metrics</name>
  <files>packages/open-bitcoin-node/src/metrics.rs, packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/support/render/inbound.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/metrics.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
    - `packages/open-bitcoin-node/src/status/inbound.rs`
  </read_first>
  <action>
    Add fixed `MetricKind` variants for peer-policy counters and render the new shared status fields in CLI status and support Markdown. Keep the renderers bounded and redacted: they may show counts, latest outcome, latest reason, and next action, but not raw peer IDs, endpoint tables, raw permission class names, or config literals.
  </action>
  <acceptance_criteria>
    - `rg -n "inbound_eviction_candidate_count|inbound_disconnect_count|inbound_active_ban_count|inbound_misbehavior_observation_count|inbound_protected_no_action_count" packages/open-bitcoin-node/src/metrics.rs`
    - `rg -n "eviction|ban|misbehavior|protected no-action|latest peer policy" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
    - `! rg -n "peer_id=|raw_permission|class_name|address_bytes" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound --no-fail-fast</automated>
  </verify>
</task>
</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-93-02-01 | Information Disclosure | status/support | mitigate | Expose bounded labels/counts only and add grep-verifiable raw evidence exclusions. |
| T-93-02-02 | Repudiation | operator evidence | mitigate | Preserve latest stable reason and next-action text in shared status/support surfaces. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
EVICT-02, EVICT-03, and EVICT-04 are visible in shared, redacted operator evidence and fixed metrics.
</success_criteria>
