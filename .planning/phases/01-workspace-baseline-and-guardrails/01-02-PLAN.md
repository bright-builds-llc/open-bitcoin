---
phase: 01-workspace-baseline-and-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .bazelversion
  - MODULE.bazel
  - .bazelrc
  - BUILD.bazel
  - packages/open-bitcoin-core/BUILD.bazel
  - packages/open-bitcoin-node/BUILD.bazel
autonomous: true
requirements:
  - ARCH-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 01-2026-04-11T11-36-20
generated_at: 2026-04-11T11:46:10Z
must_haves:
  truths:
    - Contributors can invoke first-party Rust packages from the repo root through Bazel/Bzlmod.
    - Bazel toolchain registration points at the repo's first-party Rust crates instead of treating them as external one-offs.
  artifacts:
    - .bazelversion
    - MODULE.bazel
    - packages/open-bitcoin-core/BUILD.bazel
    - packages/open-bitcoin-node/BUILD.bazel
  key_links:
    - `MODULE.bazel` registers `rules_rust` and a Rust 2024 toolchain.
    - The package BUILD files expose Bazel targets for the first-party crates created in Plan 01.
---

<objective>
Bootstrap the repo-level Bazel surface for the first-party Rust workspace.

Purpose: The project committed to Bazelisk and Bazel/Bzlmod as the top-level build tooling, so the workspace needs concrete root files and package targets before later phases can rely on them.
Output: Root Bazel config files plus package BUILD files for the first-party crates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md
@packages/Cargo.toml
@packages/open-bitcoin-core/Cargo.toml
@packages/open-bitcoin-node/Cargo.toml
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-01:** Bazel module resolution and toolchain downloads -> repo-pinned build inputs.
- **TB-02:** Repo-root build entrypoints -> first-party package targets.

### Threat Register
- **Threat ID:** T-01-03
  - **Category:** Tampering
  - **Component:** `.bazelversion` and `MODULE.bazel`
  - **Disposition:** mitigate
  - **Mitigation Plan:** Pin the Bazel version in `.bazelversion`, declare `rules_rust` explicitly in `MODULE.bazel`, and keep the Rust toolchain version recorded in source-controlled config.
- **Threat ID:** T-01-04
  - **Category:** Denial of service
  - **Component:** root build entrypoints and crate BUILD files
  - **Disposition:** mitigate
  - **Mitigation Plan:** Create minimal, deterministic BUILD targets that map directly to the first-party crates from Plan 01 and verify them with `bazel query`/`bazel build`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pin Bazelisk/Bzlmod root configuration</name>
  <files>.bazelversion, MODULE.bazel, .bazelrc</files>
  <read_first>.planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md, .planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md, .bazelversion, MODULE.bazel, .bazelrc</read_first>
  <action>Create `.bazelversion` with the pinned Bazel 8 release chosen for this repo, create `MODULE.bazel` with `module(name = "open_bitcoin")`, `bazel_dep(name = "rules_rust", version = "0.69.0")`, and a `rust.toolchain(edition = "2024", versions = ["1.85.0"])` declaration, and add `.bazelrc` defaults that keep builds deterministic and repository-root friendly. Do not use WORKSPACE as the primary bootstrap path for this new repo.</action>
  <verify>rg -n 'rules_rust|rust.toolchain|edition = \"2024\"|1.85.0' MODULE.bazel && cat .bazelversion</verify>
  <acceptance_criteria>
    - `.bazelversion` contains `8.6.0`
    - `MODULE.bazel` contains `bazel_dep(name = "rules_rust", version = "0.69.0")`
    - `MODULE.bazel` contains `rust.toolchain(`
    - `MODULE.bazel` contains `versions = ["1.85.0"]`
  </acceptance_criteria>
  <done>The repo has a pinned Bazel/Bzlmod root configuration aligned with the Phase 1 research decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the first-party crates as Bazel targets</name>
  <files>BUILD.bazel, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-node/BUILD.bazel</files>
  <read_first>MODULE.bazel, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/src/lib.rs, packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/src/lib.rs</read_first>
  <action>Create `packages/open-bitcoin-core/BUILD.bazel` with a `rust_library` target named `open_bitcoin_core_lib` that builds `src/lib.rs`, create `packages/open-bitcoin-node/BUILD.bazel` with a `rust_library` target named `open_bitcoin_node_lib` that depends on `//packages/open-bitcoin-core:open_bitcoin_core_lib`, and add a root `BUILD.bazel` with alias targets `core` and `node` pointing at those package libraries. Keep the pure-core crate dependency-free from shell code.</action>
  <verify>bazel query //:core //:node //packages/open-bitcoin-core:open_bitcoin_core_lib //packages/open-bitcoin-node:open_bitcoin_node_lib</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-core/BUILD.bazel` contains `name = "open_bitcoin_core_lib"`
    - `packages/open-bitcoin-node/BUILD.bazel` contains `name = "open_bitcoin_node_lib"`
    - `packages/open-bitcoin-node/BUILD.bazel` contains `//packages/open-bitcoin-core:open_bitcoin_core_lib`
    - `BUILD.bazel` contains `alias(name = "core"`
  </acceptance_criteria>
  <done>Contributors can target the first-party crates from the repo root through Bazel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'rules_rust|rust.toolchain|1.85.0' MODULE.bazel` passes
- [ ] `bazel query //:core //:node //packages/open-bitcoin-core:open_bitcoin_core_lib //packages/open-bitcoin-node:open_bitcoin_node_lib` passes
- [ ] `bazel build //:core //:node` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-root Bazel files are pinned and source-controlled
- First-party Rust crates are reachable as Bazel targets from the repo root
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-baseline-and-guardrails/01-02-SUMMARY.md`
</output>
