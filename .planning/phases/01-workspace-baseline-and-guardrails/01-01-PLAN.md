---
phase: 01-workspace-baseline-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - packages/Cargo.toml
  - packages/README.md
  - packages/open-bitcoin-core/Cargo.toml
  - packages/open-bitcoin-core/src/lib.rs
  - packages/open-bitcoin-node/Cargo.toml
  - packages/open-bitcoin-node/src/lib.rs
autonomous: true
requirements:
  - REF-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 01-2026-04-11T11-36-20
generated_at: 2026-04-11T11:46:10Z
must_haves:
  truths:
    - The repository vendors Bitcoin Knots under `packages/bitcoin-knots` at the pinned baseline tag.
    - The first-party Rust workspace exists under `packages/` with separate pure-core and shell crates.
  artifacts:
    - .gitmodules
    - packages/Cargo.toml
    - packages/open-bitcoin-core/Cargo.toml
    - packages/open-bitcoin-node/Cargo.toml
  key_links:
    - `.gitmodules` maps `packages/bitcoin-knots` to the Knots upstream repository.
    - `packages/Cargo.toml` lists `open-bitcoin-core` and `open-bitcoin-node` as workspace members.
---

<objective>
Establish the pinned reference baseline and the first first-party Rust workspace skeleton.

Purpose: Later Bazel, verification, and policy work needs a concrete repo layout and a vendored upstream baseline to build around.
Output: The Knots submodule is pinned under `packages/bitcoin-knots`, and the repo has a first-party Cargo workspace with core and node crates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md
@seed-prompts/2026-04-10-seed-prompt-refined-with-codex.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-01:** Upstream internet repositories -> vendored baseline state inside this repo.
- **TB-02:** Contributor edits -> first-party workspace layout that later phases will trust.

### Threat Register
- **Threat ID:** T-01-01
  - **Category:** Tampering
  - **Component:** `packages/bitcoin-knots` gitlink and `.gitmodules`
  - **Disposition:** mitigate
  - **Mitigation Plan:** Add the baseline through `git submodule`, pin the exact Knots baseline tag before committing, and make the gitlink reviewable in normal diffs.
- **Threat ID:** T-01-02
  - **Category:** Spoofing
  - **Component:** first-party crate layout under `packages/`
  - **Disposition:** mitigate
  - **Mitigation Plan:** Create explicit crate names and workspace membership so later policy checks can distinguish pure-core crates from shell crates reliably.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the Knots baseline as a pinned submodule</name>
  <files>.gitmodules, packages/bitcoin-knots</files>
  <read_first>.planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md, .planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md, seed-prompts/2026-04-10-seed-prompt-refined-with-codex.md, .gitmodules</read_first>
  <action>Add Bitcoin Knots as a git submodule at `packages/bitcoin-knots` using the upstream `https://github.com/bitcoinknots/bitcoin` repository, then check out the pinned release tag `v29.3.knots20260210` inside the submodule before committing the gitlink. Update `.gitmodules` so the path is exactly `packages/bitcoin-knots` and the submodule name is stable and human-readable.</action>
  <verify>git submodule status packages/bitcoin-knots && git -C packages/bitcoin-knots describe --tags --exact-match</verify>
  <acceptance_criteria>
    - `.gitmodules` contains `path = packages/bitcoin-knots`
    - `.gitmodules` contains `url = https://github.com/bitcoinknots/bitcoin`
    - `git -C packages/bitcoin-knots describe --tags --exact-match` prints `v29.3.knots20260210`
  </acceptance_criteria>
  <done>Knots is vendored as a reviewable git submodule pinned to the required baseline tag.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the first-party Cargo workspace and initial crate split</name>
  <files>packages/Cargo.toml, packages/README.md, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/src/lib.rs, packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/src/lib.rs</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md, .planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md, packages/Cargo.toml, packages/README.md</read_first>
  <action>Create `packages/Cargo.toml` as the first-party workspace manifest with exactly the members `open-bitcoin-core` and `open-bitcoin-node`. Create `packages/open-bitcoin-core` as the pure-core library crate and `packages/open-bitcoin-node` as the shell/runtime crate, each with minimal `Cargo.toml` and `src/lib.rs` files. Add `#![forbid(unsafe_code)]` to both crate roots, document the vendor-vs-first-party layout in `packages/README.md`, and keep the node crate depending on the core crate but not vice versa.</action>
  <verify>cargo metadata --manifest-path packages/Cargo.toml --format-version 1 --no-deps</verify>
  <acceptance_criteria>
    - `packages/Cargo.toml` contains `members = ["open-bitcoin-core", "open-bitcoin-node"]`
    - `packages/open-bitcoin-core/src/lib.rs` contains `#![forbid(unsafe_code)]`
    - `packages/open-bitcoin-node/src/lib.rs` contains `#![forbid(unsafe_code)]`
    - `cargo metadata --manifest-path packages/Cargo.toml --format-version 1 --no-deps` exits 0
  </acceptance_criteria>
  <done>The repo has a minimal first-party Rust workspace with a clear pure-core vs shell split.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git submodule status packages/bitcoin-knots` shows the submodule path
- [ ] `git -C packages/bitcoin-knots describe --tags --exact-match` returns `v29.3.knots20260210`
- [ ] `cargo metadata --manifest-path packages/Cargo.toml --format-version 1 --no-deps` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The reference baseline is pinned under `packages/bitcoin-knots`
- The first-party Cargo workspace separates `open-bitcoin-core` from `open-bitcoin-node`
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-baseline-and-guardrails/01-01-SUMMARY.md`
</output>
