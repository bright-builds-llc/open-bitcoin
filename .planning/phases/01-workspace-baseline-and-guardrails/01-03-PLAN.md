---
phase: 01-workspace-baseline-and-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/pure-core-crates.txt
  - scripts/check-pure-core-deps.sh
  - scripts/verify.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 01-2026-04-11T11-36-20
generated_at: 2026-04-11T11:46:10Z
must_haves:
  truths:
    - Pure-core crates fail verification when forbidden dependencies or forbidden I/O/runtime APIs appear.
    - Contributors and CI share one repo-native verification contract.
    - Pure-core coverage gates are enforceable from scripts and CI.
  artifacts:
    - scripts/check-pure-core-deps.sh
    - scripts/verify.sh
    - .github/workflows/ci.yml
  key_links:
    - `scripts/verify.sh` calls `scripts/check-pure-core-deps.sh`
    - `.github/workflows/ci.yml` runs the same repo-native verification contract used locally
---

<objective>
Install the architectural and verification gates that keep later implementation work honest.

Purpose: The roadmap's core promise is not just to build features but to keep the pure core pure and the verification contract hard-failing from the first real code onward.
Output: Repo-native verification scripts, a pure-core policy checker, and CI wiring that run the same contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md
@.planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md
@packages/Cargo.toml
@packages/open-bitcoin-core/Cargo.toml
@packages/open-bitcoin-node/Cargo.toml
@scripts/bright-builds-auto-update.sh
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-01:** Contributor code changes -> pure-core architectural boundary.
- **TB-02:** Local verification scripts -> CI enforcement and merge gating.

### Threat Register
- **Threat ID:** T-01-05
  - **Category:** Elevation of privilege
  - **Component:** pure-core crates
  - **Disposition:** mitigate
  - **Mitigation Plan:** Keep a source-controlled pure-core crate allowlist and fail builds when forbidden APIs or runtime crates enter those crates.
- **Threat ID:** T-01-06
  - **Category:** Repudiation
  - **Component:** verification contract between local and CI environments
  - **Disposition:** mitigate
  - **Mitigation Plan:** Make CI run the same `scripts/verify.sh` contract rather than duplicating partial checks in a separate workflow.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure-core architecture policy checks</name>
  <files>scripts/pure-core-crates.txt, scripts/check-pure-core-deps.sh</files>
  <read_first>.planning/phases/01-workspace-baseline-and-guardrails/01-CONTEXT.md, .planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md, packages/Cargo.toml, packages/open-bitcoin-core/Cargo.toml, scripts/check-pure-core-deps.sh</read_first>
  <action>Create `scripts/pure-core-crates.txt` listing `open-bitcoin-core` as the initial pure-core crate, then create `scripts/check-pure-core-deps.sh` with `set -euo pipefail`. The script must parse `cargo metadata --manifest-path packages/Cargo.toml --format-version 1`, verify the listed pure-core crates do not depend on forbidden runtime/network/randomness crates, and source-scan `packages/open-bitcoin-core/src` for forbidden imports such as `std::fs`, `std::net`, `std::env`, `std::process`, `std::thread`, `tokio`, `reqwest`, `rustls`, and `rand`.</action>
  <verify>bash scripts/check-pure-core-deps.sh</verify>
  <acceptance_criteria>
    - `scripts/pure-core-crates.txt` contains `open-bitcoin-core`
    - `scripts/check-pure-core-deps.sh` contains `cargo metadata --manifest-path packages/Cargo.toml --format-version 1`
    - `scripts/check-pure-core-deps.sh` contains `std::fs|std::net|std::env|std::process|std::thread`
    - `bash scripts/check-pure-core-deps.sh` exits 0 on the Phase 1 workspace
  </acceptance_criteria>
  <done>The repo has a hard-failing architectural gate for pure-core boundary violations.</done>
</task>

<task type="auto">
  <name>Task 2: Create the repo-native verification entrypoint</name>
  <files>scripts/verify.sh</files>
  <read_first>.planning/phases/01-workspace-baseline-and-guardrails/01-RESEARCH.md, scripts/check-pure-core-deps.sh, packages/Cargo.toml, scripts/verify.sh</read_first>
  <action>Create `scripts/verify.sh` with `set -euo pipefail` and make it run, in order: `bash scripts/check-pure-core-deps.sh`, `cargo fmt --manifest-path packages/Cargo.toml --all --check`, `cargo clippy --manifest-path packages/Cargo.toml --workspace --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features`, `cargo test --manifest-path packages/Cargo.toml --workspace --all-features`, and `cargo llvm-cov --manifest-path packages/Cargo.toml --package open-bitcoin-core --fail-under-lines 100 --summary-only`. The script must fail fast if `cargo llvm-cov` is missing and print the install hint `cargo +stable install cargo-llvm-cov --locked`.</action>
  <verify>bash scripts/verify.sh</verify>
  <acceptance_criteria>
    - `scripts/verify.sh` contains `bash scripts/check-pure-core-deps.sh`
    - `scripts/verify.sh` contains `cargo fmt --manifest-path packages/Cargo.toml --all --check`
    - `scripts/verify.sh` contains `cargo llvm-cov --manifest-path packages/Cargo.toml --package open-bitcoin-core --fail-under-lines 100 --summary-only`
    - `scripts/verify.sh` contains `cargo +stable install cargo-llvm-cov --locked`
  </acceptance_criteria>
  <done>The repo has one local verification contract that enforces architecture, lint, build, tests, and pure-core coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the verification contract in GitHub Actions</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>scripts/verify.sh, .github/pull_request_template.md, .github/workflows/ci.yml</read_first>
  <action>Create `.github/workflows/ci.yml` to run on pushes and pull requests, install Rust, install Bazelisk or otherwise honor `.bazelversion`, install `cargo-llvm-cov`, and execute `bash scripts/verify.sh`. Add a separate Bazel smoke step that runs `bazel build //:core //:node` after the Rust verification contract passes so the top-level Bazel surface stays green.</action>
  <verify>rg -n 'scripts/verify.sh|cargo-llvm-cov|bazel build //:core //:node' .github/workflows/ci.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `bash scripts/verify.sh`
    - `.github/workflows/ci.yml` contains `cargo-llvm-cov`
    - `.github/workflows/ci.yml` contains `bazel build //:core //:node`
    - `.github/workflows/ci.yml` triggers on both `push` and `pull_request`
  </acceptance_criteria>
  <done>CI enforces the same verification contract used by contributors locally.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/check-pure-core-deps.sh` passes
- [ ] `bash scripts/verify.sh` passes
- [ ] `rg -n 'scripts/verify.sh|cargo-llvm-cov|bazel build //:core //:node' .github/workflows/ci.yml` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Pure-core architecture violations fail fast
- CI and local verification use the same contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-baseline-and-guardrails/01-03-SUMMARY.md`
</output>
