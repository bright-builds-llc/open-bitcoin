---
phase: "11-panic-and-illegal-state-hardening"
plan: "03"
name: "Add Panic-Site Regression Guard"
---

# Phase 11 Plan 03: Add Panic-Site Regression Guard

## Objective

Prevent new unclassified production panic-like sites from entering first-party Rust crates.

## Tasks

1. Add a checked-in guard script that scans first-party production Rust code and skips test-only code.
2. Add an allowlist with narrow, reviewable entries for remaining classified invariant sites.
3. Wire the guard into `scripts/verify.sh`.
4. Produce phase summary evidence that the guard and repo-native verification pass.

## Verification

- `bash scripts/check-panic-sites.sh` passes.
- `bash scripts/verify.sh` passes.
