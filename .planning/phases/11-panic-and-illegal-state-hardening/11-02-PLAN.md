---
phase: "11-panic-and-illegal-state-hardening"
plan: "02"
name: "Replace Reachable Caller-Facing Crash Paths"
---

# Phase 11 Plan 02: Replace Reachable Caller-Facing Crash Paths

## Objective

Replace reachable production crash paths with typed errors while preserving external behavior.

## Tasks

1. Add or reuse narrow error variants for mempool, chainstate, wallet, and consensus boundary failures.
2. Replace reachable `expect` or `panic!` sites in mempool admission/state maintenance, chainstate apply/disconnect, wallet signing/building, RPC response construction, and taproot witness handling.
3. Keep local invariants only where the type and guard structure make the panic unreachable.
4. Add focused unit regressions for each changed reachable path.

## Verification

- Targeted package tests pass for changed crates.
- `cargo clippy --manifest-path packages/Cargo.toml --workspace --all-targets --all-features -- -D warnings` passes before final verification.
