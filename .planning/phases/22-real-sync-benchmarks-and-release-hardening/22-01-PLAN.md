---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-04-28T01-24-15
generated_at: 2026-04-28T01:24:15Z
phase: "22"
plan: "01"
title: "Runtime-backed benchmark coverage and reproducible report metadata"
status: planned
requirements:
  - SYNC-05
  - VER-05
  - VER-06
depends_on: []
verification:
  - cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture
  - bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports
artifacts:
  - packages/target/benchmark-reports/open-bitcoin-bench-smoke.json
  - packages/target/benchmark-reports/open-bitcoin-bench-smoke.md
---

# Plan 22-01: Runtime-backed benchmark coverage and reproducible report metadata

## Why

The current benchmark harness still reflects the earlier mapping-first audit phase. Phase 22 needs concrete, deterministic runtime evidence for sync, storage, dashboard, status, and wallet-rescan costs without turning local verification into a public-network timing gate.

## Must Be True

1. The benchmark registry includes deterministic scenarios that map directly to the Phase 22 runtime-hardening requirements.
2. The benchmark report contract makes the run profile and measured scenario intent explicit enough for local review.
3. The smoke path remains bounded and repo-native through `scripts/run-benchmarks.sh`.

## Tasks

### Task 1: Expand the benchmark report and registry contract

**Files:**
- `packages/open-bitcoin-bench/src/registry.rs`
- `packages/open-bitcoin-bench/src/report.rs`
- `packages/open-bitcoin-bench/src/runner.rs`
- `packages/open-bitcoin-bench/src/main.rs`

**Actions:**
1. Add the Phase 22 benchmark groups or case metadata needed to express real-sync, storage, operator-runtime, and wallet-rescan scenarios clearly.
2. Enrich the report schema with stable metadata that identifies run profile and scenario intent without introducing release-threshold logic.
3. Keep the smoke and full modes deterministic and easy to inspect in tests.

**Verify:**
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench registry:: runner:: report:: -- --nocapture`

### Task 2: Add deterministic runtime-backed benchmark cases

**Files:**
- `packages/open-bitcoin-bench/Cargo.toml`
- `packages/open-bitcoin-bench/src/cases.rs`
- `packages/open-bitcoin-bench/src/cases/*.rs`
- `packages/open-bitcoin-bench/src/fixtures.rs`

**Actions:**
1. Reuse existing repo fixtures or extract shared builders so the benchmark crate can measure headers sync, block connect, storage write or read, restart recovery, status or dashboard projection, and wallet-rescan work without public-network access.
2. Keep each benchmark case focused on one measurable concern and use tempdirs or scripted transports where persistence is required.
3. Add focused tests that prove the required runtime-backed groups remain registered and smoke-executable.

**Verify:**
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture`
- `bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports`
