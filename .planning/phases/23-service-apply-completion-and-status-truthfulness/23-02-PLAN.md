---
phase: 23-service-apply-completion-and-status-truthfulness
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - packages/open-bitcoin-cli/src/operator/service.rs
  - packages/open-bitcoin-cli/src/operator/service/fake.rs
  - packages/open-bitcoin-cli/src/operator/service/launchd.rs
  - packages/open-bitcoin-cli/src/operator/service/systemd.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/service/tests.rs
autonomous: true
requirements:
  - SVC-04
  - DASH-03
  - SVC-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-04-28T17-18-36
generated_at: 2026-04-28T17:18:36.921Z

must_haves:
  truths:
    - "Service snapshots carry manager-reported enablement when available instead of forcing downstream inference."
    - "CLI status and dashboard status projections prefer manager evidence over enum heuristics for enabled or running truth."
    - "Service status output explicitly surfaces installed, enabled, and running booleans alongside the lifecycle label."
    - "Dashboard service actions continue to rely on the shared service runtime path and do not need a forked action implementation."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/service.rs"
      provides: "explicit enabled-state evidence and status rendering"
    - path: "packages/open-bitcoin-cli/src/operator/status.rs"
      provides: "truthful service projection into the shared operator snapshot"
    - path: "packages/open-bitcoin-cli/src/operator/status/tests.rs"
      provides: "manager-evidence projection tests"
  key_links:
    - from: "service/launchd.rs and service/systemd.rs"
      to: "status.rs"
      via: "ServiceStateSnapshot::maybe_enabled"
    - from: "dashboard/action.rs"
      to: "service.rs execute_service_command"
      via: "shared confirmation-gated runtime path remains unchanged"
---

# Phase 23 Plan 02: Make Service Truth Explicit

## Objective

Preserve manager-reported startup enablement through the shared service snapshot
and use it to drive CLI status and dashboard projections so the service surface
stops guessing from `ServiceLifecycleState` alone.

## Scope

- `ServiceStateSnapshot` contract updates
- launchd and systemd enabled-state inspection
- `collect_service_status()` truthfulness
- CLI service-status rendering and focused tests

## Tasks

1. Add explicit enabled-state evidence to the shared service snapshot contract.
2. Teach launchd and systemd status inspection to populate that field from real
   manager queries when possible.
3. Update service-status rendering and `collect_service_status()` to preserve
   manager truth for failed-enabled and running-disabled combinations.
4. Prove the new behavior with fake-manager tests and service-status render
   assertions.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service::tests -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status::tests -- --nocapture`
