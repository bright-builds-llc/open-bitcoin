---
phase: 94-dos-and-resource-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/resource.rs
  - packages/open-bitcoin-network/src/resource/tests.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [DOS-01, DOS-05]
must_haves:
  truths:
    - "Inbound wire headers are classified before payload allocation."
    - "Wrong magic, malformed headers, oversized payloads, unsupported commands, bad checksums, malformed payloads, and trailing payload bytes produce stable labels."
    - "The new resource policy module is exported from open-bitcoin-network and registered in parity breadcrumbs."
  artifacts:
    - path: "packages/open-bitcoin-network/src/resource.rs"
      provides: "Pure message-envelope resource gate and stable labels"
    - path: "packages/open-bitcoin-network/src/resource/tests.rs"
      provides: "Deterministic boundary tests for parser and allocation labels"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Breadcrumb registration for new first-party Rust source files"
  key_links:
    - from: "packages/open-bitcoin-network/src/resource.rs"
      to: "open_bitcoin_codec::parse_message_header"
      via: "header classification"
      pattern: "parse_message_header"
    - from: "packages/open-bitcoin-network/src/lib.rs"
      to: "packages/open-bitcoin-network/src/resource.rs"
      via: "public re-export"
      pattern: "pub use resource::"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Create the pure Phase 94 message-envelope resource gate so inbound runtime code can reject invalid or unsupported wire input before unbounded payload allocation or peer-side work.

Purpose: Satisfy D-01, D-02, D-03, D-04, D-12, D-18, and DOS-01 with reusable functional-core policy.
Output: `open-bitcoin-network` exports stable resource labels, events, constants, and envelope decisions with deterministic tests and parity breadcrumbs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-network/src/message.rs
@packages/open-bitcoin-codec/src/network.rs
@packages/open-bitcoin-primitives/src/network.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add resource envelope contracts and exports</name>
  <files>packages/open-bitcoin-network/src/resource.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-codec/src/network.rs`
    - `packages/open-bitcoin-primitives/src/network.rs`
    - `packages/open-bitcoin-network/src/lib.rs`
    - `docs/parity/source-breadcrumbs.json`
    - `.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md`
    - `.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md`
  </read_first>
  <behavior>
    - Test 1: 24-byte header with non-local `NetworkMagic` returns label `wrong_network_magic` without reading payload bytes.
    - Test 2: payload length `PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES + 1` returns label `payload_oversized`.
    - Test 3: unsupported command `mempool` returns label `unsupported_command` and next action `payload_rejected`.
  </behavior>
  <action>
    Create `packages/open-bitcoin-network/src/resource.rs` with parity breadcrumbs for `packages/bitcoin-knots/src/protocol.h`, `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, and `packages/bitcoin-knots/test/functional/p2p_invalid_messages.py`. Define `pub const INBOUND_MESSAGE_HEADER_LEN: usize = 24;`, `pub const PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES: usize = 32 * 1024 * 1024;`, `pub enum ResourceViolationLabel { WrongNetworkMagic, MalformedHeader, PayloadOversized, InvalidChecksum, UnsupportedCommand, MalformedPayload, TrailingPayload }`, `impl ResourceViolationLabel { pub const fn as_str(self) -> &'static str }` returning exactly `wrong_network_magic`, `malformed_header`, `payload_oversized`, `invalid_checksum`, `unsupported_command`, `malformed_payload`, and `trailing_payload`; `pub enum ResourceGovernanceSource { EnvelopeGate, PayloadDecoder, RuntimeRead }` with `as_str()` values `source_envelope_gate`, `source_payload_decoder`, and `source_runtime_read`; `pub struct InboundResourceEvent { pub outcome: String, pub reason: String, pub label: String, pub source: String, pub message: String, pub next_action: String }`; `pub enum InboundEnvelopeDecision { ReadPayload { payload_len: usize }, Reject(InboundResourceEvent) }`; and `pub struct InboundEnvelopePolicy { pub expected_magic: NetworkMagic, pub max_payload_bytes: usize }`. Implement `InboundEnvelopePolicy::new(expected_magic: NetworkMagic) -> Self`, `evaluate_header(&self, header_bytes: &[u8]) -> InboundEnvelopeDecision`, `decode_payload(&self, header: &MessageHeader, payload: &[u8]) -> Result<ParsedNetworkMessage, InboundResourceEvent>`, and `event(label: ResourceViolationLabel, source: ResourceGovernanceSource, reason: impl Into<String>) -> InboundResourceEvent`. Export the new module and public types from `packages/open-bitcoin-network/src/lib.rs`. Add a `network-resource-governance` entry to `docs/parity/source-breadcrumbs.json` for `packages/open-bitcoin-network/src/resource.rs` and `packages/open-bitcoin-network/src/resource/tests.rs`.
  </action>
  <acceptance_criteria>
    - `rg -n "pub enum ResourceViolationLabel|pub struct InboundResourceEvent|pub enum InboundEnvelopeDecision|pub struct InboundEnvelopePolicy" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "wrong_network_magic|malformed_header|payload_oversized|invalid_checksum|unsupported_command|malformed_payload|trailing_payload|payload_rejected" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES: usize = 32 \\* 1024 \\* 1024|INBOUND_MESSAGE_HEADER_LEN: usize = 24" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "mod resource|pub use resource::" packages/open-bitcoin-network/src/lib.rs`
    - `rg -n "network-resource-governance|packages/open-bitcoin-network/src/resource.rs|packages/bitcoin-knots/src/net_processing.cpp" docs/parity/source-breadcrumbs.json`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <done>Resource envelope contracts are exported, breadcrumbed, and compile with no runtime socket or allocation side effects in the policy module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover envelope rejection labels with deterministic tests</name>
  <files>packages/open-bitcoin-network/src/resource/tests.rs, packages/open-bitcoin-network/src/resource.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-network/src/message/tests.rs`
    - `packages/open-bitcoin-codec/src/network.rs`
  </read_first>
  <behavior>
    - Test 1: malformed header shorter than 24 bytes maps to `malformed_header`.
    - Test 2: valid command with invalid checksum maps to `invalid_checksum`.
    - Test 3: malformed payload maps to `malformed_payload`.
    - Test 4: payload bytes with extra trailing data maps to `trailing_payload`.
  </behavior>
  <action>
    Add `#[cfg(test)] mod tests;` to `resource.rs` and create `packages/open-bitcoin-network/src/resource/tests.rs`. Tests must use Arrange/Act/Assert comments and no sleeps. Add test functions named `wrong_magic_is_rejected_before_payload_allocation`, `malformed_header_is_rejected`, `oversized_payload_is_rejected_before_vec_allocation`, `unsupported_command_is_bounded_evidence`, `invalid_checksum_is_resource_label`, `malformed_payload_is_resource_label`, and `trailing_payload_is_resource_label`. Use `NetworkMagic::mainnet()` or the repo's existing mainnet constructor from `open_bitcoin_primitives`, `MessageCommand::from_wire_bytes(*b"mempool\\0\\0\\0\\0\\0")` for unsupported command evidence, and `WireNetworkMessage::Version` fixtures from existing message tests where possible. Assert the exact `InboundResourceEvent` field values: `outcome == "rejected"`, `next_action == "payload_rejected"`, and `label` equal to the expected D-04 label.
  </action>
  <acceptance_criteria>
    - `rg -n "wrong_magic_is_rejected_before_payload_allocation|malformed_header_is_rejected|oversized_payload_is_rejected_before_vec_allocation|unsupported_command_is_bounded_evidence|invalid_checksum_is_resource_label|malformed_payload_is_resource_label|trailing_payload_is_resource_label" packages/open-bitcoin-network/src/resource/tests.rs`
    - `rg -n "// Arrange|// Act|// Assert" packages/open-bitcoin-network/src/resource/tests.rs`
    - `rg -n "assert_eq!\\(.*wrong_network_magic|assert_eq!\\(.*payload_oversized|assert_eq!\\(.*unsupported_command|assert_eq!\\(.*trailing_payload" packages/open-bitcoin-network/src/resource/tests.rs`
    - `! rg -n "sleep|TcpStream|tokio::spawn|std::thread" packages/open-bitcoin-network/src/resource.rs packages/open-bitcoin-network/src/resource/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Every D-04 parser/resource label is covered by a deterministic pure test with exact expected strings.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| remote peer -> wire parser | Untrusted bytes cross from TCP into Open Bitcoin message parsing. |
| parser -> peer manager | Parsed messages become peer-side state transitions only after policy acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-01-01 | Tampering | `InboundEnvelopePolicy::evaluate_header` | mitigate | Validate magic, command header shape, and payload length before returning `ReadPayload`. |
| T-94-01-02 | Denial of Service | inbound payload allocation | mitigate | Reject payloads larger than `PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES` before creating a payload `Vec`. |
| T-94-01-03 | Information Disclosure | resource events | mitigate | Events expose only low-cardinality labels and fixed messages; no peer endpoint, payload bytes, credentials, or permission strings. |
| T-94-01-04 | Elevation of Privilege | unsupported command handling | mitigate | Unsupported commands return bounded `unsupported_command` evidence and do not add relay, mempool, compact block, BIP37, or compact-filter support. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
</verification>

<success_criteria>
DOS-01 is satisfied at the pure-core boundary: resource labels and allocation gates exist, are exported, and are proven by deterministic tests. DOS-05 is partially satisfied through public-network-free unit verification.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-01-SUMMARY.md`.
</output>
