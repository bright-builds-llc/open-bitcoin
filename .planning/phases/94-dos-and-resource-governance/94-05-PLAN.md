---
phase: 94-dos-and-resource-governance
plan: 05
type: execute
wave: 4
depends_on: ["94-03", "94-04"]
files_modified:
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-node/src/network/inbound.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-node/src/metrics.rs
autonomous: true
requirements: [DOS-04, DOS-05]
must_haves:
  truths:
    - "Resource pressure and abuse responses are projected through shared inbound status before renderers."
    - "Metrics use fixed `MetricKind` variants with no dynamic labels."
    - "Shared resource evidence is bounded and omits raw peer identifiers, endpoints, payload bytes, permission strings, and credentials."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Shared serializable resource-governance evidence contract"
    - path: "packages/open-bitcoin-node/src/network/inbound.rs"
      provides: "Managed projection from resource events to status counters"
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Fixed resource-governance metric variants"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "current_inbound_status"
      pattern: "latest_resource_governance_decision"
    - from: "packages/open-bitcoin-node/src/metrics.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "fixed MetricKind variants"
      pattern: "InboundPayloadRejectedCount"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Project Phase 94 resource-governance evidence through the shared node status and fixed metrics layers before CLI/support rendering.

Purpose: Satisfy D-13, D-14, D-15, D-16, D-18, DOS-04, and DOS-05 while avoiding renderer-local summaries.
Output: Shared status fields, managed resource info, RPC context wiring, and fixed metric variants.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-node/src/network/inbound.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-node/src/metrics.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add shared resource status and managed projection</name>
  <files>packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-node/src/network/inbound.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-rpc/src/context/network.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/status/inbound.rs`
    - `packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `packages/open-bitcoin-node/src/network/inbound.rs`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
  </read_first>
  <behavior>
    - Test 1: default-deserialized inbound status contains unavailable `latest_resource_governance_decision` with reason `inbound resource governance evidence unavailable`.
    - Test 2: `payload_rejected` event increments `payload_rejections` and sets latest next action `payload_rejected`.
    - Test 3: `read_queue_pressure`, `write_queue_pressure`, `request_cap_reached`, `timeout_disconnect`, `churn_rejected`, and `reconnect_suppressed` map to separate counters.
  </behavior>
  <action>
    In `status/inbound.rs`, add `pub const INBOUND_RESOURCE_DECISION_UNAVAILABLE_REASON: &str = "inbound resource governance evidence unavailable";`, `pub struct InboundResourceGovernanceEvent { pub outcome: String, pub reason: String, pub label: String, pub source: String, pub message: String, pub next_action: String }`, and serde-defaulted fields on `InboundPeerServingStatus`: `resource_pressure_events: u32`, `read_queue_pressure_events: u32`, `write_queue_pressure_events: u32`, `request_cap_events: u32`, `payload_rejections: u32`, `timeout_disconnects: u32`, `churn_rejections: u32`, `reconnect_suppressions: u32`, and `latest_resource_governance_decision: FieldAvailability<InboundResourceGovernanceEvent>`. Add `latest_resource_governance_decision_unavailable()` returning the new unavailable reason. In `network/inbound.rs`, add `pub struct ManagedResourceGovernanceInfo` with the same counters and `maybe_latest_resource_governance_decision: Option<InboundResourceGovernanceEvent>`, plus `is_empty()` and `record_event(&mut self, event: InboundResourceEvent)`. The mapping must increment counters from `event.next_action` exactly: `resource_pressure_active`, `read_queue_pressure`, `write_queue_pressure`, `request_cap_reached`, `payload_rejected`, `timeout_disconnect`, `churn_rejected`, `reconnect_suppressed`. Export `ManagedResourceGovernanceInfo` from `network.rs`. Add `ManagedPeerNetwork::resource_governance_info(&self) -> ManagedResourceGovernanceInfo` and `ManagedPeerNetwork::record_resource_governance_event(&mut self, event: InboundResourceEvent)`. In `context/network.rs`, include listener `maybe_latest_resource_event` and managed network resource info in `current_inbound_status()`, and add `ManagedRpcContext::record_inbound_resource_event(&mut self, event: InboundResourceEvent)`.
  </action>
  <acceptance_criteria>
    - `rg -n "INBOUND_RESOURCE_DECISION_UNAVAILABLE_REASON|InboundResourceGovernanceEvent|resource_pressure_events|read_queue_pressure_events|write_queue_pressure_events|request_cap_events|payload_rejections|timeout_disconnects|churn_rejections|reconnect_suppressions|latest_resource_governance_decision" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `rg -n "ManagedResourceGovernanceInfo|record_event|record_resource_governance_event|resource_governance_info" packages/open-bitcoin-node/src/network/inbound.rs packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-rpc/src/context/network.rs`
    - `rg -n "resource_pressure_active|read_queue_pressure|write_queue_pressure|request_cap_reached|payload_rejected|timeout_disconnect|churn_rejected|reconnect_suppressed" packages/open-bitcoin-node/src/network/inbound.rs packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `! rg -n "peer_id|raw_endpoint|payload_bytes|permission_string|credential|secret" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-node/src/network/inbound.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc context --no-fail-fast</automated>
  </verify>
  <done>Resource events flow through shared status and managed network projections with bounded, redacted fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixed resource-governance metric variants</name>
  <files>packages/open-bitcoin-node/src/metrics.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/metrics.rs`
    - `packages/open-bitcoin-node/src/status/inbound.rs`
    - `packages/open-bitcoin-node/src/status/inbound/tests.rs`
  </read_first>
  <behavior>
    - Test 1: `MetricKind::ALL` includes each Phase 94 resource metric exactly once.
    - Test 2: `MetricKind::as_str()` returns the exact snake_case strings listed in the action.
  </behavior>
  <action>
    Add these `MetricKind` variants after existing Phase 93 inbound policy metrics: `InboundResourcePressureActiveCount`, `InboundReadQueuePressureCount`, `InboundWriteQueuePressureCount`, `InboundRequestCapReachedCount`, `InboundPayloadRejectedCount`, `InboundTimeoutDisconnectCount`, `InboundChurnRejectedCount`, and `InboundReconnectSuppressedCount`. Add them to `MetricKind::ALL` and update the array length from `26` to `34`. Add `as_str()` mappings exactly: `inbound_resource_pressure_active_count`, `inbound_read_queue_pressure_count`, `inbound_write_queue_pressure_count`, `inbound_request_cap_reached_count`, `inbound_payload_rejected_count`, `inbound_timeout_disconnect_count`, `inbound_churn_rejected_count`, and `inbound_reconnect_suppressed_count`. Update metrics tests to assert the new names and the `ALL.len() == 34` invariant.
  </action>
  <acceptance_criteria>
    - `rg -n "InboundResourcePressureActiveCount|InboundReadQueuePressureCount|InboundWriteQueuePressureCount|InboundRequestCapReachedCount|InboundPayloadRejectedCount|InboundTimeoutDisconnectCount|InboundChurnRejectedCount|InboundReconnectSuppressedCount" packages/open-bitcoin-node/src/metrics.rs`
    - `rg -n "inbound_resource_pressure_active_count|inbound_read_queue_pressure_count|inbound_write_queue_pressure_count|inbound_request_cap_reached_count|inbound_payload_rejected_count|inbound_timeout_disconnect_count|inbound_churn_rejected_count|inbound_reconnect_suppressed_count" packages/open-bitcoin-node/src/metrics.rs`
    - `rg -n "pub const ALL: \\[Self; 34\\]" packages/open-bitcoin-node/src/metrics.rs`
    - `! rg -n "String\\)|BTreeMap<.*MetricKind|HashMap<.*MetricKind" packages/open-bitcoin-node/src/metrics.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Resource-governance metrics are fixed variants with exact low-cardinality names and updated retention coverage.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| runtime listener/peer policy -> shared status | Effectful resource events become serialized operator-visible evidence. |
| shared status -> metrics | Resource counters become retained metric series. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-05-01 | Information Disclosure | inbound status | mitigate | Status fields are counters and low-cardinality events only; acceptance criteria reject raw peer IDs, endpoints, payloads, permissions, and credentials. |
| T-94-05-02 | Denial of Service | metrics cardinality | mitigate | Use fixed `MetricKind` variants and no dynamic metric labels. |
| T-94-05-03 | Repudiation | operator evidence | mitigate | Preserve latest resource-governance outcome, reason, label, source, message, and next action. |
| T-94-05-04 | Tampering | status defaults | mitigate | Add serde defaults so older status payloads cannot omit resource fields into inconsistent state. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc context --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
DOS-04 resource pressure evidence is available through shared status and fixed metrics without renderer-local summaries or dynamic labels. DOS-05 remains deterministic through unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-05-SUMMARY.md`.
</output>
