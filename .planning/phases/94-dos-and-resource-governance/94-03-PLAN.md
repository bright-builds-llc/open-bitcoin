---
phase: 94-dos-and-resource-governance
plan: 03
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
autonomous: true
requirements: [DOS-01, DOS-02, DOS-03, DOS-04, DOS-05]
must_haves:
  truths:
    - "The opt-in inbound listener evaluates the header before allocating payload bytes."
    - "The opt-in inbound listener consumes `ResourceGovernancePolicy::decide_queue` before reads and writes, and records resulting resource events."
    - "Socket read/write waits are bounded by resource-policy timeouts so idle `read_exact` and `write_all` cannot wait indefinitely."
    - "Accept-loop churn, repeated-failure, and banned/discouraged reconnect suppression are evaluated before admission or peer work."
    - "Runtime resource rejections are recorded as bounded listener evidence."
    - "Slow handshakes and idle peers use policy output and remain testable without public-network traffic."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      provides: "Runtime adapter for resource envelope, queue/backpressure, timeout, churn, and reconnect decisions"
    - path: "packages/open-bitcoin-rpc/src/inbound_listener/tests.rs"
      provides: "Loopback/synthetic inbound resource adapter tests"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Phase 93 ban/discourage inputs for reconnect suppression and resource-event recording"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      to: "packages/open-bitcoin-network/src/resource.rs"
      via: "InboundEnvelopePolicy and ResourceGovernancePolicy"
      pattern: "InboundEnvelopePolicy|ResourceGovernancePolicy|decide_queue|decide_timeout|decide_churn|decide_repeated_failure|decide_reconnect"
    - from: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      to: "packages/open-bitcoin-rpc/src/context/network.rs"
      via: "ManagedRpcContext reconnect-suppression and resource-event methods"
      pattern: "reconnect_suppression_input_for_remote_addr|record_inbound_resource_event"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Wire the pure resource envelope, queue/backpressure, timeout, churn, and reconnect policies into the Phase 90 opt-in inbound listener without broadening public-network defaults or relay support.

Purpose: Satisfy D-01, D-03, D-04, D-05, D-06, D-09, D-10, D-11, D-12, D-13, D-17, D-18, DOS-01, DOS-02, DOS-03, and DOS-04 at the runtime boundary.
Output: Runtime listener evidence for payload rejection, queue/backpressure enforcement, timeout disconnects, churn rejection, repeated-failure limits, and reconnect suppression.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-rpc/src/inbound_listener.rs
@packages/open-bitcoin-network/src/resource.rs
@packages/open-bitcoin-rpc/src/context/network.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace listener payload-length parsing with envelope policy</name>
  <files>packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
  </read_first>
  <behavior>
    - Test 1: oversized header returns `payload_oversized` and does not allocate payload bytes.
    - Test 2: wrong magic returns `wrong_network_magic` and closes the message loop.
    - Test 3: unsupported command returns `unsupported_command`, records evidence, and does not call `receive_inbound_wire_message`.
  </behavior>
  <action>
    In `inbound_listener.rs`, remove listener-local constants `MESSAGE_HEADER_LEN`, `PAYLOAD_SIZE_OFFSET`, `PAYLOAD_SIZE_LEN`, and `MAX_INBOUND_RUNTIME_PAYLOAD_BYTES` or replace all uses with `INBOUND_MESSAGE_HEADER_LEN` and `PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES` imported from `open_bitcoin_network`. Add `enum ReadWireMessageOutcome { Message(ParsedNetworkMessage), Rejected(InboundResourceEvent) }`. Change `read_wire_message` to `async fn read_wire_message(stream: &tokio::net::TcpStream, policy: &InboundEnvelopePolicy) -> io::Result<ReadWireMessageOutcome>`. The function must read exactly `INBOUND_MESSAGE_HEADER_LEN` bytes, call `policy.evaluate_header(&header)` before allocating a payload `Vec`, allocate only when the result is `InboundEnvelopeDecision::ReadPayload { payload_len }`, read exactly that payload, then call `policy.decode_payload(&header_message, &payload)` or the equivalent policy helper before returning a parsed message. Update `handle_inbound_stream` to build `let envelope_policy = InboundEnvelopePolicy::new(context.network_info().network_magic);` after admission and before the message loop. On `Rejected(event)`, call `lock_evidence(&evidence).record_resource_event(event)` and break the loop.
  </action>
  <acceptance_criteria>
    - `rg -n "ReadWireMessageOutcome|InboundEnvelopePolicy|InboundEnvelopeDecision|InboundResourceEvent|INBOUND_MESSAGE_HEADER_LEN|PHASE94_MAX_INBOUND_RUNTIME_PAYLOAD_BYTES" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `! rg -n "PAYLOAD_SIZE_OFFSET|PAYLOAD_SIZE_LEN|MAX_INBOUND_RUNTIME_PAYLOAD_BYTES|payload_len_from_header" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `rg -n "payload_oversized|wrong_network_magic|unsupported_command|receive_inbound_wire_message" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `rg -n "evaluate_header\\(&header\\).*vec!|InboundEnvelopeDecision::ReadPayload" packages/open-bitcoin-rpc/src/inbound_listener.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast</automated>
  </verify>
  <done>Listener wire reads route through the pure envelope policy and expose rejected payload evidence before peer processing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record timeout, churn, and reconnect resource evidence in listener state</name>
  <files>packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs, packages/open-bitcoin-rpc/src/context/network.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/peer_policy.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
  </read_first>
  <behavior>
    - Test 1: resource event `slow_handshake` increments `timeout_disconnects` and stores latest event next action `timeout_disconnect`.
    - Test 2: a paused Tokio clock advances past the Phase 94 read timeout while `read_exact` is waiting and returns a `ReadWireMessageOutcome::Rejected` event labeled `idle_peer` or `slow_handshake`; no test uses wall-clock sleeps.
    - Test 3: resource event `connection_churn_limited` increments `churn_rejections` and stores next action `churn_rejected`.
    - Test 4: accept-loop repeated failures return `repeated_failure_limited` before spawning `handle_inbound_stream`.
    - Test 5: Phase 93 `PeerBanBook::maybe_ban_decision` and discourage/misbehavior inputs map to `reconnect_suppressed_banned` and `reconnect_suppressed_discouraged` before admission.
  </behavior>
  <action>
    Extend `InboundListenerEvidence` with serde-free runtime fields `pub resource_rejections: usize`, `pub timeout_disconnects: usize`, `pub churn_rejections: usize`, `pub reconnect_suppressions: usize`, `pub maybe_latest_resource_event: Option<InboundResourceEvent>`, and fixed-window counters needed for accept-loop churn/repeated-failure suppression. Initialize every new counter to `0` and the latest event to `None` in `from_activation`, `inactive`, and `listening`. Add `fn record_resource_event(&mut self, event: InboundResourceEvent)` that increments `resource_rejections` for `next_action == "payload_rejected"`, `timeout_disconnects` for `next_action == "timeout_disconnect"`, `churn_rejections` for `next_action == "churn_rejected"`, and `reconnect_suppressions` for `next_action == "reconnect_suppressed"`, then stores the event. In `packages/open-bitcoin-rpc/src/context/network.rs`, add `ManagedRpcContext::record_inbound_resource_event(&mut self, event: InboundResourceEvent)` and `ManagedRpcContext::reconnect_suppression_input_for_remote_addr(&self, remote_addr: SocketAddr, now_unix_seconds: i64) -> ReconnectSuppressionInput`; the reconnect method must consume Phase 93 source-of-truth inputs (`PeerBanBook::maybe_ban_decision`, `BanScope`, `BanDecision`, `MisbehaviorDecision`, and `MisbehaviorResponse::Discourage` or the existing managed peer-policy projection), not listener-local ban maps. In `accept_loop`, before fetching a peer id or spawning `handle_inbound_stream`, call `ResourceGovernancePolicy::default().decide_churn(ConnectionChurnInput { ... })`, `decide_repeated_failure(RepeatedFailureInput { ... })`, and `decide_reconnect(context.lock().await.reconnect_suppression_input_for_remote_addr(remote_addr, now))`; for any non-`Accept` decision, call `record_resource_event` on listener evidence, call `ManagedRpcContext::record_inbound_resource_event`, and `continue` without admission or peer work. Add `resource_timeout_event(policy: &ResourceGovernancePolicy, connected_at_unix_seconds: i64, last_activity_unix_seconds: i64, now_unix_seconds: i64, handshake_state: InboundHandshakeState) -> Option<InboundResourceEvent>` and use it to derive timeout events for both pre-read and post-message activity checks. Replace unbounded waits in `read_exact` and `write_all` with `tokio::time::timeout(Duration::from_secs(PHASE94_SLOW_HANDSHAKE_TIMEOUT_SECONDS as u64), stream.readable())` or `Duration::from_secs(PHASE94_IDLE_PEER_TIMEOUT_SECONDS as u64)` based on `InboundHandshakeState`; on timeout, return `ReadWireMessageOutcome::Rejected(event)` or an equivalent typed write outcome and record it before disconnecting. Do not add sleeps to tests; use fixed timestamps or `tokio::time::pause` / `advance` for socket timeout tests.
  </action>
  <acceptance_criteria>
    - `rg -n "resource_rejections|timeout_disconnects|churn_rejections|reconnect_suppressions|maybe_latest_resource_event|record_resource_event|resource_timeout_event" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `rg -n "decide_churn|decide_repeated_failure|decide_reconnect|ConnectionChurnInput|RepeatedFailureInput|ReconnectSuppressionInput|reconnect_suppression_input_for_remote_addr|record_inbound_resource_event" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/context/network.rs`
    - `rg -n "tokio::time::timeout|PHASE94_SLOW_HANDSHAKE_TIMEOUT_SECONDS|PHASE94_IDLE_PEER_TIMEOUT_SECONDS|read_exact.*timeout|write_all.*timeout" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `rg -n "slow_handshake|idle_peer|connection_churn_limited|repeated_failure_limited|reconnect_suppressed_banned|reconnect_suppressed_discouraged|timeout_disconnect|churn_rejected|reconnect_suppressed" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `! rg -n "sleep|tokio::time::sleep|std::thread::sleep" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `! rg -n "public|0\\.0\\.0\\.0|mainnet listener" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Listener evidence records resource rejections and timeout/churn/reconnect next actions with deterministic tests and no public-network dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce queue/backpressure decisions in runtime read and write paths</name>
  <files>packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs, packages/open-bitcoin-rpc/src/context/network.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/inbound/permissions.rs`
  </read_first>
  <behavior>
    - Test 1: a read queue input above `PHASE94_MAX_PEER_READ_QUEUE_BYTES` calls `ResourceGovernancePolicy::decide_queue`, records `read_queue_pressure`, and does not enter `read_wire_message`.
    - Test 2: a write queue input above `PHASE94_MAX_PEER_WRITE_QUEUE_BYTES` records `write_queue_pressure`, skips `write_all`, and disconnects the admitted peer.
    - Test 3: aggregate queued message pressure records `resource_pressure_active` through both `InboundListenerEvidence::record_resource_event` and `ManagedRpcContext::record_inbound_resource_event`.
  </behavior>
  <action>
    Add a small runtime queue/accounting helper in `inbound_listener.rs`: `struct RuntimeQueuePressureState` with per-peer and aggregate read/write byte counts and queued-message counts, plus `fn queue_pressure_input(&self, active_permission_effects: Vec<PermissionEffectLabel>, inactive_permission_effects: Vec<InactivePermissionEffectLabel>) -> QueuePressureInput`. In `handle_inbound_stream`, after admission succeeds and before each call to `read_wire_message`, build a `QueuePressureInput` using the admitted peer record's `permission_decision.active_effects().to_vec()` and `permission_decision.inactive_effects().to_vec()`, then call `ResourceGovernancePolicy::default().decide_queue(input)`. For `Backpressure`, `Disconnect`, or `RecordMisbehavior`, call `InboundListenerEvidence::record_resource_event(event.clone())`, call `ManagedRpcContext::record_inbound_resource_event(event)`, and break before reading from the socket. Before each `write_all` response, account for `response.len()` and response count in the same helper, call `decide_queue`, record any resulting event through both evidence paths, skip the write on pressure, and disconnect. Keep actual queue counters bounded with saturating arithmetic and do not log or store raw payload bytes, peer ids, or endpoints in the resource event.
  </action>
  <acceptance_criteria>
    - `rg -n "RuntimeQueuePressureState|queue_pressure_input|QueuePressureInput|decide_queue|PHASE94_MAX_PEER_READ_QUEUE_BYTES|PHASE94_MAX_PEER_WRITE_QUEUE_BYTES" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `rg -n "record_resource_event\\(event\\.clone\\(\\)\\)|record_inbound_resource_event\\(event\\)|read_queue_pressure|write_queue_pressure|resource_pressure_active" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `rg -n "active_effects\\(\\)\\.to_vec\\(\\)|inactive_effects\\(\\)\\.to_vec\\(\\)" packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `! rg -n "payload_bytes|peer_id.*resource|raw_endpoint|permission_string|credential|secret" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>`ResourceGovernancePolicy::decide_queue` is consumed by real listener read/write paths, pressure decisions enforce backpressure or disconnect behavior, and resulting events are recorded in listener and managed resource evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| TCP stream -> listener adapter | Untrusted inbound bytes cross into async runtime code. |
| listener adapter -> managed RPC context | Accepted parsed messages may mutate managed node state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-03-01 | Denial of Service | `read_wire_message` | mitigate | Call `InboundEnvelopePolicy::evaluate_header` before allocating a payload buffer. |
| T-94-03-02 | Tampering | parsed wire messages | mitigate | Call resource payload decode before `receive_inbound_wire_message`; reject unsupported/malformed payloads as evidence. |
| T-94-03-03 | Repudiation | listener resource evidence | mitigate | Record stable latest resource event and bounded counters in `InboundListenerEvidence`. |
| T-94-03-04 | Information Disclosure | runtime evidence | mitigate | Store only event labels, reasons, sources, messages, and next actions; no raw endpoints or payload bytes. |
| T-94-03-05 | Denial of Service | `read_exact` / `write_all` waits | mitigate | Wrap socket readiness waits in Phase 94 timeout durations and convert elapsed waits into resource events before disconnect. |
| T-94-03-06 | Denial of Service | accept loop admission path | mitigate | Evaluate churn, repeated-failure, and Phase 93 ban/discourage reconnect suppression before peer id allocation or admission. |
| T-94-03-07 | Denial of Service | runtime read/write queues | mitigate | Call `ResourceGovernancePolicy::decide_queue` before socket reads and response writes; record and enforce pressure decisions. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
- `rg -n "decide_queue|tokio::time::timeout|decide_churn|decide_reconnect|record_inbound_resource_event" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/context/network.rs`
</verification>

<success_criteria>
DOS-01, DOS-02, and DOS-03 are enforced by the loopback-safe inbound listener adapter, and DOS-04 has bounded listener evidence ready for shared status projection.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-03-SUMMARY.md`.
</output>
