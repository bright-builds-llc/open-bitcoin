---
phase: 94-dos-and-resource-governance
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - packages/open-bitcoin-network/src/resource.rs
  - packages/open-bitcoin-network/src/resource/tests.rs
autonomous: true
requirements: [DOS-02, DOS-03, DOS-05]
must_haves:
  truths:
    - "Read/write queue pressure is decided by pure data-in/data-out policy."
    - "Inventory, getdata, header, block, and transaction request caps have named constants and stable outcomes."
    - "Slow handshakes, idle peers, churn, repeated failures, and banned/discouraged reconnect attempts are deterministic timestamp/counter decisions."
  artifacts:
    - path: "packages/open-bitcoin-network/src/resource.rs"
      provides: "Pure queue, request, timeout, churn, failure, and reconnect policy"
    - path: "packages/open-bitcoin-network/src/resource/tests.rs"
      provides: "Synthetic deterministic policy tests"
  key_links:
    - from: "packages/open-bitcoin-network/src/resource.rs"
      to: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      via: "permission effect inputs"
      pattern: "PermissionEffectLabel|InactivePermissionEffectLabel"
    - from: "packages/open-bitcoin-network/src/resource.rs"
      to: "packages/open-bitcoin-network/src/peer_policy.rs"
      via: "ban/discourage reconnect input"
      pattern: "BanDecision|MisbehaviorKind"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Extend the pure resource-governance module with queue/request, timeout, churn, repeated-failure, and reconnect policies consumed by later runtime and peer-manager plans.

Purpose: Satisfy D-02, D-05 through D-12, D-18, DOS-02, and DOS-03 without socket effects, sleeps, or public-network verification.
Output: A typed policy surface with stable resource-pressure labels and deterministic tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-network/src/resource.rs
@packages/open-bitcoin-network/src/inbound/permissions.rs
@packages/open-bitcoin-network/src/peer_policy.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add queue and request resource policy</name>
  <files>packages/open-bitcoin-network/src/resource.rs, packages/open-bitcoin-network/src/resource/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/resource/tests.rs`
    - `packages/open-bitcoin-network/src/inbound/permissions.rs`
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/message.rs`
  </read_first>
  <behavior>
    - Test 1: peer read queue above `PHASE94_MAX_PEER_READ_QUEUE_BYTES` returns label `read_queue_pressure` and next action `read_queue_pressure`.
    - Test 2: peer write queue above `PHASE94_MAX_PEER_WRITE_QUEUE_BYTES` returns label `write_queue_pressure` and next action `write_queue_pressure`.
    - Test 3: inbound getdata inventory above `PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS` returns label `request_cap_reached`.
    - Test 4: inactive relay-like effects `InactivePermissionEffectLabel::Relay`, `ForceRelay`, `Mempool`, `BloomFilter`, and `BlockFilters` do not raise request or queue caps.
  </behavior>
  <action>
    In `resource.rs`, add named constants: `PHASE94_MAX_PEER_READ_QUEUE_BYTES: usize = 1 * 1024 * 1024`, `PHASE94_MAX_PEER_WRITE_QUEUE_BYTES: usize = 1 * 1024 * 1024`, `PHASE94_MAX_AGGREGATE_READ_QUEUE_BYTES: usize = 8 * 1024 * 1024`, `PHASE94_MAX_AGGREGATE_WRITE_QUEUE_BYTES: usize = 8 * 1024 * 1024`, `PHASE94_MAX_PEER_QUEUED_MESSAGES: usize = 128`, `PHASE94_MAX_AGGREGATE_QUEUED_MESSAGES: usize = 1024`, `PHASE94_MAX_INBOUND_TX_REQUESTS_PER_PEER: usize = 1024`, `PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER: usize = DEFAULT_MAX_BLOCKS_IN_FLIGHT_PER_PEER`, `PHASE94_MAX_HEADER_LOCATOR_HASHES: usize = MAX_HEADERS_RESULTS`, and `PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS: usize = MAX_INV_SIZE`. Add `pub enum ResourcePressureLabel { ResourcePressureActive, ReadQueuePressure, WriteQueuePressure, RequestCapReached }` with `as_str()` values `resource_pressure_active`, `read_queue_pressure`, `write_queue_pressure`, and `request_cap_reached`. Add `pub enum ResourceGovernanceDecision { Accept, Backpressure(InboundResourceEvent), Disconnect(InboundResourceEvent), RecordMisbehavior(InboundResourceEvent) }`, `pub struct QueuePressureInput`, `pub struct RequestPressureInput`, and `pub struct ResourceGovernancePolicy`. Implement `ResourceGovernancePolicy::default()`, `decide_queue(&self, input: QueuePressureInput) -> ResourceGovernanceDecision`, and `decide_request(&self, input: RequestPressureInput) -> ResourceGovernanceDecision`. Queue inputs must include `peer_read_queue_bytes`, `peer_write_queue_bytes`, `aggregate_read_queue_bytes`, `aggregate_write_queue_bytes`, `peer_queued_messages`, `aggregate_queued_messages`, `active_permission_effects: Vec<PermissionEffectLabel>`, and `inactive_permission_effects: Vec<InactivePermissionEffectLabel>`. Request inputs must include `inventory_items`, `getdata_items`, `header_locator_hashes`, `requested_blocks_in_flight`, `requested_txids_in_flight`, `requested_wtxids_in_flight`, and the same permission effect vectors. Decisions must use `next_action` strings `resource_pressure_active`, `read_queue_pressure`, `write_queue_pressure`, and `request_cap_reached`.
  </action>
  <acceptance_criteria>
    - `rg -n "PHASE94_MAX_PEER_READ_QUEUE_BYTES|PHASE94_MAX_PEER_WRITE_QUEUE_BYTES|PHASE94_MAX_AGGREGATE_READ_QUEUE_BYTES|PHASE94_MAX_AGGREGATE_WRITE_QUEUE_BYTES|PHASE94_MAX_INBOUND_TX_REQUESTS_PER_PEER|PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER|PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "pub enum ResourcePressureLabel|pub enum ResourceGovernanceDecision|pub struct QueuePressureInput|pub struct RequestPressureInput|pub struct ResourceGovernancePolicy" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "resource_pressure_active|read_queue_pressure|write_queue_pressure|request_cap_reached" packages/open-bitcoin-network/src/resource.rs packages/open-bitcoin-network/src/resource/tests.rs`
    - `rg -n "InactivePermissionEffectLabel::Relay|InactivePermissionEffectLabel::ForceRelay|InactivePermissionEffectLabel::Mempool|InactivePermissionEffectLabel::BloomFilter|InactivePermissionEffectLabel::BlockFilters" packages/open-bitcoin-network/src/resource/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast</automated>
  </verify>
  <done>Pure queue/request policy returns stable decisions from typed inputs and proves inactive relay-like effects do not grant capacity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timeout, churn, failure, and reconnect policy</name>
  <files>packages/open-bitcoin-network/src/resource.rs, packages/open-bitcoin-network/src/resource/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/resource/tests.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/peer_policy.rs`
  </read_first>
  <behavior>
    - Test 1: handshaking peer older than `PHASE94_SLOW_HANDSHAKE_TIMEOUT_SECONDS` returns `slow_handshake` and next action `timeout_disconnect`.
    - Test 2: established peer idle longer than `PHASE94_IDLE_PEER_TIMEOUT_SECONDS` returns `idle_peer` and next action `timeout_disconnect`.
    - Test 3: churn/repeated-failure windows return `connection_churn_limited` and `repeated_failure_limited`.
    - Test 4: active ban and discouraged reconnect inputs return `reconnect_suppressed_banned` and `reconnect_suppressed_discouraged`.
  </behavior>
  <action>
    Add constants `PHASE94_SLOW_HANDSHAKE_TIMEOUT_SECONDS: i64 = 60`, `PHASE94_IDLE_PEER_TIMEOUT_SECONDS: i64 = 1_800`, `PHASE94_CONNECTION_CHURN_WINDOW_SECONDS: i64 = 60`, `PHASE94_MAX_CONNECTIONS_PER_CHURN_WINDOW: usize = 16`, `PHASE94_REPEATED_FAILURE_WINDOW_SECONDS: i64 = 300`, and `PHASE94_MAX_REPEATED_FAILURES_PER_WINDOW: usize = 8`. Add `pub enum ResourceLifecycleLabel { SlowHandshake, IdlePeer, ConnectionChurnLimited, RepeatedFailureLimited, ReconnectSuppressedBanned, ReconnectSuppressedDiscouraged }` with `as_str()` values `slow_handshake`, `idle_peer`, `connection_churn_limited`, `repeated_failure_limited`, `reconnect_suppressed_banned`, and `reconnect_suppressed_discouraged`. Add input structs `ResourceTimeoutInput { handshake_state: InboundHandshakeState, connected_at_unix_seconds: i64, last_activity_unix_seconds: i64, now_unix_seconds: i64 }`, `ConnectionChurnInput { window_started_unix_seconds: i64, now_unix_seconds: i64, connection_attempts_in_window: usize }`, `RepeatedFailureInput { window_started_unix_seconds: i64, now_unix_seconds: i64, failures_in_window: usize }`, and `ReconnectSuppressionInput { banned: bool, discouraged: bool }`. Implement methods on `ResourceGovernancePolicy`: `decide_timeout`, `decide_churn`, `decide_repeated_failure`, and `decide_reconnect`. These methods must create `InboundResourceEvent` values with `next_action` exactly `timeout_disconnect`, `churn_rejected`, or `reconnect_suppressed`.
  </action>
  <acceptance_criteria>
    - `rg -n "PHASE94_SLOW_HANDSHAKE_TIMEOUT_SECONDS|PHASE94_IDLE_PEER_TIMEOUT_SECONDS|PHASE94_CONNECTION_CHURN_WINDOW_SECONDS|PHASE94_MAX_CONNECTIONS_PER_CHURN_WINDOW|PHASE94_REPEATED_FAILURE_WINDOW_SECONDS|PHASE94_MAX_REPEATED_FAILURES_PER_WINDOW" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "pub enum ResourceLifecycleLabel|pub struct ResourceTimeoutInput|pub struct ConnectionChurnInput|pub struct RepeatedFailureInput|pub struct ReconnectSuppressionInput" packages/open-bitcoin-network/src/resource.rs`
    - `rg -n "slow_handshake|idle_peer|connection_churn_limited|repeated_failure_limited|reconnect_suppressed_banned|reconnect_suppressed_discouraged|timeout_disconnect|churn_rejected|reconnect_suppressed" packages/open-bitcoin-network/src/resource.rs packages/open-bitcoin-network/src/resource/tests.rs`
    - `! rg -n "sleep|Instant::now\\(|SystemTime::now\\(|TcpStream|tokio::spawn" packages/open-bitcoin-network/src/resource.rs packages/open-bitcoin-network/src/resource/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Timeout, churn, repeated-failure, and reconnect suppression decisions are pure, timestamp-injected, and covered by deterministic tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| runtime counters -> resource policy | Effectful socket and queue observations enter pure policy as bounded numeric inputs. |
| peer permission config -> resource policy | Permission-derived labels affect scoped decisions without exposing raw config strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-02-01 | Denial of Service | queue/request policy | mitigate | Enforce named per-peer and aggregate queue/request constants with deterministic `Backpressure` or `Disconnect` outcomes. |
| T-94-02-02 | Elevation of Privilege | permission effects | mitigate | Treat inactive relay-like effects as evidence only; tests assert they do not raise resource caps or enable serving behavior. |
| T-94-02-03 | Spoofing | reconnect suppression | mitigate | Consume explicit banned/discouraged booleans from Phase 93 state and return low-cardinality suppression labels. |
| T-94-02-04 | Repudiation | timeout/churn decisions | mitigate | Emit stable labels and next actions from injected timestamps/counters, making decisions reproducible in tests. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network resource --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
DOS-02 and DOS-03 have pure policy coverage for queues, requests, timeouts, churn, failures, and reconnect suppression. DOS-05 remains loopback/public-network-free through pure unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-02-SUMMARY.md`.
</output>
