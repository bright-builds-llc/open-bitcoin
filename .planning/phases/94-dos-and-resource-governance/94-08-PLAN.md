---
phase: 94-dos-and-resource-governance
plan: 08
type: execute
wave: 7
depends_on: ["94-07"]
files_modified:
  - scripts/check-phase94-dos-resource-governance.ts
  - scripts/check-phase94-dos-resource-governance.test.ts
  - scripts/verify.sh
autonomous: true
requirements: [DOS-05]
must_haves:
  truths:
    - "Default verification includes deterministic Phase 94 resource-governance checks."
    - "The checker rejects missing labels, missing docs, dynamic metric omissions, public-network commands, and positive deferred-feature claims."
    - "The checker is wired after Phase 93 in `bash scripts/verify.sh`."
  artifacts:
    - path: "scripts/check-phase94-dos-resource-governance.ts"
      provides: "Deterministic Phase 94 verification checker"
    - path: "scripts/check-phase94-dos-resource-governance.test.ts"
      provides: "Checker tests"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase94-dos-resource-governance.ts"
      via: "run_step"
      pattern: "check-phase94-dos-resource-governance"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Add the deterministic Phase 94 checker and wire it into the repo-native verifier.

Purpose: Satisfy D-17, D-19, DOS-05, and the final public-network-free verification contract.
Output: Bun checker/test files and `scripts/verify.sh` steps after Phase 93.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@scripts/check-phase92-address-boundaries.ts
@scripts/check-phase92-address-boundaries.test.ts
@scripts/check-phase93-peer-policy.ts
@scripts/check-phase93-peer-policy.test.ts
@scripts/verify.sh
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Phase 94 deterministic checker and tests</name>
  <files>scripts/check-phase94-dos-resource-governance.ts, scripts/check-phase94-dos-resource-governance.test.ts</files>
  <read_first>
    - `scripts/check-phase93-peer-policy.ts`
    - `scripts/check-phase93-peer-policy.test.ts`
    - `scripts/check-phase92-address-boundaries.ts`
    - `scripts/check-phase92-address-boundaries.test.ts`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
    - `docs/parity/catalog/p2p.md`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-node/src/logging.rs`
    - `packages/open-bitcoin-node/src/metrics.rs`
  </read_first>
  <behavior>
    - Test 1: checker passes a fixture containing all Phase 94 labels, fixed metric names, docs anchors, and verifier wiring.
    - Test 2: checker fails when `payload_oversized`, `slow_handshake`, or `inbound_payload_rejected_count` is missing.
    - Test 3: checker fails positive claims for transaction relay, compact block relay, mempool propagation, public inbound defaults, public-network CI, production service, or production full-node readiness.
  </behavior>
  <action>
    Create `scripts/check-phase94-dos-resource-governance.ts` following the actual Phase 93 checker style in `scripts/check-phase93-peer-policy.ts`. The checker must read these files: `packages/open-bitcoin-network/src/resource.rs`, `packages/open-bitcoin-rpc/src/inbound_listener.rs`, `packages/open-bitcoin-network/src/peer.rs`, `packages/open-bitcoin-node/src/status/inbound.rs`, `packages/open-bitcoin-node/src/network/inbound.rs`, `packages/open-bitcoin-node/src/logging.rs`, `packages/open-bitcoin-node/src/metrics.rs`, `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`, `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`, `docs/operator/runtime-guide.md`, `docs/architecture/status-snapshot.md`, `docs/architecture/operator-observability.md`, `docs/parity/catalog/p2p.md`, `docs/parity/checklist.md`, `docs/parity/index.json`, and `scripts/verify.sh`. Require these labels exactly: `wrong_network_magic`, `malformed_header`, `payload_oversized`, `invalid_checksum`, `unsupported_command`, `malformed_payload`, `trailing_payload`, `slow_handshake`, `idle_peer`, `connection_churn_limited`, `repeated_failure_limited`, `reconnect_suppressed_banned`, `reconnect_suppressed_discouraged`, `resource_pressure_active`, `read_queue_pressure`, `write_queue_pressure`, `request_cap_reached`, `payload_rejected`, `timeout_disconnect`, `churn_rejected`, and `reconnect_suppressed`. Require runtime wiring strings `decide_queue`, `tokio::time::timeout`, `decide_churn`, `decide_repeated_failure`, `decide_reconnect`, `record_inbound_resource_event`, and `reconnect_suppression_input_for_remote_addr`. Require structured log strings `INBOUND_RESOURCE_GOVERNANCE_LOG_SOURCE`, `inbound_resource_governance_log_record`, `inbound_resource_governance`, `outcome=`, `reason=`, `label=`, `source=`, `message=`, and `next_action=`. Require metric strings `inbound_resource_pressure_active_count`, `inbound_read_queue_pressure_count`, `inbound_write_queue_pressure_count`, `inbound_request_cap_reached_count`, `inbound_payload_rejected_count`, `inbound_timeout_disconnect_count`, `inbound_churn_rejected_count`, and `inbound_reconnect_suppressed_count`. Reject positive claim patterns for `transaction relay support`, `compact block relay support`, `mempool propagation support`, `broad address relay support`, `public inbound default`, `public-network CI`, `production service operation`, `production full-node readiness`, `BIP37 support`, and `compact filter support`. Reject raw resource-evidence terms `peer_id`, `raw_endpoint`, `payload_bytes`, `permission_string`, `credential`, and `secret` in Phase 94 status/log/support render paths. Create `scripts/check-phase94-dos-resource-governance.test.ts` with positive and negative fixtures using Bun's test runner.
  </action>
  <acceptance_criteria>
    - `rg -n "wrong_network_magic|payload_oversized|unsupported_command|slow_handshake|connection_churn_limited|resource_pressure_active|request_cap_reached|reconnect_suppressed" scripts/check-phase94-dos-resource-governance.ts scripts/check-phase94-dos-resource-governance.test.ts`
    - `rg -n "decide_queue|tokio::time::timeout|decide_churn|decide_repeated_failure|decide_reconnect|record_inbound_resource_event|reconnect_suppression_input_for_remote_addr" scripts/check-phase94-dos-resource-governance.ts scripts/check-phase94-dos-resource-governance.test.ts`
    - `rg -n "INBOUND_RESOURCE_GOVERNANCE_LOG_SOURCE|inbound_resource_governance_log_record|inbound_resource_governance|outcome=|reason=|label=|source=|message=|next_action=" scripts/check-phase94-dos-resource-governance.ts scripts/check-phase94-dos-resource-governance.test.ts`
    - `rg -n "inbound_resource_pressure_active_count|inbound_payload_rejected_count|inbound_reconnect_suppressed_count" scripts/check-phase94-dos-resource-governance.ts scripts/check-phase94-dos-resource-governance.test.ts`
    - `rg -n "transaction relay support|compact block relay support|mempool propagation support|public inbound default|production service operation|production full-node readiness|BIP37 support|compact filter support" scripts/check-phase94-dos-resource-governance.ts scripts/check-phase94-dos-resource-governance.test.ts`
    - `rg -n "describe\\(|test\\(|expect\\(" scripts/check-phase94-dos-resource-governance.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase94-dos-resource-governance.test.ts</automated>
    <automated>bun run scripts/check-phase94-dos-resource-governance.ts</automated>
  </verify>
  <done>Checker and tests enforce Phase 94 labels, metrics, docs, verifier wiring, and no-claim boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 94 checker into default verification</name>
  <files>scripts/verify.sh</files>
  <read_first>
    - `scripts/verify.sh`
    - `scripts/check-phase94-dos-resource-governance.ts`
    - `scripts/check-phase94-dos-resource-governance.test.ts`
    - `scripts/check-phase93-peer-policy.ts`
  </read_first>
  <action>
    In `scripts/verify.sh`, add Phase 94 checker commands immediately after the Phase 93 checker entries in both `VERIFY_COMMAND_ORDER` and the executable `run_step` section. The printed command list must include `bun test scripts/check-phase94-dos-resource-governance.test.ts` followed by `bun run scripts/check-phase94-dos-resource-governance.ts`. The executable section must add `run_step "Phase 94 DoS/resource governance checker tests" bun test scripts/check-phase94-dos-resource-governance.test.ts` followed by `run_step "Phase 94 DoS/resource governance checker" bun run scripts/check-phase94-dos-resource-governance.ts`. Do not add public-network, service-manager, or mainnet listener commands to `scripts/verify.sh`.
  </action>
  <acceptance_criteria>
    - `rg -n "bun test scripts/check-phase94-dos-resource-governance.test.ts|bun run scripts/check-phase94-dos-resource-governance.ts|Phase 94 DoS/resource governance checker tests|Phase 94 DoS/resource governance checker" scripts/verify.sh`
    - `rg -n "check-phase93-peer-policy.*check-phase94-dos-resource-governance|Phase 93.*Phase 94" scripts/verify.sh`
    - `! rg -n "openbitcoinlisten=0\\.0\\.0\\.0|public-network|mainnet listener|systemctl|launchctl|service-manager" scripts/verify.sh`
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase94-dos-resource-governance.test.ts</automated>
    <automated>bun run scripts/check-phase94-dos-resource-governance.ts</automated>
    <automated>bash scripts/verify.sh --fast</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>`bash scripts/verify.sh` runs the Phase 94 checker and tests after Phase 93 without public-network or service-manager commands; `--fast` is only an iteration shortcut, while default `bash scripts/verify.sh` is the final proof.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| source/docs -> release verifier | Text and source artifacts become automated release confidence signals. |
| verifier -> CI/default local checks | Checker commands run in the default verification path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-08-01 | Tampering | verifier wiring | mitigate | Require Phase 94 checker commands in both printed and executable `scripts/verify.sh` sections. |
| T-94-08-02 | Repudiation | no-claim boundaries | mitigate | Checker fails positive deferred-feature and production-readiness claims. |
| T-94-08-03 | Denial of Service | default verification | mitigate | Checker remains local/static and `--fast` verification avoids public-network listeners. |
| T-94-08-04 | Information Disclosure | checker corpus | mitigate | Checker scans for labels/claims only and does not read secrets or external endpoints. |
</threat_model>

<verification>
- `bun test scripts/check-phase94-dos-resource-governance.test.ts`
- `bun run scripts/check-phase94-dos-resource-governance.ts`
- `bash scripts/verify.sh --fast`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
DOS-05 is complete: Phase 94 has deterministic checker coverage and default verifier wiring that stays public-network-free.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-08-SUMMARY.md`.
</output>
