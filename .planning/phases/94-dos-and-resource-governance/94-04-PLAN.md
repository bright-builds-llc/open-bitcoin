---
phase: 94-dos-and-resource-governance
plan: 04
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - packages/open-bitcoin-network/src/error.rs
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/inventory_state.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-network/src/compatibility.rs
autonomous: true
requirements: [DOS-02, DOS-04, DOS-05]
must_haves:
  truths:
    - "Peer request handling enforces request and inventory caps through the shared resource policy."
    - "Resource-limit disconnects have an explicit stable disconnect reason."
    - "Request caps do not enable transaction relay, mempool propagation, compact block relay, BIP37, or compact-filter serving."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "PeerManager request-cap policy hooks"
    - path: "packages/open-bitcoin-network/src/peer/inventory_state.rs"
      provides: "Inventory request cap integration"
    - path: "packages/open-bitcoin-network/src/error.rs"
      provides: "Resource-limit disconnect reason"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer.rs"
      to: "packages/open-bitcoin-network/src/resource.rs"
      via: "ResourceGovernancePolicy::decide_request"
      pattern: "decide_request"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Apply the pure request/resource policy to peer-manager inventory and request paths while preserving the deferred relay/no-claim boundary.

Purpose: Satisfy D-02, D-05, D-07, D-08, D-13, D-18, DOS-02, and DOS-04 for peer-side work.
Output: Bounded request handling with explicit resource-limit disconnect evidence and tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/inventory_state.rs
@packages/open-bitcoin-network/src/resource.rs
@packages/open-bitcoin-network/src/error.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit resource-limit disconnect reason</name>
  <files>packages/open-bitcoin-network/src/error.rs, packages/open-bitcoin-network/src/compatibility.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/error.rs`
    - `packages/open-bitcoin-network/src/compatibility.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
  </read_first>
  <behavior>
    - Test 1: `DisconnectReason::ResourceLimit` displays exactly `resource limit reached`.
    - Test 2: compatibility diagnosis treats `ResourceLimit` as a peer rejection, not as sync success.
  </behavior>
  <action>
    Add `ResourceLimit` to `DisconnectReason` in `packages/open-bitcoin-network/src/error.rs` and make its `Display` output exactly `resource limit reached`. Update error tests to assert that string. Update `packages/open-bitcoin-network/src/compatibility.rs` so `Some(DisconnectReason::ResourceLimit)` maps to `CompatibilityDiagnosis::VersionRejected` or the existing closest rejection diagnosis used for `DuplicateVersion` and `SelfConnection`; do not introduce a success diagnosis for resource-limit disconnects. Add a peer test helper assertion that `PeerAction::Disconnect(DisconnectReason::ResourceLimit)` is available for Phase 94 request-cap tests.
  </action>
  <acceptance_criteria>
    - `rg -n "ResourceLimit" packages/open-bitcoin-network/src/error.rs packages/open-bitcoin-network/src/compatibility.rs packages/open-bitcoin-network/src/peer/tests.rs`
    - `rg -n "resource limit reached" packages/open-bitcoin-network/src/error.rs`
    - `rg -n "DisconnectReason::ResourceLimit" packages/open-bitcoin-network/src/compatibility.rs packages/open-bitcoin-network/src/peer/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network error --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compatibility --no-fail-fast</automated>
  </verify>
  <done>Resource-limit disconnects have a stable typed reason and compatibility handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce request caps in PeerManager paths</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/inventory_state.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer/inventory_state.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-network/src/message.rs`
  </read_first>
  <behavior>
    - Test 1: inbound `Inv` that would push tx requests beyond `PHASE94_MAX_INBOUND_TX_REQUESTS_PER_PEER` returns `PeerAction::Disconnect(DisconnectReason::ResourceLimit)`.
    - Test 2: inbound `GetData` with more than `PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS` entries returns resource-limit disconnect and does not return `ServeInventory`.
    - Test 3: headers-to-block request handling never exceeds `PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER`.
    - Test 4: tests assert no mempool, compact block, BIP37, compact-filter, or transaction relay handler is added.
  </behavior>
  <action>
    Import `ResourceGovernancePolicy`, `RequestPressureInput`, `ResourceGovernanceDecision`, `PHASE94_MAX_INBOUND_TX_REQUESTS_PER_PEER`, `PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER`, and `PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS` into `peer.rs` or `peer/inventory_state.rs`. In `handle_inventory`, before adding txid/wtxid requests, build `RequestPressureInput` with `inventory_items: inventory.inventory.len()`, `getdata_items: 0`, `header_locator_hashes: 0`, current `peer.requested_blocks.len()`, `peer.requested_txids.len()`, and `peer.requested_wtxids.len()`, plus empty permission effect vectors until the peer record exposes effects directly. If `ResourceGovernancePolicy::default().decide_request(input)` returns `Backpressure`, `Disconnect`, or `RecordMisbehavior`, return `Ok(vec![PeerAction::Disconnect(DisconnectReason::ResourceLimit)])`. In the `WireNetworkMessage::GetData(inventory)` branch, apply the same policy with `getdata_items: inventory.inventory.len()` and return resource-limit disconnect when the cap is exceeded; otherwise preserve the existing `ServeInventory(inventory.inventory)` behavior. In `handle_headers_with_policy`, keep existing truncation but use `PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER` as the maximum in-flight block request cap and assert it equals `DEFAULT_MAX_BLOCKS_IN_FLIGHT_PER_PEER`. Do not add variants or branches for `mempool`, `cmpctblock`, `getcfheaders`, `getcfcheckpt`, `filterload`, `filteradd`, or `filterclear`.
  </action>
  <acceptance_criteria>
    - `rg -n "ResourceGovernancePolicy|RequestPressureInput|PHASE94_MAX_INBOUND_TX_REQUESTS_PER_PEER|PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER|PHASE94_MAX_INBOUND_REQUEST_INVENTORY_ITEMS|DisconnectReason::ResourceLimit" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/tests.rs`
    - `rg -n "request_cap_reached|resource limit reached|ServeInventory" packages/open-bitcoin-network/src/peer/tests.rs`
    - `! rg -n "Mempool|Cmpct|Compact|FilterLoad|FilterAdd|FilterClear|GetCF|CFHeaders|CFCheckpt|transaction relay|mempool propagation" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/inventory_state.rs`
    - `rg -n "assert_eq!\\(PHASE94_MAX_INBOUND_BLOCK_REQUESTS_PER_PEER, DEFAULT_MAX_BLOCKS_IN_FLIGHT_PER_PEER\\)" packages/open-bitcoin-network/src/peer/tests.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>PeerManager enforces request caps through resource policy and keeps deferred relay surfaces absent.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| parsed peer message -> PeerManager | Untrusted but parsed inventory/request messages can trigger peer work and response generation. |
| PeerManager -> local data serving | `GetData` can request inventory service from local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-04-01 | Denial of Service | `handle_inventory` | mitigate | Run `ResourceGovernancePolicy::decide_request` before increasing requested tx/block state. |
| T-94-04-02 | Denial of Service | `GetData` handling | mitigate | Reject over-cap request inventory with `DisconnectReason::ResourceLimit` before `ServeInventory`. |
| T-94-04-03 | Elevation of Privilege | deferred relay features | mitigate | Grep-verifiable absence of mempool, compact block, BIP37, compact-filter, and transaction relay handlers in touched peer files. |
| T-94-04-04 | Repudiation | resource disconnects | mitigate | Use explicit `DisconnectReason::ResourceLimit` with stable display string and tests. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compatibility --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
DOS-02 request caps are enforced in peer-manager paths, DOS-04 has a stable resource-limit disconnect reason, and DOS-05 remains covered by deterministic unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-04-SUMMARY.md`.
</output>
