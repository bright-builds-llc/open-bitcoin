---
phase: 94-dos-and-resource-governance
plan: 06
type: execute
wave: 5
depends_on: ["94-05"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements: [DOS-04, DOS-05]
must_haves:
  truths:
    - "Operator status renders resource-governance counters and latest next action from shared status."
    - "Support bundles render redacted Phase 94 resource evidence and actionable next-action text."
    - "Renderers do not invent local summaries or expose raw peer/payload/permission data."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      provides: "Human status text for shared resource evidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "Support Markdown for shared resource evidence"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "InboundResourceGovernanceEvent import"
      pattern: "InboundResourceGovernanceEvent"
    - from: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "shared status fields"
      pattern: "latest_resource_governance_decision"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 94-2026-06-26T15-47-23
generated_at: 2026-06-26T16:21:12.422Z
---

<objective>
Render shared Phase 94 resource-governance evidence in operator status and support bundle output.

Purpose: Satisfy D-13, D-14, D-15, D-17, D-18, DOS-04, and DOS-05 at operator surfaces.
Output: Bounded status/support text with next-action labels and tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-RESEARCH.md
@packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
@packages/open-bitcoin-node/src/status/inbound.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render resource evidence in operator status</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render/tests.rs`
    - `packages/open-bitcoin-node/src/status/inbound.rs`
  </read_first>
  <behavior>
    - Test 1: status text includes `resource evidence:` with all Phase 94 counters.
    - Test 2: latest resource decision text includes `next_action=payload_rejected`.
    - Test 3: unavailable latest resource decision includes `inbound resource governance evidence unavailable`.
  </behavior>
  <action>
    Import `InboundResourceGovernanceEvent` in `status/render/inbound.rs`. Add `resource_governance_text(status: &InboundPeerServingStatus) -> String` and call it from `available_inbound_status_text` after `peer_policy_text(status)`. The status text must include one segment starting exactly `resource evidence:` followed by fields `resource_pressure_events=`, `read_queue_pressure_events=`, `write_queue_pressure_events=`, `request_cap_events=`, `payload_rejections=`, `timeout_disconnects=`, `churn_rejections=`, `reconnect_suppressions=`, and `latest resource governance decision=`. Add `latest_resource_governance_decision_text` and `resource_governance_decision_text`; the event formatter must include `outcome=`, `reason=`, `label=`, `source=`, `message=`, and `next_action=`. Update `status/render/tests.rs` to assert exact substrings including `resource evidence:`, `payload_rejections=1`, and `next_action=payload_rejected`.
  </action>
  <acceptance_criteria>
    - `rg -n "InboundResourceGovernanceEvent|resource_governance_text|latest_resource_governance_decision_text|resource_governance_decision_text" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`
    - `rg -n "resource evidence:|resource_pressure_events=|read_queue_pressure_events=|write_queue_pressure_events=|request_cap_events=|payload_rejections=|timeout_disconnects=|churn_rejections=|reconnect_suppressions=|next_action=" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs`
    - `rg -n "inbound resource governance evidence unavailable|payload_rejections=1|next_action=payload_rejected" packages/open-bitcoin-cli/src/operator/status/render/tests.rs`
    - `! rg -n "peer_id|raw_endpoint|payload_bytes|permission_string|credential|secret" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status::render --no-fail-fast</automated>
  </verify>
  <done>Operator status renders Phase 94 resource evidence from shared status fields with exact next-action output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render resource evidence and next actions in support bundles</name>
  <files>packages/open-bitcoin-cli/src/operator/support/render/inbound.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
    - `packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `packages/open-bitcoin-node/src/status/inbound.rs`
  </read_first>
  <behavior>
    - Test 1: support Markdown includes `## Inbound Resource Governance Evidence`.
    - Test 2: support Markdown includes the Phase 94 next-action sentence.
    - Test 3: inactive relay-like permission next action remains unchanged and continues to warn that relay-like effects are inactive evidence.
  </behavior>
  <action>
    In `support/render/inbound.rs`, add `const PHASE94_RESOURCE_GOVERNANCE_NEXT_ACTION: &str = "Treat Phase 94 as bounded inbound resource-governance evidence only; inspect resource labels before raising listener exposure, queue caps, request caps, or timeout thresholds.";`. Add `push_inbound_resource_governance(output: &mut String, evidence: &InboundPeerServingStatus)` and call it after `push_inbound_peer_policy`. The section heading must be exactly `## Inbound Resource Governance Evidence`. Render each counter as Markdown bullet labels: `Resource pressure events`, `Read queue pressure events`, `Write queue pressure events`, `Request cap events`, `Payload rejections`, `Timeout disconnects`, `Churn rejections`, and `Reconnect suppressions`. Render latest event with `Latest resource governance decision:` and include `next_action=`. Add the Phase 94 next-action constant as `- Next action: {PHASE94_RESOURCE_GOVERNANCE_NEXT_ACTION}` in that section. Keep existing `INACTIVE_RELAY_PERMISSION_NEXT_ACTION`, `PHASE92_ADDRESS_BOUNDARY_NEXT_ACTION`, and `PHASE93_PEER_POLICY_NEXT_ACTION` behavior unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n "PHASE94_RESOURCE_GOVERNANCE_NEXT_ACTION|push_inbound_resource_governance|Inbound Resource Governance Evidence|Latest resource governance decision" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
    - `rg -n "Resource pressure events|Read queue pressure events|Write queue pressure events|Request cap events|Payload rejections|Timeout disconnects|Churn rejections|Reconnect suppressions|next_action=" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `rg -n "bounded inbound resource-governance evidence only|inactive Phase 91 evidence" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `! rg -n "peer_id|raw_endpoint|payload_bytes|permission_string|credential|secret" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Support bundles render bounded Phase 94 evidence, exact next-action text, and no raw sensitive evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| shared status -> operator text | Machine-readable evidence becomes human-readable CLI output. |
| shared status -> support bundle | Diagnostic evidence may be exported for troubleshooting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-94-06-01 | Information Disclosure | support bundle rendering | mitigate | Render only shared counters and latest low-cardinality event fields; grep rejects raw peer/payload/permission/credential terms. |
| T-94-06-02 | Repudiation | status rendering | mitigate | Include latest resource-governance outcome, reason, label, source, message, and next action in both status and support output. |
| T-94-06-03 | Elevation of Privilege | operator next actions | mitigate | Next-action text explicitly says Phase 94 is bounded evidence only and does not imply relay or public exposure readiness. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status::render --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
DOS-04 is visible in operator status and support bundles through shared status fields, and DOS-05 remains covered by local deterministic tests.
</success_criteria>

<output>
After completion, create `.planning/phases/94-dos-and-resource-governance/94-06-SUMMARY.md`.
</output>
