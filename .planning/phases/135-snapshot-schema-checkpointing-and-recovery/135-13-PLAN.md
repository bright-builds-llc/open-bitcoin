---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "13"
type: execute
wave: 13
depends_on: ["135-12"]
gap_closure: true
files_modified:
  - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/representability.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPDUR-01, MPDUR-02, MPDUR-03, MPDUR-04]
must_haves:
  truths:
    - "Every save path enforces one versioned representability contract equal to persisted_mempool_input_limits() before replacing durable bytes: 220096 records, 5000 unbroadcast members, 4194304 bytes per transaction, 67108864 aggregate transaction bytes, 1636801 aggregate input edges, 102300 per-record input edges, and 268435456 final encoded bytes."
    - "Representability failure does not drop live records, does not rewrite acceptance ages or unbroadcast membership, preserves the prior durable snapshot, and exact-aborts any unachieved prepared write capability."
    - "The supported persistence surface is exactly that persisted-input contract; a live mempool that exceeds it fails capture/encode rather than writing a checkpoint the same binary's reader would reject."
    - "Exact-boundary prepare → encode → Sync write → reopen succeeds, and the matching one-over path fails closed."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs
      provides: "Single representability predicate used by capture and encode"
      contains: "assert_mempool_snapshot_representable"
      exports: ["assert_mempool_snapshot_representable"]
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      provides: "Encoder compares final bytes to limits.max_encoded_bytes, not a snapshot-derived ceiling"
      contains: "assert_mempool_snapshot_representable"
    - path: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      provides: "Capture proves representability before reserve_next"
      contains: "assert_mempool_snapshot_representable"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/representability.rs
      provides: "Exact and one-over prepare/encode/Sync/reopen regressions"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs
      via: "PrepareSnapshot proves representability before reserving a write capability"
      pattern: "assert_mempool_snapshot_representable"
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      via: "encode_mempool_snapshot rejects bytes.len() > limits.max_encoded_bytes from persisted_mempool_input_limits()"
      pattern: "bytes.len\\(\\) > limits.max_encoded_bytes"
    - from: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      via: "encode failure exact-aborts and never calls put_bytes"
      pattern: "abort_failed_write"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-13T16:43:49.000Z
---

<objective>
Close CR-01 / truths 10 and 14 by making writer and reader share one persisted-input representability contract before any durable replacement.

Purpose: A successful Sync checkpoint must be admissible to the same binary's bounded startup reader, and an unrepresentable live mempool must fail closed without dropping records.
Output: One representability module, capture-before-reserve, encoder fixed-ceiling check, and exact/one-over Sync/reopen plus abort-preserve regressions.
</objective>

<execution-context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-12-PLAN.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md

<interfaces>
Live numbers from `persisted_mempool_input_limits()` / store test `persisted_input_limits_use_the_finite_format_contract`:

```
max_encoded_bytes            = 268_435_456   // 256 MiB
max_records                  = 220_096
max_unbroadcast_members      = 5_000
max_transaction_bytes        = 4_194_304     // 4 MiB
max_total_transaction_bytes  = 67_108_864    // 64 MiB
max_input_edges              = 1_636_801
max_input_edges_per_record   = 102_300
```

Current encoder (must be replaced) in `encode_mempool_snapshot`:
```rust
let max_encoded_bytes = encoded_size_upper_bound(
    total_transaction_bytes, // snapshot-derived, NOT the reader contract
    dto.records.len(),
    dto.unbroadcast_members.len(),
)?;
if bytes.len() > max_encoded_bytes { /* ResourceBoundExceeded */ }
```

Current capture in `LifecycleCommand::PrepareSnapshot` checks only
`mempool.entries().len() > MempoolCapacityBounds::from_capacity(...).max_live_entries()`
and then `MempoolSnapshot::try_new_current(...)` + `reserve_next`. It does not prove persisted representability.

Current write execution already exact-aborts encode failure:
```rust
let bytes = match encode(&snapshot) {
    Ok(bytes) => bytes,
    Err(error) => return Err(abort_failed_write(..., SnapshotWriteFailure::Encode)),
};
if let Err(error) = save(bytes, PersistMode::Sync) { /* abort Storage */ }
```

`save_mempool_snapshot` encodes then `put_bytes`; encode `Err` never replaces the prior key.

Required new contract:
```rust
pub(crate) fn assert_mempool_snapshot_representable(
    snapshot: &MempoolSnapshot,
) -> Result<MempoolSnapshotPersistedInputLimits, MempoolSnapshotError>;
```

Discretion (D-01 one atomic envelope, no journal/chunk): do not raise reader limits and do not silently drop records. Explicitly constrain the supported persistence surface to `persisted_mempool_input_limits()`. Live policy may still admit more accounted bytes than 64 MiB aggregate canonical transaction bytes; those states fail capture/encode with `ResourceBoundExceeded`.
</interfaces>
</context>

<project-guidance>
Follow repo-local `AGENTS.md`, `AGENTS.bright-builds.md`, empty override table, and architecture/code-shape/testing/verification/Rust standards. New file `representability.rs` uses `foo.rs` + `foo/` (already true for `mempool.rs`). Add it to the existing `node-mempool-storage` breadcrumb group in `docs/parity/source-breadcrumbs.json` with the same Knots persist anchors. Never `unwrap()`. Do not run `bash scripts/verify.sh`. Do not edit Plans 01-12 or their summaries except this plan's own summary. Keep MPDUR Pending. Do not add Phase 137 presentation or chunked/multi-key snapshots.
</project-guidance>

<decision-coverage>
D-01/D-04 are fully covered by one atomic envelope that fails closed when unrepresentable. D-07 is fully covered by exact-abort of unachieved encode failure and retention of the prior snapshot. D-05 lock-across-I/O remains: representability is pure and runs during prepare/encode, not while holding authority across Fjall I/O. D-13/MPDUR-03 remain full: representability must not rewrite ages or drop unbroadcast members. No decision is partial.
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared representability contract and close the encoder under reader limits</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs</file>
    <file>docs/parity/source-breadcrumbs.json</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/snapshot_codec.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - `assert_mempool_snapshot_representable` loads `persisted_mempool_input_limits()` and returns `ResourceBoundExceeded` when any of these is true: `records.len() > 220_096`, `unbroadcast_members().len() > 5_000`, any canonical `encode_transaction(..., WithWitness)` length `> 4_194_304`, checked sum of those lengths `> 67_108_864` or overflow, any `transaction.inputs.len() > 102_300`, checked sum of input counts `> 1_636_801` or overflow, or `persisted_mempool_input_limits()` is `None`.
    - A snapshot at each exact limit is `Ok` and returns the same `MempoolSnapshotPersistedInputLimits` as `persisted_mempool_input_limits().unwrap` values above.
    - `encode_mempool_snapshot` calls `assert_mempool_snapshot_representable` first, then encodes, then rejects `bytes.len() > limits.max_encoded_bytes` (`268_435_456`). It must not call `encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len())` to decide acceptance.
    - One-over per-transaction (`4_194_305` canonical bytes) and one-over aggregate (`67_108_865` summed canonical bytes) encode to `StorageError::Corruption` with detail `mempool snapshot exceeds a resource bound` and do not drop sibling records to make encode succeed.
    - Record-count one-over is proven on the predicate: `records.len() == 220_097` returns `ResourceBoundExceeded` without requiring a 220_097-object Sync test. Use a cheap count-only fixture or a focused helper that the production function calls for the count check.
  </behavior>
  <action>
    Create `packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs` with the Knots persist breadcrumb block used by `mempool.rs`. Add `mod representability;` next to `mod decode;` in `mempool.rs` and `pub(crate) use representability::assert_mempool_snapshot_representable;`. Re-export that symbol from `snapshot_codec.rs` beside `encode_mempool_snapshot`.

    Implement `assert_mempool_snapshot_representable` exactly as specified in `<behavior>`. Canonical transaction bytes must come from `encode_transaction(transaction, TransactionEncoding::WithWitness)` (same as `encode_canonical_transaction`). Use checked addition only; no `saturating_add`. Do not mutate `snapshot.records` or `unbroadcast_members`. Do not rewrite `MempoolAcceptanceTime`. Map missing limits to `MempoolSnapshotError::ResourceBoundExceeded`.

    Rewrite `encode_mempool_snapshot` to:

    ```rust
    pub(crate) fn encode_mempool_snapshot(snapshot: &MempoolSnapshot) -> Result<Vec<u8>, StorageError> {
        let limits = assert_mempool_snapshot_representable(snapshot).map_err(snapshot_failure)?;
        let dto = MempoolSnapshotV2Dto::try_from(snapshot)?;
        let bytes = encode_versioned(StorageNamespace::Mempool, &dto)?;
        if bytes.len() > limits.max_encoded_bytes {
            return Err(snapshot_failure(MempoolSnapshotError::ResourceBoundExceeded));
        }
        Ok(bytes)
    }
    ```

    Delete the snapshot-derived `encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len())` acceptance block. Keep `encoded_size_upper_bound` itself; `persisted_mempool_input_limits()` still uses it to prove the format formula.

    Append `packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs` to the `node-mempool-storage` `files` array in `docs/parity/source-breadcrumbs.json`.

    In `mempool_limits.rs` add one-concern tests: `representability_accepts_exact_per_transaction_bytes`, `representability_rejects_one_over_per_transaction_bytes`, `representability_rejects_one_over_aggregate_transaction_bytes`, `representability_rejects_one_over_record_count`, and `encode_mempool_snapshot_uses_persisted_encoded_byte_ceiling`. Build oversized transactions by padding `script_pubkey` or witness so `encode_transaction(..., WithWitness).len()` is exactly `4_194_304` or `4_194_305`. For aggregate one-over, two records whose canonical lengths sum to `67_108_865` are enough; do not allocate a 256 MiB encoded buffer. For record-count one-over, if materializing `220_097` transactions exceeds 60 seconds, extract `fn persisted_record_count_is_representable(record_count: usize, max_records: usize) -> bool` used by the production function and test `220_096` vs `220_097` on that helper; still call it from `assert_mempool_snapshot_representable`. Every test uses Arrange/Act/Assert.

    Keep `mempool.rs` and `representability.rs` each at or below 628 physical lines.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-13-representability -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features representability_ encode_mempool_snapshot_uses_persisted_encoded_byte_ceiling -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'encoded_size_upper_bound\(\s*total_transaction_bytes' packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs` returns no match.</criterion>
    <criterion>`rg -n 'assert_mempool_snapshot_representable' packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs packages/open-bitcoin-node/src/storage/snapshot_codec.rs` shows the encode call and the re-export.</criterion>
    <criterion>`rg -n 'if bytes.len\(\) > limits.max_encoded_bytes' packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs` matches inside `encode_mempool_snapshot`.</criterion>
    <criterion>The named representability/encode tests pass, and one-over cases return detail `mempool snapshot exceeds a resource bound` without shrinking `records`.</criterion>
    <criterion>`docs/parity/source-breadcrumbs.json` lists `representability.rs` under `node-mempool-storage`.</criterion>
  </acceptance-criteria>
  <done>Writer encode is closed under the same persisted-input limits the reader uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove representability at capture and through prepare → encode → Sync → reopen</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/representability.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/representability.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - `LifecycleCommand::PrepareSnapshot` still applies the live `MempoolCapacityBounds::from_capacity(...).max_live_entries()` check, then builds the snapshot, then calls `assert_mempool_snapshot_representable(&snapshot)` before `reserve_next`. Representability failure returns `LifecycleProjectionError::MempoolSnapshot(MempoolSnapshotError::ResourceBoundExceeded)` and does not reserve a capability.
    - Capture uses `CapturedMempoolGeneration::try_new(network.lifecycle_generation.raw())` (Plan 12) so a terminal live generation cannot be persisted.
    - Exact per-transaction `4_194_304` canonical bytes: `prepare_mempool_snapshot_write` → `encode_mempool_snapshot` → `execute_prepared_mempool_snapshot_write` (`PersistMode::Sync`) → reopen `load_mempool_snapshot_with_limits(MempoolSnapshotDecodeLimits::for_persisted_input())` returns the same record count, same acceptance time, and same unbroadcast set.
    - One-over per-transaction `4_194_305`: after a prior Sync snapshot is stored, a prepared write whose snapshot fails representability/encode returns `SnapshotWriteExecutionError::Encode` with detail `mempool snapshot exceeds a resource bound`, `put_bytes` is not reached, the prior snapshot reloads unchanged, and a later valid prepare succeeds (capability was exact-aborted).
    - Acceptance times and unbroadcast members on the preserved prior snapshot are byte-for-byte the same values written before the failed attempt (MPDUR-03).
  </behavior>
  <action>
    In `LifecycleCommand::PrepareSnapshot` inside `apply_lifecycle_command`, after `MempoolSnapshot::try_new_current(...)` succeeds and before `snapshot_effect_ledger.reserve_next(...)`, call `crate::storage::snapshot_codec::assert_mempool_snapshot_representable(&snapshot).map_err(LifecycleProjectionError::MempoolSnapshot)?;`. Keep the existing live `max_live_entries` check. Switch the generation constructor to `CapturedMempoolGeneration::try_new(network.lifecycle_generation.raw()).map_err(LifecycleProjectionError::MempoolSnapshot)?` if Plan 12 left `new` in place. Do not drop records to pass representability. Do not hold the authority lock across encode/Fjall I/O; this check is pure and stays inside the existing short prepare critical section.

    Create `packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/representability.rs` and declare `mod representability;` from the existing `snapshot_persistence.rs` test module tree (follow `gap_closure.rs` / `load_limits.rs`). Reuse `temp_store_path`, `empty_network_handle` / `checkpoint_network_handle`, `snapshot_decode_limits`, and `remove_dir_if_exists` from the parent tests. Add:

    1. `exact_per_transaction_boundary_prepares_encodes_syncs_and_reopens` — construct a current-v2 snapshot whose single record encodes to exactly `4_194_304` witness bytes, known acceptance time `PolicyTime::from_unix_seconds(90)`, empty or singleton unbroadcast consistent with that identity. Prefer `save_mempool_snapshot(..., PersistMode::Sync)` plus `execute_prepared_mempool_snapshot_write` on a handle that captured an equivalent representable snapshot. Reopen a new `FjallNodeStore::open` on the same path and load with `MempoolSnapshotDecodeLimits::for_persisted_input()`.

    2. `one_over_per_transaction_encode_aborts_and_preserves_prior_snapshot` — first Sync-save a small valid snapshot (`mempool_snapshot()` or the existing helper). Prepare a write. Execute with the real `encode_mempool_snapshot` against a snapshot that is one byte over `4_194_304` (inject by using `execute_prepared_mempool_snapshot_write_with` only if the prepared snapshot itself is the one-over value; otherwise call `encode_mempool_snapshot` on the one-over snapshot, assert `Err`, then run the prepared executor with that failing encode closure that forwards `encode_mempool_snapshot(&one_over)`). Assert `SnapshotWriteExecutionError::Encode`, prior snapshot still loads, and `prepare_mempool_snapshot_write` succeeds afterward.

    Do not add a 256 MiB encoded-body Sync test. Do not change `put_bytes`, `SNAPSHOT_KEY`, or `PersistMode::Sync`. Do not invent a second snapshot key or chunked format. If `snapshot_persistence.rs` exceeds 628 lines after the `mod` line, keep the new tests only in the child file.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-13-sync-reopen -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features exact_per_transaction_boundary_prepares_encodes_syncs_and_reopens one_over_per_transaction_encode_aborts_and_preserves_prior_snapshot -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'assert_mempool_snapshot_representable' packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs` matches before `reserve_next`.</criterion>
    <criterion>`rg -n 'CapturedMempoolGeneration::try_new' packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs` matches the capture path.</criterion>
    <criterion>The exact-boundary test reopens with `for_persisted_input()` and equal record/age/unbroadcast facts.</criterion>
    <criterion>The one-over test returns `SnapshotWriteExecutionError::Encode`, reloads the prior snapshot unchanged, and a subsequent prepare succeeds.</criterion>
    <criterion>No production path silently removes records, ages, or unbroadcast members to make encode succeed.</criterion>
  </acceptance-criteria>
  <done>Capture and Sync write are closed under the reader contract, and unrepresentable writes abort without durable replacement.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1. Any unresolved High severity threat blocks completion and must yield gaps; it may not be accepted or deferred.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live mempool → durable snapshot bytes | A supported live set must not be written unless the startup reader can load it. |
| Encode failure → Fjall `SNAPSHOT_KEY` | Failed representability must not replace the last durable value. |
| Capture reservation → encode | An unrepresentable snapshot must not become an achieved receipt. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-135-13-01 | High | Denial of Service | Encoder / capture | mitigate | Enforce the fixed 256 MiB / 220096 / 4 MiB / 64 MiB / edge ceilings before reserve and before `put_bytes`. | Exact/one-over unit and Sync tests. |
| T-135-13-02 | High | Tampering | `save_mempool_snapshot` / prepared write | mitigate | Representability failure never calls `put_bytes`; prepared encode failure exact-aborts. | Prior-snapshot reload after one-over. |
| T-135-13-03 | High | Tampering | Capture | mitigate | Do not drop live records to satisfy the writer; fail `ResourceBoundExceeded` instead. | Encode still contains every input record or fails. |
| T-135-13-04 | Medium | Information Disclosure | Resource-bound errors | mitigate | Keep `mempool snapshot exceeds a resource bound`; no raw tx bytes in errors. | Exact detail assertions. |
| T-135-13-05 | Medium | Elevation of Privilege | Persistence surface | mitigate | Constrain persistence to the persisted-input contract; do not raise reader limits or add chunked import. | No new schema version or second key. |

## Residual Risks

- Default live accounted capacity (300_000_000) can still admit more source bytes than 64 MiB aggregate canonical transaction bytes. Those states are not a supported checkpoint surface until they fit the format; they fail capture/encode instead of writing an unreadable Sync value.
- Writer-side structural mutation coverage is Plan 14.
</threat-model>

<verification>
Run the Task 1 representability/encode tests, then the Task 2 Sync/reopen tests, sequentially through their declared keys. Do not overlap Cargo jobs. Do not run `bash scripts/verify.sh`.
</verification>

<success-criteria>
- Capture and encode share one persisted-input representability contract.
- Exact-boundary Sync/reopen works; one-over encode aborts and preserves the prior snapshot.
- No silent record dropping; MPDUR-03 age/unbroadcast semantics unchanged.
- Reader limits are not raised; no chunked snapshot format is introduced.
- Full repository verification remains reserved for Plan 14.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-13-SUMMARY.md`.
</output>
