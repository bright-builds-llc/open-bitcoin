---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "05"
type: execute
wave: 5
depends_on: ["135-03", "135-04"]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/checkpoint.rs
  - packages/open-bitcoin-node/src/network/checkpoint/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPDUR-04]
must_haves:
  truths:
    - "A successful Sync write returns and retains an achieved receipt even when authority completion dispatch fails."
    - "One single-flight coordinator skips clean periodic ticks, coalesces newer dirty work, and never starts concurrent snapshot writes."
    - "Encoding/storage failure exact-aborts before achievement, remains dirty/retryable, and never overwrites truthful evidence."
    - "Every node-owned snapshot load is explicitly bounded while the deprecated zero-argument load remains only as an RPC compile-compatibility adapter through Plan 06."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/checkpoint.rs
      provides: "Single-flight checkpoint coordinator and state machine"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Sync-only executor returning achieved SnapshotWriteReceipt"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      via: "load_mempool_snapshot_with_limits requires caller-supplied MempoolSnapshotDecodeLimits"
      pattern: "load_mempool_snapshot_with_limits.*MempoolSnapshotDecodeLimits"
    - from: packages/open-bitcoin-node/src/network/checkpoint.rs
      to: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      via: "coordinator executes prepared snapshot outside authority and retains receipt until completion"
      pattern: "SnapshotWriteReceipt"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-02T18:01:26.808Z
---

<objective>
Execute durable snapshots truthfully and coordinate them as one coalesced flight.

Purpose: Close the achieved-effect gap between synchronous Fjall persistence and authority completion without adding a journal, actor, or second authority.
Output: Receipt-preserving Sync executor, single-flight coordinator, failure injection, and temp-store tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-03-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md
@packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs

<interfaces>
Plan 04 supplies injected-time `prepare_mempool_snapshot_write`, checkpoint evidence, and distinctly named typed transition methods `acknowledge_checkpoint_write`, `abort_checkpoint_snapshot_write`, and `complete_checkpoint_snapshot_write`. It intentionally retains the old zero-argument/untyped snapshot terminal methods only for the still-unmigrated Fjall executor; Rust method overloading is not used. `PersistMode::Sync` maps to Fjall `SyncAll`. This plan first migrates the executor and its consolidated `snapshot_persistence.rs` callers to the typed transition methods, then deletes the old snapshot-only compatibility path and renames the typed methods to final `acknowledge_write(completed_at, strength)`, `abort_snapshot_write(abort)`, and `complete_snapshot_write(receipt)` signatures in the same compile-safe task. Independent peer-effect same-name APIs remain untouched. Separately, the two RPC startup loaders cannot migrate until Plan 06 derives policy limits, so this plan adds distinctly named `load_mempool_snapshot_with_limits(limits)` for all node-owned callers while retaining the existing zero-argument `load_mempool_snapshot()` as a deprecated RPC-only adapter with unchanged legacy decode behavior. This is not Rust overloading; Plan 06 deletes the zero-argument adapter after migrating both RPC constructor families.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require bounded snapshot loads and migrate every node caller</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - `FjallNodeStore::load_mempool_snapshot_with_limits(limits: MempoolSnapshotDecodeLimits)` is the distinctly named bounded API; Rust overloading is not attempted.
    - Every node-side load supplies explicit byte, record, transaction, and unbroadcast limits; the deprecated zero-argument `load_mempool_snapshot()` remains only for the two RPC callers until Plan 06 and rejected bytes remain untouched.
  </behavior>
  <action>
    Implement D-04/D-09 by adding the exact distinctly named bounded API `pub fn load_mempool_snapshot_with_limits(&amp;self, limits: MempoolSnapshotDecodeLimits) -&gt; Result&lt;Option&lt;MempoolSnapshot&gt;, StorageError&gt;` and pass `limits` to Plan 01's bounded decoder before DTO allocation. Do not attempt same-name Rust overloading and do not add `Default` limits. Inventory `rg -n '\bload_mempool_snapshot(_with_limits)?\s*\(' packages/open-bitcoin-node packages/open-bitcoin-rpc --glob '*.rs'`. Plan 04 has consolidated node-side Fjall execution/load cases into `storage/fjall_store/tests/snapshot_persistence.rs`; migrate every node-side caller there to explicit deterministic fixture limits and confirm no load caller remains in `network/tests/lifecycle_projection_cases/effects.rs`. Retain the existing exact zero-argument `pub fn load_mempool_snapshot(&amp;self) -&gt; Result&lt;Option&lt;MempoolSnapshot&gt;, StorageError&gt;` with unchanged legacy decode behavior, mark it `#[deprecated(note = "RPC startup compatibility only; migrate and remove in Phase 135 Plan 06")]`, and document that it is neither the bounded Phase 135 API nor evidence of bounded recovery. Its only allowed callers are the two functions in `packages/open-bitcoin-rpc/src/context/mempool_recovery.rs`. Plan 06 migrates both to `load_mempool_snapshot_with_limits(policy_derived_limits)` and then deletes this zero-argument adapter atomically.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-05-bounded-load -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features snapshot_persistence -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'load_mempool_snapshot\(\)' packages/open-bitcoin-node --glob '*.rs'` finds only the deprecated adapter definition and no node-owned caller; `rg -n 'load_mempool_snapshot_with_limits\(' packages/open-bitcoin-node --glob '*.rs'` finds the bounded definition and every consolidated snapshot-persistence caller.</criterion>
    <criterion>Exact inspection finds `load_mempool_snapshot_with_limits(limits: MempoolSnapshotDecodeLimits)` plus a separately named, deprecated zero-argument adapter whose note assigns migration/removal to Plan 06; there is no attempted overload and no `Default` limits.</criterion>
    <criterion>Every node-side load call is consolidated in snapshot-persistence tests, supplies explicit limits, and oversized/corrupt failures leave stored bytes byte-identical; only the two RPC startup callers remain on the zero-argument compatibility adapter.</criterion>
  </acceptance-criteria>
  <done>Every node snapshot load crosses an explicit bounded decode contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve achieved Sync receipts across completion failure</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/bitcoin-knots/src/node/mempool_persist.cpp</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - Executor accepts only Sync for Phase 135 checkpoint calls, encodes and performs Fjall SyncAll outside authority, then returns an achieved receipt without consuming it in completion.
    - Encode/write failure exact-aborts the unachieved capability and retains dirty state/source bytes.
    - Once storage succeeds, no path calls abort; completion can be retried idempotently with the retained receipt.
  </behavior>
  <action>
    Implement D-06 through D-08 in an explicitly ordered atomic cutover. First refactor `execute_prepared_mempool_snapshot_write` into an outside-lock persistence boundary that accepts an injected `FnMut() -&gt; PolicyTime`, samples it exactly once after success or failure, and returns `SnapshotWriteReceipt` after successful encoding and `PersistMode::Sync`/Fjall `SyncAll` via Plan 04's distinct `capability.acknowledge_checkpoint_write(completed_at, CheckpointPersistenceStrength::Sync)`. Encode/storage failure uses `abort_checkpoint_snapshot_write(SnapshotWriteAbort::new(capability, failed_at, SnapshotWriteFailure::Encode|Storage))`; an abort dispatch error surfaces only as `SnapshotWriteFailure::AbortDispatch`. Remove completion dispatch from the store executor so `snapshot_persistence.rs` retains every achieved-but-unacknowledged non-Clone receipt and calls `complete_checkpoint_snapshot_write` idempotently; no achieved path may call abort. Once exact snapshot-only caller inventory proves all executor/tests use those typed transition names, delete the old snapshot `acknowledge_write()`, `abort_snapshot_write(SnapshotWriteCapability)`, and legacy completion/abort command path across `network/lifecycle_effects.rs`, `network/lifecycle_projection.rs`, `network/runtime_authority/lifecycle.rs`, and `network/runtime_authority/effects.rs`. In that same edit, rename the typed methods to the final exact APIs `SnapshotWriteCapability::acknowledge_write(completed_at, strength)`, `ManagedNetworkHandle::abort_snapshot_write(SnapshotWriteAbort)`, and `ManagedNetworkHandle::complete_snapshot_write(SnapshotWriteReceipt)`, then update the already-migrated executor/tests to those final names. Do not change `PeerEffectCapability::acknowledge_write()` or any peer-effect caller. Inventory every executor caller with `rg -n 'execute_prepared_mempool_snapshot_write(_with)?\(' packages/open-bitcoin-node`; after Plan 04's test consolidation, all are definitions or callers in `storage/fjall_store/{mempool.rs,tests/snapshot_persistence.rs}`. Add cfg(test) encode/save/time seams, Sync reopen tests, completion-dispatch retry tests, and retained-byte failure tests.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-05-store -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features snapshot_persistence -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Successful executor return type contains `SnapshotWriteReceipt`; it does not contain `EffectCompletion`.</criterion>
    <criterion>Tests prove Sync/reopen, encode failure, write failure, abort failure, and original-byte retention.</criterion>
    <criterion>`rg -n 'complete_snapshot_write' packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs` returns no production executor call.</criterion>
    <criterion>Every inventoried executor caller either completes the returned `SnapshotWriteReceipt` explicitly or asserts a pre-achievement failure; no caller still expects `EffectCompletion`.</criterion>
    <criterion>No achieved receipt can enter `abort_snapshot_write` after save success.</criterion>
    <criterion>Type-aware/exact-function inspection of `impl SnapshotWriteCapability` finds only `acknowledge_write(self, completed_at, strength)` for snapshots, and exact inspection of snapshot methods on `ManagedNetworkHandle` finds only typed `abort_snapshot_write(SnapshotWriteAbort)` and `complete_snapshot_write(SnapshotWriteReceipt)`; the inspection deliberately excludes `PeerEffectCapability`, `PeerEmissionCapability`, and their zero-argument `acknowledge_write()` callers.</criterion>
    <criterion>Exact enum/dispatcher inspection finds no legacy snapshot terminal command variant after the rename, while peer-effect command variants and signatures remain outside the migration.</criterion>
  </acceptance-criteria>
  <done>Durable storage success is an owned achieved fact that survives completion-dispatch failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the single-flight coalescing coordinator</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-03-SUMMARY.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/checkpoint.rs, packages/open-bitcoin-node/src/network/checkpoint/tests.rs, packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - State is `Idle`, `Persisting`, or `AchievedAwaitingCompletion`; only the coordinator owns in-flight execution/receipt progress while authority owns generations/evidence.
    - Periodic clean tick returns SkippedClean, concurrent tick returns Coalesced, and newer mutation during I/O yields at most one immediate follow-up after completion.
    - Achieved completion is retried before any new capture; shutdown settle can require exact current durability after producers quiesce.
  </behavior>
  <action>
    Create `network/checkpoint.rs` plus focused child tests and export a shell-oriented `MempoolCheckpointCoordinator` from `network.rs`. Implement D-05 through D-08 with a private state enum `Idle`, `Persisting`, and `AchievedAwaitingCompletion(SnapshotWriteReceipt)`. A periodic tick reads authority evidence, skips clean state, prepares with injected time/trigger, changes state before outside-lock encode/Sync, retains achieved receipt, and completes idempotently. A tick arriving while persisting coalesces without a second reservation. After stale success, re-read dirty/current and perform at most one immediate coalesced follow-up; continuous mutation remains truthfully dirty for the next tick. On completion-dispatch failure, retain the receipt and retry it before any new write. Provide shutdown-settle API that callers use only after producer quiescence and that returns typed failure unless last-durable equals current. Add fake store/time/barrier tests for clean skip, concurrency, stale high-water, duplicate completion, encode/write/completion failures, and exact loss range. Add inline and registry breadcrumbs for both new Rust paths.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-05-coordinator -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features checkpoint -- --test-threads=1 &amp;&amp; bun scripts/check-parity-breadcrumbs.ts --check &amp;&amp; bun run scripts/command-timings.ts run --key phase135-wave5-workspace-check -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>A barrier test mutates authority during blocked Sync and proves no authority lock is held, no concurrent second write starts, stale durable high-water advances, and newer dirty state survives.</criterion>
    <criterion>Completion failure leaves `AchievedAwaitingCompletion`; the next call completes the same receipt before preparing new work.</criterion>
    <criterion>Periodic clean returns `SkippedClean`, pressure returns `Coalesced`, and shutdown settle succeeds only when durable equals current.</criterion>
    <criterion>New Rust files pass parity breadcrumb validation and contain no public cadence/config knob.</criterion>
    <criterion>The timed `cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features` exits zero at the end of Wave 5, proving both RPC startup callers still compile against the deprecated zero-argument load adapter before Plan 06.</criterion>
  </acceptance-criteria>
  <done>Checkpoint work is single-flight, coalesced, Sync-durable, retryable, and truthful across every failure prefix.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Prepared capability to Fjall | Encode/fsync may fail after reservation but before achievement. |
| Fjall success to authority completion | Achieved receipt must survive dispatch failure and staleness. |
| Coordinator state to concurrent triggers | Duplicate ticks must not create concurrent snapshots or lose dirty work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-135-05-01 | Repudiation | Achieved-but-unacknowledged receipt | mitigate | Return/retain receipt immediately after SyncAll and retry idempotent completion. |
| T-135-05-02 | Tampering | Abort after success | mitigate | Store executor has no success-to-abort path; abort is capability-only before achievement. |
| T-135-05-03 | Denial of Service | Concurrent checkpoints | mitigate | Explicit single-flight state plus authority one-pending cap and bounded one-follow-up rule. |
| T-135-05-04 | Denial of Service | Lock across I/O | mitigate | Prepare then release authority before encoding/Sync; barrier test proves concurrent mutation. |
| T-135-05-05 | Tampering | Stale completion | mitigate | Exact receipt completion advances high-water but cannot clear a newer dirty generation. |
</threat-model>

<verification>
Run timed snapshot-persistence and checkpoint suites sequentially, breadcrumb checks, the timed all-target/all-feature workspace Cargo check as the Wave 5 compile gate, no-lock barrier assertions, and `git diff --check`.
</verification>

<success-criteria>
- D-05 through D-08 and coordinator/store portions of D-14 are complete.
- MPDUR-04 has Sync-only successful boundaries, retained receipts, and single-flight coalescing.
- No journal, actor, multi-key manifest, or second mutation authority is introduced.
- The workspace, including both not-yet-migrated RPC snapshot loaders, compiles at the Wave 5 boundary through only the explicitly deprecated zero-argument load adapter.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-05-SUMMARY.md`.
</output>
