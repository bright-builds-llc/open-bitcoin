---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "14"
type: execute
wave: 14
depends_on: ["135-13"]
gap_closure: true
files_modified:
  - scripts/check-phase135-snapshot-recovery.ts
  - scripts/check-phase135-snapshot-recovery.test.ts
  - scripts/check-phase135-snapshot-recovery/source.ts
  - scripts/check-phase135-snapshot-recovery/persisted-input.ts
  - scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [MPDUR-01, MPDUR-02, MPDUR-03, MPDUR-04]
must_haves:
  truths:
    - "The live checker fails when the RPC recovery body constructs decode limits with five usize::MAX arguments instead of MempoolSnapshotDecodeLimits::for_persisted_input()."
    - "The live checker fails when prepare_recovery_topology replaces if records.len() > limits.max_vertices with if false, or replaces validate_parent_edges(record.record.transaction.inputs.len()) with validate_parent_edges(0)."
    - "The live checker fails when #[cfg(any())] is placed on or immediately above validate_raw_object_keys(bytes)?; or the Serde deserializer construction."
    - "The live checker fails when encode_mempool_snapshot restores a snapshot-derived encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len()) ceiling."
    - "Each of those five mutations yields exactly one intended diagnostic; the unmodified live corpus stays green."
    - "Parity remains in_progress, MPDUR rows remain Pending, and only the independent verifier may run phase135-14-full-verify after 135-14-SUMMARY.md exists."
  artifacts:
    - path: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      provides: "Production-use checks for RPC constructor, topology site guards, and writer representability"
      contains: "MempoolSnapshotDecodeLimits::for_persisted_input()"
    - path: scripts/check-phase135-snapshot-recovery/source.ts
      provides: "Attribute-aware direct-statement indexing"
      contains: "statementHasDisablingAttribute"
    - path: scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts
      provides: "Exact one-diagnostic mutations for WR-02, WR-03, WR-04, and CR-01 writer bypasses"
      contains: "usize::MAX, usize::MAX, usize::MAX, usize::MAX, usize::MAX"
    - path: docs/metrics/lines-of-code.md
      provides: "Fresh repository-generated LOC evidence"
    - path: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-14-SUMMARY.md
      provides: "Immutable final executor evidence for independent verification"
  key_links:
    - from: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      to: packages/open-bitcoin-rpc/src/context/mempool_recovery.rs
      via: "checker requires for_persisted_input inside recover_mempool_snapshot_with_loader"
      pattern: "fn recover_mempool_snapshot_with_loader"
    - from: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      to: packages/open-bitcoin-node/src/network/recovery/topology.rs
      via: "checker requires vertex and per-record edge guards inside prepare_recovery_topology"
      pattern: "if records.len\\(\\) > limits.max_vertices"
    - from: scripts/check-phase135-snapshot-recovery/source.ts
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs
      via: "directStatementIndex rejects attribute-disabled preflight/Serde statements"
      pattern: "statementHasDisablingAttribute"
    - from: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-14-SUMMARY.md
      to: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      via: "independent verifier creates a fresher candidate report only after the immutable final summary"
      pattern: "phase135-14-full-verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-13T16:43:49.000Z
---

<objective>
Close WR-02/WR-03/WR-04 / truth 17 and the CR-01 writer-side checker hole, then reserve the only full repository verifier for an independent post-summary transaction.

Purpose: The live checker is green today while four copied-corpus bypasses and a snapshot-derived encoder ceiling produce zero diagnostics. Production-use and attribute-aware proofs must fail those exact mutations.
Output: Attribute-aware direct-statement validation, five exact one-diagnostic mutations, fresh LOC, immutable Plan 14 summary, and the exclusive `phase135-14-full-verify` protocol.
</objective>

<execution-context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-PLAN.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-13-PLAN.md
@scripts/check-phase135-snapshot-recovery.ts
@scripts/check-phase135-snapshot-recovery/source.ts
@scripts/check-phase135-snapshot-recovery/persisted-input.ts
@scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts

<interfaces>
Current checker holes reproduced in VERIFICATION.md:

1. RPC startup (`mempool_recovery.rs` line 104) can become
   `MempoolSnapshotDecodeLimits::new(usize::MAX, usize::MAX, usize::MAX, usize::MAX, usize::MAX)`
   and today's checker still passes because it only inspects the store `for_persisted_input` body.
2. Production vertex guard `if records.len() > limits.max_vertices` in `prepare_recovery_topology` can become `if false`.
3. Production per-record call `limits.validate_parent_edges(record.record.transaction.inputs.len())` can become `validate_parent_edges(0)`.
4. `#[cfg(any())]` immediately above `validate_raw_object_keys(bytes)?;` compiles the guard out; `directStatementIndex` still treats the statement as live because it only checks brace depth and same-line whitespace.
5. Encoder can restore snapshot-derived
   `encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len())`.

Stable exports that must remain:
```typescript
export const PHASE135_TARGET_FILES: readonly string[];
export const PHASE135_DIAGNOSTICS: Readonly<Record<string, string>>;
export function checkPhase135SnapshotRecovery(maybeRepoRoot?: string): string[];
export function checkPersistedInputContract(...): void;
export function persistedInputMutations(...): readonly Phase135Mutation[];
```

Extend `PersistedInputSources` with:
```typescript
export type PersistedInputSources = {
  codec: string;
  codecDecode: string;
  capture: string;
  store: string;
  topology: string;
  staging: string;
  startup: string; // packages/open-bitcoin-rpc/src/context/mempool_recovery.rs
};
```

`FILES.startup` already exists in the root checker. Wire it into `checkPersistedInputContract`.

Required production anchors after Plan 13:
```
assert_mempool_snapshot_representable
if bytes.len() > limits.max_encoded_bytes
MempoolSnapshotDecodeLimits::for_persisted_input()
if records.len() > limits.max_vertices
limits.validate_parent_edges(record.record.transaction.inputs.len())
validate_raw_object_keys(bytes)?;
let mut deserializer = serde_json::Deserializer::from_slice(bytes);
```
</interfaces>
</context>

<project-guidance>
Follow repo-local `AGENTS.md`, `AGENTS.bright-builds.md`, empty override table, and code-shape/testing/verification/TypeScript standards. Bun + TypeScript only; no Python or new dependency. Keep root CLI/test entrypoints stable. Every touched TypeScript file must be at most 628 physical lines. One concern per mutation/test with Arrange/Act/Assert through the existing assertion helper. Generate LOC only with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`. Do not run `bash scripts/verify.sh`. Do not create or edit canonical `135-VERIFICATION.md`. Do not promote MPDUR or parity status. Do not plan Phase 137 presentation.
</project-guidance>

<decision-coverage>
D-04/D-09/D-14/D-15 are fully covered by production-use and attribute-aware structural proofs plus exact mutations. D-13/MPDUR-03 remain full because checker changes must not recouple age or unbroadcast semantics. Writer representability from Plan 13 is fully guarded here. No decision is partial.
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require production-use anchors and attribute-aware direct statements</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>scripts/check-phase135-snapshot-recovery.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/source.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/persisted-input.ts</file>
    <file>packages/open-bitcoin-rpc/src/context/mempool_recovery.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/topology.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery.ts, scripts/check-phase135-snapshot-recovery/source.ts, scripts/check-phase135-snapshot-recovery/persisted-input.ts</files>
  <behavior>
    - `directStatementIndex` returns -1 when the candidate statement has a Rust attribute on the same line or on the immediately preceding non-empty line matching `/^\s*#\[/`.
    - `checkPersistedInputContract` extracts `body(startup, "fn recover_mempool_snapshot_with_loader")` and requires the exact executable call `MempoolSnapshotDecodeLimits::for_persisted_input()`. Presence of that identifier only in tests or in `fjall_store/mempool.rs` is insufficient. The startup body must not contain `usize::MAX`.
    - Topology production body `prepare_recovery_topology` must contain `if records.len() > limits.max_vertices` and `limits.validate_parent_edges(record.record.transaction.inputs.len())`. Source-wide helper tokens alone are insufficient.
    - Encode body `encode_mempool_snapshot` must contain `assert_mempool_snapshot_representable` and `bytes.len() > limits.max_encoded_bytes`, and must not contain `encoded_size_upper_bound(total_transaction_bytes`.
    - Capture body must still contain `assert_mempool_snapshot_representable` after Plan 13.
    - Unrelated Phase 135 checks, diagnostics, and filesystem-only behavior stay stable.
  </behavior>
  <action>
    In `source.ts`, add `statementHasDisablingAttribute(maskedBody: string, statementIndex: number): boolean` that walks backward from `statementIndex`: skip spaces/tabs; if the same line contains `#[` before the statement, return true; if the previous non-empty line matches `/^\s*#\[/`, return true. Call it from `directStatementIndex` so an attributed match is ignored (`continue` / treat as not found). Keep comment/string masking unchanged. Do not add a Rust parser dependency.

    Extend `PersistedInputSources` with `startup: string`. In the root checker, pass `startup: get(FILES.startup)` into `checkPersistedInputContract`.

    In `persisted-input.ts`:
    - After existing decode bounds, require `body(sources.startup, "fn recover_mempool_snapshot_with_loader")` to include `MempoolSnapshotDecodeLimits::for_persisted_input()` and to exclude `usize::MAX` and `MempoolSnapshotDecodeLimits::new(`. Route failures through `diagnostics.bounds` / `PHASE135_DIAGNOSTICS.bounds`.
    - In topology checks, extract `body(sources.topology, "pub(crate) fn prepare_recovery_topology(")` and require the exact production strings `if records.len() > limits.max_vertices` and `limits.validate_parent_edges(record.record.transaction.inputs.len())`. Keep the existing helper-token checks. Route misses through `diagnostics.topology`.
    - In decode bounds, extract `body(sources.codec, "pub(crate) fn encode_mempool_snapshot(")` and require `assert_mempool_snapshot_representable` plus `bytes.len() > limits.max_encoded_bytes`. Fail if that encode body includes `encoded_size_upper_bound(total_transaction_bytes`. Also require `assert_mempool_snapshot_representable` in `body(sources.capture, "LifecycleCommand::PrepareSnapshot")` or the `PrepareSnapshot` arm text already assembled as `sources.capture`. Route through `diagnostics.bounds`.
    - Keep the existing preflight/Serde `directStatementIndex` comparison; attributed preflight now makes `preflightIndex < 0` and fails bounds.

    Do not duplicate root orchestration. Do not claim seven maxima coexist. Keep each TypeScript file at or below 628 lines.
  </action>
  <verify>
    <automated>bun run scripts/check-phase135-snapshot-recovery.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The live checker exits 0 on the unmodified corpus and still exports `PHASE135_TARGET_FILES`, `PHASE135_DIAGNOSTICS`, and `checkPhase135SnapshotRecovery`.</criterion>
    <criterion>`persisted-input.ts` contains `fn recover_mempool_snapshot_with_loader`, `if records.len() > limits.max_vertices`, `validate_parent_edges(record.record.transaction.inputs.len())`, and `assert_mempool_snapshot_representable`.</criterion>
    <criterion>`source.ts` contains `statementHasDisablingAttribute` and `directStatementIndex` calls it.</criterion>
    <criterion>`wc -l` on the three touched TypeScript files reports each at or below 628.</criterion>
  </acceptance-criteria>
  <done>Production-use and attribute-aware anchors are required by the live checker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact one-diagnostic mutations for the four reproduced bypasses and the writer ceiling</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>scripts/check-phase135-snapshot-recovery.test.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/persisted-input.ts</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts, scripts/check-phase135-snapshot-recovery.test.ts</files>
  <behavior>
    - Mutation "RPC persisted-input constructor replaced with five usize::MAX arguments" replaces `MempoolSnapshotDecodeLimits::for_persisted_input()` inside `packages/open-bitcoin-rpc/src/context/mempool_recovery.rs` with `MempoolSnapshotDecodeLimits::new(usize::MAX, usize::MAX, usize::MAX, usize::MAX, usize::MAX)` and yields exactly `PHASE135_DIAGNOSTICS.bounds`.
    - Mutation "topology vertex guard compiled out" replaces `if records.len() > limits.max_vertices` with `if false` and yields exactly `PHASE135_DIAGNOSTICS.topology`.
    - Mutation "per-record edge validation bypassed" replaces `limits.validate_parent_edges(record.record.transaction.inputs.len())` with `limits.validate_parent_edges(0)` and yields exactly `PHASE135_DIAGNOSTICS.topology`.
    - Mutation "raw-key preflight disabled by cfg(any())" inserts `#[cfg(any())]\n    ` immediately before `validate_raw_object_keys(bytes)?;` in `decode.rs` and yields exactly `PHASE135_DIAGNOSTICS.bounds`.
    - Mutation "encoder restores snapshot-derived encoded ceiling" replaces `bytes.len() > limits.max_encoded_bytes` or the Plan 13 representability/encode ceiling with `encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len())` acceptance and yields exactly `PHASE135_DIAGNOSTICS.bounds`.
    - Each new mutation is `exact: true`. All 75 prior cases remain green. The unmodified live corpus passes.
  </behavior>
  <action>
    Add the five mutations above to `persistedInputMutations`. Extend `PersistedInputMutationPaths` with `startup: string` if the RPC file is not already in the fixture map; the root test must copy `FILES.startup` into the temp corpus the same way it copies codec/store/topology. Use the existing `replace` helper. Do not weaken prior mutations. Do not add a cfg-parser dependency.

    For the writer mutation, replace the exact Plan 13 encode acceptance so the production checker sees `encoded_size_upper_bound(total_transaction_bytes` again. Prefer replacing
    `if bytes.len() > limits.max_encoded_bytes {`
    with
    `let max_encoded_bytes = encoded_size_upper_bound(total_transaction_bytes, dto.records.len(), dto.unbroadcast_members.len()).unwrap_or(usize::MAX);\n    if bytes.len() > max_encoded_bytes {`
    only if `total_transaction_bytes` still exists; if Plan 13 removed that local, insert the snapshot-derived call as the sole encoded-byte predicate inside `encode_mempool_snapshot` and delete `assert_mempool_snapshot_representable` from that body.

    Keep fixture lifecycle and generic assertions in the root test. Every case remains one concern. Do not edit parity status, REQUIREMENTS, or canonical verification.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts && bun run scripts/check-phase135-snapshot-recovery.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The five named mutations each fail with exactly one intended diagnostic (`bounds` or `topology` as specified).</criterion>
    <criterion>The full mutation suite stays green for every pre-existing case, and the unmodified live corpus exits 0.</criterion>
    <criterion>`wc -l` on `persisted-input-mutations.ts`, `persisted-input.ts`, `source.ts`, the root checker, and the root test is at most 628 each.</criterion>
    <criterion>The root test still imports `persistedInputMutations` from the child module and does not inline the new fixtures.</criterion>
  </acceptance-criteria>
  <done>Every reproduced checker bypass now fails with one stable diagnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh LOC, publish immutable Plan 14 evidence, and reserve the independent verifier</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/verification.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-PLAN.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md</file>
    <file>scripts/generate-loc-report.ts</file>
    <file>scripts/verify.sh</file>
  </read-first>
  <files>docs/metrics/lines-of-code.md, .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-14-SUMMARY.md</files>
  <action>
    Run the mutation suite and live checker, focused Plan 12/13 Rust tests if those binaries are already built, `bun run scripts/check-parity-breadcrumbs.ts --check`, `bun run scripts/bright-builds-check.ts all`, TypeScript `wc -l` audit, and `git diff --check`. Generate `docs/metrics/lines-of-code.md` only with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`, then prove freshness with the same command plus `--check`.

    Create immutable `135-14-SUMMARY.md` only after executor-owned commands pass. Record checker line counts, the five new mutation names and exact diagnostics, live-corpus pass, pending parity/MPDUR status, and that the executor did not run `bash scripts/verify.sh` and did not edit canonical `135-VERIFICATION.md`. Make no executor-owned repository edit after the summary commit.

    Do not run `bash scripts/verify.sh`. If source-truth gaps remain (checker still misses a listed bypass, representability/generation regressions fail, or High threats are unresolved), the independent verifier protocol below forbids the reserved full command and must write `status: gaps_found`.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts && bun run scripts/check-phase135-snapshot-recovery.ts && bun run scripts/check-parity-breadcrumbs.ts --check && bun run scripts/bright-builds-check.ts all && bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check && git diff --check</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Mutation/live checker, breadcrumbs, Bright Builds, LOC freshness, line-count audit, and `git diff --check` pass on the final executor tree.</criterion>
    <criterion>Every touched TypeScript file is at most 628 physical lines and the summary records the exact `wc -l` results.</criterion>
    <criterion>The summary is created last, keeps MPDUR Pending and parity `in_progress`, and contains no claim that full/default/canonical verification already passed.</criterion>
    <criterion>The executor tree does not contain a new or edited canonical `135-VERIFICATION.md` from this plan.</criterion>
  </acceptance-criteria>
  <done>Immutable Plan 14 evidence is ready for the exclusive independent verifier transaction.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1. Any unresolved High severity threat blocks completion and must yield gaps; it may not be accepted or deferred.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust production body → structural checker | Identifier presence in helpers/tests must not attest a live call site. |
| Outer Rust attributes → direct statements | `#[cfg(any())]` can compile a required guard out while leaving the token in source. |
| Encoder ceiling → reader contract | A snapshot-derived bound can acknowledge an unreadable Sync write. |
| Immutable summary → canonical verifier report | Only an independent post-summary transaction may attest the default verifier. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-135-14-01 | High | Spoofing | RPC recovery constructor | mitigate | Require `for_persisted_input` inside `recover_mempool_snapshot_with_loader`, not store-only. | Five-`usize::MAX` mutation → bounds. |
| T-135-14-02 | High | Tampering | Topology production body | mitigate | Require vertex and per-record edge guards at `prepare_recovery_topology` use sites. | `if false` and `validate_parent_edges(0)` mutations → topology. |
| T-135-14-03 | High | Tampering | Raw-key preflight | mitigate | Attribute-aware `directStatementIndex` rejects `#[cfg(any())]` on/above the statement. | cfg mutation → bounds. |
| T-135-14-04 | High | Tampering | Encoder representability | mitigate | Require `assert_mempool_snapshot_representable` and `limits.max_encoded_bytes`; reject snapshot-derived `encoded_size_upper_bound(total_transaction_bytes`. | Writer-ceiling mutation → bounds. |
| T-135-14-05 | High | Repudiation | Canonical verification attestation | mitigate | Candidate report precedes the exact final command; failure/tree change forces `gaps_found`; success permits no later edit. | Independent `phase135-14-full-verify` protocol. |
| T-135-14-06 | Medium | Elevation of Privilege | Requirement/claim promotion | mitigate | Keep Phase 135 in_progress and MPDUR Pending until independent verification. | No REQUIREMENTS/parity promotion. |

## Residual Risks

- The dependency-free checker remains a structural proof, not a Rust compiler frontend. Exact body extraction, attribute awareness, and one-diagnostic mutations address the reproduced bypasses.
- Full repository verification is intentionally deferred past the immutable summary.
</threat-model>

<verification>
Executor-owned verification ends after Task 3. The executor must not run `bash scripts/verify.sh` and must not create, edit, restore, replace, stage, or forge canonical `135-VERIFICATION.md`.

The following phase-level transaction belongs exclusively to the independent standard `gsd-verifier` spawned by `/gsd-execute-phase` after `135-14-SUMMARY.md` exists:

1. Independently verify all Phase 135 source, behavior, artifact, key-link, threat, requirement, lifecycle, scope, checker-line-count, and protected-hash truths against the final post-summary tree. Explicitly adjudicate the Plan 12 terminal-generation gap, the Plan 13 writer/reader representability gap, and the Plan 14 checker bypasses (WR-02, WR-03, WR-04, writer ceiling).
2. If any source truth, lifecycle invariant, file-shape gate, or unresolved High threat fails, immediately write a fresh lifecycle-valid canonical `135-VERIFICATION.md` with `status: gaps_found`, name the failed truth/threat, and return non-passed. Do not run the default verifier and do not promote the phase.
3. If all source truths pass, write a lifecycle-valid candidate canonical `135-VERIFICATION.md` on the post-summary tree with `status: passed`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 135-2026-08-02T17-41-48`, and `generated_at` later than `135-14-SUMMARY.md`. Before execution, the candidate must name exactly `bun run scripts/command-timings.ts run --key phase135-14-full-verify -- bash scripts/verify.sh`, record `exit_status: 0` and `result: passed` only as a conditional transactional attestation, and state that passed is valid solely if the immediately following command exits zero on the unchanged candidate-report tree.
4. Without any intervening repository edit, run exactly `bun run scripts/command-timings.ts run --key phase135-14-full-verify -- bash scripts/verify.sh` as the final command. Do not use `--fast`, do not overlap Cargo jobs, distinguish quiet polling from timeout, and poll a yielded session at least every 60 seconds with liveness evidence before any termination decision.
5. If the command fails or the repository tree changes, immediately rewrite canonical `135-VERIFICATION.md` to lifecycle-valid `status: gaps_found`, record the exact command, nonzero/freshness failure, and affected truth, then return non-passed. Do not complete or promote the phase.
6. If the command exits zero and the tree is unchanged, make no further repository edit and return `passed`; the candidate report's conditional attestation is now truthful for the exact validated tree.

Standard `/gsd-execute-phase` may update roadmap/requirements only after `gsd-verifier` returns `passed`. A gap, stale lifecycle provenance, unresolved High threat, command failure, or post-command edit stops completion through the existing standard path.
</verification>

<success-criteria>
- WR-02, WR-03, WR-04, and the CR-01 writer-ceiling bypass each fail with exactly one intended diagnostic.
- Production RPC, topology, preflight, and encode anchors are required at their use sites, not as source-wide tokens.
- Direct-statement validation is attribute-aware.
- Every touched TypeScript file is at most 628 physical lines.
- Parity remains in_progress/Pending with Phase 136-138/public/readiness exclusions intact.
- Independent lifecycle-valid verification runs the exact `phase135-14-full-verify` command last and reports passed only after zero exit on the unchanged candidate-report tree.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-14-SUMMARY.md`.
</output>
