---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "03"
type: execute
wave: 4
depends_on: ["135-02", "135-04"]
files_modified:
  - packages/open-bitcoin-node/src/network/recovery.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases/connected_block_removal.rs
autonomous: true
requirements: [MPDUR-02, MPDUR-03]
must_haves:
  truths:
    - "A prepared recovery installs once through ManagedNetworkHandle and no partially rebuilt mempool or cache is visible."
    - "Canonical survivors rebuild every derived projection while recovery initializes both current and last-durable generation from the captured generation with an empty loss interval."
    - "Recovery evidence and exact unbroadcast membership describe final survivors only, with no relay scheduling side effect."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      provides: "Startup-only InstallRecovery lifecycle command"
      contains: "InstallRecovery"
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      provides: "Consuming authoritative recovery install"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      via: "ManagedNetworkHandle consumes PreparedMempoolRecovery through LifecycleCommand"
      pattern: "InstallRecovery"
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      via: "InstallRecovery initializes current_generation and last_durable_generation from captured_generation while clearing dirty and in-flight checkpoint state"
      pattern: "last_durable_generation"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-02T18:01:26.808Z
---

<objective>
Install staged recovery atomically through the sole runtime authority.

Purpose: Replace the current stored-order live mutation path with one startup-only complete projection.
Output: A consuming recovery command, all-cache rebuild, generation semantics, and authority integration tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-02-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md
@packages/open-bitcoin-node/src/network/lifecycle_projection.rs
@packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs

<interfaces>
Plan 02 creates non-Clone `PreparedMempoolRecovery`. Plan 04 extends snapshot generation/capture contracts. Existing authority accepts all runtime mutation through `LifecycleCommand` and applies dependent projections in the order compact, serving, fanout, peer, unbroadcast, persistence, evidence. This Wave 4 plan must leave the workspace compilable while `packages/open-bitcoin-rpc/src/context/mempool_recovery.rs` still uses the pre-Phase-135 no-time recovery calls: introduce the staged/injected-time APIs as the production target, but retain deprecated `ManagedPeerNetwork::recover_mempool_snapshot` and `ManagedNetworkHandle::recover_mempool_snapshot` adapters with their existing signatures solely for those RPC callers until Plan 06. Those adapters preserve the existing legacy live-replay behavior; they must not fabricate `PolicyTime`, captured generation, durable evidence, or authoritative-unbroadcast claims and must be deleted in Plan 06 after RPC migration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the startup-only consuming recovery command</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-02-SUMMARY.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/network/recovery.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/staging.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/recovery.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs</files>
  <behavior>
    - `ManagedNetworkHandle` prepares recovery outside the guard and consumes it exactly once through `LifecycleCommand::InstallRecovery`.
    - Install is valid only for a fresh startup authority and fails before mutation on epoch/generation/state mismatch.
    - The command replaces canonical mempool plus all projections in one short authority mutation and cannot be cloned/replayed.
    - Successful install sets both current_generation and last_durable_generation to captured_generation, clears dirty and in-flight checkpoint state, and yields an empty `(last_durable,current]` loss interval immediately after restart.
  </behavior>
  <action>
    Implement D-09 and D-12 by adding `LifecycleCommand::InstallRecovery(PreparedMempoolRecovery)` and a matching result/facade. Keep chainstate/policy replay outside the authority; only consume the fully prepared result under the guard. Validate startup-only preconditions (fresh canonical mempool/projections, matching authority incarnation, no pending lifecycle effects, captured generation representable) before mutation. In `authority.rs`, replace the staged mempool and rebuild compact candidate inputs, serving txid/wtxid maps, inert fanout membership, peer-known/request state, exact unbroadcast set, and bounded recovery evidence from canonical final survivors. Explicitly set `current_generation = captured_generation` and `last_durable_generation = Some(captured_generation)`, clear `dirty_generation` and every in-flight/pending snapshot binding, and initialize evidence so `checkpoint_evidence` reports no loss interval immediately after recovery. The next ordinary mutation must advance exactly once from the captured generation and open the corresponding dirty/loss range. Do not seed outbound queues, advance dirty state for exact replay, or expose a public runtime import API.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-03-install -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features recovery_cases -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'InstallRecovery|PreparedMempoolRecovery' packages/open-bitcoin-node/src/network/{lifecycle_projection.rs,runtime_authority/lifecycle.rs,runtime_authority/effects.rs}` finds one closed command path.</criterion>
    <criterion>Tests prove stale/non-fresh install fails with an exact before/after aggregate snapshot and consumes no partial state.</criterion>
    <criterion>Successful install sets current and last-durable generation to the captured generation, leaves dirty and in-flight empty, and `checkpoint_evidence` reports an empty loss interval before the next mutation.</criterion>
    <criterion>The first ordinary post-recovery mutation advances exactly once from captured generation and opens `(captured_generation,current_generation]` without regressing last-durable evidence.</criterion>
    <criterion>No recovered survivor is enqueued for whole-mempool fanout or treated as already transported.</criterion>
  </acceptance-criteria>
  <done>Prepared recovery has one non-replayable startup path through the existing sole authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove complete final-state rebuild and recovery evidence</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-02-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/network/tests/recovery_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases/connected_block_removal.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/recovery.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/tests/recovery_cases.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases/connected_block_removal.rs</files>
  <behavior>
    - Summary counts include recovered, confirmed, duplicate, missing-parent, policy-incompatible, expired, and evicted final outcomes.
    - Reconciliation reports zero mismatches across canonical, compact, serving, fanout, peer, unbroadcast, persistence, and evidence state.
    - Failure injection at every pre-install validation point preserves the exact aggregate.
    - Node-owned recovery paths use injected startup time and staged installation, while the deprecated no-time recovery signatures remain compile-compatible only for the two RPC startup callers until Plan 06.
  </behavior>
  <action>
    Extend `ManagedMempoolRecoverySummary` with `dropped_expired_count` and construct it only from Plan 02 final records. Add the injected-startup-time prepare-outside/install-once flow: `prepare_mempool_recovery_at(snapshot, verify_flags, consensus_params, startup_at: PolicyTime)` captures the current chainstate/policy inputs under a short read, stages after releasing the guard, and returns non-Clone `PreparedMempoolRecovery`; `install_mempool_recovery(prepared)` consumes it through `LifecycleCommand::InstallRecovery`. Inventory every current caller with `rg -n '\brecover_mempool_snapshot\s*\(' packages/open-bitcoin-node packages/open-bitcoin-rpc --glob '*.rs'`. Migrate all node-owned callers in `network/tests/recovery_cases.rs` and all three callers at lines currently found in `network/tests/mempool_lifecycle_cases/connected_block_removal.rs` to pass deterministic injected startup times and perform staged installation. In the same compile-safe edit, retain the existing no-time signatures on both `ManagedPeerNetwork` in `network/recovery.rs` and `ManagedNetworkHandle` in `network/runtime_authority.rs`, mark both `#[deprecated(note = "RPC startup compatibility only; migrate and remove in Phase 135 Plan 06")]`, and document that their sole allowed consumers are the two startup composition calls in `packages/open-bitcoin-rpc/src/context/mempool_recovery.rs`. Their bodies must preserve the pre-Phase-135 stored-order/live-replay behavior needed by those callers and must not route through the staged API by inventing startup time, captured generation, durable evidence, or authoritative unbroadcast membership. Plan 06 owns migration of both RPC constructor families and atomic deletion of both adapters; do not leave any additional node caller on them. Keep storage-level snapshot failures distinct from record drops. Add integration scenarios for mixed independent components, reverse-order parent/child, expiry, capacity trim, exact surviving unbroadcast, rolling reset, connected-block removal/replacement after recovery, and a subsequent mutation. Reuse the reconciliation oracle to assert every derived target equals canonical survivors. Add test-only pre-install failure injection before any aggregate mutation and assert exact state equality for each failure point.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-03-recovery-integration -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features recovery_cases -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-03-reconciliation -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features lifecycle_projection_cases::reconciliation -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-wave4-workspace-check -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>All seven stable record classes have independent count assertions and snapshot-level failures do not fabricate record counts.</criterion>
    <criterion>Mixed-component integration proves valid independent records survive invalid/expired/missing components.</criterion>
    <criterion>Reconciliation is exact after recovery and after the first post-recovery lifecycle mutation.</criterion>
    <criterion>Every injected validation failure leaves canonical and all dependent projections byte-for-byte/equality unchanged.</criterion>
    <criterion>`rg -n 'recover_mempool_snapshot\s*\(' packages/open-bitcoin-node --glob '*.rs'` finds only the two explicitly deprecated compatibility definitions/delegation and no node-owned test caller; `rg -n 'prepare_mempool_recovery_at|install_mempool_recovery' packages/open-bitcoin-node/src/network/runtime_authority.rs packages/open-bitcoin-node/src/network/tests/recovery_cases.rs packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases/connected_block_removal.rs` finds the staged facade and every migrated node test path.</criterion>
    <criterion>Exact inspection of both deprecated adapters finds the Plan 06 removal note and confirms they preserve the legacy no-time behavior without constructing a `PolicyTime`, captured generation, durable checkpoint evidence, or authoritative-unbroadcast restoration claim.</criterion>
    <criterion>The timed `cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features` exits zero at the end of Wave 4, proving both RPC startup callers still compile against the deprecated recovery adapters before Plan 06.</criterion>
    <criterion>The three connected-block-removal scenarios recover with explicit deterministic `PolicyTime`, then prove confirmed removal, conflict/descendant cleanup, and replacement identity behavior against the installed staged state.</criterion>
  </acceptance-criteria>
  <done>Recovery publishes only complete final truth and the entire authority reconciles after one atomic install.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Prepared recovery to authority | An opaque staged value requests complete runtime replacement. |
| Canonical survivors to derived projections | Cache drift or partial application can create false serve/relay truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-135-03-01 | Spoofing | Recovery authority | mitigate | Bind install to the current authority incarnation and fresh-start state. |
| T-135-03-02 | Tampering | Partial projection | mitigate | Validate first, then consume one complete projection through sole dispatcher. |
| T-135-03-03 | Elevation of Privilege | Runtime import/rebroadcast | mitigate | Keep facade startup-only and build inert fanout state without emissions. |
| T-135-03-04 | Repudiation | Recovery evidence | mitigate | Counts derive solely from final typed records and reconcile with canonical membership. |
| T-135-03-05 | Denial of Service | Authority critical section | mitigate | All graph/policy work completes before lock; install performs bounded replacement only. |
</threat-model>

<verification>
Run recovery and reconciliation suites sequentially through the timing wrapper, then run the timed all-target/all-feature workspace Cargo check as the Wave 4 compile gate, inspect dispatcher reachability, and run `git diff --check`.
</verification>

<success-criteria>
- D-09 through D-13 and the authority portion of D-14 are fully implemented.
- MPDUR-02 and MPDUR-03 are observable through final outcomes, exact rebuild, and rolling reset.
- ManagedNetworkHandle remains the sole mutation authority and recovery never exposes partial state.
- The workspace, including both not-yet-migrated RPC recovery callers, compiles at the Wave 4 boundary through only the explicitly deprecated compatibility adapters.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-03-SUMMARY.md`.
</output>
