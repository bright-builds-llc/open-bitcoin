---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "02"
type: execute
wave: 3
depends_on: ["135-01", "135-04"]
files_modified:
  - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
  - packages/open-bitcoin-node/src/network/recovery.rs
  - packages/open-bitcoin-node/src/network/recovery/topology.rs
  - packages/open-bitcoin-node/src/network/recovery/staging.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPDUR-02, MPDUR-03]
must_haves:
  truths:
    - "Recovery validates dependencies and deterministically replays parent-before-child into a fresh staged mempool."
    - "Invalid components do not prevent independent valid components from recovery, and all outcomes are classified only after final membership."
    - "Preserved ages and exact surviving unbroadcast semantics remain while rolling fee and decay-gate state reset to the fresh restart baseline."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/recovery/topology.rs
      provides: "Bounded deterministic dependency analysis"
    - path: packages/open-bitcoin-node/src/network/recovery/staging.rs
      provides: "Opaque PreparedMempoolRecovery built outside runtime authority"
  key_links:
    - from: packages/open-bitcoin-node/src/network/recovery/staging.rs
      to: packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
      via: "validated source envelope replayed using preserved acceptance time"
      pattern: "PreparedMempoolRecovery"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-02T18:01:26.808Z
---

<objective>
Build the pure, deterministic staged recovery engine.

Purpose: Keep graph analysis and policy replay outside live authority so partial reconstruction is never observable.
Output: Bounded topology sorting, staged replay/finalization, complete typed outcomes, and pure tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md
@packages/open-bitcoin-node/src/network/recovery.rs
@packages/open-bitcoin-node/src/network/tests/recovery_cases.rs

<interfaces>
Plan 01 supplies a validated source-only v2 payload plus temporary verified in-memory compatibility members required by the pre-Phase-135 `network/recovery.rs`. Plan 04 has already moved every production snapshot capture to explicit authority-owned generation, `PolicyTime`, trigger, and authoritative unbroadcast membership, so this plan is the atomic removal point: delete `MempoolSnapshotRecord::{txid,wtxid,fee_sats,virtual_size,metadata}` and the deprecated parameterless `MempoolSnapshot::from_mempool` compatibility path while migrating `network/recovery.rs` and its tests to derive identity and admission context from canonical transaction plus preserved acceptance time. This plan also introduces `MempoolRecoveryStatus::DroppedExpired` atomically with every `network/recovery.rs` exhaustive match and count path. `Mempool::new(PolicyConfig)` is the fresh restart baseline; replay receives explicit chainstate, flags, consensus params, policy, and startup `PolicyTime`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded deterministic dependency analysis</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>packages/open-bitcoin-node/src/network/recovery.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/recovery_cases.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-core/src/primitives/transaction.rs</file>
    <file>packages/bitcoin-knots/src/node/mempool_persist.cpp</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/network/recovery.rs, packages/open-bitcoin-node/src/network/recovery/topology.rs, packages/open-bitcoin-node/src/network/tests/recovery_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Dependencies derive only from canonical transaction inputs whose parent transaction is present in the validated snapshot identity map; an absent external prevout is not classified by topology.
    - Kahn ordering uses a BTreeSet keyed by txid then wtxid and is stable across stored order.
    - Failed in-snapshot parents, cyclic components, and over-limit components are typed and bounded while independent components and records with external prevouts remain ordered for replay.
  </behavior>
  <action>
    Start with the compile-safe schema cutover promised by Plans 01 and 04: in `storage/mempool_snapshot.rs`, remove the temporary `MempoolSnapshotRecord::{txid,wtxid,fee_sats,virtual_size,metadata}` compatibility members and deprecated parameterless `MempoolSnapshot::from_mempool` path. In the same task/commit, migrate every read in `network/recovery.rs` and `network/tests/recovery_cases.rs`: derive txid/wtxid once with the repository canonical transaction functions, derive fee/vsize only through current chainstate/policy admission, and construct recovery admission metadata solely from preserved `MempoolAcceptanceTime` plus the final authoritative-unbroadcast decision (never historical origin/relay metadata). No caller may observe the removed fields between commits; run Cargo immediately after this atomic edit. Then create `network/recovery/topology.rs` under the existing `recovery.rs` entrypoint. Implement D-02, D-04, D-09, and D-10 with a std-only bounded Kahn graph: build canonical identity and parent/child maps once, reject duplicate/foreign identities before graph work, use a `BTreeSet` ready queue ordered by `(txid,wtxid)`, and cap vertices/edges with the validated envelope limits. Create an edge only when an input's previous txid resolves to a transaction inside the snapshot; do not classify an absent external prevout as a missing parent because it may be a valid current-chainstate UTXO. Propagate `DroppedMissingParent` only from failed or cyclic in-snapshot parents at this stage, without blocking independent components. Return an opaque ordered topology result that carries external-prevout records forward to Task 2 admission; do not mutate `ManagedPeerNetwork`, read clocks, inspect chainstate, or use a graph dependency. Add the new Rust path's inline Knots breadcrumbs and registry entry in `docs/parity/source-breadcrumbs.json` in the same task.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-02-topology -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features recovery_cases -- --test-threads=1 &amp;&amp; bun scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Parent/child fixtures produce identical ordered identities for forward, reverse, and shuffled input.</criterion>
    <criterion>Fixtures prove an external prevout absent from the snapshot remains ordered when current chainstate may satisfy it, while failed/cyclic in-snapshot parents and edge-limit cases affect only their component.</criterion>
    <criterion>`rg -n 'BTreeSet|checked_|DroppedMissingParent' packages/open-bitcoin-node/src/network/recovery/topology.rs` finds the deterministic/bounded implementation.</criterion>
    <criterion>Exact struct/impl inspection finds no `txid`, `wtxid`, `fee_sats`, `virtual_size`, or `metadata` member on the current v2 source record and no parameterless compatibility capture constructor; `network/recovery.rs` derives identities from `record.transaction` and uses preserved acceptance time rather than removed metadata.</criterion>
    <criterion>Parity breadcrumb validation passes for the new Rust path.</criterion>
  </acceptance-criteria>
  <done>Recovery ordering is deterministic, bounded, and component-local before any policy replay begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage policy replay and derive final recovery truth</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/network/recovery.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/topology.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/recovery_cases.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/expiry.rs</file>
    <file>packages/open-bitcoin-mempool/src/fee/rolling.rs</file>
    <file>packages/bitcoin-knots/src/txmempool.cpp</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/network/recovery.rs, packages/open-bitcoin-node/src/network/recovery/staging.rs, packages/open-bitcoin-node/src/network/tests/recovery_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Replay uses a fresh Mempool and current chainstate/consensus/static/effective policy with preserved acceptance times and injected startup time.
    - Current-chainstate admission owns classification of external prevouts: only an external prevout proven absent there becomes `DroppedMissingParent`, after which its in-snapshot descendants receive the same class.
    - `Recovered` and `DroppedEvicted` are decided from final post-expiry/post-capacity membership, with `DroppedExpired` distinct.
    - Restored unbroadcast equals persisted identities intersect final survivors and local-intent eligibility; rolling rate, gate, and decay timestamp equal a fresh pool.
  </behavior>
  <action>
    Create `network/recovery/staging.rs` and define non-Clone `PreparedMempoolRecovery` containing the complete fresh staged `Mempool`, final ordered `MempoolRecoveryRecord`s, exact surviving unbroadcast set, captured generation/time, and projection inputs needed by Plan 03. At the start of this task, add `MempoolRecoveryStatus::DroppedExpired` to `storage/mempool_snapshot.rs` and update every exhaustive status match and `ManagedMempoolRecoverySummary` count path in `network/recovery.rs` in the same commit, before running Cargo; no wave may contain the new variant without its exhaustive consumers. Replay D-09 through D-13 using the topology result, current chainstate/flags/consensus/current `PolicyConfig`, preserved acceptance times, and injected startup `PolicyTime`. For every input whose parent is absent from the snapshot, let current-chainstate admission prove whether the prevout exists; emit `DroppedMissingParent` only after that proof fails, then propagate the result to in-snapshot descendants. A snapshot-external prevout that exists in current chainstate must remain eligible for ordinary policy admission. Per D-11, classify `Recovered` only after final post-policy/post-capacity membership and derive restored unbroadcast only from that same final set. Classify confirmed, duplicate, proven missing-parent, policy-incompatible, and expired records during staging, then run final expiry/capacity behavior and only afterward classify survivors as `Recovered` and admitted-but-absent records as `DroppedEvicted`. Finalize every rolling-fee field and decay gate to the fresh restart baseline. Restore unbroadcast only from the persisted set intersected with final surviving identities; assign local/requested recovery metadata only to that exact set and recovery-unknown/not-requested to all other survivors. Add inline/registry breadcrumbs for the new file. Do not touch live caches, advance ordinary dirty generation, or schedule relay.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-02-staging -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features recovery_cases -- --test-threads=1 &amp;&amp; bun scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Tests cover parent-before-child replay, an external prevout satisfied by current chainstate, an external prevout proven absent and propagated to descendants, independent salvage, confirmed/duplicate/policy/expired/evicted classes, exact expiry cutoff, and final capacity trimming.</criterion>
    <criterion>A transaction initially admitted but later trimmed is `DroppedEvicted`, never `Recovered`.</criterion>
    <criterion>Legacy unknown time is never substituted with startup time; known acceptance age survives exactly.</criterion>
    <criterion>Rolling rate, block gate, and decay-time fields match `Mempool::new` after staging, and cleared/historical local metadata cannot resurrect unbroadcast membership.</criterion>
    <criterion>`rg -n 'DroppedExpired|dropped_expired_count' packages/open-bitcoin-node/src/storage/mempool_snapshot.rs packages/open-bitcoin-node/src/network/recovery.rs packages/open-bitcoin-node/src/network/tests/recovery_cases.rs` finds the variant, every exhaustive summary/count consumer, and an independent count assertion in this plan.</criterion>
  </acceptance-criteria>
  <done>One opaque staged result contains complete final membership and typed truth without exposing partial recovery or historical pressure.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Validated envelope to dependency graph | Adversarial topology can amplify work or obscure missing parents. |
| Staged admission to final membership | Transient admission must not be reported as durable recovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-135-02-01 | Denial of Service | Dependency graph | mitigate | Bound vertices/edges, use deterministic O((V+E) log V) collections, and stop component-local cycles. |
| T-135-02-02 | Tampering | Stored identities/fees | mitigate | Derive identity, fee, vsize, and topology exclusively from canonical transactions/current policy. |
| T-135-02-03 | Elevation of Privilege | Unbroadcast restoration | mitigate | Intersect explicit persisted membership with final eligible survivors; legacy restores none. |
| T-135-02-04 | Repudiation | Recovery outcomes | mitigate | Derive recovered/evicted only from final membership and retain stable exhaustive classes. |
| T-135-02-05 | Tampering | Rolling fee restart | mitigate | Final staged pool must match every rolling field of a fresh pool, not only numeric floor zero. |
</threat-model>

<verification>
Run the timed recovery suite and breadcrumb checker; inspect that the new modules are pure and contain no filesystem, mutex, clock, or network I/O imports.
</verification>

<success-criteria>
- D-02 through D-04 and D-09 through D-14 are fully represented in pure recovery behavior.
- MPDUR-02 and MPDUR-03 have deterministic, final-membership-based proof.
- The staged result is consumable once and no live runtime state changes during preparation.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-02-SUMMARY.md`.
</output>
