---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "11"
type: execute
wave: 11
depends_on: ["135-10"]
gap_closure: true
files_modified:
  - scripts/check-phase135-snapshot-recovery.ts
  - scripts/check-phase135-snapshot-recovery.test.ts
  - scripts/check-phase135-snapshot-recovery/persisted-input.ts
  - scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts
  - docs/parity/catalog/mempool-policy.md
  - docs/parity/checklist.md
  - docs/parity/index.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [MPDUR-02]
must_haves:
  truths:
    - "The stable Phase 135 checker entrypoints delegate persisted-input contract checking and mutation fixtures to responsibility-focused child modules, and every touched TypeScript file is at most 628 physical lines."
    - "The live checker requires the four globally coexisting encoded dimensions, the individually derived aggregate/per-record edge ceilings, policy-independent load/topology construction, current-policy capture/final membership, and final DroppedEvicted rewrite without claiming seven maxima coexist."
    - "Mutations that recouple load or topology to current live policy, restore 50,000/1,600,000 literals, remove checked aggregate/per-record input derivation, bypass transaction-byte cross-field counters, or bypass final DroppedEvicted classification each fail with the intended stable diagnostic."
    - "Parity catalog/checklist/index describe the corrected two-boundary model while retaining Phase 135 in_progress, MPDUR rows Pending, WR-01 open/non-blocking, and all Phase 136-138/public/readiness exclusions."
    - "Tracked LOC is freshly generated, focused aggregate checks pass, and immutable 135-11-SUMMARY.md is the last executor-owned artifact."
    - "Only the independent verifier may create the final lifecycle-valid candidate report and run the exact phase135-11-full-verify transaction; failure or tree change immediately rewrites gaps_found, while zero exit on the unchanged tree permits no later edit."
  artifacts:
    - path: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      provides: "Production structural checks for persisted-input versus live-policy responsibility"
      contains: "checkPersistedInputContract"
    - path: scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts
      provides: "Focused recoupling/fixed-cap/edge/counter/final-membership mutation fixtures"
      contains: "persistedInputMutations"
    - path: scripts/check-phase135-snapshot-recovery.ts
      provides: "Stable live checker entrypoint and Phase 135 orchestration"
      contains: "checkPhase135SnapshotRecovery"
    - path: scripts/check-phase135-snapshot-recovery.test.ts
      provides: "Stable mutation-suite entrypoint with delegated persisted-input cases"
      contains: "persistedInputMutations"
    - path: docs/parity/catalog/mempool-policy.md
      provides: "Auditable two-boundary parity narrative with bounded claims"
      contains: "persisted"
    - path: docs/metrics/lines-of-code.md
      provides: "Fresh repository-generated LOC evidence"
    - path: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md
      provides: "Immutable final executor evidence for independent verification"
  key_links:
    - from: scripts/check-phase135-snapshot-recovery.ts
      to: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      via: "stable root orchestration calls the focused production persisted-input contract checker"
      pattern: "checkPersistedInputContract"
    - from: scripts/check-phase135-snapshot-recovery.test.ts
      to: scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts
      via: "stable root test entrypoint registers the focused persisted-input mutation matrix"
      pattern: "persistedInputMutations"
    - from: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      via: "exact anchors prove checked global encoded dimensions and individual edge derivation"
      pattern: "MempoolSnapshotPersistedInputLimits"
    - from: scripts/check-phase135-snapshot-recovery/persisted-input.ts
      to: packages/open-bitcoin-node/src/network/recovery/staging.rs
      via: "checker proves format-bounded topology precedes fresh current-policy replay and final DroppedEvicted classification"
      pattern: "DroppedEvicted"
    - from: docs/parity/catalog/mempool-policy.md
      to: docs/parity/checklist.md
      via: "both preserve the same in-progress two-boundary MPDUR-02 claim"
      pattern: "in progress|Pending"
    - from: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md
      to: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      via: "the independent verifier creates a fresher candidate report only after the immutable final summary"
      pattern: "phase_lifecycle_id: 135-2026-08-02T17-41-48"
    - from: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      to: scripts/verify.sh
      via: "the candidate names the exact final command and becomes truthful only after zero exit on the unchanged tree"
      pattern: "phase135-11-full-verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-04T13:16:43.243Z
---

<objective>
Close Phase 135 evidence after the persisted-input repair by modularizing the oversized checker/mutation responsibilities, reconciling parity/LOC artifacts, and reserving the final default verifier for one independent post-summary transaction.

Purpose: Make the corrected MPDUR-02 boundary durable against structural regression without exceeding managed TypeScript file-shape limits or allowing executor-owned verification claims.
Output: Responsibility-focused checker modules, complete mutation coverage, reconciled pending parity evidence, fresh LOC, immutable Plan 11 summary, and an exact independent verifier protocol.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md
@scripts/check-phase135-snapshot-recovery.ts
@scripts/check-phase135-snapshot-recovery.test.ts
@scripts/check-phase135-snapshot-recovery/source.ts

<interfaces>
Plan 10 provides these live anchors:
```rust
pub(crate) struct MempoolSnapshotPersistedInputLimits {
    pub(crate) max_encoded_bytes: usize,
    pub(crate) max_records: usize,
    pub(crate) max_unbroadcast_members: usize,
    pub(crate) max_transaction_bytes: usize,
    pub(crate) max_total_transaction_bytes: usize,
    pub(crate) max_input_edges: usize,
    pub(crate) max_input_edges_per_record: usize,
}

pub(crate) fn persisted_mempool_input_limits()
    -> Option<MempoolSnapshotPersistedInputLimits>;

pub fn MempoolSnapshotDecodeLimits::for_persisted_input()
    -> Result<MempoolSnapshotDecodeLimits, SyncRecoveryCategory>;

pub(crate) fn RecoveryTopologyLimits::for_persisted_input()
    -> Result<RecoveryTopologyLimits, MempoolSnapshotError>;
```

The stable TypeScript entrypoints remain:
```typescript
export const PHASE135_TARGET_FILES: readonly string[];
export const PHASE135_DIAGNOSTICS: Readonly<Record<string, string>>;
export function checkPhase135SnapshotRecovery(
  maybeRepoRoot?: string,
): string[];
```

Create these focused child contracts without changing the root executable/test paths:
```typescript
export type PersistedInputSources = {
  codec: string;
  codecDecode: string;
  capture: string;
  store: string;
  topology: string;
  staging: string;
};

export function checkPersistedInputContract(
  sources: PersistedInputSources,
  failures: string[],
  diagnostics: { bounds: string; topology: string; staging: string },
): void;

export function persistedInputMutations(/* existing path/helper contract */):
  readonly Phase135Mutation[];
```

The production checker must encode this distinction exactly:

- Global encoded dimensions that coexist: 268,435,456 encoded bytes; 220,096 records; 5,000 unbroadcast members; 67,108,864 aggregate transaction bytes.
- Individual allocation ceilings inside the transaction-byte budgets: 4,194,304 per transaction; 1,636,801 aggregate edges; 102,300 per-record edges. Do not require, document, or mutate a seven-field simultaneous maximum tuple.
</interfaces>
</context>

<project-guidance>
This plan is informed by repo-local `AGENTS.md`, `AGENTS.bright-builds.md`, the empty concrete override table, and architecture/code-shape/testing/verification/TypeScript standards. Use Bun and TypeScript only; add no Python or dependency. Keep root CLI/test entrypoints stable, use plain functions/composition, and split by responsibility so every touched TypeScript file is at most 628 physical lines. New or modified unit tests cover one concern and visibly use Arrange/Act/Assert sections unless truly trivial. Generate LOC only through the repository-owned Bun script. Preserve canonical planning/report/config artifacts and Plans/Summaries 135-01 through 135-10; only the independent verifier may touch canonical `135-VERIFICATION.md`. WR-01 stays non-blocking/deferred.
</project-guidance>

<decision-coverage>
D-04/D-09/D-10/D-11 are fully guarded by the production checker and mutation matrix; D-14/D-15 are fully covered by responsibility-based checker modules, focused tests, parity/LOC reconciliation, and independent verification. All other D-01 through D-13 behavior remains implemented and regression-guarded without redesign. No decision is partial, and no deferred Phase 136-138/public/readiness feature appears.
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the production persisted-input checker responsibility</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md</file>
    <file>scripts/check-phase135-snapshot-recovery.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/source.ts</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/transaction.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/topology.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/staging.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery.ts, scripts/check-phase135-snapshot-recovery/persisted-input.ts</files>
  <behavior>
    - The child checker requires the concrete global encoded tuple and separately requires both checked edge divisions/one-over relationships without treating edge ceilings as extra encoded allocation.
    - Store/topology must use for_persisted_input with no PolicyConfig or MempoolCapacityBounds dependency; capture must still use current MempoolCapacityBounds.
    - Staging must prepare format-bounded topology before fresh Mempool::new(config), intersect final membership, and preserve admitted-but-absent DroppedEvicted classification.
    - Root CLI output, exported entrypoint, diagnostics, target-file discovery, deterministic filesystem-only behavior, and unrelated Phase 135 checks remain stable.
  </behavior>
  <action>
    Create `scripts/check-phase135-snapshot-recovery/persisted-input.ts` and move the complete production responsibility for codec/load/topology/staging persisted-input checks out of the 625-line root checker. Export the concrete `PersistedInputSources` type and `checkPersistedInputContract(...)` contract shown above. Use existing `body`, `hasAll`, and `addFailure` helpers from `source.ts`; do not introduce a parser/dependency or duplicate general checker orchestration.

    Require exact structural anchors for: checked derivation of 220,096 records from encoded/aggregate/unbroadcast/envelope/record charges; 41-byte minimum input width; checked aggregate/per-record divisions producing 1,636,801 and 102,300; encoded-size exact/one-over proofs; streaming per-record and aggregate transaction-byte counters; `MempoolSnapshotDecodeLimits::for_persisted_input`; `RecoveryTopologyLimits::for_persisted_input`; current live capture through `MempoolCapacityBounds::from_capacity(mempool.config().mempool_capacity)`; checked topology edge accumulation; fresh current-policy replay; and final DroppedEvicted rewrite. Explicitly reject `from_policy`, `50_000`, `1_600_000`, `usize::MAX`, saturation, and serialized-transaction-length capacity derivation on production persisted-input paths. Do not reject the named 50,001 test fixture or legitimate test-only `usize::MAX` current-policy cases.

    Keep `scripts/check-phase135-snapshot-recovery.ts` as the stable executable/export owner. Add the new module to the checker source inventory/determinism corpus, assemble its six source strings, call `checkPersistedInputContract`, and remove the superseded root bounds/topology/staging block so responsibilities are not duplicated. Preserve every unrelated diagnostic/check. Run the live checker at this task boundary. Every added TypeScript unit test in later Task 2 must be single-concern with Arrange/Act/Assert.
  </action>
  <verify>
    <automated>bun run scripts/check-phase135-snapshot-recovery.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The stable live checker exits zero and still exports `PHASE135_TARGET_FILES`, `PHASE135_DIAGNOSTICS`, and `checkPhase135SnapshotRecovery`.</criterion>
    <criterion>`persisted-input.ts` owns the complete corrected bounds/topology/staging responsibility; the root contains exactly one call to `checkPersistedInputContract` and no duplicate old block.</criterion>
    <criterion>The child checker text explicitly distinguishes global encoded dimensions from individual transaction/edge ceilings and contains no assertion that seven maxima coexist.</criterion>
    <criterion>`wc -l scripts/check-phase135-snapshot-recovery.ts scripts/check-phase135-snapshot-recovery/persisted-input.ts` reports each file at or below 628 physical lines.</criterion>
  </acceptance-criteria>
  <done>The production checker has one focused persisted-input module and a stable compact root entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract mutation fixtures and reconcile pending parity evidence</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md</file>
    <file>scripts/check-phase135-snapshot-recovery.test.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/persisted-input.ts</file>
    <file>docs/parity/catalog/mempool-policy.md</file>
    <file>docs/parity/checklist.md</file>
    <file>docs/parity/index.json</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery.test.ts, scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts, docs/parity/catalog/mempool-policy.md, docs/parity/checklist.md, docs/parity/index.json</files>
  <behavior>
    - Separate mutations recouple store decoding to max_live_entries, recouple topology vertices/edges to current policy, restore 50,000, restore 1,600,000, remove aggregate edge division, remove per-record edge division, bypass streaming aggregate/per-record transaction-byte counters, and bypass final DroppedEvicted rewrite.
    - Each new mutation produces exactly one intended bounds, topology, or staging diagnostic; the live corpus and all prior mutation/claim cases remain green.
    - Root mutation entrypoint remains stable and compact by delegating the persisted-input mutation matrix to a focused child fixture module.
    - Parity prose describes format-bounded load/topology followed by current-policy replay/final membership, while status remains in_progress/Pending and WR-01 remains open.
  </behavior>
  <action>
    Create `scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts`. Move the existing bounds/topology/staging mutation responsibility out of the 628-line root test and add the eight concrete mutation families listed in `<behavior>`, including separate aggregate and per-record derivation/counter cases. Export a typed `persistedInputMutations(...)` collection that the stable root test combines with its remaining schema/compatibility/authority/checkpoint/parity/claim mutations. Keep fixture creation/cleanup and generic assertion helpers in the root; do not duplicate temp-tree orchestration. Every unit test/mutation case must cover one concern and visibly follow Arrange/Act/Assert through the shared assertion helper.

    Require exact diagnostics rather than mere containment for the new cases: current-policy store coupling/fixed record cap/streaming counter bypass → `PHASE135_DIAGNOSTICS.bounds`; current-policy topology/fixed edge cap/edge derivation removal → topology or bounds as assigned by the production module; final DroppedEvicted bypass → `PHASE135_DIAGNOSTICS.staging`. Retain all existing mutation cases and WR-01's current non-blocking status; do not implement cfg parsing or add a cfg mutation.

    Reconcile only the existing Phase 135 text/row/rationale in `docs/parity/catalog/mempool-policy.md`, `docs/parity/checklist.md`, and `docs/parity/index.json`. State that format-derived candidate bounds govern persisted load/topology and current policy governs live capture plus replay/final membership. Preserve top-level and checklist status `in_progress`, all MPDUR rows `Pending`, all negative scope claims, Knots anchors, and every Phase 136-138/public/default relay/repair/readiness exclusion. Do not edit REQUIREMENTS, ROADMAP, source breadcrumbs, or canonical verification.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts &amp;&amp; bun run scripts/check-phase135-snapshot-recovery.ts &amp;&amp; jq empty docs/parity/index.json</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Every named new mutation fails with exactly one intended diagnostic, every pre-existing mutation/claim remains green, and the unmodified live corpus passes.</criterion>
    <criterion>`wc -l scripts/check-phase135-snapshot-recovery.ts scripts/check-phase135-snapshot-recovery.test.ts scripts/check-phase135-snapshot-recovery/source.ts scripts/check-phase135-snapshot-recovery/persisted-input.ts scripts/check-phase135-snapshot-recovery/persisted-input-mutations.ts` reports every touched/listed TypeScript file at or below 628 physical lines.</criterion>
    <criterion>The root checker/test entrypoints remain executable at their original paths and each delegates exactly one persisted-input responsibility.</criterion>
    <criterion>Catalog, checklist, and index agree on the two-boundary model and retain in_progress/Pending status, WR-01 open, and all deferred-scope exclusions.</criterion>
  </acceptance-criteria>
  <done>The modular mutation suite guards every repaired boundary and parity evidence remains truthful and pending.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh LOC, run aggregate focused gates, and publish immutable Plan 11 evidence</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-PLAN.md</file>
    <file>scripts/generate-loc-report.ts</file>
    <file>scripts/check-parity-breadcrumbs.ts</file>
    <file>scripts/bright-builds-check.ts</file>
    <file>scripts/verify.sh</file>
  </read-first>
  <files>docs/metrics/lines-of-code.md, .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md</files>
  <action>
    Run the mutation suite and live checker, focused Plan 10 Rust regression commands, parity breadcrumb checker, `jq empty`, Bright Builds all, TypeScript physical-line audit, and `git diff --check`. Generate `docs/metrics/lines-of-code.md` only with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`, then prove freshness with the same command plus `--check`; rerun checker/Bright Builds/diff checks after generation. Audit the final executor tree against the eight-file Plan 11 inventory plus new summary and verify protected prior plans/summaries/canonical planning/report/config artifacts are hash-identical. Perform the simplification review: stable root entrypoints, one production child responsibility, one mutation-fixture child responsibility, no duplicated arithmetic/check logic, no Python/dependency, and every touched TypeScript file at most 628 lines.

    Create immutable `135-11-SUMMARY.md` only after all executor-owned commands pass. Record checker module responsibilities and line counts, exact mutation results, parity pending status, LOC generation/freshness, focused Rust results inherited/re-run from Plan 10, breadcrumb/Bright Builds/diff/scope/hash results, WR-01 non-blocking/deferred, and the fact that neither the default verifier nor canonical Phase 135 verification has been run/edited by the executor. Make no executor-owned repository edit after the summary commit. Do not run `bash scripts/verify.sh`; the independent verifier protocol below owns the only full run.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts &amp;&amp; bun run scripts/check-phase135-snapshot-recovery.ts &amp;&amp; bun run scripts/check-parity-breadcrumbs.ts --check &amp;&amp; jq empty docs/parity/index.json &amp;&amp; bun run scripts/bright-builds-check.ts all &amp;&amp; bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Mutation/live checker, focused Rust regressions, parity breadcrumbs/JSON, Bright Builds, LOC freshness, line-count audit, and `git diff --check` pass on the final executor tree.</criterion>
    <criterion>The final scope/hash audit reports exactly eight declared checker/parity/LOC files plus `135-11-SUMMARY.md`; protected artifacts are unchanged.</criterion>
    <criterion>Every touched TypeScript file is at most 628 physical lines and the summary records the exact `wc -l` results.</criterion>
    <criterion>The summary is created last, retains WR-01 and pending parity truth, and contains no claim that full/default/canonical verification already passed.</criterion>
  </acceptance-criteria>
  <done>The final executor tree has compact checker evidence, fresh parity/LOC artifacts, and an immutable summary ready for independent transactional verification.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1. Any unresolved High severity threat blocks completion and must yield gaps; it may not be accepted or deferred.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime source to structural checker | Textual anchors must prove the corrected global/individual budget separation and control-flow wiring rather than identifier presence. |
| Mutation fixtures to checker confidence | Each removed/recoupled invariant must fail with a stable exact diagnostic without false passes or collateral failures. |
| Source truth to parity/LOC evidence | Pending evidence must remain fresh and must not promote requirements or broaden public/readiness claims. |
| Immutable summary to canonical verifier report | Only an independent post-summary transaction may attest the exact default verifier on an unchanged tree. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| T-135-11-01 | High | Spoofing | Production structural checker | mitigate | Require exact formula, constructor, counter, topology, replay, and final-membership anchors in a focused module. | Live checker plus one-concern mutation cases. |
| T-135-11-02 | High | Tampering | Persisted/live boundary regression | mitigate | Reject renewed current-policy decode/topology coupling, fixed caps, removed edge derivation/counters, and final DroppedEvicted bypass. | Eight exact diagnostic mutation families. |
| T-135-11-03 | Medium | Denial of Service | Oversized checker sources | mitigate | Split production and mutation responsibilities, preserve stable roots, and fail if any touched TypeScript file exceeds 628 lines. | Explicit wc/Bright Builds acceptance gates. |
| T-135-11-04 | High | Repudiation | Canonical verification attestation | mitigate | Candidate report precedes the exact final command; failure/tree change forces immediate gaps_found rewrite; success permits no later edit. | Independent phase135-11-full-verify protocol. |
| T-135-11-05 | Medium | Information Disclosure | Checker/parity/report output | mitigate | Keep fixed diagnostics and aggregate evidence; never include raw snapshot bytes, transactions, identities, or secrets. | Existing redaction/claim guards plus live checker. |
| T-135-11-06 | Medium | Elevation of Privilege | Requirement/claim promotion | mitigate | Keep Phase 135 in_progress and MPDUR Pending until independent verification; preserve negative public/readiness boundaries. | Mutation tests and parity/index assertions. |

## Residual Risks

- The dependency-free checker remains a narrow structural proof rather than a Rust parser. WR-01 remains explicit and non-blocking because the live preflight is unconditional; cfg hardening is not falsely claimed here.
- Full repository verification is intentionally deferred past the immutable summary, so executor-focused success cannot itself promote Phase 135.
</threat-model>

<verification>
Executor-owned verification ends after Task 3. The executor must not run `bash scripts/verify.sh` and must not create, edit, restore, replace, stage, or forge canonical `135-VERIFICATION.md`.

The following phase-level transaction belongs exclusively to the independent standard `gsd-verifier` spawned by `/gsd-execute-phase` after `135-11-SUMMARY.md` exists:

1. Independently verify all Phase 135 source, behavior, artifact, key-link, threat, requirement, lifecycle, scope, checker-line-count, and protected-hash truths against the final post-summary tree. Explicitly adjudicate both original Plan 10 gaps and retain WR-01 as non-blocking unless live source truth changed.
2. If any source truth, lifecycle invariant, file-shape gate, or unresolved High threat fails, immediately write a fresh lifecycle-valid canonical `135-VERIFICATION.md` with `status: gaps_found`, name the failed truth/threat, and return non-passed. Do not run the default verifier and do not promote the phase.
3. If all source truths pass, write a lifecycle-valid candidate canonical `135-VERIFICATION.md` on the post-summary tree with `status: passed`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 135-2026-08-02T17-41-48`, and `generated_at` later than `135-11-SUMMARY.md`. Before execution, the candidate must name exactly `bun run scripts/command-timings.ts run --key phase135-11-full-verify -- bash scripts/verify.sh`, record `exit_status: 0` and `result: passed` only as a conditional transactional attestation, and state that passed is valid solely if the immediately following command exits zero on the unchanged candidate-report tree.
4. Without any intervening repository edit, run exactly `bun run scripts/command-timings.ts run --key phase135-11-full-verify -- bash scripts/verify.sh` as the final command. Do not use `--fast`, do not overlap Cargo jobs, distinguish quiet polling from timeout, and poll a yielded session at least every 60 seconds with liveness evidence before any termination decision.
5. If the command fails or the repository tree changes, immediately rewrite canonical `135-VERIFICATION.md` to lifecycle-valid `status: gaps_found`, record the exact command, nonzero/freshness failure, and affected truth, then return non-passed. Do not complete or promote the phase.
6. If the command exits zero and the tree is unchanged, make no further repository edit and return `passed`; the candidate report's conditional attestation is now truthful for the exact validated tree.

Standard `/gsd-execute-phase` may update roadmap/requirements only after `gsd-verifier` returns `passed`. A gap, stale lifecycle provenance, unresolved High threat, command failure, or post-command edit stops completion through the existing standard path.
</verification>

<success-criteria>
- MPDUR-02's corrected persisted-input/current-policy boundary is guarded by compact production and mutation modules with stable entrypoints.
- All renewed-coupling/fixed-cap/edge/counter/final-membership mutations fail exactly, while the live corpus and prior cases pass.
- Every touched TypeScript file is at most 628 physical lines; no Python, dependency, broad refactor, or unrelated documentation is added.
- Parity remains in_progress/Pending with WR-01 open and deferred Phase 136-138/public/readiness boundaries intact.
- LOC and focused aggregate evidence are fresh, protected hashes remain unchanged, and immutable 135-11-SUMMARY.md is the final executor artifact.
- Independent lifecycle-valid verification runs the exact phase135-11-full-verify command last and reports passed only after zero exit on the unchanged candidate-report tree.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-11-SUMMARY.md`.
</output>
