---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "10"
type: execute
wave: 10
depends_on: ["135-09"]
gap_closure: true
files_modified:
  - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/load_limits.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs
  - packages/open-bitcoin-node/src/network/recovery/topology.rs
  - packages/open-bitcoin-node/src/network/recovery/staging.rs
  - packages/open-bitcoin-rpc/src/context/mempool_recovery.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/checkpoint.rs
autonomous: true
requirements: [MPDUR-02]
must_haves:
  truths:
    - "Persisted snapshot input is bounded by a finite policy-independent format contract before allocation, while current MempoolCapacityBounds continues to govern only live capture and final current-policy membership."
    - "The globally coexisting encoded tuple is the 268,435,456-byte cap, 220,096 records, 5,000 unbroadcast members, and 67,108,864 aggregate transaction bytes; the 4,194,304 per-transaction byte ceiling and both edge ceilings are individual allocation ceilings within those transaction-byte budgets, not additional simultaneously maximized dimensions."
    - "A 41-byte minimum serialized non-witness input yields checked ceilings of 1,636,801 aggregate input edges and 102,300 input edges per record, and one-over proofs fail the corresponding aggregate or per-record transaction-byte budget."
    - "Zero-current-capacity/one-candidate and one-record-current-capacity/two-candidate recovery reach current-policy replay and classify every final non-survivor as DroppedEvicted rather than failing topology preparation."
    - "The 50,001-record Sync/reopen regression remains accepted by persisted-input bounds, and the near-capacity Fjall regression derives live capacity from accounted_memory_for_entry for the exact canonical entry."
    - "Every node and RPC constructor/caller compiles without from_policy, without a compatibility wrapper, without a magic record/edge cap, and without persisting or inferring historical policy."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      provides: "Checked persisted-input format contract and encoded/edge boundary proofs"
      contains: "MempoolSnapshotPersistedInputLimits"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Policy-independent bounded Fjall startup decode limits"
      contains: "for_persisted_input"
    - path: packages/open-bitcoin-node/src/network/recovery/topology.rs
      provides: "Format-bounded candidate topology distinct from live membership"
      contains: "for_persisted_input"
    - path: packages/open-bitcoin-node/src/network/recovery/staging.rs
      provides: "Bounded topology followed by current-policy replay and final membership classification"
      contains: "MempoolRecoveryStatus::DroppedEvicted"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/load_limits.rs
      provides: "Accounted-memory-derived near-capacity Fjall round trip"
      contains: "accounted_memory_for_entry"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs
      provides: "50,001-record persisted-input regression"
      contains: "FORMER_RECORD_LIMIT_PLUS_ONE"
    - path: packages/open-bitcoin-rpc/src/context/mempool_recovery.rs
      provides: "Policy-independent decode followed by separately retained current-policy staging"
      contains: "MempoolSnapshotDecodeLimits::for_persisted_input"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      to: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      via: "for_persisted_input maps the one checked format contract into the existing five streaming decoder counters"
      pattern: "persisted_mempool_input_limits|for_persisted_input"
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      to: packages/open-bitcoin-node/src/network/recovery/topology.rs
      via: "the same max_records and derived aggregate/per-record edge ceilings bound graph allocation"
      pattern: "MempoolSnapshotPersistedInputLimits"
    - from: packages/open-bitcoin-node/src/network/recovery/topology.rs
      to: packages/open-bitcoin-node/src/network/recovery/staging.rs
      via: "format-bounded topology completes before a fresh Mempool::new(config) applies current chainstate and policy"
      pattern: "prepare_recovery_topology"
    - from: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/load_limits.rs
      to: packages/open-bitcoin-mempool/src/resource.rs
      via: "the exact fixture entry is priced by the authoritative version-1 memory ledger"
      pattern: "accounted_memory_for_entry"
    - from: packages/open-bitcoin-rpc/src/context/mempool_recovery.rs
      to: packages/open-bitcoin-node/src/network/recovery/staging.rs
      via: "startup loads with format-owned limits, then supplies current policy only to prepare_mempool_recovery_at"
      pattern: "load.*decode_limits|prepare_mempool_recovery_at"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-04T13:16:43.243Z
---

<objective>
Repair MPDUR-02 by separating persisted-input allocation ceilings from current-policy live membership and migrating every compiling node/RPC caller to the corrected contract.

Purpose: Let a finite, valid snapshot captured under a larger supported policy reach replay under a smaller current policy, where final membership and typed DroppedEvicted outcomes belong.
Output: One checked format-derived input contract, complete constructor/caller migration, repaired zero/trim/accounted-capacity regressions, and focused compiling Rust evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-09-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md

<interfaces>
From `packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs`:
```rust
const MAX_MEMPOOL_SNAPSHOT_ENCODED_BYTES: usize = 256 * 1024 * 1024;
const MAX_MEMPOOL_SNAPSHOT_TRANSACTION_BYTES: usize = 4 * 1024 * 1024;
const MAX_MEMPOOL_SNAPSHOT_TOTAL_TRANSACTION_BYTES: usize = 64 * 1024 * 1024;
const ENCODED_ENVELOPE_OVERHEAD_BYTES: usize = 1_048_576;
const ENCODED_RECORD_OVERHEAD_BYTES: usize = 512;
const ENCODED_UNBROADCAST_MEMBER_OVERHEAD_BYTES: usize = 4_096;
const HEX_CHARS_PER_TRANSACTION_BYTE: usize = 2;

pub(crate) fn encoded_size_upper_bound(
    max_total_transaction_bytes: usize,
    max_records: usize,
    max_unbroadcast_members: usize,
) -> Option<usize>;
```

From `packages/open-bitcoin-primitives/src/transaction.rs` and the canonical transaction codec:
```rust
impl OutPoint {
    pub const SERIALIZED_LEN: usize = Hash32::LEN + core::mem::size_of::<u32>();
}
```
One minimally encoded non-witness input consumes exactly `OutPoint::SERIALIZED_LEN + 1 + size_of::<u32>() = 41` transaction bytes: 36-byte outpoint, one-byte empty-script CompactSize, and four-byte sequence. Witness/script bytes can only increase the transaction-byte counters.

The checked constants therefore establish these distinct contracts:

- Globally coexisting encoded dimensions: `max_encoded_bytes = 268_435_456`, `max_records = (268_435_456 - 67_108_864 * 2 - 5_000 * 4_096 - 1_048_576) / 512 = 220_096`, `max_unbroadcast_members = 5_000`, and `max_total_transaction_bytes = 67_108_864`. Their `encoded_size_upper_bound` is exactly `268_435_456`; record `220_097` yields `268_435_968` and does not fit.
- Individual allocation ceilings embedded within transaction-byte budgets: `max_transaction_bytes = 4_194_304`; `max_input_edges = floor(67_108_864 / 41) = 1_636_801`; and `max_input_edges_per_record = floor(4_194_304 / 41) = 102_300`. Record and edge maxima do not consume separate budgets and MUST NOT be described or tested as a seven-field simultaneous maximum tuple.

From `packages/open-bitcoin-mempool/src/resource.rs`:
```rust
pub struct MempoolCapacityBounds { /* live capture/membership only */ }
pub fn accounted_memory_for_entry(
    entry: &MempoolEntry,
) -> Result<AccountedMempoolMemory, ResourceAccountingError>;
```
Keep `MempoolCapacityBounds::from_capacity(mempool.config().mempool_capacity)` on live capture. Remove current policy from persisted decode/topology construction. Current policy remains mandatory in staged admission, expiry, trimming, final membership, and outcome classification.
</interfaces>
</context>

<project-guidance>
This plan is informed by repo-local `AGENTS.md`, `AGENTS.bright-builds.md`, the empty concrete override table, and the architecture/code-shape/testing/verification/Rust standards. Use functional-core checked arithmetic, keep effects in existing shells, use no new dependency, run Cargo sequentially through `scripts/command-timings.ts`, and keep every new or modified Rust unit test focused on one behavior with visible `// Arrange`, `// Act`, and `// Assert` sections unless the test is truly trivial. No new Rust file is planned, so do not edit `docs/parity/source-breadcrumbs.json`; run its checker in Plan 11. Preserve Plans/Summaries 135-01 through 135-09 and every canonical planning/report/config artifact byte-for-byte. WR-01 remains non-blocking and deferred.
</project-guidance>

<scope-rationale>
The checker’s `scope_sanity` warning is intentionally accepted because this plan is one cohesive atomic workspace-contract migration, not eleven independent source responsibilities. The ten declared implementation/test files are the minimum complete constructor, caller, and regression-test migration: eight node files define and consume the corrected boundary, and both RPC files are required callers of that same removed constructor. `135-10-SUMMARY.md` is the only eleventh artifact and is immutable execution evidence, not an additional source responsibility.

Splitting either RPC caller into a later wave would leave the workspace noncompiling at the Plan 10 boundary and fail the required pre-commit build contract. Retaining `from_policy` as a temporary compatibility wrapper would avoid that intermediate failure only by violating the verified gap contract. The plan therefore remains one three-task cohesive unit, with eleven total artifacts including its summary, below the checker’s 15-file blocker threshold.

This accepted warning is an exhaustive scope contract, not permission to grow the plan. The executor must not expand the inventory: before creating the summary, the final scope audit must prove exactly the ten frontmatter implementation/test paths changed or created; after the summary is created last, the complete Plan 10 inventory must be exactly those ten paths plus `135-10-SUMMARY.md` and no more. Any unexpected required file turns this accepted warning into a stop/replan condition before that file is edited.
</scope-rationale>

<decision-coverage>
D-04 is fully covered in Task 1 by finite fail-closed persisted-input decoding; D-09/D-10/D-11 are fully covered in Tasks 1-2 by bounded topology followed by current-policy final membership; D-14 is fully covered by the focused regressions and compiling gates. D-01 through D-03, D-05 through D-08, D-12/D-13, and D-15 remain implemented by Plans 135-01 through 135-09 and are preserved without redesign. No decision is partial, and no deferred Phase 136-138/public/readiness surface appears.
</decision-coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define persisted-input limits and migrate every node caller atomically</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>packages/open-bitcoin-mempool/src/resource.rs</file>
    <file>packages/open-bitcoin-primitives/src/transaction.rs</file>
    <file>packages/open-bitcoin-codec/src/transaction.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/transaction.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/load_limits.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/topology.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/staging.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/recovery_cases.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/snapshot_codec.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs, packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/load_limits.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs, packages/open-bitcoin-node/src/network/recovery/topology.rs, packages/open-bitcoin-node/src/network/recovery/staging.rs</files>
  <behavior>
    - One test proves the four globally coexisting encoded dimensions fit exactly and record 220,097 is one-over; it does not claim the individual transaction/edge ceilings coexist as extra allocated dimensions.
    - Separate checked-division tests prove `1_636_801 * 41 <= 67_108_864 < 1_636_802 * 41` and `102_300 * 41 <= 4_194_304 < 102_301 * 41`.
    - Streaming tests independently prove one record cannot exceed the per-record transaction-byte counter and multiple records cannot exceed the aggregate transaction-byte counter even when record count is below 220,096; topology edge tests prove those same byte-derived ceilings fail closed through checked counters.
    - The 50,001-record Sync/reopen regression uses `for_persisted_input`, asserts `50_001 <= 220_096`, and remains below the encoded/aggregate byte counters.
    - Zero-capacity/one-candidate and two-candidate/one-current-survivor regressions reach replay and produce exact DroppedEvicted outcomes.
    - The near-capacity Fjall test prices its exact canonical `MempoolEntry` with accounted_memory_for_entry and does not derive policy capacity from serialized transaction length.
  </behavior>
  <action>
    Start RED with the two existing recovery failures and focused formula/cross-field tests. In `snapshot_codec/mempool.rs`, add crate-private `MempoolSnapshotPersistedInputLimits` with exactly `max_encoded_bytes`, `max_records`, `max_unbroadcast_members`, `max_transaction_bytes`, `max_total_transaction_bytes`, `max_input_edges`, and `max_input_edges_per_record`, plus `pub(crate) fn persisted_mempool_input_limits() -&gt; Option&lt;MempoolSnapshotPersistedInputLimits&gt;`. Derive every value through checked subtraction, multiplication, addition, and division from the live constants and `OutPoint::SERIALIZED_LEN + 1 + size_of::&lt;u32&gt;()`; prove the concrete values listed in `<interfaces>`. Re-export the crate-private type/factory through `snapshot_codec.rs`. Keep the encoder, raw-key preflight, streaming record/unbroadcast/per-transaction/aggregate counters, canonical identity validation, and original-byte retention intact. A test-only default may delegate to this one factory with an impossibility-explaining `expect`; do not duplicate arithmetic.

    Replace public Fjall `MempoolSnapshotDecodeLimits::from_policy(&amp;PolicyConfig)` with `MempoolSnapshotDecodeLimits::for_persisted_input() -&gt; Result&lt;Self, SyncRecoveryCategory&gt;`, mapping the shared five decode fields and checked-derivation failure to `ResourceExhaustion`. Replace `RecoveryTopologyLimits::from_policy(&amp;PolicyConfig)` with `RecoveryTopologyLimits::for_persisted_input() -&gt; Result&lt;Self, MempoolSnapshotError&gt;`, mapping records plus both edge ceilings. Update staging to obtain the topology limit before allocation and then retain the unchanged fresh `Mempool::new(config)` replay/final-membership path.

    In this same atomic task, migrate every open-bitcoin-node call site required for compilation: bounded-load tests inside `fjall_store/mempool.rs`, the near-capacity fixture in `load_limits.rs`, the 50,001 regression in `gap_closure.rs`, topology test helpers, and staging. Do not leave a compatibility wrapper or defer any node `from_policy` call to another task. Rename the Fjall test to `fjall_mempool_snapshot_round_trips_at_accounted_policy_capacity`; construct the exact entry with canonical txid/wtxid, `transaction_weight_and_virtual_size`, canonical metadata, and the existing `MempoolEntry::new`, set `MempoolCapacity::new(accounted_memory_for_entry(&amp;entry).expect("accounted fixture entry").as_usize())`, and assert this capacity is greater than and differs from raw canonical transaction bytes before the round trip.

    Never use `usize::MAX`, saturation, 50,000/1,600,000 literals, current live capacity, serialized transaction length, or fabricated historical policy as a persisted-input ceiling. Keep `MempoolCapacityBounds` unchanged on live capture only. Every new/modified unit test must cover one concern and visibly use Arrange/Act/Assert comments unless truly trivial.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-10-node-compile -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features --no-run &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-zero-capacity-replay -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features managed_recovery_drops_non_accepted_records_from_serving_and_fanout -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-capacity-trim-replay -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features staged_recovery_classifies_later_capacity_trim_from_final_membership -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-accounted-capacity -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features fjall_mempool_snapshot_round_trips_at_accounted_policy_capacity -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-persisted-input-limits -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features persisted_input_limits -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-former-50k -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features former_50_000 -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The complete open-bitcoin-node test target compiles with `--no-run` and every focused repaired regression passes at this task boundary; `rg -n 'MempoolSnapshotDecodeLimits::from_policy|RecoveryTopologyLimits::from_policy' packages/open-bitcoin-node --glob '*.rs'` returns no match.</criterion>
    <criterion>Formula tests assert exactly 220,096 records, 1,636,801 aggregate edges, and 102,300 per-record edges, plus each one-over inequality; prose/tests distinguish the four coexisting encoded dimensions from individual transaction/edge ceilings.</criterion>
    <criterion>Per-record and aggregate transaction-byte one-over fixtures fail through the existing streaming counters, topology uses checked edge accumulation, and no test asserts a seven-field simultaneous maximum tuple.</criterion>
    <criterion>The two named recovery regressions prepare successfully and assert exact final survivor/DroppedEvicted membership; no early topology LifecycleEffect remains.</criterion>
    <criterion>`fjall_mempool_snapshot_round_trips_at_accounted_policy_capacity` calls `accounted_memory_for_entry` for the exact entry and contains no capacity calculation from `encode_transaction(...).len()`.</criterion>
    <criterion>The 50,001-record test loads with `for_persisted_input`, remains below 220,096, and preserves deterministic first/last identities after Sync/reopen.</criterion>
  </acceptance-criteria>
  <done>The complete node crate compiles with one finite format-owned input contract and current policy acts only during live capture/replay/final membership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate RPC startup callers and prove boundary ordering</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/staging.rs</file>
    <file>packages/open-bitcoin-rpc/src/context/mempool_recovery.rs</file>
    <file>packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/checkpoint.rs</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/context/mempool_recovery.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/checkpoint.rs</files>
  <behavior>
    - Startup constructs one finite persisted-input decode contract without PolicyConfig, loads bounded bytes, and only then passes the separately retained current PolicyConfig to prepare_mempool_recovery_at.
    - Default, zero, one-entry, and usize::MAX current capacities do not change the persisted-input decode value; the distinct policies still control final replay membership.
    - Checkpoint tests use the same persisted-input factory and preserve clean-shutdown ordering.
  </behavior>
  <action>
    Migrate every remaining workspace call to `MempoolSnapshotDecodeLimits::for_persisted_input()`, including production startup, context tests, loader closures, and daemon checkpoint tests. Replace `snapshot_decode_limits_are_derived_from_the_runtime_policy` and the obsolete policy-overflow test with focused single-concern tests proving the concrete finite persisted-input value is invariant across default, zero, one-entry, and `usize::MAX` policy capacities. Preserve explicit caller-owned `MempoolSnapshotDecodeLimits::new` only for hostile narrow-limit tests. In production, retain `policy: &amp;PolicyConfig` exclusively for `prepare_mempool_recovery_at` and verify source order is factory → bounded load → current-policy staging → consuming install. Do not generically remap topology/replay errors that must become typed outcomes, add a compatibility constructor, or alter checkpoint/shutdown semantics. Use visible Arrange/Act/Assert comments in every new or modified unit test unless truly trivial.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-10-rpc-contract -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features snapshot_decode_limits -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-rpc-checkpoint -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --bin open-bitcoind checkpoint -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'MempoolSnapshotDecodeLimits::from_policy|RecoveryTopologyLimits::from_policy' packages/open-bitcoin-node packages/open-bitcoin-rpc --glob '*.rs'` returns no match and no compatibility wrapper exists.</criterion>
    <criterion>Production startup visibly calls `for_persisted_input()` before bounded load and passes current policy only to staged replay afterward.</criterion>
    <criterion>Focused tests prove all four current-capacity cases yield the identical concrete finite decode value while preserving separate policy-driven replay behavior.</criterion>
    <criterion>The complete open-bitcoin-rpc test targets compile against the removed constructors and checkpoint tests still prove clean marker ordering.</criterion>
  </acceptance-criteria>
  <done>Every RPC caller compiles against the persisted-input factory while current policy remains authoritative only at replay/final membership.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused Rust gates and publish immutable Plan 10 evidence</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards-overrides.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-PLAN.md</file>
    <file>scripts/command-timings.ts</file>
  </read-first>
  <files>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md</files>
  <action>
    Run `cargo fmt --all --check`, focused clippy, focused all-target/all-feature builds for open-bitcoin-node and open-bitcoin-rpc, the three previously failing regressions by exact name, the persisted-input formula/cross-field tests, the 50,001-record regression, and RPC startup/checkpoint tests sequentially through `scripts/command-timings.ts`; never overlap Cargo jobs. Run `git diff --check` and a scope/hash audit proving only the ten Plan 10 implementation/test files plus new `135-10-SUMMARY.md` differ from the execution baseline, while Plans/Summaries 135-01 through 135-09 and canonical CONTEXT/RESEARCH/REVIEW/VERIFICATION/STATE/ROADMAP/REQUIREMENTS/config are unchanged by this plan. Treat that inventory as exhaustive: if implementation requires any unexpected file, stop before editing it and replan rather than expanding this accepted scope warning. Perform the simplification review: one persisted-input factory owns load/topology candidate ceilings, one live bound owns capture, no duplicated arithmetic/wrapper/magic cap/policy metadata remains.

    Create immutable `135-10-SUMMARY.md` last. Record the corrected global-versus-individual arithmetic distinction, exact ceiling/one-over proofs, all node/RPC caller migrations, zero/trim/accounted-capacity and 50,001 results, timed command keys/results, scope/hash audit, no-new-Rust-file fact, and WR-01 as a non-blocking deferred warning. Do not edit checker/parity/LOC files, do not run `bash scripts/verify.sh`, do not create/edit canonical `135-VERIFICATION.md`, and do not claim Phase 135 passed; those responsibilities belong to Plan 11 and its independent verifier transaction.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-10-fmt -- cargo fmt --manifest-path packages/Cargo.toml --all --check &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-clippy -- cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-targets --all-features -- -D warnings &amp;&amp; bun run scripts/command-timings.ts run --key phase135-10-build -- cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-targets --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Formatting, clippy with warnings denied, focused builds, and every named repaired regression pass sequentially through the cooperative timing wrapper.</criterion>
    <criterion>The exhaustive final scope/hash audit reports exactly the ten frontmatter implementation/test paths plus `135-10-SUMMARY.md` and no more, with all protected planning/report/config/prior-plan artifacts unchanged; any unexpected required path is a stop/replan condition rather than an allowed deviation.</criterion>
    <criterion>The summary is created last, records no full-verifier success, leaves WR-01 open, and contains the exact global-versus-individual ceiling distinction.</criterion>
  </acceptance-criteria>
  <done>Plan 10 has compiling focused evidence and an immutable summary ready for checker/parity closure in Plan 11.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1. Any unresolved High severity threat blocks completion and must yield gaps; it may not be accepted or deferred.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Persisted Fjall bytes to streaming snapshot values | Local bytes are untrusted for encoded size, record/unbroadcast counts, per-record and aggregate transaction bytes, identities, and arithmetic before allocation. |
| Decoded candidate corpus to recovery topology | Candidate count and dependency edges require finite format-owned ceilings before graph allocation. |
| Bounded candidate to current live mempool | Current authenticated chainstate, policy, expiry, resource ledger, and final membership decide survivors; persisted input carries no policy authority. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| T-135-10-01 | High | Denial of Service | Fjall preflight/streaming decode | mitigate | Reject encoded bytes before load and enforce the four globally coexisting encoded dimensions plus individual per-transaction limits with checked counters. | Exact/one-over and cross-field streaming tests; original-byte retention tests. |
| T-135-10-02 | High | Denial of Service | Recovery topology | mitigate | Derive aggregate/per-record edges from 41-byte serialized-input minima and the existing transaction-byte budgets; use checked accumulation before graph growth. | Exact division/one-over proofs and topology counter tests. |
| T-135-10-03 | High | Tampering | Persisted versus current policy | mitigate | Persist/infer no historical policy; decode/topology use format limits and staged replay uses fresh current policy. | Zero-capacity and two-to-one final DroppedEvicted regressions. |
| T-135-10-04 | High | Elevation of Privilege | Final live membership | mitigate | Preserve fresh mempool replay, expiry, final-membership rewrite, rebuilt projections, and one atomic install. | Existing final-membership/reconciliation tests plus repaired regressions. |
| T-135-10-05 | Medium | Information Disclosure | Decode/recovery diagnostics | mitigate | Preserve fixed low-cardinality errors and aggregate statuses; never log raw bytes, transactions, or identities. | Existing error-shape/redaction tests remain green. |
| T-135-10-06 | Medium | Repudiation | Focused execution evidence | mitigate | Record exact commands/results and protected-artifact hashes in immutable 135-10-SUMMARY.md without claiming independent verification. | Task 3 scope/hash audit. |

## Residual Risks

- Format-derived limits intentionally bound supported persisted input independently of theoretical live policy maxima. Future increases must revise the authoritative format byte budget, not couple startup to current policy.
- Snapshot bytes remain unauthenticated local storage. An actor with write access can force a bounded recovery failure or current-policy rejection, but cannot bypass canonical decode, authenticated chainstate, current policy, or atomic install.
- WR-01 remains non-blocking: the current live preflight is unconditional, but outer-cfg mutation hardening is explicitly outside this gap closure.
</threat-model>

<verification>
Executor verification is limited to the focused Rust commands and immutable Plan 10 summary in Task 3. The executor must not edit checker/parity/LOC evidence, run the full repository verifier, or create/edit canonical `135-VERIFICATION.md`. Plan 11 owns those surfaces and the only final independent transactional verifier protocol.
</verification>

<success-criteria>
- MPDUR-02 candidates within the finite stable format budget reach current-policy replay independently of current live capacity.
- The global encoded tuple and individual edge ceilings have exact checked boundary/one-over proofs without a false seven-field simultaneous-max claim.
- Zero-capacity, two-to-one trimming, accounted-capacity Fjall, and 50,001-record regressions pass.
- Every node/RPC caller compiles without from_policy, compatibility wrappers, fixed caps, duplicated arithmetic, or historical-policy metadata.
- Protected Phase 135 artifacts and deferred Phase 136-138/public/readiness boundaries remain unchanged.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-10-SUMMARY.md`.
</output>
