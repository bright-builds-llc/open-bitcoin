---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "09"
type: execute
wave: 9
depends_on: ["135-08"]
gap_closure: true
files_modified:
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/resource.rs
  - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
  - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
  - packages/open-bitcoin-node/src/storage/mempool_snapshot/tests.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs
  - packages/open-bitcoin-node/src/network/recovery/topology.rs
  - packages/open-bitcoin-node/src/network/recovery/staging.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - scripts/check-phase135-snapshot-recovery/source.ts
  - scripts/check-phase135-snapshot-recovery.ts
  - scripts/check-phase135-snapshot-recovery.test.ts
  - docs/parity/source-breadcrumbs.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [MPDUR-01, MPDUR-04]
must_haves:
  truths:
    - "A supported v1 record with unknown acceptance age can recover, remain unknown, survive a later mutation and Sync checkpoint, and reopen from the current v2 format without receiving a fresh timestamp."
    - "Every live mempool that satisfies the configured accounted-memory policy has a bounded record and topology budget large enough for snapshot capture, decode, and recovery, including 50,001 valid records."
    - "The Phase 135 checker accepts raw-key preflight only as a direct top-level decode statement before direct Serde construction and rejects nested unexecuted closure or local-function decoys."
    - "The three root gaps close without changing the global schema, enabling deferred features, or inventing a separate workaround for the downstream verifier-provenance failure."
    - "After 135-09-SUMMARY.md exists, the standard gsd-verifier returns passed only when a fresh lifecycle-valid canonical report records successful exit of the timed default verifier on that exact post-summary tree."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      provides: "Backward-readable current-v2 representation for known and explicit legacy-unknown acceptance age"
      contains: "accepted_at_unix_seconds"
    - path: packages/open-bitcoin-mempool/src/resource.rs
      provides: "Auditable policy-capacity-derived upper bounds for live entry and input-edge counts"
      contains: "MempoolCapacity"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Startup decode limits derived from the same live mempool accounting invariant"
      contains: "from_policy"
    - path: scripts/check-phase135-snapshot-recovery/source.ts
      provides: "Dependency-free direct top-level Rust statement inspection"
    - path: scripts/check-phase135-snapshot-recovery.test.ts
      provides: "Nested closure and local-function bypass mutations with exact diagnostics"
    - path: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      provides: "Post-summary lifecycle-valid gsd-verifier report with source-truth adjudication and timed default-verifier command/exit evidence"
      contains: "status: passed"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      via: "PrepareSnapshot preserves LegacyUnknown in the required nullable current-v2 field"
      pattern: "MempoolSnapshotRecord::try_from_canonical"
    - from: packages/open-bitcoin-mempool/src/resource.rs
      to: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      via: "one accounting-derived record budget shared by policy-bounded load"
      pattern: "max.*entries"
    - from: packages/open-bitcoin-mempool/src/resource.rs
      to: packages/open-bitcoin-node/src/network/recovery/topology.rs
      via: "accounting-derived vertex and input-edge ceilings replace fixed snapshot-only caps"
      pattern: "max.*edges"
    - from: scripts/check-phase135-snapshot-recovery.ts
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs
      via: "direct top-level statement indices prove preflight executes before Serde construction"
      pattern: "validate_raw_object_keys"
    - from: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-09-SUMMARY.md
      to: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      via: "standard gsd-verifier runs only after all summaries and produces a fresher lifecycle-valid report"
      pattern: "phase_lifecycle_id: 135-2026-08-02T17-41-48"
    - from: .planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
      to: scripts/verify.sh
      via: "candidate passed report names the timed command, and gsd-verifier returns passed only after that command exits successfully without an intervening edit"
      pattern: "phase135-09-full-verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-03T21:28:33Z
---

<objective>
Close the remaining Phase 135 checkpoint-totality and checker-proof gaps.

Purpose: Make conservative v1 recovery closed under later current checkpointing, make durable bounds total over the live byte-accounted policy domain, and make the structural guard prove direct execution rather than textual token order.
Output: A backward-readable nullable v2 age field, accounting-derived snapshot/recovery limits, end-to-end Sync/reopen regressions, and depth-aware checker mutations.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-08-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md

<interfaces>
Current source contracts the executor must preserve while repairing closure:

From `packages/open-bitcoin-node/src/storage/mempool_snapshot.rs`:
```rust
pub enum MempoolSnapshotError {
    UnsupportedVersion,
    StructuralCorruption,
    ResourceBoundExceeded,
    IdentityMismatch,
    DecodeFailure,
}

pub struct MempoolSnapshotRecord {
    pub transaction: Transaction,
    pub acceptance_time: MempoolAcceptanceTime,
}

pub fn try_new_current(
    captured_generation: CapturedMempoolGeneration,
    captured_at: PolicyTime,
    records: Vec<MempoolSnapshotRecord>,
    unbroadcast_members: BTreeSet<MempoolMemberIdentity>,
) -> Result&lt;Self, MempoolSnapshotError&gt;;
```

From `packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs` and `decode.rs`:
```rust
struct MempoolSnapshotV2RecordDto {
    transaction: Vec&lt;u8&gt;,
    accepted_at_unix_seconds: i64,
}

// BoundedRecord already uses two levels of optionality:
// outer None = field absent, Some(None) = explicit JSON null.
maybe_accepted_at: Option&lt;Option&lt;i64&gt;&gt;,
```

From `packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs`:
```rust
pub fn from_policy(policy: &amp;PolicyConfig) -&gt; Result&lt;Self, SyncRecoveryCategory&gt;;
```

The established accounting formula in `open-bitcoin-mempool/src/resource.rs` counts a nonzero fixed entry-map key and `MempoolEntry` footprint for every live entry and a nonzero `TransactionInput` footprint for every input. Derive ceilings from that owned formula; do not add an independent transaction-count admission rule.
</interfaces>
</context>

<orchestrator-preflight>
Owner: `/gsd-execute-phase`, not the plan executor.

The current lifecycle-valid `135-VERIFICATION.md` truthfully records the pre-Plan-09 `gaps_found` state and can make normal task commits fail the active-milestone traceability hook. Before spawning the executor, the orchestrator may quarantine that old report through the established recoverable gap-cycle mechanism; it must preserve the artifact for audit and must never delete it destructively. This plan embeds the three canonical gap contracts and does not require the quarantined file at task runtime. The executor must not create, edit, restore, replace, or forge `135-VERIFICATION.md`; canonical verification ownership remains exclusively with the independent post-summary verifier.
</orchestrator-preflight>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve legacy-unknown age through current v2 checkpoints</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>packages/open-bitcoin-mempool/src/context.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot/tests.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/storage/mempool_snapshot/tests.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Current v2 keeps `accepted_at_unix_seconds` as a required record key: a number decodes to `Known`, explicit JSON `null` decodes to `LegacyUnknown`, and an absent or duplicate key remains a whole-snapshot failure.
    - Current encoding writes known timestamps as the existing number and writes `LegacyUnknown` as explicit null; neither conversion nor capture samples startup/capture time to replace unknown age.
    - Existing numeric current-v2 bytes and exact legacy-v1 bytes remain readable, v1 remains decode-only, the mempool-local version stays 2, and global `SchemaVersion::CURRENT` stays 1.
    - A real legacy-unknown recovery installs, an ordinary later mutation advances dirtiness, a Sync prepared checkpoint completes, and reopening the Fjall store yields current v2 with the recovered record still `LegacyUnknown`.
  </behavior>
  <action>
    Close the unknown-age half of MPDUR-01/MPDUR-04 per D-01, D-03, D-04, D-09, D-12, and D-14. Change only the current-v2 acceptance-time value representation from `i64` to `Option&lt;i64&gt;`; retain the exact required field name and use the decoder's existing `Option&lt;Option&lt;i64&gt;&gt;` distinction so missing and explicit null never collapse. Map `Some(seconds)` to `Known(PolicyTime)` and `None` to `LegacyUnknown`; make `MempoolSnapshotRecord::try_from_canonical`, `MempoolSnapshot::try_new_current`, and `MempoolSnapshotV2RecordDto::try_from` accept/preserve `LegacyUnknown`, while continuing to reject only known times later than `captured_at`, duplicate identities, and foreign unbroadcast members. Do not introduce v3, change the global wrapper, add a fresh-time fallback, make v1 writable, or persist historical origin/relay facts. Replace the old rejection tests with required-null round-trip, absent-field failure, old numeric-v2 compatibility, and exact v1 conservative migration assertions. Add one store/authority regression using a valid spend recovered with `LegacyUnknown`: prepare and install it, apply a second ordinary valid mutation at an injected time, reserve and execute a `CheckpointTrigger::Periodic` Sync write, complete its receipt, reopen the temporary Fjall store, load with policy-derived limits, and prove current v2 contains both the still-unknown recovered age and the known post-recovery age. Keep error assertions on stable low-cardinality classes and never print transaction or snapshot bytes.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-09-unknown-age -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features legacy_unknown -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Exact DTO inspection finds `accepted_at_unix_seconds: Option&lt;i64&gt;`, while the field list remains exactly transaction plus acceptance time and both format-version constants remain unchanged.</criterion>
    <criterion>The streaming converter requires the outer field presence and preserves the inner null; missing, duplicate, malformed, and known-future values fail closed with stable snapshot classes.</criterion>
    <criterion>Round-trip tests prove known values retain the old numeric JSON shape and unknown values use explicit null without sampling `captured_at` or startup time.</criterion>
    <criterion>The end-to-end regression observes `LegacyUnknown` after v1 recovery, after install, in the prepared current snapshot, and after Sync/reopen; it also proves the later known-age mutation survives and checkpoint evidence completes truthfully.</criterion>
  </acceptance-criteria>
  <done>Supported conservative v1 recovery is closed under all later current-v2 checkpoint operations without fabricated freshness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive checkpoint and recovery count bounds from live policy accounting</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>packages/open-bitcoin-mempool/src/resource.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/topology.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery/staging.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>docs/parity/source-breadcrumbs.json</file>
  </read-first>
  <files>packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/resource.rs, packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs, packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/storage/mempool_snapshot/tests.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/network/recovery/topology.rs, packages/open-bitcoin-node/src/network/recovery/staging.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/gap_closure.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - The deterministic mempool accounting module exposes checked, nonzero lower-bound facts for one live entry and one input edge, and derives maximum entry/input counts from `PolicyConfig::mempool_capacity` without adding an admission count limit.
    - Snapshot capture, startup decode, and recovery topology use those same policy-derived ceilings; no path clamps at 50,000 or the unrelated fixed 1,600,000 topology edge constant.
    - Decode still checks encoded bytes before loading, bounds sequence allocation before transaction parsing, and retains per-transaction, aggregate-transaction, unbroadcast, and checked encoded-size limits.
    - Small live fixtures cover the maximum policy-valid count for their configured capacity and one-over rejection, while a separate bounded 50,001-record current checkpoint performs Sync persistence and reopen beyond the former ceiling without admitting 50,001 transactions through chainstate.
  </behavior>
  <action>
    Close the record-totality half of MPDUR-01/MPDUR-04 per D-01, D-04, D-05, D-09, and D-14. In the functional-core accounting module, expose a small value contract derived from `MEMPOOL_RESOURCE_ACCOUNTING_VERSION`: compute a nonzero minimum fixed accounted footprint for every entry from the same `size_of::&lt;Txid&gt;() + size_of::&lt;MempoolEntry&gt;()` components already charged by `accounted_memory_for_entry`, and a nonzero per-input footprint from the charged `TransactionInput` element. Provide checked helpers that turn `MempoolCapacity` into conservative maximum live entries and total input edges, with explicit zero-capacity and overflow behavior; unit-test the formula against actual minimum-shaped entries so changes to accounting cannot silently invalidate the proof. Remove `MAX_MEMPOOL_SNAPSHOT_RECORDS = 50_000` and `MAX_RECOVERY_TOPOLOGY_EDGES = 1_600_000`. A domain constructor receiving an already allocated `Vec` need not pretend to prevent allocation; enforce bounds at live capture before cloning, at streaming decode before allocation/parsing, and at topology construction before graph allocation. Derive `MempoolSnapshotDecodeLimits::from_policy` from the exported accounting bound, update codec test defaults to an equivalently bounded test policy, pass the current `PolicyConfig` into recovery topology limits, and derive vertex, aggregate-edge, and per-record edge ceilings from the same capacity contract. Preserve existing checked encoded-byte, transaction-byte, unbroadcast, identity, and topology arithmetic. Do not add a Knots-incompatible live count rule, silently trim records during capture, weaken byte limits, or classify a policy-valid live state as snapshot corruption. Add small live-policy fixtures whose capacity is computed from their actual deterministic ledger: the maximum configured membership captures successfully and the immediately-over-capacity transition cannot become live. Separately, in a focused `snapshot_persistence/gap_closure.rs` module with the required parity breadcrumb, construct 50,001 unique canonical minimal snapshot records directly in linear time, pass them through current snapshot construction and the real codec plus `FjallNodeStore::save_mempool_snapshot(..., PersistMode::Sync)` path without 50,001 chainstate admissions, reopen with a policy-derived ceiling above that count, and verify count plus deterministic first/last identities; assert the corpus remains below explicit encoded-size and test-memory ceilings. Register the new Rust test source in `docs/parity/source-breadcrumbs.json`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-09-policy-bounds -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-features resource_cases -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase135-09-former-50k -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features former_50_000 -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n '50_000|MAX_MEMPOOL_SNAPSHOT_RECORDS|MAX_RECOVERY_TOPOLOGY_EDGES'` finds no production snapshot/recovery ceiling; any test mention is solely the named former-bound regression.</criterion>
    <criterion>Accounting tests prove every constructed live entry/input consumes at least the exported lower bound and that exact capacity division yields a checked deterministic ceiling.</criterion>
    <criterion>Capture checks live membership against the policy-derived ceiling before cloning, decoder sequence allocation uses the same ceiling, and topology derives vertices/edges from the recovery policy rather than a snapshot-only constant.</criterion>
    <criterion>The small live exact-capacity case captures successfully, the immediately-over-capacity transaction cannot remain in authoritative live membership, and a one-over decoder fixture receives only `ResourceBoundExceeded` without rewriting stored bytes.</criterion>
    <criterion>The 50,001-record test performs actual current snapshot encoding, Fjall Sync persistence, drop/reopen, and bounded decode with all records present while linear construction and explicit size/memory assertions prevent an unbounded test corpus.</criterion>
  </acceptance-criteria>
  <done>The durable pipeline is bounded by and total over the same accounted-memory domain as the live mempool, including states above the former fixed record ceiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove direct preflight execution and guard both repaired invariants</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>scripts/check-phase135-snapshot-recovery/source.ts</file>
    <file>scripts/check-phase135-snapshot-recovery.ts</file>
    <file>scripts/check-phase135-snapshot-recovery.test.ts</file>
    <file>scripts/verify.sh</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery/source.ts, scripts/check-phase135-snapshot-recovery.ts, scripts/check-phase135-snapshot-recovery.test.ts, docs/metrics/lines-of-code.md</files>
  <behavior>
    - The checker masks comments/literals and recognizes the exact preflight and Serde statements only at direct function-body brace depth zero; a call inside a closure, local function, async block, or nested block cannot satisfy either marker.
    - Direct preflight must precede direct Serde construction on the actual `decode_bounded_versioned` path; deletion, direct reordering, nested-closure decoy, and local-function decoy each yield exactly `PHASE135_DIAGNOSTICS.bounds`.
    - The checker also requires explicit nullable current-v2 age preservation and accounting-derived record/topology ceilings, so reverting either runtime repair cannot leave Phase 135 falsely green.
    - The checker remains deterministic, filesystem-only, dependency-free, and registered in the existing verifier order.
  </behavior>
  <action>
    Close D-14/D-15's remaining spoofing gap and update the checker to the repaired runtime contracts. In `source.ts`, add a dependency-free helper over the already comment/string/raw-string-masked, brace-balanced function body that records exact statement indices only while nested curly depth is zero; keep original source offsets or another deterministic ordering key, reject unbalanced bodies, and do not mistake braces inside masked literals/comments for control flow. In the Phase 135 checker, require the exact top-level `validate_raw_object_keys(bytes)?;` statement and exact direct `let mut deserializer = serde_json::Deserializer::from_slice(bytes);` construction, then require preflight's index to be lower. Update the schema guard from the old `i64` anchor to the required `Option&lt;i64&gt;`/explicit-null conversion while retaining the exact two-field source-only DTO and unchanged version checks. Replace fixed record/edge-constant anchors with exact accounting-derived helper, capture, decoder, and topology wiring anchors; add mutations that restore the 50,000 clamp or bypass the shared accounting helper. Add exact bounds-diagnostic mutations that move preflight into (1) an uncalled closure and (2) an uncalled local function before the direct Serde statement, leaving no direct preflight call; retain deletion, direct reordering, comment/string masking, live-corpus, and every existing claim mutation. Do not add a Rust parser dependency, weaken any unrelated Phase 135 diagnostic, reorder `scripts/verify.sh`, change parity/requirement status, or implement deferred Phase 136-138 surfaces. Refresh tracked LOC only through the repository-owned generation path.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts &amp;&amp; bun scripts/check-phase135-snapshot-recovery.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The helper has focused positive/negative coverage for top-level statements, closures, local functions, nested blocks, comments, normal strings, raw strings, and malformed/unbalanced bodies.</criterion>
    <criterion>Each preflight deletion, direct reorder, nested closure, and local-function mutation produces exactly one bounds diagnostic; no other diagnostic or false pass is accepted.</criterion>
    <criterion>Mutating nullable unknown-age preservation or reintroducing a fixed 50,000/runtime-independent topology cap fails the appropriate schema/bounds contract.</criterion>
    <criterion>The unmodified corpus and every pre-existing mutation/claim case pass, with checker registration and deferred claim boundaries unchanged.</criterion>
  </acceptance-criteria>
  <done>Phase 135's default guard proves the actual direct decode composition and prevents regression of both representability repairs.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Legacy/current snapshot bytes to acceptance-age authority | Persisted local bytes may claim a known timestamp or explicit unknown age; recovery must not fabricate freshness or leak raw data in failures. |
| Live byte-accounted mempool to durable snapshot/recovery allocation | Policy-valid membership crosses into cloned records, JSON decoding, and topology structures that need a common finite resource proof. |
| Rust decoder source to TypeScript structural evidence | A textual marker in dead nested code can spoof required preflight execution unless the checker distinguishes direct control-flow depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Verification |
| --- | --- | --- | --- | --- | --- |
| T-135-09-01 | Tampering | Acceptance-time migration and current v2 DTO | mitigate | Keep the age key required, represent unknown only as explicit null, never substitute capture/startup time, validate known-future values, and replay every record through current chainstate/policy. | Numeric-old-v2, null-current-v2, missing/duplicate/future, and v1-to-Sync-reopen regressions assert exact age semantics. |
| T-135-09-02 | Denial of Service | Capture, streaming decode, and recovery topology | mitigate | Derive entry/input ceilings from the versioned live accounting formula, check membership before cloning, check sequence counts before transaction parsing/allocation, and retain checked encoded/aggregate/edge arithmetic. | Exact-bound/one-over tests plus the explicitly size-capped 50,001-record Sync/reopen regression exercise every boundary. |
| T-135-09-03 | Spoofing | Phase 135 structural checker | mitigate | Inspect exact direct top-level statements at brace depth zero and require direct preflight before direct Serde construction. | Deletion, reordering, closure-decoy, and local-function-decoy mutations each fail with exactly the bounds diagnostic. |
| T-135-09-04 | Repudiation | Snapshot/resource failure evidence | mitigate | Preserve the existing typed low-cardinality `ResourceBoundExceeded`, structural, identity, and decode classes; do not collapse one-over bounds into generic or success states. | Focused failures assert stable classes and original stored bytes; full redaction/verification checks run last. |
| T-135-09-05 | Information Disclosure | Test and operator diagnostics | mitigate | Never include transactions, member identities, persisted JSON, paths containing secrets, or raw decoder text in runtime errors; tests assert classes/counts only. | Checker and Rust assertions use fixed diagnostics, and the default verifier retains repository redaction guards. |

## Residual Risks

- Mempool snapshot bytes are not cryptographically authenticated. An actor able to alter the local store can replace a known age with the explicit unknown marker, but cannot create unbounded work or bypass current chainstate/policy replay; authenticated local storage remains outside this phase.
- Accounting-derived ceilings are conservative upper bounds, not an extra admission policy. They may reserve more theoretical decode/topology headroom than an actually constructible mempool, while byte, aggregate transaction, and checked-allocation limits remain the tighter controls.
- The dependency-free checker is a deliberately narrow structural proof, not a Rust compiler frontend. Exact direct-statement depth plus adversarial closure/local-function mutations covers this verified bypass; Rust behavioral tests and the full repository verifier remain authoritative.
- A 50,001-record regression is intentionally heavier than ordinary unit tests. Its explicit encoded-size/memory ceiling and linear construction prevent accidental unbounded growth, but runtime can vary across machines.
</threat-model>

<verification>
Executor-owned verification ends here: run the three task-level automated commands sequentially, then run `bun scripts/check-parity-breadcrumbs.ts --check`, `bun scripts/bright-builds-check.ts all`, and `git diff --check`. Review that changes are confined to the 19 inventoried implementation/test/checker artifacts, including the repository-generated LOC report, with no global/local version bump, new live admission count limit, requirement/status promotion, verifier reordering, or Phase 136-138 surface. These focused Rust, checker, parity, Bright Builds, and diff checks are sufficient for the executor to create and commit immutable `135-09-SUMMARY.md`, then return. Do not run a redundant bare or full-workspace Cargo sequence from the repository root; the focused Rust commands already use the timing wrapper and `--manifest-path packages/Cargo.toml`. The executor must not run the final default verifier and must not create or edit `135-VERIFICATION.md`.

The following phase-level transaction is owned by the standard `gsd-verifier` that `/gsd-execute-phase` spawns after every Phase 135 summary exists. It is part of the must-have/report contract the verifier consumes, not a custom execute-phase hook:

1. Independently verify every Phase 135 source, behavior, artifact, key-link, test, lifecycle, and scope truth against the final post-summary tree.
2. If any source truth fails, write a fresh lifecycle-valid canonical `135-VERIFICATION.md` with `status: gaps_found`, record the failed truths, and return non-passed immediately. Do not run the default verifier and do not promote the phase.
3. If all source truths pass, write a lifecycle-valid candidate canonical `135-VERIFICATION.md` on the post-summary tree with `status: passed`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 135-2026-08-02T17-41-48`, and `generated_at` later than `135-09-SUMMARY.md`. Before the command runs, the candidate report must already name `bun run scripts/command-timings.ts run --key phase135-09-full-verify -- bash scripts/verify.sh`, record `exit_status: 0` and `result: passed` as a conditional transactional attestation, and state that `gsd-verifier` may return passed only if the next step actually exits zero on the unchanged tree; failure requires the rewrite in step 5.
4. Without any intervening repository edit, run `bun run scripts/command-timings.ts run --key phase135-09-full-verify -- bash scripts/verify.sh`. Do not use `--fast`, do not overlap Cargo jobs, and poll a yielded session at least every 60 seconds. This is the final validation of the exact tree containing the candidate report and every immutable summary.
5. If the command fails or changes the repository tree, immediately rewrite the canonical report to lifecycle-valid `status: gaps_found`, record the command, nonzero/freshness failure, and affected truth, then return non-passed. Do not complete or promote the phase.
6. If the command exits zero and the tree is unchanged, make no further repository edit and return `passed`; the candidate canonical report already contains the command and successful-exit attestation, which is now truthful on the exact validated tree.

Standard `/gsd-execute-phase` reads canonical verification status only after `gsd-verifier` returns. Therefore its built-in `update_roadmap`/requirement-promotion path is reachable only when this transaction returns `passed`; `gaps_found`, stale lifecycle provenance, command failure, or a verifier-induced tree change stops completion through the existing standard path.
</verification>

<success-criteria>
- D-01/D-03 current source truth is closed under recover-then-checkpoint: explicit unknown age survives v1 recovery, later mutation, Sync persistence, and current-v2 reopen without freshening.
- D-04/D-05/D-09 bounded durability is total over the live accounted-memory policy domain, with no fixed 50,000 snapshot-only ceiling or new Knots-incompatible admission rule.
- D-14/D-15 verification rejects dead nested preflight decoys and guards both repaired runtime invariants with exact stable diagnostics.
- MPDUR-01 and MPDUR-04 are the only requirements directly repaired; already-satisfied MPDUR-02/03 behavior and every deferred Phase 136-138 boundary remain unchanged.
- Independent lifecycle-valid Phase 135 re-verification reports `passed` from the repaired implementation, then the complete timed default repository verifier passes last without a provenance workaround.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-09-SUMMARY.md`.
</output>
