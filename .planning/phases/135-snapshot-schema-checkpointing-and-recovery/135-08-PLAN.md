---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "08"
type: execute
wave: 8
depends_on: ["135-07"]
files_modified:
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/confirmation_migration.rs
  - scripts/check-phase135-snapshot-recovery.ts
  - scripts/check-phase135-snapshot-recovery.test.ts
  - docs/metrics/lines-of-code.md
autonomous: true
gap_closure: true
requirements: [MPDUR-01, MPDUR-02]
must_haves:
  truths:
    - "Malformed snapshot object keys, including trailing escapes, incomplete Unicode escapes, and unterminated quoted tokens, fail before Serde can allocate for them."
    - "Legacy confirmation migration derives counts only from active-chain transaction bodies whose non-mutated Merkle root matches the stored header commitment."
    - "The Phase 135 checker fails when raw-key preflight is deleted from or moved after Serde construction inside decode_bounded_versioned."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs
      provides: "Stateful fail-closed raw JSON object-key scanner"
      contains: "validate_raw_object_keys"
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs
      provides: "Narrow-budget malformed-key regressions"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Header-to-body Merkle commitment validation before confirmation counting"
      contains: "load_chainstate_snapshot_with_confirmation_migration"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/confirmation_migration.rs
      provides: "Same-header/different-body and mutated-tree migration regressions"
    - path: scripts/check-phase135-snapshot-recovery.ts
      provides: "Function-scoped ordered preflight-before-Serde structural assertion"
    - path: scripts/check-phase135-snapshot-recovery.test.ts
      provides: "Preflight deletion and reordering mutations"
    - path: docs/metrics/lines-of-code.md
      provides: "Verifier-refreshed tracked source metrics"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs
      to: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs
      via: "decode_bounded_versioned invokes the preflight before constructing serde_json::Deserializer"
      pattern: "validate_raw_object_keys\\(bytes\\)\\?;.*Deserializer::from_slice\\(bytes\\)"
    - from: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      to: open_bitcoin_core::consensus::block_merkle_root
      via: "migration rejects a mutated or mismatched transaction tree before transaction_txid counting"
      pattern: "block_merkle_root.*maybe_mutated"
    - from: scripts/check-phase135-snapshot-recovery.test.ts
      to: scripts/check-phase135-snapshot-recovery.ts
      via: "delete and reorder mutations both produce the Phase 135 bounded-load diagnostic"
      pattern: "validate_raw_object_keys\\(bytes\\)\\?"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-03T19:36:43.955Z
---

<objective>
Close the three independently verified Phase 135 durability gaps without changing the snapshot schema, recovery architecture, or deferred scope.

Purpose: Make bounded snapshot decoding conclusive before Serde, authenticate confirmation evidence against the active-chain header commitment, and prevent the structural checker from silently losing the preflight ordering contract.
Output: Two root-cause Rust repairs with adversarial regressions, an ordered function-scoped checker assertion with deletion/reordering mutations, refreshed LOC evidence, and a passing complete repository verifier.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-06-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-07-SUMMARY.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW-FIX.md

<interfaces>
Existing contracts the executor must preserve and use directly:

From `storage/snapshot_codec/mempool/decode/key_preflight.rs`:
```rust
const MAX_MEMPOOL_FIELD_TOKEN_BYTES: usize = 64;
pub(super) fn validate_raw_object_keys(encoded: &[u8]) -> Result<(), StorageError>;
```

From `storage/snapshot_codec/mempool/decode.rs`:
```rust
pub(super) fn decode_bounded_versioned(
    bytes: &[u8],
    limits: MempoolSnapshotDecodeLimits,
) -> Result<MempoolSnapshotPayloadDto, StorageError>;
```
The current body calls `validate_raw_object_keys(bytes)?` immediately before `serde_json::Deserializer::from_slice(bytes)`; Task 3 must prove that exact composition, not corpus-wide identifier presence.

From `open_bitcoin_core::consensus`:
```rust
pub fn block_hash(header: &BlockHeader) -> BlockHash;
pub fn block_merkle_root(
    transactions: &[Transaction],
) -> Result<(MerkleRoot, bool), CodecError>;
pub fn transaction_txid(transaction: &Transaction) -> Result<Txid, CodecError>;
```
The boolean returned by `block_merkle_root` is the existing duplicate-pair mutation signal used by `check_block`; confirmation migration must reject it and a header/root mismatch before counting any txid.

From `scripts/check-phase135-snapshot-recovery/source.ts`:
```typescript
export function body(source: string, marker: string): string;
export function addFailure(
  failures: string[],
  failed: boolean,
  diagnostic: string,
): void;
```
`body` masks comments and literals and returns the brace-balanced target body, so the ordered assertion can remain dependency-free and function-scoped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make raw object-key preflight fail closed before Serde</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests/mempool_limits.rs</files>
  <behavior>
    - An object key exceeding 64 raw bytes returns the existing resource-bound failure as soon as the bound is crossed, without waiting for a closing quote.
    - A key ending in `\`, `\u`, `\u0`, `\u00`, or `\u000`, and every quoted token missing its closing quote, fails inside raw preflight before `serde_json::Deserializer::from_slice` is reachable.
    - Escaped value strings retain their existing field-specific streaming bounds; the key preflight must not impose the 64-byte key limit on transaction values.
  </behavior>
  <action>
    Repair the verified MPDUR-01 boundary at its root per D-04 and D-14. Replace the quote-skipping loop in `validate_raw_object_keys` with a small stateful JSON lexical scanner that tracks object-versus-array nesting and whether the current object expects a key, colon, value, comma, or end. Scan object-key strings incrementally: enforce `MAX_MEMPOOL_FIELD_TOKEN_BYTES` before advancing further; accept only complete JSON simple escapes (`\"`, `\\`, `\/`, `\b`, `\f`, `\n`, `\r`, `\t`) and `\u` followed by exactly four ASCII hex digits; reject a missing escaped byte, incomplete Unicode escape, invalid transition, stack underflow, or end-of-input while any quoted token remains open. Use checked cursor movement rather than `saturating_add(2)`, preserve low-cardinality `MempoolSnapshotError::{ResourceBoundExceeded,StructuralCorruption}` mapping, and keep all work allocation-bounded. Do not deserialize into `serde_json::Value`, change the local/global snapshot schema, bound ordinary value strings to 64 bytes, log raw bytes, or add a second decode path. In `key_preflight.rs`, add focused direct unit tests whose names begin `raw_key_preflight_` so trailing simple escapes, each incomplete `\u` length, and ordinary unterminated key/value strings are proven to fail inside this function without invoking Serde. In `mempool_limits.rs`, add an integration regression with an oversized unterminated escaped key and a narrow caller budget that must hit the resource-bound class; retain the existing well-formed escaped-key and transaction-value coverage.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-08-key-preflight -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features raw_key_preflight_ -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'saturating_add\(2\)' packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode/key_preflight.rs` returns no match, and exact inspection shows checked escape advancement plus four-hex-digit Unicode handling.</criterion>
    <criterion>The focused command passes direct preflight-unit tests for trailing backslash, `\u` with zero through three digits, ordinary unterminated key, and unterminated value string, plus the public decoder regression for an oversized unterminated escaped key.</criterion>
    <criterion>Oversized unterminated keys return `mempool snapshot exceeds a resource bound`; short malformed/incomplete tokens return the stable structural/decode corruption class, and no diagnostic contains raw snapshot bytes.</criterion>
    <criterion>The existing escaped transaction-token test still proves value strings use their field-specific budget rather than the 64-byte object-key budget.</criterion>
  </acceptance-criteria>
  <done>Every quoted token terminates cleanly and every object key is conclusively bounded before Serde can allocate for persisted snapshot input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate migrated confirmation counts against block Merkle commitments</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/confirmation_migration.rs</file>
    <file>packages/open-bitcoin-consensus/src/crypto.rs</file>
    <file>packages/open-bitcoin-consensus/src/block.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence/confirmation_migration.rs</files>
  <behavior>
    - Header identity must match the active-chain key, the computed transaction Merkle root must match `block.header.merkle_root`, and the existing mutated-tree signal must be false before any confirmation count is derived.
    - A syntactically valid different body stored with the expected header fails as chainstate corruption and leaves legacy confirmation evidence unpersisted.
    - A body whose computed root matches the header but is flagged as a mutated duplicate-pair tree also fails before counting.
  </behavior>
  <action>
    Repair the verified MPDUR-02 evidence boundary per D-02, D-09, and D-14. Import the existing `open_bitcoin_core::consensus::block_merkle_root` primitive into `storage/fjall_store/mempool.rs`. In `load_chainstate_snapshot_with_confirmation_migration`, immediately after the existing `block_hash(&block.header) == position.block_hash` check and before the `for transaction in &block.transactions` counting loop, compute `(actual_merkle_root, maybe_mutated)`. Map codec failure, `maybe_mutated == true`, and `actual_merkle_root != block.header.merkle_root` to typed `StorageError::Corruption` in `StorageNamespace::Chainstate` with fixed bounded detail that distinguishes derivation, mutated tree, and commitment mismatch without transaction identities or bodies. Do not call full contextual validation, alter active-chain/header selection, change `ChainstateSnapshot`, or persist any migrated counts until every active-chain block passes. Add one regression that writes the expected header/key with a different syntactically valid transaction body while keeping the header byte-for-byte identical, then proves migration fails and a reload still has `maybe_confirmed_txid_counts: None`; add an independent duplicate-pair mutated-tree regression whose header commits to that computed root and prove the same fail-closed/no-persistence result. Preserve the existing successful, missing-block, and wrong-key cases.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-08-confirmation-merkle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features confirmation_migration -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Exact function inspection finds `block_merkle_root(&amp;block.transactions)` and both mutated/mismatch guards before `for transaction in &amp;block.transactions`.</criterion>
    <criterion>The same-header/different-valid-body test proves header hash equality is insufficient and receives chainstate corruption containing the fixed Merkle-commitment mismatch class.</criterion>
    <criterion>The duplicate-pair test uses the primitive's `maybe_mutated` result with a matching header root and still receives the fixed mutated-tree corruption class.</criterion>
    <criterion>After either failure, `load_chainstate_snapshot()` returns the original legacy snapshot with unknown confirmation evidence; no false count or rewritten snapshot is persisted.</criterion>
  </acceptance-criteria>
  <done>Recovery confirmation classification can consume only txid counts authenticated by the stored active-chain header commitment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the Phase 135 checker prove preflight call presence and order</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>standards/languages/typescript-javascript.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-VERIFICATION.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-REVIEW.md</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool/decode.rs</file>
    <file>scripts/check-phase135-snapshot-recovery.ts</file>
    <file>scripts/check-phase135-snapshot-recovery.test.ts</file>
    <file>scripts/check-phase135-snapshot-recovery/source.ts</file>
    <file>scripts/verify.sh</file>
    <file>docs/metrics/lines-of-code.md</file>
  </read-first>
  <files>scripts/check-phase135-snapshot-recovery.ts, scripts/check-phase135-snapshot-recovery.test.ts, docs/metrics/lines-of-code.md</files>
  <behavior>
    - The checker extracts only the brace-balanced `decode_bounded_versioned` body from `decode.rs` and requires the exact fallible preflight call and Serde-construction call to exist in executable code.
    - The preflight call index must be lower than the Serde call index; identifier presence in imports, helper definitions, comments, strings, or concatenated sibling files is insufficient.
    - Independent mutations that delete the call and move it after Serde both fail with exactly `PHASE135_DIAGNOSTICS.bounds`.
  </behavior>
  <action>
    Close D-14/D-15's checker-bypass gap without adding a parser dependency. In `check-phase135-snapshot-recovery.ts`, keep the existing corpus checks for constants and streaming bounds, but separately call `body(get(FILES.codecDecode), "pub(super) fn decode_bounded_versioned(")`. Require exact executable-code occurrences of `validate_raw_object_keys(bytes)?;` and `serde_json::Deserializer::from_slice(bytes)`, reject either missing marker, and compare their indices so preflight must precede deserializer construction. Route every failure through the existing `PHASE135_DIAGNOSTICS.bounds`. In `check-phase135-snapshot-recovery.test.ts`, replace the misleading constant-only mutation name with an accurate key-bound mutation if it remains, then add two independent contract mutations: delete the exact preflight statement, and reorder the exact preflight statement to immediately after deserializer construction. Each mutation must prove one exact bounds diagnostic; retain the positive full-corpus test and the comment/string masking case. Do not loosen any existing Phase 135 contract, change verifier ordering, edit `scripts/verify.sh`, or broaden claim/deferred scope. Refresh `docs/metrics/lines-of-code.md` only through the repository verifier after source/test changes.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts &amp;&amp; bun scripts/check-phase135-snapshot-recovery.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`checkPhase135SnapshotRecovery` contains a `body` extraction scoped to `decode_bounded_versioned`, exact call markers, and an explicit preflight-index-before-Serde-index condition.</criterion>
    <criterion>Deleting `validate_raw_object_keys(bytes)?;` produces exactly `PHASE135_DIAGNOSTICS.bounds` even though the import/helper identifier remains elsewhere in the corpus.</criterion>
    <criterion>Moving the preflight call after `serde_json::Deserializer::from_slice(bytes)` produces exactly the same bounds diagnostic.</criterion>
    <criterion>The unmodified live corpus passes, all pre-existing mutation/claim cases remain green, verifier order remains unchanged, and generated LOC evidence is fresh.</criterion>
  </acceptance-criteria>
  <done>The default Phase 135 guard proves the allocation preflight is present at the exact decode boundary and runs before Serde.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Persisted snapshot bytes to raw-key scanner | Malformed attacker-influenced local bytes can otherwise trigger allocation before bounded deserialization rejects them. |
| Durable active-chain block body to confirmation evidence | A valid header identity does not authenticate a substituted transaction body unless its Merkle commitment is checked. |
| Rust source to structural enforcement | A checker that sees only an identifier can falsely attest that a required safety call executes in the correct order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Verification |
| --- | --- | --- | --- | --- | --- |
| T-135-08-01 | Denial of Service | Raw snapshot object keys | mitigate | Stateful pre-Serde scanner enforces the 64-byte raw key bound incrementally and rejects trailing/incomplete escapes plus every unterminated token with checked cursor movement. | Narrow-budget Rust tests cover oversized escaped keys, trailing backslash, incomplete Unicode escapes, and unterminated key/value strings. |
| T-135-08-02 | Tampering | Legacy confirmation migration | mitigate | Compute the existing txid Merkle root, reject its mutation signal, and compare it with the stored header before deriving or persisting counts. | Same-header/different-body and matching-root/mutated-tree regressions prove fail-closed behavior and no migrated evidence persistence. |
| T-135-08-03 | Spoofing | Phase structural evidence | mitigate | Scope inspection to the exact decode body and enforce fallible preflight presence before Serde construction. | Independent deletion and reordering mutations must each yield only the bounded-load diagnostic. |
| T-135-08-04 | Information Disclosure | Corruption diagnostics | mitigate | Preserve fixed low-cardinality error classes; never include snapshot bytes, transactions, or member identities. | Focused assertions inspect stable classes and the full repository redaction/verification contract remains green. |

## Residual Risks

- A corrupted local store remains unavailable until the operator restores or repairs it; destructive repair and runtime import remain explicitly deferred.
- Merkle validation authenticates the loaded body to the stored active-chain header; full contextual chain validity remains the responsibility of the existing chainstate ingestion/validation path and is not duplicated during migration.
- The dependency-free TypeScript checker is structural rather than a Rust compiler frontend; exact body extraction plus executable-code masking and deletion/reordering mutations address this specific bypass, while Rust tests and the full verifier remain the behavioral authority.
</threat-model>

<verification>
Run the Task 1 raw-key suite, Task 2 confirmation-migration suite, and Task 3 mutation/live checker sequentially through their declared commands. Then run `bun scripts/check-parity-breadcrumbs.ts --check`, `bun scripts/bright-builds-check.ts all`, and `git diff --check`; review that only the seven inventoried paths changed and that no MPDUR-03/04, Phase 136-138, schema, parity-status, requirement-status, or deferred-claim surface moved. The final command must be `bun run scripts/command-timings.ts run --key phase135-08-full-verify -- bash scripts/verify.sh`; do not use `--fast`, do not overlap Cargo jobs, and poll a yielded session at least every 60 seconds.
</verification>

<success-criteria>
- D-04's bounded fail-closed decode contract rejects every incomplete/unterminated key token before Serde and MPDUR-01 is no longer blocked.
- D-09's current-chain evidence is authenticated against the stored header Merkle commitment, including mutated-tree rejection, and MPDUR-02 is no longer blocked.
- D-14/D-15 verification catches deletion and reordering of the preflight call with stable diagnostics.
- MPDUR-03 and MPDUR-04 remain covered by completed Plans 02-07; this gap plan neither reclaims nor changes them.
- The full timed default repository verifier passes last with fresh tracked LOC evidence and no deferred-scope expansion.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-08-SUMMARY.md`.
</output>
