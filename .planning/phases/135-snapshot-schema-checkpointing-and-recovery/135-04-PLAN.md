---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "04"
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/tests.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
autonomous: true
requirements: [MPDUR-01, MPDUR-04]
must_haves:
  truths:
    - "Snapshot preparation captures canonical records, exact unbroadcast membership, generation, trigger, and injected capture time under one authority guard."
    - "Checkpoint evidence truthfully distinguishes current, dirty, in-flight, and last-durable generations and computes the exact generation loss interval."
    - "Stale and duplicate achieved receipts cannot clear or regress newer authoritative state."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      provides: "Generation/time/trigger-bound affine snapshot capability and receipt"
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      provides: "Checkpoint evidence state and pure loss-window projection"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
      via: "one guard captures mempool and authoritative unbroadcast into v2 envelope"
      pattern: "PrepareSnapshot"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-02T18:01:26.808Z
---

<objective>
Bind source capture and truthful checkpoint evidence to the sole runtime authority.

Purpose: Extend Phase 134's affine snapshot capability without creating a second authority or holding the guard during I/O.
Output: Atomic source capture, evidence/loss-window types, and stale/duplicate/failure state-machine tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-17-SUMMARY.md
@packages/open-bitcoin-node/src/network/lifecycle_effects.rs
@packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs

<interfaces>
Existing `PreparedSnapshotWrite` owns `MempoolSnapshot` plus an affine capability bound to authority epoch, persistence generation, effect ID, and snapshot identity. Plan 01 adds v2 capture generation/time and exact unbroadcast membership. Preserve `MAX_PENDING_SNAPSHOT_EFFECTS = 1` and exact receipt identity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture one coherent source envelope through the dispatcher</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/tests.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>packages/bitcoin-knots/src/node/mempool_persist.cpp</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/tests.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - `SnapshotPreparationRequest` carries injected capture PolicyTime and typed Periodic or Shutdown trigger.
    - Dispatcher constructs v2 records and exact `unbroadcast_members` from the same guard and binds envelope generation to the capability generation.
    - Prepared value leaves the guard before any encoding/storage operation and remains non-Clone.
  </behavior>
  <action>
    Implement D-01 and D-05 by replacing the zero-argument preparation API in one compile-safe change with exact signatures `SnapshotPreparationRequest::new(captured_at: PolicyTime, trigger: CheckpointTrigger)` and `ManagedNetworkHandle::prepare_mempool_snapshot_write(captured_at: PolicyTime, trigger: CheckpointTrigger)`, where `CheckpointTrigger` is exactly `Periodic | Shutdown`. Under the single `apply_lifecycle_command` guard, build the Plan 01 v2 envelope from canonical mempool entries plus the exact authoritative `unbroadcast_members`, current lifecycle generation, and request time; validate every unbroadcast identity belongs to the captured canonical set before reserving the existing one-pending effect. This is the first production v2 capture: remove Plan 01's parameterless/deprecated `MempoolSnapshot::from_mempool` production call here, and do not retain any production path that invents generation, time, or unbroadcast authority. Bind trigger, captured generation/time, authority epoch, effect ID, and snapshot identity immutably through `PreparedSnapshotWrite` and `SnapshotWriteCapability`; Task 2 extends the receipt/abort terminal contracts. Migrate every compile-time caller found by `rg -n 'prepare_mempool_snapshot_write\(|SnapshotPreparationRequest::new\(' packages/open-bitcoin-node`: `network/lifecycle_projection/tests.rs`, `network/tests/lifecycle_projection_cases/effects.rs`, `network/tests/lifecycle_projection_cases/effects/contracts.rs`, `network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs`, and `storage/fjall_store/tests/snapshot_persistence.rs` must all pass explicit deterministic `PolicyTime` and `CheckpointTrigger`. The production definition/facade paths are `network/lifecycle_projection.rs` and `network/runtime_authority/effects.rs`; `network/lifecycle_effects.rs` and `network/runtime_authority/lifecycle.rs` carry/apply the immutable request. Never encode, call Fjall, or read wall-clock time while holding authority.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-04-capture -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features lifecycle_projection_cases::effects -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>A capture test with unequal canonical, historical-local, and authoritative-unbroadcast sets persists exactly the authoritative set.</criterion>
    <criterion>Envelope captured generation equals capability/receipt generation and capture time/trigger survive unchanged.</criterion>
    <criterion>`rg -n 'encode_mempool_snapshot|put_bytes|save_mempool_snapshot' packages/open-bitcoin-node/src/network/runtime_authority` finds no I/O in the authority dispatcher.</criterion>
    <criterion>Exact-function inspection of `SnapshotPreparationRequest::new` and `ManagedNetworkHandle::prepare_mempool_snapshot_write` finds only the two-argument capture-time/trigger signatures; every inventoried snapshot caller passes explicit deterministic values and peer-effect APIs are outside this check.</criterion>
  </acceptance-criteria>
  <done>One guard captures all supported source truth and exports only an affine outside-lock effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add truthful generation, completion, and loss-window evidence</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/tests.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs</files>
  <behavior>
    - Evidence contains current, optional dirty/in-flight/last-durable generations, captured/completed times, trigger, Sync strength, outcome/failure class, and overdue state; every completion/failure time originates outside the authority lock.
    - Successful stale completion advances last-durable high-water to max(existing,captured) but only exact current completion clears matching dirty state.
    - Loss range is exactly `(last_durable_generation,current_generation]`; age uses capture time and saturates at zero on clock rollback.
  </behavior>
  <action>
    Implement D-07 and D-08 with invariant-bearing `CheckpointEvidenceSnapshot`, `CheckpointOutcome`, `CheckpointPersistenceStrength::Sync`, and low-cardinality `SnapshotWriteFailure::{Encode,Storage,AbortDispatch}` types owned by the authoritative network. Because Rust cannot overload the existing Phase 134 zero-argument/untyped snapshot methods still used by the Fjall executor, add deliberately distinct typed transition names in this plan: `SnapshotWriteCapability::acknowledge_checkpoint_write(completed_at: PolicyTime, strength: CheckpointPersistenceStrength) -> SnapshotWriteReceipt`, `ManagedNetworkHandle::abort_checkpoint_snapshot_write(abort: SnapshotWriteAbort)`, and `ManagedNetworkHandle::complete_checkpoint_snapshot_write(receipt: SnapshotWriteReceipt)`. `SnapshotWriteAbort::new(capability, failed_at: PolicyTime, failure: SnapshotWriteFailure)` permits only pre-achievement encode/storage termination. Keep the old snapshot-only `acknowledge_write()`, `abort_snapshot_write(SnapshotWriteCapability)`, and `complete_snapshot_write(SnapshotWriteReceipt)` compatibility path unchanged solely for the existing `storage/fjall_store/mempool.rs` executor until Plan 05; do not overload any same-name method and do not alter the independent peer-effect `acknowledge_write()` API. Plan 05 first migrates every Fjall executor/caller to the distinct typed transition names, then deletes the old snapshot compatibility methods/command variants and renames the typed methods to the final exact names in one atomic compile-safe task. `AbortDispatch` is the fixed external classification when the typed abort command itself cannot dispatch; it must never be used to abort an already achieved receipt. Preparation records one in-flight binding. Exact pre-achievement abort clears only that binding, retains dirty state, and records the supplied encode/storage class and failure time. Completion records achieved durable high-water and the receipt's `completed_at`/Sync strength before freshness classification; duplicate completion is idempotent, stale success never clears newer dirty state, and exact current success clears only its matching dirty generation. Migrate snapshot terminal test callers in `network/tests/lifecycle_projection_cases/effects.rs`, `network/tests/lifecycle_projection_cases/effects/contracts.rs`, and `network/tests/lifecycle_projection_cases/effects/contracts/snapshot_abort.rs` to the distinct typed transition names with deterministic times/classes. Move the existing Fjall-executor-specific cases from `network/tests/lifecycle_projection_cases/effects.rs` into `storage/fjall_store/tests/snapshot_persistence.rs` while the legacy executor remains intact, so Plan 05 has one storage test owner when its return contract changes. Expose a read-only `ManagedNetworkHandle::checkpoint_evidence(now, periodic_interval)` projection that reports current/dirty/in-flight/last-durable, capture/completion, trigger, fixed Sync strength, outcome/failure, overdue, and exact generation range. Make no fixed wall-clock loss bound while pending/failing; temporal age is from capture time.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-04-evidence -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features lifecycle_projection_cases::effects -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Current success, stale success, duplicate success, exact abort, foreign receipt, and completion-dispatch failure each have independent assertions.</criterion>
    <criterion>Stale success advances durable high-water without clearing a newer dirty generation; duplicate cannot regress evidence.</criterion>
    <criterion>Loss-range tests prove `(last_durable,current]`, absent durable generation, equal current/durable, pending/failing no-bound, overdue, and backward-clock saturation.</criterion>
    <criterion>Evidence contains no raw transaction/member identifiers or dynamic labels.</criterion>
    <criterion>Exact impl/function inspection scoped to `SnapshotWriteCapability` and the snapshot methods on `ManagedNetworkHandle` finds the three distinctly named typed transition APIs with explicit time/strength/failure inputs; it intentionally permits the old snapshot compatibility methods until Plan 05 and ignores `PeerEffectCapability::acknowledge_write()` plus all peer-effect callers.</criterion>
    <criterion>An achieved receipt retains its original captured/completed times, trigger, generation, and Sync strength across an injected completion-dispatch failure; the caller still owns that non-Clone receipt for Plan 05's idempotent retry and no abort path accepts it.</criterion>
  </acceptance-criteria>
  <done>Authority exposes exact durability truth across clean, dirty, pending, stale, duplicate, and failed states.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authority capture to outside-lock capability | Immutable snapshot truth leaves the mutex for I/O. |
| Achieved receipt to durability evidence | Late, duplicate, or foreign receipts may target newer state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-135-04-01 | Tampering | Capture coherence | mitigate | Build records, unbroadcast, generation, and time under one guard and validate set membership. |
| T-135-04-02 | Spoofing | Receipt identity | mitigate | Bind epoch, generation, effect ID, snapshot identity, trigger, and capture time. |
| T-135-04-03 | Repudiation | Durable high-water | mitigate | Record achieved generation before freshness and make duplicate completion idempotent. |
| T-135-04-04 | Denial of Service | Authority lock | mitigate | No encode/storage/fsync inside dispatcher; one pending snapshot cap remains enforced. |
| T-135-04-05 | Information Disclosure | Evidence | mitigate | Fixed classes/counts/times/generations only; no raw bytes or member identities. |
</threat-model>

<verification>
Run the timed effect suite, inspect no-I/O structural greps, and run `git diff --check`.
</verification>

<success-criteria>
- D-01, D-05, D-07, D-08, and authority-focused D-14 coverage are complete.
- MPDUR-01 capture and MPDUR-04 evidence use one authority and exact receipt identity.
- No false wall-clock crash-loss bound or lock-across-I/O path exists.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-04-SUMMARY.md`.
</output>
