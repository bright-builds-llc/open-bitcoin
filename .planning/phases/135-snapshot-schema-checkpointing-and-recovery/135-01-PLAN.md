---
phase: 135-snapshot-schema-checkpointing-and-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
autonomous: true
requirements: [MPDUR-01]
must_haves:
  truths:
    - "One mempool-local v2 envelope preserves canonical witness transactions, trustworthy acceptance times, capture generation/time, and the exact authoritative unbroadcast set."
    - "The global storage schema remains unchanged and the only supported legacy path is an explicit fail-closed v1 migration."
    - "Unsupported, corrupt, oversized, or identity-mismatched snapshots fail as whole snapshots without rewriting stored bytes."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
      provides: "Source-only v2 snapshot domain contract and typed snapshot/recovery failures"
      contains: "MempoolSnapshot"
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      provides: "Bounded v2 codec plus explicit v1 compatibility decoder"
      contains: "decode_mempool_snapshot"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs
      to: packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
      via: "validated DTO conversion into invariant-bearing domain types"
      pattern: "TryFrom"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 135-2026-08-02T17-41-48
generated_at: 2026-08-02T18:01:26.808Z
---

<objective>
Define and encode the source-only durable mempool snapshot contract.

Purpose: Establish the irreversible compatibility boundary before recovery or scheduling consumes it.
Output: A bounded mempool-local v2 envelope, explicit v1 migration, typed failures, and codec tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md
@.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md
@packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
@packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs

<interfaces>
Current `MempoolSnapshotRecord` stores transaction plus redundant txid, wtxid, fee, vsize, and metadata, and `network/recovery.rs` still reads those members. This plan is deliberately additive: add the target canonical transaction/acceptance-time and envelope generation/time/unbroadcast contracts while retaining the existing redundant members as verified, non-serialized compatibility inputs so the Wave 1 crate compiles. The v2 codec serializes only canonical transaction bytes plus trustworthy acceptance time; it never serializes those compatibility members. Plan 04 first moves production capture to explicit authority-owned generation/time/unbroadcast inputs, then dependent Plan 02 atomically removes the compatibility members and migrates `network/recovery.rs` to derive txid/wtxid and temporary admission context from the canonical transaction/acceptance time. Keep `VersionedSnapshot&lt;T&gt;` and global `SchemaVersion::CURRENT` unchanged; the discriminator belongs inside the mempool payload. Preserve the existing recovery-status enum in this plan so `network/recovery.rs` remains exhaustive; Plan 02 introduces `DroppedExpired` atomically with every match/count consumer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the invariant-bearing source envelope and failure vocabulary</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/recovery_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs</file>
    <file>packages/open-bitcoin-mempool/src/context.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/bitcoin-knots/src/node/mempool_persist.cpp</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests.rs, packages/open-bitcoin-node/src/network/tests/recovery_cases.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs</files>
  <behavior>
    - V2 contains local format version, captured lifecycle generation, capture PolicyTime, canonical transactions with Known acceptance time, and a separate BTreeSet of MempoolMemberIdentity values.
    - V2 serialization cannot represent fee, vsize, topology, rolling-fee state, peer/cache state, or historical origin/relay metadata as durable authority; Wave 1 in-memory compatibility members are verified but never encoded by v2.
    - Legacy records without known time remain explicitly unknown and cannot be converted to restart time.
  </behavior>
  <action>
    Implement D-01 through D-04 as an additive, compile-safe domain contract in `storage/mempool_snapshot.rs`. Add an invariant-bearing mempool-local version type, captured-generation newtype, authoritative `Transaction` plus `MempoolAcceptanceTime`, explicit captured `PolicyTime`, and a bounded `BTreeSet&lt;MempoolMemberIdentity&gt;` for authoritative unbroadcast membership. Retain the current `MempoolSnapshotRecord::{txid,wtxid,fee_sats,virtual_size,metadata}` members and the parameterless production `MempoolSnapshot::from_mempool` path only as a clearly named/deprecated in-memory compatibility surface through Wave 1: constructors must recompute and verify txid/wtxid and fee/vsize consistency against the transaction, derive acceptance time from metadata without freshening unknown age, and the v2 DTO/encoder in Task 2 must have no fields for these redundant members. Do not migrate `network/runtime_authority/lifecycle.rs` or remove any member read by `network/recovery.rs` in this plan. Plan 04, in the next wave, replaces the production capture call and request with explicit generation, capture `PolicyTime`, trigger, and authoritative unbroadcast membership; only then does Plan 02 remove the deprecated compatibility surface while migrating every recovery read. Add typed snapshot-level failure classes for unsupported version, structural corruption, resource bounds, identity mismatch, and decode failure; keep the record-level recovery-status enum unchanged in this plan so every existing exhaustive match continues to compile. Constructors must reject v2 unknown acceptance times and unbroadcast identities absent from the canonical record set. Preserve v1 unknown age as an explicit compatibility value only. Migrate direct schema fixtures in `storage/snapshot_codec/tests.rs`, `storage/fjall_store/tests.rs`, `network/tests/recovery_cases.rs`, and `network/tests/mempool_lifecycle_cases.rs` to invariant-bearing test constructors while retaining the compatibility members until Plan 02. Plan 02 owns D-10's `DroppedExpired` variant together with `network/recovery.rs` summary and exhaustive-match plumbing. Do not change global `SchemaVersion`, persist rolling state, or infer local/requested intent from metadata.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-01-envelope -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features mempool_snapshot</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'captured_generation|captured_at|unbroadcast_members' packages/open-bitcoin-node/src/storage/mempool_snapshot.rs` finds each required source-envelope contract, while `rg -n 'DroppedExpired' packages/open-bitcoin-node/src/storage/mempool_snapshot.rs` returns no Phase 01 addition.</criterion>
    <criterion>The v2 DTO/encoder contains no `fee_sats`, `virtual_size`, `MempoolOrigin`, or `RelayIntent` field; any same-named Wave 1 member is confined to the explicitly deprecated in-memory compatibility record, verified against canonical transaction data, and scheduled for atomic removal in Plan 02.</criterion>
    <criterion>Tests prove invalid unbroadcast identities and unknown v2 acceptance times cannot construct a valid envelope, while legacy unknown time stays unknown.</criterion>
    <criterion>Direct fixtures in `storage/fjall_store/tests.rs`, `network/tests/recovery_cases.rs`, and `network/tests/mempool_lifecycle_cases.rs` use invariant-bearing builders with deterministic values; the sole remaining parameterless production capture is explicitly inventoried in `network/runtime_authority/lifecycle.rs` for Plan 04 and no other new caller is introduced.</criterion>
    <criterion>The filtered Cargo command compiles all open-bitcoin-node test modules after the additive schema change; `network/recovery.rs` continues to compile against the verified compatibility members and no capture generation/time/unbroadcast value is fabricated.</criterion>
  </acceptance-criteria>
  <done>The domain model can represent exactly the supported durable source truth and cannot represent derived-state persistence as v2 authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded v2 encoding and explicit v1 migration</name>
  <read-first>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-CONTEXT.md</file>
    <file>.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-RESEARCH.md</file>
    <file>AGENTS.md</file>
    <file>standards/core/testing.md</file>
    <file>standards/core/verification.md</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs</file>
    <file>packages/open-bitcoin-core/src/consensus.rs</file>
    <file>packages/bitcoin-knots/test/functional/mempool_persist.py</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/snapshot_codec/mempool.rs, packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs</files>
  <behavior>
    - Encoder always writes local v2 inside the unchanged global v1 wrapper.
    - Decoder preflights byte/count bounds, accepts only local v2 or the exact existing v1 shape, and validates all canonical identities before returning any record.
    - V1 fee/vsize are ignored after shape checks, v1 identities are recomputed, legacy unbroadcast is empty, and rejected bytes remain untouched.
  </behavior>
  <action>
    Implement the D-02 through D-04 compatibility boundary in `snapshot_codec/mempool.rs`. Per D-03, keep the discriminator local to the mempool payload and support only the exact tested v1 migration. Add a local tagged v2 DTO and a narrowly scoped v1 DTO matching the current on-disk shape. Preflight serialized bytes and record/unbroadcast counts through checked `MempoolSnapshotDecodeLimits` before expensive decode/allocation. Encode transactions with witness, recompute txid/wtxid with repository canonical functions, reject duplicates and any unbroadcast identity not in the canonical record map, and validate capture/acceptance times. For v1, verify stored txid/wtxid against decoded transactions, discard stored fee/vsize and origin/relay authority, map only complete known time to known age, preserve all-absent metadata as legacy unknown, and restore an empty unbroadcast set. Return one typed snapshot error without partial records and never clear or rewrite the source key on failure. Keep `SchemaVersion::CURRENT` unchanged.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase135-01-codec -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features snapshot_codec -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Round-trip tests assert v2 capture generation/time, canonical transactions, known ages, and exact unbroadcast membership.</criterion>
    <criterion>Fixtures reject unknown local version, truncated JSON, oversized bytes/counts, duplicate identities, txid/wtxid mismatch, partial v1 metadata, and foreign unbroadcast members as whole-snapshot failures.</criterion>
    <criterion>V1 tests prove fee/vsize are recomputed later, unbroadcast is empty, all-absent time is not freshened, and `SchemaVersion::CURRENT` remains unchanged.</criterion>
  </acceptance-criteria>
  <done>The codec has one current source-only format and one explicit, safe legacy migration with bounded fail-closed decoding.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fjall bytes to DTO | Untrusted/corrupt persisted bytes enter deserialization. |
| Compatibility DTO to domain envelope | Legacy redundant values must not become current authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-135-01-01 | Tampering | Snapshot version/identity | mitigate | Explicit version match and canonical txid/wtxid recomputation reject the full snapshot. |
| T-135-01-02 | Denial of Service | Snapshot decode | mitigate | Checked byte, record, transaction, and unbroadcast limits run before allocations. |
| T-135-01-03 | Elevation of Privilege | Legacy unbroadcast metadata | mitigate | V1 restores no unbroadcast membership and never infers intent from origin/relay fields. |
| T-135-01-04 | Repudiation | Rejected snapshot evidence | mitigate | Stable typed failure class is returned while original stored bytes remain untouched. |
| T-135-01-05 | Information Disclosure | Decode diagnostics | mitigate | Errors expose low-cardinality classes only, never raw transaction or snapshot bytes. |
</threat-model>

<verification>
Run the timed envelope and codec suites sequentially, then `cargo fmt` through the timing wrapper and `git diff --check`.
</verification>

<success-criteria>
- D-01 through D-04 and the codec portion of D-14 are fully implemented.
- MPDUR-01 has a source-only, locally versioned, bounded snapshot contract.
- No global schema migration or deferred runtime import/repair surface is introduced.
</success-criteria>

<output>
After completion, create `.planning/phases/135-snapshot-schema-checkpointing-and-recovery/135-01-SUMMARY.md`.
</output>
