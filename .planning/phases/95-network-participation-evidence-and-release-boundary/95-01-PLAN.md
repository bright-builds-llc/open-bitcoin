---
phase: 95-network-participation-evidence-and-release-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - BOUND-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 95-2026-06-27T12-48-17
generated_at: 2026-06-27T13:27:25.626Z
must_haves:
  truths:
    - "Support bundles preserve inbound resource-governance diagnosis without leaking raw peer, endpoint, payload, permission, config, or credential material."
    - "Resource-governance support redaction happens before JSON and Markdown rendering through the shared support status path."
    - "The redaction summary advertises inbound resource-governance safeguards alongside prior inbound support safeguards."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      provides: "Resource-governance support redaction helper wired from support_status_for_bundle"
      contains: "redact_inbound_resource_governance_evidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "Regression coverage proving JSON and Markdown support output redacts raw resource-governance material"
      contains: "inbound_support_redacts_raw_phase94_resource_governance_material"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      to: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      via: "support_status_for_bundle called by phase77_support_bundle_with_status"
      pattern: "support_status_for_bundle"
    - from: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      to: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      via: "sanitized OpenBitcoinStatusSnapshot rendered to Markdown"
      pattern: "latest_resource_governance_decision"
---

<objective>
Close the narrow BOUND-05 support-bundle gap by redacting free-form Phase 94 resource-governance decision fields before support JSON and Markdown are rendered.

Purpose: Phase 95 must prove useful inbound diagnosis without exposing peer addresses, endpoints, peer IDs, payload bytes, raw permission/config strings, or credentials.
Output: A focused Rust sanitizer plus regression test; no runtime network behavior changes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-network-participation-evidence-and-release-boundary/95-CONTEXT.md
@.planning/phases/95-network-participation-evidence-and-release-boundary/95-RESEARCH.md
@.planning/phases/94-dos-and-resource-governance/94-06-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/support/redaction.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
@packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
@packages/open-bitcoin-node/src/status/inbound.rs

<interfaces>
From packages/open-bitcoin-cli/src/operator/support/redaction.rs:
```rust
pub(crate) fn support_status_for_bundle(
    mut status: OpenBitcoinStatusSnapshot,
) -> OpenBitcoinStatusSnapshot {
    redact_inbound_endpoint_evidence(&mut status.peers.inbound);
    redact_inbound_permission_evidence(&mut status.peers.inbound);
    redact_inbound_address_evidence(&mut status.peers.inbound);
    redact_inbound_peer_policy_evidence(&mut status.peers.inbound);
    status
}
```

From packages/open-bitcoin-node/src/status/inbound.rs:
```rust
pub latest_resource_governance_decision: FieldAvailability<InboundResourceGovernanceEvent>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resource-governance support redaction</name>
  <files>packages/open-bitcoin-cli/src/operator/support/redaction.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/95-network-participation-evidence-and-release-boundary/95-CONTEXT.md
    .planning/phases/95-network-participation-evidence-and-release-boundary/95-RESEARCH.md
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
    packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
    packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test 1: A support bundle with raw strings in latest_resource_governance_decision outcome/reason/label/source/message/next_action renders JSON and Markdown without 127.0.0.1:, 0.0.0.0:, ::1, peer_id=, peer-, raw_endpoint, payload_bytes, raw_permission, permission_string, config=, rpc_password, credential, secret, or cookie=.
    - Test 2: The rendered JSON and Markdown retain a bounded redaction label so diagnosis still shows that resource-governance evidence existed.
    - Test 3: redaction_summary().safeguards includes inbound resource-governance evidence bounded/redacted.
  </behavior>
  <action>Add a failing test named `inbound_support_redacts_raw_phase94_resource_governance_material` near the existing inbound support redaction tests. Then implement `redact_inbound_resource_governance_evidence` in `redaction.rs` and call it from `support_status_for_bundle` after peer-policy redaction. Use a dedicated `REDACTED_RESOURCE_GOVERNANCE_LABEL` and a helper that reuses or extends the existing raw-evidence detector to cover endpoint, peer id, raw endpoint, payload, permission, config, RPC credential, and secret markers. Preserve safe Phase 94 labels such as `invalid_checksum`, `payload_rejected`, and `source_inbound_resource_governance`; redact only fields containing raw material. Add `INBOUND_RESOURCE_GOVERNANCE_REDACTION_SAFEGUARD` to `redaction_summary().safeguards` per D-10 through D-12.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support_redacts_raw_phase94_resource_governance_material -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_support_redaction_names_compact_evidence_bounds -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    grep -n "redact_inbound_resource_governance_evidence" packages/open-bitcoin-cli/src/operator/support/redaction.rs
    grep -n "redacted_resource_governance_evidence" packages/open-bitcoin-cli/src/operator/support/redaction.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "inbound resource-governance evidence bounded/redacted" packages/open-bitcoin-cli/src/operator/support/redaction.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "inbound_support_redacts_raw_phase94_resource_governance_material" packages/open-bitcoin-cli/src/operator/support/tests.rs
  </acceptance_criteria>
  <done>BOUND-05 support redaction preserves bounded resource-governance evidence while removing raw peer, endpoint, payload, permission, config, and credential material from support JSON and Markdown.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused Rust quality checks and refresh LOC</name>
  <files>packages/open-bitcoin-cli/src/operator/support/redaction.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs, docs/metrics/lines-of-code.md</files>
  <read_first>
    AGENTS.md
    standards/core/verification.md
    standards/languages/rust.md
    scripts/verify.sh
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
  </read_first>
  <action>Format and lint the touched Rust support code. If any verifier or hook reports a stale tracked LOC artifact, regenerate it with the repo command printed by `scripts/verify.sh` and keep only the required `docs/metrics/lines-of-code.md` freshness change. Do not edit parity breadcrumbs because this plan modifies existing Rust files only and creates no new first-party Rust source or test file.</action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    cargo fmt --manifest-path packages/Cargo.toml --all --check
    cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast
  </acceptance_criteria>
  <done>Focused Rust support verification passes, and the tracked LOC artifact is current if this plan changed first-party counted files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime status -> support bundle | Runtime status may carry peer-derived strings into locally shareable support artifacts. |
| support JSON -> external reviewer | Operators may share generated support JSON/Markdown outside their machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | I | support_status_for_bundle | mitigate | Redact resource-governance decision fields before JSON and Markdown rendering. |
| T-95-01-02 | R | support redaction tests | mitigate | Add a regression test that injects raw peer, endpoint, payload, permission, config, and credential markers and asserts absence in both render formats. |
</threat_model>

<verification>
Run the focused support tests and clippy commands listed in task verification. Full phase verification is owned by Plan 04 after docs and checker wiring exist.
</verification>

<success_criteria>
BOUND-05 is satisfied for Phase 94 resource-governance support evidence: bounded counters and latest decision presence remain visible, while raw sensitive material is removed from shareable support output.
</success_criteria>

<output>
After completion, create `.planning/phases/95-network-participation-evidence-and-release-boundary/95-01-SUMMARY.md`.
</output>
