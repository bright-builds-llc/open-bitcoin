---
phase: 66-compatibility-harness-operator-wrapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/Cargo.toml
  - packages/open-bitcoin-cli/BUILD.bazel
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/compatibility.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/source-breadcrumbs.json
  - scripts/check-phase66-compatibility-wrapper.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/66-compatibility-harness-operator-wrapper/66-VERIFICATION.md
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-03]
user_setup: []
must_haves:
  truths:
    - "Operators can run `open-bitcoin compatibility harness` through repo-local Cargo and Bazel commands instead of invoking Rust harness internals."
    - "The wrapper writes stable JSON and Markdown reports with peer endpoint, network, negotiated capabilities, failing step, diagnosis, transcript summary, redaction boundaries, and next action."
    - "Wrapper diagnosis scenarios cover compatible, version rejection, network mismatch, service-bit mismatch, unsupported message order, timeout, peer disconnect, malformed payload, and local configuration failure."
    - "Reports are generated through `open-bitcoin-network::evaluate_transcript` so failed peers do not receive useful-progress credit and diagnosis wording aligns with daemon peer-replacement behavior."
    - "Default verification remains deterministic and does not run public-network compatibility probing."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/compatibility.rs"
      provides: "operator compatibility harness wrapper and stable report rendering"
      contains: "CompatibilityHarnessReport"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "end-to-end operator wrapper regression tests"
      contains: "open_bitcoin_compatibility_harness_writes_json_and_markdown_reports"
    - path: "scripts/check-phase66-compatibility-wrapper.ts"
      provides: "deterministic Phase 66 source/docs/default-verification guard"
      contains: "Phase 66 compatibility wrapper checks passed."
    - path: "docs/operator/runtime-guide.md"
      provides: "operator-facing compatibility harness command documentation"
      contains: "Compatibility harness operator wrapper"
    - path: ".planning/phases/66-compatibility-harness-operator-wrapper/66-VERIFICATION.md"
      provides: "COMPAT-01/02/03 verification evidence"
      contains: "status: passed"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/compatibility.rs"
      to: "packages/open-bitcoin-network/src/compatibility.rs"
      via: "CLI report generation delegates diagnosis to the pure compatibility harness"
      pattern: "evaluate_transcript"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase66-compatibility-wrapper.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase66-compatibility-wrapper.ts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 66-2026-06-08T21-58-25
generated_at: 2026-06-08T21:58:25.000Z
---

<objective>
Add a documented operator wrapper around the Phase 54 compatibility transcript harness.

Purpose: COMPAT-01, COMPAT-02, and COMPAT-03 require a stable operator command and local report shape for compatibility diagnosis without broadening default verification into public-network execution.
Output: CLI wrapper, JSON/Markdown reports, tests, docs, deterministic checker, and Phase 66 verification artifact.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-compatibility-harness-operator-wrapper/66-CONTEXT.md
@.planning/phases/66-compatibility-harness-operator-wrapper/66-RESEARCH.md
@.planning/phases/54-peer-compatibility-baseline-and-diagnostic-harness/54-CONTEXT.md
@.planning/phases/54-peer-compatibility-baseline-and-diagnostic-harness/54-01-SUMMARY.md
@.planning/phases/55-outbound-handshake-compatibility-fixes/55-01-SUMMARY.md
@packages/open-bitcoin-network/src/compatibility.rs
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@scripts/check-phase65-support-review.ts
@scripts/verify.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compatibility harness CLI wrapper and report contract</name>
  <read_first>
    - `packages/open-bitcoin-network/src/compatibility.rs`
    - `packages/open-bitcoin-network/src/lib.rs`
    - `packages/open-bitcoin-cli/src/operator.rs`
    - `packages/open-bitcoin-cli/src/operator/runtime.rs`
    - `packages/open-bitcoin-cli/src/operator/support.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/Cargo.toml`
    - `packages/open-bitcoin-cli/BUILD.bazel`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/compatibility.rs, packages/open-bitcoin-cli/src/operator.rs, packages/open-bitcoin-cli/src/operator/runtime.rs, packages/open-bitcoin-cli/Cargo.toml, packages/open-bitcoin-cli/BUILD.bazel</files>
  <behavior>
    - Add `open-bitcoin compatibility harness`.
    - Arguments: `--peer-endpoint <HOST:PORT>`, `--output-dir <PATH>`, and `--scenario <compatible|version-rejected|network-mismatch|service-bit-mismatch|unsupported-message-order|timeout|peer-disconnect|malformed-payload|local-configuration-failure>`.
    - Use global `--network` for the report network and default to `mainnet` when absent.
    - Write `compatibility-harness-report.json` and `compatibility-harness-report.md`.
    - JSON includes `peer_endpoint`, `network`, `scenario`, `diagnosis`, `negotiated_capabilities`, `failing_step`, `transcript_summary`, `redaction_boundaries`, and `next_action`.
    - Markdown includes headings/labels for peer endpoint, network, negotiated capabilities, failing step, diagnosis, transcript summary, redaction boundaries, and next action.
    - Report generation calls `evaluate_transcript`; CLI code only constructs deterministic scenarios and renders stable report DTOs.
  </behavior>
  <action>
    Add a new `operator::compatibility` module with a parity breadcrumb block. Add Clap args for `CompatibilityArgs`, `CompatibilityCommand::Harness`, `CompatibilityHarnessArgs`, and `CompatibilityScenario`. Route `OperatorCommand::Compatibility` in `execute_operator_cli_inner`.

    Implement scenario-to-events helpers for all nine diagnosis values. Use `VersionMessage`, `ServiceFlags`, and `WireNetworkMessage` from `open-bitcoin-network`, plus `NetworkMagic` from `open-bitcoin-primitives` only for the wrong-network wire scenario. Add the needed Cargo and Bazel first-party dependencies.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/operator.rs` contains `Compatibility(CompatibilityArgs)` and `CompatibilityScenario`.
    - `packages/open-bitcoin-cli/src/operator/runtime.rs` routes `OperatorCommand::Compatibility`.
    - `packages/open-bitcoin-cli/src/operator/compatibility.rs` contains `CompatibilityHarnessReport`, `compatibility-harness-report.json`, `compatibility-harness-report.md`, and `evaluate_transcript`.
    - `packages/open-bitcoin-cli/Cargo.toml` contains `open-bitcoin-network` and `open-bitcoin-primitives`.
    - `packages/open-bitcoin-cli/BUILD.bazel` contains `//packages/open-bitcoin-network:open_bitcoin_network_lib` and `//packages/open-bitcoin-primitives:open_bitcoin_primitives_lib`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_compatibility --all-features</automated>
  </verify>
  <done>Operator wrapper creates stable compatibility harness reports through the pure Phase 54 harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add operator-binary tests for report shape and diagnosis coverage</name>
  <read_first>
    - `packages/open-bitcoin-cli/tests/operator_binary.rs`
    - `packages/open-bitcoin-cli/src/operator/compatibility.rs`
    - `.planning/phases/66-compatibility-harness-operator-wrapper/66-CONTEXT.md`
  </read_first>
  <files>packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <behavior>
    - End-to-end binary test runs `open-bitcoin compatibility harness --peer-endpoint 203.0.113.10:8333 --scenario service-bit-mismatch --output-dir <sandbox> --format json`.
    - Test asserts stdout includes JSON and Markdown report paths.
    - Test reads both report files and asserts JSON/Markdown contain the required stable fields.
    - Test asserts diagnosis is `service_bit_mismatch`, failing step is present, useful progress is false, and redaction boundaries omit raw wire payloads/logs/credentials.
    - Test iterates every scenario and proves each required diagnosis string is available through the wrapper.
  </behavior>
  <action>
    Extend `operator_binary.rs` with focused tests named with the `open_bitcoin_compatibility_` prefix. Keep Arrange, Act, Assert comments. Do not use public-network sockets; all inputs remain deterministic.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/tests/operator_binary.rs` contains `open_bitcoin_compatibility_harness_writes_json_and_markdown_reports`.
    - `packages/open-bitcoin-cli/tests/operator_binary.rs` contains every required diagnosis string: `version_rejected`, `network_mismatch`, `service_bit_mismatch`, `unsupported_message_order`, `timeout`, `peer_disconnect`, `malformed_payload`, `local_configuration_failure`.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_compatibility --all-features` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_compatibility --all-features</automated>
  </verify>
  <done>Operator binary tests prove report shape and diagnosis coverage deterministically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document wrapper, add checker, and produce verification</name>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `docs/parity/catalog/p2p.md`
    - `docs/parity/source-breadcrumbs.json`
    - `scripts/check-phase65-support-review.ts`
    - `scripts/verify.sh`
    - `.planning/phases/66-compatibility-harness-operator-wrapper/66-CONTEXT.md`
  </read_first>
  <files>docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md, docs/parity/source-breadcrumbs.json, scripts/check-phase66-compatibility-wrapper.ts, scripts/verify.sh, docs/metrics/lines-of-code.md, .planning/phases/66-compatibility-harness-operator-wrapper/66-VERIFICATION.md</files>
  <behavior>
    - Runtime guide contains a `Compatibility harness operator wrapper` section with repo-local Cargo and Bazel commands for `open-bitcoin compatibility harness`.
    - Docs explain JSON and Markdown report paths, report fields, diagnosis values, redaction boundaries, and opt-in local evidence status.
    - P2P parity docs mention v1.5 compatibility wrapper reports without broadening production-node or inbound/relay claims.
    - New source file has a parity breadcrumb entry in `docs/parity/source-breadcrumbs.json`.
    - Phase 66 checker requires source/docs/test strings and rejects public-network live-smoke/manual-peer commands in default verification.
    - `scripts/verify.sh` runs the checker.
    - `66-VERIFICATION.md` records status `passed` only after focused checks and `bash scripts/verify.sh` pass.
  </behavior>
  <action>
    Add a Bun checker following the Phase 65 pattern. Wire it into `scripts/verify.sh` after the Phase 65 checker. Refresh the tracked LOC report if verification says it is stale. Write the Phase 66 verification artifact after commands pass.
  </action>
  <acceptance_criteria>
    - `docs/operator/runtime-guide.md` contains `Compatibility harness operator wrapper`, `compatibility-harness-report.json`, `compatibility-harness-report.md`, and both Cargo/Bazel command forms.
    - `docs/parity/catalog/p2p.md` contains `v1.5 compatibility harness operator wrapper`.
    - `scripts/check-phase66-compatibility-wrapper.ts` contains `Phase 66 compatibility wrapper checks passed.` and all required diagnosis strings.
    - `scripts/verify.sh` contains `bun run scripts/check-phase66-compatibility-wrapper.ts`.
    - `.planning/phases/66-compatibility-harness-operator-wrapper/66-VERIFICATION.md` contains `status: passed`, `COMPAT-01`, `COMPAT-02`, `COMPAT-03`, and `bash scripts/verify.sh`.
    - `bash scripts/verify.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase66-compatibility-wrapper.ts</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_compatibility --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compatibility --all-features</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Docs, checker, and verification evidence close COMPAT-01/02/03 without default-verification drift.</done>
</task>

</tasks>

<verification>
Run:

```bash
bun run scripts/check-phase66-compatibility-wrapper.ts
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_compatibility --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compatibility --all-features
bash scripts/verify.sh
```
</verification>

<success_criteria>
- [ ] `open-bitcoin compatibility harness` writes stable JSON and Markdown reports.
- [ ] Reports include peer endpoint, network, negotiated capabilities, failing step, diagnosis, transcript summary, redaction boundaries, and next action.
- [ ] Wrapper tests cover every required diagnosis.
- [ ] Operator docs include repo-local Cargo and Bazel command forms.
- [ ] Phase 66 checker is wired into deterministic verification and default verification remains free of public-network commands.
- [ ] `66-VERIFICATION.md` records passed COMPAT-01/02/03 evidence.
</success_criteria>

<threat_model>
| Threat | Surface | Mitigation |
| --- | --- | --- |
| CLI diagnosis drifts from daemon behavior | `operator/compatibility.rs` | Delegate to `open-bitcoin-network::evaluate_transcript`; CLI only builds scenarios and renders reports. |
| Failed peer is credited as progress | Report summary | Preserve `useful_progress` from the pure harness and test failed diagnoses as false. |
| Raw or sensitive data enters reports | JSON/Markdown report | Redaction boundary section states raw wire payloads, logs, credentials, cookies, and wallet material are omitted. |
| Public-network checks enter default verification | `scripts/verify.sh` | Phase 66 checker rejects live-smoke/manual-peer public-network command strings in default verification. |
| Operator mistakes local report for production-node proof | Runtime guide and parity docs | Document wrapper reports as opt-in local compatibility evidence only. |
</threat_model>
