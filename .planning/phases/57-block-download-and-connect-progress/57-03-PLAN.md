---
phase: 57-block-download-and-connect-progress
plan: 03
type: execute
wave: 3
depends_on:
  - 57-02
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/types.rs
  - packages/open-bitcoin-node/src/sync/types/projection.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements: [BLK-02, BLK-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 57-2026-06-03T13-56-54
generated_at: 2026-06-03T14:16:42.622Z
must_haves:
  truths:
    - "Durable sync status separately reports downloaded and connected block heights."
    - "Durable sync status includes block hashes for downloaded and connected progress when available."
    - "Fresh status snapshots can prove before/after first-block progress without deriving it from raw peer activity."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "SyncProgress height and hash fields"
      contains: "maybe_connected_block_hash"
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "downloaded block hash derivation from contiguous best-chain durable bodies"
      contains: "downloaded_block"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "durable status hash regressions"
      contains: "sync_progress_reports_downloaded_and_connected_block_hashes"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "SyncRunSummary projection into SyncProgress"
      pattern: "maybe_downloaded_block_hash|maybe_connected_block_hash"
    - from: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "JSON fields consumed by Plan 04 live-smoke snapshots"
      pattern: "downloaded_block_height|connected_block_height"
---

<objective>
Expose durable block progress evidence that live smoke can quote.

Purpose: BLK-03 requires before/after durable status with block hash and height. The current status model has separate downloaded and connected heights, but the live-smoke snapshots need explicit downloaded and connected hashes too.
Output: additive `SyncProgress` hash fields and deterministic status projection tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md
@.planning/phases/57-block-download-and-connect-progress/57-RESEARCH.md
@.planning/phases/57-block-download-and-connect-progress/57-02-SUMMARY.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@packages/open-bitcoin-node/src/sync/types/projection.rs
@packages/open-bitcoin-node/src/sync/tests.rs

<interfaces>
Extend `SyncProgress` additively:
```rust
pub struct SyncProgress {
    pub header_height: u64,
    pub block_height: u64,
    pub downloaded_block_height: u64,
    pub connected_block_height: u64,
    pub maybe_downloaded_block_hash: Option<String>,
    pub maybe_connected_block_hash: Option<String>,
    pub progress_ratio: f64,
    pub messages_processed: u64,
    pub headers_received: u64,
    pub blocks_received: u64,
}
```

Use lowercase hex strings with no `0x` prefix for both hash fields. Use `None` when no corresponding block body or connected tip is available.

Locked decisions implemented here:
- D-05: persist and surface downloaded block height separately from connected block height.
- D-06: make fresh status snapshots able to report block hash and height for first block progress.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add block hashes to durable sync progress</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync/types.rs, packages/open-bitcoin-node/src/sync/types/summary.rs, packages/open-bitcoin-node/src/sync/types/projection.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/sync/types.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/types/projection.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `.planning/phases/57-block-download-and-connect-progress/57-02-SUMMARY.md`
  </read_first>
  <behavior>
    - Test `sync_progress_reports_downloaded_and_connected_block_hashes`: when a child block body is durable and connected, `sync_progress.value.downloaded_block_height == 1`, `connected_block_height == 1`, and both hash fields are `Some(<child hash hex>)`.
    - Test `sync_progress_reports_downloaded_only_block_hash`: when a contiguous best-chain block body exists but active chainstate has not connected it, `maybe_downloaded_block_hash` is `Some(...)` and `maybe_connected_block_hash` remains the active connected tip hash or `None` if no connected tip exists.
    - Test `sync_progress_omits_block_hashes_when_unavailable`: empty or header-only status serializes both hash fields as JSON null.
  </behavior>
  <action>
    Add `maybe_downloaded_block_hash: Option<String>` and `maybe_connected_block_hash: Option<String>` to `SyncProgress` in `status.rs`. Add matching fields to `SyncRunSummary` if needed so `sync_status(...)`, `metric_samples(...)`, and structured logs keep using one summary source. In `runtime_state.rs`, replace the height-only `downloaded_block_height()` helper with a helper that returns the highest contiguous downloaded best-chain height and block hash; derive connected hash from `network.maybe_chain_tip()`. Serialize hashes as lowercase hex using the same encoding style already used for chain-tip hashes in status/RPC code. Update projection and summary tests so older height semantics stay unchanged and the new hash fields are additive.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_progress --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_downloaded_block_hash|maybe_connected_block_hash" packages/open-bitcoin-node/src/status.rs packages/open-bitcoin-node/src/sync` prints definitions and assignments.
    - `rg -n "sync_progress_reports_downloaded_and_connected_block_hashes|sync_progress_reports_downloaded_only_block_hash|sync_progress_omits_block_hashes_when_unavailable" packages/open-bitcoin-node/src/sync/tests.rs packages/open-bitcoin-node/src/status.rs` prints all three test names.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_progress --all-features` exits 0.
  </acceptance_criteria>
  <done>Durable sync status can identify both the height and hash of downloaded and connected block progress.</done>
</task>

<task type="auto">
  <name>Task 2: Keep projections and existing operator status compatibility green</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/sync/types/projection.rs, packages/open-bitcoin-node/src/sync/types/summary.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/types/projection.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
  </read_first>
  <action>
    Update every existing `SyncProgress { ... }` literal in first-party Rust tests and projections to include `maybe_downloaded_block_hash` and `maybe_connected_block_hash`. Do not remove or rename the compatibility alias `block_height`; it must continue to equal connected block height. Do not widen this plan into dashboard redesign. If CLI/dashboard human renderers continue to compile without changes, leave them alone; if the new fields require explicit handling, render missing hashes as `Unavailable` rather than omitting height fields.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "block_height: self\\.best_block_height|connected_block_height: self\\.best_block_height" packages/open-bitcoin-node/src/sync/types/summary.rs` confirms the compatibility alias remains connected height.
    - `rg -n "maybe_downloaded_block_hash:|maybe_connected_block_hash:" packages/open-bitcoin-node/src/status.rs packages/open-bitcoin-node/src/sync/tests.rs` prints updated literals.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status --all-features` exits 0.
  </acceptance_criteria>
  <done>Existing status consumers remain compatible while new hash fields are available for Plan 04 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable store -> status projection | Stored block bodies and active chainstate are converted into operator-visible evidence. |
| status projection -> live-smoke report | Plan 04 will treat these fields as evidence for first block progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-03-01 | Tampering | `downloaded_block` status derivation | high | mitigate | Derive downloaded hash only from contiguous best-chain durable block bodies, not arbitrary stored blocks. |
| T-57-03-02 | Repudiation | before/after sync status evidence | high | mitigate | Add explicit downloaded and connected hash fields so reports can cite exact block identity. |
| T-57-03-03 | Information Disclosure | status JSON | low | accept | Block hashes and heights are public chain data; no peer secrets, cookies, wallet data, or raw logs are added. |
| T-57-03-04 | Denial of Service | status projection scan | medium | mitigate | Keep downloaded progress derivation bounded to current best-chain entries already tracked by the runtime; do not scan unbounded unrelated store namespaces. |
</threat_model>

<verification>
Run targeted checks after the tasks:

```bash
cargo fmt --all --manifest-path packages/Cargo.toml
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_progress --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status --all-features
bun run scripts/check-parity-breadcrumbs.ts --check
```

Full repo verification is the phase-final gate in Plan 04.
</verification>

<success_criteria>
BLK-03 is partially covered when durable status can provide before/after downloaded and connected block hash/height evidence. Plan 04 completes BLK-03 by writing the live-smoke report derivation.
</success_criteria>

<output>
After completion, create `.planning/phases/57-block-download-and-connect-progress/57-03-SUMMARY.md`.
</output>
