---
phase: 57-block-download-and-connect-progress
plan: 04
type: execute
wave: 4
depends_on:
  - 57-03
files_modified:
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
autonomous: true
requirements: [BLK-03, BLK-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 57-2026-06-03T13-56-54
generated_at: 2026-06-03T14:16:42.622Z
must_haves:
  truths:
    - "Opt-in live-smoke reports include first block progress with before/after fresh status snapshots."
    - "Connected-chain block progress is required for Phase 57 pass; header-only or downloaded-only progress receives a typed diagnosis."
    - "Operator docs and P2P parity catalog describe the bounded block-progress claim without expanding public-network verification or production-node scope."
  artifacts:
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "firstBlockProgress report schema and block-specific no-progress diagnosis"
      contains: "firstBlockProgress"
    - path: "scripts/test-run-live-mainnet-smoke.sh"
      provides: "deterministic smoke report checks"
      contains: "awaiting_blocks"
    - path: "docs/operator/runtime-guide.md"
      provides: "operator guidance for Phase 57 evidence"
      contains: "result.firstBlockProgress"
    - path: "docs/parity/catalog/p2p.md"
      provides: "bounded P2P block download/connect parity statement"
      contains: "bounded daemon block download"
  key_links:
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "openbitcoinsyncstatus"
      via: "fresh status snapshots parsed into downloaded/connected block fields"
      pattern: "downloadedBlockHeight|connectedBlockHeight"
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "packages/open-bitcoin-node/src/sync/types.rs"
      via: "peer failure reason strings mapped to NoProgressCause"
      pattern: "peer_notfound|invalid_block|disconnected_block|duplicate_or_disconnected_block|resource_limit"
---

<objective>
Produce Phase 57 live-smoke evidence or a typed block-progress diagnosis.

Purpose: BLK-03 requires first validated block connection evidence with peer endpoint, block hash, height, timestamp, and before/after durable status, or a typed diagnosis when no block connection occurs. BLK-04 requires no-credit block response paths to stay visible in reports.
Output: additive live-smoke schema/rendering/tests plus operator/parity docs for the bounded claim.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md
@.planning/phases/57-block-download-and-connect-progress/57-RESEARCH.md
@.planning/phases/57-block-download-and-connect-progress/57-03-SUMMARY.md
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md

<interfaces>
Extend the live-smoke report result additively:
```ts
type FirstBlockProgressKind = "downloaded" | "connected";

type FirstBlockProgressEvidence = {
  after: SyncStatusSnapshot;
  before: SyncStatusSnapshot;
  blockHash: string | null;
  height: number;
  kind: FirstBlockProgressKind;
  maybeLastActivityUnixSeconds: number | null;
  maybePeer: string | null;
  maybeResolvedEndpoint: string | null;
  maybeSource: string | null;
  observedAtUnixSeconds: number;
};
```

Add these `NoProgressCause` strings:
`awaiting_blocks`, `peer_notfound`, `malformed_block`, `invalid_block`, `duplicate_or_disconnected_block`, `resource_limit`.

Locked decisions implemented here:
- D-06: `result.firstBlockProgress` mirrors Phase 56 `firstHeaderProgress` with fresh before/after status snapshots.
- D-08: no block progress yields block-specific diagnosis and next operator action.
- D-09: public mainnet smoke remains opt-in and outside `bash scripts/verify.sh`.
- D-10 and D-11: do not add restart/resume proof, support bundle closeout, threat-model closeout, or release-boundary copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add firstBlockProgress and block-specific diagnoses to live smoke</name>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <read_first>
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
    - `packages/open-bitcoin-node/src/status.rs`
    - `.planning/phases/57-block-download-and-connect-progress/57-03-SUMMARY.md`
  </read_first>
  <behavior>
    - Connected block progress fixture writes JSON containing `"firstBlockProgress"` with `"kind": "connected"`, `"height": 1`, a non-null `"blockHash"`, and before/after snapshots whose connected block height increases.
    - Download-only fixture writes JSON containing `"firstBlockProgress"` with `"kind": "downloaded"` but keeps `"status": "no_progress"` and `"maybeNoProgressCause": "awaiting_blocks"` because connected height did not increase.
    - Header-only fixture keeps `result.firstHeaderProgress` but reports `"status": "no_progress"` and `"maybeNoProgressCause": "awaiting_blocks"` for Phase 57.
    - Peer failure fixtures map `block_notfound` to `peer_notfound`, `malformed_block` to `malformed_block`, `invalid_block` to `invalid_block`, `duplicate_block`, `disconnected_block`, and `non_extending_block` to `duplicate_or_disconnected_block`, and `resource_limit` to `resource_limit`.
  </behavior>
  <action>
    In `scripts/run-live-mainnet-smoke.ts`, extend `SyncStatusSnapshot`, `FinalStatusSummary`, and `RuntimeMetadataJson.sync.sync_progress` to parse `downloaded_block_height`, `connected_block_height`, `maybe_downloaded_block_hash`, and `maybe_connected_block_hash`, while preserving `blockHeight` as the connected-height compatibility alias. Add `FirstBlockProgressEvidence` and `firstBlockProgressEvidence(...)` beside `firstHeaderProgressEvidence(...)`. Track first downloaded and first connected block progress snapshots separately. Set `result.status = "passed"` only when connected block height increases beyond the initial snapshot; if only header progress or downloaded-only progress occurs, preserve the evidence fields but keep the report as `no_progress` with a block-specific cause. Extend `NoProgressCause`, `classifyNoProgressCause`, `noProgressCauseFromPeer`, and `nextActionForCause` with the exact strings listed in the interface block, including mapping durable peer failure reason `disconnected_block` to `duplicate_or_disconnected_block` unless a more explicit connected no-credit report cause is introduced in the same task with matching docs/tests. Render Markdown line `- First block progress: ...` with kind, height, block hash, peer, source, endpoint, and observed timestamp, or `Unavailable`.
  </action>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "type FirstBlockProgressEvidence|firstBlockProgressEvidence|downloadedBlockHeight|connectedBlockHeight|awaiting_blocks|peer_notfound|disconnected_block|duplicate_or_disconnected_block|resource_limit" scripts/run-live-mainnet-smoke.ts` prints all listed markers.
    - `rg -n "\\\"firstBlockProgress\\\"|\\\"kind\\\": \\\"connected\\\"|\\\"maybeNoProgressCause\\\": \\\"awaiting_blocks\\\"|\\\"disconnected_block\\\"|\\\"duplicate_or_disconnected_block\\\"|First block progress" scripts/test-run-live-mainnet-smoke.sh` prints all listed fixture checks, including disconnected mapping coverage.
    - `bash scripts/test-run-live-mainnet-smoke.sh` exits 0.
    - `bun run scripts/run-live-mainnet-smoke.ts --help` exits 0 and prints `Usage:`.
  </acceptance_criteria>
  <done>Live smoke can prove first connected block progress or produce a typed block-progress diagnosis from deterministic fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Document bounded Phase 57 evidence without broadening scope</name>
  <files>docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md</files>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `docs/parity/catalog/p2p.md`
    - `.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md`
    - `.planning/phases/57-block-download-and-connect-progress/57-RESEARCH.md`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>
    Update `docs/operator/runtime-guide.md` to describe `result.firstBlockProgress`, including `kind: "downloaded" | "connected"`, `height`, `blockHash`, `observedAtUnixSeconds`, `before`, `after`, `maybePeer`, `maybeSource`, and `maybeResolvedEndpoint`. State exactly that Phase 57 pass evidence requires `kind: "connected"` and a connected block height increase; downloaded-only evidence is useful but diagnosed as `awaiting_blocks` until active chainstate advances. Add block-specific no-progress causes `awaiting_blocks`, `peer_notfound`, `malformed_block`, `invalid_block`, `duplicate_or_disconnected_block`, and `resource_limit` with one operator action each, and state that durable peer reason `disconnected_block` maps to the duplicate/disconnected no-credit diagnosis. Update `docs/parity/catalog/p2p.md` to add a coverage bullet for bounded daemon block download and first validated block connect, and a mirrored-behavior bullet for Knots-style `getdata`/`notfound` attribution plus disconnected no-credit handling. Do not add restart/resume proof, support bundle closeout, release-readiness claims, inbound serving, transaction relay, unattended production-node operation, or public-network checks to `bash scripts/verify.sh`.
  </action>
  <verify>
    <automated>rg -n "result\\.firstBlockProgress|kind: \\\"connected\\\"|awaiting_blocks|peer_notfound|disconnected_block|bounded daemon block download|public-network checks remain opt-in" docs/operator/runtime-guide.md docs/parity/catalog/p2p.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "result\\.firstBlockProgress|downloaded-only evidence|awaiting_blocks|peer_notfound|malformed_block|invalid_block|duplicate_or_disconnected_block|disconnected_block|resource_limit" docs/operator/runtime-guide.md` prints all listed strings.
    - `rg -n "bounded daemon block download|first validated block connect|getdata.*notfound|disconnected no-credit|public-network checks remain opt-in" docs/parity/catalog/p2p.md` prints all listed strings.
    - `rg -n "restart/resume proof|support bundle closeout|unattended production-node" docs/operator/runtime-guide.md docs/parity/catalog/p2p.md` does not show new Phase 57 claim language beyond explicit deferral/scope-boundary statements.
  </acceptance_criteria>
  <done>Operator and parity docs explain the Phase 57 evidence surface and preserve deferred Phase 58/59 and production-scope boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh daemon status -> live-smoke report | Runtime status is converted into local JSON/Markdown evidence. |
| durable peer telemetry -> diagnosis | Peer failure reason strings drive next-action guidance. |
| operator command -> public network | Live smoke is opt-in and must stay outside deterministic verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-04-01 | Repudiation | `result.firstBlockProgress` | high | mitigate | Include before/after fresh status snapshots, block hash, height, timestamp, and peer/source/endpoint fields. |
| T-57-04-02 | Tampering | block no-progress diagnosis | high | mitigate | Map exact durable peer failure strings, including `disconnected_block`, to block-specific causes; do not infer pass from raw peer activity. |
| T-57-04-03 | Denial of Service | public live smoke | medium | mitigate | Keep timeout/poll controls and resource-limit diagnosis; do not add public-network smoke to `bash scripts/verify.sh`. |
| T-57-04-04 | Information Disclosure | local report output | medium | mitigate | Keep reports local under `packages/target/...`; do not embed credentials, cookies, wallet material, or raw unbounded logs. |
| T-57-04-05 | Spoofing | peer endpoint attribution | medium | mitigate | Treat peer endpoint/source as evidence labels from durable telemetry, not authentication of peer identity. |
</threat_model>

<verification>
Run the phase-final verification after the tasks:

```bash
cargo fmt --all --manifest-path packages/Cargo.toml
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_response --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_progress --all-features
bash scripts/test-run-live-mainnet-smoke.sh
bun run scripts/run-live-mainnet-smoke.ts --help
bun run scripts/check-parity-breadcrumbs.ts --check
bash scripts/verify.sh
```
</verification>

<success_criteria>
BLK-03 is complete when the live-smoke report records `result.firstBlockProgress` for connected block progress or a typed block-specific diagnosis when no connection occurs. BLK-04 is complete when live-smoke fixtures show no-credit peer outcomes remain visible and uncredited.
</success_criteria>

<output>
After completion, create `.planning/phases/57-block-download-and-connect-progress/57-04-SUMMARY.md`.
</output>
