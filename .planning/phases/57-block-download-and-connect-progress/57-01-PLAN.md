---
phase: 57-block-download-and-connect-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/sync/block_reconcile.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements: [BLK-01, BLK-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 57-2026-06-03T13-56-54
generated_at: 2026-06-03T14:16:42.622Z
must_haves:
  truths:
    - "Daemon sync requests block bodies only for validated best-chain headers that are missing locally."
    - "Per-peer and total in-flight block caps prevent duplicate requests."
    - "Block in-flight entries are released on block, notfound, invalid data, malformed data, and peer disconnect cleanup without advancing chainstate."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/block_reconcile.rs"
      provides: "best-chain-only bounded block request scheduling"
      contains: "request_missing_blocks"
    - path: "packages/open-bitcoin-node/src/sync.rs"
      provides: "peer-session in-flight cleanup around the sync shell"
      contains: "peer_requested_blocks"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "deterministic request-cap and cleanup regressions"
      contains: "bounded_block_requests"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/block_reconcile.rs"
      to: "packages/open-bitcoin-node/src/network.rs"
      via: "ManagedPeerNetwork::best_chain_entries and request_missing_blocks"
      pattern: "best_chain_entries\\(\\).*request_missing_blocks"
    - from: "packages/open-bitcoin-node/src/sync.rs"
      to: "packages/open-bitcoin-node/src/sync/block_reconcile.rs"
      via: "release_inflight_for_message plus peer disconnect cleanup"
      pattern: "release_inflight_for_message"
---

<objective>
Prove the bounded block request contract for Phase 57.

Purpose: BLK-01 and the request-cleanup half of BLK-04 require the daemon to ask for block bodies only after validated best-chain header acceptance, while keeping `sync.max_blocks_in_flight_per_peer` and `sync.max_blocks_in_flight_total` authoritative.
Output: deterministic Rust tests and tightened sync-shell scheduling/cleanup around `block_reconcile::request_missing_blocks`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md
@.planning/phases/57-block-download-and-connect-progress/57-RESEARCH.md
@.planning/phases/56-header-ibd-convergence/56-01-SUMMARY.md
@packages/open-bitcoin-node/src/sync/block_reconcile.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-network/src/peer.rs
@packages/bitcoin-knots/src/net_processing.cpp

<interfaces>
Existing request path to preserve:
```rust
pub(super) fn request_missing_blocks(
    runtime: &mut DurableSyncRuntime,
    peer_id: PeerId,
) -> Result<Vec<WireNetworkMessage>, SyncRuntimeError>;
```

Existing cleanup hooks to tighten:
```rust
pub(super) fn release_inflight_for_message(
    runtime: &mut DurableSyncRuntime,
    message: &WireNetworkMessage,
);

pub fn peer_requested_blocks(&self, peer_id: PeerId) -> ManagedResult<Vec<BlockHash>>;
```

Existing locked decisions implemented here:
- D-01: schedule blocks only from validated best-chain headers not known locally.
- D-02: enforce `sync.max_blocks_in_flight_per_peer` and `sync.max_blocks_in_flight_total`; release entries on `block`, `notfound`, invalid data, malformed data, disconnect, and end-of-peer-session cleanup.
- D-03: keep block scheduling additive to the Phase 54-56 header-first sync loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove best-chain-only bounded block scheduling</name>
  <files>packages/open-bitcoin-node/src/sync/block_reconcile.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/block_reconcile.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-network/src/peer.rs`
    - `.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md`
    - `.planning/phases/57-block-download-and-connect-progress/57-RESEARCH.md`
    - `packages/bitcoin-knots/src/net_processing.cpp`
  </read_first>
  <behavior>
    - Test `bounded_block_requests_use_validated_best_chain_headers_only`: accepted best-chain headers produce `WireNetworkMessage::GetData`, while active-chain hashes, durable local block bodies, runtime in-flight hashes, and unvalidated inventory do not.
    - Test `bounded_block_requests_respect_per_peer_and_total_caps`: with `max_blocks_in_flight_per_peer = 1` and `max_blocks_in_flight_total = 2`, one peer never receives more than one requested block and the runtime never records more than two global in-flight block hashes.
  </behavior>
  <action>
    Add the two tests above before changing production code. Keep the production scheduling path in `block_reconcile::request_missing_blocks`: compute `available_global` from `runtime.config.max_blocks_in_flight_total.saturating_sub(runtime.inflight_blocks.len())`; iterate only `runtime.network.best_chain_entries()`; skip hashes present in active chain, `runtime.inflight_blocks`, or `runtime.store.load_block(...)`; call `runtime.network.note_local_block_hash(...)` for durable local bodies; call `runtime.network.request_missing_blocks(peer_id, &requested)`; insert into `runtime.inflight_blocks` only for emitted `WireNetworkMessage::GetData` inventory whose type is `InventoryType::Block` or `InventoryType::WitnessBlock`. Do not add direct `inv`-to-block-body scheduling and do not switch daemon sync to `HeaderSyncPolicy::HeadersAndBlocks` for this phase.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_block_requests --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bounded_block_requests_use_validated_best_chain_headers_only|bounded_block_requests_respect_per_peer_and_total_caps" packages/open-bitcoin-node/src/sync/tests.rs` prints both test names.
    - `rg -n "best_chain_entries\\(\\)|max_blocks_in_flight_total|runtime\\.inflight_blocks\\.insert|InventoryType::WitnessBlock" packages/open-bitcoin-node/src/sync/block_reconcile.rs` prints all four scheduling markers.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_block_requests --all-features` exits 0.
  </acceptance_criteria>
  <done>Validated best-chain headers are the only block request candidates, and request count never exceeds the documented per-peer or total caps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove in-flight release on no-credit cleanup paths</name>
  <files>packages/open-bitcoin-node/src/sync/block_reconcile.rs, packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/block_reconcile.rs`
    - `packages/open-bitcoin-node/src/sync.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-network/src/peer.rs`
    - `.planning/phases/57-block-download-and-connect-progress/57-CONTEXT.md`
  </read_first>
  <behavior>
    - Test `notfound_releases_block_inflight_for_retry`: a requested best-chain block reported through `WireNetworkMessage::NotFound` is removed from runtime in-flight state and can be requested from a later peer without advancing `downloaded_block_height` or `connected_block_height`.
    - Test `disconnect_clears_runtime_and_peer_block_inflight`: when a peer session ends with outstanding requested blocks, both `runtime.inflight_blocks` and `PeerManager::requested_blocks` stop suppressing future legitimate requests.
    - Test `invalid_block_releases_runtime_and_peer_inflight_for_retry`: when a requested best-chain block body fails validation as invalid data, the runtime global `inflight_blocks` entry is removed, the peer-manager requested state for that hash is cleared or released, `connected_block_height` is unchanged, and a later eligible peer can request the same block hash.
    - Test `malformed_block_releases_runtime_and_peer_inflight_for_retry`: when malformed block data terminates or errors the peer receive path, cleanup removes the hash from runtime global `inflight_blocks` and peer-manager requested state before a later eligible peer retries; if malformed payload handling cannot identify a block hash directly, route this assertion through end-of-session cleanup while still proving both trackers release the originally requested hash.
  </behavior>
  <action>
    Add the four tests above. Keep `block_reconcile::release_inflight_for_message` releasing `WireNetworkMessage::Block` and `WireNetworkMessage::NotFound` inventory entries for `InventoryType::Block` and `InventoryType::WitnessBlock`. In `DurableSyncRuntime::sync_connected_peer`, release the requested block hash from both `self.inflight_blocks` and the managed peer requested-block state when a requested block is rejected as invalid data; do not leave cleanup only to telemetry. For malformed receive errors where no valid block hash can be parsed from the payload, use the end-of-session cleanup path that reads `self.network.peer_requested_blocks(peer_id)` and removes each hash from `self.inflight_blocks` before `self.network.disconnect_peer(peer_id)`, then assert peer-manager requested state is cleared by disconnect. If the tests expose a stale per-peer request map, adjust only the invalid/malformed/session cleanup paths needed to make later peers eligible for the same missing best-chain block.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_inflight --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "notfound_releases_block_inflight_for_retry|disconnect_clears_runtime_and_peer_block_inflight|invalid_block_releases_runtime_and_peer_inflight_for_retry|malformed_block_releases_runtime_and_peer_inflight_for_retry" packages/open-bitcoin-node/src/sync/tests.rs` prints all four test names.
    - `rg -n "WireNetworkMessage::NotFound|peer_requested_blocks\\(peer_id\\)|self\\.inflight_blocks\\.remove|InvalidData|MalformedBlock|malformed" packages/open-bitcoin-node/src/sync/block_reconcile.rs packages/open-bitcoin-node/src/sync.rs packages/open-bitcoin-node/src/sync/types.rs` prints cleanup and failure-path markers.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_inflight --all-features` exits 0.
  </acceptance_criteria>
  <done>`notfound`, invalid data, malformed data, and disconnect cleanup release requested block hashes from runtime and peer-manager state, allowing future eligible peers to retry without downloaded or connected progress credit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public peer -> sync shell | Untrusted peers can omit requested data, send `notfound`, disconnect, send invalid blocks, send malformed block data, or stall while block requests are in flight. |
| sync shell -> durable status | Runtime request state must not become durable false progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01-01 | Tampering | `block_reconcile::request_missing_blocks` | high | mitigate | Request only hashes from `runtime.network.best_chain_entries()` and skip active/local/in-flight hashes per D-01. |
| T-57-01-02 | Denial of Service | in-flight block tracking | high | mitigate | Enforce `max_blocks_in_flight_per_peer` in `PeerManager` and `max_blocks_in_flight_total` in `DurableSyncRuntime`; deterministic tests assert the caps. |
| T-57-01-03 | Repudiation | `notfound`, invalid, malformed, and disconnect cleanup | medium | mitigate | Release runtime and peer-manager requested hashes on `notfound`, invalid block data, malformed receive cleanup, and session cleanup, with tests proving no false progress credit. |
| T-57-01-04 | Information Disclosure | opt-in public peer endpoints | low | accept | This plan does not add live-network execution or new endpoint storage; public-network checks remain outside `bash scripts/verify.sh` per D-09. |
</threat_model>

<verification>
Run targeted checks after the tasks:

```bash
cargo fmt --all --manifest-path packages/Cargo.toml
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_block_requests --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_inflight --all-features
bun run scripts/check-parity-breadcrumbs.ts --check
```

Full repo verification is the phase-final gate in Plan 04.
</verification>

<success_criteria>
BLK-01 is covered when bounded best-chain block requests are deterministic and cap-checked. The request-cleanup portion of BLK-04 is covered when `notfound`, invalid data, malformed data, and disconnect paths release in-flight state without downloaded or connected progress.
</success_criteria>

<output>
After completion, create `.planning/phases/57-block-download-and-connect-progress/57-01-SUMMARY.md`.
</output>
