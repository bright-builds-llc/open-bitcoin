---
phase: 129-integration-guardrails-and-milestone-reconciliation
plan: "03"
type: execute
wave: 3
depends_on:
  - "129-01"
  - "129-02"
files_modified:
  - scripts/check-phase124-post-audit-gap-planning.ts
  - scripts/check-phase124-archive-ready.ts
  - scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
  - scripts/check-phase124-milestone-closeout-reconciliation.test.ts
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - HARD-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 129-2026-07-20T19-28-06
generated_at: 2026-07-20T20:17:00Z
must_haves:
  truths:
    - "The Phase 124 post-audit stage machine accepts exactly three Phase 129 states — gaps-open, verified pre-promotion, and reconciled archive-ready — and rejects every mixture (audit passed with a Pending checkbox, 39/39 without lifecycle-valid verification, promotion without a gsd-verifier verification)."
    - "Today's gaps-open assertions are preserved bit-for-bit, so the repo still verifies green immediately after this plan lands with no planning-artifact changes."
    - "HARD-05 ownership stays pinned to Phase 129 in every state; the legacy Phase 124 final-audit path stays unreachable."
    - "MILESTONES.md and PROJECT.md are guarded in the archive-ready state so the stale-routing drift that motivated HARD-05 cannot recur."
  artifacts:
    - path: scripts/check-phase124-archive-ready.ts
      provides: "Archive-ready and verified-pre-promotion stage assertions for Phase 129"
      contains: "verifyArchiveReady"
    - path: scripts/check-phase124-post-audit-gap-planning.ts
      provides: "Stage detection dispatching to the archive-ready module"
      contains: "check-phase124-archive-ready"
    - path: scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
      provides: "maybePhase129Stage fixture projections"
      contains: "maybePhase129Stage"
  key_links:
    - from: scripts/check-phase124-post-audit-gap-planning.ts
      to: scripts/check-phase124-archive-ready.ts
      via: "stage-dispatch import inside verifyPostAuditGapPlanningStage"
      pattern: "from \"\\./check-phase124-archive-ready\""
    - from: scripts/check-phase124-milestone-closeout-reconciliation.test.ts
      to: scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
      via: "createFixture with maybePhase129Stage"
      pattern: "maybePhase129Stage"
---

<objective>
Evolve the Phase 124 post-audit stage machine (D-08, D-09, D-10) so the reconciled archive-ready end-state that Plan 04 will produce is legal, fail-closed, and mixture-rejecting — while preserving today's gaps-open assertions bit-for-bit so this plan commits green before any planning artifact moves.

Purpose: HARD-05 requires roadmap, requirement coverage, phase status, and the milestone audit to agree and route v2.1 to archival; the stage machine is the deterministic guard that makes that agreement fail closed and prevents the MILESTONES.md-style drift from recurring.

Output: A new `scripts/check-phase124-archive-ready.ts` sibling module, stage dispatch inside `verifyPostAuditGapPlanningStage`, extended synthetic fixtures with `maybePhase129Stage`, and mutation tests for every new acceptance and rejection.

CRITICAL constraints for this plan:
- Do NOT modify any `.planning/*` top-level artifact (ROADMAP, REQUIREMENTS, PROJECT, STATE, MILESTONES, v2.1-MILESTONE-AUDIT) — the repo must remain in the current gaps-open state when this plan commits, and the evolved checker must pass against it.
- Keep `isPostAuditGapPlanningStage` and the dispatcher `check-phase124-milestone-closeout-reconciliation.ts` unchanged (Pitfall 2); the new stages live inside `verifyPostAuditGapPlanningStage` via the sibling module (research: the host file is 690 lines, past the ~628-line refactor trigger — new logic goes in the sibling, per the gap-closure module precedent).
- Do NOT remove or weaken any existing assertion for Phases 127/128 lifecycle, requirement projection, coverage, topology, audit, or routing in the gaps-open state.
- Prefer one commit for the whole plan (~5-6 min verify per commit).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/languages/typescript-javascript.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-CONTEXT.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-RESEARCH.md

<interfaces>
Existing stage machinery in scripts/check-phase124-post-audit-gap-planning.ts (all verified):

```typescript
type Phase128LifecycleStage = "planned" | "executing_plan_04" | "complete";
export function isPostAuditGapPlanningStage(roadmap: string): boolean; // keys on "#### Phase 127/128/129" headings — DO NOT CHANGE
export function verifyPostAuditGapPlanningStage(repoRoot: string, requirements: string, ...): void; // host for the new dispatch
// Helpers duplicated per checker by repo convention (copy, don't share):
// extractFrontmatter, exactScalar, requireContains, requireAbsent, requireCount,
// parseRequirementEntries (/^- \[([ x])\] \*\*([A-Z]+-\d+)\*\*:/gm), parseTraceabilityEntries, phaseSection
```

Fixture builder in scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts:
```typescript
export const PHASE129_ROUTE = "/gsd-plan-phase 129";
// createFixture(tempRoots, options) already models requirements/roadmap/state/audit/project/milestones
// per stage and creates phase dirs + lifecycle artifacts; roadmap fixtures pin
// "**Plans:** 0 plans" for Phase 129 and "**Execution Order:** 110 -> 126 -> 127 -> 128 -> 129".
```

Lifecycle-chain precedent to model Phase 129 lifecycle on: `verifyPhase127Lifecycle` (same file, ~lines 392-536): exact plan set, contiguous summary prefix, at most one verification with `status: passed`, `generated_by` chain (`gsd-discuss-phase` context → `gsd-plan-phase` plans → `gsd-execute-plan` summaries → `gsd-verifier` verification), pinned `phase_lifecycle_id`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the three-state Phase 129 stage machine in a sibling module</name>
  <files>scripts/check-phase124-archive-ready.ts, scripts/check-phase124-post-audit-gap-planning.ts</files>
  <read_first>
    scripts/check-phase124-post-audit-gap-planning.ts
    scripts/check-phase124-milestone-closeout-reconciliation.ts
    scripts/check-phase124-milestone-gap-closure.ts (stage-dispatch and module-split precedent only)
    .planning/v2.1-MILESTONE-AUDIT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Gaps-open (current repo state, no Phase 129 verification, audit gaps_found, 36/39): checker passes with today's assertions unchanged.
    - Verified pre-promotion (129-VERIFICATION.md present with status: passed + lifecycle_validated: true, artifacts otherwise gaps-open): checker passes.
    - Archive-ready (all end-state fields below simultaneously): checker passes.
    - Every mixture fails: audit passed + any of OBS-01/BOUND-02/HARD-05 unchecked; REQUIREMENTS 39/39 + verification absent or not lifecycle-valid; roadmap row [x] + audit gaps_found; verification passed + roadmap Next Step already `/gsd-complete-milestone v2.1` while audit gaps_found; HARD-05 traceability owner not Phase 129.
  </behavior>
  <action>
This task implements locked decisions D-08, D-09, D-10.

1. Create `scripts/check-phase124-archive-ready.ts` exporting:
   - `type Phase129ReconciliationStage = "gaps_open" | "verified_pre_promotion" | "archive_ready"`.
   - `detectPhase129ReconciliationStage(repoRoot, failures): Phase129ReconciliationStage` — detection keyed on artifact evidence, fail-closed on mixtures:
     - Read `.planning/v2.1-MILESTONE-AUDIT.md` frontmatter `status` via copied `extractFrontmatter`/`exactScalar` helpers; read `.planning/REQUIREMENTS.md` checked state for OBS-01/BOUND-02/HARD-05 via copied `parseRequirementEntries`; read the Phase 129 roadmap row checkbox; read `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md` if present.
     - If audit `status: passed` OR any of the three requirements is checked OR the roadmap Phase 129 row is `- [x]` → the claimed stage is `archive_ready`: ALL archive-ready conditions (step 2) must then hold; any missing condition is a failure (mixture rejection).
     - Else if a `129-VERIFICATION.md` exists → `verified_pre_promotion`: require frontmatter exactly `status: passed`, `lifecycle_validated: true`, `generated_by: gsd-verifier`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`; require summaries `129-01-SUMMARY.md`, `129-02-SUMMARY.md`, `129-03-SUMMARY.md` present and `129-04-SUMMARY.md` absent; all existing gaps-open assertions continue to apply.
     - Else → `gaps_open`.
   - `verifyArchiveReady(repoRoot, failures): void` asserting the reconciled end-state exactly (from 129-RESEARCH.md D-13 table — copy these values, do not re-derive):
     - REQUIREMENTS.md: OBS-01, BOUND-02, HARD-05 all `- [x]`; traceability rows for the three IDs → `Complete`; `Complete: 39`; `Pending integration gap closure: 0`; checked-count 39 via `parseRequirementEntries`.
     - ROADMAP.md: Phase 129 row `- [x]`; Phase 129 section `**Plans:** 4/4 plans complete`; execution-order line `126 -> 127 -> 128 -> 129` still present; `#### Phase 125:` and `#### Phase 126:` headings still present (keeps the legacy final-audit path unreachable, Pitfall 3); `Satisfied: 39`; `## Next Step` block contains `Run \`/gsd-complete-milestone v2.1\`.`.
     - Audit: `status: passed`, `requirements: "39/39"`, `phases: "20/20"`, `integration: "13/13"`, `flows: "11/11"`, empty gaps block (`requirements: []`, `integration: []`, `flows: []` under `gaps:`), zero `- id: ` frontmatter gap entries (requireAbsent `- id: GAP-0`, `- id: FLOW-0`, and each of the 10 requirement `- id:` lines), `## Next Action` → `Run \`/gsd-complete-milestone v2.1\``, requireAbsent `/gsd-plan-phase` and `/gsd-execute-phase` anywhere in the audit. tech_debt: requireContains the existing 1,505-line entry verbatim (`"scripts/check-phase124-milestone-gap-closure.ts is 1,505 lines and concentrates unrelated lifecycle assertions."`) and requireAbsent the cross-cutting-verification entry (`phase: cross-cutting-verification`) — Plan 04 marks that debt resolved because Phase 129 delivers exactly those guards.
     - Routing set expanded per D-13/Pitfall 5: `.planning/STATE.md`, `.planning/MILESTONES.md`, `.planning/PROJECT.md` each contain `/gsd-complete-milestone v2.1` and none contain the stale routes `/gsd-plan-phase 129`, `/gsd-plan-phase 128`, `/gsd-execute-phase 129`.
     - HARD-05 ownership (D-09): traceability owner row `| HARD-05 | Phase 129 |` present; OBS-01 and BOUND-02 owners likewise pinned to Phase 129.
     - Phase 129 lifecycle, modeled on `verifyPhase127Lifecycle`: exact plan set `129-01-PLAN.md`..`129-04-PLAN.md` (each `generated_by: gsd-plan-phase`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`), all four summaries (`generated_by: gsd-execute-plan`), context `129-CONTEXT.md` (`generated_by: gsd-discuss-phase`), exactly one `129-VERIFICATION.md` with the frontmatter pinned above, and at least one summary whose `requirements-completed` lists OBS-01, BOUND-02, and HARD-05.
2. In `scripts/check-phase124-post-audit-gap-planning.ts`, dispatch inside `verifyPostAuditGapPlanningStage`: call `detectPhase129ReconciliationStage` first; for `gaps_open` and `verified_pre_promotion` run the existing assertion set unchanged (verified_pre_promotion additionally runs the verification-frontmatter assertions from step 1); for `archive_ready` skip the gaps-open-specific assertions that the end-state legally violates (unchecked requirements, `**Plans:** 0 plans`, `status: gaps_found` audit inventory, `/gsd-plan-phase 129` routing) and run `verifyArchiveReady` plus the stage-independent assertions (topology order line, phase dirs, Phase 127/128 lifecycle checks, HARD-05 ownership). Keep `isPostAuditGapPlanningStage` byte-identical.
3. Style: copy helper functions into the new module per repo convention; `maybe` prefix for nullables; early returns; no new runtime reach (no `fetch(`/spawn tokens — build any needed forbidden-token strings by concatenation).
  </action>
  <verify>
    <automated>bun run scripts/check-phase124-post-audit-gap-planning.ts 2>/dev/null || bun run scripts/check-phase124-milestone-closeout-reconciliation.ts</automated>
    <automated>bash scripts/verify.sh (deferred to Task 2's full run; quick check: bun test scripts/check-phase124-milestone-closeout-reconciliation.test.ts)</automated>
  </verify>
  <acceptance_criteria>
    `scripts/check-phase124-archive-ready.ts` exists and exports detectPhase129ReconciliationStage and verifyArchiveReady (grep: `export function verifyArchiveReady`).
    `rg -n "check-phase124-archive-ready" scripts/check-phase124-post-audit-gap-planning.ts` matches an import.
    `rg -n "isPostAuditGapPlanningStage" scripts/check-phase124-post-audit-gap-planning.ts` shows the function body unchanged (git diff shows no edit inside it).
    Archive-ready assertions include the literal strings `/gsd-complete-milestone v2.1`, `requirements: "39/39"`, `**Plans:** 4/4 plans complete`, and `| HARD-05 | Phase 129 |` (grep the new module).
    Running the Phase 124 dispatcher against the current repo exits 0 (gaps-open state still green).
  </acceptance_criteria>
  <done>The stage machine legalizes exactly the three D-10 states with archive-ready assertions matching the D-13 end-state, and the current repo still verifies green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fixtures with maybePhase129Stage, add mixture-rejection tests, run full verification</name>
  <files>scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts, scripts/check-phase124-milestone-closeout-reconciliation.test.ts, docs/metrics/lines-of-code.md</files>
  <read_first>
    scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
    scripts/check-phase124-milestone-closeout-reconciliation.test.ts
    scripts/check-phase124-archive-ready.ts (from Task 1)
  </read_first>
  <behavior>
    - Fixture at each legal stage (gaps_open, verified_pre_promotion, archive_ready) → dispatcher returns zero failures.
    - Each mixture from Task 1's behavior list → dispatcher returns a failure naming the inconsistent artifact.
    - Reconciled archive-ready fixture still dispatches to the post-audit branch (Phase 125/126 headings intact — legacy final-audit path unreachable).
    - Mutating any single archive-ready end-state field in the fixture (audit status, a checkbox, plan count, a route, the HARD-05 owner row, verification lifecycle_validated) → failure.
  </behavior>
  <action>
1. Extend `createFixture` options in `scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts` with `maybePhase129Stage?: "gaps_open" | "verified_pre_promotion" | "archive_ready"` (default preserves today's projections exactly). Per stage, project:
   - `verified_pre_promotion`: today's artifact text plus `addPhase129Artifacts`: `129-CONTEXT.md` (`generated_by: gsd-discuss-phase`), four PLAN files (`generated_by: gsd-plan-phase`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`), summaries 01-03 (`generated_by: gsd-execute-plan`, `requirements-completed: []`), and `129-VERIFICATION.md` (`generated_by: gsd-verifier`, `status: passed`, `lifecycle_validated: true`, `lifecycle_mode: yolo`, matching lifecycle id, body naming BSRV-03, BSRV-04, CMP-04, CMP-05, OBS-01, OBS-02, OBS-03, OBS-04, BOUND-02, HARD-05).
   - `archive_ready`: requirements 39/39 with the three checkboxes `[x]` and traceability `Complete`; roadmap row `- [x]` + `**Plans:** 4/4 plans complete` + Next Step `Run \`/gsd-complete-milestone v2.1\`.` (keep `#### Phase 125:`/`#### Phase 126:` headings and the order line); audit `status: passed` with `requirements: "39/39"`, `phases: "20/20"`, `integration: "13/13"`, `flows: "11/11"`, empty gaps lists, only the 1,505-line tech_debt entry, `## Next Action` archive route; STATE/PROJECT/MILESTONES carrying `/gsd-complete-milestone v2.1` with stale routes absent; all four summaries present, the fourth with `requirements-completed: [OBS-01, BOUND-02, HARD-05]`.
2. Add `test.each`-style cases to `scripts/check-phase124-milestone-closeout-reconciliation.test.ts` covering every row in <behavior>, following the file's existing fixture-mutation test pattern (Arrange fixture / Act run dispatcher / Assert failure substring or empty). Include one regression case per mixture with an exact expected failure substring.
3. Run the full contract once for the plan: `bun run scripts/command-timings.ts run --key verify-full -- bash scripts/verify.sh` (poll ≥60s, capture liveness before terminating anything). Accept the LOC report regeneration.
4. Confirm `git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/STATE.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md` is empty, then commit the whole plan once: `node "$HOME/.cursor/get-shit-done/bin/gsd-tools.cjs" commit "feat(129-03): add archive-ready stage machine for milestone reconciliation" --files scripts/check-phase124-archive-ready.ts scripts/check-phase124-post-audit-gap-planning.ts scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts scripts/check-phase124-milestone-closeout-reconciliation.test.ts docs/metrics/lines-of-code.md`.
  </action>
  <verify>
    <automated>bun test scripts/check-phase124-milestone-closeout-reconciliation.test.ts</automated>
    <automated>bash scripts/verify.sh</automated>
    <automated>git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "maybePhase129Stage" scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts` matches.
    Test file contains cases for all three legal stages and at least five mixture rejections (grep: `archive_ready`, `verified_pre_promotion`, and failure-substring assertions).
    `bun test scripts/check-phase124-milestone-closeout-reconciliation.test.ts` exits 0.
    `bash scripts/verify.sh` exits 0 against the unchanged gaps-open repo state.
    No `.planning` top-level artifact modified by this plan.
  </acceptance_criteria>
  <done>Every archive-ready acceptance and mixture rejection is mutation-tested through synthetic fixtures, and the repo verifies green in its current state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts → stage machine | Checkers parse repo-local Markdown/YAML frontmatter to authorize milestone claims |
| stage machine → milestone promotion | An overly permissive stage would let inconsistent reconciliation states commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03-01 | Tampering | archive-ready stage detection | mitigate | Fail-closed mixture rejection; exact-string/exactScalar parsing preserved (ASVS V5 per research) |
| T-129-03-02 | Repudiation | requirement promotion | mitigate | Archive-ready requires gsd-verifier verification with lifecycle_validated: true and an activation summary |
| T-129-03-03 | Elevation | checker runtime reach | mitigate | No network/process tokens in the new module; verify.sh boundary scans unchanged |
</threat_model>

<verification>
- Dispatcher green against the current gaps-open repo; new fixture tests green; full verify green.
- Archive-ready assertions byte-match the D-13 end-state that Plan 04 will write (same literal strings in both plans).
</verification>

<success_criteria>
The Phase 124 stage machine models gaps-open → verified pre-promotion → archive-ready as distinct fail-closed states with mutation-tested mixture rejection, HARD-05 stays owned by Phase 129, and the current repo state still passes the full verification contract.
</success_criteria>

<output>
After completion, create `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-03-SUMMARY.md` with lifecycle frontmatter (`generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`) and `requirements-completed: []` — HARD-05 promotion is deferred to plan 04's verification-gated reconciliation.
</output>
