---
phase: 129-integration-guardrails-and-milestone-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
  - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts
  - scripts/check-phase127-authoritative-network-state-unification.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - OBS-01
  - BOUND-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 129-2026-07-20T19-28-06
generated_at: 2026-07-20T20:17:00Z
must_haves:
  truths:
    - "One aggregate deterministic guard fails when any of the six repaired seams (shared authoritative state, local sendcmpct emission, production announcement invocation, live per-peer header facts, transport emission, post-write-only evidence) regresses."
    - "The guard names FLOW-01, FLOW-02, FLOW-03, and FLOW-04 explicitly in its failure vocabulary and fails when any named production-path test anchor disappears."
    - "verify.sh runs the Phase 129 pair after the Phase 128 pair and before the Phase 117 pair, and Phase 117 remains the final check-phase gate."
  artifacts:
    - path: scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
      provides: "Aggregate cross-phase seam and flow guard composing the Phase 127/128 exports"
      exports: ["checkPhase129IntegrationGuardrailsAndMilestoneReconciliation"]
    - path: scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts
      provides: "Fixture-based mutation coverage for every Phase 129 assertion"
    - path: scripts/verify.sh
      provides: "Ordering comment, VERIFY_COMMAND_ORDER heredoc, and run_step wiring for the Phase 129 pair"
      contains: "check-phase129-integration-guardrails-and-milestone-reconciliation"
  key_links:
    - from: scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
      to: scripts/check-phase127-authoritative-network-state-unification.ts
      via: "imported checkPhase127AuthoritativeNetworkStateUnification(repoRoot) composition"
      pattern: "checkPhase127AuthoritativeNetworkStateUnification"
    - from: scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
      to: scripts/check-phase128-production-compact-announcement-transport.ts
      via: "imported checkPhase128ProductionCompactAnnouncementTransport(repoRoot) composition"
      pattern: "checkPhase128ProductionCompactAnnouncementTransport"
    - from: scripts/verify.sh
      to: scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
      via: "default deterministic checker invocation between Phase 128 and Phase 117"
      pattern: "run_step \"check Phase 129"
---

<objective>
Add the Phase 129 aggregate integration guard: one deterministic Bun/TypeScript checker pair that composes the Phase 127/128 seam guards, names the four repaired end-to-end flows explicitly, asserts its own verify.sh wiring, and preserves the bounded v2.1 claim boundary — wired into `scripts/verify.sh` between the Phase 128 pair and the final Phase 117 gate.

Purpose: Make the Phase 127/128 production repairs fail closed under one cross-phase surface (D-01 through D-04, D-15, D-16) so OBS-01 and BOUND-02 have deterministic guard evidence.

Output: `scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts` + `.test.ts`, an exported Phase 127 target-file list for fixture composition, and verify.sh triple wiring.

CRITICAL constraints for this plan:
- Do NOT modify `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/MILESTONES.md`, or `.planning/v2.1-MILESTONE-AUDIT.md`. The Phase 124 post-audit guard pins their current gaps-open state; reconciliation happens only in plan 04 after the stage machine is evolved in plan 03 (Pitfall 1 in 129-RESEARCH.md).
- If the executor workflow updates `.planning/STATE.md` position/progress text, it MUST preserve the literal line `Next action: Run \`/gsd-plan-phase 129\`.` — the Phase 124 routing guard requires that exact route until plan 04.
- The Phase 129 checker must NOT assert volatile planning-artifact state (no `Satisfied: 36`/`Satisfied: 39`-style pins); artifact-state assertions belong to the Phase 124 stage machine (Pitfall 12).
- Prefer one commit per task batch; every commit runs the full ~5-6 minute verifier via the pre-commit hook, and `docs/metrics/lines-of-code.md` regeneration in those commits is required freshness, not noise.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-CONTEXT.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-RESEARCH.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
From scripts/check-phase127-authoritative-network-state-unification.ts (currently NOT exported — Task 1 exports it):
```typescript
const TARGET_FILES = [ /* 18 entries incl. open-bitcoind.rs, runtime_authority.rs, inbound_status.rs, dispatch/node.rs, black_box_parity.rs, docs/parity/index.json, scripts/verify.sh, ... */ ] as const;
export function checkPhase127AuthoritativeNetworkStateUnification(maybeRepoRoot?: string): string[];
// Additionally reads every non-test .rs file under packages/open-bitcoin-rpc/src/bin/open_bitcoind/
// via productionDaemonHelperSources() — currently inbound_metrics.rs, sync_seed.rs, runtime_control.rs.
```

From scripts/check-phase128-production-compact-announcement-transport.ts (already exported):
```typescript
export const PHASE128_TARGET_FILES = [ /* 20 entries incl. peer.rs, message_dispatch.rs, compact_relay.rs, network.rs, announcement_transport.rs, block_relay_evidence.rs, sync/*.rs, inbound_listener*.rs, open-bitcoind.rs, .planning/ROADMAP.md, .planning/PROJECT.md, scripts/verify.sh, ... */ ] as const;
export function checkPhase128ProductionCompactAnnouncementTransport(maybeRepoRoot?: string): string[];
```

Verified Rust production-path flow anchors (all present in the current corpus):
- packages/open-bitcoin-rpc/tests/black_box_parity.rs:560 — `phase127_production_composition_shares_sync_serving_and_operator_authority` (FLOW-01 + FLOW-04)
- packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs:143 — `production_announcement_transport_cases_fanout_uses_live_peer_facts` (FLOW-02)
- packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs:217 — `production_announcement_transport_cases_partial_failure_credits_only_prefix_and_redacts` (FLOW-03)
- packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs:49 — `compact_success_receipt_records_achieved_effect_once` (FLOW-03 post-write evidence)
- packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs:143 — `failed_or_unsent_emission_receives_no_achieved_effect_credit` (FLOW-03 no-credit)
- packages/open-bitcoin-cli/tests/operator_flows.rs and packages/open-bitcoin-cli/tests/operator_binary.rs — FLOW-04 operator surfaces (presence anchors)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 129 aggregate checker and wire verify.sh</name>
  <files>scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts, scripts/check-phase127-authoritative-network-state-unification.ts, scripts/verify.sh</files>
  <read_first>
    scripts/check-phase128-production-compact-announcement-transport.ts
    scripts/check-phase127-authoritative-network-state-unification.ts
    scripts/verify.sh
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-RESEARCH.md
    packages/open-bitcoin-rpc/tests/black_box_parity.rs
    packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs
    packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs
  </read_first>
  <behavior>
    - Composed run: `checkPhase129IntegrationGuardrailsAndMilestoneReconciliation()` returns `[]` on the current repo and includes P127/P128-prefixed failures when an upstream seam regresses.
    - Each named flow anchor removal produces a distinct `P129 FLOW-0N` failure string.
    - Removing either Phase 129 line from the verify.sh heredoc or run_step block produces a `P129 verifier` failure.
    - A `bun (test|run) scripts/check-phaseNNN` line appearing after the Phase 117 lines produces a `P129 final gate` failure.
  </behavior>
  <action>
This task implements locked decisions D-01, D-02, D-03, D-04, D-15, and D-16.

1. In `scripts/check-phase127-authoritative-network-state-unification.ts`, export the target list so the Phase 129 fixture can compose it: rename `const TARGET_FILES` to `export const PHASE127_TARGET_FILES` (update the internal `TargetFile`/`loadCorpus` references), and add `export const PHASE127_DAEMON_HELPER_DIR = "packages/open-bitcoin-rpc/src/bin/open_bitcoind";` next to it. Change nothing else in that file; its own assertions and its test file must keep passing unmodified.

2. Create `scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts` following the Phase 127/128 checker conventions exactly: `#!/usr/bin/env bun` header, `DEFAULT_REPO_ROOT = path.resolve(import.meta.dir, "..")`, exported `checkPhase129IntegrationGuardrailsAndMilestoneReconciliation(maybeRepoRoot?: string): string[]` resolving `maybeRepoRoot ?? process.env.OPEN_BITCOIN_PHASE129_REPO_ROOT ?? DEFAULT_REPO_ROOT`, pure `node:fs`/`node:path` file reads only (no network, no process spawning), an `import.meta.main` CLI footer printing failures with a `P129` context and exiting 1 on failure, and failure strings prefixed `P129 `. The checker body:
   a. Six-seam composition (D-03): `failures.push(...checkPhase127AuthoritativeNetworkStateUnification(repoRoot)); failures.push(...checkPhase128ProductionCompactAnnouncementTransport(repoRoot));` — both imported from their sibling modules with the same `repoRoot`.
   b. Named flows (D-04) in a `checkNamedFlows` function using a `FLOW_ANCHORS` table with these exact entries (file, required substring, flow label):
      - `packages/open-bitcoin-rpc/tests/black_box_parity.rs` must contain `phase127_production_composition_shares_sync_serving_and_operator_authority` → failure `P129 FLOW-01: durable validated block to inbound serving production composition anchor is missing` and `P129 FLOW-04: authoritative sync runtime to RPC/CLI/dashboard/support production composition anchor is missing` (assert once, emit both flow labels or use two table rows against the same anchor).
      - `packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs` must contain `production_announcement_transport_cases_fanout_uses_live_peer_facts` → `P129 FLOW-02: handshake to bilateral negotiation to live header-aware announcement anchor is missing`.
      - `packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs` must contain `production_announcement_transport_cases_partial_failure_credits_only_prefix_and_redacts` → `P129 FLOW-03: high-bandwidth decision to wire emission to post-write evidence anchor is missing`.
      - `packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs` must contain `compact_success_receipt_records_achieved_effect_once` and `failed_or_unsent_emission_receives_no_achieved_effect_credit` → `P129 FLOW-03: post-write-only achieved-effect unit anchors are missing`.
      - `packages/open-bitcoin-cli/tests/operator_flows.rs` and `packages/open-bitcoin-cli/tests/operator_binary.rs` must exist and be non-empty → `P129 FLOW-04: CLI operator surface test files are missing`.
   c. Verifier wiring self-assertion (D-02) in a `checkVerifierWiring` function reading `scripts/verify.sh`: require the ordered subsequence [`bun run scripts/check-phase128-production-compact-announcement-transport.ts`, `bun test scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts`, `bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts`, `bun test scripts/check-phase117-parity-uat-release-boundary.test.ts`] as trimmed exact lines in BOTH the `VERIFY_COMMAND_ORDER` heredoc region and the live `run_step` command text (copy the `orderedLines`/`requireOrdered` helper pattern from the Phase 127 checker — repo convention is per-file helper duplication).
   d. Final-gate preservation (D-15): require that the LAST line matching `bun (test|run) scripts/check-phase\d+` in both the heredoc and the run_step text is the Phase 117 check pair (mirror `requireFinalPhaseChecker` in `scripts/check-phase124-milestone-closeout-reconciliation.ts`), with failure prefix `P129 final gate`.
   e. Deterministic-scope self-scan (D-16, Pitfall 9): scan the Phase 129 checker's own source for forbidden effect tokens, building each token by string concatenation exactly as the Phase 127/128 checkers do (`"fetch" + "("`, `"Bun." + "spawn"`, `"node:" + "child_process"`, `"http" + "://"`, `"https" + "://"`).
   f. Do NOT add any assertion against `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/MILESTONES.md`, or `.planning/v2.1-MILESTONE-AUDIT.md` (Pitfall 12). The composed Phase 128 checker already covers the stable ROADMAP/PROJECT claim sentences.

3. Wire `scripts/verify.sh` — all three surfaces in the same edit (D-02), append-only for the comment (Pitfall 7):
   - Ordering comment (lines ~295-309): append the sentence `# Phase 128 is followed by Phase 129, and Phase 129 precedes the final Phase 117 gate.` after the existing Phase 123/124/117 sentence. Never reflow or delete existing sentences — check-phase100/101/102/110/111 pin exact existing sentences.
   - `VERIFY_COMMAND_ORDER` heredoc: insert between the existing `bun run scripts/check-phase128-production-compact-announcement-transport.ts` and `bun test scripts/check-phase117-parity-uat-release-boundary.test.ts` lines:
     `bun test scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts`
     `bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts`
   - Live `run_step` block: insert between the existing `run_step "check Phase 128 production compact announcement transport" ...` and `run_step "test Phase 117 parity UAT release boundary checker" ...` lines:
     `run_step "test Phase 129 integration guardrails and milestone reconciliation checker" bun test scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts`
     `run_step "check Phase 129 integration guardrails and milestone reconciliation" bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts`
   The labels contain none of the Phase 117 `FORBIDDEN_DEFAULT_GATES` tokens (no `public-network`, `soak`, `wall-clock`, `systemd`).

Style: follow standards/languages/typescript-javascript.md — `maybe` prefix for nullable bindings, early returns, keep the file under ~628 lines.
  </action>
  <verify>
    <automated>bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts</automated>
    <automated>bun test scripts/check-phase127-authoritative-network-state-unification.test.ts</automated>
    <automated>rg -n "check-phase129-integration-guardrails-and-milestone-reconciliation" scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    `bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts` exits 0 on the current repo.
    `rg -c "check-phase129-integration-guardrails-and-milestone-reconciliation" scripts/verify.sh` reports exactly 4 occurrences (2 heredoc + 2 run_step).
    In both the heredoc and the run_step block, the two Phase 129 lines sit after the Phase 128 lines and before the Phase 117 lines.
    `rg -n "export const PHASE127_TARGET_FILES" scripts/check-phase127-authoritative-network-state-unification.ts` matches.
    The Phase 129 checker source contains the literal substrings `P129 FLOW-01`, `P129 FLOW-02`, `P129 FLOW-03`, `P129 FLOW-04`, and `P129 final gate`.
    `rg -n "fetch\(|Bun\.spawn|node:child_process" scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts` produces no match (forbidden tokens only ever appear concatenated).
    `bun test scripts/check-phase127-authoritative-network-state-unification.test.ts` still passes with the export rename.
  </acceptance_criteria>
  <done>The aggregate guard runs green in verify.sh between Phase 128 and Phase 117 and fails closed on any seam, flow-anchor, or wiring regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture-based mutation coverage for every Phase 129 assertion</name>
  <files>scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts</files>
  <read_first>
    scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
    scripts/check-phase128-production-compact-announcement-transport.test.ts
    scripts/check-phase127-authoritative-network-state-unification.test.ts
    scripts/check-phase127-authoritative-network-state-unification.ts
  </read_first>
  <behavior>
    - Test 1: "passes with the complete Phase 129 corpus" — fixture clone of the union corpus returns [].
    - Tests 2..N: one `test.each` row per assertion family; each mutation yields the exact expected failure string.
    - Composition rows: mutating a Phase 127-guarded file in the fixture surfaces a `P127`-prefixed failure through the Phase 129 checker; mutating a Phase 128-guarded file surfaces a `P128`-prefixed failure.
  </behavior>
  <action>
This task implements locked decision D-01 (mutation coverage) and guards D-03/D-04.

Create `scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts` in the established live-clone mutation style (mirror the Phase 128 test structure: `mkdtempSync` temp roots, `afterEach` cleanup, a `createFixture()` that copies live repo files, `replace(file, from, to)` mutators, Arrange/Act/Assert with `test.each`).

Fixture corpus (Pitfall 8): copy the UNION of
- `PHASE127_TARGET_FILES` (imported from the Phase 127 checker after Task 1's export),
- `PHASE128_TARGET_FILES` (imported from the Phase 128 checker),
- the entire `packages/open-bitcoin-rpc/src/bin/open_bitcoind/` directory (the Phase 127 checker reads every non-test `.rs` file there beyond its target list — copy the whole directory contents including `tests.rs`; only non-test files are scanned),
- the Phase 129 checker's own extra targets: `packages/open-bitcoin-cli/tests/operator_flows.rs`, `packages/open-bitcoin-cli/tests/operator_binary.rs`, `scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts`, `scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts` if referenced, plus any file in its own target list.
Deduplicate paths with a Set; create parent directories with `mkdirSync(..., { recursive: true })`.

Mutation table (each row: name, expected exact failure string, mutator):
1. FLOW-01/FLOW-04 anchor: replace `phase127_production_composition_shares_sync_serving_and_operator_authority` with `renamed_anchor` in `packages/open-bitcoin-rpc/tests/black_box_parity.rs` → expect the `P129 FLOW-01` failure (and the `P129 FLOW-04` failure if emitted separately). NOTE: this file is also in the Phase 127 corpus, so assert with `expect(failures).toContain(...)` rather than exact-array equality.
2. FLOW-02 anchor: replace `production_announcement_transport_cases_fanout_uses_live_peer_facts` with `renamed_anchor` → expect the `P129 FLOW-02` failure string.
3. FLOW-03 transport anchor: replace `production_announcement_transport_cases_partial_failure_credits_only_prefix_and_redacts` with `renamed_anchor` → expect the `P129 FLOW-03` failure string.
4. FLOW-03 unit anchors: replace `compact_success_receipt_records_achieved_effect_once` with `renamed_anchor` in `packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs` → expect the `P129 FLOW-03` unit-anchor failure string.
5. FLOW-04 operator surface: delete (write empty string to) `packages/open-bitcoin-cli/tests/operator_flows.rs` → expect the `P129 FLOW-04` operator-surface failure string.
6. Verifier heredoc wiring: remove the `bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts` line from the fixture's `scripts/verify.sh` heredoc → expect the `P129 verifier` failure string.
7. Verifier run_step wiring: remove the `run_step "check Phase 129 integration guardrails and milestone reconciliation" ...` line → expect the `P129 verifier` failure string.
8. Final-gate ordering: append `run_step "check Phase 130 placeholder" bun run scripts/check-phase130-placeholder.ts` after the Phase 117 run_step lines in the fixture verify.sh → expect the `P129 final gate` failure string.
9. Composed Phase 127 seam: apply one known Phase 127 mutation (pick one exact mutation and expected `P127 ...` string from `scripts/check-phase127-authoritative-network-state-unification.test.ts` and reuse it verbatim) → expect that `P127`-prefixed string in the Phase 129 checker's output.
10. Composed Phase 128 seam: reuse one exact mutation/failure pair from `scripts/check-phase128-production-compact-announcement-transport.test.ts` (for example the "local low-bandwidth version-2 offer" row: replace `announce: false,` with `announce: true,` in `packages/open-bitcoin-network/src/peer/compact_relay.rs` → `P128 local offer: production handshake must schedule sendcmpct(false, version 2)`).

Every test invokes `checkPhase129IntegrationGuardrailsAndMilestoneReconciliation(fixtureRoot)` with the explicit root (never the env var). One concept per test; Arrange/Act/Assert sections per standards/core/testing.md.
  </action>
  <verify>
    <automated>bun test scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts</automated>
    <automated>bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts</automated>
  </verify>
  <acceptance_criteria>
    `bun test scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts` passes with at least 11 tests (1 complete-corpus pass + 10 mutation rows).
    The complete-corpus test asserts `expect(failures).toEqual([])`.
    Mutation rows 9 and 10 assert `P127`- and `P128`-prefixed strings respectively, proving composition (D-03).
    The test file imports `PHASE127_TARGET_FILES` and `PHASE128_TARGET_FILES` and copies the `open_bitcoind` helper directory (grep: `PHASE127_DAEMON_HELPER_DIR` or the literal directory path appears in the fixture builder).
  </acceptance_criteria>
  <done>Every Phase 129 assertion family has an independent mutation that fails with its exact string, and the complete corpus passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full verification contract and review contributor-facing freshness</name>
  <files>docs/metrics/lines-of-code.md</files>
  <read_first>
    AGENTS.md
    scripts/verify.sh
    README.md
  </read_first>
  <action>
Run `bash scripts/verify.sh` to completion (use `bun run scripts/command-timings.ts run --key verify-full -- bash scripts/verify.sh` so duration history and the cooperative build lock stay effective). Do not interrupt a quiet session; poll liveness at least every 60 seconds and treat exceeded estimates as different from timeouts. Accept hook/verifier regeneration of `docs/metrics/lines-of-code.md` as required freshness.

Review `README.md` and the touched-area READMEs only for statements the new Phase 129 guard makes false; the expected outcome is NO README change (this plan adds verification, not behavior). If a README sentence is genuinely false, record the finding in the summary and make the minimal wording fix while preserving bounded default-off/no-production claim vocabulary (the Phase 117 claim scanner lints README wording).

Confirm no `.planning/` top-level artifact changed in this plan's diff: `git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md` must be empty.

Commit the plan's work as a single batch through `node "$HOME/.cursor/get-shit-done/bin/gsd-tools.cjs" commit "feat(129-01): add phase 129 aggregate integration guard" --files scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts scripts/check-phase127-authoritative-network-state-unification.ts scripts/verify.sh docs/metrics/lines-of-code.md` (the pre-commit hook reruns the full verifier and re-adds the LOC report).
  </action>
  <verify>
    <automated>bash scripts/verify.sh</automated>
    <automated>git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    `bash scripts/verify.sh` exits 0 with the Phase 129 test and check steps visible in its step output.
    `git status --porcelain` shows no modification to .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/MILESTONES.md, or .planning/v2.1-MILESTONE-AUDIT.md from this plan.
    The plan's changes are committed with the pre-commit hook passing.
  </acceptance_criteria>
  <done>The full deterministic contract passes with the Phase 129 guard wired, and no reconciliation-guarded artifact moved early.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust production sources → deterministic checker | A stale or helper-only pattern could pass while production wiring regresses |
| Checker self-scope | The checker itself could gain network/process reach |
| verify.sh ordering contract | Wiring edits could break older checkers that parse verify.sh as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01-01 | Tampering | check-phase129 checker | mitigate | One mutation test per assertion family, incl. composed P127/P128 rows |
| T-129-01-02 | Elevation | checker runtime scope | mitigate | Forbidden-token self-scan with concatenated tokens; node:fs reads only |
| T-129-01-03 | Tampering | scripts/verify.sh | mitigate | Append-only comment edit; ordered-subsequence assertions in the new checker and existing P124/P127/P128 checks |
| T-129-01-04 | Repudiation | claim boundary | mitigate | Phase 117 stays byte-identical as the final gate; P129 final-gate assertion |
</threat_model>

<verification>
- `bun test` mutation suite green, `bun run` checker green, full `bash scripts/verify.sh` green.
- Phase 129 lines sit between Phase 128 and Phase 117 in both the heredoc and run_step block.
- No `.planning/` top-level artifact modified.
</verification>

<success_criteria>
One aggregate fail-closed guard covers the six repaired seams and names FLOW-01 through FLOW-04, runs in default verification between Phase 128 and Phase 117, and has mutation coverage proving every assertion — with the repo still in its legal gaps-open state.
</success_criteria>

<output>
After completion, create `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-01-SUMMARY.md` with lifecycle frontmatter (`generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`) and `requirements-completed: []` — requirement promotion is deferred to plan 04's verification-gated reconciliation.
</output>
