---
phase: 129-integration-guardrails-and-milestone-reconciliation
plan: "04"
type: execute
wave: 4
depends_on:
  - "129-01"
  - "129-02"
  - "129-03"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/v2.1-MILESTONE-AUDIT.md
  - .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - OBS-01
  - BOUND-02
  - HARD-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 129-2026-07-20T19-28-06
generated_at: 2026-07-20T20:17:00Z
must_haves:
  truths:
    - "OBS-01, BOUND-02, and HARD-05 are promoted to checked/Complete only after a lifecycle-valid gsd-verifier 129-VERIFICATION.md with status: passed exists — never before."
    - "REQUIREMENTS, ROADMAP, PROJECT, STATE, MILESTONES, and the in-place rerun v2.1 audit all agree: 39/39, Phase 129 complete, audit passed with empty gaps, every routing surface pointing at /gsd-complete-milestone v2.1."
    - "The reconciliation lands as one atomic commit that the evolved Phase 124 archive-ready stage accepts, with no committed mixture state."
    - "The v2.1 claim boundary is preserved: no public relay, production-readiness, or production-funds claims enter any reconciled artifact."
  artifacts:
    - path: .planning/v2.1-MILESTONE-AUDIT.md
      provides: "In-place rerun audit"
      contains: "status: passed"
    - path: .planning/REQUIREMENTS.md
      provides: "39/39 requirement closure"
      contains: "Complete: 39"
    - path: .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md
      provides: "Activation summary promoting the three requirements"
      contains: "requirements-completed"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md
      via: "requirements-completed activation co-committed with checkbox flips"
      pattern: "requirements-completed"
    - from: .planning/v2.1-MILESTONE-AUDIT.md
      to: .planning/ROADMAP.md
      via: "shared archive route asserted by the Phase 124 archive-ready stage"
      pattern: "/gsd-complete-milestone v2.1"
---

<objective>
Perform the verification-gated milestone reconciliation (D-11, D-12, D-13, D-14): confirm the independent gsd-verifier evidence exists, then flip REQUIREMENTS/ROADMAP/PROJECT/STATE/MILESTONES and rerun the v2.1 milestone audit in place to `status: passed` — all in one atomic commit that moves the repo from the verified-pre-promotion stage to the archive-ready stage the Plan 03 checker enforces, routing to `/gsd-complete-milestone v2.1`.

Purpose: This closes OBS-01, BOUND-02, and HARD-05 as independently verified, ends the MILESTONES.md drift, and makes v2.1 archive-ready. The archival run itself is out of scope (deferred per CONTEXT).

Output: Reconciled six-artifact end-state, in-place passed audit, activation summary `129-04-SUMMARY.md`, one green commit.

CRITICAL constraints for this plan:
- HARD GATE: `129-VERIFICATION.md` is produced by the gsd-verifier during execute-phase, NOT by this plan. If it does not exist with `status: passed` when Task 1 runs, HALT this plan and report BLOCKED — never fabricate or edit the verification file.
- All artifact flips plus `129-04-SUMMARY.md` MUST land in a single commit. The pre-commit hook runs the full verifier; any partial flip commits a mixture state that the Plan 03 stage machine and `check-active-milestone-verification-traceability.ts` both reject.
- The exact end-state strings below must byte-match what the Plan 03 archive-ready checker asserts (they were authored from the same D-13 table). If Plan 03's committed checker text differs anywhere, match the checker — read it first.
- Preserve pinned sentences: PROJECT.md must keep the two Phase 128-pinned sentences (`v2.1 does not imply public relay defaults, …` and the `package relay, bloom/filter serving, … deferred` sentence); ROADMAP must keep `#### Phase 125:`/`#### Phase 126:` headings and the `126 -> 127 -> 128 -> 129` order line; `docs/parity/index.json` keeps "Phase 128 retains"/"Phase 129 retains" (do not touch that file).
- Keep all reconciled prose in no-claim form (`verifyNoClaimBoundary` + Phase 117 scanner): deferred surfaces use "remain deferred"/"does not"; no positive public/production/package/filter/readiness claims.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-CONTEXT.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-RESEARCH.md
@.planning/milestones/v2.0-MILESTONE-AUDIT.md
@scripts/check-phase124-archive-ready.ts
@scripts/check-active-milestone-verification-traceability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate on independent verification evidence</name>
  <files>(read-only gate — no files modified)</files>
  <read_first>
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-01-SUMMARY.md
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-02-SUMMARY.md
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-03-SUMMARY.md
    scripts/check-phase124-archive-ready.ts
  </read_first>
  <action>
This task enforces the D-11 promotion ordering. Verify ALL of the following before any later task runs:

1. `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md` exists with frontmatter exactly: `status: passed`, `lifecycle_validated: true`, `generated_by: gsd-verifier`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`.
2. Its text names all 10 reassigned requirement IDs: BSRV-03, BSRV-04, CMP-04, CMP-05, OBS-01, OBS-02, OBS-03, OBS-04, BOUND-02, HARD-05 (D-11 re-attestation; the traceability checker requires at minimum the OBS-01/BOUND-02/HARD-05 tokens).
3. Summaries `129-01-SUMMARY.md`, `129-02-SUMMARY.md`, `129-03-SUMMARY.md` exist with `generated_by: gsd-execute-plan` and the shared lifecycle id.
4. Read the committed `scripts/check-phase124-archive-ready.ts` and extract the exact literal strings its archive-ready assertions require (audit score lines, plan-count line, routes, tech_debt entry, traceability owner rows). Task 2 must write exactly those strings.

If ANY check in 1-3 fails: STOP the plan immediately and report `BLOCKED: 129-VERIFICATION.md missing or not lifecycle-valid — run the gsd-verifier for Phase 129 (plans 01-03) and re-run plan 04`. Do not create, edit, or backdate the verification file, and do not proceed to Task 2.
  </action>
  <verify>
    <automated>rg -c "status: passed" .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md</automated>
    <automated>rg -c "lifecycle_validated: true" .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md</automated>
    <automated>for id in BSRV-03 BSRV-04 CMP-04 CMP-05 OBS-01 OBS-02 OBS-03 OBS-04 BOUND-02 HARD-05; do rg -q "$id" .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-VERIFICATION.md || echo "MISSING $id"; done</automated>
  </verify>
  <acceptance_criteria>
    All three verify commands succeed with no MISSING output, or the plan halted with the exact BLOCKED message and made zero file changes (`git status --porcelain` empty).
    The extracted checker literals are recorded in working notes for Task 2 (summary must state that Task 2 strings were matched against the committed checker).
  </acceptance_criteria>
  <done>Promotion is provably gated on independent lifecycle-valid verification, or the plan is cleanly blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the six planning artifacts and rerun the audit in place</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/v2.1-MILESTONE-AUDIT.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/MILESTONES.md
    .planning/v2.1-MILESTONE-AUDIT.md
    .planning/milestones/v2.0-MILESTONE-AUDIT.md
    scripts/check-phase124-archive-ready.ts
  </read_first>
  <action>
Apply the D-13 field-level end-state. Do NOT commit inside this task — Task 3 commits everything atomically. Exact edits per artifact:

1. `.planning/REQUIREMENTS.md`: flip the three checkboxes to `[x]` (OBS-01 ~line 49, BOUND-02 ~line 58, HARD-05 ~line 69 — locate by ID token, not line number); flip their traceability rows (~lines 141/147/155) from `Pending` to `Complete` keeping owners `Phase 129` (D-09; OBS-01/BOUND-02 owners stay as committed); update the coverage block to `Complete: 39` and `Pending integration gap closure: 0`; refresh the trailing `*Last updated:*` line to today's date with a Phase 129 note.
2. `.planning/ROADMAP.md`: update Current Status prose to reconciled wording; milestone bullet (~line 34) to complete wording; Phase 129 row (~line 61) to `- [x] … (completed 2026-07-20)`; Phase 129 section to `**Plans:** 4/4 plans complete` plus a four-line plan list (`- [x] 129-01-PLAN.md — Aggregate integration guard`, `- [x] 129-02-PLAN.md — OBS-01 fallback-counter truthfulness`, `- [x] 129-03-PLAN.md — Archive-ready stage machine`, `- [x] 129-04-PLAN.md — Milestone reconciliation and promotion`); progress table row 129 → `4/4 | Complete | 2026-07-20`; coverage → `Satisfied: 39`, `Pending integration gap closure: 0`; `## Next Step` → `Run \`/gsd-complete-milestone v2.1\`.`. KEEP: `#### Phase 125:` and `#### Phase 126:` headings, the `126 -> 127 -> 128 -> 129` execution-order line, and all phase-detail headings (`#### Phase 127/128/129:`).
3. `.planning/PROJECT.md`: Current State paragraph → Phase 129 complete, next action `/gsd-complete-milestone v2.1`; Current Milestone `**Status:**` paragraph → reconciled/archive-ready wording; PRESERVE verbatim the two pinned sentences (`v2.1 does not imply public relay defaults, …` and `package relay, bloom/filter serving, … deferred`).
4. `.planning/STATE.md`: frontmatter `status`/`stopped_at`/`progress` refreshed; fix the pre-existing drift by reconciling `total_plans`/`completed_plans` to truthful counts including Phase 129's 4 plans (recount from `.planning/phases/*/`); Current Position → Phase 129 complete; `Next action: Run \`/gsd-complete-milestone v2.1\`.`; resolve the "Plan and execute Phase 129…" pending todo; append a decisions-log entry recording the D-06 outcome (from plan 02's summary) and the reconciliation.
5. `.planning/MILESTONES.md` (the flagship stale artifact): v2.1 section 33/39 → 39/39; "Phases 110 through 127" → "Phases 110 through 129"; plan totals recounted; audit projection ("three integration gaps") → passed rerun wording; `**What's next:** Run \`/gsd-complete-milestone v2.1\`.` replacing `/gsd-plan-phase 128`.
6. `.planning/v2.1-MILESTONE-AUDIT.md` — full in-place rewrite (D-12, keep the filename):
   - Frontmatter exactly (match the Plan 03 checker literals extracted in Task 1): `milestone: v2.1`, `milestone_name: Block Serving and Compact Block Relay Boundary`, fresh ISO `audited:` timestamp, `status: passed`, scores `requirements: "39/39"`, `phases: "20/20"`, `integration: "13/13"`, `flows: "11/11"`, `gaps:` with `requirements: []`, `integration: []`, `flows: []` (zero `- id:` entries anywhere in frontmatter), `tech_debt:` retaining ONLY the phase 124 entry with the verbatim item `"scripts/check-phase124-milestone-gap-closure.ts is 1,505 lines and concentrates unrelated lifecycle assertions."` (drop the `cross-cutting-verification` entry — resolved by this phase's guards; state that resolution in the body), `nyquist:` block unchanged (`enabled: false`, `overall: skipped`).
   - Body: refreshed Result/Definition-of-Done/coverage tables showing 39/39, 13/13 integration points, 11/11 flows with the Phase 127/128/129 repair evidence (cite the Phase 129 aggregate guard, the named FLOW-01..04 test anchors, and 129-VERIFICATION.md); a section recording the resolved cross-cutting-verification debt; `## Next Action` → `Run \`/gsd-complete-milestone v2.1\``; ZERO occurrences of `/gsd-plan-phase` or `/gsd-execute-phase` anywhere in the file; all deferred-surface sentences in no-claim form (`remain deferred`, `does not`, table rows with `deferred` status).
Cross-check every written string against the committed archive-ready checker before finishing (Task 1 item 4).
  </action>
  <verify>
    <automated>rg -c "\[x\] \*\*OBS-01|\[x\] \*\*BOUND-02|\[x\] \*\*HARD-05" .planning/REQUIREMENTS.md</automated>
    <automated>rg -c "status: passed" .planning/v2.1-MILESTONE-AUDIT.md && rg -c "/gsd-complete-milestone v2.1" .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md</automated>
    <automated>rg -c "/gsd-plan-phase|/gsd-execute-phase" .planning/v2.1-MILESTONE-AUDIT.md .planning/MILESTONES.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    REQUIREMENTS.md: three `[x]` flips, three `Complete` traceability rows with owners unchanged, `Complete: 39`, `Pending integration gap closure: 0`.
    ROADMAP.md: `- [x]` Phase 129 row, `**Plans:** 4/4 plans complete`, `Satisfied: 39`, Next Step archive route, order line and Phase 125/126 headings intact (grep each literal).
    Audit frontmatter contains exactly the score/gaps/tech_debt shape above; `rg -c "- id:" .planning/v2.1-MILESTONE-AUDIT.md` counts only the retained tech_debt structure (zero gap `- id:` entries).
    PROJECT.md still contains both pinned sentences verbatim.
    MILESTONES.md contains `39/39` and no `/gsd-plan-phase 128`.
    STATE.md `total_plans` equals `completed_plans` and both match the recounted plan total.
  </acceptance_criteria>
  <done>All six artifacts carry the byte-exact archive-ready end-state, uncommitted, ready for the atomic promotion commit.</done>
</task>

<task type="auto">
  <name>Task 3: Write the activation summary, commit atomically, and route to milestone completion</name>
  <files>.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md, docs/metrics/lines-of-code.md</files>
  <read_first>
    .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-03-SUMMARY.md (format precedent)
    scripts/check-active-milestone-verification-traceability.ts (activation format: exactly one requirements-completed field per summary)
    README.md
  </read_first>
  <action>
1. Write `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md` NOW (before the commit — the traceability checker requires the activation summary in the same commit as the checkbox flips). Frontmatter: `generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`, and exactly one `requirements-completed: [OBS-01, BOUND-02, HARD-05]` field. Body: what was reconciled, the D-06 outcome reference, the verification gate evidence, and deviations if any.
2. Review `README.md` and the workflow-facing READMEs for statements the reconciliation falsifies (e.g. "three integration gaps remain", milestone-in-progress wording); update only false statements, keeping no-claim form. Expect minimal or no changes.
3. Commit EVERYTHING from Tasks 2-3 as one atomic batch: `node "$HOME/.cursor/get-shit-done/bin/gsd-tools.cjs" commit "docs(129-04): reconcile v2.1 milestone artifacts and promote verified requirements" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md .planning/STATE.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md docs/metrics/lines-of-code.md` (plus any README touched). The pre-commit hook runs the full verifier (~5-6 min; poll ≥60s) — the evolved archive-ready stage, the traceability checker, and the Phase 117 gate must all pass on this exact commit.
4. If the hook fails: fix the named mismatch (usually a string differing from the archive-ready checker), and retry as a NEW commit attempt of the same atomic batch — never split the batch to get past the hook, and never amend a hook-rejected commit.
5. Report the end state: v2.1 is reconciled and archive-ready; the next step for the USER is `/gsd-complete-milestone v2.1` (do NOT run it — archival is out of scope per CONTEXT).
  </action>
  <verify>
    <automated>bash scripts/verify.sh</automated>
    <automated>rg -c "requirements-completed: \[OBS-01, BOUND-02, HARD-05\]" .planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-04-SUMMARY.md</automated>
    <automated>git log -1 --stat | rg -c "REQUIREMENTS.md" && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    `bash scripts/verify.sh` exits 0 on the committed archive-ready state.
    `git log -1 --stat` shows all six planning artifacts plus 129-04-SUMMARY.md in the single promotion commit; `git status --porcelain` is empty afterward.
    129-04-SUMMARY.md has exactly one `requirements-completed` field listing exactly OBS-01, BOUND-02, HARD-05.
    The final report names `/gsd-complete-milestone v2.1` as the user's next step and confirms it was not run.
  </acceptance_criteria>
  <done>The promotion commit is green under the full verification contract and v2.1 routes to archival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier evidence → requirement promotion | Promotion must not precede independent verification |
| reconciled prose → claim scanners | Audit/PROJECT/README edits pass through verifyNoClaimBoundary and the Phase 117 claim gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04-01 | Spoofing | 129-VERIFICATION.md | mitigate | Task 1 hard gate on generated_by: gsd-verifier + lifecycle id; plan halts BLOCKED rather than fabricating |
| T-129-04-02 | Repudiation | atomic promotion commit | mitigate | Single commit carrying flips + activation summary; hook-enforced full verify; stage machine rejects mixtures |
| T-129-04-03 | Tampering | claim boundary | mitigate | No-claim vocabulary in all reconciled prose; Phase 117 gate unchanged and final |
</threat_model>

<verification>
- Full verify green on the promotion commit; archive-ready stage, traceability checker, and Phase 117 gate all pass simultaneously.
- Every routing surface (ROADMAP, STATE, PROJECT, MILESTONES, audit) carries `/gsd-complete-milestone v2.1` with stale routes absent.
</verification>

<success_criteria>
OBS-01, BOUND-02, and HARD-05 are independently verified and promoted, all six planning artifacts and the in-place rerun audit agree at 39/39 with empty gaps, the repo passes the full verification contract in the archive-ready state, and v2.1 routes to `/gsd-complete-milestone v2.1` without running the archival itself.
</success_criteria>

<output>
`129-04-SUMMARY.md` is created in Task 3 and co-committed with the promotion (required by the traceability checker) — do not recreate it after completion; amend content only if the executor deviated, keeping exactly one `requirements-completed` field.
</output>
