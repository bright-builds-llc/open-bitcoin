---
phase: 129-integration-guardrails-and-milestone-reconciliation
plan: "02"
type: execute
wave: 2
depends_on:
  - "129-01"
files_modified:
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/status/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - docs/parity/source-breadcrumbs.json
  - docs/parity/catalog/p2p.md
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - OBS-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 129-2026-07-20T19-28-06
generated_at: 2026-07-20T20:17:00Z
must_haves:
  truths:
    - "The fallback facet's compact_timeout_count reports only real timeout cleanups, or the inclusive snapshot semantics are explicitly documented at the field and in parity docs — never left silently ambiguous."
    - "All six OBS-01 facets (activation, eligibility, negotiation, reconstruction, fallback, in-flight) continue to project from the single ManagedNetworkHandle authority with no schema change."
    - "Every Phase 116/121/123/127/128 deterministic guard and the new Phase 129 guard stay green after the D-06 outcome."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      provides: "Truthful (or explicitly documented) fallback counter snapshot semantics"
      contains: "CompactRelayFallbackCounters"
    - path: docs/parity/catalog/p2p.md
      provides: "Recorded D-06 semantics decision for operator fallback evidence"
  key_links:
    - from: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      to: packages/open-bitcoin-node/src/status/block_relay_evidence.rs
      via: "BlockRelayEvidenceStatus::with_components fallback facet"
      pattern: "fallback_counters|CompactRelayFallbackCounters"
---

<objective>
Resolve locked decision D-06: decide whether the snapshot-time mixing of live `getblocktxn_in_flight` entries into the fallback facet's `compact_timeout_count` misreports OBS-01 truth, then either apply the minimal Rust fix (with tests and parity breadcrumbs) or document the inclusive semantics explicitly — no other runtime change (D-05).

Purpose: OBS-01 requires RPC and shared network status to report fallback and in-flight compact-block state truthfully; the independent Phase 129 verification (D-11) will attest FLOW-04 truthfulness, so this ambiguity must be closed with evidence either way.

Output: A truthful or explicitly documented fallback counter, focused regression tests, updated parity/observability notes, and a recorded decision for the plan summary and verifier.

CRITICAL constraints for this plan:
- No new runtime features and no status-schema change (D-05). `CompactRelayFallbackCounters` keeps exactly its two fields; RPC/CLI/dashboard/support surfaces and metric label sets stay unchanged (`compact_timeout_count` is a fixed Phase 116 label).
- Do NOT modify `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/MILESTONES.md`, or `.planning/v2.1-MILESTONE-AUDIT.md`; preserve `Next action: Run \`/gsd-plan-phase 129\`.` in `.planning/STATE.md` if the workflow touches it.
- `packages/open-bitcoin-node/src/network/block_relay_evidence.rs` is in `PHASE128_TARGET_FILES`; any edit must keep the Phase 128 checker (and therefore the composed Phase 129 checker) green.
- Prefer one commit for the whole plan; every commit runs the ~5-6 minute verifier.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-CONTEXT.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-RESEARCH.md

<interfaces>
Verified current behavior in packages/open-bitcoin-node/src/network/block_relay_evidence.rs:

Durable increments on real timeout cleanup (~lines 248-256):
```rust
CompactDownloadCleanupCause::Timeout => {
    self.cleanup.compact_download_timeout_count += removed_count as u64;
    self.fallback.compact_timeout_count += removed_count as u64;
    self.fallback.compact_fallback_count += removed_count as u64;
}
```

Snapshot-time mixing (~lines 343-356):
```rust
fn fallback_counters(
    peer_manager: &PeerManager,
    mut counters: CompactRelayFallbackCounters,
) -> CompactRelayFallbackCounters {
    let timed_out = peer_manager
        .peer_ids()
        .into_iter()
        .filter_map(|peer_id| peer_manager.compact_download_peer_state(peer_id))
        .flat_map(|state| state.in_flight.values())
        .filter(|entry| entry.getblocktxn_in_flight)
        .count() as u64;
    counters.compact_timeout_count += timed_out;
    counters
}
```

The same live fact is already reported by the in-flight facet (~lines 328-338):
```rust
counters.getblocktxn_in_flight_count += state.in_flight.values()
    .filter(|entry| entry.getblocktxn_in_flight).count() as u64;
```

Status contract (packages/open-bitcoin-node/src/status/block_relay_evidence.rs, lines 48-53) — doc comment is only "Aggregate fallback counters safe for operator status surfaces":
```rust
pub struct CompactRelayFallbackCounters {
    pub compact_fallback_count: u64,
    pub compact_timeout_count: u64,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decide and apply the D-06 fallback-counter outcome</name>
  <files>packages/open-bitcoin-node/src/network/block_relay_evidence.rs, packages/open-bitcoin-node/src/status/block_relay_evidence.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/status/block_relay_evidence.rs
    packages/open-bitcoin-node/src/status/block_relay_evidence/tests.rs
    packages/open-bitcoin-node/src/sync/tests.rs
    scripts/check-phase128-production-compact-announcement-transport.ts
    scripts/check-phase116-operator-block-relay-evidence.ts
  </read_first>
  <behavior>
    - Test A (fix path): a snapshot taken while one peer has a live `getblocktxn_in_flight` entry and zero recorded Timeout cleanups reports `fallback.compact_timeout_count == 0` and `in_flight.getblocktxn_in_flight_count == 1`.
    - Test B (both paths): a recorded `CompactDownloadCleanupCause::Timeout` cleanup of one entry reports `fallback.compact_timeout_count == 1` and `fallback.compact_fallback_count == 1`.
  </behavior>
  <action>
This task implements locked decisions D-05, D-06, and D-07.

Decision procedure (execute in order, record the outcome and evidence in the summary):
1. Search for evidence that the inclusive semantics are intentional: `rg -n "compact_timeout_count" packages docs` and read every test that asserts a value influenced by `fallback_counters` (known consumers: packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-node/src/status/block_relay_evidence/tests.rs, metrics/logging/CLI test modules). Intentional means: a test name, doc comment, or parity doc explicitly states the fallback timeout counter includes live in-flight requests.
2. If NO such explicit statement exists (expected outcome per 129-RESEARCH.md Pitfall 11), the mixing is a truthfulness defect — a live in-flight `getblocktxn` request is not a timeout, and the same fact is already projected through `in_flight.getblocktxn_in_flight_count`, so the fallback facet double-reports and transiently inflates. Apply the minimal fix:
   - In `packages/open-bitcoin-node/src/network/block_relay_evidence.rs`, delete the snapshot-time addition: remove `fn fallback_counters` entirely and pass `self.fallback` directly in `BlockRelayEvidenceStatus::with_components(...)` (the call site at ~line 88 currently passes `fallback_counters(peer_manager, self.fallback)`).
   - Keep the durable Timeout-cleanup increments (record_cleanup) exactly as they are.
   - Update any test that asserted the inclusive value so it asserts timeouts-only semantics, and add the two focused regression tests from <behavior> (Arrange/Act/Assert, one concept per test) in the existing test module that exercises snapshots with live in-flight state.
   - Add a doc comment on `CompactRelayFallbackCounters.compact_timeout_count` in `packages/open-bitcoin-node/src/status/block_relay_evidence.rs`: `/// Count of compact downloads that fell back after a real timeout cleanup; live in-flight getblocktxn requests are reported only by the in-flight facet.`
3. If an explicit intentional statement IS found, do NOT change runtime code. Instead document: add a doc comment on `compact_timeout_count` stating the field includes live in-flight `getblocktxn` entries at snapshot time and why, and add Test B only.
4. Rust error-handling and style rules apply: no `unwrap()`, `let...else` for early returns, `maybe_` prefix for Options.

Whichever path executes, verify no deterministic checker regresses: the Phase 116 checker requires the fixed label `compact_timeout_count` to keep existing (field names never change), and the Phase 127/128 checkers assert structural patterns in this file that must keep matching.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key cargo-test-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features</automated>
    <automated>bun run scripts/check-phase128-production-compact-announcement-transport.ts</automated>
    <automated>bun run scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts</automated>
    <automated>bun run scripts/check-phase116-operator-block-relay-evidence.ts</automated>
  </verify>
  <acceptance_criteria>
    Exactly one of the two D-06 paths executed, and the summary records which, with the rg evidence that justified it.
    Fix path: `rg -n "counters.compact_timeout_count \+= timed_out" packages/open-bitcoin-node/src/network/block_relay_evidence.rs` produces no match, and the two regression tests from <behavior> pass.
    Document path: `rg -n "in-flight" packages/open-bitcoin-node/src/status/block_relay_evidence.rs` matches a doc comment on compact_timeout_count, and runtime code is unchanged (`git diff --stat packages/open-bitcoin-node/src/network/` empty).
    `CompactRelayFallbackCounters` still has exactly the fields compact_fallback_count and compact_timeout_count.
    cargo test for open-bitcoin-node and the Phase 116/128/129 checkers all exit 0.
  </acceptance_criteria>
  <done>OBS-01 fallback/in-flight truthfulness is either fixed or explicitly documented, with regression tests and a recorded decision.</done>
</task>

<task type="auto">
  <name>Task 2: Register parity breadcrumbs, record the decision, and run full verification</name>
  <files>docs/parity/source-breadcrumbs.json, docs/parity/catalog/p2p.md, docs/metrics/lines-of-code.md</files>
  <read_first>
    docs/parity/source-breadcrumbs.json
    docs/parity/catalog/p2p.md
    scripts/check-parity-breadcrumbs.ts
    docs/parity/index.json
  </read_first>
  <action>
This task implements locked decision D-07 and closes the plan.

1. If Task 1 touched any first-party Rust source or test file, confirm each touched path is registered in `docs/parity/source-breadcrumbs.json` (existing files are already registered; add entries only for genuinely new files, which this plan does not expect to create). Run `bun run scripts/check-parity-breadcrumbs.ts` to prove it.
2. Record the D-06 outcome in `docs/parity/catalog/p2p.md`: add one sentence to the existing compact-relay fallback/evidence discussion stating either "the operator fallback facet's `compact_timeout_count` counts only real timeout cleanups; live in-flight `getblocktxn` requests are reported by the in-flight facet" (fix path) or the documented inclusive semantics (document path). Keep no-claim vocabulary — this file is scanned by the Phase 117 claim checker; do not add public/production/package/filter claims. Do NOT reword the `docs/parity/index.json` entries containing the literal substrings "Phase 128 retains" and "Phase 129 retains" (pinned by the Phase 127 checker).
3. Run the full contract: `bun run scripts/command-timings.ts run --key verify-full -- bash scripts/verify.sh`; poll liveness at least every 60 seconds, never interrupt on estimate alone. Accept LOC report regeneration as required freshness.
4. Confirm no reconciliation-guarded artifact moved: `git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md` must be empty.
5. Commit the whole plan as one batch via `node "$HOME/.cursor/get-shit-done/bin/gsd-tools.cjs" commit "fix(129-02): resolve D-06 fallback counter truthfulness" --files <exact touched files>` (use "docs(129-02): document D-06 fallback counter semantics" if the document path executed).
  </action>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
    <automated>bash scripts/verify.sh</automated>
    <automated>git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONES.md .planning/v2.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    `bun run scripts/check-parity-breadcrumbs.ts` exits 0.
    `docs/parity/catalog/p2p.md` contains the recorded D-06 semantics sentence (grep: `compact_timeout_count`).
    `docs/parity/index.json` still contains the literal substrings "Phase 128 retains" and "Phase 129 retains".
    `bash scripts/verify.sh` exits 0.
    No .planning top-level artifact modified by this plan.
  </acceptance_criteria>
  <done>The D-06 outcome is committed with parity evidence and a clean full verification, ready for the independent verifier to cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime counters → operator status surfaces | Misleading counter semantics misreport OBS-01 truth to operators |
| Rust edit → deterministic checker corpus | block_relay_evidence.rs is guarded by Phase 116/127/128/129 checkers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-02-01 | Repudiation | fallback counter snapshot | mitigate | Fix or explicitly document; regression tests pin the chosen semantics |
| T-129-02-02 | Tampering | guarded Rust corpus | mitigate | Run Phase 116/128/129 checkers plus full verify before commit |
| T-129-02-03 | Information Disclosure | operator evidence | accept | Counters are aggregate u64s; no peer/tx payload involved |
</threat_model>

<verification>
- cargo test for open-bitcoin-node green; Phase 116/128/129 checkers green; full verify green.
- D-06 decision recorded with evidence in the summary and docs/parity/catalog/p2p.md.
</verification>

<success_criteria>
The OBS-01 fallback/in-flight ambiguity is closed with either a minimal truthful fix or explicit documented semantics, all guards stay green, and the decision is auditable for the Phase 129 verifier.
</success_criteria>

<output>
After completion, create `.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-02-SUMMARY.md` with lifecycle frontmatter (`generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 129-2026-07-20T19-28-06`) and `requirements-completed: []` — requirement promotion is deferred to plan 04.
</output>
