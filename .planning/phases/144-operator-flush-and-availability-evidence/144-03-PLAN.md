---
phase: 144-operator-flush-and-availability-evidence
plan: "03"
type: tdd
wave: 2
depends_on:
  - "144-01"
files_modified:
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/metrics/chainstate_durability.rs
  - packages/open-bitcoin-node/src/metrics/tests.rs
  - packages/open-bitcoin-node/src/metrics/tests/relay_projection.rs
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/chainstate_durability.rs
  - packages/open-bitcoin-node/src/logging/tests.rs
  - packages/open-bitcoin-node/src/logging/tests/redaction.rs
  - packages/open-bitcoin-node/src/sync/metrics.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs
  - packages/open-bitcoin-cli/src/operator/support/tests/recovery_progress_inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests/sync_fixtures.rs
autonomous: true
requirements:
  - CSOBS-01
  - CSOBS-02
user_setup: []
must_haves:
  truths:
    - "Fixed MetricKind series expose cache-size class 0/1/2, last-flush-reason class 0..4, write-kind class 0..3, recovery class 0..3, and the three availability counters with no dynamic labels (D-17, CSOBS-02)."
    - "Structured log source chainstate_durability uses cause / outcome / label plus numeric counts and optional height — never coins-best-block hash, peer ids, or outpoints (D-18)."
    - "Support Markdown renders ## Chainstate Durability with the six locked bullets plus the exact Next action sentence; free-text reasons go through existing recursive redaction (D-19)."
    - "Support JSON/Markdown reject coin records, undo blobs, peer identifiers, dynamic labels, raw hex payloads, pruned, and getblock on the new field (D-19, D-11)."
  artifacts:
    - path: packages/open-bitcoin-node/src/metrics/chainstate_durability.rs
      provides: "chainstate_durability_metric_samples"
      contains: "chainstate_durability_metric_samples"
    - path: packages/open-bitcoin-node/src/logging/chainstate_durability.rs
      provides: "CHAINSTATE_DURABILITY_LOG_SOURCE and log record helper"
      contains: "CHAINSTATE_DURABILITY_LOG_SOURCE"
    - path: packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs
      provides: "Support heading and locked bullets"
      contains: "## Chainstate Durability"
    - path: packages/open-bitcoin-cli/src/operator/support/redaction.rs
      provides: "redact_chainstate_durability"
      contains: "redact_chainstate_durability"
  key_links:
    - from: packages/open-bitcoin-node/src/status/chainstate_durability.rs
      to: packages/open-bitcoin-node/src/metrics/chainstate_durability.rs
      via: "chainstate_durability_metric_samples reads FieldAvailability"
      pattern: "chainstate_durability_metric_samples"
    - from: packages/open-bitcoin-node/src/sync/metrics.rs
      to: packages/open-bitcoin-node/src/metrics/chainstate_durability.rs
      via: "persist_metrics appends durability samples from operator snapshot"
      pattern: "chainstate_durability_metric_samples"
    - from: packages/open-bitcoin-cli/src/operator/support/render.rs
      to: packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs
      via: "push after block-relay evidence"
      pattern: "push_chainstate_durability"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 144-2026-09-17T11-23-17
generated_at: 2026-09-17T12:30:00.000Z
---

# Phase 144 Plan 03: Metrics, Logs, And Support Redaction

<objective>
Project the shared `chainstate_durability` field into fixed `MetricKind` series, an allowlisted structured log, and sanitized support Markdown/JSON.

Purpose: CSOBS-01 requires metrics, logs, and support. CSOBS-02 must appear as numeric cache-size and last-flush-reason classes. High-cardinality labels, coin dumps, and prune/archive copy are High-severity blockers (D-17, D-18, D-19).

Output: seven new MetricKind variants, `chainstate_durability_metric_samples`, `chainstate_durability_log_record`, support heading/bullets, `redact_chainstate_durability`. No RPC/CLI/dashboard row work (Plan 02). No docs/checker (Plan 04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-operator-flush-and-availability-evidence/144-01-PLAN.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/metrics/block_relay.rs
@packages/open-bitcoin-node/src/logging.rs
@packages/open-bitcoin-node/src/sync/metrics.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-cli/src/operator/support/redaction.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/support/render/block_relay.rs
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-node/src/metrics.rs today: `MetricKind::ALL` is `[Self; 74]`. Adding seven variants requires `ALL: [Self; 81]` and every match/array that names the old length.

Locked serde names (UI-SPEC):

| Variant | serde | Sample |
| --- | --- | --- |
| `ChainstateDurabilityCacheSizeClass` | `chainstate_durability_cache_size_class` | 0/1/2 for ok/large/critical |
| `ChainstateDurabilityLastFlushReasonClass` | `chainstate_durability_last_flush_reason_class` | 0..4 for none/needed/periodic/always/failed_disk |
| `ChainstateDurabilityWriteKindClass` | `chainstate_durability_write_kind_class` | 0..3 for none/flush/sync/refuse_disk_space |
| `ChainstateDurabilityRecoveryClass` | `chainstate_durability_recovery_class` | 0..3 for consistent/replayed/interrupted/fail_closed |
| `ChainstateDurabilityAvailableCount` | `chainstate_durability_available_count` | counter |
| `ChainstateDurabilityUnavailableCount` | `chainstate_durability_unavailable_count` | counter |
| `ChainstateDurabilityIndexKnownWithoutPayloadCount` | `chainstate_durability_index_known_without_payload_count` | counter |

Cache-size sample is the current None-mode class from the snapshot, not last write (Pitfall 2 / CSOBS-02).

From packages/open-bitcoin-node/src/logging.rs:

```rust
pub const BLOCK_RELAY_LOG_SOURCE: &str = "block_relay";
pub fn block_relay_log_record(...) -> StructuredLogRecord;
```

`looks_like_hex_material` redacts hex length >= 16. Do not put coins-best-block hash in the log message (D-18).

Locked log example:

`outcome=projected cause=status_projection label=chainstate_durability cache_size=ok last_flush_reason=periodic recovery_outcome=replayed last_serving_status=unavailable available_count=0 unavailable_count=1`

Support heading and bullets (exact):

```text
## Chainstate Durability
- Chainstate durability: {value}
- Cache occupancy: {value}
- Coins best-block: {value}
- Coins recovery: {value}
- Have-bytes: {value}
- Have-bytes counts: {value}
- Next action: Treat flush, coins recovery, cache-size, and have-bytes versus do-not as bounded local operator status. This is not prune-mode, archive-node serving, public-default historical serving, or production readiness.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed MetricKind series and samples</name>
  <files>
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/metrics/chainstate_durability.rs
    packages/open-bitcoin-node/src/metrics/tests.rs
    packages/open-bitcoin-node/src/metrics/tests/relay_projection.rs
    packages/open-bitcoin-node/src/sync/metrics.rs
    packages/open-bitcoin-node/src/lib.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/metrics/block_relay.rs
    packages/open-bitcoin-node/src/sync/metrics.rs
    packages/open-bitcoin-node/src/status/chainstate_durability.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `chainstate_durability_metric_kinds_are_low_cardinality_gauges_and_counters`: `chainstate_durability_metric_samples` emits exactly the seven locked kinds; serde names end with `_class` or `_count`; no dynamic label map (D-17).
    - Test `chainstate_durability_metric_status_maps_to_each_fixed_metric_kind`: Available snapshot with `cache_size=critical`, `last_flush_reason=periodic`, `recovery_outcome=replayed`, counters 3/1/1 maps to classes 2, 2, and matching counters (CSOBS-02).
    - Test `chainstate_durability_metric_unavailable_emits_no_samples`: Unavailable field returns an empty vec (do not fabricate 0/0/0 as live occupancy) (D-02).
    - Test `chainstate_durability_metric_samples_omit_hash_peer_and_pruned`: sample debug/serde contains none of a 64-hex string, `peer_id`, `pruned`, or per-block dimensions (D-12, D-17).
    - Test `metric_kind_all_len_is_81`: `MetricKind::ALL.len() == 81` after the seven additions (Pitfall 7).
  </behavior>
  <action>
    Per D-17: add the seven `MetricKind` variants immediately after `MempoolAdmissionStillPresent`. Update `ALL` to `[Self; 81]` and append the seven variants at the end of the array in the same order. `pub use` `chainstate_durability_metric_samples` from `metrics.rs` like `block_relay_metric_samples`.

    Create `metrics/chainstate_durability.rs` (`foo.rs` + `foo/` if tests need a child). Function:

    ```rust
    pub fn chainstate_durability_metric_samples(
        durability: &FieldAvailability<ChainstateDurabilityEvidence>,
        timestamp_unix_seconds: u64,
    ) -> Vec<MetricSample>;
    ```

    Class integers: cache_size ok=0 large=1 critical=2; last_flush_reason none=0 needed=1 periodic=2 always=3 failed_disk=4; write_kind none=0 flush=1 sync=2 refuse_disk_space=3; recovery consistent=0 replayed=1 interrupted=2 fail_closed=3. Do not add one series per enum value.

    In `DurableSyncRuntime::persist_metrics`, after the mempool_policy samples block, if `self.network.operator_snapshot()` is `Ok`, extend with `chainstate_durability_metric_samples(snapshot.chainstate_durability(), timestamp)`. Do not bind these series to `DASHBOARD_METRIC_KINDS`.

    Update any test that hard-codes `MetricKind::ALL.len() == 74` or exhaustively matches kinds. Combined RED+GREEN allowed. File-header `none` on the new metrics module.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-03-metrics -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_metric -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ChainstateDurabilityCacheSizeClass' packages/open-bitcoin-node/src/metrics.rs` matches
    - `rg -n 'pub const ALL: \[Self; 81\]' packages/open-bitcoin-node/src/metrics.rs` matches
    - `rg -n 'pub fn chainstate_durability_metric_samples' packages/open-bitcoin-node/src/metrics/chainstate_durability.rs` matches
    - `rg -n 'chainstate_durability_metric_samples' packages/open-bitcoin-node/src/sync/metrics.rs` matches
    - `rg -n 'HashMap|dynamic_label' packages/open-bitcoin-node/src/metrics/chainstate_durability.rs` returns no matches
  </acceptance_criteria>
  <done>Seven fixed MetricKind series project cache-size, last flush reason, write-kind, recovery, and three availability counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Allowlisted structured log record</name>
  <files>
    packages/open-bitcoin-node/src/logging.rs
    packages/open-bitcoin-node/src/logging/chainstate_durability.rs
    packages/open-bitcoin-node/src/logging/tests.rs
    packages/open-bitcoin-node/src/logging/tests/redaction.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/logging.rs
    packages/open-bitcoin-node/src/logging/tests/redaction.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `chainstate_durability_log_record_uses_fixed_source_labels_and_counts`: source is `chainstate_durability`; message contains `outcome=projected`, `cause=status_projection`, `label=chainstate_durability`, `cache_size=`, `last_flush_reason=`, `recovery_outcome=`, and integer counts (D-18, CSOBS-02).
    - Test `chainstate_durability_log_record_omits_sensitive_and_dynamic_material`: message contains no 64-char hex, `peer_id=`, `127.0.0.1:`, outpoint `txid:vout`, `pruned`, `getblock`, or `credential=` (D-18, D-11, D-12).
    - Test `initialize_fail_closed_can_emit_log_label`: mapping a fail-closed `StorageError` Display through the helper (or a thin `fail_closed` constructor) produces `recovery_outcome=fail_closed` without a hash (D-10, Pitfall 4).
  </behavior>
  <action>
    Per D-18: create `logging/chainstate_durability.rs` so `logging.rs` stays under the 628-line trigger. Add `mod chainstate_durability;` and `pub use chainstate_durability::{CHAINSTATE_DURABILITY_LOG_SOURCE, chainstate_durability_log_record};`.

    ```rust
    pub const CHAINSTATE_DURABILITY_LOG_SOURCE: &str = "chainstate_durability";

    pub fn chainstate_durability_log_record(
        durability: &FieldAvailability<ChainstateDurabilityEvidence>,
        timestamp_unix_seconds: u64,
    ) -> StructuredLogRecord;
    ```

    Available path: format the locked message with machine labels and counts. Include optional `height=` only when `maybe_coins_best_block_height` is Some. Never include `maybe_coins_best_block_hash`. Unavailable path: `outcome=unavailable cause=status_projection label=chainstate_durability` plus the reason only after existing sanitizers (`sanitized_resource_log_field` or equivalent) so hex/peer/credential reasons become `redacted_*`.

    In `sync/runtime_state.rs` next to the `block_relay_log_record` emit, also emit `chainstate_durability_log_record` from `operator_snapshot().chainstate_durability()` when that snapshot is available.

    If `initialize` already prefixes `fail_closed` on errors (Plan 01), add a unit test that a log built from that error label contains `fail_closed` and no `H` hash. Do not teach CLI to scan coins. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-03-logs -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_log -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'CHAINSTATE_DURABILITY_LOG_SOURCE' packages/open-bitcoin-node/src/logging/chainstate_durability.rs` matches
    - `rg -n 'pub fn chainstate_durability_log_record' packages/open-bitcoin-node/src/logging/chainstate_durability.rs` matches
    - `rg -n 'chainstate_durability_log_record' packages/open-bitcoin-node/src/sync/runtime_state.rs` matches
    - `rg -n 'maybe_coins_best_block_hash' packages/open-bitcoin-node/src/logging/chainstate_durability.rs` returns no matches
    - `rg -n 'peer_id=' packages/open-bitcoin-node/src/logging/chainstate_durability.rs` returns no matches
  </acceptance_criteria>
  <done>Allowlisted chainstate_durability logs carry cause/outcome/label and counts without hashes or peer ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Support bullets and redaction</name>
  <files>
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-cli/src/operator/support/render.rs
    packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
    packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs
    packages/open-bitcoin-cli/src/operator/support/tests/recovery_progress_inbound.rs
    packages/open-bitcoin-cli/src/operator/support/tests/sync_fixtures.rs
  </files>
  <read_first>
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-cli/src/operator/support/render.rs
    packages/open-bitcoin-cli/src/operator/support/render/block_relay.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `support_bundle_renders_chainstate_durability_from_shared_projection`: Markdown contains `## Chainstate Durability` and the six bullets plus the exact Next action sentence; JSON `status.chainstate_durability` matches the shared field (D-19, CSOBS-01, CSOBS-02).
    - Test `support_bundle_redacts_sensitive_chainstate_durability_reasons_in_json_and_markdown`: Unavailable reason containing `127.0.0.1:`, `peer_id=`, a 64-hex payload, or `credential=` becomes `redacted_chainstate_durability_evidence` (or the existing recursive redaction token) in both JSON and Markdown (D-19).
    - Test `support_bundle_chainstate_durability_keeps_counts_and_tip_hash_pattern`: Available counts stay numeric; coins best-block hash may remain as the existing tip-hash exception; outpoints and undo blobs do not (D-09, D-19).
    - Test `support_bundle_chainstate_durability_forbids_prune_and_getblock_copy`: Markdown/JSON for the new section contain none of `pruned`, `getblock`, `archive-node`, `production ready` (D-11, D-23).
  </behavior>
  <action>
    Per D-19 and UI-SPEC: create `support/render/chainstate_durability.rs` with `pub(super) fn push_chainstate_durability(output: &mut String, value: &FieldAvailability<ChainstateDurabilityEvidence>)`. Reuse CLI value formats (including `OK`/`LARGE`/`CRITICAL`). After `block_relay::push_block_relay_evidence(...)` in `support/render.rs`, call the new pusher. One heading, short bullets, no tables.

    In `redaction.rs`, add `pub(crate) fn redact_chainstate_durability(value: &mut FieldAvailability<ChainstateDurabilityEvidence>)` and call it from `support_status_for_bundle` next to `redact_block_relay_evidence`. Sanitize only free-text Unavailable reasons through the existing recursive/hex/peer/credential needles. Leave counts and fixed enums. Coins best-block hash follows the tip-hash exception already used for `maybe_validated_active_chain_hash` — do not add a new hash table. Reject raw hex that is not that single 64-char tip field.

    Add fixtures in `support/tests/sync_fixtures.rs` mirroring `phase116_status_with_block_relay_evidence`. File-header `none`. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-03-support -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib support_bundle_renders_chainstate_durability support_bundle_redacts_sensitive_chainstate_durability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '## Chainstate Durability' packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs` matches
    - `rg -n 'This is not prune-mode, archive-node serving, public-default historical serving, or production readiness.' packages/open-bitcoin-cli/src/operator/support/render/chainstate_durability.rs` matches
    - `rg -n 'fn redact_chainstate_durability' packages/open-bitcoin-cli/src/operator/support/redaction.rs` matches
    - `rg -n 'redact_chainstate_durability' packages/open-bitcoin-cli/src/operator/support/redaction.rs` matches inside `support_status_for_bundle`
    - `rg -n 'push_chainstate_durability' packages/open-bitcoin-cli/src/operator/support/render.rs` matches
  </acceptance_criteria>
  <done>Support bundles render the locked durability bullets and redact free-text without dumping coins, peers, or prune/getblock copy.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Snapshot → metrics store | Series must stay closed and low-cardinality |
| Snapshot → structured logs | Hex sanitizer will redact hashes; do not put hashes in the message |
| Snapshot → support bundle | Highest-risk export; recursive redaction is the last gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-144-03-01 | Information Disclosure / DoS | `MetricKind` | mitigate | High: closed enum only; no per-hash/per-peer/per-outpoint series (D-17). Block if `ALL` grows via dynamic names. |
| T-144-03-02 | Information Disclosure | `chainstate_durability_log_record` | mitigate | High: no coins-best-block hash in log fields; sanitizer already treats >=16 hex as sensitive (D-18). |
| T-144-03-03 | Information Disclosure | support Markdown/JSON | mitigate | High: `redact_chainstate_durability` plus tests for peer/credential/hex/coin-dump needles (D-19). |
| T-144-03-04 | Spoofing | support Next action / log copy | mitigate | High: exact Next action forbids prune-mode, archive-node, public-default historical serving, and production readiness (D-13, D-23). |
| T-144-03-05 | Tampering | invented tip in logs | mitigate | Fail-closed log path emits `recovery_outcome=fail_closed` without height/hash (D-10). |
</threat_model>

<verification>
```bash
bun run scripts/command-timings.ts run --key phase144-03-metrics -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_metric -- --nocapture
bun run scripts/command-timings.ts run --key phase144-03-logs -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_log -- --nocapture
bun run scripts/command-timings.ts run --key phase144-03-support -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib support_bundle_renders_chainstate_durability support_bundle_redacts_sensitive_chainstate_durability -- --nocapture
```
</verification>

<success_criteria>
- D-17: seven fixed MetricKind series; `ALL` length 81; no dynamic labels.
- D-18: log source `chainstate_durability` with cause/outcome/label and no hash.
- D-19: support heading, six bullets, Next action, redaction.
- CSOBS-02: cache-size class and last-flush-reason class are sampled from the snapshot.
</success_criteria>

<output>
After completion, create `.planning/phases/144-operator-flush-and-availability-evidence/144-03-SUMMARY.md`
</output>
