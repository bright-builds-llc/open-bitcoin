---
phase: 144-operator-flush-and-availability-evidence
plan: "02"
type: tdd
wave: 2
depends_on:
  - "144-01"
files_modified:
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/tests/rendering_and_service.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests/projection.rs
autonomous: true
requirements:
  - CSOBS-01
  - CSOBS-02
user_setup: []
must_haves:
  truths:
    - "openbitcoinnetworkstatus JSON includes top-level chainstate_durability copied from ManagedNetworkOperatorSnapshot; getblockchaininfo and getnetworkinfo stay unchanged (D-14)."
    - "Live CLI JSON copies RPC chainstate_durability; human status prints the six locked Label: value lines immediately after the block-relay cluster and before Wallet: (D-15, UI-SPEC)."
    - "Dashboard appends the same six rows after block_relay_rows inside Mempool and Wallet; MAX_DASHBOARD_CHARTS remains 8 (D-16)."
    - "Unavailable fields render Unavailable: {reason} using the snapshot reason; cache_size human tokens are OK / LARGE / CRITICAL (CSOBS-02, UI-SPEC)."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/method/node.rs
      provides: "OpenBitcoinNetworkStatusResponse.chainstate_durability"
      contains: "pub chainstate_durability"
    - path: packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs
      provides: "Six locked human Label: value lines"
      contains: "Chainstate durability:"
    - path: packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs
      provides: "Six locked dashboard rows"
      contains: "\"Chainstate durability\""
  key_links:
    - from: packages/open-bitcoin-rpc/src/dispatch/node.rs
      to: packages/open-bitcoin-node/src/network/types.rs
      via: "snapshot.chainstate_durability().clone()"
      pattern: "chainstate_durability:"
    - from: packages/open-bitcoin-cli/src/operator/status.rs
      to: packages/open-bitcoin-rpc/src/method/node.rs
      via: "live collect copies network_status.chainstate_durability"
      pattern: "chainstate_durability"
    - from: packages/open-bitcoin-cli/src/operator/dashboard/model.rs
      to: packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs
      via: "extend after block_relay_rows"
      pattern: "chainstate_durability_rows"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 144-2026-09-17T11-23-17
generated_at: 2026-09-17T12:30:00.000Z
---

# Phase 144 Plan 02: RPC, CLI, And Dashboard Projection

<objective>
Fan the Plan 01 `chainstate_durability` field through `openbitcoinnetworkstatus`, live/stopped CLI JSON and human status, and Ratatui dashboard rows without a second derivation or a ninth chart.

Purpose: Live CLI already copies `OpenBitcoinNetworkStatusResponse`, not the in-process snapshot (Pitfall 1). CSOBS-01 requires RPC + CLI + dashboard. CSOBS-02 must appear as human `OK` / `LARGE` / `CRITICAL` plus `last_flush_reason`.

Output: RPC field, CLI collect/render, six dashboard rows, focused tests. No metrics, logs, support, or docs (Plans 03–04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-operator-flush-and-availability-evidence/144-01-PLAN.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-CONTEXT.md
@packages/open-bitcoin-rpc/src/method/node.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From Plan 01 (read 144-01-SUMMARY.md if names differ):
`OpenBitcoinStatusSnapshot.chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>`
`ManagedNetworkOperatorSnapshot::chainstate_durability()`
`CHAINSTATE_DURABILITY_UNAVAILABLE_REASON`

From packages/open-bitcoin-rpc/src/dispatch/node.rs today:

```rust
Ok(OpenBitcoinNetworkStatusResponse {
    inbound: snapshot.inbound().clone(),
    relay: snapshot.relay().clone(),
    block_relay: snapshot.block_relay().clone(),
    metrics: context.metrics_status(),
    mempool: { /* ... */ },
})
```

From packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs the exact-key test currently expects:

```rust
vec!["block_relay", "inbound", "mempool", "metrics", "relay"]
```

Add `"chainstate_durability"` to that sorted key list.

From packages/open-bitcoin-cli/src/operator/status.rs live collect:

```rust
let block_relay = network_status.block_relay;
// ADD: let chainstate_durability = network_status.chainstate_durability;
```

Stopped / RPC-failure constructors already received Plan 01 `default_unavailable()` compile fixes. Live path must copy the RPC field. RPC-failure fallback in `collect_open_bitcoin_network_status` must set `chainstate_durability` to Unavailable with `openbitcoinnetworkstatus unavailable` / `openbitcoinnetworkstatus unavailable: {detail}` (UI-SPEC), not invent values.

Locked human/dashboard lines (exact labels, immediately after block-relay, before `Wallet:`):

```text
Chainstate durability: cache_size={OK|LARGE|CRITICAL} last_flush_reason={none|needed|periodic|always|failed_disk} write_kind={none|flush|sync|refuse_disk_space} readiness={not_ready|ready_to_flush}
Cache occupancy: cache_bytes={n} cache_byte_limit={n}
Coins best-block: height={n} hash={64-hex}
Coins recovery: {consistent|replayed|interrupted|fail_closed}
Have-bytes: {available|unavailable} payload_present={true|false} index_known={true|false} validated_on_active_chain={true|false}
Have-bytes counts: available_count={n} unavailable_count={n} index_known_without_payload_count={n}
```

When the whole field is Unavailable, each of the six lines/rows is `Unavailable: {reason}` with that same reason. Do not insert blank lines between the six CLI lines. Do not color-code OK / available.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish chainstate_durability on openbitcoinnetworkstatus</name>
  <files>
    packages/open-bitcoin-rpc/src/method/node.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs
  </files>
  <read_first>
    packages/open-bitcoin-rpc/src/method/node.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs
    packages/open-bitcoin-node/src/network/types.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-01-SUMMARY.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `open_bitcoin_network_status_includes_chainstate_durability_projection`: live dispatch JSON has `chainstate_durability` equal to `serde_json::to_value(snapshot.chainstate_durability())` (D-01, D-14).
    - Test `open_bitcoin_network_status_exact_keys_include_chainstate_durability`: object keys sort to `block_relay`, `chainstate_durability`, `inbound`, `mempool`, `metrics`, `relay` (Pitfall 1).
    - Test `getblockchaininfo_and_getnetworkinfo_shapes_unchanged`: existing getblockchaininfo / getnetworkinfo tests still pass and those response structs gain no `chainstate_durability` key (D-14).
    - Test `open_bitcoin_network_status_omits_getblock_and_pruned_on_durability`: the durability object has no `getblock`, `pruned`, or `peer_id` keys (D-04, D-11).
  </behavior>
  <action>
    Per D-14 and Pitfall 1: add to `OpenBitcoinNetworkStatusResponse` after `block_relay`:

    ```rust
    #[serde(default)]
    pub chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>,
    ```

    Import `ChainstateDurabilityEvidence` from `open_bitcoin_node::status`. In `open_bitcoin_network_status`, set `chainstate_durability: snapshot.chainstate_durability().clone()`. Thin clone only — no dispatch-layer relabeling, no `decide_flush`, no Fjall.

    Do not register a `getblock` method. Do not add Open Bitcoin keys to `GetBlockchainInfoResponse` or `GetNetworkInfoResponse`.

    Update `network_status_schema.rs` exact-key vector and add the named projection test next to `open_bitcoin_network_status_includes_block_relay_projection`. File-header breadcrumbs already exist on these RPC files; do not edit `source-breadcrumbs.json` (Plan 01 pre-registered / Plan 04 audits). Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-02-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features open_bitcoin_network_status -- --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'pub chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>' packages/open-bitcoin-rpc/src/method/node.rs` matches
    - `rg -n 'chainstate_durability: snapshot.chainstate_durability' packages/open-bitcoin-rpc/src/dispatch/node.rs` matches
    - `rg -n 'chainstate_durability' packages/open-bitcoin-rpc/src/dispatch/tests/network_status_schema.rs` matches
    - `rg -n 'fn getblock|\"getblock\"' packages/open-bitcoin-rpc/src/method.rs packages/open-bitcoin-rpc/src/method/node.rs` does not add a new getblock product
    - `rg -n 'chainstate_durability' packages/open-bitcoin-rpc/src/method/node.rs` does not appear on GetBlockchainInfoResponse
  </acceptance_criteria>
  <done>Open Bitcoin RPC publishes the shared field; baseline Knots-shaped methods are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI JSON collect and six human lines</name>
  <files>
    packages/open-bitcoin-cli/src/operator/status.rs
    packages/open-bitcoin-cli/src/operator/status/render.rs
    packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs
    packages/open-bitcoin-cli/src/operator/status/tests.rs
    packages/open-bitcoin-cli/src/operator/status/tests/rendering_and_service.rs
  </files>
  <read_first>
    packages/open-bitcoin-cli/src/operator/status.rs
    packages/open-bitcoin-cli/src/operator/status/render.rs
    packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `operator_status_chainstate_durability_maps_shared_contract_and_human_lines`: live collect copies RPC `chainstate_durability`; human output contains the six exact labels in order after a `Block relay` line and before `Wallet:`; `cache_size=OK` for machine `ok` (D-15, CSOBS-02, UI-SPEC).
    - Test `operator_status_chainstate_durability_fallback_uses_default_unavailable_contract`: RPC failure uses Unavailable and every durability line is `Unavailable: {reason}` with the same reason (D-02).
    - Test `operator_status_json_serializes_snapshot_chainstate_durability_only`: `--format json` / render JSON includes top-level `chainstate_durability` and no CLI-only coin table or `getblock` object (D-15, D-04).
    - Test `operator_status_human_omits_pruned_and_blank_lines_in_durability_cluster`: the six lines are single-spaced (`\n` joined, no `""` blank line between them) and contain no `pruned` / `archive-node` (D-11, D-13, UI-SPEC).
  </behavior>
  <action>
    Per D-15 and UI-SPEC: create `packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs` with `pub(super) fn chainstate_durability_lines(value: &FieldAvailability<ChainstateDurabilityEvidence>) -> Vec<String>`. Declare `mod chainstate_durability;` in `status/render.rs` next to `mod block_relay;`. After `lines.extend(block_relay_evidence_lines(&snapshot.block_relay));` call `lines.extend(chainstate_durability_lines(&snapshot.chainstate_durability));` then keep `Wallet:`.

    Human `cache_size` mapping: `ok` → `OK`, `large` → `LARGE`, `critical` → `CRITICAL`. All other enum tokens stay snake_case. Available `Coins best-block` uses `height={n} hash={64-hex}` matching existing tip-hash rows. If recovery is `fail_closed` or `interrupted` with both tip fields None, that one line is `Unavailable: {reason}` using a stable reason such as `coins best-block unavailable` — do not invent `H` hashes (D-10). When the wrapper is Unavailable, all six lines use `Unavailable: {reason}` with `reason` from the wrapper.

    In `status.rs` live collect: `let chainstate_durability = network_status.chainstate_durability;` and assign it on the snapshot. In `collect_open_bitcoin_network_status` error fallback, set `chainstate_durability: FieldAvailability::unavailable(inbound-style prefix)` so missing-method reads `openbitcoinnetworkstatus unavailable: {detail}` (UI-SPEC). Do not scan Fjall. Do not add a `getblock` CLI product.

    File-header: `none: Open Bitcoin-only support/infrastructure; no direct Bitcoin Knots source anchor identified.` Do not edit `source-breadcrumbs.json`. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-02-cli -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib operator_status_chainstate_durability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Chainstate durability:' packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs` matches
    - `rg -n 'Have-bytes counts:' packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs` matches
    - `rg -n 'chainstate_durability_lines' packages/open-bitcoin-cli/src/operator/status/render.rs` matches
    - `rg -n 'let chainstate_durability = network_status.chainstate_durability' packages/open-bitcoin-cli/src/operator/status.rs` matches
    - `rg -n 'cache_size=OK|\"OK\"' packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs` matches
    - `rg -n 'getblock|pruned' packages/open-bitcoin-cli/src/operator/status/render/chainstate_durability.rs` returns no production emit strings
  </acceptance_criteria>
  <done>CLI JSON is the shared snapshot field; human status prints the six locked lines after block-relay and before Wallet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dashboard rows after block-relay</name>
  <files>
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/tests/projection.rs
  </files>
  <read_first>
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `dashboard_model_chainstate_durability_rows_surface_shared_status_contract`: `DashboardState::from_snapshot` includes the six exact labels after existing block-relay rows inside `Mempool and Wallet`; values match the human formats (D-16, CSOBS-01, CSOBS-02).
    - Test `dashboard_model_chainstate_durability_rows_preserve_unavailable_reason_without_sensitive_text`: Unavailable snapshot reason is copied as `Unavailable: {reason}` on all six rows; values contain no `127.0.0.1:`, `peer_id=`, or `pruned` (D-02, D-12).
    - Test `dashboard_max_charts_remains_eight`: `MAX_DASHBOARD_CHARTS == 8` and `DASHBOARD_METRIC_KINDS.len() == 8`; new MetricKinds are not appended (D-16).
    - Test `dashboard_sections_remain_five_with_353530_split_unchanged`: still exactly the existing section titles `Node`, sync/peers, `Mempool and Wallet`, `Service`, `Logs and Health` — no sixth section (UI-SPEC).
  </behavior>
  <action>
    Per D-16 and Phase 137 TUI contract: create `dashboard/model/chainstate_durability.rs` with `pub(super) fn chainstate_durability_rows(snapshot: &OpenBitcoinStatusSnapshot) -> Vec<DashboardRow>` using existing `row(label, value)`. Reuse the same value formatters as CLI (duplicate small formatters in this file; do not add a web crate). Labels must be the exact title-case strings: `Chainstate durability`, `Cache occupancy`, `Coins best-block`, `Coins recovery`, `Have-bytes`, `Have-bytes counts`.

    In `dashboard_sections`, after `mempool_and_wallet.extend(block_relay_rows(snapshot));` add `mempool_and_wallet.extend(chainstate_durability_rows(snapshot));`. Keep wallet rows after those (they already come from `mempool_and_wallet_rows` before block-relay — do not reorder wallet ahead of durability; UI-SPEC says durability after block-relay inside the same section). Cyan/Bold stays on section titles only. No new chart, no new keybind, no shadcn.

    Do not edit `MAX_DASHBOARD_CHARTS` or `DASHBOARD_METRIC_KINDS` except to assert they stay 8. File-header `none` breadcrumb. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-02-dashboard -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib dashboard_model_chainstate_durability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '\"Chainstate durability\"' packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs` matches
    - `rg -n 'chainstate_durability_rows' packages/open-bitcoin-cli/src/operator/dashboard/model.rs` matches
    - `rg -n 'MAX_DASHBOARD_CHARTS: usize = 8' packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs` matches
    - `rg -n 'Color::Green|Color::Cyan' packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs` returns no matches
    - `rg -n 'getblock|pruned' packages/open-bitcoin-cli/src/operator/dashboard/model/chainstate_durability.rs` returns no matches
  </acceptance_criteria>
  <done>Dashboard shows six one-line rows after block-relay inside Mempool and Wallet; eight charts and the 35/35/30 layout stay unchanged.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Managed snapshot → RPC JSON | Authenticated Open Bitcoin status is still not a coin dump or getblock |
| RPC JSON → CLI/dashboard | Renderers must not reclassify or scan datadir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-144-02-01 | Information Disclosure | `openbitcoinnetworkstatus` | mitigate | High: clone shared field only. Tests forbid `getblock`, coin tables, and `pruned` on the new object (D-04, D-09, D-11). |
| T-144-02-02 | Spoofing | baseline RPC | mitigate | `getblockchaininfo` / `getnetworkinfo` gain no Open Bitcoin keys (D-14). |
| T-144-02-03 | Tampering | CLI/dashboard renderers | mitigate | Renderers format snapshot enums only; no `decide_flush` / Fjall in `open-bitcoin-cli` (D-01). |
| T-144-02-04 | Information Disclosure | coins best-block hash | accept | Height+64-hex on status/CLI/dashboard matches existing tip-hash practice (D-09). Not a UTXO dump. |
| T-144-02-05 | Spoofing | prune/archive copy | mitigate | High: human/dashboard modules must not emit `pruned`, `archive-node`, or `public default` (D-13, D-23). |
</threat_model>

<verification>
```bash
bun run scripts/command-timings.ts run --key phase144-02-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features open_bitcoin_network_status -- --test-threads=1 --nocapture
bun run scripts/command-timings.ts run --key phase144-02-cli -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib operator_status_chainstate_durability -- --nocapture
bun run scripts/command-timings.ts run --key phase144-02-dashboard -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib dashboard_model_chainstate_durability -- --nocapture
```
</verification>

<success_criteria>
- D-14: Open Bitcoin RPC carries the field; baseline methods unchanged.
- D-15: CLI JSON is the snapshot; six human lines after block-relay before Wallet.
- D-16: Six dashboard rows; `MAX_DASHBOARD_CHARTS = 8`.
- CSOBS-02: human `OK` / `LARGE` / `CRITICAL` plus `last_flush_reason`.
- No getblock, no ninth chart, no web UI.
</success_criteria>

<output>
After completion, create `.planning/phases/144-operator-flush-and-availability-evidence/144-02-SUMMARY.md`
</output>
