---
phase: 144-operator-flush-and-availability-evidence
plan: "04"
type: execute
wave: 3
depends_on:
  - "144-02"
  - "144-03"
files_modified:
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/operator/runtime-guide.md
  - docs/parity/source-breadcrumbs.json
  - scripts/check-phase144-operator-flush-availability-evidence.ts
  - scripts/check-phase144-operator-flush-availability-evidence.test.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - CSOBS-01
  - CSOBS-02
user_setup: []
must_haves:
  truths:
    - "status-snapshot.md, operator-observability.md, and runtime-guide.md document chainstate_durability, FieldAvailability, cache-size OK/LARGE/CRITICAL, last flush reason, and have-bytes versus do-not without prune/archive/public-default/production-readiness claims (D-20, D-23)."
    - "runtime-guide.md includes copy-pasteable repo-local Cargo and Bazel status/support inspection commands (D-20, AGENTS.md)."
    - "Deterministic checker proves RPC/CLI JSON, dashboard rows, metrics/log labels, and support redaction agree on CSOBS fields or the same unavailable reasons (D-21)."
    - "Every new or touched first-party Rust file under packages/open-bitcoin-*/src or tests has parity breadcrumbs; verify.sh runs the Phase 144 checker after the Phase 116 steps (D-22, D-23)."
  artifacts:
    - path: docs/architecture/status-snapshot.md
      provides: "Shared field contract for chainstate_durability"
      contains: "chainstate_durability"
    - path: docs/operator/runtime-guide.md
      provides: "Copy-pasteable Cargo and Bazel inspection commands"
      contains: "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format"
    - path: scripts/check-phase144-operator-flush-availability-evidence.ts
      provides: "Cross-surface CSOBS checker"
      contains: "CSOBS-01"
    - path: scripts/check-phase144-operator-flush-availability-evidence.test.ts
      provides: "Checker pass/fail fixtures"
      contains: "check-phase144"
    - path: scripts/verify.sh
      provides: "Default verifier wiring"
      contains: "check-phase144-operator-flush-availability-evidence"
  key_links:
    - from: scripts/check-phase144-operator-flush-availability-evidence.ts
      to: scripts/verify.sh
      via: "bun test then bun run after Phase 116"
      pattern: "check-phase144-operator-flush-availability-evidence"
    - from: docs/operator/runtime-guide.md
      to: scripts/check-phase144-operator-flush-availability-evidence.ts
      via: "REQUIRED_RUNTIME_COMMANDS corpus"
      pattern: "status --format"
    - from: docs/parity/source-breadcrumbs.json
      to: packages/open-bitcoin-node/src/status/chainstate_durability.rs
      via: "node-status-contract group"
      pattern: "status/chainstate_durability.rs"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 144-2026-09-17T11-23-17
generated_at: 2026-09-17T12:30:00.000Z
---

# Phase 144 Plan 04: Docs, Checker, Breadcrumbs, And Verify Wiring

<objective>
Document the `chainstate_durability` contract, add the deterministic cross-surface checker, finish parity-breadcrumb coverage, and wire both into `bash scripts/verify.sh`.

Purpose: D-20 / D-21 / D-22 / D-23. Phase 145 still owns CSVFY no-claim guardrails; this plan must describe have-bytes versus do-not and must not close those later claims.

Output: three operator/architecture doc updates, `scripts/check-phase144-operator-flush-availability-evidence.ts` plus test, verify.sh steps, breadcrumb audit.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-operator-flush-and-availability-evidence/144-CONTEXT.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
@scripts/check-phase116-operator-block-relay-evidence.ts
@scripts/check-phase116-operator-block-relay-evidence.test.ts
@scripts/verify.sh
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/operator/runtime-guide.md
@docs/parity/source-breadcrumbs.json
@AGENTS.md

<interfaces>
Copy `scripts/check-phase116-operator-block-relay-evidence.ts` structure. Recommended constants:

```ts
const REQUIRED_REQUIREMENTS = ["CSOBS-01", "CSOBS-02"] as const;
const REQUIRED_SYMBOLS = [
  "ChainstateDurabilityEvidence",
  "chainstate_durability",
  "project_chainstate_durability",
  "record_have_bytes",
  "MetricKind::ChainstateDurabilityCacheSizeClass",
  "CHAINSTATE_DURABILITY_LOG_SOURCE",
  "chainstate_durability_log_record",
  "chainstate_durability_metric_samples",
  "openbitcoinnetworkstatus",
  "redact_chainstate_durability",
] as const;
```

Required behavior tests (exact names from Plans 01–03):

```text
chainstate_durability_default_unavailable_uses_stable_reason
execute_flush_retains_last_write_decision_not_later_none_classification
have_bytes_accumulator_sets_unavailable_when_payload_absent
open_bitcoin_network_status_includes_chainstate_durability_projection
operator_status_chainstate_durability_maps_shared_contract_and_human_lines
dashboard_model_chainstate_durability_rows_surface_shared_status_contract
dashboard_max_charts_remains_eight
chainstate_durability_metric_kinds_are_low_cardinality_gauges_and_counters
chainstate_durability_log_record_omits_sensitive_and_dynamic_material
support_bundle_renders_chainstate_durability_from_shared_projection
support_bundle_redacts_sensitive_chainstate_durability_reasons_in_json_and_markdown
```

Forbidden needles on the new field / its docs: `pruned`, `block_status_pruned`, `getblock`, `peer_id=`, `127.0.0.1:`, outpoint-shaped `txid:vout`, `cmpctblock`, `archive-node`, `archive node`, `prune mode`, `public default`, `public-default historical serving`, `production ready`, `production readiness`, `assumeutxo`, `compact filter`.

Existing `block_relay.block_serving.status.pruned_count` may remain on the old surface. Do not document it as prune-mode.

`REQUIRED_RUNTIME_COMMANDS` must include all four of:

```text
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format human
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format json
bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format human
bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format json
```

plus Cargo/Bazel `support bundle` twins and `bash scripts/verify.sh`. Public-network review stays opt-in UAT only (D-20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the shared field and repo-local UAT commands</name>
  <files>
    docs/architecture/status-snapshot.md
    docs/architecture/operator-observability.md
    docs/operator/runtime-guide.md
  </files>
  <read_first>
    docs/architecture/status-snapshot.md
    docs/architecture/operator-observability.md
    docs/operator/runtime-guide.md
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    AGENTS.md
    standards/core/verification.md
  </read_first>
  <action>
    Per D-20, D-23, CSOBS-01, CSOBS-02: extend the three docs. Do not add a `getblock` section. Do not claim prune-mode, archive-node, assumeutxo, compact filters, public defaults, or production readiness. Phase 145 still owns CSVFY-01 / CSVFY-02.

    In `docs/architecture/status-snapshot.md`:
    - Add `chainstate_durability` as a top-level `FieldAvailability<ChainstateDurabilityEvidence>` field on `OpenBitcoinStatusSnapshot`.
    - State it is distinct from `recovery_evidence` (sync lock/corruption taxonomy) and `block_relay` (serving/compact counters).
    - List the locked machine field names and `Unavailable: {reason}` rule.
    - State cache_size is current None-mode occupancy (`ok`/`large`/`critical`) and last_flush_reason is the last real execute_flush reason.
    - State fail-closed / interrupted-without-B must not present an invented coins tip.

    In `docs/architecture/operator-observability.md`:
    - Add the seven fixed MetricKind serde names and the log source `chainstate_durability`.
    - Repeat: no dynamic labels; no coins-best-block hash in log fields; support uses recursive redaction.

    In `docs/operator/runtime-guide.md`:
    - Add an inspection subsection that uses have-bytes versus do-not wording.
    - Include the exact Cargo and Bazel commands from the interfaces block (not only the `open-bitcoin` alias).
    - State public-network review is opt-in UAT only.
    - Include the support Next action sentence or a sentence that is not readable as archive-node honesty.

    Write sentences that a later checker can grep: `chainstate_durability`, `cache_size`, `last_flush_reason`, `have-bytes`, `Unavailable:`. Forbidden claim phrases must not appear except inside an explicit "this is not ..." negation if the runtime-guide Next action is copied.
  </action>
  <verify>
    <automated>rg -n 'chainstate_durability' docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/operator/runtime-guide.md && rg -n 'cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format' docs/operator/runtime-guide.md && rg -n 'bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format' docs/operator/runtime-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/status-snapshot.md` contains `chainstate_durability` and `FieldAvailability`
    - `docs/architecture/status-snapshot.md` contains `last_flush_reason` and `cache_size`
    - `docs/architecture/operator-observability.md` contains `chainstate_durability_cache_size_class` and `CHAINSTATE_DURABILITY_LOG_SOURCE` or `chainstate_durability` log source
    - `docs/operator/runtime-guide.md` contains `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format human`
    - `docs/operator/runtime-guide.md` contains `bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format json`
    - `rg -n 'getblock|archive-node serving|production readiness' docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/operator/runtime-guide.md` matches only explicit "this is not" / "do not" / "not" negations, never a positive claim
  </acceptance_criteria>
  <done>Operator docs describe the shared field, CSOBS-02 cache-size and last flush reason, and copy-pasteable Cargo/Bazel inspection commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checker, breadcrumbs, and verify.sh</name>
  <files>
    scripts/check-phase144-operator-flush-availability-evidence.ts
    scripts/check-phase144-operator-flush-availability-evidence.test.ts
    scripts/verify.sh
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    scripts/check-phase116-operator-block-relay-evidence.ts
    scripts/check-phase116-operator-block-relay-evidence.test.ts
    scripts/verify.sh
    docs/parity/source-breadcrumbs.json
    scripts/check-parity-breadcrumbs.ts
    standards/core/verification.md
  </read_first>
  <behavior>
    - Test `phase144 checker passes against the live repo` in `check-phase144-operator-flush-availability-evidence.test.ts`.
    - Test `phase144 checker fails when CSOBS-01 or CSOBS-02 is missing from REQUIRED_REQUIREMENTS` via a fixture mutation.
    - Test `phase144 checker fails when getblock or pruned appears on the new-field corpus` via a fixture that injects those needles into a fake durability module.
    - Test `verify.sh lists both bun test and bun run for the Phase 144 checker` after the Phase 116 pair in both the fast bun block (~line 392) and the `run_step` block (~line 556).
  </behavior>
  <action>
    Per D-21, D-22, D-23: copy the Phase 116 checker. Filename must be `scripts/check-phase144-operator-flush-availability-evidence.ts`. Target file list must include Plan 01–03 artifacts plus the three docs and both verify.sh insertion sites.

    Require symbols, counters (`cache_size`, `last_flush_reason`, `available_count`, `unavailable_count`, `index_known_without_payload_count`, `ok`, `large`, `critical`), behavior-test names from the interfaces block, redaction needles (`peer_id=`, `127.0.0.1:`, 64-zero hash, `credential=phase144`), and the Cargo/Bazel commands.

    Forbidden claims on new-field docs and `status/chainstate_durability.rs` / CLI durability renderers: the UI-SPEC list. Allow `pruned` only on the old `block_relay` surface files.

    In `scripts/verify.sh` insert immediately after the Phase 116 bun test/run pair (both the early bun list and the later `run_step` list):

    ```bash
    bun test scripts/check-phase144-operator-flush-availability-evidence.test.ts
    bun run scripts/check-phase144-operator-flush-availability-evidence.ts
    ```

    and matching `run_step` names: `test Phase 144 operator flush and availability evidence checker` / `check Phase 144 operator flush and availability evidence`.

    Audit `docs/parity/source-breadcrumbs.json`: every new Plan 01–03 Rust path under `packages/open-bitcoin-*/src` or `tests` must belong to exactly one existing group. Add any missing files created during execution. Do not invent a CSVFY / Phase 145 no-claim checker.

    Default verification stays deterministic and public-network-free (D-23). Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun test scripts/check-phase144-operator-flush-availability-evidence.test.ts && bun run scripts/check-phase144-operator-flush-availability-evidence.ts && bun run scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'CSOBS-01' scripts/check-phase144-operator-flush-availability-evidence.ts` matches
    - `rg -n 'CSOBS-02' scripts/check-phase144-operator-flush-availability-evidence.ts` matches
    - `rg -n 'check-phase144-operator-flush-availability-evidence' scripts/verify.sh` matches at least 4 times
    - `rg -n 'status/chainstate_durability.rs' docs/parity/source-breadcrumbs.json` matches
    - `rg -n 'getblock' scripts/check-phase144-operator-flush-availability-evidence.ts` matches as a forbidden needle
    - `rg -n 'MAX_DASHBOARD_CHARTS' scripts/check-phase144-operator-flush-availability-evidence.ts` matches
  </acceptance_criteria>
  <done>Cross-surface checker covers CSOBS-01 and CSOBS-02, breadcrumbs are complete, and verify.sh runs the checker after Phase 116.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Docs / checker → operators | Copy can be misread as prune, archive, or production readiness |
| Checker → CI | A weak corpus lets coin-dump or getblock drift land |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-144-04-01 | Spoofing | runtime-guide / status-snapshot | mitigate | High: forbidden-claim needles include `archive-node`, `prune mode`, `public default`, `production ready`, `getblock`. Positive claims fail the checker (D-23). |
| T-144-04-02 | Information Disclosure | checker corpus | mitigate | High: require `redact_chainstate_durability`, peer/hex needles, and no request-log symbols (D-12, D-19). |
| T-144-04-03 | Tampering | invented tip docs | mitigate | Docs must say fail-closed / interrupted-without-B leaves coins best-block unavailable (D-10). |
| T-144-04-04 | Elevation of Privilege | Phase 145 leak | mitigate | Do not add CSVFY guardrails or flip production-readiness claims. Leave Phase 145 scope explicit. |
| T-144-04-05 | Denial of Service | verify.sh | accept | Checker is filesystem/string scan only, matching Phase 116. |
</threat_model>

<verification>
```bash
bun test scripts/check-phase144-operator-flush-availability-evidence.test.ts
bun run scripts/check-phase144-operator-flush-availability-evidence.ts
bun run scripts/check-parity-breadcrumbs.ts
rg -n 'check-phase144-operator-flush-availability-evidence' scripts/verify.sh
```
</verification>

<success_criteria>
- D-20: three docs updated with field contract and Cargo/Bazel commands.
- D-21: Phase 144 checker exists and is wired into verify.sh after Phase 116.
- D-22: breadcrumb JSON covers every new first-party Rust path.
- D-23: no Phase 145 no-claim closeout; docs/checker forbid prune/archive/public-default/production-ready/getblock claims on the new field.
- CSOBS-01 and CSOBS-02 both appear in the checker REQUIRED_REQUIREMENTS.
</success_criteria>

<output>
After completion, create `.planning/phases/144-operator-flush-and-availability-evidence/144-04-SUMMARY.md`
</output>
