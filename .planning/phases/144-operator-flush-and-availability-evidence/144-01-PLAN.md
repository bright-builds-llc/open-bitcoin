---
phase: 144-operator-flush-and-availability-evidence
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/chainstate_durability.rs
  - packages/open-bitcoin-node/src/status/chainstate_durability/tests.rs
  - packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
  - packages/open-bitcoin-node/src/chainstate/flush_lifecycle/tests.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs
  - packages/open-bitcoin-node/src/network/chainstate_durability_evidence/tests.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network/operator_snapshot.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - CSOBS-01
  - CSOBS-02
user_setup: []
must_haves:
  truths:
    - "OpenBitcoinStatusSnapshot carries one dedicated chainstate_durability FieldAvailability field that is distinct from recovery_evidence and block_relay (D-01, D-03)."
    - "Stopped or unprojected runtimes expose Unavailable with CHAINSTATE_DURABILITY_UNAVAILABLE_REASON and never fabricate cache-size, last flush reason, coins best-block, or payload-present facts (D-02)."
    - "Current cache_size is classified by decide_flush(FlushMode::None) at projection time; last_flush_reason and write_kind come from the last real execute_flush, not from that None classification (D-05, D-06, CSOBS-02)."
    - "Fail-closed and interrupted-without-B keep maybe_coins_best_block_height and maybe_coins_best_block_hash as None and never publish interrupted H hashes (D-09, D-10)."
    - "Have-bytes last_serving_status is unavailable whenever last_payload_present is false; the sibling accumulator stores last labels plus available_count, unavailable_count, and index_known_without_payload_count only (D-11, D-12, D-13)."
  artifacts:
    - path: packages/open-bitcoin-node/src/status/chainstate_durability.rs
      provides: "Shared ChainstateDurabilityEvidence contract and as_str labels"
      contains: "pub struct ChainstateDurabilityEvidence"
    - path: packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
      provides: "Retained last write decision and coins recovery outcome"
      contains: "maybe_last_write_decision"
    - path: packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs
      provides: "Sibling last/aggregate HAVL accumulator"
      contains: "index_known_without_payload_count"
    - path: packages/open-bitcoin-node/src/network/operator_snapshot.rs
      provides: "Managed snapshot copies retained flush and HAVL facts"
      contains: "chainstate_durability"
  key_links:
    - from: packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
      to: packages/open-bitcoin-node/src/status/chainstate_durability.rs
      via: "project_chainstate_durability copies retained FlushDecision facts"
      pattern: "project_chainstate_durability"
    - from: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      to: packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs
      via: "record_block_serving_evidence also records HAVL labels"
      pattern: "record_have_bytes"
    - from: packages/open-bitcoin-node/src/network/operator_snapshot.rs
      to: packages/open-bitcoin-node/src/status.rs
      via: "ManagedNetworkOperatorSnapshot.chainstate_durability"
      pattern: "chainstate_durability:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 144-2026-09-17T11-23-17
generated_at: 2026-09-17T12:30:00.000Z
---

# Phase 144 Plan 01: Shared Contract And Runtime Retention

<objective>
Create the `chainstate_durability` shared contract, retain last flush/recovery/occupancy facts on `FlushLifecycle`, retain last/aggregate HAVL labels on a sibling serve accumulator, and copy those facts onto `OpenBitcoinStatusSnapshot` / `ManagedNetworkOperatorSnapshot`.

Purpose: CSOBS-01 and CSOBS-02 cannot be rendered until the manager and serve seam remember Phase 140–143 truth. Renderers must not reopen Fjall or reclassify LARGE/CRITICAL (D-01, D-05).

Output: `ChainstateDurabilityEvidence`, retained `FlushExecution` / recovery facts, sibling HAVL accumulator, snapshot field, compile-green fixture updates, file-header breadcrumbs plus JSON pre-registration for later plan files.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-CONTEXT.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-RESEARCH.md
@.planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/status/block_relay_evidence.rs
@packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
@packages/open-bitcoin-chainstate/src/coins/flush.rs
@packages/open-bitcoin-node/src/network/block_serving.rs
@packages/open-bitcoin-node/src/network/block_relay_evidence.rs
@packages/open-bitcoin-node/src/network/operator_snapshot.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/status.rs (add one field only; do not append types here — status.rs is already ~618 lines):

```rust
#[serde(default)]
pub block_relay: BlockRelayEvidenceStatus,
// ADD immediately after block_relay:
// #[serde(default)]
// pub chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>,
#[serde(default)]
pub recovery_evidence: FieldAvailability<RecoveryEvidenceSnapshot>,
```

`FieldAvailability` stays the Phase 72 tagged enum (`state` / `value`). Do not fold CSOBS into `recovery_evidence` or `block_relay` (D-03).

From packages/open-bitcoin-chainstate/src/coins/flush.rs (project, do not recompute thresholds):

```rust
pub enum CoinsCacheSizeState { Ok = 0, Large = 1, Critical = 2 }
pub enum LastFlushReason { None, Needed, Periodic, Always, FailedDisk }
pub enum FlushDecision {
    None(FlushDecisionFacts),
    Flush(FlushDecisionFacts),
    Sync(FlushDecisionFacts),
    RefuseDiskSpace(FlushDecisionFacts),
}
pub fn decide_flush(input: FlushPolicyInput) -> FlushDecision;
```

`classify_cache_size` is `pub(crate)` in chainstate. Operator code must call `decide_flush` with `FlushMode::None` for current occupancy (D-05, D-06).

From packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs today: `FlushExecution { decision, wrote_coins }` is discarded after `execute_flush`. `initialize` discards `RecoveryDecision` after `apply_recovery_decision`. Retain both.

From packages/open-bitcoin-node/src/network/block_serving.rs:

```rust
pub(super) struct BlockServingPresenceFacts {
    pub payload_present: bool,
    pub index_known: bool,
    pub validated_on_active_chain: bool,
}
```

Do not extend `BlockRelayEvidenceStatus`. Hook a sibling recorder from `record_block_serving_evidence` in `network/block_relay_evidence.rs`.

Locked JSON shape when Available (`rename_all = "snake_case"`):

```text
cache_size: ok|large|critical
last_flush_reason: none|needed|periodic|always|failed_disk
write_kind: none|flush|sync|refuse_disk_space
readiness: not_ready|ready_to_flush
cache_bytes: u64
cache_byte_limit: u64
recovery_outcome: consistent|replayed|interrupted|fail_closed
maybe_coins_best_block_height: Option<u64>
maybe_coins_best_block_hash: Option<String>
last_serving_status: available|unavailable
last_payload_present: bool
last_index_known: bool
last_validated_on_active_chain: bool
available_count: u64
unavailable_count: u64
index_known_without_payload_count: u64
```

Human `cache_size` tokens `OK` / `LARGE` / `CRITICAL` live in CLI/dashboard Plan 02. This plan owns machine `as_str` labels only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ChainstateDurabilityEvidence and snapshot field</name>
  <files>
    packages/open-bitcoin-node/src/status/chainstate_durability.rs
    packages/open-bitcoin-node/src/status/chainstate_durability/tests.rs
    packages/open-bitcoin-node/src/status.rs
    packages/open-bitcoin-node/src/lib.rs
    packages/open-bitcoin-cli/src/operator/status.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/144-operator-flush-and-availability-evidence/144-CONTEXT.md
    .planning/phases/144-operator-flush-and-availability-evidence/144-RESEARCH.md
    packages/open-bitcoin-node/src/status.rs
    packages/open-bitcoin-node/src/status/block_relay_evidence.rs
    packages/open-bitcoin-cli/src/operator/status.rs
    docs/parity/source-breadcrumbs.json
    standards/languages/rust.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `chainstate_durability_default_unavailable_uses_stable_reason`: `ChainstateDurabilityEvidence::default_unavailable()` (or `FieldAvailability::default()` for this payload) is `Unavailable` with exact reason `chainstate durability evidence unavailable` (D-02).
    - Test `chainstate_durability_available_serializes_locked_snake_case_keys`: an Available fixture serializes `cache_size=ok`, `last_flush_reason=periodic`, `write_kind=sync`, `readiness=ready_to_flush`, `recovery_outcome=replayed`, `last_serving_status=unavailable`, and the three D-12 counters. JSON keys are exactly the locked names (D-01, D-05, D-06, D-08, D-12).
    - Test `chainstate_durability_json_omits_peer_coin_and_pruned_keys`: serialized Available JSON contains none of `pruned`, `block_status_pruned`, `peer_id`, `getblock`, `txid:vout`, `cmpctblock` (D-11, D-12, D-04).
    - Test `chainstate_durability_fail_closed_omits_coins_best_block`: when `recovery_outcome=fail_closed` or `interrupted` and coins `B` is missing, `maybe_coins_best_block_height` and `maybe_coins_best_block_hash` are JSON `null` (D-10).
    - Test `as_str_cache_size_and_last_flush_reason`: `ok`/`large`/`critical` and `none`/`needed`/`periodic`/`always`/`failed_disk` (CSOBS-02, D-05).
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-05, D-22: create `packages/open-bitcoin-node/src/status/chainstate_durability.rs` plus `status/chainstate_durability/tests.rs` (`foo.rs` + `foo/`, not `mod.rs`). In `status.rs` add `mod chainstate_durability;` and `pub use chainstate_durability::*;` next to the existing `block_relay_evidence` module lines. Do not put the new types into `status.rs`.

    Define these exact names:

    ```rust
    pub const CHAINSTATE_DURABILITY_UNAVAILABLE_REASON: &str =
        "chainstate durability evidence unavailable";

    pub struct ChainstateDurabilityEvidence { /* locked JSON fields */ }
    pub enum CacheSizeLabel { Ok, Large, Critical }
    pub enum LastFlushReasonLabel { None, Needed, Periodic, Always, FailedDisk }
    pub enum WriteKindLabel { None, Flush, Sync, RefuseDiskSpace }
    pub enum ReadinessLabel { NotReady, ReadyToFlush }
    pub enum CoinsRecoveryOutcome { Consistent, Replayed, Interrupted, FailClosed }
    pub enum ServingStatusLabel { Available, Unavailable }
    ```

    All enums use `#[serde(rename_all = "snake_case")]`. Put `as_str()` on these status-projection enums only, not on `FlushDecision` in chainstate (discretion + D-05).

    Implement `ChainstateDurabilityEvidence::default_unavailable() -> FieldAvailability<Self>` and `impl Default for FieldAvailability<ChainstateDurabilityEvidence>` that returns that unavailable reason so `#[serde(default)]` on the snapshot field deserializes old fixtures (D-02).

    Add to `OpenBitcoinStatusSnapshot` immediately after `block_relay`:

    ```rust
    #[serde(default)]
    pub chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>,
    ```

    Update every Rust `OpenBitcoinStatusSnapshot {` literal that fails to compile. Known homes: `packages/open-bitcoin-cli/src/operator/status.rs` (live, stopped, and `collect_open_bitcoin_network_status` fallback — use `default_unavailable()` / the collector reason, not a fabricated Available object), plus node `status/tests/**` and CLI dashboard/support/soak fixtures. Stopped constructors must copy the same `reason` string already used for peers/mempool, not a second local sentence (UI-SPEC). Do not add CLI human lines or dashboard rows here (Plan 02).

    Re-export `ChainstateDurabilityEvidence` from `packages/open-bitcoin-node/src/lib.rs` `pub use status::{...}` so RPC/CLI can import it.

    File-header breadcrumbs: status module uses `none: Open Bitcoin-only support/infrastructure; no direct Bitcoin Knots source anchor identified.`

    In `docs/parity/source-breadcrumbs.json` append these paths to existing groups (do not create empty groups):
    - `node-status-contract.files`: `status/chainstate_durability.rs`, `status/chainstate_durability/tests.rs`
    - `node-chainstate-adapter.files`: no new file unless you add flush tests under that tree
    - `node-network-block-relay-evidence-adapter.files`: `network/chainstate_durability_evidence.rs` and `network/chainstate_durability_evidence/tests.rs` (pre-register even if Task 3 creates them in this plan)
    - `node-observability-contracts.files`: `metrics/chainstate_durability.rs`, `logging/chainstate_durability.rs` (pre-register for Plan 03)
    - `cli-operator-onboarding-contracts.files`: `status/render/chainstate_durability.rs` (pre-register for Plan 02)
    - `cli-operator-dashboard-contracts.files`: `dashboard/model/chainstate_durability.rs` (pre-register for Plan 02)
    - `cli-operator-support-bundles.files`: `support/render/chainstate_durability.rs` (pre-register for Plan 03)

    Combined RED+GREEN in one hook-passing feat commit is allowed because pre-commit runs `verify.sh`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-01-contract -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib status::chainstate_durability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'pub struct ChainstateDurabilityEvidence' packages/open-bitcoin-node/src/status/chainstate_durability.rs` matches
    - `rg -n 'pub chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>' packages/open-bitcoin-node/src/status.rs` matches
    - `rg -n 'CHAINSTATE_DURABILITY_UNAVAILABLE_REASON' packages/open-bitcoin-node/src/status/chainstate_durability.rs` matches
    - `rg -n 'recovery_evidence|block_relay' packages/open-bitcoin-node/src/status/chainstate_durability.rs` does not introduce those as field names on the new struct
    - `rg -n 'pruned|getblock|peer_id' packages/open-bitcoin-node/src/status/chainstate_durability.rs` matches only comments that forbid those tokens
    - `rg -n 'status/chainstate_durability.rs' docs/parity/source-breadcrumbs.json` matches
  </acceptance_criteria>
  <done>Shared contract exists, snapshot field defaults to unavailable, fixtures compile, and machine labels cover CSOBS-02 cache-size and last flush reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain last FlushExecution and recovery facts</name>
  <files>
    packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
    packages/open-bitcoin-node/src/chainstate/flush_lifecycle/tests.rs
    packages/open-bitcoin-node/src/status/chainstate_durability.rs
    packages/open-bitcoin-node/src/status/chainstate_durability/tests.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    packages/open-bitcoin-node/src/status/chainstate_durability.rs
    packages/open-bitcoin-node/src/storage/coins_view.rs
    packages/open-bitcoin-network/src/header_store.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-RESEARCH.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `execute_flush_retains_last_write_decision_not_later_none_classification`: after `execute_flush(Periodic)` that returns `FlushDecision::Sync` with `LastFlushReason::Periodic`, a later `project_chainstate_durability` that refreshes occupancy via `decide_flush(FlushMode::None)` still reports `last_flush_reason=periodic` and `write_kind=sync` even if current `cache_size` is `ok` (D-05, D-06, CSOBS-02, Pitfall 2).
    - Test `project_chainstate_durability_uses_none_mode_for_current_cache_size`: grow overlay occupancy past the LARGE threshold without calling Periodic/Always; projected `cache_size` becomes `large` or `critical` while `last_flush_reason` stays the retained write reason (D-05).
    - Test `initialize_consistent_with_best_block_is_consistent`: successful `ConsistentEmptyHeads` or `OneHead` with `best_block()=Some` maps to `recovery_outcome=consistent` and publishes height+64 lowercase hex hash (D-08, D-09).
    - Test `initialize_missing_b_is_fail_closed_without_tip`: `OneHead` or consistent-empty with `best_block()=None` maps to `fail_closed` and both coins-best-block fields are `None` (D-10, research Q2).
    - Test `interrupted_replay_success_is_replayed`: successful `InterruptedTwoHeads` replay maps to `replayed` (D-08, D-10).
    - Test `initialize_error_detail_includes_fail_closed`: `InconsistentOtherCount` and missing-undo/body replay errors include the token `fail_closed` in `StorageError` Display so Plan 03 can log without scanning `H` (D-10, Pitfall 4). Do not publish `H[0]` as coins best-block.
    - Test `occupancy_uses_estimated_cache_bytes_not_fjall_len`: projector source and `flush_lifecycle.rs` contain `estimated_cache_bytes` and do not contain `fjall` `len()` as cache size (D-07).
  </behavior>
  <action>
    Per D-05, D-06, D-07, D-08, D-09, D-10: add private retention fields on `FlushLifecycle`:

    ```rust
    maybe_last_write_decision: Option<FlushDecision>,
    maybe_recovery_outcome: Option<CoinsRecoveryOutcome>,
    ```

    Update every `FlushLifecycle { ... }` constructor (`initialize`, `ready`, `ready_for_test`, `not_ready_for_test`) to start both as `None`. `ready` / test helpers used as already-consistent managers should set `maybe_recovery_outcome = Some(CoinsRecoveryOutcome::Consistent)` only when the test is modeling a successful init; do not invent a tip hash.

    In `execute_flush`, after `decide_flush`, if `mode != FlushMode::None`, store `decision` in `maybe_last_write_decision` (including `RefuseDiskSpace` / `FailedDisk`). Never let a later `FlushMode::None` overwrite that retained write. `FlushMode::None` remains observability-only (140 D-17 / 142 D-16).

    In `initialize`, after `decide_recovery` / `apply_recovery_decision`:
    - `InterruptedTwoHeads` + `Ok` replay → `CoinsRecoveryOutcome::Replayed`
    - `ConsistentEmptyHeads` or `OneHead` + `view.best_block()?.is_some()` → `Consistent`
    - those same decisions + `best_block()=None` → `FailClosed` (no tip)
    - `InconsistentOtherCount` and replay `Err` → return `StorageError` whose `Display` contains `fail_closed` (and `interrupted` when heads were two but replay could not finish). Do not return a ReadyToFlush lifecycle on those errors.

    Add `pub fn project_chainstate_durability(...)` in `status/chainstate_durability.rs` (or a `pub(crate)` method on `FlushLifecycle` that returns the evidence struct). Inputs: retained write decision, retained recovery, `ManagerReadiness`, current `cache.estimated_cache_bytes()`, `cache_byte_limit`, `mempool_leftover_bytes`, `cache_entry_count`, `now`, `next_write`, `memory_pressure`, `disk_free_bytes`, optional coins `B` hash, optional header height, plus HAVL snapshot from Task 3 (accept a `HaveBytesEvidence` value so this function stays I/O-free).

    Mapping:
    - `cache_size` / `cache_bytes` / `cache_byte_limit` from `decide_flush(FlushPolicyInput { mode: FlushMode::None, ... })`
    - `last_flush_reason` / `write_kind` from retained `maybe_last_write_decision`, or `none`/`none` if never written
    - `readiness`: `not_ready` / `ready_to_flush`
    - coins best-block: only when recovery is not `fail_closed`/`interrupted` without `B`. Encode hash with a local lowercase 64-hex helper copied from `packages/open-bitcoin-node/src/sync/tip.rs` `block_hash_hex` — do not import the sync module (cycle risk). Height comes from `HeaderStore::entry(&hash).height` as `u64`. If the header is missing, height is `None` and hash may still be published (A2). Never substitute interrupted `H` hashes (D-10).
    - Do not serialize `RecoveryDecision` `count`. Do not dump outpoints, coin values, scripts, or undo blobs (D-09).

    Do not call `classify_cache_size` from this crate. Do not use Fjall item counts. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-01-flush-retain -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib flush_lifecycle chainstate_durability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'maybe_last_write_decision' packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs` matches
    - `rg -n 'maybe_recovery_outcome' packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs` matches
    - `rg -n 'FlushMode::None' packages/open-bitcoin-node/src/status/chainstate_durability.rs` matches
    - `rg -n 'fn project_chainstate_durability' packages/open-bitcoin-node/src/status/chainstate_durability.rs packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs` matches
    - `rg -n 'fail_closed' packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs` matches
    - `rg -n 'estimated_cache_bytes' packages/open-bitcoin-node/src/status/chainstate_durability.rs packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs` matches
  </acceptance_criteria>
  <done>Flush lifecycle retains last real write and coins-marker recovery; current cache-size is a None-mode classification; fail-closed never invents a tip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sibling HAVL accumulator and managed snapshot copy</name>
  <files>
    packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs
    packages/open-bitcoin-node/src/network/chainstate_durability_evidence/tests.rs
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/network/operator_snapshot.rs
    packages/open-bitcoin-node/src/network/types.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/operator_snapshot.rs
    packages/open-bitcoin-node/src/network/types.rs
    packages/open-bitcoin-node/src/status/chainstate_durability.rs
    .planning/phases/144-operator-flush-and-availability-evidence/144-UI-SPEC.md
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `have_bytes_accumulator_sets_unavailable_when_payload_absent`: record a decision with `payload_present=false`, `index_known=true`, `validated_on_active_chain=true`. Last serving status is `unavailable`, `index_known_without_payload_count` increments by 1, and `available_count` does not (D-13, D-12).
    - Test `have_bytes_accumulator_counts_available_when_payload_present`: `payload_present=true` increments `available_count` and last status is `available` (D-11).
    - Test `have_bytes_accumulator_has_no_hash_map_or_pruned_counter`: accumulator type has no `HashMap`, no `pruned_count`, and `include_str` of the new file contains none of `pruned`, `peer_id`, `by_hash` (D-03, D-11, D-12).
    - Test `operator_snapshot_copies_chainstate_durability`: after one flush retain + one have-bytes record, `ManagedNetworkOperatorSnapshot::chainstate_durability()` is Available with matching last labels and counters (D-01).
    - Test `operator_snapshot_unprojected_is_unavailable`: before first projection/init, the managed field is Unavailable with `CHAINSTATE_DURABILITY_UNAVAILABLE_REASON` (D-02).
  </behavior>
  <action>
    Per D-03, D-11, D-12, D-13: add `packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs` plus tests (`foo.rs` + `foo/`). Declare `mod chainstate_durability_evidence;` in `packages/open-bitcoin-node/src/network.rs` next to `mod block_relay_evidence;`.

    Store last labels plus three `u64` counters only. Public-in-crate API:

    ```rust
    pub struct HaveBytesEvidence {
        pub last_serving_status: ServingStatusLabel,
        pub last_payload_present: bool,
        pub last_index_known: bool,
        pub last_validated_on_active_chain: bool,
        pub available_count: u64,
        pub unavailable_count: u64,
        pub index_known_without_payload_count: u64,
    }

    impl HaveBytesAccumulator {
        pub fn record_have_bytes(&mut self, presence: BlockServingPresenceFacts);
        pub fn snapshot(&self) -> HaveBytesEvidence;
    }
    ```

    Recording rules: if `!presence.payload_present` then last status is `Unavailable` and `unavailable_count += 1`; if `presence.index_known` also `index_known_without_payload_count += 1`. If `presence.payload_present` then last status is `Available` and `available_count += 1`. Never increment a pruned counter. Never store `block_hash` or `PeerId`.

    In `ManagedPeerNetwork::record_block_serving_evidence`, after the existing `block_relay_evidence.record_block_serving(...)` call, also call `record_have_bytes(decision.presence)`. Do not change `block_relay.block_serving.status.pruned_count`.

    Add `chainstate_durability: FieldAvailability<ChainstateDurabilityEvidence>` to `ManagedNetworkOperatorSnapshot` in `network/types.rs` with a getter `chainstate_durability()`. In `operator_snapshot.rs`, project by combining `project_chainstate_durability` (Task 2) with `HaveBytesAccumulator::snapshot()`. Look up coins `B` via the chainstate coins view `best_block()` and height via `self.peer_manager.header_store().entry(&hash).map(|entry| u64::from(entry.height))`. If flush lifecycle is `NotReady` and recovery was never recorded, keep the whole field Unavailable (D-02). Do not open Fjall from CLI.

    File-header breadcrumbs for the new network module: cite `packages/bitcoin-knots/src/node/blockstorage.cpp` (HaveBlockData vs missing) plus `packages/bitcoin-knots/src/validation.cpp`. Combined RED+GREEN allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase144-01-havl-retain -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_evidence -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fn record_have_bytes' packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs` matches
    - `rg -n 'index_known_without_payload_count' packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs` matches
    - `rg -n 'record_have_bytes' packages/open-bitcoin-node/src/network/block_relay_evidence.rs` matches
    - `rg -n 'pruned_count' packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs` returns no matches
    - `rg -n 'chainstate_durability:' packages/open-bitcoin-node/src/network/operator_snapshot.rs` matches
    - `rg -n 'pub fn chainstate_durability' packages/open-bitcoin-node/src/network/types.rs` matches
  </acceptance_criteria>
  <done>Sibling HAVL accumulator records last labels and three counters; managed operator snapshot copies the shared field without extending block_relay.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| FlushLifecycle / coins view → status contract | Durable coins and undo live behind the manager; only sanitized labels may cross |
| Serve decision → HAVL accumulator | Per-request hashes and peer ids must collapse to last labels plus three counters |
| Snapshot → later RPC/CLI/support | Untrusted operators receive the shared field only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-144-01-01 | Information Disclosure | `ChainstateDurabilityEvidence` | mitigate | High: project height+hash only. Tests forbid outpoints, values, scripts, undo blobs, coin maps (D-09). Block if any coin-dump field is added. |
| T-144-01-02 | Information Disclosure | `HaveBytesAccumulator` | mitigate | High: no `block_hash`, `PeerId`, or request log. Checker-ready `include_str` forbids `peer_id` and `by_hash` (D-12). |
| T-144-01-03 | Tampering | fail-closed coins tip | mitigate | High: `fail_closed`/`interrupted` without `B` leaves both tip fields `None`. Never publish `H` hashes (D-10). |
| T-144-01-04 | Spoofing | recovery vocabulary | mitigate | Do not reuse `SyncRecoveryCategory` / `store_corruption` as CSOBS recovery (D-08). |
| T-144-01-05 | Information Disclosure | `pruned` on new field | mitigate | High: new module forbids `pruned` / `block_status_pruned` as serialized values (D-11). |
</threat_model>

<verification>
```bash
bun run scripts/command-timings.ts run --key phase144-01-contract -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib status::chainstate_durability -- --nocapture
bun run scripts/command-timings.ts run --key phase144-01-flush-retain -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib flush_lifecycle chainstate_durability -- --nocapture
bun run scripts/command-timings.ts run --key phase144-01-havl-retain -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate_durability_evidence -- --nocapture
bun run scripts/check-parity-breadcrumbs.ts
```
</verification>

<success_criteria>
- D-01 / D-03: one `chainstate_durability` field exists and is not folded into `recovery_evidence` or `block_relay`.
- D-02: unprojected runtimes are Unavailable with the stable reason.
- D-05 / CSOBS-02: current cache-size is None-mode; last flush reason is retained from real `execute_flush`.
- D-08 / D-10: recovery outcomes are distinguishable; fail-closed invents no tip.
- D-11 / D-12 / D-13: HAVL last labels plus three counters; do-not when payload is absent.
- No `getblock`, no ninth chart, no Plan 02/03 renderer work beyond compile-fixing snapshot literals.
</success_criteria>

<output>
After completion, create `.planning/phases/144-operator-flush-and-availability-evidence/144-01-SUMMARY.md`
</output>
