---
phase: 61-resource-bounds-and-recovery-taxonomy
plan: 06
type: execute
wave: 4
depends_on: [61-03, 61-04, 61-05]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - scripts/check-phase61-resource-recovery-boundaries.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [RR-01, RR-02, RR-04]
user_setup: []
must_haves:
  truths:
    - "Operator docs list the active resource bounds and all recovery category labels."
    - "Docs provide copy-pasteable repo-local Cargo and Bazel status/support commands."
    - "A deterministic checker fails if resource/recovery docs or default public-network exclusions drift."
    - "`bash scripts/verify.sh` remains deterministic and does not run live-smoke or public-network commands."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Operator resource-bound and recovery-category guidance"
      contains: "sync.recovery_category"
    - path: "docs/architecture/status-snapshot.md"
      provides: "Shared status contract for recovery category and resource pressure"
      contains: "storage_lock_contention"
    - path: "scripts/check-phase61-resource-recovery-boundaries.ts"
      provides: "Deterministic Phase 61 documentation/default-verification guard"
      contains: "validated Phase 61 resource/recovery boundaries"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase61-resource-recovery-boundaries.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase61-resource-recovery-boundaries.ts"
    - from: "docs/operator/runtime-guide.md"
      to: "repo-local Cargo/Bazel commands"
      via: "operator copy-paste commands"
      pattern: "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 61-2026-06-06T03-43-41
generated_at: 2026-06-06T04:00:59Z
---

<objective>
Document Phase 61 resource bounds and recovery taxonomy, then lock the deterministic verification boundary.

Purpose: RR-01, RR-02, RR-04, D-09, D-10, D-11, and repo-local lessons require exact operator guidance, Cargo/Bazel command forms, and public-network exclusion from default verification.
Output: Updated docs and a Bun checker wired into `scripts/verify.sh`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-RESEARCH.md
@.codex/tasks/lessons.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@scripts/check-v1.4-release-boundaries.ts
@scripts/verify.sh

<interfaces>
Recovery labels that docs and checker must require:
```text
clean_shutdown
unclean_shutdown
incompatible_schema
store_corruption
storage_lock_contention
storage_backend_failure
resource_exhaustion
invalid_peer_data
public_network_unreachable
operator_cancellation
```

Resource pressure fields that docs and checker must require:
```text
blocks_in_flight
max_header_requests_in_flight_per_peer
max_headers_per_message
max_blocks_in_flight_per_peer
max_blocks_in_flight_total
max_messages_per_peer
max_sync_rounds
outbound_peers
target_outbound_peers
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document resource bounds and recovery category interpretation</name>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-01 through D-11
    - `.codex/tasks/lessons.md` repo-local Cargo/Bazel command lesson
  </read_first>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md</files>
  <action>
    In `docs/operator/runtime-guide.md`, update the durable recovery section to document `sync.recovery_category` as the stable machine label and keep `sync.recovery_action` as human next-action text. List all ten labels exactly: `clean_shutdown`, `unclean_shutdown`, `incompatible_schema`, `store_corruption`, `storage_lock_contention`, `storage_backend_failure`, `resource_exhaustion`, `invalid_peer_data`, `public_network_unreachable`, `operator_cancellation`. Add one sentence that storage categories outrank peer/network guidance.

    In the same guide, add an "Active resource bounds" paragraph that lists every `SyncResourcePressure` field exactly: `blocks_in_flight`, `max_header_requests_in_flight_per_peer`, `max_headers_per_message`, `max_blocks_in_flight_per_peer`, `max_blocks_in_flight_total`, `max_messages_per_peer`, `max_sync_rounds`, `outbound_peers`, `target_outbound_peers`. State that retry state, peer outcomes, metrics samples, structured logs, and support evidence remain bounded by config/retention policies or compact summaries and that Phase 61 adds no unbounded retained arrays.

    Keep copy-pasteable repo-local operator commands in both forms:
    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json`
    and
    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json`.
    Include support bundle Cargo/Bazel command forms too. Public-network live-smoke commands must remain under an explicit opt-in UAT paragraph, not default verification.

    In `docs/architecture/status-snapshot.md`, add `sync.recovery_category` semantics and expand the resource pressure list to include both observed fields (`blocks_in_flight`, `outbound_peers`) and configured bounds. In `docs/architecture/operator-observability.md`, add `recovery_category` to the shared status/log/support vocabulary and keep metrics described as bounded numeric samples.
  </action>
  <acceptance_criteria>
    - `rg -n "sync\\.recovery_category|clean_shutdown|unclean_shutdown|incompatible_schema|store_corruption|storage_lock_contention|storage_backend_failure|resource_exhaustion|invalid_peer_data|public_network_unreachable|operator_cancellation" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md` finds all labels.
    - `rg -n "blocks_in_flight|max_header_requests_in_flight_per_peer|max_headers_per_message|max_blocks_in_flight_per_peer|max_blocks_in_flight_total|max_messages_per_peer|max_sync_rounds|outbound_peers|target_outbound_peers" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md` finds all bound fields.
    - `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json" docs/operator/runtime-guide.md` finds both repo-local status commands.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "sync\\.recovery_category|storage_lock_contention|max_blocks_in_flight_total|cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md</automated>
  </verify>
  <done>Operator and architecture docs explain the exact Phase 61 labels, bounds, precedence, and repo-local command forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic Phase 61 documentation and verification-boundary checker</name>
  <read_first>
    - `scripts/check-v1.4-release-boundaries.ts`
    - `scripts/verify.sh`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-10 and D-11
  </read_first>
  <files>scripts/check-phase61-resource-recovery-boundaries.ts, scripts/verify.sh</files>
  <behavior>
    - Test: checker fails if any of the ten recovery category labels are absent from docs.
    - Test: checker fails if any of the nine resource-pressure fields are absent from docs.
    - Test: checker fails if `scripts/verify.sh` contains `run-live-mainnet-smoke`, `--manual-peer`, or `--restart-after-progress`.
    - Test: `scripts/verify.sh` invokes `bun run scripts/check-phase61-resource-recovery-boundaries.ts`.
  </behavior>
  <action>
    Create `scripts/check-phase61-resource-recovery-boundaries.ts` using the existing `scripts/check-v1.4-release-boundaries.ts` style: repo-root path resolution through `import.meta.dir`, `readText`, `requireContains`, `requireNotContains`, arrays of required strings, and a final `console.log("validated Phase 61 resource/recovery boundaries")`.

    The checker must read `docs/operator/runtime-guide.md`, `docs/architecture/status-snapshot.md`, `docs/architecture/operator-observability.md`, and `scripts/verify.sh`. It must require all labels and fields from the interface block, require both exact Cargo/Bazel `sync status` command strings, require `support bundle --output-dir=/tmp/open-bitcoin-support`, require `bash scripts/verify.sh`, and require `bash scripts/test-run-live-mainnet-smoke.sh` to remain documented as deterministic fixture review. It must assert `scripts/verify.sh` does not contain `run-live-mainnet-smoke`, `--manual-peer`, or `--restart-after-progress`.

    Wire the new checker into `scripts/verify.sh` immediately after `bun run scripts/check-v1.4-release-boundaries.ts`.
  </action>
  <acceptance_criteria>
    - `rg -n "validated Phase 61 resource/recovery boundaries|storage_lock_contention|max_blocks_in_flight_total|requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\"" scripts/check-phase61-resource-recovery-boundaries.ts` finds the checker targets.
    - `rg -n "bun run scripts/check-phase61-resource-recovery-boundaries\\.ts" scripts/verify.sh` finds the verify hook.
    - `bun run scripts/check-phase61-resource-recovery-boundaries.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase61-resource-recovery-boundaries.ts</automated>
  </verify>
  <done>Default verification has a deterministic guard for Phase 61 docs and public-network exclusion.</done>
</task>

<task type="auto">
  <name>Task 3: Run aggregate deterministic verification</name>
  <read_first>
    - `scripts/verify.sh`
    - `AGENTS.md` repo-native verification contract
    - `docs/metrics/lines-of-code.md` generated artifact guidance
  </read_first>
  <files>docs/metrics/lines-of-code.md</files>
  <action>
    Run `bash scripts/verify.sh` from the repo root after all Phase 61 implementation and docs checks pass. If the command refreshes `docs/metrics/lines-of-code.md`, keep the generated freshness change. Do not add public-network smoke, service supervision, or production-node checks to default verification.
  </action>
  <acceptance_criteria>
    - `bash scripts/verify.sh` passes.
    - `! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh` remains true.
    - If `docs/metrics/lines-of-code.md` changes, `git diff -- docs/metrics/lines-of-code.md` shows only generated LOC freshness output.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Repo-native deterministic verification passes with Phase 61 docs/checker included and public-network checks still opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/checker -> operator behavior | Documentation and deterministic checks guide what operators treat as supported evidence. |
| default verification -> public network | Default verification must not cross into live public-network availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | Repudiation | docs and checker | mitigate | Checker requires exact labels, resource fields, and repo-local commands so docs cannot drift silently. |
| T-61-06-02 | Denial of Service | `scripts/verify.sh` | mitigate | Checker asserts default verification excludes public-network live-smoke commands that could hang or fail due to external reachability. |
| T-61-06-03 | Information Disclosure | support-bundle docs | mitigate | Docs keep support evidence local/redacted and do not ask operators to commit raw reports, daemon logs, credentials, or wallet material. |
</threat_model>

<verification>
Run docs/checker and aggregate verification:

```bash
bun run scripts/check-phase61-resource-recovery-boundaries.ts
! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Docs list all Phase 61 recovery labels and resource-pressure fields.
- Docs include exact repo-local Cargo and Bazel operator commands.
- `scripts/verify.sh` runs the deterministic Phase 61 checker and still excludes public-network commands.
- Aggregate repo verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-06-SUMMARY.md`.
</output>
