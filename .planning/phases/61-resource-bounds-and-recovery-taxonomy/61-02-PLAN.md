---
phase: 61-resource-bounds-and-recovery-taxonomy
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - packages/open-bitcoin-node/src/storage.rs
  - packages/open-bitcoin-node/src/sync/types.rs
  - packages/open-bitcoin-node/src/sync/types/recovery.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [RR-02, RR-04]
user_setup: []
must_haves:
  truths:
    - "Storage, peer, stop-reason, and runtime errors map into `SyncRecoveryCategory` without renderer-local string parsing."
    - "Storage incompatibility and corruption outrank peer and public-network retry guidance."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/types/recovery.rs"
      provides: "Sync recovery mapping helpers for peer, stop, runtime, and text detail facts"
      contains: "recovery_category"
    - path: "packages/open-bitcoin-node/src/storage.rs"
      provides: "Storage recovery category mapping"
      contains: "pub const fn recovery_category"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Parity breadcrumb coverage for new sync type child module"
      contains: "packages/open-bitcoin-node/src/sync/types/recovery.rs"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/types.rs"
      to: "packages/open-bitcoin-node/src/sync/types/recovery.rs"
      via: "child module"
      pattern: "mod recovery;"
    - from: "packages/open-bitcoin-node/src/storage.rs"
      to: "SyncRecoveryCategory"
      via: "storage error/action mapping"
      pattern: "recovery_category"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 61-2026-06-06T03-43-41
generated_at: 2026-06-06T04:00:59Z
---

<objective>
Map existing low-level recovery signals into the shared Phase 61 taxonomy.

Purpose: D-05 and D-06 require storage-first typed projection from existing `StorageError`, `StorageRecoveryAction`, `PeerFailureReason`, `SyncStopReason`, and `SyncRuntimeError` facts.
Output: Pure mapping helpers and tests with no new dependencies.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-RESEARCH.md
@.planning/phases/60-unattended-sync-loop-control/60-01-SUMMARY.md
@packages/open-bitcoin-node/src/status/recovery.rs
@packages/open-bitcoin-node/src/storage.rs
@packages/open-bitcoin-node/src/sync/types.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
Recovery categories from Plan 01:
```rust
pub enum SyncRecoveryCategory {
    CleanShutdown,
    UncleanShutdown,
    IncompatibleSchema,
    StoreCorruption,
    StorageLockContention,
    StorageBackendFailure,
    ResourceExhaustion,
    InvalidPeerData,
    PublicNetworkUnreachable,
    OperatorCancellation,
}
```

Existing input facts:
```rust
pub enum StorageRecoveryAction { Restart, Reindex, Repair, RestoreFromBackup }
pub enum PeerFailureReason { AddressResolution, Compatibility, Connect, Stall, RetryBackoff, InvalidData, InvalidMagic, BlockNotFound, MalformedBlock, InvalidBlock, DuplicateBlock, DisconnectedBlock, NonExtendingBlock, Network, ResourceLimit, Storage }
pub enum SyncStopReason { TargetHeaderReached { .. }, NoProgress { .. }, MaxRoundsReached { .. }, OperatorPaused, ShutdownRequested }
pub enum SyncRuntimeError { NoPeersConfigured, AddressResolution { .. }, PeerCompatibility { .. }, Io { .. }, InvalidData { .. }, InvalidMagic { .. }, Network { .. }, ResourceLimit { .. }, Storage(StorageError) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map storage actions and errors into recovery categories</name>
  <read_first>
    - `packages/open-bitcoin-node/src/storage.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-04, D-05, D-06
  </read_first>
  <files>packages/open-bitcoin-node/src/storage.rs</files>
  <behavior>
    - Test: schema version errors map to `SyncRecoveryCategory::IncompatibleSchema`.
    - Test: corruption errors map to `SyncRecoveryCategory::StoreCorruption`.
    - Test: backend messages containing `lock`, `locked`, or `contention` map to `SyncRecoveryCategory::StorageLockContention`.
    - Test: other backend and interrupted-write failures map to `SyncRecoveryCategory::StorageBackendFailure`.
  </behavior>
  <action>
    Import `SyncRecoveryCategory` in `storage.rs`. Add `pub const fn recovery_category(self) -> SyncRecoveryCategory` on `StorageRecoveryAction` with `Restart -> StorageBackendFailure`, `Reindex -> IncompatibleSchema`, `Repair -> StoreCorruption`, and `RestoreFromBackup -> StoreCorruption`.

    Add `pub fn recovery_category(&self) -> SyncRecoveryCategory` on `StorageError`. Map `InvalidSchemaVersion` and `SchemaMismatch` to `IncompatibleSchema`; `Corruption` to `StoreCorruption`; `UnavailableNamespace` and `InterruptedWrite` to `StorageBackendFailure`; `BackendFailure` to `StorageLockContention` when `message.to_ascii_lowercase()` contains `lock`, `locked`, or `contention`, otherwise `StorageBackendFailure`.

    Extend the existing storage unit tests with `storage_recovery_category_maps_schema_corruption_lock_and_backend_states`, using explicit Arrange/Act/Assert comments.
  </action>
  <acceptance_criteria>
    - `rg -n "fn recovery_category|StorageLockContention|IncompatibleSchema|StoreCorruption" packages/open-bitcoin-node/src/storage.rs` finds the mapping and expected categories.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category --all-features</automated>
  </verify>
  <done>Storage recovery actions and errors produce typed recovery categories with lock contention distinguishable from other backend failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map sync peer, stop, runtime, and detail facts into recovery categories</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/types.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
    - `.planning/phases/60-unattended-sync-loop-control/60-01-SUMMARY.md`
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/types.rs, packages/open-bitcoin-node/src/sync/types/recovery.rs</files>
  <behavior>
    - Test: `PeerFailureReason::ResourceLimit` maps to `resource_exhaustion`.
    - Test: invalid peer data reasons map to `invalid_peer_data`: `InvalidData`, `InvalidMagic`, `BlockNotFound`, `MalformedBlock`, `InvalidBlock`, `DuplicateBlock`, `DisconnectedBlock`, `NonExtendingBlock`.
    - Test: address, compatibility, connect, stall, retry-backoff, and network reasons map to `public_network_unreachable`.
    - Test: `SyncStopReason::OperatorPaused` and `SyncStopReason::ShutdownRequested` map to `operator_cancellation`.
    - Test: `SyncRuntimeError::Storage(error)` delegates to `StorageError::recovery_category()`.
  </behavior>
  <action>
    Add `mod recovery;` to `packages/open-bitcoin-node/src/sync/types.rs` below the existing `mod projection; mod summary;` declarations.

    Create `packages/open-bitcoin-node/src/sync/types/recovery.rs` with parity breadcrumbs for `net_processing.cpp`, `headerssync.cpp`, and `sync.cpp`. Implement category helpers without new dependencies:
    - `impl PeerFailureReason { pub(crate) const fn recovery_category(&self) -> SyncRecoveryCategory { ... } }`
    - `impl SyncStopReason { pub(crate) const fn recovery_category(self) -> Option<SyncRecoveryCategory> { ... } }`
    - `impl SyncRuntimeError { pub(crate) fn recovery_category(&self) -> SyncRecoveryCategory { ... } }`
    - `pub(crate) fn recovery_category_from_error_detail(detail: &str) -> Option<SyncRecoveryCategory>`

    Use this exact `SyncRuntimeError` mapping: `NoPeersConfigured`, `AddressResolution`, `PeerCompatibility`, `Io`, and `Network` map to `PublicNetworkUnreachable`; `InvalidData` and `InvalidMagic` map to `InvalidPeerData`; `ResourceLimit` maps to `ResourceExhaustion`; `Storage(error)` delegates to `error.recovery_category()`.

    `recovery_category_from_error_detail` must return `Some(IncompatibleSchema)` for lowercase detail containing `schema invalid`, `invalid schema`, `schema mismatch`, or `invalid schema version`; `Some(StoreCorruption)` for `storage corruption`, `corrupt namespace`, or `corruption in`; `Some(StorageLockContention)` for `lock`, `locked`, or `contention`; `Some(StorageBackendFailure)` for `backend`, `unavailable namespace`, `storage failure`, or `interrupted write`; `Some(ResourceExhaustion)` for `resource limit` or `resource`; `Some(InvalidPeerData)` for `invalid data`, `malformed block`, `invalid block`, `duplicate_block`, `disconnected_block`, or `non_extending_block`; otherwise `None`.
  </action>
  <acceptance_criteria>
    - `rg -n "mod recovery;" packages/open-bitcoin-node/src/sync/types.rs` finds the child module declaration.
    - `rg -n "recovery_category_from_error_detail|PeerFailureReason|SyncStopReason|SyncRuntimeError|operator_cancellation" packages/open-bitcoin-node/src/sync/types/recovery.rs` finds the planned helpers and label coverage.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features</automated>
  </verify>
  <done>Sync runtime inputs map to `SyncRecoveryCategory` through one pure child module instead of per-renderer strings.</done>
</task>

<task type="auto">
  <name>Task 3: Register parity breadcrumbs for the sync recovery mapping module</name>
  <read_first>
    - `docs/parity/source-breadcrumbs.json`
    - `AGENTS.md` parity breadcrumb rule
  </read_first>
  <files>docs/parity/source-breadcrumbs.json</files>
  <action>
    Add `packages/open-bitcoin-node/src/sync/types/recovery.rs` to the existing sync-runtime group that already contains `packages/open-bitcoin-node/src/sync/types.rs`, `sync/types/summary.rs`, and `sync/types/projection.rs`. Use the same sync baseline breadcrumbs as that group.
  </action>
  <acceptance_criteria>
    - `rg -n "packages/open-bitcoin-node/src/sync/types/recovery.rs" docs/parity/source-breadcrumbs.json` finds the new file.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <done>The new Rust sync recovery mapping child module is covered by the parity breadcrumb manifest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer/storage/runtime errors -> status category | Untrusted peer behavior and local storage failures become operator guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Tampering | `PeerFailureReason` mapping | mitigate | Map invalid peer data to `invalid_peer_data` with tests so bad peers cannot be credited as progress or hidden as generic network issues. |
| T-61-02-02 | Repudiation | storage-first precedence | mitigate | Storage mappings are typed and tested separately, enabling runtime projection to prefer storage recovery categories before peer retry guidance. |
| T-61-02-03 | Denial of Service | resource exhaustion signals | mitigate | Map resource-limit facts to `resource_exhaustion` so bounded-resource stops are actionable and not retried indefinitely as public-network failures. |
</threat_model>

<verification>
Run focused mapping and breadcrumb checks:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features
bun run scripts/check-parity-breadcrumbs.ts --check
```
</verification>

<success_criteria>
- Storage, peer, stop, runtime, and detail facts map into `SyncRecoveryCategory`.
- Storage incompatibility/corruption/lock/backend categories are distinct and test-covered.
- No renderer needs ad hoc string classification for Phase 61 recovery labels.
</success_criteria>

<output>
After completion, create `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-02-SUMMARY.md`.
</output>
