---
phase: 61-resource-bounds-and-recovery-taxonomy
plan: 05
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [RR-02, RR-04]
user_setup: []
must_haves:
  truths:
    - "Operator status output displays the stable machine recovery category separately from human recovery action."
    - "Dashboard sync rows display the same stable category label."
    - "RPC-facing blockchain warnings include the category label when durable status has one."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      provides: "Human status recovery category rendering"
      contains: "Sync recovery category"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Dashboard recovery category row"
      contains: "Recovery category"
    - path: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      provides: "RPC warning integration for recovery category"
      contains: "recovery_category="
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      to: "SyncStatus.recovery_category"
      via: "status snapshot rendering"
      pattern: "snapshot.sync.recovery_category"
    - from: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      to: "getblockchaininfo warnings"
      via: "durable_warnings"
      pattern: "recovery_category="
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 61-2026-06-06T03-43-41
generated_at: 2026-06-06T04:00:59Z
---

<objective>
Expose the shared recovery category across the existing status, dashboard, and RPC operator surfaces.

Purpose: D-07 and RR-04 require operator-visible errors and guidance to stay typed and consistent without each renderer inventing category labels.
Output: Additive renderer rows/warnings and targeted tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-RESEARCH.md
@packages/open-bitcoin-node/src/status/recovery.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/open-bitcoin-rpc/src/dispatch/tests.rs

<interfaces>
Status field created by Plan 01:
```rust
pub recovery_category: FieldAvailability<SyncRecoveryCategory>
```

Renderer output targets:
```text
Sync recovery category: invalid_peer_data
Recovery category: invalid_peer_data
recovery_category=invalid_peer_data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render recovery category in human status and dashboard rows</name>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decision D-07
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/dashboard/model.rs</files>
  <behavior>
    - Test: human status output contains `Sync recovery category: invalid_peer_data`.
    - Test: human status output still contains the existing `Sync recovery:` line for the human recovery action.
    - Test: dashboard sync section contains row label `Recovery category` with value `invalid_peer_data`.
  </behavior>
  <action>
    In status rendering, import or use `SyncRecoveryCategory` as needed and add a helper `sync_recovery_category_availability(&FieldAvailability<SyncRecoveryCategory>) -> String` that returns `category.as_str().to_string()` for available values and the existing unavailable format for unavailable values. Add a line immediately before `Sync recovery:`:
    `Sync recovery category: {value}`.

    In dashboard model projection, add a row immediately before existing `Recovery`:
    `row("Recovery category", sync_recovery_category(&snapshot.sync.recovery_category))`.
    Add helper `sync_recovery_category` mirroring status label handling.

    Update local fixtures to set `recovery_category: FieldAvailability::available(SyncRecoveryCategory::InvalidPeerData)` and update assertions to check exact label text.
  </action>
  <acceptance_criteria>
    - `rg -n "Sync recovery category|Recovery category|sync_recovery_category|InvalidPeerData" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/dashboard/model.rs` finds helpers, labels, and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features</automated>
  </verify>
  <done>Status and dashboard render the same stable recovery category label while preserving existing human recovery guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Include recovery category in RPC-facing durable warnings</name>
  <read_first>
    - `packages/open-bitcoin-rpc/src/dispatch/node.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decision D-07
  </read_first>
  <files>packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <behavior>
    - Test: `getblockchaininfo` warnings include `recovery_category=invalid_peer_data` when durable sync status contains `SyncRecoveryCategory::InvalidPeerData`.
    - Test: existing last-error and recovery-action warnings remain present.
  </behavior>
  <action>
    In `durable_warnings`, when `durable_sync_state.sync.recovery_category` is available, push `format!("recovery_category={}", value.as_str())` before pushing `recovery_action`. Keep `last_error` first so existing warning order remains stable except for the new category between last error and recovery action.

    Update the existing `getblockchaininfo` durable sync warning fixture in `dispatch/tests.rs` to set `recovery_category: FieldAvailability::available(SyncRecoveryCategory::InvalidPeerData)` and assert warnings contain `recovery_category=invalid_peer_data`, the existing latest warning, and the existing recovery action text.
  </action>
  <acceptance_criteria>
    - `rg -n "recovery_category=|sync.recovery_category|InvalidPeerData" packages/open-bitcoin-rpc/src/dispatch/node.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds implementation and assertions.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info --all-features</automated>
  </verify>
  <done>RPC-facing warnings expose the same machine category label as status while preserving existing error and action warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable status -> CLI/dashboard/RPC output | Machine recovery state is exposed to operator-facing text and RPC clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Repudiation | status/dashboard/RPC renderers | mitigate | Render `SyncRecoveryCategory::as_str()` directly so all touched surfaces expose the same auditable label. |
| T-61-05-02 | Information Disclosure | RPC warnings | accept | Warnings contain only coarse category labels and existing action text; no new raw local paths, secrets, or peer endpoint arrays are added. |
| T-61-05-03 | Spoofing | operator guidance | mitigate | Keep category label separate from human action text so scripts can compare stable category values instead of parsing prose. |
</threat_model>

<verification>
Run focused renderer/RPC tests:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info --all-features
```
</verification>

<success_criteria>
- Status, dashboard, and RPC-facing warnings expose the same `sync.recovery_category` label.
- Existing `sync.recovery_action` guidance remains visible and separate.
- No raw support/report/public-network data is added to these renderers.
</success_criteria>

<output>
After completion, create `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-05-SUMMARY.md`.
</output>
