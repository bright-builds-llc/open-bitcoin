---
phase: 61-resource-bounds-and-recovery-taxonomy
plan: 04
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
  - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: [RR-01, RR-02, RR-04]
user_setup: []
must_haves:
  truths:
    - "Opt-in live-smoke reports use the same Phase 61 recovery category labels as Rust status."
    - "Support evidence keeps recovery category and resource-pressure summaries compact and allowlisted."
    - "Support evidence does not include raw live-smoke reports, daemon tails, endpoint tables, secrets, wallet material, or unbounded samples."
  artifacts:
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "Live-smoke recovery diagnosis labels and compact final status resource-pressure summary"
      contains: "type RecoveryDiagnosisCategory"
    - path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      provides: "Allowlisted support summary extraction"
      contains: "resourcePressure"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "Support redaction and summary regression tests"
      contains: "open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence"
  key_links:
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      via: "schema version 2 `result` and `final_status` summary fields"
      pattern: "recoveryCategory|resourcePressure"
    - from: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      to: "support-evidence.md"
      via: "Markdown summary labels"
      pattern: "Recovery category|Resource pressure"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 61-2026-06-06T03-43-41
generated_at: 2026-06-06T04:00:59Z
---

<objective>
Project Phase 61 recovery categories and resource bounds through live-smoke and compact support evidence.

Purpose: D-02, D-03, D-07, D-08, D-10, and D-11 require deterministic script/support checks without broad Phase 65 support-bundle expansion.
Output: Additive live-smoke/support fields and fixture tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-RESEARCH.md
@.planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-VERIFICATION.md
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs

<interfaces>
Phase 61 live-smoke category labels:
```typescript
type RecoveryDiagnosisCategory =
  | "clean_shutdown"
  | "unclean_shutdown"
  | "incompatible_schema"
  | "store_corruption"
  | "storage_lock_contention"
  | "storage_backend_failure"
  | "resource_exhaustion"
  | "invalid_peer_data"
  | "public_network_unreachable"
  | "operator_cancellation";
```

Compact resource pressure summary fields:
```typescript
type ResourcePressureSummary = {
  blocksInFlight: number;
  maxHeaderRequestsInFlightPerPeer: number;
  maxHeadersPerMessage: number;
  maxBlocksInFlightPerPeer: number;
  maxBlocksInFlightTotal: number;
  maxMessagesPerPeer: number;
  maxSyncRounds: number;
  outboundPeers: number;
  targetOutboundPeers: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align live-smoke recovery diagnosis labels and compact final-status bounds</name>
  <read_first>
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-04, D-06, D-07, D-10, D-11
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - Test: schema mismatch fixture reports `result.restartResumeEvidence.recoveryDiagnosis.category` as `incompatible_schema`.
    - Test: store corruption fixture reports `store_corruption`.
    - Test: storage lock fixture reports `storage_lock_contention`.
    - Test: generic storage backend fixture reports `storage_backend_failure`.
    - Test: cancellation fixture reports `operator_cancellation`.
    - Test: final status summary includes compact `recoveryCategory` and `resourcePressure` fields when status JSON makes them available.
  </behavior>
  <action>
    In `scripts/run-live-mainnet-smoke.ts`, replace the existing seven-value `RecoveryDiagnosisCategory` union with the ten Phase 61 labels from the interface block. Rename the schema classification output from `store_incompatibility` to `incompatible_schema`; keep `store_corruption`, `resource_exhaustion`, `invalid_peer_data`, `public_network_unreachable`, and cancellation as `operator_cancellation`. Add classification branches for `storage_lock_contention` and `storage_backend_failure` before public-network fallback.

    Add `type ResourcePressureSummary` with the exact camelCase fields from the interface block. Extend `FinalStatusSummary` with `recoveryCategory: RecoveryDiagnosisCategory | null` and `resourcePressure: ResourcePressureSummary | null`. When parsing status JSON, read `sync.recovery_category.value` and `sync.resource_pressure.value`; convert snake_case resource fields to the camelCase summary. Do not copy `recentPeers` beyond the existing compact peer telemetry summary and do not add raw daemon tails or endpoint arrays to the support-facing result object.

    Update `scripts/test-run-live-mainnet-smoke.sh` fixture checks for the new labels and add a fixture status payload containing `sync.recovery_category` plus `sync.resource_pressure`. Assert the report JSON contains `"recoveryCategory": "invalid_peer_data"` and `"maxBlocksInFlightTotal": 64`, and assert `scripts/verify.sh` still has no live-smoke invocation.
  </action>
  <acceptance_criteria>
    - `rg -n "clean_shutdown|unclean_shutdown|incompatible_schema|storage_lock_contention|storage_backend_failure|operator_cancellation|ResourcePressureSummary|recoveryCategory|resourcePressure" scripts/run-live-mainnet-smoke.ts` finds every planned label and summary type.
    - `rg -n "incompatible_schema|storage_lock_contention|maxBlocksInFlightTotal|recoveryCategory" scripts/test-run-live-mainnet-smoke.sh` finds fixture assertions.
    - `bash scripts/test-run-live-mainnet-smoke.sh` passes.
    - `! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh` remains true.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <done>Live-smoke fixtures use the Phase 61 label set and expose compact resource pressure without becoming default public-network verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Allowlist compact recovery and resource fields in support evidence</name>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/tests/operator_binary.rs`
    - `.planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-VERIFICATION.md`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decision D-08
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/support/live_smoke.rs, packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <behavior>
    - Test: support JSON preserves `live_smoke.summary.finalStatus.recoveryCategory`.
    - Test: support JSON preserves `live_smoke.summary.finalStatus.resourcePressure.maxBlocksInFlightTotal` and `targetOutboundPeers`.
    - Test: support Markdown includes labels `Recovery category` and `Resource pressure`.
    - Test: forbidden raw values still do not appear: raw daemon tails, endpoint tables, `manualPeers`, cookie-like text, wallet-like text, `rpcpassword`, and raw endpoint addresses.
  </behavior>
  <action>
    In `support/live_smoke.rs`, add allowlisted summary extraction for `recoveryCategory` and nested `resourcePressure` under final status. Create `RESOURCE_PRESSURE_KEYS` with exact keys `blocksInFlight`, `maxHeaderRequestsInFlightPerPeer`, `maxHeadersPerMessage`, `maxBlocksInFlightPerPeer`, `maxBlocksInFlightTotal`, `maxMessagesPerPeer`, `maxSyncRounds`, `outboundPeers`, `targetOutboundPeers`. Preserve the recursive redaction path for values.

    In `support/render.rs`, render final-status `recoveryCategory` under label `Recovery category` and nested `resourcePressure` under label `Resource pressure`. Keep the existing `Recovery diagnosis` label for restart/resume evidence.

    In `operator_binary.rs`, add or rename the support fixture test to `open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence`. Include a schema version 2 live-smoke fixture with `result.restartResumeEvidence.recoveryDiagnosis.category = "storage_lock_contention"`, `final_status.recoveryCategory = "invalid_peer_data"`, and `final_status.resourcePressure.maxBlocksInFlightTotal = 64`. Assert both JSON and Markdown retain only allowlisted compact fields and still redact/omit the existing forbidden raw markers.
  </action>
  <acceptance_criteria>
    - `rg -n "RESOURCE_PRESSURE_KEYS|recoveryCategory|resourcePressure|maxBlocksInFlightTotal|targetOutboundPeers" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs packages/open-bitcoin-cli/tests/operator_binary.rs` finds the allowlist and test assertions.
    - `rg -n "Recovery category|Resource pressure" packages/open-bitcoin-cli/src/operator/support/render.rs packages/open-bitcoin-cli/tests/operator_binary.rs` finds Markdown labels and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence --all-features</automated>
  </verify>
  <done>Support evidence exposes compact Phase 61 recovery and resource facts while preserving the Phase 59 allowlist/redaction posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local live-smoke report -> support bundle | Operator-provided JSON is summarized into support evidence. |
| public-network evidence -> deterministic fixtures | Optional public-network reports are tested by hermetic fixtures only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Information Disclosure | support live-smoke summary | mitigate | Preserve allowlist extraction and recursive redaction; add only `recoveryCategory` and compact `resourcePressure` keys. |
| T-61-04-02 | Denial of Service | support evidence size | mitigate | Do not add raw snapshots, daemon tails, endpoint tables, or unbounded arrays; tests assert forbidden raw fields remain absent. |
| T-61-04-03 | Repudiation | live-smoke diagnosis labels | mitigate | Fixture tests assert exact Phase 61 labels so optional UAT reports and support summaries can be compared by category. |
</threat_model>

<verification>
Run script/support focused checks:

```bash
bash scripts/test-run-live-mainnet-smoke.sh
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence --all-features
! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh
```
</verification>

<success_criteria>
- Live-smoke and support evidence use the same Phase 61 recovery labels.
- Support evidence includes compact resource-pressure fields and no raw/unbounded report material.
- Public-network long-run review remains opt-in and outside `scripts/verify.sh`.
</success_criteria>

<output>
After completion, create `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-04-SUMMARY.md`.
</output>
