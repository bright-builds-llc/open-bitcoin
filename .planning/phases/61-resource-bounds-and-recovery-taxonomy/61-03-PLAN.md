---
phase: 61-resource-bounds-and-recovery-taxonomy
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements: [RR-01, RR-02, RR-04]
user_setup: []
must_haves:
  truths:
    - "Durable sync status projects `sync.recovery_category` with storage-first precedence."
    - "Structured sync logs include the same recovery category label as status when a category exists."
    - "Deterministic repeated sync fixtures preserve configured resource pressure and bounded retention behavior without public-network checks."
    - "Peer retry state is keyed by resolved endpoint and bounded by candidate peers/outbound target per cycle; durable storage writes are synchronous adapter calls with no queued write backlog."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "Storage-first durable recovery category projection"
      contains: "recovery_category"
    - path: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      provides: "Summary-derived status and structured-log recovery category projection"
      contains: "recovery_category"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Deterministic resource-bound and recovery precedence tests"
      contains: "bounded_unattended_cycles_preserve_resource_pressure_and_retention"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      to: "RuntimeMetadata.maybe_last_recovery_action"
      via: "storage-first category projection"
      pattern: "maybe_last_recovery_action"
    - from: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      to: "StructuredLogRecord.message"
      via: "recovery_category=<label>"
      pattern: "recovery_category="
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "RR-01 retry/state and storage-write bounds"
      via: "deterministic repeated-cycle assertions"
      pattern: "peer retry state is keyed by resolved endpoint"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 61-2026-06-06T03-43-41
generated_at: 2026-06-06T04:00:59Z
---

<objective>
Project the shared recovery taxonomy and resource-pressure bounds through the durable sync runtime.

Purpose: RR-01, RR-02, RR-04, D-01, D-02, D-03, D-05, and D-07 require deterministic proof that active bounds and recovery labels survive long-run style cycles without unbounded growth.
Output: Runtime/status/log projection and deterministic Rust tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-RESEARCH.md
@.planning/phases/60-unattended-sync-loop-control/60-01-SUMMARY.md
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/logging.rs

<interfaces>
Runtime projection point:
```rust
pub(super) fn durable_sync_state_from_summary(
    &self,
    summary: &SyncRunSummary,
    lifecycle: SyncLifecycleState,
    maybe_last_error: Option<String>,
    timestamp: i64,
) -> Result<DurableSyncState, SyncRuntimeError>
```

Resource pressure contract from D-01:
```rust
pub struct SyncResourcePressure {
    pub blocks_in_flight: u64,
    pub max_header_requests_in_flight_per_peer: u64,
    pub max_headers_per_message: u64,
    pub max_blocks_in_flight_per_peer: u64,
    pub max_blocks_in_flight_total: u64,
    pub max_messages_per_peer: u64,
    pub max_sync_rounds: u64,
    pub outbound_peers: u32,
    pub target_outbound_peers: u32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project recovery categories in summaries, status, and structured logs</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/types/recovery.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-04, D-06, D-07
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/types/summary.rs</files>
  <behavior>
    - Test: `SyncRunSummary::sync_status()` sets `recovery_category` from latest peer failure when a failure exists.
    - Test: `SyncRunSummary::sync_status()` sets `operator_cancellation` for `SyncStopReason::OperatorPaused` and `SyncStopReason::ShutdownRequested`.
    - Test: `structured_log_records()` includes `recovery_category=invalid_peer_data` or another exact label when a category exists, and `recovery_category=unavailable` when none exists.
  </behavior>
  <action>
    In `SyncRunSummary::sync_status`, set `recovery_category` using this exact order: `maybe_stop_reason.and_then(SyncStopReason::recovery_category)`, then latest peer failure category from a new `latest_recovery_category()` helper, then `FieldAvailability::unavailable("no recovery category recorded")`.

    Add `pub(crate) fn latest_recovery_category(&self) -> Option<SyncRecoveryCategory>` that scans `peer_outcomes` in reverse and maps `maybe_failure_reason` through `PeerFailureReason::recovery_category()`.

    In `structured_log_records`, append `recovery_category={label}` to the primary sync progress message. Use the exact label from `SyncRecoveryCategory::as_str()` when available and `unavailable` when no category exists.
  </action>
  <acceptance_criteria>
    - `rg -n "latest_recovery_category|recovery_category=" packages/open-bitcoin-node/src/sync/types/summary.rs` finds the helper and structured log field.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_summary_projects_consistent_operator_evidence_fields --all-features` passes and asserts the category field.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovery_category --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovery_category --all-features</automated>
  </verify>
  <done>Summary-derived status and structured logs carry the same stable recovery category label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply storage-first recovery category precedence in durable runtime state</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/storage.rs`
    - `packages/open-bitcoin-node/src/sync/types/recovery.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-05 and D-06
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Test: when `RuntimeMetadata.maybe_last_recovery_action` is `Some(StorageRecoveryAction::Repair)`, durable status reports `sync.recovery_category = store_corruption` even when latest peer failure would map to `public_network_unreachable`.
    - Test: when `RuntimeMetadata.maybe_last_recovery_action` is `Some(StorageRecoveryAction::Repair)` and `maybe_last_error` contains a broad peer/network detail such as `peer stalled waiting for headers`, durable status reports `sync.recovery_category = store_corruption`.
    - Test: when `maybe_last_error` contains `database lock contention`, durable status reports `storage_lock_contention`.
    - Test: stopped clean metadata reports `clean_shutdown`; recovering with `last_clean_shutdown=false` reports `unclean_shutdown`.
  </behavior>
  <action>
    In `durable_sync_state_from_summary`, set `sync.recovery_category` after `sync.last_error` and before `sync.recovery_action`. Use this exact precedence:
    1. `metadata.maybe_last_recovery_action.map(StorageRecoveryAction::recovery_category)`
    2. `maybe_last_error.as_deref().and_then(recovery_category_from_error_detail)`
    3. `summary.maybe_stop_reason.and_then(SyncStopReason::recovery_category)`
    4. `summary.latest_recovery_category()`
    5. `Some(SyncRecoveryCategory::CleanShutdown)` only when `lifecycle == SyncLifecycleState::Stopped && metadata.last_clean_shutdown`
    6. `Some(SyncRecoveryCategory::UncleanShutdown)` only when `lifecycle == SyncLifecycleState::Recovering && !metadata.last_clean_shutdown`
    7. unavailable with reason `no recovery category recorded`

    This order intentionally lets broad peer/network/resource `maybe_last_error` parsing run only after durable storage metadata, per D-05. It is acceptable to classify storage-specific last-error details before metadata if the implementation introduces a storage-only helper, but broad parsing that can yield `PublicNetworkUnreachable`, `InvalidPeerData`, or `ResourceExhaustion` must remain after `metadata.maybe_last_recovery_action`.

    Keep existing `sync.recovery_action` text behavior unchanged, including storage action precedence over peer recovery action.

    Add focused tests to `sync/tests.rs` named `durable_sync_state_projects_storage_first_recovery_category`, `durable_sync_state_storage_metadata_beats_peer_network_last_error_detail`, `durable_sync_state_projects_storage_lock_category_from_last_error`, and `durable_sync_state_distinguishes_clean_and_unclean_shutdown_category`.
  </action>
  <acceptance_criteria>
    - `rg -n "recovery_category_from_error_detail|CleanShutdown|UncleanShutdown|maybe_last_recovery_action" packages/open-bitcoin-node/src/sync/runtime_state.rs packages/open-bitcoin-node/src/sync/tests.rs` finds the precedence implementation and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_sync_state_projects_storage_first_recovery_category --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_sync_state_storage_metadata_beats_peer_network_last_error_detail --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_sync_state_projects_storage_lock_category_from_last_error --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_sync_state_distinguishes_clean_and_unclean_shutdown_category --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_sync_state_projects_storage_first_recovery_category --all-features</automated>
  </verify>
  <done>Durable sync status projects recovery categories with storage and local-store signals ahead of peer/network guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove resource pressure and retention stay bounded in deterministic repeated cycles</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-node/src/metrics.rs`
    - `packages/open-bitcoin-node/src/logging.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-CONTEXT.md` decisions D-01, D-02, D-03, D-10, D-11
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Test: repeated scripted sync cycles do not report `blocks_in_flight` above `max_blocks_in_flight_total`.
    - Test: reported `outbound_peers` never exceeds `target_outbound_peers` in the durable `SyncResourcePressure`.
    - Test: peer retry state is keyed by resolved endpoint and bounded by candidate peers/outbound target per cycle; no retry queue or retained retry array grows across cycles.
    - Test: durable storage writes are synchronous adapter calls with no queued write backlog, proven by immediately reading the durable status/metadata after each scripted cycle without a drain step.
    - Test: metrics retention remains capped by `MetricRetentionPolicy::default().max_samples_per_series`.
    - Test: structured log retention uses `LogRetentionPolicy::default()` and the test asserts the default caps `max_files=14`, `max_age_days=14`, and `max_total_bytes=268435456`.
  </behavior>
  <action>
    Add `bounded_unattended_cycles_preserve_resource_pressure_and_retention` to `sync/tests.rs`. Use `ScriptedTransport` and `ScriptedResolver` only; do not call public-network code. Configure `SyncRuntimeConfig` with `target_outbound_peers: 2`, `max_messages_per_peer: 3`, `max_rounds: 5`, `max_blocks_in_flight_per_peer: 2`, `max_blocks_in_flight_total: 4`, and a temporary `maybe_log_dir`.

    Run at least three sync cycles with scripted outcomes covering one connected peer, one retry/backoff or stalled peer, and one invalid-data peer. Assert the final durable `SyncResourcePressure` has `max_header_requests_in_flight_per_peer=1`, `max_headers_per_message=2000`, `max_blocks_in_flight_per_peer=2`, `max_blocks_in_flight_total=4`, `max_messages_per_peer=3`, `max_sync_rounds=5`, `target_outbound_peers=2`, `outbound_peers <= 2`, and `blocks_in_flight <= 4`.

    Assert peer retry state is keyed by resolved endpoint and bounded by candidate peers/outbound target per cycle: use distinct resolved endpoints in the scripted resolver, assert retry/backoff evidence never exceeds the resolved candidate count for the configured target outbound peers, and do not introduce a separate retry queue. Assert durable storage writes are synchronous adapter calls with no queued write backlog by reading the durable status/metadata immediately after each scripted cycle and proving the latest cycle facts are already visible without draining a background queue.

    Assert metrics and log retention constants directly from `MetricRetentionPolicy::default()` and `LogRetentionPolicy::default()`, and assert loaded structured log record count is finite for the scripted cycles. This proves Phase 61 uses existing bounded retention rather than adding new retained arrays.
  </action>
  <acceptance_criteria>
    - `rg -n "bounded_unattended_cycles_preserve_resource_pressure_and_retention|peer retry state is keyed by resolved endpoint|durable storage writes are synchronous adapter calls with no queued write backlog|MetricRetentionPolicy::default|LogRetentionPolicy::default|max_blocks_in_flight_total: 4" packages/open-bitcoin-node/src/sync/tests.rs` finds the deterministic bound test and exact RR-01 mechanism statements.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_unattended_cycles_preserve_resource_pressure_and_retention --all-features` passes.
    - `! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh` remains true.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_unattended_cycles_preserve_resource_pressure_and_retention --all-features</automated>
  </verify>
  <done>Resource pressure, retry state bounds, synchronous storage-write/no-backlog behavior, metrics retention, structured log retention, and retry evidence are proven through deterministic scripted cycles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync runtime -> durable status/logs | Runtime facts are persisted and rendered to operators after repeated sync cycles. |
| public peer input -> resource pressure | Untrusted peers can trigger retry, invalid data, and block request paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Denial of Service | resource pressure, retry state, and storage writes | mitigate | Assert configured in-flight, message, round, outbound-peer, endpoint-keyed retry-state, and synchronous storage-write/no-backlog bounds under repeated deterministic sync cycles. |
| T-61-03-02 | Repudiation | structured sync logs | mitigate | Include `recovery_category=<label>` in structured log messages so status and logs can be compared by exact category label. |
| T-61-03-03 | Information Disclosure | structured logs | mitigate | Log only coarse category labels and bounded progress counters; do not add peer endpoint tables, raw reports, secrets, or wallet material. |
</threat_model>

<verification>
Run focused runtime tests:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovery_category --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node bounded_unattended_cycles_preserve_resource_pressure_and_retention --all-features
! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh
```
</verification>

<success_criteria>
- Durable status uses storage-first recovery category precedence.
- Structured logs include the same category label as status.
- Deterministic tests prove active resource pressure and retention remain bounded without public-network verification.
</success_criteria>

<output>
After completion, create `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-03-SUMMARY.md`.
</output>
