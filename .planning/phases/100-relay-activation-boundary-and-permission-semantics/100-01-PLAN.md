---
phase: 100-relay-activation-boundary-and-permission-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/relay.rs
  - packages/open-bitcoin-network/src/inbound/permissions.rs
  - packages/open-bitcoin-network/src/inbound.rs
  - packages/open-bitcoin-network/src/inbound/tests.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 100-2026-06-29T16-18-03
generated_at: 2026-06-29T17:07:25Z
must_haves:
  truths:
    - "Default relay activation policy is disabled and cannot make a peer relay-eligible."
    - "Outbound and manual peers become relay-eligible only when relay activation is enabled."
    - "Inbound peers require inbound serving plus scoped relay-like permission effects before relay eligibility."
    - "Protected admission effects remain separate from transaction-relay eligibility."
    - "Bloom/filter and compact-block-related permission effects remain inactive and do not affect service bits."
  artifacts:
    - path: "packages/open-bitcoin-network/src/relay.rs"
      provides: "Pure relay activation and peer eligibility policy"
      contains: "classify_relay_eligibility"
    - path: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      provides: "Scoped relay permission effect labels derived from parsed permission tokens"
      contains: "RelayPermissionEffectLabel"
    - path: "packages/open-bitcoin-network/src/inbound/tests.rs"
      provides: "Permission expansion and scoped relay-effect regression tests"
      contains: "transaction_relay_policy_input"
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Negative socket/message behavior safeguards for relay-like permissions"
      contains: "scoped_relay_permission"
  key_links:
    - from: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      to: "packages/open-bitcoin-network/src/relay.rs"
      via: "RelayPermissionEffectLabel exported from the network crate"
      pattern: "RelayPermissionEffectLabel"
    - from: "packages/open-bitcoin-network/src/relay.rs"
      to: "PeerConnectionClass"
      via: "Eligibility matrix input"
      pattern: "PeerConnectionClass::"
    - from: "packages/open-bitcoin-network/src/peer/tests.rs"
      to: "PeerManager::handle_message"
      via: "Negative tests prove no socket relay action is introduced"
      pattern: "handle_message"
---

<objective>
Create the pure relay activation and eligibility boundary in `open-bitcoin-network`.

Purpose: Phase 100 needs one typed decision point for transaction-relay activation before later v2.0 plans consume relay inventory, download, mempool, or serving behavior.
Output: A tested `relay` policy module, scoped relay permission-effect labels, stable eligibility reasons, and source breadcrumbs for the new Rust module.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-08-SUMMARY.md
@packages/open-bitcoin-network/src/inbound/permissions.rs
@packages/open-bitcoin-network/src/inbound.rs
@packages/open-bitcoin-network/src/inbound/tests.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-network/src/message.rs
@packages/open-bitcoin-network/src/lib.rs

<interfaces>
From `packages/open-bitcoin-network/src/inbound/permissions.rs`:

```rust
pub enum PeerPermissionToken {
    BloomFilter,
    BlockFilters,
    NoBan,
    ForceRelay,
    Relay,
    Mempool,
    Download,
    Addr,
    ForceInbound,
    All,
}

pub enum PermissionEffectLabel {
    AdmissionProtected,
    EvictionPolicyProtected,
    MisbehaviorPolicyProtected,
    AddressResponsePolicyInput,
    DownloadServingPolicyInput,
}

pub enum InactivePermissionEffectLabel {
    Relay,
    ForceRelay,
    Mempool,
    BloomFilter,
    BlockFilters,
}

pub enum PeerConnectionClass {
    OrdinaryInbound,
    PermissionedInbound,
    ProtectedInbound,
    Outbound,
    ManualConfigured,
}
```

The executor should keep `InactivePermissionEffectLabel::Relay`, `ForceRelay`, and `Mempool` as accepted legacy labels for existing support/status sanitizer fixtures, but Phase 100 permission parsing must stop emitting them for new permission decisions. New relay-like permission effects must use `RelayPermissionEffectLabel`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote relay-like permissions into scoped policy-effect labels</name>
  <files>packages/open-bitcoin-network/src/inbound/permissions.rs, packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/inbound/tests.rs, packages/open-bitcoin-network/src/peer/tests.rs, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>
    AGENTS.md
    standards/languages/rust.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
    packages/open-bitcoin-network/src/inbound/permissions.rs
    packages/open-bitcoin-network/src/inbound/tests.rs
    packages/open-bitcoin-network/src/peer/tests.rs
  </read_first>
  <behavior>
    - `all_permission_emits_scoped_relay_policy_effects_and_keeps_filters_inactive`: parsing `["all"]` yields active labels `admission_protected`, `eviction_policy_protected`, `misbehavior_policy_protected`, `address_response_policy_input`, `download_serving_policy_input`; relay labels `transaction_relay_policy_input`, `force_relay_policy_input`, `mempool_policy_input`; inactive labels `inactive_bloomfilter`, `inactive_blockfilters`.
    - `permissioned_admission_record_preserves_scoped_relay_policy_effect_evidence`: parsing `["in", "download", "addr", "relay", "mempool"]` keeps bounded active effects, emits `transaction_relay_policy_input` and `mempool_policy_input`, and does not emit `inactive_relay` or `inactive_mempool` for the new decision.
    - `scoped_relay_permission_effects_remain_policy_only_for_transaction_paths`: a permissioned inbound peer with `relay`, `forcerelay`, and `mempool` still produces the same existing `WtxidRelay`, `Inv`, `Tx`, and `GetData` actions as before; no new socket relay branch or mempool mutation is added in Phase 100.
  </behavior>
  <action>
    Add `RelayPermissionEffectLabel` in `packages/open-bitcoin-network/src/inbound/permissions.rs` with variants `TransactionRelayPolicyInput`, `ForceRelayPolicyInput`, and `MempoolPolicyInput`. Its `as_str()` values must be exactly `transaction_relay_policy_input`, `force_relay_policy_input`, and `mempool_policy_input`.

    Add `PeerPermissionSet::relay_permission_effects() -> Vec<RelayPermissionEffectLabel>`. The method must include `TransactionRelayPolicyInput` when `relay` is present per D-10, include both `TransactionRelayPolicyInput` and `ForceRelayPolicyInput` when `forcerelay` is present because existing token expansion makes `forcerelay` imply `relay` per D-11, and include `MempoolPolicyInput` when `mempool` is present per D-12. Keep ordering deterministic in the order above.

    Update `PeerPermissionSet::inactive_effects()` so Phase 100 parsed permission decisions emit only `inactive_bloomfilter` and `inactive_blockfilters`. Do not delete legacy `InactivePermissionEffectLabel::{Relay, ForceRelay, Mempool}` variants in this task because existing support/status redaction code still recognizes historical labels; the change is that the parser no longer emits them for new decisions per D-09 and D-13. Keep `download` and `addr` in their current bounded active-effect paths only and do not reinterpret them as transaction inventory, mempool query, rebroadcast, or public relay permission per D-08.

    Add a private `relay_permission_effects: Vec<RelayPermissionEffectLabel>` field to `InboundPermissionDecision`, populate it from `ParsedPeerPermissionClass::inbound_decision`, add a public accessor `relay_permission_effects(&self) -> &[RelayPermissionEffectLabel]`, and update `InboundPermissionDecision::ordinary()` to return an empty relay-effect vector. Re-export `RelayPermissionEffectLabel` from `packages/open-bitcoin-network/src/inbound.rs` and `packages/open-bitcoin-network/src/lib.rs`.

    Update the Phase 91 tests deliberately instead of deleting safeguards: rename the relay-like negative peer test to include `scoped_relay_permission`, assert the new relay effect labels, assert inactive labels no longer contain `inactive_relay`, `inactive_forcerelay`, or `inactive_mempool`, and keep the existing transaction-message behavior assertions unchanged. This implements D-09 through D-13 while preserving the no-socket/no-mempool Phase 100 boundary.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features --no-run</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib scoped_relay_permission -- --nocapture</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib all_permission_emits_scoped_relay -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub enum RelayPermissionEffectLabel|transaction_relay_policy_input|force_relay_policy_input|mempool_policy_input" packages/open-bitcoin-network/src/inbound/permissions.rs packages/open-bitcoin-network/src/lib.rs` finds the new enum, exact labels, and crate export.
    - `rg -n "inactive_relay|inactive_forcerelay|inactive_mempool" packages/open-bitcoin-network/src/inbound/tests.rs packages/open-bitcoin-network/src/peer/tests.rs` does not find assertions expecting those labels from newly parsed relay-like permissions.
    - `rg -n "inactive_bloomfilter|inactive_blockfilters" packages/open-bitcoin-network/src/inbound/tests.rs packages/open-bitcoin-network/src/peer/tests.rs` finds filter inactivity coverage.
    - No production code in `packages/open-bitcoin-network/src/peer.rs` branches on `RelayPermissionEffectLabel` in this task.
  </acceptance_criteria>
  <done>Relay-like permission tokens produce typed v2.0 policy input labels, bloom/filter labels remain inactive, and peer message behavior stays unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure relay activation and eligibility matrix</name>
  <files>packages/open-bitcoin-network/src/relay.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    AGENTS.md
    standards/core/architecture.md
    standards/languages/rust.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
    packages/open-bitcoin-network/src/lib.rs
    docs/parity/source-breadcrumbs.json
    packages/bitcoin-knots/src/net_permissions.h
    packages/bitcoin-knots/src/net_processing.cpp
  </read_first>
  <behavior>
    - `relay_activation_defaults_to_disabled`: `RelayActivationConfig::default().enabled` is false and default classification returns label `disabled`.
    - `relay_eligibility_matrix_covers_connection_classes`: matrix cases cover `outbound`, `manual_configured`, `ordinary_inbound`, `permissioned_inbound`, and `protected_inbound`.
    - `protected_admission_is_not_relay_eligibility`: `ProtectedInbound` with only admission/eviction protection returns `protected_not_relay`.
    - `inbound_relay_permission_requires_activation_and_inbound_serving`: inbound relay effects return `activation_required` when disabled, `inbound_serving_required` when inbound serving is false, and `eligible` only when activation and inbound serving are both true.
    - `filter_permissions_stay_inactive_for_relay_eligibility`: bloom/filter inactive effects return `permission_effect_inactive` and never set eligibility.
    - `relay_activation_does_not_change_service_bits`: `LocalPeerConfig::default().services` remains `ServiceFlags::NETWORK | ServiceFlags::WITNESS`.
  </behavior>
  <action>
    Create `packages/open-bitcoin-network/src/relay.rs` with a top parity breadcrumb block citing `packages/bitcoin-knots/src/net_permissions.h`, `packages/bitcoin-knots/src/net_permissions.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, and `packages/bitcoin-knots/test/functional/p2p_permissions.py` per D-17.

    Implement a pure data-in/data-out policy surface per D-02 with these public types and exact labels:
    - `RelayActivationConfig { pub enabled: bool }` with `Default` returning `enabled: false` per D-01 and ACT-01.
    - `RelayEligibilityReason` with `as_str()` labels exactly `eligible`, `disabled`, `activation_required`, `inbound_serving_required`, `permission_required`, `protected_not_relay`, and `permission_effect_inactive`.
    - `RelayEligibilityInput { pub activation: RelayActivationConfig, pub inbound_serving_enabled: bool, pub connection_class: PeerConnectionClass, pub relay_permission_effects: Vec<RelayPermissionEffectLabel>, pub inactive_permission_effects: Vec<InactivePermissionEffectLabel> }`.
    - `RelayEligibilityDecision { pub eligible: bool, pub reason: RelayEligibilityReason, pub relay_permission_effects: Vec<RelayPermissionEffectLabel>, pub version_message_relay: bool }`.
    - `classify_relay_eligibility(input: &RelayEligibilityInput) -> RelayEligibilityDecision`.

    The classification rules must implement the explicit eligibility matrix required by D-05:
    - If `activation.enabled` is false and no relay permission effect is present, return `disabled`.
    - If `activation.enabled` is false and a relay permission effect is present, return `activation_required`.
    - If activation is enabled and `connection_class` is `Outbound` or `ManualConfigured`, return `eligible` without requiring permission effects per D-06.
    - If activation is enabled and `connection_class` is inbound but `inbound_serving_enabled` is false, return `inbound_serving_required`.
    - If activation is enabled, inbound serving is enabled, and any relay permission effect is present, return `eligible`.
    - If activation is enabled and `connection_class` is `ProtectedInbound` without a relay permission effect, return `protected_not_relay` per D-07.
    - If activation is enabled and no relay permission effect exists but inactive bloom/filter effects exist, return `permission_effect_inactive` per D-13.
    - Otherwise return `permission_required`.

    `version_message_relay` must be `true` only for `eligible` decisions and false for every non-eligible decision. Do not wire this value into `LocalPeerConfig` or `PeerManager` in Phase 100; it is a typed policy output for later v2.0 phases per D-03.

    Export the relay module from `packages/open-bitcoin-network/src/lib.rs`. Add `packages/open-bitcoin-network/src/relay.rs` to `docs/parity/source-breadcrumbs.json` under a Phase 100 relay activation breadcrumb entry. Do not add sockets, mempool state, wall-clock time, filesystem access, or public-network behavior to this module.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features --no-run</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib relay_activation -- --nocapture</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib relay_eligibility -- --nocapture</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct RelayActivationConfig|pub enum RelayEligibilityReason|pub fn classify_relay_eligibility" packages/open-bitcoin-network/src/relay.rs` finds the public policy surface.
    - `rg -n "eligible|disabled|activation_required|inbound_serving_required|permission_required|protected_not_relay|permission_effect_inactive" packages/open-bitcoin-network/src/relay.rs` finds exact stable labels.
    - `rg -n "ServiceFlags::NETWORK \\| ServiceFlags::WITNESS" packages/open-bitcoin-network/src/relay.rs packages/open-bitcoin-network/src/message.rs` proves service-bit regression coverage remains present.
    - `rg -n "packages/open-bitcoin-network/src/relay.rs" docs/parity/source-breadcrumbs.json` finds the new breadcrumb entry.
  </acceptance_criteria>
  <done>The network crate exports a pure default-off relay activation policy and complete peer eligibility matrix covering ACT-01 through ACT-04 without touching socket or mempool behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Parsed permission config -> network policy | User-controlled JSONC/CLI permission tokens become typed permission effects and relay eligibility inputs. |
| Network policy -> later relay behavior | A pure eligibility decision will later gate transaction inventory, download, mempool, and serving behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-100-01-01 | Elevation of Privilege | `classify_relay_eligibility` | mitigate | Default activation is false; matrix tests must assert protected, permissioned, inbound, outbound, and manual decisions cannot become eligible without the required activation and permission conditions. |
| T-100-01-02 | Denial of Service | `PeerPermissionSet::relay_permission_effects` and `all` expansion | mitigate | Relay-like effects are policy inputs only; peer/socket behavior tests must prove `WtxidRelay`, `Inv`, `Tx`, and `GetData` behavior is unchanged, and no resource cap is raised in Phase 100. |
| T-100-01-03 | Information Disclosure | Permission effect labels | mitigate | New labels are fixed low-cardinality strings; do not store raw permission class names, endpoints, peer ids, transaction ids, raw hex, or credentials in the policy output. |
| T-100-01-04 | Tampering | Permission token parser | mitigate | Keep closed enum parsing and exact-token tests; unsupported aliases stay rejected at the parser boundary. |
</threat_model>

<verification>
Plan-level verification:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all --check
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features --no-run
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib relay_activation -- --nocapture
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib relay_eligibility -- --nocapture
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib scoped_relay_permission -- --nocapture
bun run scripts/check-parity-breadcrumbs.ts --check
```
</verification>

<success_criteria>
- ACT-01: `RelayActivationConfig::default().enabled == false` and default eligibility returns `disabled`.
- ACT-02: One pure matrix covers outbound, inbound, manual, protected, and permissioned peers with stable reason labels.
- ACT-03: `relay`, `forcerelay`, and `mempool` produce scoped v2.0 policy-effect labels only.
- ACT-04: `bloomfilter`, `blockfilters`, compact-block behavior, service bits, and unrelated `download`/`addr`/`noban`/`forceinbound` behavior remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-01-SUMMARY.md`.
</output>
