---
phase: 100-relay-activation-boundary-and-permission-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 100-01
files_modified:
  - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/loader.rs
  - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/http/tests.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
autonomous: true
requirements:
  - ACT-01
  - ACT-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 100-2026-06-29T16-18-03
generated_at: 2026-06-29T17:07:25Z
must_haves:
  truths:
    - "Open Bitcoin JSONC config exposes `relay.enabled` and defaults it to false."
    - "`-openbitcoinrelay` is the only new CLI activation flag and overrides JSONC deterministically."
    - "Resolved `RuntimeConfig` carries typed `RelayActivationConfig` for later v2.0 phases."
    - "Baseline Knots `whitelist`, `whitebind`, `whitelistrelay`, and `whitelistforcerelay` compatibility inputs remain rejected."
    - "No PeerManager, socket, service-bit, mempool, or public status behavior changes in this plan."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/config/open_bitcoin.rs"
      provides: "Open Bitcoin JSONC `relay.enabled` DTO"
      contains: "pub struct RelayConfig"
    - path: "packages/open-bitcoin-rpc/src/config.rs"
      provides: "Resolved runtime relay activation field"
      contains: "pub relay: RelayActivationConfig"
    - path: "packages/open-bitcoin-rpc/src/config/loader.rs"
      provides: "`-openbitcoinrelay` CLI parsing"
      contains: "openbitcoinrelay"
    - path: "packages/open-bitcoin-rpc/src/config/tests.rs"
      provides: "JSONC, CLI override, unknown-field, and baseline-rejection tests"
      contains: "openbitcoinrelay"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/config/open_bitcoin.rs"
      to: "open_bitcoin_network::RelayActivationConfig"
      via: "RelayConfig::to_activation_config"
      pattern: "to_activation_config"
    - from: "packages/open-bitcoin-rpc/src/config/loader.rs"
      to: "packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs"
      via: "CliSettings.maybe_relay_enabled"
      pattern: "maybe_relay_enabled"
    - from: "packages/open-bitcoin-rpc/src/config/loader.rs"
      to: "packages/open-bitcoin-rpc/src/config.rs"
      via: "RuntimeConfig.relay"
      pattern: "relay:"
---

<objective>
Wire Phase 100 relay activation into Open Bitcoin-owned JSONC and CLI configuration.

Purpose: Operators need an explicit default-off activation setting before any later relay behavior can consume it.
Output: `relay.enabled` JSONC support, `-openbitcoinrelay` CLI override support, typed `RuntimeConfig.relay`, and parser/precedence tests.
</objective>

<scope_rationale>
This plan intentionally lists the RPC test/context files that contain existing `RuntimeConfig` literals because adding one required `relay: RelayActivationConfig` field creates bounded mechanical compile fallout. The only semantic surfaces are the Open Bitcoin JSONC DTO, CLI parser, resolver, and runtime config field; the remaining touched files should receive literal/helper updates only and must not change PeerManager, socket, mempool, service-bit, or public-status behavior.
</scope_rationale>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-01-SUMMARY.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-02-SUMMARY.md
@packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/config/loader.rs
@packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
@packages/open-bitcoin-rpc/src/config/tests.rs

<interfaces>
Plan 100-01 must export this network contract:

```rust
pub struct RelayActivationConfig {
    pub enabled: bool,
}
```

Existing RPC config contracts:

```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(default, deny_unknown_fields)]
pub struct OpenBitcoinConfig {
    pub schema_version: u32,
    pub sync: SyncConfig,
    pub inbound: InboundConfig,
}

#[derive(Debug, Clone, Default)]
struct CliSettings {
    maybe_inbound_enabled: Option<bool>,
    maybe_inbound_permission_class_specs: Option<Vec<String>>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct RuntimeConfig {
    pub sync: DaemonSyncConfig,
    pub inbound: InboundListenerConfig,
    pub inbound_permission_validation_failures: u32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JSONC `relay.enabled` and typed runtime config</name>
  <files>packages/open-bitcoin-rpc/src/config/open_bitcoin.rs, packages/open-bitcoin-rpc/src/config.rs, packages/open-bitcoin-rpc/src/config/loader.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs, packages/open-bitcoin-rpc/src/http/tests.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/context/tests.rs</files>
  <read_first>
    AGENTS.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-01-SUMMARY.md
    packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/loader.rs
    packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
  </read_first>
  <behavior>
    - `open_bitcoin_jsonc_defaults_relay_activation_to_disabled`: `OpenBitcoinConfig::default().relay.enabled` is false and `RuntimeConfig::default().relay.enabled` is false.
    - `open_bitcoin_jsonc_accepts_relay_activation_enabled`: parsing `{ "relay": { "enabled": true } }` yields `config.relay.enabled == true` and resolved runtime `relay.enabled == true`.
    - `open_bitcoin_jsonc_rejects_unknown_relay_fields`: parsing `{ "relay": { "enabled": false, "surprise": true } }` fails with an error containing `unknown field` and `surprise`.
    - Existing `RuntimeConfig { ... }` literals compile by setting `relay: RelayActivationConfig::default()` or equivalent.
  </behavior>
  <action>
    In `packages/open-bitcoin-rpc/src/config/open_bitcoin.rs`, add `RelayConfig` with `#[serde(default, deny_unknown_fields)]`, one field `pub enabled: bool`, and a `Default` implementation where `enabled` is false. Add `pub relay: RelayConfig` to `OpenBitcoinConfig` and initialize it in `Default`.

    Add `RelayConfig::to_activation_config(&self) -> open_bitcoin_network::RelayActivationConfig` returning `RelayActivationConfig { enabled: self.enabled }`. Re-export `RelayConfig` from `packages/open-bitcoin-rpc/src/config.rs`.

    Add `pub relay: RelayActivationConfig` to `RuntimeConfig`, import `RelayActivationConfig`, and initialize it in `RuntimeConfig::default()` from `OpenBitcoinConfig::default().relay.to_activation_config()`.

    In `packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs`, add `resolve_relay_activation_config(cli: &CliSettings, maybe_config: Option<&OpenBitcoinConfig>) -> RelayActivationConfig`. For this task, it should resolve JSONC/defaults and leave the CLI override branch ready for Task 2. In `load_runtime_config_for_args`, call the resolver once and assign `RuntimeConfig { relay, ... }`.

    Update existing `RuntimeConfig` struct literals in the listed RPC test/context files with `relay: RelayActivationConfig::default()` or a shared test helper. This is mechanical fallout from carrying the typed activation config and must not change PeerManager, socket, mempool, or service-bit behavior.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --no-run</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib open_bitcoin_jsonc_defaults_relay_activation_to_disabled -- --nocapture</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib open_bitcoin_jsonc_accepts_relay_activation_enabled -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct RelayConfig|pub relay: RelayConfig|to_activation_config" packages/open-bitcoin-rpc/src/config/open_bitcoin.rs` finds the JSONC DTO and typed conversion.
    - `rg -n "pub relay: RelayActivationConfig|relay: open_bitcoin_defaults\\.relay\\.to_activation_config\\(\\)" packages/open-bitcoin-rpc/src/config.rs` finds the runtime field and default.
    - `rg -n "open_bitcoin_jsonc_defaults_relay_activation_to_disabled|open_bitcoin_jsonc_accepts_relay_activation_enabled|open_bitcoin_jsonc_rejects_unknown_relay_fields" packages/open-bitcoin-rpc/src/config/tests.rs` finds the focused tests.
    - `rg -n "PeerManager::new|LocalPeerConfig \\{" packages/open-bitcoin-rpc/src/config/open_bitcoin.rs packages/open-bitcoin-rpc/src/config/loader.rs packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs` returns no production wiring from relay config to peer sockets.
  </acceptance_criteria>
  <done>`relay.enabled` exists in the Open Bitcoin JSONC contract, resolves to typed default-off runtime config, and rejects unknown relay config fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `-openbitcoinrelay` CLI override without accepting Knots compatibility shortcuts</name>
  <files>packages/open-bitcoin-rpc/src/config/loader.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs</files>
  <read_first>
    AGENTS.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    packages/open-bitcoin-rpc/src/config/loader.rs
    packages/open-bitcoin-rpc/src/config/loader/inbound.rs
    packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
  </read_first>
  <behavior>
    - `daemon_relay_cli_override_can_enable_or_disable_open_bitcoin_jsonc`: `-openbitcoinrelay=1` overrides JSONC false to true and `-openbitcoinrelay=0` overrides JSONC true to false.
    - `daemon_relay_cli_accepts_negated_open_bitcoin_flag`: `-noopenbitcoinrelay` resolves to `relay.enabled == false`.
    - `daemon_relay_cli_rejects_invalid_boolean`: `-openbitcoinrelay=maybe` fails with exact error `invalid boolean value: maybe`.
    - Existing baseline rejection tests still reject `whitelist`, `whitebind`, `whitelistrelay`, and `whitelistforcerelay`.
  </behavior>
  <action>
    Add `maybe_relay_enabled: Option<bool>` to `CliSettings` in `packages/open-bitcoin-rpc/src/config/loader.rs`.

    In `parse_cli_args`, add an Open Bitcoin-owned match arm for `openbitcoinrelay` using existing `parse_bool(maybe_value, negated)`. This must support `-openbitcoinrelay`, `-openbitcoinrelay=1`, `-openbitcoinrelay=0`, and `-noopenbitcoinrelay`. Do not add or accept Knots `whitelist`, `whitebind`, `whitelistrelay`, or `whitelistforcerelay` as relay activation aliases per D-04.

    Update `resolve_relay_activation_config` so CLI wins over JSONC/defaults: start from `maybe_config.map(|config| config.relay.to_activation_config()).unwrap_or_default()`, then if `cli.maybe_relay_enabled` is `Some(enabled)`, set `relay.enabled = enabled`.

    Add tests in `packages/open-bitcoin-rpc/src/config/tests.rs` for JSONC false overridden by CLI true, JSONC true overridden by CLI false, `-noopenbitcoinrelay`, invalid boolean, and preservation of the existing baseline listener/permission key rejection coverage.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --no-run</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib daemon_relay_cli -- --nocapture</automated>
    <automated>timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib daemon_inbound_rejects_baseline_listener_and_permission_keys -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_relay_enabled|openbitcoinrelay" packages/open-bitcoin-rpc/src/config/loader.rs packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs packages/open-bitcoin-rpc/src/config/tests.rs` finds CLI parsing, precedence, and tests.
    - `rg -n "whitelistrelay|whitelistforcerelay|whitebind|whitelist" packages/open-bitcoin-rpc/src/config/tests.rs` still finds explicit baseline rejection coverage.
    - `rg -n "openbitcoinrelay" packages/open-bitcoin-rpc/src/config/loader/inbound.rs` returns no matches, because relay activation is not an inbound-listener flag even though it uses the same Open Bitcoin-owned CLI prefix style.
  </acceptance_criteria>
  <done>`-openbitcoinrelay` is parsed as the only CLI relay activation override, CLI precedence is tested, and Knots whitelist/whitebind shortcuts remain rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSONC file -> `OpenBitcoinConfig` | Operator-controlled `open-bitcoin.jsonc` enters serde/jsonc parsing. |
| CLI args -> `CliSettings` | Operator-controlled daemon arguments enter config precedence. |
| Resolved config -> later relay behavior | `RuntimeConfig.relay` will be consumed by later v2.0 transaction relay plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-100-02-01 | Tampering | `parse_cli_args` and `parse_open_bitcoin_jsonc_config` | mitigate | Use Open Bitcoin-owned `relay.enabled` and `-openbitcoinrelay`; keep `deny_unknown_fields`; preserve tests rejecting Knots whitelist/whitebind-like inputs. |
| T-100-02-02 | Elevation of Privilege | `RuntimeConfig.relay` | mitigate | Defaults stay false, CLI/JSONC precedence tests cover true and false overrides, and this plan does not wire activation into sockets or mempool behavior. |
| T-100-02-03 | Repudiation | Config precedence | mitigate | Tests must prove CLI wins over JSONC and exact invalid boolean errors remain visible. |
| T-100-02-04 | Information Disclosure | Config errors | mitigate | Error messages may include config keys and invalid scalar values but must not include credentials, peer ids, endpoints beyond supplied test literals, transaction ids, or raw transaction hex. |
</threat_model>

<verification>
Plan-level verification:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all --check
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --no-run
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib open_bitcoin_jsonc_accepts_relay_activation_enabled -- --nocapture
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib daemon_relay_cli -- --nocapture
timeout 90s cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib daemon_inbound_rejects_baseline_listener_and_permission_keys -- --nocapture
```
</verification>

<success_criteria>
- ACT-01: Relay activation is available only through explicit Open Bitcoin-owned `relay.enabled` and `-openbitcoinrelay` settings, both defaulting to false.
- ACT-02: Runtime config carries typed activation state for the eligibility policy without changing service bits, public defaults, sockets, or mempool behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-02-SUMMARY.md`.
</output>
