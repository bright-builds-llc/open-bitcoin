---
phase: 100-relay-activation-boundary-and-permission-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 100-01
  - 100-02
files_modified:
  - docs/architecture/config-precedence.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - scripts/check-phase100-relay-activation-boundary.ts
  - scripts/check-phase100-relay-activation-boundary.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 100-2026-06-29T16-18-03
generated_at: 2026-06-29T17:07:25Z
must_haves:
  truths:
    - "Docs state transaction relay activation is default-off and Open Bitcoin-owned."
    - "Docs name exactly which Phase 100 permission effects are scoped v2.0 policy inputs."
    - "Docs state bloom/filter, compact-block, package-relay, public-default, production-service, production-readiness, production-funds wallet, and public-network CI claims remain out of scope."
    - "A deterministic checker rejects overclaims and missing evidence roots."
    - "`bash scripts/verify.sh` runs the Phase 100 checker after Phase 99 and before pure-core checks."
    - "Final phase verification exists with `status: passed` only after `bash scripts/verify.sh` passes."
  artifacts:
    - path: "scripts/check-phase100-relay-activation-boundary.ts"
      provides: "Fixed-corpus Phase 100 no-claim and evidence checker"
      contains: "checkPhase100RelayActivationBoundary"
    - path: "scripts/check-phase100-relay-activation-boundary.test.ts"
      provides: "Mutation fixtures for missing evidence and forbidden claims"
      contains: "public relay by default"
    - path: "scripts/verify.sh"
      provides: "Default deterministic verifier wiring"
      contains: "check Phase 100 relay activation boundary"
    - path: ".planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md"
      provides: "Phase-level passed verification report"
      contains: "status: passed"
  key_links:
    - from: "docs/operator/runtime-guide.md"
      to: "scripts/check-phase100-relay-activation-boundary.ts"
      via: "Required UAT command and no-claim text"
      pattern: "openbitcoinrelay"
    - from: "docs/parity/index.json"
      to: "scripts/check-phase100-relay-activation-boundary.ts"
      via: "ACT-01 through ACT-04 parity evidence roots"
      pattern: "ACT-01"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase100-relay-activation-boundary.ts"
      via: "Visible and executable verifier order"
      pattern: "check-phase100-relay-activation-boundary"
---

<objective>
Document and guard the Phase 100 relay activation boundary.

Purpose: The policy/config work must not be interpreted as public relay support, compact-block relay, bloom/filter serving, package relay, production service operation, production full-node readiness, production-funds wallet use, or public-network CI.
Output: Updated docs/parity roots, a deterministic Bun checker with mutation tests, verifier wiring, and a passed Phase 100 verification report.
</objective>

<scope_rationale>
This plan intentionally combines docs/parity roots with the fixed-file checker because the checker validates exactly those evidence roots and no-claim boundaries. The implementation is split by task: Task 1 updates docs/parity only, Task 2 adds the deterministic checker and mutation tests, and Task 3 wires default verification plus the generated LOC artifact if `bash scripts/verify.sh` refreshes it.
</scope_rationale>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/languages/typescript-javascript.md
@standards/core/verification.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-01-SUMMARY.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-02-SUMMARY.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-10-SUMMARY.md
@.planning/phases/95-network-participation-evidence-and-release-boundary/95-04-SUMMARY.md
@.planning/phases/98-traceability-reconciliation/98-03-SUMMARY.md
@docs/architecture/config-precedence.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@docs/parity/index.json
@docs/parity/checklist.md
@scripts/check-phase91-peer-permissions.ts
@scripts/check-phase95-network-participation-release-boundary.ts
@scripts/verify.sh

<interfaces>
Checker pattern from prior phases:

```ts
export function checkPhase91PeerPermissions(
  maybeRepoRoot?: string,
): string[] {
  // fixed corpus reads
  // returns failures instead of throwing
}
```

Verifier pattern:

```bash
run_step "test Phase 99 peer-policy structured log emission checker" bun test scripts/check-phase99-peer-policy-structured-log-emission.test.ts
run_step "check Phase 99 peer-policy structured log emission" bun run scripts/check-phase99-peer-policy-structured-log-emission.ts
run_step "check pure-core dependencies" bash scripts/check-pure-core-deps.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs and parity roots for scoped Phase 100 activation semantics</name>
  <files>docs/architecture/config-precedence.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md, docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md, docs/parity/index.json, docs/parity/checklist.md</files>
  <read_first>
    AGENTS.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-RESEARCH.md
    docs/architecture/config-precedence.md
    docs/architecture/status-snapshot.md
    docs/architecture/operator-observability.md
    docs/operator/runtime-guide.md
    docs/parity/catalog/p2p.md
    docs/parity/index.json
    docs/parity/checklist.md
  </read_first>
  <action>
    Update `docs/architecture/config-precedence.md` to document `relay.enabled` JSONC and `-openbitcoinrelay` CLI precedence. Required strings: `relay.enabled`, `-openbitcoinrelay`, `default-off`, `Open Bitcoin-owned`, and `whitelist and whitebind remain rejected`.

    Update `docs/architecture/status-snapshot.md` to describe Phase 100 as a policy/config boundary, not a new broad public status claim. It must name these low-cardinality labels exactly: `transaction_relay_policy_input`, `force_relay_policy_input`, `mempool_policy_input`, `inactive_bloomfilter`, and `inactive_blockfilters`. It must state that `active_permission_effects` still covers bounded non-relay effects such as `download_serving_policy_input` and `address_response_policy_input`.

    Update `docs/architecture/operator-observability.md` to state that Phase 100 evidence uses only low-cardinality policy labels and must not expose raw permission class names, raw permission strings, peer ids, endpoints, transaction ids, raw transaction hex, credentials, or dynamic labels per D-14.

    Update `docs/operator/runtime-guide.md` with copy-pasteable repo-local UAT command forms for config activation review. Include both:
    - `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind --`
    - `bazel run //packages/open-bitcoin-rpc:open_bitcoind --`
    Each command example must include `-openbitcoinrelay=1`, `-openbitcoininbound=1`, a loopback listen value, and `-openbitcoininboundpermissionclass=relay_loopback@127.0.0.1=in,relay,forcerelay,mempool`. The text must call public-network relay review opt-in and outside `bash scripts/verify.sh`.

    Update `docs/parity/catalog/p2p.md`, `docs/parity/index.json`, and `docs/parity/checklist.md` with a Phase 100 surface id `v2-0-relay-activation-boundary`. Map ACT-01, ACT-02, ACT-03, and ACT-04 to this surface. Cite Knots anchors `packages/bitcoin-knots/src/net_permissions.h`, `packages/bitcoin-knots/src/net_permissions.cpp`, `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, and `packages/bitcoin-knots/test/functional/p2p_permissions.py` per D-17.

    Every updated doc must preserve these out-of-scope claims as explicit negative/deferred boundaries: compact block relay, bloom/filter serving, package relay, public relay by default, public-network relay CI, production service operation, production full-node readiness, and production-funds wallet use. Do not add claims that Phase 100 implements transaction download scheduling, orphan handling, mempool admission, relay serving/fanout, rebroadcast, compact block relay, bloom/filter serving, package relay, public relay defaults, public-network CI, production service operation, production full-node readiness, or production-funds wallet use. This documents the no-claim guardrail required by D-15 and keeps public-network relay review opt-in and outside default verification per D-16.
  </action>
  <verify>
    <automated>rg -n "relay.enabled|-openbitcoinrelay|default-off|Open Bitcoin-owned|whitelist and whitebind remain rejected" docs/architecture/config-precedence.md</automated>
    <automated>rg -n "transaction_relay_policy_input|force_relay_policy_input|mempool_policy_input|inactive_bloomfilter|inactive_blockfilters" docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/parity/catalog/p2p.md</automated>
    <automated>rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind --|bazel run //packages/open-bitcoin-rpc:open_bitcoind --|-openbitcoinrelay=1|-openbitcoininboundpermissionclass=relay_loopback@127.0.0.1=in,relay,forcerelay,mempool" docs/operator/runtime-guide.md</automated>
    <automated>rg -n "v2-0-relay-activation-boundary|ACT-01|ACT-02|ACT-03|ACT-04" docs/parity/catalog/p2p.md docs/parity/index.json docs/parity/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` includes exactly one Phase 100 surface id `v2-0-relay-activation-boundary` carrying `ACT-01`, `ACT-02`, `ACT-03`, and `ACT-04`.
    - The runtime guide includes both Cargo and Bazel daemon command forms and marks public-network relay review as opt-in outside default verification.
    - No updated doc says `public relay by default`, `compact block relay support`, `bloom filter serving support`, `package relay support`, `production service operation supported`, `production full-node readiness`, or `production-funds wallet use supported` as a positive claim.
  </acceptance_criteria>
  <done>Docs and parity roots explain the Phase 100 activation contract, scoped permission effects, exact deferred surfaces, and repo-local UAT command forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic Phase 100 no-claim checker and mutation tests</name>
  <files>scripts/check-phase100-relay-activation-boundary.ts, scripts/check-phase100-relay-activation-boundary.test.ts</files>
  <read_first>
    AGENTS.md
    standards/languages/typescript-javascript.md
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
    scripts/check-phase91-peer-permissions.ts
    scripts/check-phase91-peer-permissions.test.ts
    scripts/check-phase95-network-participation-release-boundary.ts
    scripts/check-phase95-network-participation-release-boundary.test.ts
    scripts/verify.sh
  </read_first>
  <behavior>
    - `passes_when_phase100_fixture_contains_activation_policy_roots_and_verify_wiring`: complete fixture returns `[]`.
    - `fails_when_any_act_requirement_is_missing_from_parity_roots`: removing any ACT-01 through ACT-04 from parity roots returns a failure mentioning the missing requirement id.
    - `fails_when_repo_local_cargo_or_bazel_relay_activation_uat_commands_are_missing`: removing either required daemon command family returns a failure mentioning `UAT command`.
    - `fails_when_scoped_relay_policy_labels_are_missing`: removing `transaction_relay_policy_input`, `force_relay_policy_input`, `mempool_policy_input`, `inactive_bloomfilter`, `inactive_blockfilters`, or `permission_effect_inactive` returns a failure mentioning `Phase 100 evidence label`.
    - `fails_when_docs_claim_default_public_relay_or_deferred_protocol_support`: positive claims for public relay by default, compact block relay support, bloom/filter serving, package relay, production service operation, production full-node readiness, production-funds wallet use, or public-network relay CI return no-claim failures.
    - `fails_when_default_verifier_wiring_is_missing_or_public_network_scoped`: missing Phase 100 verifier order or adding public-network/service-manager/multi-day commands returns default-verifier failures.
  </behavior>
  <action>
    Create `scripts/check-phase100-relay-activation-boundary.ts` as a Bun script with shebang `#!/usr/bin/env bun`. Export `checkPhase100RelayActivationBoundary(maybeRepoRoot?: string): string[]`. Use fixed corpus reads only; do not scan `.planning/` archives except for `100-VERIFICATION.md` in Task 3 if present. Support repo-root override env var `OPEN_BITCOIN_PHASE100_REPO_ROOT`, following the Phase 91 checker pattern.

    Required target files: `docs/architecture/config-precedence.md`, `docs/architecture/status-snapshot.md`, `docs/architecture/operator-observability.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/p2p.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/source-breadcrumbs.json`, and `scripts/verify.sh`.

    The checker must validate:
    - surface id `v2-0-relay-activation-boundary`;
    - requirements `ACT-01`, `ACT-02`, `ACT-03`, `ACT-04`;
    - evidence labels `relay.enabled`, `openbitcoinrelay`, `transaction_relay_policy_input`, `force_relay_policy_input`, `mempool_policy_input`, `inactive_bloomfilter`, `inactive_blockfilters`, `eligible`, `disabled`, `activation_required`, `inbound_serving_required`, `permission_required`, `protected_not_relay`, and `permission_effect_inactive`;
    - required source breadcrumb path `packages/open-bitcoin-network/src/relay.rs`;
    - required Knots anchors named in Task 1;
    - Cargo and Bazel UAT command forms from Task 1;
    - `scripts/verify.sh` visible `VERIFY_COMMAND_ORDER` and executable `run_step` order: Phase 99 checker commands must appear before Phase 100 checker commands, and Phase 100 checker commands must appear before `check pure-core dependencies`.

    The checker must reject positive overclaims for `public relay by default`, `compact block relay support`, `bloom filter serving support`, `BIP37 serving support`, `compact filter serving support`, `package relay support`, `production service operation`, `production full-node readiness`, `production-funds wallet use`, `public-network relay CI`, `transaction download scheduling`, `orphan handling`, `mempool admission`, `relay fanout`, and `rebroadcast` unless the nearby sentence is explicitly negative, deferred, out-of-scope, or no-claim wording per D-15.

    Create `scripts/check-phase100-relay-activation-boundary.test.ts` with fixture files like Phase 91/95. Tests must use Arrange, Act, Assert comments, mutate one concern at a time, and assert exact failure categories. Do not add external dependencies.
  </action>
  <verify>
    <automated>bun test scripts/check-phase100-relay-activation-boundary.test.ts</automated>
    <automated>bun run scripts/check-phase100-relay-activation-boundary.ts</automated>
    <automated>rg -n "checkPhase100RelayActivationBoundary|OPEN_BITCOIN_PHASE100_REPO_ROOT|v2-0-relay-activation-boundary|ACT-01|public relay by default|transaction_relay_policy_input" scripts/check-phase100-relay-activation-boundary.ts scripts/check-phase100-relay-activation-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test scripts/check-phase100-relay-activation-boundary.test.ts` passes with mutation coverage for requirements, labels, UAT commands, overclaims, and verifier order.
    - `bun run scripts/check-phase100-relay-activation-boundary.ts` passes against the real repository corpus.
    - The checker returns `string[]` failures and does not call `process.exit` from inside `checkPhase100RelayActivationBoundary`.
  </acceptance_criteria>
  <done>Phase 100 has deterministic fixed-file no-claim and evidence guardrails with tests that fail for missing coverage or forbidden claims.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 100 verifier and create passed verification report</name>
  <files>scripts/verify.sh, .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md, docs/metrics/lines-of-code.md</files>
  <read_first>
    AGENTS.md
    standards/core/verification.md
    scripts/verify.sh
    scripts/check-phase100-relay-activation-boundary.ts
    scripts/check-phase100-relay-activation-boundary.test.ts
    .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-CONTEXT.md
  </read_first>
  <action>
    Update both the visible `VERIFY_COMMAND_ORDER` heredoc and executable `run_step` list in `scripts/verify.sh` immediately after the Phase 99 checker commands and before `check pure-core dependencies` per D-16:
    - `bun test scripts/check-phase100-relay-activation-boundary.test.ts`
    - `bun run scripts/check-phase100-relay-activation-boundary.ts`

    Run the focused checker test and real-corpus checker, then run `bash scripts/verify.sh`. If `docs/metrics/lines-of-code.md` changes because the verifier refreshes it, keep the change because repo guidance treats it as tracked generated output.

    Only after `bash scripts/verify.sh` passes, create `.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md` with YAML frontmatter containing `status: passed`, `phase: 100-relay-activation-boundary-and-permission-semantics`, `requirements: [ACT-01, ACT-02, ACT-03, ACT-04]`, and `verified_at` in ISO format. The body must map each ACT requirement to implementation evidence, list the exact commands run, and state the residual boundary: Phase 100 defines activation and eligibility only; transaction download scheduling, orphan handling, mempool admission, relay serving/fanout, rebroadcast, compact blocks, bloom/filter serving, package relay, public relay defaults, public-network CI, production service operation, production full-node readiness, and production-funds wallet use remain out of scope.
  </action>
  <verify>
    <automated>bun test scripts/check-phase100-relay-activation-boundary.test.ts</automated>
    <automated>bun run scripts/check-phase100-relay-activation-boundary.ts</automated>
    <automated>rg -n "test Phase 99 peer-policy structured log emission checker|check Phase 99 peer-policy structured log emission|test Phase 100 relay activation boundary checker|check Phase 100 relay activation boundary|check pure-core dependencies" scripts/verify.sh</automated>
    <automated>bash scripts/verify.sh</automated>
    <automated>rg -n "status: passed|ACT-01|ACT-02|ACT-03|ACT-04|bash scripts/verify.sh|transaction download scheduling|production-funds wallet use" .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` runs Phase 100 immediately after Phase 99 in both visible and executable order.
    - `bash scripts/verify.sh` passes before `100-VERIFICATION.md` records `status: passed`.
    - `100-VERIFICATION.md` contains `status: passed`, all four ACT requirement ids, exact verification commands, and the residual boundary sentence.
    - Default verification remains deterministic and contains no public-network relay, service-manager, wall-clock soak, production-deployment, or public relay CI gate.
  </acceptance_criteria>
  <done>Default verification includes Phase 100 guardrails and the phase has a passed verification report backed by `bash scripts/verify.sh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Docs/parity roots -> operator expectations | Human-facing docs can accidentally overstate relay or production support. |
| Checker corpus -> default verifier | Static checker files decide whether Phase 100 evidence and no-claim boundaries stay intact. |
| Verification report -> phase closeout | `100-VERIFICATION.md` becomes evidence consumed by later GSD commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-100-03-01 | Repudiation | Docs and parity roots | mitigate | Add fixed evidence roots for ACT-01 through ACT-04 and require checker validation of surface id, requirement ids, Knots anchors, and UAT commands. |
| T-100-03-02 | Information Disclosure | Operator docs and support/observability docs | mitigate | Require docs to name low-cardinality labels only and explicitly prohibit raw permission names, peer ids, endpoints, transaction ids, raw transaction hex, credentials, and dynamic labels. |
| T-100-03-03 | Elevation of Privilege | No-claim scanner | mitigate | Checker rejects positive claims for public relay by default, compact blocks, bloom/filter serving, package relay, production service operation, production full-node readiness, production-funds wallet use, and public-network relay CI. |
| T-100-03-04 | Denial of Service | `scripts/verify.sh` | mitigate | Verifier additions are local Bun static checks only; no public-network, service-manager, multi-day, production-deployment, or relay CI gates may be added. |
</threat_model>

<verification>
Plan-level verification:

```bash
bun test scripts/check-phase100-relay-activation-boundary.test.ts
bun run scripts/check-phase100-relay-activation-boundary.ts
bash scripts/verify.sh
rg -n "status: passed" .planning/phases/100-relay-activation-boundary-and-permission-semantics/100-VERIFICATION.md
```
</verification>

<success_criteria>
- ACT-01: Docs and checker prove explicit default-off activation through `relay.enabled` and `-openbitcoinrelay`.
- ACT-02: Docs and checker prove the peer eligibility matrix covers default, outbound, inbound, manual, protected, and permissioned peers without public-default or service-bit claims.
- ACT-03: Docs and checker prove `relay`, `forcerelay`, and `mempool` are scoped v2.0 policy inputs only in Phase 100.
- ACT-04: Docs and checker prove bloom/filter permissions, compact-block behavior, and unrelated permissions remain inactive or unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-03-SUMMARY.md`.
</output>
