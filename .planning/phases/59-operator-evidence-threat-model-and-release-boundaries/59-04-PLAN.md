---
phase: 59-operator-evidence-threat-model-and-release-boundaries
plan: 04
type: execute
wave: 3
depends_on:
  - "59-03"
files_modified:
  - scripts/check-v1.4-release-boundaries.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - SEC-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 59-2026-06-05T15-10-59
generated_at: 2026-06-05T15:10:59.825Z
must_haves:
  truths:
    - "Default repo verification remains deterministic and public-network-free."
    - "A repo-owned checker validates v1.4 parity roots, threat model, release-readiness docs, operator commands, deferred-surface wording, and the absence of live-smoke invocation from `scripts/verify.sh`."
    - "The final closeout keeps tracked generated LOC evidence fresh when source or script files changed."
  artifacts:
    - path: "scripts/check-v1.4-release-boundaries.ts"
      provides: "Deterministic v1.4 release-boundary checker"
      contains: "v1-4-operator-evidence-release-boundaries"
    - path: "scripts/verify.sh"
      provides: "Default deterministic verification wiring"
      contains: "bun run scripts/check-v1.4-release-boundaries.ts"
    - path: "docs/metrics/lines-of-code.md"
      provides: "Fresh tracked generated LOC report"
      contains: "Generated"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-v1.4-release-boundaries.ts"
      via: "Bun command"
      pattern: "bun run scripts/check-v1\\.4-release-boundaries\\.ts"
    - from: "scripts/check-v1.4-release-boundaries.ts"
      to: "docs/parity/index.json"
      via: "parsed checklist/audit roots"
      pattern: "v1_4_threat_model|v1_4_release_boundaries"
    - from: "scripts/check-v1.4-release-boundaries.ts"
      to: "scripts/verify.sh"
      via: "public-network exclusion assertions"
      pattern: "requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\""
---

<objective>
Add deterministic v1.4 release-boundary verification and wire it into the repo-native verify contract.

Purpose: SEC-03 requires default verification to stay deterministic while docs and parity roots prove public-network checks are opt-in UAT evidence, not part of `bash scripts/verify.sh`.
Output: A Bun checker modeled on the v1.3 release-boundary pattern, `scripts/verify.sh` wiring, and final LOC freshness.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-CONTEXT.md
@.planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-RESEARCH.md
@.planning/phases/49-threat-model-and-release-boundaries/49-02-SUMMARY.md
@docs/operator/runtime-guide.md
@docs/parity/threat-model-v1.4.md
@docs/parity/release-readiness.md
@docs/parity/checklist.md
@docs/parity/index.json
@docs/parity/README.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/catalog/p2p.md
@scripts/check-v1.3-release-boundaries.ts
@scripts/verify.sh

<interfaces>
Use the v1.3 checker pattern, with v1.4 constants:

```typescript
const SURFACE_ID = "v1-4-operator-evidence-release-boundaries";
const REQUIRED_REQUIREMENTS = ["OBS-01", "OBS-02", "OBS-03", "SEC-01", "SEC-02", "SEC-03"] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic v1.4 release-boundary checker</name>
  <read_first>
    - .planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-CONTEXT.md
    - .planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-RESEARCH.md
    - scripts/check-v1.3-release-boundaries.ts
    - docs/operator/runtime-guide.md
    - docs/parity/threat-model-v1.4.md
    - docs/parity/release-readiness.md
    - docs/parity/checklist.md
    - docs/parity/index.json
    - docs/parity/README.md
    - docs/parity/deviations-and-unknowns.md
    - docs/parity/catalog/p2p.md
    - scripts/verify.sh
  </read_first>
  <files>scripts/check-v1.4-release-boundaries.ts</files>
  <action>
    Implement D-14 and D-15 by creating `scripts/check-v1.4-release-boundaries.ts` as a Bun TypeScript checker based on `scripts/check-v1.3-release-boundaries.ts`; do not add dependencies. Use `#!/usr/bin/env bun`, `node:child_process`, `node:fs`, and `node:path` only. Set `SURFACE_ID = "v1-4-operator-evidence-release-boundaries"`, `REQUIRED_REQUIREMENTS = ["OBS-01", "OBS-02", "OBS-03", "SEC-01", "SEC-02", "SEC-03"]`, and `REQUIRED_EVIDENCE = ["docs/parity/threat-model-v1.4.md", "docs/parity/release-readiness.md", "docs/operator/runtime-guide.md", "scripts/run-live-mainnet-smoke.ts", "scripts/test-run-live-mainnet-smoke.sh", "scripts/verify.sh"]`.

    The checker must parse `docs/parity/index.json`, require exactly one done checklist surface with the v1.4 surface id, require all six requirement IDs and required evidence paths on that surface, and require audit text containing `v1_4_threat_model`, `v1_4_release_boundaries`, `threat-model-v1.4.md`, `release-readiness.md`, and all six requirement IDs. It must verify `docs/parity/README.md` mentions `threat-model-v1.4.md` and still mentions `threat-model-v1.3.md`.

    It must verify `docs/operator/runtime-guide.md` contains the exact command/field strings from Plan 59-03: `bash scripts/verify.sh`, `bash scripts/test-run-live-mainnet-smoke.sh`, `bun run scripts/run-live-mainnet-smoke.ts`, `--manual-peer=HOST:8333`, `--restart-after-progress`, `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --`, `bazel run //packages/open-bitcoin-cli:open_bitcoin --`, `result.firstHeaderProgress`, `result.firstBlockProgress`, `result.restartResumeEvidence`, `support-evidence.json`, and `support-evidence.md`.

    It must verify `docs/parity/threat-model-v1.4.md` contains `STRIDE Threat Register`, `ASVS L1 Mapping`, `OWASP ASVS v5.0.0`, and `V14-TM-01` through `V14-TM-08`. It must verify `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, and `docs/parity/catalog/p2p.md` contain all deferred-surface strings: `inbound serving`, `transaction relay`, `production-funds wallet`, `migration apply mode`, `packaging`, `hosted dashboard`, `GUI`, `Windows service`, and `unattended production-node`.

    It must verify `scripts/verify.sh` contains `bun run scripts/check-v1.3-release-boundaries.ts` and, after Task 2, `bun run scripts/check-v1.4-release-boundaries.ts`; it must fail if `scripts/verify.sh` contains `run-live-mainnet-smoke` or `--restart-after-progress`. Print exactly `validated v1.4 release boundary parity roots` on success.
  </action>
  <acceptance_criteria>
    - `rg -n "SURFACE_ID|v1-4-operator-evidence-release-boundaries|REQUIRED_REQUIREMENTS|OBS-01|OBS-02|OBS-03|SEC-01|SEC-02|SEC-03|V14-TM-08|requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\"|validated v1\\.4 release boundary parity roots" scripts/check-v1.4-release-boundaries.ts` finds the checker contract.
    - The checker uses no package install or third-party dependency.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "SURFACE_ID|v1-4-operator-evidence-release-boundaries|REQUIRED_REQUIREMENTS|OBS-01|OBS-02|OBS-03|SEC-01|SEC-02|SEC-03|V14-TM-08" scripts/check-v1.4-release-boundaries.ts</automated>
    <automated>rg -n "requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\"|requireNotContains\\(verifyScript, \"--restart-after-progress\"" scripts/check-v1.4-release-boundaries.ts</automated>
  </verify>
  <done>The v1.4 checker deterministically validates docs, parity roots, deferred-surface wording, and default-verification public-network exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire checker into default verification and refresh closeout artifacts</name>
  <read_first>
    - .planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-CONTEXT.md
    - AGENTS.md
    - scripts/verify.sh
    - scripts/check-v1.4-release-boundaries.ts
    - docs/metrics/lines-of-code.md
  </read_first>
  <files>scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <action>
    Implement D-14, D-15, and D-16 by adding `bun run scripts/check-v1.4-release-boundaries.ts` to `scripts/verify.sh` immediately after the existing `bun run scripts/check-v1.3-release-boundaries.ts` line. Do not add `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, network preflight, DNS, TCP, or public-mainnet commands to `scripts/verify.sh`.

    Run `bun run scripts/check-v1.4-release-boundaries.ts`, `bash scripts/test-run-live-mainnet-smoke.sh`, and then `bash scripts/verify.sh`. If `bash scripts/verify.sh` fails only because `docs/metrics/lines-of-code.md` is stale after the new Rust/script/docs files, run `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md` once, then rerun `bash scripts/verify.sh`. Keep generated live-smoke reports, support bundles, daemon logs, metrics stores, and local datadirs out of git.
  </action>
  <acceptance_criteria>
    - `rg -n "check-v1\\.3-release-boundaries\\.ts|check-v1\\.4-release-boundaries\\.ts" scripts/verify.sh` finds both deterministic checker lines.
    - `rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh` returns no matches.
    - `bun run scripts/check-v1.4-release-boundaries.ts` prints `validated v1.4 release boundary parity roots`.
    - `bash scripts/test-run-live-mainnet-smoke.sh` passes as deterministic fixture coverage only.
    - `bash scripts/verify.sh` passes after any required LOC freshness regeneration.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-v1.4-release-boundaries.ts</automated>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
    <automated>rg -n "check-v1\\.3-release-boundaries\\.ts|check-v1\\.4-release-boundaries\\.ts" scripts/verify.sh</automated>
    <automated>! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>SEC-03 is enforced by default deterministic verification, public-network commands remain opt-in only, and final repo verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/parity roots -> automated verification | Machine and human docs become deterministic release-boundary assertions. |
| `scripts/verify.sh` -> public network | Default verification must stay deterministic and must not execute public-mainnet live smoke. |
| generated local artifacts -> git | LOC is tracked, but live-smoke reports, support bundles, logs, metrics stores, and datadirs are local-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04-01 | Repudiation | `scripts/check-v1.4-release-boundaries.ts` | mitigate | Deterministically assert requirement traces, evidence paths, threat-model IDs, command strings, and deferred-surface wording. |
| T-59-04-02 | Denial of Service | `scripts/verify.sh` | mitigate | Checker and acceptance fail if live-smoke or restart public-network commands enter default verification. |
| T-59-04-03 | Tampering | parity index JSON | mitigate | Parse `docs/parity/index.json` and require exactly one done v1.4 surface with all six requirement IDs and evidence links. |
| T-59-04-04 | Information Disclosure | generated artifacts | mitigate | Only refresh tracked LOC; do not check in live-smoke reports, support bundles, daemon logs, metrics stores, or datadirs. |
| T-59-04-05 | Spoofing | release-boundary docs | mitigate | Verify docs name the opt-in outbound IBD boundary and explicit deferred non-claims. |
</threat_model>

<verification>
Plan-level verification:

```bash
bun run scripts/check-v1.4-release-boundaries.ts
bash scripts/test-run-live-mainnet-smoke.sh
rg -n "check-v1\\.3-release-boundaries\\.ts|check-v1\\.4-release-boundaries\\.ts" scripts/verify.sh
! rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh
bash scripts/verify.sh
```

If `bash scripts/verify.sh` reports stale LOC only, refresh it with:

```bash
bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md
bash scripts/verify.sh
```
</verification>

<success_criteria>
SEC-03 is complete when the v1.4 release-boundary checker passes, `scripts/verify.sh` runs the checker without live-smoke or restart public-network commands, targeted live-smoke fixtures pass, tracked LOC evidence is fresh, and the repo-native aggregate `bash scripts/verify.sh` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/59-operator-evidence-threat-model-and-release-boundaries/59-04-SUMMARY.md`.
</output>
