---
phase: 88-deterministic-claim-guardrails
plan: 01
type: execute
subsystem: verification
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase88-deterministic-claim-guardrails.test.ts
  - scripts/check-phase88-deterministic-claim-guardrails.ts
autonomous: true
requirements:
  - REL-02
  - REL-03
  - REL-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 88-2026-06-23T20-39-38
generated_at: 2026-06-23T20:57:14.135Z
must_haves:
  truths:
    - "Default deterministic checks fail when current release/operator docs make an unscoped production full-node readiness claim."
    - "Default deterministic checks fail when current release/operator docs promote a Phase 82 deferred production-adjacent surface with positive readiness language."
    - "Valid no-claim, deferred, unsupported, historical, opt-in UAT, future-gate, and outside-default-verification wording remains allowed."
    - "The checker validates executable verifier text, not only the visible VERIFY_COMMAND_ORDER heredoc."
  artifacts:
    - path: "scripts/check-phase88-deterministic-claim-guardrails.test.ts"
      provides: "Fixture tests for production-readiness claims, deferred-surface promotions, scoped allow rules, heredoc-only verifier wiring, and default-verifier drift"
      contains: "fails_when_deferred_surface_is_promoted_with_positive_predicate"
    - path: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      provides: "Deterministic Phase 88 Bun checker"
      exports:
        - "checkPhase88DeterministicClaimGuardrails"
      contains: "OPEN_BITCOIN_PHASE88_REPO_ROOT"
  key_links:
    - from: "scripts/check-phase88-deterministic-claim-guardrails.test.ts"
      to: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      via: "direct import"
      pattern: "checkPhase88DeterministicClaimGuardrails"
    - from: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      to: "docs/parity/production-claim-boundary.md"
      via: "curated fixed-file corpus"
      pattern: "production-claim-boundary\\.md"
    - from: "scripts/check-phase88-deterministic-claim-guardrails.ts"
      to: "scripts/verify.sh"
      via: "executable verifier text inspection"
      pattern: "executableVerifyText"
---

<objective>
Create the Phase 88 deterministic claim-guardrail checker and fixture tests.

Purpose: Close REL-02 and REL-03 with a fixed-file, public-network-free scanner
that rejects overbroad production-readiness and deferred-surface promotion
language while preserving valid v1.8 no-claim wording. Start REL-04 by making
the checker able to prove verifier wiring from executed `run_step` text rather
than the heredoc alone.
Output: `scripts/check-phase88-deterministic-claim-guardrails.ts` and
`scripts/check-phase88-deterministic-claim-guardrails.test.ts`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/88-deterministic-claim-guardrails/88-CONTEXT.md
@.planning/phases/88-deterministic-claim-guardrails/88-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-04-SUMMARY.md
@.planning/phases/83-support-matrix-and-issue-evidence/83-04-SUMMARY.md
@.planning/phases/87-release-readiness-checklist/87-01-SUMMARY.md
@scripts/check-phase82-production-claim-boundary.ts
@scripts/check-phase82-production-claim-boundary.test.ts
@scripts/check-phase83-support-matrix-issue-evidence.ts
@scripts/check-phase87-release-readiness.ts
@scripts/check-phase87-release-readiness.test.ts
@scripts/verify.sh
@docs/parity/production-claim-boundary.md
@docs/parity/support-matrix.md
@docs/parity/release-readiness.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@README.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/operator-runtime-release-hardening.md

<interfaces>
From the Phase 88 checker to create:
```typescript
export function checkPhase88DeterministicClaimGuardrails(
  maybeRepoRoot = process.env.OPEN_BITCOIN_PHASE88_REPO_ROOT,
): string[];
```

Required constants:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE88_REPO_ROOT";
const SURFACE_ID = "v1-8-deterministic-claim-guardrails";
const AUDIT_KEY = "v1_8_deterministic_claim_guardrails";
const PHASE88_REQUIREMENTS = ["REL-02", "REL-03", "REL-04"] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 88 fixture tests</name>
  <files>scripts/check-phase88-deterministic-claim-guardrails.test.ts</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards/core/testing.md
    - standards/languages/typescript-javascript.md
    - .planning/phases/88-deterministic-claim-guardrails/88-CONTEXT.md
    - .planning/phases/88-deterministic-claim-guardrails/88-RESEARCH.md
    - scripts/check-phase87-release-readiness.test.ts
    - scripts/check-phase82-production-claim-boundary.test.ts
    - scripts/check-phase88-deterministic-claim-guardrails.test.ts (if already present)
  </read_first>
  <behavior>
    - Test 1: A complete fixture with the Phase 88 surface id, audit key, scoped no-claim docs, Phase 87 then Phase 88 verifier order, and no forbidden default-verifier text returns `[]`.
    - Test 2: Adding `Open Bitcoin is production full-node ready.` to `README.md` returns a failure containing `production readiness claim`.
    - Test 3: Adding `Inbound serving is production-grade and fully supported.` to `docs/operator/runtime-guide.md` returns a failure containing `deferred surface promotion`.
    - Test 4: Adding scoped text with `does not claim`, `deferred`, `unsupported`, `historical`, `opt-in UAT`, `future gate`, and `outside default verification` does not create failures.
    - Test 5: A fixture where Phase 88 commands exist only inside `VERIFY_COMMAND_ORDER` returns a failure containing `verifier-order`.
    - Test 6: Fixtures appending `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, `--restart-after-progress`, `public-network CI`, `release-blocking live sync`, `automatic support-bundle upload`, `destructive repair`, or `broad production-node readiness` to executable `scripts/verify.sh` return failures containing `default verifier boundary`.
  </behavior>
  <action>Create `scripts/check-phase88-deterministic-claim-guardrails.test.ts` with `#!/usr/bin/env bun`, `bun:test` imports, temp-dir fixture helpers, and explicit Arrange/Act/Assert comments. Import `checkPhase88DeterministicClaimGuardrails` from `./check-phase88-deterministic-claim-guardrails`. Use `REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE88_REPO_ROOT"`, `SURFACE_ID = "v1-8-deterministic-claim-guardrails"`, `AUDIT_KEY = "v1_8_deterministic_claim_guardrails"`, `PHASE87_CHECKER_COMMAND = "bun run scripts/check-phase87-release-readiness.ts"`, `PHASE88_TEST_COMMAND = "bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts"`, and `PHASE88_CHECKER_COMMAND = "bun run scripts/check-phase88-deterministic-claim-guardrails.ts"`. The fixture corpus must include exactly these current release/operator files: `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, and `docs/parity/catalog/operator-runtime-release-hardening.md`. Do not include `.planning/`, milestone archives, or recursive directory walks in fixture helper constants, per D-02 and D-03.</action>
  <verify>
    <automated>rg -n "passes_when_phase88_fixture_contains_claim_guardrails_roots_and_verify_order|fails_when_release_docs_claim_production_full_node_readiness|fails_when_deferred_surface_is_promoted_with_positive_predicate|allows_scoped_no_claim_deferred_or_opt_in_uat_wording|fails_when_phase88_verify_commands_exist_only_in_legacy_heredoc|fails_when_default_verify_contains_public_network_service_or_multiday_drift" scripts/check-phase88-deterministic-claim-guardrails.test.ts</automated>
    <automated>rg -n "OPEN_BITCOIN_PHASE88_REPO_ROOT|v1-8-deterministic-claim-guardrails|v1_8_deterministic_claim_guardrails|VERIFY_COMMAND_ORDER|Arrange|Act|Assert" scripts/check-phase88-deterministic-claim-guardrails.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase88-deterministic-claim-guardrails.test.ts` contains all six exact test names listed in the first verification command.
    - The test file contains `OPEN_BITCOIN_PHASE88_REPO_ROOT`, `v1-8-deterministic-claim-guardrails`, `v1_8_deterministic_claim_guardrails`, and `VERIFY_COMMAND_ORDER`.
    - Every test body contains explicit `// Arrange`, `// Act`, and `// Assert` comments unless the test is a single-line table helper with no assertions.
    - The fixture target-file list does not contain `.planning`, `milestones/`, `readdir`, or `glob`.
  </acceptance_criteria>
  <done>The fixture suite fully describes the Phase 88 checker contract before implementation, including positive failures and valid scoped language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the deterministic claim-guardrail checker</name>
  <files>scripts/check-phase88-deterministic-claim-guardrails.ts</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards/core/architecture.md
    - standards/core/code-shape.md
    - standards/core/testing.md
    - standards/languages/typescript-javascript.md
    - .planning/phases/88-deterministic-claim-guardrails/88-CONTEXT.md
    - .planning/phases/88-deterministic-claim-guardrails/88-RESEARCH.md
    - scripts/check-phase88-deterministic-claim-guardrails.test.ts
    - scripts/check-phase88-deterministic-claim-guardrails.ts (if already present)
    - scripts/check-phase82-production-claim-boundary.ts
    - scripts/check-phase83-support-matrix-issue-evidence.ts
    - scripts/check-phase87-release-readiness.ts
    - scripts/verify.sh
    - docs/parity/production-claim-boundary.md
    - docs/parity/support-matrix.md
    - docs/parity/release-readiness.md
  </read_first>
  <behavior>
    - Production readiness claims are rejected unless the same context unit is explicitly scoped as no-claim, deferred, unsupported, historical, opt-in UAT, future-gate, or outside-default-verification text.
    - Deferred surfaces from the Phase 82 inventory are rejected when attached to positive predicates such as `production-ready`, `production-grade`, `fully supported`, `default-verified`, `release-blocking`, `proven`, `GA`, or `certified`.
    - `scripts/verify.sh` is checked after stripping `VERIFY_COMMAND_ORDER`; heredoc-only Phase 88 commands are failures.
    - Executable verifier text containing public-network, real service-manager, package-service, multi-day, destructive repair, support-upload, or broad production-node readiness drift is a failure.
  </behavior>
  <action>Create `scripts/check-phase88-deterministic-claim-guardrails.ts` as a Bun/TypeScript checker. Export `checkPhase88DeterministicClaimGuardrails(maybeRepoRoot = process.env[REPO_ROOT_OVERRIDE_ENV]): string[]`; use `path.resolve(import.meta.dir, "..")` as the default repo root; use `existsSync` and `readFileSync` for fixed-file reads; parse `docs/parity/index.json` with `JSON.parse`; and print every failure to stderr plus `process.exitCode = 1` under `if (import.meta.main)`. Use a fixed `TARGET_FILES` array with exactly `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/catalog/operator-runtime-release-hardening.md`, and `scripts/verify.sh`; do not scan `.planning/`, milestone archives, or directories recursively. Implement context units by preserving Markdown table rows that start with `|`, splitting paragraphs on blank lines, and splitting sentence text on `.`, `!`, and `?`. Define scoped allow strings including exactly `does not claim`, `not allowed yet`, `deferred`, `unsupported`, `historical`, `opt-in UAT`, `future gate`, `outside default verification`, `defines gates`, and `future milestone`. Define deferred surfaces including exactly `inbound serving`, `address relay`, `block serving`, `transaction relay`, `compact block relay`, `production-funds wallet use`, `production-funds wallet safety`, `migration apply mode`, `signed packaging`, `package-manager distribution`, `Windows service integration`, `hosted dashboards`, `GUI parity`, `public-network default checks`, `public-network CI`, `release-blocking live sync`, `automatic support-bundle upload`, `destructive repair`, and `broad production-node readiness`. Define promotion predicates including exactly `production-ready`, `production ready`, `production-grade`, `production grade`, `fully supported`, `default-verified`, `default verified`, `release-blocking`, `release blocking`, `proven`, `GA`, and `certified`. Define exact overclaim smoke strings including `Open Bitcoin is production full-node ready.`, `v1.8 proves production full-node readiness.`, `Open Bitcoin has production full-node readiness.`, and `Open Bitcoin supports production service operation.`. Implement `executableVerifyText` with the heredoc-stripping regex used by Phase 87, then require executable order `bun run scripts/check-phase87-release-readiness.ts` before `bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts` before `bun run scripts/check-phase88-deterministic-claim-guardrails.ts`. Fail executable verifier text containing `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, `sleep 86400`, `--restart-after-progress`, `brew services`, `public-network CI`, `public-network default checks`, `release-blocking live sync`, `automatic support-bundle upload`, `destructive repair`, or `broad production-node readiness`.</action>
  <verify>
    <automated>bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts</automated>
    <automated>bun --check scripts/check-phase88-deterministic-claim-guardrails.ts</automated>
    <automated>rg -n "OPEN_BITCOIN_PHASE88_REPO_ROOT|TARGET_FILES|executableVerifyText|production-grade|fully supported|default-verified|release-blocking|automatic support-bundle upload|broad production-node readiness" scripts/check-phase88-deterministic-claim-guardrails.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts` exits 0.
    - `bun --check scripts/check-phase88-deterministic-claim-guardrails.ts` exits 0.
    - The checker exports `checkPhase88DeterministicClaimGuardrails` and contains `OPEN_BITCOIN_PHASE88_REPO_ROOT`.
    - The checker target-file constants contain the ten curated release/operator files and `scripts/verify.sh`, and do not contain `.planning`, `milestones/`, `readdir`, or `glob`.
    - The checker contains `executableVerifyText` and fails heredoc-only Phase 88 verifier wiring.
  </acceptance_criteria>
  <done>The Phase 88 checker passes its fixture suite and implements D-01 through D-13, D-15, and the checker-side portion of D-16 without broadening default verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Release/operator docs to claim scanner | Tracked docs are untrusted claim text that can overstate production readiness. |
| Deferred-surface vocabulary to release interpretation | A deferred surface can be promoted by adjacent positive predicates even when exact bad phrases are absent. |
| `scripts/verify.sh` text to default verifier contract | The visible heredoc can diverge from executed `run_step` commands, and default verification can drift into opt-in UAT behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-88-01 | Repudiation | `checkPhase88DeterministicClaimGuardrails` production-claim scan | mitigate | Use fixed release/operator corpus, exact overclaim smoke strings, context-unit scanning, and scoped allow strings from D-04 and D-05. |
| T-88-02 | Tampering | Deferred-surface promotion rules | mitigate | Require every Phase 82 deferred surface plus promotion predicates from D-09 and D-10 in constants and fixture tests. |
| T-88-03 | Denial of Service | Scoped no-claim language | mitigate | Allow explicitly negative/scoped contexts in sentence, paragraph, and table-row windows so required boundary docs do not become false positives. |
| T-88-04 | Tampering | `scripts/verify.sh` checker wiring | mitigate | Strip `VERIFY_COMMAND_ORDER` before checking executable order and add a heredoc-only fixture regression. |
| T-88-05 | Denial of Service | Default verifier drift | mitigate | Fail executable verifier text containing public-network, real service-manager, package-service, long sleep, support-upload, destructive-repair, or broad production-node readiness fragments. |
</threat_model>

<verification>
Focused checks for this plan:

```bash
bun test scripts/check-phase88-deterministic-claim-guardrails.test.ts
bun --check scripts/check-phase88-deterministic-claim-guardrails.ts
```

Do not run the real Phase 88 checker against the full repository in this plan
until Plan 02 has registered parity roots and wired `scripts/verify.sh`.
</verification>

<success_criteria>
- The new Phase 88 test file contains the exact fixture regressions for REL-02, REL-03, and REL-04 failure modes.
- The new Phase 88 checker exports the required function, uses `OPEN_BITCOIN_PHASE88_REPO_ROOT`, scans only the curated corpus, and rejects overclaims, deferred-surface promotions, heredoc-only wiring, and default-verifier drift.
- No Rust source or Rust test files are created or modified.
</success_criteria>

<output>
After completion, create `.planning/phases/88-deterministic-claim-guardrails/88-01-SUMMARY.md`.
</output>
