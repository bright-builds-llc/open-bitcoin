---
phase: 73-opt-in-uat-and-deterministic-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 73-01
  - 73-02
files_modified:
  - scripts/check-phase73-uat-verification.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 73-2026-06-13T22-08-43
generated_at: 2026-06-13T23:39:11Z
user_setup: []
must_haves:
  truths:
    - "`bash scripts/verify.sh` runs the Phase 73 checker after the Phase 72 checker."
    - "Default verification remains public-network-free, service-manager-free, long-sync-free, and current-tip-timing-free."
    - "The Phase 73 checker validates the UAT matrix and VER-02 coverage map together."
  artifacts:
    - path: "scripts/check-phase73-uat-verification.ts"
      provides: "Default verification, UAT matrix, and coverage-map checker"
      contains: "verifyVerifyScript"
    - path: "scripts/verify.sh"
      provides: "Repo-native checker wiring"
      contains: "bun run scripts/check-phase73-uat-verification.ts"
    - path: "docs/metrics/lines-of-code.md"
      provides: "Tracked generated LOC freshness"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase73-uat-verification.ts"
      via: "Bun checker invocation after Phase 72"
      pattern: "check-phase72-observability-evidence.ts[\\s\\S]*check-phase73-uat-verification.ts"
    - from: "scripts/check-phase73-uat-verification.ts"
      to: "docs/operator/runtime-guide.md"
      via: "UAT matrix docs checks"
      pattern: "Phase 73 opt-in public-mainnet UAT matrix"
---

<objective>
Wire the Phase 73 deterministic checker into default verification.

Purpose: Contributors need one local deterministic gate that proves Phase 73 coverage and UAT documentation without running public-network or service-manager workflows.
Output: The checker validates coverage, UAT docs, requirements, and default-verification exclusions, and `scripts/verify.sh` runs it after Phase 72.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-01-SUMMARY.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-02-SUMMARY.md
@scripts/check-phase73-uat-verification.ts
@scripts/check-phase72-observability-evidence.ts
@scripts/verify.sh
@docs/operator/runtime-guide.md
@docs/metrics/lines-of-code.md

<interfaces>
Existing checker-order pattern:
```typescript
const phase72 = "bun run scripts/check-phase72-observability-evidence.ts";
const phase73 = "bun run scripts/check-phase73-uat-verification.ts";
if (phase72Index === -1 || phase73Index === -1 || phase73Index < phase72Index) {
  failures.push("scripts/verify.sh must run the Phase 73 checker after the Phase 72 checker");
}
```

Forbidden default-verification strings required by D-03:
```typescript
[
  "run-live-mainnet-smoke",
  "--manual-peer",
  "--restart-after-progress",
  "systemctl",
  "launchctl",
  "-openbitcoinsync=mainnet-ibd",
  "openbitcoinsync=mainnet-ibd",
  "current-tip timing",
  "wall-clock release gate",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the checker for requirements, coverage, and UAT docs</name>
  <files>scripts/check-phase73-uat-verification.ts</files>
  <read_first>
    - `scripts/check-phase73-uat-verification.ts`
    - `scripts/check-phase72-observability-evidence.ts`
    - `scripts/verify.sh`
    - `docs/operator/runtime-guide.md`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md`
  </read_first>
  <behavior>
    - Test 1: The checker fails if `docs/operator/runtime-guide.md` lacks the Phase 73 UAT matrix title or any required workflow label.
    - Test 2: The checker fails if the Phase 73 plan set does not mention `VER-01`, `VER-02`, `VER-03`, and `VER-04`.
    - Test 3: The checker fails if the embedded VER-02 coverage map added by Plan 01 is incomplete or loses a required anchor.
  </behavior>
  <action>Extend `scripts/check-phase73-uat-verification.ts` with `verifyRequirements`, `verifyCoverageMap`, and `verifyUatMatrixDocs` only. Do not add, call, or require `verifyVerifyScript` in this task because `scripts/verify.sh` is not wired until Task 2. `verifyRequirements` must read all four Phase 73 plan files and require `VER-01`, `VER-02`, `VER-03`, and `VER-04` somewhere in the plan set. `verifyCoverageMap` must preserve the Plan 01 VER-02 coverage-map enforcement and fail if any required behavior or anchor is missing. `verifyUatMatrixDocs` must require these exact strings in `docs/operator/runtime-guide.md`: `### Phase 73 opt-in public-mainnet UAT matrix`, `Full-sync activation and review`, `Stay-current/status review`, `Same-datadir restart/resume review`, `Status-surface comparison`, `Live-smoke report collection`, `Support-bundle collection`, `Evidence proves`, `Does not prove`, `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --`, `bazel run //packages/open-bitcoin-cli:open_bitcoin --`, `bun run scripts/run-live-mainnet-smoke.ts`, and `deterministic fixture validation, not public-network UAT`. Per D-04, do not require Cargo/Bazel offline flags.</action>
  <verify>
    <automated>bun --check scripts/check-phase73-uat-verification.ts</automated>
    <automated>bun run scripts/check-phase73-uat-verification.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "verifyRequirements|verifyCoverageMap|verifyUatMatrixDocs" scripts/check-phase73-uat-verification.ts` returns matches.
    - `rg -n "verifyVerifyScript|FORBIDDEN_VERIFY_STRINGS" scripts/check-phase73-uat-verification.ts` returns no matches after this task.
    - `bun run scripts/check-phase73-uat-verification.ts` exits 0 after Plan 02 is complete.
  </acceptance_criteria>
  <done>The checker enforces Phase 73 docs, coverage, and requirements without depending on `scripts/verify.sh` wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 73 into verify.sh, enable order enforcement, and refresh generated LOC</name>
  <files>scripts/check-phase73-uat-verification.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `scripts/verify.sh`
    - `scripts/check-phase73-uat-verification.ts`
    - `scripts/check-phase72-observability-evidence.ts`
    - `docs/metrics/lines-of-code.md`
    - `AGENTS.md`
  </read_first>
  <action>Add exactly one line `bun run scripts/check-phase73-uat-verification.ts` immediately after `bun run scripts/check-phase72-observability-evidence.ts` in `scripts/verify.sh`. Then add `FORBIDDEN_VERIFY_STRINGS` and `verifyVerifyScript` to `scripts/check-phase73-uat-verification.ts`, call `verifyVerifyScript` from `main()`, and make it assert that Phase 73 runs after Phase 72 while rejecting the forbidden default-verification strings listed in `<interfaces>`. Do not add live-smoke, manual peer, restart-after-progress, service-manager, mainnet IBD activation, current-tip timing, wall-clock release gate, or offline Cargo/Bazel flags to `scripts/verify.sh` per D-01, D-03, and D-04. Refresh `docs/metrics/lines-of-code.md` with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md` so the tracked generated artifact includes the new checker.</action>
  <verify>
    <automated>rg -n "check-phase72-observability-evidence.ts|check-phase73-uat-verification.ts" scripts/verify.sh</automated>
    <automated>bun --check scripts/check-phase73-uat-verification.ts</automated>
    <automated>bun run scripts/check-phase73-uat-verification.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "verifyVerifyScript|FORBIDDEN_VERIFY_STRINGS|current-tip timing|wall-clock release gate|-openbitcoinsync=mainnet-ibd" scripts/check-phase73-uat-verification.ts` returns matches.
    - `bun --eval 'const text = await Bun.file("scripts/verify.sh").text(); if (text.indexOf("bun run scripts/check-phase72-observability-evidence.ts") >= text.indexOf("bun run scripts/check-phase73-uat-verification.ts")) throw new Error("Phase 73 checker must run after Phase 72"); for (const forbidden of ["run-live-mainnet-smoke","--manual-peer","--restart-after-progress","systemctl","launchctl","openbitcoinsync=mainnet-ibd"]) { if (text.includes(forbidden)) throw new Error(`forbidden default verifier string: ${forbidden}`); }'` exits 0.
    - `rg -n "check-phase73-uat-verification.ts" docs/metrics/lines-of-code.md` returns one match after LOC refresh.
    - `bash scripts/verify.sh` exits 0.
  </acceptance_criteria>
  <done>`bash scripts/verify.sh` runs the Phase 73 checker in the correct order and remains hermetic by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor -> `bash scripts/verify.sh` | Contributors expect the default verifier to be deterministic and safe to run offline after bootstrap. |
| checker -> docs/source evidence | The checker turns local file anchors into release evidence and must fail closed on drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03-01 | Denial of Service | `scripts/verify.sh` | mitigate | Phase 73 checker rejects public-network, service-manager, restart-after-progress, long-sync activation, and timing-gate strings. |
| T-73-03-02 | Information Disclosure | default verifier | mitigate | Keep support/live-smoke collection out of default verification so no local public-network reports or support bundles are produced by default. |
| T-73-03-03 | Tampering | checker order | mitigate | Require Phase 73 checker to run after Phase 72 so all current evidence surfaces exist before Phase 73 audits them. |
</threat_model>

<verification>
Run the checker directly and through `bash scripts/verify.sh`. Public-network UAT commands must not be run for this plan.
</verification>

<success_criteria>
`scripts/verify.sh` includes the Phase 73 checker after Phase 72, the checker passes directly, full repo verification passes, and generated LOC is fresh.
</success_criteria>

<output>
After completion, create `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-03-SUMMARY.md`
</output>
