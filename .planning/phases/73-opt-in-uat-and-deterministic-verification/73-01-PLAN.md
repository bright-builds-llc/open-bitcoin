---
phase: 73-opt-in-uat-and-deterministic-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase73-uat-verification.ts
autonomous: true
requirements:
  - VER-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 73-2026-06-13T22-08-43
generated_at: 2026-06-13T23:39:11Z
user_setup: []
must_haves:
  truths:
    - "Contributor can audit a named deterministic coverage map for every VER-02 behavior."
    - "Coverage uses existing hermetic Rust tests and source anchors before adding new fixtures."
    - "Crash recovery is represented as deterministic durable reopen/recovery evidence."
  artifacts:
    - path: "scripts/check-phase73-uat-verification.ts"
      provides: "Phase 73 Bun checker skeleton with VER-02 coverage map"
      contains: "const VER02_COVERAGE"
  key_links:
    - from: "scripts/check-phase73-uat-verification.ts"
      to: "packages/open-bitcoin-node/src/sync/tests.rs"
      via: "coverage map file and test-name anchors"
      pattern: "phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network"
    - from: "scripts/check-phase73-uat-verification.ts"
      to: "packages/open-bitcoin-chainstate/tests/parity.rs"
      via: "coverage map file and test-name anchors"
      pattern: "connect_disconnect_and_reorg_preserve_phase_four_outcomes"
---

<objective>
Create the Phase 73 deterministic coverage map for VER-02.

Purpose: VER-02 is broad, but Phases 68 through 72 already added hermetic coverage. This plan makes that coverage auditable through one Phase 73 checker without duplicating fixtures.
Output: `scripts/check-phase73-uat-verification.ts` exists, runs locally with Bun, and maps each VER-02 behavior to explicit files and test/source needles.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-VERIFICATION.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-VERIFICATION.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-VERIFICATION.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-VERIFICATION.md
@scripts/check-phase72-observability-evidence.ts
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-chainstate/tests/parity.rs
@packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs

<interfaces>
From `scripts/check-phase72-observability-evidence.ts`:
```typescript
async function readText(relativePath: string, failures: string[]): Promise<string>;
function requireContains(text: string, needle: string, label: string, failures: string[]): void;
async function readJoined(files: readonly string[], failures: string[]): Promise<string>;
```

Coverage anchors that must appear in the Phase 73 map:
```text
connect_disconnect_and_reorg_preserve_phase_four_outcomes
chainstate_snapshot_round_trips_through_storage_dto
connected_active_chain_progress_survives_runtime_reopen
phase70_reorg_records_bounded_persisted_evidence
same_datadir_reopen_connects_best_available_branch_when_blocks_are_already_local
competing_header_branch_wins_after_restart_when_it_extends_farther
bounded_block_requests_use_validated_best_chain_headers_only
phase70_notfound_releases_inflight_and_rotates_to_second_peer
block_notfound_is_peer_attributed_no_credit
phase70_duplicate_block_releases_inflight_without_credit
duplicate_block_response_is_peer_attributed_no_credit
same_datadir_reopen_does_not_duplicate_connected_block_getdata
phase69_tip_evidence_survives_runtime_reopen
phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight
phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network
bounded_unattended_cycles_preserve_resource_pressure_and_retention
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 73 VER-02 coverage checker skeleton</name>
  <files>scripts/check-phase73-uat-verification.ts</files>
  <read_first>
    - `scripts/check-phase72-observability-evidence.ts`
    - `scripts/check-phase71-resource-restart.ts`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-chainstate/tests/parity.rs`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs`
  </read_first>
  <behavior>
    - Test 1: Running the checker fails if any VER-02 behavior is missing from `VER02_COVERAGE`.
    - Test 2: Running the checker fails if a mapped file does not contain every required needle.
    - Test 3: Running the checker succeeds without public-network, service-manager, wall-clock, or process-crash execution.
  </behavior>
  <action>Create `scripts/check-phase73-uat-verification.ts` as a Bun TypeScript checker following the Phase 72 helper pattern. Include `REPO_ROOT`, `PHASE_DIR`, `PLAN_FILES` for `73-01-PLAN.md` through `73-04-PLAN.md`, `REQUIREMENT_IDS = ["VER-01", "VER-02", "VER-03", "VER-04"]`, `readText`, `readJoined`, `requireContains`, and `requireNotContains`. Add `const VER02_COVERAGE` with exactly these `behavior` keys: `durable_utxo_undo_writes`, `block_connect_disconnect_reorg_across_restart`, `best_chain_header_selection`, `peer_response_failures`, `crash_recovery_durable_reopen`, `duplicate_connect_prevention`, and `resource_bounds`. Map each behavior to the concrete anchor names listed in `<interfaces>`. Per D-05 and D-06, this map is the source of truth for VER-02 coverage. Per D-08, use `connected_active_chain_progress_survives_runtime_reopen`, `phase69_tip_evidence_survives_runtime_reopen`, and the Phase 71 same-datadir resume test as crash-recovery reopen evidence; do not add a process-level crash harness.</action>
  <verify>
    <automated>bun --check scripts/check-phase73-uat-verification.ts</automated>
    <automated>bun run scripts/check-phase73-uat-verification.ts</automated>
    <automated>rg -n "const VER02_COVERAGE|durable_utxo_undo_writes|crash_recovery_durable_reopen|phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network" scripts/check-phase73-uat-verification.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "const VER02_COVERAGE" scripts/check-phase73-uat-verification.ts` returns one match.
    - `rg -n "durable_utxo_undo_writes|block_connect_disconnect_reorg_across_restart|best_chain_header_selection|peer_response_failures|crash_recovery_durable_reopen|duplicate_connect_prevention|resource_bounds" scripts/check-phase73-uat-verification.ts` returns all seven behavior names.
    - `bun run scripts/check-phase73-uat-verification.ts` exits 0 and prints `validated Phase 73 opt-in UAT and deterministic verification evidence`.
  </acceptance_criteria>
  <done>The Phase 73 checker exists and proves every VER-02 coverage-map anchor is present in existing deterministic files.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the mapped Rust anchors are executable</name>
  <files>scripts/check-phase73-uat-verification.ts</files>
  <read_first>
    - `scripts/check-phase73-uat-verification.ts`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-chainstate/tests/parity.rs`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs`
  </read_first>
  <action>Run the focused deterministic Rust commands below and keep the checker map on existing anchors when they pass. If a command fails only because a named anchor is absent, update `VER02_COVERAGE` to a real existing anchor that proves the same VER-02 behavior. Do not add a new Rust test unless no existing anchor proves the behavior; research found named anchors for every behavior, so the expected final state is checker-only. Do not add new Rust files in this plan.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate connect_disconnect_and_reorg_preserve_phase_four_outcomes --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node connected_active_chain_progress_survives_runtime_reopen --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node same_datadir_reopen_connects_best_available_branch_when_blocks_are_already_local --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "chainstate_snapshot_round_trips_through_storage_dto" scripts/check-phase73-uat-verification.ts` returns one match.
    - `rg -n "same_datadir_reopen_does_not_duplicate_connected_block_getdata" scripts/check-phase73-uat-verification.ts` returns one match.
    - `rg -n "phase70_notfound_releases_inflight_and_rotates_to_second_peer" scripts/check-phase73-uat-verification.ts` returns one match.
    - No new `packages/open-bitcoin-*/src/**/*.rs` or `packages/open-bitcoin-*/tests/**/*.rs` file is created by this plan.
  </acceptance_criteria>
  <done>Focused Rust anchors pass and the checker map references real deterministic coverage for all VER-02 behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file content -> checker assertions | The checker treats repository files as evidence and must fail closed on missing anchors. |
| public-network UAT -> deterministic coverage map | VER-02 evidence must remain hermetic and must not invoke public peers or live timing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01-01 | Tampering | `scripts/check-phase73-uat-verification.ts` | mitigate | Require exact file paths and test/source needles for every `VER02_COVERAGE` entry. |
| T-73-01-02 | Repudiation | VER-02 coverage evidence | mitigate | Store behavior-to-anchor mapping in the checker and run focused Cargo commands for representative anchors. |
| T-73-01-03 | Denial of Service | deterministic coverage verification | mitigate | Do not run public-network, service-manager, long-sync, wall-clock, or process-crash harnesses in this plan. |
</threat_model>

<verification>
Run the task automated checks. The checker must be local, short-running, and independent of public-network access.
</verification>

<success_criteria>
`scripts/check-phase73-uat-verification.ts` contains `VER02_COVERAGE`, passes Bun syntax/runtime checks, and maps all seven VER-02 behaviors to existing deterministic anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-01-SUMMARY.md`
</output>
