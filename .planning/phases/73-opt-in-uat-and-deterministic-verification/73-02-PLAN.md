---
phase: 73-opt-in-uat-and-deterministic-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operator/runtime-guide.md
autonomous: true
requirements:
  - VER-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 73-2026-06-13T22-08-43
generated_at: 2026-06-13T23:39:11Z
user_setup: []
must_haves:
  truths:
    - "Operator can find one authoritative Phase 73 opt-in UAT command matrix."
    - "Each CLI-backed UAT workflow has both repo-local Cargo and Bazel command forms."
    - "The matrix states what each workflow proves and what it does not prove."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Central Phase 73 opt-in public-mainnet UAT matrix"
      contains: "### Phase 73 opt-in public-mainnet UAT matrix"
  key_links:
    - from: "docs/operator/runtime-guide.md"
      to: "packages/open-bitcoin-cli/src/operator.rs"
      via: "documented operator CLI command grammar"
      pattern: "sync status --format json"
    - from: "docs/operator/runtime-guide.md"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "opt-in live public-mainnet evidence wrapper"
      pattern: "bun run scripts/run-live-mainnet-smoke.ts"
---

<objective>
Add the central Phase 73 opt-in public-mainnet UAT matrix.

Purpose: Operators need copy-pasteable repo-local commands for public-mainnet review, while contributors need `bash scripts/verify.sh` to remain hermetic.
Output: `docs/operator/runtime-guide.md` contains one authoritative Phase 73 matrix covering full-sync, stay-current, restart/resume, status comparison, live-smoke, and support-bundle UAT.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md
@AGENTS.md
@docs/operator/runtime-guide.md
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-rpc/BUILD.bazel
@packages/open-bitcoin-cli/BUILD.bazel
@scripts/run-live-mainnet-smoke.ts

<interfaces>
Repo-local command forms required by D-10:
```bash
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...
bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...
```

Daemon activation command forms available in this repo:
```bash
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind -- ...
bazel run //packages/open-bitcoin-rpc:open_bitcoind -- ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the central Phase 73 UAT matrix</name>
  <files>docs/operator/runtime-guide.md</files>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-CONTEXT.md`
    - `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-RESEARCH.md`
    - `packages/open-bitcoin-cli/src/operator.rs`
    - `packages/open-bitcoin-rpc/BUILD.bazel`
    - `packages/open-bitcoin-cli/BUILD.bazel`
  </read_first>
  <action>Add a new subsection titled exactly `### Phase 73 opt-in public-mainnet UAT matrix` under `## Real-Sync Verification And Benchmarks`, before the older milestone closeout subsections. Add a Markdown table with these exact workflow labels: `Full-sync activation and review`, `Stay-current/status review`, `Same-datadir restart/resume review`, `Status-surface comparison`, `Live-smoke report collection`, and `Support-bundle collection`. For each row include columns named `Workflow`, `Copy-paste commands`, `Evidence proves`, and `Does not prove`. Include D-09 through D-12 in the prose: this matrix is authoritative for Phase 73 UAT, live public-mainnet work is opt-in, and bundle existence, daemon startup, elapsed time, or peer reachability alone are not sync-to-tip proof.</action>
  <verify>
    <automated>rg -n "### Phase 73 opt-in public-mainnet UAT matrix|Full-sync activation and review|Stay-current/status review|Same-datadir restart/resume review|Status-surface comparison|Live-smoke report collection|Support-bundle collection" docs/operator/runtime-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "### Phase 73 opt-in public-mainnet UAT matrix" docs/operator/runtime-guide.md` returns one match.
    - `rg -n "\\| Workflow \\| Copy-paste commands \\| Evidence proves \\| Does not prove \\|" docs/operator/runtime-guide.md` returns one match.
    - `rg -n "Bundle existence, daemon startup, elapsed time, or peer reachability alone are not sync-to-tip proof" docs/operator/runtime-guide.md` returns one match.
  </acceptance_criteria>
  <done>The runtime guide has a single Phase 73 UAT matrix with all six required workflows and proof semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Populate repo-local command forms for each UAT workflow</name>
  <files>docs/operator/runtime-guide.md</files>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `packages/open-bitcoin-cli/src/operator.rs`
    - `packages/open-bitcoin-rpc/BUILD.bazel`
    - `packages/open-bitcoin-cli/BUILD.bazel`
    - `scripts/run-live-mainnet-smoke.ts`
  </read_first>
  <action>Populate the matrix with copy-pasteable commands. Full-sync activation must include both `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind -- -datadir=/tmp/open-bitcoin-mainnet -openbitcoinsync=mainnet-ibd` and `bazel run //packages/open-bitcoin-rpc:open_bitcoind -- -datadir=/tmp/open-bitcoin-mainnet -openbitcoinsync=mainnet-ibd`. CLI-backed workflows must include the exact Cargo and Bazel operator forms from D-10 for `sync status --format json`, `status --format json`, `dashboard --tick-ms 1000`, `service status`, `service restart`, and `support bundle --output-dir=/tmp/open-bitcoin-support`. Live-smoke rows must include `bun run scripts/run-live-mainnet-smoke.ts --datadir=/tmp/open-bitcoin-mainnet`, an optional `--manual-peer=HOST:8333`, and an opt-in restart command containing `--restart-after-progress --timeout-seconds=180 --poll-seconds=10`. Label `bash scripts/test-run-live-mainnet-smoke.sh` as deterministic fixture validation, not public-network UAT.</action>
  <verify>
    <automated>rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --|bazel run //packages/open-bitcoin-cli:open_bitcoin --|cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind --|bazel run //packages/open-bitcoin-rpc:open_bitcoind --|bun run scripts/run-live-mainnet-smoke.ts|bash scripts/test-run-live-mainnet-smoke.sh" docs/operator/runtime-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- .*sync status --format json" docs/operator/runtime-guide.md` returns at least one match.
    - `rg -n "bazel run //packages/open-bitcoin-cli:open_bitcoin -- .*sync status --format json" docs/operator/runtime-guide.md` returns at least one match.
    - `rg -n "bazel run //packages/open-bitcoin-rpc:open_bitcoind -- .*openbitcoinsync=mainnet-ibd" docs/operator/runtime-guide.md` returns at least one match.
    - `rg -n "deterministic fixture validation, not public-network UAT" docs/operator/runtime-guide.md` returns one match.
  </acceptance_criteria>
  <done>Every Phase 73 UAT workflow has concrete repo-local command forms and explicit proof/non-proof wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs -> public-mainnet commands | Documentation can cause operators to run public-network or service-manager workflows. |
| UAT evidence -> release claims | Local UAT artifacts can be misread as production-node or release-gate proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02-01 | Spoofing | UAT matrix proof wording | mitigate | Add `Evidence proves` and `Does not prove` columns for every workflow. |
| T-73-02-02 | Information Disclosure | support/live-smoke command guidance | mitigate | Keep live-smoke and support artifacts local and redacted; do not ask for checked-in live reports. |
| T-73-02-03 | Denial of Service | public-mainnet UAT commands | mitigate | Label all public-network and restart commands as opt-in UAT outside `bash scripts/verify.sh`. |
</threat_model>

<verification>
Run the task grep checks. Do not run public-mainnet commands as verification for this plan.
</verification>

<success_criteria>
`docs/operator/runtime-guide.md` contains a Phase 73 matrix with six required workflows, exact repo-local commands, and proof/non-proof semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/73-opt-in-uat-and-deterministic-verification/73-02-SUMMARY.md`
</output>
