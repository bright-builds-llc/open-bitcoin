---
phase: 78-progress-guarantees-and-stall-diagnosis
plan: 03
type: execute
wave: 3
depends_on:
  - 78-02
  - 78-07
files_modified:
  - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
  - packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
  - packages/open-bitcoin-cli/src/operator/soak/report.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 78-2026-06-16T14-21-42
generated_at: 2026-06-16T16:06:11Z
must_haves:
  truths:
    - "Soak checkpoints preserve the typed progress-credit and stall fields from shared status."
    - "Soak reports show credited validated progress, rejected non-credit activity, expected windows, threshold state, last useful work, peer contribution, stalled subsystem, and next action."
    - "The soak ledger remains the durable source for soak events while reports remain reproducible projections."
  artifacts:
    - path: packages/open-bitcoin-cli/src/operator/soak/ledger.rs
      provides: "Expanded SoakCheckpointStatus schema for Phase 78 evidence"
    - path: packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
      provides: "Snapshot-to-checkpoint projection from shared SyncStatus fields"
    - path: packages/open-bitcoin-cli/src/operator/soak/report.rs
      provides: "JSON and Markdown report projection for progress guarantee evidence"
  key_links:
    - from: packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
      to: packages/open-bitcoin-node/src/status/progress_guarantee.rs
      via: "OpenBitcoinStatusSnapshot.sync Phase 78 fields"
      pattern: "progress_credit"
    - from: packages/open-bitcoin-cli/src/operator/soak/report.rs
      to: packages/open-bitcoin-cli/src/operator/soak/ledger.rs
      via: "SoakCheckpointStatus serialized into reports"
      pattern: "Progress credit"
---

<objective>
Carry progress guarantees and stall diagnosis through soak checkpoints and reports.

Purpose: Per D-09, the Phase 75 datadir-owned soak ledger must record the shared progress/stall facts needed to audit progress credit without making reports a separate source of truth.
Output: Expanded `SoakCheckpointStatus`, checkpoint projection from shared status, and JSON/Markdown report rendering for Phase 78 fields.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-07-SUMMARY.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-01-SUMMARY.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-04-SUMMARY.md
@.planning/phases/76-disk-and-resource-bound-enforcement/76-03-SUMMARY.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-05-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/soak/ledger.rs
@packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
@packages/open-bitcoin-cli/src/operator/soak/report.rs
@packages/open-bitcoin-cli/src/operator/soak/tests.rs
@packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs

<interfaces>
Existing checkpoint projection:
```rust
pub(crate) struct SoakCheckpointStatus {
    pub(crate) maybe_no_progress_diagnosis_label: Option<String>,
    pub(crate) maybe_resource_bound_state_label: Option<String>,
    pub(crate) maybe_validated_active_chain_height: Option<u64>,
    pub(crate) maybe_best_known_tip_height: Option<u64>,
}

pub(super) fn checkpoint_status_from_snapshot(
    snapshot: &OpenBitcoinStatusSnapshot,
) -> SoakCheckpointStatus;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand checkpoint schema with shared progress/stall fields</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/ledger.rs, packages/open-bitcoin-cli/src/operator/soak/tests.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
  - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-node/src/status/progress_guarantee.rs
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <behavior>
  - `SoakCheckpointStatus` JSON keeps stable field names for Phase 78 evidence.
  - Existing soak and support fixture constructors compile after the schema expands.
  - Oversized checkpoint guard still applies after adding fields.
  </behavior>
  <action>
  Add these exact fields to `SoakCheckpointStatus`:
  - `maybe_progress_credit_kind_label: Option<String>`
  - `maybe_progress_credit_height: Option<u64>`
  - `maybe_progress_credit_hash: Option<String>`
  - `maybe_progress_credit_work: Option<String>`
  - `maybe_progress_credit_source_unix_seconds: Option<u64>`
  - `progress_credit_rejected_activity_labels: Vec<String>`
  - `maybe_expected_progress_window_seconds: Option<u64>`
  - `maybe_no_progress_threshold_state_label: Option<String>`
  - `maybe_no_progress_threshold_seconds: Option<u64>`
  - `maybe_last_useful_work_kind_label: Option<String>`
  - `maybe_last_useful_work_height: Option<u64>`
  - `maybe_last_peer_contribution_label: Option<String>`
  - `maybe_stalled_subsystem_label: Option<String>`
  - `maybe_stall_confidence_label: Option<String>`
  - `stall_evidence_basis: Vec<String>`
  - `maybe_stall_next_action: Option<String>`
  Update every `SoakCheckpointStatus {` test/helper literal in the files listed by `rg -n "SoakCheckpointStatus \\{" packages/open-bitcoin-cli/src/operator/soak packages/open-bitcoin-cli/src/operator/support/tests.rs` with explicit `None` or empty vector values. Add `soak_progress_guarantee_report_json_preserves_checkpoint_field_names` coverage asserting the JSON keys above exist in `latest_checkpoint`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_report_json_preserves_checkpoint_field_names --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "maybe_progress_credit_kind_label|progress_credit_rejected_activity_labels|maybe_stalled_subsystem_label" packages/open-bitcoin-cli/src/operator/soak/ledger.rs`
  - `rg -n "soak_progress_guarantee_report_json_preserves_checkpoint_field_names" packages/open-bitcoin-cli/src/operator/soak/tests.rs`
  - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_report_json_preserves_checkpoint_field_names --all-features`
  </acceptance_criteria>
  <done>The soak checkpoint schema can persist every shared Phase 78 field needed for PROG-01 through PROG-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project checkpoint fields from shared status</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs, packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs</files>
  <read_first>
  - packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs
  - packages/open-bitcoin-node/src/status/progress_guarantee.rs
  - packages/open-bitcoin-cli/src/operator/sync_truth_render.rs
  </read_first>
  <behavior>
  - Available `snapshot.sync.progress_credit` populates progress credit kind, height, hash, work, timestamp, and rejected-activity labels.
  - Available `snapshot.sync.stall_diagnosis` populates stalled subsystem, confidence, evidence basis, and next action.
  - Unavailable shared fields leave optional checkpoint fields empty and do not fabricate progress.
  </behavior>
  <action>
  In `checkpoint_status_from_snapshot`, read only `snapshot.sync.progress_credit`, `snapshot.sync.expected_progress_window`, `snapshot.sync.no_progress_threshold`, `snapshot.sync.last_useful_work`, `snapshot.sync.last_peer_contribution`, and `snapshot.sync.stall_diagnosis`. Use `serde_label(&value)` for enum labels and compact string summaries for rejected activity and peer contribution. Rejected activity labels must use `kind=<label> observed_count=<n> reason=<reason>`. Peer contribution label must use `peer=<peer> kind=<label> messages=<n> headers=<n> blocks=<n> failure=<label-or-unavailable>`. Add tests named `soak_progress_guarantee_checkpoint_available_status_records_shared_fields` and `soak_progress_guarantee_checkpoint_unavailable_status_leaves_optional_fields_empty`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_checkpoint_ --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "snapshot\\.sync\\.progress_credit|snapshot\\.sync\\.stall_diagnosis|snapshot\\.sync\\.last_peer_contribution" packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs`
  - `rg -n "kind=.*observed_count=.*reason=" packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs`
  - `rg -n "soak_progress_guarantee_checkpoint_available_status_records_shared_fields|soak_progress_guarantee_checkpoint_unavailable_status_leaves_optional_fields_empty" packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs`
  - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_checkpoint_ --all-features`
  </acceptance_criteria>
  <done>Soak checkpoint collection carries Phase 78 shared status fields without renderer-local classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render progress guarantees in soak reports</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/report.rs, packages/open-bitcoin-cli/src/operator/soak/tests.rs</files>
  <read_first>
  - packages/open-bitcoin-cli/src/operator/soak/report.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests.rs
  - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
  - docs/architecture/status-snapshot.md
  </read_first>
  <behavior>
  - Markdown reports show progress credit, rejected non-credit activity, expected progress window, threshold state, last useful work, last peer contribution, stalled subsystem, confidence, evidence basis, and next action.
  - JSON report remains the serialized projection of `SoakCheckpointStatus`.
  - Report output excludes raw logs, raw status snapshots, raw live-smoke input, wallet material, credentials, and unbounded peer tables.
  </behavior>
  <action>
  In `render_soak_report_markdown`, add these exact labels under `## Latest Checkpoint`: `Progress credit`, `Rejected progress activity`, `Expected progress window seconds`, `No-progress threshold`, `Last useful work`, `Last peer contribution`, `Stalled subsystem`, `Stall confidence`, `Stall evidence basis`, and `Stall next action`. Render unavailable optional fields with the existing `push_optional` style; do not invent a successful progress line when fields are absent. Add tests named `soak_progress_guarantee_report_markdown_renders_credit_and_stall_fields` and `soak_progress_guarantee_report_excludes_forbidden_raw_material`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_report_ --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "Progress credit|Rejected progress activity|Stalled subsystem|Stall next action" packages/open-bitcoin-cli/src/operator/soak/report.rs`
  - `rg -n "soak_progress_guarantee_report_markdown_renders_credit_and_stall_fields|soak_progress_guarantee_report_excludes_forbidden_raw_material" packages/open-bitcoin-cli/src/operator/soak/tests.rs`
  - `! rg -n "raw status snapshot|rpcpassword|wallet material|unbounded peer table" packages/open-bitcoin-cli/src/operator/soak/report.rs`
  - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_progress_guarantee_report_ --all-features`
  </acceptance_criteria>
  <done>Soak reports expose the shared Phase 78 facts clearly while remaining projections over the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared status snapshot -> soak checkpoint ledger | Status facts become durable soak evidence. |
| soak ledger -> JSON/Markdown reports | Durable events become operator-readable artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-01 | Repudiation | `SoakCheckpointStatus` | mitigate | Persist exact shared status labels, heights, work, timestamps, rejected activity, threshold state, and evidence basis. |
| T-78-03-02 | Tampering | `checkpoint_status_from_snapshot` | mitigate | Project from `snapshot.sync` Phase 78 fields only; do not reclassify from report strings or peer counters. |
| T-78-03-03 | Information Disclosure | soak reports | mitigate | Render compact allowlisted fields; tests reject raw logs, raw status snapshots, credentials, wallet material, and unbounded peer tables. |
| T-78-03-04 | Denial of Service | checkpoint size | mitigate | Preserve existing checkpoint size bounds and use compact string labels for activity and peer contribution evidence. |
</threat_model>

<verification>
Run the focused `soak_progress_guarantee_` tests and `cargo check -p open-bitcoin-cli` after implementation. Do not add public-network or wall-clock soak verification.
</verification>

<success_criteria>
- Soak checkpoints carry the same progress/stall fields as `SyncStatus`.
- Soak report JSON preserves the checkpoint field names.
- Soak report Markdown is actionable and does not claim progress from rejected activity.
- Raw local evidence remains excluded.
</success_criteria>

<output>
After completion, create `.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-03-SUMMARY.md`.
</output>
