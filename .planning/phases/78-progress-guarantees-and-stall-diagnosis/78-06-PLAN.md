---
phase: 78-progress-guarantees-and-stall-diagnosis
plan: 06
type: execute
wave: 5
depends_on:
  - 78-03
  - 78-04
  - 78-05
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/README.md
  - docs/parity/catalog/p2p.md
  - docs/parity/catalog/chainstate.md
  - docs/parity/catalog/operator-runtime-release-hardening.md
  - scripts/check-phase78-progress-guarantees.ts
  - scripts/check-phase78-progress-guarantees.test.ts
  - scripts/verify.sh
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 78-2026-06-16T14-21-42
generated_at: 2026-06-16T16:06:11Z
must_haves:
  truths:
    - "Docs explain that progress credit comes only from validated durable active-chain progress or explicit stay-current evidence."
    - "Parity roots make PROG-01 through PROG-04 auditable across source, tests, docs, checker, and planning artifacts."
    - "`scripts/verify.sh` runs the Phase 78 checker after Phase 77 and remains deterministic, public-network-free, service-manager-free, and multi-day-free."
    - "A passed Phase 78 verification report records exact commands and evidence."
  artifacts:
    - path: scripts/check-phase78-progress-guarantees.ts
      provides: "Deterministic docs/source/parity/default-verification checker"
    - path: scripts/check-phase78-progress-guarantees.test.ts
      provides: "Fixture-root checker regression tests"
    - path: .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md
      provides: "Phase 78 verification evidence report"
  key_links:
    - from: scripts/verify.sh
      to: scripts/check-phase78-progress-guarantees.ts
      via: "Bun checker command immediately after Phase 77 checker"
      pattern: "bun run scripts/check-phase78-progress-guarantees.ts"
    - from: docs/parity/index.json
      to: .planning/phases/78-progress-guarantees-and-stall-diagnosis
      via: "phase78-progress-guarantees-stall-diagnosis surface id"
      pattern: "phase78-progress-guarantees-stall-diagnosis"
---

<objective>
Close Phase 78 with docs, parity roots, deterministic checker, verifier wiring, and verification evidence.

Purpose: D-15 and D-16 require a focused Bun checker for docs, parity roots, field names, and default-verification exclusions that Rust tests cannot prove.
Output: Operator/architecture/parity documentation, Phase 78 checker and checker tests, `scripts/verify.sh` wiring, and `78-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-RESEARCH.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-07-SUMMARY.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@scripts/check-phase77-corruption-lock-recovery.ts
@scripts/check-phase77-corruption-lock-recovery.test.ts
@scripts/verify.sh
@standards/languages/typescript-javascript.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document progress guarantees and parity roots</name>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md, docs/parity/index.json, docs/parity/checklist.md, docs/parity/README.md, docs/parity/catalog/p2p.md, docs/parity/catalog/chainstate.md, docs/parity/catalog/operator-runtime-release-hardening.md</files>
  <read_first>
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/README.md
  - docs/parity/catalog/operator-runtime-release-hardening.md
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <action>
  Add a Phase 78 section to `docs/operator/runtime-guide.md` with these exact terms: `progress_credit`, `last_useful_work`, `last_peer_contribution`, `expected_progress_window`, `no_progress_threshold`, `stall_diagnosis`, `validated_durable_active_chain`, `current_at_best_known_tip`, `storage_or_resource_pressure`, `at_tip_waiting`, `operator_stop`, and `local_shutdown`. State exactly that headers, downloaded block bodies, peer messages, in-flight requests, retries, and report generation are evidence only and do not advance the credited progress watermark.
  Update `docs/architecture/status-snapshot.md` and `docs/architecture/operator-observability.md` to describe the shared contract and checkpoint/report/support/live-smoke projections as compact Phase 78 evidence. Add the parity surface id `phase78-progress-guarantees-stall-diagnosis` to `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/catalog/p2p.md`, `docs/parity/catalog/chainstate.md`, and `docs/parity/catalog/operator-runtime-release-hardening.md`, covering PROG-01, PROG-02, PROG-03, and PROG-04. Keep wording scoped to progress guarantees and stall diagnosis only.
  </action>
  <verify>
    <automated>rg -n "phase78-progress-guarantees-stall-diagnosis|PROG-01|PROG-02|PROG-03|PROG-04" docs/parity/index.json docs/parity/checklist.md docs/parity/README.md docs/parity/catalog/operator-runtime-release-hardening.md</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "progress_credit|last_useful_work|last_peer_contribution|expected_progress_window|no_progress_threshold|stall_diagnosis" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md`
  - `rg -n "validated_durable_active_chain|current_at_best_known_tip|storage_or_resource_pressure|at_tip_waiting|operator_stop|local_shutdown" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md`
  - `rg -n "phase78-progress-guarantees-stall-diagnosis" docs/parity/index.json docs/parity/checklist.md docs/parity/README.md docs/parity/catalog/p2p.md docs/parity/catalog/chainstate.md docs/parity/catalog/operator-runtime-release-hardening.md`
  - `! rg -n "raw support body|raw status snapshot|broad readiness claim|automatic upload" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/parity/catalog/operator-runtime-release-hardening.md`
  </acceptance_criteria>
  <done>Operator, architecture, observability, and parity docs make Phase 78 evidence auditable without expanding deferred scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 78 deterministic checker and verifier wiring</name>
  <files>scripts/check-phase78-progress-guarantees.ts, scripts/check-phase78-progress-guarantees.test.ts, scripts/verify.sh</files>
  <read_first>
  - scripts/check-phase77-corruption-lock-recovery.ts
  - scripts/check-phase77-corruption-lock-recovery.test.ts
  - scripts/verify.sh
  - standards/languages/typescript-javascript.md
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <behavior>
  - Checker fails when any PROG requirement is missing from plan frontmatter.
  - Checker fails when required Phase 78 source, test, docs, parity, or field-name anchors are missing.
  - Checker fails when `scripts/verify.sh` omits Phase 78 or wires it before Phase 77.
  - Checker fails if default verification includes public-network, manual-peer, service-manager, or multi-day wall-clock gates.
  </behavior>
  <action>
  Create `scripts/check-phase78-progress-guarantees.ts` using the Phase 77 checker structure. Use `OPEN_BITCOIN_PHASE78_REPO_ROOT` as the repo-root override, `PHASE_DIR = ".planning/phases/78-progress-guarantees-and-stall-diagnosis"`, `PHASE78_REQUIREMENTS = ["PROG-01", "PROG-02", "PROG-03", "PROG-04"]`, `PHASE77_CHECKER_COMMAND = "bun run scripts/check-phase77-corruption-lock-recovery.ts"`, `PHASE78_TEST_COMMAND = "bun test scripts/check-phase78-progress-guarantees.test.ts"`, `PHASE78_CHECKER_COMMAND = "bun run scripts/check-phase78-progress-guarantees.ts"`, and `SURFACE_ID = "phase78-progress-guarantees-stall-diagnosis"`.
  Require anchors for these exact source symbols and field names: `ProgressCreditEvidence`, `ProgressCreditKind`, `RejectedProgressActivityKind`, `ProgressWindowEvidence`, `NoProgressThresholdEvidence`, `PeerContributionEvidence`, `StalledSubsystem`, `StallDiagnosisEvidence`, `progress_credit`, `expected_progress_window`, `no_progress_threshold`, `last_useful_work`, `last_peer_contribution`, and `stall_diagnosis`. Require runtime anchors `made_validated_durable_progress`, `classify_progress_credit`, `classify_stall_diagnosis`, `write_progress_guarantee_log`, and reject `summary.headers_received > 0 || summary.blocks_received > 0` in `packages/open-bitcoin-node/src/sync/runtime_state.rs`.
  Require test anchors for all `phase78_` Rust tests named in Plan 78-05. Require docs/parity anchors for `phase78-progress-guarantees-stall-diagnosis` and all four PROG ids. Require `scripts/verify.sh` to contain the Phase 77 checker command, then the Phase 78 test command, then the Phase 78 checker command in that order. Forbid these strings in `scripts/verify.sh`: `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl`, `launchctl`, `openbitcoinsync=mainnet-ibd`, `sleep 86400`, `multi-day wall-clock`, `lsof`, and `/proc/`.
  Create `scripts/check-phase78-progress-guarantees.test.ts` with fixture-root tests for missing requirement coverage, missing source anchors, forbidden runtime header/block credit expression, missing deterministic tests, parity root omissions, and verify-script ordering/exclusion failures. Wire `bun test scripts/check-phase78-progress-guarantees.test.ts` and `bun run scripts/check-phase78-progress-guarantees.ts` immediately after the Phase 77 checker command in `scripts/verify.sh`.
  </action>
  <verify>
    <automated>bun test scripts/check-phase78-progress-guarantees.test.ts</automated>
    <automated>bun run scripts/check-phase78-progress-guarantees.ts</automated>
  </verify>
  <acceptance_criteria>
  - `test -f scripts/check-phase78-progress-guarantees.ts`
  - `test -f scripts/check-phase78-progress-guarantees.test.ts`
  - `rg -n "OPEN_BITCOIN_PHASE78_REPO_ROOT|PHASE78_REQUIREMENTS|phase78-progress-guarantees-stall-diagnosis" scripts/check-phase78-progress-guarantees.ts`
  - `rg -n "summary\\.headers_received > 0 \\|\\| summary\\.blocks_received > 0" scripts/check-phase78-progress-guarantees.ts`
  - `rg -n "bun test scripts/check-phase78-progress-guarantees.test.ts|bun run scripts/check-phase78-progress-guarantees.ts" scripts/verify.sh`
  - `bun test scripts/check-phase78-progress-guarantees.test.ts`
  - `bun run scripts/check-phase78-progress-guarantees.ts`
  </acceptance_criteria>
  <done>The deterministic checker guards source anchors, docs, parity roots, requirements, and default-verification exclusions.</done>
</task>

<task type="auto">
  <name>Task 3: Run final verification and record evidence</name>
  <files>.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md, docs/metrics/lines-of-code.md</files>
  <read_first>
  - scripts/verify.sh
  - scripts/check-phase78-progress-guarantees.ts
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  - docs/metrics/lines-of-code.md
  </read_first>
  <action>
  Run these commands in order and stop on the first failure: `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase78_ --all-features`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase78_ --all-features`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_phase78_progress_guarantee --all-features`, `bash scripts/test-run-live-mainnet-smoke.sh`, `bun test scripts/check-phase78-progress-guarantees.test.ts`, `bun run scripts/check-phase78-progress-guarantees.ts`, `bun run scripts/check-parity-breadcrumbs.ts --check`, and `bash scripts/verify.sh`.
  If and only if all commands pass, create `.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md` with frontmatter `status: passed`, `phase: 78-progress-guarantees-and-stall-diagnosis`, `requirements: [PROG-01, PROG-02, PROG-03, PROG-04]`, and a command evidence table. Refresh `docs/metrics/lines-of-code.md` if repo hooks or verification regenerate it; do not hand-edit generated counts.
  </action>
  <verify>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
  - `test -f .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md`
  - `rg -n "status: passed|PROG-01|PROG-02|PROG-03|PROG-04|bash scripts/verify.sh" .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-VERIFICATION.md`
  - `bash scripts/verify.sh`
  </acceptance_criteria>
  <done>Phase 78 has a passed verification report backed by focused tests, checker tests, parity breadcrumbs, and full repo-native verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/docs/plans -> deterministic checker | Checker interprets repository text as evidence anchors. |
| checker -> default verification | `scripts/verify.sh` becomes the local gate for Phase 78 documentation and anchors. |
| verification output -> planning evidence | Human-readable verification report records pass/fail claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-06-01 | Repudiation | docs/parity roots | mitigate | Require the Phase 78 surface id and PROG-01 through PROG-04 across parity roots and checker anchors. |
| T-78-06-02 | Tampering | checker fixtures | mitigate | Add negative tests for missing anchors, missing requirements, forbidden runtime expression, and verify-script order failures. |
| T-78-06-03 | Denial of Service | `scripts/verify.sh` | mitigate | Keep checker short-running and forbid public-network, service-manager, process-scan, and multi-day wall-clock gates. |
| T-78-06-04 | Spoofing | verification report | mitigate | Write `status: passed` only after exact commands pass; include command evidence in the report. |
</threat_model>

<verification>
The final task runs focused Phase 78 checks, the Phase 78 checker, parity breadcrumbs, live-smoke fixture tests, and full `bash scripts/verify.sh`.
</verification>

<success_criteria>
- Docs and parity roots cover PROG-01 through PROG-04.
- Phase 78 checker and checker tests pass.
- `scripts/verify.sh` runs Phase 78 immediately after Phase 77 and remains deterministic.
- `78-VERIFICATION.md` records passed evidence after full verification.
</success_criteria>

<output>
After completion, create `.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-06-SUMMARY.md`.
</output>
