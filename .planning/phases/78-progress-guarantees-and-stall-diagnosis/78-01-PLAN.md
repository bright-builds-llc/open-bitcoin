---
phase: 78-progress-guarantees-and-stall-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status/progress_guarantee.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 78-2026-06-16T14-21-42
generated_at: 2026-06-16T16:06:11Z
must_haves:
  truths:
    - "Shared status has typed progress-credit evidence that cannot represent header-only or download-only work as credited active-chain progress."
    - "Shared status exposes expected progress window, no-progress threshold, last useful work, last peer contribution, and stall diagnosis fields with explicit unavailable reasons."
    - "Legacy node status JSON and existing node status fixtures remain readable after the additive fields land."
  artifacts:
    - path: packages/open-bitcoin-node/src/status/progress_guarantee.rs
      provides: "ProgressCreditEvidence, progress-window, threshold, peer-contribution, and stall-diagnosis DTOs"
    - path: packages/open-bitcoin-node/src/status.rs
      provides: "Additive SyncStatus FieldAvailability fields and serde defaults"
    - path: packages/open-bitcoin-node/src/status/tests.rs
      provides: "Stable label and legacy-default contract coverage"
  key_links:
    - from: packages/open-bitcoin-node/src/status.rs
      to: packages/open-bitcoin-node/src/status/progress_guarantee.rs
      via: "mod progress_guarantee; pub use progress_guarantee::*"
      pattern: "progress_guarantee"
    - from: packages/open-bitcoin-node/src/lib.rs
      to: packages/open-bitcoin-node/src/status/progress_guarantee.rs
      via: "public re-export of new status DTOs"
      pattern: "ProgressCreditEvidence"
---

<objective>
Create the shared Phase 78 progress guarantee and stall diagnosis contract.

Purpose: Phase 78 must extend the existing shared status model per D-04 through D-06 instead of adding soak-only or renderer-local state.
Output: Typed status DTOs, additive `SyncStatus` fields with legacy-safe defaults, node status fixture updates, and parity breadcrumb coverage for the new Rust source.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/status/resource_bounds.rs
@packages/open-bitcoin-node/src/status/recovery.rs
@packages/open-bitcoin-node/src/status/tests.rs
@packages/open-bitcoin-node/src/lib.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
Existing shared status pattern:
```rust
mod recovery;
mod resource_bounds;

pub use recovery::SyncRecoveryCategory;
pub use resource_bounds::*;

pub enum FieldAvailability<T> {
    Available(T),
    Unavailable { reason: String },
}

pub struct SyncStatus {
    pub sync_progress: FieldAvailability<SyncProgress>,
    pub last_successful_progress_unix_seconds: FieldAvailability<u64>,
    pub best_known_tip: FieldAvailability<BestKnownTipStatus>,
    pub stay_current: FieldAvailability<StayCurrentStatus>,
    pub no_progress_diagnosis: FieldAvailability<NoProgressDiagnosis>,
    pub resource_pressure: FieldAvailability<SyncResourcePressure>,
    pub reconcile_progress: FieldAvailability<SyncReconcileProgressStatus>,
}
```

Existing child-module pattern:
```rust
// packages/open-bitcoin-node/src/status/resource_bounds.rs
use super::FieldAvailability;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ResourceBoundSnapshot { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed progress-guarantee DTOs</name>
  <files>packages/open-bitcoin-node/src/status/progress_guarantee.rs, packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/resource_bounds.rs
  - packages/open-bitcoin-node/src/status/recovery.rs
  - packages/open-bitcoin-node/src/lib.rs
  - docs/parity/source-breadcrumbs.json
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <behavior>
  - Test stable serde labels for every new enum before runtime projection exists.
  - Test `ProgressCreditEvidence` can encode validated durable active-chain credit and current-at-best-known-tip stay-current credit, but has no header-only or download-only credit kind.
  - Test optional or missing evidence is represented by `FieldAvailability::Unavailable`, not by omitted fields.
  </behavior>
  <action>
  Create `packages/open-bitcoin-node/src/status/progress_guarantee.rs` with the top parity breadcrumb `none: Open Bitcoin-only support/infrastructure; no direct Bitcoin Knots source anchor identified.` Define these exact public contracts with `Serialize`, `Deserialize`, `Debug`, `Clone`, `PartialEq`, and `Eq` where possible:
  - `ProgressCreditKind` enum labels: `validated_durable_active_chain`, `current_at_best_known_tip`.
  - `RejectedProgressActivityKind` enum labels: `header_download`, `block_download`, `in_flight_request`, `peer_message`, `report_projection`, `retry`.
  - `RejectedProgressActivity { kind: RejectedProgressActivityKind, observed_count: u64, reason: String }`.
  - `ProgressCreditEvidence { kind: ProgressCreditKind, credited_validated_active_chain_height: u64, credited_validated_active_chain_hash: String, credited_validated_active_chain_work: String, source_unix_seconds: u64, rejected_activity: Vec<RejectedProgressActivity> }`.
  - `ProgressWindowEvidence { retry_backoff_seconds: u64, max_sync_rounds: u64, expected_progress_window_seconds: u64, tip_freshness_threshold_seconds: u64 }`.
  - `NoProgressThresholdState` enum labels: `within_window`, `exceeded`.
  - `NoProgressThresholdEvidence { threshold_seconds: u64, elapsed_since_last_useful_work_seconds: u64, state: NoProgressThresholdState, evaluated_at_unix_seconds: u64 }`.
  - `PeerContributionKind` enum labels: `headers_only`, `blocks_only`, `headers_and_blocks`, `messages_only`, `no_useful_contribution`, `failure`.
  - `PeerContributionEvidence { peer: String, maybe_resolved_endpoint: Option<String>, kind: PeerContributionKind, messages_processed: u64, headers_received: u64, blocks_received: u64, maybe_last_activity_unix_seconds: Option<u64>, maybe_failure_reason_label: Option<String> }`.
  - `StalledSubsystem` enum labels: `public_network_reachability`, `incompatible_peers`, `slow_or_stalled_peers`, `peer_failures_exhausted`, `stale_inflight_cleanup`, `branch_competition_awaiting_bodies`, `validation`, `storage_or_resource_pressure`, `at_tip_waiting`, `operator_stop`, `local_shutdown`, `unknown`.
  - `StallDiagnosisConfidence` enum labels: `high`, `medium`, `low`.
  - `StallDiagnosisEvidence { stalled_subsystem: StalledSubsystem, confidence: StallDiagnosisConfidence, evidence_basis: Vec<String>, next_action: String, maybe_no_progress_diagnosis: Option<super::NoProgressDiagnosis>, maybe_recovery_category: Option<super::SyncRecoveryCategory>, maybe_latest_stop_reason_label: Option<String>, source_unix_seconds: u64 }`.
  Add `mod progress_guarantee;` and `pub use progress_guarantee::*;` in `status.rs`, then re-export the new types from `lib.rs`. Update `docs/parity/source-breadcrumbs.json` for the new first-party Rust source per D-16.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase78_progress_guarantee_status_contract --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `test -f packages/open-bitcoin-node/src/status/progress_guarantee.rs`
  - `rg -n "pub enum ProgressCreditKind|validated_durable_active_chain|current_at_best_known_tip" packages/open-bitcoin-node/src/status/progress_guarantee.rs`
  - `rg -n "pub enum StalledSubsystem|public_network_reachability|storage_or_resource_pressure|local_shutdown" packages/open-bitcoin-node/src/status/progress_guarantee.rs`
  - `rg -n "pub use progress_guarantee::\\*;" packages/open-bitcoin-node/src/status.rs`
  - `rg -n "ProgressCreditEvidence" packages/open-bitcoin-node/src/lib.rs`
  - `rg -n "packages/open-bitcoin-node/src/status/progress_guarantee.rs" docs/parity/source-breadcrumbs.json`
  </acceptance_criteria>
  <done>The new shared DTO module exists, labels are stable, types are exported, and parity breadcrumbs cover the new Rust file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire additive SyncStatus fields and legacy defaults</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests.rs, packages/open-bitcoin-node/src/sync/types/summary.rs</files>
  <read_first>
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  </read_first>
  <behavior>
  - Legacy `SyncStatus` JSON that predates Phase 78 decodes with every new field unavailable and a stable reason.
  - Existing status constructors compile after adding the fields.
  - Summary-only status projection marks the fields unavailable until Plan 78-02 supplies runtime evidence.
  </behavior>
  <action>
  In `SyncStatus`, add these exact additive fields with serde defaults:
  - `progress_credit: FieldAvailability<ProgressCreditEvidence>` default reason `progress credit evidence unavailable`
  - `expected_progress_window: FieldAvailability<ProgressWindowEvidence>` default reason `expected progress window unavailable`
  - `no_progress_threshold: FieldAvailability<NoProgressThresholdEvidence>` default reason `no-progress threshold evidence unavailable`
  - `last_useful_work: FieldAvailability<ProgressCreditEvidence>` default reason `last useful work unavailable`
  - `last_peer_contribution: FieldAvailability<PeerContributionEvidence>` default reason `last peer contribution unavailable`
  - `stall_diagnosis: FieldAvailability<StallDiagnosisEvidence>` default reason `stall diagnosis unavailable`
  Add private default helper functions in `status.rs` using the same naming style as `no_progress_diagnosis_unavailable`. Update every `SyncStatus {` constructor found in the node package by `rg -n "SyncStatus \\{" packages/open-bitcoin-node -g'*.rs'` with unavailable field values or existing defaults. Update `SyncRunSummary::sync_status` to return all six fields as unavailable with the exact reasons above. Keep D-05 by extending existing shared status only; do not add a soak-only model. Downstream CLI/RPC constructor and fixture updates are intentionally split to Plan 78-07 so this shared contract plan stays below the scope threshold.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase78_progress_guarantee_status_contract --all-features</automated>
    <automated>cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "progress_credit: FieldAvailability<ProgressCreditEvidence>" packages/open-bitcoin-node/src/status.rs`
  - `rg -n "expected_progress_window: FieldAvailability<ProgressWindowEvidence>" packages/open-bitcoin-node/src/status.rs`
  - `rg -n "stall_diagnosis: FieldAvailability<StallDiagnosisEvidence>" packages/open-bitcoin-node/src/status.rs`
  - `rg -n "progress credit evidence unavailable|last useful work unavailable|stall diagnosis unavailable" packages/open-bitcoin-node/src/status.rs packages/open-bitcoin-node/src/status/tests.rs`
  - `cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features`
  </acceptance_criteria>
  <done>`SyncStatus` carries the Phase 78 fields everywhere status snapshots are constructed, with explicit unavailable reasons before runtime projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime status JSON -> CLI/RPC/dashboard/support consumers | Additive machine fields cross serde and renderer boundaries. |
| durable metadata -> legacy status decode | Older stored JSON may lack Phase 78 fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01-01 | Tampering | `ProgressCreditKind` | mitigate | Only define `validated_durable_active_chain` and `current_at_best_known_tip`; no header/download credit variant exists. |
| T-78-01-02 | Repudiation | `SyncStatus` defaults | mitigate | Use `FieldAvailability::Unavailable` defaults with exact reasons for every new field; test legacy JSON decoding. |
| T-78-01-03 | Information Disclosure | status DTOs | mitigate | Store bounded labels, heights, hashes, work, timestamps, and basis strings only; do not add raw peer tables, logs, credentials, or wallet material. |
| T-78-01-04 | Denial of Service | status.rs size and node constructors | mitigate | Put DTOs in `status/progress_guarantee.rs` and update node constructors found by `rg -n "SyncStatus \\{" packages/open-bitcoin-node -g'*.rs'` so compile failures expose missed fields; downstream constructors are handled by 78-07. |
</threat_model>

<verification>
Run the focused node status contract test and node cargo check listed in tasks. Do not run public-network, manual-peer, real service-manager, or multi-day soak commands.
</verification>

<success_criteria>
- `SyncStatus` has all six Phase 78 fields with stable unavailable defaults.
- New DTOs cannot represent header-only, block-download-only, retry, report, or in-flight activity as credited progress.
- Existing node status constructors compile; downstream constructor fallout is covered by Plan 78-07.
- New Rust source has a parity breadcrumb entry.
</success_criteria>

<output>
After completion, create `.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-01-SUMMARY.md`.
</output>
