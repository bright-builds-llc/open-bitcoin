---
phase: 78-progress-guarantees-and-stall-diagnosis
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - packages/open-bitcoin-node/src/sync/progress.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync.rs
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 78-2026-06-16T14-21-42
generated_at: 2026-06-16T16:06:11Z
must_haves:
  truths:
    - "Runtime projection credits progress only from validated durable active-chain evidence or current-at-best-known-tip stay-current evidence."
    - "Header downloads, block responses, peer messages, retries, and stale in-flight work are rejected activity, not progress credit."
    - "Typed stall diagnosis gives storage/recovery evidence precedence over peer retry advice and separates network, peer, validation, at-tip, operator stop, and local shutdown causes."
  artifacts:
    - path: packages/open-bitcoin-node/src/sync/progress.rs
      provides: "Pure progress-credit and stall-diagnosis classifiers"
    - path: packages/open-bitcoin-node/src/sync/runtime_state.rs
      provides: "DurableSyncRuntime projection into shared Phase 78 fields"
    - path: packages/open-bitcoin-node/src/sync.rs
      provides: "Status-derived compact structured log for progress guarantees"
  key_links:
    - from: packages/open-bitcoin-node/src/sync/runtime_state.rs
      to: packages/open-bitcoin-node/src/status/progress_guarantee.rs
      via: "FieldAvailability<ProgressCreditEvidence> and FieldAvailability<StallDiagnosisEvidence>"
      pattern: "progress_credit"
    - from: packages/open-bitcoin-node/src/sync.rs
      to: packages/open-bitcoin-node/src/sync/runtime_state.rs
      via: "durable_sync_state_from_summary result logs status-derived progress/stall evidence"
      pattern: "write_progress_guarantee_log"
---

<objective>
Project Phase 78 progress guarantees and stall diagnosis from runtime evidence.

Purpose: The shared fields from Plan 78-01 must be populated from durable active-chain, stay-current, peer, resource, recovery, and stop-reason evidence, not from renderer strings or raw message counters.
Output: Pure classifier helpers, conservative `last_successful_progress_unix_seconds` semantics, shared status projection, and compact structured-log evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
@.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-RESEARCH.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-03-SUMMARY.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-05-SUMMARY.md
@.planning/phases/76-disk-and-resource-bound-enforcement/76-03-SUMMARY.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-01-SUMMARY.md
@packages/open-bitcoin-node/src/sync/progress.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/types.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@packages/open-bitcoin-node/src/sync/tip.rs

<interfaces>
Existing no-progress classifier:
```rust
pub(super) struct NoProgressInput<'a> {
    pub(super) stay_current: Option<StayCurrentStatus>,
    pub(super) progress_signal: Option<SyncProgressSignal>,
    pub(super) recovery_category: Option<SyncRecoveryCategory>,
    pub(super) blocks_in_flight: u64,
    pub(super) maybe_reconcile_progress: Option<&'a SyncReconcileProgress>,
    pub(super) peer_outcomes: &'a [PeerSyncOutcome],
}

pub(super) fn classify_no_progress(input: &NoProgressInput<'_>) -> NoProgressDiagnosis;
```

Existing runtime projection risk:
```rust
made_useful_progress: summary.headers_received > 0 || summary.blocks_received > 0,
```
Phase 78 must replace that with validated durable active-chain progress evidence per D-01 through D-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure progress-credit and stall-diagnosis helpers</name>
  <files>packages/open-bitcoin-node/src/sync/progress.rs</files>
  <read_first>
  - packages/open-bitcoin-node/src/sync/progress.rs
  - packages/open-bitcoin-node/src/sync/types.rs
  - packages/open-bitcoin-node/src/status/progress_guarantee.rs
  - packages/open-bitcoin-node/src/status/recovery.rs
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <behavior>
  - Given a validated active-chain height/hash/work and a source timestamp, return `ProgressCreditKind::ValidatedDurableActiveChain`.
  - Given `StayCurrentStatus::CurrentAtBestKnownTip` plus matching best-known tip and connected active-chain evidence, return `ProgressCreditKind::CurrentAtBestKnownTip`.
  - Given only headers, block responses, messages, in-flight requests, retry/backoff, or report projection, return unavailable progress credit and list rejected activity.
  - Given storage or resource recovery category, return `StalledSubsystem::StorageOrResourcePressure` before any peer subsystem.
  </behavior>
  <action>
  Extend `NoProgressInput` to include `maybe_stop_reason: Option<SyncStopReason>`. Add a neighboring pure input `ProgressGuaranteeInput<'a>` with these exact fields: `sync_progress: &'a SyncProgress`, `stay_current: StayCurrentStatus`, `best_known_tip: Option<&'a BestKnownTipStatus>`, `progress_signal: Option<SyncProgressSignal>`, `recovery_category: Option<SyncRecoveryCategory>`, `blocks_in_flight: u64`, `maybe_reconcile_progress: Option<&'a SyncReconcileProgress>`, `peer_outcomes: &'a [PeerSyncOutcome]`, `maybe_stop_reason: Option<SyncStopReason>`, `retry_backoff_seconds: u64`, `max_sync_rounds: u64`, `tip_freshness_threshold_seconds: u64`, `maybe_previous_credit: Option<&'a ProgressCreditEvidence>`, and `evaluated_at_unix_seconds: u64`. Keep `retry_backoff_seconds` as `u64` because `ProgressWindowEvidence` exposes unsigned seconds; runtime projection must use a checked conversion from the existing `progress::retry_backoff_seconds(self.config.retry_backoff_ms)` helper rather than changing that helper's `i64` signature.
  Add these exact helper functions:
  - `classify_progress_credit(input: &ProgressGuaranteeInput<'_>) -> FieldAvailability<ProgressCreditEvidence>`
  - `derive_last_useful_work(input: &ProgressGuaranteeInput<'_>, maybe_current_credit: Option<&ProgressCreditEvidence>) -> FieldAvailability<ProgressCreditEvidence>`
  - `derive_progress_window(input: &ProgressGuaranteeInput<'_>) -> FieldAvailability<ProgressWindowEvidence>`
  - `derive_no_progress_threshold(input: &ProgressGuaranteeInput<'_>, maybe_last_useful_work: Option<&ProgressCreditEvidence>) -> FieldAvailability<NoProgressThresholdEvidence>`
  - `derive_last_peer_contribution(input: &ProgressGuaranteeInput<'_>) -> FieldAvailability<PeerContributionEvidence>`
  - `classify_stall_diagnosis(input: &ProgressGuaranteeInput<'_>, diagnosis: NoProgressDiagnosis) -> FieldAvailability<StallDiagnosisEvidence>`
  Mapping rules must be exact: `AddressResolution`, `Connect`, and `Network` map to `PublicNetworkReachability`; `Compatibility` and `InvalidMagic` map to `IncompatiblePeers`; `Stall`, `RetryBackoff`, and `BlockNotFound` map to `SlowOrStalledPeers`; `InvalidBlock`, `MalformedBlock`, `InvalidData`, `DisconnectedBlock`, and `NonExtendingBlock` map to `Validation`; `NoProgressDiagnosis::StaleInflightCleanup` maps to `StaleInflightCleanup`; `BranchCompetitionAwaitingBodies` maps to `BranchCompetitionAwaitingBodies`; `CurrentAtBestKnownTip` maps to `AtTipWaiting`; `SyncStopReason::OperatorPaused` maps to `OperatorStop`; `SyncStopReason::ShutdownRequested` maps to `LocalShutdown`; storage/resource recovery categories map to `StorageOrResourcePressure` first per D-08.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase78_progress_guarantee_classifier --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "struct ProgressGuaranteeInput" packages/open-bitcoin-node/src/sync/progress.rs`
  - `rg -n "fn classify_progress_credit|fn classify_stall_diagnosis|fn derive_no_progress_threshold" packages/open-bitcoin-node/src/sync/progress.rs`
  - `rg -n "StalledSubsystem::StorageOrResourcePressure" packages/open-bitcoin-node/src/sync/progress.rs`
  - `rg -n "PeerFailureReason::InvalidBlock|PeerFailureReason::Compatibility|PeerFailureReason::AddressResolution" packages/open-bitcoin-node/src/sync/progress.rs`
  - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib phase78_progress_guarantee_classifier --all-features`
  </acceptance_criteria>
  <done>Pure helpers encode the Phase 78 credit and stall taxonomy without I/O, renderer strings, or public-network assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project shared evidence from durable runtime state</name>
  <files>packages/open-bitcoin-node/src/sync/runtime_state.rs</files>
  <read_first>
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/progress.rs
  - packages/open-bitcoin-node/src/sync/tip.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <behavior>
  - Runtime `SyncStatus.progress_credit` is available only for validated durable active-chain progress or explicit current-at-tip evidence.
  - Runtime `SyncStatus.last_successful_progress_unix_seconds` is set from `last_useful_work.source_unix_seconds` or carried from previous durable state; it is not set from header/block contribution counters.
  - `tip::classify_stay_current` receives `made_useful_progress` from validated durable progress, not from `summary.headers_received` or `summary.blocks_received`.
  </behavior>
  <action>
  In `DurableSyncRuntime::durable_sync_state_from_summary`, compute `maybe_previous_credit` from `metadata.maybe_sync_state.as_ref()` with explicit pattern matching on `previous_state.sync.last_useful_work`; `FieldAvailability<T>` has associated constructors but no shared instance accessor. Use this pattern or a local helper named `maybe_available_ref` in `runtime_state.rs`:
  ```rust
  let maybe_previous_credit = metadata.maybe_sync_state.as_ref().and_then(|previous_state| {
      match &previous_state.sync.last_useful_work {
          FieldAvailability::Available(value) => Some(value),
          FieldAvailability::Unavailable { .. } => None,
      }
  });
  ```
  Compute `made_validated_durable_progress` by comparing the current available `sync.sync_progress.validated_active_chain_height`, hash, and work against the previous state's `last_useful_work` height/hash/work; treat a current-at-tip credit separately after `stay_current` is classified. Replace the exact `made_useful_progress: summary.headers_received > 0 || summary.blocks_received > 0` with `made_useful_progress: made_validated_durable_progress`.
  Build a `ProgressGuaranteeInput` using a checked conversion from the existing helper:
  ```rust
  let retry_backoff_seconds = u64::try_from(super::progress::retry_backoff_seconds(
      self.config.retry_backoff_ms,
  ))
  .unwrap_or(u64::MAX);
  ```
  then use `retry_backoff_seconds`, `self.config.max_rounds as u64`, and `self.config.tip_freshness_threshold_seconds`. Populate all six fields on `sync`: `progress_credit`, `expected_progress_window`, `no_progress_threshold`, `last_useful_work`, `last_peer_contribution`, and `stall_diagnosis`. Set `sync.last_successful_progress_unix_seconds` from `last_useful_work.source_unix_seconds` when available; otherwise carry the previous durable timestamp exactly as existing code does. Preserve D-03 by never using `SyncReconcileProgress::BranchCompetitionAwaitingBodies` as active-chain credit.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase78_progress_guarantee_projection --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "made_validated_durable_progress" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `rg -n "sync\\.progress_credit|sync\\.last_useful_work|sync\\.stall_diagnosis" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `! rg -n "summary\\.headers_received > 0 \\|\\| summary\\.blocks_received > 0" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `rg -n "u64::try_from\\(.*retry_backoff_seconds|retry_backoff_seconds = u64::try_from" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `! rg -n "last_useful_work\\.available\\(" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase78_progress_guarantee_projection --all-features`
  </acceptance_criteria>
  <done>Durable runtime status projection fills Phase 78 fields from typed runtime facts and removes header/block counters as useful-work credit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add status-derived structured log evidence</name>
  <files>packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs</files>
  <read_first>
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/logging.rs
  - .planning/phases/78-progress-guarantees-and-stall-diagnosis/78-CONTEXT.md
  </read_first>
  <behavior>
  - Structured logs contain compact Phase 78 status-derived labels after the shared status projection is computed.
  - Existing numeric metrics remain based on `MetricKind::ValidatedActiveChainHeight`, not headers or downloaded-only block counts.
  - Log-write failure continues to become a health signal before durable status is persisted.
  </behavior>
  <action>
  Add `DurableSyncRuntime::write_progress_guarantee_log(&self, state: &mut DurableSyncState, timestamp: i64)` in `sync/runtime_state.rs`. It must append one `StructuredLogRecord { source: "sync", level: Info }` with a message containing these exact keys: `progress_credit=`, `last_useful_work=`, `last_peer_contribution=`, `no_progress_threshold=`, `stalled_subsystem=`, and `stall_confidence=`. Derive labels only from `state.sync.progress_credit`, `state.sync.last_useful_work`, `state.sync.last_peer_contribution`, `state.sync.no_progress_threshold`, and `state.sync.stall_diagnosis`. In `sync.rs`, compute `let mut state = self.durable_sync_state_from_summary(...)`, call `self.write_progress_guarantee_log(&mut state, timestamp)`, and then call `self.persist_durable_sync_state(state)`. If the append fails, push `super::progress::log_write_failed_signal(&error)` into `state.health_signals` before persistence. Do not add metric dimensions or log raw peer tables.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase78_progress_guarantee_projection --all-features</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "write_progress_guarantee_log" packages/open-bitcoin-node/src/sync.rs packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `rg -n "progress_credit=.*last_useful_work=.*last_peer_contribution=.*no_progress_threshold=.*stalled_subsystem=.*stall_confidence=" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  - `rg -n "MetricKind::ValidatedActiveChainHeight" packages/open-bitcoin-node/src/sync/types/summary.rs`
  - `! rg -n "peer table|raw peer|credential|rpcpassword" packages/open-bitcoin-node/src/sync/runtime_state.rs`
  </acceptance_criteria>
  <done>Metrics/log summaries remain compact and status-derived for Phase 78 without adding public-network checks or raw local evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer/runtime summary -> shared status | Untrusted peer outcomes and local runtime state become operator-visible diagnosis. |
| prior durable status -> current status | Previous credit may be carried forward when no new useful work occurs. |
| shared status -> structured logs | Compact diagnosis labels are emitted into local log files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02-01 | Tampering | `classify_progress_credit` | mitigate | Require validated active-chain height/hash/work or current-at-tip evidence; rejected activity records headers, downloads, messages, retries, reports, and in-flight work. |
| T-78-02-02 | Repudiation | `last_successful_progress_unix_seconds` | mitigate | Source timestamp from `last_useful_work` only and carry previous durable value when no new credit exists. |
| T-78-02-03 | Denial of Service | `classify_stall_diagnosis` | mitigate | Apply storage/resource and recovery precedence before peer retry advice so operators receive the blocking local action first. |
| T-78-02-04 | Information Disclosure | progress guarantee structured log | mitigate | Log bounded labels and counters only; do not log raw peers, credentials, wallet data, daemon output, or support-bundle bodies. |
</threat_model>

<verification>
Run the focused node classifier/projection tests and cargo check for node after implementation. Full deterministic and cross-surface verification is planned in 78-05 and 78-06.
</verification>

<success_criteria>
- Header and block counters no longer drive `made_useful_progress` or the useful-work timestamp in runtime projection.
- Phase 78 status fields are populated from shared pure helpers.
- Storage/resource evidence outranks peer retry advice.
- A compact structured log line reflects shared Phase 78 status evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/78-progress-guarantees-and-stall-diagnosis/78-02-SUMMARY.md`.
</output>
