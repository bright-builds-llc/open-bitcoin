---
phase: 27-operator-runtime-benchmark-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-bench/src/cases/operator_runtime.rs
  - packages/open-bitcoin-bench/src/registry.rs
  - docs/parity/benchmarks.md
  - docs/parity/catalog/operator-runtime-release-hardening.md
autonomous: true
requirements:
  - VER-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-04-28T20-48-00
generated_at: 2026-04-28T21:02:00.000Z

must_haves:
  truths:
    - "The dashboard benchmark case collects through `collect_dashboard_snapshot()` rather than direct projection from a sample snapshot."
    - "The benchmark registry and docs no longer describe `operator-runtime` as shared-snapshot projection."
    - "Case IDs and validator expectations stay stable while fixture metadata becomes more truthful."
  artifacts:
    - path: "packages/open-bitcoin-bench/src/cases/operator_runtime.rs"
      provides: "runtime-collected dashboard benchmark path"
    - path: "packages/open-bitcoin-bench/src/registry.rs"
      provides: "truthful operator-runtime group description and notes"
    - path: "docs/parity/benchmarks.md"
      provides: "updated contributor-facing benchmark contract"
  key_links:
    - from: "packages/open-bitcoin-bench/src/cases/operator_runtime.rs"
      to: "packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"
      via: "`collect_dashboard_snapshot()`"
    - from: "packages/open-bitcoin-bench/src/registry.rs"
      to: "docs/parity/benchmarks.md"
      via: "runtime-collected operator-runtime benchmark narrative"
---

# Phase 27 Plan 02: Dashboard Fidelity And Metadata Refresh

## Objective

Upgrade the operator-runtime dashboard benchmark case to collect its snapshot
through the real dashboard runtime path and refresh the benchmark metadata and
docs so they describe the new behavior honestly.

## Scope

- `operator-runtime.dashboard-projection`
- benchmark group description and notes
- parity benchmark docs for the operator-runtime group

## Tasks

1. Route `operator-runtime.dashboard-projection` through
   `collect_dashboard_snapshot()` and preserve the existing chart/section/action
   assertions.
2. Update the case fixture labels and descriptions so the report metadata
   describes runtime-collected operator snapshots instead of a shared sample
   snapshot.
3. Update the benchmark registry and parity benchmark docs to match the new
   fidelity level.

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture`
- `bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports`
- `bun scripts/check-benchmark-report.ts --report=packages/target/benchmark-reports/open-bitcoin-bench-smoke.json`
