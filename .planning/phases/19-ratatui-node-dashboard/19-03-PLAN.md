---
phase: 19-ratatui-node-dashboard
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:

  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/mod.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/action.rs
  - packages/open-bitcoin-cli/src/operator/service.rs
  - packages/open-bitcoin-cli/Cargo.toml
  - packages/open-bitcoin-cli/BUILD.bazel
  - docs/parity/source-breadcrumbs.json
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - packages/open-bitcoin-cli/src/operator/tests.rs

requirements: [DASH-03, DASH-04]
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-27T09-02-20
generated_at: "2026-04-27T05:00:00Z"
must_haves:
  truths:

    - TTY detection selects interactive mode; non-TTY and JSON select deterministic snapshot mode.
    - Service-affecting actions execute only from explicit confirmation state.
    - `execute_service_command` remains the only service mutation path.
    - No ANSI output is emitted in snapshot/JSON mode.
    - Dashboard crate dependencies and parity metadata are fully wired.
  artifacts:

    - "path: packages/open-bitcoin-cli/src/operator/runtime.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/mod.rs"
    - "path: packages/open-bitcoin-cli/src/operator/dashboard/action.rs"
    - "path: packages/open-bitcoin-cli/tests/operator_binary.rs"
    - "path: docs/parity/source-breadcrumbs.json"
  key_links:

    - "from: packages/open-bitcoin-cli/src/operator/runtime.rs"
    - "from: packages/open-bitcoin-cli/src/operator/dashboard/action.rs"
    - "from: packages/open-bitcoin-cli/src/operator/tests.rs"

---
<objective>
Complete dashboard runtime behavior by wiring mode selection, safe action execution, and build/parity metadata so interactive and non-interactive dashboard invocations are both stable and tested.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-ratatui-node-dashboard/19-CONTEXT.md
@.planning/phases/19-ratatui-node-dashboard/19-RESEARCH.md

@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/src/operator/dashboard/mod.rs
@packages/open-bitcoin-cli/src/operator/dashboard/action.rs
@packages/open-bitcoin-cli/src/operator/service.rs
@packages/open-bitcoin-cli/src/operator/tests.rs
</context>

<interfaces>
From `packages/open-bitcoin-cli/src/operator/runtime.rs`:

```rust
pub fn execute_operator_cli_inner(
    cli: OperatorCli,
    default_data_dir: PathBuf,
) -> Result<OperatorCommandOutcome, OperatorRuntimeError>;
```

From `packages/open-bitcoin-cli/src/operator/dashboard/mod.rs`:

```rust
pub fn run_dashboard(
    args: DashboardArgs,
    runtime_ctx: &RuntimeContext,
) -> OperatorCommandOutcome;
```

From `packages/open-bitcoin-cli/src/operator/dashboard/action.rs`:

```rust
pub fn confirm_and_execute(
    action_state: &DashboardActionState,
    args: &DashboardArgs,
    cli: &OperatorCli,
) -> Result<DashboardActionState, OperatorRuntimeError>;
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire runtime dispatch with TTY and snapshot mode selection</name>
  <files>packages/open-bitcoin-cli/src/operator/runtime.rs, packages/open-bitcoin-cli/src/operator/dashboard/mod.rs</files>
  <behavior>

    - TTY-capable terminals select interactive mode; non-TTY/JSON selects snapshot mode.
    - Non-interactive mode remains deterministic and parseable.
    - Dashboard command leaves deferred branch completely replaced.
  </behavior>
  <action>

1. Replace `OperatorCommand::Dashboard` deferred branch with call to `run_dashboard`.
2. Add guarded TTY/format capability checks in runtime and avoid terminal mode for script contexts.
3. Keep status collection and service-manager dependency construction in runtime; pass into dashboard shell.
4. Ensure snapshot mode disables ANSI and emits explicit unavailable labels.

Decision coverage: D-01, D-11, D-08.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- tests::operator_dashboard_command_routing</automated>
  </verify>
  <done>Dashboard command executes through runtime and no longer returns a deferred placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce confirmation-gated service actions through service contracts</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/action.rs, packages/open-bitcoin-cli/src/operator/service.rs, packages/open-bitcoin-cli/src/operator/dashboard/mod.rs</files>
  <behavior>

    - Destructive/service-affecting actions need explicit confirmation transition.
    - Confirmed actions execute only via `execute_service_command`.
    - Rejected actions return to idle with no manager calls.
  </behavior>
  <action>

1. In action module, keep `ActionDecision` transitions explicit.
2. Implement/extend `confirm_and_execute` to call `execute_service_command` only on confirmed state.
3. Build action summary strings that include target/scope/effect.
4. Add unit tests for confirm/cancel transitions and no-op path.

Decision coverage: D-09, D-10.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --package open-bitcoin-cli --all-features -- operator::dashboard::action</automated>
  </verify>
  <done>All service-affecting actions are blocked until explicitly confirmed and executed through existing abstractions.</done>
</task>

<task type="auto">
  <name>Task 3: Add dependency/breadcrumb metadata and non-interactive CLI assertions</name>
  <files>packages/open-bitcoin-cli/Cargo.toml, packages/open-bitcoin-cli/BUILD.bazel, docs/parity/source-breadcrumbs.json, packages/open-bitcoin-cli/src/operator/tests.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <behavior>

    - Ratatui/Crossterm dependencies are added to both build systems.
    - Dashboard CLI tests assert no deferred response and valid snapshot output.
    - Parity manifest includes new dashboard module files.
  </behavior>
  <action>

1. Update `Cargo.toml` and `BUILD.bazel` for `ratatui` and `crossterm`.
2. Extend command tests to confirm snapshot path and JSON-safe output for dashboard command.
3. Add integration test for `open-bitcoin dashboard --format json` and assert no ANSI escape codes.
4. Add parity breadcrumb record covering `packages/open-bitcoin-cli/src/operator/dashboard/{mod.rs,app.rs,action.rs,model.rs,charts.rs}`.

Decision coverage: D-03, D-12.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo fmt --all && cargo clippy --package open-bitcoin-cli --all-targets --all-features -- -D warnings && cargo test --package open-bitcoin-cli --all-features</automated>
  </verify>
  <done>Build metadata and test coverage lock snapshot and runtime behavior for both interactive and non-interactive dashboard modes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard key event → action executor | Input can trigger operations; destructive actions need confirmation |
| Runtime mode selection → output stream | TTY detection controls terminal behavior vs machine-readable output |
| Service action executor → ServiceManager | Service state changes must stay within existing contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | unconfirmed action state | mitigate | explicit confirm/cancel finite states |
| T-19-10 | Information Disclosure | snapshot output | accept | snapshot contract already excludes secrets; keep plain output free of escape codes |
| T-19-11 | Denial of Service | terminal mode failure | mitigate | fallback to text snapshot mode on terminal init failure |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --package open-bitcoin-cli --all-targets --all-features -- -D warnings
cargo test --package open-bitcoin-cli --all-features
</verification>

<success_criteria>

- Dashboard command routes to runtime and no longer shows the deferred placeholder.
- Snapshot mode is deterministic and parseable in non-interactive and JSON cases.
- Service-affecting actions require explicit confirmation and delegate through existing service command contracts.
- Ratatui/Crossterm dependencies and parity breadcrumb manifests are updated.

</success_criteria>

<output>
After completion, create `.planning/phases/19-ratatui-node-dashboard/19-03-SUMMARY.md`.
</output>
