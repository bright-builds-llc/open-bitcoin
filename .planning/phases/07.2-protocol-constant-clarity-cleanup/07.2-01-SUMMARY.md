---
phase: 07.2-protocol-constant-clarity-cleanup
plan: 01
subsystem: consensus-network-primitives
tags: [rust, consensus, networking, primitives, constants]
requires:
  - phase: 07.1
    provides: maintainability cleanup before interface work so the remaining protocol-clarity pass stayed narrow and auditable
provides:
  - type-owned serialized-size constants for outpoints and inventory vectors
  - named consensus default parameters instead of duplicated protocol literals
  - opcode-domain `OP_SUCCESS` allowlist coverage with explicit boundary assertions
affects: [phase-08-interface-parity, consensus, networking, wire-encoding]
tech-stack:
  added: []
  patterns:
    - Type-owned wire-size constants for shared serialized structures
    - Opcode-domain naming for consensus-sensitive allowlists
key-files:
  created: []
  modified:
    - packages/open-bitcoin-primitives/src/transaction.rs
    - packages/open-bitcoin-primitives/src/network.rs
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/sighash.rs
    - packages/open-bitcoin-consensus/src/script/opcodes.rs
    - packages/open-bitcoin-consensus/src/script/tests.rs
    - packages/open-bitcoin-network/src/message.rs
    - packages/open-bitcoin-network/src/message/tests.rs
key-decisions:
  - Shared serialized-size constants belong on the owning wire types, not in unrelated helper modules.
  - `ConsensusParams::default` should reuse `TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY` and name the remaining protocol defaults explicitly.
  - The Taproot `OP_SUCCESS` allowlist should stay behavior-neutral while being expressed in opcode-domain terms and guarded with edge assertions.
patterns-established:
  - "When the same wire-size literal appears across callers, introduce a type-owned `SERIALIZED_LEN` constant on the owning primitive."
  - "Consensus opcode allowlists should prefer named singleton opcodes plus hex ranges over bare decimal ranges."
requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.2-2026-04-19T01-23-43
generated_at: 2026-04-19T01:27:40.562Z
duration: 4 min
completed: 2026-04-19
---

# Phase 07.2 Plan 01: Protocol Constant Clarity Cleanup Summary

**The audited residual protocol literals now resolve through named domain constants, and the Taproot `OP_SUCCESS` allowlist is expressed in opcode-domain terms with direct edge coverage.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-19T01:23:43Z
- **Completed:** 2026-04-19T01:27:40Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Added `OutPoint::SERIALIZED_LEN` and `InventoryVector::SERIALIZED_LEN`, then switched the audited prevout and inventory decode paths to use them instead of duplicated `36`-byte literals.
- Replaced the inline consensus default literals in [`packages/open-bitcoin-consensus/src/context.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-consensus/src/context.rs) with named constants and reused `TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY`.
- Rewrote the Taproot `OP_SUCCESS` predicate in [`packages/open-bitcoin-consensus/src/script/opcodes.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-consensus/src/script/opcodes.rs) using named singleton opcodes plus hex ranges, and added explicit boundary assertions in [`packages/open-bitcoin-consensus/src/script/tests.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-consensus/src/script/tests.rs).
- Added a direct inventory-payload decode regression in [`packages/open-bitcoin-network/src/message/tests.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-network/src/message/tests.rs) to satisfy the repo’s coverage gate after the size-constant extraction.

## Task Commits

No intermediate task commits were created. The plan was executed in the current workspace and verified in-place.

## Files Created/Modified

- `packages/open-bitcoin-primitives/src/transaction.rs` - adds `OutPoint::SERIALIZED_LEN`
- `packages/open-bitcoin-primitives/src/network.rs` - adds `InventoryVector::SERIALIZED_LEN`
- `packages/open-bitcoin-consensus/src/context.rs` - names consensus defaults and reuses the existing sequence-locktime granularity constant
- `packages/open-bitcoin-consensus/src/sighash.rs` - sizes prevout hashing buffers through `OutPoint::SERIALIZED_LEN`
- `packages/open-bitcoin-consensus/src/script/opcodes.rs` - names the singleton `OP_SUCCESS` opcodes and expresses the allowlist in hex ranges
- `packages/open-bitcoin-consensus/src/script/tests.rs` - adds `OP_SUCCESS` boundary assertions
- `packages/open-bitcoin-network/src/message.rs` - decodes inventory entries through `InventoryVector::SERIALIZED_LEN`
- `packages/open-bitcoin-network/src/message/tests.rs` - exercises direct inventory payload decoding for the extracted size constant

## Decisions Made

- Kept the cleanup scoped to the audited residual hotspots rather than turning it into a repo-wide literal sweep.
- Preferred type-owned constants over a shared global constants module so the meaning stays with the wire type or consensus domain that owns it.

## Deviations from Plan

Minor execution deviation: the network coverage gate needed one small direct decode test and a clearer local variable split in `decode_inventory_payload`. This stayed within the plan’s stated regression-guard scope and did not change behavior.

## Issues Encountered

- `bash scripts/verify.sh` initially failed because LLVM coverage reported one uncovered line in the multi-line `decode_inventory_payload` expression after the `InventoryVector::SERIALIZED_LEN` extraction. Splitting the expression into explicit local bindings and adding a direct unit test resolved the coverage gap.

## User Setup Required

None - no external services, config changes, or manual migration steps are required.

## Next Phase Readiness

- The consensus, primitives, and networking crates now expose clearer protocol intent before Phase 8 adds CLI and RPC surfaces.
- The finished todo can move out of pending because the audited residual gaps are closed.
- No new blockers were introduced for Phase 8.

## Self-Check: PASSED

- Verified the modified files contain the new serialized-size and protocol-default constants.
- Verified `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `bash scripts/verify.sh` all exited successfully.
