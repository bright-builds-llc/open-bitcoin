# Phase 07.2: Protocol Constant Clarity Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 07.2-protocol-constant-clarity-cleanup
**Mode:** Yolo
**Areas discussed:** scope and targeting, constant placement, verification and regression guards

---

## Scope and Targeting

| Option | Description | Selected |
|--------|-------------|----------|
| Broad sweep | Scan the repo for all inline numeric literals and replace aggressively | |
| Targeted residual cleanup | Limit the phase to the already-audited protocol-significant hotspots | ✓ |
| Defer entirely | Leave the todo open until after Phase 8 | |

**User's choice:** Yolo-selected recommended option: targeted residual cleanup.  
**Notes:** Keep the phase narrow to the audited non-test hotspots so it stays behavior-neutral and does not expand into a repo-wide readability sweep.

---

## Constant Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Global constants module | Centralize all extracted numeric values into one shared constants file | |
| Type-owned/domain-owned constants | Put serialized sizes and protocol defaults on the owning type or module | ✓ |
| Local variables only | Replace each literal with a local variable at the call site | |

**User's choice:** Yolo-selected recommended option: type-owned/domain-owned constants.  
**Notes:** `OutPoint::SERIALIZED_LEN`, `InventoryVector::SERIALIZED_LEN`, and named consensus defaults keep the meaning near the domain object instead of scattering generic constants.

---

## Verification and Regression Guards

| Option | Description | Selected |
|--------|-------------|----------|
| No new tests | Trust existing coverage and only refactor constants | |
| Boundary assertions only | Add narrow tests where extracted constants could silently change behavior | ✓ |
| Broad new suites | Add large new regression suites for all touched modules | |

**User's choice:** Yolo-selected recommended option: boundary assertions only.  
**Notes:** The `OP_SUCCESS` allowlist needs explicit edge coverage, but the rest of the phase should rely on existing crate and repo verification instead of inventing cosmetic tests.

## the agent's Discretion

- Exact constant names, as long as they encode protocol meaning directly and stay local to the owning type or module.

## Deferred Ideas

- Repo-wide magic-number cleanup outside the audited hotspots
- Early-return readability sweeps
- Panic and illegal-state hardening passes
