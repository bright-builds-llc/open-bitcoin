---
phase: 07.2-protocol-constant-clarity-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-primitives/src/transaction.rs
  - packages/open-bitcoin-primitives/src/network.rs
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/sighash.rs
  - packages/open-bitcoin-consensus/src/script/opcodes.rs
  - packages/open-bitcoin-consensus/src/script/tests.rs
  - packages/open-bitcoin-network/src/message.rs
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.2-2026-04-19T01-23-43
generated_at: 2026-04-19T01:23:43.085Z
must_haves:
  truths:
    - The audited protocol-significant magic numbers are replaced with named
      constants or type-owned serialized-size constants in the touched
      production paths.
    - The Taproot `OP_SUCCESS` predicate remains behaviorally identical while
      becoming easier to audit in opcode-domain terms.
    - Shared `36`-byte wire-size literals now resolve through `OutPoint` and
      `InventoryVector` instead of being duplicated across callers.
  artifacts:
    - packages/open-bitcoin-primitives/src/transaction.rs
    - packages/open-bitcoin-primitives/src/network.rs
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/sighash.rs
    - packages/open-bitcoin-consensus/src/script/opcodes.rs
    - packages/open-bitcoin-consensus/src/script/tests.rs
    - packages/open-bitcoin-network/src/message.rs
  key_links:
    - `ConsensusParams::default` reuses
      `TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY`.
    - `PrecomputedTransactionData::new` and `sha256_prevouts` size prevout
      buffers through `OutPoint::SERIALIZED_LEN`.
    - `decode_inventory_payload` reads inventory entries through
      `InventoryVector::SERIALIZED_LEN`.
---

<objective>
Replace the remaining audited protocol-significant magic numbers and duplicated
serialized-size literals with named constants, without widening the cleanup
beyond the confirmed hotspots.

Purpose: make the consensus and networking code easier to audit before Phase 8
layers CLI and RPC work on top of it.
Output: named protocol constants in the audited hot spots, shared wire-size
constants on the owning types, and unchanged behavior under the repo-native
verification contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-04-18-sweep-magic-numbers-for-clarity.md
@.planning/phases/07.2-protocol-constant-clarity-cleanup/07.2-CONTEXT.md
@.planning/phases/07.2-protocol-constant-clarity-cleanup/07.2-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-07.2-01:** Named consensus and opcode constants -> consensus-validation
  behavior and Taproot acceptance or rejection.
- **TB-07.2-02:** Shared wire-size constants -> prevout hashing and network
  inventory decoding paths.

### Threat Register
- **Threat ID:** T-07.2-01
  - **Category:** Tampering
  - **Component:** consensus defaults and opcode allowlist
  - **Disposition:** mitigate
  - **Mitigation Plan:** Keep extracted constants local to the owning modules,
    preserve the exact values and ranges, and add boundary assertions for the
    `OP_SUCCESS` predicate.
- **Threat ID:** T-07.2-02
  - **Category:** Denial of service
  - **Component:** serialized-size constants reused across callers
  - **Disposition:** mitigate
  - **Mitigation Plan:** Anchor wire-size constants on `OutPoint` and
    `InventoryVector`, switch only the audited call sites, and require targeted
    crate tests plus `bash scripts/verify.sh`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Introduce type-owned wire-size constants and named consensus defaults</name>
  <files>packages/open-bitcoin-primitives/src/transaction.rs, packages/open-bitcoin-primitives/src/network.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/sighash.rs, packages/open-bitcoin-network/src/message.rs</files>
  <read_first>packages/open-bitcoin-primitives/src/transaction.rs, packages/open-bitcoin-primitives/src/network.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/sighash.rs, packages/open-bitcoin-network/src/message.rs, .planning/phases/07.2-protocol-constant-clarity-cleanup/07.2-CONTEXT.md</read_first>
  <action>Add `OutPoint::SERIALIZED_LEN` to `packages/open-bitcoin-primitives/src/transaction.rs` and `InventoryVector::SERIALIZED_LEN` to `packages/open-bitcoin-primitives/src/network.rs`. In `packages/open-bitcoin-consensus/src/context.rs`, add named constants for the default coinbase maturity (`100`) and the locktime timestamp threshold (`500_000_000`), and change `ConsensusParams::default` so `sequence_locktime_granularity` uses `TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY as u32` instead of a duplicated `9`. Then replace the audited callers so `PrecomputedTransactionData::new` and `sha256_prevouts` size prevout buffers with `OutPoint::SERIALIZED_LEN`, and `decode_inventory_payload` reads entries with `InventoryVector::SERIALIZED_LEN` instead of bare `36`.</action>
  <verify>`bash -lc 'rg -n "SERIALIZED_LEN" packages/open-bitcoin-primitives/src/transaction.rs packages/open-bitcoin-primitives/src/network.rs >/dev/null && rg -n "DEFAULT_COINBASE_MATURITY|LOCKTIME_TIMESTAMP_THRESHOLD|TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY as u32" packages/open-bitcoin-consensus/src/context.rs >/dev/null && rg -n "OutPoint::SERIALIZED_LEN" packages/open-bitcoin-consensus/src/context.rs packages/open-bitcoin-consensus/src/sighash.rs >/dev/null && rg -n "InventoryVector::SERIALIZED_LEN" packages/open-bitcoin-network/src/message.rs >/dev/null'`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-primitives/src/transaction.rs` defines `OutPoint::SERIALIZED_LEN`
    - `packages/open-bitcoin-primitives/src/network.rs` defines `InventoryVector::SERIALIZED_LEN`
    - `packages/open-bitcoin-consensus/src/context.rs` defines named defaults for the `100` and `500_000_000` consensus parameters
    - `packages/open-bitcoin-consensus/src/context.rs` sets `sequence_locktime_granularity` from `TransactionInput::SEQUENCE_LOCKTIME_GRANULARITY as u32`
    - `packages/open-bitcoin-consensus/src/context.rs` and `packages/open-bitcoin-consensus/src/sighash.rs` size prevout buffers with `OutPoint::SERIALIZED_LEN`
    - `packages/open-bitcoin-network/src/message.rs` decodes inventory entries with `InventoryVector::SERIALIZED_LEN`
  </acceptance_criteria>
  <done>The audited shared-size and consensus-default literals resolve through named domain constants instead of bare repeated numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Taproot OP_SUCCESS allowlist in opcode-domain terms</name>
  <files>packages/open-bitcoin-consensus/src/script/opcodes.rs, packages/open-bitcoin-consensus/src/script/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/script/opcodes.rs, packages/open-bitcoin-consensus/src/script/tests.rs, .planning/phases/07.2-protocol-constant-clarity-cleanup/07.2-RESEARCH.md</read_first>
  <action>Add named singleton opcode constants for the current bare `0x50` and `0x62` cases in `packages/open-bitcoin-consensus/src/script/opcodes.rs`, then rewrite `is_op_success` so it uses those names plus explicit hex ranges `0x7e..=0x81`, `0x83..=0x86`, `0x89..=0x8a`, `0x8d..=0x8e`, `0x95..=0x99`, and `0xbb..=0xfe`. In `packages/open-bitcoin-consensus/src/script/tests.rs`, add or extend boundary assertions that prove `is_op_success` returns true for `0x50`, `0x62`, `0x7e`, `0x81`, `0xbb`, and `0xfe`, and false for adjacent exclusions `0x7d`, `0x82`, `0xba`, and `0xff`.</action>
  <verify>`bash -lc 'rg -n "OP_RESERVED|OP_VER|0x7e..=0x81|0xbb..=0xfe" packages/open-bitcoin-consensus/src/script/opcodes.rs >/dev/null && rg -n "0x7d|0x82|0xba|0xff|0xfe|0xbb|0x62|0x50" packages/open-bitcoin-consensus/src/script/tests.rs >/dev/null'`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-consensus/src/script/opcodes.rs` defines named constants for the bare `0x50` and `0x62` opcode cases
    - `packages/open-bitcoin-consensus/src/script/opcodes.rs` expresses the `OP_SUCCESS` ranges in hex opcode-domain terms instead of decimal ranges
    - `packages/open-bitcoin-consensus/src/script/tests.rs` contains boundary assertions for included and excluded `OP_SUCCESS` edge opcodes
    - `packages/open-bitcoin-consensus/src/script/taproot.rs` continues to consume `is_op_success` without a new verifier entrypoint
  </acceptance_criteria>
  <done>The Taproot `OP_SUCCESS` allowlist is auditable by opcode meaning without changing its behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-primitives -p open-bitcoin-consensus -p open-bitcoin-network --all-targets --all-features`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-primitives -p open-bitcoin-consensus -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- [ ] `bash scripts/verify.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The audited residual magic-number findings are closed without broadening into a repo-wide literal sweep

</success_criteria>

<output>
After completion, create `.planning/phases/07.2-protocol-constant-clarity-cleanup/07.2-01-SUMMARY.md`
</output>
