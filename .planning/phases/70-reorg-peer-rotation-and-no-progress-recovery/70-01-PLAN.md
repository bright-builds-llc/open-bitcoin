---
phase: 70-reorg-peer-rotation-and-no-progress-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-cli/src/operator/status/sync_state.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/runtime/support.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [REC-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 70-2026-06-12T14-56-46
generated_at: 2026-06-12T16:35:49Z
must_haves:
  truths:
    - "Shared status has an additive `latest_reorg` field for bounded reorg evidence."
    - "Shared status has an additive `reconcile_progress` field for branch/reorg reconcile state."
    - "Older status JSON and existing CLI/RPC/dashboard/support fixtures remain readable with Phase 70 fields unavailable."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "SyncReorgEvidence, SyncReconcileProgressStatus, latest_reorg, and reconcile_progress public status contract"
      contains: "latest_reorg"
    - path: "packages/open-bitcoin-node/src/status/tests.rs"
      provides: "Serde compatibility tests for additive reorg/reconcile fields"
      contains: "phase70_sync_reorg_evidence"
    - path: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      provides: "Summary fixture/status compatibility for new unavailable fields"
      contains: "reconcile_progress"
    - path: "packages/open-bitcoin-cli/src/operator/status/sync_state.rs"
      provides: "CLI status model compatibility for additive fields"
      contains: "latest_reorg"
  key_links:
    - from: "packages/open-bitcoin-node/src/status.rs"
      to: "packages/open-bitcoin-cli/src/operator/status/sync_state.rs"
      via: "shared status model serialization"
      pattern: "latest_reorg"
    - from: "packages/open-bitcoin-node/src/status.rs"
      to: "packages/open-bitcoin-rpc/src/dispatch/tests.rs"
      via: "RPC fixture status construction"
      pattern: "reconcile_progress"
---

<objective>
Add the public Phase 70 reorg and reconcile status contract without runtime behavior changes.

Purpose: REC-02 requires bounded operator-visible reorg evidence, and the checker requires status-contract fixture work to be separate from branch/reorg runtime work.
Output: additive status fields, serde defaults, and fixture updates for existing status consumers.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-CONTEXT.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-RESEARCH.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-02-SUMMARY.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/status/tests.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs

<interfaces>
From `packages/open-bitcoin-node/src/status.rs`:
```rust
pub struct SyncStatus {
    pub best_known_tip: FieldAvailability<BestKnownTipStatus>,
    pub stay_current: FieldAvailability<StayCurrentStatus>,
    pub stay_current_next_action: FieldAvailability<String>,
}
```

Add additive fields with serde defaults. Preserve older persisted JSON compatibility by using `FieldAvailability::unavailable(...)` defaults, following Phase 69.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded reorg and reconcile status fields</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests.rs</files>
  <read_first>
    .planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-CONTEXT.md
    .planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-RESEARCH.md
    packages/open-bitcoin-node/src/status.rs
    packages/open-bitcoin-node/src/status/tests.rs
  </read_first>
  <behavior>
    - Test 1: older `SyncStatus` JSON without Phase 70 fields deserializes with `latest_reorg` and `reconcile_progress` unavailable.
    - Test 2: `SyncReorgEvidence` serializes field names `common_ancestor_height`, `common_ancestor_hash`, `disconnected_count`, `connected_count`, `final_active_height`, `final_active_hash`, and `fully_persisted`.
    - Test 3: reconcile progress serializes a branch-awaiting-bodies state without raw undo data or raw block bodies.
  </behavior>
  <action>Add `pub struct SyncReorgEvidence` to `packages/open-bitcoin-node/src/status.rs` with exactly these public fields: `common_ancestor_height: u64`, `common_ancestor_hash: String`, `disconnected_count: u64`, `connected_count: u64`, `final_active_height: u64`, `final_active_hash: String`, and `fully_persisted: bool` per D-07 and D-08. Add a public serde-safe reconcile status type named `SyncReconcileProgressStatus` with `#[serde(rename_all = "snake_case")]` variants or tagged cases that can represent `no_change`, `extended_active_chain`, `branch_competition_awaiting_bodies`, `side_branch_preserved`, and `reorg_persisted` using bounded counts and hashes only. Add `SyncStatus.latest_reorg: FieldAvailability&lt;SyncReorgEvidence&gt;` with default reason `no reorg evidence recorded`, and `SyncStatus.reconcile_progress: FieldAvailability&lt;SyncReconcileProgressStatus&gt;` with default reason `reconcile progress unavailable` per the resolved research. Do not add raw undo dumps, raw disconnect block bodies, unbounded branch arrays, or a reorg history collection.</action>
  <acceptance_criteria>
    - `rg "pub struct SyncReorgEvidence" packages/open-bitcoin-node/src/status.rs` finds the struct.
    - `rg "SyncReconcileProgressStatus|reconcile_progress" packages/open-bitcoin-node/src/status.rs` finds the public reconcile status contract.
    - `rg "latest_reorg|no reorg evidence recorded|reconcile progress unavailable" packages/open-bitcoin-node/src/status.rs` finds additive field defaults.
    - `rg "raw undo|undo dump|raw disconnect|reorg_history|reorgs: Vec" packages/open-bitcoin-node/src/status.rs` returns no Phase 70 status additions.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase70_sync_reorg_evidence --all-features</automated>
  </verify>
  <done>`SyncStatus` exposes only bounded latest reorg evidence and reconcile progress with legacy-safe defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Update downstream status fixtures for additive fields</name>
  <files>packages/open-bitcoin-node/src/sync/types/summary.rs, packages/open-bitcoin-cli/src/operator/status/sync_state.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs, packages/open-bitcoin-cli/src/operator/runtime/support.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    packages/open-bitcoin-node/src/sync/types/summary.rs
    packages/open-bitcoin-cli/src/operator/status/sync_state.rs
    packages/open-bitcoin-cli/src/operator/status/tests.rs
    packages/open-bitcoin-cli/src/operator/status/render/tests.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    packages/open-bitcoin-cli/src/operator/runtime/support.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
    packages/open-bitcoin-node/src/status.rs
  </read_first>
  <action>Update every explicit `SyncStatus` fixture/constructor in the listed files to set `latest_reorg` unavailable with `no reorg evidence recorded` and `reconcile_progress` unavailable with `reconcile progress unavailable`. Keep renderer/support behavior unchanged in this plan: fixtures should compile through the additive fields, but Plan 70-02 owns runtime projection and Plan 70-06 owns docs/checker wording. Do not add renderer-local reorg labels or CLI-only reconcile fields.</action>
  <acceptance_criteria>
    - `rg "latest_reorg" packages/open-bitcoin-node/src/sync/types/summary.rs packages/open-bitcoin-cli/src/operator/status/sync_state.rs packages/open-bitcoin-cli/src/operator/status/tests.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs packages/open-bitcoin-cli/src/operator/runtime/support.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds fixture/model wiring.
    - `rg "reconcile_progress" packages/open-bitcoin-node/src/sync/types/summary.rs packages/open-bitcoin-cli/src/operator/status/sync_state.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds additive reconcile field wiring.
    - `rg "latest reorg|reconcile progress" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/runtime/support.rs` shows no renderer-local Phase 70 wording added by this plan.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status --all-features</automated>
  </verify>
  <done>Existing status consumers compile through the additive reorg/reconcile fields without owning runtime semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable runtime metadata -> shared status | Persisted JSON from older runs can omit Phase 70 fields. |
| sync runtime -> operator status | Status fields will later carry branch/reorg evidence from effectful runtime code. |
| shared status -> CLI/RPC/dashboard/support fixtures | Downstream consumers must not invent private status contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-01 | Repudiation | `SyncStatus.latest_reorg` | mitigate | Add bounded common-ancestor, count, final-tip, and persisted-verdict fields with serde compatibility tests. |
| T-70-01-02 | Information Disclosure | reorg status contract | mitigate | Expose bounded hashes/counts only; reject raw undo payloads, raw block bodies, and unbounded histories. |
| T-70-01-03 | Tampering | `reconcile_progress` public contract | mitigate | Use a typed serde contract instead of renderer strings or unchecked string labels. |
| T-70-01-04 | Denial of Service | legacy status decode | mitigate | Add unavailable defaults so older runtime metadata does not break status loading. |
</threat_model>

<verification>
Run these before completion:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase70_sync_reorg_evidence --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status --all-features
```
</verification>

<success_criteria>
REC-02 status-contract groundwork is satisfied when shared status exposes bounded latest reorg evidence and reconcile progress, all defaults are backward-compatible, and downstream fixtures compile without runtime behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-01-SUMMARY.md`
</output>
