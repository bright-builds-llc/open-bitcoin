---
phase: 121-block-relay-metrics-and-log-runtime-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/metrics.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements:
  - OBS-03
user_setup: []
must_haves:
  truths:
    - "DurableSyncRuntime::persist_metrics appends block_relay_metric_samples when the provider returns FieldAvailability::Available(BlockRelayEvidenceStatus)"
    - "When the provider returns Unavailable (or is unset), persist_metrics emits zero block-relay MetricKind samples — no fabricated zero-series availability evidence"
    - "write_block_relay_log emits block_relay_log_record via append_structured_record when Available, and omits block_relay source records when Unavailable"
    - "Runtime tests prove projection beyond Phase 116 helper-only coverage and assert no peer/permission/credential/tx payload leakage"
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/metrics.rs"
      provides: "set_block_relay_metric_status_provider + Available-gated persist_metrics append"
      contains: "set_block_relay_metric_status_provider"
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "write_block_relay_log using shared provider + append_structured_record"
      contains: "write_block_relay_log"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "available/unavailable persist + log + leakage runtime proofs"
      contains: "persist_metrics_appends_block_relay_status_samples_with_sync_samples"
  key_links:
    - from: "set_block_relay_metric_status_provider"
      to: "persist_metrics"
      via: "FieldAvailability::Available gate before block_relay_metric_samples"
      pattern: "block_relay_metric_samples\\(&status"
    - from: "write_block_relay_log"
      to: "block_relay_log_record"
      via: "same provider Available gate + append_structured_record"
      pattern: "block_relay_log_record\\("
    - from: "sync_once_with_resolver tick"
      to: "write_block_relay_log"
      via: "same tick as persist_metrics / write_summary_logs"
      pattern: "write_block_relay_log\\("
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 121-2026-07-14T04-25-57
generated_at: 2026-07-14T05:30:00.000Z
---

<objective>
Wire DurableSyncRuntime provider + persist_metrics + structured-log emission so OBS-03 block-relay samples and logs project through the retained runtime path when status is Available.

Purpose: Close the audit gap where Phase 116 helpers exist but persist_metrics / sync logs never emit block-relay series.
Output: Provider field/setter, Available-gated persist append, write_block_relay_log on the sync tick, and DurableSyncRuntime tests for available/unavailable/leakage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md
@.planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
From packages/open-bitcoin-node/src/sync/metrics.rs (mirror for block-relay):
```rust
pub fn set_inbound_metric_status_provider<F>(&mut self, provider: F)
where
    F: Fn() -> FieldAvailability<InboundPeerServingStatus> + Send + Sync + 'static,
{
    self.maybe_inbound_metric_status_provider = Some(Arc::new(provider));
}

// persist_metrics currently:
let mut samples = summary.metric_samples(timestamp);
if let Some(provider) = self.maybe_inbound_metric_status_provider.as_ref() {
    samples.extend(inbound_metric_samples(&provider(), timestamp));
}
self.store.append_metric_samples(&samples, MetricRetentionPolicy::default(), timestamp, self.config.persist_mode)?;
```

From packages/open-bitcoin-node/src/metrics/block_relay.rs (UNCHANGED — call-site gate required):
```rust
pub fn block_relay_metric_samples(
    block_relay: &BlockRelayEvidenceStatus,
    timestamp_unix_seconds: u64,
) -> Vec<MetricSample>;
// Always emits nine samples; nested Unavailable → 0. Do NOT call on default_unavailable
// as Available outer — that fabricates zero availability evidence (D-04).
```

From packages/open-bitcoin-node/src/logging.rs (UNCHANGED):
```rust
pub const BLOCK_RELAY_LOG_SOURCE: &str = "block_relay";
pub fn block_relay_log_record(
    block_relay: &BlockRelayEvidenceStatus,
    timestamp_unix_seconds: u64,
) -> StructuredLogRecord;
```

From packages/open-bitcoin-node/src/sync.rs tick (~203–214):
```rust
self.persist_metrics(&summary, timestamp)?;
self.write_summary_logs(&mut summary, timestamp);
// Phase 121: add write_block_relay_log(&mut summary, timestamp) on this tick
```

Required Available-gate pattern (D-04 asymmetry vs inbound):
```rust
if let Some(provider) = self.maybe_block_relay_metric_status_provider.as_ref() {
    if let FieldAvailability::Available(status) = provider() {
        samples.extend(block_relay_metric_samples(&status, timestamp));
    }
}
```

Locked decisions for this plan: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10.
Out of scope: open-bitcoind production wiring, Bun checker, verify.sh (Plan 02); package/filter/public-default/production claims; new MetricKind; sync-disabled twin metrics worker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider setter + Available-gated persist_metrics</name>
  <files>packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/metrics.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md (D-01..D-05, D-09)
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md (Pattern 1; Pitfall 1 zero-sample; Pitfall 6 crate re-export)
    - packages/open-bitcoin-node/src/sync/metrics.rs (inbound provider + persist_metrics)
    - packages/open-bitcoin-node/src/sync.rs (DurableSyncRuntime fields + open() init)
    - packages/open-bitcoin-node/src/lib.rs (metrics pub use list — block_relay_metric_samples missing)
    - packages/open-bitcoin-node/src/sync/tests.rs (`persist_metrics_appends_inbound_status_samples_with_sync_samples`, `persist_metrics_omits_inbound_samples_when_status_unavailable`)
    - packages/open-bitcoin-node/src/metrics/block_relay.rs (helper signature — do not modify)
    - packages/open-bitcoin-node/src/logging/tests.rs (`block_relay_log_record_uses_fixed_source_labels_and_counts` fixture via `with_components`)
  </read_first>
  <behavior>
    - set_block_relay_metric_status_provider stores Arc&lt;dyn Fn() -&gt; FieldAvailability&lt;BlockRelayEvidenceStatus&gt; + Send + Sync&gt; (per D-02)
    - persist_metrics with Available status appends all nine block-relay MetricKinds alongside SyncHeight (per D-03/D-05)
    - persist_metrics with Unavailable provider OR unset provider appends no BlockServedCount/CompactAnnouncedCount/… samples (per D-04)
    - Calling block_relay_metric_samples only inside FieldAvailability::Available match — never on Unavailable / default_unavailable outer Available wrap
  </behavior>
  <action>
    1. Add `maybe_block_relay_metric_status_provider: Option&lt;Arc&lt;dyn Fn() -&gt; FieldAvailability&lt;BlockRelayEvidenceStatus&gt; + Send + Sync&gt;&gt;` to `DurableSyncRuntime` in `sync.rs`; initialize to `None` in `open()` (discretion: single shared provider for metrics+logs per research).
    2. In `sync/metrics.rs`, add `set_block_relay_metric_status_provider` mirroring inbound (D-02). Import `BlockRelayEvidenceStatus` and `block_relay_metric_samples`.
    3. Extend `persist_metrics` after the inbound extend block (D-03): Available-gate then `samples.extend(block_relay_metric_samples(&amp;status, timestamp))`. Do not create a parallel metrics store. Do not add MetricKinds (D-05). Do not call the helper when Unavailable (D-04 — research Pitfall 1).
    4. Re-export `block_relay_metric_samples` from `packages/open-bitcoin-node/src/lib.rs` metrics `pub use` alongside `inbound_metric_samples` / `relay_metric_samples` (research Pitfall 6) so sync/metrics can use `crate::block_relay_metric_samples` consistently — OR import `crate::metrics::block_relay_metric_samples` consistently; prefer crate-root re-export for parity with inbound.
    5. Do not modify `metrics/block_relay.rs` or `logging.rs` helpers (D-05/D-08).
    6. TDD in `sync/tests.rs` (Arrange/Act/Assert, mirror inbound tests near `persist_metrics_appends_inbound_*`):
       - `persist_metrics_appends_block_relay_status_samples_with_sync_samples`: provider returns `FieldAvailability::available(status)` built with `BlockRelayEvidenceStatus::with_components(...)` (reuse logging/tests fixture values); assert SyncHeight present AND at least CompactAnnouncedCount (or BlockServedCount) with expected fixture value + timestamp.
       - `persist_metrics_omits_block_relay_samples_when_status_unavailable`: provider returns `FieldAvailability::unavailable(BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON)`; assert SyncHeight present and NONE of the nine block-relay MetricKinds present.
       - Optional third: provider unset (`None`) also omits block-relay kinds.
    7. Do not wire open-bitcoind here (Plan 02). Do not invent a sync-disabled twin worker.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics_appends_block_relay -- --nocapture &amp;&amp; cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics_omits_block_relay -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "set_block_relay_metric_status_provider" packages/open-bitcoin-node/src/sync/metrics.rs` finds the public setter
    - `rg -n "maybe_block_relay_metric_status_provider" packages/open-bitcoin-node/src/sync.rs` finds the struct field initialized to None
    - `rg -n "block_relay_metric_samples" packages/open-bitcoin-node/src/sync/metrics.rs` finds call inside Available match (not unconditional on provider())
    - `rg -n "persist_metrics_appends_block_relay_status_samples_with_sync_samples|persist_metrics_omits_block_relay_samples_when_status_unavailable" packages/open-bitcoin-node/src/sync/tests.rs` finds both tests
    - `rg -n "block_relay_metric_samples" packages/open-bitcoin-node/src/lib.rs` finds crate-root re-export OR sync/metrics imports `crate::metrics::block_relay_metric_samples`
    - `rg -n "pub fn block_relay_metric_samples" packages/open-bitcoin-node/src/metrics/block_relay.rs` still exists unchanged (helper not rewritten)
  </acceptance_criteria>
  <done>Provider + Available-gated persist_metrics appends block-relay samples with sync samples; unavailable omits the family; runtime tests prove it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_block_relay_log + tick wiring + leakage tests</name>
  <files>packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md (D-06..D-10)
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md (Pattern 2 shared provider; Pitfall 3 metrics-without-logs; Pitfall 4 leakage fixtures)
    - packages/open-bitcoin-node/src/sync/runtime_state.rs (`write_summary_logs`, `append_structured_record`)
    - packages/open-bitcoin-node/src/sync.rs (tick after persist_metrics)
    - packages/open-bitcoin-node/src/logging.rs (`block_relay_log_record`, `BLOCK_RELAY_LOG_SOURCE`)
    - packages/open-bitcoin-node/src/sync/tests.rs (`sync_config_with_log_dir`, `load_structured_log_records`, existing write_summary_logs tests)
    - packages/open-bitcoin-node/src/logging/tests.rs (`block_relay_log_record_omits_sensitive_and_dynamic_material` marker list)
  </read_first>
  <behavior>
    - write_block_relay_log uses the SAME maybe_block_relay_metric_status_provider; on Available calls block_relay_log_record then append_structured_record; on Unavailable/None emits nothing (D-06/D-07)
    - sync_once_with_resolver tick invokes write_block_relay_log on the same progress tick as persist_metrics / write_summary_logs
    - Emitted record source is BLOCK_RELAY_LOG_SOURCE ("block_relay") with outcome=projected cause=status_projection label=block_relay
    - Emitted message and MetricKind path contain none of: 127.0.0.1, peer_id, permission_string, credential, cookie, secret, raw tx hex markers (D-10)
  </behavior>
  <action>
    1. Add `pub(super) fn write_block_relay_log(&amp;self, summary: &amp;mut SyncRunSummary, timestamp: i64)` in `runtime_state.rs` beside `write_summary_logs` (D-06/D-08):
       - If provider is None → return.
       - If `provider()` is Unavailable → return (no record).
       - If Available(status) → `let record = block_relay_log_record(&amp;status, timestamp_u64);` then `append_structured_record(&amp;record)`; on Err push `log_write_failed_signal` into summary.health_signals (same posture as write_summary_logs).
       - Reuse existing helper unchanged — no new log writer, no log-text parsing.
    2. In `sync.rs` after `write_summary_logs(&amp;mut summary, timestamp)`, call `self.write_block_relay_log(&amp;mut summary, timestamp)` so metrics and logs share the tick (D-07). Shared provider field only — no twin setter (discretion).
    3. Import `block_relay_log_record` / `BLOCK_RELAY_LOG_SOURCE` as needed from crate logging surface.
    4. TDD in `sync/tests.rs`:
       - `write_block_relay_log_emits_when_status_available`: `sync_config_with_log_dir`, set Available provider with with_components fixture, call `write_block_relay_log`, assert `load_structured_log_records` contains source == "block_relay" and message contains `outcome=projected`, `cause=status_projection`, `label=block_relay`.
       - `write_block_relay_log_omits_when_status_unavailable`: Unavailable provider → no record with source block_relay.
       - `write_block_relay_log_omits_sensitive_markers`: Available fixture must not embed peer endpoints/permissions/credentials/tx hex; assert emitted message lacks markers from logging/tests leakage list (`127.0.0.1`, `peer_id`, `permission_string`, `credential`, `cookie`, `secret`, `0123456789abcdef` as raw payload stand-in).
    5. Keep helpers side-effect-free (D-08). No new P2P behavior. No dashboard/RPC/CLI surface expansion.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node write_block_relay_log -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn write_block_relay_log" packages/open-bitcoin-node/src/sync/runtime_state.rs` finds the method
    - `rg -n "block_relay_log_record" packages/open-bitcoin-node/src/sync/runtime_state.rs` finds helper reuse inside Available gate
    - `rg -n "append_structured_record" packages/open-bitcoin-node/src/sync/runtime_state.rs` finds call from write_block_relay_log path
    - `rg -n "write_block_relay_log" packages/open-bitcoin-node/src/sync.rs` finds tick call-site after write_summary_logs (or immediately adjacent on same tick)
    - `rg -n "write_block_relay_log_emits_when_status_available|write_block_relay_log_omits_when_status_unavailable" packages/open-bitcoin-node/src/sync/tests.rs` finds both tests
    - `rg -n "credential|cookie|127\\.0\\.0\\.1|permission_string" packages/open-bitcoin-node/src/sync/tests.rs` inside the new leakage test asserts absence (assert!(!…contains)) rather than fixture embedding
  </acceptance_criteria>
  <done>Structured block_relay logs emit through append_structured_record on the sync tick when Available, omit when Unavailable, and runtime tests prove no sensitive leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider → persist_metrics / write_block_relay_log | Untrusted or absent evidence crosses into retained metrics history and datadir structured logs |
| Status aggregates → MetricSample / StructuredLogRecord | Only fixed low-cardinality projections may cross; no peer/PII/credential payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Information Disclosure | persist_metrics / write_block_relay_log | mitigate | Reuse Phase 116 helpers only; fixed MetricKind + fixed outcome/cause/label vocabulary; runtime leakage asserts (D-05/D-07/D-10) |
| T-121-02 | Information Disclosure | runtime test fixtures | mitigate | Build status via with_components aggregates only; forbid peer endpoints, permission strings, credentials, cookies, tx hex in fixtures and expected messages |
| T-121-03 | Spoofing | Available-gate for samples | mitigate | Omit entire block-relay sample family when Unavailable; never call block_relay_metric_samples on default_unavailable under Available wrap (D-04) |
| T-121-04 | Denial of Service | metric/log label cardinality | mitigate | No new MetricKinds, no dynamic labels, no peer-id dimensions (D-05/D-07) |
| T-121-05 | Tampering | fabricated zero availability evidence | mitigate | Unavailable ⇒ zero block-relay samples and no block_relay log records; tests assert omission |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics_appends_block_relay -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics_omits_block_relay -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node write_block_relay_log -- --nocapture`
</verification>

<success_criteria>
- Provider setter exists; persist_metrics appends block-relay samples only when Available
- write_block_relay_log emits sanitized block_relay records on the same tick when Available
- Runtime tests cover available, unavailable, and leakage for both metrics and logs
- Helpers and MetricKinds remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-01-SUMMARY.md`
</output>
