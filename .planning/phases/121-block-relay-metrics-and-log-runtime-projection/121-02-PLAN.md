---
phase: 121-block-relay-metrics-and-log-runtime-projection
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - scripts/check-phase121-block-relay-metrics-log-runtime.ts
  - scripts/check-phase121-block-relay-metrics-log-runtime.test.ts
  - scripts/verify.sh
  - docs/architecture/operator-observability.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - OBS-03
user_setup: []
must_haves:
  truths:
    - "open-bitcoind wires set_block_relay_metric_status_provider from ManagedRpcContext::block_relay_evidence_status with outer Available gated on block_serving.activation"
    - "scripts/check-phase121-block-relay-metrics-log-runtime.ts proves provider, persist append, log emission, helper reuse, runtime tests, daemon wiring, and dual verify.sh inclusion"
    - "bash scripts/verify.sh runs the Phase 121 bun test and bun run checker in both visible and run_step regions"
    - "OBS-03 is marked complete in REQUIREMENTS.md after the closed DurableSyncRuntime projection flow is documented"
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs"
      provides: "Production provider from ManagedRpcContext block_relay evidence"
      contains: "set_block_relay_metric_status_provider"
    - path: "scripts/check-phase121-block-relay-metrics-log-runtime.ts"
      provides: "Deterministic Phase 121 corpus checker"
      min_lines: 80
    - path: "scripts/verify.sh"
      provides: "Dual-region Phase 121 checker wiring"
      contains: "check-phase121-block-relay-metrics-log-runtime"
  key_links:
    - from: "open-bitcoind start_daemon_sync_worker"
      to: "ManagedRpcContext::block_relay_evidence_status"
      via: "Arc-cloned shared_context provider closure"
      pattern: "block_relay_evidence_status\\(\\)"
    - from: "scripts/verify.sh"
      to: "check-phase121-block-relay-metrics-log-runtime.ts"
      via: "bun test + bun run in visible list AND run_step"
      pattern: "check-phase121-block-relay-metrics-log-runtime"
    - from: "REQUIREMENTS.md OBS-03"
      to: "Phase 121 runtime projection"
      via: "checkbox Complete + ownership Phase 121"
      pattern: "OBS-03.*Phase 121|\\[x\\].*OBS-03"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 121-2026-07-14T04-25-57
generated_at: 2026-07-14T05:30:00.000Z
---

<objective>
Wire production block-relay status into DurableSyncRuntime from ManagedRpcContext, add the Phase 121 Bun checker + verify.sh inclusion, and close OBS-03 docs/requirements evidence.

Purpose: Unit-injected providers alone leave the daemon path empty (research Pitfall 2); checker + verify.sh make OBS-03 auditable and deterministic (D-11/D-12).
Output: open-bitcoind provider wiring, check-phase121-*.ts/.test.ts, verify.sh dual wiring, operator-observability note, OBS-03 Complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-01-SUMMARY.md

<interfaces>
From packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs (inbound pattern to extend):
```rust
sync_runtime.set_inbound_metric_status_provider(move || {
    let Ok(context) = shared_context.try_lock() else {
        return inbound_status_unavailable();
    };
    context.current_inbound_status()
});
// CRITICAL: Arc::clone(&shared_context) BEFORE the inbound move so block-relay
// can own a second Arc for its closure.
```

From packages/open-bitcoin-rpc/src/context/network.rs:
```rust
pub fn block_relay_evidence_status(&self) -> BlockRelayEvidenceStatus
```

Recommended production mapping (research Pattern 3 / A1 — activation outer gate):
```rust
sync_runtime.set_block_relay_metric_status_provider(move || {
    let Ok(context) = shared_context.try_lock() else {
        return FieldAvailability::unavailable(BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON);
    };
    let status = context.block_relay_evidence_status();
    match &status.block_serving.activation {
        FieldAvailability::Available(_) => FieldAvailability::available(status),
        FieldAvailability::Unavailable { .. } => {
            FieldAvailability::unavailable(BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON)
        }
    }
});
```

From scripts/check-phase97-inbound-metrics.ts — mirror structure for Phase 121 TARGET_FILES + needles.
From scripts/verify.sh — Phase 97/116 appear in BOTH the early visible bun list (~312) AND later run_step blocks (~428). Wire Phase 121 the same way after Phase 116 entries (or after latest phase120/116 neighbor — place after Phase 116 block-relay evidence checker for observability adjacency).

Locked decisions for this plan: D-01 (production source), D-11, D-12; completes OBS-03 closeout.
Out of scope: sync-disabled twin metrics worker; new P2P; new MetricKinds; package/filter/public-default/production readiness claims.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: open-bitcoind ManagedRpcContext provider wiring</name>
  <files>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</files>
  <read_first>
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md (D-01, D-04)
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md (Pattern 3; Pitfall 2 wrong network; Open Question 2 — no twin worker)
    - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs (`start_daemon_sync_worker`, inbound `set_inbound_metric_status_provider`)
    - packages/open-bitcoin-rpc/src/context/network.rs (`block_relay_evidence_status`)
    - packages/open-bitcoin-node/src/status/block_serving.rs (`BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON`)
    - packages/open-bitcoin-node/src/sync/metrics.rs (Plan 01 setter signature)
  </read_first>
  <action>
    1. In `start_daemon_sync_worker`, before the inbound provider closure, `let block_relay_context = Arc::clone(&amp;shared_context);` so both closures can capture an Arc (inbound currently moves `shared_context`).
    2. Immediately after `set_inbound_metric_status_provider`, call `set_block_relay_metric_status_provider` (D-01/D-02 production wiring):
       - try_lock failure → `FieldAvailability::unavailable(BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON)` (reuse existing constant; do not invent dynamic reasons — research Open Question 1).
       - On lock success: `let status = context.block_relay_evidence_status();`
       - Outer Available only when `status.block_serving.activation` is Available; otherwise Unavailable with the same reason (D-04 / research A1). Do **not** wrap `BlockRelayEvidenceStatus::default_unavailable()` as Available.
    3. Import `BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON` / `FieldAvailability` / `BlockRelayEvidenceStatus` as needed from existing open-bitcoin-node status exports already used by the binary.
    4. Do **not** add a sync-disabled `start_block_relay_metrics_worker` twin (research Open Question 2 — out of Phase 121 success criteria).
    5. Do not read DurableSyncRuntime’s private outbound-sync `network.block_relay_evidence_status()` as the production source (Pitfall 2).
  </action>
  <verify>
    <automated>cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "set_block_relay_metric_status_provider" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` finds the daemon wiring call
    - `rg -n "block_relay_evidence_status" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` finds the provider body using ManagedRpcContext evidence
    - `rg -n "BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` finds unavailable mapping for lock failure and/or activation-Unavailable
    - `rg -n "start_block_relay_metrics_worker|persist_block_relay_metrics_once" packages/open-bitcoin-rpc/src/bin` returns no matches (no twin worker)
    - `rg -n "self\\.network\\.block_relay_evidence_status" packages/open-bitcoin-node/src/sync` returns no matches used as production provider substitute
  </acceptance_criteria>
  <done>Daemon sync worker feeds Available-gated BlockRelayEvidenceStatus from ManagedRpcContext into DurableSyncRuntime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 121 Bun checker + verify.sh + OBS-03 closeout</name>
  <files>scripts/check-phase121-block-relay-metrics-log-runtime.ts, scripts/check-phase121-block-relay-metrics-log-runtime.test.ts, scripts/verify.sh, docs/architecture/operator-observability.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md (D-11, D-12)
    - .planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-RESEARCH.md (Pattern 4 checker needles; Pitfall 5 dual verify.sh)
    - scripts/check-phase97-inbound-metrics.ts (TARGET_FILES, verifyRuntimeAppendAndProvider, verifyVerifierWiring, verifyNoClaimCreep)
    - scripts/check-phase97-inbound-metrics.test.ts (fixture pass/fail mutation pattern)
    - scripts/verify.sh (Phase 97/116 dual wiring regions ~312 and ~428)
    - docs/architecture/operator-observability.md (## Phase 116 block-relay operator evidence)
    - .planning/REQUIREMENTS.md (OBS-03 checkbox + traceability table)
    - packages/open-bitcoin-node/src/sync/metrics.rs (Plan 01 symbols)
    - packages/open-bitcoin-node/src/sync/runtime_state.rs (`write_block_relay_log`)
    - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs (Task 1 wiring)
  </read_first>
  <behavior>
    - checkPhase121… returns [] failures on complete corpus
    - Removing set_block_relay_metric_status_provider, block_relay_metric_samples extend, write_block_relay_log, open-bitcoind wiring, or verify.sh entries produces ≥1 failure
    - verify.sh contains both `bun test scripts/check-phase121-…test.ts` and `bun run scripts/check-phase121-….ts` in the visible list AND matching run_step entries (D-11/D-12)
    - No-claim creep rejects package relay / public default / production readiness claim phrases in docs corpus
  </behavior>
  <action>
    1. Create `scripts/check-phase121-block-relay-metrics-log-runtime.ts` (Bun/TypeScript, no package.json deps) mirroring Phase 97 structure with TARGET_FILES covering at minimum:
       - `packages/open-bitcoin-node/src/sync.rs`
       - `packages/open-bitcoin-node/src/sync/metrics.rs`
       - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
       - `packages/open-bitcoin-node/src/sync/tests.rs`
       - `packages/open-bitcoin-node/src/metrics/block_relay.rs`
       - `packages/open-bitcoin-node/src/logging.rs`
       - `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs`
       - `docs/architecture/operator-observability.md`
       - `scripts/check-phase121-block-relay-metrics-log-runtime.ts`
       - `scripts/verify.sh`
    2. Required needles (research Pattern 4 / D-11):
       - Provider: `set_block_relay_metric_status_provider`, `FieldAvailability&lt;BlockRelayEvidenceStatus&gt;`
       - Persist: `block_relay_metric_samples`, Available-gate / `samples.extend`, `append_metric_samples`
       - Logs: `write_block_relay_log`, `block_relay_log_record`, `append_structured_record`
       - Tests: `persist_metrics_appends_block_relay_status_samples_with_sync_samples`, `persist_metrics_omits_block_relay_samples_when_status_unavailable`, `write_block_relay_log_emits_when_status_available`, `write_block_relay_log_omits_when_status_unavailable`, `write_block_relay_log_omits_sensitive_markers` (D-10 leakage; optionally also assert a metric-path sensitive-marker test if present)
       - Daemon: `set_block_relay_metric_status_provider`, `block_relay_evidence_status`
       - Helper reuse (unchanged helpers still present): `pub fn block_relay_metric_samples`, `pub fn block_relay_log_record`
       - Verify: both bun test/run command strings + run_step labels for Phase 121
       - No-claim: reject phrases such as `enables package relay`, `enables public inbound default`, `proves production full-node readiness`, `enables compact block relay` as a production-default claim (mirror Phase 97/116 no-claim lists — adapt to docs corpus only)
    3. Create `scripts/check-phase121-block-relay-metrics-log-runtime.test.ts` with:
       - pass on complete corpus fixture
       - fail mutations removing key needles (provider, persist, log, daemon, verify wiring)
    4. Wire into `scripts/verify.sh` in **both** regions (Pitfall 5):
       - Visible early bun list: after Phase 116 operator block-relay evidence lines
       - `run_step` list: matching "test Phase 121…" and "check Phase 121…" entries
    5. Update `docs/architecture/operator-observability.md` Phase 116 (or add a short Phase 121 subsection after it) stating that DurableSyncRuntime `persist_metrics` / structured-log path projects `block_relay_metric_samples` and `block_relay_log_record` when block-relay status is Available via the provider — without claiming public defaults or production readiness.
    6. Mark OBS-03 complete in `.planning/REQUIREMENTS.md` (`[x] **OBS-03**` and traceability table Phase 121 → Complete). Update ROADMAP Phase 121 plan checkboxes when plans finish (executor updates plan rows to `[x]` in SUMMARY flow; at plan-write time ROADMAP already lists plans — ensure OBS-03 success criteria remain accurate).
  </action>
  <verify>
    <automated>bun test scripts/check-phase121-block-relay-metrics-log-runtime.test.ts &amp;&amp; bun run scripts/check-phase121-block-relay-metrics-log-runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-phase121-block-relay-metrics-log-runtime.ts &amp;&amp; test -f scripts/check-phase121-block-relay-metrics-log-runtime.test.ts`
    - `rg -n "check-phase121-block-relay-metrics-log-runtime" scripts/verify.sh` shows ≥4 matches (bun test, bun run, and two run_step lines — or equivalent dual-region coverage)
    - `rg -n "set_block_relay_metric_status_provider|write_block_relay_log|block_relay_metric_samples" scripts/check-phase121-block-relay-metrics-log-runtime.ts` finds required needles
    - `rg -n "\\[x\\].*OBS-03|OBS-03.*Complete" .planning/REQUIREMENTS.md` shows OBS-03 closed
    - `rg -n "DurableSyncRuntime|persist_metrics|block_relay_metric_samples" docs/architecture/operator-observability.md` finds Phase 121 projection documentation
    - `rg -n "enables package relay|proves production full-node readiness" docs/architecture/operator-observability.md` returns no matches in the new Phase 121 text
  </acceptance_criteria>
  <done>Phase 121 checker + verify.sh prove production-callable OBS-03 wiring; docs and REQUIREMENTS mark OBS-03 complete without claim creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ManagedRpcContext → DurableSyncRuntime provider | Daemon evidence lock/status crosses into retained metrics/logs |
| Checker corpus → release claim surface | Static docs/scripts must not assert deferred production capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-06 | Spoofing | open-bitcoind provider | mitigate | Outer Unavailable on lock failure and activation-Unavailable; never Available(default_unavailable) (D-04) |
| T-121-07 | Information Disclosure | daemon provider / docs | mitigate | Pass only BlockRelayEvidenceStatus aggregates; checker no-claim creep rejects production/package/public-default phrases (D-10/D-11) |
| T-121-08 | Tampering | verify.sh partial wiring | mitigate | Checker asserts both visible and run_step Phase 121 entries (D-11/D-12) |
| T-121-09 | Elevation of Privilege | observability docs | accept | Docs are non-executing; mitigated by no-claim string absence checks — residual risk low |
| T-121-10 | Denial of Service | checker/label expansion | mitigate | Checker forbids new dynamic label vocabulary; fixed MetricKind / log label needles only |
</threat_model>

<verification>
- `cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind`
- `bun test scripts/check-phase121-block-relay-metrics-log-runtime.test.ts`
- `bun run scripts/check-phase121-block-relay-metrics-log-runtime.ts`
- Confirm `rg -n "check-phase121-block-relay-metrics-log-runtime" scripts/verify.sh` covers both wiring regions
</verification>

<success_criteria>
- Daemon wires Available-gated block-relay evidence from ManagedRpcContext
- Phase 121 Bun checker + dual verify.sh wiring pass
- OBS-03 marked Complete with operator-observability runtime-projection note
- No package/filter/public-default/production readiness claim creep
</success_criteria>

<output>
After completion, create `.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-02-SUMMARY.md`
</output>
