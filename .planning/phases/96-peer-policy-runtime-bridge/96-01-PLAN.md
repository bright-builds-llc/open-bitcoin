---
phase: 96-peer-policy-runtime-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer_policy.rs
  - packages/open-bitcoin-network/src/peer_policy/tests.rs
  - packages/open-bitcoin-network/src/peer.rs
autonomous: true
requirements: [EVICT-03, EVICT-04, DOS-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 96-2026-06-28T02-38-04
generated_at: 2026-06-28T02:43:32.273Z
must_haves:
  truths:
    - "Peer-policy runtime state is pure `open-bitcoin-network` data and uses injected timestamps for expiry."
    - "Address and subnet bans are scoped; one active ban never suppresses unrelated remotes."
    - "Discouraged reconnect state is represented separately from active bans."
    - "Misbehavior decisions can be recorded as bounded runtime evidence without hiding protected-peer observations."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer_policy.rs"
      provides: "Pure peer-policy runtime state and scoped reconnect decisions"
      contains: "PeerPolicyRuntimeState"
    - path: "packages/open-bitcoin-network/src/peer_policy/tests.rs"
      provides: "Unit coverage for scoped ban, discourage, expiry, unban, and protected/no-action evidence"
      contains: "runtime_state_scopes_address_bans_to_matching_remote"
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "PeerManager API surface for pure peer-policy runtime state"
      contains: "peer_policy_runtime_state"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer.rs"
      to: "packages/open-bitcoin-network/src/peer_policy.rs"
      via: "PeerManager owns and exposes pure peer-policy runtime state"
      pattern: "PeerPolicyRuntimeState"
    - from: "packages/open-bitcoin-network/src/peer_policy.rs"
      to: "packages/open-bitcoin-network/src/resource.rs"
      via: "ReconnectSuppressionInput returned from scoped runtime state"
      pattern: "ReconnectSuppressionInput"
---

<objective>
Add pure scoped peer-policy runtime state for Phase 96.

Purpose: Close the foundation for EVICT-03, EVICT-04, and DOS-03 by giving runtime code an authoritative pure state for active bans, expired bans, unbans, discouraged remotes, misbehavior decisions, and scoped reconnect suppression.
Output: Pure Rust policy APIs and unit tests; no socket, storage, RPC, CLI, support, metric, or public-network behavior changes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
@.planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
@.planning/phases/93-eviction-ban-and-misbehavior-policy/93-CONTEXT.md
@.planning/phases/94-dos-and-resource-governance/94-CONTEXT.md
@packages/open-bitcoin-network/src/peer_policy.rs
@packages/open-bitcoin-network/src/peer_policy/tests.rs
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/resource.rs

<interfaces>
Existing policy types:
```rust
pub enum BanScope { Address(IpAddr), Subnet { network: IpAddr, prefix_bits: u8 } }
pub struct PeerBanEntry { pub scope: BanScope, pub reason: BanReason, pub created_at_unix_seconds: i64, pub expires_at_unix_seconds: i64, pub source: &'static str }
pub enum BanDecision { Active(PeerBanEntry), Expired(PeerBanEntry) }
pub enum UnbanDecision { Unbanned(PeerBanEntry), NotFound(BanScope), AlreadyExpired(PeerBanEntry) }
pub struct MisbehaviorDecision { pub peer_label: String, pub kind: MisbehaviorKind, pub score: u32, pub response: MisbehaviorResponse }
pub struct ReconnectSuppressionInput { pub banned: bool, pub discouraged: bool }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped runtime policy state</name>
  <files>packages/open-bitcoin-network/src/peer_policy.rs, packages/open-bitcoin-network/src/peer_policy/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
    packages/open-bitcoin-network/src/peer_policy.rs
    packages/open-bitcoin-network/src/peer_policy/tests.rs
    packages/open-bitcoin-network/src/resource.rs
  </read_first>
  <behavior>
    - Test 1: `runtime_state_scopes_address_bans_to_matching_remote` proves an active `BanScope::Address(127.0.0.2)` sets `ReconnectSuppressionInput { banned: true, discouraged: false }` only for `127.0.0.2`, while `127.0.0.3` remains allowed.
    - Test 2: `runtime_state_scopes_subnet_bans_to_matching_remote` proves `192.0.2.0/24` suppresses `192.0.2.10` and does not suppress `198.51.100.10`.
    - Test 3: `runtime_state_ignores_expired_bans_for_reconnect` proves an expired ban produces `banned: false`.
    - Test 4: `runtime_state_records_discouraged_reconnects_separately` proves discourage state sets `discouraged: true` without setting `banned`.
    - Test 5: `runtime_state_records_unban_and_misbehavior_decisions` proves manual unban increments bounded projection evidence and protected misbehavior records `protected_no_action`.
  </behavior>
  <action>Add `PeerPolicyRuntimeState` to `peer_policy.rs`. The type must own a `PeerBanBook`, a separate discouraged scope collection, bounded decision vectors for recent `MisbehaviorDecision`, `BanDecision`, and `UnbanDecision`, and methods named `record_ban`, `record_unban`, `record_discouragement`, `record_misbehavior`, `reconnect_suppression_input_for_ip`, `misbehavior_decisions`, `ban_decisions`, and `unban_decisions`. Add `BanScope::matches_ip(&self, remote_ip: IpAddr) -> bool` supporting exact address, IPv4 subnet, and IPv6 subnet matching with explicit prefix bounds (`prefix_bits <= 32` for IPv4 and `prefix_bits <= 128` for IPv6). Use `now_unix_seconds: i64` parameters for every expiry-sensitive method. Return `ReconnectSuppressionInput` from `reconnect_suppression_input_for_ip`; do not compute suppression from aggregate counts. Keep any bounded vectors capped with a named constant such as `MAX_PEER_POLICY_RUNTIME_DECISIONS: usize = 32`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy::tests::runtime_state -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    grep -n "pub struct PeerPolicyRuntimeState" packages/open-bitcoin-network/src/peer_policy.rs
    grep -n "pub fn matches_ip" packages/open-bitcoin-network/src/peer_policy.rs
    grep -n "reconnect_suppression_input_for_ip" packages/open-bitcoin-network/src/peer_policy.rs packages/open-bitcoin-network/src/peer_policy/tests.rs
    grep -n "MAX_PEER_POLICY_RUNTIME_DECISIONS" packages/open-bitcoin-network/src/peer_policy.rs
    grep -n "runtime_state_scopes_address_bans_to_matching_remote" packages/open-bitcoin-network/src/peer_policy/tests.rs
    grep -n "runtime_state_scopes_subnet_bans_to_matching_remote" packages/open-bitcoin-network/src/peer_policy/tests.rs
    grep -n "runtime_state_records_discouraged_reconnects_separately" packages/open-bitcoin-network/src/peer_policy/tests.rs
  </acceptance_criteria>
  <done>Pure peer-policy runtime state can answer scoped reconnect suppression and preserve bounded ban/unban/misbehavior evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Expose runtime state through PeerManager</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer_policy.rs, packages/open-bitcoin-network/src/peer_policy/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/languages/rust.md
    packages/open-bitcoin-network/src/peer.rs
    packages/open-bitcoin-network/src/peer_policy.rs
    packages/open-bitcoin-network/src/peer_policy/tests.rs
  </read_first>
  <action>Add a `peer_policy_runtime_state: PeerPolicyRuntimeState` field to `PeerManager`, initialize it in all constructors, and expose narrow methods `peer_policy_runtime_state(&self) -> &PeerPolicyRuntimeState` and `peer_policy_runtime_state_mut(&mut self) -> &mut PeerPolicyRuntimeState` or narrower record/query methods if that keeps the API clearer. Do not add socket, storage, RPC, CLI, metric, or support rendering code in this plan. Preserve existing `PeerManager::eviction_decision()` behavior.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "peer_policy_runtime_state" packages/open-bitcoin-network/src/peer.rs
    grep -n "PeerPolicyRuntimeState::default" packages/open-bitcoin-network/src/peer.rs
    grep -n "pub fn peer_policy_runtime_state" packages/open-bitcoin-network/src/peer.rs
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy -- --nocapture
  </acceptance_criteria>
  <done>`PeerManager` owns the pure runtime policy state so downstream managed runtime code has one authoritative source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer-derived policy input -> pure runtime state | Remote-address and protocol-violation observations become policy state. |
| pure runtime state -> reconnect suppression | Internal policy state decides whether a future remote should be suppressed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-01 | Denial of Service | `BanScope::matches_ip` | mitigate | Unit tests prove matching and non-matching address/subnet cases so one ban cannot suppress unrelated remotes. |
| T-96-01-02 | Tampering | expiry handling | mitigate | Every expiry-sensitive method accepts injected `now_unix_seconds`; no wall-clock reads in pure policy. |
| T-96-01-03 | Information Disclosure | runtime decision buffers | mitigate | Store bounded low-cardinality decisions only; no raw endpoint tables, payloads, credentials, or unbounded ledgers. |
| T-96-01-04 | Elevation of Privilege | protected misbehavior | mitigate | Protected peers still record `protected_no_action` evidence; protection does not hide observations. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
EVICT-03, EVICT-04, and DOS-03 have a pure runtime policy-state foundation that supports scoped ban/discourage reconnect decisions and bounded misbehavior evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/96-peer-policy-runtime-bridge/96-01-SUMMARY.md`.
</output>
