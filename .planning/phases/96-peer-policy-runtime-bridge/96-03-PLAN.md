---
phase: 96-peer-policy-runtime-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - 96-02
files_modified:
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/tests.rs
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
  - packages/open-bitcoin-rpc/src/context/peer_policy.rs
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
autonomous: true
requirements: [EVICT-03, EVICT-04, DOS-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 96-2026-06-28T02-38-04
generated_at: 2026-06-28T02:43:32.273Z
must_haves:
  truths:
    - "Peer-policy bridge evidence flows through shared inbound status, not renderer-local summaries."
    - "Peer-policy bridge outcomes emit bounded structured logs with sanitized low-cardinality fields."
    - "CLI status and support output expose bounded counters and latest safe policy event."
    - "Support redaction removes raw peer-policy material while preserving diagnostic labels."
    - "Operator-facing text keeps scoped runtime bridge wording and no public banlist or production participation claim."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Shared inbound peer-policy bridge evidence contract"
      contains: "latest_peer_policy_decision"
    - path: "packages/open-bitcoin-node/src/logging.rs"
      provides: "Structured log record builder for sanitized peer-policy events"
      contains: "inbound_peer_policy_log_record"
    - path: "packages/open-bitcoin-rpc/src/context/peer_policy.rs"
      provides: "Managed RPC peer-policy event recording and log append path"
      contains: "record_inbound_peer_policy_event_at"
    - path: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      provides: "Human status rendering from shared fields"
      contains: "peer policy evidence:"
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "Support Markdown rendering from shared fields"
      contains: "Inbound Peer Policy Evidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      provides: "Peer-policy redaction boundary"
      contains: "redact_inbound_peer_policy_evidence"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/context/tests.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "current_inbound_status projects peer-policy runtime evidence"
      pattern: "latest_peer_policy_decision"
    - from: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      to: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      via: "support_status_for_bundle redacts latest peer-policy event"
      pattern: "redact_inbound_peer_policy_evidence"
---

<objective>
Verify and, where necessary, complete the shared operator evidence path for Phase 96 peer-policy runtime bridge outcomes.

Purpose: Ensure runtime ban/unban/discourage/misbehavior decisions appear in status, RPC, CLI, support, and redaction tests without leaking raw peer-policy material or inventing public banlist behavior.
Output: Shared status/support evidence tests and minimal renderer/redaction updates only where the existing fields are insufficient.
</objective>

<scope_rationale>
This plan intentionally covers the status, log, CLI, support, and redaction evidence path together even though it touches more than ten files. The Phase 96 evidence contract is one shared boundary: runtime peer-policy events must project through `InboundPeerServingStatus`, append sanitized structured logs, and render through existing status/support surfaces without renderer-local summaries or raw leaks. Splitting these tasks would increase the risk that one surface drifts from the shared evidence contract.
</scope_rationale>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
@.planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
@.planning/phases/96-peer-policy-runtime-bridge/96-02-SUMMARY.md
@packages/open-bitcoin-node/src/logging.rs
@packages/open-bitcoin-node/src/logging/tests.rs
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-node/src/status/inbound/tests.rs
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/context/tests.rs
@packages/open-bitcoin-rpc/src/context/resource_governance.rs
@packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/status/render/tests.rs
@packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
@packages/open-bitcoin-cli/src/operator/support/redaction.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove shared status and RPC projection for bridge outcomes</name>
  <files>packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-rpc/src/context/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-02-SUMMARY.md
    packages/open-bitcoin-node/src/status/inbound.rs
    packages/open-bitcoin-node/src/status/inbound/tests.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/context/tests.rs
  </read_first>
  <behavior>
    - Test 1: `current_inbound_status_projects_runtime_peer_policy_bridge` records active ban, manual unban, and misbehavior decisions through managed runtime state and observes non-empty `latest_peer_policy_decision`.
    - Test 2: shared status counters include non-zero `active_bans`, `manual_unbans`, `misbehavior_observations`, and `protected_no_actions` when the underlying runtime state records those outcomes.
    - Test 3: unavailable status remains unavailable when no listener, admission, address, peer-policy, or resource evidence exists.
  </behavior>
  <action>Add or update tests proving `ManagedRpcContext::current_inbound_status()` projects peer-policy runtime bridge evidence from shared `InboundPeerServingStatus` fields. Avoid adding new fields unless existing fields cannot represent the required evidence. If a new field is necessary, add it with `#[serde(default)]`, an unavailable reason if it is latest-event-like, and tests in `status/inbound/tests.rs`. Keep evidence low-cardinality: `outcome`, `reason`, `label`, `source`, and `message` only.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc current_inbound_status_projects_runtime_peer_policy_bridge -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status::inbound --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    grep -n "current_inbound_status_projects_runtime_peer_policy_bridge" packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "latest_peer_policy_decision" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "active_bans" packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "manual_unbans" packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "protected_no_actions" packages/open-bitcoin-rpc/src/context/tests.rs
  </acceptance_criteria>
  <done>RPC/shared status exposes bounded runtime peer-policy bridge evidence without changing renderer-local state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded structured logs for peer-policy bridge events</name>
  <files>packages/open-bitcoin-node/src/logging.rs, packages/open-bitcoin-node/src/logging/tests.rs, packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/context/peer_policy.rs, packages/open-bitcoin-rpc/src/context/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
    packages/open-bitcoin-node/src/logging.rs
    packages/open-bitcoin-node/src/logging/tests.rs
    packages/open-bitcoin-node/src/status/inbound.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/resource_governance.rs
    packages/open-bitcoin-rpc/src/context/tests.rs
  </read_first>
  <behavior>
    - Test 1: `inbound_peer_policy_log_record_uses_allowlisted_fields` builds an `InboundPeerPolicyEvent` with safe labels and observes a structured log message containing `outcome=`, `reason=`, `label=`, `source=`, and `message=`.
    - Test 2: `inbound_peer_policy_log_record_redacts_suspicious_raw_fields` injects `127.0.0.1:18444`, `peer_id=`, `raw_endpoint`, `permission_string`, `credential`, `secret`, and `payload_bytes` markers and observes `redacted_peer_policy_field` instead of raw material.
    - Test 3: `record_inbound_peer_policy_event_appends_inbound_peer_policy_log_record` records a peer-policy event through `ManagedRpcContext` with a configured log directory and decodes one JSONL record with source `inbound_peer_policy`.
    - Test 4: `record_inbound_peer_policy_runtime_decision_projects_status_and_log` records an actual managed peer-policy runtime decision through `ManagedRpcContext`, then observes non-empty current inbound status and a JSONL peer-policy log record.
  </behavior>
  <action>Add `INBOUND_PEER_POLICY_LOG_SOURCE: &str = "inbound_peer_policy"` and `inbound_peer_policy_log_record(event: &InboundPeerPolicyEvent, timestamp_unix_seconds: u64) -> StructuredLogRecord` in `packages/open-bitcoin-node/src/logging.rs`, mirroring `inbound_resource_governance_log_record` but without `next_action`. Use a dedicated redaction literal `redacted_peer_policy_field` and sanitize empty, overlong, equals-containing, socket-address-like, hex-like, peer id, raw endpoint, payload, permission string, credential, secret, and cookie markers. Add `mod peer_policy;` to `packages/open-bitcoin-rpc/src/context.rs`. Add `packages/open-bitcoin-rpc/src/context/peer_policy.rs` with the required parity breadcrumb block at the top and a method on `ManagedRpcContext` named `record_inbound_peer_policy_event_at(event: InboundPeerPolicyEvent, timestamp_unix_seconds: u64)` that appends the structured record when a log directory is configured. The breadcrumb block must cite `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/banman.cpp`, and `packages/bitcoin-knots/src/net_permissions.cpp`. Reuse the same log retention and write-failure style as `record_inbound_resource_event_at`. Add a higher-level runtime-path helper or test-only path that records a real managed peer-policy runtime decision, projects `current_inbound_status`, and appends the JSONL peer-policy log record through `ManagedRpcContext`, not an isolated free function. Do not log raw peer IDs, endpoints, ban scopes, permission strings, credentials, payloads, or unbounded event history. Raw marker strings are allowed in test fixtures only when the assertions prove they are absent from rendered or decoded log output.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_peer_policy_log_record -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc record_inbound_peer_policy_event_appends_inbound_peer_policy_log_record -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    grep -n "INBOUND_PEER_POLICY_LOG_SOURCE" packages/open-bitcoin-node/src/logging.rs
    grep -n "inbound_peer_policy_log_record" packages/open-bitcoin-node/src/logging.rs packages/open-bitcoin-node/src/logging/tests.rs
    grep -n "redacted_peer_policy_field" packages/open-bitcoin-node/src/logging.rs packages/open-bitcoin-node/src/logging/tests.rs
    grep -n "mod peer_policy;" packages/open-bitcoin-rpc/src/context.rs
    grep -n "Parity breadcrumbs:" packages/open-bitcoin-rpc/src/context/peer_policy.rs
    grep -n "packages/bitcoin-knots/src/banman.cpp" packages/open-bitcoin-rpc/src/context/peer_policy.rs
    grep -n "record_inbound_peer_policy_event_at" packages/open-bitcoin-rpc/src/context/peer_policy.rs packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "record_inbound_peer_policy_runtime_decision_projects_status_and_log" packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "inbound_peer_policy" packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "redacted_peer_policy_field" packages/open-bitcoin-node/src/logging/tests.rs packages/open-bitcoin-rpc/src/context/tests.rs
    grep -n "assert!.*!.*contains" packages/open-bitcoin-node/src/logging/tests.rs packages/open-bitcoin-rpc/src/context/tests.rs
  </acceptance_criteria>
  <done>Peer-policy bridge events have bounded structured log evidence with raw-material redaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove CLI/support rendering and redaction boundaries</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs, packages/open-bitcoin-cli/src/operator/support/render/inbound.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs, packages/open-bitcoin-cli/src/operator/support/redaction.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
    packages/open-bitcoin-cli/src/operator/status/render/tests.rs
    packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test 1: status text includes `peer policy evidence:` with `active_bans=`, `manual_unbans=`, `misbehavior_observations=`, `protected_no_actions=`, and latest decision text.
    - Test 2: support Markdown includes `## Inbound Peer Policy Evidence`, aggregate counters, latest decision text, and a scoped runtime bridge next-action sentence.
    - Test 3: support redaction removes raw peer-policy fields containing `127.0.0.1:`, `peer_id=`, `raw_endpoint`, `permission_string`, `credential`, `secret`, or `cookie=`.
    - Test 4: safe low-cardinality labels such as `ban_active`, `unbanned`, `protected_no_action`, `source_misbehavior_policy`, and `source_peer_policy_runtime_bridge` remain visible.
  </behavior>
  <action>First inspect existing Phase 93 renderer/redaction behavior. If the existing status/support renderers already output all required counters and latest event fields from shared status, add tests only. If they do not, minimally extend `peer_policy_text`, `push_inbound_peer_policy`, or equivalent existing helpers to render exact shared field names. Add `PHASE96_PEER_POLICY_RUNTIME_BRIDGE_NEXT_ACTION` text in support rendering if no existing next action explains the scoped bridge: `Treat Phase 96 as scoped runtime peer-policy bridge evidence only; review ban, discourage, unban, and misbehavior labels before changing listener exposure or peer policy.` Update `redact_inbound_peer_policy_evidence` only if tests show raw field leakage. Do not add a public ban table, raw scope dump, dynamic metric labels, or renderer-local peer-policy summaries.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status::render --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "peer policy evidence:" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs
    grep -n "Inbound Peer Policy Evidence" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "PHASE96_PEER_POLICY_RUNTIME_BRIDGE_NEXT_ACTION" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "redact_inbound_peer_policy_evidence" packages/open-bitcoin-cli/src/operator/support/redaction.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "source_peer_policy_runtime_bridge\\|source_misbehavior_policy" packages/open-bitcoin-cli/src/operator/status/render/tests.rs packages/open-bitcoin-cli/src/operator/support/tests.rs
    ! grep -n "public banlist\\|full banlist\\|production banlist" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  </acceptance_criteria>
  <done>CLI status/support surfaces render scoped bridge evidence from shared status and preserve redaction/no-claim boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| managed runtime -> shared status | Runtime policy decisions become user-visible status data. |
| shared status -> CLI/support output | Bounded status fields are rendered for humans and support sharing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Information Disclosure | support redaction | mitigate | Tests inject raw peer-policy strings and assert they are absent from support JSON/Markdown. |
| T-96-03-02 | Repudiation | status/support rendering | mitigate | Latest bounded event includes outcome, reason, label, source, and message. |
| T-96-03-03 | Spoofing | operator wording | mitigate | Next-action text says scoped runtime bridge evidence only, not production banlist or public-network participation. |
| T-96-03-04 | Tampering | renderer-local summaries | mitigate | Tests and implementation consume shared `InboundPeerServingStatus` fields rather than local recomputation. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc current_inbound_status_projects_runtime_peer_policy_bridge -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_peer_policy_log_record -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc record_inbound_peer_policy_event_appends_inbound_peer_policy_log_record -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status::render --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
Status, RPC, CLI, support, and redaction surfaces expose bounded Phase 96 peer-policy bridge evidence from the shared status contract without leaking raw peer-policy material or implying public banlist parity.
</success_criteria>

<output>
After completion, create `.planning/phases/96-peer-policy-runtime-bridge/96-03-SUMMARY.md`.
</output>
