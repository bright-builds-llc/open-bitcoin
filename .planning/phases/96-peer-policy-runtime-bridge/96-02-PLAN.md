---
phase: 96-peer-policy-runtime-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/inbound.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
autonomous: true
requirements: [EVICT-03, EVICT-04, DOS-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 96-2026-06-28T02-38-04
generated_at: 2026-06-28T02:43:32.273Z
must_haves:
  truths:
    - "ManagedPeerNetwork projects actual peer-policy runtime decisions instead of empty decision slices."
    - "Reconnect suppression uses `remote_addr.ip()` and `now_unix_seconds`, not aggregate active-ban or discouraged-peer counts."
    - "Runtime listener reconnect suppression records existing bounded Phase 94 labels without changing public inbound defaults."
    - "Matching and non-matching remotes are both covered by deterministic tests."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "ManagedPeerNetwork peer-policy runtime projection"
      contains: "peer_policy_runtime_state"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Scoped reconnect suppression input by remote address"
      contains: "remote_addr.ip()"
    - path: "packages/open-bitcoin-rpc/src/inbound_listener/tests.rs"
      provides: "Loopback-safe reconnect suppression coverage"
      contains: "reconnect_suppression_uses_matching_remote_policy_state"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      to: "packages/open-bitcoin-rpc/src/context/network.rs"
      via: "listener asks context for scoped reconnect suppression input"
      pattern: "reconnect_suppression_input_for_remote_addr"
    - from: "packages/open-bitcoin-node/src/network.rs"
      to: "packages/open-bitcoin-node/src/network/inbound.rs"
      via: "ManagedPeerPolicyInfo::from_policy_decisions receives actual slices"
      pattern: "from_policy_decisions"
---

<objective>
Wire pure peer-policy runtime state into managed node/RPC runtime behavior.

Purpose: Close the concrete audit failures where `peer_policy_info()` passes empty policy slices and reconnect suppression ignores the connecting remote address.
Output: Managed projection APIs, scoped reconnect suppression, and deterministic node/RPC tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
@.planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
@.planning/phases/96-peer-policy-runtime-bridge/96-01-SUMMARY.md
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/network/inbound.rs
@packages/open-bitcoin-node/src/network/tests.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/inbound_listener.rs
@packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project actual runtime peer-policy decisions</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/inbound.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
    .planning/phases/96-peer-policy-runtime-bridge/96-01-SUMMARY.md
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/inbound.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-network/src/peer_policy.rs
  </read_first>
  <behavior>
    - Test 1: `managed_peer_policy_info_projects_active_runtime_bans` records an active ban in managed runtime state and observes `active_bans == 1` plus latest label `ban_active`.
    - Test 2: `managed_peer_policy_info_projects_manual_unbans` records a manual unban and observes `manual_unbans == 1` plus latest label `unbanned`.
    - Test 3: `managed_peer_policy_info_projects_runtime_misbehavior` records protected misbehavior and observes `misbehavior_observations == 1` and `protected_no_actions == 1`.
    - Test 4: existing eviction projection tests still pass.
  </behavior>
  <action>Add narrow `ManagedPeerNetwork` methods to record peer-policy runtime decisions through the Plan 01 pure state, for example `record_peer_policy_ban`, `record_peer_policy_unban`, `record_peer_policy_discouragement`, and `record_peer_policy_misbehavior`, using concrete Phase 93 types. Update `ManagedPeerNetwork::peer_policy_info()` so `ManagedPeerPolicyInfo::from_policy_decisions` receives `self.peer_manager.peer_policy_runtime_state().misbehavior_decisions()`, `ban_decisions()`, and `unban_decisions()` instead of `&[], &[], &[]`. Keep eviction candidate projection unchanged. Do not add renderer-local summaries or persistent metric samples in this plan.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node managed_peer_policy_info -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "record_peer_policy_ban" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/tests.rs
    grep -n "record_peer_policy_unban" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/tests.rs
    grep -n "record_peer_policy_misbehavior" packages/open-bitcoin-node/src/network.rs packages/open-bitcoin-node/src/network/tests.rs
    grep -n "misbehavior_decisions()" packages/open-bitcoin-node/src/network.rs
    grep -n "ban_decisions()" packages/open-bitcoin-node/src/network.rs
    grep -n "unban_decisions()" packages/open-bitcoin-node/src/network.rs
    ! grep -n "from_policy_decisions(.*&\\[\\].*&\\[\\].*&\\[\\]" packages/open-bitcoin-node/src/network.rs
  </acceptance_criteria>
  <done>Managed peer-policy evidence is backed by real runtime policy decisions, not empty projection slices.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use scoped policy state for reconnect suppression</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
    .planning/phases/96-peer-policy-runtime-bridge/96-01-SUMMARY.md
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
    packages/open-bitcoin-network/src/resource.rs
  </read_first>
  <behavior>
    - Test 1: `reconnect_suppression_uses_matching_remote_policy_state` records an active ban for `127.0.0.2`, asks for `127.0.0.2:18444`, and gets `banned: true`.
    - Test 2: `reconnect_suppression_ignores_non_matching_remote_policy_state` records an active ban for `127.0.0.2`, asks for `127.0.0.3:18444`, and gets `banned: false` and `discouraged: false`.
    - Test 3: `listener_records_scoped_banned_reconnect_suppression` proves the listener still records latest resource label `reconnect_suppressed_banned` for a matching banned remote.
    - Test 4: `listener_records_scoped_discouraged_reconnect_suppression` proves the listener records `reconnect_suppressed_discouraged` for discouraged remotes.
  </behavior>
  <action>Change `ManagedRpcContext::reconnect_suppression_input_for_remote_addr` so it calls a managed network or peer-manager scoped query with `remote_addr.ip()` and `now_unix_seconds`. Remove the ignored-input tuple `let _ = (remote_addr, now_unix_seconds);`. Remove aggregate reconnect suppression based on `peer_policy_info.active_bans > 0` or `peer_policy_info.discouraged_peers > 0`. Keep `packages/open-bitcoin-rpc/src/inbound_listener.rs` consuming `ResourceGovernancePolicy::decide_reconnect` so existing labels `reconnect_suppressed_banned` and `reconnect_suppressed_discouraged` remain stable. Add test setup helpers that seed managed peer-policy runtime state directly; do not require public peers, DNS, service-manager operations, or wall-clock sleeps.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc reconnect_suppression -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "remote_addr.ip()" packages/open-bitcoin-rpc/src/context/network.rs
    grep -n "now_unix_seconds" packages/open-bitcoin-rpc/src/context/network.rs
    ! grep -n "let _ = (remote_addr, now_unix_seconds)" packages/open-bitcoin-rpc/src/context/network.rs
    ! grep -n "active_bans > 0" packages/open-bitcoin-rpc/src/context/network.rs
    ! grep -n "discouraged_peers > 0" packages/open-bitcoin-rpc/src/context/network.rs
    grep -n "reconnect_suppression_uses_matching_remote_policy_state" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
    grep -n "reconnect_suppression_ignores_non_matching_remote_policy_state" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
    grep -n "reconnect_suppressed_banned" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  </acceptance_criteria>
  <done>Runtime reconnect suppression is scoped to the connecting remote address and timestamp while preserving Phase 94 evidence labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure policy state -> managed runtime projection | Pure decisions become managed status and reconnect suppression inputs. |
| connecting socket address -> reconnect suppression | Runtime listener uses remote address to decide suppression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-01 | Denial of Service | reconnect suppression | mitigate | Tests prove non-matching remote addresses are not suppressed by unrelated active bans. |
| T-96-02-02 | Tampering | peer-policy projection | mitigate | Projection uses authoritative runtime state slices instead of empty placeholders or aggregate counters. |
| T-96-02-03 | Repudiation | listener evidence | mitigate | Listener records stable `reconnect_suppressed_banned` and `reconnect_suppressed_discouraged` events. |
| T-96-02-04 | Elevation of Privilege | permission/protection behavior | mitigate | Protected/no-action observations remain visible through peer-policy evidence instead of bypassing records. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node managed_peer_policy_info -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc reconnect_suppression -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
The two audit failure mechanisms are removed: managed peer-policy projection no longer feeds empty decision slices, and reconnect suppression no longer ignores remote address or timestamp.
</success_criteria>

<output>
After completion, create `.planning/phases/96-peer-policy-runtime-bridge/96-02-SUMMARY.md`.
</output>
