---
phase: 96-peer-policy-runtime-bridge
plan: 04
type: execute
wave: 4
depends_on:
  - 96-01
  - 96-02
  - 96-03
files_modified:
  - scripts/check-phase96-peer-policy-runtime-bridge.ts
  - scripts/check-phase96-peer-policy-runtime-bridge.test.ts
  - scripts/verify.sh
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/index.json
  - docs/parity/source-breadcrumbs.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [EVICT-03, EVICT-04, DOS-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 96-2026-06-28T02-38-04
generated_at: 2026-06-28T02:43:32.273Z
must_haves:
  truths:
    - "A deterministic Phase 96 checker rejects empty peer-policy decision slices and aggregate-only reconnect suppression."
    - "Default verification proves Phase 96 locally without public-network, service-manager, long-running, or production-readiness gates."
    - "Parity/operator docs describe scoped runtime peer-policy bridge evidence without public banlist, relay, mempool, compact-block, public-default, or production claims."
    - "Full repo-native verification passes before phase completion."
  artifacts:
    - path: "scripts/check-phase96-peer-policy-runtime-bridge.ts"
      provides: "Phase 96 deterministic checker"
      exports: ["checkPhase96PeerPolicyRuntimeBridge"]
    - path: "scripts/check-phase96-peer-policy-runtime-bridge.test.ts"
      provides: "Mutation fixture tests for Phase 96 checker"
      contains: "fails aggregate-only reconnect suppression"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring for Phase 96"
      contains: "check-phase96-peer-policy-runtime-bridge"
    - path: "docs/parity/catalog/p2p.md"
      provides: "Scoped Phase 96 parity evidence"
      contains: "v1-9-peer-policy-runtime-bridge"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase96-peer-policy-runtime-bridge.ts"
      via: "run_step after Phase 95 checker"
      pattern: "check-phase96-peer-policy-runtime-bridge"
    - from: "scripts/check-phase96-peer-policy-runtime-bridge.ts"
      to: "packages/open-bitcoin-rpc/src/context/network.rs"
      via: "static assertion for remote_addr.ip() and no aggregate-only suppression"
      pattern: "remote_addr.ip()"
---

<objective>
Add deterministic Phase 96 checker/docs and run final repo verification.

Purpose: Make the peer-policy runtime bridge auditable and keep the release boundary from drifting into public banlist, relay, production, or public-network claims.
Output: Bun checker/test, verifier wiring, scoped parity/operator docs, source breadcrumb updates if needed, full verification evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
@.planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
@.planning/phases/96-peer-policy-runtime-bridge/96-01-SUMMARY.md
@.planning/phases/96-peer-policy-runtime-bridge/96-02-SUMMARY.md
@.planning/phases/96-peer-policy-runtime-bridge/96-03-SUMMARY.md
@scripts/check-phase94-dos-resource-governance.ts
@scripts/check-phase95-network-participation-release-boundary.ts
@scripts/check-phase95-network-participation-release-boundary.test.ts
@scripts/verify.sh
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@docs/parity/index.json
@docs/parity/source-breadcrumbs.json
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-rpc/src/context/network.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 96 deterministic checker and fixture tests</name>
  <files>scripts/check-phase96-peer-policy-runtime-bridge.ts, scripts/check-phase96-peer-policy-runtime-bridge.test.ts</files>
  <read_first>
    AGENTS.md
    standards/core/testing.md
    standards/languages/typescript-javascript.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    .planning/phases/96-peer-policy-runtime-bridge/96-RESEARCH.md
    scripts/check-phase94-dos-resource-governance.ts
    scripts/check-phase95-network-participation-release-boundary.ts
    scripts/check-phase95-network-participation-release-boundary.test.ts
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    docs/parity/catalog/p2p.md
    docs/operator/runtime-guide.md
  </read_first>
  <behavior>
    - Pass case: a fixture with pure `PeerPolicyRuntimeState`, non-empty policy decision projection, scoped `remote_addr.ip()` reconnect suppression, Phase 96 docs, and no-claim wording returns no failures.
    - Negative case: `ManagedPeerNetwork::peer_policy_info()` contains `from_policy_decisions(..., &[], &[], &[])` and checker fails.
    - Negative case: `reconnect_suppression_input_for_remote_addr` ignores `remote_addr` or `now_unix_seconds` and checker fails.
    - Negative case: reconnect suppression uses `active_bans > 0` or `discouraged_peers > 0` and checker fails.
    - Negative case: status, support, logs, or docs contain raw peer-policy material such as `peer_id=`, `raw_endpoint`, `permission_string`, `payload_bytes`, `credential`, `secret`, or socket-address-like fields and checker fails.
    - Negative case: verifier or docs introduce public-network, DNS/seed, service-manager, long-running, wildcard-listener, or production-readiness gates for Phase 96 and checker fails.
    - Negative case: docs claim public banlist, transaction relay, mempool propagation, compact block relay, public inbound default, production service, or production full-node readiness and checker fails.
    - Negative case: verifier order omits Phase 96 checker from executable `run_step` sequence and checker fails.
  </behavior>
  <action>Create `scripts/check-phase96-peer-policy-runtime-bridge.ts` with a shebang, exported `checkPhase96PeerPolicyRuntimeBridge(options: { rootDir?: string } = {}): string[]`, and CLI main consistent with Phase 94/95 checker style. Use structured file reads, no shell-embedded TypeScript, and a fixed corpus. Add assertions for `PeerPolicyRuntimeState`, `BanScope::matches_ip`, `reconnect_suppression_input_for_ip`, real `misbehavior_decisions()`, `ban_decisions()`, `unban_decisions()`, `remote_addr.ip()`, `now_unix_seconds`, `inbound_peer_policy_log_record`, `INBOUND_PEER_POLICY_LOG_SOURCE`, `redacted_peer_policy_field`, and the `Parity breadcrumbs:` block in `packages/open-bitcoin-rpc/src/context/peer_policy.rs`. Add negative assertions for `from_policy_decisions` calls that pass empty slices in `network.rs`, `_ = (remote_addr, now_unix_seconds)`, `active_bans > 0`, and `discouraged_peers > 0` in reconnect suppression code. Add raw-material scanners only over peer-policy evidence implementation and renderer surfaces, not over loopback UAT documentation: include `packages/open-bitcoin-node/src/logging.rs`, `packages/open-bitcoin-rpc/src/context/peer_policy.rs`, `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`, `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`, and `packages/open-bitcoin-cli/src/operator/support/redaction.rs`. The scanner should reject raw peer-policy output literals such as `peer_id=`, `raw_endpoint`, `permission_string`, `payload_bytes`, `credential`, `secret`, and `cookie=` unless they appear in a sanitizer predicate or redaction test fixture; it must explicitly allow repo-local loopback UAT examples in `docs/operator/runtime-guide.md` and `docs/parity/catalog/p2p.md`. Add verifier-gate assertions that reject `public-network`, `dnsseed`, `seednode`, `service-manager`, `systemd`, `launchd`, `multi-day`, `soak`, `wildcard listener`, and `production readiness` as Phase 96 default verifier requirements unless they appear in explicit no-claim/deferred/opt-in UAT context. Create `scripts/check-phase96-peer-policy-runtime-bridge.test.ts` with Arrange/Act/Assert comments and mutation fixtures for each failure class.</action>
  <verify>
    <automated>bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts</automated>
    <automated>bun run scripts/check-phase96-peer-policy-runtime-bridge.ts</automated>
  </verify>
  <acceptance_criteria>
    grep -n "checkPhase96PeerPolicyRuntimeBridge" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "from_policy_decisions" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "remote_addr.ip()" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "active_bans > 0" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "raw_endpoint" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "public-network" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "INBOUND_PEER_POLICY_LOG_SOURCE" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    grep -n "public banlist" scripts/check-phase96-peer-policy-runtime-bridge.ts scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    bun run scripts/check-phase96-peer-policy-runtime-bridge.ts
  </acceptance_criteria>
  <done>Phase 96 checker proves the runtime bridge and rejects the audit failure patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Update scoped docs, parity roots, breadcrumbs, and verifier wiring</name>
  <files>scripts/verify.sh, docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md, docs/parity/index.json, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    AGENTS.md
    standards/core/verification.md
    standards/languages/typescript-javascript.md
    .planning/phases/96-peer-policy-runtime-bridge/96-CONTEXT.md
    scripts/verify.sh
    docs/operator/runtime-guide.md
    docs/parity/catalog/p2p.md
    docs/parity/index.json
    docs/parity/source-breadcrumbs.json
    packages/open-bitcoin-network/src/peer_policy.rs
    packages/open-bitcoin-network/src/peer_policy/tests.rs
  </read_first>
  <action>Wire `bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts` and `bun run scripts/check-phase96-peer-policy-runtime-bridge.ts` into `scripts/verify.sh` after Phase 95 in both `VERIFY_COMMAND_ORDER` and executable `run_step` order. Update `docs/operator/runtime-guide.md` and `docs/parity/catalog/p2p.md` with scoped Phase 96 wording: `scoped runtime peer-policy bridge evidence`, `bounded reconnect suppression`, and `not a public banlist or production participation claim`. Because this plan touches `docs/operator/runtime-guide.md`, preserve or add repo-local UAT command examples containing `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...` and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...`; do not present only the installed `open-bitcoin` alias. Add or update `docs/parity/index.json` with a `v1-9-peer-policy-runtime-bridge` surface that cites `packages/bitcoin-knots/src/net.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/banman.h`, `packages/bitcoin-knots/src/banman.cpp`, and `packages/bitcoin-knots/src/net_permissions.cpp`. Add `packages/open-bitcoin-rpc/src/context/peer_policy.rs` to `docs/parity/source-breadcrumbs.json` with the same Knots anchors used by the source-file breadcrumb block. Run `bun run scripts/check-parity-breadcrumbs.ts --write` if the mapping or source block needs generation, then run `bun run scripts/check-parity-breadcrumbs.ts --check`. Preserve existing `peer_policy.rs` and `peer_policy/tests.rs` entries without churn.</action>
  <verify>
    <automated>rg -n "check-phase95-network-participation-release-boundary|check-phase96-peer-policy-runtime-bridge|check pure-core dependencies" scripts/verify.sh</automated>
    <automated>bun run scripts/check-phase96-peer-policy-runtime-bridge.ts</automated>
  </verify>
  <acceptance_criteria>
    grep -n "bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts" scripts/verify.sh
    grep -n "bun run scripts/check-phase96-peer-policy-runtime-bridge.ts" scripts/verify.sh
    grep -n "Phase 95 is followed by Phase 96" scripts/verify.sh
    grep -n "scoped runtime peer-policy bridge evidence" docs/operator/runtime-guide.md docs/parity/catalog/p2p.md
    grep -n "not a public banlist" docs/operator/runtime-guide.md docs/parity/catalog/p2p.md
    grep -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --" docs/operator/runtime-guide.md
    grep -n "bazel run //packages/open-bitcoin-cli:open_bitcoin --" docs/operator/runtime-guide.md
    grep -n "v1-9-peer-policy-runtime-bridge" docs/parity/index.json docs/parity/catalog/p2p.md
    grep -n "packages/bitcoin-knots/src/banman.cpp" docs/parity/index.json docs/parity/catalog/p2p.md
    grep -n "packages/open-bitcoin-rpc/src/context/peer_policy.rs" docs/parity/source-breadcrumbs.json
    bun run scripts/check-parity-breadcrumbs.ts --check
  </acceptance_criteria>
  <done>Default verification and parity/operator docs capture Phase 96 without expanding release claims.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 96 verification and refresh generated metrics</name>
  <files>docs/metrics/lines-of-code.md</files>
  <read_first>
    AGENTS.md
    standards/core/verification.md
    scripts/verify.sh
    docs/metrics/lines-of-code.md
  </read_first>
  <action>Run targeted checker/tests, targeted Rust checks from prior plans if they were not already run after final edits, then `bash scripts/verify.sh --fast` for iteration and `bash scripts/verify.sh` for final proof. If verification reports stale tracked LOC, refresh `docs/metrics/lines-of-code.md` with the repo-generated command printed by the verifier, then rerun the failing verification command. Record exact commands and outcomes in `96-04-SUMMARY.md`. Do not add public-network listener exposure, service-manager operations, DNS crawling, long-running soak, or production readiness checks.</action>
  <verify>
    <automated>bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts</automated>
    <automated>bun run scripts/check-phase96-peer-policy-runtime-bridge.ts</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc reconnect_suppression -- --nocapture</automated>
    <automated>bash scripts/verify.sh --fast</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    bun run scripts/check-phase96-peer-policy-runtime-bridge.ts
    bash scripts/verify.sh --fast
    bash scripts/verify.sh
  </acceptance_criteria>
  <done>Full Phase 96 verification is clean through the repo-native contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> static checker | Checker verifies bridge properties without executing public-network behavior. |
| docs/parity -> release claim | Documentation can accidentally imply unsupported public banlist or production behavior. |
| verify.sh -> default local workflow | Default verification must remain deterministic and local-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04-01 | Tampering | runtime bridge checker | mitigate | Checker rejects empty decision slices and aggregate-only reconnect suppression patterns. |
| T-96-04-02 | Spoofing | release docs | mitigate | Checker rejects public banlist, relay, mempool, compact-block, public-default, service, and production-readiness claims. |
| T-96-04-03 | Denial of Service | default verifier | mitigate | Verifier wiring remains Bun/static/Rust-local only; no public-network or service-manager steps. |
| T-96-04-04 | Repudiation | phase evidence | mitigate | Summary records targeted commands plus full `bash scripts/verify.sh` outcome. |
</threat_model>

<verification>
- `bun test scripts/check-phase96-peer-policy-runtime-bridge.test.ts`
- `bun run scripts/check-phase96-peer-policy-runtime-bridge.ts`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer_policy -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc reconnect_suppression -- --nocapture`
- `bash scripts/verify.sh --fast`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
Phase 96 is planned to close EVICT-03, EVICT-04, and DOS-03 bridge gaps through pure scoped policy state, managed projection, scoped runtime reconnect suppression, bounded shared evidence, deterministic checker coverage, and clean repo-native verification.
</success_criteria>

<output>
After completion, create `.planning/phases/96-peer-policy-runtime-bridge/96-04-SUMMARY.md`.
</output>
