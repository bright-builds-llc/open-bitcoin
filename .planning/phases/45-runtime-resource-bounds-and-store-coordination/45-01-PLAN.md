---
generated_by: gsd-plan-phase
phase: 45
phase_name: Runtime Resource Bounds and Store Coordination
plan: 1
lifecycle_mode: yolo
phase_lifecycle_id: "45-2026-05-26T16-41-34"
generated_at: "2026-05-26T16:41:34Z"
status: planned
requirements:
  - NODE-01
  - NODE-04
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/sync/types.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-cli/src/operator/runtime/support.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
---

# Phase 45 Plan

## Task 1: Expose Sync Resource Bounds

Extend `SyncResourcePressure` with the configured per-peer in-flight block cap, per-peer message cap, and max sync rounds. Populate those fields from `DurableSyncRuntime` status projection and preserve zero-valued placeholders only for summary-only projections that lack config context.

Acceptance:

- Durable status reports observed resource pressure and configured resource bounds together.
- CLI status and dashboard render the new fields without changing existing JSON field names.
- Existing status tests are updated for the expanded contract.

## Task 2: Add Validated Runtime Config Overrides

Extend Open Bitcoin JSONC sync config with nonzero overrides for `max_messages_per_peer`, `max_rounds`, `max_blocks_in_flight_per_peer`, and `max_blocks_in_flight_total`. Apply those overrides into `SyncRuntimeConfig` during config loading.

Acceptance:

- Valid config sets the corresponding runtime caps.
- Zero values fail config loading with field-specific diagnostics.
- Defaults remain unchanged when overrides are absent.

## Task 3: Guard Offline Mutating Sync Control

In the operator CLI offline fallback path, allow read-only `status` but reject offline `pause` and `resume` when stored runtime metadata indicates an unclean active daemon lifecycle. Return a clear second-writer conflict diagnostic that tells the operator to use live RPC or stop the daemon before mutating durable state offline.

Acceptance:

- Active, paused, recovering, or failed unclean sync metadata blocks offline mutating control.
- Clean shutdown, stopped state, or missing sync metadata remains usable for offline mutation.
- Tests cover both the refusal and allowed paths.

## Task 4: Document Operator Bounds and Coordination

Update operator-facing docs to list the runtime bounds, config keys, retention facts, and offline-control diagnostic. Include copy-pasteable repo-local Cargo and Bazel examples for status and config-driven daemon startup.

Acceptance:

- Docs explain in-flight header/block, durable write, metrics, and log bounds.
- Docs explain why offline mutating controls may refuse when live RPC is unavailable.
- Documentation uses repo-local commands.

## Task 5: Verify and Record Completion

Run focused tests while iterating, then run the required Rust and repo verification commands before committing. Create `45-SUMMARY.md` and `45-VERIFICATION.md` with command evidence and residual risks.

Acceptance:

- `cargo fmt --manifest-path packages/Cargo.toml --all`
- `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
- `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
- `cargo test --manifest-path packages/Cargo.toml --all-features`
- `bash scripts/verify.sh`
- `gsd-tools verify lifecycle 45 --require-plans --require-verification --raw`
