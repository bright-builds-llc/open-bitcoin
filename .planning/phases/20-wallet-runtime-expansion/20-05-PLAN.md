---
phase: 20-wallet-runtime-expansion
plan: 05
type: execute
wave: 4
depends_on:
  - 20-03
  - 20-04
files_modified:
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/wallet.rs
  - packages/open-bitcoin-cli/src/operator/wallet/tests.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - docs/architecture/cli-command-architecture.md
  - docs/parity/catalog/wallet.md
  - docs/parity/index.json
  - docs/parity/source-breadcrumbs.json
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - WAL-04
  - WAL-08
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-27T09-35-46
generated_at: "2026-04-27T15:15:00Z"
must_haves:
  truths:
    - "Open Bitcoin exposes its own preview/confirmation send wrapper instead of overloading baseline RPC compatibility per D-03."
    - "Backup export is Open Bitcoin-owned, one-way, and refuses unsafe target paths near external Core/Knots wallet candidates per D-13."
    - "Phase 20 closes with parity/docs/tests that explicitly record the supported wallet slice and its remaining deferrals."
  artifacts:
    - "path: packages/open-bitcoin-cli/src/operator/wallet.rs"
    - "path: packages/open-bitcoin-cli/src/operator/runtime.rs"
    - "path: packages/open-bitcoin-cli/tests/operator_binary.rs"
    - "path: docs/parity/index.json"
    - "path: README.md"
  key_links:
    - "from: packages/open-bitcoin-cli/src/operator.rs to: packages/open-bitcoin-cli/src/operator/wallet.rs via: operator wallet subcommand contract"
    - "from: packages/open-bitcoin-cli/src/operator/wallet.rs to: packages/open-bitcoin-cli/src/operator/runtime.rs via: preview/confirm and backup dispatch"
    - "from: packages/open-bitcoin-cli/tests/operator_binary.rs to: packages/open-bitcoin-cli/src/operator/wallet.rs via: end-to-end operator flow verification"
---

<objective>
Finish Phase 20 with the Open Bitcoin-owned operator workflows, backup export, and parity closeout work that the transport and status plans enable.

Purpose: Deliver D-03 and D-13 plus the remaining operator-facing pieces of WAL-04 and WAL-08, then close the phase with auditable docs and verification.
Output: operator wallet command surface, preview/confirm send wrapper, backup export flow, and Phase 20 closeout artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
@.planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
@AGENTS.md
@docs/architecture/cli-command-architecture.md
@docs/parity/catalog/wallet.md
@docs/parity/index.json
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
</context>

<interfaces>
From packages/open-bitcoin-cli/src/operator.rs:

```rust
pub enum OperatorCommand {
    Status(StatusArgs),
    Config(ConfigArgs),
    Service(ServiceArgs),
    Dashboard(DashboardArgs),
    Onboard(OnboardArgs),
}
```

From packages/open-bitcoin-cli/src/operator/runtime.rs:

```rust
pub struct OperatorCommandOutcome {
    pub stdout: OperatorStdout,
    pub stderr: OperatorStderr,
    pub exit_code: OperatorExitCode,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operator wallet workflows for preview/confirm send and Open Bitcoin backup export</name>
  <read_first>
    - packages/open-bitcoin-cli/src/operator.rs
    - packages/open-bitcoin-cli/src/operator/runtime.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
    - packages/open-bitcoin-cli/src/operator/detect.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - .planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/operator.rs,
    packages/open-bitcoin-cli/src/operator/runtime.rs,
    packages/open-bitcoin-cli/src/operator/wallet.rs,
    packages/open-bitcoin-cli/src/operator/wallet/tests.rs,
    packages/open-bitcoin-cli/tests/operator_binary.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test 1: `open-bitcoin wallet send ...` renders a deterministic preview built from the shared send-intent model and refuses mutation without explicit confirmation.
    - Test 2: confirmed operator send delegates to the wallet-scoped RPC commit path built in Plan 20-03 rather than reimplementing funding/signing locally.
    - Test 3: `open-bitcoin wallet backup ...` writes an Open Bitcoin-owned export for a managed wallet and refuses to target known Core/Knots wallet candidate paths.
    - Test 4: operator wallet tests and binary tests cover wrong-wallet selection, confirmation refusal, and unsafe backup target rejection with deterministic errors.
  </behavior>
  <action>
    1. Extend `packages/open-bitcoin-cli/src/operator.rs` with an explicit wallet operator surface for `open-bitcoin wallet send` and `open-bitcoin wallet backup`. Keep this operator-owned; do not overload `open-bitcoin-cli` compatibility parsing per D-03.
    2. Implement the new command shell in `packages/open-bitcoin-cli/src/operator/wallet.rs` and wire it through `operator/runtime.rs`:
       - preview/confirmation must be derived from the shared send-intent model and current wallet-scoped RPC/registry state per D-02 and D-03
       - commit must delegate to the `sendtoaddress`-style path from Plan 20-03
       - backup export must be Open Bitcoin-owned, one-way, and must refuse unsafe destinations that overlap detected Core/Knots wallet candidate paths per D-13
    3. Add unit and binary tests first. Keep confirmation and backup path rules deterministic and hermetic.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::wallet:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary -- --nocapture && bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - The operator path has a dedicated wallet workflow surface for preview/confirm send and backup export.
    - Preview uses the shared send-intent model and commit reuses the wallet-scoped RPC path.
    - Backup export is Open Bitcoin-owned and blocks unsafe external-wallet target paths.
    - D-17 ownership is explicit here: repo-owned operator and binary tests cover send confirmation and backup behavior before phase closeout.
  </acceptance_criteria>
  <done>Open Bitcoin exposes the required operator-owned wallet workflows without widening the baseline compatibility path.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase with parity/doc updates and repo-native verification</name>
  <read_first>
    - docs/architecture/cli-command-architecture.md
    - docs/parity/catalog/wallet.md
    - docs/parity/index.json
    - README.md
    - AGENTS.md
    - scripts/verify.sh
  </read_first>
  <files>
    docs/architecture/cli-command-architecture.md,
    docs/parity/catalog/wallet.md,
    docs/parity/index.json,
    docs/parity/source-breadcrumbs.json,
    README.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    1. Update contributor-facing and parity docs to match the shipped Phase 20 wallet surface:
       - operator wallet preview/confirm wrapper
       - `-rpcwallet` and `/wallet/<name>` routing
       - ranged single-key descriptor limits
       - wallet freshness/scanning visibility
       - Open Bitcoin-owned backup/export boundary
       - explicit deferrals that remain out of scope
    2. Update `docs/parity/index.json` and `docs/parity/catalog/wallet.md` so the milestone claims are auditable and do not overstate multiwallet, fee-estimation, or migration parity.
    3. Run the repo-native verification contract from `AGENTS.md`: `bash scripts/verify.sh`. Only then update `.planning/ROADMAP.md` and `.planning/STATE.md` to close Phase 20.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - Docs and parity ledgers describe the exact Phase 20 wallet slice and its explicit deferrals.
    - New Rust files are covered by parity breadcrumbs.
    - `bash scripts/verify.sh` passes before roadmap/state closeout metadata is updated.
    - Phase closeout traces D-17 end-to-end by naming the send, routing, cursor, rescan, and inspection/backup test surfaces that must already be green.
  </acceptance_criteria>
  <done>Phase 20 is auditable, verified, and ready for normal closeout and downstream execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> wallet send preview/confirm | Human-facing wrapper controls whether mutation proceeds |
| backup destination path -> export writer | User-supplied paths may overlap external wallet locations |
| shipped docs -> operator expectations | Parity claims influence whether users trust this wallet surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Tampering | operator wallet send flow | mitigate | Require explicit preview/confirmation in the operator wrapper before invoking the mutating commit path |
| T-20-14 | Tampering | backup export destination | mitigate | Reject paths that overlap detected external wallet candidates and keep export format Open Bitcoin-owned |
| T-20-15 | Repudiation | parity/docs closeout | mitigate | Update parity catalog/index and README so the shipped wallet slice and deferred surfaces are explicit and reviewable |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features
bash scripts/verify.sh
</verification>

<success_criteria>
  - The operator path provides a real preview/confirm send wrapper and safe backup export flow.
  - Wallet docs and parity artifacts describe the exact shipped behavior and deferrals.
  - Full repo-native verification passes before Phase 20 is marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/20-wallet-runtime-expansion/20-05-SUMMARY.md`
</output>
