---
phase: 20-wallet-runtime-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/detect.rs
  - packages/open-bitcoin-cli/src/operator/detect/tests.rs
  - packages/open-bitcoin-cli/src/operator/onboarding.rs
  - packages/open-bitcoin-cli/src/operator/onboarding/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - docs/architecture/status-snapshot.md
  - docs/parity/catalog/wallet.md
autonomous: true
requirements:
  - WAL-07
  - WAL-08
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-27T09-35-46
generated_at: "2026-04-27T15:15:00Z"
must_haves:
  truths:
    - "Status and dashboard surfaces distinguish fresh, partial, and scanning wallet state instead of relying on a raw trusted balance per D-12."
    - "Read-only external wallet inspection becomes format-aware and still never mutates Core/Knots wallet candidates per D-14."
    - "Wallet inspection output is useful for later backup/migration planning without implying Phase 21 mutation support."
  artifacts:
    - "path: packages/open-bitcoin-node/src/status.rs"
    - "path: packages/open-bitcoin-cli/src/operator/status.rs"
    - "path: packages/open-bitcoin-cli/src/operator/detect.rs"
    - "path: docs/architecture/status-snapshot.md"
    - "path: docs/parity/catalog/wallet.md"
  key_links:
    - "from: packages/open-bitcoin-node/src/status.rs to: packages/open-bitcoin-cli/src/operator/status.rs via: expanded WalletStatus freshness/scanning fields"
    - "from: packages/open-bitcoin-cli/src/operator/status.rs to: packages/open-bitcoin-cli/src/operator/dashboard/model.rs via: dashboard wallet projection"
    - "from: packages/open-bitcoin-cli/src/operator/detect.rs to: docs/parity/catalog/wallet.md via: documented read-only inspection scope"
---

<objective>
Make wallet freshness and read-only external wallet inspection visible and auditable on operator-facing surfaces.

Purpose: Deliver D-12 and D-14 plus the status/inspection half of WAL-07 and WAL-08 without overlapping the transport/method work in Plan 20-03.
Output: expanded shared wallet status contracts, operator rendering updates, and format-aware read-only inspection.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
@.planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
@AGENTS.md
@docs/architecture/status-snapshot.md
@docs/parity/catalog/wallet.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/detect.rs
@packages/open-bitcoin-cli/src/operator/onboarding.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
</context>

<interfaces>
From packages/open-bitcoin-node/src/status.rs:

```rust
pub struct WalletStatus {
    pub trusted_balance_sats: FieldAvailability<u64>,
}

pub struct OpenBitcoinStatusSnapshot {
    pub wallet: WalletStatus,
    pub health_signals: Vec<HealthSignal>,
}
```

From packages/open-bitcoin-cli/src/operator/detect.rs:

```rust
pub struct WalletCandidate {
    pub kind: WalletCandidateKind,
    pub path: PathBuf,
    pub maybe_name: Option<String>,
    pub present: bool,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand shared wallet freshness and scanning status through status and dashboard projections</name>
  <read_first>
    - packages/open-bitcoin-node/src/status.rs
    - packages/open-bitcoin-cli/src/operator/status.rs
    - packages/open-bitcoin-cli/src/operator/status/render.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    - docs/architecture/status-snapshot.md
    - .planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-node/src/status.rs,
    packages/open-bitcoin-cli/src/operator/status.rs,
    packages/open-bitcoin-cli/src/operator/status/render.rs,
    packages/open-bitcoin-cli/src/operator/status/tests.rs,
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs,
    docs/architecture/status-snapshot.md
  </files>
  <behavior>
    - Test 1: stopped, fresh, stale, partial, and scanning wallet states serialize distinctly in `OpenBitcoinStatusSnapshot`.
    - Test 2: status human and JSON renderers surface wallet freshness/scanning fields without hiding unavailable reasons.
    - Test 3: dashboard wallet projection renders freshness state instead of only `trusted_balance_sats`.
  </behavior>
  <action>
    1. Extend `packages/open-bitcoin-node/src/status.rs` with explicit wallet freshness and scanning fields driven by the durable rescan/job state from Plan 20-02. Keep `trusted_balance_sats` present, but do not let it imply freshness by itself per D-12.
    2. Update `packages/open-bitcoin-cli/src/operator/status.rs`, `status/render.rs`, and `operator/dashboard/model.rs` to consume the expanded shared `WalletStatus` directly. Do not create a dashboard-only wallet DTO.
    3. Update `docs/architecture/status-snapshot.md` so the shared contract documents fresh/partial/scanning wallet semantics and their unavailable-state behavior.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::status:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::dashboard::model:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Wallet freshness/scanning state is part of the shared status model and consumed directly by status/dashboard projections.
    - Status output can distinguish stopped, fresh, stale, partial, and actively scanning wallet views.
    - No renderer-local wallet freshness DTO is introduced.
    - D-17 ownership is explicit here: repo-owned status and dashboard tests prove the rescan/freshness truth model used by later closeout verification.
  </acceptance_criteria>
  <done>Operator-facing status and dashboard surfaces now tell the truth about wallet completeness and scan progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade read-only external wallet inspection to format-aware classification and backup-planning metadata</name>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/detect.rs
    - packages/open-bitcoin-cli/src/operator/detect/tests.rs
    - packages/open-bitcoin-cli/src/operator/onboarding.rs
    - packages/open-bitcoin-cli/src/operator/onboarding/tests.rs
    - docs/parity/catalog/wallet.md
    - packages/bitcoin-knots/doc/managing-wallets.md
    - .planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/operator/detect.rs,
    packages/open-bitcoin-cli/src/operator/detect/tests.rs,
    packages/open-bitcoin-cli/src/operator/onboarding.rs,
    packages/open-bitcoin-cli/src/operator/onboarding/tests.rs,
    docs/parity/catalog/wallet.md
  </files>
  <action>
    1. Extend `packages/open-bitcoin-cli/src/operator/detect.rs` so wallet candidates become format-aware and backup-planning friendly:
       - classify descriptor-wallet directory vs legacy `wallet.dat` vs unknown more reliably
       - attach read-only metadata needed for later migration/backup planning (product family confidence, chain scope, wallet name, path, and format hints)
       - keep all file access read-only and path/metadata bounded per D-14
    2. Wire the richer wallet-candidate metadata into an operator-facing consumer in `packages/open-bitcoin-cli/src/operator/onboarding.rs`:
       - show wallet format, likely wallet name, chain-scope hints, and read-only backup/migration cautions in human-facing detection output
       - keep the consumer explicitly read-only and avoid any mutation-oriented verbs or affordances reserved for Phase 21
    3. Add hermetic tests proving classification does not mutate file contents, permissions, or timestamps and that onboarding/detection output renders the richer wallet metadata without reading secrets into output.
    4. Update `docs/parity/catalog/wallet.md` to record the new operator-visible inspection surface honestly, including what remains deferred to Phase 21.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::detect:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::onboarding:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - External wallet candidates are classified with richer read-only metadata useful for backup and migration planning.
    - Operator-facing onboarding or detection output surfaces the richer wallet metadata in a read-only inspection path instead of keeping it internal-only.
    - Detection tests prove no writes or destructive side effects occur during inspection.
    - Parity docs describe the inspection boundary as read-only and Phase 21 still owns mutation/migration.
    - D-17 ownership is explicit here: repo-owned inspection tests cover the read-only external-wallet half of the required Phase 20 test matrix.
  </acceptance_criteria>
  <done>External wallet inspection is more useful for operators while staying strictly read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable wallet state -> status surfaces | Internal freshness and scan progress become operator-visible output |
| filesystem metadata -> read-only detector | External wallet paths are untrusted and must not be mutated |
| detector output -> migration planning docs | Classification data informs later user decisions and must stay honest about uncertainty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Information Disclosure | status.rs wallet freshness | mitigate | Surface freshness and scanning state explicitly so stale balances are not mistaken for final balances |
| T-20-11 | Tampering | operator/detect.rs external wallet inspection | mitigate | Keep inspection read-only, test for unchanged bytes/timestamps, and avoid any write-capable APIs |
| T-20-12 | Repudiation | wallet parity/docs | mitigate | Update parity catalog and status contract docs so the shipped inspection boundary is explicit and auditable |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli -p open-bitcoin-node --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-cli -p open-bitcoin-node --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status:: -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli -p open-bitcoin-node --all-features
bash scripts/verify.sh
</verification>

<success_criteria>
  - Shared status surfaces express wallet freshness and scanning honestly.
  - External wallet inspection is format-aware, hermetic, and still strictly read-only.
  - Dashboard and parity docs consume the same truth instead of inventing special-case wallet state.
</success_criteria>

<output>
After completion, create `.planning/phases/20-wallet-runtime-expansion/20-04-SUMMARY.md`
</output>
