---
phase: 20-wallet-runtime-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - packages/open-bitcoin-node/src/wallet.rs
  - packages/open-bitcoin-node/src/wallet_registry.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/storage.rs
  - packages/open-bitcoin-node/src/storage/fjall_store.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
  - packages/open-bitcoin-node/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - WAL-05
  - WAL-06
  - WAL-07
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-27T09-35-46
generated_at: "2026-04-27T15:15:00Z"
must_haves:
  truths:
    - "Open Bitcoin persists named wallet identity and selection metadata instead of relying on one anonymous managed wallet per D-05."
    - "Descriptor range/cursor state and rescan job checkpoints survive restart in Fjall-backed storage per D-10 and D-11."
    - "The node shell reloads persisted wallet rescan jobs after restart and advances them against durable sync state instead of leaving D-11 at DTO-only persistence."
    - "Wallet registry persistence stays in the node shell and keeps pure wallet code free of I/O per D-15 and D-16."
  artifacts:
    - "path: packages/open-bitcoin-node/src/wallet_registry.rs"
    - "path: packages/open-bitcoin-node/src/wallet.rs"
    - "path: packages/open-bitcoin-node/src/sync.rs"
    - "path: packages/open-bitcoin-node/src/storage/fjall_store.rs"
    - "path: packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs"
  key_links:
    - "from: packages/open-bitcoin-node/src/wallet_registry.rs to: packages/open-bitcoin-node/src/storage/fjall_store.rs via: registry snapshot + selected-wallet persistence"
    - "from: packages/open-bitcoin-node/src/wallet_registry.rs to: packages/open-bitcoin-node/src/sync.rs via: persisted rescan jobs -> restart-time advancement loop"
    - "from: packages/open-bitcoin-node/src/wallet.rs to: packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs via: upgraded wallet snapshot DTO fields"
    - "from: packages/open-bitcoin-node/src/storage/fjall_store.rs to: packages/open-bitcoin-node/src/storage/fjall_store/tests.rs via: reopen/restart assertions"
---

<objective>
Introduce the durable named-wallet registry, restart-safe wallet persistence, and restart-time rescan orchestration shell inside `open-bitcoin-node`.

Purpose: Implement D-05, D-10, D-11, D-16, and the node-owned persistence/runtime half of WAL-05 through WAL-07 before transport and CLI work starts routing wallet scope.
Output: registry/store contracts, upgraded Fjall codecs, restart-safe rescan checkpoint persistence, and startup/runtime continuation of in-flight rescans.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
@.planning/phases/20-wallet-runtime-expansion/20-RESEARCH.md
@AGENTS.md
@docs/architecture/storage-decision.md
@packages/open-bitcoin-node/src/wallet.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/storage.rs
@packages/open-bitcoin-node/src/storage/fjall_store.rs
@packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs
@packages/open-bitcoin-wallet/src/wallet.rs
</context>

<interfaces>
From packages/open-bitcoin-node/src/wallet.rs:

```rust
pub trait WalletStore {
    fn load_snapshot(&self) -> Option<WalletSnapshot>;
    fn save_snapshot(&mut self, snapshot: WalletSnapshot);
}

pub struct ManagedWallet<S> {
    store: S,
    wallet: Wallet,
}
```

From packages/open-bitcoin-node/src/storage/fjall_store.rs:

```rust
pub fn save_wallet_snapshot(&self, snapshot: &WalletSnapshot, mode: PersistMode) -> Result<(), StorageError>;
pub fn load_wallet_snapshot(&self) -> Result<Option<WalletSnapshot>, StorageError>;
pub fn save_runtime_metadata(&self, metadata: &RuntimeMetadata, mode: PersistMode) -> Result<(), StorageError>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing registry and persistence contracts for named wallets and rescan jobs</name>
  <read_first>
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/open-bitcoin-node/src/sync.rs
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/storage/fjall_store.rs
    - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - .planning/phases/20-wallet-runtime-expansion/20-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-node/src/wallet_registry.rs,
    packages/open-bitcoin-node/src/wallet.rs,
    packages/open-bitcoin-node/src/sync.rs,
    packages/open-bitcoin-node/src/storage.rs,
    packages/open-bitcoin-node/src/storage/fjall_store.rs,
    packages/open-bitcoin-node/src/storage/fjall_store/tests.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs,
    packages/open-bitcoin-node/src/lib.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test 1: registry persistence can save two managed wallets by name plus one explicit selected-wallet marker and reload them after reopening the store.
    - Test 2: upgraded wallet snapshot DTO round-trips range and `next_index` metadata without losing existing UTXO or tip fields.
    - Test 3: a per-wallet rescan job snapshot persists `wallet_name`, target tip, next cursor, and last checkpointed freshness state across reopen.
    - Test 4: missing selected-wallet metadata or unknown wallet-name lookup returns typed node-shell errors rather than panics.
  </behavior>
  <action>
    1. Define the registry-owned contracts in a new `packages/open-bitcoin-node/src/wallet_registry.rs`:
       - durable wallet name/id mapping
       - selected-wallet metadata
       - per-wallet rescan job DTO keyed by wallet name
       - typed node-shell errors for unknown wallet, duplicate wallet name, and stale selection
    2. Extend `packages/open-bitcoin-node/src/storage.rs` and the snapshot codec layer to represent:
       - the Phase 20 wallet snapshot upgrade from Plan 20-01
       - registry metadata and selected-wallet records
       - rescan job checkpoint records
    3. Update `docs/parity/source-breadcrumbs.json` for the new `packages/open-bitcoin-node/src/wallet_registry.rs` source file, then write failing Fjall and snapshot-codec tests first. Keep the persistence boundary shell-owned; do not move serialization or database imports into `open-bitcoin-wallet`.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node wallet:: -- --nocapture && bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - The node crate exposes a named-wallet registry contract, per-wallet rescan job shape, and a restart-time orchestration seam for resuming those jobs.
    - Restart/reopen tests fail until the new storage and registry paths are implemented.
    - Snapshot schema changes are localized to `open-bitcoin-node` storage/codec files.
    - New first-party Rust files introduced by this task have planned parity breadcrumb coverage and breadcrumb verification per repo-local AGENTS guidance.
  </acceptance_criteria>
  <done>Registry, selected-wallet, and rescan-job persistence contracts are fully defined by tests and node-shell types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable named-wallet registry and restart-safe rescan checkpoint persistence</name>
  <read_first>
    - packages/open-bitcoin-node/src/wallet_registry.rs
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/open-bitcoin-node/src/sync.rs
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/storage/fjall_store.rs
    - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
  </read_first>
  <files>
    packages/open-bitcoin-node/src/wallet.rs,
    packages/open-bitcoin-node/src/sync.rs,
    packages/open-bitcoin-node/src/wallet_registry.rs,
    packages/open-bitcoin-node/src/storage.rs,
    packages/open-bitcoin-node/src/storage/fjall_store.rs,
    packages/open-bitcoin-node/src/storage/fjall_store/tests.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec/wallet.rs,
    packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs,
    packages/open-bitcoin-node/src/lib.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <action>
    1. Implement a durable named-wallet registry in `packages/open-bitcoin-node/src/wallet_registry.rs` and adapt `packages/open-bitcoin-node/src/wallet.rs` so wallet mutations persist by wallet name instead of one anonymous snapshot per D-05.
    2. Extend `packages/open-bitcoin-node/src/storage/fjall_store.rs` and `snapshot_codec/wallet.rs` to persist:
       - upgraded ranged-descriptor wallet snapshots from Plan 20-01
       - selected-wallet metadata
       - per-wallet rescan job checkpoints with target tip and next cursor per D-11
    3. Implement node-owned restart-time rescan orchestration in `packages/open-bitcoin-node/src/sync.rs`:
       - reload persisted per-wallet rescan jobs on startup and when `rescanblockchain` enqueues new work
       - advance wallet rescans in bounded chunks against durable sync state rather than one full-snapshot rebuild
       - checkpoint progress after each chunk and expose completion/failure state for downstream RPC and status consumers
    4. Keep rescan persistence checkpointable and restart-safe:
       - persist after wallet mutation or bounded rescan progress
       - return typed storage/recovery errors on missing, corrupt, or mismatched records
       - do not invent a second database or JSON sidecar per the storage ADR and research anti-patterns
    5. Export the new registry/runtime types through `packages/open-bitcoin-node/src/lib.rs` for downstream RPC and CLI plans.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node wallet:: -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync:: -- --nocapture && bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - Reopening the node store preserves named wallets, selected-wallet metadata, descriptor cursor state, and rescan checkpoints.
    - Wallet persistence remains shell-owned in `open-bitcoin-node`; `open-bitcoin-wallet` gains no I/O dependencies.
    - Registry lookup and stale-selection failures surface typed errors instead of panics.
    - Restarting the node with an in-flight wallet rescan resumes bounded progress from persisted checkpoints instead of abandoning D-11 at stored metadata only.
    - D-17 ownership is explicit here: repo-owned node persistence tests prove restart-safe rescan checkpointing for the shared wallet registry.
  </acceptance_criteria>
  <done>Open Bitcoin has a durable named-wallet shell and restart-safe wallet persistence ready for wallet-scoped transport routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure wallet snapshot -> Fjall codec | Domain state crosses into durable serialization |
| wallet name selection -> registry lookup | Untrusted selection metadata chooses mutation target |
| rescan checkpoint -> restart recovery | Persisted progress must survive restart without replay ambiguity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | wallet_registry.rs | mitigate | Enforce unique wallet names and selected-wallet validation before mutation or lookup |
| T-20-05 | Availability | fjall_store rescan checkpoints | mitigate | Persist rescan target tip and next cursor after bounded progress, then cover reopen behavior with targeted store tests |
| T-20-06 | Integrity | snapshot_codec/wallet.rs | mitigate | Version and round-trip all new range/cursor fields so restart does not silently reset descriptor state |
</threat_model>

<verification>
cd /Users/peterryszkiewicz/Repos/open-bitcoin
cargo fmt --all
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features
bash scripts/verify.sh
</verification>

<success_criteria>
  - A durable named-wallet registry exists and is exported from `open-bitcoin-node`.
  - Wallet cursor and rescan job state survive reopen through Fjall-backed storage.
  - Downstream RPC and operator work can select wallets by name without inventing their own persistence rules.
</success_criteria>

<output>
After completion, create `.planning/phases/20-wallet-runtime-expansion/20-02-SUMMARY.md`
</output>
