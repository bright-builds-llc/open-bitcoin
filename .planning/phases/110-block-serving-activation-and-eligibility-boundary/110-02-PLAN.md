---
phase: 110-block-serving-activation-and-eligibility-boundary
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - packages/open-bitcoin-network/src/block_serving.rs
  - packages/open-bitcoin-network/src/block_serving/tests.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/block_serving.rs
  - packages/open-bitcoin-node/src/status/block_serving/tests.rs
  - docs/parity/source-breadcrumbs.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [BSRV-03, BSRV-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 110-2026-07-04T02-39-48
generated_at: 2026-07-04T03:31:49Z
must_haves:
  truths:
    - "Block-serving status is classified before any storage read or socket response."
    - "Only validated and available active-chain or recent-valid blocks can be marked as later-servable."
    - "Unknown, stale, side-chain, pruned, unavailable, unvalidated, and suppressed outcomes have stable sanitized labels and counters."
  artifacts:
    - path: "packages/open-bitcoin-network/src/block_serving.rs"
      provides: "Pure block-serving status classifier"
      exports: ["BlockServingChainPosition", "BlockServingValidationState", "BlockServingDataAvailability", "BlockServingStatusFacts", "BlockServingStatusDecision", "BlockServingStatusLabel", "classify_block_serving_status"]
    - path: "packages/open-bitcoin-node/src/status/block_serving.rs"
      provides: "Shared sanitized block-serving evidence contract"
      exports: ["BlockServingEvidenceStatus", "BlockServingActivationEvidence", "BlockServingEligibilityCounters", "BlockServingStatusCounters"]
  key_links:
    - from: "packages/open-bitcoin-network/src/block_serving.rs"
      to: "packages/open-bitcoin-node/src/status/block_serving.rs"
      via: "Shared labels/counters mirror classifier labels"
      pattern: "available_count|pruned_count|unvalidated_count"
    - from: "packages/open-bitcoin-node/src/status.rs"
      to: "packages/open-bitcoin-node/src/status/block_serving.rs"
      via: "mod block_serving; pub use block_serving::*;"
      pattern: "block_serving"
---

<objective>
Add the pure block-serving status classifier and shared sanitized evidence contract that later adapters must use before reading block data or formatting operator-facing evidence.

Purpose: Satisfy BSRV-03 and BSRV-05 while keeping Phase 111+ storage reads and actual block responses out of scope.
Output: Typed status facts, decisions, labels, tests, shared status counters, serde defaults, exports, and parity breadcrumbs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-CONTEXT.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-RESEARCH.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/94-dos-and-resource-governance/94-05-SUMMARY.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-03-SUMMARY.md

<interfaces>
From Plan 110-01:
```rust
pub struct BlockRelayActivationPolicy {
    pub block_serving: BlockServingActivationConfig,
    pub compact_relay: CompactRelayActivationConfig,
}

pub struct BlockServingEligibilityDecision {
    pub eligible: bool,
    pub reason: BlockServingEligibilityReason,
    pub advertises_public_service: bool,
}
```

From `packages/open-bitcoin-node/src/status.rs`:
```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case", tag = "state", content = "value")]
pub enum FieldAvailability<T> {
    Available(T),
    Unavailable { reason: String },
}
```

From `packages/open-bitcoin-node/src/status/relay_evidence.rs`:
```rust
pub struct RelayEvidenceStatus {
    pub activation: RelayEvidenceField<RelayActivationEvidence>,
    pub download_eligibility: RelayEvidenceField<RelayDownloadEligibilityCounters>,
    pub outcome_counters: RelayEvidenceField<RelayEvidenceCounters>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure block-serving status classifier</name>
  <files>packages/open-bitcoin-network/src/block_serving.rs, packages/open-bitcoin-network/src/block_serving/tests.rs</files>
  <read_first>
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-CONTEXT.md` decisions D-09 through D-12 and deferred scope.
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-RESEARCH.md` sections "Pattern 2: Status Classification Before Storage Read" and "Pitfall 1: blocks_by_hash Becomes The De Facto Serving Policy".
    - `packages/open-bitcoin-network/src/block_serving.rs` as produced by Plan 110-01.
    - `packages/open-bitcoin-chainstate/src/engine.rs` for active-chain and validated-state vocabulary only; do not import storage or mutate chainstate.
    - `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/validation.cpp`, and `packages/bitcoin-knots/src/node/blockstorage.cpp` as parity anchors for request allowance, validation state, and availability/pruned concepts.
  </read_first>
  <behavior>
    - RED test: an active-chain validated block with `BlockServingDataAvailability::Available` returns label `available`, `allow_storage_read == true`, and `may_serve_block == true`.
    - RED test: a recent-valid block with available data is later-servable; stale and side-chain blocks are not.
    - RED test: pruned, unavailable, unknown, unvalidated, and suppressed facts never allow storage reads or serving.
    - RED test: `validated`, `available`, `stale`, `side_chain`, `pruned`, `unavailable`, `unvalidated`, `unknown`, and `suppressed` labels are stable.
    - RED test: classifier accepts only fact data and has no storage, mempool, socket, filesystem, clock, service-manager, or runtime adapter inputs.
  </behavior>
  <action>Extend `packages/open-bitcoin-network/src/block_serving.rs` with `BlockServingChainPosition::{Active, RecentValid, Stale, SideChain, Unknown}`, `BlockServingValidationState::{Validated, Unvalidated, Unknown}`, `BlockServingDataAvailability::{Available, Pruned, Unavailable, Unknown}`, `BlockServingStatusLabel::{Validated, Available, Stale, SideChain, Pruned, Unavailable, Unvalidated, Unknown, Suppressed}`, `BlockServingStatusFacts`, `BlockServingStatusDecision`, and `classify_block_serving_status(&BlockServingStatusFacts) -> BlockServingStatusDecision`. `BlockServingStatusLabel::as_str()` must return exactly `validated`, `available`, `stale`, `side_chain`, `pruned`, `unavailable`, `unvalidated`, `unknown`, and `suppressed`. Use this conservative precedence: `suppressed` first, unknown chain/validation/data facts as `unknown`, `Unvalidated` as `unvalidated`, `Stale` as `stale`, `SideChain` as `side_chain`, `Pruned` as `pruned`, `Unavailable` as `unavailable`, active or recent-valid plus validated plus available as `available`, and active or recent-valid plus validated plus data unknown as `validated`. Set `allow_storage_read` and `may_serve_block` true only for `available`. Keep the classifier pure data-in/data-out; do not call chainstate engines, durable storage, mempool, sockets, or runtime adapters. Add focused tests in `block_serving/tests.rs` with Arrange/Act/Assert comments and no sleeps or public-network inputs.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib block_serving_status -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub enum BlockServingChainPosition|pub enum BlockServingValidationState|pub enum BlockServingDataAvailability|pub enum BlockServingStatusLabel|pub fn classify_block_serving_status" packages/open-bitcoin-network/src/block_serving.rs` finds the classifier contract.
    - `rg -n "validated|available|stale|side_chain|pruned|unavailable|unvalidated|unknown|suppressed" packages/open-bitcoin-network/src/block_serving.rs packages/open-bitcoin-network/src/block_serving/tests.rs` finds every stable label.
    - `! rg -n "Fjall|Store|Tcp|Socket|mempool|std::fs|SystemTime|Instant|sleep" packages/open-bitcoin-network/src/block_serving.rs` passes.
  </acceptance_criteria>
  <done>The network crate exposes a pure status classifier that distinguishes every required safe outcome before later adapters may read or serve block data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared sanitized block-serving evidence status</name>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/block_serving.rs, packages/open-bitcoin-node/src/status/block_serving/tests.rs, docs/parity/source-breadcrumbs.json, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-CONTEXT.md` decisions D-11, D-12, D-17, and D-20.
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-RESEARCH.md` sections "Pitfall 4: Status Labels Leak Sensitive Or High-Cardinality Data" and "Shared status/evidence first".
    - `packages/open-bitcoin-node/src/status.rs` for `FieldAvailability` and status module export style.
    - `packages/open-bitcoin-node/src/status/relay_evidence.rs` for shared evidence/counter patterns and unavailable/deferred defaults.
    - `packages/open-bitcoin-node/src/status/inbound.rs` for redacted inbound evidence precedent.
    - `docs/parity/source-breadcrumbs.json` before adding new Rust status files.
  </read_first>
  <behavior>
    - RED test: `BlockServingEvidenceStatus::default_unavailable()` serializes fixed unavailable reasons and default zero counters without raw peer, permission, prune-height, credential, block-hash, or transaction payload fields.
    - RED test: eligibility counters include `eligible_peer_count`, `ineligible_peer_count`, `disabled_count`, `activation_required_count`, `inbound_serving_required_count`, `permission_required_count`, `protected_not_serving_count`, `status_unavailable_count`, and `permission_effect_inactive_count`.
    - RED test: status counters include `validated_count`, `available_count`, `stale_count`, `side_chain_count`, `pruned_count`, `unavailable_count`, `unvalidated_count`, `unknown_count`, and `suppressed_count`.
  </behavior>
  <action>Create `packages/open-bitcoin-node/src/status/block_serving.rs` with a top parity breadcrumb block citing `none` only if the file is purely Open Bitcoin status infrastructure; if the implementation embeds classifier/parity semantics directly, cite `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/validation.cpp`, and `packages/bitcoin-knots/src/node/blockstorage.cpp`. Define `BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON`, `BlockServingActivationEvidence { pub block_serving_enabled: bool, pub compact_relay_enabled: bool }`, `BlockServingEligibilityCounters`, `BlockServingStatusCounters`, and `BlockServingEvidenceStatus`. Use `FieldAvailability<T>` from `status.rs`; defaults must be unavailable for uncollected activation/evidence fields and zero for safe aggregate counters only when the field is available. Add constructors `default_unavailable()` and `with_activation_eligibility_and_status(...)`. Re-export the module from `packages/open-bitcoin-node/src/status.rs` with `mod block_serving; pub use block_serving::*;`. Add `packages/open-bitcoin-node/src/status/block_serving/tests.rs` for serde/default/non-leak tests. Register both status files in `docs/parity/source-breadcrumbs.json`. Do not add CLI, dashboard, RPC, metrics, logs, support-bundle rendering, or renderer-local summaries in this plan; this creates the shared contract those later surfaces must consume.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib block_serving -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct BlockServingEvidenceStatus|pub struct BlockServingActivationEvidence|pub struct BlockServingEligibilityCounters|pub struct BlockServingStatusCounters|default_unavailable|with_activation_eligibility_and_status" packages/open-bitcoin-node/src/status/block_serving.rs` finds the shared status contract.
    - `rg -n "eligible_peer_count|protected_not_serving_count|status_unavailable_count|available_count|side_chain_count|pruned_count|unvalidated_count|suppressed_count" packages/open-bitcoin-node/src/status/block_serving.rs packages/open-bitcoin-node/src/status/block_serving/tests.rs` finds required aggregate fields and coverage.
    - `! rg -n "peer_id|endpoint|permission_string|class_name|credential|secret|prune_height|block_hash|txid|wtxid|raw_transaction" packages/open-bitcoin-node/src/status/block_serving.rs packages/open-bitcoin-node/src/status/block_serving/tests.rs` passes.
    - `rg -n "packages/open-bitcoin-node/src/status/block_serving.rs|packages/open-bitcoin-node/src/status/block_serving/tests.rs" docs/parity/source-breadcrumbs.json` finds both new Rust files.
  </acceptance_criteria>
  <done>Block-serving activation, eligibility, and status outcomes have a shared sanitized evidence contract with fixed counters and unavailable defaults, ready for later RPC/CLI/dashboard/support projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| adapter facts -> classifier | Chain/header/block availability facts become pure status decisions before reads. |
| classifier/status -> operator evidence | Internal outcomes become sanitized shared status counters and reasons. |
| status contract -> later renderers | Future RPC/CLI/dashboard/support surfaces will consume this shared contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-110-02-01 | Tampering | `classify_block_serving_status` | mitigate | Encode chain position, validation state, and data availability as enums; only `available` may set `allow_storage_read` and `may_serve_block` true. |
| T-110-02-02 | Information Disclosure | `BlockServingEvidenceStatus` | mitigate | Expose fixed aggregate counters and unavailable reasons only; tests reject peer ids, endpoints, permission strings, prune heights, block hashes, transaction ids, credentials, and raw payload terms. |
| T-110-02-03 | Repudiation | status labels | mitigate | Define stable `BlockServingStatusLabel::as_str()` outputs and mirror them in counters so later logs/metrics can be audited against fixed labels. |
| T-110-02-04 | Denial of Service | status classifier | accept | The classifier performs constant-time enum decisions over provided facts; request burst resource caps are handled in Plan 110-03. |
| T-110-02-05 | Elevation of Privilege | storage read gating | mitigate | Tests prove stale, side-chain, pruned, unavailable, unvalidated, unknown, and suppressed outcomes cannot allow storage reads or serving. |
</threat_model>

<verification>
Run after both tasks:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib block_serving -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib block_serving -- --nocapture
bun run scripts/check-parity-breadcrumbs.ts --check
bash scripts/verify.sh
```
</verification>

<success_criteria>
- BSRV-03 is satisfied by a classifier that marks only validated and available active-chain or recent-valid blocks as later-servable.
- BSRV-05 is satisfied by stable sanitized labels and aggregate counters for unknown, stale, side-chain, pruned, unavailable, unvalidated, and suppressed outcomes.
- Phase 111+ storage reads and block responses remain unimplemented and out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-02-SUMMARY.md`.
</output>
