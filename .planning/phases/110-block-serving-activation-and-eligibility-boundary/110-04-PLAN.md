---
phase: 110-block-serving-activation-and-eligibility-boundary
plan: 04
type: execute
wave: 4
depends_on: [110-03]
files_modified:
  - scripts/check-phase110-block-serving-boundary.ts
  - scripts/check-phase110-block-serving-boundary.test.ts
  - scripts/verify.sh
  - docs/architecture/config-precedence.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/checklist.md
  - docs/parity/index.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [BSRV-01, BSRV-02, BSRV-03, BSRV-05, BSRV-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 110-2026-07-04T02-39-48
generated_at: 2026-07-04T03:45:51Z
must_haves:
  truths:
    - "Docs and parity roots describe Phase 110 as a default-off activation, eligibility, status, resource, and cleanup boundary only."
    - "The deterministic checker rejects public-default, archive-node, package relay, filter serving, BIP152 implementation, public-network CI, production-readiness, service-operation, and production-funds overclaims."
    - "The repo-native verifier runs the Phase 110 checker locally and deterministically after prior phase guardrails."
  artifacts:
    - path: "docs/parity/index.json"
      provides: "Machine-readable Phase 110 parity surface with BSRV-01, BSRV-02, BSRV-03, BSRV-05, and BSRV-06 evidence"
    - path: "scripts/check-phase110-block-serving-boundary.ts"
      provides: "Deterministic Phase 110 boundary checker"
    - path: "scripts/verify.sh"
      provides: "Default local verification wiring for the Phase 110 checker"
  key_links:
    - from: "docs/parity/index.json"
      to: "docs/parity/catalog/p2p.md"
      via: "surface id v2-1-block-serving-activation-eligibility-boundary"
      pattern: "v2-1-block-serving-activation-eligibility-boundary"
    - from: "scripts/check-phase110-block-serving-boundary.ts"
      to: "docs/parity/index.json"
      via: "fixed file list and required requirement ids"
      pattern: "BSRV-01|BSRV-02|BSRV-03|BSRV-05|BSRV-06"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase110-block-serving-boundary.ts"
      via: "bun test and bun run steps after Phase 108 guardrails"
      pattern: "check-phase110-block-serving-boundary"
---

<objective>
Document and guard the Phase 110 default-off block-serving boundary after the activation, eligibility, status, resource, and cleanup contracts exist.

Purpose: Make BSRV-01, BSRV-02, BSRV-03, BSRV-05, and BSRV-06 difficult to misrepresent without adding serving effects, BIP152 codecs, compact reconstruction, or public-network gates.
Output: Docs/parity evidence updates, deterministic Bun checker, checker tests, default verifier wiring, and local verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-CONTEXT.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-RESEARCH.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-01-PLAN.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-02-PLAN.md
@.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-03-PLAN.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@scripts/check-phase100-relay-activation-boundary.ts
@scripts/check-phase100-relay-activation-boundary.test.ts
@scripts/check-phase107-runtime-relay-activation-download-eligibility.ts
@scripts/verify.sh

<interfaces>
From Plans 110-01 through 110-03:
```rust
pub struct BlockRelayActivationPolicy {
    pub block_serving: BlockServingActivationConfig,
    pub compact_relay: CompactRelayActivationConfig,
}

pub fn classify_block_serving_eligibility(input: &BlockServingEligibilityInput) -> BlockServingEligibilityDecision;
pub fn classify_block_serving_status(facts: &BlockServingStatusFacts) -> BlockServingStatusDecision;
pub fn evaluate_block_serving_resource_gate(policy: &ResourceGovernancePolicy, input: BlockServingResourceGateInput) -> BlockServingResourceGateDecision;
pub fn classify_block_inflight_cleanup(input: &BlockInFlightCleanupInput) -> BlockInFlightCleanupDecision;
```

From `packages/open-bitcoin-node/src/status/block_serving.rs`:
```rust
pub struct BlockServingEvidenceStatus {
    pub activation: FieldAvailability<BlockServingActivationEvidence>,
    pub eligibility: FieldAvailability<BlockServingEligibilityCounters>,
    pub status: FieldAvailability<BlockServingStatusCounters>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update docs and parity evidence for the bounded Phase 110 claim</name>
  <files>docs/architecture/config-precedence.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md, docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md, docs/parity/checklist.md, docs/parity/index.json, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-CONTEXT.md` decisions D-17 through D-19 and the deferred section.
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-RESEARCH.md` sections "Pitfall 3: Public-Service Claims Leak Through Docs" and "Validation Architecture".
    - `docs/architecture/config-precedence.md`, `docs/architecture/status-snapshot.md`, `docs/architecture/operator-observability.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/p2p.md`, `docs/parity/checklist.md`, and `docs/parity/index.json` before edits.
    - `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-01-PLAN.md`, `110-02-PLAN.md`, and `110-03-PLAN.md` for the exact exported type and label names to document.
  </read_first>
  <behavior>
    - RED test or checker fixture: docs/parity evidence contains config keys `block_serving.enabled` and `block_serving.compact_relay_enabled`.
    - RED test or checker fixture: docs/parity evidence contains CLI flags `-openbitcoinblockserving` and `-openbitcoincompactrelay`.
    - RED test or checker fixture: docs/parity evidence contains shared status `BlockServingEvidenceStatus`, status labels `validated`, `available`, `stale`, `side_chain`, `pruned`, `unavailable`, `unvalidated`, `unknown`, and `suppressed`, plus resource/cleanup labels `block_request_cap_reached`, `block_inflight_cleanup_released`, and `block_inflight_limit_still_reached`.
    - RED test or checker fixture: parity evidence contains requirements `BSRV-01`, `BSRV-02`, `BSRV-03`, `BSRV-05`, `BSRV-06` and surface id `v2-1-block-serving-activation-eligibility-boundary`.
  </behavior>
  <action>Update docs and parity roots to describe Phase 110 as a default-off boundary only: config keys `block_serving.enabled`, `block_serving.compact_relay_enabled`, CLI flags `-openbitcoinblockserving` and `-openbitcoincompactrelay`, shared status evidence from `BlockServingEvidenceStatus`, classifier labels `validated`, `available`, `stale`, `side_chain`, `pruned`, `unavailable`, `unvalidated`, `unknown`, `suppressed`, eligibility labels from Plan 110-01, resource label `block_request_cap_reached`, and cleanup labels from Plan 110-03. In `docs/operator/runtime-guide.md`, include repo-local command forms using `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...` and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...`; keep public-network block-serving or compact-relay review explicitly opt-in and outside `bash scripts/verify.sh`. Register or update the parity surface id `v2-1-block-serving-activation-eligibility-boundary` in `docs/parity/index.json`, `docs/parity/checklist.md`, and `docs/parity/catalog/p2p.md` with requirements `BSRV-01`, `BSRV-02`, `BSRV-03`, `BSRV-05`, and `BSRV-06` plus Knots anchors `net_permissions.h`, `net_permissions.cpp`, `net.cpp`, `net_processing.cpp`, `validation.cpp`, `node/blockstorage.cpp`, `p2p_getdata.py`, and `p2p_permissions.py`. Do not describe Phase 110 as serving full blocks, implementing compact relay, changing public defaults, enabling archive behavior, or supporting package/filter serving.</action>
  <verify>
    <automated>rg -n "block_serving.enabled|block_serving.compact_relay_enabled|-openbitcoinblockserving|-openbitcoincompactrelay|BlockServingEvidenceStatus|block_request_cap_reached|block_inflight_cleanup_released|block_inflight_limit_still_reached" docs/architecture/config-precedence.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/operator/runtime-guide.md docs/parity/catalog/p2p.md docs/parity/checklist.md docs/parity/index.json</automated>
    <automated>rg -n "BSRV-01|BSRV-02|BSRV-03|BSRV-05|BSRV-06|v2-1-block-serving-activation-eligibility-boundary" docs/parity/catalog/p2p.md docs/parity/checklist.md docs/parity/index.json</automated>
  </verify>
  <acceptance_criteria>
    - Required config keys, CLI flags, status labels, resource labels, cleanup labels, requirement ids, and parity surface id appear in the docs/parity evidence roots.
    - Public-network block-serving or compact-relay review is documented as opt-in UAT guidance only.
    - Docs do not contain positive Phase 110 claims for public serving defaults, archive-node behavior, package relay, bloom/filter serving, compact filter serving, BIP152 implementation, compact reconstruction, production full-node readiness, production service operation, or production-funds wallet use.
  </acceptance_criteria>
  <done>Docs and parity roots truthfully describe Phase 110 as a default-off policy/status/resource/cleanup boundary with exact evidence terms and no expanded capability claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 110 checker and verifier wiring</name>
  <files>scripts/check-phase110-block-serving-boundary.ts, scripts/check-phase110-block-serving-boundary.test.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `scripts/check-phase100-relay-activation-boundary.ts` and `.test.ts` for no-claim checker style.
    - `scripts/check-phase107-runtime-relay-activation-download-eligibility.ts` for status/evidence/root checker style.
    - `scripts/verify.sh` Phase 108 checker wiring location; add Phase 110 after Phase 108 and before pure-core/build verification.
    - The docs/parity files updated by Task 1.
  </read_first>
  <behavior>
    - RED test: the checker fails if any required Phase 110 requirement ID `BSRV-01`, `BSRV-02`, `BSRV-03`, `BSRV-05`, or `BSRV-06` is missing from docs/parity evidence.
    - RED test: the checker fails if required config keys, CLI flags, type names, labels, source files, or Knots anchors are missing.
    - RED test: the checker fails on positive claims for public block serving by default, archive-node behavior, package relay, bloom/filter serving, compact filter serving, public-network CI, production full-node readiness, production service operation, production-funds wallet use, full block serving responses, BIP152 codecs, compact reconstruction, `getblocktxn`, or `blocktxn`.
    - RED test: the checker allows explicit no-claim or deferred wording for out-of-scope surfaces.
    - RED test: the checker fails if `scripts/verify.sh` omits `bun test scripts/check-phase110-block-serving-boundary.test.ts` or `bun run scripts/check-phase110-block-serving-boundary.ts`.
  </behavior>
  <action>Create `scripts/check-phase110-block-serving-boundary.ts` and `.test.ts` using Bun/TypeScript, no Python, no heredoc-embedded logic, fixed target file lists, and mutation tests. The checker must validate requirements, evidence roots, required type/config/label terms, Knots anchors, verifier wiring, and no-claim language. Wire the checker in `scripts/verify.sh` immediately after the Phase 108 checker/test pair and before broader pure-core/build verification. The checker must allow explicit no-claim/deferred wording but reject positive overclaims for public serving defaults, archive-node behavior, BIP152 implementation, package relay, bloom/filter serving, compact filter serving, public-network CI, production full-node readiness, production service operation, production-funds wallet use, and Phase 111+ response/reconstruction behavior. Do not add public-network, wall-clock soak, service-manager, production-deployment, schema-push, database, ORM, or public CI gates for Phase 110.</action>
  <verify>
    <automated>bun test scripts/check-phase110-block-serving-boundary.test.ts</automated>
    <automated>bun run scripts/check-phase110-block-serving-boundary.ts</automated>
    <automated>rg -n "check-phase110-block-serving-boundary.test.ts|check-phase110-block-serving-boundary.ts" scripts/verify.sh</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bun test scripts/check-phase110-block-serving-boundary.test.ts` passes and includes mutation cases for missing requirement IDs, missing labels, forbidden public/default/production/archive claims, forbidden BIP152/response claims, and verifier omission.
    - `bun run scripts/check-phase110-block-serving-boundary.ts` passes on the updated repository.
    - `scripts/verify.sh` runs both the checker test and checker after Phase 108 guardrails.
    - `bash scripts/verify.sh` remains deterministic and contains no new public-network, wall-clock soak, service-manager, production-deployment, schema-push, database, ORM, or public CI gate for Phase 110.
  </acceptance_criteria>
  <done>Phase 110 has deterministic docs/parity/verifier evidence for the default-off activation, eligibility, status, resource-governance, cleanup, and no-claim boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| docs/parity text -> operator expectations | Human-facing artifacts can accidentally imply public/default/production capability. |
| checker -> default verifier | Deterministic static checks become release guardrails in `bash scripts/verify.sh`. |
| planning output -> implementation executors | The docs/checker plan depends on completed Phase 110 policy/status/resource contracts and must not invent serving behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-110-04-01 | Information Disclosure | docs/status evidence | mitigate | Docs and checker require fixed labels and reject raw peer, permission, prune-height, credential, block/transaction payload, and dynamic-label evidence. |
| T-110-04-02 | Repudiation | release/parity claims | mitigate | `scripts/check-phase110-block-serving-boundary.ts` checks requirements, evidence roots, Knots anchors, verifier wiring, and no-claim language deterministically. |
| T-110-04-03 | Tampering | `scripts/verify.sh` | mitigate | Mutation tests fail when Phase 110 checker wiring is removed; final verification runs full `bash scripts/verify.sh`. |
| T-110-04-04 | Elevation of Privilege | operator-facing wording | mitigate | Checker rejects claims that default daemon startup, transaction relay, permissioned peers, or docs activate public block serving, compact relay, archive behavior, package relay, or filter serving. |
</threat_model>

<verification>
Run after both tasks:

```bash
bun test scripts/check-phase110-block-serving-boundary.test.ts
bun run scripts/check-phase110-block-serving-boundary.ts
bun run scripts/check-parity-breadcrumbs.ts --check
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Docs and parity evidence truthfully describe Phase 110 as default-off policy/status/resource/cleanup boundary only.
- Default verification rejects Phase 110 overclaims and remains local, deterministic, and public-network-free.
- BSRV-01, BSRV-02, BSRV-03, BSRV-05, and BSRV-06 appear in the Phase 110 parity evidence roots.
- No actual full block serving response, BIP152 codec, compact reconstruction, missing transaction round trip, schema push, package relay, filter serving, public default, or production-readiness behavior is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/110-block-serving-activation-and-eligibility-boundary/110-04-SUMMARY.md`.
</output>
