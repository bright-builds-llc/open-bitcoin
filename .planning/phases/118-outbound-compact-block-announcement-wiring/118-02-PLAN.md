---
phase: 118-outbound-compact-block-announcement-wiring
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [CMP-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 118-2026-07-11T16-07-50
generated_at: 2026-07-11T16:20:00.000Z
must_haves:
  truths:
    - "AnnounceCompactBlock with a valid block returns Some(WireNetworkMessage::CompactBlock)."
    - "AnnounceHeaders / AnnounceInventory / Suppress still emit Headers / Inv / None."
    - "AnnounceCompactBlock construction failure falls back to Headers or Inv via remote_prefers_headers, never CompactBlock."
  artifacts:
    - path: packages/open-bitcoin-network/src/peer.rs
      provides: PeerManager::announce_block_with_action
      exports: ["announce_block_with_action"]
    - path: packages/open-bitcoin-network/src/peer/tests.rs
      provides: action-aware announce emission tests
  key_links:
    - from: packages/open-bitcoin-network/src/peer.rs
      to: packages/open-bitcoin-consensus/src/compact_block_build.rs
      via: build_compact_block_payload on AnnounceCompactBlock
      pattern: "build_compact_block_payload"
    - from: packages/open-bitcoin-network/src/peer.rs
      to: packages/open-bitcoin-network/src/peer/compact_relay.rs
      via: CompactAnnouncementAction branch
      pattern: "CompactAnnouncementAction::"
---

<objective>
Extend `PeerManager` with action-aware announce emission that can return CompactBlock/Headers/Inv/None.

Purpose: Honor D-01/D-02 in the network crate that already owns announce emission, without rewriting legacy Headers/Inv-only `announce_block` call sites.
Output: `announce_block_with_action`, focused peer tests, breadcrumbs if required for touched test coverage notes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-CONTEXT.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-RESEARCH.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-01-PLAN.md

<interfaces>
From packages/open-bitcoin-network/src/peer/compact_relay.rs:
```rust
pub enum CompactAnnouncementAction {
    AnnounceCompactBlock,
    AnnounceHeaders,
    AnnounceInventory,
    Suppress,
}
```

Existing Headers/Inv helper (keep unchanged for legacy tests) in peer.rs:
```rust
pub fn announce_block(
    &self,
    peer_id: PeerId,
    block: &Block,
) -> Result<Option<WireNetworkMessage>, NetworkError>
```

Recommended new API (D-02 + Claude discretion — do not mutate `announce_block` signature):
```rust
pub fn announce_block_with_action(
    &self,
    peer_id: PeerId,
    block: &Block,
    action: CompactAnnouncementAction,
    compact_nonce: u64, // used only for AnnounceCompactBlock
) -> Result<Option<WireNetworkMessage>, NetworkError>
```

From open-bitcoin-consensus (Plan 01):
```rust
pub fn build_compact_block_payload(
    block: &Block,
    nonce: u64,
) -> Result<CompactBlockPayload, CodecError>;
```

Construction-failure fallback (D-04): on `build_compact_block_payload` Err, emit the same Headers-or-Inv choice as today's `announce_block` using `peer.remote_prefers_headers`. Caller (Plan 03) records `CompactHeadersFallback` / `CompactInventoryFallback`. Do not reopen Phase 113 policy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add announce_block_with_action emission tests</name>
  <files>packages/open-bitcoin-network/src/peer/tests.rs</files>
  <behavior>
    - Known peer + AnnounceCompactBlock + valid coinbase block + nonce → Some(CompactBlock(_)) whose payload header matches block header and nonce matches input
    - AnnounceHeaders → Some(Headers) with one header (even if peer would otherwise prefer inv)
    - AnnounceInventory → Some(Inv) with one Block inventory
    - Suppress → Ok(None)
    - Unknown peer → Err(NetworkError::UnknownPeer)
    - AnnounceCompactBlock + empty `transactions` → Some(Headers) or Some(Inv) per `remote_prefers_headers`, never CompactBlock
  </behavior>
  <action>
    Add focused tests in `packages/open-bitcoin-network/src/peer/tests.rs` near existing announce_block coverage (~line 4780). Reuse existing peer/manager fixtures (`local_config`, `header`, block builders already in the file). Write tests against `announce_block_with_action` (will fail to compile until Task 2 adds the method — that is acceptable RED). Do not change Phase 113 `decide_compact_announcement` policy tests. Do not edit node crate.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network -- announce_block_with_action -- --nocapture 2>&1 | tee /tmp/118-02-red.log; rg -q "announce_block_with_action|cannot find|failed|FAILED|error\[E" /tmp/118-02-red.log</automated>
  </verify>
  <done>Peer tests assert CompactBlock / Headers / Inv / None / unknown-peer / construction-fallback behaviors for the new API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PeerManager::announce_block_with_action</name>
  <files>packages/open-bitcoin-network/src/peer.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Task 1 behaviors pass
    - Existing `announce_block` Headers/Inv tests still pass unchanged
  </behavior>
  <action>
    Implement `PeerManager::announce_block_with_action` in `packages/open-bitcoin-network/src/peer.rs` per D-01/D-02/D-04/D-07:

    1. Resolve peer or `Err(NetworkError::UnknownPeer(peer_id))`.
    2. Match on `action`:
       - `Suppress` → `Ok(None)`
       - `AnnounceHeaders` → `Ok(Some(Headers([block.header])))`
       - `AnnounceInventory` → `Ok(Some(Inv([Block hash])))` using `block_hash(&block.header)`
       - `AnnounceCompactBlock` → call `open_bitcoin_consensus::build_compact_block_payload(block, compact_nonce)`:
         - Ok(payload) → `Ok(Some(WireNetworkMessage::CompactBlock(payload)))`
         - Err(_) → fall back to Headers if `peer.remote_prefers_headers` else Inv (same body as existing `announce_block`); do not return Err for construction failure
    3. Leave existing `announce_block` behavior and signature intact (DRY optional: have it call the headers/inv branches only — do not force compact path through it).
    4. Import `build_compact_block_payload` and `CompactAnnouncementAction` as needed; prefer early returns / `let...else`.
    5. Ensure `peer.rs` file-top breadcrumbs already cite `net_processing.cpp`; if adding a distinct new source file instead, register it — for in-place peer.rs edits, refresh breadcrumb comment only if new Knots lines are cited. Touch `docs/parity/source-breadcrumbs.json` only if a new first-party Rust source/test file is created; existing `peer/tests.rs` is already registered — do not invent unrelated breadcrumb churn.

    Do not reopen Phase 113 policy. Do not schedule compact timeouts (120). Do not inject mempool candidates (119).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network -- announce_block_with_action -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network -- announce_block -- --nocapture</automated>
  </verify>
  <done>Action-aware announce emits CompactBlock when asked; construction failure falls back to Headers/Inv; legacy announce_block tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompactAnnouncementAction → wire message | Typed policy action crosses into outbound message construction |
| Peer lookup | Unknown peer must not emit announce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-06 | Elevation of Privilege | announce_block_with_action | mitigate | Method only honors the provided action; Phase 113 still decides eligibility upstream (D-08). No new public defaults. |
| T-118-07 | Denial of Service | AnnounceCompactBlock build | mitigate | On build Err, fall back to Headers/Inv; builder never prefills all txs (Plan 01 / D-03) |
| T-118-08 | Spoofing | false CompactAnnounced evidence | accept | Evidence timing fixed in Plan 03; this plan returns the real message type for the shell to observe |
| T-118-09 | Scope creep | package/filter coupling | mitigate | Do not couple announce to tx relay, package relay, or filters (D-07) |
</threat_model>

<verification>
`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network -- announce_block_with_action`
Regression: existing `announce_block` peer tests still pass.
</verification>

<success_criteria>
- `PeerManager::announce_block_with_action` returns CompactBlock/Headers/Inv/None per action
- Construction failure never returns CompactBlock
- Legacy `announce_block` remains Headers/Inv-only for existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/118-outbound-compact-block-announcement-wiring/118-02-SUMMARY.md`
</output>
