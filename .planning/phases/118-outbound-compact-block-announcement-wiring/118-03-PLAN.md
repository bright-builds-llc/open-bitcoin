---
phase: 118-outbound-compact-block-announcement-wiring
plan: 03
type: execute
wave: 3
depends_on: [118-02]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [CMP-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 118-2026-07-11T16-07-50
generated_at: 2026-07-11T16:20:00.000Z
must_haves:
  truths:
    - "ManagedPeerNetwork::announce_block honors CompactAnnouncementAction on the wire."
    - "compact_announced_count increments only when the returned message is CompactBlock."
    - "Construction-failure evidence uses CompactHeadersFallback/CompactInventoryFallback and never CompactAnnounced without CompactBlock."
    - "Headers/Inv/Suppress policy paths keep prior reasons and do not claim compact announce."
  artifacts:
    - path: packages/open-bitcoin-node/src/network.rs
      provides: action-honor announce + post-emission evidence
    - path: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      provides: compact_announce_evidence_reason mapper (or equivalent)
    - path: packages/open-bitcoin-node/src/network/tests.rs
      provides: false-positive closure + D-09 runtime proofs
  key_links:
    - from: packages/open-bitcoin-node/src/network.rs
      to: packages/open-bitcoin-network/src/peer.rs
      via: announce_block_with_action
      pattern: "announce_block_with_action"
    - from: packages/open-bitcoin-node/src/network.rs
      to: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      via: evidence after emission
      pattern: "record_compact_announcement_evidence"
---

<objective>
Wire `ManagedPeerNetwork::announce_block` to honor compact announcement actions and record evidence only from what was actually emitted.

Purpose: Close the CMP-05 false-positive seam (decide → record CompactAnnounced → emit Headers/Inv) identified by the v2.1 milestone audit (D-01, D-04, D-05, D-06, D-09).
Output: Action-aware shell path, evidence-after-emit mapping, updated node tests, parity breadcrumbs as needed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-CONTEXT.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-RESEARCH.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-02-PLAN.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
Today's false-positive seam (packages/open-bitcoin-node/src/network.rs ~325-393):
```rust
let announcement = self.peer_manager.decide_compact_announcement_for_peer(...)?;
self.record_compact_announcement_evidence(announcement.reason); // BUG: before emit
self.peer_manager.announce_block(peer_id, block) // ignores announcement.action
    .map_err(ManagedNetworkError::from)
```

Target flow (D-01/D-05/RESEARCH):
```text
decision = decide_compact_announcement_for_peer(...)
nonce = compact_announce_nonce(block)  // first 8 LE bytes of block_hash; deterministic
maybe_msg = peer_manager.announce_block_with_action(peer_id, block, decision.action, nonce)?
evidence_reason = compact_announce_evidence_reason(decision, maybe_msg.as_ref())
record_compact_announcement_evidence(evidence_reason)
Ok(maybe_msg)
```

Evidence mapping rules:
| decision.action + message | evidence reason |
|---|---|
| any + Some(CompactBlock(_)) | CompactAnnounced |
| AnnounceCompactBlock + Some(Headers(_)) | CompactHeadersFallback |
| AnnounceCompactBlock + Some(Inv(_)) | CompactInventoryFallback |
| AnnounceCompactBlock + None | keep decision.reason only if already suppress-class; prefer not to invent — if unreachable after Plan 02 fallback, still map safely without CompactAnnounced |
| other actions | decision.reason (preserve Phase 113 reasons) (D-06/D-07) |

Nonce (Claude discretion / RESEARCH A2): derive `u64::from_le_bytes(block_hash(&block.header).to_byte_array()[0..8])` — no new getrandom dependency. Document in a one-line comment that this is a deterministic stand-in, not Knots FastRandomContext parity.

False-positive test to fix: `packages/open-bitcoin-node/src/network/tests.rs` around the HB announce that asserts `compact_announced_count == 1` without asserting CompactBlock (~520-572). Prefer asserting returned message is CompactBlock AND count == 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix false-positive tests and add evidence-mapping coverage</name>
  <files>packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/block_relay_evidence.rs</files>
  <behavior>
    - Pure mapper: AnnounceCompactBlock + Some(CompactBlock) → CompactAnnounced
    - Pure mapper: AnnounceCompactBlock + Some(Headers) → CompactHeadersFallback
    - Pure mapper: AnnounceCompactBlock + Some(Inv) → CompactInventoryFallback
    - Pure mapper: policy Suppress/Headers/Inventory decisions keep original decision.reason when message matches that path (or None for Suppress)
    - Runtime HB eligible announce (existing compact_relay_enabled fixture + sendcmpct announce:true): `announce_block` returns Some(CompactBlock(_)) AND status `compact_announced_count == 1`
    - Low-bandwidth / disabled / suppress-style path still does not increment `compact_announced_count` (extend or add focused test using existing helpers)
  </behavior>
  <action>
    1. Add a small pure helper `compact_announce_evidence_reason(decision: CompactAnnouncementDecision, maybe_message: Option<&WireNetworkMessage>) -> CompactAnnouncementReason` in `block_relay_evidence.rs` (or adjacent private module in network.rs — prefer `block_relay_evidence.rs` so unit tests can sit next to counter logic). Export as `pub(super)` for ManagedPeerNetwork.
    2. Write unit tests for the mapper covering D-05/D-06 construction-fallback vs policy-reason preservation. Construction-failure evidence is proven here even if empty-tx blocks cannot pass serving status gates to reach AnnounceCompactBlock on the full ManagedPeerNetwork path.
    3. Update the existing Phase 116-style evidence test that currently asserts `compact_announced_count == 1` after announce without checking the message: capture `let maybe_msg = network.announce_block(...)?;` and assert `matches!(maybe_msg, Some(WireNetworkMessage::CompactBlock(_)))` plus count == 1.
    4. Add or adjust a test proving a non-HB / suppress path does not bump `compact_announced_count`.

    Tests may fail until Task 2 rewires announce_block — that is expected RED for the CompactBlock assertion.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node -- compact_announce_evidence_reason -- --nocapture; cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node -- network::tests -- --nocapture 2>&1 | tee /tmp/118-03-red.log; rg -n "CompactBlock|compact_announced_count|FAILED|error" /tmp/118-03-red.log | head -40</automated>
  </verify>
  <done>Mapper unit tests exist; HB evidence test asserts CompactBlock message + count; non-compact paths assert no false CompactAnnounced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire ManagedPeerNetwork announce_block for action honor + evidence-after-emit</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/block_relay_evidence.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Task 1 runtime assertions pass
    - Pre-emission `record_compact_announcement_evidence(announcement.reason)` call is removed
    - Construction-fallback mapper + PeerManager fallback together ensure CompactAnnounced never records without CompactBlock (D-04/D-05)
  </behavior>
  <action>
    Rewrite `ManagedPeerNetwork::announce_block` tail after `decide_compact_announcement_for_peer` (D-01/D-02/D-05):

    1. **Delete** the pre-emission `self.record_compact_announcement_evidence(announcement.reason);` call.
    2. Compute `compact_nonce` via hash-derived helper (first 8 LE bytes of `block_hash(&block.header).to_byte_array()`).
    3. Call `self.peer_manager.announce_block_with_action(peer_id, block, announcement.action, compact_nonce)?` mapped through `ManagedNetworkError::from`.
    4. Compute `evidence_reason = compact_announce_evidence_reason(announcement, maybe_message.as_ref())`.
    5. `self.record_compact_announcement_evidence(evidence_reason)`.
    6. Return `Ok(maybe_message)`.

    Keep Phase 113 decision inputs unchanged (`peer_has_previous_header: true`, `peer_has_current_header: false`, existing status/gate construction). Do not reopen negotiation policy (D-08). Do not touch DurableSyncRuntime metrics projection (121), compact download timeouts (120), or mempool candidate injection (119).

    Ensure new helper source (if new file) gets parity breadcrumbs + `docs/parity/source-breadcrumbs.json` entry citing `net_processing.cpp` / BIP152 announce anchors (D-10). Existing `network.rs` / `block_relay_evidence.rs` / `network/tests.rs` should already be registered — update comments/registry only when adding new files or new Knots anchors.

    After green focused tests, run a broader node network test slice if time permits; final phase verify remains `bash scripts/verify.sh` at execute-phase closeout (D-11).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node -- compact_announce -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node -- network::tests -- --nocapture</automated>
  </verify>
  <done>Announce path emits CompactBlock when decided; evidence records only after emission from actual message; false-positive count-without-CompactBlock is gone; policy fallbacks preserve reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy decision → wire emission | CompactAnnouncementAction must match emitted message class |
| Emission → operator evidence counters | Evidence must not claim CompactAnnounced without CompactBlock on the wire |
| Activation/negotiation gates → announce | Unchanged Phase 113 gates remain the only eligibility authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-10 | Spoofing / Tampering | record_compact_announcement_evidence | mitigate | Record only after emission; CompactAnnounced iff message is CompactBlock (D-05) |
| T-118-11 | Spoofing | construction failure path | mitigate | Map AnnounceCompactBlock + Headers/Inv to CompactHeadersFallback / CompactInventoryFallback — never CompactAnnounced (D-04) |
| T-118-12 | Elevation of Privilege | announce without negotiation/activation | mitigate | Keep decide_compact_announcement_for_peer + Phase 113 gates; this plan only honors action (D-08) |
| T-118-13 | Denial of Service | oversized announce | mitigate | Relies on Plan 01 coinbase-only builder; shell does not prefill-all |
| T-118-14 | Scope creep | package/filter/public defaults / 119-121 | mitigate | Explicit non-goals: no mempool inject, no timeout bridge, no DurableSyncRuntime metrics projection, no public default changes |
</threat_model>

<verification>
Focused node tests for compact announce evidence + network::tests.
Regression: Phase 113 policy tests remain untouched and green via network crate.
Phase closeout (executor/orchestrator): `bash scripts/verify.sh` (D-11).
</verification>

<success_criteria>
- CMP-05 runtime seam closed: HB eligible path returns CompactBlock and increments compact_announced_count once
- No CompactAnnounced evidence without CompactBlock message
- Headers/Inv/Suppress paths preserve prior reason semantics
- Deferred Phases 119–121 and package/filter/public defaults untouched
</success_criteria>

<output>
After completion, create `.planning/phases/118-outbound-compact-block-announcement-wiring/118-03-SUMMARY.md`
</output>
