---
phase: 64-service-restart-and-same-datadir-resume-evidence
plan: 03
type: execute
wave: 3
depends_on: [64-01, 64-02]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - scripts/check-phase64-service-restart-resume.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [SVC-03, RR-03, OBS-04, REL-03]
user_setup: []
must_haves:
  truths:
    - "Operator docs provide repo-local Cargo and Bazel commands for service restart, status JSON, sync status JSON, and optional restart-after-progress UAT."
    - "Docs explain clean versus unclean prior shutdown, same-datadir durable progress, stale in-flight verdict, recovery category/action, and pass/fail interpretation."
    - "Parity docs frame Phase 64 as service-supervised restart/resume evidence without broad production-node or packaged-service claims."
    - "Default verification includes a deterministic checker and does not run real service-manager or public-network commands."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Phase 64 operator service restart/resume runbook"
      contains: "Service-supervised restart/resume evidence"
    - path: "docs/parity/catalog/p2p.md"
      provides: "Scoped parity wording for service restart/resume evidence"
      contains: "service-supervised restart"
    - path: "scripts/check-phase64-service-restart-resume.ts"
      provides: "Deterministic Phase 64 source/docs/default-verification checker"
      contains: "ServiceRestartResumeStatus"
    - path: "scripts/verify.sh"
      provides: "Default verification integration"
      contains: "check-phase64-service-restart-resume"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase64-service-restart-resume.ts"
      via: "Bun checker runs without real service-manager or public-network commands"
      pattern: "check-phase64-service-restart-resume"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 64-2026-06-08T03-22-46
generated_at: 2026-06-08T03:22:46.768Z
---

<objective>
Document and deterministically guard Phase 64 service restart/resume evidence.

Purpose: OBS-04 and REL-03 require repo-local operator commands and default
verification boundaries that keep live service-manager and public-network checks
opt-in only.
Output: Runtime guide updates, parity wording, Phase 64 Bun checker, verify
integration, and refreshed generated LOC evidence if needed.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-CONTEXT.md
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-RESEARCH.md
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-01-PLAN.md
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-02-PLAN.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@scripts/check-phase63-service-lifecycle.ts
@scripts/verify.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document service-supervised restart/resume review</name>
  <read_first>
    - `.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-CONTEXT.md` decisions D-10, D-11, D-12, and D-13
    - `docs/operator/runtime-guide.md`
  </read_first>
  <files>docs/operator/runtime-guide.md</files>
  <behavior>
    - Docs include `Service-supervised restart/resume evidence`.
    - Docs include Cargo and Bazel `service restart`, `status --format json`, and `sync status --format json` commands for the same datadir.
    - Docs explain `service.restart_resume`, `prior_shutdown`, `same_datadir`, `durable_progress`, `stale_inflight`, `recovery_category`, and `next_action`.
    - Docs keep `--restart-after-progress` and real launchd/systemd review opt-in UAT only.
  </behavior>
  <action>
    Add a Phase 64 subsection near the existing service lifecycle runbook and
    restart review note. Keep copy concise and operator-focused. Use repo-local
    Cargo and Bazel command forms. Do not call the feature production service,
    production full node, packaged service guarantee, or unattended
    production-node replacement.
  </action>
  <acceptance_criteria>
    - `rg -n "Service-supervised restart/resume evidence|service.restart_resume|prior_shutdown|same_datadir|durable_progress|stale_inflight|cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service restart|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service restart" docs/operator/runtime-guide.md` finds the docs and commands.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Service-supervised restart/resume evidence|service.restart_resume|stale_inflight" docs/operator/runtime-guide.md</automated>
  </verify>
  <done>Operator docs explain how to review Phase 64 evidence through repo-local commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic Phase 64 checker and verify integration</name>
  <read_first>
    - `scripts/check-phase63-service-lifecycle.ts`
    - `scripts/verify.sh`
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-cli/src/operator/status/service_status.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `docs/operator/runtime-guide.md`
  </read_first>
  <files>scripts/check-phase64-service-restart-resume.ts, scripts/verify.sh</files>
  <behavior>
    - Checker fails if shared status types or labels are missing.
    - Checker fails if status/dashboard renderers do not mention restart/resume fields.
    - Checker fails if runtime guide omits repo-local commands or opt-in UAT boundaries.
    - Checker fails if `scripts/verify.sh` invokes `run-live-mainnet-smoke`, `--restart-after-progress`, `systemctl --user restart`, or `launchctl kickstart`.
    - `scripts/verify.sh` runs the Phase 64 checker through Bun.
  </behavior>
  <action>
    Add `scripts/check-phase64-service-restart-resume.ts` following the Phase 63
    checker style: read tracked files, assert required source/docs strings, and
    assert forbidden default-verification commands are absent from
    `scripts/verify.sh`.

    Wire it into `scripts/verify.sh` near the Phase 63 checker.
  </action>
  <acceptance_criteria>
    - `bun run scripts/check-phase64-service-restart-resume.ts` passes.
    - `rg -n "check-phase64-service-restart-resume" scripts/verify.sh scripts/check-phase64-service-restart-resume.ts` finds verify integration and checker file.
    - `rg -n "run-live-mainnet-smoke|--restart-after-progress|systemctl --user restart|launchctl kickstart" scripts/verify.sh` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase64-service-restart-resume.ts</automated>
  </verify>
  <done>Default verification deterministically guards Phase 64 source/docs boundaries without live service-manager or public-network work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refresh parity wording and generated metrics evidence</name>
  <read_first>
    - `docs/parity/catalog/p2p.md`
    - `docs/metrics/lines-of-code.md`
    - `scripts/generate-loc-report.ts`
    - `scripts/test-generate-loc-report.sh`
  </read_first>
  <files>docs/parity/catalog/p2p.md, docs/metrics/lines-of-code.md</files>
  <behavior>
    - Parity docs mention service-supervised restart/resume evidence as an opt-in operator review surface.
    - Parity docs keep public-network and real service-manager checks outside default verification.
    - LOC report is current after adding the Phase 64 checker.
  </behavior>
  <action>
    Update `docs/parity/catalog/p2p.md` with scoped Phase 64 language. Refresh
    `docs/metrics/lines-of-code.md` through `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`.
    Do not hand-edit generated LOC counts.
  </action>
  <acceptance_criteria>
    - `rg -n "service-supervised restart|service.restart_resume|default verification" docs/parity/catalog/p2p.md` finds scoped parity wording.
    - `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check</automated>
  </verify>
  <done>Phase 64 docs and generated metrics evidence are fresh and scoped.</done>
</task>

</tasks>

<verification>
Targeted plan verification:

```bash
bun run scripts/check-phase64-service-restart-resume.ts
bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check
```

Aggregate phase verification:

```bash
bash scripts/verify.sh
```
</verification>
