---
phase: 64-service-restart-and-same-datadir-resume-evidence
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/service.rs
  - packages/open-bitcoin-cli/src/operator/service/tests.rs
autonomous: true
requirements: [SVC-03, RR-03]
user_setup: []
must_haves:
  truths:
    - "Human status renders service restart/resume evidence beside service lifecycle state."
    - "Status JSON, human status, and dashboard service rows agree on datadir, prior shutdown, durable progress, stale in-flight verdict, recovery category, and next action."
    - "`open-bitcoin service restart` remains an operator-initiated service-manager action and points operators to status surfaces for restart/resume evidence."
    - "Unavailable restart/resume evidence renders as explicit unavailable reasons."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      provides: "Human status restart/resume rendering"
      contains: "restart_resume"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Dashboard service restart/resume rows"
      contains: "Restart/resume"
    - path: "packages/open-bitcoin-cli/src/operator/service.rs"
      provides: "Service restart action output guidance"
      contains: "status --format json"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      to: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      via: "both render ServiceStatus.restart_resume"
      pattern: "restart_resume"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 64-2026-06-08T03-22-46
generated_at: 2026-06-08T03:22:46.768Z
---

<objective>
Render Phase 64 service restart/resume evidence consistently across status,
dashboard, and service restart output.

Purpose: D-09 requires `open-bitcoin service restart`, status human/JSON, and
dashboard surfaces to preserve lifecycle truth while exposing restart/resume
evidence when available.
Output: Renderer/dashboard rows and service restart guidance with deterministic
Rust tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-CONTEXT.md
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-RESEARCH.md
@.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-01-PLAN.md
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/status/tests.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
@packages/open-bitcoin-cli/src/operator/service.rs
@packages/open-bitcoin-cli/src/operator/service/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render restart/resume evidence in human status</name>
  <read_first>
    - `.planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-CONTEXT.md` decisions D-01, D-02, D-03, D-09
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/status/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <behavior>
    - Test: human status includes `restart_resume=` in the Service line when restart/resume evidence is available.
    - Test: rendered text includes `datadir=`, `same_datadir=true`, `prior_shutdown=clean`, `downloaded=`, `connected=`, `stale_inflight=cleared`, `recovery_category=clean_shutdown`, and `next_action=`.
    - Test: unavailable evidence renders as `restart_resume=Unavailable: ...`.
    - Test: JSON status contains the same `service.restart_resume` object with stable serde labels.
  </behavior>
  <action>
    In `operator/status/render.rs`, extend `service_text()` with a compact
    `restart_resume={...}` segment. Add helper formatters for
    `ServiceRestartResumeStatus`, `ServiceResumeProgressStatus`,
    `ServicePriorShutdownStatus`, and `ServiceStaleInflightStatus`.

    Keep the existing service lifecycle fields and order stable; append
    restart/resume at the end of the Service line so Phase 63 text remains
    readable.
  </action>
  <acceptance_criteria>
    - `rg -n "restart_resume|same_datadir|prior_shutdown|stale_inflight|ServiceRestartResumeStatus" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/status/tests.rs` finds renderers and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_resume_status_render --all-features` passes and runs nonzero tests.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_resume_status_render --all-features</automated>
  </verify>
  <done>Human and JSON status expose the same service restart/resume evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface restart/resume evidence in dashboard service rows</name>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <behavior>
    - Test: the Service dashboard section includes rows labeled `Restart/resume`, `Prior shutdown`, `Resume progress`, `Stale in-flight`, and `Resume action`.
    - Test: available evidence values match the human status labels for clean shutdown, progress heights, stale in-flight verdict, and next action.
    - Test: unavailable evidence rows use `Unavailable: ...` reasons.
  </behavior>
  <action>
    Extend `service_rows()` in `operator/dashboard/model.rs` to append compact
    restart/resume rows after existing diagnostics. Reuse the same stable labels
    as human status: `same_datadir`, `prior_shutdown`, `downloaded`, `connected`,
    `stale_inflight`, `recovery_category`, and `next_action`.
  </action>
  <acceptance_criteria>
    - `rg -n "Restart/resume|Prior shutdown|Resume progress|Stale in-flight|Resume action|restart_resume" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs` finds rows and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_service_restart_resume --all-features` passes and runs nonzero tests.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_service_restart_resume --all-features</automated>
  </verify>
  <done>Dashboard service rows show Phase 64 restart/resume evidence without losing Phase 63 lifecycle rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add service restart output guidance</name>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/service.rs`
    - `packages/open-bitcoin-cli/src/operator/service/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/service.rs, packages/open-bitcoin-cli/src/operator/service/tests.rs</files>
  <behavior>
    - Test: successful `open-bitcoin service restart` output still includes the platform restart command.
    - Test: successful restart output also includes `Review restart/resume evidence with open-bitcoin status --format json using the same --datadir`.
    - Test: failed restart output does not claim resume evidence.
  </behavior>
  <action>
    Append a single guidance line to successful restart command output after
    rendering the manager outcome. Do not change start, stop, install, uninstall,
    enable, disable, or status output.
  </action>
  <acceptance_criteria>
    - `rg -n "Review restart/resume evidence with open-bitcoin status --format json using the same --datadir|ServiceCommand::Restart" packages/open-bitcoin-cli/src/operator/service.rs packages/open-bitcoin-cli/src/operator/service/tests.rs` finds the guidance and test.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_calls_manager_and_renders_commands --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_calls_manager_and_renders_commands --all-features</automated>
  </verify>
  <done>Service restart output points operators to the status evidence surface without claiming success on its own.</done>
</task>

</tasks>

<verification>
Targeted plan verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_resume --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_service_restart_resume --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli service_restart_calls_manager_and_renders_commands --all-features
```
</verification>
