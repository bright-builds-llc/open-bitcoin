---
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 133.1-2026-07-27T16-40-47
generated_at: 2026-07-27T16:40:47.732Z
---

# Phase 133.1: Bright Builds Verification Baseline Cleanup - Context

**Gathered:** 2026-07-27
**Status:** Ready for planning
**Source:** User-supplied implementation plan

<domain>
## Phase Boundary

Restore the repository's current Bright Builds and repo-native verification baseline before Phase 134. Eliminate the captured 82 Bright findings and the stale tracked LOC report without an exception ledger. This phase is structural verification cleanup only: preserve production behavior, public Rust APIs, RPC/CLI/wire/storage/configuration contracts, test coverage, script entrypoints, exported checker functions, live-smoke flags and exit codes, report schemas, artifact names, redaction behavior, opt-in network boundaries, and existing parity claims.

</domain>

<decisions>
## Implementation Decisions

### Planning and sequencing

- Materialize exactly six executable plans for Phase 133.1, then execute them through `/gsd-execute-phase 133.1`.
- Plan 1 owns baseline capture and mechanical metadata repair.
- Plan 2 owns Rust test split Wave A: chainstate, consensus, mempool, and wallet.
- Plan 3 owns Rust test split Wave B: CLI, network, node, and RPC.
- Plan 4 owns oversized TypeScript checker, fixture, and test splits other than the dedicated live-smoke entrypoint.
- Plan 5 owns the live-mainnet-smoke TypeScript and shell decomposition.
- Plan 6 owns generated-artifact refresh, complete verification, and final diff review.
- Phase 133.1 blocks Phase 134. Phase 134 itself remains untouched.

### Failure baseline and exception policy

- Record the initial baseline as 82 Bright findings: 70 oversized tracked source files and 12 lesson-schema findings, plus a stale `docs/metrics/lines-of-code.md` failure from the repo verifier.
- Full cleanup is required. Do not create `.bright-builds-rules-checks.tsv` and do not add temporary file-length or lesson exceptions.
- Do not edit the managed Bright Builds updater, checker, sidecar, or standards files.

### Lesson metadata

- Convert every existing repo-local lesson block to numbered `1. Date`, `2. What went wrong`, `3. Preventive rule`, and `4. Trigger signal to catch it earlier` fields.
- Preserve lesson IDs, timestamps, ordering, and meaning; edit only the affected blocks.

### Rust test splits

- Split all 48 oversized Rust test files.
- Keep each root `tests.rs` as a thin shared-fixture and module index, with scenarios moved into behavior- or phase-named child modules.
- Where an existing child module is oversized, use the `foo.rs` plus `foo/` child layout rather than `foo/mod.rs`.
- Target no more than 550 physical lines per resulting source file; no tracked source file may exceed the Bright checker limit of 628.
- Keep production APIs unchanged. Test-only helpers may become `pub(super)` where child modules require access.
- Preserve Rust test counts and leaf names. Capture before/after inventories for every affected package.
- Add every new first-party Rust source/test path to its existing parity breadcrumb group and regenerate/check breadcrumb blocks after each Rust wave.
- Run affected-package Cargo tests through `bun run scripts/command-timings.ts run --key <stable-key> -- <command>`. Do not overlap Cargo jobs that share a target directory.

### TypeScript checker and test splits

- Split the 20 oversized non-live-smoke TypeScript checker/test/fixture files while keeping original entrypoints and exported APIs callable from their current paths.
- Extract constants/types, parsing, lifecycle checks, routing checks, and filesystem adapters into directories named after their original scripts.
- Split Bun test files by scenario family while preserving test titles and shared fixtures.
- Split the Phase 124 fixture module by lifecycle stage.
- Keep every resulting TypeScript file at or below 550 physical lines and run every changed Bun test family.

### Live-smoke decomposition

- Decompose `scripts/run-live-mainnet-smoke.ts` into options, preflight, command, status, session, diagnosis, report, and CLI modules while preserving the original entrypoint.
- Preserve all flags, exit codes, JSON/Markdown schemas, artifact names, redaction behavior, and explicit opt-in public-network boundaries.
- Turn `scripts/test-run-live-mainnet-smoke.sh` into a thin orchestrator over scenario-specific shell helpers.
- Replace embedded Bun assertion programs with a checked-in TypeScript assertion helper.
- Keep every resulting TypeScript and shell file below 550 physical lines.
- Run the complete hermetic live-smoke harness after decomposition.

### Final acceptance

- Regenerate `docs/metrics/lines-of-code.md` only after all file moves are complete.
- Require `bun scripts/bright-builds-check.ts all` to report `findings=0` and `exceptions=0`.
- Require the LOC report check and parity breadcrumb check to pass.
- Require `bash scripts/verify.sh --profile` to pass.
- Review the final diff for accidental production, parity, or operator-surface changes.
- Keep local atomic GSD commits. Do not push.
- If the work is pushed later, both the main CI and Bright Builds Checks workflows must pass.

### the agent's Discretion

- Exact scenario-module names and the placement of shared test-only helpers, provided the six-plan ownership and compatibility constraints above remain intact.
- Exact affected-package test command partitioning and stable timing keys.
- Exact internal module APIs for TypeScript decompositions, provided original entrypoints and exported APIs remain stable.

</decisions>

<canonical-refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Repository workflow and standards

- `AGENTS.md` — Repo-local verification, parity breadcrumb, Cargo/Bazel timing, generated LOC, and GSD workflow requirements.
- `AGENTS.bright-builds.md` — Installed Bright Builds workflow and managed-file constraints.
- `standards-overrides.md` — Repository standards overrides; currently contains no active project-specific exception.
- `standards/core/code-shape.md` — Oversized-file, embedded foreign-code, and script decomposition rules.
- `standards/core/verification.md` — Sync-first and repo-native verification requirements.
- `standards/core/testing.md` — Test preservation and focused test expectations.
- `standards/languages/rust.md` — Rust module layout and test-code guidance.
- `standards/languages/typescript-javascript.md` — Bun/TypeScript module and checker guidance.

### Verification and generated artifacts

- `scripts/bright-builds-check.ts` — Managed checker entrypoint; read-only for this phase.
- `scripts/verify.sh` — Final repo-native verification contract.
- `scripts/command-timings.ts` — Required wrapper for ad-hoc Cargo and Bazel verification.
- `docs/metrics/lines-of-code.md` — Tracked generated LOC artifact, regenerated only after moves complete.
- `docs/parity/source-breadcrumbs.json` — Source breadcrumb group ownership for new Rust paths.
- `scripts/check-parity-breadcrumbs.ts` — Breadcrumb generator/checker.
- `.codex/tasks/lessons.md` — Repo-local lesson ledger requiring numbered field repair.

### Dedicated live-smoke surface

- `scripts/run-live-mainnet-smoke.ts` — Stable live-smoke entrypoint and schema owner.
- `scripts/test-run-live-mainnet-smoke.sh` — Hermetic live-smoke test harness and current embedded assertion owner.

</canonical-refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- Existing Rust test module splits already use root `tests.rs` files with sibling `tests/` child modules.
- Existing checker tests use Bun and can preserve current test titles while moving scenario families to sibling files.
- `scripts/check-parity-breadcrumbs.ts` already owns both generated breadcrumb updates and validation.

### Established Patterns

- Keep functional core behavior and production adapters unchanged; this phase moves test and automation code across module seams.
- Prefer re-exports from original TypeScript checker paths to keep their current import and CLI surface stable.
- Use `foo.rs` plus `foo/` for touched multi-file Rust modules.

### Integration Points

- Rust child module visibility may require `pub(super)` for shared fixtures.
- TypeScript entrypoints must continue to invoke or export the same externally callable functions.
- Live-smoke shell helpers must remain hermetic and must not weaken the explicit public-network opt-in boundary.

</code_context>

<specifics>
## Specific Ideas

- The user selected full cleanup specifically to avoid an exception ledger and to establish a clean blocking baseline before Phase 134.
- Test leaf names and inventories are compatibility evidence, not implementation details that may be renamed for convenience.
- LOC regeneration must happen after all structural moves so the tracked report represents the final tree.

</specifics>

<deferred>
## Deferred Ideas

- Recurrence prevention from the earlier audit's separate updater/checker finding remains out of scope.
- Optional `maybe` naming and unrelated audit recommendations remain out of scope except where the live-smoke split naturally removes embedded JavaScript.
- Remote push and CI monitoring are not part of this phase.

</deferred>

*Context gathered: 2026-07-27 via PRD Express Path*
