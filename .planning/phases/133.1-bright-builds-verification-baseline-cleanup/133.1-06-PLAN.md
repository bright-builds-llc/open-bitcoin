---
phase: 133.1-bright-builds-verification-baseline-cleanup
plan: "06"
type: execute
wave: 6
depends_on:
  - "133.1-05"
files_modified:
  - docs/metrics/lines-of-code.md
  - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-FINAL-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Bright Builds reports findings=0 and exceptions=0 with no exception ledger"
    - "The tracked LOC report represents the final post-move worktree and passes check mode"
    - "Parity breadcrumbs and the full repo-native profile verifier pass"
    - "The Plan 06 local diff gate finds no production/public-interface, parity-claim, operator-surface, managed-asset, hook/verifier, or Phase 134 change"
    - "After every GSD finalization artifact is committed, the orchestrator repeats scope checks and observes an empty worktree"
  artifacts:
    - path: "docs/metrics/lines-of-code.md"
      provides: "Fresh final-tree generated LOC report"
    - path: ".planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-FINAL-VERIFICATION.md"
      provides: "Command results and final scope review evidence"
      contains: "findings=0"
  key_links:
    - from: "final source tree"
      to: "docs/metrics/lines-of-code.md"
      via: "worktree-mode deterministic generator after all moves"
      pattern: "Source fingerprint"
    - from: "scripts/verify.sh"
      to: "Bright, parity, LOC, Rust, Bazel, and test contracts"
      via: "--profile run"
      pattern: "Verification profile"
    - from: "bright-builds-rules.audit.md"
      to: "all exact managed installation paths"
      via: "anchored phase-start git diff --quiet comparison"
      pattern: "## Managed files"
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 133.1-2026-07-27T16-40-47
generated_at: 2026-07-27T16:46:59.446Z
---

<objective>
Refresh generated evidence only after all moves, run the complete acceptance contract, and reject any structural-cleanup scope drift.

Purpose: Establish a clean blocking verification baseline before Phase 134.
Output: Fresh LOC report, zero Bright findings/exceptions, passing parity/full verification, and an evidence-backed final diff review.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@bright-builds-rules.audit.md
@standards/core/verification.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-CONTEXT.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-01-SUMMARY.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-02-SUMMARY.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-03-SUMMARY.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-04-SUMMARY.md
@.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-05-SUMMARY.md
@scripts/generate-loc-report.ts
@scripts/check-parity-breadcrumbs.ts
@scripts/bright-builds-check.ts
@scripts/verify.sh
@docs/metrics/lines-of-code.md
</context>

<execution-policy>
Plan 06 ends the deferred-hook sequence. Do not use `git commit --no-verify`: stage only each task's authorized files and use normal atomic commits so the repository's real Git hooks run. Regenerate `docs/metrics/lines-of-code.md` before the first Plan 06 commit, then run `bash scripts/verify.sh --profile` explicitly. Do not edit `.githooks/`, `scripts/verify.sh`, the managed/updater/sidecar/standards paths enumerated in `bright-builds-rules.audit.md`, or `.bright-builds-rules-checks.tsv`. Parse `START` only from exactly one anchored `Phase-start commit: &lt;40 hex&gt;` line and validate it with `git cat-file -e "$START^{commit}"`.
</execution-policy>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate final LOC evidence and close Bright/parity checks</name>
  <files>docs/metrics/lines-of-code.md, .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-FINAL-VERIFICATION.md</files>
  <read-first>
    - docs/metrics/lines-of-code.md
    - scripts/generate-loc-report.ts
    - scripts/check-parity-breadcrumbs.ts
    - scripts/bright-builds-check.ts
    - bright-builds-rules.audit.md
    - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md
    - all five Phase 133.1 plan summaries
  </read-first>
  <action>
    After confirming Plans 01–05 are complete and no source moves remain, extract `START` only with the anchored expression `^Phase-start commit: ([0-9a-f]{40})$`, require exactly one match, and validate the commit object. Regenerate the tracked report exactly once with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`, then rerun the same command with `--check`. Run `bun run scripts/check-parity-breadcrumbs.ts --check`. Run `bun scripts/bright-builds-check.ts all` and record its complete summary in `133.1-FINAL-VERIFICATION.md`. The required result is `file-lengths ... findings=0`, `lessons ... findings=0`, `all findings=0`, and `exceptions=0`; `.bright-builds-rules-checks.tsv` must remain absent.

    Deterministically enumerate `git diff --name-only --diff-filter=ACMR "$START" --`, `LC_ALL=C sort -u`, filter the phase's source extensions `rs|ts|tsx|js|jsx|mjs|cjs|sh|bash|zsh`, require every retained path still exists, measure physical lines with `wc -l`, and fail on any value above 550. Record the sorted `path<TAB>lines` list in the final verification report. Independently retain the managed Bright checker as the repository-global hard-limit proof that no tracked source exceeds 628.
  </action>
  <verify>
    <automated>START="$(sed -n 's/^Phase-start commit: \([0-9a-f]\{40\}\)$/\1/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md)" &amp;&amp; test "$(sed -n '/^Phase-start commit: [0-9a-f]\{40\}$/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md | wc -l | tr -d ' ')" = 1 &amp;&amp; git cat-file -e "$START^{commit}" &amp;&amp; bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check &amp;&amp; bun run scripts/check-parity-breadcrumbs.ts --check &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; test ! -e .bright-builds-rules-checks.tsv &amp;&amp; bash -c 'set -euo pipefail; start="$1"; failed=0; while IFS= read -r file; do test -f "$file"; lines="$(wc -l &lt; "$file" | tr -d " ")"; if test "$lines" -gt 550; then printf "%s\\t%s\\n" "$file" "$lines" &gt;&amp;2; failed=1; fi; done &lt; &lt;(git diff --name-only --diff-filter=ACMR "$start" -- | LC_ALL=C sort -u | rg "\\.(rs|ts|tsx|js|jsx|mjs|cjs|sh|bash|zsh)$" || true); test "$failed" -eq 0' phase1331 "$START"</automated>
  </verify>
  <acceptance-criteria>
    - LOC check exits 0 after final worktree-mode regeneration
    - Parity breadcrumb check exits 0
    - Bright summary reports findings=0 and exceptions=0
    - `.bright-builds-rules-checks.tsv` does not exist
    - Every resulting touched source file is at or below 550 and none of the repository's tracked source files exceeds 628
  </acceptance-criteria>
  <done>Generated LOC, parity lineage, lesson schema, and file-length gates are clean on the final tree.</done>
</task>

<task type="auto">
  <name>Task 2: Run full profile verification and perform the interim local scope review</name>
  <files>.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-FINAL-VERIFICATION.md</files>
  <read-first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - bright-builds-rules.audit.md
    - scripts/verify.sh
    - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-CONTEXT.md
    - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md
    - docs/parity/index.json
    - docs/parity/source-breadcrumbs.json
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
  </read-first>
  <action>
    Run `bash scripts/verify.sh --profile` as the final repo-native contract. Allow its timing wrapper and cooperative lock to run uninterrupted; poll resumable execution at least every 60 seconds and do not terminate merely because an estimate is exceeded. Record the exit status and profile summary.

    Resolve `START` only from exactly one anchored baseline field, then run `git diff --check`, `git diff --stat "$START"`, and `git diff --name-status "$START"` as an interim local gate before later SUMMARY/VERIFICATION/phase-completion metadata exists. Review every path and hunk. Reject and fix any change outside test/automation structural moves, lesson numbering, breadcrumb path registration, generated LOC, and Phase 133.1 artifacts. Mechanically require no diff under `.planning/phases/134-*`; no change to production Rust files, public Rust items, RPC/CLI/wire/storage/config contracts, report field names, artifact names, redaction patterns, live-smoke flags/exits/opt-in guards, or parity claim/index semantics.

    Read the exact `## Managed files` list in `bright-builds-rules.audit.md` and require `git diff --quiet "$START" --` across these exact paths: `AGENTS.md`, `AGENTS.bright-builds.md`, `CONTRIBUTING.md`, `.github/pull_request_template.md`, `bright-builds-rules.audit.md`, `standards/index.md`, `standards/core/architecture.md`, `standards/core/code-shape.md`, `standards/core/frontend-ui.md`, `standards/core/local-guidance.md`, `standards/core/operability.md`, `standards/core/testing.md`, `standards/core/verification.md`, `standards/languages/rust.md`, `standards/languages/typescript-javascript.md`, `README.md`, `scripts/bright-builds-auto-update.sh`, `.github/workflows/bright-builds-auto-update.yml`, `scripts/bright-builds-check.ts`, and `.github/workflows/bright-builds-checks.yml`. Separately require no diff for `.githooks/` or `scripts/verify.sh`. Rerun any affected focused check and then `bash scripts/verify.sh --profile` after corrections. Record this review as `interim`; it does not replace the post-finalization orchestrator gate below.
  </action>
  <verify>
    <automated>START="$(sed -n 's/^Phase-start commit: \([0-9a-f]\{40\}\)$/\1/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md)" &amp;&amp; test "$(sed -n '/^Phase-start commit: [0-9a-f]\{40\}$/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md | wc -l | tr -d ' ')" = 1 &amp;&amp; bash scripts/verify.sh --profile &amp;&amp; git diff --check &amp;&amp; test -z "$(git diff --name-only "$START" -- | rg '^\\.planning/phases/134-' || true)" &amp;&amp; git diff --quiet "$START" -- AGENTS.md AGENTS.bright-builds.md CONTRIBUTING.md .github/pull_request_template.md bright-builds-rules.audit.md standards/index.md standards/core/architecture.md standards/core/code-shape.md standards/core/frontend-ui.md standards/core/local-guidance.md standards/core/operability.md standards/core/testing.md standards/core/verification.md standards/languages/rust.md standards/languages/typescript-javascript.md README.md scripts/bright-builds-auto-update.sh .github/workflows/bright-builds-auto-update.yml scripts/bright-builds-check.ts .github/workflows/bright-builds-checks.yml .githooks scripts/verify.sh</automated>
  </verify>
  <acceptance-criteria>
    - `bash scripts/verify.sh --profile` exits 0
    - `git diff --check` exits 0
    - Interim verification report records findings=0, exceptions=0, passing LOC/parity/profile checks, exact managed-path protection, and zero unexpected paths
    - No Phase 134 file, production/public contract, managed Bright asset, verifier wiring, parity claim, or operator surface changes
    - Final diff contains only the authorized structural cleanup, metadata repair, breadcrumbs, generated LOC, and Phase 133.1 evidence
  </acceptance-criteria>
  <done>The executable source work has a fully passing profile and a clean interim scope review, ready for GSD finalization artifacts and the blocking orchestrator gate.</done>
</task>

</tasks>

The parent `/root` orchestrator—not the generic XML parser or plan executor—must run the following block after all SUMMARY, VERIFICATION, phase-completion ROADMAP/STATE/REQUIREMENTS, and PROJECT evolution commits.

<post-finalization-gate owner="orchestrator" blocking="true">
  <when>Run only after all six `*-SUMMARY.md` files, `133.1-VERIFICATION.md`, phase-completion records, and every authorized ROADMAP/STATE/REQUIREMENTS/PROJECT metadata update have been committed with real hooks.</when>
  <read-first>
    - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md
    - .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - bright-builds-rules.audit.md
  </read-first>
  <action>
    Parse `START` only from the unique anchored `Phase-start commit: &lt;40 hex&gt;` field and validate the commit. Run `git diff --check "$START..HEAD"`. Review `git diff --name-status "$START..HEAD"` path by path. Allow the exact source/test/automation/evidence/LOC/lesson/breadcrumb paths owned by Plans 01–06 plus expected GSD metadata under the Phase 133.1 directory and `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, and `.planning/PROJECT.md`. Reject changes to every `.planning/phases/134-*` path, production/public surfaces, hook/verifier files, managed paths, parity claims, or any unrelated file. Require `git diff --quiet "$START..HEAD" --` for the exact managed paths listed in Task 2 and for `.githooks/` plus `scripts/verify.sh`. Finally require `git status --porcelain` to emit no bytes. Do not create another tracked report after this gate because that would violate the clean-worktree condition.
  </action>
  <verify>
    <automated>START="$(sed -n 's/^Phase-start commit: \([0-9a-f]\{40\}\)$/\1/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md)" &amp;&amp; test "$(sed -n '/^Phase-start commit: [0-9a-f]\{40\}$/p' .planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-BASELINE.md | wc -l | tr -d ' ')" = 1 &amp;&amp; git cat-file -e "$START^{commit}" &amp;&amp; git diff --check "$START..HEAD" &amp;&amp; test -z "$(git diff --name-only "$START..HEAD" -- | rg '^\\.planning/phases/134-' || true)" &amp;&amp; git diff --quiet "$START..HEAD" -- AGENTS.md AGENTS.bright-builds.md CONTRIBUTING.md .github/pull_request_template.md bright-builds-rules.audit.md standards/index.md standards/core/architecture.md standards/core/code-shape.md standards/core/frontend-ui.md standards/core/local-guidance.md standards/core/operability.md standards/core/testing.md standards/core/verification.md standards/languages/rust.md standards/languages/typescript-javascript.md README.md scripts/bright-builds-auto-update.sh .github/workflows/bright-builds-auto-update.yml scripts/bright-builds-check.ts .github/workflows/bright-builds-checks.yml .githooks scripts/verify.sh &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance-criteria>
    - All SUMMARY, VERIFICATION, phase completion, and allowed planning metadata commits are included in `START..HEAD`
    - Scoped name-status review contains only Plan 01–06 outputs and expected GSD metadata
    - No Phase 134, managed, hook/verifier, production/public, parity-claim, operator-surface, or unrelated path changed
    - `git diff --check "$START..HEAD"` exits 0 and `git status --porcelain` is empty
  </acceptance-criteria>
</post-finalization-gate>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Final worktree → generated LOC/parity evidence | Generated artifacts must describe the actual final tree |
| Structural diff → production/public surfaces | Interim and post-finalization reviews must not alter externally observable behavior |
| Live-smoke decomposition → public-network boundary | Default verification must remain hermetic and opt-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-133.1-50 | Tampering | generated artifacts | mitigate | Regenerate after all moves, then check mode plus full verifier |
| T-133.1-51 | Elevation of Privilege | production/public interfaces | mitigate | Start-commit name-status and full diff review reject non-test production changes |
| T-133.1-52 | Information Disclosure | live-smoke/reporting | mitigate | Preserve redaction tests and verify no default public-network invocation |
| T-133.1-53 | Repudiation | final acceptance | mitigate | Persist exact command summaries and reviewed path categories |
| T-133.1-54 | Denial of Service | full verification | mitigate | Use repo timing/cooperative lock, sequential jobs, and liveness polling |
| T-133.1-55 | Tampering | finalization metadata/cleanliness | mitigate | Blocking orchestrator gate reviews START..HEAD after all metadata commits and requires empty porcelain |
</threat-model>

<verification>
Require zero Bright findings/exceptions, passing LOC and breadcrumbs, real hooks, `bash scripts/verify.sh --profile`, the interim scope review, and the blocking post-finalization clean-worktree gate.
</verification>

<success-criteria>
- `bun scripts/bright-builds-check.ts all` reports findings=0 and exceptions=0.
- LOC and parity checks pass on the final tree.
- `bash scripts/verify.sh --profile` passes.
- Interim and post-finalization reviews prove no production/public, managed, parity-claim, operator, live-opt-in, or Phase 134 drift.
- After all GSD metadata commits, the orchestrator observes an empty `git status --porcelain`.
</success-criteria>

<output>
After completion, create `.planning/phases/133.1-bright-builds-verification-baseline-cleanup/133.1-06-SUMMARY.md`.
</output>
