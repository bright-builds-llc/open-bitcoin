---
phase: 123-runtime-timing-and-evidence-integrity
plan: 07
type: execute
wave: 5
depends_on:
  - 123-01
  - 123-02
  - 123-03
  - 123-04
  - 123-05
  - 123-06
files_modified:
  - scripts/check-phase123-runtime-timing-evidence-integrity.ts
  - scripts/check-phase123-runtime-timing-evidence-integrity.test.ts
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/catalog/p2p.md
  - docs/parity/source-breadcrumbs.json
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - HARD-02
  - HARD-03
  - HARD-04
decisions:
  - D-10
  - D-11
  - D-12
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:06:00Z
must_haves:
  truths:
    - "A deterministic Phase 123 checker enforces typed idle timing, complete first-party trait migration, both post-write seams, private served evidence, one authoritative snapshot, provider absence, parity, and verifier wiring."
    - "Mutation tests independently break every critical invariant, including pre-write encoding failure protection."
    - "Checker Task 1 contains and runs only synthetic fixtures; parity Task 2 runs only local parity/breadcrumb checks; Task 3 adds/enables the real-repository corpus test and runs it only after parity and verify.sh wiring exist."
    - "One done parity surface owns exactly HARD-02, HARD-03, and HARD-04 with bounded Knots anchors and no public/schema/production claim expansion."
    - "All three new Rust test files have exact breadcrumb entries."
    - "Final focused checks and bash scripts/verify.sh pass sequentially with generated LOC freshness retained."
  artifacts:
    - path: "scripts/check-phase123-runtime-timing-evidence-integrity.ts"
      provides: "Deterministic cross-cutting HARD-02/HARD-03/HARD-04 guard"
      contains: "checkPhase123RuntimeTimingEvidenceIntegrity"
    - path: "scripts/check-phase123-runtime-timing-evidence-integrity.test.ts"
      provides: "Independent sensitivity mutations including encoding failure"
      contains: "encoding failure"
    - path: "docs/parity/index.json"
      provides: "Exact Phase 123 done surface"
      contains: "v2-1-runtime-timing-evidence-integrity"
    - path: "scripts/verify.sh"
      provides: "Default deterministic checker execution"
      contains: "check-phase123-runtime-timing-evidence-integrity"
  key_links:
    - from: "scripts/check-phase123-runtime-timing-evidence-integrity.ts"
      to: "packages/open-bitcoin-node/src/sync.rs"
      via: "ordered idle and authoritative-snapshot checks"
    - from: "scripts/check-phase123-runtime-timing-evidence-integrity.ts"
      to: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      via: "encode-before-write and Written-only acknowledgement checks"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase123-runtime-timing-evidence-integrity.test.ts"
      via: "visible and run_step mutation command"
---

<objective>
Close Phase 123 with mutation-sensitive evidence, exact parity traceability, source breadcrumbs, verifier wiring, simplification review, and the full repository contract.

Purpose: prevent all three runtime-integrity corrections from regressing or being claimed beyond scope.
Output: Phase 123 checker/mutations, parity roots, breadcrumb manifest, verifier integration, generated LOC freshness, and verified summaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-01-SUMMARY.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-02-SUMMARY.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-03-SUMMARY.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-04-SUMMARY.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-05-SUMMARY.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the deterministic Phase 123 checker and synthetic mutation matrix</name>
  <files>scripts/check-phase123-runtime-timing-evidence-integrity.ts, scripts/check-phase123-runtime-timing-evidence-integrity.test.ts</files>
  <read-first>
    - `scripts/check-phase122-compact-relay-peer-completion.ts` and `scripts/check-phase122-compact-relay-peer-completion.test.ts` — latest fixed-corpus parser, failure, and mutation patterns.
    - `scripts/check-phase121-block-relay-metrics-log-runtime.ts` and `scripts/check-phase121-block-relay-metrics-log-runtime.test.ts` — corrected compatibility assertions.
    - `packages/open-bitcoin-node/src/sync/types.rs`, `sync/tcp.rs`, `sync.rs`, `sync/session.rs`, `lib.rs`, and `packages/open-bitcoin-bench/src/runtime_fixtures.rs` — final receive, idle, send, re-export, and external implementation invariants.
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs`, `metrics/block_relay.rs`, and `logging.rs` — private schema boundary and explicit served projection.
    - `packages/open-bitcoin-rpc/src/context.rs`, `context/network.rs`, `inbound_listener.rs`, and `inbound_listener/tests.rs` — typed carrier, encode guard, Written acknowledgement, and exact encoding-failure test.
    - `packages/open-bitcoin-node/src/sync/tests/runtime_timing_cases.rs`, `runtime_write_evidence_cases.rs`, and `runtime_projection_cases.rs` — exact focused test vocabulary.
    - `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/catalog/p2p.md`, `docs/parity/source-breadcrumbs.json`, and `scripts/verify.sh` — later-task targets represented synthetically in the passing fixture.
  </read-first>
  <action>
Create exported pure `checkPhase123RuntimeTimingEvidenceIntegrity(options: { rootDir?: string } = {}): string[]` over a fixed `TARGET_FILES` tuple, stable `P123` failures, `existsSync`/`readFileSync`, typed JSON parsing, and `import.meta.main`. Do not shell out, inspect processes, use the network, or mutate the real repository.

Require: distinct Message/Idle/Closed and byte-progress TCP behavior; node public re-export plus every first-party `SyncPeerSession` implementation (including bench) using `SyncPeerReceiveOutcome`; Idle clock/expiration/target/send/continue order and Message-only progress; sync send-before-ack; `EncodedWireResponse`, complete-batch `encode_messages(...)?` before zip/write, exact `phase123_inbound_encoding_failure_does_not_increment_served`, Written-only acknowledgement, and no byte decoding; private `ManagedBlockRelayEvidenceState.served_count` plus non-serialized runtime snapshot and absence from public status files; eligible-independent metric/log mapping; one direct runtime snapshot shared by metrics/logs; provider absence and inbound-provider presence; every exact Phase 123 Rust test; migrated Phase 121 guarantees; exact parity ownership/anchors/no-claims/breadcrumbs; and both Phase 123 commands after Phase 122 and before Phase 117 in both verifier regions.

Build a complete synthetic passing corpus under a temporary fixture root. Add one Arrange/Act/Assert mutation per invariant: collapse Idle into Closed; remove clock; remove target guard; restore an old Option implementation in bench or remove re-export; move sync ack before send; remove inbound Written ack; bypass the pre-write encoding failure guard by replacing `encode_messages(&responses)?` with fabricated bytes or moving acknowledgement before encoding; remove the exact encoding-failure test; restore eligible proxy; add `served_count` to public status; restore provider; resample for logs; remove omission test; mutate one HARD ID; remove mutation command; remove live checker command. Assert each exact `P123` failure. Do not add a test that calls the checker with its default root, `process.cwd()`, a repository-relative root, or the real Phase 123 corpus in this task. Task verification intentionally runs only temporary synthetic fixtures, because live parity/verifier artifacts do not exist until Tasks 2–3.
  </action>
  <verify>
    <automated>bun test scripts/check-phase123-runtime-timing-evidence-integrity.test.ts</automated>
  </verify>
  <acceptance-criteria>
    - The exported checker and `import.meta.main` exist, and it has no shell/network/process mutation path.
    - `rg -n "encoding failure|encode_messages|fabricated|before encoding|phase123_inbound_encoding_failure" scripts/check-phase123-runtime-timing-evidence-integrity.test.ts` finds dedicated guard mutations rather than an extra assertion in a multi-concern test.
    - Every listed mutation asserts a specific `P123` failure and the untouched synthetic fixture passes.
    - `scripts/check-phase123-runtime-timing-evidence-integrity.test.ts` contains no real-repository corpus test, default-root checker invocation, `process.cwd()` root, or repository-root fixture; Task 1 does not run the live checker.
  </acceptance-criteria>
  <done>The synthetic mutation suite proves the static guard is sensitive before live parity and verifier artifacts are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add exact parity evidence and source breadcrumbs without running the live Phase 123 checker</name>
  <files>docs/parity/index.json, docs/parity/checklist.md, docs/parity/catalog/p2p.md, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - `docs/parity/index.json`, `docs/parity/checklist.md`, and `docs/parity/catalog/p2p.md` — existing Phase 122 placement and exact evidence shapes.
    - `docs/parity/source-breadcrumbs.json` and `scripts/check-parity-breadcrumbs.ts` — exact membership/header rules.
    - `packages/bitcoin-knots/src/net.cpp`, `src/net_processing.cpp`, and `test/functional/p2p_compactblocks.py` — maintenance, typed block send, and fallback anchors.
    - `.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md` — exact claim boundary.
  </read-first>
  <action>
Add one `done` surface `v2-1-runtime-timing-evidence-integrity` titled `v2.1 Runtime Timing and Evidence Integrity` with requirements exactly `["HARD-02", "HARD-03", "HARD-04"]`. Link the typed receive/TCP/runtime/session/bench files, private evidence/metric/log files, RPC typed carrier/listener, all three focused Rust test files, migrated Phase 121 checker, Phase 123 checker/test, operator doc, breadcrumbs, and verifier. Use upstream `net.cpp`, `net_processing.cpp`, and functional `p2p_compactblocks.py`.

Rationale must name receive-independent idle expiration, successful typed Block writes, runtime-only served evidence with unchanged public status, and one authoritative sync-network snapshot. Preserve default-off block/compact serving, archive/package/filter/public-network CI/production service/full-node/funds no-claims; state Open Bitcoin keeps its blocking runtime and existing timeout constant rather than claiming Knots thread cadence.

Add the matching checklist row and P2P catalog section. Register `runtime_timing_cases.rs`, `runtime_write_evidence_cases.rs`, and `runtime_projection_cases.rs` with defensible `net.cpp`/`net_processing.cpp`/`p2p_compactblocks.py` anchors. Confirm existing RPC files remain covered. Do not use `none` for these new runtime tests. Task verification runs only parity-local checks; defer the live Phase 123 checker until Task 3 wires verify.sh.
  </action>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance-criteria>
    - Exactly one done surface has requirements exactly HARD-02,HARD-03,HARD-04 in order.
    - Checklist/catalog contain the exact surface/IDs and Knots maintenance/send/fallback anchors.
    - `rg -n "runtime_timing_cases.rs|runtime_write_evidence_cases.rs|runtime_projection_cases.rs" docs/parity/source-breadcrumbs.json` finds all new Rust files.
    - Breadcrumb checker exits 0; Task 2 does not run the live Phase 123 checker.
  </acceptance-criteria>
  <done>Exact machine/human parity roots and source provenance exist without depending prematurely on verifier wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Add the real corpus test, wire default verification, simplify, and pass the repository contract</name>
  <files>scripts/check-phase123-runtime-timing-evidence-integrity.test.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read-first>
    - `scripts/verify.sh` — visible and run_step regions around Phase 122/117; preserve every command and failure.
    - Phase 121 and Phase 123 checker/test files — exact commands, synthetic-only Task 1 state, and compatibility gate.
    - `scripts/command-timings.ts` — timing/lock contract.
    - `docs/metrics/lines-of-code.md` — verifier-refreshed tracked artifact.
    - Plans 01–06 summaries and the complete Phase 123 diff — simplification and scope review.
  </read-first>
  <action>
After Task 2 has installed parity and breadcrumbs, add exact Arrange/Act/Assert test `phase123_real_repository_corpus_passes` to `scripts/check-phase123-runtime-timing-evidence-integrity.test.ts`. It calls `checkPhase123RuntimeTimingEvidenceIntegrity` against the actual repository root and requires an empty failure array. Keep every Task 1 synthetic mutation unchanged.

Insert exactly `bun test scripts/check-phase123-runtime-timing-evidence-integrity.test.ts` and `bun run scripts/check-phase123-runtime-timing-evidence-integrity.ts` in both visible and executable `run_step` regions after the Phase 122 pair and before the Phase 117 pair. Use labels `test Phase 123 runtime timing and evidence integrity checker` and `check Phase 123 runtime timing and evidence integrity`. Do not remove, swallow, or weaken any existing command. Only after both verifier regions are complete may this task run the Bun test (now including the real corpus), the live checker, or the full verifier.

Run the ordered verification below. Perform the explicit simplification review: one receive enum, one clock path, one managed-network acknowledgement, one private served counter/snapshot, one typed inbound carrier/encode helper, one authoritative tick snapshot, one migrated Phase 121 checker. Remove any duplicate provider/counter/status field/resample/receipt/decoder/timer found. Confirm public status/RPC/CLI/dashboard/support schemas are unchanged. Run `git diff --check`, inspect the full diff and breadcrumbs, then run `bash scripts/verify.sh`; poll resumable work at least every 60 seconds and retain refreshed LOC output.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase123-node-runtime-tests-final -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_ -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-rpc-inbound-tests-final -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc phase123_inbound_ -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-workspace-all-targets-check -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features</automated>
    <automated>bun test scripts/check-phase121-block-relay-metrics-log-runtime.test.ts</automated>
    <automated>bun run scripts/check-phase121-block-relay-metrics-log-runtime.ts</automated>
    <automated>bun test scripts/check-phase123-runtime-timing-evidence-integrity.test.ts</automated>
    <automated>bun run scripts/check-phase123-runtime-timing-evidence-integrity.ts</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
    <automated>git diff --check</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance-criteria>
    - Each Phase 123 command occurs exactly twice in `scripts/verify.sh`, once visible and once executable, ordered Phase 121, 122, 123, then 117 in both regions.
    - `rg -n "phase123_real_repository_corpus_passes|checkPhase123RuntimeTimingEvidenceIntegrity" scripts/check-phase123-runtime-timing-evidence-integrity.test.ts` finds the real corpus test added only in Task 3 while all synthetic mutations remain.
    - Focused node/RPC tests and workspace all-target/all-feature check run sequentially through the timing wrapper and exit 0, including the bench trait implementation.
    - Phase 121 fixture/live, Phase 123 fixture/live, breadcrumbs, and `git diff --check` exit 0; this is the first task-local live Phase 123 checker run.
    - `bash scripts/verify.sh` exits 0 with format, clippy warnings denied, all-target build/tests, architecture/parity checks, LOC freshness, and Bazel smoke build.
    - Final diff has no async redesign, dependency, public-network/default soak gate, public operator schema expansion, duplicate evidence owner/snapshot, or sensitive dynamic evidence.
  </acceptance-criteria>
  <done>The live checker runs only after all required artifacts exist, and the complete deterministic repository contract passes.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Corpus to checker | Weak substrings can pass broken order/ownership | Fixed files, typed JSON, ordered assertions, independent mutations |
| Parity to claims | Bounded evidence can imply public/production/schema guarantees | Exact HARD ownership, pinned anchors, explicit deviations/no-claims |
| Source to breadcrumbs | New runtime files can lose provenance | Exact manifest membership and breadcrumb checker |
| Verification ordering | Live checker can fail before its own artifacts exist | Synthetic-only Task 1, parity-local Task 2, real-corpus/live/full Task 3 |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-07-01 | Tampering | mitigate | Mutation suite includes ordering, encoding, schema, parity, and verifier failures |
| T-123-07-02 | Spoofing | mitigate | One done surface owns exactly three HARD IDs |
| T-123-07-03 | Repudiation | mitigate | Behavior tests, checker, breadcrumbs, and full verifier layer evidence |
| T-123-07-04 | Information Disclosure | mitigate | Checker rejects public served field and dynamic sensitive evidence |
| T-123-07-05 | Denial of Service | mitigate | Default gate stays deterministic/local; no public-network or wall-clock soak |
</threat-model>

<simplification-review>
- Verify one typed receive outcome, clock seam, acknowledgement API, private served counter/snapshot, inbound carrier, and authoritative tick snapshot are sufficient.
- Remove compatibility provider, serialized served field, metrics-global atomic, response decoder, second sample, timer worker, or checker copy.
- Migrate Phase 121 in place and keep Phase 123 checker cross-cutting rather than duplicating all old checks.
- Keep documentation limited to parity root/catalog/checklist/breadcrumbs and corrected operator provenance.
</simplification-review>

<verification>
- Task-local ordering is mandatory: synthetic fixtures only, then parity-local, then add/enable the real corpus test and run live checker/verifier.
- Cargo/Bazel work is sequential and timing-wrapped where ad hoc; `bash scripts/verify.sh` is the final gate.
- Review all seven summaries against D-01 through D-12 and HARD-02 through HARD-04 before writing the final summary.
</verification>

<success-criteria>
Phase 123 is complete when HARD-02, HARD-03, and HARD-04 have focused behavior tests and exact parity ownership; mutation coverage detects idle, first-party compile, write, encoding, private-schema, projection, provenance, omission, parity, and verifier regressions; and the full deterministic repository contract passes.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-07-SUMMARY.md`.
</output>
