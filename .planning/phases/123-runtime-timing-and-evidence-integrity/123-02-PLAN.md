---
phase: 123-runtime-timing-and-evidence-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network/tests.rs
autonomous: true
requirements:
  - HARD-03
decisions:
  - D-04
  - D-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:01:00Z
must_haves:
  truths:
    - "One private managed-network served-block counter advances only through typed post-write acknowledgement of WireNetworkMessage::Block."
    - "A crate-private non-serialized BlockRelayRuntimeEvidenceSnapshot carries public status plus served_count for later runtime projection."
    - "BlockServingStatusCounters, BlockRelayEvidenceStatus, RPC JSON, CLI/dashboard, and support schemas gain no served_count field or fixture fallout."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      provides: "Private authoritative counter, runtime-only snapshot, and typed acknowledgement"
      contains: "BlockRelayRuntimeEvidenceSnapshot"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      to: "packages/open-bitcoin-node/src/status/block_serving.rs"
      via: "public status remains unchanged and excludes the private count"
---

<objective>
Create one authoritative runtime-only successful-block-write counter and snapshot without expanding any public operator schema.

Purpose: establish the HARD-03 evidence owner before either transport is wired.
Output: private counter/snapshot, typed acknowledgement/accessor, and unchanged serialized status proof.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a private served counter and non-serialized runtime snapshot</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/block_relay_evidence.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <read-first>
    - `packages/open-bitcoin-node/src/network.rs` — private child-module declarations and the crate-visible re-export seam required by sync modules.
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs` — private `ManagedBlockRelayEvidenceState`, public status projection, and current evidence mutation methods.
    - `packages/open-bitcoin-node/src/status/block_serving.rs` — serialized `BlockServingStatusCounters` contract that must not change.
    - `packages/open-bitcoin-node/src/status/block_relay_evidence.rs` — serialized `BlockRelayEvidenceStatus` contract that must not change.
    - `packages/open-bitcoin-node/src/network/tests.rs` — managed-network evidence and JSON serialization fixtures.
    - `packages/open-bitcoin-rpc/src/dispatch/node.rs` and CLI operator status tests — consumers whose schema must remain unchanged and therefore are read-only.
  </read-first>
  <action>
Add private `served_count: u64` to `ManagedBlockRelayEvidenceState`. Add `pub(crate) struct BlockRelayRuntimeEvidenceSnapshot { pub(crate) status: BlockRelayEvidenceStatus, pub(crate) served_count: u64 }` in the same module; derive only ordinary debug/clone/equality traits needed by tests, never `Serialize` or `Deserialize`. Add only `pub(crate) use block_relay_evidence::BlockRelayRuntimeEvidenceSnapshot;` in `network.rs` so sync sibling modules can name it; do not publicly re-export it from `lib.rs` or `status`.

Add `ManagedBlockRelayEvidenceState::record_wire_message_written(&mut self, message: &WireNetworkMessage)` with an early return for non-block messages. For `WireNetworkMessage::Block(_)`, call `note_observed()` and increment `served_count` with the repository's existing counter arithmetic convention. Add `ManagedPeerNetwork::acknowledge_wire_message_written(&mut self, message: &WireNetworkMessage)` as the sole mutation delegate. Add `pub(crate) fn block_relay_runtime_evidence_snapshot(&self) -> BlockRelayRuntimeEvidenceSnapshot` for node runtime projection and a narrow non-serialized `pub fn block_served_write_count(&self) -> u64` accessor needed by the separate RPC crate's transport tests. Keep `block_relay_evidence_status()` byte-for-byte behaviorally unchanged.

Add tests `phase123_block_acknowledgement_increments_private_served_count`, `phase123_non_block_acknowledgement_does_not_increment_private_served_count`, and `phase123_public_block_relay_status_omits_runtime_served_count`. The JSON test acknowledges a block, serializes `block_relay_evidence_status()`, asserts no key named `served_count` occurs, and confirms existing status/eligibility keys remain. Do not modify status structs, RPC/CLI/dashboard/support fixtures, or architecture schema docs.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase123-private-served-counter-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_ -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-public-status-schema-test -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_public_block_relay_status_omits_runtime_served_count -- --nocapture</automated>
  </verify>
  <acceptance-criteria>
    - `rg -n "served_count: u64|struct BlockRelayRuntimeEvidenceSnapshot|record_wire_message_written|acknowledge_wire_message_written" packages/open-bitcoin-node/src/network/block_relay_evidence.rs` finds the private owner and typed path.
    - `rg -n "pub\(crate\) use block_relay_evidence::BlockRelayRuntimeEvidenceSnapshot" packages/open-bitcoin-node/src/network.rs` finds exactly the internal sibling-module exposure.
    - `bash -c 'output=$(rg -n "served_count" packages/open-bitcoin-node/src/status/block_serving.rs packages/open-bitcoin-node/src/status/block_relay_evidence.rs packages/open-bitcoin-rpc/src/method/node.rs packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs packages/open-bitcoin-cli/src/operator/support/render/block_relay.rs); status=$?; if (( status == 0 )); then printf "%s\n" "$output"; exit 1; fi; (( status == 1 )) && exit 0; exit "$status"'` exits 0, proving the exact public block-serving status, block-relay status/RPC response, CLI status, dashboard, and support renderer surfaces contain no `served_count`; unrelated transaction-relay schema files are outside this assertion.
    - `rg -n "Serialize|Deserialize" packages/open-bitcoin-node/src/network/block_relay_evidence.rs` does not attach either derive to `BlockRelayRuntimeEvidenceSnapshot`.
    - Both timing-wrapped test commands exit 0 and the serialized public status remains unchanged after a real acknowledgement.
  </acceptance-criteria>
  <done>The managed network owns achieved-effect evidence privately, and public status/RPC/CLI/dashboard/support schemas remain unchanged.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Serve decision to achieved evidence | Eligibility can spoof completed service | Private counter mutates only after typed write acknowledgement |
| Runtime evidence to public status | Internal count can accidentally expand RPC/CLI/support schema | Separate non-serialized carrier; unchanged status structs and JSON regression test |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-02-01 | Spoofing | mitigate | No eligibility/construction path can mutate served_count |
| T-123-02-02 | Tampering | mitigate | One acknowledgement delegate owns mutation |
| T-123-02-03 | Repudiation | mitigate | Focused acknowledgement and unchanged-schema tests |
| T-123-02-04 | Information Disclosure | mitigate | Runtime snapshot is non-serialized and status JSON omits the field |
| T-123-02-05 | Denial of Service | mitigate | One bounded u64; no receipts, per-peer map, or retry store |
</threat-model>

<simplification-review>
- One private u64 and one small snapshot establish the achieved-effect owner; no serialized field or second status object is added.
- One acknowledgement method serves both transports in Plans 03 and 04.
- Projection helper signature changes stay deferred to Plan 05 so this plan leaves no temporarily uncompilable caller state.
- Public operator schema and all CLI/dashboard/support fixtures remain untouched.
</simplification-review>

<verification>
- Run the two Cargo commands sequentially through `scripts/command-timings.ts`.
- Run `git diff --check` and inspect the diff for any `served_count` under `status/`, RPC dispatch shapes, CLI operator models, or support serialization.
- Full all-target and repository verification occurs in Plan 123-07 after transport wiring.
</verification>

<success-criteria>
The HARD-03 evidence owner is ready when one private managed-network counter advances only for typed Block acknowledgement and public schemas omit it; Plan 05 atomically changes all projection signatures and callers.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-02-SUMMARY.md`.
</output>
