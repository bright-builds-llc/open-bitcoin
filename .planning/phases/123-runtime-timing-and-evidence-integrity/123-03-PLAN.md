---
phase: 123-runtime-timing-and-evidence-integrity
plan: 03
type: execute
wave: 2
depends_on:
  - 123-01
  - 123-02
files_modified:
  - packages/open-bitcoin-node/src/sync/session.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs
autonomous: true
requirements:
  - HARD-03
decisions:
  - D-05
  - D-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:02:00Z
must_haves:
  truths:
    - "DurableSyncRuntime::send_all acknowledges each successful typed Block immediately after session.send succeeds."
    - "Failed and non-block sends do not count, while a later batch failure preserves every successful Block prefix exactly once."
    - "The scripted session fixture is built against Plan 01's SyncPeerReceiveOutcome and Plan 02's private counter API."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/session.rs"
      provides: "Per-message send-then-ack ordering"
      contains: "acknowledge_wire_message_written"
    - path: "packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs"
      provides: "Success/failure/non-block/partial-prefix behavior tests"
      contains: "phase123_sync_partial_batch_counts_each_successful_block_before_failure"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/session.rs"
      to: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      via: "session.send succeeds before managed-network acknowledgement"
---

<objective>
Wire the authoritative served-block counter into the durable sync transport's completed-write boundary.

Purpose: close the outbound half of HARD-03 with exact partial-batch semantics.
Output: mutable send_all ordering and focused scripted-session tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-outbound-compact-block-announcement-wiring/118-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acknowledge successful sync-session Block writes per message</name>
  <files>packages/open-bitcoin-node/src/sync/session.rs, packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs</files>
  <read-first>
    - `packages/open-bitcoin-node/src/sync/session.rs` — current `send_all` loop and `SyncPeerSession::send` success boundary.
    - `packages/open-bitcoin-node/src/sync/tests.rs` — Plan 01-converted `ScriptedSession`, runtime/store fixtures, and child-module registrations; this file is explicitly modified after Plan 01, never in parallel with it.
    - `packages/open-bitcoin-node/src/sync/types.rs` — Plan 01 `SyncPeerReceiveOutcome` required by every scripted implementation.
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs` — Plan 02 acknowledgement and runtime served-count accessor.
    - `.planning/phases/118-outbound-compact-block-announcement-wiring/118-CONTEXT.md` — achieved-effect evidence precedent.
  </read-first>
  <action>
Change `DurableSyncRuntime::send_all` from `&self` to `&mut self`. Inside the existing per-message loop, execute `session.send(message, self.config.network.magic())?` first and `self.network.acknowledge_wire_message_written(message)` immediately afterward. Do not acknowledge after the whole batch, before send, on error, or in message construction; add no retry, receipt ID, atomic, or generalized emitter.

Register `mod runtime_write_evidence_cases;` from the already Plan-01-updated `sync/tests.rs`. Create `sync/tests/runtime_write_evidence_cases.rs` with `net.cpp` and `net_processing.cpp` parity breadcrumbs and a scripted session whose send fails at an exact 1-based call. Its `receive` implementation must use `SyncPeerReceiveOutcome`, even if these tests exercise only send.

Add one-concern Arrange/Act/Assert tests `phase123_sync_block_write_success_increments_served_once`, `phase123_sync_block_write_failure_does_not_increment_served`, `phase123_sync_non_block_write_does_not_increment_served`, `phase123_sync_partial_batch_counts_each_successful_block_before_failure`, and `phase123_sync_two_successful_blocks_before_later_failure_count_two`. Use `[Block success, non-block success, Block failure] -> 1` and `[Block success, Block success, non-block failure] -> 2`, reading the runtime counter accessor rather than serialized status, metrics, or logs.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase123-sync-write-evidence-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_sync_ -- --nocapture</automated>
  </verify>
  <acceptance-criteria>
    - `sync/session.rs` orders `session.send(...)` before `acknowledge_wire_message_written(message)` inside the same loop and `send_all` has an `&mut self` receiver.
    - `packages/open-bitcoin-node/src/sync/tests.rs` explicitly contains `mod runtime_write_evidence_cases;` after the Plan 01 module registration.
    - `rg -n "SyncPeerReceiveOutcome" packages/open-bitcoin-node/src/sync/tests.rs packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs` confirms every relevant fixture uses the changed trait.
    - `rg -n "phase123_sync_block_write_success|phase123_sync_block_write_failure|phase123_sync_non_block_write|phase123_sync_partial_batch|phase123_sync_two_successful_blocks" packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs` finds all five exact tests.
    - The timing-wrapped command exits 0 with counts 1, 0, 0, 1, and 2.
  </acceptance-criteria>
  <done>The sync transport records each completed Block write exactly once and retains successful-prefix evidence across later failure.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Typed send intent to completed write | Intent can be counted before effect | Acknowledge immediately after successful `session.send` only |
| Batch loop to aggregate evidence | End-of-batch acknowledgement loses successful prefix | Acknowledge per message before processing the next item |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-03-01 | Spoofing | mitigate | Failed/non-block paths cannot mutate served count |
| T-123-03-02 | Tampering | mitigate | Source-order assertion and partial-prefix tests |
| T-123-03-03 | Repudiation | mitigate | Five focused deterministic write outcomes |
| T-123-03-04 | Denial of Service | mitigate | No retry or receipt storage added |
</threat-model>

<simplification-review>
- One existing loop gains one post-success call; no emitter abstraction is introduced.
- One focused child module reuses runtime fixtures and avoids broad sync test reorganization.
- The private counter remains the sole owner; no transport-local count exists.
</simplification-review>

<verification>
- Run the Cargo command through the timing wrapper and do not overlap it with other Cargo jobs.
- Run `git diff --check` and inspect send-before-ack source order.
- Plan 123-07 registers the new test file breadcrumb before full verification.
</verification>

<success-criteria>
The sync half of HARD-03 is complete when successful Block writes count once, failures/non-blocks count zero, and both partial-batch shapes retain exactly their successful Block prefix.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-03-SUMMARY.md`.
</output>
