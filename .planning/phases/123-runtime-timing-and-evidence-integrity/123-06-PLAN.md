---
phase: 123-runtime-timing-and-evidence-integrity
plan: 06
type: execute
wave: 4
depends_on:
  - 123-05
files_modified:
  - scripts/check-phase121-block-relay-metrics-log-runtime.ts
  - scripts/check-phase121-block-relay-metrics-log-runtime.test.ts
  - docs/architecture/operator-observability.md
autonomous: true
requirements:
  - HARD-04
decisions:
  - D-08
  - D-09
  - D-11
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:05:00Z
must_haves:
  truths:
    - "The Phase 121 checker enforces the corrected direct runtime-only snapshot rather than obsolete provider wiring."
    - "Phase 121 helper reuse, persistence, structured logging, omission, leakage, verifier inclusion, and no-twin-worker guards remain intact."
    - "Operator prose identifies ManagedRpcContext as a separate network and DurableSyncRuntime::network as the sole sync projection source."
    - "The checker fixture, live-corpus assertion, and operator prose migrate in one atomic task; no live checker runs against stale documentation."
  artifacts:
    - path: "scripts/check-phase121-block-relay-metrics-log-runtime.ts"
      provides: "Backward-compatible checker for corrected provenance"
      contains: "verifyAuthoritativeSnapshotAndPersist"
    - path: "scripts/check-phase121-block-relay-metrics-log-runtime.test.ts"
      provides: "Mutations for direct sampling, same-snapshot use, and obsolete provider absence"
      contains: "second sample"
    - path: "docs/architecture/operator-observability.md"
      provides: "Corrected sync evidence provenance"
      contains: "DurableSyncRuntime::network"
  key_links:
    - from: "scripts/check-phase121-block-relay-metrics-log-runtime.ts"
      to: "packages/open-bitcoin-node/src/sync.rs"
      via: "direct snapshot/order assertions"
    - from: "docs/architecture/operator-observability.md"
      to: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      via: "runtime-only status plus served-count snapshot description"
---

<objective>
Migrate Phase 121's deterministic guard and operator prose to the corrected Phase 123 authoritative snapshot architecture.

Purpose: prevent older verification from requiring the wrong network while preserving every valid observability guarantee.
Output: migrated checker/mutations and corrected provenance documentation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically migrate the Phase 121 checker, live corpus, and operator provenance</name>
  <files>scripts/check-phase121-block-relay-metrics-log-runtime.ts, scripts/check-phase121-block-relay-metrics-log-runtime.test.ts, docs/architecture/operator-observability.md</files>
  <read-first>
    - `scripts/check-phase121-block-relay-metrics-log-runtime.ts` — provider/persist/daemon assertions plus valid helper/log/leakage/no-twin-worker checks.
    - `scripts/check-phase121-block-relay-metrics-log-runtime.test.ts` — fixed corpus and obsolete provider mutations.
    - `docs/architecture/operator-observability.md` — Phase 121 provider provenance, omission, sanitization, and no-claim language enforced by the live corpus.
    - `packages/open-bitcoin-node/src/sync.rs`, `sync/metrics.rs`, and `sync/runtime_state.rs` — Plan 05 final names and order.
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs` — non-serialized snapshot fields.
    - `scripts/verify.sh` — existing Phase 121 commands whose inclusion checks must remain.
  </read-first>
  <action>
Migrate, do not delete, `checkPhase121BlockRelayMetricsLogRuntime`. Replace provider/daemon positive checks with `verifyAuthoritativeSnapshotAndPersist` plus explicit obsolete-provider absence checks. Require one `self.network.block_relay_runtime_evidence_snapshot()` call, activation availability gating, one `maybe_block_relay_snapshot` local, the same reference passed to `persist_metrics` and `write_block_relay_log`, explicit `snapshot.served_count` use in both helpers, `block_relay_metric_samples`, `block_relay_log_record`, append functions, unavailable omission tests, sensitive-marker tests, existing Phase 121 verifier commands, and no twin worker. Require absence of `set_block_relay_metric_status_provider`, `maybe_block_relay_metric_status_provider`, and daemon `block_relay_context`.

Update the fixed fixture to corrected architecture. Replace obsolete missing-provider mutations with independent mutations that restore any provider token, remove direct self.network sampling, substitute a second sample for the log argument, remove activation omission, drop helper reuse, persistence, structured logging, leakage, verifier inclusion, no-claim text, or no-twin-worker. Keep stable `P121` failures and one real-repository corpus test.

Replace only the stale provider paragraph with the exact flow `DurableSyncRuntime::network -> one availability-gated BlockRelayRuntimeEvidenceSnapshot -> block_relay_metric_samples / block_relay_log_record -> retained metrics / structured logs`. State that the snapshot combines unchanged sanitized `BlockRelayEvidenceStatus` with a runtime-only, non-serialized `served_count`; `ManagedRpcContext` owns a separate network instance and is not the sync projection source. Preserve aggregate-only redaction, unavailable omission, default-off/public-network/production no-claim text, and all unrelated sections.

Only after the checker source, fixture, real-repository corpus assertion, and operator document all describe the corrected architecture may verification run. This ordering ensures the live checker never evaluates stale provider prose.
  </action>
  <verify>
    <automated>bun test scripts/check-phase121-block-relay-metrics-log-runtime.test.ts</automated>
    <automated>bun run scripts/check-phase121-block-relay-metrics-log-runtime.ts</automated>
  </verify>
  <acceptance-criteria>
    - Both Phase 121 Bun commands exit 0 only after all three files are migrated.
    - `rg -n "verifyAuthoritativeSnapshotAndPersist|block_relay_runtime_evidence_snapshot|maybe_block_relay_snapshot|served_count" scripts/check-phase121-block-relay-metrics-log-runtime.ts` finds corrected positive assertions.
    - Provider tokens occur only in explicit absence assertions or mutation inputs, never as required production wiring.
    - Existing helper, persistence, structured-log, leakage, verifier, no-claim, and no-twin-worker checks remain present.
    - `rg -n "DurableSyncRuntime::network|BlockRelayRuntimeEvidenceSnapshot|runtime-only|non-serialized|ManagedRpcContext.*separate network" docs/architecture/operator-observability.md` finds the corrected bounded flow.
    - No text claims a new RPC/CLI/dashboard/support field, unified mutable network, public default, or production readiness.
  </acceptance-criteria>
  <done>The older gate and operator documentation atomically enforce authoritative provenance without losing valid observability or no-claim guarantees.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Old checker to corrected code | Stale assertions can force wrong architecture back in | Migrate positive provenance and assert provider absence |
| Docs to operator expectations | Runtime-only evidence can be mistaken for schema/public expansion | Explicit non-serialized wording and unchanged no-claim text |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-06-01 | Spoofing | mitigate | Checker requires direct sync-owned sample |
| T-123-06-02 | Tampering | mitigate | Mutations detect provider restoration and second sampling |
| T-123-06-03 | Repudiation | mitigate | Live corpus plus mutation fixture remain in verifier |
| T-123-06-04 | Information Disclosure | mitigate | Docs/checker retain aggregate leakage rules |
</threat-model>

<simplification-review>
- Migrate Phase 121 in place; do not duplicate it into a second compatibility checker.
- Change only stale provenance assertions/prose; preserve every valid existing guard.
- Describe the existing private snapshot, not a new public status abstraction.
</simplification-review>

<verification>
- Run the Bun fixture and live checker only after checker, fixture, real corpus, and docs are all updated in the single task.
- Run `git diff --check` and inspect for lost Phase 121 guarantees or broadened claims.
- Plan 123-07 adds the new checker and final repository wiring.
</verification>

<success-criteria>
Compatibility is restored when Phase 121's checker and docs require the one direct runtime-only snapshot, reject obsolete provider wiring/resampling, and preserve helper, persistence, log, omission, leakage, verifier, and no-claim guarantees.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-06-SUMMARY.md`.
</output>
