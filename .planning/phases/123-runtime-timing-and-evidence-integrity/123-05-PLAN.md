---
phase: 123-runtime-timing-and-evidence-integrity
plan: 05
type: execute
wave: 3
depends_on:
  - 123-01
  - 123-02
  - 123-03
files_modified:
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/metrics.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-node/src/sync/tests/runtime_projection_cases.rs
  - packages/open-bitcoin-node/src/metrics/block_relay.rs
  - packages/open-bitcoin-node/src/metrics/tests.rs
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
autonomous: true
requirements:
  - HARD-03
  - HARD-04
decisions:
  - D-04
  - D-07
  - D-08
  - D-09
  - D-10
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:04:00Z
must_haves:
  truths:
    - "DurableSyncRuntime samples one BlockRelayRuntimeEvidenceSnapshot from self.network after peer processing."
    - "One availability decision gates the snapshot; the same borrowed status and served_count feed metrics and logs."
    - "Unobserved evidence emits neither block-relay metric samples nor a block_relay log record."
    - "BlockServedCount and block_served_count use the private runtime served_count independently of eligible_peer_count."
    - "The block-relay provider field/setter/lookups and daemon ManagedRpcContext closure are absent; inbound metric provider remains unchanged."
    - "Runtime tests prove compact activity and served writes on the sync-owned network project coherently without public schema expansion."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync.rs"
      provides: "One authoritative availability-gated runtime snapshot"
      contains: "maybe_authoritative_block_relay_snapshot"
    - path: "packages/open-bitcoin-node/src/sync/metrics.rs"
      provides: "Explicit optional runtime snapshot input to persistence"
      contains: "maybe_block_relay_snapshot"
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "Same optional runtime snapshot input to structured logging"
      contains: "maybe_block_relay_snapshot"
    - path: "packages/open-bitcoin-node/src/sync/tests/runtime_projection_cases.rs"
      provides: "Omission, same-snapshot, and inbound-provider regression tests"
      contains: "phase123_sync_network_compact_activity_projects_same_snapshot_to_metrics_and_log"
    - path: "packages/open-bitcoin-node/src/metrics/block_relay.rs"
      provides: "BlockServedCount projection from explicit runtime served count"
      contains: "served_count"
    - path: "packages/open-bitcoin-node/src/logging.rs"
      provides: "Structured block_served_count from explicit runtime served count"
      contains: "block_served_count"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync.rs"
      to: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      via: "one direct block_relay_runtime_evidence_snapshot call"
    - from: "packages/open-bitcoin-node/src/sync.rs"
      to: "packages/open-bitcoin-node/src/sync/metrics.rs"
      via: "same snapshot reference passed to persist_metrics"
    - from: "packages/open-bitcoin-node/src/sync.rs"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "same snapshot reference passed to write_block_relay_log"
---

<objective>
Atomically correct served-block projection and project block-relay evidence from the exact managed network owned by DurableSyncRuntime using one runtime-only snapshot per tick.

Purpose: complete HARD-03 projection and close HARD-04 while preserving omission, inbound metrics, and unchanged operator schemas.
Output: explicit served-count helper signatures, complete caller/test migration, provider removal, direct snapshot plumbing, daemon cleanup, and authoritative runtime behavior tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically migrate every projection signature, caller, provider test, and authoritative runtime path</name>
  <files>packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/metrics.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-node/src/sync/tests/runtime_projection_cases.rs, packages/open-bitcoin-node/src/metrics/block_relay.rs, packages/open-bitcoin-node/src/metrics/tests.rs, packages/open-bitcoin-node/src/logging.rs, packages/open-bitcoin-node/src/logging/tests.rs, packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</files>
  <read-first>
    - `packages/open-bitcoin-node/src/sync.rs` — runtime fields, open initialization, peer processing, summary refresh, persist/log tick, and Plan 01 clock changes.
    - `packages/open-bitcoin-node/src/sync/metrics.rs` and `sync/runtime_state.rs` — provider setter/lookups plus every production call to the retained metric/log helpers.
    - `packages/open-bitcoin-node/src/sync/tests.rs` — Plan 01/03-converted fixtures and every obsolete provider or old-signature test caller compiled in the node test target.
    - `packages/open-bitcoin-node/src/metrics/block_relay.rs`, `metrics/tests.rs`, `logging.rs`, and `logging/tests.rs` — old two-argument helper signatures, eligibility proxies, fixtures, and sensitive-marker assertions.
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs` — Plan 02 `BlockRelayRuntimeEvidenceSnapshot` and private served count.
    - `packages/open-bitcoin-node/src/sync/tests/runtime_timing_cases.rs` and `runtime_write_evidence_cases.rs` — focused module conventions and changed runtime fixtures.
    - `packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs` — deterministic sync-owned compact activity path.
    - `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` — wrong `block_relay_context` closure plus Plan 01 daemon clock wiring.
    - `.planning/phases/121-block-relay-metrics-and-log-runtime-projection/121-CONTEXT.md` — availability omission and sanitized helper guarantees.
  </read-first>
  <action>
Treat this task as the first and only node test-target compile boundary for all projection changes. First change `block_relay_metric_samples` to `(block_relay: &BlockRelayEvidenceStatus, served_count: u64, timestamp_unix_seconds: u64)` and use only `served_count` for `MetricKind::BlockServedCount`. Change `block_relay_log_record` to the equivalent three-argument signature and use only `served_count` for `block_served_count={}`. Keep all other fixed kinds, labels, sources, and sanitization unchanged. Update every production and test caller returned by `rg -n "block_relay_metric_samples\(|block_relay_log_record\(" packages/open-bitcoin-node/src` within this task. In metric/log fixtures use `eligible_peer_count: 2` and `let served_count = 9_u64`; add `phase123_block_served_metric_uses_runtime_count` and `phase123_block_served_log_uses_runtime_count`, require metric `9.0` and log `block_served_count=9`, and preserve leakage tests with explicit safe counts.

Delete `maybe_block_relay_metric_status_provider`, its initialization, `set_block_relay_metric_status_provider`, daemon `block_relay_context`, its closure, and only imports obsolete from that bridge. Preserve `set_inbound_metric_status_provider` and Plan 01's live clock wiring. Add `fn maybe_authoritative_block_relay_snapshot(&self) -> Option<BlockRelayRuntimeEvidenceSnapshot>` that calls `self.network.block_relay_runtime_evidence_snapshot()` exactly once, returns Some only when `snapshot.status.block_serving.activation` is `FieldAvailability::Available(_)`, and otherwise None. After peer processing and final `refresh_summary_progress`, bind one `let maybe_block_relay_snapshot = self.maybe_authoritative_block_relay_snapshot();`.

Change `persist_metrics` to accept `Option<&BlockRelayRuntimeEvidenceSnapshot>` and call the new metric helper only for Some. Change `write_block_relay_log` to the same explicit input and call the new log helper only for Some. Pass the same `maybe_block_relay_snapshot.as_ref()` to both without clone/resample. Preserve append, retention, health signals, fixed helpers, and inbound-provider behavior.

Before verification, remove or rewrite every obsolete provider/old-signature test in `sync/tests.rs`, register `mod runtime_projection_cases;`, and create the focused module with `net.cpp`, `net_processing.cpp`, and `p2p_compactblocks.py` breadcrumbs. Add `phase123_unobserved_authoritative_network_omits_block_relay_metrics_and_log`, asserting SyncHeight remains while all nine block-relay kinds and `BLOCK_RELAY_LOG_SOURCE` are absent. Add `phase123_sync_network_compact_activity_projects_same_snapshot_to_metrics_and_log`, performing real compact activity on `runtime.network`, keeping eligibility 2, acknowledging nine typed Block writes, then asserting served 9 and compact activity in both effects from the one snapshot. Add `phase123_inbound_metric_provider_remains_unchanged`, passing None for block relay while inbound samples remain. Preserve the Phase 121 sensitive-marker assertion. No node Cargo command may run until helper signatures, all production callers, all compiled test callers, provider removal, daemon cleanup, and all focused tests are complete.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase123-authoritative-node-test-target -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_ --no-run</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-runtime-projection-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase123_ -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-block-relay-regression-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-authoritative-daemon-compile -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc daemon_sync --no-run</automated>
  </verify>
  <acceptance-criteria>
    - `rg -n "block_relay_metric_samples\(|block_relay_log_record\(" packages/open-bitcoin-node/src` shows every definition and caller using the explicit served-count signature before the first Cargo command.
    - `rg -n "eligible_peer_count" packages/open-bitcoin-node/src/metrics/block_relay.rs packages/open-bitcoin-node/src/logging.rs` returns no matches, while the two exact projection tests use eligible 2 and served 9.
    - `rg -n "maybe_block_relay_metric_status_provider|set_block_relay_metric_status_provider|block_relay_context" packages/open-bitcoin-node/src packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` returns no matches, including compiled tests.
    - `rg -n "fn maybe_authoritative_block_relay_snapshot|block_relay_runtime_evidence_snapshot" packages/open-bitcoin-node/src/sync.rs` finds one helper and one direct sample; file order shows the same `.as_ref()` passed to metrics then logs.
    - `rg -n "phase123_unobserved_authoritative_network_omits|phase123_sync_network_compact_activity_projects_same_snapshot|phase123_inbound_metric_provider_remains" packages/open-bitcoin-node/src/sync/tests/runtime_projection_cases.rs` finds all three authoritative tests.
    - `rg -n "set_inbound_metric_status_provider|maybe_inbound_metric_status_provider" packages/open-bitcoin-node/src/sync/metrics.rs packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` still finds inbound behavior.
    - All four timing-wrapped commands exit 0; the first command compiles the complete node test target only after every signature consumer and obsolete provider test has migrated.
  </acceptance-criteria>
  <done>Projection signatures, runtime callers, test callers, provider removal, authoritative sampling, omission, and independent served mapping compile and pass as one atomic state.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| RPC network vs sync network | Another instance can spoof runtime activity | Sample `self.network` only |
| Runtime snapshot to two effects | Independent samples can disagree | One local immutable snapshot borrowed twice |
| Unobserved state to evidence | Zero values can imply observation | Gate once on activation availability and omit both outputs |
| Eligibility to served output | Proxy counts can impersonate completed writes | Explicit private served_count plus eligible=2/served=9 tests |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-05-01 | Spoofing | mitigate | Sync-owned network is sole projection source |
| T-123-05-02 | Tampering | mitigate | Same snapshot supplies status and private served count |
| T-123-05-03 | Repudiation | mitigate | Runtime activity test ties mutations to retained outputs |
| T-123-05-04 | Information Disclosure | mitigate | Existing fixed helpers; no schema field or dynamic label |
| T-123-05-05 | Denial of Service | mitigate | No worker, provider polling loop, or new store |
</threat-model>

<simplification-review>
- Delete the obsolete provider rather than retaining compatibility plumbing.
- One `Option<BlockRelayRuntimeEvidenceSnapshot>` feeds both effects without clone or resample.
- Keep inbound metrics on their existing separate provider.
- Use the Plan 02 private carrier; do not create a second status/snapshot type.
</simplification-review>

<verification>
- Run all Cargo commands sequentially through `scripts/command-timings.ts`.
- Run `git diff --check` and inspect for provider remnants, a second snapshot call, schema changes, sleeps, or public-network work.
- Plan 123-06 migrates the older checker/docs after this architecture exists; Plan 123-07 performs full verification.
</verification>

<success-criteria>
HARD-03 projection and HARD-04 are closed when helpers use the private achieved-effect count independently of eligibility, every runtime tick samples one availability-gated snapshot from DurableSyncRuntime::network, both outputs share it, unobserved evidence is omitted, the wrong provider is absent, and inbound metrics/public schemas remain unchanged.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-05-SUMMARY.md`.
</output>
