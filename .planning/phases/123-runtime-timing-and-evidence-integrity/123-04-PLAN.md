---
phase: 123-runtime-timing-and-evidence-integrity
plan: 04
type: execute
wave: 2
depends_on:
  - 123-02
files_modified:
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
autonomous: true
requirements:
  - HARD-03
decisions:
  - D-05
  - D-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 123-2026-07-15T18-12-00
generated_at: 2026-07-16T01:03:00Z
must_haves:
  truths:
    - "Inbound responses retain typed WireNetworkMessage identity beside encoded bytes in an exact pub(crate) carrier until the write outcome."
    - "Only Ok(WriteWireMessageOutcome::Written) acknowledges; encoding failure, rejection, write error, and non-block success do not count."
    - "Each successful Block prefix is counted before a later batch failure returns."
    - "A dedicated failed-encoding test proves the counter remains zero before the listener write loop."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/context.rs"
      provides: "Non-serialized typed response carrier"
      contains: "EncodedWireResponse"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Encode-before-write pairing and runtime counter test accessor"
      contains: "encode_wire_responses"
    - path: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      provides: "Written-only acknowledgement bridge"
      contains: "acknowledge_inbound_response_write"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      via: "ManagedRpcContext delegates typed write acknowledgement"
    - from: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      to: "packages/open-bitcoin-rpc/src/context.rs"
      via: "Written outcome forwards response.message without decoding bytes"
---

<objective>
Carry typed inbound response identity to the successful socket-write boundary and acknowledge only achieved Block writes.

Purpose: close the inbound half of HARD-03, including encoding and partial-batch failure guards.
Output: typed encoded carrier, encode-before-write helper, Written-only bridge, and seven focused tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-CONTEXT.md
@.planning/phases/123-runtime-timing-and-evidence-integrity/123-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retain typed identity through encoding and acknowledge Written only</name>
  <files>packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <read-first>
    - `packages/open-bitcoin-rpc/src/context.rs` — ManagedRpcContext ownership and crate-visible carrier placement.
    - `packages/open-bitcoin-rpc/src/context/network.rs` — current `receive_inbound_wire_message -> Vec<Vec<u8>>` and encode call.
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs` — queue accounting, `write_all_for_state`, and Written/Rejected/Err match.
    - `packages/open-bitcoin-rpc/src/inbound_listener/resource_runtime.rs` — exact `WriteWireMessageOutcome` variants.
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs` — listener/context fixtures and deterministic write outcomes.
    - `packages/open-bitcoin-node/src/network.rs` and `network/block_relay_evidence.rs` — `encode_messages`, Plan 02 acknowledgement, and runtime count accessor.
    - `packages/open-bitcoin-network/src/message.rs` — `MAX_INV_SIZE` and oversized inventory encoding failure path for the dedicated guard test.
  </read-first>
  <action>
Define exactly `pub(crate) struct EncodedWireResponse { pub(crate) message: WireNetworkMessage, pub(crate) bytes: Vec<u8> }` in `context.rs`; do not derive `Serialize` or `Deserialize`. Change the externally unnecessary method to `pub(crate) fn receive_inbound_wire_message(&mut self, peer_id: u64, message: WireNetworkMessage, timestamp: i64) -> Result<Vec<EncodedWireResponse>, ManagedNetworkError>` so no public method exposes a crate-private return type under `-D warnings`. Add `pub(crate) fn encode_wire_responses(&self, responses: Vec<WireNetworkMessage>) -> Result<Vec<EncodedWireResponse>, ManagedNetworkError>` as the small reusable step: call existing `self.network.encode_messages(&responses)?` for the complete batch first, then zip each original message with its encoded bytes in order. An encoding error returns before any carrier reaches the listener.

Add crate-internal `ManagedRpcContext::acknowledge_wire_message_written(&mut self, message: &WireNetworkMessage)` and a test-only/runtime-private accessor delegating to Plan 02's `block_served_write_count`. In `inbound_listener.rs`, use `response.bytes.len()` and `&response.bytes`. Add exactly an async borrowed bridge `async fn acknowledge_inbound_response_write(write_result: &io::Result<WriteWireMessageOutcome>, response: &EncodedWireResponse, context: &Arc<tokio::sync::Mutex<ManagedRpcContext>>)` whose guard is `let Ok(WriteWireMessageOutcome::Written) = write_result else { return; };`, then locks the shared context and acknowledges `&response.message`. Call `acknowledge_inbound_response_write(&write_result, &response, &context).await` immediately after `write_all_for_state(...).await` and before the existing owned `match write_result`. This preserves the owned result for rejection/termination handling without cloning or moving the response early. Preserve rejection evidence and termination behavior. Never parse response bytes to rediscover type.

Add Arrange/Act/Assert tests `phase123_inbound_written_block_increments_served_once`, `phase123_inbound_rejected_block_does_not_increment_served`, `phase123_inbound_write_error_block_does_not_increment_served`, `phase123_inbound_written_non_block_does_not_increment_served`, `phase123_inbound_partial_batch_counts_successful_block_prefix`, `phase123_inbound_two_blocks_before_later_failure_count_two`, and `phase123_inbound_encoding_failure_does_not_increment_served`. Use `[Block Written, non-block Written, Block Err] -> 1` and `[Block Written, Block Written, non-block Err] -> 2`. For encoding failure, pass an `InventoryList` with `MAX_INV_SIZE + 1` items into `encode_wire_responses`, assert it returns `Err` before any write bridge call, and assert runtime served count remains zero. Also assert a successfully encoded carrier still matches `WireNetworkMessage::Block`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase123-rpc-inbound-write-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc phase123_inbound_ -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase123-rpc-inbound-regression-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener -- --nocapture</automated>
  </verify>
  <acceptance-criteria>
    - `rg -n "pub\(crate\) struct EncodedWireResponse|pub\(crate\) message: WireNetworkMessage|pub\(crate\) bytes: Vec<u8>" packages/open-bitcoin-rpc/src/context.rs` finds the exact non-serialized visibility.
    - `rg -n "pub\(crate\) fn receive_inbound_wire_message|Result<Vec<EncodedWireResponse>" packages/open-bitcoin-rpc/src/context/network.rs` finds a visibility-compatible return surface.
    - `rg -n "async fn acknowledge_inbound_response_write|&io::Result<WriteWireMessageOutcome>|&EncodedWireResponse|&Arc<tokio::sync::Mutex<ManagedRpcContext>>|acknowledge_inbound_response_write\(&write_result, &response, &context\)" packages/open-bitcoin-rpc/src/inbound_listener.rs` finds the borrowed async bridge and pre-match call.
    - `rg -n "decode_wire|ParsedNetworkMessage" packages/open-bitcoin-rpc/src/inbound_listener.rs` finds no response-byte decoding in the acknowledgement path.
    - `rg -n "phase123_inbound_encoding_failure_does_not_increment_served" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs` finds a dedicated one-concern failed-encoding test using `MAX_INV_SIZE + 1`.
    - Both timing-wrapped commands exit 0; the seven exact tests prove counts 1, 0, 0, 0, 1, 2, and 0.
  </acceptance-criteria>
  <done>The inbound transport counts only actual typed Block writes, including exact successful prefixes, and failed encoding cannot reach acknowledgement.</done>
</task>

</tasks>

<threat-model>
## Trust boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Typed response to bytes | Identity can be erased or reconstructed incorrectly | Pair original message with bytes before write; never decode after write |
| Encoding to listener | Failed encoding can be mistaken for sendable intent | Encode complete batch with `?` before carriers/write loop |
| Socket outcome to evidence | Rejected/error writes can overcount | `let Ok(Written) ... else return` guard |

## STRIDE register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-123-04-01 | Spoofing | mitigate | Typed identity survives to the effect boundary |
| T-123-04-02 | Tampering | mitigate | Written-only guard and encode-before-zip ordering |
| T-123-04-03 | Repudiation | mitigate | Seven exact success/failure/prefix tests |
| T-123-04-04 | Information Disclosure | mitigate | Carrier is ephemeral, crate-visible, and non-serialized |
| T-123-04-05 | Denial of Service | mitigate | Existing queue/rejection controls remain; no retry storage |
</threat-model>

<simplification-review>
- One two-field carrier preserves identity; no generalized receipt framework exists.
- One helper centralizes encode-before-zip and enables deterministic failed-encoding coverage.
- One Written-only bridge serves every inbound response; no branch duplicates acknowledgement.
- No public RPC/status/CLI/dashboard/support field is added.
</simplification-review>

<verification>
- Run Cargo commands sequentially through `scripts/command-timings.ts`.
- Run `git diff --check` and inspect for any acknowledgement before encoding/write or byte decoder.
- Existing RPC source files remain covered by current breadcrumb groups; Plan 123-07 checks the manifest.
</verification>

<success-criteria>
The inbound half of HARD-03 is complete when typed identity survives encoding, only Written Block outcomes count, encoding/rejection/error/non-block paths stay zero, and partial batches retain exactly their successful Block prefix.
</success-criteria>

<output>
After completion, create `.planning/phases/123-runtime-timing-and-evidence-integrity/123-04-SUMMARY.md`.
</output>
