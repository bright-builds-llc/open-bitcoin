---
phase: 108-durable-mempool-relay-state-recovery
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [MEM-04, MEM-05, REL-01, REL-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 108-2026-07-03T14-09-06
generated_at: 2026-07-03T14:39:26.759Z
must_haves:
  truths:
    - "Recovered confirmed and conflicting transactions are removed from mempool and relay-serving caches on block connect."
    - "Recovered replaced, evicted, expired, rejected, and reorg-reconsidered transactions leave no stale serving, fanout, recent-reject, txid, or wtxid state."
    - "Reorg reconsideration for recovered records remains bounded to the Phase 103 flow and uses `MempoolOutcome` vocabulary."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/mempool_lifecycle.rs"
      provides: "Block connect and reorg cleanup hooks for recovered state"
      contains: "apply_connected_block_mempool_lifecycle"
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Accepted/replaced/evicted/expired outcome cleanup path"
      contains: "apply_admitted_outcome"
    - path: "packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs"
      provides: "Recovered lifecycle cleanup regression tests"
      contains: "recovered"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/mempool_lifecycle.rs"
      to: "packages/open-bitcoin-node/src/network/inventory.rs"
      via: "remove_stored_transactions_with_status"
      pattern: "remove_stored_transactions_with_status"
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-node/src/network/relay_serving.rs"
      via: "TxServingRecordStatus::Replaced/Evicted/Expired"
      pattern: "TxServingRecordStatus::(Replaced|Evicted|Expired)"
---

<objective>
Prove and repair restart lifecycle coherence for recovered relay state.

Purpose: Once recovery rehydrates serving and fanout identity, every existing mempool lifecycle boundary must clean recovered state exactly as it cleans live accepted state.
Output: Lifecycle cleanup fixes and deterministic tests for recovered confirmed, conflicting, replaced, evicted, expired, and reorg-reconsidered records.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-RESEARCH.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-UI-SPEC.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-01-SUMMARY.md
@AGENTS.md

<interfaces>
From Plan 108-01:
```rust
pub fn recover_mempool_snapshot(&mut self, snapshot: &MempoolSnapshot, verify_flags: ScriptVerifyFlags, consensus_params: ConsensusParams) -> Result<ManagedMempoolRecoverySummary, ManagedNetworkError>;
pub fn latest_mempool_recovery_summary(&self) -> Option<ManagedMempoolRecoverySummary>;
```

From `packages/open-bitcoin-node/src/network/inventory.rs`:
```rust
pub(super) fn remove_stored_transactions_with_status(&mut self, txids: &[Txid], status: TxServingRecordStatus) -> Result<(), ManagedNetworkError>;
```

From `packages/open-bitcoin-node/src/network/mempool_lifecycle.rs`:
```rust
pub(super) fn apply_connected_block_mempool_lifecycle(&mut self, block: &Block) -> Result<MempoolLifecycleSummary, ManagedNetworkError>;
pub(super) fn apply_reorg_mempool_lifecycle(&mut self, disconnect_blocks: &[Block], replacement_branch: &[AnchoredBlock], verify_flags: ScriptVerifyFlags, consensus_params: ConsensusParams) -> Result<ManagedMempoolReorgLifecycle, ManagedNetworkError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean recovered confirmed, conflict, and replacement state</name>
  <files>packages/open-bitcoin-node/src/network/mempool_lifecycle.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs, packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs</files>
  <read_first>
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md` decisions D-04 and D-06.
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-01-SUMMARY.md` for the recovery API and summary names created by Plan 108-01.
    - `packages/open-bitcoin-node/src/network/recovery.rs` for `recover_mempool_snapshot` and `ManagedMempoolRecoverySummary`.
    - `packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` for `apply_connected_block_mempool_lifecycle`.
    - `packages/open-bitcoin-node/src/network/admission_bridge.rs` for `apply_admitted_outcome`.
    - `packages/open-bitcoin-node/src/network/inventory.rs` for `remove_stored_transactions_with_status`.
    - `packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs` and `relay_serving_cases.rs` for existing block-connect and serving outcome assertions.
  </read_first>
  <behavior>
    - Test 1: `recovered_confirmed_transaction_is_removed_from_serving_and_fanout_after_block_connect` recovers a snapshot, connects a block containing that transaction, and proves mempool entry, `transactions_by_txid`, `transactions_by_wtxid`, `relay_serving_info().serveable_transactions`, and `relay_fanout_info().known_transactions` are all zero or absent.
    - Test 2: `recovered_conflicting_transaction_removes_descendant_serving_and_fanout_state` recovers a parent/descendant snapshot, connects a conflicting block transaction, and proves both original and descendant txids are non-serveable and produce `confirmed` or conflict cleanup evidence instead of `served`.
    - Test 3: `recovered_replacement_cleans_old_txid_and_preserves_new_accepted_identity` recovers an old transaction, applies a replacement through `apply_admitted_outcome`, and proves the old txid/wtxid are not serveable while the replacement is serveable through negotiated txid/wtxid identity.
  </behavior>
  <action>
    Add recovered-state variants of existing lifecycle tests instead of creating a separate recovered cache. Use `recover_mempool_snapshot` from Plan 108-01 to seed state before each lifecycle action.
    If a test fails because cleanup only removes live-admission paths, fix the existing shared cleanup route rather than adding a recovery-only cleanup branch. The required route is `remove_stored_transactions_with_status` -> `relay_fanout.cleanup_transactions` -> `relay_serving.remove_transactions`.
    For replacement, preserve the current `apply_admitted_outcome` behavior: call `remove_stored_transactions_with_status(&removed, TxServingRecordStatus::Replaced)`, call `remove_stored_transactions_with_status(outcome.evicted(), TxServingRecordStatus::Evicted)`, then call `relay_serving.record_replaced(transaction.clone(), &removed)` and `store_transaction(transaction)`.
    Keep txid/wtxid identity consistent for recovered records; `InventoryType::Transaction` and `InventoryType::WitnessTransaction` must report `replaced` or `confirmed` for stale recovered records and `served` only for the currently accepted transaction.
    Do not add package relay, unbounded descendant recursion, public-network behavior, sleeps, timers, or socket I/O.
  </action>
  <acceptance_criteria>
    - `rg -n "recovered_confirmed_transaction_is_removed_from_serving_and_fanout_after_block_connect|recovered_conflicting_transaction_removes_descendant_serving_and_fanout_state|recovered_replacement_cleans_old_txid_and_preserves_new_accepted_identity" packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs` finds all test names.
    - `rg -n "recover_mempool_snapshot|relay_serving_info\\(\\)\\.serveable_transactions|relay_fanout_info\\(\\)\\.known_transactions|transactions_by_txid|transactions_by_wtxid" packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs` finds recovered-state assertions.
    - `rg -n "remove_stored_transactions_with_status\\(&removed, TxServingRecordStatus::Replaced\\)|remove_stored_transactions_with_status\\(outcome\\.evicted\\(\\), TxServingRecordStatus::Evicted\\)" packages/open-bitcoin-node/src/network/admission_bridge.rs` finds the shared replacement/eviction cleanup path.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovered_confirmed_transaction_is_removed_from_serving_and_fanout_after_block_connect -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovered_conflicting_transaction_removes_descendant_serving_and_fanout_state -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovered_replacement_cleans_old_txid_and_preserves_new_accepted_identity -- --exact --nocapture</automated>
  </verify>
  <done>Recovered confirmed, conflicting, and replaced records clean through the same shared lifecycle paths as live records, with no stale serving or fanout identity left behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep recovered eviction, expiry, recent-reject, and reorg reconsideration bounded</name>
  <files>packages/open-bitcoin-node/src/network/mempool_lifecycle.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs, packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md` decisions D-04, D-05, D-06, and D-13.
    - `packages/open-bitcoin-node/src/network/recovery.rs` for recovered snapshot seeding.
    - `packages/open-bitcoin-node/src/network/relay_fanout.rs` for `cleanup_transactions`, recent reject cleanup, and `ManagedRelayFanoutInfo`.
    - `packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` for reorg reconsideration flow.
    - `packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs` for existing fanout cleanup assertions.
    - `packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs` for existing reorg tests.
  </read_first>
  <behavior>
    - Test 1: `recovered_evicted_and_expired_transactions_clear_serving_fanout_and_recent_rejects` recovers accepted records, forces evicted and expired outcomes, and proves serving status is `evicted` or `expired`, fanout known transaction count drops, and recent reject state cannot suppress an unrelated later accepted transaction.
    - Test 2: `reorg_reconsiders_recovered_transactions_through_mempool_outcome` recovers a snapshot, performs a bounded replacement-branch reorg, and proves `ManagedMempoolReorgLifecycle.reconsidered` contains `MempoolOutcome::Accepted` or `MempoolOutcome::Replaced` for eligible disconnected transactions without direct serving mutation.
    - Test 3: `reorg_does_not_unbound_recurse_or_queue_package_relay_for_recovered_state` uses a parent/descendant fixture and proves only disconnected block transactions are reconsidered; package relay, package orphan behavior, and unbounded descendant recursion remain absent.
  </behavior>
  <action>
    Extend existing lifecycle and fanout tests so recovered records pass through eviction, expiry, and reorg boundaries after restart.
    If needed, adjust `remove_evicted_outcome`, `apply_reorg_mempool_lifecycle`, or fanout cleanup so every recovered `MempoolOutcome::Evicted` maps to `TxServingRecordStatus::Evicted`, every recovered `MempoolOutcome::Expired` maps to `TxServingRecordStatus::Expired`, and every cleanup goes through `remove_stored_transactions_with_status`.
    Preserve the Phase 103 bounded reorg shape: iterate only `disconnect_blocks.iter().rev()` and `block.transactions.iter().skip(1)`, call `self.mempool.submit_transaction_outcome`, then call `apply_admitted_outcome` only for `MempoolOutcome::Accepted` or `MempoolOutcome::Replaced`.
    Do not add package relay, cluster mempool policy, broad package orphan behavior, recursive descendant walking beyond the existing mempool lifecycle summary, public-network relay tests, sleeps, timers, or service-manager checks.
    Refresh `docs/metrics/lines-of-code.md` only if Rust source/test line counts change and the LOC check requires it.
  </action>
  <acceptance_criteria>
    - `rg -n "recovered_evicted_and_expired_transactions_clear_serving_fanout_and_recent_rejects|reorg_reconsiders_recovered_transactions_through_mempool_outcome|reorg_does_not_unbound_recurse_or_queue_package_relay_for_recovered_state" packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs` finds all test names.
    - `rg -n "MempoolOutcome::Evicted|MempoolOutcome::Expired|TxServingRecordStatus::Evicted|TxServingRecordStatus::Expired|remove_stored_transactions_with_status" packages/open-bitcoin-node/src/network/admission_bridge.rs packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` finds explicit lifecycle mappings.
    - `rg -n "disconnect_blocks\\.iter\\(\\)\\.rev\\(\\)|block\\.transactions\\.iter\\(\\)\\.skip\\(1\\)|submit_transaction_outcome|apply_admitted_outcome" packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` finds the bounded reorg flow.
    - `! rg -n "package relay|cluster mempool|sleep\\(|tokio::time|SystemTime::now|Instant::now" packages/open-bitcoin-node/src/network/mempool_lifecycle.rs packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovered_evicted_and_expired_transactions_clear_serving_fanout_and_recent_rejects -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node reorg_reconsiders_recovered_transactions_through_mempool_outcome -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node reorg_does_not_unbound_recurse_or_queue_package_relay_for_recovered_state -- --exact --nocapture</automated>
  </verify>
  <done>Recovered eviction, expiry, recent-reject, and reorg paths are bounded, use `MempoolOutcome`, and leave no stale recovered relay state after cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Recovered mempool state -> block connect lifecycle | Previously durable local state is removed when current chainstate confirms or conflicts with it. |
| Reorg disconnected transactions -> mempool admission | Reconsidered transactions re-enter through `MempoolOutcome`, not direct cache mutation. |
| Cleanup labels -> operator evidence | Lifecycle outcomes become fixed labels such as `confirmed`, `replaced`, `evicted`, and `expired`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-108-06 | T | `apply_connected_block_mempool_lifecycle` | mitigate | Tests must prove recovered confirmed/conflicting records are removed from mempool, `transactions_by_txid`, `transactions_by_wtxid`, serving cache, and fanout identity. |
| T-108-07 | R | `apply_reorg_mempool_lifecycle` | mitigate | Reorg tests must assert reconsidered recovered records are represented as `MempoolOutcome` values and that only disconnected block transactions are reconsidered. |
| T-108-08 | D | recovered fanout cleanup | mitigate | Cleanup must use `cleanup_transactions` and remove txid/wtxid recent-reject identity so stale recovered records cannot suppress unrelated later acceptance. |
| T-108-09 | I | lifecycle evidence | mitigate | Keep evidence low-cardinality; do not add raw transaction hex, txids, wtxids, peer ids, endpoints, permission strings, or free-form rejection text to status/log/support paths. |
| T-108-10 | S | false production-readiness or public-propagation claims | mitigate | Tests and docs in later plans must keep recovered relay state as local bounded evidence only; this plan adds no public-network or service-manager paths. |
</threat_model>

<verification>
Run the task-level tests plus:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node mempool_lifecycle_cases -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_fanout_cases -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_serving_cases -- --nocapture
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings
```
</verification>

<success_criteria>
Plan 108-02 is complete when recovered records are cleaned across block connect, conflict removal, replacement, eviction, expiry, and reorg reconsideration through the existing live lifecycle paths, with no stale serving/fanout/recent-reject indexes.
</success_criteria>

<output>
After completion, create `.planning/phases/108-durable-mempool-relay-state-recovery/108-02-SUMMARY.md`.
</output>
