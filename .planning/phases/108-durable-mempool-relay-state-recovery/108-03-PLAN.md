---
phase: 108-durable-mempool-relay-state-recovery
plan: 03
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - packages/open-bitcoin-node/src/status/relay_evidence.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-node/src/network/recovery.rs
  - packages/open-bitcoin-node/src/network/relay_fanout.rs
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/metrics/tests.rs
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/tests.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/render/relay.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
  - packages/open-bitcoin-cli/src/operator/support/render/relay.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [MEM-06, REL-01, REL-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 108-2026-07-03T14-09-06
generated_at: 2026-07-03T14:39:26.759Z
must_haves:
  truths:
    - "RPC, CLI, dashboard, metrics, logs, and support surfaces distinguish recovery replay from live admission with fixed aggregate counters."
    - "Recovery evidence never exposes raw transaction hex, txids, wtxids, peer ids, endpoints, permission strings, credentials, dynamic labels, or free-form storage/corruption text."
    - "Baseline RPC response shapes stay unchanged; recovery details live only under Open Bitcoin relay evidence/status/support surfaces."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/relay_evidence.rs"
      provides: "Shared sanitized `RelayRecoveryCounters` status field"
      exports: ["RelayRecoveryCounters"]
    - path: "packages/open-bitcoin-cli/src/operator/status/render/relay.rs"
      provides: "Human `Relay recovery` status line"
      contains: "Relay recovery"
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Fixed low-cardinality recovery metric kinds"
      contains: "RelayRecoveryRecoveredCount"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/recovery.rs"
      to: "packages/open-bitcoin-node/src/status/relay_evidence.rs"
      via: "Managed recovery summary converted into RelayRecoveryCounters"
      pattern: "RelayRecoveryCounters"
    - from: "packages/open-bitcoin-cli/src/operator/support/redaction.rs"
      to: "packages/open-bitcoin-cli/src/operator/support/render/relay.rs"
      via: "support_status_for_bundle before Markdown rendering"
      pattern: "support_status_for_bundle"
---

<objective>
Project recovered relay state through sanitized operator evidence surfaces.

Purpose: Operators need truthful recovery replay evidence, but it must be aggregate, fixed-label, and non-promissory across status, RPC, terminal, dashboard, metrics, logs, and support output.
Output: Shared recovery counters, status/RPC projection, terminal/dashboard/support rendering, fixed metrics/logs, and redaction tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-RESEARCH.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-UI-SPEC.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-01-SUMMARY.md
@AGENTS.md

<interfaces>
From `packages/open-bitcoin-node/src/status/relay_evidence.rs`:
```rust
pub struct RelayEvidenceCounters {
    pub accepted_count: u64,
    pub rejected_count: u64,
    pub orphaned_count: u64,
    pub requested_count: u64,
    pub served_count: u64,
    pub announced_count: u64,
    pub suppressed_count: u64,
    pub evicted_count: u64,
    pub expired_count: u64,
    pub rebroadcast_deferred_count: u64,
}
pub enum RelayEvidenceField<T> {
    Implemented(T),
    Unavailable { reason: String },
    Deferred { reason: String },
    IntentionallyDifferent { reason: String },
}
```

From `108-UI-SPEC.md`:
```text
Relay recovery: recovered_count=0 dropped_confirmed_count=0 dropped_duplicate_count=0 dropped_missing_parent_count=0 dropped_policy_incompatible_count=0 dropped_evicted_count=0
redacted_relay_mempool_evidence
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared recovery counters to relay evidence status</name>
  <files>packages/open-bitcoin-node/src/status/relay_evidence.rs, packages/open-bitcoin-node/src/status/tests.rs, packages/open-bitcoin-node/src/network/recovery.rs, packages/open-bitcoin-node/src/network/relay_fanout.rs, packages/open-bitcoin-rpc/src/method/node.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md` decisions D-07, D-08, D-09, D-10, D-11, and D-12.
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-UI-SPEC.md` "Copywriting Contract", "Data Presentation Contract", and "Redaction And Safety".
    - `packages/open-bitcoin-node/src/status/relay_evidence.rs` for `RelayEvidenceStatus`, `RelayEvidenceField`, and `RelayEvidenceCounters`.
    - `packages/open-bitcoin-node/src/network/recovery.rs` for `ManagedMempoolRecoverySummary` from Plan 108-01.
    - `packages/open-bitcoin-node/src/network/relay_fanout.rs` for `relay_evidence_status_from_parts`.
    - `packages/open-bitcoin-rpc/src/method/node.rs` for `OpenBitcoinNetworkStatusResponse`.
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs` for `openbitcoinnetworkstatus` projection tests.
  </read_first>
  <behavior>
    - Test 1: `relay_evidence_status_projects_recovery_counters` builds a managed network with a recovery summary and proves `openbitcoinnetworkstatus.relay.recovery_counters` contains `recovered_count`, `dropped_confirmed_count`, `dropped_duplicate_count`, `dropped_missing_parent_count`, `dropped_policy_incompatible_count`, and `dropped_evicted_count`.
    - Test 2: `baseline_rpc_methods_omit_recovery_evidence` proves `getmempoolinfo`, `getnetworkinfo`, and `sendrawtransaction` response structs do not serialize `recovery_counters` or `Relay recovery`.
    - Test 3: `relay_recovery_counters_default_to_zero_without_snapshot` proves default/offline status serializes implemented zero counters rather than unavailable free-form text.
  </behavior>
  <action>
    In `packages/open-bitcoin-node/src/status/relay_evidence.rs`, add:
    `pub struct RelayRecoveryCounters` with exact public `u64` fields in this order: `recovered_count`, `dropped_confirmed_count`, `dropped_duplicate_count`, `dropped_missing_parent_count`, `dropped_policy_incompatible_count`, `dropped_evicted_count`.
    Add `#[derive(Debug, Clone, Copy, Default, PartialEq, Eq, Serialize, Deserialize)]`.
    Add `pub recovery_counters: RelayEvidenceField<RelayRecoveryCounters>` to `RelayEvidenceStatus`, with `#[serde(default = "recovery_counters_default")]`.
    Add `fn recovery_counters_default() -> RelayEvidenceField<RelayRecoveryCounters>` returning `RelayEvidenceField::implemented(RelayRecoveryCounters::default())`.
    Add a constructor or helper named `with_activation_recovery_and_counters(activation: RelayActivationEvidence, download_eligibility: RelayDownloadEligibilityCounters, recovery: RelayRecoveryCounters, counters: RelayEvidenceCounters) -> Self`.
    In `packages/open-bitcoin-node/src/network/recovery.rs`, add a conversion from `ManagedMempoolRecoverySummary` to `RelayRecoveryCounters` without string parsing and keep storage-error details out of the struct.
    In `packages/open-bitcoin-node/src/network/relay_fanout.rs`, pass the latest managed recovery counters into `relay_evidence_status_from_parts` and populate `status.recovery_counters` from them while leaving `outcome_counters` as live serving/fanout/local-submission evidence.
    Keep `GetMempoolInfoResponse`, `GetNetworkInfoResponse`, and `SendRawTransactionResponse` unchanged; only `OpenBitcoinNetworkStatusResponse.relay` gains the new recovery field through the shared status type.
    This implements D-07 and D-08 without adding a recovery-only state enum.
  </action>
  <acceptance_criteria>
    - `rg -n "pub struct RelayRecoveryCounters|recovered_count|dropped_confirmed_count|dropped_duplicate_count|dropped_missing_parent_count|dropped_policy_incompatible_count|dropped_evicted_count|recovery_counters" packages/open-bitcoin-node/src/status/relay_evidence.rs` finds all recovery fields and the status field.
    - `rg -n "with_activation_recovery_and_counters|recovery_counters_default|RelayEvidenceField::implemented\\(RelayRecoveryCounters::default\\(\\)\\)" packages/open-bitcoin-node/src/status/relay_evidence.rs` finds the constructor/default path.
    - `rg -n "RelayRecoveryCounters|latest_mempool_recovery_summary|recovery_counters" packages/open-bitcoin-node/src/network/recovery.rs packages/open-bitcoin-node/src/network/relay_fanout.rs` finds the managed projection.
    - `! rg -n "recovery_counters|Relay recovery" packages/open-bitcoin-rpc/src/method/node.rs | rg "GetMempoolInfoResponse|GetNetworkInfoResponse|SendRawTransactionResponse"` exits 0, proving baseline RPC structs remain unchanged.
    - `rg -n "relay_evidence_status_projects_recovery_counters|baseline_rpc_methods_omit_recovery_evidence|relay_recovery_counters_default_to_zero_without_snapshot" packages/open-bitcoin-node/src/status/tests.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds all test names.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_evidence_status_projects_recovery_counters -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc baseline_rpc_methods_omit_recovery_evidence -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture</automated>
  </verify>
  <done>Shared status distinguishes recovery replay from live admission with fixed counters, while baseline-compatible RPC responses remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and sanitize recovery evidence across operator, metric, log, and support surfaces</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render/relay.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs, packages/open-bitcoin-cli/src/operator/support/redaction.rs, packages/open-bitcoin-cli/src/operator/support/render/relay.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs, packages/open-bitcoin-node/src/metrics.rs, packages/open-bitcoin-node/src/metrics/tests.rs, packages/open-bitcoin-node/src/logging.rs, packages/open-bitcoin-node/src/logging/tests.rs, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-CONTEXT.md` decisions D-07, D-08, D-09, D-13, and D-14.
    - `.planning/phases/108-durable-mempool-relay-state-recovery/108-UI-SPEC.md` exact labels `Relay recovery`, field order, support next-action copy, and redaction rules.
    - `packages/open-bitcoin-cli/src/operator/status/render/relay.rs` for current `Relay evidence` line rendering.
    - `packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs` for dashboard row construction.
    - `packages/open-bitcoin-cli/src/operator/support/render/relay.rs` for support Markdown section and next action.
    - `packages/open-bitcoin-cli/src/operator/support/redaction.rs` for `redacted_relay_mempool_evidence`.
    - `packages/open-bitcoin-node/src/metrics.rs` for `MetricKind::ALL`, `relay_metric_samples`, and fixed relay metric names.
    - `packages/open-bitcoin-node/src/logging.rs` for `relay_mempool_log_record`.
  </read_first>
  <behavior>
    - Test 1: `operator_status_renders_relay_recovery_counters` proves human status contains exactly `Relay recovery: recovered_count=1 dropped_confirmed_count=2 dropped_duplicate_count=3 dropped_missing_parent_count=4 dropped_policy_incompatible_count=5 dropped_evicted_count=6`.
    - Test 2: `dashboard_sections_surface_relay_recovery_row` proves the dashboard has exactly one `Relay recovery` row and no multiline cell.
    - Test 3: `support_bundle_renders_relay_recovery_evidence_from_shared_projection` proves support Markdown includes a `- Relay recovery: ...` bullet and the Phase 108 next action from the UI spec.
    - Test 4: `support_bundle_redacts_sensitive_recovery_reason_in_json_and_markdown` proves an unavailable recovery reason containing raw tx hex, txid, wtxid, socket address, permission string, credential, cookie, secret, and dynamic label becomes `redacted_relay_mempool_evidence`.
    - Test 5: `relay_metric_kinds_include_recovery_counters` and `relay_mempool_log_record_includes_recovery_counts_without_dynamic_labels` prove fixed metric kinds and structured logs include recovery counts only.
  </behavior>
  <action>
    In terminal status and dashboard rendering, add a separate line/row immediately after `Relay evidence` with exact label `Relay recovery` and exact field order:
    `recovered_count`, `dropped_confirmed_count`, `dropped_duplicate_count`, `dropped_missing_parent_count`, `dropped_policy_incompatible_count`, `dropped_evicted_count`.
    In support Markdown, add a `- Relay recovery: ...` bullet immediately after `- Relay evidence: ...`.
    Update the support next-action text to the Phase 108 UI contract exactly:
    `Treat recovered relay/mempool evidence as bounded local status and local troubleshooting/parity-review evidence only; do not treat it as public propagation, compact-block relay, production-readiness proof, a release validator, public-network proof, production-service proof, production full-node readiness proof, production-funds wallet safety proof, or authorization for destructive repair.`
    In `support/redaction.rs`, sanitize `relay.recovery_counters` unavailable/deferred/intentionally-different reasons with the same `redacted_relay_mempool_evidence` path as other relay fields.
    In `packages/open-bitcoin-node/src/metrics.rs`, add fixed `MetricKind` variants named `RelayRecoveryRecoveredCount`, `RelayRecoveryDroppedConfirmedCount`, `RelayRecoveryDroppedDuplicateCount`, `RelayRecoveryDroppedMissingParentCount`, `RelayRecoveryDroppedPolicyIncompatibleCount`, and `RelayRecoveryDroppedEvictedCount`; add snake_case names `relay_recovery_recovered_count`, `relay_recovery_dropped_confirmed_count`, `relay_recovery_dropped_duplicate_count`, `relay_recovery_dropped_missing_parent_count`, `relay_recovery_dropped_policy_incompatible_count`, and `relay_recovery_dropped_evicted_count`.
    Update `MetricKind::ALL` length and `relay_metric_samples` so recovery metrics are emitted from `RelayEvidenceStatus.recovery_counters` only when implemented.
    In `relay_mempool_log_record`, append recovery counts using exact keys `recovered`, `dropped_confirmed`, `dropped_duplicate`, `dropped_missing_parent`, `dropped_policy_incompatible`, and `dropped_evicted`; do not add dynamic labels or free-form reason text.
    Do not add ANSI color, icons, cards, web UI, new CTA, destructive repair command, raw logs, txids, wtxids, endpoints, peer ids, permission strings, credentials, or dynamic metric labels.
  </action>
  <acceptance_criteria>
    - `rg -n "Relay recovery: recovered_count=\\{\\}|Relay recovery" packages/open-bitcoin-cli/src/operator/status/render/relay.rs packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs packages/open-bitcoin-cli/src/operator/support/render/relay.rs` finds the new label in all three renderers.
    - `rg -n "Treat recovered relay/mempool evidence as bounded local status and local troubleshooting/parity-review evidence only" packages/open-bitcoin-cli/src/operator/support/render/relay.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds the Phase 108 next-action copy.
    - `rg -n "RelayRecoveryRecoveredCount|RelayRecoveryDroppedConfirmedCount|RelayRecoveryDroppedDuplicateCount|RelayRecoveryDroppedMissingParentCount|RelayRecoveryDroppedPolicyIncompatibleCount|RelayRecoveryDroppedEvictedCount|relay_recovery_recovered_count|relay_recovery_dropped_evicted_count" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/metrics/tests.rs` finds fixed metric kinds and names.
    - `rg -n "recovered=|dropped_confirmed=|dropped_duplicate=|dropped_missing_parent=|dropped_policy_incompatible=|dropped_evicted=" packages/open-bitcoin-node/src/logging.rs packages/open-bitcoin-node/src/logging/tests.rs` finds structured log recovery keys.
    - `rg -n "operator_status_renders_relay_recovery_counters|dashboard_sections_surface_relay_recovery_row|support_bundle_renders_relay_recovery_evidence_from_shared_projection|support_bundle_redacts_sensitive_recovery_reason_in_json_and_markdown|relay_metric_kinds_include_recovery_counters|relay_mempool_log_record_includes_recovery_counts_without_dynamic_labels" packages/open-bitcoin-cli/src/operator/status/tests.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs packages/open-bitcoin-cli/src/operator/support/tests.rs packages/open-bitcoin-node/src/metrics/tests.rs packages/open-bitcoin-node/src/logging/tests.rs` finds all tests.
    - `! rg -n "txid=|wtxid=|127\\.0\\.0\\.1:|198\\.51\\.100\\.|permission_string|credential=|secret=|cookie=|dynamic_label" packages/open-bitcoin-node/src/logging.rs packages/open-bitcoin-cli/src/operator/status/render/relay.rs packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs packages/open-bitcoin-cli/src/operator/support/render/relay.rs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator_status_renders_relay_recovery_counters -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_relay_recovery_row -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_bundle_redacts_sensitive_recovery_reason_in_json_and_markdown -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_metric_kinds_include_recovery_counters -- --exact --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_mempool_log_record_includes_recovery_counts_without_dynamic_labels -- --exact --nocapture</automated>
  </verify>
  <done>Every operator evidence surface renders recovered relay state as fixed aggregate counters, redacts sensitive recovery reasons, and avoids propagation or production-readiness claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Managed recovery summary -> status/RPC JSON | In-memory recovery evidence becomes serialized operator data. |
| Status snapshot -> CLI/dashboard/support | Shared relay evidence becomes terminal and support text. |
| Status snapshot -> metrics/logs | Recovery counters become persisted low-cardinality telemetry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-108-11 | I | `RelayRecoveryCounters` | mitigate | Counters are numeric fixed fields only; no arrays of transactions, peer objects, raw storage details, txids, wtxids, endpoints, permission strings, or credentials. |
| T-108-12 | I | support redaction | mitigate | Add `recovery_counters` to `redact_relay_mempool_evidence` and test hostile reason strings sanitize to `redacted_relay_mempool_evidence`. |
| T-108-13 | R | metrics/logging | mitigate | Add fixed `MetricKind` variants and fixed log keys only; tests reject dynamic labels and sensitive markers. |
| T-108-14 | S | baseline RPC compatibility | mitigate | Keep `getmempoolinfo`, `getnetworkinfo`, and `sendrawtransaction` response structs unchanged; recovery evidence appears only in Open Bitcoin-specific status. |
| T-108-15 | S | false public propagation claims | mitigate | UI copy uses "bounded local status" next action and explicitly excludes public propagation, public-network proof, production-service proof, production readiness, production funds, and destructive repair. |
</threat_model>

<verification>
Run task-level tests plus:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node relay_evidence_status -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node logging -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-cli -p open-bitcoin-rpc --all-targets --all-features -- -D warnings
```
</verification>

<success_criteria>
Plan 108-03 is complete when recovery replay evidence is visible as `Relay recovery` fixed counters in Open Bitcoin status, CLI, dashboard, metrics, logs, and support bundles, with baseline RPC shapes unchanged and redaction covering hostile recovery reason text.
</success_criteria>

<output>
After completion, create `.planning/phases/108-durable-mempool-relay-state-recovery/108-03-SUMMARY.md`.
</output>
