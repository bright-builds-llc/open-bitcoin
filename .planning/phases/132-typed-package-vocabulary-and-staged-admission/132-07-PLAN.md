---
phase: 132-typed-package-vocabulary-and-staged-admission
plan: "07"
type: tdd
wave: 7
depends_on:
  - "06"
files_modified:
  - packages/open-bitcoin-consensus/src/classify.rs
  - packages/open-bitcoin-consensus/src/script/witness.rs
  - packages/open-bitcoin-mempool/src/fee.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/policy.rs
  - packages/open-bitcoin-mempool/src/policy/output.rs
  - packages/open-bitcoin-mempool/src/policy/truc.rs
  - packages/open-bitcoin-mempool/src/policy/ephemeral.rs
  - packages/open-bitcoin-mempool/src/package/report.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/prospective.rs
  - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PACK-07
user_setup: []
must_haves:
  truths:
    - "TRUC `Reject`, `Accept`, and `Enforce` modes preserve distinct pinned behavior; only enforced version 3 receives TRUC topology/size rules and the explicit static-floor exception"
    - "Enforced TRUC validates 10,000-vB maximum, 1,000-vB child maximum, two-member ancestor/descendant limits, version inheritance in both directions, and no sibling/parent-and-child topology using the prospective graph"
    - "Pay-to-anchor is an explicit witness-v1 program `0x4e73` classification with upgradable-witness consensus behavior and non-empty-witness standardness rejection"
    - "PolicyConfig owns typed ephemeral permissions with exact source defaults `anchor=true`, `send=false`, `dust=false`: `anchor` gates P2A, `send` gates dusty non-anchor outputs, and `dust` additionally gates nonzero-valued dust"
    - "Dust thresholds derive from an independent 3,000 sat/kvB `DustRelayFeeRate`; permitted ephemeral dust requires zero base and modified fee and complete spending of every dusty parent output"
    - "D-13 TRUC runs before replacement with direct conflicts plus eligible sibling-eviction intent, so child replacement and sibling eviction are evaluated hypothetically without pre-staged removals"
    - "Ephemeral policy runs after limited replacement and before the separate late script checker; TRUC/ephemeral/script failures discard prospective changes"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/policy/truc.rs"
      provides: "Pure TRUC inheritance, topology, size, and replacement-aware policy"
      contains: "MAX_TRUC_VIRTUAL_SIZE"
    - path: "packages/open-bitcoin-consensus/src/classify.rs"
      provides: "Explicit `PayToAnchor` script classification"
      contains: "PayToAnchor"
    - path: "packages/open-bitcoin-mempool/src/policy/ephemeral.rs"
      provides: "Typed permissions plus zero-fee and complete-dust-spend policy"
      contains: "EphemeralPolicy"
    - path: "packages/open-bitcoin-mempool/src/policy/output.rs"
      provides: "Role-typed dust relay threshold and reachable ephemeral standardness"
      contains: "DustRelayFeeRate"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs"
      provides: "Pinned TRUC, P2A, and ephemeral dust parity matrix"
      contains: "ephemeral"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/policy/truc.rs"
      via: "D-13 pre-replacement TRUC stage receives direct conflicts and eligible sibling-eviction intent"
      pattern: "validate_truc_package"
    - from: "packages/open-bitcoin-mempool/src/policy/output.rs"
      to: "packages/open-bitcoin-consensus/src/classify.rs"
      via: "PayToAnchor standardness and dust-threshold classification"
      pattern: "PayToAnchor"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/policy/ephemeral.rs"
      via: "post-replacement, pre-late-script zero-fee/complete-spend check"
      pattern: "validate_ephemeral_spends"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 132-2026-07-25T18-13-00
generated_at: 2026-07-25T18:13:00.349Z
---

<objective>
Complete pinned TRUC, pay-to-anchor, dust-relay, and ephemeral-spend policy over the staged package engine with exact source defaults and parity fixtures.

Purpose: Deliver the remaining D-12/D-13 PACK-07 surface at full fidelity, including all three ephemeral permissions, without adding RPC/operator adapters.
Output: Pure TRUC/ephemeral modules, explicit P2A classification, typed configuration/defaults, package-engine wiring, and direct Knots-anchored tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-RESEARCH.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-06-SUMMARY.md
@packages/open-bitcoin-consensus/src/classify.rs
@packages/open-bitcoin-consensus/src/script/witness.rs
@packages/open-bitcoin-mempool/src/types.rs
@packages/open-bitcoin-mempool/src/policy/output.rs
@packages/open-bitcoin-mempool/src/pool/package_admission.rs
@packages/bitcoin-knots/src/policy/truc_policy.h
@packages/bitcoin-knots/src/policy/truc_policy.cpp
@packages/bitcoin-knots/src/policy/ephemeral_policy.h
@packages/bitcoin-knots/src/policy/ephemeral_policy.cpp
@packages/bitcoin-knots/test/functional/mempool_truc.py
@packages/bitcoin-knots/test/functional/mempool_ephemeral_dust.py

<interfaces>
Exact policy/default contracts:
```rust
pub const MAX_TRUC_VIRTUAL_SIZE: usize = 10_000;
pub const MAX_TRUC_CHILD_VIRTUAL_SIZE: usize = 1_000;
pub const MAX_TRUC_ANCESTOR_COUNT: usize = 2;
pub const MAX_TRUC_DESCENDANT_COUNT: usize = 2;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct DustRelayFeeRate(FeeRate); // default 3_000 sat/kvB

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct EphemeralPolicy {
    pub anchor: bool,
    pub send: bool,
    pub dust: bool,
}

impl Default for EphemeralPolicy {
    // exact Knots source defaults
    // anchor=true, send=false, dust=false
}

pub(crate) fn validate_truc_package<V: MempoolView>(
    view: &V,
    members: &[PreparedCandidate],
    policy: TrucPolicy,
    direct_conflicts: &BTreeSet<Txid>,
    maybe_sibling_eviction: Option<EligibleSiblingEviction>,
) -> Result<(), TrucPolicyError>;

pub(crate) fn validate_ephemeral_spends<V: MempoolView>(
    view: &V,
    members: &[PreparedCandidate],
    permissions: EphemeralPolicy,
    dust_relay_fee_rate: DustRelayFeeRate,
) -> Result<(), EphemeralPolicyError>;
```

Exact P2A script:
```text
OP_1 0x02 0x4e 0x73
witness version = 1
program bytes = [0x4e, 0x73]
```
</interfaces>
</context>

<scope-safety>
This plan intentionally owns the cross-crate P2A classification and the two tightly coupled mempool policy modules because the exact permission predicates must be reachable through standardness before ephemeral spend validation, and TRUC must consume replacement intent from the package engine before removal staging. Splitting those seams would create an intermediate state with dead ephemeral policy or incorrectly ordered TRUC. Each task remains a separately testable vertical slice, and no RPC, CLI, persistence, or operator configuration files are in scope.
</scope-safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify pay-to-anchor and install typed dust/ephemeral defaults</name>
  <files>packages/open-bitcoin-consensus/src/classify.rs, packages/open-bitcoin-consensus/src/script/witness.rs, packages/open-bitcoin-mempool/src/fee.rs, packages/open-bitcoin-mempool/src/types.rs, packages/open-bitcoin-mempool/src/policy.rs, packages/open-bitcoin-mempool/src/policy/output.rs, packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-consensus/src/classify.rs (`ScriptPubKeyType` and witness-program match order)
    - packages/open-bitcoin-consensus/src/script/witness.rs (upgradable witness and witness-stack validation branches)
    - packages/open-bitcoin-mempool/src/fee.rs (semantic fee-role wrapper conventions)
    - packages/open-bitcoin-mempool/src/types.rs (`PolicyConfig` fields/defaults and `TrucPolicy`)
    - packages/open-bitcoin-mempool/src/policy.rs (transaction standardness order and input witness access)
    - packages/open-bitcoin-mempool/src/policy/output.rs (current fixed 330/546 dust thresholds and unconditional rejection)
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (existing Phase 132 policy target to extend with P2A/default cases)
    - packages/bitcoin-knots/src/script/script.cpp (`IsPayToAnchor`)
    - packages/bitcoin-knots/src/policy/policy.h (dust relay rate and ephemeral permission defaults)
    - packages/bitcoin-knots/src/policy/policy.cpp (P2A standardness and witness stuffing rejection)
    - packages/bitcoin-knots/src/test/txvalidation_tests.cpp (P2A/ephemeral unit cases)
    - docs/parity/source-breadcrumbs.json (consensus and mempool-policy registrations)
  </read-first>
  <behavior>
    - Script bytes `0x51 0x02 0x4e 0x73` classify as `PayToAnchor`; nearby version/program/length variants do not
    - Consensus verification treats P2A as an upgradable witness program rather than Taproot or a new spend rule
    - Standardness rejects a P2A spend with non-empty witness
    - Dust thresholds are calculated from `DustRelayFeeRate`, with default 3000 sat/kvB reproducing existing ordinary 330/546 thresholds
    - `PolicyConfig::default()` has `EphemeralPolicy { anchor: true, send: false, dust: false }`
    - With defaults, exact P2A zero-valued dust is permitted, while dusty non-anchor output and every nonzero-valued dust output reject
    - Disabling `anchor` rejects P2A regardless of `send`/`dust`; dusty non-anchor output requires `send`; any nonzero-valued dust additionally requires `dust`
    - No CLI, RPC, JSONC, persistence, or environment configuration is added
  </behavior>
  <action>
    Add `ScriptPubKeyType::PayToAnchor` and classify it before generic `WitnessUnknown`: exact witness version 1 and exact two-byte program `[0x4e, 0x73]` only. In consensus witness verification, route `PayToAnchor` through the same upgradable-witness success semantics as the prior generic unknown version; do not treat it as Taproot or add consensus rules. In mempool standardness, reject non-empty witness when spending a P2A output.

    Add semantic `DustRelayFeeRate(FeeRate)` with default `3_000 sat/kvB`. Replace fixed dust policy source with the Knots size-based dust formula using the configured dust relay rate while preserving ordinary default thresholds. Define `EphemeralPolicy { anchor, send, dust }` and add `ephemeral_policy` plus `dust_relay_fee_rate` to `PolicyConfig`; exact defaults are `anchor: true`, `send: false`, `dust: false`.

    Implement the three booleans as the exact independent predicates from `policy.cpp`: `anchor == false` rejects a P2A output irrespective of the other permissions; if an output is dust and not P2A, `send == false` rejects it; if any dust output has nonzero value, `dust == false` rejects it. Thus `dust` never grants a dusty non-anchor form without `send`, and `send` is a concrete output-standardness gate rather than a future seam. Refactor output standardness so only outputs permitted by those predicates can reach the package spend policy. Add the full boolean/value/form combination matrix, exact classification/default/threshold/witness tests, and breadcrumbs for `script.cpp`, `policy.{h,cpp}`, `txvalidation_tests.cpp`, and ephemeral functional anchors. Add no Phase 137 adapter/config surface.
  </action>
  <acceptance-criteria>
    - `rg -n "PayToAnchor|0x4e|0x73" packages/open-bitcoin-consensus/src/classify.rs packages/open-bitcoin-consensus/src/script/witness.rs packages/open-bitcoin-mempool/src/policy/output.rs` returns explicit classification/handling
    - `rg -n "struct DustRelayFeeRate|3_000" packages/open-bitcoin-mempool/src/fee.rs` returns typed default rate
    - `rg -n "struct EphemeralPolicy|anchor: true|send: false|dust: false|dust_relay_fee_rate" packages/open-bitcoin-mempool/src/types.rs` returns exact permissions/defaults
    - `rg -n "PayToAnchor.*anchor|dust.*send|nonzero.*dust|send.*non.?anchor" packages/open-bitcoin-mempool/src/policy/output.rs packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs` returns the three exact predicates and combination tests
    - `rg -n "rpc|cli|jsonc|env::|storage" packages/open-bitcoin-mempool/src/types.rs packages/open-bitcoin-mempool/src/policy/output.rs` returns 0 new adapter/config code
    - `bun run scripts/command-timings.ts run --key phase132-p2a-consensus -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus classify` exits 0
    - `bun run scripts/command-timings.ts run --key phase132-p2a-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool pay_to_anchor` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-p2a-consensus -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus classify &amp;&amp; bun run scripts/command-timings.ts run --key phase132-p2a-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool pay_to_anchor</automated>
  </verify>
  <done>P2A is explicitly and safely classified, dust uses a semantic rate, and all three pinned ephemeral permissions/defaults exist without operator-surface scope creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce complete TRUC inheritance, topology, size, and replacement-aware rules</name>
  <files>packages/open-bitcoin-mempool/src/policy.rs, packages/open-bitcoin-mempool/src/policy/truc.rs, packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/policy/truc.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/types.rs (`TrucPolicy` exact modes/default)
    - packages/open-bitcoin-mempool/src/policy.rs (policy child-module/export pattern)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (D-13 order: static then TRUC then rolling)
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (overlay-aware ancestor/descendant/replacement facts)
    - packages/open-bitcoin-mempool/src/package/report.rs (typed hard TRUC failure variants)
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (existing target to extend with the TRUC matrix)
    - packages/bitcoin-knots/src/policy/truc_policy.h (exact constants and interfaces)
    - packages/bitcoin-knots/src/policy/truc_policy.cpp (inheritance/topology/replacement logic)
    - packages/bitcoin-knots/test/functional/mempool_truc.py (direct behavior matrix)
    - docs/parity/source-breadcrumbs.json (`mempool-policy` group)
  </read-first>
  <behavior>
    - `Reject` hard rejects version 3; `Accept` treats version 3 as ordinary; `Enforce` applies all TRUC checks
    - Enforced TRUC rejects transaction vsize above 10000, child vsize above 1000, and prospective ancestor/descendant count above two
    - Version inheritance fails when a TRUC child spends non-TRUC parent or a non-TRUC child spends TRUC parent, in package or mempool
    - Sibling topology and a member that is both parent and child within the candidate package reject
    - A direct conflict that is the existing TRUC child is treated as hypothetical child replacement even though no removal has been staged
    - An otherwise eligible sole sibling produces sibling-eviction intent that allows the caller to continue to later limited-RBF validation; ineligible sibling topology rejects
    - TRUC failure is hard policy failure and leaves overlay/base state unchanged
  </behavior>
  <action>
    Create `policy/truc.rs` with the four exact constants in the interface block and typed `TrucPolicyError` variants. `validate_truc_package` must branch exactly: `Reject` rejects any version 3; `Accept` performs no TRUC topology/size enforcement and grants no static exception; `Enforce` treats version 3 as TRUC, enforces 10,000-vB per transaction, 1,000-vB child, ancestor count at most two, descendant count at most two, and version inheritance in both directions.

    Evaluate topology over the original pre-replacement `ProspectiveMempool`, including in-package ancestors/additions. Pass the deterministic direct-conflict set and an optional eligible sibling-eviction intent into `validate_truc_package`; do not assume or require any replacement removal to have been staged. When the existing sole TRUC child is a direct conflict, evaluate the descendant limit as hypothetical child replacement. When the parent has exactly one otherwise eligible child with no descendants, return/consume explicit sibling-eviction intent so later limited-RBF policy can decide and stage the removal; reject multiple children, a sibling with descendants, ordinary package siblings, a candidate with both an in-package parent and child, and other prohibited topology. Keep the module pure and independent of peer origin.

    Wire the D-13 TRUC stage after ordinary static-floor selection/explicit Enforce-v3 exception and before aggregate rolling, general limits, limited replacement, ephemeral policy, and the separate late script checker. Limited replacement remains the only later stage that materializes the accepted conflict/sibling removals. Add typed report mappings and the full `mempool_truc.py` matrix, with direct pinned tests for child replacement via direct conflict and eligible/ineligible sibling eviction before removal staging. Register `truc.rs` with `truc_policy.{h,cpp}`, `mempool_options.h`, and `mempool_truc.py` anchors.
  </action>
  <acceptance-criteria>
    - `rg -n "MAX_TRUC_VIRTUAL_SIZE: usize = 10_000|MAX_TRUC_CHILD_VIRTUAL_SIZE: usize = 1_000|MAX_TRUC_ANCESTOR_COUNT: usize = 2|MAX_TRUC_DESCENDANT_COUNT: usize = 2" packages/open-bitcoin-mempool/src/policy/truc.rs` returns all constants
    - `rg -n "TrucPolicy::Reject|TrucPolicy::Accept|TrucPolicy::Enforce|direct_conflicts|maybe_sibling_eviction|inherit|sibling" packages/open-bitcoin-mempool/src/policy/truc.rs` returns every policy branch and pre-replacement input
    - `rg -n "validate_truc_package" packages/open-bitcoin-mempool/src/pool/package_admission.rs` appears before aggregate rolling assessment
    - `rg -n "truc.*reject|truc.*accept|10_000|1_000|inherit|child.*replacement|sibling.*eviction" packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs` returns the pinned matrix
    - `rg -n "policy/truc.rs" docs/parity/source-breadcrumbs.json` returns the new source
    - `bun run scripts/command-timings.ts run --key phase132-truc-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool truc` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-truc-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool truc</automated>
  </verify>
  <done>All three TRUC modes and the complete enforced inheritance/topology/size/replacement-aware behavior are wired in the locked policy order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce zero-fee ephemeral dust and complete parent-dust spending</name>
  <files>packages/open-bitcoin-mempool/src/policy.rs, packages/open-bitcoin-mempool/src/policy/output.rs, packages/open-bitcoin-mempool/src/policy/ephemeral.rs, packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/policy.rs (policy module/export order to extend)
    - packages/open-bitcoin-mempool/src/policy/ephemeral.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/package/report.rs (typed hard ephemeral failure variants)
    - packages/open-bitcoin-mempool/src/types.rs (`EphemeralPolicy` exact permissions/defaults)
    - packages/open-bitcoin-mempool/src/fee.rs (`CandidateFees { base, modified }`, `DustRelayFeeRate`)
    - packages/open-bitcoin-mempool/src/policy/output.rs (Task 1 permitted ephemeral standardness and dust calculation)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (D-13 post-limits/replacement, pre-script stage)
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (in-package and in-mempool parent/output lookup)
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (existing target to extend with ephemeral cases)
    - packages/bitcoin-knots/src/policy/ephemeral_policy.h
    - packages/bitcoin-knots/src/policy/ephemeral_policy.cpp (zero-fee and complete-spend checks)
    - packages/bitcoin-knots/src/test/txvalidation_tests.cpp (direct unit anchors)
    - packages/bitcoin-knots/test/functional/mempool_ephemeral_dust.py (multiple output/parent/in-mempool cases)
    - docs/parity/source-breadcrumbs.json (`mempool-policy` group)
  </read-first>
  <behavior>
    - Permitted dusty parent succeeds only when both base and modified fee are exactly zero
    - Nonzero base or injected nonzero modified fee hard rejects the dusty parent
    - A child of a dusty in-package or in-mempool parent spends every dust output of that parent
    - Missing one of multiple dust outputs, missing dust from any one of multiple dusty parents, or partial in-mempool parent spend rejects
    - Permission combinations prove `anchor` gates P2A, `send` gates dusty non-anchor output, and `dust` additionally gates nonzero-valued dust; default remains anchor=true/send=false/dust=false
    - Ephemeral checks run after topology/limited replacement and before the separate late script checker; ephemeral failure executes zero scripts and leaves overlay/base state unchanged
  </behavior>
  <action>
    Create `policy/ephemeral.rs` with typed `EphemeralPolicyError`. Identify dust using `DustRelayFeeRate` and the Task 1 output policy. For every candidate transaction with a permitted dust output, require both `CandidateFees.base.to_sats() == 0` and `CandidateFees.modified.to_sats() == 0`; no aggregate child fee may satisfy this rule. For every child, inspect all in-package and still-present in-mempool parents through the prospective view; if any parent has dust outputs, require the child inputs to spend every dust outpoint from every such parent.

    Consume only outputs already admitted by Task 1's exact permission predicates: `anchor` gates P2A; `send` is required for dusty non-anchor outputs; `dust` is additionally required for every nonzero-valued dust output. Preserve defaults `true/false/false`; never interpret `dust` as permission for non-anchor form and do not add CLI/RPC/config/persistence adapters. Run `validate_ephemeral_spends` after topology/general limits/limited replacement and before the separate `run_late_script_checks` stage, matching D-13. An ephemeral failure must return before any contextual script invocation and must not apply or trim.

    Add direct parity tests for zero/nonzero base/modified fees, complete/partial multi-dust spending, multiple dusty parents, in-mempool parent, the full P2A/non-anchor × zero/nonzero-value × permission combination matrix, dry-run/submit equality, rollback, and an objective stage probe showing ephemeral rejection causes zero script calls while success calls scripts only afterward. Register `ephemeral.rs` with `ephemeral_policy.{h,cpp}`, `policy.{h,cpp}`, `txvalidation_tests.cpp`, and `mempool_ephemeral_dust.py`.
  </action>
  <acceptance-criteria>
    - `rg -n "fn validate_ephemeral_spends|base.*== 0|modified.*== 0|dust.*outpoint|EphemeralPolicyError" packages/open-bitcoin-mempool/src/policy/ephemeral.rs` returns zero-fee and complete-spend seams
    - `rg -n "validate_ephemeral_spends|run_late_script_checks" packages/open-bitcoin-mempool/src/pool/package_admission.rs` shows ephemeral validation before the separate late script checker
    - `rg -n "nonzero.*base|nonzero.*modified|multiple.*dust|multiple.*parent|in_mempool.*dust|anchor.*send.*dust|ephemeral.*script" packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs` returns the full matrix and stage probe
    - `rg -n "policy/ephemeral.rs" docs/parity/source-breadcrumbs.json` returns the new source
    - `bun run scripts/command-timings.ts run --key phase132-ephemeral-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool ephemeral` exits 0 with nonzero tests
    - `bun run scripts/command-timings.ts run --key phase132-package-policy-all -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases` exits 0
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-ephemeral-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool ephemeral &amp;&amp; bun run scripts/command-timings.ts run --key phase132-package-policy-all -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases</automated>
  </verify>
  <done>Ephemeral permissions/defaults, zero-fee dust, complete parent-dust spending, and locked policy ordering are fully implemented and parity-tested.</done>
</task>

</tasks>

<threat-model>
## Assets

- TRUC topology/resource bounds and version inheritance.
- Standardness/consensus separation for pay-to-anchor.
- Dust relay economics and ephemeral-parent sponsorship integrity.
- Default permission policy and mutation-free failures.

## Trust Boundaries

| Boundary | Untrusted or adversarial state crossing it |
| --- | --- |
| Script bytes/witness → classification and standardness | Arbitrary witness version/program/stack |
| Prospective package graph → TRUC policy | Versions, sizes, ancestors, descendants, replacements |
| Candidate fees/parent outputs → ephemeral policy | Base/modified fees, dust outputs, child inputs |
| Typed permissions → policy decision | Three independent booleans with pinned defaults |

## Threats and Mitigations

| Threat ID | STRIDE | Threat | Disposition | Concrete mitigation |
| --- | --- | --- | --- | --- |
| T-132-33 | Elevation of Privilege/Policy bypass | `Accept` is confused with `Enforce` or static exception leaks | mitigate | Task 2 has explicit three-way branch and mode-specific tests |
| T-132-34 | Denial of Service | TRUC graph exceeds size/count/topology bounds or incorrectly observes removals from a later stage | mitigate | Task 2 enforces 10000/1000 vB and two-member bounds over the pre-replacement graph using explicit direct-conflict/sibling intent |
| T-132-35 | Spoofing/Tampering | P2A is confused with Taproot or accepts witness stuffing | mitigate | Task 1 exact `[0x4e,0x73]` classification, upgradable consensus route, non-empty-witness standardness rejection |
| T-132-36 | Elevation of Privilege | Permission roles are conflated so `dust` grants non-anchor output or `send` bypasses nonzero dust | mitigate | Task 1 implements and combination-tests exact anchor-form, non-anchor-send, and nonzero-dust predicates with pinned defaults |
| T-132-37 | Denial of Service/Policy bypass | Fee-paying child sponsors nonzero-fee dusty parent | mitigate | Task 3 requires both base and modified parent fees equal zero independently of aggregate |
| T-132-38 | Tampering | Child leaves some dust output unspent | mitigate | Task 3 enumerates every dust outpoint from every in-package/in-mempool dusty parent |
| T-132-39 | Tampering/Information Disclosure | TRUC/ephemeral failure leaks staged membership/rolling/effects or invokes scripts too early | mitigate | Pre-replacement TRUC and post-replacement ephemeral checks run before the separate late checker/apply; rollback and stage-probe tests cover state plus script-call count |

## Security Verification

- Direct source-anchored mode/size/inheritance/topology tests.
- Exact P2A near-miss and witness-stuffing tests.
- Unequal base/modified fee and multi-parent/multi-output dust tests.
- Source scan proves no Phase 137 adapter/config code.
</threat-model>

<verification>
Run consensus classification, TRUC, ephemeral, complete package-policy, and mempool library suites through timed commands; run breadcrumb checker; inspect diff for accidental RPC/node/config dependencies.
</verification>

<success-criteria>
- Full pinned TRUC modes, bounds, inheritance, topology, and replacement interaction pass.
- P2A has explicit classification with correct consensus/standardness separation.
- All three ephemeral permissions exist with `anchor=true`, `send=false`, `dust=false` and enforce the exact P2A/non-anchor/nonzero predicates.
- Zero-fee and complete-spend dust rules cover package and in-mempool parents.
- Child replacement and eligible sibling eviction pass the pre-replacement TRUC stage through explicit direct-conflict/intent inputs.
- Ephemeral failure occurs before scripts; all failures remain prospective and effect-free.
</success-criteria>

<output>
After completion, create `.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-07-SUMMARY.md`
</output>
