---
phase: 132-typed-package-vocabulary-and-staged-admission
plan: "08"
type: execute
wave: 8
depends_on:
  - "07"
files_modified:
  - packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/README.md
  - README.md
  - docs/parity/source-breadcrumbs.json
  - docs/parity/catalog/mempool-policy.md
  - docs/metrics/lines-of-code.md
  - scripts/check-phase132-typed-package-staged-admission.ts
  - scripts/check-phase132-typed-package-staged-admission.test.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-04
  - PACK-05
  - PACK-06
  - PACK-07
user_setup: []
must_haves:
  truths:
    - "A crate-internal package-parity unit module covers exact package bounds, positive checked refinement, report cardinality/order/status and nonempty/consistent-fee-group validation, fingerprint/order, dry-run equality, partial acceptance, stale-base rejection, sparse apply, late-script ordering, fee groups, final trim, witness alias, conservative limited-RBF counting, pre-replacement TRUC, and exact ephemeral predicates"
    - "Rustdoc compile-fail tests and the Phase 132 checker prove callers cannot directly construct the submission refinement or construct/mutate package reports and fee groups outside their checked public constructors"
    - "Crate-internal generated max-bound graphs prove sparse overlay state/delta equals full recomputation and package work performs no per-member whole-mempool clone/recompute"
    - "Every new first-party Rust source/test file has a defensible Knots breadcrumb and the mempool parity catalog states exact supported behavior and intentional Rust-owned implementation differences"
    - "Contributor-facing READMEs describe bounded local dry-run/submission without claiming Phase 133 peer assembly, Phase 134 cache projection, Phase 137 adapters, general package wire, public/default relay, guaranteed propagation, or production readiness"
    - "A mutation-tested Phase 132 checker is wired into the default verifier after Phase 131 and the complete `bash scripts/verify.sh` contract passes"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs"
      provides: "Crate-internal pinned/generated package-admission parity closure with access to test-only oracles and instrumentation"
      contains: "max_bound"
    - path: "packages/open-bitcoin-mempool/src/pool/tests.rs"
      provides: "Library unit-test registry for the internal package-parity closure"
      contains: "mod package_parity_cases"
    - path: "docs/parity/catalog/mempool-policy.md"
      provides: "Auditable Phase 132 behavior, source anchors, intentional differences, and exclusions"
      contains: "Typed Package Vocabulary and Staged Admission"
    - path: "scripts/check-phase132-typed-package-staged-admission.ts"
      provides: "Structural/claim/ordering/breadcrumb verifier for PACK-01 through PACK-07"
      contains: "PACK-07"
    - path: "scripts/verify.sh"
      provides: "Default invocation of the Phase 132 checker and tests"
      contains: "check-phase132-typed-package-staged-admission"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs"
      to: "packages/bitcoin-knots/src/test/txpackage_tests.cpp"
      via: "fixed shape/fingerprint/partial-acceptance vectors"
      pattern: "txpackage_tests"
    - from: "packages/open-bitcoin-mempool/src/pool/tests.rs"
      to: "packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs"
      via: "crate-internal unit-test module registration"
      pattern: "mod package_parity_cases"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase132-typed-package-staged-admission.ts"
      via: "Bun test and run steps immediately after Phase 131 checker"
      pattern: "check-phase132-typed-package-staged-admission"
    - from: "docs/parity/source-breadcrumbs.json"
      to: "all new Phase 132 Rust paths"
      via: "mempool-policy/lifecycle/resource/consensus registrations"
      pattern: "package_admission"
    - from: "scripts/check-phase132-typed-package-staged-admission.ts"
      to: "packages/open-bitcoin-mempool/src/package.rs and packages/open-bitcoin-mempool/src/package/report.rs"
      via: "mutation-tested exact opacity, checked-constructor, read-only accessor, and compile-fail API assertions"
      pattern: "compile_fail"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 132-2026-07-25T18-13-00
generated_at: 2026-07-25T18:13:00.349Z
---

<objective>
Close Phase 132 with integrated pinned/generated parity proof, complete breadcrumb/catalog/README truth, a mutation-tested phase checker, and the repository-wide verification contract.

Purpose: Prove PACK-01 through PACK-07 as one coherent bounded pure-core surface while preserving all deferred phase and release-claim boundaries.
Output: Crate-internal parity suite, audited docs/breadcrumbs, Phase 132 verifier wiring, refreshed LOC artifact, and green full verification.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-RESEARCH.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-07-SUMMARY.md
@packages/open-bitcoin-mempool/src/pool/tests.rs
@docs/parity/source-breadcrumbs.json
@docs/parity/catalog/mempool-policy.md
@scripts/check-phase131-rolling-fee-expiry-pressure.ts
@scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
@scripts/verify.sh

<interfaces>
Final deterministic verification sequence:
```bash
bun run scripts/command-timings.ts run --key phase132-package-api-doc-tests -- \
  cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --doc
bun run scripts/command-timings.ts run --key phase132-mempool-tests -- \
  cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool
bun run scripts/check-parity-breadcrumbs.ts
bun run scripts/command-timings.ts run --key phase132-bazel-smoke -- \
  bazel build //packages/open-bitcoin-mempool:open_bitcoin_mempool_lib
bash scripts/verify.sh
```

Required claim boundary:
```text
Supported: bounded local dry-run and child-with-unconfirmed-parents submission,
typed ordered results, individual-first partial acceptance, effective fee groups,
coherent staged apply, limited package RBF, TRUC, P2A/ephemeral dust, final membership.

Not claimed: peer package assembly, cross-cache projection, RPC/CLI/dashboard package
adapters, snapshot/recovery, fanout/transport receipts, general package wire protocol,
arbitrary multi-parent peer assembly, public/default/production relay, guaranteed
propagation, live public-network default CI, or production readiness.
```
</interfaces>
</context>

<scope-safety>
This closure plan owns one crate-internal oracle module, its parity documentation/checker, and the final repository verification contract. Those artifacts intentionally span ten files because the unit-test registry and mutation-tested checker must bind executable internal behavior, breadcrumb evidence, contributor claims, and verifier wiring in one audit boundary; splitting them would weaken the final PACK-01 through PACK-07 proof. Production implementation remains entirely in Plans 01 through 07.
</scope-safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the package engine with crate-internal pinned and recomputation oracles</name>
  <files>packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs (new crate-internal unit-test target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests.rs (existing unit-test module registry and shared fixture imports)
    - packages/open-bitcoin-mempool/src/package.rs (final public package/command/output API)
    - packages/open-bitcoin-mempool/src/package/report.rs (final ordered result/group vocabulary)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (final D-13 order and public methods)
    - packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs (test-only recomputation/counter helpers)
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (advanced-policy fixture builders)
    - packages/bitcoin-knots/src/test/txpackage_tests.cpp (shape/fingerprint/partial/effective-fee anchors)
    - packages/bitcoin-knots/test/functional/mempool_package_rbf.py
    - packages/bitcoin-knots/test/functional/mempool_truc.py
    - packages/bitcoin-knots/test/functional/mempool_ephemeral_dust.py
    - packages/bitcoin-knots/src/test/txvalidation_tests.cpp
    - docs/parity/source-breadcrumbs.json (all Phase 132 new-path registrations)
  </read-first>
  <behavior>
    - Fixed boundary/fingerprint/report vectors agree with pinned Knots anchors and D-01's unconditional 404000 bound
    - Positive singleton/CWUP refinement and report constructor cases prove checked construction, including too-few/too-many cardinality, swapped identity order, mismatched package status, and empty/duplicate/inconsistent-rate fee-group rejection
    - Dry-run and submit reports are equal from the same base while dry-run state fingerprint is unchanged
    - Valid parent/invalid child, later valid after hard failure, and reconsiderable residual group exercise individual-first partial acceptance
    - Static/member versus rolling/group, alias exclusion, final trim rewrite, and lifecycle final membership agree
    - Policy-stage probes prove scripts occur only after static/TRUC/rolling/limits/replacement/ephemeral checks, and no pre-script failure invokes the checker
    - Limited RBF rejects when overlapping direct-conflict descendant counts conservatively sum above 100 even if the union is at most 100
    - Pre-replacement TRUC covers direct-conflict child replacement and eligible sibling eviction; exact P2A/non-anchor/nonzero permission predicates and zero-fee/complete-spend cases remain integrated
    - Deterministic generated max-25-member graphs match recomputed membership/relations/aggregates/spent index/ledger/rolling/delta and record zero per-member full clones/recomputes plus one final trim
  </behavior>
  <action>
    Create `src/pool/tests/package_parity_cases.rs` as a crate-internal unit-test module and register it with `mod package_parity_cases;` in `src/pool/tests.rs`. Keep every closure case in this module; do not create a separate integration-test target. Because this module is compiled inside the library test harness, it may directly exercise the narrow `#[cfg(test)]` oracle hooks, crate-private revision preparation/apply seams, sparse patches, stale-transition controls, recomputation materializers, clone counters, and trim counters that are unavailable to Rust integration tests. Reuse production policy functions rather than duplicating policy.

    Include fixed vectors/cases for empty/25/26, 404000/404001, duplicate identity, topology/conflict, fingerprint permutations/order, positive singleton/child-with-unconfirmed-parents `try_from_package` refinement, `PackageReport::try_new` too-few/too-many/swapped-identity/mismatched-status failures, `EffectiveFeeGroup::try_new` empty/duplicate/inconsistent-rate failures, accessor-preserved order, dry-run/submit equality, partial acceptance, stale membership/rolling base, sparse patch apply, fee groups, witness alias, final trim, and objective D-13 stage/failure ordering. Use only checked constructors and read-only accessors in this downstream suite; the external literal/mutation rejection remains owned by Plan 01 rustdoc compile-fail tests. Include the overlapping-direct-conflict case whose descendant-count sum exceeds 100 while its union does not, pre-replacement TRUC child replacement and eligible/ineligible sibling eviction, exact P2A classification, all cumulative `anchor`/`send`/`dust` predicates, and ephemeral zero-fee/complete-spend behavior.

    Add a deterministic seeded max-bound graph matrix whose only full materialization occurs inside test oracle code. After singleton, residual, replacement, and trim sub-deltas, compare entries, parent/child relations, ancestor/descendant aggregates, spent index, resource ledger, rolling state, ordered report, and lifecycle final membership against full recomputation. Assert instrumentation reports zero production whole-mempool clones/recomputes per member and exactly one final trim.

    Keep tests hermetic: no network, wall clock, randomness without explicit seed, sleep, filesystem service, or adapter crates. Add a parity breadcrumb header to the new Rust test file and register its exact `src/pool/tests/package_parity_cases.rs` path in `docs/parity/source-breadcrumbs.json` with `packages.cpp`, `validation.{h,cpp}`, `txpackage_tests.cpp`, `txvalidation_tests.cpp`, and the three relevant functional tests.
  </action>
  <acceptance-criteria>
    - `rg -n "max_bound|try_from_package|too_(few|many)|swapped.*identity|mismatched.*status|empty.*fee.*group|inconsistent.*rate|dry_run.*submit|valid_parent.*invalid_child|stale|static.*rolling|script.*order|overlap.*100|child.*replacement|sibling.*eviction|pay_to_anchor|anchor.*send.*dust|ephemeral" packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs` returns every integrated behavior family
    - `rg -n "revision|patch|recompute|zero.*clone|one.*trim|25" packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs` returns the internal stale-transition, sparse-patch, recomputation, and bounded-work proof
    - `rg -n "SystemTime::now|thread::sleep|TcpStream|reqwest|rand::thread_rng" packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs` returns 0
    - `rg -n "^mod package_parity_cases;$" packages/open-bitcoin-mempool/src/pool/tests.rs` returns the unit-test registry entry
    - `rg -n "src/pool/tests/package_parity_cases.rs" docs/parity/source-breadcrumbs.json` returns the new test path with non-`none` Knots anchors
    - `bun run scripts/command-timings.ts run --key phase132-package-parity -- bash -c 'set -euo pipefail; test_output="$(cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib package_parity_cases)"; printf "%s\n" "$test_output"; rg -q "test result: ok\\. [1-9][0-9]* passed" &lt;&lt;&lt; "$test_output"'` exits 0 after executing a nonzero number of crate-internal package-parity tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-package-parity -- bash -c 'set -euo pipefail; test_output="$(cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib package_parity_cases)"; printf "%s\n" "$test_output"; rg -q "test result: ok\\. [1-9][0-9]* passed" &lt;&lt;&lt; "$test_output"'</automated>
  </verify>
  <done>One hermetic crate-internal unit suite executes the complete typed/staged package behavior, stale/patch transitions, bounded-work instrumentation, and final truth against pinned and independent oracles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize parity docs, claims, breadcrumbs, and mutation-tested Phase 132 checker</name>
  <files>packages/README.md, README.md, docs/parity/source-breadcrumbs.json, docs/parity/catalog/mempool-policy.md, scripts/check-phase132-typed-package-staged-admission.ts, scripts/check-phase132-typed-package-staged-admission.test.ts, scripts/verify.sh</files>
  <read-first>
    - scripts/check-phase132-typed-package-staged-admission.ts (new target; confirm absent before creation)
    - scripts/check-phase132-typed-package-staged-admission.test.ts (new target; confirm absent before creation)
    - README.md (current mempool status and v2.2 future-work wording)
    - packages/README.md (pure-core crate ownership summary)
    - packages/open-bitcoin-mempool/src/pool/tests.rs (`package_parity_cases` crate-internal module registry)
    - packages/open-bitcoin-mempool/src/package.rs (opaque private-field `SubmissionPackage`, checked constructor/accessors, and E0451 compile-fail doctest)
    - packages/open-bitcoin-mempool/src/package/report.rs (private-field `PackageReport`/`EffectiveFeeGroup`, checked constructors/accessors, and E0451/E0616 compile-fail doctests)
    - docs/parity/source-breadcrumbs.json (complete manifest, all Plan 01–07 registrations)
    - docs/parity/catalog/mempool-policy.md (Phase 130/131 facts and Phase 132 ownership boundary)
    - scripts/check-phase131-rolling-fee-expiry-pressure.ts (checker structure and safe file-reading helpers)
    - scripts/check-phase131-rolling-fee-expiry-pressure.test.ts (mutation-test pattern)
    - scripts/verify.sh (Phase 130/131 checker ordering in both command-order and run-step sections)
    - .planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md (D-01 through D-16 and deferred ideas)
    - .planning/REQUIREMENTS.md (PACK-01 through PACK-07 exact claims)
    - packages/bitcoin-knots/doc/policy/packages.md and source/test anchors listed in research
  </read-first>
  <behavior>
    - Catalog lists all PACK-01 through PACK-07 behaviors, exact Knots anchors, sparse-overlay/ordered-type intentional differences, and deferred boundaries
    - Every new Phase 132 Rust source/test path is present in the breadcrumb manifest with no unjustified `none`
    - Checker requires `package_parity_cases` to be registered under `pool::tests` and breadcrumbed as a crate-internal Rust test path
    - Checker requires private refinement/report/group representations, sole checked constructors, read-only accessors, no public fields/setters/mutable access, and error-code-specific direct-construction/mutation compile-fail doctests
    - README status changes from package depth being wholly future work to bounded local pure-core package admission, while peer/RPC/public relay remain explicitly deferred
    - Checker fails when any bound/default/type/stage/order/breadcrumb/claim string is removed or mutated
    - Checker rejects general package wire, peer package assembly, RPC adapter, public/default relay, guaranteed propagation, public-network CI, or production-readiness claims
    - `scripts/verify.sh` runs checker tests and checker immediately after Phase 131 in both ordering surfaces
  </behavior>
  <action>
    Update `mempool-policy.md` with a `Typed Package Vocabulary and Staged Admission` section documenting exact bounds, refinement, fingerprint/order, dry-run/submit capabilities, partial acceptance, sparse guarded apply, fee roles/groups, limited RBF, TRUC modes/default, P2A, ephemeral permissions/defaults (`anchor=true`, `send=false`, `dust=false`), zero-fee/complete-spend rules, one trim, final rewrite, and lifecycle facts. Cite exact pinned files/tests. Document intentional Rust differences: opaque refinements, request-aligned vectors, sparse overlay, deterministic Rust-owned accounting; do not imply external behavior divergence.

    Audit `source-breadcrumbs.json` for every new Phase 132 path across package, pool, policy, consensus, and tests. Update root/package READMEs with bounded local pure-core support and the exact exclusion paragraph from the interface block.

    Create a Bun checker and mutation-tested companion that assert PACK-01 through PACK-07 source/doc evidence: `25`, `404_000`; `SubmissionPackage` as a private-field struct with `try_from_package` as its only public constructor, payload-free public kind enum, immutable `package()`/`kind()` accessors, no public raw-payload variants/fields/`From`/`Default`/unchecked constructor/mutable accessor, and an E0451 direct-construction compile-fail doctest; `PackageReport` and `EffectiveFeeGroup` private invariant fields, checked `try_new` with member cardinality/order/status and nonempty/duplicate/effective-rate consistency validation, read-only slice/scalar accessors, no `Default`/unchecked construction/setter/mutable accessor, E0451 literal-construction and E0616 field-mutation compile-fail doctests; command separation; ordered result variants; crate-private sparse `MempoolPatch` with revision-first apply and no production `MempoolState`/recompute; the `mod package_parity_cases;` unit-test registry entry and exact breadcrumb path; static → TRUC → rolling → limits → limited replacement → ephemeral → late scripts ordering; zero script calls on earlier failure; conservative pre-union 100-candidate counting; TRUC direct-conflict/sibling-intent inputs; P2A bytes; dust rate 3000; exact cumulative ephemeral predicates/defaults; one trim/final rewrite; no adapter imports; all breadcrumbs; and narrow claims.

    Mutation cases must fail independently when a refinement/report/group field becomes public, `SubmissionPackage` regresses to raw-payload variants, an unchecked constructor/mutable accessor appears, a compile-fail privacy proof is removed or changed to the wrong error code, member cardinality/order/status or nonempty/consistent-rate group-constructor evidence is removed, the internal parity-module registry/breadcrumb disappears, late-script order drifts, the overlap-count guard is weakened, TRUC pre-replacement inputs are removed, or any permission predicate is removed/inverted. Wire test/run steps immediately after Phase 131 and before Phase 117 in both `VERIFY_COMMAND_ORDER` and `run_step` sections. The default workspace `cargo test` already reaches doctests, but retain the explicit Plan 01 and final `cargo test -p open-bitcoin-mempool --doc` commands so API opacity cannot be filtered out by unit-test name selection.
  </action>
  <acceptance-criteria>
    - `rg -n "Typed Package Vocabulary and Staged Admission|PACK-01|PACK-07|anchor=true|send=false|dust=false|sparse|ordered|one.*trim" docs/parity/catalog/mempool-policy.md` returns complete documented evidence
    - `rg -n "bounded local|package admission|peer.*defer|general package wire|guaranteed propagation" README.md packages/README.md` returns supported/deferred claims
    - `bun run scripts/check-parity-breadcrumbs.ts` exits 0
    - `bun test scripts/check-phase132-typed-package-staged-admission.test.ts` exits 0 with mutation coverage
    - `bun run scripts/check-phase132-typed-package-staged-admission.ts` exits 0 and reports PACK-01 through PACK-07
    - `rg -n "package_parity_cases|src/pool/tests/package_parity_cases.rs" scripts/check-phase132-typed-package-staged-admission.ts scripts/check-phase132-typed-package-staged-admission.test.ts` returns both registry/breadcrumb enforcement and its mutation case
    - `rg -n "SubmissionPackage|PackageReport|EffectiveFeeGroup|compile_fail|E0451|E0616|try_from_package|try_new|mutable|public field" scripts/check-phase132-typed-package-staged-admission.ts scripts/check-phase132-typed-package-staged-admission.test.ts` returns exact opacity checks and independent mutations
    - `rg -n "check-phase132-typed-package-staged-admission" scripts/verify.sh` returns at least four references covering test/run in both ordering surfaces
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts &amp;&amp; bun test scripts/check-phase132-typed-package-staged-admission.test.ts &amp;&amp; bun run scripts/check-phase132-typed-package-staged-admission.ts</automated>
  </verify>
  <done>Parity evidence, breadcrumbs, READMEs, claim boundaries, and mutation-tested default-verifier ownership are complete.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Rust, Bazel, and repository verification and review the final diff</name>
  <files>docs/metrics/lines-of-code.md</files>
  <read-first>
    - scripts/verify.sh (repo-native full verification contract)
    - scripts/command-timings.ts (cooperative build-lock/timing wrapper contract)
    - packages/open-bitcoin-mempool/BUILD.bazel (exact Bazel library target)
    - docs/metrics/lines-of-code.md (tracked generated freshness artifact)
    - AGENTS.md (Rust pre-commit order, timed command rule, quiet-command polling rule)
    - all `132-*-SUMMARY.md` files produced by Plans 01–07 (implemented files, deviations, residual risks)
  </read-first>
  <action>
    Run the final commands in the interface block sequentially, including the explicit package rustdoc API-visibility suite before the broader crate tests, and never overlap Cargo jobs against the shared target directory. Poll any yielded/resumable verification session at least every 60 seconds and capture liveness evidence before considering termination. Run `cargo fmt --all` through the repo manifest if formatting changes are required, then rerun focused tests before the full verifier. Allow the repo hook/verifier workflow to refresh `docs/metrics/lines-of-code.md`; keep that tracked freshness change.

    Run `git diff --check`, inspect `git diff --stat`, and review every Phase 132 source/doc/script diff for unintended adapter scope, new dependencies, raw `Vec<Transaction>` package entry points, bool capability switches, saturating/wrapping accounting, per-member full clones/recomputes, unregistered Rust files, or broadened relay/production claims. Fix any in-scope issue and rerun the affected focused command plus `bash scripts/verify.sh`.
  </action>
  <acceptance-criteria>
    - `bun run scripts/command-timings.ts run --key phase132-package-api-doc-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --doc` exits 0 after executing the E0451/E0616 opacity compile-fail doctests
    - `bun run scripts/command-timings.ts run --key phase132-mempool-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool` exits 0
    - `bun run scripts/check-parity-breadcrumbs.ts` exits 0
    - `bun run scripts/command-timings.ts run --key phase132-bazel-smoke -- bazel build //packages/open-bitcoin-mempool:open_bitcoin_mempool_lib` exits 0
    - `bash scripts/verify.sh` exits 0
    - `git diff --check` exits 0
    - `git diff --name-only` contains no node, network, RPC, CLI, storage, dashboard, snapshot, or transport implementation file introduced by Phase 132
  </acceptance-criteria>
  <verify>
    <automated>bash scripts/verify.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>The complete repo-native Rust/Bazel/coverage/checker contract passes, LOC freshness is retained, and final diff review finds no scope or claim leakage.</done>
</task>

</tasks>

<threat-model>
## Assets

- Crate-internal integrated PACK-01 through PACK-07 behavior and bounded-work proof.
- Auditable Knots source lineage and truthful contributor claims.
- Default verifier ownership preventing silent regression.

## Trust Boundaries

| Boundary | Untrusted or mutable state crossing it |
| --- | --- |
| Production package engine → crate-internal parity unit suite | Cross-module behavior, private transition state, and hidden instrumentation drift |
| Source/docs/breadcrumbs → Phase 132 checker | Claims and structural evidence that can become stale |
| Phase checker → default verifier | Ordering/wiring that can be removed or bypassed |
| Full workspace → Cargo/Bazel/coverage gates | Cross-crate compile and policy regressions |

## Threats and Mitigations

| Threat ID | STRIDE | Threat | Disposition | Concrete mitigation |
| --- | --- | --- | --- | --- |
| T-132-40 | Denial of Service | Malformed/max-bound or adversarial graph work becomes unbounded | mitigate | Task 1 pins limits and asserts zero per-member clone/recompute plus one trim |
| T-132-41 | Tampering | Stale-base/partial mutation regression passes unit slices | mitigate | Task 1 integrates membership/rolling stale and failure-injection state fingerprints |
| T-132-42 | Spoofing | Witness alias or post-trim result/lifecycle truth regresses | mitigate | Task 1 cross-checks requested/existing identities and final delta membership |
| T-132-43 | Elevation of Privilege/Policy bypass | Static/TRUC/RBF/ephemeral policy ordering drifts | mitigate | Tasks 1–2 assert source order, constants, defaults, and behavior mutations |
| T-132-44 | Repudiation | New Rust behavior lacks auditable Knots anchors | mitigate | Task 2 audits every new path and runs breadcrumb checker |
| T-132-45 | Information Disclosure/Scope leakage | Docs or code claim/implement peer/RPC/public relay effects | mitigate | Task 2 checker rejects adapter imports and forbidden claim families |
| T-132-46 | Tampering | Default verifier silently stops running Phase 132 checks | mitigate | Task 2 mutation-tests dual ordering/wiring; Task 3 runs full verifier |
| T-132-47 | Denial of Service/Tampering | Integer/accounting underflow or trim drift escapes focused tests | mitigate | Task 1 generated recomputation oracle plus Task 3 full Rust/Bazel/coverage gates |
| T-132-48 | Elevation of Privilege/Tampering | Public API drift reopens unchecked submission refinement or report/group construction and mutation | mitigate | Tasks 1–2 require positive checked-constructor cases, error-code-specific compile-fail doctests, exact opacity checks, and independent checker mutations; Task 3 explicitly runs package doctests |

## Security Verification

- Crate-internal parity suite covers every required threat family, including test-only revision/patch/oracle/instrumentation seams.
- Rustdoc compile-fail cases prove external construction/mutation is rejected specifically by field privacy, while crate-internal tests prove checked constructors accept and reject the intended cases.
- Mutation checker proves opacity/constructor/accessor, structural/default, and claim needles are required.
- Full repo verifier supplies format, clippy, build, test, benchmark, Bazel, coverage, architecture, panic, breadcrumb, and file-length gates.
</threat-model>

<verification>
Execute the five-command sequence from the interface block, confirm explicit rustdoc opacity tests and the checker are default-verifier reachable, run `git diff --check`, and review the final changed-file list against the pure-core Phase 132 boundary.
</verification>

<success-criteria>
- All PACK requirement behaviors have deterministic integrated proof.
- Submission refinement and report/group invariants are externally opaque, checked-constructor-only, and protected by executable compile-fail plus mutation regressions.
- All Phase 132 Rust files have source breadcrumbs and catalog coverage.
- README/catalog claims remain narrow and truthful.
- Phase 132 checker is mutation-tested and default-verifier wired.
- `bash scripts/verify.sh` and diff review pass without deferred-scope implementation.
</success-criteria>

<output>
After completion, create `.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-08-SUMMARY.md`
</output>
