---
phase: 132-typed-package-vocabulary-and-staged-admission
plan: "04"
type: tdd
wave: 4
depends_on:
  - "01"
  - "03"
files_modified:
  - packages/open-bitcoin-mempool/src/package.rs
  - packages/open-bitcoin-mempool/src/package/report.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/prospective.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PACK-02
  - PACK-03
  - PACK-04
  - PACK-05
user_setup: []
must_haves:
  truths:
    - "Dry-run and submission are distinct command/output types over the same package evaluation engine"
    - "Submission accepts only Plan 01's opaque checked refinement and reads its private package/kind state through immutable accessors; it cannot reconstruct or mutate the child-with-unconfirmed-parents capability"
    - "Dry-run evaluates the full prospective pipeline and returns the same ordered report vocabulary without changing entries, indexes, ledger, rolling fee, revision, relay, persistence, caches, or evidence"
    - "New members are evaluated individually in request order against an evolving prospective view and successful singleton admissions remain staged"
    - "Only missing-input or typed reconsiderable members enter the residual group; a hard failure suppresses residual evaluation but does not stop later singleton evaluation"
    - "A failed residual package does not erase prior singleton success, so a valid parent can remain accepted when its child fails"
    - "Submission performs one guarded live apply and returns a committed lifecycle delta; dry-run cannot return committed facts"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      provides: "D-13 individual-first state machine and command-specific prepare/discard/apply APIs"
      contains: "dry_run_package"
    - path: "packages/open-bitcoin-mempool/src/package.rs"
      provides: "Distinct dry-run/submission commands and impossible-state-free outputs"
      contains: "DryRunPackageCommand"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs"
      provides: "Ordered partial-acceptance, residual, dry-run, and guarded-submit regressions"
      contains: "valid_parent"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/pool/candidate.rs"
      via: "ordered pre-script preparation followed by the separate late checker after applicable package policy"
      pattern: "(prepare_candidate|check_candidate_scripts)"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/pool/prospective.rs"
      via: "retain successes and compose coherent sub-deltas"
      pattern: "ProspectiveMempool"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "Mempool::apply_prepared"
      via: "submission only, exactly once after full preparation"
      pattern: "apply_prepared"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/package.rs"
      via: "submission consumes the checked refinement through `SubmissionPackage::package()`/`kind()` and never destructures private state"
      pattern: "command\\.package\\.(package|kind)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 132-2026-07-25T18-13-00
generated_at: 2026-07-25T18:13:00.349Z
---

<objective>
Implement the shared individual-first package state machine with distinct dry-run and child-with-unconfirmed-parents submission capabilities, ordered partial acceptance, residual reconsideration, and one guarded apply.

Purpose: Deliver D-06 through D-10 and the orchestration portions of D-13/D-16 without global all-or-nothing semantics or adapter side effects.
Output: Package command/output types, package admission module, partial-acceptance tests, and public pure-core entry points.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-RESEARCH.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-01-SUMMARY.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-03-SUMMARY.md
@packages/open-bitcoin-mempool/src/package.rs
@packages/open-bitcoin-mempool/src/package/report.rs
@packages/open-bitcoin-mempool/src/pool/candidate.rs
@packages/open-bitcoin-mempool/src/pool/prospective.rs
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/test/txpackage_tests.cpp

<interfaces>
Plan 01 checked input/report prerequisites:
```rust
pub struct SubmissionPackage { /* private package + kind */ }
impl SubmissionPackage {
    pub fn try_from_package(
        package: WellFormedPackage,
        chainstate: &ChainstateSnapshot,
    ) -> Result<Self, PackageShapeError>;
    pub fn package(&self) -> &WellFormedPackage;
    pub fn kind(&self) -> SubmissionPackageKind;
}

impl PackageReport {
    pub fn try_new(
        package: &WellFormedPackage,
        status: PackageStatus,
        members: Vec<PackageMemberResult>,
        effective_fee_groups: Vec<EffectiveFeeGroup>,
    ) -> Result<Self, PackageReportError>;
    pub fn members(&self) -> &[PackageMemberResult];
}
```

Required command/output capabilities:
```rust
pub struct DryRunPackageCommand {
    pub package: WellFormedPackage,
    pub context: AdmissionContext,
}

pub struct SubmitPackageCommand {
    pub package: SubmissionPackage,
    pub context: AdmissionContext,
}

pub struct DryRunPackageResult {
    pub report: PackageReport,
}

pub struct SubmittedPackageResult {
    pub report: PackageReport,
    pub delta: MempoolLifecycleDelta,
}

impl Mempool {
    pub fn dry_run_package(
        &self,
        command: DryRunPackageCommand,
        chainstate: &ChainstateSnapshot,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<DryRunPackageResult, MempoolError>;

    pub fn submit_package(
        &mut self,
        command: SubmitPackageCommand,
        chainstate: &ChainstateSnapshot,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<SubmittedPackageResult, MempoolError>;
}
```
</interfaces>
</context>

<scope-safety>
The command types, ordered state machine, prospective composition, and public dry-run/submit entry points share one input-aligned result vector and one prepared package patch, so splitting their ownership would duplicate classification or expose an apply-capable intermediate API. The two tasks separate capability/classification from residual composition/apply, while later plans extend the explicit policy slots before the already separate late script checker.
</scope-safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode distinct dry-run/submission capabilities and ordered classification</name>
  <files>packages/open-bitcoin-mempool/src/package.rs, packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests.rs (module registry to extend)
    - packages/open-bitcoin-mempool/src/lib.rs (public exports to extend)
    - packages/open-bitcoin-mempool/src/package.rs (Plan 01 private refinement representation, sole checked constructor, and immutable `package()`/`kind()` accessors)
    - packages/open-bitcoin-mempool/src/package/report.rs (private-field ordered report, checked `PackageReport::try_new`, and read-only accessors)
    - packages/open-bitcoin-mempool/src/pool.rs (module/export and guarded apply interfaces)
    - packages/open-bitcoin-mempool/src/pool/candidate.rs (shared non-mutating preparation)
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (overlay-first lookup and sub-delta composition)
    - packages/open-bitcoin-mempool/src/outcome.rs (hard/reconsiderable single-attempt mapping inputs)
    - packages/bitcoin-knots/src/validation.cpp (`AcceptMultipleTransactions` lines 2031–2096)
    - packages/bitcoin-knots/src/validation.h (existing/witness-alias result meanings)
    - packages/bitcoin-knots/src/test/txpackage_tests.cpp (partial acceptance and duplicate/alias cases)
    - docs/parity/source-breadcrumbs.json (`mempool-policy` group)
  </read-first>
  <behavior>
    - Exact mempool wtxid is `AlreadyPresent`; same txid with different requested wtxid is `SameTxidDifferentWitness` carrying the existing wtxid; neither is prepared as a new candidate
    - New members are individually evaluated in request order through pre-script preparation, applicable singleton policy, and the separate late script checker; only then is success retained in the prospective view
    - Missing-input and explicitly typed reconsiderable failures are collected for residual evaluation
    - Hard failure sets `skip_residual` but later request members still receive singleton evaluation/results
    - Results remain exactly input-index aligned regardless of lookup-map ordering
  </behavior>
  <action>
    Add the four exact public command/output types from the interface block. `DryRunPackageCommand` accepts `WellFormedPackage`; `SubmitPackageCommand` accepts only Plan 01's checked opaque `SubmissionPackage`. Consume submission members/kind only through `SubmissionPackage::package()` and `SubmissionPackage::kind()`; do not pattern-match a raw-payload enum, add a public field/constructor, reconstruct the refinement, or obtain mutable package state. Neither command contains an apply boolean, relay flag, persistence callback, cache handle, or evidence publisher. Dry-run output contains only opaque `PackageReport`; submitted output contains opaque `PackageReport` plus committed `MempoolLifecycleDelta`.

    Create `pool/package_admission.rs` with private `evaluate_package(base, package, ...) -> Result<PreparedPackageEvaluation, MempoolError>`. At one base revision, classify each member in request order as exact existing wtxid, same-txid/different-witness alias, or new. For every new member, call Plan 02 `prepare_candidate` against the evolving `ProspectiveMempool`; this produces pre-script facts only. Run the applicable singleton policy slots and then the separate `check_candidate_scripts` seam before marking that attempt successful and retaining its singleton `SubDelta`. Plans 05 through 07 fill the explicit static/TRUC/rolling/limits/replacement/ephemeral slots before this checker; do not put script execution back inside `prepare_candidate`. Map only missing-input and typed reconsiderable failures into a residual vector. On a hard failure set `skip_residual = true` but continue evaluating later members individually. Store intermediate results in an input-sized vector; maps are lookup projections only.

    Add exact/alias/order/hard-versus-reconsiderable tests plus a stage probe proving singleton pre-script policy failure executes zero scripts and singleton script failure does not retain/apply the candidate. Export the pure-core APIs from `lib.rs` and register `package_admission.rs` plus its test file with `validation.{h,cpp}` and `txpackage_tests.cpp` breadcrumbs.
  </action>
  <acceptance-criteria>
    - `rg -n "struct DryRunPackageCommand|struct SubmitPackageCommand|struct DryRunPackageResult|struct SubmittedPackageResult" packages/open-bitcoin-mempool/src/package.rs` returns all four capability types
    - `rg -n "command\\.package\\.(package|kind)\\(" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns immutable refined-input access and `! rg -n "SubmissionPackage::(Single|ChildWithUnconfirmedParents)|SubmissionPackage \\{" packages/open-bitcoin-mempool/src/pool/package_admission.rs` confirms no downstream refinement reconstruction
    - `rg -n "fn evaluate_package|AlreadyPresent|SameTxidDifferentWitness|skip_residual|Reconsiderable" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns the ordered classification state machine
    - `rg -n "prepare_candidate|check_candidate_scripts" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns distinct pre-script and late-script calls
    - `rg -n "apply: bool|relay|persist|evidence|ManagedNetwork" packages/open-bitcoin-mempool/src/package.rs packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns 0 production capability leaks
    - `rg -n "pool/package_admission.rs|package_admission_cases.rs" docs/parity/source-breadcrumbs.json` returns both paths
    - `bun run scripts/command-timings.ts run --key phase132-package-classify -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_admission_cases` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-package-classify -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_admission_cases</automated>
  </verify>
  <done>Command capabilities are unambiguous, classification is identity-correct and request ordered, and hard failures do not suppress later singleton evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose residual partial acceptance, dry-run discard, and one guarded submit</name>
  <files>packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (Task 1 ordered classification/intermediate result vector)
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (`SubDelta::compose`, transition preparation, immutable base)
    - packages/open-bitcoin-mempool/src/package/report.rs (private invariant fields, `PackageReport::try_new`, read-only accessors, and reconsiderable/hard variants)
    - packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs (Task 1 fixture builders and ordering assertions)
    - packages/bitcoin-knots/src/validation.cpp (`AcceptSubPackage`, `AcceptMultipleTransactions`, `ProcessNewPackage`)
    - packages/bitcoin-knots/src/test/txpackage_tests.cpp (valid parent/invalid child and package-fee residual cases)
  </read-first>
  <behavior>
    - Reconsiderable parent plus fee-paying child can be evaluated as one residual subpackage over singleton successes
    - Valid parent plus invalid child returns partial status and keeps the parent prospectively/finally present
    - Residual policy/late-script/composition failure discards only that residual sub-delta, not earlier fully checked singleton successes
    - Dry-run and submit from identical base produce identical ordered reports; dry-run preserves a byte-for-byte state fingerprint including revision and rolling fields
    - Submit performs exactly one `apply_prepared`; stale apply returns typed error and no partial mutation
  </behavior>
  <action>
    Complete the D-07 state machine: if `skip_residual` is false and the reconsiderable vector is non-empty, evaluate it as one residual subpackage against the prospective view that already contains fully checked singleton successes. Prepare residual candidates without scripts, run the residual policy-stage slots, then invoke the separate late checker before composing a successful residual `SubDelta`. Plans 05 through 07 fill those policy slots in exact D-13 order. On residual hard/reconsiderable/late-script/limit/composition failure, retain earlier singleton successes and record ordered residual member failures. Package status must distinguish complete, partial, and failed prospective outcomes. Finalize every report only with `PackageReport::try_new`; never use a struct literal, field assignment, mutable accessor, or unchecked reconstruction.

    Implement public `dry_run_package(&self, ...)` by evaluating the complete pipeline and returning only its report while dropping the owned prepared transition. Capture and compare a deterministic private state fingerprint covering entries/ids/relations, spent index, ledger, every `RollingFeeState` field, and revision before/after. Implement `submit_package(&mut self, ...)` by evaluating once and invoking `apply_prepared` exactly once; return the report and committed delta. Do not enqueue relay, mutate persistence/serving/compact/orphan/retry state, or publish metrics/log/evidence per D-16.

    Add focused tests for valid-parent/invalid-child, hard-failure-then-later-valid, reconsiderable residual success/failure, dry-run equality/non-mutation, residual composition failure, and stale submit. Use Arrange/Act/Assert and one behavior per test.
  </action>
  <acceptance-criteria>
    - `rg -n "pub fn dry_run_package|pub fn submit_package|apply_prepared" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns both APIs and one submission apply site
    - `rg -n "valid_parent.*invalid_child|hard.*later.*valid|dry_run.*unchanged|residual.*failure|stale.*submit" packages/open-bitcoin-mempool/src/pool/tests/package_admission_cases.rs` returns all core scenarios
    - `rg -n "ManagedNetwork|storage|relay_queue|compact|orphan|metric|log" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns 0
    - `bun run scripts/command-timings.ts run --key phase132-partial-package -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_admission_cases` exits 0
    - `bun run scripts/command-timings.ts run --key phase132-dry-run -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool dry_run` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-partial-package -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_admission_cases &amp;&amp; bun run scripts/command-timings.ts run --key phase132-dry-run -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool dry_run</automated>
  </verify>
  <done>Individual-first partial acceptance, residual retry, dry-run non-mutation, and one guarded submission apply are behaviorally proven.</done>
</task>

</tasks>

<threat-model>
## Assets

- Ordered truthful per-member outcomes and package-wide partial/complete status.
- Dry-run non-mutation guarantee across all core state.
- One coherent committed package delta with no adapter effects.

## Trust Boundaries

| Boundary | Untrusted or staged state crossing it |
| --- | --- |
| Typed package command → classification | Caller-controlled member identities and transaction contents |
| Individual outcomes → residual group | Hard versus reconsiderable failure classification |
| Prepared evaluation → dry-run discard or submit apply | Capability boundary controlling live mutation |

## Threats and Mitigations

| Threat ID | STRIDE | Threat | Disposition | Concrete mitigation |
| --- | --- | --- | --- | --- |
| T-132-16 | Tampering | Individual-first logic becomes global atomic rejection | mitigate | Task 2 retains successful singleton sub-deltas and tests valid parent/invalid child |
| T-132-17 | Elevation of Privilege/Policy bypass | Hard failures enter the residual fee group | mitigate | Task 1 has separate hard/reconsiderable enums and `skip_residual` gate |
| T-132-18 | Spoofing | Witness alias is evaluated/admitted as requested wtxid | mitigate | Task 1 classifies by txid+wtxid and carries existing wtxid without candidate preparation |
| T-132-19 | Tampering/Information Disclosure | Dry-run mutates rolling/membership or publishes effects | mitigate | Task 2 uses `&self`, drops owned transition, fingerprints all core state, and has no adapter dependencies |
| T-132-20 | Tampering | Residual failure leaks partial live mutation | mitigate | Task 2 composes only owned overlay sub-deltas and applies once after complete preparation |

## Security Verification

- State-fingerprint tests cover dry-run and failure paths.
- Call-site scan proves exactly one guarded apply in submission and none in dry-run.
- Dependency/source scans prove no node/RPC/storage/network adapter imports.
</threat-model>

<verification>
Run focused package-admission and dry-run tests, the full mempool library suite, and parity breadcrumb checker; inspect public API exports to ensure no bool capability switch or optional committed delta exists.
</verification>

<success-criteria>
- PACK-02 dry-run is behaviorally identical to prospective submit evaluation and byte-for-byte non-mutating.
- PACK-03 returns typed package status and ordered member outcomes.
- PACK-04 preserves individual-first partial acceptance.
- PACK-05 commits a composed transition once and discards all failed preparation.
</success-criteria>

<output>
After completion, create `.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-04-SUMMARY.md`
</output>
