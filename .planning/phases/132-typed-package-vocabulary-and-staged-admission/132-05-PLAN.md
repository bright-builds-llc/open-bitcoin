---
phase: 132-typed-package-vocabulary-and-staged-admission
plan: "05"
type: tdd
wave: 5
depends_on:
  - "04"
files_modified:
  - packages/open-bitcoin-mempool/src/fee.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/package/report.rs
  - packages/open-bitcoin-mempool/src/pool/candidate.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/prospective.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PACK-03
  - PACK-06
  - PACK-07
user_setup: []
must_haves:
  truths:
    - "Every ordinary new package member independently passes the static relay floor; high-fee siblings cannot sponsor an ordinary below-static member"
    - "A non-empty eligible new-member group can satisfy the active rolling floor from checked aggregate fee and vsize"
    - "Incremental relay fee is used only for replacement/pressure policy and never contaminates static or rolling package admission"
    - "Only version 3 under `TrucPolicy::Enforce` receives the explicit static-floor exception; `Accept` is the pinned default and applies ordinary fee rules"
    - "Package members reach the separate script checker only after static floor, pre-replacement TRUC, aggregate rolling floor, general limits, limited replacement, and ephemeral checks succeed"
    - "Exactly one pressure trim runs after the late script stage and every initially successful result is rewritten from authoritative final prospective membership"
    - "The committed lifecycle delta contains only actually final newly admitted/removal identities; exact existing members, aliases, failed candidates, and post-trim-absent admissions cannot appear as admitted"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/fee.rs"
      provides: "Multi-member static and aggregate rolling assessment with checked candidate fee facts"
      contains: "evaluate_package_fee_group"
    - path: "packages/open-bitcoin-mempool/src/types.rs"
      provides: "Pinned three-state TRUC policy vocabulary with default Accept"
      contains: "enum TrucPolicy"
    - path: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      provides: "D-13 fee ordering, one trim, final rewrite, and final lifecycle assembly"
      contains: "rewrite_final_membership"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs"
      provides: "Unequal fee-role, alias exclusion, aggregate rolling, trim rewrite, and delta-truth cases"
      contains: "static_floor"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/candidate.rs"
      to: "CandidateFees { base, modified }"
      via: "fee facts retained from consensus preparation through policy"
      pattern: "CandidateFees"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/fee.rs"
      via: "ordinary member-static checks then one aggregate rolling assessment"
      pattern: "evaluate_package_fee_group"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      via: "one final prospective trim after the late script stage and before result/delta finalization"
      pattern: "trim_prospective_to_capacity"
    - from: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      to: "packages/open-bitcoin-mempool/src/pool/candidate.rs"
      via: "late package script stage uses Plan 02 checker only after all pre-script policy stages"
      pattern: "check_candidate_scripts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 132-2026-07-25T18-13-00
generated_at: 2026-07-25T18:13:00.349Z
---

<objective>
Complete package effective-fee grouping, explicit TRUC fee-mode vocabulary, one final pressure trim, final-membership rewriting, and lifecycle facts derived only from final truth.

Purpose: Fulfill D-11, fee/final-truth portions of D-12 through D-15, PACK-06, and the post-trim identity boundary of PACK-07.
Output: Checked group assessment, `TrucPolicy`, final rewrite/lifecycle logic, and focused fee/trim parity tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-RESEARCH.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-04-SUMMARY.md
@.planning/phases/130-resource-time-and-fee-primitives/130-02-SUMMARY.md
@.planning/phases/130-resource-time-and-fee-primitives/130-04-SUMMARY.md
@packages/open-bitcoin-mempool/src/fee.rs
@packages/open-bitcoin-mempool/src/types.rs
@packages/open-bitcoin-mempool/src/package/report.rs
@packages/open-bitcoin-mempool/src/pool/package_admission.rs
@packages/open-bitcoin-mempool/src/pool/lifecycle.rs
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/validation.h

<interfaces>
Existing Phase 130 roles:
```rust
pub struct StaticRelayFeeRate(FeeRate);
pub struct IncrementalRelayFeeRate(FeeRate);
pub struct RollingMempoolFeeRate(FeeRate);
```

Required group/policy contracts:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct CandidateFees {
    pub base: Amount,
    pub modified: Amount,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TrucPolicy {
    Reject,
    Accept,
    Enforce,
}

pub fn evaluate_package_fee_group(
    members: &[PackageFeeMember],
    static_floor: StaticRelayFeeRate,
    rolling_floor: RollingMempoolFeeRate,
    truc_policy: TrucPolicy,
) -> Result<PackageFeeGroupAssessment, PackageFeeError>;

pub struct EffectiveFeeGroup { /* private invariant-bearing fields */ }
impl EffectiveFeeGroup {
    pub fn try_new(
        id: EffectiveFeeGroupId,
        ordered_wtxids: Vec<Wtxid>,
        base_fee_sats: Amount,
        modified_fee_sats: Amount,
        virtual_size: TransactionVirtualSize,
        effective_fee_rate: FeeRate,
    ) -> Result<Self, EffectiveFeeGroupError>;
    pub fn ordered_wtxids(&self) -> &[Wtxid];
}
```

Pinned fee decision table:
```text
ordinary member: independent static pass + singleton/group rolling pass
TRUC v3 / Reject: hard non-standard reject
TRUC v3 / Accept: ordinary static + rolling rules
TRUC v3 / Enforce: static exception + ordinary rolling rule; Plan 07 enforces full topology/size policy
existing exact or witness alias: excluded from fee groups and lifecycle admission
```
</interfaces>

<scope-safety>
The plan spans fee roles, report finalization, package orchestration, lifecycle assembly, and their focused test module because D-11/D-13/D-15 form one ordered truth boundary: fee grouping determines eligible members, the late script stage gates trim, and trim determines both the report and lifecycle delta. The two tasks separate fee assessment from finalization while retaining a single owner for the only package stage-order function; splitting further would permit contradictory group/report/delta semantics.
</scope-safety>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize fee roles to non-empty ordered effective groups</name>
  <files>packages/open-bitcoin-mempool/src/fee.rs, packages/open-bitcoin-mempool/src/types.rs, packages/open-bitcoin-mempool/src/pool/candidate.rs, packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, packages/open-bitcoin-mempool/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests.rs (module registry to extend)
    - packages/open-bitcoin-mempool/src/lib.rs (public fee/policy exports to extend)
    - docs/parity/source-breadcrumbs.json (register the new package policy test path)
    - packages/open-bitcoin-mempool/src/fee.rs (`PackageFeeFloorAssessment` and semantic fee roles)
    - packages/open-bitcoin-mempool/src/types.rs (`PolicyConfig::default` and current fee/RBF fields)
    - packages/open-bitcoin-mempool/src/pool/candidate.rs (`CandidateFees` produced by Plan 02)
    - packages/open-bitcoin-mempool/src/package/report.rs (private-field `EffectiveFeeGroup`, sole checked `try_new`, read-only accessors, and eligible member variants)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (Plan 04 singleton/residual ordering)
    - packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs (Phase 130 unequal-role fixture pattern)
    - packages/bitcoin-knots/src/validation.cpp (static check lines 1097–1112 and group fee lines 1831–1856)
    - packages/bitcoin-knots/doc/policy/packages.md (effective-feerate group semantics)
    - packages/bitcoin-knots/src/kernel/mempool_options.h (`TrucPolicy::{Reject,Accept,Enforce}`, default Accept)
    - packages/bitcoin-knots/test/functional/mempool_truc.py (enforced-v3 static exception)
  </read-first>
  <behavior>
    - Ordinary group with one member below static is rejected even when aggregate exceeds static and rolling floors
    - Every ordinary member above static plus aggregate above rolling passes, with ordered requested wtxids in one non-empty fee group
    - Aggregate exactly at rolling passes; one satoshi below required group fee fails reconsiderably
    - Incremental relay fee changes do not change static/rolling assessment results
    - TRUC mode defaults to `Accept`; only `Enforce` version 3 bypasses the static check, while `Reject` hard rejects and `Accept` applies ordinary rules
    - Exact existing and witness-alias members are absent from group fee/vsize/membership
  </behavior>
  <action>
    Make `CandidateFees { base: Amount, modified: Amount }` a stable typed fact used by candidate preparation, report arithmetic, replacement, and later ephemeral policy; production preparation sets both from the consensus fee, while pure tests can supply unequal values. Replace the one-member `evaluate_package_fee_floors` limitation with `evaluate_package_fee_group` over a checked non-empty ordered slice of `PackageFeeMember { identity, version, fees, virtual_size }`.

    Execute the fee portion of D-13 without scripts: for every ordinary new member independently require `modified` fee rate at or above `StaticRelayFeeRate`; for version 3, `TrucPolicy::Reject` is hard non-standard, `Accept` remains ordinary, and only `Enforce` skips the static check; then sum fees/vsize with checked arithmetic and require aggregate at or above `RollingMempoolFeeRate`. Incremental relay fee must not be an input to this function. Return an `EffectiveFeeGroup` only through Plan 01's checked `EffectiveFeeGroup::try_new`; its ordered wtxids must exactly match eligible new group members. Read membership through `ordered_wtxids()` and scalar accessors; never use a struct literal, field assignment, setter, or mutable collection accessor. Keep exact existing and witness aliases out of the input. `evaluate_package_fee_group` and `prepare_candidate` must not call `check_candidate_scripts`.

    Add `truc_policy: TrucPolicy` to `PolicyConfig` with source default `Accept`; export it. Add unequal-value and boundary tests, register the new test path with `validation.cpp`, `mempool_options.h`, `txpackage_tests.cpp`, and `mempool_truc.py` anchors.
  </action>
  <acceptance-criteria>
    - `rg -n "struct CandidateFees|base: Amount|modified: Amount|fn evaluate_package_fee_group" packages/open-bitcoin-mempool/src/fee.rs packages/open-bitcoin-mempool/src/pool/candidate.rs` returns typed fee facts and group assessment
    - `rg -n "enum TrucPolicy|Reject|Accept|Enforce|truc_policy: TrucPolicy" packages/open-bitcoin-mempool/src/types.rs` returns exact three-state policy
    - `rg -n "truc_policy: TrucPolicy::Accept" packages/open-bitcoin-mempool/src/types.rs` returns the pinned default
    - `rg -n "EffectiveFeeGroup::try_new|ordered_wtxids\\(" packages/open-bitcoin-mempool/src/fee.rs packages/open-bitcoin-mempool/src/pool/package_admission.rs packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs` returns checked construction/read-only use and `! rg -n "EffectiveFeeGroup \\{" packages/open-bitcoin-mempool/src/fee.rs packages/open-bitcoin-mempool/src/pool/package_admission.rs` confirms no downstream literal bypass
    - `rg -n "incremental_relay_fee" packages/open-bitcoin-mempool/src/fee.rs` returns no occurrence inside `evaluate_package_fee_group`
    - `rg -n "package_policy_cases.rs" docs/parity/source-breadcrumbs.json` returns the new test path
    - `bun run scripts/command-timings.ts run --key phase132-package-fees -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases` exits 0 with nonzero fee tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-package-fees -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases</automated>
  </verify>
  <done>Package fee groups preserve static/member, rolling/aggregate, incremental/replacement separation and expose exact ordered non-empty membership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trim once and rewrite ordered results from final prospective membership</name>
  <files>packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/pool/lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/package/report.rs (`PriorMemberSuccess`, alias, present, and post-trim variants)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (D-13 pipeline through fee assessment)
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (final view, pressure trim, and lifecycle fact storage)
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs (`MempoolLifecycleDeltaBuilder` identity/final-membership invariants)
    - packages/open-bitcoin-mempool/src/pool/pressure.rs (`trim_prospective_to_capacity`)
    - packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs (Task 1 target to extend with final-truth cases)
    - packages/bitcoin-knots/src/validation.cpp (`ProcessNewPackage` trim and rewrite lines 2099–2145)
    - packages/bitcoin-knots/src/validation.h (witness alias and final result meanings)
  </read-first>
  <behavior>
    - Package evaluation has one objective stage trace: static floor → TRUC → aggregate rolling floor → general limits → limited replacement → ephemeral → scripts → final trim → report/lifecycle finalization
    - A failure at any static/TRUC/rolling/limit/replacement/ephemeral stage records zero script invocations and zero final trims
    - Script failure occurs after all pre-script stages, records no trim/apply, and discards that sub-delta
    - Package evaluation invokes final pressure trim exactly once after successful late scripts and before report/lifecycle finalization
    - A newly staged member removed by pressure becomes `PostTrimAbsent(PriorMemberSuccess::NewlyPresent)` and is not admitted in the delta
    - An exact existing member or witness-alias target removed by replacement/pressure is rewritten to typed post-trim absence
    - Witness alias final presence is queried by txid and reports the existing wtxid; requested alias wtxid never appears in admitted/removal facts
    - Final lifecycle delta contains deterministic actual admissions/removals/final membership and agrees with the final prospective view
    - Dry-run and submit reports remain identical when evaluated from the same base
  </behavior>
  <action>
    Finish D-13/D-15 ordering in `package_admission.rs` with named sealed stages in this exact sequence: ordinary static-floor checks; Plan 07 pre-replacement TRUC hook; aggregate rolling-floor assessment; general ancestor/descendant limits; Plan 06 limited-replacement hook; Plan 07 ephemeral hook; `run_late_script_checks`, which calls Plan 02 `check_candidate_scripts` for each prepared member; `trim_prospective_to_capacity` exactly once; then report/lifecycle finalization. No earlier helper, including `prepare_candidate`, fee assessment, TRUC, limits, replacement, or ephemeral policy, may call script validation.

    Add a test-only `PackagePolicyStage` trace or equivalent objective probe owned by this module. It must record the exact stage sequence and invocation counts without affecting production results. Tests must inject a failure independently at static, TRUC, rolling, limits, replacement, ephemeral, and scripts; every pre-script failure proves script count zero, script failure proves all earlier stages completed and trim/apply counts zero, and success proves exactly one script pass per prepared member followed by one trim.

    After trim, run `rewrite_final_membership(package, prospective, intermediate_results)`: query ordinary/exact requested members by wtxid; query witness aliases by txid and preserve `existing_wtxid`; convert every prior-success state absent from final prospective membership into `PostTrimAbsent` with its typed prior origin.

    Build `MempoolLifecycleDelta` only after rewriting. Record `admitted` only for newly staged identities still present; record replacement/pressure removals only for actual existing identities; record final membership for every affected real identity. Never record requested witness-alias wtxid, exact-existing identity as newly admitted, or failed/post-trim-absent candidate as admitted. Complete all builder composition before Plan 02 guarded apply.

    Add pressure-capacity fixtures forcing removal of new, exact-existing, and alias-target members; assert the objective stage/failure order above, one trim invocation, rolling bump once, report rewrite, delta membership, ordered fee-group membership, and dry-run/submit report equality.
  </action>
  <acceptance-criteria>
    - `rg -n "check_candidate_scripts|run_late_script_checks|trim_prospective_to_capacity|fn rewrite_final_membership|PostTrimAbsent" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns the late-script/final ordering seams
    - `rg -n "record_admitted|record_removal|record_final_membership" packages/open-bitcoin-mempool/src/pool/package_admission.rs packages/open-bitcoin-mempool/src/pool/lifecycle.rs` returns final delta assembly
    - `rg -n "static.*truc.*rolling.*limit.*replacement.*ephemeral.*script.*trim|pre_script.*no_script|script.*failure.*no_trim|newly.*post_trim|alias.*existing_wtxid|trim.*once|dry_run.*submit.*equal" packages/open-bitcoin-mempool/src/pool/tests/package_policy_cases.rs` returns objective ordering/failure and final-truth fixtures
    - `bun run scripts/command-timings.ts run --key phase132-final-rewrite -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases` exits 0
    - `bun run scripts/command-timings.ts run --key phase132-lifecycle-regression -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool lifecycle_delta_cases` exits 0
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-final-rewrite -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_policy_cases &amp;&amp; bun run scripts/command-timings.ts run --key phase132-lifecycle-regression -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool lifecycle_delta_cases</automated>
  </verify>
  <done>One final trim establishes authoritative membership, ordered reports are rewritten from it, and lifecycle facts contain only real final identities.</done>
</task>

</tasks>

<threat-model>
## Assets

- Static, rolling, and incremental fee-role separation.
- Effective fee-group arithmetic and identity membership.
- Final post-trim report/lifecycle truth.

## Trust Boundaries

| Boundary | Untrusted or derived state crossing it |
| --- | --- |
| Candidate fee facts → fee-group assessment | Fees, versions, vsize, and member identities |
| Policy-success overlay → final pressure trim | Adversarial fee/topology/resource state |
| Intermediate results → report/lifecycle finalization | Potentially stale success or alias states |

## Threats and Mitigations

| Threat ID | STRIDE | Threat | Disposition | Concrete mitigation |
| --- | --- | --- | --- | --- |
| T-132-21 | Elevation of Privilege/Policy bypass | Aggregate fee sponsors an ordinary member below static floor | mitigate | Task 1 checks every ordinary member before aggregate rolling |
| T-132-22 | Elevation of Privilege | `TrucPolicy::Accept` accidentally grants enforced static exception | mitigate | Task 1 exact three-way decision tests; only Enforce+v3 bypasses static |
| T-132-23 | Tampering/Denial of Service | Fee/vsize totals overflow or empty group passes | mitigate | Task 1 uses checked non-empty group constructors and exact boundary tests |
| T-132-24 | Spoofing/Tampering | Intermediate success remains accepted after pressure/replacement removal | mitigate | Task 2 rewrites every prior success from final prospective membership |
| T-132-25 | Spoofing | Witness alias or failed candidate appears in lifecycle admissions | mitigate | Task 2 final delta is built only from real final identities and tests existing wtxid behavior |
| T-132-26 | Denial of Service | Scripts execute before cheap bounded policy or multiple trims amplify graph work/rolling bumps | mitigate | Task 2 has an objective stage/failure trace, one late script call path, one trim call site, and invocation-count regressions |

## Security Verification

- Unequal-value fee tests distinguish all semantic fee roles.
- Stage/failure probes prove scripts are never invoked before static/TRUC/rolling/limits/replacement/ephemeral success.
- Final rewrite fixtures force new/existing/alias target removals.
- Lifecycle-delta tests assert no phantom requested-witness identity.
</threat-model>

<verification>
Run package policy, lifecycle delta, pressure, and fee-role suites through the timed wrapper; run breadcrumb checker; inspect that incremental fee appears only in replacement/pressure paths.
</verification>

<success-criteria>
- PACK-06 static and rolling package fee semantics match pinned decisions.
- Effective groups are non-empty, ordered, checked, and exclude existing/aliases.
- One final trim determines all reported and committed membership.
- No alias or failed/post-trim candidate contaminates lifecycle facts.
</success-criteria>

<output>
After completion, create `.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-05-SUMMARY.md`
</output>
