---
phase: 132-typed-package-vocabulary-and-staged-admission
plan: "03"
type: tdd
wave: 3
depends_on:
  - "02"
files_modified:
  - packages/open-bitcoin-mempool/src/resource.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/prospective.rs
  - packages/open-bitcoin-mempool/src/pool/topology.rs
  - packages/open-bitcoin-mempool/src/pool/pressure.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PACK-05
user_setup: []
must_haves:
  truths:
    - "The production preparation path represents only touched additions, removals, relation/stat updates, spent-outpoint edits, resource changes, rolling state, and lifecycle facts"
    - "Overlay lookups are overlay-first and then fall back to the immutable base unless the identity is removed"
    - "Adding/removing a member updates every affected parent/child aggregate and accounted-memory contribution with checked arithmetic"
    - "Phase 131 pressure selection and rolling bump operate once against the prospective view without materializing a whole-mempool clone"
    - "The prepared transition is an owned sparse `MempoolPatch` with revision tokens and lifecycle delta; normal preparation/apply never materializes a full `MempoolState`"
    - "A test-only materialization/recomputation oracle proves sparse overlay state equals canonical full recomputation across generated graphs"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/prospective.rs"
      provides: "Sparse prospective mempool view, composable checked patch, and overlay-aware lookups"
      contains: "ProspectiveMempool"
    - path: "packages/open-bitcoin-mempool/src/resource.rs"
      provides: "Checked ledger subtraction/replacement primitives"
      contains: "checked_remove_entry"
    - path: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      provides: "View-based pressure victim selection and patch application"
      contains: "select_eviction_package"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs"
      provides: "Generated graph, composition, trim, and no-full-clone oracles"
      contains: "prospective"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/prospective.rs"
      to: "packages/open-bitcoin-mempool/src/resource.rs"
      via: "subtract old touched entry then add updated touched entry"
      pattern: "checked_(remove|replace)_entry"
    - from: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      to: "ProspectiveMempool"
      via: "overlay-aware descendant package selection/removal"
      pattern: "Prospective"
    - from: "packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs"
      to: "recompute_state and recompute_resource_ledger"
      via: "test-only full-state oracle after every patch"
      pattern: "recompute_(state|resource_ledger)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 132-2026-07-25T18-13-00
generated_at: 2026-07-25T18:13:00.349Z
---

<objective>
Build the sparse prospective overlay, checked accounting/topology patch composition, and overlay-aware single final pressure trim that make package staging coherent and bounded.

Purpose: Fulfill D-08/D-09 without per-member whole-mempool clones while preserving Phase 131 resource and pressure invariants.
Output: Prospective view/patch module, checked ledger mutation primitives, factored pressure policy, and recomputation-oracle tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-CONTEXT.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-RESEARCH.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-02-SUMMARY.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-01-SUMMARY.md
@packages/open-bitcoin-mempool/src/resource.rs
@packages/open-bitcoin-mempool/src/pool.rs
@packages/open-bitcoin-mempool/src/pool/topology.rs
@packages/open-bitcoin-mempool/src/pool/pressure.rs
@packages/bitcoin-knots/src/txmempool.h

<interfaces>
Plan 02 sparse-transition contract:
```rust
struct MempoolPatch {
    base_revision: MempoolRevision,
    next_revision: MempoolRevision,
    entry_upserts: BTreeMap<Txid, MempoolEntry>,
    entry_removals: BTreeSet<Txid>,
    spent_updates: BTreeMap<OutPoint, Option<Txid>>,
    topology_updates: BTreeMap<Txid, TopologyUpdate>,
    resource_delta: MempoolResourceDelta,
    rolling_fee_state: RollingFeeState,
    delta: MempoolLifecycleDelta,
}
```

Required sparse view contract:
```rust
struct ProspectiveMempool<'base> {
    base: &'base Mempool,
    added_or_updated: HashMap<Txid, MempoolEntry>,
    removed: BTreeMap<MempoolMemberIdentity, MempoolRemovalFact>,
    spent_updates: HashMap<OutPoint, Option<Txid>>,
    topology_updates: BTreeMap<Txid, TopologyUpdate>,
    resource_delta: MempoolResourceDelta,
    prospective_resource_usage: MempoolResourceUsage,
    rolling_fee_state: RollingFeeState,
}

impl ProspectiveMempool<'_> {
    fn maybe_entry(&self, txid: &Txid) -> Option<&MempoolEntry>;
    fn maybe_spender(&self, outpoint: &OutPoint) -> Option<Txid>;
    fn stage_candidate(&mut self, candidate: PreparedCandidate) -> Result<SubDelta, MempoolError>;
    fn compose(&mut self, sub_delta: SubDelta) -> Result<(), MempoolError>;
    fn prepare_patch(self, delta: MempoolLifecycleDelta)
        -> Result<MempoolPatch, MempoolError>;
}
```
</interfaces>

<scope-safety>
The file span is one bounded ownership unit: `prospective.rs` is the sparse transition, `resource.rs` and `topology.rs` supply its checked touched-state primitives, `pressure.rs` is the one final view-based consumer, and the test registry/oracle verifies the same contract. Splitting these files into independently executable plans would leave a normal path that either cannot account for topology-driven resource changes or must temporarily materialize full state, violating D-08/D-09.
</scope-safety>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checked sparse overlay and touched-state accounting</name>
  <files>packages/open-bitcoin-mempool/src/resource.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/pool/topology.rs, packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs (new target; confirm absent before creation)
    - packages/open-bitcoin-mempool/src/pool/tests.rs (module registry to extend)
    - packages/open-bitcoin-mempool/src/resource.rs (typed ledger, accounted-memory formula, full recomputation oracle)
    - packages/open-bitcoin-mempool/src/pool.rs (`MempoolPatch`, test-only `recompute_state`, revision-bound apply from Plan 02)
    - packages/open-bitcoin-mempool/src/pool/topology.rs (ancestor/descendant/conflict traversal signatures)
    - packages/open-bitcoin-mempool/src/pool/candidate.rs (`PreparedCandidate` and fee/input facts)
    - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs (checked arithmetic and oracle style)
    - packages/bitcoin-knots/src/txmempool.h (`CTxMemPool::ChangeSet` touched-state model)
    - packages/bitcoin-knots/src/txmempool.cpp (ancestor/descendant update ordering)
    - docs/parity/source-breadcrumbs.json (`mempool-resource-accounting` and `mempool-lifecycle` groups)
  </read-first>
  <behavior>
    - Overlay entry/spender lookup observes staged additions/updates/removals before immutable base state
    - Staging an insertion/removal updates direct relations plus every affected ancestor/descendant aggregate
    - Ledger replacement subtracts the old touched entry and adds the updated entry plus spent-index changes without underflow or overflow
    - Composing two coherent sub-deltas detects duplicate add/remove, conflicting txid/wtxid pairs, double-spend edits, and integer underflow before changing the overlay
    - Deterministic generated graphs materialized only in tests match `recompute_state` and `recompute_resource_ledger`
  </behavior>
  <action>
    Add `checked_remove_entry`, `checked_replace_entry`, and `checked_remove_spent_outpoints` to `MempoolResourceLedger`; each returns `ResourceAccountingError::Underflow { component }` or existing overflow and never saturates/wraps. Update `MempoolError::InternalInvariant` mapping at the pool boundary.

    Create `pool/prospective.rs` with the exact sparse fields/interface above. Use overlay-first lookup, retain the immutable base by borrow, and own only touched entries/removals/spent edits/topology replacements, checked resource deltas/prospective usage, rolling facts, and lifecycle facts. `stage_candidate` must determine changed direct parents/children, recompute affected aggregate stats from the view, subtract each old touched entry's accounted value, add each new touched value, and validate spent-index changes. `SubDelta` composition must be checked and fail closed on identity, outpoint, relation, lifecycle, or arithmetic conflicts.

    `prepare_patch` must consume the overlay into one owned `MempoolPatch` containing the Plan 02 revision tokens, only touched facts, final rolling facts, and the checked lifecycle delta. It must not build `MempoolState`, clone the base entries/indexes, or call `recompute_state`; those operations are test-oracle-only.

    Add a `#[cfg(test)]` materializer that builds a full state solely for oracle comparison. Create deterministic Arrange/Act/Assert cases for insertion, descendant removal, replacement, multi-subdelta composition, and arithmetic/conflict failure. Register new source/test files with `txmempool.{h,cpp}`, `validation.cpp`, and package test breadcrumbs.
  </action>
  <acceptance-criteria>
    - `rg -n "checked_remove_entry|checked_replace_entry|checked_remove_spent_outpoints|Underflow" packages/open-bitcoin-mempool/src/resource.rs` returns every checked subtraction surface
    - `rg -n "struct ProspectiveMempool|fn maybe_entry|fn maybe_spender|fn stage_candidate|fn compose|fn prepare_patch" packages/open-bitcoin-mempool/src/pool/prospective.rs` returns the complete overlay contract
    - `rg -n "MempoolPatch|entry_upserts|entry_removals|spent_updates|topology_updates|resource_delta|rolling_fee_state|delta" packages/open-bitcoin-mempool/src/pool/prospective.rs` returns every sparse transition component
    - `rg -n "PreparedMempoolTransition|state: MempoolState" packages/open-bitcoin-mempool/src/pool/prospective.rs` returns 0
    - `rg -n "recompute_state|entries\\.clone\\(\\)" packages/open-bitcoin-mempool/src/pool/prospective.rs` returns only `#[cfg(test)]`-guarded oracle references and no production call
    - `rg -n "recompute_state|recompute_resource_ledger" packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs` returns both oracles
    - `rg -n "pool/prospective.rs|prospective_oracle_cases.rs" docs/parity/source-breadcrumbs.json` returns both new paths
    - `bun run scripts/command-timings.ts run --key phase132-prospective-oracle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prospective_oracle_cases` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-prospective-oracle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prospective_oracle_cases</automated>
  </verify>
  <done>Sparse staged mutations compose with checked topology/resource updates, and every tested patch matches independent full-state recomputation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run Phase 131 pressure selection and trim over the overlay exactly once</name>
  <files>packages/open-bitcoin-mempool/src/pool/prospective.rs, packages/open-bitcoin-mempool/src/pool/pressure.rs, packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/pool/prospective.rs (Task 1 lookup/removal/resource/rolling APIs)
    - packages/open-bitcoin-mempool/src/pool/pressure.rs (Phase 131 descendant-score order and rolling bump)
    - packages/open-bitcoin-mempool/src/pool/topology.rs (`collect_descendants`)
    - packages/open-bitcoin-mempool/src/fee/rolling.rs (`track_package_removed`)
    - packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs (victim ordering and rolling-bump fixtures)
    - packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs (Task 1 target to extend with pressure/bounded-work cases)
    - packages/bitcoin-knots/src/txmempool.cpp (`TrimToSize`, descendant package removal)
    - packages/bitcoin-knots/src/validation.cpp (`LimitMempoolSize` once after package processing)
  </read-first>
  <behavior>
    - Overlay trim selects the identical descendant-score victim and txid tie-break as live Phase 131 trim
    - Removing a pressure victim removes its complete descendant package and updates all touched state/resource facts
    - Rolling bump uses actual evicted aggregate fee/vsize plus incremental relay fee exactly once per evicted package
    - The max 25-member staging scenario invokes no whole-mempool clone/recompute and one final trim call
    - Failed trim preparation leaves base mempool and base rolling state unchanged
  </behavior>
  <action>
    Factor `pool/pressure.rs` into view-based pure `select_eviction_package(view) -> Option<(Txid, FeeRate)>` and overlay mutation `trim_prospective_to_capacity(&mut ProspectiveMempool, &PolicyConfig) -> Result<Vec<MempoolRemovalFact>, MempoolError>`. Preserve Phase 131 ordering: minimum descendant score, then txid; remove victim plus all descendants; bump prospective rolling state from actual evicted package feerate plus `IncrementalRelayFeeRate`. Do not change live state or use `recompute_state`.

    Retain the existing live `trim_to_size` behavior by routing it through the same selection rule or a thin compatible view. Instrument the test-only overlay with clone/recompute/trim counters and assert a 25-member staged case observes zero production full-state clones/recomputes and exactly one caller-triggered final trim. Add failure injection for arithmetic/composition failure and assert base entries, ledger, spent index oracle, rolling state, and revision are unchanged.
  </action>
  <acceptance-criteria>
    - `rg -n "fn select_eviction_package|fn trim_prospective_to_capacity" packages/open-bitcoin-mempool/src/pool/pressure.rs` returns both shared pressure seams
    - `rg -n "descendant_score|incremental_relay_fee_rate|track_package_removed" packages/open-bitcoin-mempool/src/pool/pressure.rs` returns preserved Phase 131 policy inputs
    - `rg -n "entries\\.clone|recompute_state" packages/open-bitcoin-mempool/src/pool/pressure.rs` returns 0
    - `rg -n "25|zero.*clone|one.*trim|failed.*unchanged" packages/open-bitcoin-mempool/src/pool/tests/prospective_oracle_cases.rs` returns bounded-work and rollback assertions
    - `bun run scripts/command-timings.ts run --key phase132-overlay-pressure -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prospective_oracle_cases` exits 0
    - `bun run scripts/command-timings.ts run --key phase132-pressure-regression -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool pressure_cases` exits 0
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase132-overlay-pressure -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prospective_oracle_cases &amp;&amp; bun run scripts/command-timings.ts run --key phase132-pressure-regression -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool pressure_cases</automated>
  </verify>
  <done>The prospective engine reuses exact Phase 131 eviction/rolling behavior, performs one final trim without full-state clones, and failure cannot leak rolling or membership changes.</done>
</task>

</tasks>

<threat-model>
## Assets

- Sparse overlay coherence across entries, relations, spent outpoints, resource ledger, rolling fee, and lifecycle facts.
- Accounted-capacity enforcement and Phase 131 eviction ordering.
- Bounded CPU/memory work for 25-member adversarial graphs.

## Trust Boundaries

| Boundary | Untrusted or derived state crossing it |
| --- | --- |
| Prepared candidate → sparse overlay | New entry, conflicts, outpoints, fees, and topology relations |
| Sub-delta → composed package patch | Potentially conflicting additions/removals/accounting facts |
| Prospective state → pressure selector | Adversarial graph and fee aggregates |

## Threats and Mitigations

| Threat ID | STRIDE | Threat | Disposition | Concrete mitigation |
| --- | --- | --- | --- | --- |
| T-132-11 | Tampering/Denial of Service | Integer/accounting subtraction underflows and admits an over-capacity state | mitigate | Task 1 adds checked remove/replace primitives and oracle equality |
| T-132-12 | Tampering | Partial sub-delta composition leaves relations/indexes inconsistent or full-state replacement overwrites untouched facts | mitigate | Task 1 validates conflicts/arithmetic before merging owned sparse patch facts, and normal apply receives no full `MempoolState` |
| T-132-13 | Denial of Service | Adversarial graph triggers per-member full clone/recompute | mitigate | Tasks 1–2 ban production recompute/clone and assert counters at the 25-member bound |
| T-132-14 | Tampering/Denial of Service | Trim removes an incomplete descendant package or bumps from wrong fee facts | mitigate | Task 2 reuses descendant-score/tie-break rules and removes the complete view-derived descendant set |
| T-132-15 | Information Disclosure/Effect leakage | Prospective failure changes live rolling or membership state | mitigate | Overlay borrows immutable base and owns rolling changes until guarded Plan 02 apply |

## Security Verification

- Generated-graph oracle compares all membership, relations, aggregates, spent index, ledger, and rolling facts.
- Limit-bound counters prove zero production clone/recompute per member and one final trim.
- Failure-injection snapshots prove base state is unchanged.
- Source scans prove full materialization and `recompute_state` occur only in test-oracle code.
</threat-model>

<verification>
Run prospective oracle tests, existing pressure/resource suites, the full mempool library suite, and the parity breadcrumb checker; review production source for any `recompute_state` or entry-clone use in the new overlay path.
</verification>

<success-criteria>
- Sparse patch composition includes revision/lifecycle facts, is checked, and is recomputation-faithful without normal-path full-state materialization.
- Accounting cannot wrap, saturate, or underflow.
- Pressure trim preserves Phase 131 selection/bump semantics over the prospective view.
- Production max-bound work has no per-member whole-mempool clone/recompute.
</success-criteria>

<output>
After completion, create `.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-03-SUMMARY.md`
</output>
