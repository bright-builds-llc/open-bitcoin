---
phase: 117-parity-traceability-uat-and-release-guardrails
plan: "02"
type: tdd
wave: 2
depends_on:
  - "117-01"
files_modified:
  - scripts/check-phase117-parity-uat-release-boundary.ts
  - scripts/check-phase117-parity-uat-release-boundary.test.ts
  - scripts/verify.sh
requirements:
  - BOUND-02
  - BOUND-04
autonomous: true
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 117-2026-07-10T05-06-19
generated_at: 2026-07-10T05:26:08.268Z
user_setup: []
must_haves:
  truths:
    - "A deterministic aggregate checker rejects broader v2.1 overclaims while allowing the bounded/default-off compact-relay claim."
    - "Checker fixtures prove exactly-once ownership, concrete anchors, UAT commands, verifier order, and default-gate boundaries."
    - "The Phase 117 checker pair is ordered after Phase 116 and before pure-core checks in both verifier-order surfaces."
  artifacts:
    - path: "scripts/check-phase117-parity-uat-release-boundary.ts"
      provides: "Pure deterministic Phase 117 repository validator plus thin CLI shell"
      contains: "v2-1-parity-uat-release-boundary"
    - path: "scripts/check-phase117-parity-uat-release-boundary.test.ts"
      provides: "Mutation fixtures for BOUND-01 through BOUND-05 guardrails"
      contains: "describe"
    - path: "scripts/verify.sh"
      provides: "Default checker ordering after Phase 116"
      contains: "check-phase117-parity-uat-release-boundary"
  key_links:
    - from: "scripts/check-phase117-parity-uat-release-boundary.ts"
      to: "docs/parity/index.json"
      via: "surface and exactly-once requirement validation"
      pattern: "REQUIRED_SURFACE"
    - from: "scripts/check-phase117-parity-uat-release-boundary.ts"
      to: "scripts/verify.sh"
      via: "visible and executable order checks"
      pattern: "VERIFY_COMMAND_ORDER|run_step"
---

# Phase 117 Plan 02: Deterministic No-Claim and Verifier-Boundary Checkers

<objective>
Add the aggregate deterministic guardrail that turns the Phase 117 parity corpus into enforceable release-boundary evidence.

Purpose: BOUND-02 and BOUND-04 require current-doc claim checking and proof that the default verifier stays local, deterministic, and free of public-network or production gates. The checker must recognize the newly valid bounded v2.1 compact-relay claim instead of copying the v2.0 global deferral rule.

Output: A pure/testable Bun checker, mutation fixtures, and exact verifier wiring after Phase 116.
</objective>

<execution_context>
@.planning/phases/117-parity-traceability-uat-and-release-guardrails/117-CONTEXT.md
@.planning/phases/117-parity-traceability-uat-and-release-guardrails/117-RESEARCH.md
@scripts/check-phase106-parity-uat-release-boundary.ts
@scripts/check-phase106-parity-uat-release-boundary.test.ts
@scripts/check-phase116-operator-block-relay-evidence.ts
@scripts/check-phase116-operator-block-relay-evidence.test.ts
@scripts/verify.sh
</execution_context>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Claim-bearing documentation | Weak qualifiers can hide public/default/production overclaims. | Paragraph-aware capability-plus-claim-strength checks and negative fixtures. |
| Default verifier | Public network, soak, service-manager, or deployment commands could become release gates. | Inspect executable `run_step` scope separately from documentation and the visible order block. |
| Fixture repository | Tests can pass against an unrealistic corpus. | Build a complete minimal passing fixture and mutate one concern per test. |

No shell command may read credentials, contact public peers, start services, or mutate production state.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Phase 117 checker around pure validation helpers and mutation fixtures</name>
  <files>
    scripts/check-phase117-parity-uat-release-boundary.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
  </files>
  <read_first>
    scripts/check-phase106-parity-uat-release-boundary.ts
    scripts/check-phase106-parity-uat-release-boundary.test.ts
    scripts/check-phase116-operator-block-relay-evidence.ts
    scripts/check-phase116-operator-block-relay-evidence.test.ts
    docs/parity/index.json
    docs/parity/source-breadcrumbs.json
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `scripts/check-phase117-parity-uat-release-boundary.ts` with pure validation helpers that accept loaded text/JSON and return stable failure strings, plus a thin repository filesystem/CLI shell. Keep the implementation within the repository's file/function limits by extracting named helpers when needed.

    Validate these exact surfaces: `v2-1-block-serving-activation-eligibility`, the existing Phase 111 full-block surface ID, `v2-1-bip152-wire-codec-message-semantics`, `v2-1-compact-relay-negotiation-announcement-policy`, `v2-1-compact-block-reconstruction`, `v2-1-missing-transaction-fallback-validation-handoff`, `v2-1-operator-block-relay-evidence`, and `v2-1-parity-uat-release-boundary`. Discover the exact existing Phase 110/111 IDs from `docs/parity/index.json` instead of renaming them.

    Require every one of these 34 IDs exactly once: `BSRV-01` through `BSRV-06`; `CMP-01` through `CMP-06`; `RCN-01` through `RCN-07`; `GOV-01` through `GOV-05`; `OBS-01` through `OBS-05`; `BOUND-01` through `BOUND-05`.

    Require concrete anchors for `protocol.h`, `blockencodings.h`, `blockencodings.cpp`, `net_processing.cpp`, `net_processing.h`, `net.h`, `net_permissions.h`, `validation.cpp`, `node/blockstorage.cpp`, `p2p_getdata.py`, `p2p_compactblocks.py`, and `p2p_permissions.py`. Require the v2.1 breadcrumb groups named in `117-CONTEXT.md` and reject missing group/file evidence.

    Use a curated corpus containing current README, parity release/production/deviation/support/P2P docs, runtime guide, status snapshot, and operator observability docs. Allow paragraphs that explicitly scope the capability as bounded plus default-off or opt-in, and paragraphs that say deferred, unsupported, future, outside default verification, or not a production claim. Reject positive support/default/readiness claims for package relay, BIP37 bloom serving, compact filters, public serving/relay defaults, archive-node or production-scale historical serving, public-network CI/release gates, production service/deployment, production full-node readiness, and production-funds wallet safety/use.

    Require exact repo-local commands for status human/JSON, RPC extension inspection, support bundle collection, the Phase 117 checker pair, and `bash scripts/verify.sh`. Check that default executable `run_step` commands do not invoke public-network/live-mainnet/soak/service-manager/launchd/systemd/production-deployment gates.

    Create mutation tests with one concern each: passing fixture; missing surface; duplicated requirement; missing Knots anchor; missing breadcrumb group; missing Cargo command; missing Bazel command; wrong visible order; wrong executable order; forbidden public-default claim; forbidden production claim; accidental public-network default gate; allowed bounded/default-off claim; allowed deferred/optional-UAT wording. Use explicit Arrange, Act, Assert sections.
  </action>
  <verify>
    <automated>bun test scripts/check-phase117-parity-uat-release-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file contains at least one passing fixture and the fourteen mutation concerns listed in the action.
    - `bun test scripts/check-phase117-parity-uat-release-boundary.test.ts` exits zero.
    - The checker source contains all five `BOUND-0` IDs and detects all 34 v2.1 requirement IDs exactly once.
    - A fixture paragraph with bounded/default-off compact relay passes while a public-by-default compact-relay paragraph fails.
    - The checker exports or otherwise isolates pure validation helpers from the filesystem/CLI shell.
  </acceptance_criteria>
  <done>The mutation suite passes and proves ownership, anchors, commands, ordering, allowed scoped claims, forbidden overclaims, and gate boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Phase 117 checker pair into both verifier-order surfaces</name>
  <files>
    scripts/verify.sh
    scripts/check-phase117-parity-uat-release-boundary.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
  </files>
  <read_first>
    scripts/verify.sh
    scripts/check-phase116-operator-block-relay-evidence.ts
    scripts/check-phase117-parity-uat-release-boundary.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
  </read_first>
  <action>
    Add these exact commands immediately after the Phase 116 pair in the `VERIFY_COMMAND_ORDER` block:

    `bun test scripts/check-phase117-parity-uat-release-boundary.test.ts`

    `bun run scripts/check-phase117-parity-uat-release-boundary.ts`

    Add these exact executable steps immediately after the Phase 116 `run_step` pair and before the first pure-core check:

    `run_step "test Phase 117 parity UAT release boundary checker" bun test scripts/check-phase117-parity-uat-release-boundary.test.ts`

    `run_step "check Phase 117 parity UAT release boundary" bun run scripts/check-phase117-parity-uat-release-boundary.ts`

    Update the Phase 117 checker's verifier-order assertions so they fail when either command pair is missing, reversed, before Phase 116, or after pure-core checks. Do not add any public-network or operator-service command to the verifier.
  </action>
  <verify>
    <automated>bun test scripts/check-phase117-parity-uat-release-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` contains both exact Phase 117 commands in `VERIFY_COMMAND_ORDER` after Phase 116.
    - `scripts/verify.sh` contains both exact Phase 117 `run_step` lines after Phase 116 and before pure-core checks.
    - The checker test fails under wrong-order mutations and passes under the intended order.
    - No new verifier line contains `live-mainnet`, `public-network`, `soak`, `launchctl`, `systemctl`, or production deployment commands.
  </acceptance_criteria>
  <done>Both Phase 117 commands are wired after Phase 116 in visible and executable verifier order, with no new external gate.</done>
</task>

</tasks>

<verification>
```bash
bun test scripts/check-phase117-parity-uat-release-boundary.test.ts
rg -n "check-phase117-parity-uat-release-boundary" scripts/verify.sh
git diff --check
```

The repository-mode Phase 117 checker may remain red until Plan 03 supplies its final documentation corpus. Fixture tests and verifier wiring must be green in this plan.
</verification>

<success_criteria>
- Mutation fixtures cover ownership, anchors, commands, claims, gates, and ordering.
- The bounded/default-off v2.1 compact-relay claim is allowed; broader public/archive/production claims are rejected.
- The Phase 117 pair is wired after Phase 116 in both verifier-order surfaces.
- Default verification remains deterministic and local.
</success_criteria>

<output>
After completion, create `117-02-SUMMARY.md` with checker architecture, fixture cases, verifier-order evidence, focused test output, and the expected temporary repository-mode dependency on Plan 03 docs.
</output>
