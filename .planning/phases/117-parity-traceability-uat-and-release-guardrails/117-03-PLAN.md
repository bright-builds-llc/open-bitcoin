---
phase: 117-parity-traceability-uat-and-release-guardrails
plan: "03"
type: execute
wave: 3
depends_on:
  - "117-02"
files_modified:
  - README.md
  - docs/parity/README.md
  - docs/parity/release-readiness.md
  - docs/parity/production-claim-boundary.md
  - docs/parity/deviations-and-unknowns.md
  - docs/parity/support-matrix.md
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
requirements:
  - BOUND-03
  - BOUND-05
autonomous: true
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 117-2026-07-10T05-06-19
generated_at: 2026-07-10T05:26:08.268Z
user_setup: []
must_haves:
  truths:
    - "Contributor, parity, release, and operator docs describe one bounded/default-off v2.1 claim and the same deferred surfaces."
    - "docs/parity/release-readiness.md is the release-facing v2.1 handoff; no competing changelog is created."
    - "Operator guidance includes exact Cargo and Bazel commands and keeps public-network review optional."
  artifacts:
    - path: "README.md"
      provides: "Concise current v2.1 claim and parity/release pointers"
      contains: "default-off"
    - path: "docs/parity/release-readiness.md"
      provides: "Canonical v2.1 release-review handoff"
      contains: "v2.1 Block Serving and Compact Block Relay Boundary"
    - path: "docs/operator/runtime-guide.md"
      provides: "Repo-local Cargo/Bazel UAT guidance"
      contains: "Phase 117"
  key_links:
    - from: "README.md"
      to: "docs/parity/release-readiness.md"
      via: "v2.1 release evidence pointer"
      pattern: "release-readiness.md"
    - from: "docs/operator/runtime-guide.md"
      to: "scripts/check-phase117-parity-uat-release-boundary.ts"
      via: "required command corpus"
      pattern: "check-phase117-parity-uat-release-boundary"
---

# Phase 117 Plan 03: README, Operator Docs, Runtime Docs, and Release Notes

<objective>
Replace stale all-deferred block-relay wording with a consistent, evidence-focused v2.1 release handoff while preserving every broader no-claim boundary.

Purpose: BOUND-03 and BOUND-05 require contributor and operator documentation to describe what v2.1 actually shipped, how to inspect it locally, and what remains explicitly unsupported. The completed Phase 117 checker becomes the deterministic guard against future drift.

Output: Updated README, parity/release/support docs, runtime/UAT guidance, and status/observability narratives that pass the repository-mode Phase 117 checker.
</objective>

<execution_context>
@.planning/phases/117-parity-traceability-uat-and-release-guardrails/117-CONTEXT.md
@.planning/phases/117-parity-traceability-uat-and-release-guardrails/117-RESEARCH.md
@scripts/check-phase117-parity-uat-release-boundary.ts
@docs/operator/runtime-guide.md
@docs/parity/release-readiness.md
</execution_context>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Required control |
| --- | --- | --- |
| Contributor/release copy | A scoped capability can become a marketing or production claim. | Use explicit bounded/default-off/opt-in wording and enumerate deferred surfaces next to the positive claim. |
| Operator commands | A documented public-network workflow can be mistaken for a default gate. | Label public-network review optional and keep required evidence limited to local deterministic commands. |
| Support/status docs | Documentation can promise raw peer or transaction evidence that the redacted contract does not expose. | Describe aggregate fixed counters and redaction boundaries only. |

No documentation may instruct production-funds use, destructive migration, public-default serving, or unbounded historical serving.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Publish one bounded v2.1 contributor and release narrative</name>
  <files>
    README.md
    docs/parity/README.md
    docs/parity/release-readiness.md
    docs/parity/production-claim-boundary.md
    docs/parity/deviations-and-unknowns.md
    docs/parity/support-matrix.md
  </files>
  <read_first>
    README.md
    docs/parity/README.md
    docs/parity/release-readiness.md
    docs/parity/production-claim-boundary.md
    docs/parity/deviations-and-unknowns.md
    docs/parity/support-matrix.md
    docs/parity/catalog/p2p.md
    .planning/PROJECT.md
    .planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-SUMMARY.md
  </read_first>
  <action>
    Update current-state and release-boundary sections to use this semantic contract: Open Bitcoin v2.1 provides bounded, explicit, default-off block serving and compact-block relay with deterministic local evidence and optional public-network operator review.

    In each claim-bearing document, state that these remain deferred or unsupported: package relay; BIP37 bloom-filter serving; compact-filter serving; public serving/relay defaults; archive-node or production-scale historical serving; public-network CI/release gates; production service/deployment; production full-node readiness; production-funds wallet safety/use; packaging; GUI/hosted dashboards; migration apply mode; destructive repair; automatic support upload.

    Add a `v2.1 Block Serving and Compact Block Relay Boundary` release handoff section to `docs/parity/release-readiness.md` that links the Phase 110 through 117 parity surfaces, Phase 116 operator evidence, Phase 117 checker/test, `scripts/verify.sh`, and the Phase 117 UAT package. Keep README concise and link to `docs/parity/release-readiness.md` instead of creating a changelog. Update production boundary, deviations, and support matrix rows so block serving/compact relay are no longer described as wholly absent, while public-default/archive/production forms remain deferred.
  </action>
  <verify>
    <automated>bun run scripts/check-phase117-parity-uat-release-boundary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `v2.1`, `default-off`, `block serving`, `compact`, and a link to `docs/parity/release-readiness.md`.
    - `docs/parity/release-readiness.md` contains a v2.1 section linking Phase 117 checker/test, `scripts/verify.sh`, and `117-UAT.md`.
    - `docs/parity/production-claim-boundary.md`, `deviations-and-unknowns.md`, and `support-matrix.md` distinguish bounded v2.1 support from deferred public-default/archive/production scope.
    - No new release-notes or changelog file is created.
    - `bun run scripts/check-phase117-parity-uat-release-boundary.ts` exits zero after both tasks in this plan.
  </acceptance_criteria>
  <done>Contributor and release docs share one scoped v2.1 narrative and every broader deferred surface remains explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add exact operator inspection commands and optional UAT boundaries</name>
  <files>
    docs/operator/runtime-guide.md
    docs/architecture/status-snapshot.md
    docs/architecture/operator-observability.md
  </files>
  <read_first>
    docs/operator/runtime-guide.md
    docs/architecture/status-snapshot.md
    docs/architecture/operator-observability.md
    .planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-CONTEXT.md
    .planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-SUMMARY.md
    scripts/check-phase117-parity-uat-release-boundary.ts
  </read_first>
  <action>
    Add a Phase 117 review section to `docs/operator/runtime-guide.md` that reuses the shared Phase 116 `block_relay` status contract and includes these exact commands:

    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format human`

    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format json`

    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format human`

    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format json`

    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -regtest openbitcoinnetworkstatus`

    `bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -regtest openbitcoinnetworkstatus`

    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- support bundle --output-dir=/tmp/open-bitcoin-block-relay-support`

    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- support bundle --output-dir=/tmp/open-bitcoin-block-relay-support`

    `bun test scripts/check-phase117-parity-uat-release-boundary.test.ts`

    `bun run scripts/check-phase117-parity-uat-release-boundary.ts`

    `bash scripts/verify.sh`

    If a daemon command is required for an optional local/regtest workflow, use `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind -- ...` and `bazel run //packages/open-bitcoin-rpc:open_bitcoind -- ...` with explicit non-production arguments discovered from existing runtime-guide examples. Do not invent a public-network invocation.

    State that public-network block-serving or compact-relay review is optional, can be recorded as not run, and is never required by pre-commit, default CI, release-boundary verification, `scripts/verify.sh`, wall-clock soak, service-manager, or production-deployment workflows. Update status/observability docs to describe aggregate-only `block_relay` evidence and redaction of raw payloads, hashes, endpoints, permissions, credentials, and dynamic labels.
  </action>
  <verify>
    <automated>bun run scripts/check-phase117-parity-uat-release-boundary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator/runtime-guide.md` contains every exact command listed in the action.
    - The runtime guide states public-network review is optional and not part of pre-commit, default CI, release-boundary verification, or `bash scripts/verify.sh`.
    - `docs/architecture/status-snapshot.md` and `operator-observability.md` describe aggregate `block_relay` evidence and its redaction boundary without promising peer/block/transaction detail.
    - The Phase 117 checker and its test both exit zero.
  </acceptance_criteria>
  <done>Operator docs contain the exact repo-local command corpus, aggregate/redacted evidence semantics, and optional public-network boundary.</done>
</task>

</tasks>

<verification>
```bash
bun test scripts/check-phase117-parity-uat-release-boundary.test.ts
bun run scripts/check-phase117-parity-uat-release-boundary.ts
git diff --check
```
</verification>

<success_criteria>
- README, release, parity, production-boundary, support, runtime, status, and observability docs agree on the bounded/default-off v2.1 claim.
- Public-default, archive, filter, package-relay, public-network-gate, production, and production-funds scope remains explicitly deferred.
- Exact repo-local Cargo and Bazel commands are copy-pasteable.
- The repository-mode Phase 117 checker passes.
</success_criteria>

<output>
After completion, create `117-03-SUMMARY.md` with updated document sections, exact command corpus, claim-boundary wording, checker outputs, and deferred surfaces.
</output>
