# Phase 07.4: Let-else opportunity sweep - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 07.4-let-else-opportunity-sweep
**Mode:** Yolo
**Areas discussed:** scope and audit rubric, shortlist triage, verification and stop conditions

---

## Scope and Audit Rubric

| Option | Description | Selected |
|--------|-------------|----------|
| Repo-wide let-else sweep | Revisit all first-party production and test code for any remaining `let...else` opportunities | |
| Narrow production-only shortlist | Audit only the remaining production candidates and adopt changes only where `let...else` is a clear readability improvement | ✓ |
| Deferred audit only | Record possible candidates but make no code changes in this phase | |

**User's choice:** Yolo-selected recommended option: narrow production-only shortlist.  
**Notes:** This keeps the phase behavior-neutral and prevents the already-complete `07.3` maintainability sweep from widening into a generic style pass.

---

## Shortlist Triage

| Option | Description | Selected |
|--------|-------------|----------|
| Reopen all prior non-targets | Bring `header_store.rs`, `descriptor.rs`, and `address.rs` back into active scope | |
| Adopt only the clear wins | Touch `open-bitcoin-node/src/network.rs`, `wallet/build.rs`, and `classify.rs`, and record the rest as deferred | ✓ |
| Legacy follow-on expansion | Reopen deeper consensus/script internals if more `let...else` opportunities appear | |

**User's choice:** Yolo-selected recommended option: adopt only the clear wins.  
**Notes:** `header_store.rs` remains deferred because its current branches are already clearer without forced `let...else`; `descriptor.rs`, `address.rs`, and all test-only opportunities stay out of scope.

---

## Verification and Stop Conditions

| Option | Description | Selected |
|--------|-------------|----------|
| Crate checks only | Use targeted crate tests and stop once touched crates pass | |
| Full repo-native gate | Use targeted checks while iterating, then finish on `bash scripts/verify.sh` | ✓ |
| Broaden with new regression suites | Add new repo-owned tests to justify the sweep | |

**User's choice:** Yolo-selected recommended option: full repo-native gate.  
**Notes:** The phase keeps narrow crate checks for iteration, but completion still requires the repo-native verification contract. If a candidate requires extra indirection or semantic risk just to justify `let...else`, it is deferred rather than forced.

## the agent's Discretion

- Exact helper naming when a tiny helper makes the `let...else` adoption clearer.
- Leaving a shortlisted file untouched if the final implementation pass does not produce a clearly better production-only `let...else` conversion.

## Deferred Ideas

- `packages/open-bitcoin-network/src/header_store.rs` — audited and deferred
- `packages/open-bitcoin-wallet/src/descriptor.rs` — parser-style `if let` branches remain clearer as-is
- `packages/open-bitcoin-wallet/src/address.rs` — remains better paired with the deferred panic sweep
- Test-only `let...else` opportunities
