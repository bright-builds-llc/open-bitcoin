---
phase: 07.4-let-else-opportunity-sweep
plan: 01
subsystem: consensus-node-wallet
tags: [rust, maintainability, let-else, readability]
requires:
  - phase: 07.3
    provides: the earlier hotspot sweep that left only a narrow production shortlist for a final let-else pass
provides:
  - local `let...else` guard adoption in `open-bitcoin-node`, `open-bitcoin-wallet`, and `open-bitcoin-consensus`
  - explicit defer decisions for `header_store.rs`, `descriptor.rs`, and `address.rs`
affects: [phase-08-interface-parity, maintainability]
tech-stack:
  added: []
  patterns:
    - `let...else` when the failure path is only `continue` or `return Ok(None)`
    - explicit audit-and-defer decisions for low-yield or non-production candidates
key-files:
  created: []
  modified:
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-wallet/src/wallet/build.rs
    - packages/open-bitcoin-consensus/src/classify.rs
key-decisions:
  - `header_store.rs` was reviewed and deferred because its remaining branches are already clearer without a forced `let...else` rewrite.
  - `descriptor.rs`, `address.rs`, and all test-only opportunities remained out of scope.
  - The wallet helper was reshaped to satisfy the repo coverage gate without broadening the phase.
patterns-established:
  - "Use `let...else` when a missing value only leads to `continue` or an immediate `Ok(None)` return."
  - "If a candidate needs extra indirection just to justify `let...else`, defer it instead of forcing the rewrite."
requirements-completed: []
generated_by: codex
generated_at: 2026-04-19T09:55:55.597Z
duration: short
completed: 2026-04-19
---

# Phase 07.4 Plan 01: Let-Else Opportunity Sweep Summary

**Applied the final production-only `let...else` wins in `classify.rs`, `open-bitcoin-node/src/network.rs`, and `wallet/build.rs`, while explicitly deferring the lower-yield shortlist entries.**

## Accomplishments

- Replaced the `maybe_push_len` loop branch in `packages/open-bitcoin-consensus/src/classify.rs` with a `let...else { continue; }` guard.
- Adopted `let...else` in `packages/open-bitcoin-node/src/network.rs` for `collect_outbound` and the inventory-serving miss paths where the failure branch only continues with a deferred item.
- Extracted a small `build_change_output` helper in `packages/open-bitcoin-wallet/src/wallet/build.rs` that uses `let...else` to keep the optional change path out of the main transaction-building flow.
- Recorded the audit outcome that `packages/open-bitcoin-network/src/header_store.rs` stays deferred, and that `descriptor.rs`, `address.rs`, and test-only files remain out of scope.

## Verification

- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-features`
- `bash scripts/verify.sh`

All verification passed after a small helper-signature adjustment to avoid coverage misses on wrapped call-site lines in `wallet/build.rs`.

## Self-Check: PASSED

- Verified the adopted edits stayed inside the approved production-only shortlist.
- Verified deferred candidates were recorded in `07.4-RESEARCH.md`.
- Verified the repo-native `bash scripts/verify.sh` gate passed on the final code.
