---
phase: 07.4-let-else-opportunity-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-wallet/src/wallet/build.rs
  - packages/open-bitcoin-consensus/src/classify.rs
autonomous: true
requirements: []
user_setup: []
generated_by: codex
lifecycle_mode: manual
phase_lifecycle_id: 07.4-2026-04-19T09-51-04
generated_at: 2026-04-19T09:51:04.217Z
must_haves:
  truths:
    - The adopted `let...else` conversions are limited to clear production-only readability wins in the shortlisted files.
    - `header_store.rs`, `descriptor.rs`, `address.rs`, and test-only opportunities remain explicitly deferred.
    - No public signatures or externally observable behaviors change.
  artifacts:
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-wallet/src/wallet/build.rs
    - packages/open-bitcoin-consensus/src/classify.rs
  key_links:
    - `collect_outbound` and `serve_inventory` keep the same output behavior while using clearer guard exits.
    - `build_transaction` keeps the same change-output semantics while delegating the optional branch to a helper that uses `let...else`.
    - `is_push_only` keeps the same push parsing behavior while using a `let...else` loop guard.
---

<objective>
Audit the shortlisted production files, adopt only the high-confidence
`let...else` wins, record deferred candidates, and close the sweep on the
repo-native verification gate.

Purpose: finish the pre-Phase-8 let-else review without widening into a generic
style sweep.
Output: a small, behavior-neutral production refactor set plus recorded defer
decisions for the remaining shortlist.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.4-let-else-opportunity-sweep/07.4-CONTEXT.md
@.planning/phases/07.4-let-else-opportunity-sweep/07.4-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the adopted production-only let-else conversions</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-wallet/src/wallet/build.rs, packages/open-bitcoin-consensus/src/classify.rs</files>
  <read_first>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-wallet/src/wallet/build.rs, packages/open-bitcoin-consensus/src/classify.rs, .planning/phases/07.4-let-else-opportunity-sweep/07.4-CONTEXT.md, .planning/phases/07.4-let-else-opportunity-sweep/07.4-RESEARCH.md</read_first>
  <action>In `packages/open-bitcoin-node/src/network.rs`, replace the `if let PeerAction::Send(message)` loop filter in `collect_outbound` with a `let ... else { continue; }` guard, and rewrite the `Block`, `Transaction`, and `WitnessTransaction` branches in `serve_inventory` so missing entries push to `missing` through `let ... else { missing.push(request); continue; }` instead of nested `if let` or `else` blocks. In `packages/open-bitcoin-wallet/src/wallet/build.rs`, extract the optional change-output branch into one tiny private helper that begins with `let Some(change_script) = maybe_change_script else { return Ok(None); };`, preserving the current fee, dust, and change-output semantics. In `packages/open-bitcoin-consensus/src/classify.rs`, replace the `if let Some(push_len) = maybe_push_len` branch in `is_push_only` with a `let Some(push_len) = maybe_push_len else { continue; };` loop guard, keeping the current push-length validation intact. Do not widen the phase into `header_store.rs`, `descriptor.rs`, `address.rs`, or test-only cleanup.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-features`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/network.rs` uses `let...else` in `collect_outbound` and the three adopted `serve_inventory` branches
    - `packages/open-bitcoin-wallet/src/wallet/build.rs` contains a private helper that starts with `let Some(change_script) = maybe_change_script else`
    - `packages/open-bitcoin-consensus/src/classify.rs` uses `let...else { continue; }` for `maybe_push_len` in `is_push_only`
    - No edits land in `packages/open-bitcoin-network/src/header_store.rs`, `packages/open-bitcoin-wallet/src/descriptor.rs`, `packages/open-bitcoin-wallet/src/address.rs`, or test-only files
  </acceptance_criteria>
  <done>The adopted `let...else` conversions are in place and remain local, readable, and behavior-neutral.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the sweep and record deferred shortlist decisions</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-wallet/src/wallet/build.rs, packages/open-bitcoin-consensus/src/classify.rs, .planning/phases/07.4-let-else-opportunity-sweep/07.4-RESEARCH.md</files>
  <read_first>scripts/verify.sh, .planning/phases/07.4-let-else-opportunity-sweep/07.4-RESEARCH.md</read_first>
  <action>Confirm the research artifact still records the keep or defer decision for every shortlisted file, then run the narrow crate-scoped checks for the touched crates and finish with `bash scripts/verify.sh`. If verification exposes a blocker caused by the `let...else` refactor, apply the smallest fix in the already adopted production files only. Do not widen the sweep to justify additional edits.</action>
  <verify>`cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-features && bash scripts/verify.sh`</verify>
  <acceptance_criteria>
    - `.planning/phases/07.4-let-else-opportunity-sweep/07.4-RESEARCH.md` contains an adopt or defer decision for each shortlisted production file
    - `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-targets --all-features -- -D warnings` passes
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-features` passes
    - `bash scripts/verify.sh` passes
  </acceptance_criteria>
  <done>The phase closes with verified let-else adoptions and explicitly recorded defer decisions for the remaining shortlist.</done>
</task>

</tasks>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-node -p open-bitcoin-wallet --all-features`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The phase adopts only high-confidence production-only `let...else` conversions.
- Deferred shortlist decisions are recorded explicitly.
- No public surfaces or externally observable behavior change.
- `bash scripts/verify.sh` passes.
</success_criteria>
