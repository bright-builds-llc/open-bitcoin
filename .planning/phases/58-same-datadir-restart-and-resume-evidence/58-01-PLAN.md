---
phase: 58-same-datadir-restart-and-resume-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements:
  - RESUME-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 58-2026-06-05T12-58-05
generated_at: 2026-06-05T13:15:50Z
must_haves:
  truths:
    - "DurableSyncRuntime can reopen the same Fjall datadir after header-only progress and report the persisted header state."
    - "DurableSyncRuntime can reopen after downloaded and connected block progress and preserve downloaded and connected height/hash evidence."
    - "Already connected blocks are not requested or connected again after same-datadir reopen."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Deterministic same-datadir resume tests using real Fjall reopen and ScriptedTransport"
      contains: "same_datadir"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "DurableSyncRuntime::open"
      via: "FjallNodeStore::open on the same temp path"
      pattern: "FjallNodeStore::open\\(&path\\).*DurableSyncRuntime::open"
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "getdata duplicate guard"
      via: "ScriptedTransport::sent_messages and getdata_block_hashes"
      pattern: "getdata_block_hashes\\(&transport\\.sent_messages\\(\\)\\)"
---

<objective>
Prove the deterministic RESUME-01 regression guard: same-datadir reopen preserves durable headers, block bodies, active chainstate, downloaded height/hash, connected height/hash, and does not duplicate block requests or connects.

Purpose: Phase 58 needs durable resume evidence before live-smoke restart reporting can claim same-datadir resume.
Output: Focused Rust tests in `packages/open-bitcoin-node/src/sync/tests.rs`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
@.planning/phases/57-block-download-and-connect-progress/57-01-SUMMARY.md
@.planning/phases/57-block-download-and-connect-progress/57-02-SUMMARY.md
@.planning/phases/57-block-download-and-connect-progress/57-03-SUMMARY.md
@.planning/phases/56-header-ibd-convergence/56-01-SUMMARY.md
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/block_reconcile.rs
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-node/src/status.rs
@packages/bitcoin-knots/test/functional/feature_init.py
@packages/bitcoin-knots/src/node/blockstorage.cpp

<interfaces>
Existing test helpers in `packages/open-bitcoin-node/src/sync/tests.rs`:

```rust
fn getdata_block_hashes(messages: &[WireNetworkMessage]) -> Vec<BlockHash>;
fn save_best_chain_with_active_blocks(
    path: &Path,
    best_chain: &[(&Block, u32)],
    active_chain: &[(&Block, u32)],
);
```

Existing reopen pattern:

```rust
let store = FjallNodeStore::open(&path).expect("reopen store");
let mut runtime = DurableSyncRuntime::open(store, sync_config()).expect("runtime");
let mut transport = ScriptedTransport::new(vec![version_verack_script(height)]);
let summary = runtime.sync_once(&mut transport, now).expect("sync after restart");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove header and block status survives same-datadir reopen</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - .planning/phases/56-header-ibd-convergence/56-01-SUMMARY.md
    - .planning/phases/57-block-download-and-connect-progress/57-03-SUMMARY.md
    - packages/open-bitcoin-node/src/sync.rs
    - packages/open-bitcoin-node/src/sync/tests.rs
    - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Same-datadir header-only resume: save accepted headers, drop the runtime/store scope, reopen the same path, and assert `best_header_height` and durable sync status still report the header height.
    - Same-datadir block resume: save headers plus block bodies and active chainstate, reopen the same path, and assert `downloaded_block_height`, `connected_block_height`, `maybe_downloaded_block_hash`, and `maybe_connected_block_hash` match the expected block hash.
  </behavior>
  <action>
    Add focused tests that make D-02 and D-13 explicit. Reuse existing helpers instead of direct Fjall key inspection. If existing tests already cover part of the behavior, rename or extend them so the Phase 58 evidence is obvious and grep-visible with `same_datadir`. Keep tests in the existing file; do not create a new Rust test file unless the file becomes unmanageable. If a new Rust file is created, update `docs/parity/source-breadcrumbs.json` before verification.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node same_datadir --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node restart --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "same_datadir.*header|same_datadir.*downloaded|maybe_downloaded_block_hash|maybe_connected_block_hash" packages/open-bitcoin-node/src/sync/tests.rs` finds the new or tightened assertions.
    - The tests use `FjallNodeStore::open(&path)` before and after dropping the first scope.
    - The tests include explicit `// Arrange`, `// Act`, and `// Assert` sections for each non-trivial case.
    - No public-network command appears in the task verification, per D-16.
  </acceptance_criteria>
  <done>Header-only and block-status same-datadir reopen behavior is proven with deterministic Rust tests and durable height/hash assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reopen avoids duplicate getdata and duplicate connects</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - .planning/phases/57-block-download-and-connect-progress/57-01-SUMMARY.md
    - .planning/phases/57-block-download-and-connect-progress/57-02-SUMMARY.md
    - packages/open-bitcoin-node/src/sync/block_reconcile.rs
    - packages/open-bitcoin-node/src/sync/tests.rs
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Already connected block hashes are absent from post-reopen `getdata` requests.
    - Already connected blocks are not counted as new `blocks_received` after reopen.
    - Best-chain reconciliation after reopen still connects locally stored better-branch blocks when they were not already connected.
  </behavior>
  <action>
    Tighten the existing restart tests around `restart_reconnects_persisted_blocks_before_re_requesting_them` and `restart_reorgs_to_best_available_branch_when_blocks_are_already_local`, or add new `same_datadir_*` tests if tightening would blur existing intent. Assert through `ScriptedTransport::sent_messages()`, `getdata_block_hashes`, `summary.blocks_received`, `summary.best_block_height`, and `runtime.snapshot_summary()` rather than log text. This implements D-02 and D-13 without broadening into live public-network evidence.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node same_datadir --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node restart --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getdata_block_hashes\\(&transport\\.sent_messages\\(\\)\\)|same_datadir.*duplicate|blocks_received, 0|best_available_branch" packages/open-bitcoin-node/src/sync/tests.rs` finds duplicate-request and reconciliation assertions.
    - A post-reopen test asserts that the connected block hash is not in `getdata_block_hashes(&transport.sent_messages())`.
    - A post-reopen test asserts the active chain height/hash stays stable when the block was already connected.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node restart --all-features` passes without network access.
  </acceptance_criteria>
  <done>RESUME-01 has deterministic no-duplicate request/connect coverage over real same-datadir reopen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripted peer transport -> sync runtime | Test peer messages model untrusted public peer data entering the sync shell. |
| Fjall store -> reopened runtime | Durable data is read after process interruption and must not be trusted without normal runtime reconciliation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-01 | Tampering | `DurableSyncRuntime::open` resume path | mitigate | Tests must assert invalid or duplicate peer block data does not advance active chainstate and already connected hashes are not re-requested. |
| T-58-01-02 | Repudiation | restart evidence tests | mitigate | Test names and assertions must explicitly include same-datadir reopen and no-duplicate behavior so RESUME-01 evidence is auditable. |
| T-58-01-03 | Denial of service | block request reconciliation | mitigate | Duplicate `getdata` for connected blocks must be absent after reopen, preserving Phase 57 in-flight bound behavior. |
| T-58-01-04 | Information disclosure | deterministic tests | accept | Tests use temp datadirs and synthetic blocks only; no operator datadir or public network data is read. |
</threat_model>

<verification>
Targeted plan verification:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node same_datadir --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node restart --all-features
```

Full phase verification is deferred to the final Phase 58 plan after all script/docs work lands, to avoid parallel `docs/metrics/lines-of-code.md` churn.
</verification>

<success_criteria>
RESUME-01 has deterministic, public-network-free tests proving the same datadir can be reopened with durable header, block-body, active-chain, downloaded-height/hash, connected-height/hash, and no duplicate block request/connect evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/58-same-datadir-restart-and-resume-evidence/58-01-SUMMARY.md`.
</output>
