---
phase: 58-same-datadir-restart-and-resume-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
autonomous: true
requirements:
  - RESUME-01
  - RESUME-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 58-2026-06-05T12-58-05
generated_at: 2026-06-05T13:15:50Z
must_haves:
  truths:
    - "Operator can opt into a two-session live-smoke restart flow with `--restart-after-progress`."
    - "The live-smoke report contains compact `result.restartResumeEvidence` that proves the restart boundary and same-datadir resume."
    - "Fresh post-relaunch `openbitcoinsyncstatus` snapshots are collected without requiring fresh public-network progress after restart."
  artifacts:
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "Restart option, two-session daemon orchestration, restart evidence schema, and Markdown rendering"
      contains: "restartResumeEvidence"
    - path: "scripts/test-run-live-mainnet-smoke.sh"
      provides: "Deterministic mocked two-session restart fixture"
      contains: "--restart-after-progress"
  key_links:
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "openbitcoinsyncstatus"
      via: "fresh status command for both daemon sessions"
      pattern: "openbitcoinsyncstatus"
    - from: "scripts/test-run-live-mainnet-smoke.sh"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "mock daemon/status binaries and grep assertions"
      pattern: "--restart-after-progress"
---

<objective>
Add the opt-in operator-facing live-smoke restart flow and compact schema v2 restart evidence.

Purpose: Phase 58 needs a public UAT path that uses the same selected datadir, intentionally terminates the first daemon, relaunches the daemon, and captures fresh same-datadir resume evidence.
Output: `scripts/run-live-mainnet-smoke.ts` and `scripts/test-run-live-mainnet-smoke.sh` support deterministic restart-report fixtures and opt-in public UAT.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
@.planning/phases/57-block-download-and-connect-progress/57-04-SUMMARY.md
@.planning/phases/51-live-smoke-fresh-status-integration/51-01-SUMMARY.md
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh

<interfaces>
Current report result shape in `scripts/run-live-mainnet-smoke.ts`:

```typescript
type ReportStatus =
  | "passed"
  | "preflight_failed"
  | "runtime_failed"
  | "no_progress"
  | "cancelled";

type SmokeReport = {
  result: {
    blockDelta: number;
    firstBlockProgress: FirstBlockProgressEvidence | null;
    firstHeaderProgress: FirstHeaderProgressEvidence | null;
    headerDelta: number;
    maybeNoProgressCause: NoProgressCause | null;
    message: string;
    nextAction: string;
    progressDetected: boolean;
    status: ReportStatus;
  };
  schema_version: 2;
  snapshots: SyncStatusSnapshot[];
};
```

Existing status command contract:

```typescript
statusSpec.args = [
  "-rpcconnect=127.0.0.1",
  `-rpcport=${rpcPort}`,
  "-rpcuser=smoke",
  "-rpcpassword=smoke",
  "openbitcoinsyncstatus",
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add restart schema contracts and status timestamp parsing</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - .planning/phases/57-block-download-and-connect-progress/57-04-SUMMARY.md
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
    - packages/open-bitcoin-node/src/status.rs
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - Existing non-restart smoke reports include `result.restartResumeEvidence: null`.
    - `--help` documents `--restart-after-progress`.
    - Status snapshots and final status summaries expose `maybeLastSuccessfulProgressUnixSeconds` parsed from `sync.last_successful_progress_unix_seconds`.
  </behavior>
  <action>
    Extend `Options` with `restartAfterProgress: boolean` and parse exactly `--restart-after-progress`. Add `restartResumeEvidence: RestartResumeEvidence | null` under `SmokeReport.result` per D-05, leaving existing `firstHeaderProgress` and `firstBlockProgress` untouched per D-07. Add typed contracts with these exact enum values: `RestartStatus = "not_requested" | "completed" | "blocked_before_restart" | "cancelled"` and `DuplicateConnectVerdict = "no_duplicate_connect_observed" | "duplicate_connect_suspected" | "unavailable"`. Add `maybeLastSuccessfulProgressUnixSeconds: number | null` to `SyncStatusSnapshot` and `FinalStatusSummary` by reading `sync.last_successful_progress_unix_seconds`. Keep raw daemon tails, raw status snapshot arrays, raw endpoint tables, and raw options out of `RestartResumeEvidence` per D-08.
  </action>
  <verify>
    <automated>bun run scripts/run-live-mainnet-smoke.ts --help</automated>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "restartAfterProgress|--restart-after-progress|restartResumeEvidence|type RestartStatus|type DuplicateConnectVerdict|maybeLastSuccessfulProgressUnixSeconds" scripts/run-live-mainnet-smoke.ts` finds the new contracts.
    - `bash scripts/test-run-live-mainnet-smoke.sh` asserts `"restartResumeEvidence": null` for a normal non-restart run.
    - `bun run scripts/run-live-mainnet-smoke.ts --help` prints `--restart-after-progress`.
    - No support-bundle allowlist file is modified.
  </acceptance_criteria>
  <done>The additive schema contract is present, non-restart reports remain explicit, and latest durable progress timestamps are available for restart evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement two-session same-datadir restart orchestration</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - With `--restart-after-progress`, session one runs until header, downloaded-block, or connected-block progress is observed, then the script intentionally terminates that daemon.
    - Session two starts a new daemon process with the same resolved datadir and fresh RPC port, then captures fresh `openbitcoinsyncstatus` evidence.
    - Fresh post-restart public progress is optional: durable same-datadir resume plus typed post-restart blocker remains acceptable per D-03.
  </behavior>
  <action>
    Refactor the current one-process body into a reusable session helper that preserves argument-array `spawn` and `execFileSync` usage. Implement `--restart-after-progress` per D-01 and D-04: resolve the selected datadir once, pass the same datadir to both daemon launches, terminate session one intentionally after observed progress, and start session two with a new RPC port. Populate `result.restartResumeEvidence` with compact fields per D-06: `sameDatadir` with `requestedPathMatched` and `resolvedPathMatched` booleans, `restartStatus`, `beforeRestart`, `afterRestart`, `peerOutcomeSummary`, `duplicateConnectVerdict`, and `maybePostRestartProgressDelta`. Add mocked live-smoke fixture coverage per D-14 for before/restart/after snapshots, same datadir, runtime phase, peer summaries, latest progress timestamp, duplicate-connect verdict, and recovery-diagnosis placeholder wiring. Use `result.status = "passed"` for restart mode when session one observed progress and session two confirms durable heights/hashes are not lower than the before-restart summary, even when post-restart height delta is zero. Set `restartStatus = "blocked_before_restart"` when no pre-restart progress was captured, and `restartStatus = "cancelled"` only for operator cancellation before enough evidence is captured per D-12. Do not add public-network live smoke to `bash scripts/verify.sh` per D-16.
  </action>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
    <automated>bun run scripts/run-live-mainnet-smoke.ts --help</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-run-live-mainnet-smoke.sh` has a mocked `--restart-after-progress` run that starts the mock daemon twice and exits successfully.
    - The mocked restart report contains `"restartStatus": "completed"`, `"requestedPathMatched": true`, `"resolvedPathMatched": true`, `"duplicateConnectVerdict": "no_duplicate_connect_observed"`, `"beforeRestart"`, `"afterRestart"`, and `"maybePostRestartProgressDelta"`.
    - The mocked restart report contains `"firstHeaderProgress"` or `"firstBlockProgress"` as local evidence but uses `"restartResumeEvidence"` as the restart proof.
    - The Markdown report contains the heading or line text `Restart/resume evidence`.
    - A deterministic check proves `JSON.stringify(report.result.restartResumeEvidence)` does not contain `stdoutTail`, `stderrTail`, `endpoint_outcomes`, `snapshots`, or `manualPeers`.
  </acceptance_criteria>
  <done>The opt-in live-smoke restart flow proves a two-session same-datadir boundary with compact restart evidence and deterministic fixture coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args/env -> live-smoke process | Operator-supplied datadir, peer, timeout, and override command paths enter the TypeScript script. |
| live-smoke process -> child daemon/status commands | The script spawns local binaries and reads JSON status from a daemon-controlled RPC surface. |
| report writer -> local evidence files | Report JSON and Markdown may be shared by operators and must avoid compact-evidence leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Tampering / Elevation of privilege | daemon/status command execution | mitigate | Preserve `spawn`/`execFileSync` argument arrays and existing shell-metacharacter fixture guard; do not introduce shell command strings. |
| T-58-02-02 | Spoofing | same-datadir restart evidence | mitigate | Resolve the datadir once, compare requested and resolved path identity across both sessions, and record booleans in `sameDatadir`. |
| T-58-02-03 | Information disclosure | `result.restartResumeEvidence` | mitigate | Keep stdout/stderr tails, raw snapshots, endpoint tables, options, manual peers, and raw datadir strings out of the compact object per D-08. |
| T-58-02-04 | Denial of service | public-network restart review | mitigate | Keep restart mode explicit behind `--restart-after-progress`, retain timeout/poll/disk preflight bounds, and keep it outside `bash scripts/verify.sh`. |
| T-58-02-05 | Repudiation | restart boundary proof | mitigate | Report before-restart and after-restart durable summaries plus session restart status so reviewers can distinguish same-process progress from post-relaunch resume. |
</threat_model>

<verification>
Targeted plan verification:

```bash
bash scripts/test-run-live-mainnet-smoke.sh
bun run scripts/run-live-mainnet-smoke.ts --help
```

Public-network UAT remains opt-in only:

```bash
bun run scripts/run-live-mainnet-smoke.ts \
  --datadir=/tmp/open-bitcoin-mainnet \
  --manual-peer=HOST:8333 \
  --restart-after-progress \
  --timeout-seconds=180 \
  --poll-seconds=10
```
</verification>

<success_criteria>
RESUME-01 and RESUME-02 have an opt-in two-session live-smoke restart path that confirms same-datadir durable resume from fresh daemon status snapshots without requiring fresh post-restart public progress.
</success_criteria>

<output>
After completion, create `.planning/phases/58-same-datadir-restart-and-resume-evidence/58-02-SUMMARY.md`.
</output>
