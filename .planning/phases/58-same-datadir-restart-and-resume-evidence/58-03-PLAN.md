---
phase: 58-same-datadir-restart-and-resume-evidence
plan: 03
type: execute
wave: 2
depends_on:
  - "58-02"
files_modified:
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
autonomous: true
requirements:
  - RESUME-02
  - RESUME-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 58-2026-06-05T12-58-05
generated_at: 2026-06-05T13:15:50Z
must_haves:
  truths:
    - "Restart reports classify recovery guidance into the seven Phase 58 categories with storage health taking precedence."
    - "Operator docs provide copy-pasteable Cargo, Bazel, and opt-in live-smoke commands for same-datadir restart/resume review."
    - "Parity docs describe explicit restart/resume evidence without broadening into unattended production operation or Phase 59 support-bundle work."
  artifacts:
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "Recovery diagnosis helper and restart report integration"
      contains: "RecoveryDiagnosisCategory"
    - path: "scripts/test-run-live-mainnet-smoke.sh"
      provides: "Fixture matrix for all RESUME-03 diagnosis categories"
      contains: "store_incompatibility"
    - path: "docs/operator/runtime-guide.md"
      provides: "Same-datadir restart/resume commands and pass/fail guidance"
      contains: "--restart-after-progress"
    - path: "docs/parity/catalog/p2p.md"
      provides: "Scoped P2P parity wording for restart/resume evidence"
      contains: "restartResumeEvidence"
  key_links:
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "docs/operator/runtime-guide.md"
      via: "shared diagnosis category names"
      pattern: "store_incompatibility|intentional_cancellation"
    - from: "docs/parity/catalog/p2p.md"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "result.restartResumeEvidence schema field"
      pattern: "restartResumeEvidence"
---

<objective>
Close Phase 58 recovery diagnosis, operator guidance, and scoped parity wording after the restart evidence schema exists.

Purpose: RESUME-03 requires typed recovery guidance, and operators need exact review commands and pass/fail interpretation without Phase 59 support-bundle or release-boundary scope.
Output: Recovery diagnosis fixtures in the smoke runner plus docs/parity updates for same-datadir restart/resume evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
@.planning/phases/58-same-datadir-restart-and-resume-evidence/58-02-SUMMARY.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
@packages/open-bitcoin-node/src/storage.rs
@packages/open-bitcoin-node/src/sync/types/projection.rs

<interfaces>
Recovery categories locked by Phase 58 context:

```typescript
type RecoveryDiagnosisCategory =
  | "peer_incompatibility"
  | "public_network_unreachable"
  | "invalid_peer_data"
  | "store_corruption"
  | "store_incompatibility"
  | "resource_exhaustion"
  | "intentional_cancellation";
```

Storage health messages already distinguish schema and corruption:

```rust
StorageError::InvalidSchemaVersion { .. } => "storage schema version invalid during sync"
StorageError::SchemaMismatch { .. } => "storage schema mismatch during sync"
StorageError::Corruption { namespace, .. } => "storage corruption in {namespace} during sync"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add storage-first recovery diagnosis matrix</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-02-SUMMARY.md
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/sync/types/projection.rs
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - Recovery diagnosis returns exactly one user-facing category from D-09.
    - Underlying causes are preserved alongside the category: existing no-progress cause, peer failure reason, storage recovery action text when available, and last-error detail.
    - Storage corruption or incompatibility is classified before peer/network/timeout guidance per D-11.
    - Intentional restart interruption is not failure, but cancellation before evidence is captured is `intentional_cancellation` per D-12.
  </behavior>
  <action>
    Add a pure TypeScript helper that derives `RecoveryDiagnosis` for `result.restartResumeEvidence` and Markdown rendering. Use these exact category values: `peer_incompatibility`, `public_network_unreachable`, `invalid_peer_data`, `store_corruption`, `store_incompatibility`, `resource_exhaustion`, and `intentional_cancellation`. Preserve underlying causes per D-10 with nullable fields such as `maybeNoProgressCause`, `maybePeerFailureReason`, `maybeStorageRecoveryAction`, and `maybeLastError`. Apply precedence in this order: schema invalid/mismatch -> `store_incompatibility`; storage corruption/corrupt namespace -> `store_corruption`; resource pressure/resource_limit -> `resource_exhaustion`; early operator cancellation -> `intentional_cancellation`; invalid/malformed block or validation data -> `invalid_peer_data`; handshake/service/version capability failures -> `peer_incompatibility`; DNS/TCP/unreachable/timeout/no outbound peers -> `public_network_unreachable`. Extend `scripts/test-run-live-mainnet-smoke.sh` with a narrow fixture matrix covering all seven categories, including one storage-first case where a peer failure is present but a schema or corruption error wins. This implements D-09 through D-15 without a local peer harness and without public-network checks in `scripts/verify.sh` per D-16.
  </action>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
    <automated>bun run scripts/run-live-mainnet-smoke.ts --help</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "type RecoveryDiagnosisCategory|peer_incompatibility|public_network_unreachable|invalid_peer_data|store_corruption|store_incompatibility|resource_exhaustion|intentional_cancellation" scripts/run-live-mainnet-smoke.ts` finds all categories.
    - `scripts/test-run-live-mainnet-smoke.sh` greps each category value in generated JSON.
    - A fixture with storage schema/corruption text and a peer failure reason produces `store_incompatibility` or `store_corruption`, not peer guidance.
    - A cancelled-before-evidence fixture produces `"category": "intentional_cancellation"`.
  </acceptance_criteria>
  <done>RESUME-03 diagnosis is deterministic, storage-first, and covered by script fixtures for all seven categories.</done>
</task>

<task type="auto">
  <name>Task 2: Document restart/resume UAT commands and scoped parity claim</name>
  <read_first>
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-RESEARCH.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-02-SUMMARY.md
    - docs/operator/runtime-guide.md
    - docs/parity/catalog/p2p.md
    - AGENTS.md
  </read_first>
  <files>docs/operator/runtime-guide.md, docs/parity/catalog/p2p.md</files>
  <action>
    Update `docs/operator/runtime-guide.md` per D-17 with copy-pasteable commands for same-datadir restart/resume review: the opt-in `bun run scripts/run-live-mainnet-smoke.ts --datadir=/tmp/open-bitcoin-mainnet --manual-peer=HOST:8333 --restart-after-progress --timeout-seconds=180 --poll-seconds=10` command, Cargo `open-bitcoin sync status --format json` against the same datadir, and Bazel `//packages/open-bitcoin-cli:open_bitcoin` status against the same datadir. Add pass/fail interpretation using exact report fields: `result.status`, `result.restartResumeEvidence.restartStatus`, `sameDatadir.requestedPathMatched`, `sameDatadir.resolvedPathMatched`, before/after header/downloaded/connected heights and hashes, `duplicateConnectVerdict`, `maybePostRestartProgressDelta`, and `recoveryDiagnosis.category`. State explicitly per D-03 that fresh post-restart progress is stronger evidence but not required when durable same-datadir resume is confirmed and a typed post-restart blocker is present. Update `docs/parity/catalog/p2p.md` per D-18 to describe explicit opt-in same-datadir restart/resume evidence through `result.restartResumeEvidence` while preserving known gaps for unattended production operation, service-manager restart policy, inbound serving, transaction relay, packaging, hosted dashboards, GUI, production-funds wallet use, migration apply mode, and broad production-node readiness. Do not edit support-bundle allowlisting, threat-model closeout, release-readiness docs, or `bash scripts/verify.sh`.
  </action>
  <verify>
    <automated>rg -n -- "--restart-after-progress|restartResumeEvidence|same-datadir restart|same datadir|duplicateConnectVerdict|store_incompatibility|intentional_cancellation" docs/operator/runtime-guide.md docs/parity/catalog/p2p.md</automated>
    <automated>! rg -n "run-live-mainnet-smoke|--restart-after-progress" scripts/verify.sh</automated>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator/runtime-guide.md` contains the exact strings `--restart-after-progress`, `result.restartResumeEvidence.restartStatus`, `duplicateConnectVerdict`, `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --`, and `bazel run //packages/open-bitcoin-cli:open_bitcoin --`.
    - `docs/operator/runtime-guide.md` names all seven recovery categories from D-09.
    - `docs/parity/catalog/p2p.md` contains `restartResumeEvidence` and keeps `daemon-integrated, unattended public-network full sync through open-bitcoind` or equivalent unattended-production wording as a known gap.
    - `rg -n "run-live-mainnet-smoke|--restart-after-progress" scripts/verify.sh` returns no matches.
    - `bash scripts/verify.sh` passes after any generated `docs/metrics/lines-of-code.md` freshness update required by repo hooks.
  </acceptance_criteria>
  <done>Operator and parity docs explain how to review Phase 58 evidence and preserve the explicit opt-in, non-production scope boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon status/errors -> recovery diagnosis | Untrusted peer and storage failure text is mapped into operator-facing categories. |
| generated report -> operator docs/parity claims | Local evidence can be over-shared or over-claimed if docs misstate the boundary. |
| verification scripts -> public network | Default repo verification must not cross into public mainnet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03-01 | Tampering | recovery classification | mitigate | Apply storage-first precedence and fixture coverage for schema mismatch, corruption, peer invalid data, public-network unreachability, resource exhaustion, peer incompatibility, and cancellation. |
| T-58-03-02 | Information disclosure | restart evidence docs | mitigate | Document compact report fields only; do not add support-bundle allowlisting or raw daemon/status data handling in this phase. |
| T-58-03-03 | Repudiation | operator pass/fail interpretation | mitigate | Docs must name exact JSON fields reviewers inspect, including same-datadir booleans and duplicate-connect verdict. |
| T-58-03-04 | Denial of service | default verification | mitigate | Acceptance requires `scripts/verify.sh` contains no live-smoke restart command and public-network checks remain opt-in. |
| T-58-03-05 | Spoofing | parity claim boundary | mitigate | Parity docs must state explicit restart/resume review only and preserve unattended production sync as a known gap. |
</threat_model>

<verification>
Final Phase 58 verification for this plan:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node same_datadir --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node restart --all-features
bash scripts/test-run-live-mainnet-smoke.sh
bun run scripts/run-live-mainnet-smoke.ts --help
! rg -n "run-live-mainnet-smoke|--restart-after-progress" scripts/verify.sh
bash scripts/verify.sh
```

The negated `rg` check against `scripts/verify.sh` should pass only when no live-smoke restart command is present; if it fails, remove the default public-network coupling before rerunning verification.
</verification>

<success_criteria>
RESUME-02 and RESUME-03 are complete when restart reports include typed recovery diagnosis, operator docs provide same-datadir restart/resume UAT commands and pass/fail interpretation, parity docs preserve the explicit opt-in scope boundary, and full deterministic repo verification passes without adding public-network live smoke to `bash scripts/verify.sh`.
</success_criteria>

<output>
After completion, create `.planning/phases/58-same-datadir-restart-and-resume-evidence/58-03-SUMMARY.md`.
</output>
