---
phase: 97-inbound-metrics-sample-production
plan: 03
type: execute
wave: 3
depends_on:
  - 97-01
  - 97-02
files_modified:
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - docs/architecture/operator-observability.md
  - docs/operator/runtime-guide.md
autonomous: true
requirements: [INB-05, DOS-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 97-2026-06-28T16-11-36
generated_at: 2026-06-28T16:34:06Z
must_haves:
  truths:
    - "Dashboard history renders retained inbound samples through the existing metric chart path and existing labels."
    - "No new dashboard screen, panel, chart component, keybinding, palette, or operator workflow is added."
    - "Status and support evidence can carry retained inbound metric samples without raw peer, endpoint, permission, or credential material."
    - "Operator docs explain the closed inbound counter to retained metrics flow without public-network or production participation claims."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Bounded existing dashboard chart selection for retained inbound sample history"
      contains: "MAX_DASHBOARD_CHARTS"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs"
      provides: "Dashboard retained inbound sample rendering proof"
      contains: "dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "Support bundle retained inbound metric sample proof"
      contains: "support_bundle_preserves_retained_inbound_metric_samples"
    - path: "docs/architecture/operator-observability.md"
      provides: "Architecture-level low-cardinality retained inbound metrics contract"
      contains: "InboundPeerServingStatus aggregate counters -> fixed MetricSample values"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      to: "packages/open-bitcoin-node/src/metrics.rs"
      via: "bounded dashboard metric selector can choose retained inbound MetricKind values"
      pattern: "MetricKind::InboundResourcePressureActiveCount"
    - from: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      to: "packages/open-bitcoin-node/src/metrics.rs"
      via: "support bundle status fixture carries retained inbound MetricSample values"
      pattern: "InboundReconnectSuppressedCount"
---

<objective>
Prove retained inbound samples render through existing operator evidence surfaces.

Purpose: Satisfy D-10 and D-11 by using the existing dashboard/status/support paths for retained inbound history instead of creating a new UI or evidence store.
Output: Dashboard registration/tests, status/support tests, and bounded operator documentation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/frontend-ui.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
@.planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
@.planning/phases/97-inbound-metrics-sample-production/97-UI-SPEC.md
@.planning/phases/97-inbound-metrics-sample-production/97-02-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
@packages/open-bitcoin-node/src/status/tests.rs
@docs/architecture/operator-observability.md
@docs/operator/runtime-guide.md

<interfaces>
Existing dashboard contract:
```rust
pub const DASHBOARD_METRIC_KINDS: [MetricKind; 8] = [
    MetricKind::HeaderHeight,
    MetricKind::DownloadedBlockHeight,
    MetricKind::ConnectedBlockHeight,
    MetricKind::SyncHeight,
    MetricKind::PeerCount,
    MetricKind::MempoolTransactions,
    MetricKind::DiskUsageBytes,
    MetricKind::RpcHealth,
];

fn dashboard_charts(snapshot: &OpenBitcoinStatusSnapshot) -> Vec<MetricChart>;
fn metric_label(kind: MetricKind) -> &'static str;
```

UI contract from 97-UI-SPEC:
```text
Do not add a new dashboard screen, panel, modal, tab, chart component, color palette,
keybinding, or operator workflow for Phase 97.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded dashboard chart selection for retained inbound metric samples</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/frontend-ui.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    .planning/phases/97-inbound-metrics-sample-production/97-UI-SPEC.md
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    packages/open-bitcoin-node/src/metrics.rs
  </read_first>
  <behavior>
    - Test 1: `dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row` creates an `OpenBitcoinStatusSnapshot` with retained inbound samples and proves charts include `Inbound admits`, `Inbound resource pressure`, and `Inbound reconnect suppressions` with sparkline-safe numeric points while `state.charts.len() <= MAX_DASHBOARD_CHARTS`.
    - Test 2: `dashboard_metric_labels_cover_all_fixed_inbound_series` proves every fixed inbound `MetricKind` from Plan 97-01 has a stable `metric_label` without requiring every inbound series to be shown at once.
    - Test 3: `dashboard_inbound_metric_history_uses_existing_empty_state` proves available metrics with no samples still render `Unavailable: no metric samples recorded` through `chart_availability`.
  </behavior>
  <action>Keep the live dashboard chart row bounded at eight charts. In `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`, introduce `const MAX_DASHBOARD_CHARTS: usize = 8`, keep the existing baseline `DASHBOARD_METRIC_KINDS` at eight entries, and add an `INBOUND_DASHBOARD_METRIC_CANDIDATES` fixed array containing the 23 inbound metric kinds in the same order used by `MetricKind::ALL`. Add a pure selector such as `dashboard_metric_kinds(snapshot: &OpenBitcoinStatusSnapshot) -> Vec<MetricKind>` that starts with the existing baseline list, finds inbound candidate kinds with retained samples, and substitutes up to three retained inbound kinds into the optional tail slots currently occupied by `MempoolTransactions`, `DiskUsageBytes`, and `RpcHealth`. The result must be stable, deduplicated, and capped with `MAX_DASHBOARD_CHARTS`; it must include no new chart row, panel, screen, tab, selector, metric picker, export action, palette, keybinding, or explanatory dashboard copy. Update `dashboard_charts` to iterate over the selector result and keep `MetricChart`, `derive_metric_points`, `metric_label`, `chart_availability`, action handling, layout, colors, title copy, and keyboard controls unchanged per D-10 and the UI-SPEC. Tests must assert retained inbound samples render through `DashboardState::from_snapshot` and existing `metric_label` strings only.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_metric --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "MAX_DASHBOARD_CHARTS: usize = 8" packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    grep -n "INBOUND_DASHBOARD_METRIC_CANDIDATES" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "dashboard_metric_kinds" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "MetricKind::InboundAdmittedPeerCount" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "MetricKind::InboundInactivePermissionEffectCount" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "MetricKind::InboundResourcePressureActiveCount" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "MetricKind::InboundReconnectSuppressedCount" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    grep -n "dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row" packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    rg -n "Phase 97|filter selector|metric picker|export action|new dashboard" packages/open-bitcoin-cli/src/operator/dashboard/model.rs returns no matches
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_metric --no-fail-fast
  </acceptance_criteria>
  <done>Existing dashboard chart history renders retained inbound metric samples with static labels, a bounded eight-chart row, and no new UI surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove retained inbound samples in status, support, and operator docs</name>
  <files>packages/open-bitcoin-node/src/status/tests.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs, docs/architecture/operator-observability.md, docs/operator/runtime-guide.md</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/testing.md
    standards/core/verification.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    .planning/phases/97-inbound-metrics-sample-production/97-UI-SPEC.md
    packages/open-bitcoin-node/src/status/tests.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
    packages/open-bitcoin-cli/src/operator/support.rs
    packages/open-bitcoin-cli/src/operator/support/render.rs
    docs/architecture/operator-observability.md
    docs/operator/runtime-guide.md
  </read_first>
  <behavior>
    - Test 1: `status_snapshot_serializes_retained_inbound_metric_samples` proves `OpenBitcoinStatusSnapshot.metrics.samples` round-trips an `InboundAdmittedPeerCount` and `InboundPayloadRejectedCount` sample.
    - Test 2: `support_bundle_preserves_retained_inbound_metric_samples` proves a local support bundle fixture includes retained inbound metrics and keeps them as kind/value/timestamp.
    - Test 3: `support_bundle_retained_inbound_metrics_are_redacted_aggregate_evidence` proves support JSON/Markdown output does not contain `peer_id`, `raw_endpoint`, `permission_string`, `payload_bytes`, `credential`, `secret`, socket addresses, or permission class names from metric samples.
  </behavior>
  <action>Add status/support tests that use `MetricsStatus::available_with_samples(MetricRetentionPolicy::default(), vec![MetricSample::new(...)])` with fixed inbound metric kinds from Plan 97-01. The status test must assert serde JSON exposes `kind`, `value`, and `timestamp_unix_seconds` for inbound samples and no peer/endpoint/label fields. The support test must use the existing support bundle construction path and assert retained inbound metric samples survive in the bundle's status/metrics evidence without adding support-specific metric rendering or a second history store. Update `docs/architecture/operator-observability.md` with the exact closed flow sentence `InboundPeerServingStatus aggregate counters -> fixed MetricSample values -> FjallNodeStore::append_metric_samples -> dashboard/status/support retained history`. Update `docs/operator/runtime-guide.md` with repo-local Cargo and Bazel review commands that mention status, dashboard, and support bundle review for retained inbound metric history. Keep the wording aggregate and bounded: explicitly state retained local inbound metric evidence does not claim transaction relay, compact block relay, mempool propagation, public inbound defaults, packaged service operation, or production full-node readiness.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status_snapshot_serializes_retained_inbound_metric_samples -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_bundle_preserves_retained_inbound_metric_samples -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_bundle_retained_inbound_metrics_are_redacted_aggregate_evidence -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    grep -n "status_snapshot_serializes_retained_inbound_metric_samples" packages/open-bitcoin-node/src/status/tests.rs
    grep -n "support_bundle_preserves_retained_inbound_metric_samples" packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "support_bundle_retained_inbound_metrics_are_redacted_aggregate_evidence" packages/open-bitcoin-cli/src/operator/support/tests.rs
    grep -n "InboundPeerServingStatus aggregate counters -> fixed MetricSample values -> FjallNodeStore::append_metric_samples -> dashboard/status/support retained history" docs/architecture/operator-observability.md docs/operator/runtime-guide.md
    grep -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --" docs/operator/runtime-guide.md
    grep -n "bazel run //packages/open-bitcoin-cli:open_bitcoin --" docs/operator/runtime-guide.md
    grep -n "Retained local inbound metric evidence does not claim transaction relay, compact block relay, mempool propagation, public inbound defaults, packaged service operation, or production full-node readiness." docs/operator/runtime-guide.md
    rg -n "peer_id|raw_endpoint|permission_string|payload_bytes|credential|secret|dynamic label" packages/open-bitcoin-node/src/status/tests.rs packages/open-bitcoin-cli/src/operator/support/tests.rs docs/architecture/operator-observability.md docs/operator/runtime-guide.md returns matches only in explicit forbidden/redaction assertions
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_bundle_preserves_retained_inbound_metric_samples -- --nocapture
  </acceptance_criteria>
  <done>Status, support, and operator documentation prove retained inbound metric history as aggregate evidence and preserve the v1.9 no-claim boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained metrics -> dashboard model | Durable metric samples become terminal chart data. |
| retained metrics -> support bundle | Local status metrics become shareable support evidence. |
| docs -> operator interpretation | Documentation can accidentally overstate what retained inbound metrics prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Information Disclosure | dashboard/support retained samples | mitigate | Tests assert inbound samples remain kind/value/timestamp and reject raw peer, endpoint, permission, payload, credential, and dynamic-label material. |
| T-97-03-02 | Spoofing | dashboard availability | mitigate | Existing `chart_availability` empty state remains unchanged and tested for no-sample history. |
| T-97-03-03 | Tampering | metric registration | mitigate | Tests prove every fixed inbound metric kind has a stable label and the bounded selector can render retained inbound samples without expanding `state.charts` beyond `MAX_DASHBOARD_CHARTS`. |
| T-97-03-04 | Repudiation | operator docs | mitigate | Docs state the exact closed evidence flow and explicitly preserve the no-claim boundary for relay, public defaults, service operation, and production readiness. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_metric --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status_snapshot_serializes_retained_inbound_metric_samples -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support_bundle_preserves_retained_inbound_metric_samples -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
Retained inbound metric samples are visible through existing dashboard/status/support evidence paths, with static labels, bounded aggregate history, no new UI, and no expanded v1.9 participation claims.
</success_criteria>

<output>
After completion, create `.planning/phases/97-inbound-metrics-sample-production/97-03-SUMMARY.md`.
</output>
