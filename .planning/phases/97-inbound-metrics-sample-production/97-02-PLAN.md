---
phase: 97-inbound-metrics-sample-production
plan: 02
type: execute
wave: 2
depends_on:
  - 97-01
files_modified:
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
autonomous: true
requirements: [INB-05, DOS-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 97-2026-06-28T16-11-36
generated_at: 2026-06-28T16:34:06Z
must_haves:
  truths:
    - "Runtime progress collection appends sync and inbound metric samples through one existing Fjall metrics append path."
    - "Inbound samples are derived from the shared inbound status projection, not direct network internals or duplicate counter state."
    - "Unavailable or busy inbound status produces no inbound samples while sync samples still persist."
    - "Retained metrics history contains inbound admission/resource samples after a deterministic local runtime cycle."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "Combined sync plus inbound metrics append path"
      contains: "inbound_metric_samples"
    - path: "packages/open-bitcoin-node/src/sync.rs"
      provides: "DurableSyncRuntime inbound status provider hook"
      contains: "set_inbound_metric_status_provider"
    - path: "packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs"
      provides: "Daemon wiring from ManagedRpcContext current inbound status to runtime metric persistence"
      contains: "current_inbound_status"
    - path: "packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs"
      provides: "Loopback/synthetic proof of retained inbound metric samples"
      contains: "persists_inbound_metric_samples"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "shared-context status provider supplies FieldAvailability<InboundPeerServingStatus>"
      pattern: "set_inbound_metric_status_provider"
    - from: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store.rs"
      via: "existing FjallNodeStore::append_metric_samples retention path"
      pattern: "append_metric_samples"
---

<objective>
Persist inbound metric samples through the existing runtime metrics retention path.

Purpose: Close INT-02 and FLOW-02 by appending retained inbound samples from canonical status alongside existing sync samples, per D-01, D-02, D-07, D-08, and D-09.
Output: Runtime provider hook, combined append logic, daemon wiring, and deterministic retention tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
@.planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
@.planning/phases/97-inbound-metrics-sample-production/97-01-SUMMARY.md
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/storage/fjall_store.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs

<interfaces>
Plan 97-01 creates:
```rust
pub fn inbound_metric_samples(
    inbound: &FieldAvailability<InboundPeerServingStatus>,
    timestamp_unix_seconds: u64,
) -> Vec<MetricSample>;
```

Existing retention path:
```rust
impl FjallNodeStore {
    pub fn append_metric_samples(
        &self,
        samples: &[MetricSample],
        retention: MetricRetentionPolicy,
        now_unix_seconds: u64,
        mode: PersistMode,
    ) -> Result<MetricsStorageSnapshot, StorageError>;
}

impl ManagedRpcContext {
    pub fn current_inbound_status(&self) -> FieldAvailability<InboundPeerServingStatus>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append sync and inbound samples in the runtime metrics path</name>
  <files>packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    .planning/phases/97-inbound-metrics-sample-production/97-01-SUMMARY.md
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/sync/tests.rs
    packages/open-bitcoin-node/src/storage/fjall_store.rs
  </read_first>
  <behavior>
    - Test 1: `persist_metrics_appends_inbound_status_samples_with_sync_samples` proves one call through runtime metrics persistence retains a sync sample and `MetricKind::InboundResourcePressureActiveCount` for the same timestamp.
    - Test 2: `persist_metrics_omits_inbound_samples_when_status_unavailable` proves unavailable inbound status returns only existing sync samples.
    - Test 3: `persist_metrics_uses_existing_retention_policy_for_inbound_series` proves inbound samples are pruned/coalesced by `MetricRetentionPolicy::default()` through `FjallNodeStore::append_metric_samples`.
  </behavior>
  <action>Add an optional provider hook to `DurableSyncRuntime` named `set_inbound_metric_status_provider`. Use a concrete type such as `Arc<dyn Fn() -> FieldAvailability<InboundPeerServingStatus> + Send + Sync + 'static>` stored as `maybe_inbound_metric_status_provider`. Initialize it to `None` in `DurableSyncRuntime::open`. In `persist_metrics`, build `let mut samples = summary.metric_samples(timestamp);`, then if the provider exists, call it once, pass the result to `inbound_metric_samples(&inbound_status, timestamp)`, and extend `samples`. Call `self.store.append_metric_samples(&samples, MetricRetentionPolicy::default(), timestamp, self.config.persist_mode)` exactly once so D-07 and D-08 use the existing retention path and one combined retained metrics batch. If the provider returns `FieldAvailability::Unavailable`, `inbound_metric_samples` must add nothing per D-03. Do not read sockets, RPC state, peer manager internals, listener internals, or storage outside the existing append path.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics_appends_inbound_status_samples_with_sync_samples -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "set_inbound_metric_status_provider" packages/open-bitcoin-node/src/sync.rs packages/open-bitcoin-node/src/sync/tests.rs
    grep -n "maybe_inbound_metric_status_provider" packages/open-bitcoin-node/src/sync.rs packages/open-bitcoin-node/src/sync/runtime_state.rs
    grep -n "inbound_metric_samples" packages/open-bitcoin-node/src/sync/runtime_state.rs packages/open-bitcoin-node/src/sync/tests.rs
    grep -n "append_metric_samples(&samples" packages/open-bitcoin-node/src/sync/runtime_state.rs
    grep -n "persist_metrics_appends_inbound_status_samples_with_sync_samples" packages/open-bitcoin-node/src/sync/tests.rs
    grep -n "persist_metrics_omits_inbound_samples_when_status_unavailable" packages/open-bitcoin-node/src/sync/tests.rs
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics --no-fail-fast
  </acceptance_criteria>
  <done>Durable sync metrics persistence can append retained inbound samples from a canonical status provider without duplicating counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire daemon shared inbound status into metric persistence</name>
  <files>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    .planning/phases/97-inbound-metrics-sample-production/97-01-SUMMARY.md
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/metrics.rs
  </read_first>
  <behavior>
    - Test 1: `open_bitcoind_daemon_sync_persists_inbound_metric_samples_from_shared_context` records inbound admission/resource evidence on `ManagedRpcContext`, runs a deterministic local sync cycle, and finds retained `MetricKind::InboundAdmittedPeerCount` and `MetricKind::InboundResourcePressureActiveCount` in Fjall metrics status.
    - Test 2: `open_bitcoind_daemon_sync_omits_inbound_samples_when_context_busy` uses a held shared context lock and proves the provider returns unavailable, so no inbound samples are appended.
    - Test 3: `open_bitcoind_inbound_metric_provider_uses_current_inbound_status` proves the provider calls `ManagedRpcContext::current_inbound_status()` and does not read `ManagedPeerNetwork` or listener evidence directly.
  </behavior>
  <action>Reorder `main` narrowly so `ManagedRpcContext::from_runtime_config(&runtime)` and `Arc<tokio::sync::Mutex<ManagedRpcContext>>` are created before `start_daemon_sync_worker`. Change `start_daemon_sync_worker` to accept the shared context, then set the `DurableSyncRuntime` provider from Task 1 before spawning `daemon_sync_worker`. Add a helper such as `configure_inbound_metric_status_provider(sync_runtime: &mut DurableSyncRuntime, shared_context: Arc<tokio::sync::Mutex<ManagedRpcContext>>)`. The provider closure must call `shared_context.try_lock()`: on success return `context.current_inbound_status()`, and on lock failure return `FieldAvailability::unavailable("inbound status provider unavailable: shared context busy")` so D-03 emits no inbound samples. Keep the provider aggregate-only and public-network-free per D-09; do not bind listeners, dial peers, inspect sockets, add service-manager checks, or add raw peer/endpoint labels.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_daemon_sync_persists_inbound_metric_samples_from_shared_context -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_inbound_metric -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "configure_inbound_metric_status_provider" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
    grep -n "set_inbound_metric_status_provider" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    grep -n "current_inbound_status()" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    grep -n "try_lock()" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    grep -n "inbound status provider unavailable: shared context busy" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    grep -n "open_bitcoind_daemon_sync_persists_inbound_metric_samples_from_shared_context" packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
    rg -n "active_bans > 0|resource_pressure_events > 0|inbound_admission_info\\(" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs returns no matches
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_inbound_metric -- --nocapture
  </acceptance_criteria>
  <done>`open-bitcoind` persists retained inbound samples from the existing shared status projection during deterministic local runtime cycles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC shared context -> sync runtime provider | Shared status crosses from async daemon context into sync runtime metrics persistence. |
| runtime metrics batch -> Fjall store | Numeric samples are appended to durable retained history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Spoofing | busy/unavailable provider | mitigate | Provider returns `FieldAvailability::Unavailable`; mapper emits no inbound samples instead of zero-valued evidence. |
| T-97-02-02 | Tampering | duplicate runtime counter state | mitigate | Provider calls `ManagedRpcContext::current_inbound_status()` only; acceptance rejects direct network aggregate shortcuts. |
| T-97-02-03 | Denial of Service | metrics retention | mitigate | Existing `FjallNodeStore::append_metric_samples` and `MetricRetentionPolicy::default()` remain the only retention path. |
| T-97-02-04 | Information Disclosure | retained metric history | mitigate | Samples remain kind/value/timestamp only; no peer identifiers, endpoints, raw reasons, or dynamic labels enter persistence. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node persist_metrics --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_inbound_metric -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
Runtime metric persistence appends sync and inbound samples through the existing Fjall retention path using canonical inbound status, and deterministic tests prove retained inbound admission and resource-governance samples without public-network exposure.
</success_criteria>

<output>
After completion, create `.planning/phases/97-inbound-metrics-sample-production/97-02-SUMMARY.md`.
</output>
