---
phase: 97-inbound-metrics-sample-production
plan: 04
type: execute
wave: 4
depends_on:
  - 97-01
  - 97-02
  - 97-03
files_modified:
  - scripts/check-phase97-inbound-metrics.ts
  - scripts/check-phase97-inbound-metrics.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [INB-05, DOS-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 97-2026-06-28T16-11-36
generated_at: 2026-06-28T16:34:06Z
must_haves:
  truths:
    - "A deterministic Phase 97 checker proves inbound metric samples are produced from canonical status evidence."
    - "The checker proves retained samples flow through the existing Fjall metrics history path and existing dashboard registration."
    - "Default verification runs the Phase 97 checker after Phase 96 without public-network, service-manager, long-running, or production-readiness gates."
    - "Full repo-native verification passes before Phase 97 completion."
  artifacts:
    - path: "scripts/check-phase97-inbound-metrics.ts"
      provides: "Phase 97 deterministic checker"
      exports: ["checkPhase97InboundMetrics"]
    - path: "scripts/check-phase97-inbound-metrics.test.ts"
      provides: "Mutation fixture tests for Phase 97 checker"
      contains: "fails inactive permission label count mapping"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring for Phase 97"
      contains: "check-phase97-inbound-metrics"
    - path: "docs/metrics/lines-of-code.md"
      provides: "Tracked generated LOC artifact refreshed by verification when needed"
      contains: "scripts/check-phase97-inbound-metrics.ts"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase97-inbound-metrics.ts"
      via: "run_step after Phase 96 checker"
      pattern: "check-phase97-inbound-metrics"
    - from: "scripts/check-phase97-inbound-metrics.ts"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "static assertion for combined append through FjallNodeStore::append_metric_samples"
      pattern: "append_metric_samples(&samples"
---

<objective>
Add deterministic Phase 97 verification and run final repo checks.

Purpose: Lock INB-05 and DOS-04 gap closure with a checker that rejects retained-metrics shortcuts, dynamic labels, raw evidence, and public-network verification creep, per D-12 and D-13.
Output: Bun checker/test, verifier wiring, refreshed generated metrics if required, and final verification evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.9-MILESTONE-AUDIT.md
@.planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
@.planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
@.planning/phases/97-inbound-metrics-sample-production/97-01-SUMMARY.md
@.planning/phases/97-inbound-metrics-sample-production/97-02-SUMMARY.md
@.planning/phases/97-inbound-metrics-sample-production/97-03-SUMMARY.md
@scripts/check-phase96-peer-policy-runtime-bridge.ts
@scripts/check-phase96-peer-policy-runtime-bridge.test.ts
@scripts/verify.sh
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@docs/architecture/operator-observability.md
@docs/operator/runtime-guide.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 97 deterministic checker and mutation tests</name>
  <files>scripts/check-phase97-inbound-metrics.ts, scripts/check-phase97-inbound-metrics.test.ts</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/testing.md
    standards/languages/typescript-javascript.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    .planning/phases/97-inbound-metrics-sample-production/97-UI-SPEC.md
    scripts/check-phase96-peer-policy-runtime-bridge.ts
    scripts/check-phase96-peer-policy-runtime-bridge.test.ts
    scripts/verify.sh
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/status/inbound.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    packages/open-bitcoin-cli/src/operator/support/tests.rs
    docs/architecture/operator-observability.md
    docs/operator/runtime-guide.md
  </read_first>
  <behavior>
    - Pass case: complete corpus with pure `inbound_metric_samples`, numeric permission aggregates, provider-based runtime append, dashboard registration, support/status proof, docs flow wording, and verifier wiring returns no failures.
    - Negative case: mapper uses `inactive_permission_effects.len()` and checker fails.
    - Negative case: mapper omits `InboundPermissionValidationFailureCount` or `permission_validation_failures` and checker fails.
    - Negative case: runtime persistence appends only `summary.metric_samples(timestamp)` or calls `append_metric_samples` before extending inbound samples and checker fails.
    - Negative case: daemon provider reads `inbound_admission_info()` or resource counters directly instead of `current_inbound_status()` and checker fails.
    - Negative case: dashboard labels exist but bounded inbound chart candidates or selector proof omit inbound kinds and checker fails.
    - Negative case: retained metrics, docs, or tests introduce dynamic labels, peer ids, endpoints, raw permission strings, payload bytes, credentials, secrets, or public-network requirements and checker fails.
    - Negative case: `scripts/verify.sh` omits Phase 97 checker/test command from either `VERIFY_COMMAND_ORDER` or executable `run_step` order and checker fails.
  </behavior>
  <action>Create `scripts/check-phase97-inbound-metrics.ts` with a shebang, exported `checkPhase97InboundMetrics(options: { rootDir?: string } = {}): string[]`, and CLI main consistent with Phase 96 checker style. Use Bun/TypeScript file reads over a fixed corpus; do not embed shell logic. The checker must assert all 23 fixed inbound `MetricKind` names appear in `inbound_metric_samples`, `INBOUND_DASHBOARD_METRIC_CANDIDATES`, and checker constants; assert the dashboard keeps `MAX_DASHBOARD_CHARTS: usize = 8`, uses `dashboard_metric_kinds`, and includes the test `dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row`; assert `FieldAvailability::Unavailable` returns no samples; assert `inactive_permission_effect_observations` appears and `inactive_permission_effects.len()` is absent from mapping; assert `permission_validation_failures` and `count_inbound_permission_validation_failures` exist; assert `persist_metrics` builds a mutable `samples` batch, extends it with `inbound_metric_samples`, and calls `append_metric_samples(&samples, MetricRetentionPolicy::default(), timestamp, ...)`; assert `open-bitcoind.rs` uses `set_inbound_metric_status_provider`, `try_lock()`, and `current_inbound_status()`; assert docs contain the exact closed flow sentence from Plan 03; and assert `scripts/verify.sh` orders Phase 97 after Phase 96 in both command-order text and executable `run_step` sequence. Create `scripts/check-phase97-inbound-metrics.test.ts` with Arrange/Act/Assert comments and mutation fixtures for every failure class above.</action>
  <verify>
    <automated>bun test scripts/check-phase97-inbound-metrics.test.ts</automated>
    <automated>bun run scripts/check-phase97-inbound-metrics.ts</automated>
  </verify>
  <acceptance_criteria>
    grep -n "checkPhase97InboundMetrics" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "InboundInactivePermissionEffectCount" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "inactive_permission_effects.len" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "permission_validation_failures" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "append_metric_samples(&samples" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "current_inbound_status()" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "MAX_DASHBOARD_CHARTS" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "INBOUND_DASHBOARD_METRIC_CANDIDATES" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "dashboard_charts_render_retained_inbound_metric_samples_without_expanding_row" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    grep -n "public-network" scripts/check-phase97-inbound-metrics.ts scripts/check-phase97-inbound-metrics.test.ts
    bun test scripts/check-phase97-inbound-metrics.test.ts
    bun run scripts/check-phase97-inbound-metrics.ts
  </acceptance_criteria>
  <done>Phase 97 checker rejects the known retained-inbound-metrics gap patterns and passes the completed implementation corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 97 checker into default verification and run final checks</name>
  <files>scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/verification.md
    standards/languages/typescript-javascript.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    scripts/verify.sh
    scripts/check-phase97-inbound-metrics.ts
    scripts/check-phase97-inbound-metrics.test.ts
    docs/metrics/lines-of-code.md
  </read_first>
  <action>Wire `bun test scripts/check-phase97-inbound-metrics.test.ts` and `bun run scripts/check-phase97-inbound-metrics.ts` into `scripts/verify.sh` immediately after the Phase 96 checker in both the `VERIFY_COMMAND_ORDER` heredoc and executable `run_step` calls. Update the nearby verifier-order comment to state `Phase 96 is followed by Phase 97.` Keep the default verifier deterministic and local-only: do not add public-network listener checks, DNS/seed crawling, real service-manager actions, long-running soak, wildcard listener checks, relay checks, mempool checks, compact-block checks, production service checks, or production full-node readiness checks. Run targeted checker tests, targeted Rust tests from Plans 01 through 03 that were affected by final checker wiring if needed, then `bash scripts/verify.sh --fast` for iteration and `bash scripts/verify.sh` for final proof. If verification reports stale tracked LOC, refresh `docs/metrics/lines-of-code.md` with the repo-generated LOC command printed by the verifier and rerun the failing verification command.</action>
  <verify>
    <automated>bun test scripts/check-phase97-inbound-metrics.test.ts</automated>
    <automated>bun run scripts/check-phase97-inbound-metrics.ts</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_inbound_metric -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_metric --no-fail-fast</automated>
    <automated>bash scripts/verify.sh --fast</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    grep -n "bun test scripts/check-phase97-inbound-metrics.test.ts" scripts/verify.sh
    grep -n "bun run scripts/check-phase97-inbound-metrics.ts" scripts/verify.sh
    grep -n "Phase 96 is followed by Phase 97" scripts/verify.sh
    rg -n "check-phase96-peer-policy-runtime-bridge|check-phase97-inbound-metrics|check pure-core dependencies" scripts/verify.sh
    bun test scripts/check-phase97-inbound-metrics.test.ts
    bun run scripts/check-phase97-inbound-metrics.ts
    bash scripts/verify.sh --fast
    bash scripts/verify.sh
  </acceptance_criteria>
  <done>Default verification includes the deterministic Phase 97 checker and full repo-native verification is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> static checker | Checker verifies required retained-inbound-metrics wiring without executing public network behavior. |
| verify.sh -> default local workflow | Phase 97 enters the default deterministic verification contract. |
| generated LOC -> tracked docs | Verification may refresh a tracked generated artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Tampering | Phase 97 checker | mitigate | Mutation tests cover missing mapper, wrong permission source, direct network sampling, missing retention append, missing dashboard registration, raw evidence, and missing verifier wiring. |
| T-97-04-02 | Denial of Service | default verifier | mitigate | `verify.sh` additions are Bun/static checks only; no public-network, service-manager, long-running, wildcard listener, relay, mempool, compact-block, or production checks. |
| T-97-04-03 | Repudiation | phase evidence | mitigate | Summary records targeted checker, Rust, fast verifier, and full verifier outcomes. |
| T-97-04-04 | Information Disclosure | checker corpus | mitigate | Checker rejects raw peer, endpoint, permission, payload, credential, secret, and dynamic-label evidence in retained metrics surfaces. |
</threat_model>

<verification>
- `bun test scripts/check-phase97-inbound-metrics.test.ts`
- `bun run scripts/check-phase97-inbound-metrics.ts`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoind_inbound_metric -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_metric --no-fail-fast`
- `bash scripts/verify.sh --fast`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
Phase 97 has deterministic checker coverage wired into default verification, and full repo-native verification proves retained inbound metrics are produced from canonical runtime/status evidence, retained through existing metrics history, rendered through existing dashboard/status/support paths, and kept public-network-free.
</success_criteria>

<output>
After completion, create `.planning/phases/97-inbound-metrics-sample-production/97-04-SUMMARY.md`.
</output>
