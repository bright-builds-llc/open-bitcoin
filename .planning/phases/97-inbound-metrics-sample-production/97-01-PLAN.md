---
phase: 97-inbound-metrics-sample-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/metrics/tests.rs
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/loader.rs
  - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
autonomous: true
requirements: [INB-05, DOS-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 97-2026-06-28T16-11-36
generated_at: 2026-06-28T16:34:06Z
must_haves:
  truths:
    - "Every fixed inbound MetricKind has exactly one numeric aggregate source from InboundPeerServingStatus."
    - "Unavailable inbound status produces no inbound MetricSample values."
    - "InboundInactivePermissionEffectCount counts inactive permission effect observations, not distinct inactive labels."
    - "InboundPermissionValidationFailureCount is backed by a numeric config/admission validation aggregate, not runtime-created labels."
    - "The mapping remains pure and has no storage, socket, runtime, or dashboard side effects."
  artifacts:
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Pure inbound status to MetricSample mapper"
      contains: "inbound_metric_samples"
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Canonical inbound status aggregates for permission metric sources"
      contains: "inactive_permission_effect_observations"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Runtime status projection from managed admission/config evidence"
      contains: "permission_validation_failures"
    - path: "packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs"
      provides: "Minimal aggregate permission validation failure source"
      contains: "count_inbound_permission_validation_failures"
  key_links:
    - from: "packages/open-bitcoin-node/src/metrics.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "inbound_metric_samples reads FieldAvailability<InboundPeerServingStatus>"
      pattern: "FieldAvailability<InboundPeerServingStatus>"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "current_inbound_status projects managed/config aggregates into shared status"
      pattern: "inactive_permission_effect_observations"
---

<objective>
Create the pure inbound metrics contract for Phase 97.

Purpose: Close the metric vocabulary gap for INB-05 and DOS-04 by making retained inbound samples derivable from canonical shared status only, per D-01 through D-06.
Output: Numeric status aggregates, pure `inbound_metric_samples` mapper, and focused Rust tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
@.planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
@.planning/phases/97-inbound-metrics-sample-production/97-UI-SPEC.md
@.planning/v1.9-MILESTONE-AUDIT.md
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/metrics/tests.rs
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs

<interfaces>
Existing metric sample contract:
```rust
pub enum MetricKind {
    InboundAdmittedPeerCount,
    InboundRejectedPeerCount,
    InboundCapRejectCount,
    InboundReservedSlotRejectCount,
    InboundDuplicateRejectCount,
    InboundSelfConnectionRejectCount,
    InboundPermissionedAdmitCount,
    InboundProtectedAdmitCount,
    InboundInactivePermissionEffectCount,
    InboundPermissionValidationFailureCount,
    InboundEvictionCandidateCount,
    InboundDisconnectCount,
    InboundActiveBanCount,
    InboundMisbehaviorObservationCount,
    InboundProtectedNoActionCount,
    InboundResourcePressureActiveCount,
    InboundReadQueuePressureCount,
    InboundWriteQueuePressureCount,
    InboundRequestCapReachedCount,
    InboundPayloadRejectedCount,
    InboundTimeoutDisconnectCount,
    InboundChurnRejectedCount,
    InboundReconnectSuppressedCount,
}

pub struct MetricSample {
    pub kind: MetricKind,
    pub value: f64,
    pub timestamp_unix_seconds: u64,
}
```

Existing canonical source shape:
```rust
pub enum FieldAvailability<T> {
    Available(T),
    Unavailable { reason: String },
}

pub struct InboundPeerServingStatus {
    pub admitted_inbound_peers: u32,
    pub rejected_inbound_peers: u32,
    pub cap_rejects: u32,
    pub reserved_slot_rejects: u32,
    pub duplicate_rejects: u32,
    pub self_connection_rejects: u32,
    pub permissioned_inbound_peers: u32,
    pub protected_inbound_peers: u32,
    pub inactive_permission_effects: Vec<String>,
    pub eviction_candidates_evaluated: u32,
    pub disconnects_requested: u32,
    pub active_bans: u32,
    pub misbehavior_observations: u32,
    pub protected_no_actions: u32,
    pub resource_pressure_events: u32,
    pub read_queue_pressure_events: u32,
    pub write_queue_pressure_events: u32,
    pub request_cap_events: u32,
    pub payload_rejections: u32,
    pub timeout_disconnects: u32,
    pub churn_rejections: u32,
    pub reconnect_suppressions: u32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add numeric permission metric source aggregates to inbound status</name>
  <files>packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-rpc/src/config.rs, packages/open-bitcoin-rpc/src/config/loader.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs, packages/open-bitcoin-rpc/src/context/network.rs</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    packages/open-bitcoin-node/src/status/inbound.rs
    packages/open-bitcoin-node/src/status/inbound/tests.rs
    packages/open-bitcoin-node/src/network/inbound.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/loader.rs
    packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
  </read_first>
  <behavior>
    - Test 1: `inbound_status_serializes_permission_metric_source_aggregates` proves new shared status fields serialize as numeric values and legacy snapshots default both fields to `0`.
    - Test 2: `current_inbound_status_projects_inactive_permission_effect_observations` proves a permission decision with five inactive effect observations projects `inactive_permission_effect_observations == 5`, while `inactive_permission_effects.len()` remains a separate label list.
    - Test 3: `inbound_permission_validation_failure_count_is_config_validation_aggregate` proves malformed permission class config contributes one numeric validation failure through the new config helper and does not expose token, address, class name, reason, or label dimensions.
  </behavior>
  <action>Add two serde-defaulted numeric fields to `InboundPeerServingStatus`: `inactive_permission_effect_observations: u32` and `permission_validation_failures: u32`. In `ManagedRpcContext::current_inbound_status`, set `inactive_permission_effect_observations` from existing `ManagedInboundAdmissionInfo::inactive_permission_effect_observations` using the existing `usize_to_u32` helper; do not use `inactive_permission_effects.len()` for this value. Add a minimal aggregate config/admission validation source for `permission_validation_failures`: extend `RuntimeConfig` with `pub inbound_permission_validation_failures: u32`, set the default to `0`, set the resolved value in `loader.rs`, and add a pure helper in `config/loader/open_bitcoin_runtime.rs` named `count_inbound_permission_validation_failures(configs: &[InboundPermissionClassConfig]) -> u32`. That helper must call the same `ParsedPeerPermissionClass::parse` and duplicate-address validation logic used by `resolve_permission_class_registry`, count failed classes/duplicates as aggregate numeric failures, and return only the count. Per D-01 and D-02, project `RuntimeConfig::inbound_permission_validation_failures` into `InboundPeerServingStatus::permission_validation_failures` through `ManagedRpcContext`; do not add a metrics-only counter, dynamic labels, peer identifiers, endpoints, raw permission class strings, tokens, reasons, or addresses. Update direct struct literals in affected tests with explicit aggregate values.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status::inbound --no-fail-fast</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission_config_rejects_malformed_classes_with_stable_errors -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission_validation_failure_count_is_config_validation_aggregate -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc current_inbound_status --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    grep -n "inactive_permission_effect_observations" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-node/src/status/inbound/tests.rs
    grep -n "permission_validation_failures" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-rpc/src/config.rs packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/config/tests.rs
    grep -n "count_inbound_permission_validation_failures" packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs packages/open-bitcoin-rpc/src/config/tests.rs
    rg -n "inactive_permission_effects\\.len\\(\\)" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-rpc/src/context/network.rs returns no matches
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status::inbound --no-fail-fast
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission_validation_failure_count_is_config_validation_aggregate -- --nocapture
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc current_inbound_status --no-fail-fast
  </acceptance_criteria>
  <done>Canonical inbound status exposes the two numeric permission aggregates needed by the metric mapper without dynamic dimensions or duplicate metric state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map canonical inbound status to fixed MetricSample values</name>
  <files>packages/open-bitcoin-node/src/metrics.rs, packages/open-bitcoin-node/src/metrics/tests.rs, packages/open-bitcoin-node/src/status/inbound.rs</files>
  <read_first>
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/rust.md
    .planning/phases/97-inbound-metrics-sample-production/97-CONTEXT.md
    .planning/phases/97-inbound-metrics-sample-production/97-RESEARCH.md
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/metrics/tests.rs
    packages/open-bitcoin-node/src/status.rs
    packages/open-bitcoin-node/src/status/inbound.rs
  </read_first>
  <behavior>
    - Test 1: `inbound_metric_samples_return_empty_when_status_unavailable` proves `FieldAvailability::Unavailable` returns an empty vector, per D-03.
    - Test 2: `inbound_metric_samples_map_every_fixed_inbound_kind` proves all 23 inbound `MetricKind` variants are present exactly once with the supplied timestamp and expected numeric values.
    - Test 3: `inbound_metric_samples_use_observation_count_not_inactive_label_count` proves `InboundInactivePermissionEffectCount` equals `inactive_permission_effect_observations` when the inactive label vector has a different length.
    - Test 4: `inbound_metric_samples_are_low_cardinality` proves produced samples contain only kind, value, and timestamp, with no labels or raw runtime material.
  </behavior>
  <action>In `packages/open-bitcoin-node/src/metrics.rs`, add `pub fn inbound_metric_samples(inbound: &FieldAvailability<InboundPeerServingStatus>, timestamp_unix_seconds: u64) -> Vec<MetricSample>`. Import the shared status types from `crate::status`. The function must early-return `Vec::new()` for `FieldAvailability::Unavailable` per D-03 and D-06. For `Available(status)`, return one `MetricSample::new` for each fixed inbound `MetricKind`, using the same timestamp for all samples and `f64::from(...)` conversions from status aggregates. Map `InboundInactivePermissionEffectCount` from `status.inactive_permission_effect_observations`; map `InboundPermissionValidationFailureCount` from `status.permission_validation_failures`; map resource-governance samples from `resource_pressure_events`, `read_queue_pressure_events`, `write_queue_pressure_events`, `request_cap_events`, `payload_rejections`, `timeout_disconnects`, `churn_rejections`, and `reconnect_suppressions`. Do not add new `MetricKind` variants, labels, dimensions, storage access, network access, runtime access, or dashboard code. Keep the mapper pure per D-04, D-05, and D-06.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    grep -n "pub fn inbound_metric_samples" packages/open-bitcoin-node/src/metrics.rs
    grep -n "FieldAvailability::Unavailable" packages/open-bitcoin-node/src/metrics.rs
    grep -n "MetricKind::InboundAdmittedPeerCount" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/metrics/tests.rs
    grep -n "MetricKind::InboundReconnectSuppressedCount" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/metrics/tests.rs
    grep -n "inactive_permission_effect_observations" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/metrics/tests.rs
    grep -n "permission_validation_failures" packages/open-bitcoin-node/src/metrics.rs packages/open-bitcoin-node/src/metrics/tests.rs
    rg -n "peer_id|endpoint|remote_addr|class_name|raw_permission|raw_config|label:" packages/open-bitcoin-node/src/metrics.rs returns no matches
    cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast
  </acceptance_criteria>
  <done>Pure inbound metric sample production is deterministic, low-cardinality, and fully sourced from shared status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/admission validation -> shared status | Validation outcomes become aggregate status evidence. |
| shared inbound status -> metrics mapper | Operator-visible counters become retained numeric metric samples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Information Disclosure | `inbound_metric_samples` | mitigate | Mapper emits only fixed `MetricKind`, numeric value, and timestamp; tests and acceptance grep reject peer ids, endpoints, raw permission strings, and labels. |
| T-97-01-02 | Spoofing | unavailable inbound status | mitigate | `FieldAvailability::Unavailable` returns no samples so absent evidence cannot masquerade as zero-valued availability. |
| T-97-01-03 | Tampering | permission inactive-effect metric | mitigate | Source is `inactive_permission_effect_observations`, not distinct labels; tests use mismatched label length to catch drift. |
| T-97-01-04 | Denial of Service | metric cardinality | mitigate | Use existing fixed `MetricKind` variants only; no dynamic series or runtime-created dimensions. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node status::inbound --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc current_inbound_status --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
INB-05 and DOS-04 have a pure, testable inbound status to MetricSample mapping that covers admission, permission, peer-policy, and resource-governance counters without dynamic labels, raw peer material, or manufactured unavailable evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/97-inbound-metrics-sample-production/97-01-SUMMARY.md`.
</output>
