# Phase 07.1: Codebase Maintainability Refactor Wave - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 07.1-codebase-maintainability-refactor-wave
**Mode:** Yolo
**Areas discussed:** Wave order and scope, tests-only extraction policy, wallet module boundary, script module boundary, related todo handling

---

## Wave order and scope

| Option | Description | Selected |
|--------|-------------|----------|
| Tests-only first, then wallet, then script | Lowest-risk first; structural cleanup prepares the two largest hotspots for focused refactors | ✓ |
| Wallet first | Starts with the smaller of the two major production refactors | |
| Script first | Tackles the highest-risk hotspot immediately | |

**User's choice:** Tests-only first, then wallet, then script  
**Notes:** This was already explicitly chosen in the thread before yolo discuss ran, so the recommendation engine preserved it as a locked decision.

---

## Tests-only extraction policy

| Option | Description | Selected |
|--------|-------------|----------|
| Move inline tests to sibling Rust files only | Keep production code behavior stable; stop after extraction when a file becomes acceptably sized | ✓ |
| Split production code during the same first wave | More aggressive cleanup, higher regression risk | |
| Limit extraction to only the top two files | Smaller first wave, leaves most oversized hotspots unchanged | |

**User's choice:** Move inline tests to sibling Rust files only  
**Notes:** Recommended because most oversized files are oversized largely due to inline test modules, and this keeps the first wave low-risk.

---

## Wallet module boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Keep public wallet API in `wallet.rs`, move internals by responsibility | Preserves downstream imports while isolating scan, build, and sign logic | ✓ |
| Split wallet by data type | Would scatter behavior across types and make high-level flows harder to follow | |
| Keep `wallet.rs` mostly intact after test extraction | Leaves the largest production hotspot oversized | |

**User's choice:** Keep public wallet API in `wallet.rs`, move internals by responsibility  
**Notes:** This aligns with the existing wallet crate re-export surface and the Phase 7 decision to keep wallet behavior pure-core while leaving persistence adapter-owned.

---

## Script module boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Keep public verifier API in `script.rs`, split helpers and execution branches by responsibility | Preserves the canonical script surface while making the file auditable | ✓ |
| Split by opcode family only | Risks obscuring the higher-level verifier flow | |
| Leave production `script.rs` unsplit after test extraction | Leaves the largest hotspot far above the file-size trigger | |

**User's choice:** Keep public verifier API in `script.rs`, split helpers and execution branches by responsibility  
**Notes:** Recommended because earlier consensus phases already locked `verify_input_script` as the single canonical spending-path verifier.

---

## Related todo handling

| Option | Description | Selected |
|--------|-------------|----------|
| Fold only the oversized-files todo into this phase | Matches the phase intent without widening scope | ✓ |
| Fold all matched maintainability todos into this phase | Broadens the phase into multiple cleanup campaigns | |
| Defer all matched todos | Loses the direct linkage between the maintainability phase and its originating todo | |

**User's choice:** Fold only the oversized-files todo into this phase  
**Notes:** The other matched todos were reviewed and deferred because they would widen the phase beyond the agreed structural refactor wave.

---

## the agent's Discretion

- Exact child-file names for the wallet and script module trees
- The precise grouping of low-level script helpers, as long as the public
  surface stays stable and the split improves cohesion

## Deferred Ideas

- AI-agent-friendly CLI surface
- Reduce nesting with early returns
- Sweep magic numbers for clarity
- Sweep panics and illegal states
