---
phase: 07.1-codebase-maintainability-refactor-wave
plan: 02
type: execute
wave: 2
depends_on:
  - "07.1-01"
files_modified:
  - packages/open-bitcoin-wallet/src/wallet.rs
  - packages/open-bitcoin-wallet/src/wallet/build.rs
  - packages/open-bitcoin-wallet/src/wallet/scan.rs
  - packages/open-bitcoin-wallet/src/wallet/sign.rs
  - packages/open-bitcoin-wallet/src/wallet/tests.rs
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.1-2026-04-18T17-01-44
generated_at: 2026-04-18T17:13:04.090Z
must_haves:
  truths:
    - `Wallet`, `BuildRequest`, `BuiltTransaction`, `WalletSnapshot`,
      `WalletUtxo`, and `WalletBalance` remain the wallet crate's public API.
    - Scan, build, and signing logic move into named child files so
      `wallet.rs` becomes an entrypoint rather than a dumping ground.
    - Wallet behavior remains deterministic and pure-core after the split.
  artifacts:
    - packages/open-bitcoin-wallet/src/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet/build.rs
    - packages/open-bitcoin-wallet/src/wallet/scan.rs
    - packages/open-bitcoin-wallet/src/wallet/sign.rs
  key_links:
    - `open-bitcoin-wallet/src/lib.rs` continues to re-export the same wallet
      surface after the internal module split.
---

<objective>
Split `packages/open-bitcoin-wallet/src/wallet.rs` into coherent production
modules without changing the wallet crate's public surface.

Purpose: keep the first wallet implementation auditable and navigable before
Phase 8 layers CLI or RPC interfaces on top of it.
Output: `wallet.rs` as a thin entrypoint plus dedicated child modules for scan,
build, and sign responsibilities.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-CONTEXT.md
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-RESEARCH.md
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-01-PLAN.md
@.planning/phases/07-wallet-core-and-adapters/07-CONTEXT.md
@.planning/phases/07-wallet-core-and-adapters/07-VERIFICATION.md
@packages/open-bitcoin-wallet/src/wallet.rs
@packages/open-bitcoin-wallet/src/lib.rs
@scripts/verify.sh
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-07.1-03:** Wallet crate public API -> downstream wallet and node callers.
- **TB-07.1-04:** Deterministic wallet decisions -> future Phase 8 interface
  adapters.

### Threat Register
- **Threat ID:** T-07.1-03
  - **Category:** Tampering
  - **Component:** wallet behavior
  - **Disposition:** mitigate
  - **Mitigation Plan:** Keep type definitions and public methods stable in
    `wallet.rs`, move implementation only, and preserve the existing wallet test
    corpus.
- **Threat ID:** T-07.1-04
  - **Category:** Elevation of privilege
  - **Component:** pure-core boundary
  - **Disposition:** mitigate
  - **Mitigation Plan:** Do not move persistence, I/O, or runtime concerns into
    the wallet crate while splitting internal modules.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the wallet module topology and move scan logic</name>
  <files>packages/open-bitcoin-wallet/src/wallet.rs, packages/open-bitcoin-wallet/src/wallet/scan.rs</files>
  <read_first>packages/open-bitcoin-wallet/src/wallet.rs, .planning/phases/07-wallet-core-and-adapters/07-CONTEXT.md, .planning/phases/07-wallet-core-and-adapters/07-VERIFICATION.md</read_first>
  <action>Create `packages/open-bitcoin-wallet/src/wallet/scan.rs` and move the scan-oriented logic there: `rescan_chainstate`, `balance`, `is_spendable`, and `spend_height`, along with any scan-only helpers they require. Keep `wallet.rs` as the entry file that owns the public data types and delegates to the new module instead of carrying the full implementation inline.</action>
  <verify>`bash -lc 'test -f packages/open-bitcoin-wallet/src/wallet/scan.rs && rg -n "mod scan;|rescan_chainstate|balance" packages/open-bitcoin-wallet/src/wallet.rs >/dev/null'`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-wallet/src/wallet/scan.rs` exists
    - `packages/open-bitcoin-wallet/src/wallet.rs` contains `mod scan;`
    - `Wallet::rescan_chainstate` and `Wallet::balance` remain callable from `wallet.rs`
  </acceptance_criteria>
  <done>Wallet scan and spendability logic move behind a named module boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Move build and coin-selection logic into a dedicated module</name>
  <files>packages/open-bitcoin-wallet/src/wallet.rs, packages/open-bitcoin-wallet/src/wallet/build.rs</files>
  <read_first>packages/open-bitcoin-wallet/src/wallet.rs, packages/open-bitcoin-wallet/src/wallet/tests.rs</read_first>
  <action>Create `packages/open-bitcoin-wallet/src/wallet/build.rs` and move the transaction-building and selection logic there: `build_transaction`, `input_contexts_for`, `validation_context`, `resolve_change_descriptor`, `estimate_vsize`, `estimate_change_vsize`, `placeholder_input`, `amount_from_sats`, `compare_wallet_utxos`, and `compare_effective_value`. Keep the public `Wallet::build_transaction` entrypoint in `wallet.rs`, but delegate its internals to the new module.</action>
  <verify>`bash -lc 'test -f packages/open-bitcoin-wallet/src/wallet/build.rs && rg -n "mod build;|build_transaction" packages/open-bitcoin-wallet/src/wallet.rs >/dev/null'`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-wallet/src/wallet/build.rs` exists
    - `packages/open-bitcoin-wallet/src/wallet.rs` contains `mod build;`
    - `Wallet::build_transaction` remains defined in `wallet.rs`
  </acceptance_criteria>
  <done>Wallet construction logic moves behind a dedicated build-focused module.</done>
</task>

<task type="auto">
  <name>Task 3: Move signing helpers into a dedicated module and reduce wallet.rs below the trigger</name>
  <files>packages/open-bitcoin-wallet/src/wallet.rs, packages/open-bitcoin-wallet/src/wallet/sign.rs, packages/open-bitcoin-wallet/src/wallet/tests.rs</files>
  <read_first>packages/open-bitcoin-wallet/src/wallet.rs, packages/open-bitcoin-wallet/src/lib.rs, packages/open-bitcoin-wallet/src/wallet/tests.rs</read_first>
  <action>Create `packages/open-bitcoin-wallet/src/wallet/sign.rs` and move the signing implementation there: `sign_transaction`, `build_and_sign`, `sign_ecdsa_low_s`, `push_script`, `p2pkh_script`, and `standard_wallet_verify_flags`. Keep `wallet.rs` responsible for public type definitions, constructors, accessors, and method stubs that delegate to `scan`, `build`, and `sign`. Update `wallet/tests.rs` imports only as needed to follow the new internal layout, and do not change the symbols re-exported by `packages/open-bitcoin-wallet/src/lib.rs`.</action>
  <verify>`bash -lc 'test -f packages/open-bitcoin-wallet/src/wallet/sign.rs && test $(wc -l < packages/open-bitcoin-wallet/src/wallet.rs) -lt 628 && rg -n "mod sign;|pub struct Wallet|pub struct BuildRequest|pub struct BuiltTransaction" packages/open-bitcoin-wallet/src/wallet.rs >/dev/null'`</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-wallet/src/wallet/sign.rs` exists
    - `packages/open-bitcoin-wallet/src/wallet.rs` contains `mod sign;`
    - `wc -l packages/open-bitcoin-wallet/src/wallet.rs` returns a value below `628`
    - `packages/open-bitcoin-wallet/src/lib.rs` still re-exports `BuildRequest`, `BuiltTransaction`, `Wallet`, `WalletBalance`, `WalletSnapshot`, and `WalletUtxo`
  </acceptance_criteria>
  <done>The wallet production hotspot becomes a navigable multi-file module with a stable public surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-wallet --all-targets`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-wallet --all-targets --all-features -- -D warnings`
- [ ] `bash scripts/verify.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `wallet.rs` is below the oversized-file trigger and delegates to named child modules

</success_criteria>

<output>
After completion, create `.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-02-SUMMARY.md`
</output>
