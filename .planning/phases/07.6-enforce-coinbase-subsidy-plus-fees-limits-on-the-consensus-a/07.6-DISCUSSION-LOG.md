# Phase 07.6: Enforce coinbase subsidy-plus-fees limits on the consensus and active chainstate paths - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution
> agents. Decisions are captured in CONTEXT.md; this log preserves the
> alternatives considered.

**Date:** 2026-04-22T10:23:54.928Z
**Phase:** 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
**Mode:** Yolo
**Areas discussed:** Fix boundary, Reward-limit contract, Verification and closeout

---

## Fix boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Limit it to coinbase subsidy-plus-fees enforcement and the minimal supporting plumbing and regressions needed to prove `bad-cb-amount` on the active path | Fix only the reward-limit acceptance hole and the exact proof chain needed to close it. | ✓ |
| Widen into a broader chainstate correctness sweep | Fold in the other chainstate warnings from the latest `07.5` review. | |
| Fold in unrelated 07.5 review warnings | Use this phase to address all remaining follow-up findings from `07.5-REVIEW.md`. | |

**User's choice:** Limit it to coinbase subsidy-plus-fees enforcement and the minimal supporting plumbing and regressions needed to prove `bad-cb-amount` on the active path
**Notes:** Yolo mode keeps `07.6` phase-local and narrow. The `time-too-new` wrapper warning and the transactional `reorg` warning stay deferred instead of being silently folded in.

---

## Reward-limit contract

| Option | Description | Selected |
|--------|-------------|----------|
| Aggregate non-coinbase fees exactly once, enforce `subsidy(height) + total_fees` with `bad-cb-amount`, and reject overpaying coinbases before committing live chainstate | Match Knots reward-limit behavior on both the pure-core and active connect paths. | ✓ |
| Enforce only a pure-core helper and leave live chainstate to a later phase | Close the helper gap now but defer the active path. | |
| Document the gap without fixing the active path | Treat it as known debt instead of an urgent parity defect. | |

**User's choice:** Aggregate non-coinbase fees exactly once, enforce `subsidy(height) + total_fees` with `bad-cb-amount`, and reject overpaying coinbases before committing live chainstate
**Notes:** The active chainstate path is the actual acceptance surface, so this phase should not stop at a helper-only change.

---

## Verification and closeout

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted consensus and chainstate regressions for overpaying coinbases, plus a final truthful summary and `bash scripts/verify.sh` | Close the bug only after the exact reward-limit proof chain passes. | ✓ |
| Repo-native verification only | Trust the broad verification script without phase-specific reward-limit regressions. | |
| Unit tests only | Skip the repo-native contract and broader final proof chain. | |

**User's choice:** Targeted consensus and chainstate regressions for overpaying coinbases, plus a final truthful summary and `bash scripts/verify.sh`
**Notes:** This is the same standard applied to the earlier `07.5` parity fixes: exact proof strings for the targeted gap plus the repo-native verifier before claiming closure.

---

## the agent's Discretion

- Exact helper names and whether subsidy helpers live in `block.rs` or a small sibling module
- Whether the clearest deterministic test uses a halving-interval fixture, a fee-only block, or another minimal coinbase-boundary setup
- The exact summary phrasing used to supersede any earlier “final” `07.5` closeout artifact once the new proof chain lands

## Deferred Ideas

- The `connect_block()` wrapper time-bypass warning from `07.5-REVIEW.md`
- The `disconnect_tip()` / `reorg()` transactional warning from `07.5-REVIEW.md`
- Phase 8 operator-interface work
- AI-agent-friendly CLI surface
