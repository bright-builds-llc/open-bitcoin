---
phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
verified: 2026-04-22T13:30:28Z
status: passed
score: 9/9 must-haves verified
generated_by: gsd-verifier
lifecycle_mode: yolo
phase_lifecycle_id: 07.6-2026-04-22T10-23-54
generated_at: 2026-04-22T13:30:28Z
lifecycle_validated: true
overrides_applied: 0
---

# Phase 07.6: Enforce coinbase subsidy-plus-fees limits on the consensus and active chainstate paths Verification Report

**Phase Goal:** Mirror Knots-style coinbase subsidy-plus-fees reward-limit enforcement on both the pure-core contextual block-validation path and the active chainstate connect path, with focused proof that overpaying coinbases fail before live state commit.
**Verified:** 2026-04-22T13:30:28Z
**Status:** passed
**Re-verification:** No — initial verification of the current tree; `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-VERIFICATION.pre-gap.md` was used as historical context only.

Verification used the repo-local guidance in `AGENTS.md`, `AGENTS.bright-builds.md`, `standards-overrides.md`, the Bright Builds canonical standards pages under `../coding-and-architecture-requirements/standards/`, the phase context/plans/summaries, and the required verifier reference files. The earlier pre-gap report identified the missing `MAX_MONEY` / `MoneyRange` boundary; the current tree closes that gap in both reward-limit paths.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `validate_block_with_context()` rejects coinbases whose outputs exceed `subsidy(height) + total_fees` with `bad-cb-amount`. | ✓ VERIFIED | `packages/open-bitcoin-consensus/src/block.rs:48-79` enforces the subsidy-plus-fees limit and `packages/open-bitcoin-consensus/src/block.rs:310-329` calls it from contextual block validation. The matching Knots behavior remains at `packages/bitcoin-knots/src/validation.cpp:2914-2917` and `2974-2977`. |
| 2 | `connect_block_with_current_time()` applies the same reward-limit rule before committing `self.utxos`, `self.undo_by_block`, or `self.active_chain`. | ✓ VERIFIED | `packages/open-bitcoin-chainstate/src/engine.rs:114-158` stages `next_utxos` and `block_undo`, enforces the reward limit at `144-150`, and only assigns live state at `156-158`. |
| 3 | Focused consensus and chainstate regressions prove exact-fee acceptance, `+1` overpay rejection, and unchanged live state on failure. | ✓ VERIFIED | Consensus proofs exist at `packages/open-bitcoin-consensus/src/block/tests.rs:1428-1458`; live-path proofs exist at `packages/open-bitcoin-chainstate/src/engine/tests.rs:750-802` and `919-977`. The exact tests passed during this verification run. |
| 4 | `bash scripts/verify.sh` passes, and the Phase 07.6 summary cites the new proof strings without reopening already-green Phase 07.5 evidence. | ✓ VERIFIED | `bash scripts/verify.sh` exited `0` in `18.290s` on 2026-04-22. `07.6-02-SUMMARY.md:76-85` and `07.6-03-SUMMARY.md:80-87,126-128` cite the new reward-limit proofs and carry Phase 07.5 evidence by reference. |
| 5 | Consensus-only regressions prove both exact-fee acceptance and `+1` overpay rejection without relying on chainstate. | ✓ VERIFIED | `packages/open-bitcoin-consensus/src/block/tests.rs:1428-1458` covers both outcomes entirely inside the consensus crate. |
| 6 | Deterministic subsidy-halving regressions prove Knots-compatible boundary behavior, including zero subsidy after 64 halvings. | ✓ VERIFIED | `packages/open-bitcoin-consensus/src/context.rs:17-39` adds `subsidy_halving_interval`; `packages/open-bitcoin-consensus/src/block.rs:38-45` implements the halving schedule; `packages/open-bitcoin-consensus/src/block/tests.rs:1377-1394` proves the 64-halving zero-subsidy boundary; `packages/open-bitcoin-consensus/src/context/tests.rs:338-345` confirms the default interval is carried in context. |
| 7 | Pure-core contextual block validation rejects accumulated non-coinbase fees once the running total leaves `0..=MAX_MONEY`, returning `bad-txns-accumulated-fee-outofrange` before an inflated reward limit can be computed. | ✓ VERIFIED | `packages/open-bitcoin-consensus/src/block.rs:316-320` now rejects the running fee total as soon as it leaves `0..=MAX_MONEY`, and `packages/open-bitcoin-consensus/src/block/tests.rs:1462-1492` proves the `MAX_MONEY < total_fees <= i64::MAX` boundary. |
| 8 | The live chainstate connect path applies the same `MAX_MONEY` / `MoneyRange` fee boundary before `enforce_coinbase_reward_limit()` and before staged state is committed. | ✓ VERIFIED | `packages/open-bitcoin-chainstate/src/engine.rs:128-150` mirrors the `MAX_MONEY` boundary before the shared helper runs and before any live-state assignment. |
| 9 | Focused consensus and chainstate regressions prove the `MAX_MONEY < total_fees <= i64::MAX` boundary while preserving the existing exact-limit, `bad-cb-amount`, and unchanged-snapshot proof chain. | ✓ VERIFIED | `packages/open-bitcoin-consensus/src/block/tests.rs:1462-1537` and `packages/open-bitcoin-chainstate/src/engine/tests.rs:806-916` lock in the new boundary, while the existing exact-limit and `bad-cb-amount` proofs remain at `1428-1458` and `919-977`. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `packages/open-bitcoin-consensus/src/context.rs` | Consensus subsidy-halving parameter | ✓ VERIFIED | `ConsensusParams.subsidy_halving_interval` exists at `17-39` with the Knots default `210_000`. |
| `packages/open-bitcoin-consensus/src/block.rs` | Shared subsidy/reward-limit helper and contextual fee aggregation | ✓ VERIFIED | `block_subsidy()` and `enforce_coinbase_reward_limit()` live at `38-79`; `validate_block_with_context()` aggregates fees once and applies both the `MAX_MONEY` boundary and the reward-limit helper at `295-329`. |
| `packages/open-bitcoin-consensus/src/block/tests.rs` | Focused reward-limit regressions | ✓ VERIFIED | The file contains subsidy-halving, exact-limit, `bad-cb-amount`, `MAX_MONEY`, and i64-overflow reward-limit proofs at `1377-1537`. |
| `packages/open-bitcoin-chainstate/src/engine.rs` | Pre-commit chainstate reward-limit enforcement using the shared consensus helper | ✓ VERIFIED | `connect_block_with_current_time()` stages local state, reuses returned fees, enforces the `MAX_MONEY` boundary, and calls the shared helper before live assignment at `114-158`. |
| `packages/open-bitcoin-chainstate/src/engine/tests.rs` | Exact-fee acceptance, overpay rejection, unchanged-snapshot, and `MAX_MONEY` live-path regressions | ✓ VERIFIED | The file proves returned fees, exact-limit acceptance, `bad-cb-amount` rejection, unchanged snapshots, and `MAX_MONEY` boundary handling at `622-977`. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `packages/open-bitcoin-consensus/src/transaction.rs` | `packages/open-bitcoin-consensus/src/block.rs` | `validate_transaction_with_context()` fee return is aggregated exactly once in `validate_block_with_context()` | WIRED | `block.rs:314-322` consumes the returned `Amount` once per non-coinbase transaction. |
| `packages/open-bitcoin-consensus/src/context.rs` | `packages/open-bitcoin-consensus/src/block.rs` | `ConsensusParams.subsidy_halving_interval` drives Knots-style subsidy calculation | WIRED | `context.rs:17-39` defines/defaults the field and `block.rs:39-44` uses it in `block_subsidy()`. |
| `packages/open-bitcoin-consensus/src/block.rs` | `packages/open-bitcoin-consensus/src/block.rs` | The running contextual fee total is range-checked before reward comparison | WIRED | `block.rs:316-320` rejects on `MAX_MONEY` overflow before `enforce_coinbase_reward_limit()` is called at `324-329`. |
| `packages/open-bitcoin-chainstate/src/engine.rs` | `packages/open-bitcoin-consensus/src/block.rs` | Shared consensus reward-limit helper imported from the consensus crate | WIRED | `engine.rs:3` imports `enforce_coinbase_reward_limit`, and `engine.rs:144-150` calls it. |
| `packages/open-bitcoin-chainstate/src/engine.rs` | `packages/open-bitcoin-chainstate/src/engine.rs` | Reward-limit checks run before `self.utxos`, `self.undo_by_block`, and `self.active_chain` assignment | WIRED | `engine.rs:128-150` performs the `MAX_MONEY` and reward-limit checks before the live assignments at `156-158`. |
| `packages/open-bitcoin-chainstate/src/engine/tests.rs` | `packages/open-bitcoin-chainstate/src/engine.rs` | Live-path regressions prove `bad-txns-accumulated-fee-outofrange` and unchanged snapshots | WIRED | `engine/tests.rs:806-916` asserts the exact reject reason and `snapshot_before == snapshot_after`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `packages/open-bitcoin-consensus/src/block.rs` | `total_fees_sats` | `validate_transaction_with_context()` over the supplied `TransactionValidationContext.inputs` | Yes | ✓ FLOWING |
| `packages/open-bitcoin-chainstate/src/engine.rs` | `fee` / `total_fees_sats` | `apply_non_coinbase_transaction()` returns the validated fee from `validate_transaction_with_context()` over staged `next_utxos` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Pure-core exact-limit acceptance | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_accepts_exact_coinbase_reward_limit -- --exact` | `1 passed` | ✓ PASS |
| Pure-core `bad-cb-amount` overpay rejection | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_coinbase_overpay_with_bad_cb_amount -- --exact` | `1 passed` | ✓ PASS |
| Pure-core `MAX_MONEY` boundary rejection | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_accumulated_fees_above_max_money -- --exact` | `1 passed` | ✓ PASS |
| Live-path exact-limit acceptance | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_accepts_exact_coinbase_reward_limit -- --exact` | `1 passed` | ✓ PASS |
| Live-path `bad-cb-amount` rejection preserves snapshot | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact` | `1 passed` | ✓ PASS |
| Live-path `MAX_MONEY` rejection preserves snapshot | `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot -- --exact` | `1 passed` | ✓ PASS |
| Repo-native verification contract | `bash scripts/verify.sh` | Exit `0`; completed in `18.290s` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `CONS-02` | `07.6-01`, `07.6-02`, `07.6-03` | The node validates scripts, transactions, and blocks with consensus behavior matching the pinned baseline. | ✓ SATISFIED | Knots applies `MoneyRange(nFees)` before reward comparison at `validation.cpp:2914-2917` and rejects overpaying coinbases at `2974-2977`; Open Bitcoin now mirrors both rules at `block.rs:316-329` and `engine.rs:128-150`, with passing consensus and chainstate regressions. |
| `CONS-03` | `07.6-01`, `07.6-02`, `07.6-03` | Automated fixtures surface any consensus mismatch with the baseline before merge. | ✓ SATISFIED | `block/tests.rs:1428-1537` and `engine/tests.rs:750-977` now cover exact-limit acceptance, `bad-cb-amount`, `MAX_MONEY`, and unchanged snapshots, and the targeted tests all passed. |
| `VER-01` | `07.6-02`, `07.6-03` | Contributors can run a repo-native verification flow that enforces formatting, linting, build, tests, and architecture-policy checks for changed paths. | ✓ SATISFIED | `bash scripts/verify.sh` exited `0` on the current tree during this verification pass. |
| `VER-02` | `07.6-01`, `07.6-02`, `07.6-03` | CI fails when pure-core packages lose 100% unit-test coverage or leak forbidden I/O/runtime dependencies. | ✓ SATISFIED | `bash scripts/verify.sh` reported pure-core dependency/import checks passed and the pure-core workspace test/coverage gates remained green. |

All requirement IDs declared in the 07.6 plan frontmatter (`CONS-02`, `CONS-03`, `VER-01`, `VER-02`) are present in `.planning/REQUIREMENTS.md` and were accounted for above. No orphaned Phase `07.6` requirement IDs were found in the requirements traceability table.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `packages/open-bitcoin-consensus/src/context.rs` | `363-378` | Unchecked `i64` accumulation and `.sum()` in `check_tx_inputs()` | ⚠️ Warning | Residual fee-source overflow risk noted in `07.6-REVIEW.md`; it does not negate the closed 07.6 reward-limit path, but it remains a separate consensus correctness follow-up. |

No TODO/FIXME/placeholder markers or stub-style implementations were found in the scoped 07.6 reward-limit artifacts.

---

_Verified: 2026-04-22T13:30:28Z_
_Verifier: the agent (gsd-verifier)_
