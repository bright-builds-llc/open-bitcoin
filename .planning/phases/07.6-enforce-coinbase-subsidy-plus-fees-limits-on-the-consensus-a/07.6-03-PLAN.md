---
phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
plan: 03
type: execute
wave: 3
depends_on:
  - 07.6-01
  - 07.6-02
files_modified:
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
autonomous: true
gap_closure: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.6-2026-04-22T10-23-54
generated_at: 2026-04-22T12:55:01Z
must_haves:
  truths:
    - "Pure-core contextual block validation rejects accumulated non-coinbase fees once the running total leaves `0..=MAX_MONEY`, returning `bad-txns-accumulated-fee-outofrange` before an inflated reward limit can be computed."
    - "The live chainstate connect path applies the same `MAX_MONEY` / MoneyRange fee boundary before `enforce_coinbase_reward_limit()` and before staged state is committed."
    - "Focused consensus and chainstate regressions prove the `MAX_MONEY < total_fees <= i64::MAX` boundary while preserving the existing exact-limit, `bad-cb-amount`, and unchanged-snapshot proof chain."
  artifacts:
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
  key_links:
    - "`packages/open-bitcoin-consensus/src/block.rs` reuses the existing `validate_transaction_with_context()` fee return, then rejects once the running total leaves `0..=MAX_MONEY` with `bad-txns-accumulated-fee-outofrange`."
    - "`packages/open-bitcoin-chainstate/src/engine.rs` mirrors that boundary on the fee returned by `apply_non_coinbase_transaction()` before calling `enforce_coinbase_reward_limit()`."
    - "`packages/open-bitcoin-chainstate/src/engine/tests.rs` proves the `MAX_MONEY` boundary maps through `ChainstateError::BlockValidation` and leaves `Chainstate::snapshot()` unchanged."
---

<objective>
Close the remaining verifier-confirmed `MAX_MONEY` / Knots `MoneyRange(nFees)` gap in the existing Phase 07.6 reward-limit implementation.

Purpose: align both fee accumulators with Knots at the exact fee-range boundary without reopening the already-green exact-limit, `bad-cb-amount`, or unrelated 07.5 and 07.6 work.
Output: `MAX_MONEY`-guarded fee accumulators in `block.rs` and `engine.rs`, plus focused regressions proving `bad-txns-accumulated-fee-outofrange` for `MAX_MONEY < total_fees <= i64::MAX`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-CONTEXT.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-RESEARCH.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-VERIFICATION.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-01-SUMMARY.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/validation.cpp
@packages/open-bitcoin-primitives/src/amount.rs
@packages/open-bitcoin-consensus/src/block.rs
@packages/open-bitcoin-consensus/src/block/tests.rs
@packages/open-bitcoin-chainstate/src/engine.rs
@packages/open-bitcoin-chainstate/src/engine/tests.rs
@scripts/verify.sh

<interfaces>
From `packages/open-bitcoin-primitives/src/amount.rs`:
```rust
pub const MAX_MONEY: i64 = 21_000_000 * COIN;
```

From `packages/open-bitcoin-consensus/src/block.rs`:
```rust
pub fn enforce_coinbase_reward_limit(
    block: &Block,
    height: u32,
    total_fees_sats: i64,
    consensus_params: &ConsensusParams,
) -> Result<(), BlockValidationError>;

pub fn validate_block_with_context(
    block: &Block,
    transaction_contexts: &[TransactionValidationContext],
    block_context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;

fn accumulated_fee_out_of_range() -> BlockValidationError;
```

From `packages/open-bitcoin-chainstate/src/engine.rs`:
```rust
pub fn connect_block_with_current_time(
    &mut self,
    block: &Block,
    chain_work: u128,
    current_time: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ChainstateError>;

fn accumulated_fee_out_of_range() -> ChainstateError;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the Knots `MoneyRange` / `MAX_MONEY` fee boundary in pure-core block validation</name>
  <files>
    packages/open-bitcoin-consensus/src/block.rs
    packages/open-bitcoin-consensus/src/block/tests.rs
  </files>
  <read_first>
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - .planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-VERIFICATION.md
    - .planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-01-SUMMARY.md
    - packages/bitcoin-knots/src/validation.cpp
    - packages/open-bitcoin-primitives/src/amount.rs
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - ../coding-and-architecture-requirements/standards/core/testing.md
    - ../coding-and-architecture-requirements/standards/languages/rust.md
  </read_first>
  <behavior>
    - `validate_block_with_context()` rejects a block once the running non-coinbase fee total exceeds `MAX_MONEY`, even when the sum still fits in `i64`.
    - The reject reason is `bad-txns-accumulated-fee-outofrange`, not `bad-cb-amount`, because the fee total itself is out of range before reward comparison.
    - The existing exact-limit and `bad-cb-amount` proofs remain green for in-range fee totals.
  </behavior>
  <action>Per D-01 through D-04 and the first verifier gap, keep the existing `validate_transaction_with_context()` fee source and the existing `accumulated_fee_out_of_range()` helper, but change `packages/open-bitcoin-consensus/src/block.rs` so the running `total_fees_sats` is rejected as soon as it leaves the Knots `MoneyRange` boundary `0..=MAX_MONEY`, not only when `checked_add` overflows `i64`. Keep `enforce_coinbase_reward_limit()` and the already-green `bad-cb-amount` path intact for in-range totals; do not reopen `context.rs`, `transaction.rs`, or the advisory 07.6 `context.rs` review finding. In `packages/open-bitcoin-consensus/src/block/tests.rs`, add a focused regression named `validate_block_with_context_rejects_accumulated_fees_above_max_money` that builds `MAX_MONEY < total_fees <= i64::MAX`, asserts `BlockValidationResult::Consensus`, and checks the exact reject reason `bad-txns-accumulated-fee-outofrange`. Keep the existing i64-overflow-only proof only if it still adds value after the MoneyRange boundary test; do not delete the existing exact-limit or `bad-cb-amount` reward-limit proofs.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_accumulated_fees_above_max_money -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_coinbase_overpay_with_bad_cb_amount -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAX_MONEY|bad-txns-accumulated-fee-outofrange" packages/open-bitcoin-consensus/src/block.rs packages/open-bitcoin-consensus/src/block/tests.rs` shows the new `MAX_MONEY` guard and the boundary regression in the modified files.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_accumulated_fees_above_max_money -- --exact` exits 0.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_coinbase_overpay_with_bad_cb_amount -- --exact` stays green, proving the in-range overpay path still reports `bad-cb-amount`.
  </acceptance_criteria>
  <done>Pure-core block validation now matches Knots on the fee-total MoneyRange boundary and exposes it through a targeted regression instead of relying on i64 overflow alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the `MAX_MONEY` fee boundary on the live chainstate path before commit</name>
  <files>
    packages/open-bitcoin-chainstate/src/engine.rs
    packages/open-bitcoin-chainstate/src/engine/tests.rs
  </files>
  <read_first>
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-VERIFICATION.md
    - .planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-primitives/src/amount.rs
    - scripts/verify.sh
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - ../coding-and-architecture-requirements/standards/core/verification.md
    - ../coding-and-architecture-requirements/standards/core/testing.md
    - ../coding-and-architecture-requirements/standards/languages/rust.md
  </read_first>
  <behavior>
    - `connect_block_with_current_time()` rejects `MAX_MONEY < total_fees <= i64::MAX` with `ChainstateError::BlockValidation { source.reject_reason == "bad-txns-accumulated-fee-outofrange" }`.
    - The rejected live-path attempt leaves `Chainstate::snapshot()` unchanged because the boundary is enforced before `self.utxos`, `self.undo_by_block`, and `self.active_chain` are assigned.
    - `bash scripts/verify.sh` passes, and the resulting `07.6-03-SUMMARY.md` can cite the new MoneyRange proof strings while carrying forward `07.6-01-SUMMARY.md` and `07.6-02-SUMMARY.md` by reference instead of rewriting them.
  </behavior>
  <action>Per D-01, D-02, D-04, D-05, D-06, and D-07 plus the second verifier gap, keep using the fee returned by `apply_non_coinbase_transaction()` in `packages/open-bitcoin-chainstate/src/engine.rs`, but reject the running `total_fees_sats` as soon as it leaves `0..=MAX_MONEY` instead of waiting for `i64` overflow. Use the existing local `accumulated_fee_out_of_range()` mapping, enforce the boundary before calling `enforce_coinbase_reward_limit()`, and leave the existing staged `next_utxos` / `block_undo` / `position` flow intact. Do not widen into the deferred `connect_block()` wrapper time-bypass warning, `reorg()` safety work, or any `context.rs` changes. In `packages/open-bitcoin-chainstate/src/engine/tests.rs`, add a focused regression named `connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot` that creates `MAX_MONEY < total_fees <= i64::MAX`, asserts `bad-txns-accumulated-fee-outofrange` through `ChainstateError::BlockValidation`, and proves `snapshot_before == snapshot_after`. After the targeted tests pass, rerun `bash scripts/verify.sh` so the final plan summary can cite the exact new proof strings honestly.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAX_MONEY|bad-txns-accumulated-fee-outofrange" packages/open-bitcoin-chainstate/src/engine.rs packages/open-bitcoin-chainstate/src/engine/tests.rs` shows the live-path `MAX_MONEY` guard and the new boundary regression in the modified files.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot -- --exact` exits 0.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact` stays green, proving the existing `bad-cb-amount` and unchanged-snapshot path still holds for in-range totals.
    - `bash scripts/verify.sh` exits 0 on the final tree.
  </acceptance_criteria>
  <done>The live chainstate path now matches Knots on the fee-total MoneyRange boundary before commit, and the phase can close the exact remaining gap without rewriting the earlier 07.6 evidence.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if either accumulator can still feed `enforce_coinbase_reward_limit()` with `total_fees_sats > MAX_MONEY`, or if the new live-path boundary test does not prove the snapshot stays unchanged, stop and leave Phase 07.6 open.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted block transactions -> consensus fee accumulator | Non-coinbase fees come from untrusted block data and must stay inside Knots `MoneyRange` before reward comparison. |
| staged chainstate validation -> live state commit | The live path must reject out-of-range fee totals before staged UTXO, undo, and chain-tip state is committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-05 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Reject the running fee total once it leaves `0..=MAX_MONEY` and pin the exact reject reason with `validate_block_with_context_rejects_accumulated_fees_above_max_money`. |
| T-07.6-06 | T | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Mirror the same `MAX_MONEY` boundary before `enforce_coinbase_reward_limit()`, and prove `bad-txns-accumulated-fee-outofrange` plus unchanged snapshots with `connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot`. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_accumulated_fees_above_max_money -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_accumulated_fees_above_max_money_without_mutating_snapshot -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- Both Phase 07.6 fee accumulators reject `MAX_MONEY < total_fees <= i64::MAX` with `bad-txns-accumulated-fee-outofrange` before any inflated reward limit is computed.
- The live chainstate path enforces that boundary before commit and preserves `Chainstate::snapshot()` on failure.
- `07.6-03-SUMMARY.md` can cite the new MoneyRange proof strings and a passing `bash scripts/verify.sh` while carrying forward `07.6-01-SUMMARY.md` and `07.6-02-SUMMARY.md` by reference.
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-03-SUMMARY.md`
</output>
