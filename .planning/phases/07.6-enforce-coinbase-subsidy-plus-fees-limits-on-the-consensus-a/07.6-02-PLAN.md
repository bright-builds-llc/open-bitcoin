---
phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
plan: 02
type: execute
wave: 2
depends_on:
  - 07.6-01
files_modified:
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
autonomous: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
must_haves:
  truths:
    - "The active chainstate connect path rejects an overpaying coinbase with `bad-cb-amount` before live state is committed."
    - "A failed live-path reward-limit check leaves the chainstate snapshot unchanged."
    - "Phase 07.6 closeout evidence cites the new reward-limit proofs plus `bash scripts/verify.sh` without reopening already-green 07.5 evidence."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/engine.rs
      provides: "Pre-commit chainstate reward-limit enforcement using the shared consensus helper"
      contains: "connect_block_with_current_time"
    - path: packages/open-bitcoin-chainstate/src/engine/tests.rs
      provides: "Exact-fee acceptance, overpay rejection, and unchanged-snapshot regressions"
      contains: "bad-cb-amount"
  key_links:
    - from: packages/open-bitcoin-chainstate/src/engine.rs
      to: packages/open-bitcoin-consensus/src/block.rs
      via: "shared consensus reward-limit helper imported from the consensus crate"
      pattern: "bad-cb-amount"
    - from: packages/open-bitcoin-chainstate/src/engine.rs
      to: packages/open-bitcoin-chainstate/src/engine.rs
      via: "reward-limit check runs before `self.utxos`, `self.undo_by_block`, and `self.active_chain` assignment"
      pattern: "self.utxos = next_utxos"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.6-2026-04-22T10-23-54
generated_at: 2026-04-22T11:28:30Z
---

<objective>
Wire the consensus-owned reward-limit contract into the live chainstate connect path and close the phase with truthful proof strings.

Purpose: Satisfy D-05 through D-07 by making the active block-connect path enforce `subsidy(height) + total_fees` before committing staged state, then capture the narrow reward-limit evidence chain without reopening deferred 07.5 warnings.
Output: Updated chainstate fee aggregation, unchanged-snapshot live-path regressions, and verification evidence ready for the plan summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-CONTEXT.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-RESEARCH.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
@packages/open-bitcoin-chainstate/src/engine.rs
@packages/open-bitcoin-chainstate/src/engine/tests.rs
@packages/open-bitcoin-consensus/src/transaction.rs
@packages/open-bitcoin-consensus/src/block.rs

<interfaces>
From `packages/open-bitcoin-chainstate/src/engine.rs`:
```rust
pub fn connect_block_with_current_time(
    &mut self,
    block: &Block,
    chain_work: u128,
    current_time: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ChainstateError>;

fn apply_non_coinbase_transaction(
    next_utxos: &mut HashMap<OutPoint, Coin>,
    block_undo: &mut BlockUndo,
    transaction: &Transaction,
    block_time: i64,
    verify_flags: ScriptVerifyFlags,
    block_context: &BlockValidationContext,
) -> Result<(), ChainstateError>;
```

From `packages/open-bitcoin-chainstate/src/error.rs`:
```rust
pub enum ChainstateError {
    BlockValidation { source: BlockValidationError },
    TransactionValidation { source: TxValidationError },
    // ...
}
```

Target contract introduced by `07.6-01`:
```rust
pub fn enforce_coinbase_reward_limit(
    block: &Block,
    height: u32,
    total_fees: Amount,
    params: &ConsensusParams,
) -> Result<(), BlockValidationError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return non-coinbase fees and enforce the reward limit before commit</name>
  <read_first>
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/error.rs
    - packages/open-bitcoin-consensus/src/transaction.rs
    - packages/open-bitcoin-consensus/src/block.rs
  </read_first>
  <files>
    packages/open-bitcoin-chainstate/src/engine.rs
    packages/open-bitcoin-chainstate/src/engine/tests.rs
  </files>
  <behavior>
    - Test 1: `apply_non_coinbase_transaction()` returns the fee from `validate_transaction_with_context()` while still consuming inputs and recording undo data.
    - Test 2: `connect_block_with_current_time()` maps an overpaying coinbase to `ChainstateError::BlockValidation` with reject reason `bad-cb-amount`.
  </behavior>
  <acceptance_criteria>
    - `apply_non_coinbase_transaction()` returns the validated fee instead of recomputing it, satisfying D-04.
    - `connect_block_with_current_time()` accumulates those fees with checked arithmetic and calls the shared consensus helper before `self.utxos`, `self.undo_by_block`, or `self.active_chain` are assigned, satisfying D-05.
    - The task does not modify the deferred local `connect_block()` time-bypass warning or the deferred `reorg()` transactional warning from 07.5.
  </acceptance_criteria>
  <action>Per D-02, D-04, and D-05, change `apply_non_coinbase_transaction()` in `packages/open-bitcoin-chainstate/src/engine.rs` to return `Result<Amount, ChainstateError>` after calling `validate_transaction_with_context()`, then have `connect_block_with_current_time()` accumulate `total_fees` with checked addition and call the shared helper from `07.6-01` before any assignment to `self.utxos`, `self.undo_by_block`, or `self.active_chain`. Map helper failures through `ChainstateError::BlockValidation { source }`, keep the existing staged `next_utxos` and `block_undo` pattern intact, and do not widen scope into the deferred `connect_block()` wrapper or `reorg()` safety work.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::apply_non_coinbase_transaction_returns_fee_and_records_undo -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_maps_coinbase_overpay_to_block_validation_error -- --exact</automated>
  </verify>
  <done>The live connect path now consumes the shared consensus reward-limit contract and still stages mutations locally until the check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove unchanged live state on failure and close the reward-limit evidence chain</name>
  <read_first>
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
    - scripts/verify.sh
  </read_first>
  <files>
    packages/open-bitcoin-chainstate/src/engine/tests.rs
  </files>
  <behavior>
    - Test 1: A block whose coinbase pays exactly `subsidy(height) + total_fees` connects successfully on the live chainstate path.
    - Test 2: The same block with a `+1` satoshi coinbase overpay fails with `bad-cb-amount`.
    - Test 3: The snapshot before the failed connect attempt matches the snapshot after the failure exactly.
  </behavior>
  <acceptance_criteria>
    - The live-path regression proves exact-fee acceptance, +1 overpay rejection, and unchanged snapshot state per D-06.
    - `bash scripts/verify.sh` passes on the final tree, satisfying D-07 and `VER-01`.
    - The resulting summary is ready to cite the new reward-limit proof strings while explicitly carrying forward 07.5’s already-green time/difficulty/lax-DER evidence by reference instead of rewriting it.
  </acceptance_criteria>
  <action>Add focused chainstate regressions in `packages/open-bitcoin-chainstate/src/engine/tests.rs` for the exact-fee pass path and the `+1` satoshi overpay fail path, asserting that the error comes back as `bad-cb-amount` and that `Chainstate::snapshot()` is identical before and after the rejected attempt. After the targeted tests pass, run the repo-native verifier `bash scripts/verify.sh`. In the summary for this plan, list the new exact test names and `bash scripts/verify.sh`, then point back to `07.5-04-SUMMARY.md` for the already-green contextual-difficulty and lax-DER proof chain instead of reopening it.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_accepts_exact_coinbase_reward_limit -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact
bash scripts/verify.sh</automated>
  </verify>
  <done>The live chainstate path is parity-correct for subsidy-plus-fees enforcement, the unchanged-snapshot guard is proven, and the summary can close 07.6 without touching deferred 07.5 warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consensus-owned reward-limit helper → live chainstate commit | An untrusted block crosses from staged validation into the state-commit boundary and must be rejected before live mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-03 | T | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Call the shared consensus reward-limit helper before assigning `self.utxos`, `self.undo_by_block`, or `self.active_chain`, and regress unchanged snapshots on failure. |
| T-07.6-04 | D | `packages/open-bitcoin-chainstate/src/engine.rs` fee accumulator | mitigate | Reuse the validated fee returned by `apply_non_coinbase_transaction()`, use checked addition, and run targeted live-path tests plus `bash scripts/verify.sh`. |
</threat_model>

<verification>
- The targeted `engine::tests::*coinbase_reward_limit*` proofs pass exactly.
- `bash scripts/verify.sh` passes on the final tree.
- The plan summary cites only the new reward-limit proofs plus the carried-forward `07.5-04-SUMMARY.md` references for already-green evidence.
</verification>

<success_criteria>
- `connect_block_with_current_time()` rejects an overpaying coinbase with `bad-cb-amount` before live state commit.
- A rejected live-path connect leaves the chainstate snapshot unchanged.
- The final verification chain contains the exact new test names and a passing `bash scripts/verify.sh`, with no scope creep into deferred 07.5 warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md`
</output>
