---
phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
plan: "02"
subsystem: chainstate
tags: [rust, chainstate, consensus, coinbase, fees]
requires: [07.6-01]
provides:
  - live chainstate reward-limit enforcement before staged state commit
  - fee-return plumbing from non-coinbase transaction application into the live block-fee accumulator
  - exact-limit and unchanged-snapshot regressions for `bad-cb-amount` on the live path
affects: [07.6-closeout, chainstate, consensus-parity, verification]
tech-stack:
  added: []
  patterns:
    - shared consensus reward checks stay in the consensus crate and are invoked from chainstate before live assignment
    - live-path failure regressions compare `Chainstate::snapshot()` before and after the rejected connect attempt
key-files:
  created:
    - .planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md
  modified:
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
key-decisions:
  - "Returned the validated `Amount` from `apply_non_coinbase_transaction()` instead of adding a second fee-calculation path."
  - "Ran `enforce_coinbase_reward_limit()` after staged UTXO and undo updates but before `self.utxos`, `self.undo_by_block`, or `self.active_chain` assignment."
  - "Carried forward the already-green 07.5 contextual-difficulty and lax-DER evidence by reference to `07.5-04-SUMMARY.md` instead of reopening it here."
patterns-established:
  - "Live chainstate monetary checks should reuse the consensus-owned helper and map failures through `ChainstateError::BlockValidation`."
  - "When repo coverage gates expose a narrow new error helper, add the smallest direct regression instead of widening production logic."
requirements-completed: [CONS-02, CONS-03, VER-01, VER-02]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.6-2026-04-22T10-23-54
generated_at: 2026-04-22T12:20:07Z
duration: 2m 7s
completed: 2026-04-22
---

# Phase 07.6 Plan 02 Summary

**Live chainstate block connection now reuses the consensus reward-limit helper, rejects `bad-cb-amount` before commit, and proves snapshot stability on failure**

## Performance

- **Duration:** 2m 7s
- **Started:** 2026-04-22T12:18:00Z
- **Completed:** 2026-04-22T12:20:07Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Returned validated non-coinbase fees from `apply_non_coinbase_transaction()` and accumulated them once on the live connect path.
- Enforced `subsidy(height) + total_fees` through `open_bitcoin_consensus::block::enforce_coinbase_reward_limit(...)` before staged chainstate writes are committed.
- Added exact-limit acceptance, `+1` overpay rejection, and unchanged-snapshot proofs for the live chainstate path.

## Task Commits

Each task was committed atomically:

1. **Task 1: Return non-coinbase fees and enforce the reward limit before commit** - `56c63c0` (`fix`)
2. **Task 2: Prove unchanged live state on failure and close the reward-limit evidence chain** - `8f249b9` (`test`)

## Files Created/Modified

- `packages/open-bitcoin-chainstate/src/engine.rs` - returns validated fees from the non-coinbase apply helper, accumulates them with checked addition, and calls the shared consensus reward-limit helper before live assignment.
- `packages/open-bitcoin-chainstate/src/engine/tests.rs` - proves fee return, `bad-cb-amount` mapping, exact-limit acceptance, unchanged snapshots on failure, and direct overflow error mapping.
- `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md` - authoritative closeout artifact for the live chainstate reward-limit work.

## Decisions Made

- Kept the reward-limit contract in the consensus crate and imported it into chainstate instead of re-implementing reward math in `engine.rs`.
- Preserved the existing staged `next_utxos` and `block_undo` pattern so the rejected live connect path still leaves `Chainstate::snapshot()` unchanged.
- Added a direct chainstate regression for the overflow error helper after `verify.sh` flagged the new lines as uncovered, instead of inventing a large live overflow fixture in this phase.

## Verification Evidence

- New reward-limit proof strings for this plan:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::apply_non_coinbase_transaction_returns_fee_and_records_undo -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_maps_coinbase_overpay_to_block_validation_error -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_accepts_exact_coinbase_reward_limit -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_without_mutating_snapshot -- --exact`
- `bash scripts/verify.sh`
- Repo-required pre-commit verification was also run before each commit: `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo clippy --manifest-path packages/Cargo.toml --workspace --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features`, and `cargo test --manifest-path packages/Cargo.toml --workspace --all-features`.
- The already-green contextual-difficulty and lax-DER proof chain remains carried forward by reference from `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md`; this plan does not reopen that evidence.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added direct chainstate coverage for the accumulated-fee overflow mapping**
- **Found during:** Task 1 verification
- **Issue:** `bash scripts/verify.sh` flagged the new `accumulated_fee_out_of_range()` helper in `packages/open-bitcoin-chainstate/src/engine.rs` as uncovered.
- **Fix:** Added `accumulated_fee_out_of_range_maps_to_block_validation_error` in `packages/open-bitcoin-chainstate/src/engine/tests.rs` to lock the live-path reject reason without widening production scope.
- **Files modified:** `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** `bash scripts/verify.sh`
- **Committed in:** `56c63c0`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The auto-fix was required by the repo-native coverage gate and stayed inside the planned chainstate reward-limit surface.

## Issues Encountered

- The repo-managed verification hook reran the full `scripts/verify.sh` contract on each commit, so task commits needed to absorb coverage fixes before they could land.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 07.6 now has both sides of the reward-limit proof chain: `07.6-01-SUMMARY.md` closes the pure-core contract and this summary closes the live chainstate connect path.
- `CONS-02`, `CONS-03`, `VER-01`, and `VER-02` now have exact reward-limit proof strings plus a passing `bash scripts/verify.sh` on the final tree.
- The deferred 07.5 time-bypass and reorg warnings remain untouched, and the already-green contextual-difficulty and lax-DER evidence stays anchored in `07.5-04-SUMMARY.md`.

## Threat Flags

None - the plan closed the staged-to-live reward-limit trust boundary without adding new endpoints, auth flows, file access patterns, or schema changes.

## Closeout Metadata
*Phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a*
*Completed: 2026-04-22*

## Self-Check: PASSED

- Found `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-02-SUMMARY.md`
- Found commit `56c63c0`
- Found commit `8f249b9`
- No stub or placeholder markers found in the touched plan files
