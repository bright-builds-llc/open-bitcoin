---
phase: 07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/context/tests.rs
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
autonomous: true
requirements:
  - CONS-02
  - CONS-03
  - VER-02
must_haves:
  truths:
    - "Pure-core contextual block validation rejects a coinbase that pays more than subsidy(height) plus total fees with `bad-cb-amount`."
    - "Consensus-only regressions prove both exact-fee acceptance and +1 overpay rejection without relying on chainstate."
    - "Subsidy math is configurable from `ConsensusParams` so deterministic tiny-halving fixtures can exercise the boundary."
  artifacts:
    - path: packages/open-bitcoin-consensus/src/context.rs
      provides: "Consensus subsidy-halving parameter"
      contains: "subsidy_halving_interval"
    - path: packages/open-bitcoin-consensus/src/block.rs
      provides: "Shared subsidy and coinbase reward-limit helper plus fee aggregation in `validate_block_with_context()`"
      contains: "bad-cb-amount"
    - path: packages/open-bitcoin-consensus/src/block/tests.rs
      provides: "Focused reward-limit regressions"
      contains: "validate_block_with_context"
  key_links:
    - from: packages/open-bitcoin-consensus/src/transaction.rs
      to: packages/open-bitcoin-consensus/src/block.rs
      via: "`validate_transaction_with_context()` fee return is aggregated exactly once in `validate_block_with_context()`"
      pattern: "validate_transaction_with_context"
    - from: packages/open-bitcoin-consensus/src/context.rs
      to: packages/open-bitcoin-consensus/src/block.rs
      via: "`ConsensusParams.subsidy_halving_interval` drives Knots-style subsidy calculation"
      pattern: "subsidy_halving_interval"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.6-2026-04-22T10-23-54
generated_at: 2026-04-22T11:28:30Z
---

<objective>
Add the pure-core subsidy-plus-fees contract that Phase 07.6 needs before the live chainstate path can consume it.

Purpose: Close the consensus-side half of CR-01 by matching Knots `GetBlockSubsidy()` behavior and wiring block-level fee aggregation into `validate_block_with_context()` per D-03 and D-04.
Output: Updated consensus params, one shared reward-limit helper in `block.rs`, and focused pure-core regressions for exact-fee and overpay boundaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-CONTEXT.md
@.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-RESEARCH.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/validation.h
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-consensus/src/transaction.rs
@packages/open-bitcoin-consensus/src/block.rs

<interfaces>
From `packages/open-bitcoin-consensus/src/context.rs`:
```rust
pub struct ConsensusParams {
    pub coinbase_maturity: u32,
    pub locktime_threshold: u32,
    pub sequence_locktime_granularity: u32,
    pub pow_limit_bits: u32,
    pub pow_target_spacing_seconds: i64,
    pub pow_target_timespan_seconds: i64,
    pub allow_min_difficulty_blocks: bool,
    pub no_pow_retargeting: bool,
    pub enforce_bip34_height_in_coinbase: bool,
    pub enforce_bip113_median_time_past: bool,
    pub enforce_segwit: bool,
}

pub struct BlockValidationContext {
    pub height: u32,
    pub previous_header: BlockHeader,
    pub maybe_retarget_anchor: Option<RetargetAnchor>,
    pub maybe_min_difficulty_recovery_target: Option<MinDifficultyRecoveryTarget>,
    pub previous_median_time_past: i64,
    pub current_time: i64,
    pub consensus_params: ConsensusParams,
}
```

From `packages/open-bitcoin-consensus/src/transaction.rs`:
```rust
pub fn check_tx_inputs(
    transaction: &Transaction,
    context: &TransactionValidationContext,
) -> Result<Amount, TxValidationError>;

pub fn validate_transaction_with_context(
    transaction: &Transaction,
    context: &TransactionValidationContext,
) -> Result<Amount, TxValidationError>;
```

From `packages/open-bitcoin-consensus/src/block.rs`:
```rust
pub fn validate_block_with_context(
    block: &Block,
    transaction_contexts: &[TransactionValidationContext],
    block_context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Knots-style subsidy contract and shared reward-limit entrypoint</name>
  <read_first>
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/context/tests.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/bitcoin-knots/src/validation.cpp
    - packages/bitcoin-knots/src/validation.h
  </read_first>
  <files>
    packages/open-bitcoin-consensus/src/context.rs
    packages/open-bitcoin-consensus/src/context/tests.rs
    packages/open-bitcoin-consensus/src/block.rs
    packages/open-bitcoin-consensus/src/block/tests.rs
  </files>
  <behavior>
    - Test 1: `block_subsidy()` returns the default 50 BTC subsidy at height 0 and honors a tiny custom halving interval.
    - Test 2: `block_subsidy()` returns zero once the halving count reaches 64, matching Knots.
  </behavior>
  <acceptance_criteria>
    - `ConsensusParams` includes `subsidy_halving_interval` with a Knots-compatible default of `210_000` per D-03.
    - `packages/open-bitcoin-consensus/src/block.rs` exposes one consensus-owned helper for subsidy and coinbase reward-limit enforcement so Plan 02 can import it instead of duplicating logic.
    - Existing context tests and the new subsidy-helper tests compile against the expanded contract without touching deferred `connect_block()` or `reorg()` warnings.
  </acceptance_criteria>
  <action>Per D-01, D-02, and D-03, extend `ConsensusParams` in `packages/open-bitcoin-consensus/src/context.rs` with `subsidy_halving_interval`, default it to the Knots schedule, and update `packages/open-bitcoin-consensus/src/context/tests.rs` so the context surface explicitly covers the new field. In `packages/open-bitcoin-consensus/src/block.rs`, add one public consensus-owned helper pair for subsidy and reward-limit enforcement (for example `block_subsidy()` and `enforce_coinbase_reward_limit()`) that take `height`, `total_fees`, and `&ConsensusParams`; keep the logic in the consensus crate, use checked raw-satoshi math rather than converting the block-level limit back into `Amount`, and do not invent a second fee-calculation path.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features context::tests::block_context_carries_expected_pow_and_time_fields -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::block_subsidy_follows_halving_schedule_and_zeroes_after_sixty_four_halvings -- --exact</automated>
  </verify>
  <done>The consensus crate has a parameterized subsidy contract and a public reward-limit helper that later plans can call without widening the phase surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate contextual fees once and reject pure-core coinbase overpay</name>
  <read_first>
    - packages/open-bitcoin-consensus/src/transaction.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
  </read_first>
  <files>
    packages/open-bitcoin-consensus/src/block.rs
    packages/open-bitcoin-consensus/src/block/tests.rs
  </files>
  <behavior>
    - Test 1: `validate_block_with_context()` accepts a block whose coinbase pays exactly `subsidy(height) + total_fees`.
    - Test 2: `validate_block_with_context()` rejects the same block when the coinbase output is increased by 1 satoshi and returns `bad-cb-amount`.
    - Test 3: Checked fee accumulation rejects overflow with `bad-txns-accumulated-fee-outofrange` instead of silently wrapping.
  </behavior>
  <acceptance_criteria>
    - `validate_block_with_context()` sums the `Amount` returned by `validate_transaction_with_context()` exactly once per non-coinbase transaction, satisfying D-04.
    - Overpaying coinbases fail through `block_error(BlockValidationResult::Consensus, "bad-cb-amount", ...)`, satisfying D-03.
    - The new regressions stay focused on the reward-limit gap and do not rerun or rewrite already-green 07.5 time/difficulty/lax-DER evidence.
  </acceptance_criteria>
  <action>In `packages/open-bitcoin-consensus/src/block.rs`, change `validate_block_with_context()` so it accumulates the returned `Amount` from `validate_transaction_with_context()` exactly once per D-04, maps accumulator overflow to `block_error(BlockValidationResult::Consensus, "bad-txns-accumulated-fee-outofrange", ...)`, and calls the shared reward-limit helper before the sigop pass completes. Add focused regressions in `packages/open-bitcoin-consensus/src/block/tests.rs` named for exact-fee acceptance, +1 overpay rejection, and accumulator-overflow handling. Keep the scope narrow: do not touch `packages/open-bitcoin-consensus/src/transaction.rs` fee semantics, the 07.5 current-time path, or chainstate transactional behavior.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_accepts_exact_coinbase_reward_limit -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_coinbase_overpay_with_bad_cb_amount -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_rejects_accumulated_fee_overflow -- --exact</automated>
  </verify>
  <done>The pure-core validation path now matches Knots on subsidy-plus-fees enforcement, with regressions that make the gap reproducible before merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted block data → `validate_block_with_context()` | Coinbase values and transaction fees arrive from an untrusted block and must be validated before consensus acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-01 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Aggregate fees exactly once from `validate_transaction_with_context()` and reject overpaying coinbases with `bad-cb-amount` per D-03 and D-04. |
| T-07.6-02 | D | `packages/open-bitcoin-consensus/src/block.rs` fee accumulator | mitigate | Use checked raw-satoshi accumulation and fail with `bad-txns-accumulated-fee-outofrange` instead of wrapping or saturating. |
</threat_model>

<verification>
- Targeted consensus helper and block-validation tests named in the task `verify` blocks pass exactly.
- `packages/open-bitcoin-consensus/src/context/tests.rs` still proves the expanded context contract after `subsidy_halving_interval` is added.
</verification>

<success_criteria>
- `ConsensusParams` carries a configurable subsidy-halving interval with a Knots-compatible default.
- `validate_block_with_context()` rejects coinbase overpay with `bad-cb-amount` and fee-overflow with `bad-txns-accumulated-fee-outofrange`.
- Focused pure-core regressions prove exact-fee acceptance and +1 overpay rejection.
</success_criteria>

<output>
After completion, create `.planning/phases/07.6-enforce-coinbase-subsidy-plus-fees-limits-on-the-consensus-a/07.6-01-SUMMARY.md`
</output>
