---
phase: 145-parity-roots-and-no-claim-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/catalog/chainstate.md
  - docs/parity/catalog/p2p.md
  - docs/parity/source-breadcrumbs.json
requirements:
  - CSVFY-01
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 145-2026-09-18T03-36-29
generated_at: 2026-09-18T04:30:00.000Z
user_setup: []
must_haves:
  truths:
    - "Every v2.3 requirement ID has exactly one machine-readable surface owner, and Phase 145 owns only CSVFY-01 and CSVFY-02."
    - "Human checklist and catalog pages present disk-backed coins, cache-flush, manager restart, and honest availability as the current v2.3 claim, with leftover snapshot blobs labeled historical rather than live UTXO truth."
    - "Parity roots cite the five D-06 Knots files and name CanFlushToDisk and CheckBlockDataAvailability; HaveBlockData is not treated as a pinned-tree symbol."
  artifacts:
    - path: "docs/parity/index.json"
      provides: "Six Phase 139-144 surfaces plus closeout v2-3-parity-roots-and-no-claim-guardrails"
      contains: "v2-3-parity-roots-and-no-claim-guardrails"
    - path: "docs/parity/checklist.md"
      provides: "Mirrored human owners for the same seven surface IDs"
      contains: "v2-3-coins-view-cache-contract"
    - path: "docs/parity/catalog/chainstate.md"
      provides: "Current v2.3 claim above labeled historical snapshot-engine prose"
      contains: "Current v2.3 claim"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Dedicated CheckBlockDataAvailability group for has_block"
      contains: "node-stored-block-presence"
  key_links:
    - from: "docs/parity/index.json"
      to: "docs/parity/checklist.md"
      via: "matching v2-3 surface IDs and exactly-once requirement ownership"
      pattern: "v2-3-(coins-view-cache-contract|pure-flush-policy-and-typed-decisions|durable-fjall-coins-adapter|manager-flush-lifecycle-and-restart|honest-stored-block-availability|operator-flush-availability-evidence|parity-roots-and-no-claim-guardrails)"
    - from: "docs/parity/index.json"
      to: "packages/bitcoin-knots/src/node/blockstorage.cpp"
      via: "closeout upstream.sources"
      pattern: "packages/bitcoin-knots/src/node/blockstorage.cpp"
---

# Phase 145 Plan 01: Inventory and Parity-Root Backfill

<objective>
Give every v2.3 requirement a machine-readable owner, refresh the snapshot-era chainstate catalog, and split the stored-block presence breadcrumb so CSVFY-01 has auditable Knots roots.

Purpose: D-01 through D-08 require index/checklist ownership and honest anchors before the last-gate checker can name evidence. `catalog/chainstate.md` still describes Phase 4 snapshot persistence as live coverage.

Output: Seven `in_progress` surfaces, current v2.3 catalog prose, serve-path honesty next to reserved `Pruned`, and a dedicated `has_block` breadcrumb group. No requirement flips, no `verify.sh` wiring, no runtime coins/flush/manager/serve changes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-CONTEXT.md
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/catalog/chainstate.md
@docs/parity/catalog/p2p.md
@docs/parity/source-breadcrumbs.json

<interfaces>
Existing `docs/parity/index.json` top-level `surfaces[]` v2.2 names that must stay `done`:
- `v2-2-resource-time-fee-primitives`
- `v2-2-package-aware-download-orphan-bridge`
- `v2-2-authoritative-cross-cache-lifecycle-integration`
- `v2 snapshot schema, checkpointing, and recovery` (human title; do not rename)
- `v2-2-rpc-and-sanitized-operator-evidence`
- `v2-2-typed-package-staged-admission`
- `v2-2-rolling-fee-expiry-pressure`
- `v2-2-receive-independent-maintenance-and-transport-receipts`
- `v2-2-parity-uat-release-boundary`

Copy the Phase 138 closeout object shape from `docs/parity/index.json` `v2-2-parity-uat-release-boundary`:
`id`, `title`, `status`, `requirements`, `evidence`, `rationale`, `upstream.sources`, `upstream.tests`, `known_gaps`, `suspected_unknowns`.

Prescribed v2.3 surface map (D-03, research Pattern 2). Use these exact IDs as both top-level `name` and checklist `id`:

| Surface id / top-level name | Requirements | Phase |
|---|---|---|
| `v2-3-coins-view-cache-contract` | CACHE-01 | 139 |
| `v2-3-pure-flush-policy-and-typed-decisions` | FLUSH-01, MGR-03 | 140 |
| `v2-3-durable-fjall-coins-adapter` | COIN-01, CSOBS-03 | 141 |
| `v2-3-manager-flush-lifecycle-and-restart` | MGR-01, MGR-02, FLUSH-02 | 142 |
| `v2-3-honest-stored-block-availability` | HAVL-01, HAVL-02, HAVL-03 | 143 |
| `v2-3-operator-flush-availability-evidence` | CSOBS-01, CSOBS-02 | 144 |
| `v2-3-parity-roots-and-no-claim-guardrails` | CSVFY-01, CSVFY-02 | 145 |

D-06 files that the closeout `upstream.sources` must list exactly:
- `packages/bitcoin-knots/src/coins.h`
- `packages/bitcoin-knots/src/coins.cpp`
- `packages/bitcoin-knots/src/validation.cpp`
- `packages/bitcoin-knots/src/node/chainstate.cpp`
- `packages/bitcoin-knots/src/node/blockstorage.cpp`

Closeout `upstream.tests` must include `packages/bitcoin-knots/src/test/blockmanager_tests.cpp`.

Pinned symbols to name in catalog prose, not as path tokens:
- `CanFlushToDisk` in `node/chainstate.cpp` and `validation.cpp`
- `CheckBlockDataAvailability` in `node/blockstorage.cpp`
- `FlushStateToDisk` / `GetCoinsCacheSizeState` in `validation.cpp`

`HaveBlockData` is the locked discussion name only. It is not a symbol in `packages/bitcoin-knots`. Do not add a required-anchor token `HaveBlockData`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity index/checklist → auditors | Missing or duplicate owners hide gaps or invent a second requirement source. |
| Catalog prose → operators | Snapshot-era coverage read as live UTXO truth, or `HaveBlockData` claimed as a Knots symbol. |
| Breadcrumb groups → source review | A `none` group that hides `has_block` makes the serve-path anchor unauditable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | `docs/parity/index.json` | mitigate | Each of the 15 v2.3 IDs appears on exactly one checklist surface. CSVFY-01 and CSVFY-02 appear only on `v2-3-parity-roots-and-no-claim-guardrails`. Do not create `docs/parity/v2.3-closeout.md`. |
| T-145-02 | Spoofing | `docs/parity/catalog/chainstate.md` | mitigate | Add a labeled **Current v2.3 claim** section. Relabel Phase 4 snapshot persistence as historical. Record D-07 leftover-snapshot non-authority. |
| T-145-03 | Spoofing | catalog / closeout anchors | mitigate | Require the five D-06 files. Write `HaveBlockData` only as the discussion name and immediately name `CheckBlockDataAvailability`. Name `CanFlushToDisk` beside D-06 `CanFlush`. |
| T-145-04 | Repudiation | `docs/parity/source-breadcrumbs.json` | mitigate | Split `fjall_store/blocks.rs` and `block_presence.rs` onto `node-stored-block-presence` citing `node/blockstorage.cpp`. Leave remaining `node-storage-contract` files on explicit `none`. |
| T-145-05 | Elevation of Privilege | REQUIREMENTS / surface status | mitigate | Keep all seven new surfaces `in_progress`. Do not flip CACHE-01, MGR-03, or CSVFY checkboxes. Do not archive v2.3. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Backfill six Phase 139-144 owners and the CSVFY closeout surface</name>
  <files>
    docs/parity/index.json
    docs/parity/checklist.md
  </files>
  <read_first>
    docs/parity/index.json
    docs/parity/checklist.md
    .planning/REQUIREMENTS.md
    docs/parity/catalog/chainstate.md
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-CONTEXT.md
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
  </read_first>
  <action>
    Add the seven surfaces from the interface table to all three roots (per D-01, D-02, D-03):

    1. `docs/parity/index.json` top-level `surfaces[]`, immediately after `v2-2-parity-uat-release-boundary`, as `{ "name": "&lt;id&gt;", "status": "in_progress" }`.
    2. `docs/parity/index.json` `checklist.surfaces[]`, immediately after the `v2-2-parity-uat-release-boundary` object, using the Phase 138 closeout schema.
    3. `docs/parity/checklist.md` table rows immediately after the `v2-2-parity-uat-release-boundary` row.

    Status for all seven this plan is `in_progress` / `` `in_progress` `` (D-04). Do not promote existing earlier-milestone `done` surfaces. Do not rename `"v2 snapshot schema, checkpointing, and recovery"`.

    Exact ownership and evidence (do not put CACHE/FLUSH/COIN/MGR/HAVL/CSOBS on the closeout surface):

    1. `v2-3-coins-view-cache-contract` — CACHE-01 only. Evidence: `docs/parity/catalog/chainstate.md`, `packages/open-bitcoin-chainstate/src/coins.rs`, `packages/open-bitcoin-chainstate/src/coins/cache.rs`, `.planning/phases/139-coins-view-cache-contract-and-engine-apply/139-VERIFICATION.md`. Knots sources: `packages/bitcoin-knots/src/coins.h`, `packages/bitcoin-knots/src/coins.cpp`.

    2. `v2-3-pure-flush-policy-and-typed-decisions` — FLUSH-01, MGR-03. Evidence: `docs/parity/catalog/chainstate.md`, `packages/open-bitcoin-chainstate/src/coins/flush.rs`, `.planning/phases/140-pure-flush-policy-and-typed-decisions/140-VERIFICATION.md`. Knots sources: `packages/bitcoin-knots/src/validation.cpp` (`FlushStateToDisk`, `GetCoinsCacheSizeState`).

    3. `v2-3-durable-fjall-coins-adapter` — COIN-01, CSOBS-03. Evidence: `docs/parity/catalog/chainstate.md`, `packages/open-bitcoin-node/src/storage/coins_view.rs`, `packages/open-bitcoin-node/src/storage/fjall_store/coins.rs`. Knots sources: `packages/bitcoin-knots/src/coins.h`, `packages/bitcoin-knots/src/coins.cpp`. Record D-07: Fjall per-outpoint coins instead of LevelDB `chainstate/`.

    4. `v2-3-manager-flush-lifecycle-and-restart` — MGR-01, MGR-02, FLUSH-02. Evidence: `docs/parity/catalog/chainstate.md`, `packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs`, `packages/open-bitcoin-node/src/chainstate/replay.rs`. Knots sources: `packages/bitcoin-knots/src/node/chainstate.cpp` (`CanFlushToDisk`), `packages/bitcoin-knots/src/validation.cpp`. Record D-07: single active chainstate, not assumeutxo dual-chainstate; disk-space probe may stay fail-open at `u64::MAX`.

    5. `v2-3-honest-stored-block-availability` — HAVL-01, HAVL-02, HAVL-03. Evidence: `docs/parity/catalog/p2p.md`, `docs/parity/catalog/chainstate.md`, `packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs`. Knots sources: `packages/bitcoin-knots/src/node/blockstorage.cpp` (`CheckBlockDataAvailability`). Record D-07: `Pruned` stays reserved and is not emitted on production paths.

    6. `v2-3-operator-flush-availability-evidence` — CSOBS-01, CSOBS-02. Evidence: `docs/parity/catalog/chainstate.md`, `docs/operator/runtime-guide.md` Phase 144 section, `packages/open-bitcoin-node/src/network/chainstate_durability_evidence.rs`. Do not re-derive `chainstate_durability` fields (D-26).

    7. `v2-3-parity-roots-and-no-claim-guardrails` — CSVFY-01, CSVFY-02 only. Evidence: `.planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md` (named now; file lands in Plan 03), `scripts/check-phase145-parity-uat-release-boundary.ts` (named now; file lands in Plan 02), `docs/parity/release-readiness.md`, `docs/parity/catalog/chainstate.md`, `docs/parity/index.json`. `upstream.sources` is the five D-06 files. `upstream.tests` includes `packages/bitcoin-knots/src/test/blockmanager_tests.cpp`. `known_gaps` must list FUT-18 through FUT-26 as deferred and must say Phase 145 owns only CSVFY-01 and CSVFY-02. `suspected_unknowns` must say milestone archival remains `/gsd-complete-milestone v2.3` after this phase passes.

    Closeout `known_gaps` must also record these D-07 intentional differences verbatim as differences, not as missing deliverables: Fjall per-outpoint coins instead of LevelDB `chainstate/`; first-party overlay occupancy instead of C++ allocator / LevelDB SizeEstimate; leftover snapshot blobs are non-authoritative after the schema 1→2 one-way migration; `Pruned` stays reserved and is not emitted on production paths; single active chainstate, not assumeutxo dual-chainstate; disk-space probe may stay fail-open at `u64::MAX` where the node crate forbids unsafe `statvfs`.

    Do not create a new catalog page. Do not edit `.planning/REQUIREMENTS.md`. Do not write `Phase 145 is complete`, `Phase 145 is done`, or `Phase 145 shipped`.
  </action>
  <verify>
    <automated>rg -n "v2-3-(coins-view-cache-contract|pure-flush-policy-and-typed-decisions|durable-fjall-coins-adapter|manager-flush-lifecycle-and-restart|honest-stored-block-availability|operator-flush-availability-evidence|parity-roots-and-no-claim-guardrails)" docs/parity/index.json docs/parity/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` top-level `surfaces[]` and `checklist.surfaces[]`, plus `docs/parity/checklist.md`, each contain all seven exact new surface IDs
    - the closeout checklist object lists `"CSVFY-01"` and `"CSVFY-02"` and none of CACHE, FLUSH, COIN, MGR, HAVL, or CSOBS
    - CACHE-01 appears only under `v2-3-coins-view-cache-contract`
    - FLUSH-01 and MGR-03 appear only under `v2-3-pure-flush-policy-and-typed-decisions`
    - COIN-01 and CSOBS-03 appear only under `v2-3-durable-fjall-coins-adapter`
    - MGR-01, MGR-02, and FLUSH-02 appear only under `v2-3-manager-flush-lifecycle-and-restart`
    - HAVL-01, HAVL-02, and HAVL-03 appear only under `v2-3-honest-stored-block-availability`
    - CSOBS-01 and CSOBS-02 appear only under `v2-3-operator-flush-availability-evidence`
    - closeout `upstream.sources` contains the five D-06 file paths and does not contain the token `HaveBlockData`
    - closeout `upstream.tests` contains `packages/bitcoin-knots/src/test/blockmanager_tests.cpp`
    - all seven new surfaces have `"status": "in_progress"`
    - top-level surfaces still include the exact name `"v2 snapshot schema, checkpointing, and recovery"` with `"status": "done"`
    - `docs/parity/v2.3-closeout.md` does not exist
    - `rg -n "Phase 145 is (complete|done|shipped)" docs/parity/index.json docs/parity/checklist.md` prints no matches
  </acceptance_criteria>
  <done>Machine and checklist owners exist for all 15 v2.3 IDs without promoting surfaces or flipping Pending rows.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh chainstate catalog, serve-path honesty, and has_block breadcrumb split</name>
  <files>
    docs/parity/catalog/chainstate.md
    docs/parity/catalog/p2p.md
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    docs/parity/catalog/chainstate.md
    docs/parity/catalog/p2p.md
    docs/parity/source-breadcrumbs.json
    packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
    packages/bitcoin-knots/src/node/blockstorage.cpp
    packages/bitcoin-knots/src/node/chainstate.cpp
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
  </read_first>
  <action>
    Refresh `docs/parity/catalog/chainstate.md` (per D-08, D-07). Insert a new section titled exactly `## Current v2.3 claim` above the existing `## Coverage` list. That new section must state, in prose that includes this exact substring:

    disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present

    Also state these companion facts (D-15 / D-07): leftover snapshot blobs are non-authoritative after the schema 1→2 one-way migration; restart tip and UTXO view come from durable coins best-block; `Pruned` stays reserved; first-party overlay occupancy is not C++ allocator / LevelDB SizeEstimate; Fjall per-outpoint coins replace LevelDB `chainstate/`; disk-space probe may stay fail-open at `u64::MAX`; Phase 144 `chainstate_durability` is the shared operator contract and is not re-derived here.

    Relabel the existing Coverage bullets that say `pure-core active-chain snapshots` and `node-side in-memory snapshot persistence` as historical Phase 4 / later-snapshot history. Change the current heading `## Coverage` to `## Historical Phase 4 snapshot-engine coverage` or add an immediate sentence under Coverage: `The following bullets are historical Phase 4 snapshot-engine coverage, not live v2.3 coin truth.`

    In `## Known gaps`, delete the live-gap reading of `disk-backed coins databases, cache-flush policy`. Keep assumeutxo as deferred FUT-21, not as a missing v2.3 deliverable. Keep prune/archive, compact filters, public defaults, and production readiness as deferred.

    Cite D-06 files in `## Knots sources`. In `## Knots behaviors mirrored here` or the new current-claim section, name `FlushStateToDisk`, `GetCoinsCacheSizeState`, `CanFlushToDisk`, and `CheckBlockDataAvailability`. If `HaveBlockData` appears, it must appear only as the locked discussion name in the same sentence as `CheckBlockDataAvailability`.

    In `docs/parity/catalog/p2p.md`, immediately after the existing reserved-`Pruned` / missing-payload paragraph that already contains `block_status_pruned` and `CheckBlockDataAvailability` or `HaveBlockData` discussion, add one sentence that says the pinned Knots serve-path symbol is `BlockManager::CheckBlockDataAvailability` in `packages/bitcoin-knots/src/node/blockstorage.cpp`, that `HaveBlockData` is the locked discussion name only, and that `packages/bitcoin-knots/src/test/blockmanager_tests.cpp` is the matching test anchor. Do not add a `getblock` product. Do not globally allow unscoped `historical serving`, `archive`, or `prune`.

    In `docs/parity/source-breadcrumbs.json` (D-05):

    - Create a new group with `"label": "node-stored-block-presence"`.
    - Move only these two files out of `node-storage-contract` into that group: `packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs`, `packages/open-bitcoin-node/src/storage/fjall_store/tests/block_presence.rs`.
    - Set that group's `breadcrumbs` to `["packages/bitcoin-knots/src/node/blockstorage.cpp"]`.
    - Leave remaining `node-storage-contract` files in place with `"breadcrumbs": []`.
    - Do not invent a `HaveBlockData` breadcrumb path.
    - Do not change `chainstate-engine`, `node-coins-adapter`, `node-chainstate-adapter`, or `node-network-chainstate-durability-evidence` file lists unless a file is currently missing from its existing group; those groups already cite the D-06 files.

    Do not add first-party Rust source. Do not rewrite `chainstate_durability`. Do not flip REQUIREMENTS.
  </action>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/catalog/chainstate.md` contains the heading `## Current v2.3 claim` and the exact D-14 substring `disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present`
    - `docs/parity/catalog/chainstate.md` contains `CanFlushToDisk` and `CheckBlockDataAvailability`
    - `docs/parity/catalog/chainstate.md` no longer lists `disk-backed coins databases, cache-flush policy` as a live Known-gap deliverable
    - leftover snapshot wording in `docs/parity/catalog/chainstate.md` includes `non-authoritative`
    - `docs/parity/catalog/p2p.md` contains `CheckBlockDataAvailability` and `blockmanager_tests.cpp`
    - `docs/parity/source-breadcrumbs.json` group `node-stored-block-presence` lists `packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs` and `packages/open-bitcoin-node/src/storage/fjall_store/tests/block_presence.rs` and cites `packages/bitcoin-knots/src/node/blockstorage.cpp`
    - `node-storage-contract` no longer lists those two files and still has `"breadcrumbs": []`
    - `bun run scripts/check-parity-breadcrumbs.ts` exits 0
    - `rg -n "HaveBlockData" docs/parity/catalog/chainstate.md docs/parity/catalog/p2p.md docs/parity/index.json docs/parity/source-breadcrumbs.json` matches only discussion-name prose that also names `CheckBlockDataAvailability` in the same file, never as a breadcrumb path
  </acceptance_criteria>
  <done>Current catalog claim, serve-path honesty, and the has_block breadcrumb split are auditable without flipping requirements or adding runtime behavior.</done>
</task>

</tasks>

<verification>
```bash
rg -n "v2-3-(coins-view-cache-contract|pure-flush-policy-and-typed-decisions|durable-fjall-coins-adapter|manager-flush-lifecycle-and-restart|honest-stored-block-availability|operator-flush-availability-evidence|parity-roots-and-no-claim-guardrails)" docs/parity/index.json docs/parity/checklist.md
rg -n "Current v2.3 claim|CheckBlockDataAvailability|CanFlushToDisk|non-authoritative" docs/parity/catalog/chainstate.md
rg -n "node-stored-block-presence" docs/parity/source-breadcrumbs.json
bun run scripts/check-parity-breadcrumbs.ts
git diff --check
```
</verification>

<success_criteria>
- All 15 v2.3 requirement IDs have exactly one index/checklist owner.
- Phase 145 owns only CSVFY-01 and CSVFY-02.
- `catalog/chainstate.md` presents disk-backed coins as current claim and leftover snapshots as historical.
- Serve-path roots name `CheckBlockDataAvailability`; `HaveBlockData` is not a required symbol token.
- No REQUIREMENTS checkbox, surface `done`, last-gate, or `verify.sh` change lands in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/145-parity-roots-and-no-claim-guardrails/145-01-SUMMARY.md`
</output>
