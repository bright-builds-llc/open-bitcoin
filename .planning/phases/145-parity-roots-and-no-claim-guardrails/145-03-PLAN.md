---
phase: 145-parity-roots-and-no-claim-guardrails
plan: "03"
type: execute
wave: 3
depends_on:
  - "145-02"
files_modified:
  - README.md
  - docs/operator/runtime-guide.md
  - docs/parity/release-readiness.md
  - docs/parity/production-claim-boundary.md
  - docs/parity/support-matrix.md
  - .planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md
  - scripts/verify.sh
requirements:
  - CSVFY-02
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 145-2026-09-18T03-36-29
generated_at: 2026-09-18T04:30:00.000Z
user_setup: []
must_haves:
  truths:
    - "README and the runtime guide contain the verbatim D-14 sentence and still contain the verbatim v2.2 D-21 sentence."
    - "Committed 145-UAT.md lists the repo-local Cargo and Bazel twins and records public-network review as not run."
    - "scripts/verify.sh runs the Phase 145 pair immediately after Phase 144 in both the visible order block and the live run_step chain, and Phase 138 remains the file-final check-phase* command."
  artifacts:
    - path: "README.md"
      provides: "Current v2.3 scoped claim plus preserved v2.2 D-21 sentence"
      contains: "disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present"
    - path: ".planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md"
      provides: "Committed deterministic UAT package with optional public-network not run"
      contains: "status: not run"
    - path: "scripts/verify.sh"
      provides: "144 then 145 insertion before Phase 121"
      contains: "check-phase145-parity-uat-release-boundary"
  key_links:
    - from: "README.md"
      to: "docs/operator/runtime-guide.md"
      via: "verbatim D-14 sentence in both required files"
      pattern: "disk-backed per-outpoint coins, typed cache-flush policy"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase145-parity-uat-release-boundary.ts"
      via: "visible and executable pair immediately after Phase 144"
      pattern: "bun run scripts/check-phase145-parity-uat-release-boundary.ts"
---

# Phase 145 Plan 03: Claim Copy, UAT Package, and verify.sh Wiring

<objective>
Publish the bounded v2.3 claim, commit the UAT package, and wire the Phase 145 checker immediately after Phase 144.

Purpose: D-10, D-12, D-14, D-18, D-19, D-20, and D-25 make the live corpus match what Plan 02 already tests. Phase 138 stays the file-final `check-phase*` gate.

Output: D-14 sentence on README and runtime-guide, `145-UAT.md`, release-handoff copy, and lockstep `verify.sh` insertion after Phase 144. No REQUIREMENTS flip. No milestone archive.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-CONTEXT.md
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md
@README.md
@docs/operator/runtime-guide.md
@docs/parity/release-readiness.md
@scripts/verify.sh
@scripts/check-phase145-parity-uat-release-boundary/constants.ts

<interfaces>
Required D-14 sentence, copied verbatim into `README.md` and `docs/operator/runtime-guide.md` (D-14):

disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present

Required v2.2 D-21 sentence that must remain intact in those same files (D-12):

bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members

README P2P-row clause that must stay for Phase 117:

package relay, bloom/filter serving, public defaults, archive/production-scale serving, public-network gates, and production readiness remain deferred

Denied positive claims, copied verbatim from D-16. Do not add a new positive clause that asserts any of:
- prune-mode product behavior
- archive-node or production-scale historical serving
- assumeutxo/assumevalid/IBD snapshot shortcuts
- compact-filter or BIP37 serving
- public serving or relay by default
- public-network CI or release-blocking historical-serving/long-chain flush runs
- production full-node readiness
- production service operation
- production-funds wallet safety
- LevelDB `chainstate/` live import/export
- automatic destructive reindex or coins repair

Required `verify.sh` insertion after the Phase 144 pair and before Phase 121:

```
bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
bun run scripts/check-phase145-parity-uat-release-boundary.ts
```

```
run_step "test Phase 145 parity UAT release boundary checker" bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
run_step "check Phase 145 parity UAT release boundary" bun run scripts/check-phase145-parity-uat-release-boundary.ts
```

Do not insert Phase 145 after Phase 138.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live claim-bearing docs → operators | Unscoped prune/archive/assumeutxo/production wording can be read as a shipped capability. |
| README status block → Phase 138 checker | Removing the v2.2 D-21 sentence fails the v2.2 last-gate. |
| Default verifier → release gates | Public-network or file-final 145 placement either broadens v2.3 or breaks v2.2 last-gate pins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-11 | Spoofing | README.md, runtime-guide, release-readiness, support-matrix, production-claim-boundary | mitigate | Copy the D-14 sentence verbatim. Keep the D-21 sentence. Keep the README P2P-row `package relay` … `remain deferred` clause. Use no-claim markers on every D-16 topic. |
| T-145-12 | Tampering | README status block | mitigate | Replace `start future work with /gsd-new-milestone` as current-state copy. State that v2.3 is the active milestone. Do not delete archived v2.1/v2.2 history sentences. |
| T-145-13 | Elevation of Privilege | 145-UAT.md / verify.sh | mitigate | Required tests stay deterministic. Public-network review is `status: not run` and never a default, CI, or release gate. Do not add `run-live-mainnet-smoke` or `command-timings.ts` as `run_step`s. |
| T-145-14 | Denial of Service | `scripts/verify.sh` last-gate pins | mitigate | Insert 145 after 144 and before 121. Leave Phase 138 as the last `check-phase*` command. Update the ordering comment to say 145 is the v2.3 closeout, not the file-final gate. |
| T-145-15 | Information Disclosure | UAT examples | mitigate | Reuse `/tmp/open-bitcoin-chainstate-durability-support` and `/tmp/open-bitcoin-preview`. Do not add peer IDs, coin dumps, or new credentials. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write 145-UAT.md and publish the D-14 claim without removing D-21</name>
  <files>
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md
    README.md
    docs/operator/runtime-guide.md
    docs/parity/release-readiness.md
    docs/parity/production-claim-boundary.md
    docs/parity/support-matrix.md
  </files>
  <read_first>
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md
    README.md
    docs/operator/runtime-guide.md
    docs/parity/release-readiness.md
    docs/parity/production-claim-boundary.md
    docs/parity/support-matrix.md
    scripts/check-phase145-parity-uat-release-boundary/constants.ts
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-CONTEXT.md
  </read_first>
  <action>
    Create `.planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md` by copying the 138-UAT.md section shape (Required Deterministic Tests, Optional Public-Network Review, Summary, Gaps, Focused Command Evidence). Differences (D-18, D-19):

    - Title: `Phase 145 UAT`
    - Required tests stay deterministic and may still say `result: pending` until Plan 04 / execution evidence exists. Do not mark the package `status: passed` in this plan.
    - Required test 1 expected text must include the seven `v2-3-*` surface IDs and exactly-once ownership of all 15 v2.3 IDs, with CSVFY only on `v2-3-parity-roots-and-no-claim-guardrails`.
    - Required test 2 expected text must include the verbatim D-14 sentence and the D-16 deny list copied verbatim.
    - Required test 3 expected text must include the exact Cargo/Bazel/daemon strings from Plan 02 `REQUIRED_UAT_COMMANDS`.
    - Required test 4 expected text must include `bun test scripts/check-phase145-parity-uat-release-boundary.test.ts`, `bun run scripts/check-phase145-parity-uat-release-boundary.ts`, and `bash scripts/verify.sh`.
    - Optional Public-Network Review must be exactly `status: not run` and must say: this does not create a gap and is never a default, CI, or release gate.

    Add a `## Phase 145 Parity Roots And No-Claim Guardrails Review` section to `docs/operator/runtime-guide.md` after the Phase 144 section. The section must contain the verbatim D-14 sentence and the companion allowed wording (D-15): sanitized operator flush/recovery/cache-size/have-bytes evidence, leftover snapshot non-authority after one-way migration, restart from durable coins best-block, and hermetic default verification. Reuse the Phase 144 Cargo/Bazel status and support-bundle blocks unchanged, plus the daemon / `open-bitcoin-cli` preview forms from Plan 02.

    Update `README.md` (D-25, D-12):

    - Keep the existing Status block sentence that contains the verbatim D-21 sentence.
    - Add a current-state sentence that includes the verbatim D-14 sentence and says v2.3 is the active milestone.
    - Remove or rewrite `start future work with `/gsd-new-milestone`` as current-state copy. Historical `v2.2 shipped and was archived on 2026-08-22` may stay.
    - Change the Chainstate notes cell that currently says `Disk-backed databases and full manager behavior remain follow-up depth.` so it no longer calls disk-backed coins a follow-up gap. Point at `docs/parity/catalog/chainstate.md`. Keep prune/archive/assumeutxo/production as deferred.
    - Keep the P2P-row clause `package relay, bloom/filter serving, public defaults, archive/production-scale serving, public-network gates, and production readiness remain deferred`.
    - Link `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/catalog/chainstate.md`, and `docs/parity/release-readiness.md` as the current evidence roots. Do not invent a changelog tree.

    Add a current v2.3 handoff section near the top of `docs/parity/release-readiness.md` (after the existing intro is fine) titled `## v2.3 Chainstate Durability And Historical Serving`. It must contain the D-14 sentence, say this file is the v2.3 release-review handoff, keep default verification hermetic, and list FUT-18 through FUT-26 as deferred. Do not delete historical v1.x/v2.1/v2.2 sections.

    Add one no-claim row or sentence each to `docs/parity/production-claim-boundary.md` and `docs/parity/support-matrix.md` stating that v2.3 does not add the D-16 list. Use `deferred` / `does not` markers. Keep existing v2.1/v2.2 deferred rows.

    Forbidden in any new positive clause on those live files: the D-16 list above, unscoped `historical serving`, unscoped `archive`, unscoped `prune`, and `package relay` without a same-clause no-claim marker.

    Do not scan or rewrite historical `.planning/` archives. Do not archive v2.3. Do not run `/gsd-complete-milestone`.
  </action>
  <verify>
    <automated>rg -n "disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present" README.md docs/operator/runtime-guide.md .planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md docs/parity/release-readiness.md</automated>
  </verify>
  <acceptance_criteria>
    - `145-UAT.md` exists and contains `status: not run`, `bash scripts/verify.sh`, both `open-bitcoin` Cargo/Bazel `status --format human` forms, and `/tmp/open-bitcoin-chainstate-durability-support`
    - `docs/operator/runtime-guide.md` contains the heading `Phase 145 Parity Roots And No-Claim Guardrails Review` and the verbatim D-14 sentence
    - `README.md` and `docs/operator/runtime-guide.md` each contain the verbatim D-14 sentence and the verbatim D-21 sentence
    - `README.md` still contains `package relay` and `remain deferred` in the same P2P-row sentence
    - `README.md` does not contain `start future work with \`/gsd-new-milestone\``
    - `README.md` does not contain `Disk-backed databases and full manager behavior remain follow-up depth`
    - `docs/parity/release-readiness.md` contains `## v2.3 Chainstate Durability And Historical Serving` and the D-14 sentence
    - `rg -n "provides prune-mode|supports archive-node|enables assumeutxo" README.md docs/operator/runtime-guide.md docs/parity/release-readiness.md docs/parity/support-matrix.md docs/parity/production-claim-boundary.md` prints no matches
    - `145-UAT.md` does not contain `status: passed`
  </acceptance_criteria>
  <done>Committed UAT and live docs state D-14 exactly, keep the v2.2 D-21 sentence, and do not flip requirement boxes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 145 immediately after Phase 144 and update the ordering comment</name>
  <files>
    scripts/verify.sh
  </files>
  <read_first>
    scripts/verify.sh
    scripts/check-phase145-parity-uat-release-boundary/verifier.ts
    scripts/check-phase138-parity-uat-release-boundary/verifier.ts
    scripts/check-phase144-operator-flush-availability-evidence.ts
  </read_first>
  <action>
    Update `scripts/verify.sh` in lockstep on all three surfaces (D-10, D-20):

    1. Ordering comment currently around line 304 that ends `Phase 138 is the final changed-path v2.2 release-boundary and no-claim gate. The current-documentation reconciliation test/check pair runs immediately after that final Phase 138 gate.` Keep those two sentences. Append this exact additional sentence:

    `Phase 145 is the v2.3 closeout and no-claim gate and is wired immediately after Phase 144; Phase 138 remains the file-final check-phase* command.`

    Do not replace `Phase 138 is the final changed-path` with Phase 145.

    2. In `VERIFY_COMMAND_ORDER`, insert the exact 145 test/check pair immediately after the Phase 144 pair (currently lines 394–395) and immediately before the Phase 121 pair.

    3. Insert the matching `run_step` lines immediately after the Phase 144 `run_step` pair (currently lines 560–561) and immediately before the Phase 121 `run_step` pair. Use the titles from the interface block.

    Do not add public-network, soak, service-manager, wall-clock, or `command-timings.ts` `run_step`s. Do not move Phase 145 after Phase 138. Do not edit Phase 124/129/130/131/138 last-gate helpers.

    After the live docs from Task 1 exist, run `bun test scripts/check-phase145-parity-uat-release-boundary.test.ts` and `bun run scripts/check-phase145-parity-uat-release-boundary.ts`. The live checker may still allow `in_progress` surfaces and unchecked CSVFY boxes; Plan 04 tightens those. If the live checker fails only on missing D-14/UAT/order, fix the live corpus rather than weakening the checker.
  </action>
  <verify>
    <automated>bun test scripts/check-phase145-parity-uat-release-boundary.test.ts && bun run scripts/check-phase145-parity-uat-release-boundary.ts && bun test scripts/check-phase138-parity-uat-release-boundary.test.ts && bun run scripts/check-phase138-parity-uat-release-boundary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` `VERIFY_COMMAND_ORDER` contains the 144 pair, then the 145 pair, then the 121 pair
    - `scripts/verify.sh` live `run_step` chain matches that same 144 then 145 then 121 order
    - `scripts/verify.sh` still contains `Phase 138 is the final changed-path v2.2 release-boundary`
    - `scripts/verify.sh` contains `Phase 145 is the v2.3 closeout and no-claim gate and is wired immediately after Phase 144`
    - the last `bun run scripts/check-phase` command in `VERIFY_COMMAND_ORDER` is still `bun run scripts/check-phase138-parity-uat-release-boundary.ts`
    - `bun test scripts/check-phase145-parity-uat-release-boundary.test.ts` exits 0
    - `bun run scripts/check-phase145-parity-uat-release-boundary.ts` exits 0
    - `bun run scripts/check-phase138-parity-uat-release-boundary.ts` exits 0
    - `rg -n "run-live-mainnet-smoke|wall-clock" scripts/verify.sh` prints no matches inside `run_step` lines
  </acceptance_criteria>
  <done>Phase 145 is the v2.3 closeout gate immediately after Phase 144; Phase 138 remains the file-final check-phase* command.</done>
</task>

</tasks>

<verification>
```bash
rg -n "disk-backed per-outpoint coins, typed cache-flush policy" README.md docs/operator/runtime-guide.md
rg -n "bounded local-package APIs, same-peer 1P1C assembly" README.md docs/operator/runtime-guide.md
rg -n "check-phase145-parity-uat-release-boundary" scripts/verify.sh
rg -n "status: not run" .planning/phases/145-parity-roots-and-no-claim-guardrails/145-UAT.md
bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
bun run scripts/check-phase145-parity-uat-release-boundary.ts
bun run scripts/check-phase138-parity-uat-release-boundary.ts
git diff --check
```
</verification>

<success_criteria>
- Live docs state the locked D-14 sentence without removing the v2.2 D-21 sentence.
- `145-UAT.md` is the only closeout UAT package and records public-network review as not run.
- `verify.sh` visible and executable order is 144 → 145 → 121, and 138 remains file-final.
- Leftover REQUIREMENTS checkboxes and surface `done` promotions are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/145-parity-roots-and-no-claim-guardrails/145-03-SUMMARY.md`
</output>
