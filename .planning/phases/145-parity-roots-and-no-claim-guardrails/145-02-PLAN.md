---
phase: 145-parity-roots-and-no-claim-guardrails
plan: "02"
type: tdd
wave: 2
depends_on:
  - "145-01"
files_modified:
  - scripts/check-phase145-parity-uat-release-boundary.ts
  - scripts/check-phase145-parity-uat-release-boundary.test.ts
  - scripts/check-phase145-parity-uat-release-boundary/checks.ts
  - scripts/check-phase145-parity-uat-release-boundary/constants.ts
  - scripts/check-phase145-parity-uat-release-boundary/claims.ts
  - scripts/check-phase145-parity-uat-release-boundary/verifier.ts
  - scripts/check-phase145-parity-uat-release-boundary/test-fixtures.ts
requirements:
  - CSVFY-01
  - CSVFY-02
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 145-2026-09-18T03-36-29
generated_at: 2026-09-18T04:30:00.000Z
user_setup: []
must_haves:
  truths:
    - "A filesystem-only Phase 145 checker returns string[] failures, names all 15 v2.3 IDs exactly once, and requires the five D-06 Knots files plus the has_block breadcrumb group."
    - "Positive D-16 overclaims fail and the verbatim D-14 sentence plus deferred/not-run wording pass, without scanning .planning/ history."
    - "Verifier checks require 144 then 145, keep 117 and 138 present, and do not assert that Phase 145 is the file-final check-phase* command."
  artifacts:
    - path: "scripts/check-phase145-parity-uat-release-boundary.ts"
      provides: "Thin CLI plus re-export of the pure Phase 145 checker"
      contains: "checkPhase145ParityUatReleaseBoundary"
    - path: "scripts/check-phase145-parity-uat-release-boundary/constants.ts"
      provides: "D-14 sentence, D-16 deny list, surface map, UAT commands, Knots anchors"
      contains: "disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present"
    - path: "scripts/check-phase145-parity-uat-release-boundary/verifier.ts"
      provides: "144-then-145 order plus historical phase-dir presence"
      contains: "check-phase144-operator-flush-availability-evidence"
  key_links:
    - from: "scripts/check-phase145-parity-uat-release-boundary/checks.ts"
      to: "docs/parity/index.json"
      via: "exactly-once v2.3 requirement ownership"
      pattern: "v2-3-parity-roots-and-no-claim-guardrails"
    - from: "scripts/check-phase145-parity-uat-release-boundary/verifier.ts"
      to: "scripts/verify.sh"
      via: "visible and executable 144 then 145 order, not file-final last-gate"
      pattern: "check-phase145-parity-uat-release-boundary"
---

# Phase 145 Plan 02: Last-Gate Checker and Mutation Fixtures

<objective>
Install the v2.3 closeout checker as a fixture-tested Bun pair that names CSVFY evidence without wiring `verify.sh` yet.

Purpose: D-09, D-11, D-13, D-16, and D-17 turn Plan 01 surfaces into an enforceable no-claim and ownership gate. Wiring the live verifier before README/UAT exist would fail default verify.

Output: Phase 138-shaped export, directory helpers, and mutation tests. No `verify.sh` edit. No REQUIREMENTS flip. Do not retarget Phase 138 file-final last-gate helpers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-CONTEXT.md
@.planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
@scripts/check-phase138-parity-uat-release-boundary.ts
@scripts/check-phase138-parity-uat-release-boundary/checks.ts
@scripts/check-phase138-parity-uat-release-boundary/constants.ts
@scripts/check-phase138-parity-uat-release-boundary/claims.ts
@scripts/check-phase138-parity-uat-release-boundary/verifier.ts
@scripts/check-phase138-parity-uat-release-boundary/test-fixtures.ts
@docs/parity/index.json

<interfaces>
Copy the Phase 138 export shape:

```typescript
export { checkPhase145ParityUatReleaseBoundary } from "./check-phase145-parity-uat-release-boundary/checks.ts";

export function checkPhase145ParityUatReleaseBoundary(maybeRepoRoot?: string): string[] {
  const repoRoot = path.resolve(
    maybeRepoRoot ?? process.env.OPEN_BITCOIN_PHASE145_REPO_ROOT ?? DEFAULT_REPO_ROOT,
  );
  return failures;
}
```

Repo-root reads must reject path escapes: `path.relative(repoRoot, absolute)` must not start with `..` (Phase 138 `isInsideRepo`).

Do **not** copy Phase 138 `verifier.ts` last-command pin. Phase 138, 124, 129, 130, 131, and `check-current-documentation-reconciliation.ts` require Phase 138 to remain the file-final `check-phase*` command. Phase 145 asserts 144 then 145 only.

Required D-14 sentence, copied verbatim (D-14):

disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present

Required D-16 deny topics, copied verbatim as positive-claim substrings:
- prune-mode product behavior
- archive-node or production-scale historical serving
- assumeutxo/assumevalid/IBD snapshot shortcuts
- compact-filter or BIP37 serving
- public serving or relay by default
- public-network CI or release-blocking historical-serving/long-chain flush runs
- production full-node readiness
- production service operation
- production-funds wallet safety
- LevelDB `chainstate/` live import/export
- automatic destructive reindex or coins repair

Store those as lowercase `DENIED_OVERCLAIMS` needles:
`prune-mode`, `archive-node`, `production-scale historical serving`, `assumeutxo`, `assumevalid`, `ibd snapshot`, `compact-filter`, `bip37`, `public serving or relay by default`, `public-network ci`, `production full-node readiness`, `production service operation`, `production-funds`, `leveldb chainstate`, `destructive reindex`.

Do not add `package relay` to a Phase 145 allow list. Keep Phase 138 `D21_SENTENCE` out of this checker's required-sentence list; Plan 03 keeps that sentence in live files so Phase 138 still passes.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `OPEN_BITCOIN_PHASE145_REPO_ROOT` → filesystem | A relative override can escape the fixture and read secrets or `.planning/` history. |
| Claim corpus → live docs | Scanning archives punishes historical prune/assumeutxo discussion and hides live overclaims. |
| Verifier-order assertion → later checkers | Treating Phase 145 as file-final `check-phase*` breaks Phase 138 and several v2.2 last-gate pins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-06 | Tampering | `OPEN_BITCOIN_PHASE145_REPO_ROOT` | mitigate | `path.resolve` then `isInsideRepo` before every `readFileSync`. Fail closed on missing or escaped paths. |
| T-145-07 | Information Disclosure | claim corpus | mitigate | Curated current files only (D-11). Never add `.planning/phases/` or `.planning/milestones/` to `CLAIM_FILES`. `afterEach` `rmSync` temp fixture roots. |
| T-145-08 | Spoofing | `claims.ts` | mitigate | Require the verbatim D-14 sentence on README and runtime-guide. Deny D-16 positive clauses. Accept `deferred`, `not run`, `future-gated`, `without broadening`, and `does not`. |
| T-145-09 | Denial of Service | checker source | mitigate | Filesystem-only: no `fetch(`, `Bun.spawn`, or `node:child_process`. Do not add public-network or wall-clock gates. |
| T-145-10 | Elevation of Privilege | `verifier.ts` | mitigate | Assert 144 then 145. Require 117 and 138 present. Do not assert Phase 145 is the last `check-phase*` in the file. Do not edit 124/129/130/131/138 last-gate helpers. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Phase 145 checker, ownership, claims, and fixtures</name>
  <files>
    scripts/check-phase145-parity-uat-release-boundary.ts
    scripts/check-phase145-parity-uat-release-boundary.test.ts
    scripts/check-phase145-parity-uat-release-boundary/checks.ts
    scripts/check-phase145-parity-uat-release-boundary/constants.ts
    scripts/check-phase145-parity-uat-release-boundary/claims.ts
    scripts/check-phase145-parity-uat-release-boundary/test-fixtures.ts
  </files>
  <read_first>
    scripts/check-phase138-parity-uat-release-boundary.ts
    scripts/check-phase138-parity-uat-release-boundary/checks.ts
    scripts/check-phase138-parity-uat-release-boundary/constants.ts
    scripts/check-phase138-parity-uat-release-boundary/claims.ts
    scripts/check-phase138-parity-uat-release-boundary/test-fixtures.ts
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
    docs/parity/index.json
  </read_first>
  <behavior>
    - `checkPhase145ParityUatReleaseBoundary()` on a complete fixture returns `[]`.
    - Duplicate or missing ownership of any of the 15 v2.3 IDs, or CSVFY on a non-closeout surface, fails.
    - Missing any of the five D-06 files on the closeout surface fails. A required-anchor list containing `HaveBlockData` as if it were a path or symbol fails the test suite (the constant list must not include that token).
    - Missing breadcrumb group `node-stored-block-presence` or missing `packages/bitcoin-knots/src/node/blockstorage.cpp` on that group fails.
    - A positive D-16 overclaim such as `Open Bitcoin provides prune-mode product behavior.` fails; the verbatim D-14 sentence passes; `Open Bitcoin does not add prune-mode product behavior.` passes.
    - Missing the D-14 sentence on README or runtime-guide fails.
    - Checker source contains none of `fetch(`, `Bun.spawn`, `node:child_process`.
    - Adding `.planning/milestones` to the fixture claim corpus is not required for a pass.
  </behavior>
  <action>
    Follow the Phase 138 directory split so no new file exceeds the repo ~628-line trigger. Root `scripts/check-phase145-parity-uat-release-boundary.ts` is CLI + `export { checkPhase145ParityUatReleaseBoundary }` only, with `if (import.meta.main)` printing failures and `process.exit(1)`.

    Implement `checkPhase145ParityUatReleaseBoundary(maybeRepoRoot?: string): string[]` in `checks.ts` using `OPEN_BITCOIN_PHASE145_REPO_ROOT` (D-09). Prefix the optional argument `maybeRepoRoot`. Do not spawn cargo, bun, or the network.

    `constants.ts` must encode:

    - `D14_SENTENCE` = `disk-backed per-outpoint coins, typed cache-flush policy, fuller chainstate-manager behavior for the single active chainstate, and honest stored-block availability that serves or reports a stored block only when the payload bytes are present`
    - `D14_REQUIRED_FILES` = `README.md`, `docs/operator/runtime-guide.md`
    - `CLOSEOUT_SURFACE` = `v2-3-parity-roots-and-no-claim-guardrails`
    - `REQUIREMENTS_BY_SURFACE` matching the Plan 01 table exactly
    - `CLAIM_FILES` = `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/checklist.md`, `docs/parity/catalog/chainstate.md`, `docs/parity/catalog/p2p.md`, `docs/parity/release-readiness.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`
    - Do not put any `.planning/` path in `CLAIM_FILES`
    - `REQUIRED_KNOTS_ANCHORS` = the five D-06 files only
    - `REQUIRED_BREADCRUMB_GROUPS` = `chainstate-engine`, `node-coins-adapter`, `node-chainstate-adapter`, `node-network-chainstate-durability-evidence`, `node-stored-block-presence`
    - `NO_CLAIM_MARKERS` copied from Phase 138, including `future`, `future-gated`, `deferred`, `not run`, `without broadening`, `does not`, `remain deferred`
    - `DENIED_OVERCLAIMS` using the lowercase needles listed in interfaces
    - `REQUIRED_UAT_COMMANDS` listed in Task 2 (define the array now so fixtures can include them)

    Copy Phase 138 `claims.ts` paragraph/clause helpers. Require `D14_SENTENCE` on `D14_REQUIRED_FILES`. Do not require the v2.2 D-21 sentence in this checker; Plan 03 keeps that sentence in the live files.

    Surface ownership: all 15 v2.3 IDs exactly once. CSVFY-01 and CSVFY-02 only on the closeout surface. Required top-level names are the seven `v2-3-*` IDs. This plan must NOT require `done` status and must NOT require REQUIREMENTS `- [x]` (Plan 04 tightens those pins).

    `test-fixtures.ts`: `createFixture({ maybeMutate })` writes the curated corpus into a temp root, including in_progress surfaces, the D-14 sentence, D-06 anchors, breadcrumb groups, UAT command strings, and a `scripts/verify.sh` stub. Tests are Arrange / Act / Assert, one mutation each. `afterEach` deletes temp roots.

    Required mutations in `scripts/check-phase145-parity-uat-release-boundary.test.ts`: passing fixture; duplicate requirement owner; CSVFY on a Phase 139 surface; missing D-06 file on closeout; missing `node-stored-block-presence`; missing D-14 sentence; positive `Open Bitcoin provides prune-mode product behavior.`; positive `Open Bitcoin provides archive-node or production-scale historical serving.`; positive `Open Bitcoin enables assumeutxo`; accepted deferred `Open Bitcoin does not add prune-mode product behavior.`; accepted D-14 sentence.

    Do not wire `scripts/verify.sh`. Do not edit Phase 138/124/129/130/131/117 checkers.
  </action>
  <verify>
    <automated>bun test scripts/check-phase145-parity-uat-release-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase145-parity-uat-release-boundary.ts` exports `checkPhase145ParityUatReleaseBoundary` and the implementation reads `OPEN_BITCOIN_PHASE145_REPO_ROOT`
    - `constants.ts` contains the verbatim D-14 sentence and does not contain `HaveBlockData` as a required-anchor token
    - `CLAIM_FILES` does not contain `.planning/phases` or `.planning/milestones`
    - checker source contains none of `fetch(`, `Bun.spawn`, `node:child_process`
    - `bun test scripts/check-phase145-parity-uat-release-boundary.test.ts` exits 0
    - a fixture paragraph `Open Bitcoin provides prune-mode product behavior.` fails
    - a fixture paragraph containing the verbatim D-14 sentence and `Open Bitcoin does not add prune-mode product behavior.` passes
    - no new file under `scripts/check-phase145-parity-uat-release-boundary/` exceeds 628 lines
  </acceptance_criteria>
  <done>The mutation suite proves ownership, D-06 anchors, breadcrumbs, and D-14/D-16 claims against fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert 144-then-145 verifier order, 117/138 presence, and historical phase paths</name>
  <files>
    scripts/check-phase145-parity-uat-release-boundary/verifier.ts
    scripts/check-phase145-parity-uat-release-boundary/constants.ts
    scripts/check-phase145-parity-uat-release-boundary/checks.ts
    scripts/check-phase145-parity-uat-release-boundary/test-fixtures.ts
    scripts/check-phase145-parity-uat-release-boundary.test.ts
  </files>
  <read_first>
    scripts/check-phase138-parity-uat-release-boundary/verifier.ts
    scripts/check-phase144-operator-flush-availability-evidence.ts
    scripts/verify.sh
    .planning/phases/145-parity-roots-and-no-claim-guardrails/145-RESEARCH.md
  </read_first>
  <behavior>
    - Fixture `verify.sh` whose visible and executable order is Phase 144 test/check then Phase 145 test/check passes that check.
    - Fixture missing the 144 pair immediately before the 145 pair fails.
    - Fixture missing `bun run scripts/check-phase117-parity-uat-release-boundary.ts` or `bun run scripts/check-phase138-parity-uat-release-boundary.ts` fails.
    - Fixture `run_step` lines containing `public-network`, `wall-clock`, `run-live-mainnet-smoke`, or `command-timings.ts` fail.
    - The checker does not fail a fixture whose last `check-phase*` command is still Phase 138.
    - Missing a verifier-referenced `.planning/phases/...` path that appears in fixture `verify.sh` or `scripts/check-phase*.ts` fails with `missing verifier-referenced historical phase path`.
    - Required UAT command strings missing from 145-UAT.md or the runtime-guide fail.
  </behavior>
  <action>
    Add `verifier.ts` (D-10, D-13, D-20, D-21). Assert all of:

    1. Visible `VERIFY_COMMAND_ORDER` and executable `run_step` lines contain this exact adjacent sequence:

    ```
    bun test scripts/check-phase144-operator-flush-availability-evidence.test.ts
    bun run scripts/check-phase144-operator-flush-availability-evidence.ts
    bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
    bun run scripts/check-phase145-parity-uat-release-boundary.ts
    ```

    2. Both texts still contain `bun run scripts/check-phase117-parity-uat-release-boundary.ts` and `bun run scripts/check-phase138-parity-uat-release-boundary.ts`.
    3. Default `run_step` lines do not contain `public-network`, `wall-clock`, `run-live-mainnet-smoke`, or `command-timings.ts`.
    4. Do **not** assert that Phase 145 is the last `check-phase*` in the whole file. A passing fixture may still end with `bun run scripts/check-phase138-parity-uat-release-boundary.ts`.

    Historical phase-dir check (D-21): scan fixture `scripts/verify.sh` plus every fixture `scripts/check-phase*.ts` for `/.planning\/phases\/[A-Za-z0-9._-]+/` and fail if a referenced path is missing or escapes the repo. Do not require every historical directory in the tree — only paths those verifier files already name.

    Pin `REQUIRED_UAT_COMMANDS` to these exact strings (D-19) and require them to appear in `145-UAT.md` or `docs/operator/runtime-guide.md` inside the fixture:

    ```
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format human
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- status --format json
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format human
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- status --format json
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- support bundle --output-dir=/tmp/open-bitcoin-chainstate-durability-support
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- support bundle --output-dir=/tmp/open-bitcoin-chainstate-durability-support
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind -- -datadir=/tmp/open-bitcoin-preview
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -rpcconnect=127.0.0.1 -rpcport=18443 -rpcuser=preview -rpcpassword=preview getblockchaininfo
    bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -rpcconnect=127.0.0.1 -rpcport=18443 -rpcuser=preview -rpcpassword=preview getblockchaininfo
    bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
    bun run scripts/check-phase145-parity-uat-release-boundary.ts
    bash scripts/verify.sh
    ```

    Add mutations: 145 placed before 144; 117 removed; 138 removed; `run-live-mainnet-smoke` added as a `run_step`; last command still 138 still passes; missing `/tmp/open-bitcoin-chainstate-durability-support` command; referenced `.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails` deleted from the fixture tree.

    Do not wire live `scripts/verify.sh`. Do not retarget Phase 138 `final gate must end with` assertions.
  </action>
  <verify>
    <automated>bun test scripts/check-phase145-parity-uat-release-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `verifier.ts` contains `check-phase144-operator-flush-availability-evidence` and `check-phase145-parity-uat-release-boundary`
    - `verifier.ts` contains `check-phase117-parity-uat-release-boundary` and `check-phase138-parity-uat-release-boundary`
    - `verifier.ts` does not contain `final gate must end with` or `lastPhaseCommand !==`
    - `constants.ts` contains `status --format human`, `open-bitcoind`, and `/tmp/open-bitcoin-chainstate-durability-support`
    - a fixture whose last `check-phase*` is Phase 138 still returns `[]` when 144-then-145 order and 117/138 presence are intact
    - `bun test scripts/check-phase145-parity-uat-release-boundary.test.ts` exits 0
    - `rg -n "check-phase145-parity-uat-release-boundary" scripts/verify.sh` prints no matches
  </acceptance_criteria>
  <done>Verifier fixtures prove 144-then-145 order, keep 138 as file-final, and fail missing historical phase paths.</done>
</task>

</tasks>

<verification>
```bash
bun test scripts/check-phase145-parity-uat-release-boundary.test.ts
rg -n "HaveBlockData" scripts/check-phase145-parity-uat-release-boundary
rg -n "check-phase145-parity-uat-release-boundary" scripts/verify.sh
git diff --check
```
</verification>

<success_criteria>
- The Phase 145 checker names 15-ID ownership, D-06 files, breadcrumbs, D-14/D-16 claims, UAT commands, 144-then-145 order, 117/138 presence, and historical phase-dir presence.
- The checker is filesystem-only and ignores `.planning/` history as a claim surface.
- Live `scripts/verify.sh` is unchanged.
- Leftover REQUIREMENTS checkboxes and surface `done` promotions are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/145-parity-roots-and-no-claim-guardrails/145-02-SUMMARY.md`
</output>
