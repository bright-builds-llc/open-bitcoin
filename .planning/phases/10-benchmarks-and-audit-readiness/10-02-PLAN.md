---
phase: 10-benchmarks-and-audit-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/parity/index.json
  - docs/parity/README.md
  - docs/parity/catalog/README.md
  - docs/parity/checklist.md
  - docs/parity/deviations-and-unknowns.md
  - .planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md
autonomous: true
requirements:
  - AUD-01
user_setup: []
must_haves:
  truths:
    - "A contributor can inspect one parity checklist covering every in-scope initial-milestone surface per D-06."
    - "Checklist statuses use exactly `planned`, `in_progress`, `done`, `deferred`, and `out_of_scope` per D-07."
    - "Every `done` item has concise evidence links, and every `deferred` or `out_of_scope` item has rationale per D-07."
    - "`docs/parity/index.json` remains the machine-readable root per D-08."
    - "Known gaps and suspected unknowns are surfaced instead of hidden per D-09."
  artifacts:
    - path: docs/parity/index.json
      provides: "Machine-readable checklist root"
      contains: "status_taxonomy"
    - path: docs/parity/checklist.md
      provides: "Human-readable parity checklist"
      contains: "reference-baseline"
    - path: docs/parity/deviations-and-unknowns.md
      provides: "Known gaps, suspected unknowns, deferred surfaces, and folded todo risk capture"
      contains: "Panic And Illegal-State Exposure"
  key_links:
    - from: docs/parity/index.json
      to: docs/parity/checklist.md
      via: "checklist.path"
      pattern: "\"path\": \"checklist.md\""
    - from: docs/parity/checklist.md
      to: .planning/phases
      via: "evidence links for done surfaces"
      pattern: ".planning/phases/"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-24T10-47-33
generated_at: 2026-04-24T11:03:39Z
---

<objective>
Create the parity checklist and deviation or unknown audit artifacts.

Purpose: Satisfy `AUD-01` by making every in-scope initial-milestone surface reviewable through a machine-readable root and concise Markdown views.
Output: Extended `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/deviations-and-unknowns.md`, and discovery links from the parity docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@docs/parity/README.md
@docs/parity/index.json
@docs/parity/catalog/README.md
@docs/parity/catalog/core-domain-and-serialization.md
@docs/parity/catalog/consensus-validation.md
@docs/parity/catalog/chainstate.md
@docs/parity/catalog/mempool-policy.md
@docs/parity/catalog/p2p.md
@docs/parity/catalog/wallet.md
@docs/parity/catalog/rpc-cli-config.md
@docs/parity/catalog/verification-harnesses.md
@docs/parity/benchmarks.md
@.planning/phases/08-rpc-cli-and-config-parity/08-VERIFICATION.md
@.planning/phases/09-parity-harnesses-and-fuzzing/09-VERIFICATION.md
@.planning/todos/pending/2026-04-18-ai-agent-friendly-cli-surface.md
@.planning/todos/pending/2026-04-18-sweep-panics-and-illegal-states.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add machine-readable checklist data to the parity index</name>
  <read_first>
    - .planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
    - .planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - docs/parity/index.json
    - docs/parity/catalog/core-domain-and-serialization.md
    - docs/parity/catalog/consensus-validation.md
    - docs/parity/catalog/chainstate.md
    - docs/parity/catalog/mempool-policy.md
    - docs/parity/catalog/p2p.md
    - docs/parity/catalog/wallet.md
    - docs/parity/catalog/rpc-cli-config.md
    - docs/parity/catalog/verification-harnesses.md
    - docs/parity/benchmarks.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-VERIFICATION.md
    - .planning/phases/09-parity-harnesses-and-fuzzing/09-VERIFICATION.md
  </read_first>
  <files>
    docs/parity/index.json
  </files>
  <behavior>
    - Test 1: every checklist surface status is one of the five locked taxonomy values.
    - Test 2: every `done` surface has at least one evidence link.
    - Test 3: every `deferred` or `out_of_scope` surface has a non-empty rationale.
  </behavior>
  <action>
    Extend `docs/parity/index.json` without replacing the existing `baseline`, `deviations`, `catalog`, or `surfaces` roots. Add a top-level `checklist` object with these exact keys:
    - `path`: `"checklist.md"`
    - `status_taxonomy`: `["planned", "in_progress", "done", "deferred", "out_of_scope"]`
    - `surfaces`: an array of checklist records

    Each checklist record must use these exact keys: `id`, `title`, `status`, `requirements`, `evidence`, `rationale`, `known_gaps`, and `suspected_unknowns`. Use only the locked status values from D-07.

    Required checklist surface ids per D-06:
    - `reference-baseline`
    - `architecture-workspace`
    - `core-serialization`
    - `consensus-validation`
    - `chainstate`
    - `mempool-policy`
    - `p2p-networking`
    - `wallet`
    - `rpc-cli-config`
    - `verification-harnesses-fuzzing`
    - `benchmarks-audit-readiness`

    Set completed prior surfaces to `done` only when they have evidence links to existing catalog docs, verification reports, summaries, or scripts. Set `benchmarks-audit-readiness` to `in_progress` in this task, with evidence to `docs/parity/benchmarks.md`, `scripts/run-benchmarks.sh`, and `10-01-SUMMARY.md`; Plan 10-03 will promote it after release-readiness exists. Do not add GUI, dashboard, broad CLI feature work, broad panic refactors, or the reviewed reduce-nesting todo to the checklist.

    Add a top-level `audit` object with keys `deviations_and_unknowns`, `benchmarks`, and `release_readiness`. Use:
    - `deviations_and_unknowns.path`: `"deviations-and-unknowns.md"`
    - `benchmarks.path`: `"benchmarks.md"`
    - `benchmarks.report_dir`: `"packages/target/benchmark-reports"`
    - `release_readiness.path`: `"release-readiness.md"`
    - `release_readiness.status`: `"planned"`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const idx=JSON.parse(fs.readFileSync('docs/parity/index.json','utf8')); const allowed=['planned','in_progress','done','deferred','out_of_scope']; if (JSON.stringify(idx.checklist.status_taxonomy)!==JSON.stringify(allowed)) throw new Error('bad taxonomy'); const ids=idx.checklist.surfaces.map(s=>s.id); for (const id of ['reference-baseline','architecture-workspace','core-serialization','consensus-validation','chainstate','mempool-policy','p2p-networking','wallet','rpc-cli-config','verification-harnesses-fuzzing','benchmarks-audit-readiness']) if (!ids.includes(id)) throw new Error('missing '+id); for (const s of idx.checklist.surfaces) { if (!allowed.includes(s.status)) throw new Error('bad status '+s.id); if (s.status==='done' && (!Array.isArray(s.evidence)||s.evidence.length===0)) throw new Error('missing evidence '+s.id); if ((s.status==='deferred'||s.status==='out_of_scope') && !s.rationale) throw new Error('missing rationale '+s.id); }"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` contains `"status_taxonomy": [` followed by the exact five statuses `planned`, `in_progress`, `done`, `deferred`, and `out_of_scope`.
    - `docs/parity/index.json` contains all eleven required surface ids.
    - `docs/parity/index.json` contains `"deviations_and_unknowns"`, `"benchmarks"`, and `"release_readiness"` under a top-level `"audit"` object.
    - No checklist record uses `seeded`, `complete`, `unknown`, `blocked`, or any status outside the locked taxonomy.
  </acceptance_criteria>
  <done>The parity index is the machine-readable checklist root and validates the locked taxonomy, required surface coverage, and evidence/rationale rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add human-readable checklist and deviation or unknown views</name>
  <read_first>
    - .planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
    - .planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
    - docs/parity/index.json
    - docs/parity/README.md
    - docs/parity/catalog/README.md
    - docs/parity/catalog/core-domain-and-serialization.md
    - docs/parity/catalog/rpc-cli-config.md
    - docs/parity/catalog/verification-harnesses.md
    - .planning/todos/pending/2026-04-18-ai-agent-friendly-cli-surface.md
    - .planning/todos/pending/2026-04-18-sweep-panics-and-illegal-states.md
  </read_first>
  <files>
    docs/parity/README.md
    docs/parity/catalog/README.md
    docs/parity/checklist.md
    docs/parity/deviations-and-unknowns.md
  </files>
  <action>
    Create `docs/parity/checklist.md` as the human-readable view of `docs/parity/index.json`. It must contain these exact headings: `# Parity Checklist`, `## Status Taxonomy`, `## Surface Status`, and `## Evidence Rules`. The `## Surface Status` table must include columns `Surface`, `Status`, `Requirements`, `Evidence`, `Known Gaps`, and `Suspected Unknowns`. Include every required surface id from Task 1 exactly once.

    Create `docs/parity/deviations-and-unknowns.md` with these exact headings: `# Deviations And Unknowns`, `## Intentional Deviations`, `## Deferred Surfaces`, `## Suspected Unknowns`, `## Folded Todo Audit`, and `## Follow-Up Triggers`. Include:
    - current `docs/parity/index.json` deviations, even when empty
    - deferred or out-of-scope surfaces from `rpc-cli-config.md` and `verification-harnesses.md`
    - suspected unknowns summarized from all catalog entries
    - an `AI-Agent-Friendly CLI Surface` entry that treats the folded todo as evidence/risk review only, not new Phase 10 CLI behavior
    - a `Panic And Illegal-State Exposure` entry that treats the folded todo as a risk/follow-up capture item only, not a broad refactor

    Update `docs/parity/README.md` to list `checklist.md`, `deviations-and-unknowns.md`, and `benchmarks.md` under `## Files`. Update `docs/parity/catalog/README.md` so its current entries table includes `wallet.md` and `rpc-cli-config.md` if they are missing, while preserving existing entries.
  </action>
  <verify>
    <automated>rg "reference-baseline|architecture-workspace|core-serialization|consensus-validation|chainstate|mempool-policy|p2p-networking|wallet|rpc-cli-config|verification-harnesses-fuzzing|benchmarks-audit-readiness" docs/parity/checklist.md</automated>
    <automated>rg "AI-Agent-Friendly CLI Surface|Panic And Illegal-State Exposure|Follow-Up Triggers" docs/parity/deviations-and-unknowns.md</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/checklist.md` contains the exact status taxonomy string ``planned`, `in_progress`, `done`, `deferred`, `out_of_scope``.
    - `docs/parity/checklist.md` contains all eleven required surface ids exactly once in the surface table.
    - `docs/parity/deviations-and-unknowns.md` contains the headings `Intentional Deviations`, `Deferred Surfaces`, `Suspected Unknowns`, `Folded Todo Audit`, and `Follow-Up Triggers`.
    - `docs/parity/deviations-and-unknowns.md` contains `AI-Agent-Friendly CLI Surface` and `Panic And Illegal-State Exposure`, and does not instruct implementation of broad CLI features or broad panic refactors.
    - `docs/parity/README.md` links to `checklist.md`, `deviations-and-unknowns.md`, and `benchmarks.md`.
    - `docs/parity/catalog/README.md` links to `wallet.md` and `rpc-cli-config.md`.
  </acceptance_criteria>
  <done>Reviewers have concise Markdown views for checklist status, evidence, deviations, deferred surfaces, suspected unknowns, and folded todo risks without duplicating whole phase reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity JSON to reviewers/tools | Machine-readable checklist data is consumed by humans and future tooling. |
| Catalog summaries to audit docs | Existing catalog gaps and suspected unknowns are summarized into Markdown. |
| Planning todos to audit docs | Folded todos enter audit output as risk/evidence items, not implementation scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | S | `docs/parity/index.json` | mitigate | Validate every status against the exact locked taxonomy and require evidence links for `done` checklist records. |
| T-10-02-02 | T | `docs/parity/checklist.md` | mitigate | Treat Markdown as a view of `index.json`; include all required surface ids exactly once and run targeted grep checks. |
| T-10-02-03 | I | `docs/parity/deviations-and-unknowns.md` | mitigate | Summarize env-gated Knots and CLI/RPC surfaces without copying secrets, credentials, or raw environment values. |
| T-10-02-04 | R | `docs/parity/deviations-and-unknowns.md` | mitigate | Preserve deferred rationale and follow-up triggers so gaps are attributable to documented scope decisions rather than silent omission. |
</threat_model>

<verification>
- `node -e "const fs=require('fs'); const idx=JSON.parse(fs.readFileSync('docs/parity/index.json','utf8')); if (!idx.checklist || !idx.audit) throw new Error('missing audit roots')"`
- `rg "AI-Agent-Friendly CLI Surface|Panic And Illegal-State Exposure" docs/parity/deviations-and-unknowns.md`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- `AUD-01` is materially satisfied by a parity checklist that covers every in-scope initial-milestone surface.
- Checklist data lives in `docs/parity/index.json` and Markdown companions only help reviewers navigate it.
- Deferred ideas remain deferred: no public dashboard, no broad CLI additions, no broad panic refactor, and no reduce-nesting follow-on work.
</success_criteria>

<output>
After completion, create `.planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md`.
</output>
