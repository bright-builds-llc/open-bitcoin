---
phase: 10-benchmarks-and-audit-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - packages/open-bitcoin-bench/src/lib.rs
  - packages/open-bitcoin-bench/src/main.rs
  - packages/open-bitcoin-bench/src/fixtures.rs
  - packages/open-bitcoin-bench/src/cases.rs
  - packages/open-bitcoin-bench/src/cases/consensus.rs
  - packages/open-bitcoin-bench/src/cases/codec.rs
  - packages/open-bitcoin-bench/src/cases/chainstate.rs
  - packages/open-bitcoin-bench/src/cases/mempool.rs
  - packages/open-bitcoin-bench/src/cases/network.rs
  - packages/open-bitcoin-bench/src/cases/wallet.rs
  - packages/open-bitcoin-bench/src/cases/rpc_cli.rs
  - .planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md
autonomous: true
requirements:
  - PAR-02
user_setup: []
must_haves:
  truths:
    - "Benchmark smoke execution covers all critical first-party paths named in D-01."
    - "Stateful benchmark cases reset or clone prepared fixtures so mutation does not leak across iterations."
    - "Optional Knots JSON/bin inputs are recorded as report metadata only; default execution remains mapping-only per D-03."
    - "A smoke run emits JSON and Markdown reports without timing thresholds per D-04 and D-05."
  artifacts:
    - path: packages/open-bitcoin-bench/src/fixtures.rs
      provides: "Deterministic benchmark fixtures adapted from existing tests"
      contains: "BenchFixtures"
    - path: packages/open-bitcoin-bench/src/cases.rs
      provides: "Measured case registration across all benchmark groups"
      contains: "registered_cases"
    - path: packages/open-bitcoin-bench/src/main.rs
      provides: "CLI execution for list/smoke/full modes and optional Knots inputs"
      contains: "optional_knots_source"
  key_links:
    - from: packages/open-bitcoin-bench/src/cases.rs
      to: packages/open-bitcoin-bench/src/registry.rs
      via: "case group ids and Knots mappings"
      pattern: "registry"
    - from: packages/open-bitcoin-bench/src/main.rs
      to: packages/open-bitcoin-bench/src/report.rs
      via: "JSON and Markdown report writing"
      pattern: "write"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-24T10-47-33
generated_at: 2026-04-24T11:14:30Z
---

<objective>
Implement benchmark fixtures and measured cases for every required group.

Purpose: Complete the `PAR-02` runtime benchmark surface while keeping Plan 10-01's contracts intact and preserving D-01 coverage without making full Knots execution a default requirement.
Output: Deterministic fixtures, benchmark case modules, CLI execution wiring, and smoke JSON/Markdown output from `open-bitcoin-bench`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md
@AGENTS.md
@packages/open-bitcoin-bench/src/registry.rs
@packages/open-bitcoin-bench/src/runner.rs
@packages/open-bitcoin-bench/src/report.rs
@packages/open-bitcoin-chainstate/tests/parity.rs
@packages/open-bitcoin-mempool/tests/parity.rs
@packages/open-bitcoin-network/tests/parity.rs
@packages/open-bitcoin-wallet/src/wallet/tests.rs
@packages/open-bitcoin-rpc/tests/black_box_parity.rs
@packages/open-bitcoin-codec/testdata/block_header.hex
@packages/open-bitcoin-codec/testdata/transaction_valid.hex
@packages/open-bitcoin-codec/testdata/message_header.hex

<interfaces>
Use the Plan 10-01 contracts directly:

```rust
pub fn registered_cases() -> Vec<BenchCase>;
pub fn run_case(case: &BenchCase, mode: RunMode) -> Result<BenchResult, BenchError>;
pub fn write_json_report(report: &BenchReport, path: &Path) -> Result<(), BenchError>;
pub fn write_markdown_report(report: &BenchReport, path: &Path) -> Result<(), BenchError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic fixtures and consensus, codec, chainstate, mempool, and network cases</name>
  <files>
    packages/open-bitcoin-bench/src/lib.rs
    packages/open-bitcoin-bench/src/fixtures.rs
    packages/open-bitcoin-bench/src/cases.rs
    packages/open-bitcoin-bench/src/cases/consensus.rs
    packages/open-bitcoin-bench/src/cases/codec.rs
    packages/open-bitcoin-bench/src/cases/chainstate.rs
    packages/open-bitcoin-bench/src/cases/mempool.rs
    packages/open-bitcoin-bench/src/cases/network.rs
  </files>
  <behavior>
    - Test 1: `registered_cases()` includes at least one case for `consensus-script`, `block-transaction-codec`, `chainstate`, `mempool-policy`, and `network-wire-sync`.
    - Test 2: stateful chainstate and mempool cases can run twice in a row with the same prepared fixture state.
    - Test 3: codec and network cases consume checked-in fixture bytes or fixture-built values and return errors instead of panicking on fixture failures.
  </behavior>
  <action>
    Add `fixtures.rs` for deterministic setup adapted from existing tests, keeping fixture construction outside measured loops unless setup is explicitly the measured operation. Add `cases.rs` plus `cases/consensus.rs`, `cases/codec.rs`, `cases/chainstate.rs`, `cases/mempool.rs`, and `cases/network.rs`.

    Required coverage from D-01:
    - `consensus-script`: exercise `verify_input_script`, `count_legacy_sigops`, or another existing script validation helper using deterministic standard-script or P2SH-style contexts.
    - `block-transaction-codec`: exercise transaction parse/encode plus block header or block parse/encode using checked-in testdata or fixture-built values.
    - `chainstate`: exercise `Chainstate::connect_block`, `disconnect_tip`, or `reorg` using cloned prepared snapshots so mutation does not leak across iterations.
    - `mempool-policy`: exercise `Mempool::accept_transaction` and one cheap policy helper such as standard-transaction validation or virtual-size calculation.
    - `network-wire-sync`: exercise `WireNetworkMessage::encode_wire`, `ParsedNetworkMessage::decode_wire`, and either `HeaderStore::locator` or a cheap peer/sync planning helper.

    Do not widen production visibility broadly. If a fixture needs support-only construction, keep it in the benchmark crate by composing public APIs or add the narrowest test/bench support helper consistent with existing crate patterns.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features</automated>
    <automated>node -e "const fs=require('fs'); const body=fs.readFileSync('packages/open-bitcoin-bench/src/cases.rs','utf8')+fs.readdirSync('packages/open-bitcoin-bench/src/cases').map(f=>fs.readFileSync('packages/open-bitcoin-bench/src/cases/'+f,'utf8')).join('\\n'); for (const id of ['consensus-script','block-transaction-codec','chainstate','mempool-policy','network-wire-sync']) { if (!body.includes(id)) throw new Error('missing benchmark case '+id); }"</automated>
  </verify>
  <done>Core node benchmark cases are registered, repeatable, fixture-backed, and covered by focused tests without adding timing gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add wallet and RPC/CLI cases plus CLI report execution</name>
  <files>
    packages/open-bitcoin-bench/src/main.rs
    packages/open-bitcoin-bench/src/fixtures.rs
    packages/open-bitcoin-bench/src/cases.rs
    packages/open-bitcoin-bench/src/cases/wallet.rs
    packages/open-bitcoin-bench/src/cases/rpc_cli.rs
  </files>
  <behavior>
    - Test 1: `registered_cases()` includes at least one case for `wallet` and `rpc-cli`.
    - Test 2: `cargo run ... -- --smoke --output-dir packages/target/benchmark-reports` creates `open-bitcoin-bench-smoke.json` and `open-bitcoin-bench-smoke.md`.
    - Test 3: `--knots-json <PATH>` and `--knots-bin <PATH>` record optional Knots source metadata without requiring either path by default.
  </behavior>
  <action>
    Add `cases/wallet.rs` and `cases/rpc_cli.rs`. Wallet cases must exercise `Wallet::rescan_chainstate`, `balance`, and either `build_and_sign` or `build_transaction` plus `sign_transaction`. RPC/CLI cases must exercise `normalize_method_call`, `dispatch` against `ManagedRpcContext::for_local_operator`, `parse_cli_args`, and `render_rpc_result` where those APIs are available through the existing first-party interface surface.

    Update `cases.rs` so `registered_cases()` returns all seven required D-01 groups. Update `main.rs` so `--smoke`, `--full`, `--iterations <N>`, `--output-dir <PATH>`, `--knots-json <PATH>`, `--knots-bin <PATH>`, and `--list` are supported. Default output directory must be `packages/target/benchmark-reports` when no explicit path is provided. Write `open-bitcoin-bench-smoke.json` and `open-bitcoin-bench-smoke.md` for smoke mode, and corresponding `full` filenames for full mode. Unknown flags must return a nonzero exit with an actionable error and no panic.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features</automated>
    <automated>cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --smoke --output-dir packages/target/benchmark-reports</automated>
    <automated>node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('packages/target/benchmark-reports/open-bitcoin-bench-smoke.json','utf8')); const ids=new Set(r.groups.map(g=>g.id)); for (const id of ['consensus-script','block-transaction-codec','chainstate','mempool-policy','network-wire-sync','wallet','rpc-cli']) { if (!ids.has(id)) throw new Error('missing benchmark group '+id); } if (r.groups.some(g=>g.threshold_status)) throw new Error('timing threshold leaked into smoke report'); if (!fs.existsSync('packages/target/benchmark-reports/open-bitcoin-bench-smoke.md')) throw new Error('missing markdown report');"</automated>
  </verify>
  <done>All D-01 benchmark groups execute through the benchmark CLI, emit JSON/Markdown smoke reports, and keep optional Knots inputs explicit rather than default verification requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark fixtures to first-party APIs | Benchmark cases call consensus, codec, chainstate, mempool, network, wallet, RPC, and CLI APIs. |
| CLI args to report output | User-selected mode, iterations, output dir, and optional Knots paths affect report generation. |
| Optional Knots paths to report metadata | External JSON/bin paths are recorded when explicitly provided. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | D | `packages/open-bitcoin-bench/src/main.rs` | mitigate | Enforce smoke iteration bounds and full-mode hard cap inherited from `runner.rs`; reject invalid iteration values. |
| T-10-02-02 | T | `packages/open-bitcoin-bench/src/cases/*.rs` | mitigate | Reset or clone mutable fixtures for stateful cases so iteration count cannot change measured preconditions. |
| T-10-02-03 | I | `packages/open-bitcoin-bench/src/main.rs` | mitigate | Record optional Knots file paths only; do not read or print environment dumps or credentials. |
| T-10-02-04 | R | `packages/open-bitcoin-bench/src/report.rs` | mitigate | Smoke JSON must include every group id and no timing threshold status, proving reports are trend/audit evidence only. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features`
- `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --smoke --output-dir packages/target/benchmark-reports`
- `node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('packages/target/benchmark-reports/open-bitcoin-bench-smoke.json','utf8')); if (!Array.isArray(r.groups) || r.groups.length < 7) throw new Error('benchmark report missing groups')"`
</verification>

<success_criteria>
- Every D-01 benchmark group has at least one smoke-safe first-party case.
- Stateful cases remain repeatable across iterations.
- Smoke reports are structured, threshold-free, and independent of default Knots execution.
</success_criteria>

<output>
After completion, create `.planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md`.
</output>
