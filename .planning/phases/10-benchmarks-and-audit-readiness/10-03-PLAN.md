---
phase: 10-benchmarks-and-audit-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - scripts/run-benchmarks.sh
  - scripts/verify.sh
  - .github/workflows/ci.yml
  - docs/parity/benchmarks.md
  - .planning/phases/10-benchmarks-and-audit-readiness/10-03-SUMMARY.md
autonomous: true
requirements:
  - PAR-02
user_setup: []
must_haves:
  truths:
    - "A contributor can run benchmarks through a repo-owned `scripts/run-benchmarks.sh` entrypoint."
    - "`scripts/verify.sh` invokes only bounded smoke mode after Plan 10-02 proves smoke execution per the resolved research choice."
    - "CI preserves generated benchmark reports as artifacts without making timing thresholds release gates."
    - "Reviewers can inspect checked-in benchmark scope, report locations, and Knots mapping policy."
  artifacts:
    - path: scripts/run-benchmarks.sh
      provides: "Repo-owned benchmark wrapper"
      contains: "OPEN_BITCOIN_BENCHMARK_REPORT_DIR"
    - path: scripts/verify.sh
      provides: "Repo-native smoke benchmark invocation"
      contains: "run-benchmarks.sh --smoke"
    - path: .github/workflows/ci.yml
      provides: "Benchmark report artifact upload"
      contains: "benchmark-reports"
    - path: docs/parity/benchmarks.md
      provides: "Reviewer-facing benchmark scope and Knots mapping guide"
      contains: "Benchmark Groups"
  key_links:
    - from: scripts/verify.sh
      to: scripts/run-benchmarks.sh
      via: "bounded smoke invocation"
      pattern: "run-benchmarks.sh --smoke"
    - from: docs/parity/benchmarks.md
      to: packages/target/benchmark-reports
      via: "generated report path"
      pattern: "open-bitcoin-bench-smoke.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-24T10-47-33
generated_at: 2026-04-24T11:14:30Z
---

<objective>
Wire benchmark execution into repo verification, CI artifacts, and parity docs.

Purpose: Finish the `PAR-02` benchmark surface by making bounded smoke reports easy to generate, verify, upload, and review without default Knots builds or timing gates.
Output: `scripts/run-benchmarks.sh`, `scripts/verify.sh` smoke integration, CI artifact upload, and `docs/parity/benchmarks.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh
@.github/workflows/ci.yml
@packages/open-bitcoin-bench/src/main.rs
@packages/open-bitcoin-bench/src/registry.rs
@packages/open-bitcoin-bench/src/report.rs
@docs/parity/README.md

<interfaces>
Use the Plan 10-02 CLI contract:

```text
open-bitcoin-bench --list
open-bitcoin-bench --smoke --output-dir <PATH>
open-bitcoin-bench --full --iterations <N> --output-dir <PATH>
open-bitcoin-bench --smoke --output-dir <PATH> --knots-json <PATH>
open-bitcoin-bench --smoke --output-dir <PATH> --knots-bin <PATH>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark wrapper script with bounded smoke defaults</name>
  <files>
    scripts/run-benchmarks.sh
  </files>
  <action>
    Add `scripts/run-benchmarks.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Support these exact options: `--smoke`, `--full`, `--iterations <N>`, `--output-dir <PATH>`, `--knots-json <PATH>`, `--knots-bin <PATH>`, and `--list`. Default output dir must be `${OPEN_BITCOIN_BENCHMARK_REPORT_DIR:-$PWD/packages/target/benchmark-reports}`. The script must call `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- ...` using Bash arrays and must not use `eval`, `bash -c`, or shell-built command strings from user input. Make the script executable.
  </action>
  <verify>
    <automated>bash scripts/run-benchmarks.sh --list</automated>
    <automated>bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports</automated>
    <automated>node -e "const fs=require('fs'); const script=fs.readFileSync('scripts/run-benchmarks.sh','utf8'); for (const marker of ['set -euo pipefail','--smoke','--full','--iterations','--output-dir','--knots-json','--knots-bin','--list','OPEN_BITCOIN_BENCHMARK_REPORT_DIR']) { if (!script.includes(marker)) throw new Error('missing script marker '+marker); } for (const forbidden of ['eval ','bash -c']) { if (script.includes(forbidden)) throw new Error('forbidden shell pattern '+forbidden); }"</automated>
  </verify>
  <done>The benchmark wrapper runs list and bounded smoke modes, writes reports to the configured target directory, and avoids shell-evaluating user input.</done>
</task>

<task type="auto">
  <name>Task 2: Add verify, CI, and reviewer documentation wiring</name>
  <files>
    scripts/verify.sh
    .github/workflows/ci.yml
    docs/parity/benchmarks.md
  </files>
  <action>
    Update `scripts/verify.sh` to export `OPEN_BITCOIN_BENCHMARK_REPORT_DIR="${OPEN_BITCOIN_BENCHMARK_REPORT_DIR:-$PWD/packages/target/benchmark-reports}"`, create the directory, run `bash scripts/run-benchmarks.sh --smoke --output-dir "$OPEN_BITCOIN_BENCHMARK_REPORT_DIR"`, and include `//:bench` in the Bazel smoke build. Keep verification threshold-free per D-04.

    Update `.github/workflows/ci.yml` to set `OPEN_BITCOIN_BENCHMARK_REPORT_DIR: ${{ github.workspace }}/packages/target/benchmark-reports` and upload a separate artifact named `benchmark-reports` from `packages/target/benchmark-reports` with `if-no-files-found: ignore`.

    Create `docs/parity/benchmarks.md` with these exact sections: `# Benchmarks`, `## Benchmark Scope`, `## Benchmark Groups`, `## Knots Mapping`, `## Running Locally`, `## Reports`, and `## Non-Goals`. State that benchmark reports are audit/trend evidence, not release timing gates. State that Knots execution is optional report enrichment through explicit JSON/bin paths and that mapping-only is the default.
  </action>
  <verify>
    <automated>bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports</automated>
    <automated>node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('packages/target/benchmark-reports/open-bitcoin-bench-smoke.json','utf8')); const ids=new Set(r.groups.map(g=>g.id)); for (const id of ['consensus-script','block-transaction-codec','chainstate','mempool-policy','network-wire-sync','wallet','rpc-cli']) { if (!ids.has(id)) throw new Error('missing benchmark group '+id); } const docs=fs.readFileSync('docs/parity/benchmarks.md','utf8'); for (const marker of ['# Benchmarks','## Benchmark Scope','## Benchmark Groups','## Knots Mapping','## Running Locally','## Reports','## Non-Goals','not release timing gates','mapping-only']) { if (!docs.includes(marker)) throw new Error('missing benchmark doc marker '+marker); } const ci=fs.readFileSync('.github/workflows/ci.yml','utf8'); for (const marker of ['OPEN_BITCOIN_BENCHMARK_REPORT_DIR','benchmark-reports','packages/target/benchmark-reports']) { if (!ci.includes(marker)) throw new Error('missing CI marker '+marker); }"</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Repo-native verification runs bounded benchmark smoke mode, CI uploads benchmark reports, and parity docs explain benchmark scope, report paths, and mapping-only default comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell args to benchmark wrapper | User-provided output and optional Knots paths enter `scripts/run-benchmarks.sh`. |
| Benchmark reports to CI artifacts | Generated JSON/Markdown reports are collected by CI. |
| Documentation to reviewers | Benchmark docs frame the meaning of smoke and optional baseline results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | E | `scripts/run-benchmarks.sh` | mitigate | Pass args through arrays and reject shell-evaluated command construction. |
| T-10-03-02 | D | `scripts/verify.sh` | mitigate | Invoke only bounded `--smoke` mode and rely on the benchmark crate's iteration caps. |
| T-10-03-03 | I | `.github/workflows/ci.yml` | mitigate | Upload generated benchmark report files only; do not include environment dumps or credentials. |
| T-10-03-04 | S | `docs/parity/benchmarks.md` | mitigate | State reports are audit/trend evidence, not timing release gates, to prevent false readiness claims. |
</threat_model>

<verification>
- `bash scripts/run-benchmarks.sh --smoke --output-dir packages/target/benchmark-reports`
- `node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('packages/target/benchmark-reports/open-bitcoin-bench-smoke.json','utf8')); if (!Array.isArray(r.groups) || r.groups.length < 7) throw new Error('benchmark report missing groups')"`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- `PAR-02` benchmark reports are generated by a repo-owned script and checked by `scripts/verify.sh`.
- CI uploads benchmark report artifacts from `packages/target/benchmark-reports`.
- Docs make clear that default comparison is mapping-only and threshold-free.
</success_criteria>

<output>
After completion, create `.planning/phases/10-benchmarks-and-audit-readiness/10-03-SUMMARY.md`.
</output>
