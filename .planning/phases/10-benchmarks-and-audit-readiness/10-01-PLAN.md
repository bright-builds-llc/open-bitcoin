---
phase: 10-benchmarks-and-audit-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/Cargo.lock
  - BUILD.bazel
  - packages/open-bitcoin-bench/Cargo.toml
  - packages/open-bitcoin-bench/BUILD.bazel
  - packages/open-bitcoin-bench/src/lib.rs
  - packages/open-bitcoin-bench/src/main.rs
  - packages/open-bitcoin-bench/src/error.rs
  - packages/open-bitcoin-bench/src/registry.rs
  - packages/open-bitcoin-bench/src/runner.rs
  - packages/open-bitcoin-bench/src/report.rs
  - .planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md
autonomous: true
requirements:
  - PAR-02
user_setup: []
must_haves:
  truths:
    - "A contributor can build a first-party benchmark crate without adding a production runtime dependency per D-02."
    - "The benchmark registry contains every D-01 benchmark group and required Knots mapping metadata per D-03."
    - "The runner exposes bounded smoke/full execution modes without wall-clock pass/fail thresholds per D-04."
    - "The report schema is JSON-first and includes Markdown summary support per D-05."
  artifacts:
    - path: packages/open-bitcoin-bench/src/registry.rs
      provides: "Static benchmark group and Knots mapping registry"
      contains: "consensus-script"
    - path: packages/open-bitcoin-bench/src/runner.rs
      provides: "Bounded benchmark timing loop using Instant and black_box"
      contains: "RunMode"
    - path: packages/open-bitcoin-bench/src/report.rs
      provides: "Stable JSON and Markdown benchmark report schema"
      contains: "schema_version"
    - path: BUILD.bazel
      provides: "Root Bazel alias for the benchmark binary"
      contains: "name = \"bench\""
  key_links:
    - from: packages/open-bitcoin-bench/src/registry.rs
      to: packages/bitcoin-knots/src/bench
      via: "static benchmark/source mappings"
      pattern: "knots_mapping"
    - from: BUILD.bazel
      to: packages/open-bitcoin-bench/BUILD.bazel
      via: "root benchmark alias"
      pattern: "open_bitcoin_bench"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-24T10-47-33
generated_at: 2026-04-24T11:14:30Z
---

<objective>
Create the first-party benchmark crate, registry, runner, and report contracts.

Purpose: Establish the `PAR-02` benchmark foundation while preserving D-02 through D-05: first-party workspace ownership, no new production dependency, Knots mapping by default, bounded smoke behavior, and stable JSON/Markdown report output.
Output: `open-bitcoin-bench` crate, Cargo/Bazel workspace wiring, static benchmark group metadata, runner/report code, and build/list verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/Cargo.toml
@BUILD.bazel
@packages/open-bitcoin-test-harness/src/report.rs
@packages/bitcoin-knots/doc/benchmarking.md
@packages/bitcoin-knots/src/bench/bench_bitcoin.cpp
@packages/bitcoin-knots/src/bench/verify_script.cpp
@packages/bitcoin-knots/src/bench/checkblock.cpp
@packages/bitcoin-knots/src/bench/readwriteblock.cpp
@packages/bitcoin-knots/src/bench/mempool_stress.cpp
@packages/bitcoin-knots/src/bench/rpc_mempool.cpp
@packages/bitcoin-knots/src/bench/wallet_balance.cpp
@packages/bitcoin-knots/src/bench/coin_selection.cpp
@packages/bitcoin-knots/src/bench/wallet_create_tx.cpp
@packages/bitcoin-knots/src/bench/addrman.cpp
@packages/bitcoin-knots/src/bench/peer_eviction.cpp

<interfaces>
New contracts created for downstream Phase 10 plans:

```rust
pub struct BenchCase {
    pub id: &'static str,
    pub group: BenchGroupId,
    pub description: &'static str,
    pub knots_mapping: &'static KnotsMapping,
    pub run_once: fn() -> Result<(), BenchError>,
}

pub struct KnotsMapping {
    pub benchmark_names: &'static [&'static str],
    pub source_files: &'static [&'static str],
    pub notes: &'static str,
}

pub enum RunMode {
    Smoke,
    Full { iterations: u64 },
}

pub struct BenchReport {
    pub schema_version: u32,
    pub baseline: String,
    pub mode: String,
    pub generated_at_unix_seconds: u64,
    pub groups: Vec<BenchGroupReport>,
    pub optional_knots_source: Option<KnotsSource>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create benchmark crate contracts, registry, runner, and reports</name>
  <files>
    packages/Cargo.toml
    packages/Cargo.lock
    packages/open-bitcoin-bench/Cargo.toml
    packages/open-bitcoin-bench/src/lib.rs
    packages/open-bitcoin-bench/src/main.rs
    packages/open-bitcoin-bench/src/error.rs
    packages/open-bitcoin-bench/src/registry.rs
    packages/open-bitcoin-bench/src/runner.rs
    packages/open-bitcoin-bench/src/report.rs
  </files>
  <behavior>
    - Test 1: `open-bitcoin-bench --list` prints all seven required benchmark group ids without executing timing loops.
    - Test 2: runner unit tests prove smoke mode rejects unbounded iteration counts and full mode rejects values above the hard cap.
    - Test 3: report unit tests serialize JSON with `schema_version`, `baseline`, `mode`, `groups`, and `optional_knots_source`.
  </behavior>
  <action>
    Add `"open-bitcoin-bench"` to `packages/Cargo.toml` workspace members, then create `packages/open-bitcoin-bench` as a first-party Rust binary/library crate named `open-bitcoin-bench`. Use only existing workspace-compatible dependencies: first-party `open-bitcoin-*` crates as needed, `serde = { version = "1.0.228", features = ["derive"] }`, and `serde_json = "1.0.149"`. Do not add Criterion, Divan, nightly `test`, `test::Bencher`, or any production runtime dependency, per D-02 and the resolved research choice. Let Cargo update `packages/Cargo.lock` only as required for the new workspace member.

    Add `#![forbid(unsafe_code)]` in `src/lib.rs`. Export `error`, `registry`, `runner`, and `report`. Implement `registry.rs` with static metadata for exactly these benchmark group ids from D-01: `consensus-script`, `block-transaction-codec`, `chainstate`, `mempool-policy`, `network-wire-sync`, `wallet`, and `rpc-cli`. Each group must include at least one Knots benchmark/source mapping. Include mapping strings for `VerifyScriptBench`, `DeserializeBlockTest`, `ComplexMemPool`, `RpcMempool`, `WalletBalance`, `CoinSelection`, `WalletCreateTx`, `AddrMan`, and `EvictionProtection`, even when the mapping is documentation-only for the current smoke cases.

    Implement `runner.rs` using `std::time::Instant` and `std::hint::black_box`. Smoke mode must be bounded to 1 to 10 iterations per case. Full mode must require an explicit `--iterations <N>` value and enforce a hard cap of 10000. Do not enforce machine-dependent timing thresholds.

    Implement `report.rs` with typed JSON serialization and Markdown writing helpers. JSON must include `schema_version`, `baseline`, `mode`, `generated_at_unix_seconds`, `groups`, and `optional_knots_source`. Markdown table cells must escape `|` characters. Implement `src/main.rs` with `--list` and argument-error handling; benchmark execution is wired to real cases in Plan 10-02.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features</automated>
    <automated>cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --list</automated>
  </verify>
  <done>The crate compiles, lists all required group ids, validates bounded run modes, and serializes the stable JSON/Markdown report schema with static Knots mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire benchmark crate into Cargo and Bazel workspaces</name>
  <files>
    BUILD.bazel
    packages/open-bitcoin-bench/BUILD.bazel
  </files>
  <action>
    Add `packages/open-bitcoin-bench/BUILD.bazel` with a `rust_library` and `rust_binary` matching existing first-party Bazel patterns. Add a root `BUILD.bazel` alias named `bench` targeting `//packages/open-bitcoin-bench:open_bitcoin_bench`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features</automated>
    <automated>bazel build //:bench</automated>
    <automated>node -e "const fs=require('fs'); const registry=fs.readFileSync('packages/open-bitcoin-bench/src/registry.rs','utf8'); for (const id of ['consensus-script','block-transaction-codec','chainstate','mempool-policy','network-wire-sync','wallet','rpc-cli']) { if (!registry.includes(id)) throw new Error('missing group '+id); } for (const marker of ['VerifyScriptBench','DeserializeBlockTest','ComplexMemPool','RpcMempool','WalletBalance','CoinSelection','WalletCreateTx','AddrMan','EvictionProtection']) { if (!registry.includes(marker)) throw new Error('missing Knots mapping '+marker); }"</automated>
  </verify>
  <done>Cargo and Bazel build the benchmark crate through repo-native entrypoints, and the registry contains every required D-01 group plus required D-03 Knots mapping markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to benchmark binary | User-provided flags enter the benchmark shell. |
| Static registry to reports | Benchmark ids and Knots mapping metadata become machine-readable and Markdown output. |
| Report writer to filesystem | Report helpers will write JSON and Markdown paths in downstream plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T | `packages/open-bitcoin-bench/src/report.rs` | mitigate | Serialize JSON through `serde_json` and escape Markdown table separators before writing summaries. |
| T-10-01-02 | D | `packages/open-bitcoin-bench/src/runner.rs` | mitigate | Bound smoke mode to 1 to 10 iterations and cap explicit full mode at 10000 iterations. |
| T-10-01-03 | S | `packages/open-bitcoin-bench/src/registry.rs` | mitigate | Keep benchmark ids and Knots mappings static and validate required ids in automated checks. |
| T-10-01-04 | I | `packages/open-bitcoin-bench/src/report.rs` | mitigate | Report optional Knots source metadata only; do not dump environment variables or credentials. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench --all-features`
- `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --list`
- `bazel build //:bench`
</verification>

<success_criteria>
- The first-party benchmark crate exists and builds through Cargo and Bazel.
- All seven D-01 benchmark group ids and required D-03 Knots mappings are present before benchmark cases are implemented.
- The runner and report contracts are bounded, threshold-free, JSON-first, and ready for Plan 10-02 cases.
</success_criteria>

<output>
After completion, create `.planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md`.
</output>
