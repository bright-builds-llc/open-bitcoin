---
phase: 10-benchmarks-and-audit-readiness
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - docs/parity/index.json
  - docs/parity/README.md
  - docs/parity/catalog/README.md
  - docs/parity/checklist.md
  - docs/parity/deviations-and-unknowns.md
  - .planning/phases/10-benchmarks-and-audit-readiness/10-04-SUMMARY.md
autonomous: true
requirements:
  - AUD-01
user_setup: []
must_haves:
  truths:
    - "A contributor can inspect one parity checklist covering every in-scope initial-milestone surface per D-06."
    - "Checklist statuses use exactly `planned`, `in_progress`, `done`, `deferred`, and `out_of_scope` per D-07."
    - "Every `done` item has concise evidence links, and every `deferred` or `out_of_scope` item has rationale per D-07."
    - "`docs/parity/index.json` remains the machine-readable root per D-08."
    - "Known gaps, suspected unknowns, and folded todo risks are surfaced without broad follow-on implementation per D-09."
  artifacts:
    - path: docs/parity/index.json
      provides: "Machine-readable checklist root"
      contains: "status_taxonomy"
    - path: docs/parity/checklist.md
      provides: "Human-readable parity checklist"
      contains: "benchmarks-audit-readiness"
    - path: docs/parity/deviations-and-unknowns.md
      provides: "Known gaps, suspected unknowns, deferred surfaces, and folded todo risk capture"
      contains: "Panic And Illegal-State Exposure"
  key_links:
    - from: docs/parity/index.json
      to: docs/parity/checklist.md
      via: "checklist.path"
      pattern: "\"path\": \"checklist.md\""
    - from: docs/parity/checklist.md
      to: .planning/phases
      via: "evidence links for done surfaces"
      pattern: ".planning/phases/"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-24T10-47-33
generated_at: 2026-04-24T11:14:30Z
---

<objective>
Create the parity checklist and deviation or unknown audit artifacts.

Purpose: Satisfy `AUD-01` by making every in-scope initial-milestone surface reviewable through a machine-readable root and concise Markdown views.
Output: Extended `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/deviations-and-unknowns.md`, and discovery links from the parity docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-CONTEXT.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-RESEARCH.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-01-SUMMARY.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-02-SUMMARY.md
@.planning/phases/10-benchmarks-and-audit-readiness/10-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@docs/parity/README.md
@docs/parity/index.json
@docs/parity/catalog/README.md
@docs/parity/catalog/core-domain-and-serialization.md
@docs/parity/catalog/consensus-validation.md
@docs/parity/catalog/chainstate.md
@docs/parity/catalog/mempool-policy.md
@docs/parity/catalog/p2p.md
@docs/parity/catalog/wallet.md
@docs/parity/catalog/rpc-cli-config.md
@docs/parity/catalog/verification-harnesses.md
@docs/parity/benchmarks.md
@.planning/phases/08-rpc-cli-and-config-parity/08-VERIFICATION.md
@.planning/phases/09-parity-harnesses-and-fuzzing/09-VERIFICATION.md
@.planning/todos/pending/2026-04-18-ai-agent-friendly-cli-surface.md
@.planning/todos/pending/2026-04-18-sweep-panics-and-illegal-states.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add machine-readable checklist data to the parity index</name>
  <files>
    docs/parity/index.json
  </files>
  <behavior>
    - Test 1: every checklist surface status is one of the five locked taxonomy values.
    - Test 2: every `done` surface has at least one evidence link.
    - Test 3: every `deferred` or `out_of_scope` surface has a non-empty rationale.
  </behavior>
  <action>
    Extend `docs/parity/index.json` without replacing the existing `baseline`, `deviations`, `catalog`, or `surfaces` roots. Add a top-level `checklist` object with these exact keys: `path`, `status_taxonomy`, and `surfaces`. The `status_taxonomy` value must be exactly `["planned", "in_progress", "done", "deferred", "out_of_scope"]`.

    Each checklist record must use these exact keys: `id`, `title`, `status`, `requirements`, `evidence`, `rationale`, `known_gaps`, and `suspected_unknowns`. Required surface ids per D-06 are `reference-baseline`, `architecture-workspace`, `core-serialization`, `consensus-validation`, `chainstate`, `mempool-policy`, `p2p-networking`, `wallet`, `rpc-cli-config`, `verification-harnesses-fuzzing`, and `benchmarks-audit-readiness`.

    Set completed prior surfaces to `done` only when they have evidence links to existing catalog docs, verification reports, summaries, or scripts. Set `benchmarks-audit-readiness` to `in_progress` in this plan, with evidence to `docs/parity/benchmarks.md`, `scripts/run-benchmarks.sh`, and `10-03-SUMMARY.md`; Plan 10-05 will promote it after release-readiness exists. Do not add GUI, dashboard, broad CLI feature work, broad panic refactors, or the reviewed reduce-nesting todo to the checklist.

    Add a top-level `audit` object with keys `deviations_and_unknowns`, `benchmarks`, and `release_readiness`. Use `deviations_and_unknowns.path = "deviations-and-unknowns.md"`, `benchmarks.path = "benchmarks.md"`, `benchmarks.report_dir = "packages/target/benchmark-reports"`, `release_readiness.path = "release-readiness.md"`, and `release_readiness.status = "planned"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const idx=JSON.parse(fs.readFileSync('docs/parity/index.json','utf8')); const allowed=['planned','in_progress','done','deferred','out_of_scope']; if (JSON.stringify(idx.checklist.status_taxonomy)!==JSON.stringify(allowed)) throw new Error('bad taxonomy'); const required=['reference-baseline','architecture-workspace','core-serialization','consensus-validation','chainstate','mempool-policy','p2p-networking','wallet','rpc-cli-config','verification-harnesses-fuzzing','benchmarks-audit-readiness']; const ids=idx.checklist.surfaces.map(s=>s.id); for (const id of required) if (!ids.includes(id)) throw new Error('missing '+id); for (const s of idx.checklist.surfaces) { if (!allowed.includes(s.status)) throw new Error('bad status '+s.id); if (s.status==='done' && (!Array.isArray(s.evidence)||s.evidence.length===0)) throw new Error('missing evidence '+s.id); if ((s.status==='deferred'||s.status==='out_of_scope') && !s.rationale) throw new Error('missing rationale '+s.id); } if (!idx.audit || !idx.audit.deviations_and_unknowns || !idx.audit.benchmarks || !idx.audit.release_readiness) throw new Error('missing audit roots');"</automated>
  </verify>
  <done>The parity index is the machine-readable checklist root and validates the locked taxonomy, required surface coverage, and evidence/rationale rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add human-readable checklist and deviation or unknown views</name>
  <files>
    docs/parity/README.md
    docs/parity/catalog/README.md
    docs/parity/checklist.md
    docs/parity/deviations-and-unknowns.md
  </files>
  <action>
    Create `docs/parity/checklist.md` as the human-readable view of `docs/parity/index.json`. It must contain these exact headings: `# Parity Checklist`, `## Status Taxonomy`, `## Surface Status`, and `## Evidence Rules`. The `## Surface Status` table must include columns `Surface`, `Status`, `Requirements`, `Evidence`, `Known Gaps`, and `Suspected Unknowns`. Include every required surface id from Task 1 exactly once.

    Create `docs/parity/deviations-and-unknowns.md` with these exact headings: `# Deviations And Unknowns`, `## Intentional Deviations`, `## Deferred Surfaces`, `## Suspected Unknowns`, `## Folded Todo Audit`, and `## Follow-Up Triggers`. Include current `docs/parity/index.json` deviations, deferred or out-of-scope surfaces from `rpc-cli-config.md` and `verification-harnesses.md`, suspected unknowns summarized from catalog entries, an `AI-Agent-Friendly CLI Surface` entry as evidence/risk review only, and a `Panic And Illegal-State Exposure` entry as risk/follow-up capture only.

    Update `docs/parity/README.md` to list `checklist.md`, `deviations-and-unknowns.md`, and `benchmarks.md` under `## Files`. Update `docs/parity/catalog/README.md` so its current entries table includes `wallet.md` and `rpc-cli-config.md` if they are missing, while preserving existing entries.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checklist=fs.readFileSync('docs/parity/checklist.md','utf8'); for (const marker of ['# Parity Checklist','## Status Taxonomy','## Surface Status','## Evidence Rules','`planned`, `in_progress`, `done`, `deferred`, `out_of_scope`']) { if (!checklist.includes(marker)) throw new Error('missing checklist marker '+marker); } for (const id of ['reference-baseline','architecture-workspace','core-serialization','consensus-validation','chainstate','mempool-policy','p2p-networking','wallet','rpc-cli-config','verification-harnesses-fuzzing','benchmarks-audit-readiness']) { const count=checklist.split(id).length-1; if (count !== 1) throw new Error('surface id count for '+id+' was '+count); } const deviations=fs.readFileSync('docs/parity/deviations-and-unknowns.md','utf8'); for (const marker of ['# Deviations And Unknowns','## Intentional Deviations','## Deferred Surfaces','## Suspected Unknowns','## Folded Todo Audit','## Follow-Up Triggers','AI-Agent-Friendly CLI Surface','Panic And Illegal-State Exposure']) { if (!deviations.includes(marker)) throw new Error('missing deviations marker '+marker); }"</automated>
    <automated>node -e "const fs=require('fs'); const readme=fs.readFileSync('docs/parity/README.md','utf8'); for (const marker of ['checklist.md','deviations-and-unknowns.md','benchmarks.md']) { if (!readme.includes(marker)) throw new Error('missing README link '+marker); } const catalog=fs.readFileSync('docs/parity/catalog/README.md','utf8'); for (const marker of ['wallet.md','rpc-cli-config.md']) { if (!catalog.includes(marker)) throw new Error('missing catalog link '+marker); }"</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Reviewers have concise Markdown views for checklist status, evidence, deviations, deferred surfaces, suspected unknowns, and folded todo risks without duplicating whole phase reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity JSON to reviewers/tools | Machine-readable checklist data is consumed by humans and future tooling. |
| Catalog summaries to audit docs | Existing catalog gaps and suspected unknowns are summarized into Markdown. |
| Planning todos to audit docs | Folded todos enter audit output as risk/evidence items, not implementation scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | S | `docs/parity/index.json` | mitigate | Validate every status against the exact locked taxonomy and require evidence links for `done` checklist records. |
| T-10-04-02 | T | `docs/parity/checklist.md` | mitigate | Treat Markdown as a view of `index.json`; validate all required surface ids exactly once with a Node loop. |
| T-10-04-03 | I | `docs/parity/deviations-and-unknowns.md` | mitigate | Summarize env-gated Knots and CLI/RPC surfaces without copying secrets, credentials, or raw environment values. |
| T-10-04-04 | R | `docs/parity/deviations-and-unknowns.md` | mitigate | Preserve deferred rationale and follow-up triggers so gaps are attributable to documented scope decisions rather than silent omission. |
</threat_model>

<verification>
- `node -e "const fs=require('fs'); const idx=JSON.parse(fs.readFileSync('docs/parity/index.json','utf8')); if (!idx.checklist || !idx.audit) throw new Error('missing audit roots')"`
- `node -e "const fs=require('fs'); const d=fs.readFileSync('docs/parity/deviations-and-unknowns.md','utf8'); for (const marker of ['AI-Agent-Friendly CLI Surface','Panic And Illegal-State Exposure']) if (!d.includes(marker)) throw new Error('missing '+marker)"`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- `AUD-01` is materially satisfied by a parity checklist that covers every in-scope initial-milestone surface.
- Checklist data lives in `docs/parity/index.json` and Markdown companions help reviewers navigate it.
- Deferred ideas remain deferred: no public dashboard, no broad CLI additions, no broad panic refactor, and no reduce-nesting follow-on work.
</success_criteria>

<output>
After completion, create `.planning/phases/10-benchmarks-and-audit-readiness/10-04-SUMMARY.md`.
</output>
