---
phase: 02-core-domain-and-serialization-foundations
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - docs/parity/README.md
  - docs/parity/index.json
  - docs/parity/catalog/README.md
  - docs/parity/catalog/core-domain-and-serialization.md
autonomous: true
requirements:
  - REF-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-04-11T14-58-23
generated_at: 2026-04-11T15:05:55.563Z
must_haves:
  truths:
    - Contributors can inspect a living subsystem catalog for the baseline domain/codec surfaces implemented in Phase 2.
    - The parity root index points to the subsystem catalog instead of hiding quirks and unknowns in code comments only.
  artifacts:
    - docs/parity/catalog/README.md
    - docs/parity/catalog/core-domain-and-serialization.md
    - docs/parity/index.json
  key_links:
    - The Phase 2 catalog cites concrete Knots source/tests/docs for each tracked subsystem area.
    - The root parity index links to the new catalog artifacts and records their status.
---

<objective>
Seed the living reference catalog for the domain and codec surfaces introduced in
Phase 2.

Purpose: `REF-03` requires an inspectable catalog of features, quirks, known
bugs, and unknowns. Phase 2 is the first point where those shared subsystem
artifacts can be grounded in actual first-party code and baseline sources.
Output: `docs/parity/` includes subsystem catalog artifacts for the new
domain/serialization foundations and the root parity index references them.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-domain-and-serialization-foundations/02-CONTEXT.md
@.planning/phases/02-core-domain-and-serialization-foundations/02-RESEARCH.md
@.planning/phases/02-core-domain-and-serialization-foundations/02-02-SUMMARY.md
@docs/parity/README.md
@docs/parity/index.json
@AGENTS.md
@packages/bitcoin-knots/src/serialize.h
@packages/bitcoin-knots/src/primitives/transaction.h
@packages/bitcoin-knots/src/primitives/block.h
@packages/bitcoin-knots/src/protocol.h
@packages/bitcoin-knots/src/test/serialize_tests.cpp
@packages/bitcoin-knots/src/test/transaction_tests.cpp
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-02-07:** Upstream baseline files/tests -> contributor-facing catalog assertions.
- **TB-02-08:** First-party implementation status -> parity index and catalog status fields.

### Threat Register
- **Threat ID:** T-02-07
  - **Category:** Repudiation
  - **Component:** subsystem catalog provenance
  - **Disposition:** mitigate
  - **Mitigation Plan:** Cite concrete Knots source/test paths for each tracked surface and record known unknowns explicitly.
- **Threat ID:** T-02-08
  - **Category:** Integrity
  - **Component:** root parity index status fields
  - **Disposition:** mitigate
  - **Mitigation Plan:** Update `docs/parity/index.json` together with the new catalog docs so root status and detailed notes stay aligned.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the subsystem catalog artifacts for Phase 2</name>
  <files>docs/parity/catalog/README.md, docs/parity/catalog/core-domain-and-serialization.md</files>
  <read_first>docs/parity/README.md, docs/parity/index.json, .planning/phases/02-core-domain-and-serialization-foundations/02-CONTEXT.md, .planning/phases/02-core-domain-and-serialization-foundations/02-RESEARCH.md, packages/bitcoin-knots/src/serialize.h, packages/bitcoin-knots/src/primitives/transaction.h, packages/bitcoin-knots/src/primitives/block.h, packages/bitcoin-knots/src/protocol.h</read_first>
  <action>Create `docs/parity/catalog/README.md` to explain the subsystem catalog contract and add `docs/parity/catalog/core-domain-and-serialization.md` documenting the Phase 2 surfaces. The catalog entry must include: tracked subsystem areas, canonical Knots source/test references, first-party artifact links, known quirks, known bugs (or explicit `None yet`), and suspected unknowns that later phases still need to investigate.</action>
  <verify>rg -n \"Known quirks|Known bugs|Suspected unknowns|Canonical upstream references\" docs/parity/catalog/core-domain-and-serialization.md</verify>
  <acceptance_criteria>
    - `docs/parity/catalog/README.md` exists
    - `docs/parity/catalog/core-domain-and-serialization.md` contains `Known quirks`
    - `docs/parity/catalog/core-domain-and-serialization.md` contains `Known bugs`
    - `docs/parity/catalog/core-domain-and-serialization.md` contains `Suspected unknowns`
  </acceptance_criteria>
  <done>The repo has a human-readable subsystem catalog for the Phase 2 domain and codec surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Link the catalog into the machine-readable parity root</name>
  <files>docs/parity/index.json, docs/parity/README.md</files>
  <read_first>docs/parity/index.json, docs/parity/README.md, docs/parity/catalog/core-domain-and-serialization.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `docs/parity/index.json` so the root parity artifact records the new catalog location and the status of the Phase 2 domain/serialization surfaces. Update `docs/parity/README.md` so contributors know to extend `docs/parity/catalog/` when new subsystem quirks or unknowns are discovered. Keep JSON formatting stable and machine-readable.</action>
  <verify>node -e \"JSON.parse(require('fs').readFileSync('docs/parity/index.json','utf8')); console.log('ok')\"</verify>
  <acceptance_criteria>
    - `docs/parity/index.json` parses as JSON
    - `docs/parity/index.json` contains `core-domain-and-serialization`
    - `docs/parity/README.md` contains `docs/parity/catalog/`
  </acceptance_criteria>
  <done>The parity root index and README expose the new catalog instead of leaving Phase 2 reference knowledge implicit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "JSON.parse(require('fs').readFileSync('docs/parity/index.json','utf8'))"` passes
- [ ] `rg -n "Known quirks|Known bugs|Suspected unknowns" docs/parity/catalog/core-domain-and-serialization.md` finds all sections
- [ ] The new catalog files cite Knots source/test references and first-party artifacts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contributors can inspect a living Phase 2 subsystem catalog under `docs/parity/`
- The root parity index points to the catalog and records Phase 2 surface status
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-domain-and-serialization-foundations/02-04-SUMMARY.md`
</output>
