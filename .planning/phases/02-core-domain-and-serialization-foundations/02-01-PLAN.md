---
phase: 02-core-domain-and-serialization-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BUILD.bazel
  - packages/Cargo.toml
  - scripts/pure-core-crates.txt
  - packages/open-bitcoin-core/Cargo.toml
  - packages/open-bitcoin-core/src/lib.rs
  - packages/open-bitcoin-primitives/Cargo.toml
  - packages/open-bitcoin-primitives/BUILD.bazel
  - packages/open-bitcoin-primitives/src/lib.rs
  - packages/open-bitcoin-primitives/src/amount.rs
  - packages/open-bitcoin-primitives/src/hash.rs
  - packages/open-bitcoin-primitives/src/script.rs
  - packages/open-bitcoin-primitives/src/network.rs
  - packages/open-bitcoin-primitives/src/transaction.rs
  - packages/open-bitcoin-primitives/src/block.rs
  - packages/open-bitcoin-codec/Cargo.toml
  - packages/open-bitcoin-codec/BUILD.bazel
  - packages/open-bitcoin-codec/src/lib.rs
autonomous: true
requirements:
  - ARCH-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-04-11T14-58-23
generated_at: 2026-04-11T15:05:55.563Z
must_haves:
  truths:
    - The workspace exposes reusable pure-core crates for invariant-bearing Bitcoin domain types and codecs.
    - Later phases can depend on stable Rust types for amounts, hashes, scripts, transactions, blocks, and message envelopes instead of raw primitives.
  artifacts:
    - packages/open-bitcoin-primitives/Cargo.toml
    - packages/open-bitcoin-primitives/src/lib.rs
    - packages/open-bitcoin-codec/Cargo.toml
    - packages/open-bitcoin-core/src/lib.rs
  key_links:
    - packages/Cargo.toml lists `open-bitcoin-primitives` and `open-bitcoin-codec` as workspace members.
    - packages/open-bitcoin-core re-exports the new pure-core libraries for downstream consumers.
---

<objective>
Define the reusable pure-core crate boundaries and the invariant-bearing domain
types that later codecs and validation logic will depend on.

Purpose: Phase 2 needs stable Rust domain APIs before it can implement byte
codecs, fixtures, or reference-catalog artifacts.
Output: Two reusable pure-core crates (`open-bitcoin-primitives` and
`open-bitcoin-codec`) exist in the workspace, and the primitives crate exports
checked domain types for the shared Bitcoin data model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-domain-and-serialization-foundations/02-CONTEXT.md
@.planning/phases/02-core-domain-and-serialization-foundations/02-RESEARCH.md
@AGENTS.md
@packages/Cargo.toml
@packages/README.md
@packages/open-bitcoin-core/Cargo.toml
@packages/open-bitcoin-core/src/lib.rs
@packages/bitcoin-knots/src/consensus/amount.h
@packages/bitcoin-knots/src/uint256.h
@packages/bitcoin-knots/src/script/script.h
@packages/bitcoin-knots/src/primitives/transaction.h
@packages/bitcoin-knots/src/primitives/block.h
@packages/bitcoin-knots/src/protocol.h
</context>

<threat_model>
## Threat Model

### Trust Boundaries
- **TB-02-01:** Raw byte/primitive inputs -> first-party domain types consumed by later consensus and wallet code.
- **TB-02-02:** Workspace/package topology -> later pure-core/runtime dependency enforcement.

### Threat Register
- **Threat ID:** T-02-01
  - **Category:** Tampering
  - **Component:** package membership, pure-core allowlist, and crate re-exports
  - **Disposition:** mitigate
  - **Mitigation Plan:** Update Cargo/Bazel metadata and `scripts/pure-core-crates.txt` together so the new crates are enforceable and reviewable.
- **Threat ID:** T-02-02
  - **Category:** Integrity
  - **Component:** invariant-bearing domain constructors
  - **Disposition:** mitigate
  - **Mitigation Plan:** Encode size/range/format rules in fallible constructors and narrow types instead of allowing unchecked primitives through the core.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 2 pure-core crate topology to Cargo and Bazel</name>
  <files>packages/Cargo.toml, BUILD.bazel, scripts/pure-core-crates.txt, packages/open-bitcoin-primitives/Cargo.toml, packages/open-bitcoin-primitives/BUILD.bazel, packages/open-bitcoin-codec/Cargo.toml, packages/open-bitcoin-codec/BUILD.bazel, packages/open-bitcoin-codec/src/lib.rs, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/src/lib.rs</files>
  <read_first>packages/Cargo.toml, BUILD.bazel, scripts/pure-core-crates.txt, packages/README.md, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/src/lib.rs, .planning/phases/02-core-domain-and-serialization-foundations/02-CONTEXT.md</read_first>
  <action>Create two new first-party pure-core crates under `packages/`: `open-bitcoin-primitives` for invariant-bearing data types and `open-bitcoin-codec` for parse/serialize entrypoints. Add both crates to `packages/Cargo.toml`, create `Cargo.toml` and `BUILD.bazel` files for each crate, update the repo-root `BUILD.bazel` aliases to expose the new crates as Bazel targets, add both crate names to `scripts/pure-core-crates.txt`, and update `packages/open-bitcoin-core` so it depends on and publicly re-exports `open-bitcoin-primitives` and `open-bitcoin-codec` instead of remaining an isolated stub.</action>
  <verify>cargo metadata --manifest-path packages/Cargo.toml --format-version 1 --no-deps && bazel query //:core //packages/open-bitcoin-primitives:open_bitcoin_primitives //packages/open-bitcoin-codec:open_bitcoin_codec</verify>
  <acceptance_criteria>
    - `packages/Cargo.toml` contains `open-bitcoin-primitives` and `open-bitcoin-codec`
    - `scripts/pure-core-crates.txt` contains `open-bitcoin-primitives`
    - `scripts/pure-core-crates.txt` contains `open-bitcoin-codec`
    - `packages/open-bitcoin-core/src/lib.rs` contains `pub use open_bitcoin_primitives`
    - `packages/open-bitcoin-core/src/lib.rs` contains `pub use open_bitcoin_codec`
  </acceptance_criteria>
  <done>The workspace and purity gates know about the new pure-core crates, and downstream code can import them through `open-bitcoin-core`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement invariant-bearing primitives and shared domain structs</name>
  <files>packages/open-bitcoin-primitives/src/lib.rs, packages/open-bitcoin-primitives/src/amount.rs, packages/open-bitcoin-primitives/src/hash.rs, packages/open-bitcoin-primitives/src/script.rs, packages/open-bitcoin-primitives/src/network.rs, packages/open-bitcoin-primitives/src/transaction.rs, packages/open-bitcoin-primitives/src/block.rs</files>
  <read_first>packages/bitcoin-knots/src/consensus/amount.h, packages/bitcoin-knots/src/uint256.h, packages/bitcoin-knots/src/script/script.h, packages/bitcoin-knots/src/primitives/transaction.h, packages/bitcoin-knots/src/primitives/block.h, packages/bitcoin-knots/src/protocol.h, .planning/phases/02-core-domain-and-serialization-foundations/02-CONTEXT.md</read_first>
  <action>Implement the primitives crate with fallible, invariant-bearing Rust types. Include at least: `Amount` enforcing `0..=MAX_MONEY`, fixed-width hash wrappers for transaction/block identifiers, `ScriptBuf` enforcing byte-faithful script storage and size limits, `OutPoint`, `TransactionInput`, `TransactionOutput`, `Transaction`, `BlockHeader`, `Block`, `NetworkMagic`, `MessageCommand`, `MessageHeader`, `InventoryVector`, and `BlockLocator`. Keep constructors and accessors explicit, avoid `unwrap()`, and keep wire-visible distinctions such as transaction identity variants and message-header boundaries represented in the type surface.</action>
  <verify>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-primitives</verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-primitives/src/amount.rs` contains `MAX_MONEY`
    - `packages/open-bitcoin-primitives/src/script.rs` contains `MAX_SCRIPT_SIZE`
    - `packages/open-bitcoin-primitives/src/network.rs` contains `pub struct MessageHeader`
    - `packages/open-bitcoin-primitives/src/transaction.rs` contains `pub struct Transaction`
    - `packages/open-bitcoin-primitives/src/block.rs` contains `pub struct BlockHeader`
  </acceptance_criteria>
  <done>The repo has strongly typed reusable domain structures that carry the baseline invariants later phases rely on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo metadata --manifest-path packages/Cargo.toml --format-version 1 --no-deps` passes
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-primitives` passes
- [ ] `bash scripts/check-pure-core-deps.sh` passes after the new pure-core crates are added
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The workspace exposes reusable pure-core crates for Bitcoin domain values and codecs
- Later phase code can depend on typed amounts, hashes, scripts, transactions, blocks, and message headers instead of raw primitives
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-domain-and-serialization-foundations/02-01-SUMMARY.md`
</output>
