---
phase: 138-parity-adversarial-pressure-restart-and-release-guardrails
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/index.json
  - docs/parity/checklist.md
  - .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md
  - README.md
  - docs/operator/runtime-guide.md
  - docs/parity/release-readiness.md
  - docs/parity/support-matrix.md
  - docs/parity/service-operation-expectations.md
requirements:
  - MPVFY-03
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 138-2026-08-22T04-13-58
generated_at: 2026-08-22T05:05:00.000Z
user_setup: []
must_haves:
  truths:
    - "PACK-01 through PACK-07, PRESS-01 through PRESS-05, PPKG-04, IBR-01 through IBR-04, and MPVFY-01 through MPVFY-04 each have exactly one machine-readable surface owner."
    - "Operators can copy Cargo and Bazel UAT commands for package dry-run, submit, Knots RPC, and JSON status without using only the installed open-bitcoin alias."
    - "Live claim-bearing docs state the D-21 scoped sentence without the unscoped words package relay."
  artifacts:
    - path: "docs/parity/index.json"
      provides: "Backfilled PACK, PRESS, Phase 136, and MPVFY closeout surfaces"
      contains: "v2-2-parity-uat-release-boundary"
    - path: "docs/parity/checklist.md"
      provides: "Mirrored human owners for the same four surface IDs"
      contains: "v2-2-typed-package-staged-admission"
    - path: ".planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md"
      provides: "Committed deterministic UAT package with optional public-network not run"
      contains: "status: not run"
  key_links:
    - from: "docs/parity/index.json"
      to: "docs/parity/checklist.md"
      via: "matching v2-2 surface IDs and requirement ownership"
      pattern: "v2-2-(typed-package-staged-admission|rolling-fee-expiry-pressure|receive-independent-maintenance-and-transport-receipts|parity-uat-release-boundary)"
    - from: ".planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md"
      to: "docs/operator/runtime-guide.md"
      via: "copy-pasteable Cargo and Bazel command forms"
      pattern: "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin"
---

# Phase 138 Plan 02: Parity Owners, UAT Package, and D-21 Wording

<objective>
Give every leftover v2.2 requirement a machine-readable owner, add the MPVFY closeout surface, and publish copy-pasteable UAT plus the locked scoped claim.

Purpose: MPVFY-03 and D-10 through D-16 require index/checklist ownership before the last-gate checker can name evidence. Live docs must state D-21 without tripping Phase 117's archived `package relay` deny.

Output: Four new/backfilled surfaces still `in_progress`, `138-UAT.md`, and claim-bearing docs that use the exact D-21 sentence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-RESEARCH.md
@.planning/phases/117-parity-traceability-uat-and-release-guardrails/117-UAT.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/operator/runtime-guide.md
@docs/parity/release-readiness.md
@README.md

<interfaces>
Existing top-level `docs/parity/index.json` `surfaces[]` v2.2 names that must be preserved:
- `v2-2-resource-time-fee-primitives` (keep `in_progress` this plan)
- `v2-2-package-aware-download-orphan-bridge` (keep `done`)
- `v2-2-authoritative-cross-cache-lifecycle-integration` (keep `in_progress`)
- `v2 snapshot schema, checkpointing, and recovery` (keep this human `name`; keep `in_progress`)
- `v2-2-rpc-and-sanitized-operator-evidence` (keep `in_progress`)

Checklist already has `v2-2-snapshot-schema-checkpointing-recovery` as the machine id for the Phase 135 human title. Do not rename that human title (D-13 / research).

Phase 117 CLAIM_FILES that must remain 117-safe:
README.md, docs/operator/runtime-guide.md, docs/parity/release-readiness.md, docs/parity/support-matrix.md.
Phase 117 `DANGEROUS_CLAIMS` includes the substring `package relay`. Positive verbs plus that substring fail unless a no-claim marker (`deferred`, `remain deferred`, `does not`, …) is in the same clause.

Required D-21 sentence to copy verbatim into README, runtime-guide, release-readiness, and support-matrix:

bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members

Do not write the two-word phrase `package relay` in any new positive clause. Existing "package relay … remain deferred" sentences may stay for FUT-12.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live claim-bearing docs → operators | Unscoped "package relay" or production/public-relay wording can be read as a shipped capability. |
| Parity index → auditors | Missing PACK/PRESS/136/MPVFY owners hide unimplemented-looking gaps or duplicate ownership. |
| UAT package → CI | Optional public-network review can be mistaken for a default or release gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-06 | Spoofing | README.md, runtime-guide, release-readiness, support-matrix | mitigate | Copy the D-21 sentence verbatim. Never add a positive clause containing `package relay`. Keep FUT-12 "package relay remain deferred". |
| T-138-07 | Elevation of Privilege | 138-UAT.md | mitigate | Required tests stay deterministic. Optional public-network review is `status: not run` and "never required for pre-commit, default CI, release-boundary verification, or `bash scripts/verify.sh`". |
| T-138-08 | Tampering | docs/parity/index.json | mitigate | Each of the 40 v2.2 IDs appears on exactly one surface. MPVFY-01..04 appear only on `v2-2-parity-uat-release-boundary`. |
| T-138-09 | Repudiation | 138-UAT.md command blocks | mitigate | Require the exact Cargo and Bazel forms from D-16 / Phase 137 runtime-guide, not the installed alias alone. |
| T-138-10 | Information Disclosure | UAT examples | mitigate | Keep `/tmp/open-bitcoin-mainnet` placeholders and `'<hex>'` tokens; do not paste real txids, wallet descriptors, or datadir paths. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Backfill PACK, PRESS, Phase 136, and MPVFY surface owners</name>
  <files>
    docs/parity/index.json
    docs/parity/checklist.md
  </files>
  <read_first>
    docs/parity/index.json
    docs/parity/checklist.md
    .planning/REQUIREMENTS.md
    docs/parity/catalog/mempool-policy.md
    docs/parity/catalog/rpc-cli-config.md
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
  </read_first>
  <action>
    Add these four surfaces to `docs/parity/index.json` top-level `surfaces[]` and to `checklist.surfaces[]`, plus matching rows in `docs/parity/checklist.md`. Status for all four this plan is `in_progress` (D-12, D-14). Do not promote existing `in_progress` surfaces to `done` (Plan 04). Do not rename the Phase 135 top-level `name` `"v2 snapshot schema, checkpointing, and recovery"`.

    Exact new IDs and ownership (D-10, D-12, D-14):

    1. `v2-2-typed-package-staged-admission`
       - requirements: `PACK-01`, `PACK-02`, `PACK-03`, `PACK-04`, `PACK-05`, `PACK-06`, `PACK-07`
       - owning phase: 132
       - evidence roots: `docs/parity/catalog/mempool-policy.md` section "Typed Package Vocabulary and Staged Admission", `packages/open-bitcoin-mempool/src/package`, `scripts/check-phase132-typed-package-staged-admission.ts`
       - Knots anchors: `packages/bitcoin-knots/src/policy/packages.cpp`, `packages/bitcoin-knots/src/rpc/mempool.cpp`, `packages/bitcoin-knots/src/test/txpackage_tests.cpp`

    2. `v2-2-rolling-fee-expiry-pressure`
       - requirements: `PRESS-01`, `PRESS-02`, `PRESS-03`, `PRESS-04`, `PRESS-05`
       - owning phase: 131
       - evidence roots: existing PRESS catalog prose in `docs/parity/catalog/mempool-policy.md`, `packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs`, `scripts/check-phase131-rolling-fee-expiry-pressure.ts`
       - Knots anchors: `packages/bitcoin-knots/src/txmempool.cpp`, `packages/bitcoin-knots/test/functional/mempool_limit.py`

    3. `v2-2-receive-independent-maintenance-and-transport-receipts`
       - requirements: `PPKG-04`, `IBR-01`, `IBR-02`, `IBR-03`, `IBR-04`
       - owning phase: 136
       - evidence roots: `packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs`, `packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs`, `packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs`, `.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-VERIFICATION.md`
       - Knots anchors: `packages/bitcoin-knots/src/net_processing.cpp`

    4. `v2-2-parity-uat-release-boundary`
       - requirements: `MPVFY-01`, `MPVFY-02`, `MPVFY-03`, `MPVFY-04` only
       - owning phase: 138
       - evidence roots: `138-UAT.md`, `scripts/check-phase138-parity-uat-release-boundary.ts` (named now; file lands in Plan 03), `docs/parity/release-readiness.md`
       - Knots anchors: `packages/bitcoin-knots/src/txmempool.cpp`, `packages/bitcoin-knots/src/policy/packages.cpp`, `packages/bitcoin-knots/src/rpc/mempool.cpp`, `packages/bitcoin-knots/src/net_processing.cpp`, `packages/bitcoin-knots/src/test/txpackage_tests.cpp`, `packages/bitcoin-knots/test/functional/mempool_limit.py`, `packages/bitcoin-knots/test/functional/mempool_persist.py`, `packages/bitcoin-knots/test/functional/p2p_opportunistic_1p1c.py`

    Each new `checklist.surfaces[]` / index entry must include evidence paths, Knots anchors, intentional differences, deferred scope (FUT-12 through FUT-17), and the owning phase — same shape as `v2-2-resource-time-fee-primitives`.

    Preserve existing owners: FEEP on `v2-2-resource-time-fee-primitives`, PPKG-01..03 on `v2-2-package-aware-download-orphan-bridge`, MPLIFE on `v2-2-authoritative-cross-cache-lifecycle-integration`, MPDUR on `v2-2-snapshot-schema-checkpointing-recovery`, MPOBS on `v2-2-rpc-and-sanitized-operator-evidence`.

    In the Phase 134 surface `known_gaps`, keep a line that still contains the exact substring `Phase 138 owns` (required by `scripts/check-phase134-authoritative-lifecycle/scope.ts`). Preferred wording: `Phase 138 owns MPVFY-01 through MPVFY-04`. Never write `Phase 138 is complete`, `Phase 138 is done`, or `Phase 138 shipped` on README, packages/README, mempool-policy catalog, checklist, or index.json.

    Do not create a new catalog page. Prefer index/checklist rows pointing at existing `docs/parity/catalog/mempool-policy.md` and `docs/parity/catalog/rpc-cli-config.md` prose (D-11, D-12). Do not edit `docs/parity/catalog/mempool-policy.md` in this plan (Plan 01 owns the PRESS-05 sentence). Do not flip `.planning/REQUIREMENTS.md` checkboxes (Plan 04).
  </action>
  <verify>
    <automated>rg -n "v2-2-(typed-package-staged-admission|rolling-fee-expiry-pressure|receive-independent-maintenance-and-transport-receipts|parity-uat-release-boundary)" docs/parity/index.json docs/parity/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` and `docs/parity/checklist.md` each contain all four exact new surface IDs
    - the closeout surface lists `MPVFY-01` through `MPVFY-04` and none of PACK, PRESS, PPKG, IBR, FEEP, MPLIFE, MPDUR, or MPOBS
    - PACK-01 through PACK-07 appear only under `v2-2-typed-package-staged-admission`
    - PRESS-01 through PRESS-05 appear only under `v2-2-rolling-fee-expiry-pressure`
    - PPKG-04 and IBR-01 through IBR-04 appear only under `v2-2-receive-independent-maintenance-and-transport-receipts`
    - top-level surfaces still include the exact name `"v2 snapshot schema, checkpointing, and recovery"`
    - `v2-2-package-aware-download-orphan-bridge` remains `done`
    - `docs/parity/index.json` still contains `Phase 138 owns`
    - `rg -n "Phase 138 is (complete|done|shipped)" docs/parity/index.json docs/parity/checklist.md README.md` prints no matches
  </acceptance_criteria>
  <done>Machine and checklist owners exist for PACK, PRESS, Phase 136, and MPVFY without promoting surfaces or flipping Pending rows.</done>
</task>

<task type="auto">
  <name>Task 2: Write 138-UAT.md and state the D-21 claim without unscoped package relay</name>
  <files>
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md
    README.md
    docs/operator/runtime-guide.md
    docs/parity/release-readiness.md
    docs/parity/support-matrix.md
    docs/parity/service-operation-expectations.md
  </files>
  <read_first>
    .planning/phases/117-parity-traceability-uat-and-release-guardrails/117-UAT.md
    docs/operator/runtime-guide.md
    docs/parity/service-operation-expectations.md
    docs/parity/release-readiness.md
    docs/parity/support-matrix.md
    README.md
    scripts/check-phase117-parity-uat-release-boundary.ts
  </read_first>
  <action>
    Create `.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md` by copying the 117-UAT.md sections (Required Deterministic Tests, Optional Public-Network Review, Summary, Gaps, Focused Command Evidence). Differences (D-15, D-16):

    - Title: `Phase 138 UAT`
    - Required tests stay deterministic and may still say `result: pending` until Plan 04 / execution evidence exists. Do not mark the package `status: passed` in this plan.
    - Required test 1 expected text must include: 4×6 MPVFY-01 matrix, Instant-free PRESS-05 work-count smoke, and the composition test name `recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert`.
    - Required test 2 expected text must include the four new surface IDs and exactly-once ownership of all 40 v2.2 IDs, with MPVFY only on `v2-2-parity-uat-release-boundary`.
    - Required test 3 expected text must include the D-21 sentence and the D-22 deny list.
    - Required test 4 expected text must include these exact command strings (already published in `docs/operator/runtime-guide.md` Phase 137 section):

    ```
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -datadir=/tmp/open-bitcoin-mainnet testmempoolaccept '["&lt;hex&gt;"]'
    bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -datadir=/tmp/open-bitcoin-mainnet testmempoolaccept '["&lt;hex&gt;"]'
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -datadir=/tmp/open-bitcoin-mainnet submitpackage '["&lt;hex&gt;"]'
    bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -datadir=/tmp/open-bitcoin-mainnet submitpackage '["&lt;hex&gt;"]'
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package dry-run --hex '&lt;hex&gt;'
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package dry-run --hex '&lt;hex&gt;'
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package submit --hex '&lt;hex&gt;'
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package submit --hex '&lt;hex&gt;'
    cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet status --format json
    bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet status --format json
    ```

    Use the real `<hex>` token in the file, not the XML escape. Also require `bun test scripts/check-phase138-parity-uat-release-boundary.test.ts`, `bun run scripts/check-phase138-parity-uat-release-boundary.ts`, and `bash scripts/verify.sh`.

    - Optional Public-Network Review must be exactly `status: not run` (not `pending`) and must say it is never a default, CI, or release gate.

    Add a `## Phase 138 Parity UAT And Release Boundary Review` section to `docs/operator/runtime-guide.md` after the Phase 137 section. The section must contain the verbatim D-21 sentence:

    `bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members`

    plus companion wording: persist canonical entries / acceptance times / surviving local unbroadcast, rebuild derived state and reset rolling fee on restart, and hermetic default verification.

    Reuse the Phase 137 Cargo/Bazel command blocks unchanged in that new section (checker Plan 03 will require those strings in the runtime guide).

    Update `README.md`, `docs/parity/release-readiness.md`, and `docs/parity/support-matrix.md` so each contains the same verbatim D-21 sentence. Keep the existing README P2P row clause `package relay, bloom/filter serving, public defaults, archive/production-scale serving, public-network gates, and production readiness remain deferred` — that no-claim marker keeps Phase 117 passing.

    Forbidden in any new positive clause on those live files (D-21, D-22, D-24): `package relay` without a same-clause no-claim marker; `general package wire` as enabled; `arbitrary multi-parent`; `whole-mempool rebroadcast`; `public relay by default`; `production relay`; `guaranteed propagation`; `public-network CI` as a default/release gate; `production full-node readiness`; `production-funds`.

    If `docs/parity/service-operation-expectations.md` is missing any of the ten Cargo/Bazel strings above, add them. Do not invent an installed-alias-only workflow.

    Do not scan or rewrite historical `.planning/` archives. Do not archive v2.2. Do not run `/gsd-complete-milestone`.
  </action>
  <verify>
    <automated>rg -n "bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members" README.md docs/operator/runtime-guide.md docs/parity/release-readiness.md docs/parity/support-matrix.md .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `138-UAT.md` exists and contains `status: not run`, `bash scripts/verify.sh`, both `open-bitcoin-cli` Cargo/Bazel `testmempoolaccept` forms, and both `open-bitcoin` Cargo/Bazel `package dry-run` forms
    - `docs/operator/runtime-guide.md` contains the heading `Phase 138 Parity UAT And Release Boundary Review` and the verbatim D-21 sentence
    - README.md, release-readiness.md, and support-matrix.md each contain the verbatim D-21 sentence
    - `rg -n "supports package relay|provides package relay|enables package relay" README.md docs/operator/runtime-guide.md docs/parity/release-readiness.md docs/parity/support-matrix.md` prints no matches
    - README.md still contains `package relay` and `remain deferred` in the same P2P-row sentence
    - `138-UAT.md` does not contain `status: passed`
  </acceptance_criteria>
  <done>Committed UAT and live docs state D-21 exactly, keep 117's deferred package-relay archive wording, and do not flip requirement boxes.</done>
</task>

</tasks>

<verification>
```bash
rg -n "v2-2-(typed-package-staged-admission|rolling-fee-expiry-pressure|receive-independent-maintenance-and-transport-receipts|parity-uat-release-boundary)" docs/parity/index.json docs/parity/checklist.md
rg -n "bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages" README.md docs/operator/runtime-guide.md docs/parity/release-readiness.md
rg -n "status: not run" .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-UAT.md
git diff --check
```
</verification>

<success_criteria>
- PACK, PRESS, Phase 136 IBR/PPKG-04, and MPVFY each have one index/checklist owner.
- `138-UAT.md` is the only closeout UAT package and records public-network review as not run.
- Live docs state the locked D-21 sentence without a positive unscoped `package relay` claim.
- No REQUIREMENTS checkbox, surface `done`, last-gate, or `verify.sh` change lands in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-02-SUMMARY.md`
</output>
