---
phase: 138-parity-adversarial-pressure-restart-and-release-guardrails
status: pending
source:
  - 138-01-SUMMARY.md
  - 138-02-PLAN.md
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 138-2026-08-22T04-13-58
generated_at: 2026-08-22T06:45:00.000Z
---

# Phase 138 UAT

## Current Test

[required deterministic tests pending later-plan evidence]

## Required Deterministic Tests

### 1. MPVFY-01 coverage composition

expected: The 4×6 MPVFY-01 matrix names pinned-Knots fixtures, fake-clock scenarios, randomized graph-oracle tests, and failure injection across package, rolling-fee, pressure, expiry, recovery, and retry. Instant-free PRESS-05 work-count smoke stays on the default verifier path. The hermetic restart composition is `recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert`.
result: pending
evidence: Plan 01 landed the named composition and Instant-free PRESS-05 work-count pins. The Phase 138 checker that names every matrix cell lands in a later plan.

### 2. Exactly-once v2.2 surface ownership

expected: `docs/parity/index.json` and `docs/parity/checklist.md` contain `v2-2-typed-package-staged-admission`, `v2-2-rolling-fee-expiry-pressure`, `v2-2-receive-independent-maintenance-and-transport-receipts`, and `v2-2-parity-uat-release-boundary`. All 40 v2.2 IDs have exactly-once ownership. MPVFY-01 through MPVFY-04 appear only on `v2-2-parity-uat-release-boundary`.
result: pending
evidence: Plan 02 backfilled the four owners. Later plans still own last-gate checking and requirement-row flips.

### 3. D-21 scoped claim and D-22 deny list

expected: Live claim-bearing docs state bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members. The D-22 deny list rejects general package wire, arbitrary multi-parent assembly, whole-mempool rebroadcast, public/default/production relay, guaranteed propagation, public-network CI as a default or release gate, production full-node readiness, and production-funds claims.
result: pending
evidence: Plan 02 states the D-21 sentence on README, runtime-guide, release-readiness, support-matrix, and this UAT package. The Phase 138 last-gate checker lands in a later plan.

### 4. Repo-local Cargo and Bazel operator commands

expected: Operators can copy these exact forms without using only the installed `open-bitcoin` alias:

```
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -datadir=/tmp/open-bitcoin-mainnet testmempoolaccept '["<hex>"]'
bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -datadir=/tmp/open-bitcoin-mainnet testmempoolaccept '["<hex>"]'
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- -datadir=/tmp/open-bitcoin-mainnet submitpackage '["<hex>"]'
bazel run //packages/open-bitcoin-cli:open_bitcoin_cli -- -datadir=/tmp/open-bitcoin-mainnet submitpackage '["<hex>"]'
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package dry-run --hex '<hex>'
bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package dry-run --hex '<hex>'
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package submit --hex '<hex>'
bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet package submit --hex '<hex>'
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet status --format json
bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet status --format json
```

Also require `bun test scripts/check-phase138-parity-uat-release-boundary.test.ts`, `bun run scripts/check-phase138-parity-uat-release-boundary.ts`, and `bash scripts/verify.sh`.
result: pending
evidence: The command strings are published here and in `docs/operator/runtime-guide.md`. The Phase 138 checker pair is named now and lands in a later plan.

## Optional Public-Network Review

status: not run

No public peer contact was needed or performed. Public-network package,
pressure, or restart review is optional operator UAT and may be recorded as
not run. This does not create a gap and is never a default, CI, or release
gate. It is never required for pre-commit, default CI, release-boundary
verification, or `bash scripts/verify.sh`.

Copy-pasteable local evidence commands remain available in
[`docs/operator/runtime-guide.md`](../../../docs/operator/runtime-guide.md#phase-138-parity-uat-and-release-boundary-review).

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

[]

## Focused Command Evidence

- `bun test scripts/check-phase138-parity-uat-release-boundary.test.ts`: Phase 138 last-gate mutation suite.
- `bun run scripts/check-phase138-parity-uat-release-boundary.ts`: Phase 138 last-gate checker.
- `bash scripts/verify.sh`: required deterministic contract; not a public-network gate.
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib retry`: Phase 136 retry evidence; named by the Phase 138 checker.
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib maintenance_tick`: Phase 136 maintenance-tick evidence; named by the Phase 138 checker.
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib package_fanout`: Phase 136 package-fanout evidence; named by the Phase 138 checker.
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib retry_worker`: Phase 136 retry-worker evidence; named by the Phase 138 checker.
