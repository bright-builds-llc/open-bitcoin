---
phase: 138-parity-adversarial-pressure-restart-and-release-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs
  - docs/parity/source-breadcrumbs.json
  - packages/open-bitcoin-bench/src/cases/mempool.rs
  - scripts/check-phase131-rolling-fee-expiry-pressure.ts
  - scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
  - docs/parity/catalog/mempool-policy.md
  - docs/parity/benchmarks.md
requirements:
  - MPVFY-01
  - MPVFY-02
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 138-2026-08-22T04-13-58
generated_at: 2026-08-22T05:05:00.000Z
user_setup: []
must_haves:
  truths:
    - "One named hermetic fake-clock test proves restart install preserves local-package membership and surviving unbroadcast, resets rolling fee, remints retry timers, and leaves the aggregate unchanged on injected install failure."
    - "Default verification no longer fails the Phase 131 sustained-pressure bench on a 2-second Instant wall-clock budget."
    - "PRESS-05 remains closed by the recomputation oracle, N=24 trim cycles, one retained entry, and rolling-fee bump — not by silicon latency."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs"
      provides: "D-04/D-05 restart × package × unbroadcast × remint × inject composition"
      contains: "recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert"
    - path: "packages/open-bitcoin-bench/src/cases/mempool.rs"
      provides: "Work-count sustained-pressure smoke without Instant ceiling"
      contains: "SUSTAINED_PRESSURE_TRIM_CYCLES"
    - path: "scripts/check-phase131-rolling-fee-expiry-pressure.ts"
      provides: "PRESS-05 pin retargeted to oracle + work-count symbols"
      contains: "SUSTAINED_PRESSURE_TRIM_CYCLES"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/tests/recovery_cases.rs"
      to: "packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs"
      via: "mod restart_composition"
      pattern: "mod restart_composition"
    - from: "scripts/check-phase131-rolling-fee-expiry-pressure.ts"
      to: "packages/open-bitcoin-bench/src/cases/mempool.rs"
      via: "work-count PRESS-05 pin"
      pattern: "SUSTAINED_PRESSURE_TRIM_CYCLES"
---

# Phase 138 Plan 01: Restart Composition and Work-Count PRESS-05 Gate

<objective>
Close the only missing MPVFY-01 cell and retire the MPVFY-02 wall-clock smoke gate without rewriting package, pressure, recovery, or retry runtime.

Purpose: D-01 through D-08 require named existing 4×6 proof plus one hermetic restart composition, and default `verify.sh` must stop using a 2-second `Instant` ceiling. PRESS-05 stays closed by oracle + work counts.

Output: One new Rust composition test with a recovery breadcrumb, Instant-free sustained-pressure bench, retargeted Phase 131 PRESS-05 pin, and catalog/benchmark wording that says default smoke is work-count / `threshold_free`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-RESEARCH.md
@packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
@packages/open-bitcoin-node/src/network/tests/recovery_cases/staging.rs
@packages/open-bitcoin-node/src/network/tests/recovery_cases/metadata.rs
@packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs
@packages/open-bitcoin-bench/src/cases/mempool.rs
@scripts/check-phase131-rolling-fee-expiry-pressure.ts
@docs/parity/catalog/mempool-policy.md
@docs/parity/source-breadcrumbs.json

<interfaces>
From scripts/check-phase117-parity-uat-release-boundary.ts (export shape to copy later; this plan does not create the 138 checker):
```typescript
export function checkPhase117ParityUatReleaseBoundary(maybeRepoRoot?: string): string[]
```

From packages/open-bitcoin-node/src/network/tests/recovery_cases.rs:
```rust
fn relay_enabled_network_with_chain(
    nonce: u64,
    block_count: u32,
    mempool_config: PolicyConfig,
) -> (ManagedPeerNetwork<MemoryChainstateStore>, Vec<Txid>, Block);
fn spend_transaction(prevout: Txid, output_value: i64) -> Transaction;
fn prepare_and_install_mempool_recovery(
    network: ManagedPeerNetwork<MemoryChainstateStore>,
    snapshot: &MempoolSnapshot,
    startup_at: PolicyTime,
) -> (ManagedNetworkHandle, ManagedMempoolRecoverySummary);
```

From packages/open-bitcoin-node/src/network/lifecycle_projection.rs:
```rust
pub struct RecoveryInstallFailureGuard;
impl RecoveryInstallFailureGuard {
    pub fn inject(point: RecoveryInstallFailurePoint) -> Self;
}
```

From packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs:
```rust
fn recovery_install_clears_due_time_and_cursor(); // remint-to-None only — not the D-04 intersection
handle.retry_timer_for_test() -> Result<(Option<u64>, Option<MempoolMemberIdentity>), _>;
handle.set_retry_timer_for_test(maybe_due: Option<u64>, maybe_cursor: Option<MempoolMemberIdentity>);
```

Cite separately — do not treat these as the D-04 composition (per D-05):
- `rolling_fee_restarts_at_zero_without_durability`
- `recovery_keeps_unbroadcast_only_for_the_exact_surviving_member`
- `recovery_install_clears_due_time_and_cursor`
- `maintenance_tick_remints_due_time_from_injected_context`
- `every_injected_recovery_install_validation_failure_preserves_the_exact_aggregate`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Default verifier → bench | A wall-clock Instant gate makes default verify silicon-dependent and can look like a public-network/soak requirement. |
| Restart composition test → runtime | A process-kill or public-network soak would broaden v2.2 claims and break hermetic CI. |
| PRESS-05 evidence → catalog | Deleting the Instant ceiling without keeping oracle/work-count pins would silently reopen PRESS-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | `packages/open-bitcoin-bench/src/cases/mempool.rs` | mitigate | Delete only `Instant::now`, `started.elapsed()`, and `SUSTAINED_PRESSURE_MAX_ELAPSED`. Keep `SUSTAINED_PRESSURE_TRIM_CYCLES = 24`, one-retained-entry, and rolling-fee-bump checks. |
| T-138-02 | Denial of Service | default `verify.sh` smoke | mitigate | Do not add public-network, soak, process-kill, or replacement wall-clock gates. |
| T-138-03 | Information Disclosure | restart composition fixtures | accept | Synthetic coinbase/spend fixtures only; no operator datadir, secrets, or `.planning/` history. |
| T-138-04 | Elevation of Privilege | recovery install inject | mitigate | Use existing `RecoveryInstallFailureGuard::inject` under `cfg(test)`; do not add production failure hooks. |
| T-138-05 | Spoofing | PRESS-05 catalog sentence | mitigate | Rewrite the catalog to say default smoke is work-count / `threshold_free`; timings are `--full` / UAT only. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the hermetic restart × package × remint × inject composition</name>
  <files>
    packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs
    packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-RESEARCH.md
    packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    packages/open-bitcoin-node/src/network/tests/recovery_cases/staging.rs
    packages/open-bitcoin-node/src/network/tests/recovery_cases/metadata.rs
    packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs
    docs/parity/source-breadcrumbs.json
  </read_first>
  <behavior>
    - Test `recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert` builds a source-only `MempoolSnapshot` with a local-origin parent+child package, `RelayIntent::Requested`, known acceptance times, and surviving unbroadcast membership for both members.
    - `RecoveryInstallFailureGuard::inject` on a prepared install returns `Err` and `authority_debug_snapshot_for_test()` equals the pre-install baseline.
    - Successful `prepare_mempool_recovery_at` + `install_mempool_recovery` then keeps both canonical members, keeps both unbroadcast identities, rebuilds derived fanout/serving indexes, and sets `rolling_mempool_fee_rate().fee_rate().sats_per_kvb() == 0`.
    - After successful install, `retry_timer_for_test()` is `(None, None)`; one subsequent `maintenance_tick` with injected `RetryDecisionContext` remints due time via `next_retry_due_unix_seconds` without reading wall-clock time.
    - The test uses `PolicyTime::from_unix_seconds` only. It must not call `std::thread::sleep`, `Instant::now`, `std::process`, or any public-network API.
  </behavior>
  <action>
    Create `packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs` as a child of the existing `recovery_cases.rs` test root (per D-04, D-05). Do not add this case to `staging.rs` (already 483 lines).

    Copy the breadcrumb block from `recovery_cases/staging.rs` onto the new file:

    ```
    // Parity breadcrumbs:
    // - packages/bitcoin-knots/src/node/mempool_persist.cpp
    // - packages/bitcoin-knots/src/txmempool.cpp
    // - packages/bitcoin-knots/src/validation.cpp
    // - packages/bitcoin-knots/test/functional/mempool_persist.py
    ```

    Append the new path to the existing `node-mempool-recovery-topology` `files` array in `docs/parity/source-breadcrumbs.json` immediately after `packages/open-bitcoin-node/src/network/tests/recovery_cases/staging.rs`. Do not create a new breadcrumb group.

    Add `mod restart_composition;` next to `mod staging;` in `recovery_cases.rs`.

    Reuse `relay_enabled_network_with_chain`, `spend_transaction`, `txid`/`wtxid`, `MempoolSnapshot::try_new_current`, `MempoolSnapshotRecord::try_from_canonical`, `ManagedNetworkHandle::from_network_fixture`, `prepare_mempool_recovery_at`, `install_mempool_recovery`, and `RecoveryInstallFailureGuard::inject` exactly as `staging.rs` / `metadata.rs` already do. Build the parent as `spend_transaction(coinbase_txids[0], 499_999_000)` and the child as `spend_transaction(txid(&parent), 499_998_000)`. Admit both on a source handle with `submit_local_transaction_outcome_at(..., RelayIntent::Requested)` so canonical metadata is Local+Requested, then capture the snapshot from those admitted records plus `BTreeSet` unbroadcast membership for both identities.

    Pre-seed `set_retry_timer_for_test(Some(1_000), Some(parent_identity))` before the injected-failure attempt so remint-to-`None` is observable.

    After successful install, call the same maintenance-tick entry `maintenance_tick_cases.rs` uses (`handle` method that remints from injected `RetryDecisionContext`) and assert the due time equals `next_retry_due_unix_seconds(context)`.

    Do not rewrite `install_mempool_recovery`, snapshot schema, retry worker, or package admission. Do not add process-kill, public-network, or wall-clock soak restart gates (D-04). Do not use `unwrap()`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase138-01-restart-composition -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/network/tests/recovery_cases.rs` contains `mod restart_composition;`
    - `packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs` contains the exact fn name `recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert`
    - that file contains `RecoveryInstallFailureGuard::inject`, `rolling_mempool_fee_rate`, `retry_timer_for_test`, and `PolicyTime::from_unix_seconds`
    - that file does not contain `Instant::now`, `thread::sleep`, `std::process`, `fetch(`, or `127.0.0.1`
    - `docs/parity/source-breadcrumbs.json` group `node-mempool-recovery-topology` lists `packages/open-bitcoin-node/src/network/tests/recovery_cases/restart_composition.rs`
    - `bun run scripts/check-parity-breadcrumbs.ts` exits 0
    - the focused cargo test exits 0
  </acceptance_criteria>
  <done>The named D-04/D-05 composition exists, is hermetic, and is registered on the existing recovery breadcrumb group.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the Phase 131 2s Instant smoke gate and keep PRESS-05 oracle/work-counts</name>
  <files>
    packages/open-bitcoin-bench/src/cases/mempool.rs
    scripts/check-phase131-rolling-fee-expiry-pressure.ts
    scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
    docs/parity/catalog/mempool-policy.md
    docs/parity/benchmarks.md
  </files>
  <read_first>
    packages/open-bitcoin-bench/src/cases/mempool.rs
    scripts/check-phase131-rolling-fee-expiry-pressure.ts
    scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
    docs/parity/catalog/mempool-policy.md
    docs/parity/benchmarks.md
    scripts/check-benchmark-report.ts
  </read_first>
  <action>
    In `packages/open-bitcoin-bench/src/cases/mempool.rs` (per D-07, D-06, D-08):

    - Delete `const SUSTAINED_PRESSURE_MAX_ELAPSED: Duration = Duration::from_millis(2_000);`
    - Delete `let started = Instant::now();`, `let elapsed = started.elapsed();`, and the `if elapsed > SUSTAINED_PRESSURE_MAX_ELAPSED { return Err(...) }` block (currently around lines 151, 181, 198–206).
    - Remove `Instant` from the `std::time` import. Remove `Duration` from that import only if it becomes unused.
    - Keep `const SUSTAINED_PRESSURE_CASE_ID: &str = "mempool-policy.sustained-pressure-trim";`
    - Keep `const SUSTAINED_PRESSURE_TRIM_CYCLES: usize = 24;`
    - Keep the N=24 admit/trim loop, the `entries().len() != 1` failure, and the `rolling_mempool_fee_rate().fee_rate().sats_per_kvb() == 0` bump failure.

    Do not delete `sustained_pressure_oracle_agrees_across_fill_trim_block_decay_expiry_refill_reorg`, `recompute_resource_ledger`, or `rolling_fee_restarts_at_zero_without_durability` from `packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs`.

    In `scripts/check-phase131-rolling-fee-expiry-pressure.ts` function `checkPress05OracleAndBench`:

    - Keep the oracle pins for `sustained_pressure_oracle`, `recompute_resource_ledger`, and `rolling_fee_restarts_at_zero`.
    - Replace the bench pin `!bench.includes("SUSTAINED_PRESSURE_MAX_ELAPSED")` with pins that `bench` contains all of: `mempool-policy.sustained-pressure-trim`, `SUSTAINED_PRESSURE_TRIM_CYCLES`, and `const SUSTAINED_PRESSURE_TRIM_CYCLES: usize = 24`.
    - Keep `catalog.includes("sustained-pressure-trim")`.
    - Change the failure string to: `P131 PRESS-05: hermetic sustained-pressure bench work-count bounds must remain verifier-reachable`
    - Do not change `requireFinalPhaseChecker` or any `PHASE117_CHECK` last-gate assertion in this plan (Plan 03 owns that).

    In `scripts/check-phase131-rolling-fee-expiry-pressure.test.ts`, keep the "PRESS-05 bench threshold" mutation but retarget it so removing `SUSTAINED_PRESSURE_TRIM_CYCLES` (not the Instant constant) produces `P131 PRESS-05: hermetic sustained-pressure bench work-count bounds must remain verifier-reachable`.

    In `docs/parity/catalog/mempool-policy.md` section `### Sustained-pressure bounds (PRESS-05)`, replace the sentence that currently says `2s wall-time ceiling under the default verifier` with this exact wording:

    `mempool-policy.sustained-pressure-trim` (N=24 admit/trim cycles) enforces work-count bounds under default smoke; default smoke stays `threshold_free` and must not use a wall-clock Instant ceiling. Documented timing budgets belong only on the opt-in `--full` / UAT path.

    In `docs/parity/benchmarks.md`, add one sentence under Modes or Benchmark Scope stating that `mempool-policy.sustained-pressure-trim` default-smoke evidence is work-count / `threshold_free`, not a 2-second Instant gate. Do not add latency fields to `scripts/check-benchmark-report.ts`. Do not make `bun run scripts/command-timings.ts` a release or default-verify gate (D-09).
  </action>
  <verify>
    <automated>bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SUSTAINED_PRESSURE_MAX_ELAPSED|Instant::now" packages/open-bitcoin-bench/src/cases/mempool.rs` prints no matches
    - `packages/open-bitcoin-bench/src/cases/mempool.rs` still contains `SUSTAINED_PRESSURE_TRIM_CYCLES`, `mempool-policy.sustained-pressure-trim`, `entries().len() != 1`, and `rolling_mempool_fee_rate`
    - `scripts/check-phase131-rolling-fee-expiry-pressure.ts` contains `SUSTAINED_PRESSURE_TRIM_CYCLES` and does not contain `SUSTAINED_PRESSURE_MAX_ELAPSED`
    - `scripts/check-phase131-rolling-fee-expiry-pressure.ts` still contains `requireFinalPhaseChecker` checking `PHASE117_CHECK`
    - `docs/parity/catalog/mempool-policy.md` contains `threshold_free` and `opt-in` and does not contain `2s wall-time ceiling under the default verifier`
    - `packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs` still contains `sustained_pressure_oracle` and `rolling_fee_restarts_at_zero`
    - `bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts` exits 0
  </acceptance_criteria>
  <done>Default PRESS-05 smoke is Instant-free and still oracle- and work-count-pinned; Phase 131 last-gate still requires Phase 117 until Plan 03.</done>
</task>

</tasks>

<verification>
```bash
bun run scripts/command-timings.ts run --key phase138-01-restart-composition -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib recovery_restart_preserves_local_package_unbroadcast_remints_retry_and_keeps_injected_install_failure_inert -- --nocapture
bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
bun run scripts/check-parity-breadcrumbs.ts
rg -n "SUSTAINED_PRESSURE_MAX_ELAPSED|Instant::now" packages/open-bitcoin-bench/src/cases/mempool.rs
git diff --check
```
</verification>

<success_criteria>
- The D-04/D-05 composition is a named hermetic Rust test with a recovery breadcrumb.
- The 2-second Instant smoke gate is gone from the bench and from the Phase 131 checker pin.
- PRESS-05 oracle, N=24, one retained entry, and rolling-fee bump remain.
- No last-gate, REQUIREMENTS, or `in_progress` surface status changes land in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-01-SUMMARY.md`
</output>
