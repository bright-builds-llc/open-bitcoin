---
phase: 138-parity-adversarial-pressure-restart-and-release-guardrails
plan: "04"
type: execute
wave: 3
depends_on:
  - "138-03"
files_modified:
  - scripts/check-phase135-snapshot-recovery.ts
  - scripts/check-phase135-snapshot-recovery.test.ts
  - scripts/check-phase138-parity-uat-release-boundary/checks.ts
  - scripts/check-phase138-parity-uat-release-boundary/test-fixtures.ts
  - scripts/check-phase138-parity-uat-release-boundary.test.ts
  - .planning/REQUIREMENTS.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
requirements:
  - MPVFY-03
  - MPVFY-04
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 138-2026-08-22T04-13-58
generated_at: 2026-08-22T05:05:00.000Z
user_setup: []
must_haves:
  truths:
    - "Every implemented-but-Pending v2.2 requirement checkbox is [x] and the traceability table says Complete, with no leftover Pending rows."
    - "Completed v2.2 surfaces are done, including the Phase 135 human title row, without renaming that title."
    - "ROADMAP, PROJECT, and STATE agree that v2.2 implementation and closeout evidence exist, and they do not archive the milestone."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Flipped PACK, PPKG-04, MPDUR, IBR, MPOBS, and MPVFY checkboxes"
      contains: "- [x] **MPVFY-04**"
    - path: "docs/parity/index.json"
      provides: "Promoted v2.2 surfaces including the closeout owner"
      contains: "v2-2-parity-uat-release-boundary"
    - path: "scripts/check-phase135-snapshot-recovery.ts"
      provides: "Parity lock requiring done/Complete instead of in_progress/Pending"
      contains: "MPDUR-01 through MPDUR-04"
  key_links:
    - from: "scripts/check-phase138-parity-uat-release-boundary/checks.ts"
      to: ".planning/REQUIREMENTS.md"
      via: "require every v2.2 ID to be checked [x] exactly once"
      pattern: "- \\[x\\] \\*\\*MPVFY-0"
    - from: "scripts/check-phase135-snapshot-recovery.ts"
      to: "docs/parity/index.json"
      via: "Phase 135 surface done + MPDUR Complete"
      pattern: "v2 snapshot schema, checkpointing, and recovery"
---

# Phase 138 Plan 04: Flip Pending Rows, Promote Surfaces, Reconcile Metadata

<objective>
Close the v2.2 ledger only after the Plan 03 checker can name evidence, then reconcile planning metadata without archiving.

Purpose: D-17 and D-18 exist specifically to avoid a Phase 129-style leftover-Pending reconciliation. Phase 135's hard `in_progress` + MPDUR-Pending lock must move in the same change as the flip.

Output: All leftover v2.2 IDs Complete, required surfaces `done`, tightened 135/138 checkers, and ROADMAP/PROJECT/STATE agreement. No `/gsd-complete-milestone`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
@.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-RESEARCH.md
@scripts/check-phase135-snapshot-recovery.ts
@scripts/check-phase134-authoritative-lifecycle/scope.ts
@docs/parity/index.json
@docs/parity/checklist.md

<interfaces>
Phase 135 lock today (`scripts/check-phase135-snapshot-recovery.ts` ~474–501) fails unless all of these are true:
- top-level surface name `"v2 snapshot schema, checkpointing, and recovery"` status `in_progress`
- checklist id `v2-2-snapshot-schema-checkpointing-recovery` status `in_progress`
- checklist human row `| v2 snapshot schema, checkpointing, and recovery | In progress |`
- catalog/checklist/index contain `MPDUR-01 through MPDUR-04 remain pending`
- REQUIREMENTS contains `- [ ] **MPDUR-0X**` for 01–04
- checklist contains `| MPDUR-0X | Pending |`

Phase 135 mutations in `scripts/check-phase135-snapshot-recovery.test.ts` currently fail when those fields are changed to complete/done. After this plan they must fail when changed *away* from done/Complete.

Phase 134 `scope.ts` still requires `known_gaps` to contain `phase 138 owns` and forbids `\bphase 138 (?:is )?(?:implemented|complete|done|shipped)\b` on README, packages/README, mempool catalog, checklist, and index.json. Keep `Phase 138 owns MPVFY-01 through MPVFY-04`. Never write `Phase 138 is complete` on those files.

IDs to flip from `- [ ]` to `- [x]` in REQUIREMENTS.md (D-17):
PACK-01..07, PPKG-04, MPDUR-01..04, IBR-01..04, MPOBS-01..03, MPVFY-01..04.
FEEP, PRESS, PPKG-01..03, and MPLIFE are already `[x]`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md checkboxes → milestone audit | Flipping before named evidence recreates the Phase 129 leftover-Pending problem in reverse (false Complete). |
| Phase 135 lock → index/checklist | Promoting MPDUR while the lock still requires Pending makes default verify fail. |
| Planning metadata → archive workflow | Saying the milestone is archived or running `/gsd-complete-milestone` exceeds this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-16 | Spoofing | REQUIREMENTS.md / ROADMAP.md | mitigate | Flip only after `bun run scripts/check-phase138-parity-uat-release-boundary.ts` names evidence. Then tighten that checker to require `[x]` and `done`. |
| T-138-17 | Tampering | Phase 135 parity lock | mitigate | Change the lock and the MPDUR/surface flip in the same task. Keep WR-01 and deferred-claim denies. Do not rename the Phase 135 human title. |
| T-138-18 | Elevation of Privilege | PROJECT.md / STATE.md | mitigate | Reconcile implementation/closeout evidence as complete. Do not archive v2.2, invent a milestone-audit workflow, or write production/public-relay claims. |
| T-138-19 | Repudiation | Phase 134 scanned claim surfaces | mitigate | On README, packages/README, mempool catalog, checklist, and index.json write `Phase 138 owns MPVFY-01 through MPVFY-04` / `Phase 138 closes the v2.2 claim boundary`, never `Phase 138 is complete`. |
| T-138-20 | Information Disclosure | STATE.md | accept | Planning metadata only; no secrets, datadirs, or identifiers. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Unlock Phase 135, flip leftover Pending rows, promote surfaces, tighten the 138 checker</name>
  <files>
    scripts/check-phase135-snapshot-recovery.ts
    scripts/check-phase135-snapshot-recovery.test.ts
    scripts/check-phase138-parity-uat-release-boundary/checks.ts
    scripts/check-phase138-parity-uat-release-boundary/test-fixtures.ts
    scripts/check-phase138-parity-uat-release-boundary.test.ts
    .planning/REQUIREMENTS.md
    docs/parity/index.json
    docs/parity/checklist.md
  </files>
  <read_first>
    scripts/check-phase135-snapshot-recovery.ts
    scripts/check-phase135-snapshot-recovery.test.ts
    scripts/check-phase138-parity-uat-release-boundary/checks.ts
    scripts/check-phase134-authoritative-lifecycle/scope.ts
    .planning/REQUIREMENTS.md
    docs/parity/index.json
    docs/parity/checklist.md
  </read_first>
  <action>
    First retarget the Phase 135 parity lock so it can accept the flip (D-13, research pitfall 3):

    In `scripts/check-phase135-snapshot-recovery.ts`:
    - Require top-level name `"v2 snapshot schema, checkpointing, and recovery"` status `done` (do not change the name string).
    - Require checklist id `v2-2-snapshot-schema-checkpointing-recovery` status `done`.
    - Require checklist human row `| v2 snapshot schema, checkpointing, and recovery | Done |`.
    - Require REQUIREMENTS `- [x] **MPDUR-01**` through `- [x] **MPDUR-04**`.
    - Require checklist `| MPDUR-01 | Complete |` through `| MPDUR-04 | Complete |`.
    - Stop requiring the substring `MPDUR-01 through MPDUR-04 remain pending`.
    - Keep requiring `WR-01 remains open and non-blocking` and the format-owned vs current-policy sentences already pinned.
    - Change `PHASE135_DIAGNOSTICS.parity` to: `P135 parity: evidence is done and MPDUR requirements are complete`
    - Keep `PHASE135_DIAGNOSTICS.claims` deferred-claim denies unchanged.

    Update `scripts/check-phase135-snapshot-recovery.test.ts` mutations `parity index status completes` and `checklist requirement completes` so they now mutate *away* from `done` / `Complete` / `- [x] **MPDUR-01**` and still expect `PHASE135_DIAGNOSTICS.parity`.

    Then flip `.planning/REQUIREMENTS.md`:
    - Checkbox section: change `- [ ] **PACK-0X**`, `- [ ] **PPKG-04**`, `- [ ] **MPDUR-0X**`, `- [ ] **IBR-0X**`, `- [ ] **MPOBS-0X**`, `- [ ] **MPVFY-0X**` to `- [x]`.
    - Traceability table Status column for those same IDs: `Pending` → `Complete`.
    - Do not add, remove, or remap any ID. Do not touch FUT-12 through FUT-17.

    Then promote surfaces in `docs/parity/index.json` and `docs/parity/checklist.md` from `in_progress` / `In progress` to `done` / `Done` for:
    - `v2-2-resource-time-fee-primitives`
    - `v2-2-authoritative-cross-cache-lifecycle-integration`
    - top-level name `v2 snapshot schema, checkpointing, and recovery` and checklist id `v2-2-snapshot-schema-checkpointing-recovery` (human title unchanged)
    - `v2-2-rpc-and-sanitized-operator-evidence`
    - `v2-2-typed-package-staged-admission`
    - `v2-2-rolling-fee-expiry-pressure`
    - `v2-2-receive-independent-maintenance-and-transport-receipts`
    - `v2-2-parity-uat-release-boundary`

    Keep `v2-2-package-aware-download-orphan-bridge` `done`.

    In the Phase 135 `known_gaps` array, replace `MPDUR-01 through MPDUR-04 remain pending until independent phase verification records its result.` with `MPDUR-01 through MPDUR-04 are closed by Phase 135 verification and Phase 138 named evidence.` Keep `Phase 138 owns final adversarial, parity, release, and claim proof.` or equivalent that still contains `Phase 138 owns`. Keep `WR-01 remains open and non-blocking`.

    In the Phase 134 `known_gaps`, keep `Phase 138 owns` (prefer `Phase 138 owns MPVFY-01 through MPVFY-04`). Do not write `Phase 138 is complete` on index, checklist, README, packages/README, or mempool-policy catalog.

    Tighten the Phase 138 checker (D-25, D-17): require every v2.2 ID in REQUIREMENTS.md to match `- [x] **ID**`, require the nine v2.2 surfaces listed above to be `done`, and add mutations that a leftover `- [ ] **MPVFY-01**` or an `in_progress` closeout surface fails. Update fixtures so the passing corpus uses `[x]` and `done`.
  </action>
  <verify>
    <automated>bun test scripts/check-phase135-snapshot-recovery.test.ts scripts/check-phase138-parity-uat-release-boundary.test.ts scripts/check-phase134-authoritative-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **PACK-01**` through `- [x] **PACK-07**`, `- [x] **PPKG-04**`, `- [x] **MPDUR-01**` through `- [x] **MPDUR-04**`, `- [x] **IBR-01**` through `- [x] **IBR-04**`, `- [x] **MPOBS-01**` through `- [x] **MPOBS-03**`, and `- [x] **MPVFY-01**` through `- [x] **MPVFY-04**`
    - `.planning/REQUIREMENTS.md` does not contain `- [ ] **PACK-`, `- [ ] **PPKG-04`, `- [ ] **MPDUR-`, `- [ ] **IBR-`, `- [ ] **MPOBS-`, or `- [ ] **MPVFY-`
    - `docs/parity/index.json` top-level name `"v2 snapshot schema, checkpointing, and recovery"` has `"status": "done"`
    - `docs/parity/checklist.md` still contains the human title `v2 snapshot schema, checkpointing, and recovery` and no longer contains `| v2 snapshot schema, checkpointing, and recovery | In progress |`
    - `scripts/check-phase135-snapshot-recovery.ts` contains `P135 parity: evidence is done and MPDUR requirements are complete` and does not contain `evidence stays in progress and MPDUR requirements stay pending`
    - `docs/parity/index.json` still contains `Phase 138 owns` and does not contain `Phase 138 is complete`
    - the automated bun test command exits 0
  </acceptance_criteria>
  <done>Leftover Pending rows and in_progress v2.2 surfaces are closed, and both the 135 and 138 checkers require that closed state.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP, PROJECT, and STATE without archiving</name>
  <files>
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    .planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-CONTEXT.md
  </read_first>
  <action>
    Reconcile planning metadata so v2.2 implementation and closeout evidence agree (D-18). Do not archive. Do not run `/gsd-complete-milestone v2.2`. Do not create a milestone-audit document.

    `.planning/ROADMAP.md`:
    - Keep Phase 138 Goal and Success Criteria as they are.
    - Keep `**Plans:** 4 plans` and the four plan checkboxes. Do not mark Phase 138 itself `[x]` or `Complete` in this task; execute-phase owns that after verification.
    - Remove any remaining `TBD` plan count for Phase 138.

    `.planning/PROJECT.md`:
    - Replace the Current State sentence `PPKG-04 and IBR-01 through IBR-04 stay Pending until Phase 138 closeout. Phase 137 is next for RPC and sanitized operator evidence.` with wording that Phase 130–138 implementation and closeout evidence now cover the scoped v2.2 surface, using the verbatim D-21 sentence: `bounded local-package APIs, same-peer 1P1C assembly over ordinary transaction messages, ordinary transaction fanout, and initial-broadcast-retry of locally submitted unbroadcast members`.
    - State that milestone archival remains `/gsd-complete-milestone v2.2` after Phase 138 verification passes.
    - Do not add a positive unscoped `package relay` clause. Historical v2.1 archive sentences that say package relay remained deferred at the v2.1 boundary may stay if they keep a no-claim marker.

    `.planning/STATE.md`:
    - Set `Current focus` / `Current Position` to Phase 138 closeout evidence, not Phase 137.
    - Record that leftover Pending v2.2 rows were flipped after named checker evidence.
    - Do not claim the milestone is archived.
    - On any sentence that might later be copied onto a Phase 134 claim surface, prefer `Phase 138 owns MPVFY-01 through MPVFY-04` over `Phase 138 is complete`.

    After the metadata edits, run `bun run scripts/check-phase138-parity-uat-release-boundary.ts` and `bun run scripts/check-phase135-snapshot-recovery.ts` so the flipped corpus still passes. Do not start a full `bash scripts/verify.sh` unless those focused checkers pass; if they pass, run `bash scripts/verify.sh` as the plan-level verification contract.
  </action>
  <verify>
    <automated>bun run scripts/check-phase138-parity-uat-release-boundary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the verbatim D-21 sentence and does not contain `Phase 137 is next`
    - `.planning/PROJECT.md` does not contain `PPKG-04 and IBR-01 through IBR-04 stay Pending`
    - `.planning/STATE.md` mentions Phase 138 and does not say the v2.2 milestone is archived
    - `.planning/ROADMAP.md` contains `**Plans:** 4 plans` and does not contain `0/TBD`
    - none of ROADMAP.md, PROJECT.md, or STATE.md contain `/gsd-complete-milestone v2.2` as a completed action; it may appear only as a future command after verification
    - `bun run scripts/check-phase138-parity-uat-release-boundary.ts` exits 0
  </acceptance_criteria>
  <done>Planning metadata agrees that v2.2 implementation and closeout evidence are in place, and the milestone remains unarchived.</done>
</task>

</tasks>

<verification>
```bash
bun test scripts/check-phase135-snapshot-recovery.test.ts scripts/check-phase138-parity-uat-release-boundary.test.ts scripts/check-phase134-authoritative-lifecycle.test.ts
bun run scripts/check-phase138-parity-uat-release-boundary.ts
bun run scripts/check-phase135-snapshot-recovery.ts
rg -n "^- \\[ \\] \\*\\*(PACK-|PPKG-04|MPDUR-|IBR-|MPOBS-|MPVFY-)" .planning/REQUIREMENTS.md
bash scripts/verify.sh
git diff --check
```
</verification>

<success_criteria>
- No leftover implemented-Pending v2.2 requirement rows remain.
- Required v2.2 surfaces are `done` without renaming the Phase 135 human title.
- Phase 135 and Phase 138 checkers require that closed state.
- ROADMAP, PROJECT, and STATE agree and do not archive v2.2.
</success_criteria>

<output>
After completion, create `.planning/phases/138-parity-adversarial-pressure-restart-and-release-guardrails/138-04-SUMMARY.md`
</output>
