---
phase: 04-chainstate-and-utxo-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - packages/open-bitcoin-node/Cargo.toml
  - packages/open-bitcoin-node/BUILD.bazel
  - packages/open-bitcoin-node/src/chainstate.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-chainstate/tests/parity.rs
  - docs/parity/catalog/chainstate.md
  - docs/parity/index.json
autonomous: true
requirements:
  - CHAIN-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-04-12T23-38-43
generated_at: 2026-04-12T23:38:43.326Z
must_haves:
  truths:
    - Storage remains outside the pure chainstate core through an explicit
      adapter boundary.
    - Targeted parity fixtures cover connect, disconnect, and reorg outcomes
      against the modeled Knots semantics for the Phase 4 surface.
    - The parity ledger can honestly move the chainstate surface to done after
      verification passes.
  artifacts:
    - packages/open-bitcoin-node/src/chainstate.rs
    - packages/open-bitcoin-chainstate/tests/parity.rs
    - docs/parity/catalog/chainstate.md
    - docs/parity/index.json
  key_links:
    - The node adapter persists and restores chainstate snapshots without
      leaking I/O into the pure core.
---

<objective>
Add storage adapters and chainstate parity fixtures.

Purpose: close the phase with an adapter-owned persistence boundary and the
auditable parity fixtures/docs needed to mark chainstate complete.
Output: a node-side snapshot store abstraction with an in-memory adapter,
targeted chainstate parity tests, and updated parity docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chainstate-and-utxo-engine/04-CONTEXT.md
@.planning/phases/04-chainstate-and-utxo-engine/04-RESEARCH.md
@.planning/phases/04-chainstate-and-utxo-engine/04-02-PLAN.md
@docs/parity/index.json
@docs/parity/catalog/consensus-validation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a node-side chainstate snapshot store</name>
  <files>packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/BUILD.bazel, packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-node/src/lib.rs</files>
  <read_first>packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-chainstate/src/lib.rs</read_first>
  <action>Add a `ChainstateStore` trait in `open-bitcoin-node` that loads and saves pure-core `ChainstateSnapshot` values, plus a repo-owned in-memory implementation and a thin orchestrator wrapper that keeps persistence outside `open-bitcoin-chainstate`.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node`</verify>
  <acceptance_criteria>
    - `open-bitcoin-node` depends on `open-bitcoin-chainstate`
    - Snapshot persistence lives in the shell crate, not the pure-core crate
  </acceptance_criteria>
  <done>The runtime shell owns the first adapter boundary for chainstate snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted chainstate parity fixtures</name>
  <files>packages/open-bitcoin-chainstate/tests/parity.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/lib.rs, packages/bitcoin-knots/src/coins.cpp, packages/bitcoin-knots/src/validation.cpp</read_first>
  <action>Create repo-owned parity tests that encode the Phase 4 surface: connect updates tip and spendable outputs, disconnect restores prior coins, reorg selects the heavier branch, and deterministic tie-breaking stays stable. Use focused Rust fixtures rather than a full node harness.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --test parity`</verify>
  <acceptance_criteria>
    - `tests/parity.rs` exists and exercises the documented Phase 4 truths
  </acceptance_criteria>
  <done>The phase has dedicated parity fixtures instead of relying only on internal unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update parity documentation for the chainstate surface</name>
  <files>docs/parity/catalog/chainstate.md, docs/parity/index.json</files>
  <read_first>docs/parity/catalog/consensus-validation.md, docs/parity/index.json</read_first>
  <action>Add a chainstate catalog entry that points at the first-party Rust implementation and the specific Knots sources this phase mirrored, then update `docs/parity/index.json` so the `chainstate` surface can move to `done` once the phase verification is green.</action>
  <verify>`rg -n '\"chainstate\"' docs/parity/index.json && test -f docs/parity/catalog/chainstate.md`</verify>
  <acceptance_criteria>
    - Parity docs describe the implemented chainstate slice and remaining out-of-scope areas
    - `docs/parity/index.json` reflects the completed chainstate surface
  </acceptance_criteria>
  <done>Parity status is auditable from the repo docs instead of implicit in code only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --test parity`
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node`
- [ ] `bash scripts/verify.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Storage concerns stay outside the pure chainstate core
- Chainstate parity status is documented and auditable

  </success_criteria>

<output>
After completion, create `.planning/phases/04-chainstate-and-utxo-engine/04-03-SUMMARY.md`
</output>
