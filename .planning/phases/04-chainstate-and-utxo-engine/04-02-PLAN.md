---
phase: 04-chainstate-and-utxo-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/lib.rs
  - packages/open-bitcoin-chainstate/src/types.rs
autonomous: true
requirements:
  - CHAIN-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-04-12T23-38-43
generated_at: 2026-04-12T23:38:43.326Z
must_haves:
  truths:
    - Connecting a block updates the active tip and UTXO set using the existing
      consensus validators and typed spend contexts.
    - Disconnecting a block restores the prior UTXO state and tip metadata.
    - Reorg helpers prefer the heavier candidate chain and can apply explicit
      disconnect/connect paths deterministically.
  artifacts:
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/lib.rs
  key_links:
    - Chainstate derives transaction and block validation contexts from stored
      UTXO metadata before mutating state.
---

<objective>
Implement connect, disconnect, and reorg behavior.

Purpose: turn the typed chainstate model into a working pure-core state engine
that future mempool, networking, and wallet code can trust.
Output: chainstate engine APIs for direct block connect, tip disconnect, best
tip preference, and explicit reorg application with deterministic tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chainstate-and-utxo-engine/04-CONTEXT.md
@.planning/phases/04-chainstate-and-utxo-engine/04-RESEARCH.md
@.planning/phases/04-chainstate-and-utxo-engine/04-01-PLAN.md
@packages/open-bitcoin-consensus/src/block.rs
@packages/open-bitcoin-consensus/src/context.rs
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/coins.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive contextual validation inputs from chainstate</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/lib.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-chainstate/src/types.rs</read_first>
  <action>Implement helpers that turn active-chain metadata and current UTXO entries into `BlockValidationContext` and `TransactionValidationContext` values. Include previous median time past calculation over the active chain and fail fast when a referenced outpoint is missing or already spent.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate derives_contexts_from_chainstate_metadata`</verify>
  <acceptance_criteria>
    - Context derivation uses explicit UTXO metadata instead of hidden global access
    - Missing prevouts surface as typed chainstate errors
  </acceptance_criteria>
  <done>Chainstate can construct consensus-ready contexts from its own pure data.</done>
</task>

<task type="auto">
  <name>Task 2: Apply block connect and disconnect mutations with undo data</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/lib.rs</files>
  <read_first>packages/bitcoin-knots/src/validation.cpp, packages/bitcoin-knots/src/coins.cpp</read_first>
  <action>Implement block connect by validating the block, recording undo data for every spent input, spending inputs before adding outputs, and updating the active tip. Implement disconnect by removing created outputs in reverse transaction order, replaying undo entries in reverse input order, and restoring the previous tip hash and height.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate connect_and_disconnect_round_trip_utxos_and_tip`</verify>
  <acceptance_criteria>
    - Connect stores undo data for later rollback
    - Disconnect restores both UTXO contents and active-tip metadata
  </acceptance_criteria>
  <done>Direct extension and rollback work without adapter-owned state.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic reorg and best-tip tests</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/lib.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs</read_first>
  <action>Add a best-tip preference helper based on cumulative work, then height, then block hash, plus a reorg API that applies an explicit disconnect path and replacement branch. Cover coinbase maturity rejection, heavier-branch takeover, and deterministic tie-breaking in focused tests using easy-difficulty blocks.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate reorg_prefers_heavier_branch_and_preserves_expected_utxos`</verify>
  <acceptance_criteria>
    - The engine exposes a deterministic best-tip preference helper
    - Reorg tests assert final tip and final UTXO contents, not just intermediate steps
  </acceptance_criteria>
  <done>The pure-core engine can express the targeted Phase 4 reorg scenarios.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Connect, disconnect, and reorg behavior are exercised by deterministic tests

  </success_criteria>

<output>
After completion, create `.planning/phases/04-chainstate-and-utxo-engine/04-02-SUMMARY.md`
</output>
