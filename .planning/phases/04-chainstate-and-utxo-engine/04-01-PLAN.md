---
phase: 04-chainstate-and-utxo-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/BUILD.bazel
  - BUILD.bazel
  - packages/README.md
  - scripts/pure-core-crates.txt
  - packages/open-bitcoin-core/Cargo.toml
  - packages/open-bitcoin-core/BUILD.bazel
  - packages/open-bitcoin-core/src/lib.rs
  - packages/open-bitcoin-chainstate/Cargo.toml
  - packages/open-bitcoin-chainstate/BUILD.bazel
  - packages/open-bitcoin-chainstate/src/error.rs
  - packages/open-bitcoin-chainstate/src/lib.rs
  - packages/open-bitcoin-chainstate/src/types.rs
autonomous: true
requirements:
  - CHAIN-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-04-12T23-38-43
generated_at: 2026-04-12T23:38:43.326Z
must_haves:
  truths:
    - The workspace exposes a pure-core chainstate crate instead of burying
      chainstate state in the runtime shell.
    - UTXO entries, undo records, and chain-position metadata are represented
      as explicit typed Rust state.
  artifacts:
    - packages/open-bitcoin-chainstate/Cargo.toml
    - packages/open-bitcoin-chainstate/src/lib.rs
    - packages/open-bitcoin-chainstate/src/types.rs
  key_links:
    - open-bitcoin-core re-exports open-bitcoin-chainstate for downstream
      consumers.
---

<objective>
Define the chainstate and UTXO data model in the pure core.

Purpose: establish the crate and state boundaries that later connect,
disconnect, and adapter work will rely on.
Output: a new pure-core `open-bitcoin-chainstate` crate with typed UTXO,
undo, snapshot, and chain-position models wired into Cargo, Bazel, and
`open-bitcoin-core`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chainstate-and-utxo-engine/04-CONTEXT.md
@.planning/phases/04-chainstate-and-utxo-engine/04-RESEARCH.md
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-primitives/src/block.rs
@packages/open-bitcoin-primitives/src/transaction.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the new pure-core chainstate crate into the workspace</name>
  <files>packages/Cargo.toml, packages/BUILD.bazel, BUILD.bazel, packages/README.md, scripts/pure-core-crates.txt, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-core/src/lib.rs, packages/open-bitcoin-chainstate/Cargo.toml, packages/open-bitcoin-chainstate/BUILD.bazel, packages/open-bitcoin-chainstate/src/lib.rs</files>
  <read_first>packages/Cargo.toml, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-consensus/Cargo.toml, packages/open-bitcoin-consensus/BUILD.bazel</read_first>
  <action>Create `packages/open-bitcoin-chainstate` as a new pure-core crate, add it to the Cargo workspace and Bazel aliases, list it in `scripts/pure-core-crates.txt`, describe it in `packages/README.md`, and re-export it from `open-bitcoin-core`. Keep dependencies one-way from chainstate into first-party pure-core crates only.</action>
  <verify>`cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate`</verify>
  <acceptance_criteria>
    - `packages/Cargo.toml` includes `open-bitcoin-chainstate`
    - `BUILD.bazel` exposes a public `chainstate` alias
    - `open-bitcoin-core` re-exports `open_bitcoin_chainstate`
  </acceptance_criteria>
  <done>The workspace builds the new crate and downstream crates can import it.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed chainstate, UTXO, and undo models</name>
  <files>packages/open-bitcoin-chainstate/src/error.rs, packages/open-bitcoin-chainstate/src/lib.rs, packages/open-bitcoin-chainstate/src/types.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-primitives/src/block.rs, packages/open-bitcoin-primitives/src/transaction.rs, packages/bitcoin-knots/src/coins.h</read_first>
  <action>Implement the first pure-core chainstate types: coin or UTXO entry metadata, transaction-undo and block-undo records, chain-position metadata for tip selection, and a serializable snapshot shape. Use explicit names and narrow constructors so later connect/disconnect code does not rely on tuples, untyped maps, or runtime-specific state.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib`</verify>
  <acceptance_criteria>
    - `types.rs` defines explicit coin, undo, snapshot, and chain-position structs
    - `error.rs` defines typed phase-appropriate chainstate errors
  </acceptance_criteria>
  <done>The crate exposes the minimal pure-core state model needed for block mutation work.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for the data model invariants</name>
  <files>packages/open-bitcoin-chainstate/src/lib.rs, packages/open-bitcoin-chainstate/src/types.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/types.rs</read_first>
  <action>Add focused unit tests that exercise the new types: null or genesis handling where relevant, snapshot round-trips, and deterministic tip ordering inputs. Keep each test on one concern and structure them as Arrange, Act, Assert.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib`</verify>
  <acceptance_criteria>
    - New tests cover the typed model instead of leaving the crate as wiring-only
  </acceptance_criteria>
  <done>The crate has meaningful unit tests before behavior-heavy logic lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib`
- [ ] `cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The pure-core chainstate crate exists and is reusable from `open-bitcoin-core`

  </success_criteria>

<output>
After completion, create `.planning/phases/04-chainstate-and-utxo-engine/04-01-SUMMARY.md`
</output>
