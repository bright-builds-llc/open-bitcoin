---
phase: 49-threat-model-and-release-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - docs/parity/checklist.md
  - docs/parity/index.json
  - docs/parity/README.md
  - docs/parity/deviations-and-unknowns.md
  - scripts/check-v1.3-release-boundaries.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - PROOF-06
  - SEC-01
  - SEC-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 49-2026-05-27T21-24-44
generated_at: 2026-05-27T21:53:40Z
must_haves:
  truths:
    - "Reviewer can reach the v1.3 threat model and Phase 50 acceptance contract from docs/parity/index.json, docs/parity/checklist.md, and docs/parity/README.md."
    - "A deterministic Bun assertion proves PROOF-06, SEC-01, and SEC-02 remain linked through the parity root without public-network access."
    - "Default repo verification still runs bash scripts/verify.sh without invoking live-mainnet smoke or other public-network checks."
  artifacts:
    - path: "docs/parity/index.json"
      provides: "Machine-readable parity root link to v1.3 threat model and release boundary"
      contains: "v1-3-threat-model-release-boundaries"
    - path: "docs/parity/checklist.md"
      provides: "Human-readable checklist row for Phase 49 requirements"
      contains: "v1-3-threat-model-release-boundaries"
    - path: "scripts/check-v1.3-release-boundaries.ts"
      provides: "Deterministic docs/root assertion for Phase 49 traceability"
      contains: "PROOF-06"
    - path: "scripts/verify.sh"
      provides: "Repo-native verification wiring for deterministic Phase 49 assertion"
      contains: "check-v1.3-release-boundaries.ts"
  key_links:
    - from: "docs/parity/index.json"
      to: "docs/parity/threat-model-v1.3.md"
      via: "checklist surface evidence or audit link"
      pattern: "threat-model-v1.3.md"
    - from: "scripts/verify.sh"
      to: "scripts/check-v1.3-release-boundaries.ts"
      via: "bun run deterministic assertion"
      pattern: "check-v1.3-release-boundaries.ts"
---

<objective>
Wire the new v1.3 threat model and release boundary into parity roots and deterministic verification.

Purpose: Phase 49 must be discoverable from the existing parity review path and must include a scriptable assertion because `docs/parity/index.json` changes.
Output: Updated parity root/checklist/README/deviation docs plus `scripts/check-v1.3-release-boundaries.ts` wired into `bash scripts/verify.sh`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-threat-model-and-release-boundaries/49-CONTEXT.md
@.planning/phases/49-threat-model-and-release-boundaries/49-RESEARCH.md
@.planning/phases/49-threat-model-and-release-boundaries/49-01-SUMMARY.md
@docs/parity/threat-model-v1.3.md
@docs/parity/release-readiness.md
@docs/parity/checklist.md
@docs/parity/index.json
@docs/parity/README.md
@docs/parity/deviations-and-unknowns.md
@scripts/verify.sh
@scripts/check-benchmark-report.ts
@scripts/check-parity-breadcrumbs.ts

Material repo rules read during planning: `AGENTS.md`, `AGENTS.bright-builds.md`, `standards-overrides.md`, Bright Builds `standards/index.md`, `core/architecture.md`, `core/code-shape.md`, `core/verification.md`, `core/testing.md`, and `languages/typescript-javascript.md` at pinned commit `05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Existing `docs/parity/index.json` root shape:

```json
{
  "baseline": "29.3.knots20260210",
  "deviations": [],
  "catalog": {},
  "checklist": {
    "path": "checklist.md",
    "status_taxonomy": ["planned", "in_progress", "done", "deferred", "out_of_scope"],
    "surfaces": [
      {
        "id": "security-analysis-audit",
        "title": "Security Analysis Audit",
        "status": "done",
        "requirements": [],
        "evidence": [],
        "rationale": "",
        "known_gaps": [],
        "suspected_unknowns": []
      }
    ]
  },
  "audit": {
    "release_readiness": {
      "path": "release-readiness.md",
      "status": "done",
      "evidence": []
    }
  }
}
```

New checklist surface id to use everywhere: `v1-3-threat-model-release-boundaries`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link Phase 49 from parity roots and deferred-surface docs</name>
  <files>docs/parity/checklist.md, docs/parity/index.json, docs/parity/README.md, docs/parity/deviations-and-unknowns.md</files>
  <read_first>
    - docs/parity/threat-model-v1.3.md
    - docs/parity/release-readiness.md
    - docs/parity/checklist.md
    - docs/parity/index.json
    - docs/parity/README.md
    - docs/parity/deviations-and-unknowns.md
    - .planning/phases/49-threat-model-and-release-boundaries/49-CONTEXT.md
    - .planning/phases/49-threat-model-and-release-boundaries/49-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Update the existing parity entrypoints per D-05, D-10, and D-11 without creating a separate release-claims schema or support-bundle manifest. In `docs/parity/checklist.md`, add one `Surface Status` table row with surface id ``v1-3-threat-model-release-boundaries``, status ``done``, requirements ``PROOF-06`, `SEC-01`, `SEC-02``, evidence links to `[v1.3 threat model](threat-model-v1.3.md)`, `[release readiness](release-readiness.md)`, `[operator runtime guide](../operator/runtime-guide.md)`, and `[Phase 48 support summary](../../.planning/phases/48-support-evidence-and-operator-runbooks/48-SUMMARY.md)`, known gaps stating `Phase 50 still owns live public-mainnet progress or diagnosed-blocker closeout`, and suspected unknowns stating `Future production-node, inbound-serving, relay, production-funds wallet, migration apply, packaging, hosted/public dashboard, and GUI claims need separate gates`. In `docs/parity/index.json`, add a checklist surface object with id `v1-3-threat-model-release-boundaries`, title `v1.3 Threat Model And Release Boundaries`, status `done`, requirements `["PROOF-06", "SEC-01", "SEC-02"]`, evidence paths `["docs/parity/threat-model-v1.3.md", "docs/parity/release-readiness.md", "docs/operator/runtime-guide.md", ".planning/phases/48-support-evidence-and-operator-runbooks/48-SUMMARY.md"]`, the same known gap and suspected unknown strings, and a rationale explaining that Phase 49 makes the v1.3 threat model, acceptance criteria, and shipped claim boundary auditable from parity roots. Also add or update `audit` links for `v1_3_threat_model` and `v1_3_release_boundaries` with `path`, `status: "done"`, `requirements`, and `evidence` fields; do not add a machine-readable release-claims matrix. In `docs/parity/README.md`, add `threat-model-v1.3.md` to the `## Files` list as the v1.3 scoped threat model and release boundary companion. In `docs/parity/deviations-and-unknowns.md`, add a `v1.3 Threat Model And Release Boundaries` paragraph or subsection that repeats the explicit non-claims from D-04 and D-06: inbound serving, transaction relay, production-funds wallet use, migration apply mode, packaging, hosted/public dashboard, GUI work, and unattended production-node readiness. Keep the support bundle language local/redacted only per D-12.</action>
  <verify>
    <automated>jq empty docs/parity/index.json</automated>
    <automated>rg -n "v1-3-threat-model-release-boundaries|threat-model-v1\\.3\\.md|PROOF-06|SEC-01|SEC-02" docs/parity/checklist.md docs/parity/index.json docs/parity/README.md docs/parity/deviations-and-unknowns.md</automated>
    <automated>rg -n "inbound serving|transaction relay|production-funds|migration apply mode|packaging|hosted/public dashboard|GUI|unattended production-node" docs/parity/deviations-and-unknowns.md</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.checklist.surfaces[] | select(.id == "v1-3-threat-model-release-boundaries")' docs/parity/index.json` returns one object.
    - `docs/parity/checklist.md` contains ``v1-3-threat-model-release-boundaries``.
    - `docs/parity/README.md` contains `threat-model-v1.3.md`.
    - `docs/parity/deviations-and-unknowns.md` contains `v1.3 Threat Model And Release Boundaries`.
    - `docs/parity/index.json`, `docs/parity/checklist.md`, and `docs/parity/deviations-and-unknowns.md` contain `PROOF-06`, `SEC-01`, and `SEC-02`.
  </acceptance_criteria>
  <done>Reviewers can start at `docs/parity/index.json`, `docs/parity/checklist.md`, or `docs/parity/README.md` and reach the Phase 49 threat model, release boundary, acceptance contract, and deferred-claim language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic release-boundary assertion to verification</name>
  <files>scripts/check-v1.3-release-boundaries.ts, scripts/verify.sh</files>
  <read_first>
    - scripts/verify.sh
    - scripts/check-benchmark-report.ts
    - scripts/check-parity-breadcrumbs.ts
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/release-readiness.md
    - docs/parity/threat-model-v1.3.md
    - docs/parity/deviations-and-unknowns.md
    - docs/parity/README.md
    - .planning/phases/49-threat-model-and-release-boundaries/49-CONTEXT.md
    - .planning/phases/49-threat-model-and-release-boundaries/49-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Running `bun run scripts/check-v1.3-release-boundaries.ts` succeeds when parity roots contain `PROOF-06`, `SEC-01`, `SEC-02`, `threat-model-v1.3.md`, and `v1-3-threat-model-release-boundaries`.
    - Test 2: The script fails with a nonzero exit when `docs/parity/index.json` is missing the `v1-3-threat-model-release-boundaries` checklist surface.
    - Test 3: The script fails with a nonzero exit when `scripts/verify.sh` contains `run-live-mainnet-smoke`, preserving D-09.
  </behavior>
  <action>Create `scripts/check-v1.3-release-boundaries.ts` using the repo's Bun TypeScript script style: `#!/usr/bin/env bun`, Node standard-library imports only, early returns or focused helper functions, no new package dependencies, and no Python. The script must parse `docs/parity/index.json`, read `docs/parity/checklist.md`, `docs/parity/release-readiness.md`, `docs/parity/threat-model-v1.3.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/README.md`, and `scripts/verify.sh`. It must throw explicit `Error` messages when checks fail. Implement helper functions for `readText`, `requireContains`, and `requireArrayIncludes`; use `maybe...` names for nullable lookups. Required assertions: `checklist.surfaces` contains one surface with id `v1-3-threat-model-release-boundaries`; that surface has status `done`; its `requirements` array includes `PROOF-06`, `SEC-01`, and `SEC-02`; its `evidence` array includes `docs/parity/threat-model-v1.3.md` and `docs/parity/release-readiness.md`; serialized `index.audit` contains `v1_3_threat_model`, `v1_3_release_boundaries`, `threat-model-v1.3.md`, `release-readiness.md`, `PROOF-06`, `SEC-01`, and `SEC-02`; `docs/parity/checklist.md` contains `v1-3-threat-model-release-boundaries`; `docs/parity/README.md` contains `threat-model-v1.3.md`; `docs/parity/release-readiness.md` contains `v1.3 Release Claim Boundary Matrix`, `Phase 50 Evidence Acceptance Contract`, `bash scripts/verify.sh`, `bun run scripts/run-live-mainnet-smoke.ts`, `support-evidence.json`, and `support-evidence.md`; `docs/parity/threat-model-v1.3.md` contains `STRIDE Threat Register`, `V13-TM-01`, `V13-TM-02`, `V13-TM-03`, `V13-TM-04`, `V13-TM-05`, `V13-TM-06`, `public peer input`, `resource exhaustion`, `storage corruption`, `operator RPC controls`, `log/report redaction`, and `live evidence handling`; `docs/parity/deviations-and-unknowns.md` contains `inbound serving`, `transaction relay`, `production-funds`, `migration apply mode`, `packaging`, `hosted/public dashboard`, `GUI`, and `unattended production-node`; and `scripts/verify.sh` does not contain `run-live-mainnet-smoke`. Wire `bun run scripts/check-v1.3-release-boundaries.ts` into `scripts/verify.sh` immediately after `bun run scripts/check-parity-breadcrumbs.ts --check`. Do not add any public-network command to `scripts/verify.sh`.</action>
  <verify>
    <automated>bun run scripts/check-v1.3-release-boundaries.ts</automated>
    <automated>rg -n "check-v1\\.3-release-boundaries\\.ts" scripts/verify.sh</automated>
    <automated>if rg -n "run-live-mainnet-smoke" scripts/verify.sh; then exit 1; fi</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-v1.3-release-boundaries.ts` exists and starts with `#!/usr/bin/env bun`.
    - `bun run scripts/check-v1.3-release-boundaries.ts` exits 0.
    - `scripts/verify.sh` contains `bun run scripts/check-v1.3-release-boundaries.ts`.
    - `rg -n "run-live-mainnet-smoke" scripts/verify.sh` returns no matches.
    - `bash scripts/verify.sh` passes.
  </acceptance_criteria>
  <done>The parity-root traceability assertion is deterministic, part of repo-native verification, and proves Phase 49 links without adding public-network checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown parity docs -> JSON parity root | Human-maintained docs are mirrored into machine-readable checklist and audit paths. |
| Verification script -> local filesystem | A Bun script reads docs and shell scripts to enforce release-boundary invariants. |
| Default verification -> public network | Repo-native verification must stay deterministic and public-network-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Tampering | `docs/parity/index.json` | mitigate | Add deterministic Bun assertions that required surface id, requirements, and evidence links are present and JSON parses cleanly. |
| T-49-02-02 | Repudiation | parity checklist and README discoverability | mitigate | Add the same `v1-3-threat-model-release-boundaries` id and `PROOF-06`/`SEC-01`/`SEC-02` traces to human and JSON roots. |
| T-49-02-03 | Denial of Service | `scripts/verify.sh` | mitigate | Keep the new check as local file parsing only; explicitly fail if `scripts/verify.sh` contains `run-live-mainnet-smoke`. |
| T-49-02-04 | Information Disclosure | docs examples and validator strings | mitigate | Validator checks for documentation links and known non-claim strings only; it must not read live reports, support bundles, cookies, wallets, or logs. |
</threat_model>

<verification>
Run after both tasks:

```bash
jq empty docs/parity/index.json
bun run scripts/check-v1.3-release-boundaries.ts
if rg -n "run-live-mainnet-smoke" scripts/verify.sh; then exit 1; fi
bash scripts/verify.sh
```
</verification>

<success_criteria>
- `docs/parity/index.json`, `docs/parity/checklist.md`, and `docs/parity/README.md` link to the v1.3 threat model and release-boundary docs.
- `docs/parity/deviations-and-unknowns.md` repeats the v1.3 non-claim boundaries.
- `scripts/check-v1.3-release-boundaries.ts` passes and is wired into `bash scripts/verify.sh`.
- `scripts/verify.sh` still contains no `run-live-mainnet-smoke` invocation.
- Full repo-native `bash scripts/verify.sh` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/49-threat-model-and-release-boundaries/49-02-SUMMARY.md`.
</output>
