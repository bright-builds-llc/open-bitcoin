---
phase: 91-peer-permissions-and-connection-classes
plan: 10
type: execute
wave: 8
depends_on: [91-09]
files_modified:
  - scripts/check-phase91-peer-permissions.test.ts
  - scripts/check-phase91-peer-permissions.ts
  - scripts/verify.sh
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04]
must_haves:
  truths:
    - "A deterministic checker validates Phase 91 evidence and no-claim boundaries."
    - "`scripts/verify.sh` runs the Phase 91 checker after Phase 90."
    - "Default verification remains local, deterministic, and public-network-free."
  artifacts:
    - path: "scripts/check-phase91-peer-permissions.ts"
      provides: "Fixed-file Phase 91 evidence and boundary checker"
    - path: "scripts/check-phase91-peer-permissions.test.ts"
      provides: "Checker fixture tests"
    - path: "scripts/verify.sh"
      provides: "Repo verifier wiring"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase91-peer-permissions.ts"
      via: "run_step"
      pattern: "check Phase 91 peer permissions"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Add deterministic verification for Phase 91 permission evidence and boundaries.

Purpose: Keep permission docs, parity roots, source breadcrumbs, status labels, UAT commands, and deferred-surface no-claim language from drifting.
Output: Bun checker, fixture tests, and verifier wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-09-SUMMARY.md
@scripts/check-phase90-inbound-listener-admission.ts
@scripts/check-phase90-inbound-listener-admission.test.ts
@scripts/verify.sh
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/source-breadcrumbs.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 91 checker fixture tests</name>
  <files>scripts/check-phase91-peer-permissions.test.ts</files>
  <read_first>
    - `scripts/check-phase90-inbound-listener-admission.test.ts`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-09-SUMMARY.md`
  </read_first>
  <behavior>
    - Positive fixtures pass when they include PERM-01 through PERM-04, the Phase 91 surface id, exact tokens, active/inactive labels, Cargo/Bazel UAT commands, and Knots anchors.
    - Negative fixtures fail when `all` is documented as active relay support, when whitelist/whitebind is accepted, when public inbound defaults are claimed, or when support evidence includes raw config/peer details.
  </behavior>
  <action>
    Create `scripts/check-phase91-peer-permissions.test.ts` following the Phase 90 checker test style with Arrange/Act/Assert comments. Use `OPEN_BITCOIN_PHASE91_REPO_ROOT` fixture override. Cover missing requirements, missing UAT command units, missing source breadcrumb mappings, missing inactive effect labels, forbidden whitelist compatibility claims, and forbidden relay/production/public-default claims. This task enforces D-18 and D-19 through deterministic local checker fixtures.
  </action>
  <acceptance_criteria>
    - `rg -n "PERM-01|PERM-02|PERM-03|PERM-04|v1-9-peer-permissions-connection-classes|openbitcoininboundpermissionclass|inactive_relay|whitebind|public inbound by default|Arrange|Act|Assert" scripts/check-phase91-peer-permissions.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase91-peer-permissions.test.ts</automated>
  </verify>
  <done>Checker fixtures prove required evidence and fail on Phase 91 overclaims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fixed-file Phase 91 checker</name>
  <files>scripts/check-phase91-peer-permissions.ts, scripts/check-phase91-peer-permissions.test.ts</files>
  <read_first>
    - `scripts/check-phase90-inbound-listener-admission.ts`
    - `scripts/check-phase91-peer-permissions.test.ts`
    - `docs/operator/runtime-guide.md`
    - `docs/parity/catalog/p2p.md`
    - `docs/parity/index.json`
    - `docs/parity/checklist.md`
    - `docs/parity/source-breadcrumbs.json`
  </read_first>
  <behavior>
    - The real repo checker validates fixed files only and does not scan `.planning/` archives.
    - It validates the Phase 91 parity surface, requirements, Knots anchors, UAT commands, source breadcrumb paths, status/support labels, inactive effects, and forbidden overclaims.
  </behavior>
  <action>
    Implement `checkPhase91PeerPermissions()` using the Phase 90 checker pattern. Validate `docs/operator/runtime-guide.md`, architecture docs, parity docs, `docs/parity/source-breadcrumbs.json`, and `scripts/verify.sh`. Required labels include `permission_class`, `active_permission_effects`, `inactive_permission_effects`, `latest_permission_decision`, `inactive_relay`, `inactive_forcerelay`, `inactive_mempool`, `inactive_bloomfilter`, and `inactive_blockfilters`. Forbidden unscoped phrases include transaction relay support, compact block relay support, mempool propagation support, BIP37 serving, compact filter serving, full address relay, public inbound by default, and production full-node readiness. This task enforces D-01 through D-20 by checking vocabulary, config boundary, status/support labels, inactive effects, UAT commands, and no-claim language.
  </action>
  <acceptance_criteria>
    - `rg -n "checkPhase91PeerPermissions|REQUIRED_UAT_COMMANDS|REQUIRED_EVIDENCE_LABELS|FORBIDDEN|inactive_relay|inactive_blockfilters" scripts/check-phase91-peer-permissions.ts` passes.
    - `bun run scripts/check-phase91-peer-permissions.ts` exits successfully on the real repo.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase91-peer-permissions.test.ts</automated>
    <automated>bun run scripts/check-phase91-peer-permissions.ts</automated>
  </verify>
  <done>Checker validates Phase 91 evidence and no-claim boundaries deterministically against fixed local files.</done>
</task>

<task type="auto">
  <name>Task 3: Wire checker into repo verification and run final checks</name>
  <files>scripts/verify.sh</files>
  <read_first>
    - `scripts/verify.sh`
    - `scripts/check-phase91-peer-permissions.ts`
    - `scripts/check-phase91-peer-permissions.test.ts`
  </read_first>
  <action>
    Add visible command-order entries and executable `run_step` calls for `bun test scripts/check-phase91-peer-permissions.test.ts` and `bun run scripts/check-phase91-peer-permissions.ts` immediately after the Phase 90 inbound listener admission checker. Preserve deterministic local verification; do not add public-network, service-manager, long-running, or listener-exposure checks. Run focused checker commands and attempt `bash scripts/verify.sh`. If `docs/metrics/lines-of-code.md` is stale, refresh it with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md` and rerun `bash scripts/verify.sh`. This task implements D-18 by keeping default verification deterministic, local, and public-network-free.
  </action>
  <acceptance_criteria>
    - `rg -n "test Phase 91 peer permissions checker|check Phase 91 peer permissions|check-phase91-peer-permissions" scripts/verify.sh` passes.
    - `! rg -n "public-network|service-manager|multi-day|whitebind|whitelist" scripts/verify.sh` proves Phase 91 did not add public-network, service-manager, long-running, or Knots whitelist execution to the default verifier.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase91-peer-permissions.test.ts</automated>
    <automated>bun run scripts/check-phase91-peer-permissions.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Repo verification runs the Phase 91 checker in deterministic local verification order and full repo verification passes or records only an external blocker with exact output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/parity evidence -> verification | Static artifacts can drift from implemented permission behavior. |
| verifier script -> default local checks | Checker wiring must not add public-network or service-manager requirements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-10-01 | Tampering | `scripts/verify.sh` order | mitigate | Checker validates executable `run_step` order after Phase 90. |
| T-91-10-02 | Repudiation | parity evidence | mitigate | Checker fails missing PERM-01 through PERM-04, surface id, anchors, and UAT command units. |
| T-91-10-03 | Elevation of Privilege | docs overclaims | mitigate | Checker fails unscoped relay, mempool, filter, public-default, and production-readiness claims. |
| T-91-10-04 | Denial of Service | default verifier | mitigate | Checker is fixed-file and local; no public network, service manager, or long-running probes. |
</threat_model>

<verification>
- `bun test scripts/check-phase91-peer-permissions.test.ts`
- `bun run scripts/check-phase91-peer-permissions.ts`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
PERM-01 through PERM-04 have deterministic guardrails in default repo verification, and Phase 91 cannot drift into deferred relay, filter, address-relay, public-default, or production-readiness claims unnoticed.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-10-SUMMARY.md`.
</output>
