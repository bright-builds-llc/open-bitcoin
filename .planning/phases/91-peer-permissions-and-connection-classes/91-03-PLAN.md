---
phase: 91-peer-permissions-and-connection-classes
plan: 03
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - packages/open-bitcoin-network/src/inbound.rs
  - packages/open-bitcoin-network/src/inbound/tests.rs
  - packages/open-bitcoin-node/src/network/inbound.rs
  - packages/open-bitcoin-node/src/network/tests.rs
autonomous: true
requirements: [PERM-02, PERM-04]
must_haves:
  truths:
    - "Admission records preserve connection class and permission-effect evidence."
    - "Protected inbound peers can consume reserved admission capacity; ordinary inbound peers cannot."
    - "Managed counters distinguish ordinary, permissioned, protected, active, and inactive permission outcomes."
  artifacts:
    - path: "packages/open-bitcoin-network/src/inbound.rs"
      provides: "Admission request/record fields for connection class and permission decision"
    - path: "packages/open-bitcoin-node/src/network/inbound.rs"
      provides: "Managed counters and admission recording for permission evidence"
    - path: "packages/open-bitcoin-node/src/network/tests.rs"
      provides: "No outbound starvation and reserved-capacity tests"
  key_links:
    - from: "InboundPermissionDecision"
      to: "InboundAdmissionRequest"
      via: "typed field"
      pattern: "permission_decision"
    - from: "PeerConnectionClass::ProtectedInbound"
      to: "InboundAdmissionSlotClass::Reserved"
      via: "slot_class mapping"
      pattern: "ProtectedInbound.*Reserved|slot_class"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Thread permission evidence through the pure admission and managed network records.

Purpose: Make permission effects observable in admission and later status/support paths without hiding state mutation inside the runtime accept loop.
Output: Typed admission request/record evidence plus managed counters for permissioned and protected inbound peers.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-03-SUMMARY.md
@packages/open-bitcoin-network/src/inbound.rs
@packages/open-bitcoin-network/src/inbound/tests.rs
@packages/open-bitcoin-node/src/network/inbound.rs
@packages/open-bitcoin-node/src/network/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend admission request and record with permission evidence</name>
  <files>packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/inbound/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/inbound/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md`
  </read_first>
  <behavior>
    - Ordinary admission defaults to `ordinary_inbound`, empty active effects, empty inactive effects, and `InboundAdmissionSlotClass::Ordinary`.
    - Permissioned admission records carry stable active and inactive effect labels.
    - Protected admission records use `InboundAdmissionSlotClass::Reserved`.
  </behavior>
  <action>
    Add `connection_class: PeerConnectionClass` and `permission_decision: InboundPermissionDecision` to `InboundAdmissionRequest` and `InboundPeerRecord`. Provide a constructor or helper for ordinary inbound admission so existing call sites can opt into the conservative default without raw strings. In `admit_request`, copy the typed permission evidence into `InboundPeerRecord`. Keep duplicate, self-connection, cap, reserved-slot, and shutdown rejection ordering unchanged. This task implements D-07, D-10, D-13, and D-19 by carrying pure permission evidence into admission records and testing positive bounded effects plus negative safeguards.
  </action>
  <acceptance_criteria>
    - `rg -n "connection_class|permission_decision|PeerConnectionClass|InboundPermissionDecision" packages/open-bitcoin-network/src/inbound.rs packages/open-bitcoin-network/src/inbound/tests.rs` finds fields and tests.
    - `rg -n "ordinary_inbound|permissioned_inbound|protected_inbound|Reserved|Ordinary" packages/open-bitcoin-network/src/inbound/tests.rs` finds admission class coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture</automated>
  </verify>
  <done>Pure admission records carry typed permission evidence while preserving Phase 90 rejection semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Count permissioned and protected admits in managed network evidence</name>
  <files>packages/open-bitcoin-node/src/network/inbound.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/network/inbound.rs`
    - `packages/open-bitcoin-node/src/network/tests.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
  </read_first>
  <behavior>
    - Protected inbound admission increments `reserved_inbound_admits` and `protected_inbound_admits`.
    - Permissioned non-protected inbound admission increments `permissioned_inbound_admits` but not `reserved_inbound_admits`.
    - Permissioned inbound admission does not change `target_outbound_peers`, outbound peer count, or outbound compatibility progress.
  </behavior>
  <action>
    Extend `ManagedInboundAdmissionInfo` with bounded numeric counters: `permissioned_inbound_admits`, `protected_inbound_admits`, `active_permission_effect_observations`, and `inactive_permission_effect_observations`. Update `record_admit` to inspect the `InboundPeerRecord` rather than only `InboundAdmissionSlotClass`. Preserve all existing Phase 90 counters. Add tests proving ordinary peers still cannot consume protected capacity and outbound counts remain unchanged after protected/permissioned inbound admits. This task implements D-08, D-09, D-17, and D-19 by proving protected-slot behavior, low-cardinality counters, and no outbound sync starvation.
  </action>
  <acceptance_criteria>
    - `rg -n "permissioned_inbound_admits|protected_inbound_admits|active_permission_effect_observations|inactive_permission_effect_observations" packages/open-bitcoin-node/src/network/inbound.rs packages/open-bitcoin-node/src/network/tests.rs` finds counters and tests.
    - `rg -n "target_outbound|outbound_peers|reserved_inbound_admits" packages/open-bitcoin-node/src/network/tests.rs` finds no-starvation coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Managed admission evidence distinguishes ordinary, permissioned, and protected inbound peers without affecting outbound sync accounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure permission decision -> admission policy | Typed class/effect evidence controls admission slot selection. |
| managed network -> status/support | Admission counters become operator-visible diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03-01 | Elevation of Privilege | `InboundAdmissionRequest.slot_class` | mitigate | Derive reserved slot use from typed `PeerConnectionClass`; tests prove ordinary peers cannot consume reserved capacity. |
| T-91-03-02 | Denial of Service | inbound permission counters | mitigate | Tests assert inbound permissioned/protected admits do not change outbound peer counts or sync targets. |
| T-91-03-03 | Information Disclosure | `ManagedInboundAdmissionInfo` | mitigate | Store only numeric counters and stable effect labels, not endpoints, peer ids, class names, or raw config strings. |
| T-91-03-04 | Repudiation | latest admission evidence | mitigate | Preserve stable rejection reasons and permission class labels for later status/support projection. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-02 and PERM-04 have an admission evidence path: protected peers can use reserved slots, ordinary peers cannot, permission effects are counted, and outbound sync is not starved or reclassified.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-03-SUMMARY.md`.
</output>
