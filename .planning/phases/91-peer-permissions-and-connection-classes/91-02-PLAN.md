---
phase: 91-peer-permissions-and-connection-classes
plan: 02
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
  - packages/open-bitcoin-rpc/src/config/loader.rs
  - packages/open-bitcoin-rpc/src/config/loader/inbound.rs
  - packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
autonomous: true
requirements: [PERM-01, PERM-03]
must_haves:
  truths:
    - "Operators define inbound permission classes only through Open Bitcoin JSONC or Open Bitcoin-prefixed daemon CLI flags."
    - "Malformed classes fail with deterministic field and token errors."
    - "Knots `whitelist` and `whitebind` remain rejected and do not imply compatibility."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/config/open_bitcoin.rs"
      provides: "JSONC DTOs for `inbound.permission_classes`"
    - path: "packages/open-bitcoin-rpc/src/config/loader/inbound.rs"
      provides: "Open Bitcoin-prefixed CLI permission-class parser"
    - path: "packages/open-bitcoin-rpc/src/config/tests.rs"
      provides: "Config, CLI, validation, and baseline-rejection coverage"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs"
      to: "open_bitcoin_network::PeerPermissionClassRegistry"
      via: "runtime config resolver"
      pattern: "PeerPermissionClassRegistry|ParsedPeerPermissionClass"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Wire Phase 91 permission classes into the Open Bitcoin-owned config boundary.

Purpose: Implement D-02 and D-03 without accepting Knots `-whitelist`, `-whitebind`, or aliases as full baseline compatibility.
Output: JSONC and CLI parsing that resolves into the pure network permission registry from Plan 01.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-RESEARCH.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md
@packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
@packages/open-bitcoin-rpc/src/config/loader.rs
@packages/open-bitcoin-rpc/src/config/loader/inbound.rs
@packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
@packages/open-bitcoin-rpc/src/config/tests.rs
@docs/architecture/config-precedence.md

<interfaces>
Use the Plan 01 network types directly; do not add an RPC-local permission model:
```rust
// Expected exports from Plan 01
pub struct PeerPermissionClassRegistry { /* opaque */ }
pub struct ParsedPeerPermissionClass { /* typed class */ }
pub struct PeerPermissionParseError { /* stable field/token error */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JSONC permission class config and resolver validation</name>
  <files>packages/open-bitcoin-rpc/src/config/open_bitcoin.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/config/open_bitcoin.rs`
    - `packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs`
    - `packages/open-bitcoin-rpc/src/config/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md`
  </read_first>
  <behavior>
    - JSONC accepts `inbound.permission_classes` entries shaped as `{ "name": "operator_loopback", "addresses": ["127.0.0.1"], "permissions": ["in", "noban", "forceinbound", "download", "addr"] }`.
    - JSONC rejects empty class names, empty address lists, CIDR/hostnames/socket endpoints, duplicate literal IP addresses across classes, unknown fields, unsupported tokens, direction-only tokens, and `out` combinations.
    - Resolved runtime config carries the Plan 01 `PeerPermissionClassRegistry` on `RuntimeConfig.inbound` or an immediately owned child field.
  </behavior>
  <action>
    Add `InboundPermissionClassConfig` to `open_bitcoin.rs` under `InboundConfig` with `name`, `addresses`, and `permissions` fields. Update `resolve_inbound_listener_config` so it maps raw JSONC DTOs through the Plan 01 parser into the network registry and maps parser failures to `ConfigError` messages prefixed with `Error resolving Open Bitcoin inbound config:`. The error must name the exact field path and offending token/value, for example `inbound.permission_classes[0].permissions token "oopsie"`. Preserve `deny_unknown_fields` and disabled-by-default inbound listener behavior. This task implements D-02 and D-03 by keeping Open Bitcoin JSONC as the entry point and preserving explicit field/token validation.
  </action>
  <acceptance_criteria>
    - `rg -n "permission_classes|InboundPermissionClassConfig|PeerPermissionClassRegistry" packages/open-bitcoin-rpc/src/config/open_bitcoin.rs packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs packages/open-bitcoin-rpc/src/config/tests.rs` finds JSONC and resolver wiring.
    - `rg -n "oopsie|direction-only|127.0.0.1/24|localhost|127.0.0.1:8333|duplicate" packages/open-bitcoin-rpc/src/config/tests.rs` finds stable negative coverage.
    - Existing tests for disabled inbound defaults still assert `runtime.inbound.enabled == false`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_config -- --nocapture</automated>
  </verify>
  <done>JSONC permission classes resolve into typed network config with deterministic field/token errors and no baseline whitelist compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Open Bitcoin-prefixed CLI permission class overrides</name>
  <files>packages/open-bitcoin-rpc/src/config/loader.rs, packages/open-bitcoin-rpc/src/config/loader/inbound.rs, packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs, packages/open-bitcoin-rpc/src/config/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/config/loader.rs`
    - `packages/open-bitcoin-rpc/src/config/loader/inbound.rs`
    - `packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs`
    - `packages/open-bitcoin-rpc/src/config/tests.rs`
  </read_first>
  <behavior>
    - Repeated `-openbitcoininboundpermissionclass=name@127.0.0.1=in,noban,forceinbound,download,addr` flags override JSONC `inbound.permission_classes` as a full list.
    - Missing values and malformed specs produce deterministic CLI errors naming `openbitcoininboundpermissionclass`.
    - `-whitelist`, `-whitebind`, `-whitelistrelay`, and `-whitelistforcerelay` remain invalid CLI parameters.
  </behavior>
  <action>
    Add `maybe_inbound_permission_class_specs: Option<Vec<String>>` to `CliSettings`. Extend `parse_inbound_cli_arg` with repeatable `openbitcoininboundpermissionclass`, requiring the exact format `<class_name>@<literal_ip>=<comma-separated tokens>`. Convert CLI specs to the same `InboundPermissionClassConfig` DTOs used by JSONC before resolving through the Plan 01 parser. Do not accept Knots spellings, aliases, or baseline `bitcoin.conf` keys. This task implements D-02, D-03, and D-18 by using Open Bitcoin-prefixed CLI controls with deterministic local tests only.
  </action>
  <acceptance_criteria>
    - `rg -n "openbitcoininboundpermissionclass|maybe_inbound_permission_class_specs" packages/open-bitcoin-rpc/src/config/loader.rs packages/open-bitcoin-rpc/src/config/loader/inbound.rs packages/open-bitcoin-rpc/src/config/tests.rs` finds parser and tests.
    - `rg -n "whitelistforcerelay|whitelistrelay|whitebind|whitelist" packages/open-bitcoin-rpc/src/config/tests.rs` finds rejection coverage.
    - `rg -n "name@127\\.0\\.0\\.1=in,noban,forceinbound,download,addr" packages/open-bitcoin-rpc/src/config/tests.rs` finds CLI success coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc daemon_inbound_permission -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Open Bitcoin CLI can define inbound permission classes through one explicit flag; baseline Knots permission flags remain rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONC/CLI -> runtime config | Operator-provided permission text enters typed config. |
| bitcoin.conf/Knots CLI -> Open Bitcoin config | Baseline-looking keys must not silently activate Open Bitcoin permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-01 | Elevation of Privilege | `resolve_inbound_listener_config` | mitigate | Parse into Plan 01 closed types and fail closed on unsupported tokens, aliases, missing `in`, and `out` combinations. |
| T-91-02-02 | Spoofing | class match addresses | mitigate | Accept literal IP addresses only and reject CIDR, hostnames, and socket endpoints with field-specific errors. |
| T-91-02-03 | Tampering | CLI overrides | mitigate | Treat CLI permission-class flags as a complete override list with deterministic ordering; reject malformed specs. |
| T-91-02-04 | Repudiation | validation errors | mitigate | Error messages name field and token/value so operators can prove why config was rejected. |
</threat_model>

<verification>
Run the focused RPC config suite and clippy:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc daemon_inbound_permission -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-01 and PERM-03 are satisfied at the config boundary: operators have explicit Open Bitcoin-owned permission-class inputs, invalid inputs are deterministic, and no Knots whitelist/whitebind compatibility is implied.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-02-SUMMARY.md`.
</output>
