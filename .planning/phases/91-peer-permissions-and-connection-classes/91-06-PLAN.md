---
phase: 91-peer-permissions-and-connection-classes
plan: 06
type: execute
wave: 6
depends_on: [91-05]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
autonomous: true
requirements: [PERM-04]
must_haves:
  truths:
    - "Operator status renders permission evidence from the shared status contract."
    - "Status output keeps inbound serving separate from outbound sync."
    - "Status output does not show raw config strings, user class names, peer ids, or secrets."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      provides: "Human status rendering for permission evidence"
    - path: "packages/open-bitcoin-cli/src/operator/status/tests.rs"
      provides: "JSON status collection and non-leak tests"
  key_links:
    - from: "OpenBitcoinStatusSnapshot.peers.inbound"
      to: "Inbound serving:"
      via: "status renderer"
      pattern: "active_permission_effects|inactive_permission_effects"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Render permission evidence in the operator status surface.

Purpose: Make PERM-04 visible to operators through the existing shared-status status path, not a renderer-local summary.
Output: Status renderer and tests for permission class/effect labels and redaction.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-07-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/status/render/tests.rs
@packages/open-bitcoin-cli/src/operator/status/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render permission fields in human status</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md`
  </read_first>
  <behavior>
    - Human `Inbound serving:` text includes `permission_class`, `permissioned_inbound_peers`, `protected_inbound_peers`, `active_permission_effects`, `inactive_permission_effects`, and `latest_permission_decision`.
    - The renderer uses shared status fields only and keeps `Peers: in=<n> out=<n>` unchanged.
  </behavior>
  <action>
    Extend `inbound_status_text` and focused renderer tests to include the Plan 05 permission fields. Use comma-separated stable labels for effect lists and `none` for empty lists. Do not render raw config class names, peer ids, endpoints beyond existing bounded endpoint display, or credential values. This task implements D-15, D-16, and D-17 by rendering shared low-cardinality status fields instead of renderer-local or raw config summaries.
  </action>
  <acceptance_criteria>
    - `rg -n "permission_class|permissioned_inbound_peers|protected_inbound_peers|active_permission_effects|inactive_permission_effects|latest_permission_decision" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs` finds renderer and test coverage.
    - `! rg -n "raw_config|class_name|peer_id|rpc_password|cookie" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs` proves the renderer does not leak raw config strings, peer ids, or credentials.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture</automated>
  </verify>
  <done>Human status exposes permission evidence from shared status and keeps peer counts separate from inbound diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve JSON status projection and redaction tests</name>
  <files>packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/status/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md`
  </read_first>
  <behavior>
    - Fake live RPC maps Plan 05 permission fields into `OpenBitcoinStatusSnapshot.peers.inbound`.
    - Missing `openbitcoinnetworkstatus` keeps permission evidence unavailable without failing peer counts.
    - JSON status contains no raw class names, raw permission config strings, peer ids, or RPC secrets.
  </behavior>
  <action>
    Update status collector fixtures for the extended `InboundPeerServingStatus`. Add assertions for available permission evidence, missing-method unavailable behavior, and absence of representative forbidden strings (`operator_loopback`, `in,noban`, `127.0.0.1 permission class`, `rpc_password`, `cookie`). Keep JSON rendering as serde projection of `OpenBitcoinStatusSnapshot`. This task implements D-15, D-16, D-18, and D-19 through deterministic local status tests covering shared evidence, redaction, and inactive relay-like effects.
  </action>
  <acceptance_criteria>
    - `rg -n "active_permission_effects|inactive_permission_effects|latest_permission_decision|operator_loopback|rpc_password|cookie" packages/open-bitcoin-cli/src/operator/status/tests.rs` finds positive and negative assertions.
    - `rg -n "OpenBitcoinStatusSnapshot" packages/open-bitcoin-cli/src/operator/status/tests.rs` confirms shared snapshot projection remains the JSON source.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>JSON and human status surfaces expose safe permission evidence and preserve unavailable behavior for older daemons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared status -> terminal output | Status data becomes human-visible operator text. |
| RPC status -> JSON status | Live daemon extension response becomes local JSON output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06-01 | Information Disclosure | status renderer | mitigate | Render only shared machine labels and effect labels; tests assert raw config/user/secret strings are absent. |
| T-91-06-02 | Repudiation | older-daemon fallback | mitigate | Preserve explicit unavailable reason when permission status cannot be collected. |
| T-91-06-03 | Denial of Service | status collection | accept | This plan only renders bounded fields from existing RPC status; no new network calls beyond Plan 05 extension. |
| T-91-06-04 | Tampering | peer count display | mitigate | Renderer tests keep baseline `Peers: in/out` separate from permission diagnostics. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_status -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-04 is operator-visible in `open-bitcoin status` through shared status fields, with safe labels and unavailable behavior preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-06-SUMMARY.md`.
</output>
