---
phase: 91-peer-permissions-and-connection-classes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/inbound.rs
  - packages/open-bitcoin-network/src/inbound/permissions.rs
  - packages/open-bitcoin-network/src/inbound/tests.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03]
must_haves:
  truths:
    - "Knots-anchored permission tokens parse into typed Open Bitcoin domain values."
    - "`all` exposes active bounded effects and inactive relay/filter/mempool effects separately."
    - "Connection class labels are stable machine labels, not raw config or user labels."
  artifacts:
    - path: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      provides: "Peer permission tokens, parsed sets, effects, class registry, and labels"
    - path: "packages/open-bitcoin-network/src/inbound/tests.rs"
      provides: "Deterministic parser, `all`, class, and inactive-effect tests"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Breadcrumb registration for the new network permission source file"
  key_links:
    - from: "packages/open-bitcoin-network/src/inbound.rs"
      to: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      via: "child module export"
      pattern: "mod permissions|pub use permissions::"
    - from: "packages/open-bitcoin-network/src/inbound/permissions.rs"
      to: "packages/bitcoin-knots/src/net_permissions.cpp"
      via: "top-of-file parity breadcrumb and parser vocabulary"
      pattern: "net_permissions"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Create the pure peer-permission and connection-class domain model for Phase 91.

Purpose: Give later config, admission, status, and support plans typed values so unsupported or deferred permission tokens cannot become active behavior by accident.
Output: A new pure `open-bitcoin-network` permission module, exports, tests, and parity breadcrumb registration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-RESEARCH.md
@packages/open-bitcoin-network/src/inbound.rs
@packages/open-bitcoin-network/src/inbound/tests.rs
@packages/open-bitcoin-network/src/lib.rs
@docs/parity/source-breadcrumbs.json
@packages/bitcoin-knots/src/net_permissions.h
@packages/bitcoin-knots/src/net_permissions.cpp
@packages/bitcoin-knots/test/functional/p2p_permissions.py

<interfaces>
Existing Phase 90 admission seam:
```rust
pub enum InboundAdmissionSlotClass {
    Ordinary,
    Reserved,
}

pub struct InboundAdmissionRequest {
    pub peer_id: PeerId,
    pub remote_endpoint: String,
    pub slot_class: InboundAdmissionSlotClass,
    pub counters: InboundAdmissionCounters,
    pub existing_endpoint_keys: BTreeSet<String>,
    pub existing_peer_ids: BTreeSet<PeerId>,
    pub local_nonce: u64,
    pub maybe_remote_nonce: Option<u64>,
    pub is_shutdown_requested: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed permission vocabulary and effect parsing</name>
  <files>packages/open-bitcoin-network/src/inbound/permissions.rs, packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/inbound/tests.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/inbound/tests.rs`
    - `packages/open-bitcoin-network/src/lib.rs`
    - `packages/bitcoin-knots/src/net_permissions.h`
    - `packages/bitcoin-knots/src/net_permissions.cpp`
    - `packages/bitcoin-knots/test/functional/p2p_permissions.py`
    - `docs/parity/source-breadcrumbs.json`
  </read_first>
  <behavior>
    - Test exact token acceptance for `bloomfilter`, `blockfilters`, `noban`, `forcerelay`, `relay`, `mempool`, `download`, `addr`, `forceinbound`, `in`, `out`, and `all` per D-01.
    - Test rejection of unsupported aliases such as `bloom`, `compactfilters`, and `cfilters` with a field and token in the error per D-03.
    - Test `all` expands to active labels `admission_protected`, `eviction_policy_protected`, `misbehavior_policy_protected`, `address_response_policy_input`, and `download_serving_policy_input`, while relay-like labels remain inactive per D-05 and D-14.
  </behavior>
  <action>
    Create `packages/open-bitcoin-network/src/inbound/permissions.rs` with a top-of-file parity breadcrumb block citing `net_permissions.h`, `net_permissions.cpp`, and `p2p_permissions.py`. Define typed values: `PeerPermissionToken`, `PeerPermissionDirection`, `PeerPermissionSet`, `PermissionEffectLabel`, `InactivePermissionEffectLabel`, and `PeerPermissionParseError`. Parse raw tokens once at the boundary into those types; do not pass raw strings through policy code. Implement stable `as_str()` labels for every public enum. Export the new types from `inbound.rs` and crate `lib.rs`. Register `packages/open-bitcoin-network/src/inbound/permissions.rs` in `docs/parity/source-breadcrumbs.json` under `network-inbound-admission` or a new `network-peer-permissions` label with Knots permission anchors. This task implements D-01, D-03, D-04, D-05, D-10, D-11, D-12, D-13, and D-14 by making the exact vocabulary typed and splitting active bounded effects from inactive relay/filter/mempool effects.
  </action>
  <acceptance_criteria>
    - `rg -n "pub enum PeerPermissionToken|pub enum PermissionEffectLabel|pub enum InactivePermissionEffectLabel|struct PeerPermissionSet|struct PeerPermissionParseError" packages/open-bitcoin-network/src/inbound/permissions.rs` finds all types.
    - `rg -n "inactive_relay|inactive_forcerelay|inactive_mempool|inactive_bloomfilter|inactive_blockfilters|admission_protected|download_serving_policy_input|address_response_policy_input" packages/open-bitcoin-network/src/inbound/permissions.rs packages/open-bitcoin-network/src/inbound/tests.rs` finds stable labels.
    - `rg -n "bloom|compactfilters|cfilters" packages/open-bitcoin-network/src/inbound/tests.rs` finds negative alias coverage.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network permission -- --nocapture</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <done>Permission vocabulary is typed, exact, tested, exported, and breadcrumb-registered; `all` cannot activate relay, mempool, force-relay, bloom, or blockfilter behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add permission class registry and connection-class labels</name>
  <files>packages/open-bitcoin-network/src/inbound/permissions.rs, packages/open-bitcoin-network/src/inbound.rs, packages/open-bitcoin-network/src/inbound/tests.rs, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/inbound/permissions.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/inbound/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md`
  </read_first>
  <behavior>
    - Test a matched literal IP class with `in,noban,forceinbound` resolves to `protected_inbound` and `InboundAdmissionSlotClass::Reserved` per D-06 through D-08.
    - Test a matched literal IP class with `in,download,addr` resolves to `permissioned_inbound` and `InboundAdmissionSlotClass::Ordinary`.
    - Test unmatched inbound peers resolve to `ordinary_inbound` and never consume reserved slots.
    - Test direction-only, missing `in`, and any `out` class definition returns a deterministic field/token error per D-03 and D-09.
  </behavior>
  <action>
    Extend the permission module with `PermissionClassName`, `ParsedPeerPermissionClass`, `PeerPermissionClassRegistry`, `PeerConnectionClass`, and `InboundPermissionDecision`. Use literal `std::net::IpAddr` match addresses only; reject CIDR, hostnames, and socket endpoints with stable errors naming `inbound.permission_classes[].addresses[]`. Class labels exposed to status must be machine labels (`ordinary_inbound`, `permissioned_inbound`, `protected_inbound`, `outbound`, `manual_configured`), not user-provided class names. Add `slot_class()` on `PeerConnectionClass` so only protected inbound classes consume `InboundAdmissionSlotClass::Reserved`. Do not add transaction relay, mempool, compact-block, BIP37, compact-filter, address relay, ban, or misbehavior behavior. This task implements D-06, D-07, D-08, and D-09 by making connection classes explicit, pure, reserved-slot aware, and separate from outbound sync accounting.
  </action>
  <acceptance_criteria>
    - `rg -n "PeerConnectionClass|PeerPermissionClassRegistry|ParsedPeerPermissionClass|InboundPermissionDecision|ordinary_inbound|permissioned_inbound|protected_inbound" packages/open-bitcoin-network/src/inbound/permissions.rs packages/open-bitcoin-network/src/inbound/tests.rs` finds the class model and tests.
    - `rg -n "IpAddr" packages/open-bitcoin-network/src/inbound/permissions.rs` finds literal IP matching.
    - `! rg -n "Cidr|Subnet|IpNet|SocketAddr" packages/open-bitcoin-network/src/inbound/permissions.rs` proves there is no CIDR/subnet or endpoint matcher.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Connection classes are typed and deterministic; protected peers can use reserved admission capacity, ordinary peers cannot, and class evidence uses stable machine labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/CLI text -> network domain | Raw permission tokens cross into typed pure policy. |
| permission model -> later runtime callers | Active/inactive effects determine whether downstream plans can use a privilege. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01-01 | Elevation of Privilege | `PeerPermissionSet` parser | mitigate | Closed enums and exact-token tests reject unsupported tokens and aliases instead of treating them as active. |
| T-91-01-02 | Elevation of Privilege / Tampering | `all` expansion | mitigate | Store active and inactive effect labels separately; tests assert relay, force-relay, mempool, bloomfilter, and blockfilters remain inactive. |
| T-91-01-03 | Spoofing / Elevation of Privilege | class address matcher | mitigate | Literal `IpAddr` matching only; reject CIDR, hostnames, and endpoints to avoid hand-rolled subnet mistakes. |
| T-91-01-04 | Information Disclosure | class labels | mitigate | Public labels are machine labels and effect labels only; user class names are not status/metric labels. |
</threat_model>

<verification>
Run focused pure-core checks first, then ensure breadcrumb metadata is valid:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network permission -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network inbound -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
</verification>

<success_criteria>
PERM-01, PERM-02, and PERM-03 have a pure typed foundation. Raw permission strings do not survive parsing, `all` is auditable, inactive effects are observable as data, and no deferred relay/filter/mempool behavior is reachable from these types.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md`.
</output>
