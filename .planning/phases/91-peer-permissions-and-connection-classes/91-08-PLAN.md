---
phase: 91-peer-permissions-and-connection-classes
plan: 08
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - packages/open-bitcoin-network/src/peer/tests.rs
autonomous: true
requirements: [PERM-03]
must_haves:
  truths:
    - "Relay-like permissions do not change transaction relay behavior."
    - "Mempool and filter-like permissions do not create message handling or service-bit support."
    - "Compact block relay remains deferred and inactive."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Negative relay, mempool, filter, and compact-block permission safeguards"
  key_links:
    - from: "PeerPermissionSet"
      to: "PeerManager message handling"
      via: "negative tests"
      pattern: "inactive_relay|inactive_mempool|inactive_blockfilters"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Add deterministic negative tests for relay and mempool permission safeguards.

Purpose: Prove PERM-03 directly against existing peer message paths so parsed permissions cannot silently activate transaction relay, mempool propagation, BIP37, compact filters, or compact blocks.
Output: Focused `open-bitcoin-network` peer tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-network/src/message.rs
@packages/bitcoin-knots/src/net_processing.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove relay and mempool permissions stay inactive</name>
  <files>packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-01-SUMMARY.md`
  </read_first>
  <behavior>
    - A permission set containing `in,relay,forcerelay,mempool` exposes inactive labels but does not make `PeerManager` rebroadcast, accept unsolicited tx relay, or add mempool query support.
    - Existing `WtxidRelay`, `Inv`, `Tx`, and `GetData` behaviors remain unchanged when permission evidence exists as data.
  </behavior>
  <action>
    Add tests that construct Plan 01 permission sets with inactive relay-like labels and then exercise existing `PeerManager` message handling for `WtxidRelay`, transaction `Inv`, `Tx`, and `GetData`. Assert the actions are exactly the current behavior from existing tests and that no permission data path changes them. Do not modify production peer behavior unless a compile signature update from Plan 01 requires passing ordinary permission evidence into helpers. This task implements D-14, D-18, and D-19 by proving relay, force-relay, and mempool permissions stay inactive under deterministic peer tests.
  </action>
  <acceptance_criteria>
    - `rg -n "inactive_relay|inactive_forcerelay|inactive_mempool|WtxidRelay|WireNetworkMessage::Tx|GetData" packages/open-bitcoin-network/src/peer/tests.rs` finds safeguard tests.
    - `! rg -n "Mempool|Bloom|FilterLoad|compact filter|compact block relay" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/message.rs` proves there is no new production support for those surfaces.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network relay_permission -- --nocapture</automated>
  </verify>
  <done>Relay, force-relay, and mempool permissions are proven inactive against existing peer message paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove bloom, blockfilter, and compact-block permissions stay inactive</name>
  <files>packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
  </read_first>
  <behavior>
    - `bloomfilter` and `blockfilters` parsed from a permission class appear only as inactive labels.
    - Local service bits remain `NETWORK | WITNESS`; no bloom or compact-filter service bit is advertised.
    - Compact block inventory remains existing unsupported/deferred behavior and is not activated by permission labels.
  </behavior>
  <action>
    Add tests around permission sets containing `in,bloomfilter,blockfilters,all`. Assert service flags from local config are unchanged, inactive filter labels are present, and existing compact-block inventory handling remains unchanged. Keep tests deterministic and local; do not add any BIP37, BIP157, compact-filter, or compact-block handling. This task implements D-05, D-14, D-18, and D-19 by proving `all` expansion cannot activate bloom, blockfilter, or compact-block behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "inactive_bloomfilter|inactive_blockfilters|ServiceFlags::NETWORK \\| ServiceFlags::WITNESS|CompactBlock|all" packages/open-bitcoin-network/src/peer/tests.rs` finds coverage.
    - `! rg -n "NODE_BLOOM|BIP37|BIP157|FilterLoad|CFilters|CompactBlockRelay" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/message.rs` proves there is no new production activation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network filter_permission -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Bloom, blockfilter, and compact-block-like permission evidence cannot activate deferred filter or compact-block behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed permission data -> peer message behavior | Inactive labels must not change protocol message handling. |
| local service flags -> remote peer observation | Service bits must not advertise deferred filter support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-08-01 | Elevation of Privilege | relay permissions | mitigate | Negative tests assert `relay` and `forcerelay` do not alter transaction relay actions. |
| T-91-08-02 | Elevation of Privilege | mempool permission | mitigate | Negative tests assert no mempool query or propagation behavior appears. |
| T-91-08-03 | Spoofing | service bits | mitigate | Tests assert service flags do not advertise bloom or compact-filter support. |
| T-91-08-04 | Tampering | compact block handling | mitigate | Tests assert compact block behavior remains unchanged and not permission-gated. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network relay_permission -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network filter_permission -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-03 has deterministic negative proof: relay-like, mempool, bloom/filter, and compact-block-style permissions remain inactive evidence only.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-08-SUMMARY.md`.
</output>
