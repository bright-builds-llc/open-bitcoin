---
phase: 91-peer-permissions-and-connection-classes
plan: 07
type: execute
wave: 6
depends_on: [91-05]
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements: [PERM-04]
must_haves:
  truths:
    - "Support bundles include bounded permission evidence useful for diagnosis."
    - "Support bundles redact raw peer details, raw config, raw class names, and credentials."
    - "Support Markdown and JSON agree because both derive from the shared status snapshot."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support.rs"
      provides: "Support snapshot sanitization for permission evidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "Markdown rendering for bounded permission evidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "Redaction and support evidence tests"
  key_links:
    - from: "OpenBitcoinStatusSnapshot.peers.inbound"
      to: "support bundle JSON/Markdown"
      via: "sanitized shared status"
      pattern: "active_permission_effects|inactive_permission_effects"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Add support-bundle permission evidence with redaction.

Purpose: Satisfy PERM-04 support requirements without exposing peer details, raw permission config, or secrets.
Output: Sanitized support JSON and Markdown permission evidence with tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-08-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render bounded permission evidence in support Markdown</name>
  <files>packages/open-bitcoin-cli/src/operator/support/render/inbound.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`
    - `packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md`
  </read_first>
  <behavior>
    - Markdown `## Inbound Serving` includes permission class, permissioned/protected counts, active effects, inactive effects, and latest permission decision.
    - Next action text distinguishes inactive relay-like permissions from active bounded effects without claiming relay support.
  </behavior>
  <action>
    Extend `push_available_inbound` to render Plan 05 permission fields with stable labels. Add next-action guidance: if inactive relay-like effects are present, render `Relay, mempool, bloom, and blockfilter permissions are recorded as inactive Phase 91 evidence; do not treat them as relay support.` Keep wording diagnostic and scoped. Do not add transaction relay, compact block relay, mempool propagation, BIP37, compact-filter serving, full address relay, or production readiness claims. This task implements D-14, D-16, D-17, and D-19 by making inactive/deferred permission effects visible in redacted support evidence.
  </action>
  <acceptance_criteria>
    - `rg -n "permission_class|permissioned_inbound_peers|protected_inbound_peers|active_permission_effects|inactive_permission_effects|latest_permission_decision" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds Markdown coverage.
    - `rg -n "Relay, mempool, bloom, and blockfilter permissions are recorded as inactive" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds scoped next-action text.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture</automated>
  </verify>
  <done>Support Markdown includes safe permission diagnostics and clear inactive relay-like permission wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize support JSON permission evidence</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support.rs`
    - `packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md`
  </read_first>
  <behavior>
    - Support JSON includes stable class/effect labels and latest decision reason.
    - Support JSON excludes raw class names, raw `in,noban,...` config strings, peer ids, unbounded endpoint tables, and credential material.
  </behavior>
  <action>
    Reuse the existing support snapshot sanitization path. If Plan 05 status fields include any value derived from a user-provided class name, strip or replace it before serialization; the preferred output is machine labels only. Add tests with representative forbidden strings: `operator_loopback`, `in,noban,forceinbound`, `peer_id=`, `127.0.0.1:`, `rpc_password`, and `cookie`. This task implements D-16 and D-18 by keeping support evidence deterministic, local, and free of raw peer/config/credential details.
  </action>
  <acceptance_criteria>
    - `rg -n "operator_loopback|in,noban,forceinbound|peer_id=|rpc_password|cookie" packages/open-bitcoin-cli/src/operator/support/tests.rs` finds forbidden-string assertions.
    - `rg -n "sanitize|redact|permission" packages/open-bitcoin-cli/src/operator/support.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds support sanitization updates.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Support bundle JSON and Markdown expose bounded permission decisions while redacting raw config, peer, endpoint, and credential details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared status -> support artifact | Runtime status becomes a shareable diagnostic bundle. |
| operator config names -> support output | User-provided labels must not leak into support evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-07-01 | Information Disclosure | support JSON | mitigate | Tests assert raw config strings, class names, peer ids, endpoints, and credentials are absent. |
| T-91-07-02 | Information Disclosure | support Markdown | mitigate | Render low-cardinality machine labels and counts only. |
| T-91-07-03 | Repudiation | support decision evidence | mitigate | Preserve latest permission decision reason and inactive effect labels so operators can explain outcomes. |
| T-91-07-04 | Elevation of Privilege | support wording | mitigate | Next-action text explicitly states relay-like permissions are inactive evidence, not relay support. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli inbound_support -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-04 support evidence is diagnosable and redacted. Support artifacts explain permission decisions without leaking secrets or implying deferred behavior is supported.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-07-SUMMARY.md`.
</output>
