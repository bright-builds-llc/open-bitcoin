---
phase: 91-peer-permissions-and-connection-classes
plan: 05
type: execute
wave: 5
depends_on: [91-04]
files_modified:
  - packages/open-bitcoin-node/src/status/inbound.rs
  - packages/open-bitcoin-node/src/status/inbound/tests.rs
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [PERM-02, PERM-03, PERM-04]
must_haves:
  truths:
    - "Open Bitcoin network status exposes permission class and active/inactive effect evidence."
    - "Metrics remain low-cardinality numeric counters only."
    - "`getnetworkinfo` does not gain detailed permission fields."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/inbound.rs"
      provides: "Shared inbound permission status contract"
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "Low-cardinality permission metric kinds"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Projection from managed admission evidence to shared status"
  key_links:
    - from: "ManagedInboundAdmissionInfo"
      to: "InboundPeerServingStatus"
      via: "current_inbound_status"
      pattern: "permission"
    - from: "openbitcoinnetworkstatus"
      to: "InboundPeerServingStatus"
      via: "serde status contract"
      pattern: "active_permission_effects|inactive_permission_effects"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Project permission evidence through shared status, RPC extension, and metrics contracts.

Purpose: Satisfy PERM-04 using one shared source of truth rather than renderer-local summaries.
Output: Extended inbound status contract, low-cardinality metric kinds, and RPC tests that protect baseline `getnetworkinfo`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-03-SUMMARY.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-04-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-05-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-06-SUMMARY.md
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/dispatch/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared inbound status with permission evidence</name>
  <files>packages/open-bitcoin-node/src/status/inbound.rs, packages/open-bitcoin-node/src/status/inbound/tests.rs, packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/status/inbound.rs`
    - `packages/open-bitcoin-node/src/status/inbound/tests.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs`
  </read_first>
  <behavior>
    - `openbitcoinnetworkstatus` JSON includes `permissioned_inbound_peers`, `protected_inbound_peers`, `permission_class`, `active_permission_effects`, `inactive_permission_effects`, and `latest_permission_decision`.
    - Inactive `relay`, `forcerelay`, `mempool`, `bloomfilter`, and `blockfilters` evidence is visible when those tokens are parsed.
    - `getnetworkinfo` output still omits `permission_class`, `active_permission_effects`, and `inactive_permission_effects`.
  </behavior>
  <action>
    Add shared status structs such as `InboundPermissionEvidence` and `InboundPermissionDecisionEvent` under `status/inbound.rs`. Extend `InboundPeerServingStatus` with low-cardinality fields: `permissioned_inbound_peers`, `protected_inbound_peers`, `permission_class`, `active_permission_effects`, `inactive_permission_effects`, and `latest_permission_decision`. Project from managed admission evidence in `ManagedRpcContext::current_inbound_status`. Re-export new status types from `open-bitcoin-node/src/lib.rs`. Update RPC dispatch tests for available/unavailable permission evidence and baseline `getnetworkinfo` shape. This task implements D-15, D-16, and D-19 by projecting shared status/support-safe evidence and proving negative relay safeguards in RPC status tests.
  </action>
  <acceptance_criteria>
    - `rg -n "InboundPermissionEvidence|InboundPermissionDecisionEvent|permissioned_inbound_peers|protected_inbound_peers|active_permission_effects|inactive_permission_effects|latest_permission_decision" packages/open-bitcoin-node/src/status/inbound.rs packages/open-bitcoin-node/src/status/inbound/tests.rs packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds shared status and tests.
    - `rg -n "getnetworkinfo.*permission|permission_class.*getnetworkinfo" packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds a negative regression test.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_status -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture</automated>
  </verify>
  <done>Shared status and RPC extension expose bounded permission evidence without changing `getnetworkinfo`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add low-cardinality permission metric kinds</name>
  <files>packages/open-bitcoin-node/src/metrics.rs</files>
  <read_first>
    - `packages/open-bitcoin-node/src/metrics.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md`
  </read_first>
  <behavior>
    - Metric kinds include numeric counters for permissioned admits, protected admits, inactive relay-like permission observations, and permission validation failures.
    - Metric labels do not include peer ids, endpoints, user class names, raw tokens, or raw config strings.
  </behavior>
  <action>
    Add `MetricKind` variants with stable labels: `InboundPermissionedAdmitCount`, `InboundProtectedAdmitCount`, `InboundInactivePermissionEffectCount`, and `InboundPermissionValidationFailureCount`. Update `MetricKind::ALL`, `as_str()`, and tests. Keep labels fixed and low-cardinality; do not add metric dimensions or string labels. This task implements D-17 by adding numeric low-cardinality permission counters with no peer ids, endpoints, user labels, or raw config strings.
  </action>
  <acceptance_criteria>
    - `rg -n "InboundPermissionedAdmitCount|InboundProtectedAdmitCount|InboundInactivePermissionEffectCount|InboundPermissionValidationFailureCount|inbound_permissioned_admit_count|inbound_protected_admit_count|inbound_inactive_permission_effect_count|inbound_permission_validation_failure_count" packages/open-bitcoin-node/src/metrics.rs` finds all metric labels.
    - `! rg -n "peer_id|endpoint|class_name|raw_permission|raw_config" packages/open-bitcoin-node/src/metrics.rs` proves there are no new high-cardinality label fields.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Metrics expose permission activity as bounded numeric series with no peer or config identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| managed network -> shared status | Internal permission evidence becomes operator-visible JSON. |
| status -> metrics | Permission activity becomes long-lived numeric observability data. |
| Open Bitcoin RPC -> baseline RPC expectations | Extension status must not alter `getnetworkinfo`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-05-01 | Information Disclosure | `InboundPeerServingStatus` | mitigate | Expose only machine labels and effect labels; no endpoints, peer ids, raw class names, or raw permission strings. |
| T-91-05-02 | Elevation of Privilege | status inactive effects | mitigate | Tests assert inactive relay/mempool/filter labels appear as inactive evidence, not active status. |
| T-91-05-03 | Tampering | metric labels | mitigate | Add enum-only numeric metric kinds with fixed labels and no dynamic dimensions. |
| T-91-05-04 | Spoofing / Repudiation | `getnetworkinfo` boundary | mitigate | Dispatch regression tests assert baseline method omits detailed Open Bitcoin permission evidence. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node inbound_status -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-02, PERM-03, and PERM-04 evidence is visible through Open Bitcoin-owned status and metrics without leaking secrets or mutating baseline RPC shape.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-05-SUMMARY.md`.
</output>
