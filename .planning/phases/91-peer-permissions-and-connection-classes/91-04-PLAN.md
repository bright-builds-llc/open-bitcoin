---
phase: 91-peer-permissions-and-connection-classes
plan: 04
type: execute
wave: 4
depends_on: [91-02, 91-03]
files_modified:
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03]
must_haves:
  truths:
    - "Runtime inbound admission resolves permission class before calling managed admission."
    - "Permission matching uses remote literal IP only, not endpoint strings or user labels."
    - "Runtime listener remains a thin adapter over pure permission and admission decisions."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/context.rs"
      provides: "ManagedRpcContext storage for resolved permission registry"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Remote-address class resolution and admission helper"
    - path: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      provides: "Accept-loop call into permission-aware admission"
  key_links:
    - from: "RuntimeConfig.inbound"
      to: "ManagedRpcContext"
      via: "from_runtime_config"
      pattern: "permission"
    - from: "handle_inbound_stream"
      to: "record_inbound_admission"
      via: "SocketAddr remote_addr"
      pattern: "remote_addr"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 91-2026-06-25T13-36-41
generated_at: 2026-06-25T14:51:51Z
---

<objective>
Wire resolved permission classes into the runtime inbound admission path.

Purpose: Make class selection happen before managed admission while keeping socket I/O isolated in the RPC listener shell.
Output: `ManagedRpcContext` stores the resolved registry and listener admission uses typed permission decisions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-CONTEXT.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-02-SUMMARY.md
@.planning/phases/91-peer-permissions-and-connection-classes/91-03-SUMMARY.md
@.planning/phases/90-inbound-listener-and-admission-policy/90-04-SUMMARY.md
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/inbound_listener.rs
@packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store resolved permission registry on ManagedRpcContext</name>
  <files>packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/context.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs`
    - `.planning/phases/91-peer-permissions-and-connection-classes/91-02-SUMMARY.md`
  </read_first>
  <behavior>
    - `ManagedRpcContext::from_runtime_config` stores the resolved permission registry from `RuntimeConfig.inbound`.
    - Test helpers without configured classes preserve ordinary inbound admission.
    - A configured literal IP class resolves to a protected or permissioned decision without reading raw config strings.
  </behavior>
  <action>
    Add a `PeerPermissionClassRegistry` field to `ManagedRpcContext`. Initialize it from the resolved inbound config in `from_runtime_config`; initialize it to empty in `new` and `for_local_operator`. Add a helper such as `permission_decision_for_remote_addr(remote_addr: SocketAddr)` in `context/network.rs` that calls the pure registry and returns an `InboundPermissionDecision`. Keep user-provided class names private to config validation and do not expose them in `ManagedRpcContext` status. This task implements D-07 and D-18 by keeping class selection pure and verified with synthetic local context tests.
  </action>
  <acceptance_criteria>
    - `rg -n "PeerPermissionClassRegistry|permission_decision_for_remote_addr|permission_classes" packages/open-bitcoin-rpc/src/context.rs packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds storage and tests.
    - `! rg -n "class_name|raw_permission|raw_config" packages/open-bitcoin-rpc/src/context.rs packages/open-bitcoin-rpc/src/context/network.rs` proves there is no status-oriented raw config leak.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc permission_context -- --nocapture</automated>
  </verify>
  <done>Runtime context owns typed permission class selection without raw config leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make listener admission permission-aware</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
  </read_first>
  <behavior>
    - `handle_inbound_stream` passes `SocketAddr` remote address into a permission-aware admission helper before peer insertion.
    - A protected class consumes reserved capacity in a loopback test while ordinary peers still receive `reserved_slot_unavailable`.
    - Inactive relay/mempool/filter effects appear in admission evidence but do not change wire-message handling.
  </behavior>
  <action>
    Replace the hard-coded `InboundAdmissionSlotClass::Ordinary` listener path with a method such as `record_inbound_admission_for_remote_addr(peer_id, remote_addr, is_shutdown_requested)`. The method must resolve class/effects from the remote IP, populate `InboundAdmissionRequest.connection_class` and `permission_decision`, and pass the derived slot class into managed admission. Keep `handle_inbound_stream` free of permission parsing and raw token logic. This task implements D-07, D-08, D-09, D-10, and D-19 by choosing bounded permission effects before admission without changing outbound sync or relay behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "record_inbound_admission_for_remote_addr|permission_decision|remote_addr\\.ip|ProtectedInbound|Reserved" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/inbound_listener/tests.rs` finds wiring and tests.
    - `! rg -n "relay|mempool|bloomfilter|blockfilters|forcerelay" packages/open-bitcoin-rpc/src/inbound_listener.rs` proves the listener has no behavior activation beyond data labels.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener -- --nocapture</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Runtime inbound peers are classified before admission through typed pure policy, and listener I/O remains a thin shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket remote address -> permission policy | Runtime peer address determines configured class match. |
| permission policy -> listener admission | Typed decision changes slot class and evidence before peer insertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-01 | Spoofing | remote address matching | mitigate | Match only `SocketAddr::ip()` against literal configured `IpAddr`; do not parse endpoint strings in listener logic. |
| T-91-04-02 | Elevation of Privilege | listener admission helper | mitigate | Derive slot class from `PeerConnectionClass`, not raw tokens; tests cover reserved-capacity behavior. |
| T-91-04-03 | Denial of Service | permissioned inbound admission | mitigate | Reuse Phase 90 caps and reserved-slot policy; no outbound sync counters or targets are changed. |
| T-91-04-04 | Tampering | runtime accept loop | mitigate | Keep parsing out of `handle_inbound_stream`; it only passes typed remote address evidence into context. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc permission_context -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_permission -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener -- --nocapture`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
PERM-01 through PERM-03 are wired into runtime admission. Permissioned inbound peers are classified deterministically, protected peers can consume reserved capacity, ordinary peers cannot, and no deferred relay or mempool path is activated.
</success_criteria>

<output>
After completion, create `.planning/phases/91-peer-permissions-and-connection-classes/91-04-SUMMARY.md`.
</output>
