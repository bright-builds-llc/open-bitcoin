---
phase: 131-rolling-fee-expiry-and-descendant-eviction-core
plan: 03
type: tdd
wave: 3
depends_on:
  - 02
files_modified:
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool/expiry.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/tests.rs
autonomous: true
requirements:
  - PRESS-04
user_setup: []
must_haves:
  truths:
    - "Pure expire(now) removes Known acceptance ages older than configured expiry and all their descendants"
    - "Expiry removals emit MempoolRemovalCause::Expiry with Direct/Descendant roles and leave no stale indexes"
    - "LegacyUnknown acceptance times are skipped (not invented); pure core never reads SystemTime"
    - "ManagedNetworkHandle can invoke expiry with shell-injected PolicyTime"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/expiry.rs"
      provides: "Expire-shaped pure API"
      exports: ["expire", "DEFAULT_MEMPOOL_EXPIRY_HOURS"]
    - path: "packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs"
      provides: "Age + descendant cleanup + LegacyUnknown skip fixtures"
      contains: "expiry_"
    - path: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      provides: "expire_mempool(PolicyTime) authority mutate"
      contains: "expire_mempool"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      to: "packages/open-bitcoin-mempool/src/pool/expiry.rs"
      via: "ManagedNetworkHandle mutate → Mempool::expire"
      pattern: "expire_mempool"
    - from: "packages/open-bitcoin-mempool/src/pool/expiry.rs"
      to: "packages/open-bitcoin-mempool/src/pool/topology.rs"
      via: "collect_descendants + recompute_state"
      pattern: "collect_descendants"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 131-2026-07-24T22-07-47
generated_at: 2026-07-24T23:45:00.000Z
---

<objective>
Add a pure `Expire`-shaped mempool API with injected `PolicyTime`, descendant-safe cleanup, and minimal `ManagedNetworkHandle` wiring (PRESS-04).

Purpose: Aged entries and their descendant packages leave cleanly without inventing LegacyUnknown times or sampling clocks in core.
Output: `pool/expiry.rs`, expiry fixtures, authority `expire_mempool` hook. Full receive-independent maintenance loops stay Phase 136.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md

<interfaces>
Knots:
```cpp
// DEFAULT_MEMPOOL_EXPIRY_HOURS = 336
// Expire(time_point cutoff): remove entries with entry_time < cutoff (+ descendants)
// LimitMempoolSize: Expire(...) then TrimToSize(...)
```

Target pure API shape:
```rust
impl Mempool {
    /// Removes entries with Known(accepted_at) &lt; now - expiry_duration (+ descendants).
    /// Skips MempoolAcceptanceTime::LegacyUnknown (D-12 / RESEARCH A1).
    /// Returns MempoolLifecycleDelta with Expiry Direct/Descendant facts.
    pub fn expire(&mut self, now: PolicyTime) -> Result&lt;MempoolLifecycleDelta, MempoolError&gt;;
}
```

PolicyConfig addition:
```rust
pub mempool_expiry: Duration /* or hours: u64 */;
// Default: 336 hours = 336 * 3600 seconds
pub const DEFAULT_MEMPOOL_EXPIRY_HOURS: u64 = 336;
```

From packages/open-bitcoin-mempool/src/context.rs:
```rust
pub enum MempoolAcceptanceTime {
    Known(PolicyTime),
    LegacyUnknown,
}
```

Shell (D-12):
```rust
impl ManagedNetworkHandle {
    pub fn expire_mempool(&self, now: PolicyTime) -> Result&lt;MempoolLifecycleDelta, ...&gt;;
}
// Sample time in callers/tests; project Expiry removals through existing serving maps
// (admission_bridge / mempool_lifecycle already map Expiry → serving). Do not build Phase 136 timers.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing expiry cleanup fixtures</name>
  <files>packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs</files>
  <read_first>
    - .planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md (D-10..D-12)
    - .planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md (Pattern 3, Pitfall 4)
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs (MempoolRemovalCause::Expiry)
    - packages/open-bitcoin-mempool/src/context.rs (MempoolAcceptanceTime)
  </read_first>
  <behavior>
    - `expiry_removes_aged_entry_and_descendants`: parent accepted_at far in past, child newer but descendant of expired parent; both removed; roles Direct on aged roots selected, Descendant on children; ledger oracle matches
    - `expiry_retains_fresh_known_entries`: accepted_at within window → retained
    - `expiry_skips_legacy_unknown_without_inventing_time`: LegacyUnknown entry remains; no panic; sweep succeeds
    - `expiry_emits_mempool_removal_cause_expiry`: delta.removed causes are Expiry only for expired set
  </behavior>
  <action>
    Create `expiry_cases.rs` + module registration. Use explicit `PolicyTime` values and a short test `mempool_expiry` override once the field exists (or construct via whatever temporary test seam Task 2 adds). Tests fail until `Mempool::expire` exists. Assert `recompute_resource_ledger(&amp;entries, &amp;spent) == mempool.resource_ledger()` after expiry. No `SystemTime` in pure tests.
  </action>
  <acceptance_criteria>
    - `rg -n "expiry_removes_aged|expiry_retains_fresh|expiry_skips_legacy|expiry_emits_mempool" packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs` returns ≥4 matches
    - `rg -n "SystemTime::now" packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-04-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib expiry_</automated>
  </verify>
  <done>Failing PRESS-04 expiry fixtures exist for age, descendants, LegacyUnknown skip, and Expiry cause.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure expire + ManagedNetworkHandle hook</name>
  <files>packages/open-bitcoin-mempool/src/types.rs, packages/open-bitcoin-mempool/src/pool/expiry.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/mempool_lifecycle.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs, packages/open-bitcoin-node/src/network/runtime_authority/tests.rs, packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs</files>
  <read_first>
    - packages/open-bitcoin-mempool/src/pool/pressure.rs (shared removal/recompute patterns from Plan 01)
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs (delta builder, Expiry cause)
    - packages/open-bitcoin-node/src/network/runtime_authority.rs (mutate patterns)
    - packages/open-bitcoin-node/src/network/admission_bridge.rs (Expiry → serving mapping — reuse, do not reclassify)
  </read_first>
  <action>
    Per D-10..D-12 and RESEARCH Pattern 3/4:
    1. Add `DEFAULT_MEMPOOL_EXPIRY_HOURS: u64 = 336` and `PolicyConfig.mempool_expiry_hours` (or equivalent Duration) defaulting to 336.
    2. Implement `pool/expiry.rs`: cutoff = `now.unix_seconds() - (expiry_hours as i64 * 3600)`; stage every entry with `MempoolAcceptanceTime::Known(t)` where `t.unix_seconds() &lt; cutoff`; skip `LegacyUnknown`; for each staged root collect descendants; remove through shared topology helpers + `recompute_state` / ledger update; emit `MempoolLifecycleDelta` with `MempoolRemovalCause::Expiry` and Direct/Descendant roles (D-11).
    3. Expose `Mempool::expire(&amp;mut self, now: PolicyTime) -&gt; Result&lt;MempoolLifecycleDelta, MempoolError&gt;`. Pure core must not call `SystemTime` (D-12).
    4. After expiry in any combined maintenance helper (optional pure `limit_mempool_size(now)`), may call accounted `trim_to_size` like Knots `LimitMempoolSize` — only if it stays pure and tested; otherwise keep expire separate and let admission trim continue to enforce capacity.
    5. Add `ManagedNetworkHandle::expire_mempool(now: PolicyTime)` that mutates solely through existing authority, applies Expiry removals to serving/orphan cleanup using existing cause maps (minimal Phase 134 scope — do not claim full cross-cache projection).
    6. Node unit test: inject PolicyTime, call handle.expire_mempool, assert membership + serving status updates for expired txids.
    7. Do not invent Phase 136 periodic timers; API + authority + tests suffice (RESEARCH open question recommendation).
  </action>
  <acceptance_criteria>
    - `rg -n "DEFAULT_MEMPOOL_EXPIRY_HOURS|336" packages/open-bitcoin-mempool/src` matches
    - `rg -n "fn expire|LegacyUnknown" packages/open-bitcoin-mempool/src/pool/expiry.rs` matches
    - `rg -n "SystemTime" packages/open-bitcoin-mempool/src/pool/expiry.rs` returns 0
    - `rg -n "fn expire_mempool" packages/open-bitcoin-node/src/network/runtime_authority.rs` matches
    - `bun run scripts/command-timings.ts run --key mempool-press-04-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib expiry_` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-04-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib expiry_; bun run scripts/command-timings.ts run --key mempool-press-04-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node expire_mempool</automated>
  </verify>
  <done>PRESS-04 pure expiry + authority hook green; LegacyUnknown skipped; descendants/indexes cleaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell PolicyTime → expire API | Only authority-injected time may expire entries |
| Expiry delta → serving maps | Removals must use fixed Expiry labels, not free-form strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03 | Tampering | pool/expiry.rs | mitigate | Task 2: mandatory collect_descendants + recompute; Task 1 oracle equality |
| T-131-04 | Spoofing | Mempool::expire | mitigate | Task 2: injected PolicyTime only; skip LegacyUnknown rather than inventing acceptance times (D-12) |
| T-131-05 | Information Disclosure | lifecycle Expiry labels | mitigate | Reuse `MempoolRemovalCause::Expiry.as_str() == "expiry"`; no identity in shared metrics |
| T-131-02 | Denial of Service | stale descendant retention | mitigate | Task 1–2: aged root removes entire descendant package |
| T-131-01 | Elevation of Privilege | second mutation authority | mitigate | Task 2: expire only via ManagedNetworkHandle mutate path |
</threat_model>

<verification>
Mempool `expiry_` tests + node `expire_mempool` test. Confirm admission_bridge Expiry serving mapping still covers new producer.
</verification>

<success_criteria>
- Expire removes aged Known entries + descendants with Expiry roles
- LegacyUnknown skipped; no core clock reads
- Authority hook available without Phase 136 timers
</success_criteria>

<output>
After completion, create `.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-03-SUMMARY.md`
</output>
