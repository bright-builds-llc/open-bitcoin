---
phase: 131-rolling-fee-expiry-and-descendant-eviction-core
plan: 05
type: tdd
wave: 5
depends_on:
  - 04
files_modified:
  - packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/fee/rolling.rs
  - packages/open-bitcoin-mempool/src/pool/expiry.rs
  - packages/open-bitcoin-mempool/src/pool/pressure.rs
  - packages/open-bitcoin-bench/src/cases/mempool.rs
  - packages/open-bitcoin-bench/src/cases.rs
  - packages/open-bitcoin-bench/src/registry.rs
  - docs/parity/source-breadcrumbs.json
  - docs/parity/catalog/mempool-policy.md
  - scripts/check-phase131-rolling-fee-expiry-pressure.ts
  - scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - PRESS-05
user_setup: []
must_haves:
  truths:
    - "Deterministic fill→trim→block→decay→expiry→refill→reorg sequences agree with recompute_resource_ledger and rolling-fee state after each committed step"
    - "Hermetic sustained-pressure work stays within documented performance thresholds enforced by open-bitcoin-bench / default verifier"
    - "Restart baseline rolling fee remains zero (non-durable) and is documented"
    - "New first-party Rust sources carry parity breadcrumbs; Phase 131 checker is wired into verify.sh"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs"
      provides: "Multi-step oracle scenario for PRESS-05"
      contains: "sustained_pressure_oracle"
    - path: "packages/open-bitcoin-bench/src/cases/mempool.rs"
      provides: "Hermetic sustained-pressure threshold case"
      contains: "mempool-pressure"
    - path: "scripts/check-phase131-rolling-fee-expiry-pressure.ts"
      provides: "Verifier ownership checks for PRESS surfaces"
      contains: "PRESS-05"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase131-rolling-fee-expiry-pressure.ts"
      via: "run_step after Phase 130 checker"
      pattern: "check-phase131-rolling-fee-expiry-pressure"
    - from: "sustained_pressure_cases.rs"
      to: "recompute_resource_ledger"
      via: "oracle assert after each transition"
      pattern: "recompute_resource_ledger"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 131-2026-07-24T22-07-47
generated_at: 2026-07-24T23:45:00.000Z
---

<objective>
Lock PRESS-05 with hermetic sustained-pressure oracle scenarios, documented performance thresholds in `open-bitcoin-bench`, parity breadcrumbs/docs finalization, and a Phase 131 verifier checker wired into `scripts/verify.sh`.

Purpose: Prove bounded, recomputation-faithful pressure/rolling/expiry sequences without public-network or soak gates.
Output: Sustained scenario tests, bench case + thresholds, breadcrumb coverage, `check-phase131-*.ts` in verify.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-VALIDATION.md
@packages/open-bitcoin-bench/src/cases/mempool.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
Scenario skeleton (D-13/D-14):
```rust
// 1) fill under small MempoolCapacity until trim fires (Pressure + bump)
// 2) connect block (open decay gate; remove confirmed)
// 3) advance PolicyTime → decay
// 4) expire aged Known entries
// 5) refill / admit replacements
// 6) reorg lifecycle with explicit ReorgLifecycleContext time
// After EACH step:
assert_eq!(mempool.resource_ledger(), recompute_resource_ledger(entries, spent)?);
assert_eq!(mempool.rolling_mempool_fee_rate(), /* expected from state machine */);
// No SystemTime; no network.
```

Restart non-durability (D-15):
```rust
let mut m = Mempool::new(config);
// bump rolling via pressure...
let restarted = Mempool::new(config); // or recovery path without rolling persistence
assert_eq!(restarted.rolling_mempool_fee_rate(), RollingMempoolFeeRate::ZERO);
```

Bench pattern (extend existing mempool.rs cases array):
```rust
// New case id e.g. "mempool-policy.sustained-pressure-trim"
// Pure durability; deterministic fixture count N admits + trims
// Fail if wall duration exceeds documented threshold constant in the case module
// Threshold chosen during implementation from local timing; must be hermetic and fail default verify when exceeded
```

Verifier pattern (mirror Phase 130 checker wiring in scripts/verify.sh ~lines 570–571):
```
bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts
bun run scripts/check-phase131-rolling-fee-expiry-pressure.ts
```
Checker should assert presence of: accounted trim limiter, track_package_removed / ROLLING_FEE_HALFLIFE, expire API + DEFAULT_MEMPOOL_EXPIRY_HOURS=336, capacityenforcement accounted_memory, rolling parity active, breadcrumb entries for new src files, and no public-network soak language in phase docs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sustained oracle scenario + restart-zero assertion</name>
  <files>packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs</files>
  <read_first>
    - .planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md (D-13..D-15)
    - packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs
    - packages/open-bitcoin-mempool/src/pool/tests/rolling_fee_cases.rs
    - packages/open-bitcoin-mempool/src/pool/tests/expiry_cases.rs
    - packages/open-bitcoin-mempool/src/resource.rs (`recompute_resource_ledger`)
  </read_first>
  <behavior>
    - `sustained_pressure_oracle_agrees_across_fill_trim_block_decay_expiry_refill_reorg`: multi-step sequence; after each committed transition, resource ledger equals oracle and rolling fee matches state-machine expectation; final membership has no dangling children
    - `rolling_fee_restarts_at_zero_without_durability`: after in-memory bump, a freshly constructed Mempool (simulating restart without rolling persistence) has `RollingMempoolFeeRate::ZERO` (D-15)
  </behavior>
  <action>
    Add `sustained_pressure_cases.rs` + module registration. Keep the scenario fully hermetic with explicit `PolicyTime` / `BlockLifecycleContext` / `ReorgLifecycleContext`. Reuse helpers from earlier PRESS test modules where possible. Do not sleep; do not touch network crates. Make the oracle test fail if any step leaves ledger drift or stale descendants.
  </action>
  <acceptance_criteria>
    - `rg -n "sustained_pressure_oracle|rolling_fee_restarts_at_zero" packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs` matches
    - `rg -n "recompute_resource_ledger" packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs` matches
    - `rg -n "SystemTime::now|std::thread::sleep" packages/open-bitcoin-mempool/src/pool/tests/sustained_pressure_cases.rs` returns 0
    - `bun run scripts/command-timings.ts run --key mempool-press-05-oracle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib sustained_pressure` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-05-oracle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib sustained_pressure</automated>
  </verify>
  <done>PRESS-05 oracle scenario and restart-zero contract are automated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Hermetic bench threshold + breadcrumbs + Phase 131 verifier</name>
  <files>packages/open-bitcoin-bench/src/cases/mempool.rs, packages/open-bitcoin-bench/src/cases.rs, packages/open-bitcoin-bench/src/registry.rs, docs/parity/source-breadcrumbs.json, docs/parity/catalog/mempool-policy.md, scripts/check-phase131-rolling-fee-expiry-pressure.ts, scripts/check-phase131-rolling-fee-expiry-pressure.test.ts, scripts/verify.sh, packages/open-bitcoin-mempool/src/fee/rolling.rs, packages/open-bitcoin-mempool/src/pool/expiry.rs, packages/open-bitcoin-mempool/src/pool/pressure.rs</files>
  <read_first>
    - packages/open-bitcoin-bench/src/cases/mempool.rs
    - packages/open-bitcoin-bench/src/registry.rs
    - docs/parity/source-breadcrumbs.json (mempool-policy / mempool-resource-accounting / mempool-lifecycle groups)
    - scripts/check-phase130-resource-time-fee-primitives.ts (structure to mirror)
    - scripts/verify.sh (Phase 130 run_step region ~570)
    - AGENTS.md Repo-Local Guidance (parity breadcrumbs requirement)
  </read_first>
  <action>
    Per D-14 and RESEARCH validation architecture:
    1. Add a Pure hermetic bench case (e.g. `mempool-policy.sustained-pressure-trim`) that performs a bounded fill/trim loop and fails if elapsed time exceeds a documented constant in the case file (choose N and threshold from local timing during implementation; must be tight enough to catch obvious quadratic blowups, loose enough for CI noise — document both in case comments and catalog).
    2. Register the case in `cases.rs` / `registry.rs` as required by existing bench patterns.
    3. Ensure every new Phase 131 first-party Rust source under `packages/open-bitcoin-mempool/src/**` and new test modules carry parity breadcrumb headers; register paths in `docs/parity/source-breadcrumbs.json` under `mempool-policy` / `mempool-resource-accounting` / `mempool-lifecycle` as appropriate (use `none` only if no Knots anchor — prefer Knots TrimToSize/GetMinFee/Expire anchors).
    4. Finalize catalog prose for sustained-pressure bounds and non-durable rolling fee (D-15).
    5. Create `scripts/check-phase131-rolling-fee-expiry-pressure.ts` + `.test.ts` asserting PRESS-01..05 surface presence (accounted trim, track_package_removed / HALFLIFE, expire + 336h, evidence labels, breadcrumb paths, no public-network soak in phase 131 docs).
    6. Wire bun test + bun run steps into `scripts/verify.sh` immediately after the Phase 130 checker steps (both the early bun-test block and the run_step block if both exist — match Phase 130 dual wiring).
    7. Do not add Criterion-only or sleep-based CI gates; do not claim Phase 134/135/136 scope.
  </action>
  <acceptance_criteria>
    - `rg -n "sustained-pressure|mempool-pressure" packages/open-bitcoin-bench/src/cases/mempool.rs` matches
    - `rg -n "fee/rolling.rs|pool/expiry.rs|pool/pressure.rs|sustained_pressure_cases" docs/parity/source-breadcrumbs.json` matches new paths
    - `rg -n "check-phase131-rolling-fee-expiry-pressure" scripts/verify.sh` returns ≥2 matches (test + run, mirroring Phase 130)
    - `bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts` and `bun run scripts/check-phase131-rolling-fee-expiry-pressure.ts` exit 0
    - `bun run scripts/command-timings.ts run --key mempool-press-05-bench -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture` includes the new case path or package tests green as applicable to how bench cases are invoked (follow existing bench verification pattern in verify/check-benchmark-report)
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase131-rolling-fee-expiry-pressure.test.ts; bun run scripts/check-phase131-rolling-fee-expiry-pressure.ts; bun run scripts/command-timings.ts run --key mempool-press-05-oracle -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib sustained_pressure; bun run scripts/command-timings.ts run --key mempool-press-05-bench -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-bench -- --nocapture</automated>
  </verify>
  <done>PRESS-05 hermetic bounds, breadcrumbs, and verify.sh Phase 131 checker are live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermetic tests/bench → CI | Thresholds must not require public network |
| Checker scripts → claim surface | Labels and ownership strings must stay low-cardinality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02 | Denial of Service | sustained trim loop | mitigate | Task 2: hermetic bench threshold fails default verify on unbounded clone/recompute blowups |
| T-131-03 | Tampering | sustained_pressure_cases oracle | mitigate | Task 1: recompute_resource_ledger equality after every transition |
| T-131-05 | Information Disclosure | check-phase131 script | mitigate | Task 2: assert fixed evidence strings / forbid free-form soak claims |
| T-131-04 | Spoofing | scenario clocks | mitigate | Task 1: PolicyTime-only; grep ban SystemTime/sleep |
| T-131-01 | Denial of Service | fee-floor churn | accept | Covered by Plans 01–02 mechanics; this plan only regression-oracles the integrated sequence |
</threat_model>

<verification>
Sustained mempool tests green; Phase 131 bun checker green and referenced from verify.sh; breadcrumbs cover new sources.
</verification>

<success_criteria>
- Multi-step oracle scenario passes
- Hermetic perf threshold exists and is verifier-reachable
- Rolling restart-zero documented/tested
- Phase 131 checker wired into default verify
</success_criteria>

<output>
After completion, create `.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-05-SUMMARY.md`
</output>
