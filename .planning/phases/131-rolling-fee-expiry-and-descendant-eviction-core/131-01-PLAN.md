---
phase: 131-rolling-fee-expiry-and-descendant-eviction-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-mempool/src/fee.rs
  - packages/open-bitcoin-mempool/src/fee/rolling.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/pressure.rs
  - packages/open-bitcoin-mempool/src/pool/admission.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests/outcome_cases.rs
autonomous: true
requirements:
  - PRESS-01
  - PRESS-02
user_setup: []
must_haves:
  truths:
    - "Trim stops when accounted memory is within MempoolCapacity; total_virtual_size alone never drives eviction"
    - "Pressure selects the lowest descendant_score package (txid tie-break) and removes victim plus all descendants"
    - "Each pressure package removal bumps RollingMempoolFeeRate to package_feerate + incremental when strictly greater and clears the block-since-bump gate"
    - "Effective admission remains max(static, rolling); incremental is bump input only"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/fee/rolling.rs"
      provides: "RollingFeeState with track_package_removed bump semantics"
      exports: ["RollingFeeState", "track_package_removed"]
    - path: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      provides: "Accounted-capacity trim_to_size + package feerate helper"
      contains: "accounted_memory"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs"
      provides: "PRESS-01/02 hermetic trim and bump fixtures"
      contains: "accounted_capacity_trim"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/admission.rs"
      to: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      via: "trim_to_size after prospective admit"
      pattern: "trim_to_size\\("
    - from: "packages/open-bitcoin-mempool/src/pool/pressure.rs"
      to: "packages/open-bitcoin-mempool/src/fee/rolling.rs"
      via: "track_package_removed before package remove"
      pattern: "track_package_removed"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 131-2026-07-24T22-07-47
generated_at: 2026-07-24T23:45:00.000Z
---

<objective>
Switch mempool capacity enforcement from `legacy_vsize_trim_limit` to accounted memory vs `MempoolCapacity`, and bump the rolling floor from each evicted descendant package using Knots `trackPackageRemoved` semantics (PRESS-01, PRESS-02).

Purpose: Sustained pressure stays memory-bounded and the rolling floor rises from the package actually removed.
Output: Pure `RollingFeeState` bump API, accounted `trim_to_size`, pressure unit tests; evidence enum flip deferred to Plan 04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md

<interfaces>
<!-- Key contracts — use these directly; do not rediscover via scavenger hunt. -->

From packages/open-bitcoin-mempool/src/pool.rs (current trim limiter — REPLACE):
```rust
while state.resource_ledger.total_virtual_size() > config.legacy_vsize_trim_limit {
    let Some(victim_txid) = select_eviction_candidate(&state.entries) else { break; };
    // collect_descendants + remove + recompute_state — no rolling bump today
}
```

Target trim limiter (per D-01/D-03):
```rust
while state.resource_ledger.accounted_memory().as_usize()
    > config.mempool_capacity.as_usize()
{
    // select → package feerate → track_package_removed → remove package → recompute
}
```

From packages/bitcoin-knots/src/txmempool.cpp `trackPackageRemoved` (port exactly):
- Build `CFeeRate` from modified fees/size with descendants
- Add `incremental_relay_feerate`
- If `removed > rollingMinimumFeeRate`: set rolling to removed; set `blockSinceLastRollingFeeBump = false`

From packages/open-bitcoin-mempool/src/fee.rs:
```rust
pub struct FeeRate { /* sats_per_kvb: i64 */ }
impl FeeRate {
    pub fn from_fee_sats_and_vbytes(fee_sats: i64, virtual_size: TransactionVirtualSize) -> Self;
    pub const fn sats_per_kvb(self) -> i64;
}
pub struct IncrementalRelayFeeRate(FeeRate);
pub struct RollingMempoolFeeRate(FeeRate);
pub fn effective_admission_fee_rate(static_floor, rolling_floor) -> EffectiveAdmissionFeeRate;
// effective = max(static, rolling) — NEVER fold incremental into effective (D-06)
```

From packages/open-bitcoin-mempool/src/types.rs `MempoolEntry`:
```rust
pub descendant_stats: AggregateStats; // count, virtual_size, total_fee_sats
pub fn descendant_score(&self) -> ...; // existing Knots-shaped score; keep txid tie-break (D-04)
```

Package feerate for bump (NOT descendant_score, NOT single-tx rate):
```rust
FeeRate::from_fee_sats_and_vbytes(
    victim.descendant_stats.total_fee_sats,
    victim.descendant_stats.virtual_size,
)
// then add incremental sats_per_kvb (checked/saturating add of sats_per_kvb integers)
```

Knots constants relevant here:
- Bump uses package + incremental; bump only when strictly greater
- Clear block-since-bump on successful bump (decay ownership is Plan 02; still clear the gate flag here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing PRESS-01/02 pressure fixtures</name>
  <files>packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs</files>
  <read_first>
    - .planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md (D-01..D-06)
    - .planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md (Pattern 1, pitfalls 2–3)
    - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
    - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
    - packages/open-bitcoin-mempool/src/pool.rs (trim_to_size, select_eviction_candidate)
  </read_first>
  <behavior>
    - `accounted_capacity_trim_evicts_until_usage_within_capacity`: fill mempool with known-acceptance entries under a tiny `mempool_capacity`; after admit/trim, `accounted_memory().as_usize() <= mempool_capacity.as_usize()`; assert `total_virtual_size` may still be &gt; some prior vsize-only limit that would have trimmed differently if used
    - `pressure_bump_uses_descendant_package_feerate_plus_incremental`: parent+child package where child raises package feerate; after pressure eviction of that package, `rolling_mempool_fee_rate().fee_rate().sats_per_kvb()` equals `llround`-equivalent integer from package fee/vsize + incremental sats/kvB (strict greater path)
    - `pressure_bump_skips_when_not_strictly_greater`: preset rolling above package+incremental; after trim, rolling unchanged
    - `pressure_removes_victim_and_descendants_with_roles`: Pressure Direct on victim, Descendant on children; no orphan children remain; `recompute_resource_ledger` equals cached ledger
  </behavior>
  <action>
    Create `pressure_cases.rs` and `mod pressure_cases;` in `pool/tests.rs`. Write #[test] functions named with the prefixes above. Use small `PolicyConfig { mempool_capacity: MempoolCapacity::new(...) }` for capacity — do not rely on `legacy_vsize_trim_limit` as the active limiter (per D-01/D-02). Tests must fail against current vsize-driven `trim_to_size` and missing bump. Do not flip `MempoolCapacityEnforcement` or delete `legacy_vsize_trim_limit` yet (Plan 04).
  </action>
  <acceptance_criteria>
    - `rg -n "accounted_capacity_trim|pressure_bump_uses_descendant|pressure_bump_skips|pressure_removes_victim" packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs` returns ≥4 matches
    - `rg -n "mod pressure_cases" packages/open-bitcoin-mempool/src/pool/tests.rs` matches
    - `bun run scripts/command-timings.ts run --key mempool-press-01-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib pressure_ -- --nocapture` shows failing (or compile-fail against missing APIs) before Task 2/3 land
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-01-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib pressure_</automated>
  </verify>
  <done>Failing PRESS-01/02 fixtures exist and target accounted capacity + package bump behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RollingFeeState bump + accounted trim</name>
  <files>packages/open-bitcoin-mempool/src/fee/rolling.rs, packages/open-bitcoin-mempool/src/fee.rs, packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/pool/pressure.rs, packages/open-bitcoin-mempool/src/pool.rs</files>
  <read_first>
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/pool.rs (trim_to_size through recompute_state)
    - packages/open-bitcoin-mempool/src/pool/topology.rs
    - packages/bitcoin-knots/src/txmempool.cpp (TrimToSize, trackPackageRemoved) if submodule present; otherwise RESEARCH Pattern 1
    - Task 1 pressure_cases.rs expectations
  </read_first>
  <action>
    Per D-01..D-06 and RESEARCH Pattern 1:
    1. Add `fee/rolling.rs` with `RollingFeeState` holding at least: internal rolling rate (FeeRate and/or f64 for later decay), `block_since_last_rolling_fee_bump: bool`, `last_rolling_fee_update: PolicyTime`. Export `track_package_removed(&amp;mut self, package_plus_incremental: FeeRate)` that bumps only when `package_plus_incremental &gt; current rolling FeeRate`, then sets rolling and `block_since_last_rolling_fee_bump = false`.
    2. Wire `fee.rs` as `pub mod rolling;` (or module tree matching crate style: prefer `fee.rs` + `fee/` folder per repo convention).
    3. Move/replace private `trim_to_size` / `select_eviction_candidate` into `pool/pressure.rs` (or keep select in pressure and call from pool). Limiter MUST be `state.resource_ledger.accounted_memory().as_usize() &gt; config.mempool_capacity.as_usize()` — never `total_virtual_size` / `legacy_vsize_trim_limit` (D-01/D-02).
    4. Before removing each selected package: compute package feerate from victim `descendant_stats.total_fee_sats` + `descendant_stats.virtual_size` via `FeeRate::from_fee_sats_and_vbytes`; add incremental as `FeeRate::from_sats_per_kvb(package.sats_per_kvb().saturating_add(config.incremental_relay_fee_rate.fee_rate().sats_per_kvb()))`; call `track_package_removed`.
    5. Keep selection: min `descendant_score()`, then txid tie-break (D-04). Remove via `collect_descendants` + entry delete + `recompute_state` (D-11).
    6. Store `RollingFeeState` on `Mempool` (replace bare `RollingMempoolFeeRate` field or wrap it). Keep `set_rolling_mempool_fee_rate` as test inject seam that updates state without claiming production ownership (Claude discretion).
    7. Do NOT change effective admission derivation; do NOT put incremental into effective (D-06).
    8. Do NOT flip capacity-enforcement / rolling-parity enums yet (Plan 04). Do NOT implement decay math yet (Plan 02) beyond clearing the block gate on bump.
  </action>
  <acceptance_criteria>
    - `rg -n "accounted_memory\\(\\)\\.as_usize\\(\\)|mempool_capacity\\.as_usize" packages/open-bitcoin-mempool/src/pool/pressure.rs packages/open-bitcoin-mempool/src/pool.rs` matches trim loop
    - `rg -n "legacy_vsize_trim_limit" packages/open-bitcoin-mempool/src/pool/pressure.rs packages/open-bitcoin-mempool/src/pool.rs` has zero matches in trim loop conditions (field may still exist on PolicyConfig)
    - `rg -n "fn track_package_removed|block_since_last_rolling_fee_bump" packages/open-bitcoin-mempool/src/fee/rolling.rs` matches
    - `rg -n "from_fee_sats_and_vbytes|descendant_stats" packages/open-bitcoin-mempool/src/pool/pressure.rs` matches bump numerator/denominator
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-01-impl -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib pressure_</automated>
  </verify>
  <done>Accounted trim + track_package_removed bump compile and pressure_ tests progress toward green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate trim fixtures and green PRESS-01/02</name>
  <files>packages/open-bitcoin-mempool/src/pool/admission.rs, packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs, packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs, packages/open-bitcoin-mempool/src/pool/tests/outcome_cases.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, packages/open-bitcoin-mempool/src/pool/tests/pressure_cases.rs</files>
  <read_first>
    - packages/open-bitcoin-mempool/src/pool/admission.rs (trim_to_size call site)
    - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs (`legacy_vsize_trim_limit_is_independent_from_accounted_capacity`)
    - All fixtures that set `legacy_vsize_trim_limit:` (rg across open-bitcoin-mempool)
  </read_first>
  <action>
    Ensure admission still calls the upgraded trim and records `MempoolRemovalCause::Pressure` with Direct/Descendant roles. Migrate every mempool unit fixture that previously relied on tiny `legacy_vsize_trim_limit` to drive eviction so it uses a correspondingly small `mempool_capacity` instead (per D-02 active-limiter retirement). Update `legacy_vsize_trim_limit_is_independent_from_accounted_capacity` to assert that trim ignores leftover `legacy_vsize_trim_limit` values when accounted usage is under capacity (or rewrite to accounted-capacity semantics). Keep `PolicyConfig.legacy_vsize_trim_limit` field for now so Phase 130 checker still passes until Plan 04 deletes it. Make all `pressure_` tests and previously failing trim-dependent mempool lib tests pass.
  </action>
  <acceptance_criteria>
    - `bun run scripts/command-timings.ts run --key mempool-press-01-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib pressure_` exits 0
    - `rg -n "while state.resource_ledger.total_virtual_size\\(\\) &gt; config.legacy_vsize_trim_limit" packages/open-bitcoin-mempool/src` returns 0 matches
    - Admission path still emits Pressure removals: `rg -n "MempoolRemovalCause::Pressure" packages/open-bitcoin-mempool/src/pool/admission.rs` matches
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-01-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib</automated>
  </verify>
  <done>PRESS-01/02 green in open-bitcoin-mempool; trim is accounted-memory; bump matches package+incremental strict-greater semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Peer/local admission → pure trim | Untrusted tx volume can force eviction; policy must bound memory |
| Pure rolling state → operator evidence | Rolling floor affects later admission; must not be spoofable from core clocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Denial of Service | Rolling fee / trim loop | mitigate | Task 2: bump rolling from package+incremental on each pressure package; clears block gate so churn cannot immediately re-admit same feerate band without a later block (decay in Plan 02) |
| T-131-02 | Denial of Service | trim_to_size / MempoolCapacity | mitigate | Task 2–3: enforce `accounted_memory > capacity` loop until within bounds; reject unbounded memory growth |
| T-131-03 | Tampering | descendant package removal | mitigate | Task 2: always `collect_descendants` + `recompute_state`; Task 1 oracle asserts ledger equality |
| T-131-04 | Spoofing | PressureDecisionContext / clocks | accept | Pure trim does not sample clocks; admission shell already injects acceptance time (Phase 130). No new core clock reads in this plan |
| T-131-05 | Information Disclosure | evidence labels | accept | Enforcement label flip deferred to Plan 04; this plan does not invent free-form evidence strings |
</threat_model>

<verification>
Targeted: `cargo test -p open-bitcoin-mempool --lib pressure_` then full `-p open-bitcoin-mempool --lib`.
Do not flip RPC `capacityenforcement` yet — Phase 130 checker still requires legacy_vsize seam until Plan 04.
</verification>

<success_criteria>
- Accounted-capacity trim is the sole active limiter
- Descendant-score package eviction + rolling bump match Knots trackPackageRemoved strict-greater rules
- Effective admission remains max(static, rolling)
</success_criteria>

<output>
After completion, create `.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-01-SUMMARY.md`
</output>
