---
phase: 131-rolling-fee-expiry-and-descendant-eviction-core
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - packages/open-bitcoin-rpc/src
  - docs/parity/catalog/mempool-policy.md
  - docs/parity/index.json
  - scripts/check-phase130-resource-time-fee-primitives.ts
  - scripts/check-phase130-resource-time-fee-primitives.test.ts
autonomous: true
requirements:
  - PRESS-01
  - PRESS-03
user_setup: []
must_haves:
  truths:
    - "Operator capacityenforcement evidence is accounted_memory, not legacy_vsize"
    - "Rolling-fee parity status is active (not deferred) once bump+decay are live"
    - "PolicyConfig no longer exposes an active or residual legacy_vsize_trim_limit field"
    - "Phase 130 checker no longer requires the live legacy_vsize seam after historical rewrite"
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      provides: "MempoolCapacityEnforcement::AccountedMemory + RollingFeeParityStatus::Active"
      contains: "accounted_memory"
    - path: "docs/parity/catalog/mempool-policy.md"
      provides: "Updated Phase 131 enforcement and rolling-fee ownership prose"
      contains: "accounted_memory"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      to: "RPC/node mempool info"
      via: "capacity_enforcement.as_str() / rolling_fee_parity.as_str()"
      pattern: "AccountedMemory|accounted_memory"
    - from: "scripts/check-phase130-resource-time-fee-primitives.ts"
      to: "historical Phase 130 artifacts"
      via: "legacy seam check rewritten off live pool.rs requirement"
      pattern: "checkLegacyEnforcementSeam|Phase 130"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 131-2026-07-24T22-07-47
generated_at: 2026-07-24T23:45:00.000Z
---

<objective>
Flip capacity-enforcement and rolling-fee parity evidence to live accounted-memory / active labels, delete the `legacy_vsize_trim_limit` seam, and update parity docs plus the Phase 130 checker so verify stays green (PRESS-01/03 operator truth).

Purpose: Operators and checkers must see truthful enforcement and live rolling-fee parity after bump/decay/expiry exist.
Output: Enum + RPC/node fixture updates, deleted PolicyConfig field, catalog/index updates, Phase 130 checker historicalization.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-CONTEXT.md
@.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-RESEARCH.md
@docs/parity/catalog/mempool-policy.md
@scripts/check-phase130-resource-time-fee-primitives.ts

<interfaces>
Current (Phase 130 transitional — REPLACE):
```rust
pub enum MempoolCapacityEnforcement { LegacyVsize } // as_str => "legacy_vsize"
pub enum RollingFeeParityStatus { Deferred } // as_str => "deferred"
// pressure_summary hardcodes both
pub struct PolicyConfig { pub legacy_vsize_trim_limit: TransactionVirtualSize, ... }
```

Target (D-02, D-09, RESEARCH A2 / open Q1):
```rust
pub enum MempoolCapacityEnforcement {
    AccountedMemory, // as_str => "accounted_memory"
}
pub enum RollingFeeParityStatus {
    Active, // as_str => "active"
}
// Delete LegacyVsize / Deferred variants entirely (no dual live labels).
// Delete PolicyConfig.legacy_vsize_trim_limit and DEFAULT_LEGACY_VSIZE_TRIM_LIMIT.
```

Phase 130 checker today (`checkLegacyEnforcementSeam`) REQUIRES live:
- `pool.rs` contains `legacy_vsize_trim_limit`
- lifecycle contains `Self::LegacyVsize => "legacy_vsize"`
- catalog contains `fixed \`legacy_vsize\` during Phase 130`

Rewrite that function to assert historical Phase 130 ownership via SUMMARY/catalog archive wording (e.g. Phase 130 still documented as having introduced the transitional seam) WITHOUT requiring the live field — otherwise Plan 04 cannot delete the seam.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip enums, delete legacy_vsize field, update call sites</name>
  <files>packages/open-bitcoin-mempool/src/types.rs, packages/open-bitcoin-mempool/src/pool/lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs, packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/recovery_cases.rs, packages/open-bitcoin-rpc/src</files>
  <read_first>
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs (MempoolCapacityEnforcement, RollingFeeParityStatus, pressure_summary)
    - packages/open-bitcoin-mempool/src/types.rs (PolicyConfig)
    - `rg -n "legacy_vsize_trim_limit|LegacyVsize|RollingFeeParityStatus::Deferred|capacity_enforcement" packages packages/open-bitcoin-rpc scripts docs`
  </read_first>
  <action>
    Per D-02 and D-09:
    1. Replace `MempoolCapacityEnforcement` with sole variant `AccountedMemory` (`as_str` → `"accounted_memory"`).
    2. Replace `RollingFeeParityStatus` with sole variant `Active` (`as_str` → `"active"`). Remove `Deferred`.
    3. Update `pressure_summary` to emit the new variants (not hard-coded legacy/deferred).
    4. Delete `PolicyConfig.legacy_vsize_trim_limit` and `DEFAULT_LEGACY_VSIZE_TRIM_LIMIT`; fix Default impl and every struct literal across mempool/node/rpc/tests (rg must go to 0 for the field name in Rust sources).
    5. Update assertions that expected `"legacy_vsize"` / `Deferred` (node `mempool_info_exposes_truthful_resource_and_fee_roles`, lifecycle_cases, rpc getmempoolinfo tests).
    6. Keep fee-role field separation unchanged (static/incremental/rolling/effective).
  </action>
  <acceptance_criteria>
    - `rg -n "legacy_vsize_trim_limit" packages --glob '*.rs'` returns 0
    - `rg -n "LegacyVsize|\"legacy_vsize\"" packages/open-bitcoin-mempool packages/open-bitcoin-node packages/open-bitcoin-rpc --glob '*.rs'` returns 0 for enforcement enum/tests (catalog markdown updated in Task 2)
    - `rg -n "RollingFeeParityStatus::Deferred|\"deferred\"" packages/open-bitcoin-mempool/src/pool/lifecycle.rs packages/open-bitcoin-mempool/src/pool/tests packages/open-bitcoin-node --glob '*.rs'` returns 0 for parity status
    - `rg -n "AccountedMemory|\"accounted_memory\"|RollingFeeParityStatus::Active|\"active\"" packages/open-bitcoin-mempool/src/pool/lifecycle.rs` matches
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key mempool-press-evidence-rust -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib lifecycle_; bun run scripts/command-timings.ts run --key mempool-press-evidence-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib mempool_info_exposes</automated>
  </verify>
  <done>Live evidence enums and PolicyConfig seam match accounted-memory + active rolling parity; Rust call sites compile.</done>
</task>

<task type="auto">
  <name>Task 2: Update parity docs and Phase 130 checker historical seam</name>
  <files>docs/parity/catalog/mempool-policy.md, docs/parity/index.json, scripts/check-phase130-resource-time-fee-primitives.ts, scripts/check-phase130-resource-time-fee-primitives.test.ts</files>
  <read_first>
    - docs/parity/catalog/mempool-policy.md (capacityenforcement / Phase 131 boundary sections)
    - docs/parity/index.json (rolling-min-fee deferred notes)
    - scripts/check-phase130-resource-time-fee-primitives.ts (`checkLegacyEnforcementSeam`)
    - scripts/check-phase130-resource-time-fee-primitives.test.ts
  </read_first>
  <action>
    1. Update `docs/parity/catalog/mempool-policy.md`: `capacityenforcement` = `accounted_memory`; document that Phase 131 owns accounted trim + live rolling bump/decay; retain a short historical note that Phase 130 shipped the transitional `legacy_vsize` seam.
    2. Update `docs/parity/index.json` so rolling-min-fee / capacity enforcement ownership is no longer Deferred-to-131 for the live claim — point at Phase 131 completion of PRESS mechanics (wording accurate; do not claim Phase 138 adversarial soak).
    3. Rewrite `checkLegacyEnforcementSeam` so Phase 130 checker remains historically true without requiring live `legacy_vsize_trim_limit` in `pool.rs` or `LegacyVsize` in lifecycle. Example: require Phase 130 SUMMARY or catalog historical sentence; optionally require live lifecycle now contains `AccountedMemory` only if that belongs in a new Phase 131 checker (prefer leave live accounted asserts to Plan 05 checker). Update matching bun tests.
    4. Do not add public-network gates. Do not claim durable rolling fee (D-15).
  </action>
  <acceptance_criteria>
    - `rg -n "accounted_memory" docs/parity/catalog/mempool-policy.md` matches
    - `rg -n "fixed \`legacy_vsize\` during Phase 130" docs/parity/catalog/mempool-policy.md` may remain only as historical prose, not as current enforcement claim
    - `rg -n "legacy_vsize_trim_limit" scripts/check-phase130-resource-time-fee-primitives.ts` does not require it present in live pool.rs (function rewritten)
    - `bun test scripts/check-phase130-resource-time-fee-primitives.test.ts` and `bun run scripts/check-phase130-resource-time-fee-primitives.ts` exit 0
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase130-resource-time-fee-primitives.test.ts; bun run scripts/check-phase130-resource-time-fee-primitives.ts</automated>
  </verify>
  <done>Parity docs and Phase 130 checker reflect Phase 131 live accounted enforcement without blocking seam deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain enums → RPC/metrics | Evidence strings cross the operator boundary |
| Checker scripts → CI truth | Incorrect historical locks can force insecure dual semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-05 | Information Disclosure | MempoolCapacityEnforcement / RollingFeeParityStatus | mitigate | Task 1: fixed low-cardinality `accounted_memory` / `active` only; delete legacy/deferred live variants |
| T-131-05 | Tampering | check-phase130 checker | mitigate | Task 2: stop requiring live legacy seam that would allow silent dual limiters |
| T-131-02 | Denial of Service | PolicyConfig leftover limiter | mitigate | Task 1: delete `legacy_vsize_trim_limit` so no fixture can re-enable vsize trim |
| T-131-01 | Spoofing | rolling parity label | mitigate | Task 1: `Active` only after Plans 01–02 delivered bump+decay (this plan depends on 03 which depends on 02) |
</threat_model>

<verification>
Mempool lifecycle tests, node mempool_info test, Phase 130 bun checker green after historical rewrite.
</verification>

<success_criteria>
- capacityenforcement=accounted_memory and rolling_fee_parity=active in live summary/RPC
- legacy_vsize_trim_limit gone from Rust sources
- Phase 130 checker does not resurrect the live legacy seam
</success_criteria>

<output>
After completion, create `.planning/phases/131-rolling-fee-expiry-and-descendant-eviction-core/131-04-SUMMARY.md`
</output>
