---
phase: 71-resource-bounds-and-durable-restart-resume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements: [RES-01, RES-02, RES-04]
user_setup: []
must_haves:
  truths:
    - "Same-datadir resume is proven for clean shutdown, unclean shutdown, mid-download interruption, mid-connect interruption, and stale in-flight work."
    - "Already connected blocks are not requested or connected again after reopen."
    - "Synthetic long-chain tests exercise resource bounds without public-network access."
    - "Long-chain test evidence stays within configured peer, in-flight, message, round, metrics, and log bounds."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Phase 71 restart/resume matrix and synthetic long-chain tests"
      contains: "phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Deterministic long-chain resource-bound proof"
      contains: "phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "packages/open-bitcoin-node/src/sync.rs"
      via: "DurableSyncRuntime::open and sync_once scripted transport fixtures"
      pattern: "DurableSyncRuntime::open"
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "packages/open-bitcoin-node/src/storage/fjall_store.rs"
      via: "FjallNodeStore reopen on the same temp datadir"
      pattern: "FjallNodeStore::open\\(&path\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 71-2026-06-13T10-34-37
generated_at: 2026-06-13T10:57:42Z
---

<objective>
Add deterministic node-runtime tests for the Phase 71 restart/resume matrix and synthetic long-chain resource-bound proof.

Purpose: RES-02 and RES-04 require restart safety and long-chain bound evidence that does not depend on public-mainnet timing.
Output: Two named `phase71_` Rust tests in the existing sync fixture module.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-SUMMARY.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-02-SUMMARY.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-03-SUMMARY.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-03-SUMMARY.md
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/block_reconcile.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs

<interfaces>
From packages/open-bitcoin-node/src/sync/tests.rs:
```rust
fn temp_store_path(test_name: &str) -> PathBuf;
fn sync_config() -> SyncRuntimeConfig;
fn sync_config_with_log_dir(log_dir: &Path) -> SyncRuntimeConfig;
fn version_verack_script(start_height: i32) -> Vec<WireNetworkMessage>;
fn headers_script(start_height: i32, headers: Vec<BlockHeader>) -> Vec<WireNetworkMessage>;
fn build_block(previous_block_hash: BlockHash, height: u32) -> Block;
```

From packages/open-bitcoin-node/src/sync/tests.rs:
```rust
struct ScriptedTransport;
impl ScriptedTransport {
    fn new(scripts: Vec<Vec<WireNetworkMessage>>) -> Self;
    fn with_connect_results(scripts: Vec<Result<Vec<WireNetworkMessage>, SyncRuntimeError>>) -> Self;
    fn sent_messages(&self) -> Vec<WireNetworkMessage>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the same-datadir restart and interruption matrix</name>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - .planning/phases/58-same-datadir-restart-and-resume-evidence/58-CONTEXT.md
    - .planning/phases/64-service-restart-and-same-datadir-resume-evidence/64-CONTEXT.md
    - .planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-SUMMARY.md
    - .planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-03-SUMMARY.md
    - packages/open-bitcoin-node/src/sync/tests.rs
    - packages/open-bitcoin-node/src/sync.rs
    - packages/open-bitcoin-node/src/sync/block_reconcile.rs
  </read_first>
  <behavior>
    - Test `phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight` covers five named cases: `clean_shutdown`, `unclean_shutdown`, `mid_download_interruption`, `mid_connect_interruption`, and `stale_inflight_after_reopen`.
    - The clean and unclean cases assert `sync.recovery_category` is `clean_shutdown` and `unclean_shutdown` respectively.
    - The mid-download case proves durable headers or downloaded bodies survive reopen and that reopened `blocks_in_flight` is `0` before a new request cycle.
    - The mid-connect case proves connected active-chain height/hash/work survive reopen and `ScriptedTransport::sent_messages()` does not include a duplicate `getdata` request for the already connected block.
    - The stale in-flight case proves stale runtime-only work is cleared, reassigned, or diagnosed with `NoProgressDiagnosis::StaleInflightCleanup`, without hiding durable connected/downloaded progress.
  </behavior>
  <action>Add the named matrix test using existing `DurableSyncRuntime`, `FjallNodeStore`, `ScriptedTransport`, `RuntimeMetadata`, `SyncRecoveryCategory`, and `NoProgressDiagnosis` helpers. Use Arrange/Act/Assert comments. Implement D-04, D-05, and D-06 exactly: same datadir, real Fjall reopen, no duplicate connected-block request, and explicit stale in-flight cleanup or diagnosis.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight|clean_shutdown|unclean_shutdown|mid_download_interruption|mid_connect_interruption|stale_inflight_after_reopen" packages/open-bitcoin-node/src/sync/tests.rs` finds all six strings.
    - `rg -n "NoProgressDiagnosis::StaleInflightCleanup|SyncRecoveryCategory::CleanShutdown|SyncRecoveryCategory::UncleanShutdown|sent_messages\\(\\)" packages/open-bitcoin-node/src/sync/tests.rs` finds all four patterns.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_same_datadir_resume_matrix --all-features` passes and runs at least one test.
  </acceptance_criteria>
  <done>The restart/resume matrix is auditable by test name and proves all five RES-02 interruption cases without public-network access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic synthetic long-chain resource-bound coverage</name>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
    - packages/open-bitcoin-node/src/sync/tests.rs
    - packages/open-bitcoin-node/src/sync/block_reconcile.rs
    - packages/open-bitcoin-node/src/metrics.rs
    - packages/open-bitcoin-node/src/logging.rs
  </read_first>
  <behavior>
    - Test `phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network` uses first-party mined synthetic blocks and `ScriptedTransport`; it must not use DNS seeds, manual public peers, sleeps, wall-clock timing thresholds, or live network calls.
    - The test builds a deterministic chain of exactly `48` synthetic blocks, uses `target_outbound_peers: 2`, `max_blocks_in_flight_per_peer: 2`, `max_blocks_in_flight_total: 4`, `max_messages_per_peer: 12`, and `max_rounds: 32`.
    - The test asserts `blocks_in_flight <= 4`, `outbound_peers <= 2`, `max_messages_per_peer == 12`, `max_sync_rounds == 32`, metrics retention equals `30`, `2880`, `86400`, and log retention equals `14`, `14`, `268435456`.
    - The test reopens the same store after partial progress and asserts the reopened runtime resumes from durable headers/downloaded/connected progress instead of replaying unsafe in-memory assumptions.
  </behavior>
  <action>Add the named test and helper code only inside `packages/open-bitcoin-node/src/sync/tests.rs`. Reuse `build_block`, `headers_script`, `ScriptedTransport`, `MetricRetentionPolicy::default()`, and `LogRetentionPolicy::default()` per D-10 and D-11. Keep public-network full-sync and service-manager checks out of the test per D-12.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network|48|target_outbound_peers: 2|max_blocks_in_flight_total: 4|max_messages_per_peer: 12|max_rounds: 32" packages/open-bitcoin-node/src/sync/tests.rs` finds all six patterns.
    - `rg -n "MetricRetentionPolicy::default\\(\\)|LogRetentionPolicy::default\\(\\)|max_samples_per_series, 2_880|max_total_bytes, 268_435_456" packages/open-bitcoin-node/src/sync/tests.rs` finds all four patterns.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_synthetic_long_chain --all-features` passes and runs at least one test.
  </acceptance_criteria>
  <done>RES-04 has a deterministic long-chain test that exercises configured resource bounds, retention policies, durable reopen, and no public-network access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripted peer messages -> sync runtime | Deterministic peer fixtures simulate untrusted public peer headers, blocks, and failures. |
| runtime memory -> durable store reopen | In-memory in-flight work must not become trusted durable progress after interruption. |
| durable store -> resumed active chain | Reopened state must preserve validated progress without duplicate connect or unsafe replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | D | `DurableSyncRuntime` block download loop | mitigate | Synthetic long-chain test asserts peer, message, round, and in-flight caps stay within configured values. |
| T-71-02-02 | T | same-datadir resume | mitigate | Matrix test asserts durable connected progress survives reopen and connected blocks are not requested again. |
| T-71-02-03 | R | restart/resume evidence | mitigate | Named test cases record clean, unclean, mid-download, mid-connect, and stale in-flight outcomes. |
| T-71-02-04 | S | public-network shortcut proof | mitigate | Tests use `ScriptedTransport` and local Fjall stores only; no DNS, manual public peer, or timing proof is accepted. |
</threat_model>

<verification>
Run the two focused `phase71_` node tests, then run `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_ --all-features`.
</verification>

<success_criteria>
- RES-02 restart/resume matrix is covered by exact test names and assertions.
- RES-04 long-chain proof is deterministic and public-network-free.
- Long-chain test asserts peer, in-flight, message, round, metrics, and log bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/71-resource-bounds-and-durable-restart-resume/71-02-SUMMARY.md`.
</output>
