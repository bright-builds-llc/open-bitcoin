---
phase: 71-resource-bounds-and-durable-restart-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/runtime/support.rs
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
autonomous: true
requirements: [RES-01]
user_setup: []
must_haves:
  truths:
    - "Support evidence remains compact and redacted during long sync review."
    - "Runtime support output lists the configured sync resource bounds operators can inspect."
    - "Live-smoke support summaries use allowlisted fields and exclude raw peer, log, stdout, stderr, and credential material."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/runtime/support.rs"
      provides: "Runtime support rendering tests for SyncResourcePressure fields"
      contains: "phase71_runtime_support_resource_pressure_lists_all_configured_bounds"
    - path: "packages/open-bitcoin-cli/src/operator/support.rs"
      provides: "Support bundle redaction and compact evidence tests"
      contains: "phase71_support_redaction_names_compact_evidence_bounds"
    - path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      provides: "Allowlisted live-smoke support summary tests"
      contains: "phase71_live_smoke_summary_is_allowlisted_and_bounded"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      to: "packages/open-bitcoin-cli/src/operator/support.rs"
      via: "collect_live_smoke_evidence calls live_smoke::summary"
      pattern: "live_smoke::summary"
    - from: "packages/open-bitcoin-cli/src/operator/runtime/support.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "sync_pressure_text consumes SyncResourcePressure"
      pattern: "fn sync_pressure_text\\(value: &SyncResourcePressure\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 71-2026-06-13T10-34-37
generated_at: 2026-06-13T10:57:42Z
---

<objective>
Add deterministic support-surface proof that Phase 71 resource evidence remains bounded, redacted, and sourced from existing typed runtime fields.

Purpose: RES-01 requires documented and tested bounds for support evidence, metrics/log summaries, peer/resource facts, and operator-facing resource pressure without adding raw report material.
Output: Focused CLI tests that prove compact support evidence and exact resource-pressure rendering.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/open-bitcoin-cli/src/operator/runtime/support.rs
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs

<interfaces>
From packages/open-bitcoin-node/src/status.rs:
```rust
pub struct SyncResourcePressure {
    pub blocks_in_flight: u64,
    pub max_header_requests_in_flight_per_peer: u64,
    pub max_headers_per_message: u64,
    pub max_blocks_in_flight_per_peer: u64,
    pub max_blocks_in_flight_total: u64,
    pub max_messages_per_peer: u64,
    pub max_sync_rounds: u64,
    pub outbound_peers: u32,
    pub target_outbound_peers: u32,
}
```

From packages/open-bitcoin-cli/src/operator/runtime/support.rs:
```rust
fn sync_pressure_text(value: &SyncResourcePressure) -> String
```

From packages/open-bitcoin-cli/src/operator/support/live_smoke.rs:
```rust
pub(super) fn summary(value: &Value) -> Option<Value>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove compact support and live-smoke evidence bounds</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/live_smoke.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
  </read_first>
  <behavior>
    - Test `phase71_support_redaction_names_compact_evidence_bounds`: `redaction_summary().omitted` contains exactly `RPC cookie contents`, `RPC password and RPC auth values`, `wallet private material and raw wallet files`, and `raw unbounded log contents`; `redaction_summary().safeguards` contains exactly `credential sources are represented as metadata only`, `live smoke reports are summarized from allowlisted fields only`, and `logs are limited to existing structured status signals`.
    - Test `phase71_live_smoke_summary_is_allowlisted_and_bounded`: a JSON report containing allowlisted `resourcePressure` fields plus raw `rawPeerTable`, `daemonStdout`, `daemonStderr`, `rawLogTail`, `rpcpassword`, and `cookie` values produces a summary containing `resourcePressure`, `blocksInFlight`, and `maxBlocksInFlightTotal` but not the raw field names or their raw values.
  </behavior>
  <action>Add the two tests above without changing production support output. Use `serde_json::json!` fixtures inside the existing modules. Implement per D-01 and D-02 by reusing existing compact support summaries and allowlists, not by creating a parallel resource model or copying raw live-smoke/log material into the bundle.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_support_redaction_names_compact_evidence_bounds --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_live_smoke_summary_is_allowlisted_and_bounded --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase71_support_redaction_names_compact_evidence_bounds|raw unbounded log contents|live smoke reports are summarized from allowlisted fields only" packages/open-bitcoin-cli/src/operator/support.rs` finds all three strings.
    - `rg -n "phase71_live_smoke_summary_is_allowlisted_and_bounded|rawPeerTable|daemonStdout|maxBlocksInFlightTotal" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` finds all four strings.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_ --all-features` passes.
  </acceptance_criteria>
  <done>Support bundle redaction and live-smoke summary tests prove compact allowlisted evidence and reject raw peer/log/stdout/stderr/credential material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime support renders all configured resource bounds</name>
  <files>packages/open-bitcoin-cli/src/operator/runtime/support.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - packages/open-bitcoin-cli/src/operator/runtime/support.rs
    - packages/open-bitcoin-node/src/status.rs
    - .planning/phases/61-resource-bounds-and-recovery-taxonomy/61-06-SUMMARY.md
  </read_first>
  <behavior>
    - Test `phase71_runtime_support_resource_pressure_lists_all_configured_bounds`: support text for an available `SyncResourcePressure` includes the exact line `Resource pressure: header_requests_in_flight_per_peer=1 headers_per_message=2000 blocks_in_flight=8/16/64 messages_per_peer=64 sync_rounds=8 outbound_peers=2/4`.
    - The same test asserts `Bounded counters: messages_processed=7 headers_received=3 blocks_received=1` stays present so resource evidence and progress counters remain compact adjacent facts.
  </behavior>
  <action>Add the named test in the existing test module. If the existing support fixture already has the target values, extend that fixture instead of duplicating setup. Keep the output sourced from `sync_pressure_text(&SyncResourcePressure)` per D-01 and D-13; do not add renderer-local resource labels or raw arrays.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_runtime_support_resource_pressure_lists_all_configured_bounds --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase71_runtime_support_resource_pressure_lists_all_configured_bounds|Resource pressure: header_requests_in_flight_per_peer=1 headers_per_message=2000 blocks_in_flight=8/16/64 messages_per_peer=64 sync_rounds=8 outbound_peers=2/4|Bounded counters: messages_processed=7 headers_received=3 blocks_received=1" packages/open-bitcoin-cli/src/operator/runtime/support.rs` finds all three strings.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_runtime_support_resource_pressure_lists_all_configured_bounds --all-features` passes.
  </acceptance_criteria>
  <done>Runtime support evidence exposes all existing `SyncResourcePressure` fields as one compact bounded line with bounded progress counters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-smoke report -> support bundle | Local JSON report contents are untrusted input to support evidence generation. |
| durable status -> operator support output | Persisted local status can contain peer and runtime evidence that must remain bounded and redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01-01 | I | `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` | mitigate | Add an allowlist regression proving raw peer/log/stdout/stderr/credential fields are omitted from summaries. |
| T-71-01-02 | D | `packages/open-bitcoin-cli/src/operator/runtime/support.rs` | mitigate | Assert runtime support renders configured caps from `SyncResourcePressure` instead of unbounded peer or log arrays. |
| T-71-01-03 | R | support evidence rendering | mitigate | Keep exact redaction and safeguard strings under test so support output can be audited after changes. |
</threat_model>

<verification>
Run focused CLI tests for `phase71_`, then run the aggregate verifier in Plan 71-04 after all Phase 71 code and docs are complete.
</verification>

<success_criteria>
- RES-01 support-evidence compactness is covered by named deterministic tests.
- Tests prove raw live-smoke fields are not copied into support summaries.
- Resource-pressure support output lists all existing configured sync bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/71-resource-bounds-and-durable-restart-resume/71-01-SUMMARY.md`.
</output>
