---
phase: 71-resource-bounds-and-durable-restart-resume
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/storage.rs
  - packages/open-bitcoin-node/src/storage/fjall_store.rs
  - packages/open-bitcoin-node/src/sync/types/recovery.rs
  - packages/open-bitcoin-node/src/sync/progress.rs
autonomous: true
requirements: [RES-03]
user_setup: []
must_haves:
  truths:
    - "Storage-first recovery guidance distinguishes schema mismatch, corruption, lock contention, low disk, and storage pressure."
    - "Low disk and storage pressure map to typed resource exhaustion with precise free-disk guidance."
    - "Recovery guidance does not imply automatic repair, reindex, source datadir mutation, or hidden storage mutation."
  artifacts:
    - path: "packages/open-bitcoin-node/src/storage.rs"
      provides: "Typed storage recovery action and category mapping"
      contains: "FreeDisk"
    - path: "packages/open-bitcoin-node/src/storage/fjall_store.rs"
      provides: "Backend storage error action selection"
      contains: "for_backend_message"
    - path: "packages/open-bitcoin-node/src/sync/types/recovery.rs"
      provides: "Error-detail recovery category mapping"
      contains: "no space left on device"
    - path: "packages/open-bitcoin-node/src/sync/progress.rs"
      provides: "No-progress storage/resource next-action guidance"
      contains: "free disk"
  key_links:
    - from: "packages/open-bitcoin-node/src/storage.rs"
      to: "packages/open-bitcoin-node/src/sync/types/recovery.rs"
      via: "StorageError::recovery_category feeds SyncRuntimeError::recovery_category"
      pattern: "Self::Storage\\(error\\) => error.recovery_category\\(\\)"
    - from: "packages/open-bitcoin-node/src/sync/progress.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "NoProgressDiagnosis::StorageOrResourceBlocked next action"
      pattern: "NoProgressDiagnosis::StorageOrResourceBlocked"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 71-2026-06-13T10-34-37
generated_at: 2026-06-13T10:57:42Z
---

<objective>
Refine typed storage-pressure recovery so low disk and storage pressure produce precise, storage-first operator guidance.

Purpose: RES-03 requires schema mismatch, corruption markers, lock contention, low disk, and storage pressure to remain typed and actionable without hidden data mutation.
Output: Storage action/category mapping, backend pressure detection, recovery-detail classification, and no-progress guidance tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-02-SUMMARY.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-05-SUMMARY.md
@packages/open-bitcoin-node/src/storage.rs
@packages/open-bitcoin-node/src/storage/fjall_store.rs
@packages/open-bitcoin-node/src/sync/types/recovery.rs
@packages/open-bitcoin-node/src/sync/progress.rs

<interfaces>
From packages/open-bitcoin-node/src/storage.rs:
```rust
pub enum StorageRecoveryAction {
    Restart,
    Reindex,
    Repair,
    RestoreFromBackup,
}

impl StorageRecoveryAction {
    pub const fn recovery_category(self) -> SyncRecoveryCategory;
    pub const fn operator_message(self) -> &'static str;
}

pub enum StorageError {
    BackendFailure {
        namespace: StorageNamespace,
        message: String,
        action: StorageRecoveryAction,
    },
}
```

From packages/open-bitcoin-node/src/sync/progress.rs:
```rust
pub(super) const fn no_progress_next_action(diagnosis: NoProgressDiagnosis) -> &'static str;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map low disk and storage pressure to typed free-disk recovery</name>
  <files>packages/open-bitcoin-node/src/storage.rs, packages/open-bitcoin-node/src/storage/fjall_store.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/storage/fjall_store.rs
    - .planning/phases/61-resource-bounds-and-recovery-taxonomy/61-02-SUMMARY.md
  </read_first>
  <behavior>
    - `StorageRecoveryAction::FreeDisk` serializes as `free_disk`, maps to `SyncRecoveryCategory::ResourceExhaustion`, and returns exactly `Free disk space for the selected datadir, then retry sync.` from `operator_message()`.
    - `StorageRecoveryAction::for_backend_message(message: &str)` returns `FreeDisk` for `no space left on device`, `ENOSPC`, `disk full`, `low disk`, and `storage pressure`; it returns `Restart` for `block flush failed`.
    - `StorageError::BackendFailure { message, action: FreeDisk }` returns `SyncRecoveryCategory::ResourceExhaustion`.
    - `storage/fjall_store.rs` local `backend_failure` uses `StorageRecoveryAction::for_backend_message(&message)` instead of always using `StorageRecoveryAction::Restart`.
  </behavior>
  <action>Add the `FreeDisk` variant and `for_backend_message` helper. Keep schema mismatch, corruption, and lock contention behavior unchanged. Implement D-07, D-08, and D-09: low-disk/storage-pressure evidence maps to `resource_exhaustion` with precise free-disk guidance, while schema/corruption/lock categories retain storage-first precedence and no automatic mutation is implied.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category_maps_low_disk_and_storage_pressure --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "FreeDisk|free_disk|Free disk space for the selected datadir, then retry sync\\.|for_backend_message|no space left on device|ENOSPC|disk full|low disk|storage pressure" packages/open-bitcoin-node/src/storage.rs` finds all nine patterns.
    - `rg -n "StorageRecoveryAction::for_backend_message" packages/open-bitcoin-node/src/storage/fjall_store.rs` finds the backend action selection.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category_maps_low_disk_and_storage_pressure --all-features` passes.
  </acceptance_criteria>
  <done>Low disk and storage pressure are typed as resource exhaustion with a precise free-disk recovery action, while existing storage categories remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refine recovery-detail and no-progress guidance for storage pressure</name>
  <files>packages/open-bitcoin-node/src/sync/types/recovery.rs, packages/open-bitcoin-node/src/sync/progress.rs</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - packages/open-bitcoin-node/src/sync/types/recovery.rs
    - packages/open-bitcoin-node/src/sync/progress.rs
    - packages/open-bitcoin-node/src/status/recovery.rs
  </read_first>
  <behavior>
    - `recovery_category_from_error_detail` returns `SyncRecoveryCategory::ResourceExhaustion` for `no space left on device`, `ENOSPC`, `disk full`, `low disk`, and `storage pressure` before generic backend failure checks.
    - `no_progress_next_action(NoProgressDiagnosis::StorageOrResourceBlocked)` returns exactly `Inspect storage health, free disk when storage pressure is reported, close competing storage handles, or increase bounded resource limits.`
    - Existing tests for schema mismatch, store corruption, storage lock contention, storage backend failure, and resource exhaustion still pass.
  </behavior>
  <action>Add pressure signals to recovery-detail classification and update the bounded no-progress action string. Do not add renderer-local recovery text; keep guidance in shared sync progress code per D-03 and D-07.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase70_no_progress --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "no space left on device|ENOSPC|disk full|low disk|storage pressure|ResourceExhaustion" packages/open-bitcoin-node/src/sync/types/recovery.rs` finds all six patterns.
    - `rg -n "Inspect storage health, free disk when storage pressure is reported, close competing storage handles, or increase bounded resource limits\\." packages/open-bitcoin-node/src/sync/progress.rs` finds the exact guidance string.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase70_no_progress --all-features` passes.
  </acceptance_criteria>
  <done>Recovery-detail parsing and no-progress guidance distinguish storage pressure from generic failure while preserving the shared typed recovery path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage backend error -> recovery category | Backend error strings are untrusted and must be classified conservatively. |
| recovery category -> operator next action | Operator guidance must not suggest unsafe mutation or hide storage blockers behind peer retry advice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | D | storage backend pressure | mitigate | Map low disk and storage pressure to `resource_exhaustion` and `FreeDisk` guidance. |
| T-71-03-02 | T | recovery action selection | mitigate | Keep schema, corruption, and lock categories storage-first and under regression tests. |
| T-71-03-03 | R | operator diagnosis | mitigate | Shared no-progress guidance names free disk, competing handles, storage health, and bounded limits without implying automatic repair. |
| T-71-03-04 | E | hidden mutation | mitigate | Do not add repair, reindex, source datadir, or wallet mutation behavior; only classify and guide. |
</threat_model>

<verification>
Run focused storage and recovery tests, then include these changes in the full Phase 71 checker and `bash scripts/verify.sh` in Plan 71-04.
</verification>

<success_criteria>
- RES-03 low disk and storage pressure are typed and actionable.
- Storage-first precedence remains intact for schema mismatch, corruption, and lock contention.
- No hidden mutation or automatic repair behavior is added.
</success_criteria>

<output>
After completion, create `.planning/phases/71-resource-bounds-and-durable-restart-resume/71-03-SUMMARY.md`.
</output>
