---
phase: 71-resource-bounds-and-durable-restart-resume
plan: 04
type: execute
wave: 2
depends_on: [71-01, 71-02, 71-03]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/architecture/storage-decision.md
  - docs/parity/catalog/p2p.md
  - docs/parity/catalog/chainstate.md
  - scripts/check-phase71-resource-restart.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]
user_setup: []
must_haves:
  truths:
    - "Operator docs describe bounded long sync, safe same-datadir resume, diagnosed storage/resource blockers, and deferred production-node scope."
    - "Architecture docs name exact resource, restart/resume, and storage-pressure evidence contracts."
    - "Parity docs scope Phase 71 to outbound full-sync review without inbound serving, relay, production-wallet, migration apply mode, packaging, GUI, hosted dashboard, or broad production-node claims."
    - "Default verification runs a deterministic Phase 71 checker and remains public-network-free."
  artifacts:
    - path: "scripts/check-phase71-resource-restart.ts"
      provides: "Deterministic Phase 71 source/docs/test/default-verification checker"
      contains: "validated Phase 71 resource/restart evidence"
    - path: "scripts/verify.sh"
      provides: "Repo-native checker wiring"
      contains: "bun run scripts/check-phase71-resource-restart.ts"
    - path: "docs/operator/runtime-guide.md"
      provides: "Operator-facing Phase 71 resource and restart/resume guidance"
      contains: "Phase 71 resource bounds and restart/resume proof"
    - path: "docs/architecture/status-snapshot.md"
      provides: "Shared status contract guidance"
      contains: "phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight"
  key_links:
    - from: "scripts/check-phase71-resource-restart.ts"
      to: "packages/open-bitcoin-node/src/sync/tests.rs"
      via: "checker requires exact phase71 test names"
      pattern: "phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase71-resource-restart.ts"
      via: "default deterministic verification"
      pattern: "bun run scripts/check-phase71-resource-restart.ts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 71-2026-06-13T10-34-37
generated_at: 2026-06-13T10:57:42Z
---

<objective>
Close Phase 71 by documenting the resource/restart truth contract and wiring a deterministic checker into repo verification.

Purpose: RES-01 through RES-04 require auditable operator guidance and deterministic verification while public-network full-sync review remains opt-in.
Output: Operator/architecture/parity docs, `scripts/check-phase71-resource-restart.ts`, `scripts/verify.sh` wiring, and refreshed LOC metrics.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
@.planning/phases/70-reorg-peer-rotation-and-no-progress-recovery/70-VERIFICATION.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/architecture/storage-decision.md
@docs/parity/catalog/p2p.md
@docs/parity/catalog/chainstate.md
@scripts/check-phase70-reorg-recovery.ts
@scripts/verify.sh

<interfaces>
From scripts/check-phase70-reorg-recovery.ts:
```ts
function requireContains(text: string, needle: string, label: string, failures: string[]): void;
function requireNotContains(text: string, needle: string, label: string, failures: string[]): void;
async function readJoined(files: readonly string[], failures: string[]): Promise<string>;
```

Checker output convention:
```text
validated Phase 71 resource/restart evidence
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Phase 71 resource, restart, and storage-pressure evidence</name>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md, docs/architecture/storage-decision.md, docs/parity/catalog/p2p.md, docs/parity/catalog/chainstate.md</files>
  <read_first>
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/operator/runtime-guide.md
    - docs/architecture/status-snapshot.md
    - docs/architecture/operator-observability.md
    - docs/architecture/storage-decision.md
    - docs/parity/catalog/p2p.md
    - docs/parity/catalog/chainstate.md
  </read_first>
  <action>Update the listed docs with concrete Phase 71 wording. Add a `### Phase 71 resource bounds and restart/resume proof` heading to `docs/operator/runtime-guide.md`. Under that heading include these exact strings: `peers, in-flight blocks, request queues, retry maps, cache retention, synchronous storage writes, metrics retention, structured log retention, and support evidence compactness`; `same-datadir resume matrix: clean shutdown, unclean shutdown, mid-download interruption, mid-connect interruption, stale in-flight cleanup`; `Free disk space for the selected datadir, then retry sync.`; `phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network`; and `phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight`. In architecture docs, name `SyncResourcePressure`, `SyncRecoveryCategory::ResourceExhaustion`, `StorageRecoveryAction::FreeDisk`, `MetricRetentionPolicy`, and `LogRetentionPolicy`. In parity docs, state that Phase 71 remains outbound explicit opt-in full-sync review and does not add inbound serving, address relay, block serving, transaction relay, compact block relay, production-funds wallet claims, migration apply mode, signed packaging, Windows service support, GUI, hosted dashboards, or broad production-node readiness. Implement D-12, D-13, and D-14; do not add public-network commands to default verification docs.</action>
  <verify>
    <automated>rg -n "Phase 71 resource bounds and restart/resume proof|same-datadir resume matrix: clean shutdown, unclean shutdown, mid-download interruption, mid-connect interruption, stale in-flight cleanup|Free disk space for the selected datadir, then retry sync\\.|phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/architecture/storage-decision.md docs/parity/catalog/p2p.md docs/parity/catalog/chainstate.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 71 resource bounds and restart/resume proof" docs/operator/runtime-guide.md` finds the heading.
    - `rg -n "SyncResourcePressure|SyncRecoveryCategory::ResourceExhaustion|StorageRecoveryAction::FreeDisk|MetricRetentionPolicy|LogRetentionPolicy" docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/architecture/storage-decision.md` finds all five strings.
    - `rg -n "inbound serving|address relay|block serving|transaction relay|compact block relay|production-funds wallet|migration apply mode|signed packaging|Windows service support|GUI|hosted dashboards|broad production-node readiness" docs/parity/catalog/p2p.md docs/parity/catalog/chainstate.md` finds the deferred scope terms.
  </acceptance_criteria>
  <done>Docs explain exactly what Phase 71 proves, how operators interpret storage/resource blockers, which deterministic tests prove it, and which production surfaces remain out of scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the deterministic Phase 71 checker</name>
  <files>scripts/check-phase71-resource-restart.ts</files>
  <read_first>
    - scripts/check-phase70-reorg-recovery.ts
    - scripts/verify.sh
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-CONTEXT.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-RESEARCH.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-01-PLAN.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-02-PLAN.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-03-PLAN.md
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-04-PLAN.md
  </read_first>
  <behavior>
    - Checker requires plan files `71-01-PLAN.md`, `71-02-PLAN.md`, `71-03-PLAN.md`, and `71-04-PLAN.md`.
    - Checker requires requirement IDs `RES-01`, `RES-02`, `RES-03`, and `RES-04` in plan text.
    - Checker requires source/test needles: `phase71_support_redaction_names_compact_evidence_bounds`, `phase71_live_smoke_summary_is_allowlisted_and_bounded`, `phase71_runtime_support_resource_pressure_lists_all_configured_bounds`, `phase71_same_datadir_resume_matrix_covers_clean_unclean_mid_download_mid_connect_and_stale_inflight`, `phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network`, `StorageRecoveryAction::FreeDisk`, `for_backend_message`, and `Free disk space for the selected datadir, then retry sync.`
    - Checker requires docs needles from Task 1.
    - Checker defines helper assertions that Task 3 will use to verify `scripts/verify.sh` runs `bun run scripts/check-phase71-resource-restart.ts` after `bun run scripts/check-phase70-reorg-recovery.ts`.
    - Checker defines helper assertions that Task 3 will use to reject default verification strings `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl`, `launchctl`, and `openbitcoinsync=mainnet-ibd` in `scripts/verify.sh`.
  </behavior>
  <action>Create `scripts/check-phase71-resource-restart.ts` using the Phase 70 checker style: `#!/usr/bin/env bun`, `path.resolve(import.meta.dir, "..")`, `readText`, `readJoined`, `requireContains`, `requireNotContains`, a `failures: string[]`, and final output exactly `validated Phase 71 resource/restart evidence`. Use Bun/TypeScript per repo guidance. Keep the checker local and deterministic; it must only read files and inspect strings. Do not make Task 2's verification depend on the Phase 71 `scripts/verify.sh` wiring, because Task 3 adds that wiring.</action>
  <verify>
    <automated>bun --check scripts/check-phase71-resource-restart.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validated Phase 71 resource/restart evidence|RES-01|RES-02|RES-03|RES-04|phase71_synthetic_long_chain_exercises_resource_bounds_without_public_network|StorageRecoveryAction::FreeDisk|requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\"" scripts/check-phase71-resource-restart.ts` finds all eight patterns.
    - `bun --check scripts/check-phase71-resource-restart.ts` exits 0.
  </acceptance_criteria>
  <done>The Phase 71 checker script is present and syntax-valid; Task 3 wires it into default verification and runs it end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Wire checker into repo verification and refresh generated LOC</name>
  <files>scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - scripts/verify.sh
    - scripts/check-phase71-resource-restart.ts
    - docs/metrics/lines-of-code.md
    - AGENTS.md
  </read_first>
  <action>Add `bun run scripts/check-phase71-resource-restart.ts` to `scripts/verify.sh` immediately after `bun run scripts/check-phase70-reorg-recovery.ts`. Run `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md` after adding the checker so the tracked generated LOC artifact includes the new TypeScript file. Do not add `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl`, `launchctl`, or `openbitcoinsync=mainnet-ibd` to `scripts/verify.sh`.</action>
  <verify>
    <automated>bun run scripts/check-phase71-resource-restart.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bun run scripts/check-phase71-resource-restart\\.ts" scripts/verify.sh` finds the Phase 71 checker call.
    - `node -e 'const fs=require("node:fs"); const text=fs.readFileSync("scripts/verify.sh","utf8"); const phase70=text.indexOf("bun run scripts/check-phase70-reorg-recovery.ts"); const phase71=text.indexOf("bun run scripts/check-phase71-resource-restart.ts"); if (phase70 < 0 || phase71 < 0 || phase71 < phase70) process.exit(1);'` exits 0.
    - `bash -c 'if rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress|systemctl|launchctl|openbitcoinsync=mainnet-ibd" scripts/verify.sh; then exit 1; fi'` exits 0.
    - `rg -n "check-phase71-resource-restart\\.ts" docs/metrics/lines-of-code.md` finds the checker in the generated LOC report.
    - `bash scripts/verify.sh` passes.
  </acceptance_criteria>
  <done>Default verification includes the deterministic Phase 71 checker, excludes public-network/service-manager commands, and has a fresh tracked LOC report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/checker -> release claims | Documentation and checker wording can accidentally broaden the shipped claim. |
| default verification -> public network | `scripts/verify.sh` must stay hermetic and avoid public peers, service managers, and timing-dependent full sync. |
| support evidence docs -> operator action | Recovery guidance must be precise without implying hidden mutation or unsafe repair. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | R | Phase 71 documentation | mitigate | Checker requires exact RES-01 through RES-04 docs/test/source needles. |
| T-71-04-02 | D | `scripts/verify.sh` | mitigate | Checker and acceptance criteria reject public-network, manual-peer, restart-after-progress, system service-manager, and mainnet IBD activation strings. |
| T-71-04-03 | I | support evidence docs | mitigate | Docs preserve redaction/allowlist language and avoid raw logs, raw peer tables, credentials, and wallet material. |
| T-71-04-04 | T | storage recovery wording | mitigate | Docs name `StorageRecoveryAction::FreeDisk` and state no automatic repair or hidden mutation. |
</threat_model>

<verification>
Run:
1. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase71_ --all-features`
2. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase71_ --all-features`
3. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node storage_recovery_category_maps_low_disk_and_storage_pressure --all-features`
4. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_recovery_category --all-features`
5. `bun run scripts/check-phase71-resource-restart.ts`
6. `bash scripts/verify.sh`
</verification>

<success_criteria>
- RES-01, RES-02, RES-03, and RES-04 are all covered by plan frontmatter, tests, docs, and checker needles.
- Phase 71 docs and checker preserve locked decisions D-01 through D-15.
- Default verification remains deterministic and public-network-free.
</success_criteria>

<output>
After completion, create `.planning/phases/71-resource-bounds-and-durable-restart-resume/71-04-SUMMARY.md`.
</output>
