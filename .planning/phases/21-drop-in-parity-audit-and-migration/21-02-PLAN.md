---
phase: 21-drop-in-parity-audit-and-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - packages/open-bitcoin-cli/src/operator/migration.rs
  - packages/open-bitcoin-cli/src/operator/migration/tests.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - packages/open-bitcoin-cli/tests/operator_flows.rs
autonomous: true
requirements:
  - CLI-07
  - WAL-08
  - MIG-03
  - MIG-04
  - MIG-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-04-27T23-11-20
generated_at: 2026-04-27T23:11:20.765Z
must_haves:
  truths:
    - "Binary-level migration dry runs can inspect realistic sandboxed Core/Knots evidence without mutating it."
    - "Relevant migration notices stay synchronized with the parity ledger."
    - "Migration output remains secret-safe while still showing paths and uncertainty."
---

<objective>
Prove the migration planner behaves correctly end to end and make the parity-difference notices auditable. This plan owns the binary-level dry-run test, the docs-sync guard for migration notices, and the final output polish needed for `MIG-05`.

Purpose: close the executable safety and parity-notice loop for `CLI-07`, `WAL-08`, `MIG-03`, `MIG-04`, and `MIG-05`.
Output: sandboxed migration binary coverage plus tests that keep runtime notices in sync with the parity ledger.
</objective>

<context>
@.planning/phases/21-drop-in-parity-audit-and-migration/21-CONTEXT.md
@.planning/phases/21-drop-in-parity-audit-and-migration/21-RESEARCH.md
@.planning/phases/21-drop-in-parity-audit-and-migration/21-01-PLAN.md
@packages/open-bitcoin-cli/src/operator/migration.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
@packages/open-bitcoin-cli/tests/operator_flows.rs
@docs/parity/index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an end-to-end dry-run migration binary test</name>
  <files>
    packages/open-bitcoin-cli/tests/operator_binary.rs
  </files>
  <action>
    1. Reuse the existing temp-home sandbox pattern to seed a realistic detected Core/Knots install.
    2. Run `open-bitcoin migrate plan` against that sandbox.
    3. Assert:
       - the plan succeeds
       - explanation sections and action groups are rendered
       - wallet/config/service evidence is visible
       - source files remain byte-for-byte unchanged after the run
       - secrets such as cookie contents do not appear in stdout
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary -- --nocapture</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Keep migration notices in sync with the parity ledger</name>
  <files>
    packages/open-bitcoin-cli/src/operator/migration.rs,
    packages/open-bitcoin-cli/src/operator/migration/tests.rs,
    packages/open-bitcoin-cli/tests/operator_flows.rs
  </files>
  <action>
    1. Add the minimal runtime notice model needed by migration output.
    2. Keep the command runtime self-contained for installed binaries by embedding the notice data in code rather than reading repo docs at runtime.
    3. Add tests that read `docs/parity/index.json` from the repo root and assert the runtime notice ids and summaries stay synchronized with the parity ledger records.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_flows -- --nocapture</automated>
  </verify>
</task>

</tasks>
