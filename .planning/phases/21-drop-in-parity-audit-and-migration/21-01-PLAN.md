---
phase: 21-drop-in-parity-audit-and-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/migration.rs
  - packages/open-bitcoin-cli/src/operator/migration/tests.rs
  - packages/open-bitcoin-cli/src/operator/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - WAL-08
  - MIG-03
  - MIG-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-04-27T23-11-20
generated_at: 2026-04-27T23:11:20.765Z
must_haves:
  truths:
    - "A new operator-owned `migrate plan` command exists without repurposing onboarding."
    - "Migration planning is dry-run only and never mutates source installs."
    - "Planner logic is testable without filesystem mutation or subprocesses."
    - "Source ambiguity stays explicit instead of being guessed away."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/migration.rs"
      provides: "migration plan types, explanation rendering, action groups, and pure planner helpers"
    - path: "packages/open-bitcoin-cli/src/operator/tests.rs"
      provides: "CLI parsing and routing coverage for `migrate plan`"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator.rs"
      to: "packages/open-bitcoin-cli/src/operator/migration.rs"
      via: "new clap command and argument types"
    - from: "packages/open-bitcoin-cli/src/operator/runtime.rs"
      to: "packages/open-bitcoin-cli/src/operator/migration.rs"
      via: "runtime dispatch with shared detection/config inputs"
---

<objective>
Introduce the Phase 21 migration planner as a pure, dry-run-first operator surface. This plan owns the new command shape, planner types, explanation section, action-group rendering, and focused unit tests before later plans add parity-audit docs and broader end-to-end proof.

Purpose: satisfy the core CLI and planning half of `WAL-08`, `MIG-03`, and `MIG-04` without mixing in doc-ledger closeout.
Output: `migrate plan` command contract, pure migration planner helpers, and tests for dry-run safety, ambiguity handling, and redaction.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-drop-in-parity-audit-and-migration/21-CONTEXT.md
@.planning/phases/21-drop-in-parity-audit-and-migration/21-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@docs/architecture/cli-command-architecture.md
@docs/architecture/config-precedence.md
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/src/operator/detect.rs
@packages/open-bitcoin-cli/src/operator/onboarding.rs
@packages/open-bitcoin-cli/src/operator/service.rs
@packages/open-bitcoin-cli/src/operator/wallet.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `migrate plan` CLI contract and failing planner tests</name>
  <files>
    packages/open-bitcoin-cli/src/operator.rs,
    packages/open-bitcoin-cli/src/operator/migration.rs,
    packages/open-bitcoin-cli/src/operator/migration/tests.rs,
    packages/open-bitcoin-cli/src/operator/tests.rs,
    docs/parity/source-breadcrumbs.json
  </files>
  <action>
    1. Add a new operator command branch for `migrate`.
    2. Define migration-specific args for at least `plan` and `--source-datadir`.
    3. Create the pure planner types in `operator/migration.rs` with:
       - explanation sections for benefits, tradeoffs, unsupported surfaces, rollback, and backup requirements
       - action groups for config, files, service, wallet, and operator follow-up
       - typed outcomes for unambiguous and ambiguous source selection
    4. Write failing tests first for:
       - dry-run output includes the explanation section and action groups
       - planner leaves cookie contents and raw wallet data out of rendered output
       - ambiguous detection produces explicit manual-review guidance
       - the new clap routing parses `open-bitcoin migrate plan`
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli migration:: -- --nocapture</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire runtime dispatch without adding a source-mutation path</name>
  <files>
    packages/open-bitcoin-cli/src/operator.rs,
    packages/open-bitcoin-cli/src/operator/runtime.rs,
    packages/open-bitcoin-cli/src/operator/migration.rs,
    packages/open-bitcoin-cli/src/operator/migration/tests.rs
  </files>
  <action>
    1. Dispatch `migrate plan` from `operator/runtime.rs` using the existing shared config resolution and install detections.
    2. Preserve the source-vs-target boundary: global operator flags remain the Open Bitcoin target environment; migration-specific source input is separate.
    3. Keep the planner dry-run only. Do not add apply or write branches in this plan.
    4. Render stable human and JSON output through the new migration module.
  </action>
  <verify>
    <automated>cd /Users/peterryszkiewicz/Repos/open-bitcoin && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator:: -- --nocapture</automated>
  </verify>
</task>

</tasks>
