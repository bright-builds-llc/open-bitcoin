---
phase: 133-package-aware-download-and-orphan-bridge
plan: "03"
type: execute
wave: 3
depends_on:
  - "133-02"
files_modified:
  - packages/open-bitcoin-mempool/src/package/report.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PPKG-02
  - PPKG-03
user_setup: []
must_haves:
  truths:
    - "The node refines each neutral pair once through `WellFormedPackage` and `SubmissionPackage`, then calls the Phase 132 package engine exactly once"
    - "The exact cached `PackageFingerprint`, ordered `PackageReport`, and committed `MempoolLifecycleDelta` survive the bridge without flattening or recomputation"
    - "Hard, terminal, missing-input, other reconsiderable, and failed-package feedback update only candidate/reject/orphan correctness state"
    - "Package admission does not project serving, fanout, compact, persistence, unbroadcast, retry, or operator effects before Phase 134"
  artifacts:
    - path: "packages/open-bitcoin-node/src/mempool.rs"
      provides: "Thin authoritative package submission adapter"
      contains: "submit_package"
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Candidate refinement, one authoritative call, exact result preservation, and exhaustive feedback"
      contains: "SubmittedPackageResult"
    - path: "packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs"
      provides: "Arrival-order, provenance, exact-call, feedback, and no-projection integration proof"
      contains: "same_peer"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-mempool/src/pool/package_admission.rs"
      via: "`ManagedMempool::submit_package` exactly once per candidate"
      pattern: "submit_package"
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      via: "typed feedback only; no network-local package policy"
      pattern: "record.*feedback"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 133-2026-07-26T16-12-51
generated_at: 2026-07-26T16:59:01Z
---

<objective>
Refine neutral peer candidates in the node shell, submit them once to the authoritative Phase 132 engine, and feed exact typed results back into bounded candidate state.

Purpose: Complete PPKG-02 and PPKG-03 without duplicating package policy or prematurely implementing Phase 134 lifecycle projection.
Output: Exact missing-parent reports, managed package adapter, package bridge result, exhaustive feedback, and integration tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-CONTEXT.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-RESEARCH.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-02-SUMMARY.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-01-SUMMARY.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-04-SUMMARY.md
@.planning/phases/132-typed-package-vocabulary-and-staged-admission/132-05-SUMMARY.md
@packages/open-bitcoin-mempool/src/package.rs
@packages/open-bitcoin-mempool/src/package/report.rs
@packages/open-bitcoin-mempool/src/pool/package_admission.rs
@packages/open-bitcoin-node/src/network/admission_bridge.rs

<interfaces>
Authoritative Phase 132 call:
```rust
pub fn submit_package(
    &mut self,
    command: SubmitPackageCommand,
    chainstate: &ChainstateSnapshot,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<SubmittedPackageResult, MempoolError>;
```

Required bridge preservation:
```rust
pub(super) struct ManagedPeerPackageAdmission {
    pub origins: [PeerId; 2],
    pub submitted: SubmittedPackageResult,
}
```

`submitted.report.fingerprint()`, `submitted.report.members()`, and `submitted.delta` remain authoritative. Network feedback receives only typed identities, fingerprint bytes, missing parents, and terminal/reconsiderable classifications.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry exact remaining parents in missing-input reports</name>
  <files>packages/open-bitcoin-mempool/src/package/report.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-mempool/src/package/report.rs (`ReconsiderableMemberFailure::MissingInputs`, identity accessors)
    - packages/open-bitcoin-mempool/src/pool/package_admission.rs (`evaluate_singleton`, prospective overlay, missing-input mapping)
    - packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs (Phase 132 ordered report/oracle cases)
    - packages/open-bitcoin-mempool/src/outcome.rs (`MempoolOutcome::Orphaned` exact parent precedent)
    - packages/bitcoin-knots/src/validation.cpp (package missing-input result semantics)
  </read-first>
  <behavior>
    - `MissingInputs` contains a non-empty deterministic deduplicated `Vec<Txid>` of parents still absent after considering chainstate and earlier staged package members
    - In-package accepted parents are not reported missing
    - Multiple external missing parents remain exact and request-order independent
    - Report identity and fee-group invariants remain unchanged
  </behavior>
  <action>
    Extend `ReconsiderableMemberFailure::MissingInputs` with `missing_parents: Vec<Txid>` so D-12 restaging never guesses. During package evaluation, collect every distinct external parent txid whose referenced output is absent from the prospective overlay/chainstate after earlier members are staged; sort/deduplicate deterministically and require non-empty. Preserve `requested` identity and all existing report opacity/accessors.

    Add focused package-parity tests for one/multiple remaining parents, parent satisfied by an earlier member, and no false inclusion from unrelated inputs. Update every match site exhaustively; do not add an optional field or infer from display strings.
  </action>
  <acceptance-criteria>
    - `rg -n "MissingInputs \\{|missing_parents: Vec<Txid>" packages/open-bitcoin-mempool/src/package/report.rs` returns the exact typed payload
    - `rg -n "missing_parents|sort|dedup|prospective|chainstate" packages/open-bitcoin-mempool/src/pool/package_admission.rs` returns deterministic computation
    - `rg -n "multiple.*missing|earlier.*parent|missing_parents" packages/open-bitcoin-mempool/src/pool/tests/package_parity_cases.rs` returns all cases
    - `bun run scripts/command-timings.ts run --key phase133-missing-parent-report -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_parity_cases` exits 0
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-missing-parent-report -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool package_parity_cases</automated>
  </verify>
  <done>Authoritative package reports carry exact remaining parents for deterministic restaging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Submit one refined candidate and preserve exact authoritative truth</name>
  <files>packages/open-bitcoin-node/src/mempool.rs, packages/open-bitcoin-node/src/network/types.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/action_translation.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-node/src/mempool.rs (`ManagedMempool` singleton adapter pattern)
    - packages/open-bitcoin-node/src/network/types.rs (`ManagedNetworkError` conversions)
    - packages/open-bitcoin-node/src/network/admission_bridge.rs (`process_peer_transaction_admission`, singleton/orphan bridge, old reject helper)
    - packages/open-bitcoin-node/src/network/action_translation.rs (`ReceivedTransaction` handling and relay-fanout call)
    - packages/open-bitcoin-node/src/network/tests.rs (focused package bridge test registry)
    - packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs (new focused exact-call/report/no-projection test target)
    - packages/open-bitcoin-mempool/src/package.rs (`WellFormedPackage`, `SubmissionPackage`, `SubmitPackageCommand`, `SubmittedPackageResult`)
    - packages/open-bitcoin-mempool/src/package/shape.rs (only legal refinements)
    - packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs (Plan 02 opaque candidate/cursor)
  </read-first>
  <behavior>
    - Neutral candidate is consumed into exactly two ordered transactions and two aligned origins
    - `WellFormedPackage::try_from` and `SubmissionPackage::try_from_package` execute once at the node boundary
    - Candidate admission calls `ManagedMempool::submit_package` exactly once and returns the same report fingerprint and delta
    - Existing peer singleton classification uses the same typed Phase 132 report vocabulary rather than guessing from `RelayFeeTooLow`
    - Action translation does not call relay fanout or serving projection for a package result
  </behavior>
  <action>
    Add a thin `ManagedMempool::submit_package` adapter that passes `SubmitPackageCommand` to `Mempool::submit_package` with the current immutable chain snapshot and returns `SubmittedPackageResult` unchanged. Add a typed `ManagedNetworkError` conversion for package-shape/refinement failures.

    Refactor peer singleton evaluation to use a one-member `WellFormedPackage`/`SubmissionPackage` through the authoritative package engine so hard versus reconsiderable classification comes from `PackageMemberResult`, never from `MempoolError` strings or coarse `MempoolOutcomeLabel::RelayFeeTooLow`. Preserve current singleton outward compatibility by mapping the typed singleton report/delta at the node shell only.

    For a reconsiderable parent, consume the Plan 02 candidate, construct `[parent, child]` once, refine once, copy the cached fingerprint only via `fingerprint()/as_bytes()`, and call the adapter exactly once per non-suppressed candidate per D-08/D-11. Store the exact `SubmittedPackageResult` and aligned origins in `ManagedPeerPackageAdmission`; do not flatten it to `MempoolOutcome`. If the cached fingerprint already exists in reconsiderable package evidence, advance the bounded cursor without calling the mempool.

    Implement D-13 explicitly: update action translation with singleton versus package branches, preserve the package `SubmittedPackageResult` for Phase 134, and perform no package-delta projection in Phase 133. Existing singleton behavior may retain its current projection, but the package branch must not call relay fanout, serving, compact reconstruction, persistence dirtiness/snapshots, peer known/request projection, unbroadcast/retry state, or operator evidence adapters; Phase 134 owns those consequences and Phase 136 owns fanout/receipts. Remove `note_recent_reject_for_outcome`.

    Create and register an initial focused `package_bridge_cases` Arrange/Act/Assert test that drives one neutral candidate through the node boundary and proves exactly one package-engine call, identity-equal report fingerprint/member ordering, delta equality, and unchanged serving/fanout/compact/persistence/unbroadcast/retry projections. Task 3 extends this same module with the full feedback and arrival-order matrix.
  </action>
  <acceptance-criteria>
    - `rg -n "pub fn submit_package|SubmittedPackageResult|SubmitPackageCommand" packages/open-bitcoin-node/src/mempool.rs` returns the thin adapter
    - `rg -n "WellFormedPackage::try_from|SubmissionPackage::try_from_package|submit_package|ManagedPeerPackageAdmission" packages/open-bitcoin-node/src/network/admission_bridge.rs` returns the one refinement/call path
    - `! rg -n "RelayFeeTooLow|note_recent_reject_for_outcome|reason\\.contains|to_string\\(\\).*reconsider" packages/open-bitcoin-node/src/network/admission_bridge.rs` confirms no heuristic classification
    - `rg -n "Package.*=>|package_admission" packages/open-bitcoin-node/src/network/action_translation.rs` returns an explicit no-fanout package branch
    - `rg -n "^mod package_bridge_cases;$" packages/open-bitcoin-node/src/network/tests.rs` returns the focused test registry entry
    - `rg -n "one.*submit|exact.*report|exact.*fingerprint|no.*projection" packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs` returns the behavioral proof
    - `bun run scripts/command-timings.ts run --key phase133-node-package-behavior -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node package_bridge_cases` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-node-package-behavior -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node package_bridge_cases</automated>
  </verify>
  <done>The node is the sole candidate-to-package composition point and preserves exact Phase 132 report/fingerprint/delta truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply exhaustive bounded feedback and integration proof</name>
  <files>packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-node/src/network/admission_bridge.rs (Task 2 result and network feedback APIs)
    - packages/open-bitcoin-node/src/network/tests.rs (split test-module registry)
    - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs (fixture helpers to reuse)
    - packages/open-bitcoin-mempool/src/package/report.rs (complete member/status variants)
    - packages/open-bitcoin-network/src/peer/transaction_relay/reject_evidence.rs (hard/reconsiderable transaction/package methods)
    - packages/bitcoin-knots/test/functional/p2p_opportunistic_1p1c.py (both arrival orders, wrong peer, failed fingerprint, two-parent boundary)
    - docs/parity/source-breadcrumbs.json (new test registration)
  </read-first>
  <behavior>
    - Finally-present, already-present, witness-alias, hard-rejected, and post-trim-absent members retire the candidate
    - Hard-rejected inserts only requested wtxid into hard evidence
    - Missing-input restages exact remaining parents with retained announcers; package-fee/replacement insert requested wtxid only into reconsiderable evidence
    - Partial/failed exact packages record only the unchanged cached fingerprint and allow newest-first fallback
    - Both arrival orders, different deliverer, wrong peer, newest fallback, sibling/multi-parent exclusion, and exact one-call behavior pass
    - Package delta remains unprojected across all Phase 134/136 surfaces
  </behavior>
  <action>
    Implement D-12 as an exhaustive match over `PackageMemberResult` and `PackageStatus`. Retire terminal members; insert hard wtxid only for `HardRejected`; restage only `MissingInputs` using its exact parent vector and retained announcers; insert PackageFee/PackageReplacement requested wtxids only in reconsiderable transaction evidence. Record the unchanged report fingerprint in reconsiderable package evidence whenever status is `Partial` or `Failed`. Never put bodies/provenance in filters and never punish peers from filter results.

    Create `package_bridge_cases.rs` with focused Arrange/Act/Assert cases for child-first, parent-first retransmission, qualifying announcer different from body deliverer, wrong peer, newest eligible child, failed-fingerprint fallback, multiple parents/grandchildren exclusion, hard versus reconsiderable separation, every feedback variant, exact report/fingerprint/delta preservation, one package submit count, and no serving/fanout/compact/persistence/unbroadcast/retry projection. Use a cfg(test) counter at the managed adapter or mempool test seam to prove one call; do not add a production counter.

    Register the new test module and breadcrumb it to `p2p_opportunistic_1p1c.py`, `net_processing.cpp`, `txdownloadman_impl.cpp`, `txorphanage.cpp`, and `validation.cpp`.
  </action>
  <acceptance-criteria>
    - `rg -n "FinallyPresent|AlreadyPresent|SameTxidDifferentWitness|HardRejected|PostTrimAbsent|MissingInputs|PackageFee|PackageReplacement|PackageStatus::(Partial|Failed)" packages/open-bitcoin-node/src/network/admission_bridge.rs` returns exhaustive feedback
    - `rg -n "child_first|parent_first|different.*deliver|wrong_peer|newest|failed.*fingerprint|multiple_parent|grandchild|one.*submit|no.*fanout|no.*serving" packages/open-bitcoin-node/src/network/tests/package_bridge_cases.rs` returns all integration families
    - `rg -n "^mod package_bridge_cases;$" packages/open-bitcoin-node/src/network/tests.rs` returns the registry entry
    - `rg -n "package_bridge_cases.rs|p2p_opportunistic_1p1c.py" docs/parity/source-breadcrumbs.json` returns direct lineage
    - `bun run scripts/command-timings.ts run --key phase133-package-bridge -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node package_bridge_cases` exits 0 with nonzero tests
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-package-bridge -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node package_bridge_cases</automated>
  </verify>
  <done>Authoritative results drive only bounded candidate correctness feedback, with exact one-call identity/lifecycle truth and no Phase 134/136 projection.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Opaque network candidate → node package types | Provenance-qualified untrusted transactions cross crate boundaries |
| Node bridge → mempool engine | Package policy must have exactly one authority |
| Package report → network feedback | Authoritative identities/classifications alter bounded peer state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-133-20 | Spoofing | candidate origins | mitigate | Consume private Plan 02 candidate with aligned `[peer, peer]`; no caller boolean or field constructor |
| T-133-21 | Tampering | package identity | mitigate | Construct once through Phase 132 types and propagate cached fingerprint; never recompute or peer-qualify |
| T-133-22 | Tampering / Elevation of Privilege | package policy | mitigate | One thin `submit_package` adapter and tests proving exactly one engine call with no node/network policy branches |
| T-133-23 | Denial of Service | failed candidate loop | mitigate | Failed-fingerprint filter plus bounded newest-first cursor capped by per-parent policy |
| T-133-24 | Repudiation / Denial of Service | probabilistic evidence | mitigate | Membership only suppresses work; no punishment, disconnect, or score update |
| T-133-25 | Tampering | lifecycle projection | mitigate | Explicit package branch preserves exact delta and asserts zero serving/fanout/compact/persistence/retry projection |
</threat-model>

<verification>
Run mempool and node focused commands sequentially through `scripts/command-timings.ts`, then run breadcrumb validation and `git diff --check`.
</verification>

<success-criteria>
- Peer candidates receive the exact authoritative Phase 132 report/fingerprint/delta from one call.
- Every D-12 feedback class is exhaustive and bounded.
- PPKG-02/PPKG-03 tests cover both arrival orders and provenance/identity boundaries.
- No package policy duplication, coarse fee heuristic, package wire protocol, multi-parent assembly, fanout, receipt, or full lifecycle projection is introduced.
</success-criteria>

<output>
After completion, create `.planning/phases/133-package-aware-download-and-orphan-bridge/133-03-SUMMARY.md`.
</output>
