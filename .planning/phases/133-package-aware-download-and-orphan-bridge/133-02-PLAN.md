---
phase: 133-package-aware-download-and-orphan-bridge
plan: "02"
type: execute
wave: 2
depends_on:
  - "133-01"
files_modified:
  - packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay/tests/orphanage_cases.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay/tests/scheduler_cases/received_cases.rs
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/inventory_state.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - PPKG-01
  - PPKG-02
user_setup: []
must_haves:
  truths:
    - "One orphan body retains a bounded set of announcers, and disconnect removes only the departing announcer until no announcer remains"
    - "Either child-first or parent-first ordinary transaction arrival can reach a parent-triggered candidate without retaining a reconsiderable-parent body cache"
    - "Only the newest eligible child spending the parent and announced by the parent-supplying peer can form exactly `[parent, child]` with aligned `[peer, peer]` origins"
    - "Candidate traversal, announcer storage, TTL, global/per-peer bodies, and per-parent work remain explicitly bounded"
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      provides: "Shared orphan bodies, bounded announcers, newest-first parent index, cursor, and opaque neutral 1P1C candidate"
      contains: "SamePeerOneParentOneChildCandidate"
    - path: "packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs"
      provides: "Received-transaction provenance captured before request cleanup"
      contains: "ReceivedTransactionProvenance"
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "Coherent transaction-download/orphan/evidence ownership and candidate API"
      contains: "same_peer"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      via: "bounded announcers transferred with ordinary transaction receipt before cleanup"
      pattern: "announcers"
    - from: "packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs"
      to: "packages/open-bitcoin-network/src/peer.rs"
      via: "opaque cursor/candidate constructed only from retained same-peer evidence"
      pattern: "SamePeerOneParentOneChild"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 133-2026-07-26T16-12-51
generated_at: 2026-07-26T16:59:01Z
---

<objective>
Extend the bounded orphan/download core with retained announcer provenance and deterministic newest-first same-peer 1P1C candidate construction.

Purpose: Deliver the pure-network portion of PPKG-02 while preserving PPKG-01 resource bounds and the ordinary-message-only boundary.
Output: Provenance receipts, shared-body announcer ownership, parent index/cursor, opaque neutral candidate, and migrated node ownership seams.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-CONTEXT.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-RESEARCH.md
@.planning/phases/133-package-aware-download-and-orphan-bridge/133-01-SUMMARY.md
@.planning/phases/102-orphan-handling-and-admission-outcome-bridge/102-03-SUMMARY.md
@packages/bitcoin-knots/src/txorphanage.h
@packages/bitcoin-knots/src/txorphanage.cpp
@packages/bitcoin-knots/src/node/txdownloadman_impl.cpp

<interfaces>
Existing orphan input stores one owner:
```rust
pub struct OrphanStageInput {
    pub peer_id: PeerId,
    pub transaction: Transaction,
    pub txid: Txid,
    pub wtxid: Wtxid,
    pub missing_parents: Vec<Txid>,
    pub now_unix_seconds: i64,
}
```

Required neutral network contract:
```rust
pub struct ReceivedTransactionProvenance {
    pub delivered_by: PeerId,
    pub announcers: Vec<PeerId>, // transient deterministic snapshot from bounded scheduler state
}
pub struct ReceivedTransactionResult {
    pub actions: Vec<TxDownloadAction>,
    pub maybe_provenance: Option<ReceivedTransactionProvenance>,
}
pub struct BoundedOrphanAnnouncers { /* created by TxOrphanage under OrphanPolicy */ }
pub struct SamePeerOneParentOneChildCandidate { /* private [parent, child] + [peer, peer] */ }
impl SamePeerOneParentOneChildCandidate {
    pub fn into_ordered_parts(self) -> ([Transaction; 2], [PeerId; 2]);
}
```

The candidate type itself is the eligibility proof. No public boolean or public-field constructor may forge it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture receipt announcers before scheduler cleanup</name>
  <files>packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs, packages/open-bitcoin-network/src/peer/transaction_relay.rs, packages/open-bitcoin-network/src/peer/transaction_relay/tests/scheduler_cases/received_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs (`announcements`, `in_flight`, `record_received_transaction`, `clear_pending_relay`)
    - packages/open-bitcoin-network/src/peer/inventory_state.rs (`handle_inventory`, `handle_transaction`)
    - packages/open-bitcoin-network/src/peer/transaction_relay.rs (`TxDownloadAction`, exports)
    - packages/bitcoin-knots/src/node/txdownloadman_impl.cpp (`ReceivedTx` announcer capture)
    - packages/bitcoin-knots/test/functional/p2p_orphan_handling.py (different announcer/deliverer cases)
  </read-first>
  <behavior>
    - Receipt provenance unions txid and wtxid announcers with the delivering peer before either announcement map is cleared
    - The delivering peer is always retained in the transient receipt provenance
    - Deduplication and ordering are deterministic over the scheduler's already bounded announcement state
    - Existing `record_received_transaction -> Vec<TxDownloadAction>` and `PeerAction::ReceivedTransaction(Transaction)` consumers remain source-compatible throughout Task 1
    - Task 1 does not route late inventory or mutate orphan state before Task 2 creates the bounded orphan API and Task 3 installs its owner
  </behavior>
  <action>
    Implement the receipt-capture prerequisite for D-05/D-09 additively by adding `ReceivedTransactionProvenance` and `ReceivedTransactionResult { actions, maybe_provenance }`. Add `record_received_transaction_with_provenance` to capture txid/wtxid announcement peers and the delivering peer before `clear_pending_relay`, deterministically deduplicate/order that transient snapshot, and return it beside the existing cleanup actions. Keep the existing `record_received_transaction -> Vec<TxDownloadAction>` as a temporary compatibility wrapper that delegates to the new method and discards only the new provenance result. Keep `PeerAction::ReceivedTransaction(Transaction)` unchanged in this task, so every network/node match arm and existing admission call remains compile-compatible. Mark this wrapper for coordinated removal in Task 3. The source maps remain governed by the existing scheduler announcement limits, and `delivered_by` must always be present whenever receipt is not identity-suppressed.

    Do not reference `OrphanPolicy`, `TxOrphanage::add_announcer`, or late-inventory routing in this task: Task 2 creates the retained bounded type/API, then Task 3 routes inventory after moving orphan ownership. Preserve identity-mismatch, request caps, delays, and fallback scheduling.
  </action>
  <acceptance-criteria>
    - `rg -n "ReceivedTransactionProvenance|ReceivedTransactionResult|record_received_transaction_with_provenance|delivered_by|announcers" packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs packages/open-bitcoin-network/src/peer/transaction_relay.rs` returns the additive typed receipt path
    - `rg -n "record_received_transaction\\(" packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs` confirms the legacy wrapper still serves the unchanged ordinary path
    - `rg -n "different.*deliver|delivered_by|dedup|order" packages/open-bitcoin-network/src/peer/transaction_relay/tests/scheduler_cases/received_cases.rs` returns receipt provenance cases
    - `! rg -n "Package|SubmissionPackage|open_bitcoin_mempool" packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs packages/open-bitcoin-network/src/peer/inventory_state.rs` confirms the pure network boundary
    - `bun run scripts/command-timings.ts run --key phase133-announcer-capture -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network received_cases` exits 0
    - `bash scripts/verify.sh` exits 0 with all unchanged cross-crate consumers compiling before Task 2 begins
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-announcer-capture -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network received_cases &amp;&amp; bash scripts/verify.sh</automated>
  </verify>
  <done>The additive scheduler API captures deterministic announcers before cleanup while the legacy transaction action and every existing network/node consumer still compile unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build shared-body newest-first candidate selection</name>
  <files>packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs, packages/open-bitcoin-network/src/peer/transaction_relay.rs, packages/open-bitcoin-network/src/peer/transaction_relay/tests/orphanage_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    - packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs (`OrphanEntry`, insert/remove/expiry/eviction/reconsideration/cleanup)
    - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs (Task 1 `ReceivedTransactionProvenance` input)
    - packages/open-bitcoin-network/src/peer/transaction_relay/tests/orphanage_cases.rs (existing cap/TTL/reconsideration fixtures)
    - packages/open-bitcoin-network/src/peer/transaction_relay/reject_evidence.rs (Plan 01 hard/reconsiderable query types)
    - packages/bitcoin-knots/src/txorphanage.cpp (`AddChildrenToWorkSet`, announcer cleanup, newest-first)
    - packages/bitcoin-knots/src/node/txdownloadman_impl.cpp (`PackageToValidate` selection)
    - packages/bitcoin-knots/test/functional/p2p_opportunistic_1p1c.py (arrival order, same-peer, newest fallback)
  </read-first>
  <behavior>
    - `OrphanPolicy` defines an explicit `max_announcers_per_orphan`, and staging converts Task 1 receipt provenance into `BoundedOrphanAnnouncers`
    - Existing `OrphanStageInput` and `stage_missing_parent(input)` node call sites remain source-compatible through a singleton-provenance wrapper
    - `TxOrphanage::add_announcer` adds one peer only when the body exists and never clones the body, changes missing parents, or refreshes TTL
    - One body has a bounded announcer set and per-peer counts; disconnect removes one announcer and deletes the body only at zero announcers
    - `children_by_parent` is newest-first and every removal path updates bodies, pending work, index, and counts atomically
    - Selection requires reconsiderable parent evidence, matching announcer peer, and non-hard-rejected child
    - Cursor visits at most `max_reconsiderations_per_parent` and returns exactly one `[parent, child]` candidate per step
    - Siblings are considered independently; multiple parents, grandchildren, and arbitrary graphs are never aggregated
  </behavior>
  <action>
    First create the retained API required by later routing without breaking current node call sites: add `OrphanPolicy.max_announcers_per_orphan` with an explicit default independent of connected peer count, add `BoundedOrphanAnnouncers`, and add `stage_missing_parent_with_provenance(input, provenance)` to deterministically cap Task 1 provenance while retaining `delivered_by`. Keep the existing `OrphanStageInput.peer_id` and `stage_missing_parent(input)` signature as a temporary compatibility wrapper that constructs singleton provenance from `peer_id` and delegates to the new method. Add `TxOrphanage::add_announcer(wtxid, peer_id)` as an additive API. `add_announcer` must be a no-op when the body is absent and must never clone/replace the body, alter missing parents, or refresh expiry. Task 3 owns the coordinated call-site cutover to real scheduler provenance and removes the singleton wrapper; Task 2 must not change node signatures or imports.

    Replace `OrphanEntry.peer_id` with one shared body plus `BoundedOrphanAnnouncers` per D-03/D-05. Add monotonic insertion sequence and `children_by_parent: BTreeMap<Txid, ...>` ordered newest first. Centralize removal so expiry, eviction, terminal feedback, restaging, and disconnect cannot leave stale parent-index, pending, or per-peer entries. Add a test-only recomputation oracle for all indexes/counts.

    Implement D-06, D-07, and D-10 with private `SamePeerCandidateCursor` and `SamePeerOneParentOneChildCandidate`. Only `TxOrphanage`/`PeerManager` may construct them after proving parent reconsiderable membership and `announcers.contains(parent_peer)`. Candidate storage is exactly topological `[parent, child]` plus aligned `[parent_peer, parent_peer]`; peer ids never affect content identity. Expose consume-only ordered accessors, no public fields, boolean proof, unchecked constructor, or mutable accessor.

    Candidate iteration must skip hard-rejected children, use newest-first order, carry a bounded cursor so node feedback can continue after a previously failed fingerprint, and stop at the existing per-parent cap. Register exact Knots orphanage/txdownload/functional anchors.
  </action>
  <acceptance-criteria>
    - `rg -n "max_announcers_per_orphan|BoundedOrphanAnnouncers|stage_missing_parent_with_provenance|fn add_announcer" packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs` returns the additive retained cap/type/APIs
    - `rg -n "pub fn stage_missing_parent\\(|peer_id: PeerId" packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs packages/open-bitcoin-node/src/network/admission_bridge.rs` confirms the singleton compatibility route remains available to unchanged node callers
    - `rg -n "late.*announ|does_not.*refresh|missing.*body" packages/open-bitcoin-network/src/peer/transaction_relay/tests/orphanage_cases.rs` returns direct `add_announcer` behavior coverage
    - `rg -n "children_by_parent|insertion_sequence|SamePeerCandidateCursor|SamePeerOneParentOneChildCandidate|into_ordered_parts" packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs` returns index and opaque candidate contracts
    - `! rg -n "pub (members|origins|eligib)|pub fn (new|unchecked)|same_peer: bool" packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs` confirms non-forgeable eligibility
    - `rg -n "newest|wrong_peer|different.*deliver|disconnect|index.*oracle|sibling|grandchild|multiple_parent|max_reconsiderations" packages/open-bitcoin-network/src/peer/transaction_relay/tests/orphanage_cases.rs` returns bounded selection/cleanup coverage
    - `rg -n "orphanage.rs|orphanage_cases.rs|p2p_opportunistic_1p1c.py" docs/parity/source-breadcrumbs.json` returns source lineage
    - `bun run scripts/command-timings.ts run --key phase133-orphan-candidate -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network orphanage_cases` exits 0
    - `bash scripts/verify.sh` exits 0 with the existing node staging calls still compiling before Task 3 begins
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-orphan-candidate -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network orphanage_cases &amp;&amp; bash scripts/verify.sh</automated>
  </verify>
  <done>Bounded retained state and provenance-aware staging exist additively, while unchanged node callers compile through the explicit singleton compatibility wrapper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Co-locate orphan/download ownership and preserve node seams</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/inventory_state.rs, packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs, packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs, packages/open-bitcoin-network/src/peer/tests.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/relay_serving.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/action_translation.rs, packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs</files>
  <read-first>
    - packages/open-bitcoin-network/src/peer.rs (`PeerManager` transaction-download/evidence fields and disconnect cleanup)
    - packages/open-bitcoin-network/src/peer/inventory_state.rs (ordinary inv/tx actions)
    - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs (Task 1 provenance result and legacy wrapper)
    - packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs (Task 2 provenance-aware staging and singleton wrapper)
    - packages/open-bitcoin-network/src/peer/tests.rs (`PeerAction::ReceivedTransaction` construction/matches requiring coordinated cutover)
    - packages/open-bitcoin-node/src/network.rs (`ManagedPeerNetwork.orphanage`)
    - packages/open-bitcoin-node/src/network/relay_serving.rs (`from_peer_manager` orphanage construction)
    - packages/open-bitcoin-node/src/network/admission_bridge.rs (all current orphanage calls)
    - packages/open-bitcoin-node/src/network/action_translation.rs (`disconnect_peer_at` and received transaction bridge)
    - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs (existing bounded bridge regressions)
  </read-first>
  <behavior>
    - Inventory, transaction receipt, orphan staging, candidate selection, and disconnect cleanup mutate one pure network owner
    - Task 3 atomically changes `PeerAction::ReceivedTransaction` to carry provenance, migrates every network/node consumer, and removes both temporary compatibility wrappers
    - Late ordinary inventory calls the Task 2 `add_announcer` API only after `TxOrphanage` is owned by `PeerManager`
    - Existing singleton orphan staging/reconsideration tests remain passing through `PeerManager` methods
    - Node code consumes typed actions/candidates and cannot reconstruct announcer qualification
    - No package policy or mempool type enters `open-bitcoin-network`
  </behavior>
  <action>
    Perform the coordinated signature cutover in the same task that installs the new owner. Change inventory receipt handling to call Task 1's provenance-returning scheduler method and emit `PeerAction::ReceivedTransaction { transaction, provenance }`; update every construction/match in `peer/tests.rs`, node action translation, admission bridge, and node tests. Move `TxOrphanage` ownership into `PeerManager` so scheduler announcements, receipt provenance, orphan bodies, evidence filters, and disconnect cleanup are coordinated in one pure network owner. Remove the separate `ManagedPeerNetwork.orphanage` field and constructor assignment. Replace direct node orphanage mutation with narrow `PeerManager` methods that pass the real receipt provenance into Task 2's provenance-aware staging, plus expire, restage, record terminal outcome, begin/advance same-peer cursor, count, and test policy injection. Once all call sites use the typed path, remove Task 1's legacy scheduler wrapper and Task 2's singleton `stage_missing_parent(input)` wrapper/legacy `OrphanStageInput.peer_id`; no temporary compatibility API may survive Task 3.

    After that ownership exists, route ordinary inv announcements through `PeerManager` to Task 2's `TxOrphanage::add_announcer` when the body is already retained. This D-09 late-inventory path must not duplicate/replace the body or refresh TTL, and it must remain an ordinary inventory action with no package wire variant.

    Update action translation and current admission bridge only enough to preserve existing singleton behavior and carry the new provenance/candidate types into Plan 03. Do not refine a package, call `submit_package`, apply cross-cache package deltas, enqueue fanout, or store candidate members in this plan. Extend existing node tests for both arrival-order state setup and coherent disconnect/late-announcer ownership without asserting package admission yet.
  </action>
  <acceptance-criteria>
    - `! rg -n "orphanage: TxOrphanage" packages/open-bitcoin-node/src/network.rs` confirms removal of split ownership
    - `rg -n "ReceivedTransaction.*provenance|stage_missing_parent_with_provenance" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/tests.rs packages/open-bitcoin-node/src/network/action_translation.rs packages/open-bitcoin-node/src/network/admission_bridge.rs` returns the coordinated typed cutover
    - `! rg -n "pub fn record_received_transaction\\(|pub fn stage_missing_parent\\(" packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs` confirms temporary compatibility wrappers are removed
    - `rg -n "orphanage: TxOrphanage|stage_orphan|add_announcer|same_peer.*candidate|cleanup_peer" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/inventory_state.rs` returns coherent pure ownership and late-inv routing
    - `! rg -n "open_bitcoin_mempool|SubmissionPackage|WellFormedPackage" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/inventory_state.rs packages/open-bitcoin-network/src/peer/transaction_relay/orphanage.rs` confirms D-10
    - `bun run scripts/command-timings.ts run --key phase133-node-orphan-seam -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge_cases` exits 0
    - `bash scripts/verify.sh` exits 0 after the compatibility removal and cross-crate cutover
  </acceptance-criteria>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase133-node-orphan-seam -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge_cases &amp;&amp; bash scripts/verify.sh</automated>
  </verify>
  <done>One pure network owner retains provenance and constructs candidates; node seams compile and preserve bounded singleton orphan behavior for Plan 03.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Multiple peer inv/tx streams → shared orphan body | Untrusted announcer and body provenance converge |
| Orphan state → opaque candidate | Same-peer eligibility becomes an admission capability |
| Peer disconnect/expiry/eviction → indexes | Cleanup must remove all derived references without deleting other announcers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-133-10 | Spoofing | `SamePeerOneParentOneChildCandidate` | mitigate | Private fields/constructors; eligibility created only from retained announcers; aligned origins fixed to qualifying peer |
| T-133-11 | Denial of Service | orphan announcers/bodies | mitigate | One shared body, explicit announcer/global/per-peer/TTL caps, no per-peer body copies |
| T-133-12 | Denial of Service | parent traversal | mitigate | Newest-first parent index and cursor capped by `max_reconsiderations_per_parent` |
| T-133-13 | Tampering | orphan indexes | mitigate | One removal helper plus recomputation oracle across expiry, eviction, restage, terminal feedback, and disconnect |
| T-133-14 | Repudiation | probabilistic child suppression | mitigate | Hard/reconsiderable filter hits suppress selection only and never punish the announcing peer |
</threat-model>

<verification>
Run the network and node focused commands sequentially through `scripts/command-timings.ts`, then run the breadcrumb checker and `git diff --check`.
</verification>

<success-criteria>
- PPKG-02 has an ordinary-message, sender-aware, same-peer exact 1P1C candidate in the pure network crate.
- Orphan bodies/provenance and traversal remain bounded under adversarial peers.
- Both arrival orders and different announcer/body-deliverer cases are deterministic.
- No parent cache, package wire message, multi-parent package, mempool dependency, or package policy duplication exists.
</success-criteria>

<output>
After completion, create `.planning/phases/133-package-aware-download-and-orphan-bridge/133-02-SUMMARY.md`.
</output>
