---
phase: 106-parity-traceability-uat-and-release-boundary-guardrails
plan: 106-02
type: implementation
wave: 2
depends_on:
  - 106-01
files_modified:
  - scripts/check-phase106-parity-uat-release-boundary.ts
  - scripts/check-phase106-parity-uat-release-boundary.test.ts
  - scripts/verify.sh
requirements:
  - BOUND-02
  - BOUND-04
  - BOUND-05
requirements_addressed:
  - BOUND-02
  - BOUND-04
  - BOUND-05
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 106-2026-07-02T03-46-34
generated_at: 2026-07-02T04:12:00Z
user_setup: []
must_haves:
  truths:
    - "The checker verifies exactly-one ownership for all 32 v2.0 requirements across Phase 100-106 surfaces."
    - "The checker fails on unsupported positive claims for production/public relay boundaries."
    - "The repo verifier runs the Phase 106 checker and tests after Phase 105 guardrails."
  artifacts:
    - path: "scripts/check-phase106-parity-uat-release-boundary.ts"
      provides: "Deterministic Phase 106 guardrail"
      contains: "Traceability, UAT command, no-claim, Knots anchor, and verifier wiring checks"
    - path: "scripts/check-phase106-parity-uat-release-boundary.test.ts"
      provides: "Regression fixtures for guardrail drift"
      contains: "Passing fixture and failure cases for missing BOUND requirements, duplicate ownership, missing commands, unsupported claims, and verifier drift"
    - path: "scripts/verify.sh"
      provides: "Repo-native Phase 106 verification wiring"
      contains: "Phase 106 test/check commands after Phase 105"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase106-parity-uat-release-boundary.ts"
      via: "run_step wiring"
      pattern: "bun run scripts/check-phase106-parity-uat-release-boundary.ts"
---

# Phase 106 Plan 02: Deterministic Guardrail Checker and Verification Wiring

<objective>
Add the deterministic guardrail that keeps the v2.0 parity, UAT, and release-boundary claims auditable in normal repository verification.
</objective>

<context>
Phase 106 is primarily a claim-boundary phase. The implementation risk is documentation drift: a future edit could imply compact block relay, package relay, bloom/filter serving, public relay defaults, public-network CI, production service operation, production full-node readiness, or production-funds wallet safety without evidence. The checker should turn those risks into fast local failures.
</context>

<tasks>
<task id="106-02-01">
<name>Add the Phase 106 checker</name>
<type>implementation</type>
<files>
scripts/check-phase106-parity-uat-release-boundary.ts
</files>
<read_first>
scripts/check-phase105-operator-relay-evidence.ts
scripts/check-phase95-network-participation-release-boundary.ts
scripts/check-phase98-traceability-reconciliation.ts
docs/parity/index.json
docs/operator/runtime-guide.md
</read_first>
<action>
Implement a Bun TypeScript checker that reads repo files, parses `docs/parity/index.json`, verifies all seven v2.0 surfaces cover exactly 32 requirements once, verifies the Phase 106 surface covers `BOUND-01` through `BOUND-05`, verifies required Knots anchors and evidence roots, checks exact repo-local UAT command prefixes, rejects unsupported positive claims in release-facing docs, and asserts `scripts/verify.sh` runs the Phase 106 test/check pair after Phase 105.
</action>
<verify>
Run:

```bash
bun run scripts/check-phase106-parity-uat-release-boundary.ts
```
</verify>
<acceptance_criteria>
The checker passes on the repository state and reports actionable failures for missing traceability, missing commands, unsupported claims, or verifier drift.
</acceptance_criteria>
<done>The Phase 106 checker validates traceability, UAT commands, no-claim boundaries, Knots anchors, and verifier wiring.</done>
</task>

<task id="106-02-02">
<name>Add checker regression fixtures</name>
<type>implementation</type>
<files>
scripts/check-phase106-parity-uat-release-boundary.test.ts
</files>
<read_first>
scripts/check-phase105-operator-relay-evidence.test.ts
scripts/check-phase106-parity-uat-release-boundary.ts
</read_first>
<action>
Add fixture-based tests for a passing repository slice and failure cases covering missing `BOUND-*` requirements, duplicate v2.0 requirement ownership, missing Cargo/Bazel UAT commands, unsupported positive production/public relay claims, missing Knots anchors, and missing verifier wiring.
</action>
<verify>
Run:

```bash
bun test scripts/check-phase106-parity-uat-release-boundary.test.ts
```
</verify>
<acceptance_criteria>
The tests prove the checker fails for each high-risk drift class and passes for the current intended repository shape.
</acceptance_criteria>
<done>Checker fixtures cover the main traceability, UAT, no-claim, anchor, and verifier drift paths.</done>
</task>

<task id="106-02-03">
<name>Wire Phase 106 into repo verification</name>
<type>implementation</type>
<files>
scripts/verify.sh
</files>
<read_first>
scripts/verify.sh
</read_first>
<action>
Add the Phase 106 Bun test and checker to the visible verifier order block and executable `run_step` sequence immediately after Phase 105 and before pure-core checks.
</action>
<verify>
Search the visible and executable verifier order for the Phase 105 then Phase 106 sequence, then run focused Phase 106 tests and checker.
</verify>
<acceptance_criteria>
`scripts/verify.sh` includes Phase 106 guardrails in default verification and the checker itself confirms the ordering.
</acceptance_criteria>
<done>Repo-native verification runs the Phase 106 checker and test pair after Phase 105.</done>
</task>
</tasks>

<verification>
Run after implementation:

```bash
bun test scripts/check-phase106-parity-uat-release-boundary.test.ts
bun run scripts/check-phase106-parity-uat-release-boundary.ts
bash scripts/verify.sh
```
</verification>

<success_criteria>
Plan 106-02 is complete when the deterministic checker and tests pass, `scripts/verify.sh` runs them in the default sequence, and repo-native verification either passes or the known Cargo-test hang is recorded as a blocking verification failure.
</success_criteria>
