---
phase: 119-compact-receive-mempool-candidate-injection
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - RCN-02
  - RCN-03
  - GOV-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 119-2026-07-13T16-08-52
generated_at: 2026-07-13T17:42:56Z
must_haves:
  truths:
    - "Connected-block mempool removals forward each removal.wtxid into PeerManager::on_mempool_transaction_removed"
    - "Evict/expire and replaced/evicted victim exits with available wtxid also clear matching volatile partial slots"
    - "Runtime tests prove injected-path reconstruction Ready/missing, ShortIdCollision failure, and lifecycle slot cleanup"
    - "Package relay, bloom/filter serving, compact filters, timeout ticks, and metrics projection remain untouched"
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/mempool_lifecycle.rs"
      provides: "Lifecycle hook using removal.wtxid"
      contains: "on_mempool_transaction_removed"
    - path: "packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs"
      provides: "Runtime injected-path RCN-02/RCN-03/GOV-04 proofs"
  key_links:
    - from: "apply_connected_block_mempool_lifecycle"
      to: "PeerManager::on_mempool_transaction_removed"
      via: "MempoolLifecycleRemoval.wtxid"
      pattern: "removal\\.wtxid"
    - from: "compact_receive_cases runtime tests"
      to: "ManagedPeerNetwork::receive_message CompactBlock"
      via: "populated mempool / colliding candidates"
      pattern: "receive_message.*CompactBlock|WireNetworkMessage::CompactBlock"
---

<objective>
Hook mempool-removal lifecycle into partial compact cleanup and prove RCN-02/RCN-03/GOV-04 on the live injected receive path with deterministic local tests and parity breadcrumbs.

Purpose: Finish Phase 119 audit gap closure without activating deferred surfaces or Phase 120/121 work.
Output: Lifecycle wtxid hooks, compact_receive_cases runtime suite, breadcrumb registry updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-CONTEXT.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-RESEARCH.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-02-SUMMARY.md
@packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
@packages/open-bitcoin-node/src/network/admission_bridge.rs
@packages/open-bitcoin-network/src/compact_reconstruction.rs
@packages/open-bitcoin-mempool/src/pool/lifecycle.rs
@packages/open-bitcoin-mempool/src/outcome.rs

<interfaces>
PeerManager forwarder (Plan 01):
```rust
pub fn on_mempool_transaction_removed(&mut self, removed_wtxid: &Wtxid);
```

MempoolLifecycleRemoval (already has wtxid):
```rust
pub struct MempoolLifecycleRemoval {
    pub txid: Txid,
    pub wtxid: Wtxid,
    pub reason: MempoolLifecycleRemovalReason,
}
```

Wtxid resolution table (D-07 / RESEARCH Pattern 5):
| Exit path | Hook with |
| remove_for_connected_block | removal.wtxid (required) |
| MempoolOutcome::Evicted / Expired | outcome.maybe_wtxid() |
| Replaced/Accepted victim txids | resolve wtxid from relay_serving BEFORE demotion; skip if unavailable |
| Replaced admitted wtxid | DO NOT call removal hook |

Do NOT call on_compact_download_block_connected in this phase unless a D-09 test absolutely requires it (prefer leave for Phase 120 / GOV-03).
Do NOT call expire_compact_download_timeouts (Phase 120).
Do NOT project DurableSyncRuntime metrics/logs (Phase 121).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook mempool lifecycle removals into PeerManager forwarder</name>
  <files>packages/open-bitcoin-node/src/network/mempool_lifecycle.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs</files>
  <behavior>
    - apply_connected_block_mempool_lifecycle, after remove_for_connected_block, calls peer_manager.on_mempool_transaction_removed for each summary.removed[*].wtxid (D-07 minimum).
    - Evicted/Expired admission outcomes call the forwarder with outcome.maybe_wtxid() when present.
    - Replaced/evicted victims: resolve wtxid from relay_serving records before demotion and forward those wtxids; never forward the newly admitted Replaced wtxid as a removal.
    - Hook only clears volatile partial slots — no package/filter/timeout activation (D-08).
  </behavior>
  <action>
    Per D-07 and D-08:

    1. In `apply_connected_block_mempool_lifecycle`, after obtaining `summary` and before or after TxServing cleanup (order: prefer forwarding compact cleanup using wtxid from summary first, keep txid path for serving status):
       ```rust
       for removal in &summary.removed {
           self.peer_manager.on_mempool_transaction_removed(&removal.wtxid);
       }
       ```
       Keep existing `removed_txids` → `remove_stored_transactions_with_status` behavior.

    2. In admission_bridge paths that already handle Evicted/Expired (`remove_evicted_outcome` or equivalent), call `on_mempool_transaction_removed` with the outcome wtxid.

    3. For Replaced/Accepted victims: before `remove_stored_transactions_with_status` / `record_replaced` demotes victims, look up each victim txid's wtxid from `relay_serving` (records_by_txid / status_wtxid_for_txid pattern — add a small `pub(super)` helper on RelayServingCache if needed) and forward those wtxids. Never hook `outcome.maybe_wtxid()` for Replaced as a removal (that is the admitted tx still in mempool).

    4. Add/extend mempool_lifecycle_cases proving connected-block removal clears an in-flight matched compact slot when the removed wtxid was matched (seed partial via live CompactBlock receive from Plan 02 path, then connect a block that confirms/conflicts the tx, assert slot cleared via peer_manager.compact_download_peer_state inspection).

    Do not activate package relay, bloom/filter, or compact filters. Do not schedule timeout ticks.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib mempool_lifecycle -- --nocapture</automated>
  </verify>
  <done>Connected-block and other wtxid-bearing mempool exits forward into PeerManager::on_mempool_transaction_removed; lifecycle test proves slot cleanup; deferred surfaces untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime injected-path reconstruction and RCN-03 outcomes</name>
  <files>packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <behavior>
    - Live receive with mempool candidates reconstructs or reports Ready-with-missing without relying solely on empty facts (RCN-02 / D-09.1).
    - Injected path yields typed collision/failure outcome (ShortIdCollision or download Fallback/Suppressed mapping from Failed) when candidates are constructed to collide (RCN-03 / D-09.2).
    - Missing short IDs produce GetBlockTxn / missing-index actions on the injected path (D-09.2).
    - Duplicate-match clearing behavior remains consistent when two mempool candidates share a short ID bucket conflict pattern already covered by Phase 114 — at least one injected-path test asserts non-silent typed handling (D-09.2).
    - Lifecycle cleanup after removal is covered (D-09.3) — may share setup with Task 1 test or a dedicated case in this module.
    - Assert package/filter/public-default settings remain default-off / untouched in a lightweight guard assertion (D-09.4).
  </behavior>
  <action>
    Per D-09: create `packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs` and register `mod compact_receive_cases;` in `tests.rs`.

    Reuse helpers from tests.rs (`compact_relay_enabled_managed_network`, negotiate sendcmpct, connect tip, compact payload builders). Prefer calling `ManagedPeerNetwork::receive_message(..., WireNetworkMessage::CompactBlock(...))` — do not only call `peer_manager.handle_compact_block_download` (RESEARCH Pitfall 2).

    Required tests (one concern each, Arrange/Act/Assert):
    1. `phase119_live_receive_with_mempool_candidates_reconstructs_or_requests_missing` — mempool admits txs matching short IDs; assert Completed ReceivedBlock or Ready GetBlockTxn with fewer missing indexes than empty-facts would need.
    2. `phase119_live_receive_short_id_collision_is_typed` — arrange colliding candidate set (two different wtxids mapping to same short id for the payload nonce/selector); assert typed failure/fallback outcome on live receive (not silent success).
    3. `phase119_live_receive_missing_short_ids_request_getblocktxn` — leave at least one short id unmatched; assert GetBlockTxn outbound / missing evidence.
    4. `phase119_mempool_removal_clears_matched_partial_slot` — match via live receive, then remove via connected-block lifecycle; assert slot cleared.
    5. `phase119_package_filter_surfaces_untouched` — assert block_relay/compact defaults and absence of package/filter activation flags remaining as before this phase (simple policy/default assertions).

    Duplicate blocktxn response remains Phase 115 coverage; if cheap, add a one-liner comment in the module pointing there rather than re-testing blocktxn.

    Follow standards/core/testing.md: one concern per test.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib compact_receive_cases -- --nocapture</automated>
  </verify>
  <done>Runtime suite proves injected-path reconstruction, collision, missing, lifecycle cleanup, and no package/filter activation.</done>
</task>

<task type="auto">
  <name>Task 3: Parity breadcrumbs and focused verification</name>
  <files>docs/parity/source-breadcrumbs.json, packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs, packages/open-bitcoin-node/src/network/mempool_lifecycle.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs</files>
  <action>
    Per D-10 and D-11:
    1. Ensure every new/touched first-party Rust source/test file under packages/open-bitcoin-*/src or */tests from Plans 01–03 has a parity breadcrumb block and matching `docs/parity/source-breadcrumbs.json` entry. Prefer Knots anchors:
       - `packages/bitcoin-knots/src/blockencodings.cpp` (InitData)
       - `packages/bitcoin-knots/src/net_processing.cpp` (compact receive + AddToCompactExtraTransactions)
       - `packages/bitcoin-knots/src/net_processing.h` (extra-txn defaults)
       - `packages/bitcoin-knots/test/functional/p2p_compactblocks.py`
    2. Run `bun scripts/check-parity-breadcrumbs.ts` (or the verify-integrated check) and fix any missing entries.
    3. Run focused cargo tests for open-bitcoin-network forwarder + open-bitcoin-node compact_receive / mempool_lifecycle / admission_bridge.
    4. Confirm no Phase 120/121 symbols were newly called from node shell (`expire_compact_download_timeouts`, DurableSyncRuntime metric projection for block-relay).
    5. Do not claim public-network CI or production readiness.
  </action>
  <verify>
    <automated>bun scripts/check-parity-breadcrumbs.ts && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib on_mempool_transaction_removed -- --nocapture && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib 'compact_receive|mempool_lifecycle' -- --nocapture</automated>
  </verify>
  <done>Parity breadcrumbs pass; focused network/node tests for Phase 119 seams pass; deferred Phase 120/121 work absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mempool lifecycle → PeerManager | Trusted local removal events clear untrusted-peer-derived partial matches |
| Runtime tests | Hermetic local fixtures only — no public-network CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-09 | Tampering / Integrity | Lifecycle wtxid hook | mitigate | Clear only slots matching removed wtxid; never hook Replaced admitted wtxid; never mutate chainstate from partial state |
| T-119-10 | Denial of Service | Runtime test fixtures | accept | Hermetic unit tests; no public network exposure |
| T-119-11 | Elevation of Privilege | GOV-04 scope | mitigate | Explicit D-09.4 guard that package/filter/public-default surfaces stay untouched; no timeout/metrics activation |
| T-119-12 | Repudiation | Parity breadcrumbs | mitigate | Register Knots anchors for new/touched files via source-breadcrumbs.json so reconstruction wiring remains auditable |
</threat_model>

<verification>
- Lifecycle removals forward wtxid into PeerManager and clear matched slots.
- compact_receive_cases covers reconstruction, collision, missing, lifecycle, and no package/filter activation.
- Parity breadcrumb check passes.
- Phase 120/121 work not introduced.
</verification>

<success_criteria>
- on_mempool_transaction_removed is hooked from mempool lifecycle without activating package relay or filters.
- Runtime tests cover reconstruction, collision, duplicate/typed conflict, missing, and lifecycle cleanup outcomes on the injected path.
- RCN-02, RCN-03, and GOV-04 phase success criteria are met for Phase 119.
</success_criteria>

<output>
After completion, create `.planning/phases/119-compact-receive-mempool-candidate-injection/119-03-SUMMARY.md`
</output>
