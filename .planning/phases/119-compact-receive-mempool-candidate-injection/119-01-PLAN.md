---
phase: 119-compact-receive-mempool-candidate-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer/compact_download_state.rs
  - packages/open-bitcoin-network/src/peer/message_dispatch.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network/compact_receive_candidates.rs
  - packages/open-bitcoin-node/src/network.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - RCN-02
  - GOV-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 119-2026-07-13T16-08-52
generated_at: 2026-07-13T17:42:56Z
must_haves:
  truths:
    - "PeerManager can clear matching volatile partial compact slots by wtxid without importing open-bitcoin-mempool"
    - "CompactExtraTxnBuffer enforces Knots-aligned slot and byte bounds with FIFO overwrite"
    - "Empty-facts CompactBlock in message_dispatch is documented as non-production for live shell receive"
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/compact_download_state.rs"
      provides: "PeerManager::on_mempool_transaction_removed forwarder"
      contains: "on_mempool_transaction_removed"
    - path: "packages/open-bitcoin-node/src/network/compact_receive_candidates.rs"
      provides: "CompactExtraTxnBuffer + mempool snapshot helpers"
      exports: ["CompactExtraTxnBuffer", "mempool_compact_candidate_owned"]
  key_links:
    - from: "PeerManager::on_mempool_transaction_removed"
      to: "PartialCompactBlock::on_mempool_transaction_removed"
      via: "walk compact_download_states[*].in_flight[*].partial"
      pattern: "partial\\.on_mempool_transaction_removed"
    - from: "CompactExtraTxnBuffer::push"
      to: "Knots DEFAULT_BLOCK_RECONSTRUCTION_EXTRA_TXN bounds"
      via: "named constants matching net_processing.h"
      pattern: "32768|10_000_000|100_000"
---

<objective>
Add the PeerManager mempool-removal forwarder and the node-owned Knots-shaped `CompactExtraTxnBuffer` plus mempool snapshot helpers so later plans can inject live candidates without redesigning reconstruction.

Purpose: Close the GOV-04 forwarder gap and D-05 buffer foundation before wiring the live receive seam.
Output: Network forwarder + annotated empty-facts path; `compact_receive_candidates.rs` with buffer + candidate snapshot helpers and unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-CONTEXT.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-RESEARCH.md
@packages/open-bitcoin-network/src/peer/compact_download_state.rs
@packages/open-bitcoin-network/src/compact_reconstruction.rs
@packages/open-bitcoin-network/src/compact_download.rs
@packages/bitcoin-knots/src/net_processing.h

<interfaces>
<!-- Key contracts — use directly; do not redesign -->

From packages/open-bitcoin-network/src/peer/compact_download_state.rs:
```rust
pub struct CompactBlockReceiveFacts<'a> {
    pub candidates: &'a [(&'a Wtxid, &'a Transaction)],
    pub extra: &'a [(&'a Wtxid, &'a Transaction)],
}

pub fn handle_compact_block_download(
    &mut self,
    peer_id: PeerId,
    payload: CompactBlockPayload,
    facts: CompactBlockReceiveFacts<'_>,
    now_unix_seconds: i64,
) -> Result<Vec<PeerAction>, NetworkError>;
```

From packages/open-bitcoin-network/src/compact_download.rs:
```rust
pub struct CompactDownloadInFlight {
    pub partial: PartialCompactBlock,
    // ...
}
// PeerManager.compact_download_states: map PeerId -> CompactDownloadPeerState
// CompactDownloadPeerState.in_flight: BTreeMap<BlockHash, CompactDownloadInFlight>
```

From packages/open-bitcoin-network/src/compact_reconstruction.rs:
```rust
pub fn on_mempool_transaction_removed(&mut self, removed_wtxid: &Wtxid);
```

From packages/open-bitcoin-mempool (for helper signatures only — network crate must NOT depend on mempool):
```rust
// Mempool::entries() -> &HashMap<Txid, MempoolEntry>
// MempoolEntry { transaction, wtxid, ... }
```

Knots extra-txn defaults (packages/bitcoin-knots/src/net_processing.h):
- DEFAULT_BLOCK_RECONSTRUCTION_EXTRA_TXN = 32768
- DEFAULT_BLOCK_RECONSTRUCTION_EXTRA_TXN_SIZE = 10_000_000
- BLOCK_RECONSTRUCTION_EXTRA_TXN_PER_TXN_SIZE_LIMIT = 100_000
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PeerManager mempool-removal forwarder</name>
  <files>packages/open-bitcoin-network/src/peer/compact_download_state.rs, packages/open-bitcoin-network/src/peer/message_dispatch.rs, packages/open-bitcoin-network/src/peer/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Given an in-flight CompactDownloadInFlight whose partial has a matched slot for wtxid W, calling PeerManager::on_mempool_transaction_removed(&W) clears that slot (txn_available/slot_wtxids become None for that index).
    - Calling with an unrelated wtxid leaves matched slots unchanged.
    - Forwarder walks every peer's in_flight map; no mempool types appear in the network crate.
  </behavior>
  <action>
    Per D-07/D-08 and RESEARCH Pattern 3: add `PeerManager::on_mempool_transaction_removed(&mut self, removed_wtxid: &Wtxid)` in `compact_download_state.rs` that iterates `self.compact_download_states.values_mut()`, then each `state.in_flight.values_mut()`, then calls `in_flight.partial.on_mempool_transaction_removed(removed_wtxid)`. Do not schedule timeouts, do not call `on_compact_download_block_connected`, and do not import mempool.

    In `message_dispatch.rs` CompactBlock branch that still uses `CompactBlockReceiveFacts::default()`, add a short comment that production live receive must inject facts via the node shell (`ManagedPeerNetwork::receive_*`) per D-03; keep the empty-facts path callable for PeerManager-only tests (do not `#[cfg(test)]` gate it).

    Add a focused Arrange/Act/Assert unit test in `peer/tests.rs` that seeds compact download in-flight partial state (reuse existing Phase 115 compact download helpers/patterns), matches a wtxid, calls the forwarder, and asserts the slot cleared.

    Update `docs/parity/source-breadcrumbs.json` if the touched files need refreshed anchors; prefer Knots `net_processing.cpp` / `blockencodings.cpp` (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib on_mempool_transaction_removed -- --nocapture</automated>
  </verify>
  <done>PeerManager forwarder exists, clears matching partial slots by wtxid, empty-facts CompactBlock path is annotated as non-production, and the new unit test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompactExtraTxnBuffer and mempool snapshot helpers</name>
  <files>packages/open-bitcoin-node/src/network/compact_receive_candidates.rs, packages/open-bitcoin-node/src/network.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - CompactExtraTxnBuffer::with_defaults() uses Knots slot capacity 32768, aggregate byte budget 10_000_000, per-tx reject gate 100_000.
    - push inserts (Wtxid, Transaction); when full, overwrites in ring order; when aggregate size exceeds budget, clears next slots until under budget (Knots-shaped).
    - push_gated rejects bodies whose approximate size exceeds the per-tx limit without inserting.
    - mempool_compact_candidate_owned(mempool) returns Vec&lt;(Wtxid, Transaction)&gt; from Mempool::entries() using entry.wtxid and cloned transaction (D-04).
    - compact_extra_owned(buffer) returns non-null slots as owned pairs for facts adaptation.
  </behavior>
  <action>
    Per D-05 (Claude's Discretion within Knots bounds): create `packages/open-bitcoin-node/src/network/compact_receive_candidates.rs` with:
    1. Named constants matching Knots defaults.
    2. `CompactExtraTxnBuffer` storing `Vec&lt;Option&lt;(Wtxid, Transaction)&gt;&gt;`, write cursor, and approximate size tracking (serialized/virtual size approximation is acceptable — do not require RecursiveDynamicUsage identity per RESEARCH A1).
    3. `new(max_slots, max_bytes, per_tx_size_limit)` for tests + `with_defaults()`.
    4. `push(wtxid, tx)` ring insert; `push_gated` applying per-tx size gate; `iter_available()` / `to_owned_pairs()` for facts.
    5. `mempool_compact_candidate_owned(mempool: &Mempool) -> Vec&lt;(Wtxid, Transaction)&gt;` adapting `entries().values()` (D-04).

    Register `mod compact_receive_candidates;` in `packages/open-bitcoin-node/src/network.rs` (do not yet add the field to ManagedPeerNetwork — that is Plan 02).

    Put unit tests in the same file under `#[cfg(test)]` covering capacity overwrite, byte-budget eviction, per-tx gate, and mempool snapshot mapping. Prefer `foo.rs` + sibling tests in-file (existing node pattern).

    Add parity breadcrumb block at top of the new file and register in `docs/parity/source-breadcrumbs.json` pointing at Knots `net_processing.cpp` `AddToCompactExtraTransactions` / `net_processing.h` defaults (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib compact_receive_candidates -- --nocapture</automated>
  </verify>
  <done>CompactExtraTxnBuffer and mempool snapshot helpers exist with Knots-aligned bounds, unit tests pass, and parity breadcrumbs are registered for the new file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PeerManager compact download state | Untrusted CompactBlock already validated elsewhere; this plan only clears matched volatile slots by wtxid |
| CompactExtraTxnBuffer | Node-owned memory holding recent/orphan/reject bodies that later feed reconstruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Denial of Service | CompactExtraTxnBuffer | mitigate | Cap slots at 32768, aggregate ~10MB, per-tx 100KB gate matching Knots defaults |
| T-119-02 | Tampering | PeerManager::on_mempool_transaction_removed | mitigate | Only clear slots matching exact removed wtxid; no chainstate mutation (D-06 / RCN-06) |
| T-119-03 | Elevation of Privilege | message_dispatch CompactBlock path | accept | Empty-facts path remains for tests; production inject is Plan 02 — annotate to prevent silent regression to empty facts |
| T-119-04 | Information Disclosure | Extra buffer contents | accept | In-process volatile tx bodies already known to node; no new external surface |
</threat_model>

<verification>
- Network crate tests for forwarder pass.
- Node crate compact_receive_candidates tests pass.
- No open-bitcoin-mempool dependency added to open-bitcoin-network Cargo.toml.
- Do not call expire_compact_download_timeouts or project metrics (Phase 120/121 out of scope).
</verification>

<success_criteria>
- PeerManager::on_mempool_transaction_removed walks all in-flight partials and clears matching wtxid slots.
- CompactExtraTxnBuffer implements Knots-shaped ring with named bound constants.
- Empty-facts CompactBlock dispatch is explicitly documented as non-production for live shell receive.
</success_criteria>

<output>
After completion, create `.planning/phases/119-compact-receive-mempool-candidate-injection/119-01-SUMMARY.md`
</output>
