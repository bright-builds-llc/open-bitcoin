---
phase: 119-compact-receive-mempool-candidate-injection
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/compact_receive_candidates.rs
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - RCN-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 119-2026-07-13T16-08-52
generated_at: 2026-07-13T17:42:56Z
must_haves:
  truths:
    - "Live ManagedPeerNetwork::receive_message CompactBlock path no longer always uses CompactBlockReceiveFacts::default()"
    - "Live receive builds CompactBlockReceiveFacts from current mempool candidates plus CompactExtraTxnBuffer extras"
    - "Orphaned, rejected (size-gated), and replaced-victim bodies are pushed into the extra buffer when available"
  artifacts:
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "CompactBlock intercept in receive_message and receive_sync_message"
      contains: "handle_compact_block_download"
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Extra buffer feed pushes for orphan/reject/replaced victims"
  key_links:
    - from: "ManagedPeerNetwork::receive_message"
      to: "PeerManager::handle_compact_block_download"
      via: "shell-built CompactBlockReceiveFacts"
      pattern: "handle_compact_block_download"
    - from: "admission_bridge outcomes"
      to: "CompactExtraTxnBuffer::push"
      via: "orphan/reject/replaced victim bodies"
      pattern: "compact_extra_txn|push_gated|push\\("
---

<objective>
Wire `ManagedPeerNetwork` live CompactBlock receive to inject mempool + bounded extras into `handle_compact_block_download`, and feed the extra ring from admission outcomes.

Purpose: Close the RCN-02 runtime seam — production receive must supply non-empty facts (D-01..D-06).
Output: Shell intercept on both receive paths, Owned snapshot→facts adaptation, extra-buffer field + admission feeds.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-CONTEXT.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-RESEARCH.md
@.planning/phases/119-compact-receive-mempool-candidate-injection/119-01-SUMMARY.md
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/network/compact_receive_candidates.rs
@packages/open-bitcoin-node/src/network/admission_bridge.rs
@packages/open-bitcoin-node/src/network/relay_serving.rs

<interfaces>
From Plan 01 (must exist before this plan):
```rust
// compact_receive_candidates.rs
pub struct CompactExtraTxnBuffer { /* ... */ }
pub fn mempool_compact_candidate_owned(mempool: &Mempool) -> Vec<(Wtxid, Transaction)>;
impl CompactExtraTxnBuffer {
    pub fn with_defaults() -> Self;
    pub fn push(&mut self, wtxid: Wtxid, tx: Transaction);
    pub fn push_gated(&mut self, wtxid: Wtxid, tx: Transaction) -> bool;
    pub fn to_owned_pairs(&self) -> Vec<(Wtxid, Transaction)>;
}
```

Current broken live path (network.rs receive_message / receive_sync_message):
```rust
let actions = self.peer_manager.handle_message(peer_id, message, timestamp)?;
// CompactBlock always hits CompactBlockReceiveFacts::default() inside message_dispatch
```

Borrow rule (RESEARCH Pitfall 1): snapshot owned candidates/extras BEFORE mutably borrowing peer_manager.

Admission feed sources (Knots-aligned, D-05/D-08 — no package relay):
1. Orphaned tx body when staged
2. Rejected tx body via push_gated (per-tx size limit)
3. Replaced victim bodies: look up from relay_serving / transactions_by_txid BEFORE remove_stored_transactions_with_status demotion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shell CompactBlock intercept with mempool+extra facts</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/relay_serving.rs, packages/open-bitcoin-node/src/network/compact_receive_candidates.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <behavior>
    - receive_message(CompactBlock) and receive_sync_message(CompactBlock) call PeerManager::handle_compact_block_download with facts built from mempool entries + compact_extra_txn buffer — not CompactBlockReceiveFacts::default().
    - Non-CompactBlock messages still use handle_message unchanged.
    - After CompactBlock intercept, existing note_block_relay_observed + record_compact_download_evidence + process_actions behavior is preserved.
    - Candidate/extra collection clones transactions only; no chainstate mutation from partial compact state (D-06).
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-04, D-06:

    1. Add `compact_extra_txn: CompactExtraTxnBuffer` field to `ManagedPeerNetwork` and initialize with `CompactExtraTxnBuffer::with_defaults()` in `from_peer_manager` (relay_serving.rs constructors).

    2. Add a focused helper on ManagedPeerNetwork (in `compact_receive_candidates.rs` or network.rs impl) that:
       - Snapshots `mempool_compact_candidate_owned(self.mempool.mempool())`
       - Snapshots `self.compact_extra_txn.to_owned_pairs()`
       - Builds `Vec&lt;(&Wtxid, &Transaction)&gt;` refs into those owned locals
       - Returns owned snapshots + a way to build `CompactBlockReceiveFacts` (avoid simultaneous borrow of mempool and peer_manager)

    3. In both `receive_message` and `receive_sync_message`, match `WireNetworkMessage::CompactBlock(payload)` before `handle_message`:
       - Collect owned candidates/extras
       - Call `self.peer_manager.handle_compact_block_download(peer_id, payload, facts, timestamp)?`
       - Run the same observed_block_relay / evidence / process_actions path as today
       - Early-return that branch so CompactBlock never falls through to empty-facts `handle_message`

    4. Import `CompactBlockReceiveFacts` from open_bitcoin_network in the node crate.

    5. Add at least one runtime smoke test in `network/tests.rs` (or new `tests/compact_receive_cases.rs` wired from tests.rs) that:
       - Uses `compact_relay_enabled_managed_network`
       - Negotiates sendcmpct + connects a tip
       - Admits a mempool tx whose short ID appears in a CompactBlockPayload
       - Calls `receive_message(..., CompactBlock(...))`
       - Asserts a non-empty-facts outcome: Ready/missing GetBlockTxn, Completed ReceivedBlock, or typed Fallback — NOT the empty-candidates-only path that would always request every short-id index when mempool had the txs
       Prefer asserting that when mempool supplies all non-prefilled short IDs, the receive path completes or requests fewer missing indexes than the empty-facts baseline.

    Do NOT hook lifecycle removals here (Plan 03). Do NOT schedule timeouts (Phase 120).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib compact_receive -- --nocapture</automated>
  </verify>
  <done>Both receive_* CompactBlock paths inject mempool+extra facts into handle_compact_block_download; at least one runtime test proves live receive is not empty-facts-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed CompactExtraTxnBuffer from admission outcomes</name>
  <files>packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs, packages/open-bitcoin-node/src/network/compact_receive_candidates.rs</files>
  <behavior>
    - When a peer/local tx is Orphaned and staged, its (wtxid, body) is pushed into compact_extra_txn.
    - When a tx is Rejected and a body is available, push_gated inserts only if under per-tx size limit.
    - When Replaced, victim bodies looked up from stored transactions / relay serving records BEFORE demotion are pushed; the admitted Replaced wtxid is NOT treated as a removal feed for extras-as-removal (push victims only).
    - Package relay / bloom / compact-filter surfaces remain untouched (D-08).
  </behavior>
  <action>
    Per D-05 and RESEARCH Pattern 4 feeds:

    In `admission_bridge.rs`, at the points where Orphaned / Rejected / Replaced outcomes are already handled:
    1. Orphaned: after staging orphan body, `self.compact_extra_txn.push(wtxid, transaction.clone())` (or shared helper).
    2. Rejected: if transaction body is in scope, `self.compact_extra_txn.push_gated(wtxid, tx.clone())`.
    3. Replaced: BEFORE `remove_stored_transactions_with_status` / `record_replaced` removes victim indexes, resolve victim Transaction bodies from `transactions_by_txid` (or relay_serving records) and push each available victim; use victim wtxid from relay_serving record when present.

    Keep helpers small with early returns. Do not invent package-relay APIs.

    Extend admission_bridge_cases (or compact_receive tests) with Arrange/Act/Assert proofs that orphan and reject feeds land in `compact_extra_txn` (expose a `#[cfg(test)]` / `pub(super)` inspector if needed, e.g. `compact_extra_txn_len()` or test-only accessor).

    Refresh parity breadcrumbs if admission_bridge touch warrants updated Knots anchors for AddToCompactExtraTransactions call sites (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib admission_bridge -- --nocapture</automated>
  </verify>
  <done>Orphan/reject/replaced-victim feeds push into CompactExtraTxnBuffer; tests prove at least orphan and reject inserts; no package/filter activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wire CompactBlock → shell intercept | Untrusted peer payload enters reconstruction via existing init_compact_block_download validation |
| Admission outcomes → extra buffer | Locally observed tx bodies enter bounded reconstruction cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05 | Tampering | CompactBlock intercept facts | mitigate | Reuse existing CompactBlockInitOutcome Invalid/Failed paths; do not weaken payload validation; no chainstate writes from partial state (D-06) |
| T-119-06 | Denial of Service | Extra buffer feeds from rejects | mitigate | push_gated enforces per-tx size limit; ring still capped by Plan 01 bounds |
| T-119-07 | Elevation of Privilege | Admission bridge feeds | mitigate | Do not add package relay, bloom, or compact-filter surfaces (D-08); only push into existing reconstruction buffer |
| T-119-08 | Spoofing | Candidate source trust | accept | Mempool/extra candidates are local node state, not peer-asserted identity; short-ID matching remains cryptographic via existing SipHash helpers |
</threat_model>

<verification>
- Live CompactBlock receive uses injected facts on both receive_message and receive_sync_message.
- Admission feeds populate the extra buffer without unbounded growth.
- Existing non-compact message paths unchanged.
</verification>

<success_criteria>
- Inbound CompactBlock on ManagedPeerNetwork no longer always uses empty CompactBlockReceiveFacts::default().
- Live receive supplies mempool candidates and bounded extras into handle_compact_block_download.
- Extra buffer receives Knots-aligned orphan/reject/replaced-victim feeds.
</success_criteria>

<output>
After completion, create `.planning/phases/119-compact-receive-mempool-candidate-injection/119-02-SUMMARY.md`
</output>
