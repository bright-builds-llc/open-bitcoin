---
phase: 141-durable-fjall-coins-adapter
plan: "03"
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-node/src/chainstate.rs
  - packages/open-bitcoin-node/src/chainstate/tests.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
autonomous: true
requirements:
  - COIN-01
user_setup: []
must_haves:
  truths:
    - "ChainstateStore exposes get_coin, have_coin, batch_write, best_block, head_blocks, load_undo, and save_undo while load_snapshot and save_snapshot remain for leftover persist writes."
    - "MemoryChainstateStore is the in-memory test parent for that full view-backed surface; undo lives on the store, not only inside a leftover snapshot DTO."
    - "encode_block_undo / decode_block_undo exist as their own snapshot_codec functions so Plan 04 can persist undo: records without embedding undo in live coin truth."
    - "ManagedChainstate::persist still calls save_snapshot(self.chainstate.snapshot()) unchanged."
  artifacts:
    - path: packages/open-bitcoin-node/src/chainstate.rs
      provides: "Grown ChainstateStore + MemoryChainstateStore view-backed impl"
      contains: "fn load_undo"
    - path: packages/open-bitcoin-node/src/storage/snapshot_codec.rs
      provides: "Standalone BlockUndo encode/decode"
      contains: "pub(crate) fn encode_block_undo"
  key_links:
    - from: packages/open-bitcoin-node/src/chainstate.rs
      to: packages/open-bitcoin-chainstate/src/coins.rs
      via: "MemoryChainstateStore delegates coins methods to MemoryCoinsView"
      pattern: "fn batch_write"
    - from: packages/open-bitcoin-node/src/storage/snapshot_codec.rs
      to: packages/open-bitcoin-core/src
      via: "BlockUndoDto extracted as encode_block_undo"
      pattern: "fn encode_block_undo"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 141-2026-09-04T17-37-21
generated_at: 2026-09-04T18:25:00.000Z
---

# Phase 141 Plan 03: Undo Codec Extract and Memory View-Backed Store

<objective>
Grow `ChainstateStore` / `MemoryChainstateStore` to the view-backed surface and extract standalone undo encode/decode so disconnect does not require a full leftover UTXO blob and Plan 04 can persist `undo:` records.

Purpose: D-22 requires the in-memory test parent to implement the same store surface as the later Fjall adapter. D-13 needs `BlockUndo` as its own codec before Fjall records land in Plan 04. Leftover `save_snapshot` / `load_snapshot` stay so Phase 139/140 persist write sites still compile (D-19).

Output: Grown trait, `MemoryChainstateStore` coins/undo maps, `encode_block_undo` / `decode_block_undo`. No Fjall `save_undo` yet, no schema bump, no persist cutover, no `ReplayBlocks`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/141-durable-fjall-coins-adapter/141-CONTEXT.md
@.planning/phases/141-durable-fjall-coins-adapter/141-RESEARCH.md
@packages/open-bitcoin-node/src/chainstate.rs
@packages/open-bitcoin-node/src/chainstate/tests.rs
@packages/open-bitcoin-node/src/storage/snapshot_codec.rs
@packages/open-bitcoin-chainstate/src/coins.rs
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/chainstate.rs today:
```rust
pub trait ChainstateStore {
    fn load_snapshot(&self) -> Option<ChainstateSnapshot>;
    fn save_snapshot(&mut self, snapshot: ChainstateSnapshot);
}

pub struct MemoryChainstateStore {
    maybe_snapshot: Option<ChainstateSnapshot>,
}

impl ManagedChainstate<S: ChainstateStore> {
    fn persist(&mut self) {
        self.store.save_snapshot(self.chainstate.snapshot());
    }
}
```

Target trait — keep leftover methods first, then add view-backed methods (D-22). Use `open_bitcoin_core::chainstate::{BlockUndo, Coin, CoinsBatch}` and `open_bitcoin_core::primitives::{BlockHash, OutPoint}`:
```rust
pub trait ChainstateStore {
    fn load_snapshot(&self) -> Option<ChainstateSnapshot>;
    fn save_snapshot(&mut self, snapshot: ChainstateSnapshot);
    fn get_coin(&self, outpoint: &OutPoint) -> Result<Option<Coin>, ChainstateError>;
    fn have_coin(&self, outpoint: &OutPoint) -> Result<bool, ChainstateError>;
    fn best_block(&self) -> Result<Option<BlockHash>, ChainstateError>;
    fn head_blocks(&self) -> Result<Vec<BlockHash>, ChainstateError>;
    fn batch_write(
        &mut self,
        writes: CoinsBatch,
        maybe_best_block: Option<BlockHash>,
    ) -> Result<(), ChainstateError>;
    fn load_undo(&self, block_hash: BlockHash) -> Result<Option<BlockUndo>, ChainstateError>;
    fn save_undo(&mut self, block_hash: BlockHash, undo: BlockUndo) -> Result<(), ChainstateError>;
}
```

`MemoryChainstateStore` fields after this plan:
```rust
pub struct MemoryChainstateStore {
    maybe_snapshot: Option<ChainstateSnapshot>,
    coins: MemoryCoinsView,
    undo_by_block: HashMap<BlockHash, BlockUndo>,
}
```
`from_snapshot` hydrates `coins` from `snapshot.utxos` and `undo_by_block` from `snapshot.undo_by_block`, and keeps `maybe_snapshot = Some(snapshot)` so leftover persist tests still see the DTO. `Default` is empty coins, empty undo, `maybe_snapshot = None`. `batch_write` updates `coins` only (not `maybe_snapshot.utxos`) so leftover snapshot writes can diverge from view truth — the locked Plan 04 reopen window. `save_undo` / `load_undo` use `undo_by_block` only, not `maybe_snapshot.undo_by_block` after the initial hydrate.

`ManagedChainstate::persist` body must remain exactly `self.store.save_snapshot(self.chainstate.snapshot());`.

From snapshot_codec.rs — `BlockUndoDto` / `TryFrom` already exist. Extract:
```rust
pub(crate) fn encode_block_undo(undo: &BlockUndo) -> Result<Vec<u8>, StorageError>;
pub(crate) fn decode_block_undo(bytes: &[u8]) -> Result<BlockUndo, StorageError>;
```
Implement by `encode_versioned(StorageNamespace::Chainstate, &BlockUndoDto::from(undo))` and `decode_versioned` + `BlockUndo::try_from`. Do not change leftover `encode_chainstate_snapshot` JSON as live coin truth (D-12).

Do not edit `fjall_store.rs`, `coins_view.rs`, `SchemaVersion::CURRENT`, `sync.rs`, or `runtime_state.rs`. Only `MemoryChainstateStore` implements the trait in this plan (it is the sole impl today).
</interfaces>
</context>

<feature>
  <name>View-backed MemoryChainstateStore and standalone undo codec</name>
  <files>packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-node/src/storage/snapshot_codec.rs</files>
  <behavior>
    Memory store get_coin/batch_write/best_block/head_blocks/load_undo/save_undo work without reading leftover snapshot.utxos after hydrate. encode_block_undo round-trips BlockUndo through JSON that is not the full snapshot DTO.
  </behavior>
  <implementation>
    Delegate coins methods to MemoryCoinsView. Keep save_snapshot/load_snapshot for D-19 leftover writes. Extract BlockUndoDto encode/decode without deleting leftover snapshot encode.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing MemoryChainstateStore surface and undo-codec tests</name>
  <files>
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/chainstate/tests.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
  </files>
  <read_first>
    .planning/phases/141-durable-fjall-coins-adapter/141-CONTEXT.md
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/chainstate/tests.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/types.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `memory_store_from_snapshot_serves_coins_from_view_not_later_snapshot_utxos`: build a `ChainstateSnapshot` with one utxo (`Txid::from_byte_array([1; 32])`, vout 0, height 4, mtp 9) and one `undo_by_block` entry for `BlockHash::from_byte_array([2; 32])`. `MemoryChainstateStore::from_snapshot`. Then `save_snapshot` a different snapshot whose `utxos` is empty and whose `undo_by_block` is empty. `get_coin` still returns the original coin. `load_undo([2; 32])` still returns the original undo. `load_snapshot()` returns the empty leftover snapshot. This proves leftover DTO is not live coin/undo truth after hydrate (D-15 shape on the memory parent, D-22).
    - Test `memory_store_batch_write_updates_view_and_best_block`: `Default` store. `batch_write` one `unspent_dirty` coin with `Some(BlockHash::from_byte_array([3; 32]))`. `get_coin` is `Ok(Some(coin))`, `best_block` is `Ok(Some([3; 32]))`, `head_blocks` is `Ok(vec![])`, `load_snapshot()` is `None`.
    - Test `memory_store_save_undo_round_trips_without_snapshot`: `save_undo([4; 32], BlockUndo { transactions: vec![TxUndo { restored_inputs: vec![coin] }] })` then `load_undo` equals that value. `load_undo([9; 32])` is `Ok(None)`.
    - Test `managed_persist_still_calls_save_snapshot`: keep the existing test `persist path still calls save_snapshot` in `chainstate/tests.rs` (current string `expect("persist path still calls save_snapshot")`). After edits it must still pass and `fn persist` must still contain `save_snapshot(self.chainstate.snapshot())` (D-19).
    - Test `encode_block_undo_round_trips_and_is_not_full_snapshot`: `encode_block_undo` / `decode_block_undo` on a one-tx undo with height 6 and mtp 11. Decoded equals input. Encoded JSON must contain `transactions` and must not contain the key `"utxos"` (D-12, D-13).
  </behavior>
  <action>
    Create compiling stubs so tests fail on behavior (RED). Per D-13, D-15, D-19, D-22:

    1. Add the new `ChainstateStore` methods. Temporary stubs on `MemoryChainstateStore` may return `Ok(None)` / `Ok(vec![])` / `Ok(())` so the new tests compile and fail.
    2. Add the five tests to `packages/open-bitcoin-node/src/chainstate/tests.rs` except put `encode_block_undo_round_trips_and_is_not_full_snapshot` in `packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs`.
    3. Declare `pub(crate) fn encode_block_undo` and `pub(crate) fn decode_block_undo` in `snapshot_codec.rs`. Stub decode to `Corruption` so the round-trip test fails.
    4. Do not change `ManagedChainstate::persist`. Do not add `FjallNodeStore` methods. Do not bump schema. Do not create `undo:` Fjall keys (Plan 04). Do not add LevelDB or rust-bitcoin. `MemoryCoinsView` is already exported from `open_bitcoin_core::chainstate`.
    5. Keep `chainstate.rs` under 628 lines. If the file would exceed that, move `MemoryChainstateStore` impls to `chainstate/memory_store.rs` (foo.rs plus foo/; `chainstate.rs` already has `chainstate/tests.rs`).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase141-03-store-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate::tests -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase141-03-undo-codec-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::snapshot_codec::tests -- encode_block_undo --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/chainstate.rs` contains `fn load_undo` and `fn save_undo` and `fn get_coin` and `fn batch_write` on `trait ChainstateStore`
    - `packages/open-bitcoin-node/src/chainstate.rs` `fn persist` still contains `save_snapshot(self.chainstate.snapshot())`
    - `packages/open-bitcoin-node/src/chainstate/tests.rs` contains `memory_store_from_snapshot_serves_coins_from_view_not_later_snapshot_utxos` and `memory_store_save_undo_round_trips_without_snapshot`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec.rs` contains `pub(crate) fn encode_block_undo` and `pub(crate) fn decode_block_undo`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs` contains `encode_block_undo_round_trips_and_is_not_full_snapshot`
    - Task 1 `chainstate::tests` cargo test is RED on the new store-surface tests before Task 2
  </acceptance_criteria>
  <done>Failing tests exist for the view-backed MemoryChainstateStore surface and standalone undo codec; leftover persist is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MemoryChainstateStore surface and undo encode/decode</name>
  <files>
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/chainstate/tests.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    packages/open-bitcoin-chainstate/src/coins/memory.rs
    standards/core/code-shape.md
  </read_first>
  <behavior>
    Same tests as Task 1 must pass, including the existing persist snapshot test.
  </behavior>
  <action>
    Implement GREEN per D-13, D-19, D-22:

    **MemoryChainstateStore:** Add `coins: MemoryCoinsView` and `undo_by_block: HashMap<BlockHash, BlockUndo>`. `from_snapshot(snapshot)` calls `MemoryCoinsView::from_coins(snapshot.utxos.clone(), snapshot.active_chain.last().map(|p| p.block_hash))` and `undo_by_block = snapshot.undo_by_block.clone()`, then stores `maybe_snapshot = Some(snapshot)`. Delegate `get_coin` / `have_coin` / `best_block` / `head_blocks` / `batch_write` to `self.coins` (Plan 01 fallible signatures). `save_undo` inserts into `undo_by_block`. `load_undo` returns `Ok(self.undo_by_block.get(&block_hash).cloned())`. `save_snapshot` / `load_snapshot` only touch `maybe_snapshot`.

    **Undo codec:** `encode_block_undo` uses existing `BlockUndoDto::from` + `encode_versioned(StorageNamespace::Chainstate, ...)`. `decode_block_undo` uses `decode_versioned` + `BlockUndo::try_from`. Do not encode the full `ChainstateSnapshotDto`.

    **Illegal:** Changing `persist` to coins writes (D-19). Fjall `undo:` keys (Plan 04). Schema bump. `ReplayBlocks`. `unwrap()` in production. Putting Fjall in `chainstate.rs`.

    After GREEN, existing `chainstate/tests.rs` suite must still pass. Keep production files under 628 lines.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase141-03-store-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate::tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate::tests` exits 0
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::snapshot_codec::tests -- encode_block_undo` exits 0
    - `packages/open-bitcoin-node/src/chainstate.rs` contains `MemoryCoinsView` and `undo_by_block` and `fn persist` still contains `save_snapshot(self.chainstate.snapshot())`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec.rs` `fn encode_block_undo` contains `BlockUndoDto` and does not call `encode_chainstate_snapshot`
    - `rg -n "save_chainstate_snapshot|persist_progress" packages/open-bitcoin-node/src/chainstate.rs` prints nothing
    - `bash scripts/check-pure-core-deps.sh` exits 0
  </acceptance_criteria>
  <done>MemoryChainstateStore implements the full view-backed surface; undo encodes as its own record; leftover persist writes are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Leftover `ChainstateSnapshot` DTO → live view | Hydrating coins from a later `save_snapshot` would restore dual truth on the test parent. |
| Undo JSON bytes → `BlockUndo` | Decode must fail closed as `StorageError::Corruption`, not invent empty undo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Tampering | `MemoryChainstateStore::get_coin` | mitigate | Serve `MemoryCoinsView` after hydrate; test proves a later empty leftover snapshot does not clear coins (D-15, D-22). |
| T-141-12 | Tampering | `decode_block_undo` | mitigate | Reuse `decode_versioned` + `TryFrom`; invalid JSON is `Corruption`. |
| T-141-13 | Elevation of Privilege | `ManagedChainstate::persist` | accept | Leftover snapshot writes stay until Phase 142 (D-19). Test locks the `save_snapshot` call site. |
| T-141-14 | Information Disclosure | undo encode | mitigate | Encode only `BlockUndoDto`; test forbids `"utxos"` in the undo blob. |
</threat_model>

<verification>
`bun run scripts/command-timings.ts run --key phase141-03-store-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib chainstate::tests`
`bun run scripts/command-timings.ts run --key phase141-03-undo-codec -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::snapshot_codec::tests -- encode_block_undo`
`bash scripts/check-pure-core-deps.sh`
Do not overlap Cargo jobs on the same target directory.
</verification>

<success_criteria>
- `ChainstateStore` has the seven view-backed methods plus leftover snapshot load/save.
- `MemoryChainstateStore` serves coins and undo from its own maps.
- `encode_block_undo` / `decode_block_undo` exist and omit `"utxos"`.
- `ManagedChainstate::persist` still writes leftover snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/141-durable-fjall-coins-adapter/141-03-SUMMARY.md`
</output>
