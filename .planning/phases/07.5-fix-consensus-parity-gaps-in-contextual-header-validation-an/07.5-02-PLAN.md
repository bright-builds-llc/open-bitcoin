---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 02
type: execute
wave: 2
depends_on:
  - 07.5-01
files_modified:
  - packages/open-bitcoin-consensus/src/signature.rs
  - packages/open-bitcoin-consensus/src/signature/tests.rs
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
autonomous: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-20T12:40:57Z
must_haves:
  truths:
    - Non-strict legacy ECDSA verification accepts Knots-compatible lax DER signatures instead of collapsing them to plain false when strict DER parsing fails.
    - `STRICTENC`, `DERSIG`, and `LOW_S` still enforce strict DER, defined hash types, and low-S behavior exactly where the flags require them.
    - The phase closes on passing targeted regressions plus `bash scripts/verify.sh`, with summary evidence that lets a follow-up review or verifier close CR-01 and CR-02.
  artifacts:
    - packages/open-bitcoin-consensus/src/signature.rs
    - packages/open-bitcoin-consensus/src/signature/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
  key_links:
    - `TransactionSignatureChecker::verify_ecdsa` still routes through `parse_ecdsa_signature_for_verification` before signature verification.
    - `packages/open-bitcoin-consensus/src/signature.rs` uses `ecdsa::Signature::from_der_lax()` only when `STRICTENC`, `DERSIG`, and `LOW_S` are all absent.
    - `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` cites CR-01, CR-02, and the requirement mapping that replaced roadmap `TBD`.
---

<objective>
Restore Knots-compatible non-strict legacy ECDSA verification, then close Phase 07.5 with full repo-native verification and explicit closure evidence for the two 07.4 review findings.

Purpose: close review finding CR-02 and leave auditable proof that both contextual-header and lax-DER parity gaps are actually fixed before Phase 8 starts.
Output: a lax-DER non-strict parse path in `signature.rs`, a focused regression using a Knots-compatible historical vector, and a summary backed by passing targeted tests plus `bash scripts/verify.sh`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md
@.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-REVIEW.md
@.planning/phases/03.4-consensus-parity-closure/03.4-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/pubkey.cpp
@packages/bitcoin-knots/src/script/interpreter.cpp
@packages/bitcoin-knots/src/test/data/script_tests.json
@scripts/verify.sh
@rust-toolchain.toml

<interfaces>
From packages/open-bitcoin-consensus/src/signature.rs:
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum SignatureError {
    EmptySignature,
    InvalidDer,
    InvalidHashType(u32),
    InvalidPublicKey,
    NonCompressedPublicKey,
    NonLowS,
    IncorrectSignature,
    UnsupportedSigVersion,
}

fn parse_ecdsa_signature_for_verification(
    bytes: &[u8],
    flags: ScriptVerifyFlags,
) -> Result<Option<ParsedEcdsaSignature>, SignatureError>;
```

From packages/open-bitcoin-consensus/src/signature.rs:
```rust
impl<'a, C: Verification> TransactionSignatureChecker<'a, C> {
    pub fn verify_ecdsa(
        &self,
        request: EcdsaVerificationRequest<'_>,
        flags: ScriptVerifyFlags,
    ) -> Result<bool, SignatureError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Use lax DER parsing only on the non-strict legacy ECDSA path</name>
  <files>packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, packages/bitcoin-knots/src/pubkey.cpp, packages/bitcoin-knots/src/script/interpreter.cpp, packages/bitcoin-knots/src/test/data/script_tests.json, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-REVIEW.md, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/testing.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - Non-strict legacy parsing returns `Some(ParsedEcdsaSignature)` for a Knots-compatible lax-DER vector that strict DER parsing rejects.
    - `STRICTENC`, `DERSIG`, and `LOW_S` still return `InvalidHashType`, `InvalidDer`, or `NonLowS` exactly where they do today.
    - Public-key parsing and hybrid-key normalization behavior remain unchanged outside the signature-parse branch.
  </behavior>
  <action>In `packages/open-bitcoin-consensus/src/signature.rs`, leave `parse_ecdsa_signature()` unchanged but rewrite `parse_ecdsa_signature_for_verification()` so it computes one `require_strict_der` boolean from `ScriptVerifyFlags::DERSIG | ScriptVerifyFlags::LOW_S | ScriptVerifyFlags::STRICTENC`. When `require_strict_der` is true, keep using `ecdsa::Signature::from_der(der).map_err(|_| SignatureError::InvalidDer)?`. When it is false, replace the current strict parse fallback with `ecdsa::Signature::from_der_lax(der)` and return `Ok(None)` only when lax parsing still fails; do not add a custom DER parser or a new crypto dependency. Keep the existing `STRICTENC` hashtype validation before parsing, keep the `LOW_S` normalization check after parsing, and leave `parse_public_key_for_verification()` untouched. In `packages/open-bitcoin-consensus/src/signature/tests.rs`, add a focused regression named `non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector` that embeds one historical Knots-compatible lax-DER signature byte sequence from `script_tests.json` or an equivalent upstream vector and asserts three exact outcomes: `ScriptVerifyFlags::NONE` returns `Some(...)`, `ScriptVerifyFlags::DERSIG` returns `Err(SignatureError::InvalidDer)`, and `ScriptVerifyFlags::LOW_S` still enforces `Err(SignatureError::NonLowS)` when the vector is high-S. Keep the existing `verification_helpers_follow_flag_gated_legacy_rules` test and update it only if the new vector makes one assertion redundant.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "from_der_lax" packages/open-bitcoin-consensus/src/signature.rs` returns exactly one match.
    - `rg -n "fn non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector" packages/open-bitcoin-consensus/src/signature/tests.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The non-strict legacy ECDSA path matches Knots’ lax-DER behavior without weakening the strict-flag gates or changing unrelated signature code.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase with repo-native verification and explicit review-gap evidence</name>
  <files>packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md</files>
  <read_first>packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-PLAN.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-REVIEW.md, scripts/verify.sh, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/verification.md</read_first>
  <behavior>
    - The final tree passes the targeted contextual-header regression, the lax-DER regression, and the repo-native verification contract.
    - The summary gives downstream review or verification work enough exact evidence to close CR-01 and CR-02 without re-discovering the fixes.
  </behavior>
  <action>After Task 1 and plan `07.5-01` are complete, run `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact`, then `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`, then `bash scripts/verify.sh` exactly as D-08 and repo-local guidance require. If verification fails because the new signature regression or the contextual-header plumbing exposed a missing targeted assertion, add only the smallest test change needed and rerun the same sequence; do not widen into unrelated consensus cleanup. In `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md`, explicitly cite `CR-01` and `CR-02` from `07.4-REVIEW.md`, name the exact new proof points `bad-diffbits`, `time-too-new`, and `from_der_lax`, and record that Phase 07.5 replaces roadmap `Requirements: TBD` with the explicit mapping `CONS-02`, `CONS-03`, `VER-01`, and `VER-02`. State that the summary exists to support the follow-up review or verifier pass required by D-08; do not mark the review gap as closed in docs without the actual passing commands.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify.sh` exits with status 0.
    - `rg -n "CR-01|CR-02|CONS-02|CONS-03|VER-01|VER-02|bad-diffbits|time-too-new|from_der_lax" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` returns all nine exact strings.
    - `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` states that the summary supports the follow-up review or verifier pass required by D-08.
  </acceptance_criteria>
  <done>The lax-DER fix is proven, the repo-native verifier passes, and the phase summary leaves an explicit audit trail for closing both 07.4 review findings.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if the lax-DER repair weakens any `STRICTENC`, `DERSIG`, or `LOW_S` path or if the final verifier does not pass, stop and leave the phase open rather than documenting a false closure.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script verification flags -> signature parsing mode | Strict flag combinations must keep their current validation semantics while non-strict paths gain Knots-compatible lax DER support |
| verification evidence -> review closure | The follow-up reviewer or verifier will rely on the summary and command results to decide whether CR-01 and CR-02 are actually closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-04 | T | `packages/open-bitcoin-consensus/src/signature.rs` | mitigate | Gate `from_der_lax()` behind the explicit non-strict branch only, keep strict parsing and low-S checks intact, and prove the behavior with an upstream-compatible vector plus existing strict-gate tests. |
| T-07.5-05 | R | `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` | mitigate | Require the summary to cite CR-01, CR-02, the exact new proof points, and the passing verification commands so review closure is evidence-backed rather than aspirational. |
| T-07.5-06 | D | phase closeout verification | mitigate | Run the two exact targeted regressions and then `bash scripts/verify.sh`; if any command fails, keep the phase open and fix the failing path instead of downgrading the acceptance criteria. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The legacy signature parser uses `from_der_lax()` only on the non-strict path and keeps strict-flag semantics unchanged.
- The phase ends with passing targeted regressions plus the repo-native verification contract.
- The final summary contains enough exact evidence for a follow-up review or verifier pass to close CR-01 and CR-02 honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md`
</output>
