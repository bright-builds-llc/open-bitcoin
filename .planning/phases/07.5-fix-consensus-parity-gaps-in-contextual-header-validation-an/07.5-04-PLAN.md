---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 04
type: execute
wave: 4
depends_on:
  - 07.5-03
files_modified:
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/context/tests.rs
  - packages/open-bitcoin-consensus/src/block/difficulty.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-consensus/tests/parity_closure.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T23:31:08Z
must_haves:
  truths:
    - Contextual difficulty parity matches Knots' non-boundary min-difficulty recovery after a special block by reusing the last non-special target instead of `previous_header.bits`.
    - The live chainstate connect path derives that recovery target from active-chain history and injects it into typed block-validation context on non-boundary heights.
    - Phase 07.5 closeout evidence only claims CR-01 closure after the new non-boundary recovery regressions and `bash scripts/verify.sh` pass, while preserving the existing CR-02 proof chain.
  artifacts:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/block/difficulty.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
  key_links:
    - packages/open-bitcoin-chainstate/src/engine.rs derives `maybe_min_difficulty_recovery_target` from `active_chain` and threads it into `BlockValidationContext`.
    - packages/open-bitcoin-consensus/src/block/difficulty.rs uses that typed input on the on-time non-boundary `allow_min_difficulty_blocks` branch instead of falling back to `context.previous_header.bits`.
    - `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md` cites the new recovery proof strings plus `bash scripts/verify.sh` before claiming CR-01 closure, and `07.5-03-SUMMARY.md` is corrected to stop overclaiming.
---

<objective>
Close the final Phase 07.5 verifier gap by restoring Knots-style non-boundary min-difficulty recovery after a special block and by correcting the summary chain so CR-01 is only claimed after that branch is actually proven.

Purpose: finish the last actionable contextual-difficulty parity gap before Phase 8 and leave truthful evidence for the follow-up reviewer or verifier.
Output: typed recovery-target context in consensus validation, active-chain walkback plumbing from chainstate, focused recovery regressions in consensus and chainstate, a corrected `07.5-03-SUMMARY.md`, and a new `07.5-04-SUMMARY.md` that becomes the authoritative gap-closure artifact.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/pow.cpp
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-consensus/src/block/difficulty.rs
@packages/open-bitcoin-chainstate/src/engine.rs
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-consensus/src/context.rs:
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BlockValidationContext {
    pub height: u32,
    pub previous_header: BlockHeader,
    pub maybe_retarget_anchor: Option<RetargetAnchor>,
    pub previous_median_time_past: i64,
    pub current_time: i64,
    pub consensus_params: ConsensusParams,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct RetargetAnchor {
    pub first_block_time: i64,
}
```

From packages/open-bitcoin-consensus/src/block/difficulty.rs:
```rust
pub(crate) fn difficulty_adjustment_interval(
    consensus_params: &crate::context::ConsensusParams,
) -> i64;

pub(crate) fn next_work_required(
    header: &BlockHeader,
    context: &BlockValidationContext,
) -> Result<u32, BlockValidationError>;
```

From packages/open-bitcoin-chainstate/src/engine.rs:
```rust
pub fn connect_block_with_current_time(
    &mut self,
    block: &Block,
    chain_work: u128,
    current_time: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ChainstateError>;

fn maybe_retarget_anchor(
    active_chain: &[ChainPosition],
    height: u32,
    consensus_params: &ConsensusParams,
) -> Option<RetargetAnchor>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed non-boundary min-difficulty recovery input and make the consensus helper follow Knots' walkback rule</name>
  <files>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block/difficulty.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block/difficulty.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, packages/bitcoin-knots/src/pow.cpp, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - On non-boundary heights with `allow_min_difficulty_blocks` enabled, a late special block still returns `pow_limit_bits`, but the next on-time block reuses the last non-special target instead of `context.previous_header.bits`.
    - `BlockValidationContext` carries one new optional, `maybe_`-prefixed typed recovery field for that last non-special target, and existing constructors outside the new regression paths set it explicitly to `None`.
    - The consensus regression proves the recovered target branch directly, while the existing retarget-boundary regression remains green without rewriting its scope.
  </behavior>
  <action>Per D-01 through D-04 and the verifier's remaining `missing` items, add a small typed value object in `packages/open-bitcoin-consensus/src/context.rs` named `MinDifficultyRecoveryTarget` with `bits: u32`, then extend `BlockValidationContext` with `maybe_min_difficulty_recovery_target: Option<MinDifficultyRecoveryTarget>`. Keep `maybe_retarget_anchor` and `current_time` unchanged. In `packages/open-bitcoin-consensus/src/block/difficulty.rs`, leave the already-green retarget-boundary math and the existing late special-block `pow_limit_bits` branch intact, but replace the on-time non-boundary fallback from Knots `pow.cpp:29-35`: when `allow_min_difficulty_blocks` is true and the candidate block is not late, use `context.maybe_min_difficulty_recovery_target.bits` as the expected target, and return `BlockValidationResult::InvalidHeader` with reject reason `bad-diffbits` plus a concrete debug string such as `missing min-difficulty recovery target` if that field is absent instead of silently falling back to `context.previous_header.bits`. Add a focused helper regression in `packages/open-bitcoin-consensus/src/block/difficulty.rs` named `next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block` that models one normal block, one late special min-difficulty block, and the next on-time block, then asserts the recovered target equals the earlier non-special bits and not the special block's `pow_limit_bits`. In `packages/open-bitcoin-consensus/src/block/tests.rs`, update any touched `BlockValidationContext` constructors to set the new field explicitly and keep `contextual_header_rejects_previous_bits_at_retarget_boundary` unchanged except for constructor compile fixes. In `packages/open-bitcoin-consensus/src/context/tests.rs`, extend the existing block-context coverage to assert the new typed field round-trips. In `packages/open-bitcoin-consensus/tests/parity_closure.rs`, make only the constructor updates needed for the expanded context surface; do not add a second parity harness for this gap.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features context::tests::block_context_carries_expected_pow_and_time_fields -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub maybe_min_difficulty_recovery_target: Option<MinDifficultyRecoveryTarget>" packages/open-bitcoin-consensus/src/context.rs` returns exactly one match.
    - `rg -n "struct MinDifficultyRecoveryTarget" packages/open-bitcoin-consensus/src/context.rs` returns exactly one match.
    - `rg -n "fn next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block" packages/open-bitcoin-consensus/src/block/difficulty.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The consensus helper can no longer accept the on-time recovery block against `previous_header.bits` after a special block, and the new typed context makes the missing recovery input explicit instead of implicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive the recovery target from active-chain history and prove the live connect path uses it</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/block/difficulty.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, packages/bitcoin-knots/src/pow.cpp, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md</read_first>
  <behavior>
    - Chainstate computes the Knots walkback result from `active_chain` only on non-boundary heights when `allow_min_difficulty_blocks` is true, and passes that exact target into `BlockValidationContext`.
    - The explicit `current_time` path and the already-green retarget-boundary anchor path remain intact; this task does not re-open their logic.
    - The new chainstate regression proves a recovered non-special target succeeds after a late special block and that reusing the special block's `pow_limit_bits` fails with `bad-diffbits`.
  </behavior>
  <action>Per D-02 through D-04 and the verifier's remaining `missing` items, extend `packages/open-bitcoin-chainstate/src/engine.rs` with a sibling helper named `maybe_min_difficulty_recovery_target(...)` that mirrors Knots `GetNextWorkRequired()` on `pow.cpp:31-35`: on non-boundary heights when `allow_min_difficulty_blocks` is true, start from the current tip and walk backward while a position has `header.bits == consensus_params.pow_limit_bits` and `height % difficulty_adjustment_interval(consensus_params) != 0`, then return that stopping block's bits inside `MinDifficultyRecoveryTarget`; otherwise return `None`. Keep `maybe_retarget_anchor(...)`, `current_time`, and all node-layer files untouched. Inject the new recovery field into `BlockValidationContext` inside `connect_block_with_current_time()`. In `packages/open-bitcoin-chainstate/src/engine/tests.rs`, add `connect_block_recovers_last_non_special_target_after_special_min_difficulty_block` using a short deterministic interval and an active chain that contains one normal non-special block followed by a late special min-difficulty block. The test should first show that an on-time next block with the recovered earlier bits connects successfully, then assert that an otherwise identical block reusing the special block's `pow_limit_bits` returns `ChainstateError::BlockValidation { source.reject_reason == "bad-diffbits" }`. Keep the existing `connect_block_rejects_wrong_bits_at_retarget_boundary` and `connect_block_uses_explicit_current_time_for_future_time_rejection` tests green without changing their assertions.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn maybe_min_difficulty_recovery_target" packages/open-bitcoin-chainstate/src/engine.rs` returns exactly one match.
    - `rg -n "maybe_min_difficulty_recovery_target" packages/open-bitcoin-chainstate/src/engine.rs` shows the helper result flowing into `BlockValidationContext`.
    - `rg -n "fn connect_block_recovers_last_non_special_target_after_special_min_difficulty_block" packages/open-bitcoin-chainstate/src/engine/tests.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The live connect path supplies the exact recovery target Knots would use after a special block, and the active-path regression proves the next on-time block no longer depends on `previous_header.bits`.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the closeout evidence so CR-01 is only claimed after the recovery branch is proven</name>
  <files>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md</files>
  <read_first>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md, packages/open-bitcoin-consensus/src/block/difficulty.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, scripts/verify.sh, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/verification.md</read_first>
  <behavior>
    - `07.5-03-SUMMARY.md` stops claiming CR-01 was already closed by the boundary-only proof set and explicitly points to `07.5-04-SUMMARY.md` for the final recovery proof.
    - `07.5-04-SUMMARY.md` becomes the authoritative gap-closure artifact and cites the new recovery proof strings, the already-green boundary and CR-02 proof chains, and the passing repo-native verifier.
    - If the new recovery regressions or `bash scripts/verify.sh` fail, the summary must keep CR-01 open rather than restating the previous overclaim.
  </behavior>
  <action>After Tasks 1 and 2 are green, rerun only the proof surface needed for this gap closure: `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`, and then `bash scripts/verify.sh`. Do not rerun or rewrite the already-green lax-DER implementation work except to reference its existing proof chain from `07.5-02-SUMMARY.md`. If any command fails, keep CR-01 marked open in the summary files instead of claiming closure. In `07.5-03-SUMMARY.md`, replace the current premature CR-01 closure language with a superseded note pointing to `07.5-04-SUMMARY.md`. Then create `07.5-04-SUMMARY.md` as the authoritative artifact, explicitly citing `CR-01`, `CR-02`, `CONS-02`, `CONS-03`, `VER-01`, `VER-02`, `next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block`, `connect_block_recovers_last_non_special_target_after_special_min_difficulty_block`, `contextual_header_rejects_previous_bits_at_retarget_boundary`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, the existing CR-02 proof strings from `07.5-02-SUMMARY.md`, and `bash scripts/verify.sh`. Keep retarget-boundary, explicit-current-time, and lax-DER implementation details out of scope here except where that proof chain needs to reference them.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "superseded|07\\.5-04|CR-01" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md` shows the old overclaim has been replaced with a handoff to `07.5-04`.
    - `rg -n "CR-01|CR-02|CONS-02|CONS-03|VER-01|VER-02|next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block|connect_block_recovers_last_non_special_target_after_special_min_difficulty_block|contextual_header_rejects_previous_bits_at_retarget_boundary|connect_block_rejects_wrong_bits_at_retarget_boundary|bash scripts/verify\\.sh" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md` returns all required proof strings.
    - `bash scripts/verify.sh` exits with status 0 on the final tree.
  </acceptance_criteria>
  <done>The remaining CR-01 gap is proven on the corrected recovery branch, `07.5-03-SUMMARY.md` no longer overclaims closure, and `07.5-04-SUMMARY.md` gives the follow-up verifier or reviewer one truthful artifact for the final Phase 07.5 decision.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if the on-time non-boundary `allow_min_difficulty_blocks` branch can still validate against `previous_header.bits` after a special block, or if the summary chain still claims CR-01 closure without the new recovery proofs, stop and leave the phase open.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active-chain history -> typed min-difficulty recovery target | The engine must derive the same last non-special target Knots would walk back to before consensus computes expected work |
| typed recovery target -> `next_work_required()` | Missing or wrong recovery context would silently create a consensus split on testnet-like networks unless the helper rejects it |
| summary evidence -> follow-up review or verifier closure | Downstream closure decisions depend on the summary naming the actual recovery proofs instead of the incomplete boundary-only proof set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-10 | T | `packages/open-bitcoin-consensus/src/block/difficulty.rs` | mitigate | Replace the on-time non-boundary fallback with the typed recovery target, reject missing recovery context with `bad-diffbits`, and pin the branch in `next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block`. |
| T-07.5-11 | T | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Derive `maybe_min_difficulty_recovery_target` from `active_chain` using the Knots walkback rule only on the relevant non-boundary heights, then prove it through `connect_block_recovers_last_non_special_target_after_special_min_difficulty_block`. |
| T-07.5-12 | R | `07.5-03-SUMMARY.md` and `07.5-04-SUMMARY.md` | mitigate | Supersede the old overclaim, require the new recovery proof strings plus `bash scripts/verify.sh` in `07.5-04-SUMMARY.md`, and reference the existing CR-02 proof chain instead of inventing new unsupported claims. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The consensus helper matches Knots on the non-boundary min-difficulty recovery branch and can no longer silently fall back to `previous_header.bits` after a late special block.
- The active chainstate path derives and injects the recovery target through typed context without reopening unrelated retarget-boundary, explicit-current-time, or lax-DER implementation work.
- The final summary chain is truthful: `07.5-03-SUMMARY.md` is superseded, and `07.5-04-SUMMARY.md` closes CR-01 only after the new recovery proofs and `bash scripts/verify.sh` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md`
</output>
