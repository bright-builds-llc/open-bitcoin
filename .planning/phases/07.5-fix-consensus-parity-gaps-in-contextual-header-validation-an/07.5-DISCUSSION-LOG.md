# Phase 07.5: Fix consensus parity gaps in contextual header validation and lax DER signature verification - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution
> agents. Decisions are captured in CONTEXT.md; this log preserves the
> alternatives considered.

**Date:** 2026-04-20T12:08:15.308Z
**Phase:** 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
**Mode:** Yolo
**Areas discussed:** Fix boundary, Contextual header validation parity, Lax DER verification parity, Verification and completion

---

## Fix boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Limit the phase to the two reviewed consensus parity gaps plus directly required supporting plumbing | Fix only the two review findings, allowing minimal supporting updates in context, callers, and tests where the fixes genuinely require them. | ✓ |
| Widen into a broader consensus parity audit | Use this phase as a general pass over other consensus discrepancies. | |
| Defer one issue and fix only the easier gap | Narrow to a single finding now and leave the other for later. | |

**User's choice:** Limit the phase to the two reviewed consensus parity gaps plus directly required supporting plumbing
**Notes:** Yolo mode kept the phase narrow and phase-local: fix the two parity findings, permit only the supporting file surface they need, and avoid turning this into a general audit before Phase 8.

---

## Contextual header validation parity

| Option | Description | Selected |
|--------|-------------|----------|
| Match Knots for `bad-diffbits`, `time-too-old`, and `time-too-new` on the active block-validation path | Treat Knots contextual header validation and next-work logic as the canonical behavior source. | ✓ |
| Patch only the missing future-time check | Fix the obvious time guard and leave difficulty behavior unchanged. | |
| Patch only the missing difficulty check | Fix `nBits` comparison but leave future-time enforcement for later. | |

**User's choice:** Match Knots for `bad-diffbits`, `time-too-old`, and `time-too-new` on the active block-validation path
**Notes:** The phase is a parity fix, so partial closure is the wrong target. The header path should be corrected as one coherent contextual contract, even if that requires minimal context plumbing.

---

## Lax DER verification parity

| Option | Description | Selected |
|--------|-------------|----------|
| Use lax DER parsing on non-strict paths while preserving strict DER and low-S enforcement when the flags require it | Match Knots legacy verification behavior without weakening strict flag-gated paths. | ✓ |
| Keep strict DER everywhere and weaken the review expectation | Preserve the current Open Bitcoin behavior even if Knots accepts more legacy encodings. | |
| Disable the non-strict path entirely | Treat all malformed DER encodings as invalid regardless of flags. | |

**User's choice:** Use lax DER parsing on non-strict paths while preserving strict DER and low-S enforcement when the flags require it
**Notes:** The canonical behavior comes from Knots `CPubKey::Verify()` and the script flag rules, so the Open Bitcoin parser should match that split rather than reinterpret it as a policy choice.

---

## Verification and completion

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted parity regressions, repo-native verification, and a follow-up review/verifier pass | Close the phase only after the two fixes are covered directly and the repo-native contract plus follow-up review confirm the gaps are gone. | ✓ |
| Repo-native verification only | Rely on the existing verify script without phase-specific regression evidence. | |
| Unit tests only | Skip the repo-native contract and broader review/verifier loop. | |

**User's choice:** Targeted parity regressions, repo-native verification, and a follow-up review/verifier pass
**Notes:** These issues came from an advisory review, so the clean closeout is to land the fixes, rerun the review/verifier path, and prove the findings are actually resolved rather than only compiling.

---

## the agent's Discretion

- Exact helper names and placement for any new contextual validation or lax DER parsing helpers
- Whether the best regression evidence comes from repo-owned tests or imported Knots-compatible vector material
- The smallest supporting-file surface required to keep the two fixes typed, explicit, and parity-correct

## Deferred Ideas

- AI-agent-friendly CLI surface remains a Phase 8 concern.
- Do not widen this phase into a broader consensus parity audit.
- Do not pull 07.4 readability cleanup or panic-hardening work into this phase.
