---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 04
subsystem: consensus
tags: [rust, consensus, pow, chainstate, verification]
requires:
  - phase: 07.5-03
    provides: boundary retarget-anchor plumbing and the boundary-only CR-01 proof chain that required correction
  - phase: 07.5-02
    provides: the carried-forward CR-02 lax-DER proof strings
  - phase: 07.4-sweep-the-codebase-for-let-else-opportunities
    provides: the CR-01 review finding that remained open after the boundary-only closure attempt
provides:
  - typed min-difficulty recovery-target context for non-boundary contextual difficulty checks
  - active-chain walkback parity for post-special min-difficulty recovery on the live connect path
  - the authoritative Phase 07.5 closeout artifact for CR-01, CR-02, CONS-02, CONS-03, VER-01, and VER-02
affects: [phase-8-operator-interfaces, consensus-parity, follow-up-review]
tech-stack:
  added: []
  patterns:
    - non-boundary min-difficulty recovery is injected as typed context rather than reconstructed inside pure-core consensus code
    - phase-closeout summaries supersede earlier overclaims instead of restating them as final truth
key-files:
  created:
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
  modified:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/context/tests.rs
    - packages/open-bitcoin-consensus/src/block/difficulty.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-consensus/tests/parity_closure.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
key-decisions:
  - "Carry the recovered last-non-special target as a typed `MinDifficultyRecoveryTarget` instead of silently falling back to `previous_header.bits`."
  - "Derive the recovery target from `active_chain` in chainstate so consensus stays pure and Knots' walkback rule is preserved on the live path."
  - "Supersede `07.5-03-SUMMARY.md` instead of editing its claims into partial closure language, so reviewers have one authoritative artifact."
patterns-established:
  - "On allow-min-difficulty networks, non-boundary on-time work now requires caller-supplied `maybe_min_difficulty_recovery_target` context."
  - "Recovery regressions pair consensus-helper proof with live chainstate proof before a phase summary claims parity closure."
requirements-completed: [CONS-02, CONS-03, VER-01, VER-02]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T23:59:47Z
duration: 10m
completed: 2026-04-21
---

# Phase 07.5 Plan 04: Non-Boundary Min-Difficulty Recovery Closure Summary

**Typed recovery targets now preserve Knots-style post-special difficulty on both the consensus helper and the live chainstate connect path.**

## Performance

- **Duration:** 10m
- **Started:** 2026-04-21T23:50:13Z
- **Completed:** 2026-04-21T23:59:47Z
- **Tasks:** 1
- **Files modified:** 9

## Accomplishments

- Added `maybe_min_difficulty_recovery_target` to `BlockValidationContext` and made `next_work_required()` reuse the last non-special target on the on-time non-boundary allow-min-difficulty branch.
- Derived that recovery target from `active_chain` with a Knots-style walkback and proved the live connect path rejects stale post-special `pow_limit_bits`.
- Replaced the premature `07.5-03-SUMMARY.md` CR-01 closeout claim with a superseded handoff and recorded the final truthful CR-01 and CR-02 evidence here.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add typed non-boundary min-difficulty recovery input and make the consensus helper follow Knots' walkback rule** - `b9dc4ae` (`fix`)

No metadata commit was created here. Per user instruction, `STATE.md` and `ROADMAP.md` remain untouched for the orchestrator-owned post-verification flow.

## Files Created/Modified

- `packages/open-bitcoin-consensus/src/context.rs` - adds the typed optional `maybe_min_difficulty_recovery_target` field to contextual block validation state.
- `packages/open-bitcoin-consensus/src/block/difficulty.rs` - replaces the non-boundary on-time fallback with the typed recovery target and rejects missing recovery context with `bad-diffbits`.
- `packages/open-bitcoin-consensus/src/block/tests.rs` - updates helper contexts for the new typed field and adds contextual-header coverage for rejecting stale post-special bits.
- `packages/open-bitcoin-consensus/src/context/tests.rs` - exercises the expanded block-validation context surface.
- `packages/open-bitcoin-consensus/tests/parity_closure.rs` - keeps the parity-closure harness compiling against the expanded context.
- `packages/open-bitcoin-chainstate/src/engine.rs` - walks back through `active_chain` to derive the last non-special target and injects it into `BlockValidationContext`.
- `packages/open-bitcoin-chainstate/src/engine/tests.rs` - proves the live connect path rejects stale post-special `pow_limit_bits` and accepts the recovered target.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md` - marks the boundary-only artifact as superseded by this summary.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md` - records the authoritative gap-closure evidence.

## Decisions Made

- Used a narrow `MinDifficultyRecoveryTarget` value object instead of widening consensus code with ad hoc `u32` fallback behavior.
- Kept the Knots walkback in chainstate so the pure-core consensus helper only consumes typed context and does not traverse chain history directly.
- Carried the existing CR-02 lax-DER proof chain forward from `07.5-02-SUMMARY.md` rather than rerunning or rewriting that already-green implementation work.

## Verification Evidence

- `CR-01` closes here, not in `07.5-03-SUMMARY.md`, after `next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block`, `connect_block_recovers_last_non_special_target_after_special_min_difficulty_block`, `contextual_header_rejects_previous_bits_at_retarget_boundary`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, and `bash scripts/verify.sh` all passed on the corrected tree.
- `CR-02` remains closed via the carried-forward proof strings from `07.5-02-SUMMARY.md`: `non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector`, `verification_helpers_follow_flag_gated_legacy_rules`, and the final `bash scripts/verify.sh` rerun on this tree.
- Supporting consensus evidence also now includes `contextual_header_rejects_previous_bits_after_special_min_difficulty_block`, which proves `check_block_header_contextual()` accepts the recovered target and rejects stale post-special `pow_limit_bits`.
- `CONS-02`, `CONS-03`, `VER-01`, and `VER-02` are satisfied on the final tree by the exact proof commands below plus the passing repo-native verifier.
- Exact proof commands rerun for this closeout:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::difficulty::tests::next_work_required_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`
- `bash scripts/verify.sh`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Ran `cargo fmt` after the repo-native verifier rejected formatting drift in the touched Rust files**
- **Found during:** Task 1
- **Issue:** `bash scripts/verify.sh` failed on formatting after the recovery-path edits landed.
- **Fix:** Ran `cargo fmt --manifest-path packages/Cargo.toml --all` before rerunning the proof surface and repo-native verifier.
- **Files modified:** `packages/open-bitcoin-consensus/src/block/difficulty.rs`, `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** `cargo fmt --manifest-path packages/Cargo.toml --all`, `bash scripts/verify.sh`
- **Committed in:** `b9dc4ae`

**2. [Rule 3 - Blocking] Updated an existing helper test to model the new typed recovery input on the default non-boundary path**
- **Found during:** Task 1
- **Issue:** `block::tests::difficulty_helpers_cover_contextual_work_branches` still assumed the old silent fallback to `previous_header.bits` for a default-params non-boundary case, which caused `bash scripts/verify.sh` to fail after the real recovery fix landed.
- **Fix:** Passed `MinDifficultyRecoveryTarget { bits: previous_header.bits }` explicitly in that helper test so it matched the live typed-context contract.
- **Files modified:** `packages/open-bitcoin-consensus/src/block/tests.rs`
- **Verification:** `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact`, `bash scripts/verify.sh`
- **Committed in:** `b9dc4ae`

***

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes were necessary to satisfy the repo’s verification contract without widening scope beyond the intended recovery branch and summary correction.

## Issues Encountered

- The worktree already contained orchestrator-owned `.planning/STATE.md` and `.planning/config.json` edits plus an untracked verifier report, so staging stayed limited to plan-owned summary files and task-owned Rust files.
- `07.5-03-SUMMARY.md` already had local text drift, so the correction was applied as a targeted superseded handoff instead of a broad summary rewrite.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `07.5-04-SUMMARY.md` is now the authoritative Phase 07.5 artifact for `CR-01`, `CR-02`, `CONS-02`, `CONS-03`, `VER-01`, and `VER-02`.
- The follow-up reviewer or verifier can close the phase against the exact proof strings listed above plus the carried-forward CR-02 evidence from `07.5-02-SUMMARY.md`.
- `STATE.md` and `ROADMAP.md` still need orchestrator-owned updates after verification.

## Threat Flags

None - the plan closed the remaining contextual-difficulty trust-boundary gap without introducing new network endpoints, auth paths, file access patterns, or schema changes.

---
*Phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an*
*Completed: 2026-04-21*

## Self-Check: PASSED

- Found `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md`
- Found `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md`
- Found commit `b9dc4ae`
