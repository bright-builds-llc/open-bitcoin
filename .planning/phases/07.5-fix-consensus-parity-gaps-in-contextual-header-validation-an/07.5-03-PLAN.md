---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 03
type: execute
wave: 3
depends_on:
  - 07.5-01
  - 07.5-02
files_modified:
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/context/tests.rs
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-consensus/tests/parity_closure.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T00:47:44Z
must_haves:
  truths:
    - Active contextual block-header validation rejects retarget-boundary headers that reuse stale previous `bits`, while preserving the already-verified `time-too-old` and `time-too-new` behavior.
    - The live chainstate connect path supplies the retarget anchor through typed validation context so `next_work_required()` uses the same boundary logic in production and in consensus tests.
    - Phase 07.5 closeout evidence no longer relies on the misleading `difficulty_helpers_cover_contextual_work_branches` assertion or premature CR-01 claims in the existing summaries.
  artifacts:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
  key_links:
    - packages/open-bitcoin-chainstate/src/engine.rs computes and injects a typed retarget anchor into `BlockValidationContext` before calling `check_block_contextual`.
    - packages/open-bitcoin-consensus/src/block.rs uses the injected retarget anchor when `height % difficulty_adjustment_interval == 0` and `no_pow_retargeting` is false.
    - packages/open-bitcoin-consensus/src/block/tests.rs and packages/open-bitcoin-chainstate/src/engine/tests.rs prove that stale previous bits fail with `bad-diffbits` at retarget boundaries.
---

<objective>
Close the remaining CR-01 verifier gap by restoring Knots-style retarget-boundary `next_work_required()` behavior on the active contextual validation path and by correcting the premature closeout evidence that currently claims CR-01 is already closed.

Purpose: finish the actual contextual-header parity work before Phase 8 and leave truthful evidence for the required follow-up verifier or reviewer pass.
Output: typed retarget-anchor context for contextual block validation, corrected retarget-boundary regressions on the consensus core and chainstate path, and superseded or corrected 07.5 summary evidence for CR-01.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/pow.cpp
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-consensus/src/block.rs
@packages/open-bitcoin-consensus/src/block/tests.rs
@packages/open-bitcoin-consensus/src/crypto.rs
@packages/open-bitcoin-chainstate/src/engine.rs
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-consensus/src/context.rs:
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BlockValidationContext {
    pub height: u32,
    pub previous_header: BlockHeader,
    pub previous_median_time_past: i64,
    pub current_time: i64,
    pub consensus_params: ConsensusParams,
}
```

From packages/open-bitcoin-consensus/src/block.rs:
```rust
pub fn check_block_header_contextual(
    header: &BlockHeader,
    context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;

fn next_work_required(header: &BlockHeader, context: &BlockValidationContext) -> u32;
```

From packages/open-bitcoin-chainstate/src/engine.rs:
```rust
pub fn connect_block_with_current_time(
    &mut self,
    block: &Block,
    chain_work: u128,
    current_time: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ChainstateError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed retarget-anchor context and fix boundary `next_work_required()` parity in the consensus core</name>
  <files>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs, packages/open-bitcoin-consensus/src/crypto.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md, packages/bitcoin-knots/src/validation.cpp, packages/bitcoin-knots/src/pow.cpp, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - `BlockValidationContext` carries a `maybe_`-prefixed retarget-anchor input sufficient to compute Knots-style next-work values at adjustment boundaries.
    - `next_work_required()` no longer returns `context.previous_header.bits` unconditionally when retargeting is enabled at a boundary height.
    - The misleading `difficulty_helpers_cover_contextual_work_branches` assertion is rewritten so it surfaces the retarget difference instead of codifying the bug, and a contextual regression proves stale previous bits fail with `bad-diffbits`.
  </behavior>
  <action>Per D-01 through D-04 and the verifier gap, extend `packages/open-bitcoin-consensus/src/context.rs` so `BlockValidationContext` includes one optional, `maybe_`-prefixed typed retarget-anchor field that gives the consensus core the first-block timestamp of the current adjustment window. Use the smallest typed surface that can drive Knots `GetNextWorkRequired()` parity at retarget boundaries; do not add clocks, globals, or unrelated PoW policy knobs. Update `packages/open-bitcoin-consensus/src/block.rs` so the retarget-enabled boundary branch mirrors Knots `CalculateNextWorkRequired()`: use `previous_header.bits` as the old target, derive `actual_timespan` from `previous_header.time - anchor_time`, clamp it to one quarter and four times `pow_target_timespan_seconds`, cap the recomputed target at `pow_limit_bits`, and return the recomputed compact value instead of silently falling back to `previous_header.bits` or `pow_limit_bits`. Keep the existing non-boundary `time-too-old`, `time-too-new`, and current min-difficulty behavior intact unless a change is strictly required to keep the retarget boundary coherent; do not widen into the broader testnet back-scan, checkpoint, or phase-8 scope. In `packages/open-bitcoin-consensus/src/block/tests.rs`, explicitly supersede the misleading helper evidence by rewriting `difficulty_helpers_cover_contextual_work_branches` so its retarget-enabled branch asserts a recomputed value that is different from `previous_header.bits`, then add a new regression named `contextual_header_rejects_previous_bits_at_retarget_boundary` that uses a short deterministic adjustment interval and asserts `check_block_header_contextual()` returns `bad-diffbits` when a boundary header reuses the stale previous bits. Update `packages/open-bitcoin-consensus/src/context/tests.rs` and `packages/open-bitcoin-consensus/tests/parity_closure.rs` for the new typed field without changing unrelated parity coverage.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features context::tests::block_context_carries_expected_pow_and_time_fields -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_.*retarget" packages/open-bitcoin-consensus/src/context.rs` returns the new typed retarget-anchor field.
    - `rg -n "fn contextual_header_rejects_previous_bits_at_retarget_boundary" packages/open-bitcoin-consensus/src/block/tests.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact` exits with status 0 and no longer asserts the retarget-enabled branch returns `previous_header.bits`.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The consensus core has the explicit retarget anchor it was missing, the boundary `next_work_required()` branch matches Knots-style retarget math instead of reusing stale bits, and the prior misleading helper assertion has been replaced by regressions that fail if CR-01 reappears.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the retarget anchor from chainstate into contextual validation and prove the live path rejects stale boundary bits</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/block.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md</read_first>
  <behavior>
    - The chainstate caller computes the retarget anchor from active-chain history only when the next block height lands on a difficulty-adjustment boundary.
    - Non-boundary heights keep the new retarget-anchor field empty while preserving the already-verified explicit `current_time` plumbing.
    - A chainstate regression proves the active connect path returns `bad-diffbits` when a retarget-boundary block reuses stale previous bits.
  </behavior>
  <action>Per D-02 through D-04 and the verification gap, update `packages/open-bitcoin-chainstate/src/engine.rs` so `connect_block_with_current_time()` derives the retarget-anchor value from `self.active_chain` before building `BlockValidationContext`. Use the same adjustment interval formula as the consensus core (`max(1, pow_target_timespan_seconds / pow_target_spacing_seconds)`), and when `height > 0`, `no_pow_retargeting` is false, and `height % interval == 0`, look back exactly one adjustment window to capture the first block time of that window; otherwise set the new field to `None`. Keep the existing `current_time` path unchanged and do not widen this gap plan into the review-only WR-01 or WR-02 follow-ons, `ManagedChainstate`, `network.rs`, or any lax-DER files. In `packages/open-bitcoin-chainstate/src/engine/tests.rs`, add `connect_block_rejects_wrong_bits_at_retarget_boundary` using a short deterministic difficulty interval and a block whose boundary `bits` incorrectly reuse the previous header value, then assert `ChainstateError::BlockValidation { source.reject_reason == "bad-diffbits" }`. Keep the existing explicit-current-time future-time regression green so the gap plan preserves the already-verified `time-too-new` path while adding the missing retarget anchor.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_.*retarget" packages/open-bitcoin-chainstate/src/engine.rs` returns the new context plumbing.
    - `rg -n "fn connect_block_rejects_wrong_bits_at_retarget_boundary" packages/open-bitcoin-chainstate/src/engine/tests.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact` exits with status 0.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The live chainstate path provides the missing retarget anchor through typed context, the active connect path now rejects stale retarget-boundary bits, and the already-verified explicit-time behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run truthful closeout evidence and correct the premature CR-01 summary claims</name>
  <files>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md</files>
  <read_first>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, scripts/verify.sh, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/verification.md</read_first>
  <behavior>
    - Existing 07.5 summaries stop claiming CR-01 was already closed before the retarget-boundary gap closure landed.
    - The new 07.5 gap summary ties CR-01 closure to the corrected helper regression, the active-path retarget regression, the preserved CR-02 lax-DER proof, and `bash scripts/verify.sh`.
    - Summary edits happen only after the targeted tests and repo-native verification pass on the corrected tree.
  </behavior>
  <action>After Tasks 1 and 2 are green, rerun the exact proof commands with fully qualified test paths: `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::verification_helpers_follow_flag_gated_legacy_rules -- --exact`, then `bash scripts/verify.sh`. If any command fails, stop and hand the failure back to the owning code task instead of editing summaries around it. In `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md`, replace the current CR-01 closure language with a precise note that plan 01 restored time plumbing and partial contextual parity but was superseded by gap plan `07.5-03` for retarget-boundary difficulty. In `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md`, replace the premature statement that a follow-up reviewer or verifier can close CR-01 from the existing evidence with a superseded note pointing at the new retarget-boundary regressions and `07.5-03-SUMMARY.md`. Then create `07.5-03-SUMMARY.md` as the authoritative closeout artifact, explicitly citing CR-01, CR-02, `CONS-02`, `CONS-03`, `VER-01`, `VER-02`, `difficulty_helpers_cover_contextual_work_branches`, `contextual_header_rejects_previous_bits_at_retarget_boundary`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, `non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector`, `verification_helpers_follow_flag_gated_legacy_rules`, and `bash scripts/verify.sh`. Do not touch lax-DER implementation files in this task; keep CR-02 evidence intact and only correct the misleading CR-01 closeout chain.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::verification_helpers_follow_flag_gated_legacy_rules -- --exact && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "superseded|07\\.5-03|CR-01" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` shows the old premature CR-01 claims have been corrected.
    - `rg -n "CR-01|CR-02|CONS-02|CONS-03|VER-01|VER-02|difficulty_helpers_cover_contextual_work_branches|contextual_header_rejects_previous_bits_at_retarget_boundary|connect_block_rejects_wrong_bits_at_retarget_boundary|non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector|verification_helpers_follow_flag_gated_legacy_rules|bash scripts/verify\\.sh" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md` returns all required proof strings.
    - `bash scripts/verify.sh` exits with status 0 on the final tree.
  </acceptance_criteria>
  <done>The remaining CR-01 gap is fixed and proven, the two earlier summaries no longer overclaim closure, and Phase 07.5 has one authoritative gap-closure summary that gives the follow-up verifier or reviewer truthful evidence for both CR-01 and CR-02.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if the retarget-boundary branch still has any silent fallback that can accept stale previous bits, or if the summary edits claim CR-01 closure without the new retarget regressions and `bash scripts/verify.sh` passing, stop and leave the phase open.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chainstate history -> contextual next-work calculation | The active chain must provide the correct retarget anchor so consensus does not validate boundary headers against stale difficulty |
| typed validation context -> consensus core | Missing retarget-anchor data must not silently degrade into permissive fallback behavior |
| summary evidence -> follow-up review or verifier closure | Downstream closure decisions depend on the summaries naming the correct proof points instead of the old misleading helper assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-07 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Recompute retarget-boundary work from typed anchor data, remove the stale-bits fallback, and pin the boundary failure mode with `contextual_header_rejects_previous_bits_at_retarget_boundary`. |
| T-07.5-08 | T | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Derive the retarget anchor from `active_chain` only at adjustment heights and inject it into `BlockValidationContext` before calling `check_block_contextual`. |
| T-07.5-09 | R | `07.5-01-SUMMARY.md`, `07.5-02-SUMMARY.md`, `07.5-03-SUMMARY.md` | mitigate | Rewrite the premature CR-01 claims, require the exact new regression names plus `bash scripts/verify.sh` in the final summary, and keep CR-02 evidence unchanged rather than restated from memory. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::verification_helpers_follow_flag_gated_legacy_rules -- --exact`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- Retarget-enabled contextual header validation computes Knots-style boundary work from explicit typed anchor data and rejects stale previous bits with `bad-diffbits`.
- The chainstate caller provides that anchor on the live connect path without reopening unrelated current-time, network, or lax-DER work.
- Existing 07.5 summaries are corrected so CR-01 is only claimed closed after the new boundary regressions and repo-native verification pass, and `07.5-03-SUMMARY.md` becomes the authoritative closeout evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md`
</output>
