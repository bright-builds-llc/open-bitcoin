---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 03
subsystem: consensus
tags: [rust, consensus, pow, chainstate, verification]
requires:
  - phase: 07.5-01
    provides: explicit current-time contextual validation and the initial CR-01 proof chain
  - phase: 07.5-02
    provides: CR-02 lax-DER proof points and the earlier phase-closeout summaries that required correction
  - phase: 07.4-sweep-the-codebase-for-let-else-opportunities
    provides: the CR-01 review finding that defined the retarget-boundary gap
provides:
  - typed retarget-anchor context for contextual header validation at difficulty-adjustment boundaries
  - live chainstate retarget-anchor injection plus stale-boundary-bits rejection on the connect path
  - truthful Phase 07.5 closeout evidence for CR-01, CR-02, CONS-02, CONS-03, VER-01, and VER-02
affects: [phase-8-operator-interfaces, consensus-parity, follow-up-review]
tech-stack:
  added: []
  patterns:
    - typed retarget anchor injection from chainstate into pure-core consensus validation
    - direct helper coverage for extracted pure-core retarget math under strict repo verification gates
key-files:
  created:
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md
    - packages/open-bitcoin-consensus/src/block/difficulty.rs
  modified:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/context/tests.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-consensus/tests/parity_closure.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
key-decisions:
  - "Carry only the first-block time of the adjustment window in a typed `RetargetAnchor` instead of widening contextual validation with broader chainstate or clock data."
  - "Keep Knots-style retarget math in the consensus crate and make chainstate responsible only for anchor lookup from active-chain history."
  - "Supersede the earlier CR-01 summary claims instead of rewriting them into partial truths, so follow-up review can rely on one authoritative closeout artifact."
patterns-established:
  - "Retarget-boundary contextual validation takes caller-supplied anchor time explicitly through `BlockValidationContext.maybe_retarget_anchor`."
  - "Coverage and file-length gate failures are resolved by extracting pure helpers into sibling modules and adding direct helper tests instead of weakening verification."
requirements-completed: [CONS-02, CONS-03, VER-01, VER-02]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T12:07:53Z
duration: 11h 20m
completed: 2026-04-21
---

# Phase 07.5 Plan 03: Retarget-Boundary Contextual Difficulty Closure Summary

**Typed retarget anchors now drive Knots-style boundary work on both the consensus core and the live chainstate path, and Phase 07.5 closeout evidence is finally truthful about CR-01 and CR-02.**

## Performance

- **Duration:** 11h 20m
- **Started:** 2026-04-21T00:47:44Z
- **Completed:** 2026-04-21T12:07:53Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Added a typed retarget-anchor input to contextual validation and restored Knots-style boundary recomputation so `next_work_required()` no longer accepts stale previous `bits`.
- Threaded the retarget anchor from `active_chain` into the live connect path and proved `connect_block_rejects_wrong_bits_at_retarget_boundary` fails with `bad-diffbits` for stale boundary headers.
- Corrected the earlier Phase 07.5 summaries so CR-01 is only claimed closed after `difficulty_helpers_cover_contextual_work_branches`, `contextual_header_rejects_previous_bits_at_retarget_boundary`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, the CR-02 signature regressions, and `bash scripts/verify.sh` all passed on the corrected tree.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add typed retarget-anchor context and fix boundary `next_work_required()` parity in the consensus core** - `61c3ed8` (`fix`)
2. **Task 2: Thread the retarget anchor from chainstate into contextual validation and prove the live path rejects stale boundary bits** - `43d77a9` (`fix`)
3. **Task 3: Re-run truthful closeout evidence and correct the premature CR-01 summary claims** - recorded in this summary commit (`chore`)

No metadata commit was created here. Per user instruction, `STATE.md` and `ROADMAP.md` remain untouched for the orchestrator-owned post-verification flow.

## Files Created/Modified

- `packages/open-bitcoin-consensus/src/context.rs` - adds the typed optional `maybe_retarget_anchor` field used for boundary work calculation.
- `packages/open-bitcoin-consensus/src/block.rs` - keeps contextual header validation focused on the public behavior while delegating retarget math into a sibling module.
- `packages/open-bitcoin-consensus/src/block/difficulty.rs` - owns the extracted Knots-style boundary retarget math and direct helper coverage needed by the repo verification gates.
- `packages/open-bitcoin-consensus/src/block/tests.rs` - replaces the misleading helper assertion and pins the retarget-boundary rejection path.
- `packages/open-bitcoin-consensus/src/context/tests.rs` - exercises the new typed retarget-anchor field.
- `packages/open-bitcoin-consensus/tests/parity_closure.rs` - keeps parity-closure coverage compiling against the expanded contextual block state.
- `packages/open-bitcoin-chainstate/src/engine.rs` - derives the boundary retarget anchor from `active_chain` and injects it into `BlockValidationContext`.
- `packages/open-bitcoin-chainstate/src/engine/tests.rs` - proves the live path rejects stale retarget-boundary bits and covers the interval helper edge case required by coverage.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md` - marks the earlier CR-01 claim as superseded by this gap-close plan.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` - preserves the CR-02 proof chain while pointing final CR-01 closure to this summary.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md` - records the authoritative Phase 07.5 closeout evidence.

## Decisions Made

- Used a minimal `RetargetAnchor` value object instead of passing more chainstate or block-index surface into pure-core consensus code.
- Extracted the retarget helper logic into `block/difficulty.rs` rather than weakening the repo’s file-length or coverage gates.
- Kept the Task 3 summary authoritative and pointed the earlier summaries at it instead of trying to preserve their original CR-01 wording.

## Verification Evidence

- `CR-01` now closes on the corrected retarget-boundary proof chain: `difficulty_helpers_cover_contextual_work_branches`, `contextual_header_rejects_previous_bits_at_retarget_boundary`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, and `bash scripts/verify.sh`.
- `CR-02` remains closed by `non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector`, `verification_helpers_follow_flag_gated_legacy_rules`, and `bash scripts/verify.sh`.
- `CONS-02`, `CONS-03`, `VER-01`, and `VER-02` are satisfied on the final tree by the exact proof commands rerun during Task 3 plus the passing repo-native verifier.
- Exact proof commands rerun for this closeout:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::difficulty_helpers_cover_contextual_work_branches -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::contextual_header_rejects_previous_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features signature::tests::verification_helpers_follow_flag_gated_legacy_rules -- --exact`
- `bash scripts/verify.sh`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Extracted retarget math into a sibling module after the repo file-length gate blocked the Task 1 commit**
- **Found during:** Task 1
- **Issue:** `packages/open-bitcoin-consensus/src/block.rs` exceeded the repo’s 628-line production-file limit after the boundary retarget work landed.
- **Fix:** Moved the pure retarget math into `packages/open-bitcoin-consensus/src/block/difficulty.rs` and kept `block.rs` as the public entrypoint.
- **Files modified:** `packages/open-bitcoin-consensus/src/block.rs`, `packages/open-bitcoin-consensus/src/block/difficulty.rs`, `packages/open-bitcoin-consensus/src/block/tests.rs`
- **Verification:** `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`, `cargo test --manifest-path packages/Cargo.toml --all-features`, `bash scripts/verify.sh`
- **Committed in:** `61c3ed8`

**2. [Rule 3 - Blocking] Added direct helper coverage for the extracted retarget math after the repo coverage gate failed**
- **Found during:** Task 1
- **Issue:** Moving the retarget math into `block/difficulty.rs` exposed uncovered helper branches and blocked `bash scripts/verify.sh`.
- **Fix:** Added focused unit coverage for the missing-anchor, invalid-timespan, pow-limit-cap, invalid-compact, and compact-encoding helper paths in `block/difficulty.rs`.
- **Files modified:** `packages/open-bitcoin-consensus/src/block/difficulty.rs`
- **Verification:** `bash scripts/verify.sh`
- **Committed in:** `61c3ed8`

**3. [Rule 3 - Blocking] Added a direct chainstate helper regression after the Task 2 coverage gate failed**
- **Found during:** Task 2
- **Issue:** The new chainstate difficulty-interval helper left one uncovered edge branch (`pow_target_spacing_seconds <= 0`), which blocked `bash scripts/verify.sh`.
- **Fix:** Added `engine::tests::difficulty_interval_helper_clamps_non_positive_spacing` to cover the clamped-interval edge case explicitly.
- **Files modified:** `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::difficulty_interval_helper_clamps_non_positive_spacing -- --exact`, `bash scripts/verify.sh`
- **Committed in:** `43d77a9`

---

**Total deviations:** 3 auto-fixed (3 blocking)
**Impact on plan:** All three fixes were necessary to satisfy the repo’s verification contract without widening the parity scope. No functional scope creep beyond the retarget-boundary closure work.

## Issues Encountered

- The repo’s commit hook reran `bash scripts/verify.sh`, which surfaced file-length and coverage blockers that were not visible from the task-local tests alone.
- `cargo test ... -- --exact` still required fully qualified test paths, so the final closeout evidence records the exact module-qualified names instead of the shorter human-friendly labels.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 07.5 now has one authoritative closeout artifact for CR-01 and CR-02 in `07.5-03-SUMMARY.md`.
- The follow-up reviewer or verifier can close CR-01, CR-02, `CONS-02`, `CONS-03`, `VER-01`, and `VER-02` against the exact proof strings listed above.
- No code blocker remains before Phase 8. `STATE.md` and `ROADMAP.md` still need orchestrator-owned updates after verification.

## Threat Flags

None - the plan closed the retarget-boundary trust-boundary gap without introducing new network endpoints, auth surfaces, file access paths, or schema changes.

---
*Phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an*
*Completed: 2026-04-21*

## Self-Check: PASSED

- Found `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-03-SUMMARY.md`
- Found commit `61c3ed8`
- Found commit `43d77a9`
