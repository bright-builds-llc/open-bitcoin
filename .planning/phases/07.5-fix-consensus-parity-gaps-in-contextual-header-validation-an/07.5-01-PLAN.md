---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/context/tests.rs
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
  - packages/open-bitcoin-chainstate/tests/parity.rs
  - packages/open-bitcoin-consensus/tests/parity_closure.rs
  - packages/open-bitcoin-node/src/chainstate.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-mempool/tests/parity.rs
autonomous: true
requirements:
  - CONS-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-20T12:40:57Z
must_haves:
  truths:
    - Active contextual block-header validation rejects incorrect `bits`, timestamps at or below previous median-time-past, and timestamps more than two hours beyond caller-supplied current time.
    - The active block-connect path supplies `current_time` explicitly from its caller instead of reading clocks or chain globals inside pure-core consensus code.
    - Targeted consensus, chainstate, mempool, and managed-node tests prove the contextual header rules now execute on real block-connect paths.
  artifacts:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-node/src/chainstate.rs
    - packages/open-bitcoin-node/src/network.rs
  key_links:
    - packages/open-bitcoin-node/src/network.rs threads its existing `timestamp` input into `ManagedChainstate::connect_block` for `PeerAction::ReceivedBlock`.
    - packages/open-bitcoin-chainstate/src/engine.rs builds `BlockValidationContext` with `current_time` before calling `check_block_contextual`.
    - packages/open-bitcoin-consensus/src/block.rs emits `bad-diffbits`, `time-too-old`, and `time-too-new` through `BlockValidationError` on the contextual path.
---

<objective>
Restore Knots-compatible contextual header validation on the active block-connect path by adding the missing typed time and proof-of-work inputs, then threading them through the real chainstate and managed-node callers.

Purpose: close review finding CR-01 before Phase 8 builds operator interfaces on top of the current consensus surface.
Output: explicit `current_time` and proof-of-work schedule fields in consensus context, Knots-style `bad-diffbits` and `time-too-new` checks in the contextual header path, and active-path caller plumbing that proves those checks run outside isolated unit tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md
@.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-REVIEW.md
@.planning/phases/03-consensus-validation-engine/03-05-SUMMARY.md
@.planning/phases/03.4-consensus-parity-closure/03.4-02-SUMMARY.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/pow.cpp
@scripts/verify.sh
@rust-toolchain.toml

<interfaces>
From packages/open-bitcoin-consensus/src/context.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct ConsensusParams {
    pub coinbase_maturity: u32,
    pub locktime_threshold: u32,
    pub sequence_locktime_granularity: u32,
    pub enforce_bip34_height_in_coinbase: bool,
    pub enforce_bip113_median_time_past: bool,
    pub enforce_segwit: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BlockValidationContext {
    pub height: u32,
    pub previous_header: BlockHeader,
    pub previous_median_time_past: i64,
    pub consensus_params: ConsensusParams,
}
```

From packages/open-bitcoin-consensus/src/block.rs:
```rust
pub fn check_block_header_contextual(
    header: &BlockHeader,
    context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;

pub fn check_block_contextual(
    block: &Block,
    context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;
```

From packages/open-bitcoin-chainstate/src/engine.rs:
```rust
impl Chainstate {
    pub fn connect_block(
        &mut self,
        block: &Block,
        chain_work: u128,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<ChainPosition, ChainstateError>;

    pub fn reorg(
        &mut self,
        disconnect_blocks: &[Block],
        replacement_branch: &[AnchoredBlock],
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<ChainTransition, ChainstateError>;
}
```

From packages/open-bitcoin-node/src/network.rs:
```rust
pub fn receive_message(
    &mut self,
    peer_id: PeerId,
    message: WireNetworkMessage,
    timestamp: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<Vec<WireNetworkMessage>, ManagedNetworkError>;

pub fn connect_local_block(
    &mut self,
    block: &Block,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ManagedNetworkError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the missing typed inputs and Knots-style contextual header checks in the consensus core</name>
  <files>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/context/tests.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/src/validation.rs, packages/open-bitcoin-consensus/src/crypto.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-REVIEW.md, packages/bitcoin-knots/src/validation.cpp, packages/bitcoin-knots/src/pow.cpp, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - `check_block_header_contextual` returns `bad-diffbits` when `header.bits` does not match the next required work, `time-too-old` when `header.time <= previous_median_time_past`, and `time-too-new` when `header.time > current_time + 7_200`.
    - `BlockValidationContext` gains explicit `current_time` input and `ConsensusParams` gains only the minimal proof-of-work schedule fields needed for `GetNextWorkRequired()` parity.
    - The new consensus tests pin the exact reject reasons and leave checkpoint, version, and timewarp behavior untouched in this phase.
  </behavior>
  <action>In `packages/open-bitcoin-consensus/src/context.rs`, extend `ConsensusParams` with these exact fields and types: `pow_limit_bits: u32`, `pow_target_spacing_seconds: i64`, `pow_target_timespan_seconds: i64`, `allow_min_difficulty_blocks: bool`, and `no_pow_retargeting: bool`. Set `ConsensusParams::default()` to the current easy-target regtest-friendly values `pow_limit_bits: 0x207f_ffff`, `pow_target_spacing_seconds: 600`, `pow_target_timespan_seconds: 1_209_600`, `allow_min_difficulty_blocks: true`, and `no_pow_retargeting: true`, leaving the existing locktime and segwit fields intact. Extend `BlockValidationContext` with `current_time: i64`. In `packages/open-bitcoin-consensus/src/block.rs`, add a private `const MAX_FUTURE_BLOCK_TIME_SECONDS: i64 = 7_200;`, a private `difficulty_adjustment_interval(&ConsensusParams) -> i64`, and private `next_work_required(...)` or equivalent helpers that mirror the non-checkpoint parts of Knots `GetNextWorkRequired()` without adding new dependencies or moving the logic into adapters. Keep the helper private to `block.rs`; do not create a new crate or widen `crypto.rs` into a generic difficulty API unless a tiny local encode or decode helper is impossible to keep private. Update `check_block_header_contextual` so it computes `expected_bits`, rejects mismatches with `BlockValidationResult::InvalidHeader`, reject reason `bad-diffbits`, and debug message `incorrect proof of work`, preserves the existing `time-too-old` branch, and adds `BlockValidationResult::TimeFuture`, reject reason `time-too-new`, and debug message `block timestamp too far in the future` when `i64::from(header.time) > context.current_time + MAX_FUTURE_BLOCK_TIME_SECONDS`. In `packages/open-bitcoin-consensus/src/block/tests.rs`, replace the existing broad contextual-header assertion with a targeted regression named `contextual_header_parity_covers_diffbits_future_time_and_mtp` that proves success, `bad-diffbits`, `time-too-old`, and `time-too-new` on deterministic fixtures. In `packages/open-bitcoin-consensus/src/context/tests.rs`, add `block_context_carries_expected_pow_and_time_fields` to assert the new fields are present and defaulted correctly. Do not add checkpoint, bad-version, or `time-timewarp-attack` logic in this phase; D-01 limits the fix to the reviewed contextual difficulty and future-time gaps.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block_context_carries_expected_pow_and_time_fields -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub current_time: i64" packages/open-bitcoin-consensus/src/context.rs` returns exactly one match.
    - `rg -n "pow_limit_bits|pow_target_spacing_seconds|pow_target_timespan_seconds|allow_min_difficulty_blocks|no_pow_retargeting" packages/open-bitcoin-consensus/src/context.rs` returns all five field names.
    - `rg -n "\"bad-diffbits\"|\"time-too-new\"" packages/open-bitcoin-consensus/src/block.rs` returns both reject reasons.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The consensus core owns the missing contextual header parity rules and exposes them only through typed context, with exact reject reasons proven in focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread explicit current time through chainstate and managed-node block-connect callers</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-chainstate/tests/parity.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs, packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/mempool.rs, packages/open-bitcoin-mempool/tests/parity.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-chainstate/tests/parity.rs, packages/open-bitcoin-consensus/tests/parity_closure.rs, packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/mempool.rs, packages/open-bitcoin-mempool/tests/parity.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/block.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/verification.md</read_first>
  <behavior>
    - The active block-connect path passes caller-owned `current_time` into `BlockValidationContext` instead of reading a clock inside pure-core consensus or chainstate code.
    - `ManagedPeerNetwork::receive_message(..., timestamp, ...)` uses that existing `timestamp` value when a `PeerAction::ReceivedBlock` reaches chainstate.
    - Local block-connect helpers and parity fixtures stay deterministic by passing explicit fixture times such as `i64::from(block.header.time)` unless the test is intentionally proving `time-too-new`.
  </behavior>
  <action>In `packages/open-bitcoin-chainstate/src/engine.rs`, change `Chainstate::connect_block` to `pub fn connect_block(&mut self, block: &Block, chain_work: u128, current_time: i64, verify_flags: ScriptVerifyFlags, consensus_params: ConsensusParams) -> Result<ChainPosition, ChainstateError>` and change `reorg` to accept the same `current_time: i64` argument before `verify_flags`. Build `BlockValidationContext` with that exact `current_time` field and propagate the same value through replacement-branch `connect_block` calls. Update `packages/open-bitcoin-chainstate/src/engine/tests.rs` with a focused regression named `connect_block_uses_explicit_current_time_for_future_time_rejection` that proves the active chainstate path now returns `time-too-new` when the caller supplies a clock below the block timestamp by more than 7_200 seconds. Update `packages/open-bitcoin-chainstate/tests/parity.rs`, `packages/open-bitcoin-mempool/tests/parity.rs`, and any `BlockValidationContext` literal in `packages/open-bitcoin-consensus/tests/parity_closure.rs` to pass explicit fixture times. In `packages/open-bitcoin-node/src/chainstate.rs`, mirror the signature additions on `ManagedChainstate::connect_block` and `ManagedChainstate::reorg`. In `packages/open-bitcoin-node/src/network.rs`, change `process_actions` to accept `timestamp: i64`, thread `receive_message` and `receive_wire_message` through that signature, and pass `timestamp` into `self.chainstate.connect_block` for `PeerAction::ReceivedBlock`. Keep `connect_local_block` deterministic by passing `i64::from(block.header.time)` into `ManagedChainstate::connect_block`; do not add a runtime clock source in this phase. Update inline tests in `packages/open-bitcoin-node/src/chainstate.rs`, `packages/open-bitcoin-node/src/mempool.rs`, and `packages/open-bitcoin-node/src/network/tests.rs` so the changed signatures compile, and add a managed-node regression named `managed_network_rejects_future_block_using_message_timestamp` that proves the peer-received path now surfaces `time-too-new`. Do not read `SystemTime`, `Instant`, or chain globals anywhere in the consensus or chainstate crates, and do not widen this task into checkpoint, peer-selection, mempool-policy, or RPC work.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate -p open-bitcoin-node -p open-bitcoin-mempool --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features managed_network_rejects_future_block_using_message_timestamp -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "current_time: i64" packages/open-bitcoin-chainstate/src/engine.rs packages/open-bitcoin-node/src/chainstate.rs` returns the updated public signatures.
    - `rg -n "BlockValidationContext \\{[[:space:][:print:]]*current_time" -U packages/open-bitcoin-chainstate/src/engine.rs` returns the `current_time` field in the contextual validation construction.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact` exits with status 0.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features managed_network_rejects_future_block_using_message_timestamp -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The active block-connect path now enforces future-time rejection through explicit caller plumbing rather than hidden runtime state, and the adapter tests prove the wiring works end to end.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if this plan can only be implemented by reading clocks inside pure-core code or by changing the `bad-diffbits`, `time-too-old`, or `time-too-new` reject contract, stop and keep the current call path unchanged until a narrower typed seam is found.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned `current_time` -> consensus block validation | Untrusted or stale caller time must be passed explicitly and used deterministically in the contextual header checks |
| previous header plus consensus params -> next-work calculation | Missing or wrong proof-of-work schedule fields would silently accept headers that Knots rejects |
| managed-node message timestamp -> chainstate connect path | The active peer block path must not drop the available `timestamp` before contextual validation runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-01 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Implement `bad-diffbits`, `time-too-old`, and `time-too-new` with exact reject strings and pin them in `contextual_header_parity_covers_diffbits_future_time_and_mtp`. |
| T-07.5-02 | T | `packages/open-bitcoin-consensus/src/context.rs` | mitigate | Add only the five named proof-of-work fields and `current_time`, keep defaults explicit, and avoid widening into unrelated consensus-parameter cleanup. |
| T-07.5-03 | D | `packages/open-bitcoin-chainstate/src/engine.rs` and `packages/open-bitcoin-node/src/network.rs` | mitigate | Thread `current_time` and `timestamp` explicitly through the active connect path, forbid hidden clock reads, and prove the end-to-end rejection with `managed_network_rejects_future_block_using_message_timestamp`. |
</threat_model>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-chainstate -p open-bitcoin-node -p open-bitcoin-mempool --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-chainstate -p open-bitcoin-node -p open-bitcoin-mempool --all-features`
</verification>

<success_criteria>
- The consensus core exposes explicit `current_time` and proof-of-work schedule inputs and uses them to emit `bad-diffbits`, `time-too-old`, and `time-too-new` on the contextual header path.
- The active block-connect call path from `ManagedPeerNetwork::receive_message(..., timestamp, ...)` to `check_block_contextual` keeps the time input explicit and deterministic.
- No new runtime clock reads, checkpoint rules, version checks, or unrelated policy changes are introduced while closing CR-01.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md`
</output>
