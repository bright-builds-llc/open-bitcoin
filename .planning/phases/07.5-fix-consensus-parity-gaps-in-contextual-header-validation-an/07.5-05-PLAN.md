---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 05
type: execute
wave: 5
depends_on:
  - 07.5-04
files_modified:
  - packages/open-bitcoin-consensus/src/context.rs
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
  - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - CONS-02
  - CONS-03
  - VER-01
  - VER-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-22T01:30:57Z
must_haves:
  truths:
    - "Active consensus and live chainstate reject blocks whose coinbase outputs exceed subsidy(height) plus aggregated non-coinbase fees with reject reason `bad-cb-amount`."
    - "Per-transaction fees are aggregated exactly once from the existing `validate_transaction_with_context()` path instead of being computed and discarded."
    - "Phase 07.5 closeout evidence names the new overpay-rejection proof chain and passing `bash scripts/verify.sh` without reopening the already-green difficulty or lax-DER implementation work."
  artifacts:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md
  key_links:
    - packages/open-bitcoin-consensus/src/transaction.rs returns `Amount` fees that `validate_block_with_context()` and `connect_block_with_current_time()` now aggregate instead of dropping.
    - packages/open-bitcoin-consensus/src/block.rs provides the shared subsidy-plus-fees `bad-cb-amount` helper that the active chainstate path calls before committing `self.utxos`, `self.undo_by_block`, or `self.active_chain`.
    - `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md` is marked superseded by `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md`, which carries forward the prior CR-02 and contextual-difficulty proof chains plus the new coinbase-limit evidence.
---

<objective>
Close the remaining Phase 07.5 verifier blocker by enforcing the Knots-style coinbase subsidy-plus-fees limit on both the pure consensus block-validation helper and the active chainstate block-connect path.

Purpose: remove the last consensus-critical acceptance hole in the touched 07.5 block-connect surface without widening back into already-green contextual-difficulty or lax-DER work.
Output: a typed subsidy schedule input in consensus params, shared pure-core `bad-cb-amount` enforcement in `block.rs`, live fee aggregation on the connect path, focused regressions for the exact reward boundary, and a new `07.5-05-SUMMARY.md` that supersedes the previous final artifact truthfully.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-RESEARCH.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
@.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/validation.h
@packages/bitcoin-knots/src/validation.cpp
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-consensus/src/transaction.rs
@packages/open-bitcoin-consensus/src/block.rs
@packages/open-bitcoin-chainstate/src/engine.rs
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-consensus/src/context.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct ConsensusParams {
    pub coinbase_maturity: u32,
    pub locktime_threshold: u32,
    pub sequence_locktime_granularity: u32,
    pub pow_limit_bits: u32,
    pub pow_target_spacing_seconds: i64,
    pub pow_target_timespan_seconds: i64,
    pub allow_min_difficulty_blocks: bool,
    pub no_pow_retargeting: bool,
    pub enforce_bip34_height_in_coinbase: bool,
    pub enforce_bip113_median_time_past: bool,
    pub enforce_segwit: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BlockValidationContext {
    pub height: u32,
    pub previous_header: BlockHeader,
    pub maybe_retarget_anchor: Option<RetargetAnchor>,
    pub maybe_min_difficulty_recovery_target: Option<MinDifficultyRecoveryTarget>,
    pub previous_median_time_past: i64,
    pub current_time: i64,
    pub consensus_params: ConsensusParams,
}
```

From packages/open-bitcoin-consensus/src/transaction.rs:
```rust
pub fn validate_transaction_with_context(
    transaction: &Transaction,
    context: &TransactionValidationContext,
) -> Result<Amount, TxValidationError>;
```

From packages/open-bitcoin-consensus/src/block.rs:
```rust
pub fn validate_block_with_context(
    block: &Block,
    transaction_contexts: &[TransactionValidationContext],
    block_context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;
```

From packages/open-bitcoin-chainstate/src/engine.rs:
```rust
pub fn connect_block_with_current_time(
    &mut self,
    block: &Block,
    chain_work: u128,
    current_time: i64,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<ChainPosition, ChainstateError>;

fn apply_non_coinbase_transaction(
    next_utxos: &mut HashMap<OutPoint, Coin>,
    block_undo: &mut BlockUndo,
    transaction: &Transaction,
    block_time: i64,
    verify_flags: ScriptVerifyFlags,
    block_context: &BlockValidationContext,
) -> Result<(), ChainstateError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregate fees in consensus block validation and enforce the Knots `bad-cb-amount` limit</name>
  <files>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/transaction.rs, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, packages/bitcoin-knots/src/validation.h, packages/bitcoin-knots/src/validation.cpp, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - `ConsensusParams` carries a typed subsidy-halving interval so the new reward helper follows Knots `GetBlockSubsidy()` instead of hardcoding an untyped magic number.
    - `validate_block_with_context()` aggregates the `Amount` returned by each non-coinbase `validate_transaction_with_context()` call and rejects any block whose coinbase outputs exceed `subsidy(height) + total_fees` with reject reason `bad-cb-amount`.
    - Focused unit coverage proves both the subsidy schedule edge cases and the exact `subsidy + fees` reward boundary, without touching the already-green contextual-difficulty or lax-DER tests.
  </behavior>
  <action>Per D-02, D-07, and D-08, keep the fix inside the existing consensus seams and do not reopen unrelated 07.5 work. In `packages/open-bitcoin-consensus/src/context.rs`, add `subsidy_halving_interval: u32` to `ConsensusParams` with a Bitcoin-default value of `210_000` so the block-subsidy rule stays typed and baseline-aligned instead of being hidden as a local constant. In `packages/open-bitcoin-consensus/src/block.rs`, add a small pure helper pair named `block_subsidy(height, consensus_params)` and `validate_coinbase_amount(block, height, total_fees, consensus_params)` that mirrors Knots `validation.cpp:2212-2222` and `validation.cpp:2974-2977`: halve the subsidy by `height / subsidy_halving_interval`, clamp to zero after 64 halvings, sum subsidy plus aggregated fees with checked `i64` arithmetic, sum the coinbase outputs with checked `i64` arithmetic, and reject any overflow or overpay with `BlockValidationResult::Consensus`, reject reason `bad-cb-amount`, and a Knots-style debug string `coinbase pays too much (actual={actual} vs limit={limit})` on the overpay branch. Still in `validate_block_with_context()`, accumulate the returned fees from the existing non-coinbase loop exactly once before sigop accounting and call `validate_coinbase_amount()` before returning success. In `packages/open-bitcoin-consensus/src/block/tests.rs`, add `block_subsidy_matches_knots_halving_schedule` covering height 0, one halving boundary, and the zero-subsidy-after-64-shifts case, plus `validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit` that seeds `block_context.height` to a zero-subsidy height with `subsidy_halving_interval: 1`, uses a fee-paying transaction to establish a non-zero `total_fees`, and proves coinbase outputs exactly equal to the fee pass while outputs one sat higher fail with `bad-cb-amount`. Keep the existing reject-reason mappings, sigop logic, and earlier 07.5 parity regressions untouched.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::block_subsidy_matches_knots_halving_schedule -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "subsidy_halving_interval" packages/open-bitcoin-consensus/src/context.rs` returns the new typed field and default initialization.
    - `rg -n "fn block_subsidy|fn validate_coinbase_amount|bad-cb-amount" packages/open-bitcoin-consensus/src/block.rs` returns the new helper surface and reject path.
    - `rg -n "fn block_subsidy_matches_knots_halving_schedule|fn validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit" packages/open-bitcoin-consensus/src/block/tests.rs` returns exactly one match for each test.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit -- --exact` exits with status 0.
  </acceptance_criteria>
  <done>The pure-core block-validation path no longer discards fees, the reward limit matches Knots semantics, and a focused consensus regression proves `bad-cb-amount` on the exact subsidy-plus-fees boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread aggregated fees through the active connect path and reject overpaying coinbases before commit</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/transaction.rs, packages/open-bitcoin-consensus/src/block.rs, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-CONTEXT.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/architecture.md, ../coding-and-architecture-requirements/standards/core/testing.md</read_first>
  <behavior>
    - The live chainstate connect path aggregates the fee returned by each validated non-coinbase transaction and calls the shared consensus reward-limit helper before committing `self.utxos`, `self.undo_by_block`, or `self.active_chain`.
    - Existing per-transaction validation order, explicit `current_time` plumbing, retarget-boundary handling, and non-boundary min-difficulty recovery remain intact.
    - A focused chainstate regression proves the active path accepts the exact fee-boundary reward, rejects `+1` with `bad-cb-amount`, and leaves chainstate unchanged on failure.
  </behavior>
  <action>Per D-02, D-07, and D-08, keep this fix narrow to the active block-connect seam the verifier flagged. In `packages/open-bitcoin-chainstate/src/engine.rs`, change `apply_non_coinbase_transaction()` to return the validated `Amount` fee after `validate_transaction_with_context()` succeeds and before the existing undo bookkeeping runs. In `connect_block_with_current_time()`, sum those returned fees exactly once while preserving the current apply/add ordering for transaction effects, then call the new shared consensus helper from `packages/open-bitcoin-consensus/src/block.rs` before assigning `self.utxos`, `self.undo_by_block`, or `self.active_chain`. Do not widen into `disconnect_tip()`, `reorg()`, wrapper-clock behavior, or any node-layer files. In `packages/open-bitcoin-chainstate/src/engine/tests.rs`, add `connect_block_rejects_overpaying_coinbase_before_committing_chainstate` by seeding a direct `Chainstate` fixture at a height where `subsidy_halving_interval: 1` makes the next block subsidy zero, then building one candidate block whose coinbase outputs exactly equal the fee from a non-coinbase spend and a sibling block whose coinbase outputs pay one sat more. Assert the exact-fee block connects, the `+1` block returns `ChainstateError::BlockValidation { source.reject_reason == \"bad-cb-amount\" }`, and the rejected attempt leaves `chainstate.snapshot()` unchanged. Keep `connect_block_uses_explicit_current_time_for_future_time_rejection`, `connect_block_rejects_wrong_bits_at_retarget_boundary`, and `connect_block_recovers_last_non_special_target_after_special_min_difficulty_block` green without changing their assertions.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_before_committing_chainstate -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_uses_explicit_current_time_for_future_time_rejection -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_wrong_bits_at_retarget_boundary -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_recovers_last_non_special_target_after_special_min_difficulty_block -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn apply_non_coinbase_transaction|validate_coinbase_amount|bad-cb-amount|total_fees" packages/open-bitcoin-chainstate/src/engine.rs` shows the fee-returning helper and the reward-limit check before final state commit.
    - `rg -n "fn connect_block_rejects_overpaying_coinbase_before_committing_chainstate" packages/open-bitcoin-chainstate/src/engine/tests.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_before_committing_chainstate -- --exact` exits with status 0.
    - The new test asserts `chainstate.snapshot()` is unchanged after the rejected overpaying coinbase attempt.
  </acceptance_criteria>
  <done>The active block-connect path no longer accepts an overpaying coinbase into chainstate, and the live regression proves fee aggregation reaches a `bad-cb-amount` check before final state commit.</done>
</task>

<task type="auto">
  <name>Task 3: Supersede the previous final artifact and record the coinbase-limit proof chain</name>
  <files>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md</files>
  <read_first>.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-REVIEW.md, .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-VERIFICATION.md, packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-chainstate/src/engine.rs, scripts/verify.sh, AGENTS.md, AGENTS.bright-builds.md, ../coding-and-architecture-requirements/standards/core/verification.md</read_first>
  <behavior>
    - `07.5-04-SUMMARY.md` stops presenting itself as the authoritative final closeout artifact and explicitly hands off to `07.5-05-SUMMARY.md` for the verifier-discovered coinbase-limit closure.
    - `07.5-05-SUMMARY.md` becomes the authoritative Phase 07.5 artifact and cites the new coinbase-limit proofs, the carried-forward contextual-difficulty proof chain from `07.5-04-SUMMARY.md`, the carried-forward CR-02 proof chain from `07.5-02-SUMMARY.md`, and the final `bash scripts/verify.sh` rerun.
    - If the new proof commands or `bash scripts/verify.sh` fail, the summary stays truthful and leaves `CONS-02` and `CONS-03` open instead of reasserting closure.
  </behavior>
  <action>After Tasks 1 and 2 are green, rerun only the proof surface needed for this gap plus the repo-native verifier: `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::block_subsidy_matches_knots_halving_schedule -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_before_committing_chainstate -- --exact`, and `bash scripts/verify.sh`. Do not rerun or rewrite the already-green retarget-boundary, non-boundary recovery, explicit-current-time, or lax-DER implementation work except to reference their proof strings from `07.5-02-SUMMARY.md` and `07.5-04-SUMMARY.md`. Update `07.5-04-SUMMARY.md` so it is explicitly superseded by `07.5-05-SUMMARY.md` because the verifier found the unresolved `bad-cb-amount` blocker after the earlier closeout. Then create `07.5-05-SUMMARY.md` as the authoritative artifact for the final 07.5 decision, explicitly citing `CR-01`, `CONS-02`, `CONS-03`, `VER-01`, `VER-02`, `block_subsidy_matches_knots_halving_schedule`, `validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit`, `connect_block_rejects_overpaying_coinbase_before_committing_chainstate`, the carried-forward contextual-difficulty proof strings from `07.5-04-SUMMARY.md`, the carried-forward CR-02 proof strings from `07.5-02-SUMMARY.md`, and `bash scripts/verify.sh`. If any proof command fails, make the summary say the blocker remains open and stop short of claiming final closure.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::block_subsidy_matches_knots_halving_schedule -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit -- --exact && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_before_committing_chainstate -- --exact && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "superseded|07\\.5-05|bad-cb-amount|coinbase-limit" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-04-SUMMARY.md` shows the old final-artifact claim has been replaced with a handoff to `07.5-05`.
    - `rg -n "CR-01|CONS-02|CONS-03|VER-01|VER-02|block_subsidy_matches_knots_halving_schedule|validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit|connect_block_rejects_overpaying_coinbase_before_committing_chainstate|bash scripts/verify\\.sh" .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md` returns all required proof strings.
    - `bash scripts/verify.sh` exits with status 0 on the final tree.
  </acceptance_criteria>
  <done>The truthful summary chain is restored: `07.5-04-SUMMARY.md` is superseded, `07.5-05-SUMMARY.md` becomes the authoritative Phase 07.5 artifact, and the final closure claim depends on the new coinbase-limit proofs plus the repo-native verifier.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if any execution path can still accept an overpaying coinbase without emitting `bad-cb-amount`, or if the summary chain still presents `07.5-04-SUMMARY.md` as final after the verifier-discovered blocker, stop and leave Phase 07.5 open.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated transaction fee -> block reward limit | The fee returned by pure transaction validation must cross into block-level reward enforcement without being dropped, double-counted, or overflowed. |
| pure-core reward helper -> active chainstate commit path | Chainstate must call the shared consensus helper before committing new UTXOs, undo data, or chain positions so a bad coinbase cannot enter live state. |
| phase summary evidence -> follow-up verifier closure | Downstream closure depends on the final summary naming the new coinbase-limit proofs and the carried-forward earlier proof chains truthfully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-13 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Aggregate returned fees exactly once, use checked arithmetic for subsidy, fees, and coinbase totals, and pin the boundary with `block_subsidy_matches_knots_halving_schedule` plus `validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit`. |
| T-07.5-14 | T | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Return the validated fee from `apply_non_coinbase_transaction()`, call the shared consensus `bad-cb-amount` helper before assigning live state, and prove rejected overpay attempts leave `chainstate.snapshot()` unchanged. |
| T-07.5-15 | R | `07.5-04-SUMMARY.md` / `07.5-05-SUMMARY.md` | mitigate | Supersede the old final artifact, require the new exact proof strings plus `bash scripts/verify.sh`, and carry forward earlier green proof chains by reference instead of silently assuming they remain authoritative. |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::block_subsidy_matches_knots_halving_schedule -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features block::tests::validate_block_with_context_enforces_coinbase_subsidy_plus_fees_limit -- --exact`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features engine::tests::connect_block_rejects_overpaying_coinbase_before_committing_chainstate -- --exact`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The pure-core block-validation path and the live chainstate connect path both enforce `subsidy(height) + total_fees` with reject reason `bad-cb-amount`.
- Fee aggregation is wired from the existing `validate_transaction_with_context()` result into both block-level and live connect-path reward checks without reopening unrelated contextual-difficulty or lax-DER implementation work.
- `07.5-05-SUMMARY.md` becomes the authoritative Phase 07.5 artifact, `07.5-04-SUMMARY.md` is explicitly superseded, and the final closeout claim cites the new coinbase-limit proofs plus `bash scripts/verify.sh`.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-05-SUMMARY.md`
</output>
