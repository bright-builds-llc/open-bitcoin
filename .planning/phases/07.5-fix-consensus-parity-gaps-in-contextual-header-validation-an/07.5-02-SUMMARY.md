---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 02
subsystem: consensus
tags: [rust, consensus, ecdsa, der, verification]
requires:
  - phase: 07.5-01
    provides: contextual header parity evidence for CR-01 on the active validation path
  - phase: 07.4-sweep-the-codebase-for-let-else-opportunities
    provides: the CR-01 and CR-02 review findings closed by Phase 07.5
provides:
  - Knots-compatible non-strict legacy ECDSA parsing via flag-gated `from_der_lax`
  - focused legacy-signature regressions for lax-DER acceptance and strict-flag behavior
  - explicit closeout evidence for CR-01, CR-02, CONS-02, CONS-03, VER-01, and VER-02
affects: [phase-8-operator-interfaces, consensus-parity, follow-up-review]
tech-stack:
  added: []
  patterns:
    - non-strict legacy signature parsing stays in one helper with strict flags gating DER parsing mode
    - phase summaries record both repo-required commands and the exact proof commands when cargo test name filtering is ambiguous
key-files:
  created:
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md
  modified:
    - packages/open-bitcoin-consensus/src/signature.rs
    - packages/open-bitcoin-consensus/src/signature/tests.rs
key-decisions:
  - "Use `ecdsa::Signature::from_der_lax()` only when `DERSIG`, `LOW_S`, and `STRICTENC` are all absent."
  - "Keep the `LOW_S` proof on an upstream high-S vector because Knots rejects lax DER earlier on strict-gated paths."
  - "Record fully qualified test-path runs as the actual proof because `cargo test <short-name> -- --exact` filters to zero tests in this workspace."
patterns-established:
  - "Legacy ECDSA verification keeps strict DER and low-S enforcement unchanged while non-strict legacy paths accept Knots-compatible lax DER."
  - "Phase-closeout evidence cites review IDs, exact proof-point strings, and the repo-native verifier before claiming readiness for follow-up review."
requirements-completed: [CONS-02, CONS-03, VER-01, VER-02]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T00:11:21Z
duration: 20m 8s
completed: 2026-04-21
---

# Phase 07.5 Plan 02: Fix Consensus Parity Gaps In Contextual Header Validation And Legacy DER Summary

**Knots-compatible non-strict legacy ECDSA parsing now uses `from_der_lax`, and Phase 07.5 closes with explicit CR-01 and CR-02 verification evidence.**

## Performance

- **Duration:** 20m 8s
- **Started:** 2026-04-20T23:51:13Z
- **Completed:** 2026-04-21T00:11:21Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Restored Knots-compatible non-strict legacy ECDSA parsing by gating `from_der_lax` behind the absence of `DERSIG`, `LOW_S`, and `STRICTENC`.
- Added focused upstream-backed regression coverage for the non-strict lax-DER path without weakening strict DER, low-S, or public-key parsing behavior.
- Closed Phase 07.5 with explicit audit evidence tying CR-01 and CR-02 to passing regressions plus `bash scripts/verify.sh`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Use lax DER parsing only on the non-strict legacy ECDSA path** - `c775541` (`fix`)
2. **Task 2: Close the phase with repo-native verification and explicit review-gap evidence** - recorded in this summary commit (`chore`)

No metadata commit was created here. Per user instruction, `STATE.md` and `ROADMAP.md` remain untouched for the orchestrator-owned post-verification flow.

## Files Created/Modified

- `packages/open-bitcoin-consensus/src/signature.rs` - switches the non-strict legacy verification branch to `from_der_lax` while leaving strict-flag gates unchanged.
- `packages/open-bitcoin-consensus/src/signature/tests.rs` - adds the targeted upstream lax-DER regression and keeps strict-gate coverage explicit.
- `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md` - records the closeout evidence for follow-up review.

## Decisions Made

- Reused the existing `secp256k1` crate’s `from_der_lax` helper instead of adding a custom DER parser or new dependency.
- Kept the `LOW_S` proof separate from the lax-DER proof because actual Knots gating rejects non-strict DER under `LOW_S` before low-S normalization would run.
- Treated the short-name `cargo test ... -- --exact` commands as conformance-only here and captured full test-path runs as the actual execution proof.

## Verification Evidence

- `CR-01` remains closed from plan `07.5-01` via `contextual_header_parity_covers_diffbits_future_time_and_mtp`, including the exact proof points `bad-diffbits` and `time-too-new`.
- `CR-02` closes here via `from_der_lax`, `non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector`, and `verification_helpers_follow_flag_gated_legacy_rules`.
- The plan-specified commands `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features contextual_header_parity_covers_diffbits_future_time_and_mtp -- --exact`, `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector -- --exact`, and `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features verification_helpers_follow_flag_gated_legacy_rules -- --exact` all exited successfully in this workspace, but Cargo filtered them to zero executed tests under `--exact`.
- Actual targeted proof used the fully qualified test paths `block::tests::contextual_header_parity_covers_diffbits_future_time_and_mtp`, `signature::tests::non_strict_legacy_verification_accepts_knots_compatible_lax_der_vector`, and `signature::tests::verification_helpers_follow_flag_gated_legacy_rules`, each of which executed and passed.
- `bash scripts/verify.sh` passed on the final tree.
- Phase 07.5 now satisfies `CONS-02`, `CONS-03`, `VER-01`, and `VER-02`.
- This summary exists to support the follow-up review or verifier pass required by D-08; it does not claim those review steps were skipped.

## Deviations from Plan

None - implementation stayed within the planned signature files and closeout summary scope.

## Issues Encountered

- The plan’s short-name `cargo test ... -- --exact` commands are not sufficient proof in this workspace because Cargo treats them as zero-test filters; the fully qualified test paths were run and recorded instead.
- The task wording implied one vector could prove both lax-DER acceptance and `LOW_S`, but Knots’ strict flag gating rejects lax DER earlier on `LOW_S`; the regression therefore pairs an upstream lax-DER vector with an upstream high-S vector to keep the evidence honest.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The follow-up reviewer or verifier can now close CR-01 and CR-02 against explicit proof points instead of rediscovering the fixes.
- No known implementation blocker remains before Phase 8, aside from the planned review or verifier pass required by D-08.

## Threat Flags

None - no new security-relevant surface was introduced beyond the planned consensus-signature parsing branch and closeout documentation.

---
*Phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an*
*Completed: 2026-04-21*

## Self-Check: PASSED

- Found `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-02-SUMMARY.md`
- Found commit `27a9626`
- Found commit `53f5932`
- Found commit `c775541`
