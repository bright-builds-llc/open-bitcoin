---
phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an
plan: 01
subsystem: consensus
tags: [rust, consensus, pow, chainstate, networking]
requires:
  - phase: 03-consensus-validation-engine
    provides: typed contextual block validation and stable validation errors
  - phase: 03.4-consensus-parity-closure
    provides: parity-oriented consensus regression patterns
  - phase: 07.4-sweep-the-codebase-for-let-else-opportunities
    provides: the CR-01 review finding that triggered this plan
provides:
  - typed contextual PoW schedule and current-time inputs in consensus validation
  - Knots-style bad-diffbits, time-too-old, and time-too-new checks on the contextual header path
  - explicit peer-message timestamp plumbing from managed networking into chainstate block connection
affects: ["07.5-02", phase-8-operator-interfaces, consensus-parity]
tech-stack:
  added: []
  patterns:
    - typed contextual clock injection into pure-core consensus
    - narrow connect_block_with_current_time adapter delegates for effectful callers
key-files:
  created:
    - .planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md
  modified:
    - packages/open-bitcoin-consensus/src/context.rs
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-node/src/chainstate.rs
    - packages/open-bitcoin-node/src/network.rs
key-decisions:
  - "Keep contextual difficulty and future-time checks inside the consensus core by extending typed validation context instead of reading clocks or chain globals."
  - "Preserve deterministic local callers by making existing connect_block helpers thin delegates to new current-time-aware helpers."
patterns-established:
  - "Consensus contextual validation takes caller-owned time explicitly through BlockValidationContext."
  - "Managed peer block handling forwards the network timestamp unchanged into chainstate validation."
requirements-completed: [CONS-02]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.5-2026-04-20T12-08-15
generated_at: 2026-04-21T00:01:53Z
duration: 10m 39s
completed: 2026-04-21
---

# Phase 07.5 Plan 01: Fix Consensus Parity Gaps In Contextual Header Validation Summary

**Contextual header validation now enforces Knots-style diffbits, MTP, and future-time rules through typed consensus context and explicit peer timestamp plumbing.**

## Performance

- **Duration:** 10m 39s
- **Started:** 2026-04-20T23:51:13.849Z
- **Completed:** 2026-04-21T00:01:53Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Added explicit `current_time` plus the minimal PoW schedule fields to `ConsensusParams` and `BlockValidationContext`.
- Restored contextual header rejects for `bad-diffbits`, `time-too-old`, and `time-too-new` with focused regression coverage.
- Threaded peer-owned message timestamps through chainstate and managed networking via narrow `connect_block_with_current_time` helpers.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the missing typed inputs and Knots-style contextual header checks in the consensus core** - `27a9626` (`feat`)
2. **Task 2: Thread explicit current time through the chainstate and managed-node active path** - `53f5932` (`feat`)

No metadata commit was created here. Per user instruction, `STATE.md` and `ROADMAP.md` remain untouched for the orchestrator-owned post-verification step.

## Files Created/Modified

- `packages/open-bitcoin-consensus/src/context.rs` - extends consensus validation context with typed PoW schedule inputs and explicit current time.
- `packages/open-bitcoin-consensus/src/block.rs` - enforces contextual difficulty, MTP, and future-time rules in the consensus core.
- `packages/open-bitcoin-consensus/src/block/tests.rs` - pins exact contextual header reject behavior and covers the new work-selection helper branches.
- `packages/open-bitcoin-consensus/src/context/tests.rs` - verifies the new typed context defaults.
- `packages/open-bitcoin-consensus/tests/parity_closure.rs` - keeps parity-closure tests compiling against the expanded block context.
- `packages/open-bitcoin-chainstate/src/engine.rs` - adds the explicit current-time connect helper and constructs `BlockValidationContext` from caller-owned time.
- `packages/open-bitcoin-chainstate/src/engine/tests.rs` - proves chainstate rejects future blocks when the caller-supplied time is too old.
- `packages/open-bitcoin-node/src/chainstate.rs` - mirrors the current-time-aware chainstate helper in the managed adapter layer.
- `packages/open-bitcoin-node/src/network.rs` - threads peer message timestamps into received-block validation.
- `packages/open-bitcoin-node/src/network/tests.rs` - proves the managed peer path surfaces `time-too-new` and keeps block-sync fixtures aligned with the explicit-time contract.

## Decisions Made

- Kept the contextual parity fix inside the pure-core consensus path by extending typed context rather than moving difficulty or time checks into adapters.
- Used `connect_block_with_current_time` as the single narrow seam for explicit caller time while preserving the existing deterministic `connect_block` entrypoints for unchanged local callers.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added a compile-safe interim `current_time` initialization so Task 1 could satisfy repo verification**
- **Found during:** Task 1
- **Issue:** Adding `current_time` to `BlockValidationContext` broke workspace compilation before Task 2 introduced the planned explicit-time helper seam.
- **Fix:** Initialized `current_time` deterministically from `block.header.time` in chainstate during Task 1, then extended that seam into `connect_block_with_current_time` in Task 2.
- **Files modified:** `packages/open-bitcoin-chainstate/src/engine.rs`, `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`, `cargo test --manifest-path packages/Cargo.toml --all-features`, `bash scripts/verify.sh`
- **Committed in:** `27a9626`

**2. [Rule 3 - Blocking] Updated an existing managed-network sync fixture to use realistic message timestamps**
- **Found during:** Task 2 verification
- **Issue:** The existing in-memory block-sync test delivered peer messages with toy timestamps (`2` through `7`), which correctly triggered the new `time-too-new` rejection once explicit caller time was enforced.
- **Fix:** Switched the sync fixture to use a caller-owned timestamp derived from the delivered block time so the test models a realistic peer receive path.
- **Files modified:** `packages/open-bitcoin-node/src/network/tests.rs`
- **Verification:** `cargo test --manifest-path packages/Cargo.toml --all-features`, `bash scripts/verify.sh`
- **Committed in:** `53f5932`

***

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes were necessary to keep the intermediate and final states verifiable under the repo’s Rust and coverage gates. No scope creep.

## Issues Encountered

- Cargo `-- --exact` did not select the named unit tests by short name alone, so verification was rerun with the full test paths to prove the intended regressions actually executed.
- The repo verification hook initially failed coverage on the new `next_work_required` helper branches, which was resolved by adding a direct helper test instead of weakening coverage requirements.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan `07.5-01` restored explicit current-time plumbing and partial contextual parity, but CR-01 remained open on retarget-boundary difficulty until gap plan `07.5-03` added the typed retarget anchor and boundary regressions.
- Phase `07.5-02` stayed focused on lax DER parity, and `07.5-03` is the authoritative closeout artifact for the remaining contextual-difficulty gap.
- No known blockers remain for the next plan in this phase.

***
*Phase: 07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an*
*Completed: 2026-04-21*

## Self-Check: PASSED

- Found `.planning/phases/07.5-fix-consensus-parity-gaps-in-contextual-header-validation-an/07.5-01-SUMMARY.md`
- Found commit `27a9626`
- Found commit `53f5932`
