---
phase: 80-opt-in-soak-uat-and-release-boundaries
plan: 03
type: execute
wave: 2
depends_on: ["80-01", "80-02"]
files_modified:
  - scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts
  - scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-VERIFICATION.md
autonomous: true
requirements: [VER-05, VER-06, VER-07, REL-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 80-2026-06-17T22-54-57
generated_at: 2026-06-18T00:02:49Z
user_setup: []
must_haves:
  truths:
    - "`bash scripts/verify.sh` runs Phase 75 through Phase 80 deterministic checkers in order and remains local, bounded, and public-network-free."
    - "A Phase 80 Bun/TypeScript checker with fixture tests guards UAT docs, v1.7 parity roots, support/soak schema anchors, forbidden default-verification strings, and release-boundary non-claims."
    - "Default verification does not require public mainnet syncing, multi-day soak, large disk/state, real service-manager installation, wallet mutation, current-tip timing, process scans, or external network dependency."
    - "Phase 80 records passed verification evidence after focused checks and full repo-native verification."
  artifacts:
    - path: "scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts"
      provides: "Deterministic Phase 80 release-boundary checker"
      contains: "validated Phase 80 opt-in soak UAT and release boundaries"
    - path: "scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts"
      provides: "Fixture-root checker regression tests"
      contains: "passes_when_phase80_fixture_contains_every_release_boundary_anchor"
    - path: "scripts/verify.sh"
      provides: "Repo-native verifier wiring after Phase 79"
      contains: "bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts"
    - path: ".planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-VERIFICATION.md"
      provides: "Passed Phase 80 verification evidence"
      contains: "status: passed"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts"
    - from: "scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts"
      to: "docs/parity/index.json"
      via: "structured parity root parse"
      pattern: "v1-7-full-sync-soak-recovery-release-boundaries"
---

<objective>
Close Phase 80 with a deterministic release-boundary checker, verifier wiring, simplification pass, and verification evidence.

Purpose: VER-05 requires bounded default verification, VER-06 and VER-07 require guarded operator/parity audit roots, and REL-04 requires a deterministic check against v1.7 claim drift.
Output: Phase 80 Bun checker/test pair, `scripts/verify.sh` wiring after Phase 79, LOC freshness, and `80-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-CONTEXT.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-RESEARCH.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-01-SUMMARY.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-02-SUMMARY.md
@scripts/check-phase79-diagnostics-support-bundle.ts
@scripts/check-phase79-diagnostics-support-bundle.test.ts
@scripts/check-v1.6-release-boundaries.ts
@scripts/verify.sh
@docs/operator/runtime-guide.md
@README.md
@docs/parity/release-readiness.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/catalog/operator-runtime-release-hardening.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/forensics.rs
@packages/open-bitcoin-cli/src/operator/support/soak_evidence.rs
@packages/open-bitcoin-cli/src/operator/soak/report.rs
@packages/open-bitcoin-cli/src/operator/soak/ledger.rs
</context>

<interfaces>
From existing Phase 79 checker pattern:
```typescript
const maybeRepoRoot = process.env[REPO_ROOT_OVERRIDE_ENV];
const REPO_ROOT =
  maybeRepoRoot === undefined ? path.resolve(import.meta.dir, "..") : path.resolve(maybeRepoRoot);

function requireContains(text: string, needle: string, label: string, failures: string[]): void;
function requireNotContains(text: string, needle: string, label: string, failures: string[]): void;
```

Required Phase 80 checker constants:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE80_REPO_ROOT";
const PHASE80_REQUIREMENTS = ["VER-05", "VER-06", "VER-07", "REL-04"] as const;
const SURFACE_ID = "v1-7-full-sync-soak-recovery-release-boundaries";
const PHASE80_TEST_COMMAND =
  "bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts";
const PHASE80_CHECKER_COMMAND =
  "bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts";
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic Phase 80 checker and fixture tests</name>
  <files>scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts, scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts</files>
  <read_first>
    - `scripts/check-phase79-diagnostics-support-bundle.ts`
    - `scripts/check-phase79-diagnostics-support-bundle.test.ts`
    - `scripts/check-v1.6-release-boundaries.ts`
    - `docs/operator/runtime-guide.md`
    - `README.md`
    - `docs/parity/release-readiness.md`
    - `docs/parity/index.json`
    - `docs/parity/checklist.md`
    - `docs/parity/README.md`
    - `docs/parity/deviations-and-unknowns.md`
    - `docs/parity/catalog/operator-runtime-release-hardening.md`
    - `packages/open-bitcoin-cli/src/operator/support.rs`
    - `packages/open-bitcoin-cli/src/operator/support/forensics.rs`
    - `packages/open-bitcoin-cli/src/operator/support/soak_evidence.rs`
    - `packages/open-bitcoin-cli/src/operator/soak/report.rs`
    - `packages/open-bitcoin-cli/src/operator/soak/ledger.rs`
  </read_first>
  <behavior>
    - RED: Add fixture-root tests before relying on the real worktree.
    - Required test names: `passes_when_phase80_fixture_contains_every_release_boundary_anchor`, `fails_when_uat_matrix_missing_workflow_or_proof_boundary`, `fails_when_parity_roots_omit_v1_7_requirements_or_surface`, `fails_when_verify_order_or_default_boundary_forbidden_strings_drift`, and `fails_when_broad_claim_or_new_manifest_appears`.
  </behavior>
  <action>
    Create the Phase 80 checker/test pair per D-01, D-02, D-03, D-04, D-09, D-12, D-15, and D-16. Follow the Phase 79 checker/test style: Bun, TypeScript, fixture roots under `os.tmpdir()`, failure accumulation, no new packages, no network calls, and `maybe...` naming for nullable values. Do not add a runtime sandbox, hermetic container, or strict offline dependency mode as the Phase 80 proof; the checker must prove the repository's deterministic verifier boundary from local files and fixtures.

    The checker must verify:
    - Phase 80 plan frontmatter across `80-01-PLAN.md`, `80-02-PLAN.md`, and `80-03-PLAN.md` covers VER-05, VER-06, VER-07, and REL-04.
    - `docs/operator/runtime-guide.md` contains `Phase 80 v1.7 opt-in soak UAT matrix`, exactly the four workflow names from D-05, `Evidence proves`, `Does not prove`, Cargo command form, Bazel command form, and the typed evidence/non-proof phrases from Plan 01.
    - `README.md`, `docs/parity/release-readiness.md`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/deviations-and-unknowns.md`, and `docs/parity/catalog/operator-runtime-release-hardening.md` contain `explicit opt-in full-sync soak and recovery hardening`, `v1-7-full-sync-soak-recovery-release-boundaries`, VER-05, VER-06, VER-07, REL-04, and the D-15 non-claim list where claim-bearing.
    - `docs/parity/index.json` parses and has exactly one top-level/checklist v1.7 closeout surface with id or name `v1-7-full-sync-soak-recovery-release-boundaries`, status `done`, requirements including VER-05, VER-06, VER-07, and REL-04, and evidence including existing roots, `scripts/check-parity-breadcrumbs.ts`, Phase 75 through Phase 80 checker paths, and `scripts/verify.sh`.
    - Support and soak schema anchors exist in current Rust source without adding Rust files: `SupportEvidenceBundle`, `support_forensics`, `resource_bound_evidence`, `soak_evidence`, `SupportForensicsEvidence`, `ForensicTimelineEntry`, `CheckpointChainEvidence`, `ForensicNarrative`, `SoakSupportEvidence`, `SoakReportProjection`, and `SoakLedgerEventEnvelope`.
    - No new evidence manifest path exists for Phase 80. Fail if a fixture or real tree contains paths such as `docs/parity/v1.7-evidence-manifest.json`, `docs/parity/evidence-manifest-v1.7.json`, or `docs/parity/release-evidence-v1.7.json`.
    - `scripts/verify.sh` runs Phase 75 through Phase 79 checker commands before Phase 80, and will run Phase 80 test/checker immediately after `bun run scripts/check-phase79-diagnostics-support-bundle.ts` once Task 2 wires it.
    - `scripts/verify.sh` does not contain forbidden default-verification strings: `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl`, `launchctl`, `-openbitcoinsync=mainnet-ibd`, `openbitcoinsync=mainnet-ibd`, `sleep 86400`, `sleep 259200`, `multi-day wall-clock`, `current tip`, `current-tip`, `release-blocking live sync`, `/proc`, `lsof`, `fallocate`, `mkfile`, `dd if=`, or `107374182400`.

    Use targeted claim checks only. Do not scan all historical docs for generic words like `production` or `mainnet`; check exact v1.7/current claim-bearing files and anchors so historical v1.3 through v1.6 evidence remains valid.
  </action>
  <verify>
    <automated>bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts</automated>
    <automated>bun --check scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts</automated>
  </verify>
  <acceptance_criteria>
    - The checker test file contains all five required test names and uses Arrange, Act, Assert comments for non-trivial tests.
    - Fixture tests prove positive and negative behavior for UAT anchors, parity roots, verify ordering/forbidden strings, broad claim drift, and forbidden manifest creation.
    - The checker uses `OPEN_BITCOIN_PHASE80_REPO_ROOT` for fixture roots and exits non-zero with concrete failure lines.
    - The checker contains the full D-03 forbidden default-verification list plus large-disk/current-tip guard strings.
  </acceptance_criteria>
  <done>Phase 80 has a deterministic closure checker with fixture-root regression coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Phase 80 checker, run simplification pass, and record verification</name>
  <files>scripts/verify.sh, docs/metrics/lines-of-code.md, .planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-VERIFICATION.md</files>
  <read_first>
    - `scripts/verify.sh`
    - `scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`
    - `scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts`
    - `docs/metrics/lines-of-code.md`
    - `scripts/generate-loc-report.ts`
    - `.planning/phases/79-diagnostics-and-support-bundle-forensics/79-VERIFICATION.md`
  </read_first>
  <action>
    Update `scripts/verify.sh` per D-01 and D-12 so this exact pair appears immediately after `bun run scripts/check-phase79-diagnostics-support-bundle.ts`:
    - `bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts`
    - `bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`

    Run this explicit simplification pass before full verification:
    - Confirm the phase did not add Rust source/test files. If `git diff --name-only` includes `packages/open-bitcoin-*/src/*.rs` or `packages/open-bitcoin-*/tests/*.rs`, stop and add the required source breadcrumbs per D-10 and D-11 before continuing.
    - Confirm the phase did not add a new evidence manifest. Remove any `docs/parity/*manifest*v1.7*` or `docs/parity/*evidence*v1.7*.json` file unless a concrete replanned gap exists.
    - Confirm the runtime-guide Phase 80 matrix has exactly four workflows and no broad v1.6-style scenario sweep.
    - Confirm `scripts/verify.sh` contains no public-network, manual-peer, real-service-manager, process-table, current-tip, multi-day sleep, or large-disk stress command.
    - Confirm claim-bearing docs say what v1.7 proves and does not prove without phrases like `simplified version`, `static for now`, `future enhancement`, `placeholder`, `basic version`, or `will be wired later`.

    Regenerate tracked LOC only if needed with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`.

    Run focused checks, then full repo verification. After `bash scripts/verify.sh` passes, create `.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-VERIFICATION.md` with frontmatter `phase: 80-opt-in-soak-uat-and-release-boundaries`, `status: passed`, `verified_at: <UTC timestamp>`, `requirements: [VER-05, VER-06, VER-07, REL-04]`, `generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, and `phase_lifecycle_id: 80-2026-06-17T22-54-57`.

    The verification body must list exact commands run:
    - `bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts`
    - `bun --check scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`
    - `bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`
    - `bun run scripts/check-parity-breadcrumbs.ts --check`
    - `bash scripts/verify.sh`

    Record that default verification stayed deterministic and did not require public mainnet syncing, multi-day soak, large disk/state, real service-manager installation, wallet mutation, current-tip timing, process scans, or external network dependency.
  </action>
  <verify>
    <automated>bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts</automated>
    <automated>bun --check scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts</automated>
    <automated>bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` runs the Phase 80 checker test and checker immediately after Phase 79.
    - `bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts` passes against the real worktree.
    - `rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress|systemctl|launchctl|openbitcoinsync=mainnet-ibd|sleep 86400|sleep 259200|release-blocking live sync|lsof|/proc|fallocate|mkfile|dd if=|107374182400" scripts/verify.sh` returns no matches.
    - `.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-VERIFICATION.md` contains `status: passed`, VER-05, VER-06, VER-07, REL-04, and `bash scripts/verify.sh`.
    - `bash scripts/verify.sh` passes.
  </acceptance_criteria>
  <done>Phase 80 deterministic checker is wired into repo-native verification and passed verification evidence is recorded.</done>
</task>

</tasks>

<simplification_pass>
This plan's Task 2 contains the required explicit simplification pass. Do not skip it: the pass is part of the Phase 80 acceptance criteria because this phase is about preventing claim and verification expansion, not adding behavior.
</simplification_pass>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository files -> checker verdict | Checker consumes local docs, JSON, scripts, and Rust anchors and must fail deterministically on missing or overbroad evidence. |
| default verifier -> external world | `scripts/verify.sh` must remain bounded and must not trigger public networks, real service managers, current-tip gates, process scans, large disk tests, wallet mutation, or multi-day waits. |
| support/soak schemas -> release claim | Existing typed evidence anchors must support the docs claim without a new schema registry or manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-01 | D | `scripts/verify.sh` | mitigate | Phase 80 checker rejects D-03 forbidden strings and verifies Phase 75-80 checker ordering. |
| T-80-03-02 | R | Phase 80 checker | mitigate | Fixture tests prove missing UAT, parity, ordering, forbidden-claim, and forbidden-manifest failures before real-worktree execution. |
| T-80-03-03 | T | `docs/parity/index.json` | mitigate | Checker parses JSON and requires one done v1.7 closeout surface with VER-05, VER-06, VER-07, REL-04, and evidence paths. |
| T-80-03-04 | I | support-bundle evidence | mitigate | Checker anchors existing support/forensics redaction and schema types and docs reject raw logs, artifact existence, or stale reports as proof. |
| T-80-03-05 | S/R | release wording | mitigate | Checker requires scoped v1.7 claim and D-15 non-claims in current claim-bearing docs while avoiding broad scans of historical docs. |
</threat_model>

<verification>
- `bun test scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts`
- `bun --check scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`
- `bun run scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- VER-05 is satisfied by deterministic verifier wiring and forbidden-default-check guards.
- VER-06 is guarded by checker anchors for the four-workflow runtime-guide UAT matrix and repo-local Cargo/Bazel commands.
- VER-07 is guarded by checker anchors for source breadcrumbs, support/soak schemas, deterministic checkers, fixture tests, operator docs, and parity roots.
- REL-04 is guarded by checker anchors for scoped v1.7 claim wording and explicit non-claims.
- Phase 80 verification evidence records a passed default verifier without public-network, multi-day, large-disk, service-manager, wallet-mutation, current-tip, process-scan, or external network dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-03-SUMMARY.md`.
</output>
