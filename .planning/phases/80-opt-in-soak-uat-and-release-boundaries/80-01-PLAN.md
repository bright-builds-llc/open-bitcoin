---
phase: 80-opt-in-soak-uat-and-release-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operator/runtime-guide.md
  - README.md
autonomous: true
requirements: [VER-06, REL-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 80-2026-06-17T22-54-57
generated_at: 2026-06-18T00:02:49Z
user_setup: []
must_haves:
  truths:
    - "Operators can find one Phase 80 v1.7 UAT matrix with exactly four workflows: multi-day soak lifecycle, bounded recovery drill, support-bundle generation, and post-failure diagnosis."
    - "Each CLI-backed workflow includes copy-pasteable repo-local Cargo and Bazel command forms."
    - "Each UAT workflow states what typed evidence can prove and what it does not prove."
    - "README names the current v1.7 boundary as explicit opt-in full-sync soak and recovery hardening, not production-node readiness."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Focused v1.7 opt-in soak UAT matrix"
      contains: "Phase 80 v1.7 opt-in soak UAT matrix"
    - path: "README.md"
      provides: "Contributor-facing v1.7 operator/release posture"
      contains: "explicit opt-in full-sync soak and recovery hardening"
  key_links:
    - from: "docs/operator/runtime-guide.md"
      to: "packages/open-bitcoin-cli"
      via: "repo-local Cargo operator command"
      pattern: "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --"
    - from: "docs/operator/runtime-guide.md"
      to: "//packages/open-bitcoin-cli:open_bitcoin"
      via: "repo-local Bazel operator command"
      pattern: "bazel run //packages/open-bitcoin-cli:open_bitcoin --"
---

<objective>
Create the operator-facing Phase 80 UAT entrypoint and update README posture for the scoped v1.7 closeout.

Purpose: VER-06 requires copy-pasteable repo-local operator commands, and REL-04 requires the public-facing claim to remain scoped to explicit opt-in soak and recovery hardening.
Output: a focused runtime-guide UAT matrix plus README wording that points at v1.7 without broad production claims.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-CONTEXT.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-RESEARCH.md
@docs/operator/runtime-guide.md
@README.md
@docs/parity/release-readiness.md
@docs/parity/checklist.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add focused v1.7 opt-in UAT matrix</name>
  <files>docs/operator/runtime-guide.md</files>
  <read_first>
    - `docs/operator/runtime-guide.md`
    - `.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md`
    - `.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md`
    - `.planning/phases/79-diagnostics-and-support-bundle-forensics/79-CONTEXT.md`
    - `.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-CONTEXT.md`
  </read_first>
  <action>
    Add a new section named `### Phase 80 v1.7 opt-in soak UAT matrix` near the existing Phase 75 through Phase 79 operator evidence sections. Implement D-05, D-06, D-07, and D-08 exactly: this must be one reviewer-friendly matrix with exactly four workflows, not a broad Phase 73/v1.6 scenario sweep.

    The matrix must have columns for workflow, repo-local Cargo commands, repo-local Bazel commands, evidence proves, and does not prove. Use the headings `Evidence proves` and `Does not prove` so the Phase 80 checker can anchor them.

    Workflow 1, `Multi-day soak lifecycle`, must include Cargo and Bazel forms for `soak start`, `soak resume`, `soak stop`, and `soak report` using the existing Phase 75 command shape. Use `/tmp/open-bitcoin-mainnet`, `--network mainnet`, `--elapsed-time-seconds 259200`, `--checkpoint-interval-seconds 300`, `--target-height <target-height>`, `--peer-policy daemon-configured`, `--disk-budget-bytes 107374182400`, and `--stop-condition elapsed-time`.

    Workflow 2, `Bounded recovery drill`, must include Cargo and Bazel forms for:
    - `status --format json` against `/tmp/open-bitcoin-recovery-drill`;
    - `support bundle --output-dir=/tmp/open-bitcoin-recovery-support --format json` against the same datadir.
    Its proof text must require typed `recovery_evidence`, recovery action class, cause, next action, and related support evidence when present. Its non-proof text must say the drill does not repair stores, delete locks, prove source-datadir safety, prove process attribution, or authorize mutation.

    Workflow 3, `Support-bundle generation`, must include Cargo and Bazel forms for `support bundle --output-dir=/tmp/open-bitcoin-support --format json` against `/tmp/open-bitcoin-mainnet`. Its proof text must require redaction facts, `support_forensics`, forensic timeline, checkpoint chain, failure narrative, evidence basis, next action, confidence, and resource/support bounds. Its non-proof text must state that bundle existence, copied files, raw logs, daemon startup, peer reachability, elapsed time, or stale reports do not prove soak stability.

    Workflow 4, `Post-failure diagnosis`, must include Cargo and Bazel forms for `soak report --run-id <run-id>`, `status --format json`, and `support bundle --output-dir=/tmp/open-bitcoin-support --format json` against `/tmp/open-bitcoin-mainnet`. Its proof text must require a typed final outcome, `support_forensics.verdict`, likely cause, evidence basis, next action, checkpoint-chain validation, resource/recovery/stall evidence, and whether the run proved soak stability, diagnosed a blocker, or stopped inconclusively. Its non-proof text must reject production-node readiness, current-tip timing SLAs, release-blocking live sync, public-network CI, inbound serving, relay, wallet safety, migration apply, packaging, GUI, hosted dashboards, automatic upload, and destructive repair.

    Keep public-network, service-manager, multi-day wall-clock, large-disk, and current-tip checks as opt-in UAT only. Do not imply any of these commands belongs in `bash scripts/verify.sh` per D-02, D-03, and D-15.
  </action>
  <verify>
    <automated>rg -n "Phase 80 v1.7 opt-in soak UAT matrix|Multi-day soak lifecycle|Bounded recovery drill|Support-bundle generation|Post-failure diagnosis|Evidence proves|Does not prove" docs/operator/runtime-guide.md</automated>
    <automated>rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --|bazel run //packages/open-bitcoin-cli:open_bitcoin --|soak start|soak resume|soak stop|soak report|support bundle --output-dir=/tmp/open-bitcoin-support --format json" docs/operator/runtime-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - The new matrix contains exactly the four D-05 workflows and no additional Phase 80 UAT workflow rows.
    - Each workflow includes at least one Cargo form and one Bazel form, with the multi-day lifecycle and post-failure diagnosis workflows including all listed subcommands.
    - The matrix explicitly says what typed evidence proves and what it does not prove for every workflow.
    - The runtime guide still says default verification is deterministic and excludes public-network, service-manager, multi-day, large-disk, current-tip, and release-blocking live-sync gates.
  </acceptance_criteria>
  <done>Operators have a focused Phase 80 v1.7 UAT matrix with copy-paste Cargo/Bazel commands and evidence boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refresh README v1.7 operator and release posture</name>
  <files>README.md</files>
  <read_first>
    - `README.md`
    - `docs/operator/runtime-guide.md`
    - `docs/parity/release-readiness.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>
    Update README claim-bearing sections per D-13, D-14, and D-15 while leaving the managed badge block untouched. Replace current-final v1.6 wording with current v1.7 wording that says the active closeout is source-built, explicit opt-in full-sync soak and recovery hardening.

    The README must point readers to `docs/operator/runtime-guide.md`, `docs/parity/release-readiness.md`, `docs/parity/index.json`, and `docs/parity/checklist.md` for the v1.7 review posture. Keep v1.6 described as historical explicit opt-in full-sync completion evidence, not as the current final boundary after Phase 80.

    Required current v1.7 claim wording:
    - `explicit opt-in full-sync soak and recovery hardening`;
    - `durable multi-day soak evidence`;
    - `resource bounds`;
    - `recovery diagnosis`;
    - `progress guarantees`;
    - `stall diagnosis`;
    - `support-bundle forensics`;
    - `deterministic release-boundary checks`.

    Required non-claims:
    - inbound serving;
    - address relay;
    - block serving;
    - transaction relay;
    - compact block relay;
    - production-funds wallet safety or production-funds wallet use;
    - migration apply mode;
    - signed packaging;
    - Windows service support;
    - GUI parity or GUI readiness;
    - hosted dashboards;
    - public-network default checks or public-network CI;
    - release-blocking live sync;
    - automatic support-bundle upload;
    - destructive repair;
    - broad production-node readiness.

    Do not add marketing language or broader support claims. Keep the README concise and route detailed command matrices to `docs/operator/runtime-guide.md`.
  </action>
  <verify>
    <automated>rg -n "explicit opt-in full-sync soak and recovery hardening|durable multi-day soak evidence|support-bundle forensics|deterministic release-boundary checks" README.md</automated>
    <automated>rg -n "inbound serving|address relay|block serving|transaction relay|compact block relay|production-funds wallet|migration apply mode|signed packaging|Windows service support|GUI|hosted dashboards|public-network CI|release-blocking live sync|automatic support-bundle upload|destructive repair|production-node readiness" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README identifies v1.7 as the current scoped soak/recovery closeout after Phase 80.
    - README preserves v1.6 only as historical full-sync completion evidence.
    - README links to runtime-guide and parity roots instead of duplicating the command matrix.
    - README does not claim production-node readiness, production wallet safety, inbound/relay behavior, packaging, GUI, hosted dashboards, public-network CI, release-blocking live sync, automatic support upload, or destructive repair.
  </acceptance_criteria>
  <done>README reflects the scoped v1.7 operator/release boundary and preserves explicit deferred production-adjacent scope.</done>
</task>

</tasks>

<simplification_pass>
Before completing this plan, review the docs diff and remove any duplicated command prose that is already covered by older Phase 75 through Phase 79 sections unless the new Phase 80 matrix needs it as its reviewer entrypoint. Keep exactly four UAT workflows, avoid a v1.6-style broad sweep, avoid a new evidence manifest, and keep detailed command examples in `docs/operator/runtime-guide.md` rather than expanding README.
</simplification_pass>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs -> human release interpretation | Operators and reviewers may overinterpret UAT commands or artifacts as production proof. |
| opt-in UAT -> default verification | Long-running, public-network, service-manager, current-tip, and large-disk workflows must remain outside `bash scripts/verify.sh`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01-01 | S/R | `docs/operator/runtime-guide.md` | mitigate | Each UAT row must separate `Evidence proves` from `Does not prove` so artifact presence cannot impersonate soak stability proof. |
| T-80-01-02 | D | UAT command matrix | mitigate | Label public-network and multi-day workflows as opt-in only and explicitly keep them outside deterministic verification per D-02 and D-03. |
| T-80-01-03 | E/R | `README.md` | mitigate | README must preserve D-15 non-claims and avoid broad production-node, wallet, migration, packaging, GUI, hosted-dashboard, and public-network CI wording. |
| T-80-01-04 | I | support-bundle workflow docs | mitigate | Support-bundle generation text must describe redacted typed evidence and state that raw logs, endpoint tables, and artifact existence are not proof. |
</threat_model>

<verification>
- `rg -n "Phase 80 v1.7 opt-in soak UAT matrix|Multi-day soak lifecycle|Bounded recovery drill|Support-bundle generation|Post-failure diagnosis|Evidence proves|Does not prove" docs/operator/runtime-guide.md`
- `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --|bazel run //packages/open-bitcoin-cli:open_bitcoin --" docs/operator/runtime-guide.md`
- `rg -n "explicit opt-in full-sync soak and recovery hardening|support-bundle forensics|deterministic release-boundary checks" README.md`
</verification>

<success_criteria>
- VER-06 is satisfied by one focused Phase 80 runtime-guide UAT matrix with Cargo and Bazel command forms for the four required workflows.
- REL-04 is supported by README wording that identifies v1.7 as scoped soak/recovery hardening only.
- No default-verification, production-node, wallet-production, migration-apply, packaging, GUI, hosted-dashboard, automatic-upload, destructive-repair, or public-network CI claim is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-01-SUMMARY.md`.
</output>
