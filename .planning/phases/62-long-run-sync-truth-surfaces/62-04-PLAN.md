---
phase: 62-long-run-sync-truth-surfaces
plan: 04
type: execute
wave: 3
depends_on: [62-02, 62-03]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - scripts/check-phase62-sync-truth-surfaces.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [OBS-01, OBS-02]
user_setup: []
must_haves:
  truths:
    - "Operator and architecture docs describe the canonical Phase 62 sync truth projection and the status/dashboard/RPC/metrics/log/live-smoke agreement rule."
    - "Docs preserve the UI-SPEC field order and copy rules, including `Unavailable: {reason}` and stable snake_case machine labels."
    - "A deterministic Bun checker fails if Rust, TypeScript, docs, or default verification drift from the Phase 62 field contract."
    - "`bash scripts/verify.sh` includes only deterministic Phase 62 checks and still excludes public-network live-smoke commands."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Operator Phase 62 truth-surface review workflow"
      contains: "Phase 62 sync truth fields"
    - path: "docs/architecture/status-snapshot.md"
      provides: "Canonical shared status snapshot field contract"
      contains: "configured_targets"
    - path: "docs/architecture/operator-observability.md"
      provides: "Metrics/log/live-smoke bounded observability contract"
      contains: "latest_stop_reason"
    - path: "scripts/check-phase62-sync-truth-surfaces.ts"
      provides: "Deterministic cross-surface field-contract guard"
      contains: "validated Phase 62 sync truth surfaces"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase62-sync-truth-surfaces.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase62-sync-truth-surfaces.ts"
    - from: "scripts/check-phase62-sync-truth-surfaces.ts"
      to: "Rust/TS/docs truth fields"
      via: "exact string assertions"
      pattern: "configured_targets"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-06T19-46-48
generated_at: 2026-06-06T20:29:59Z
---

<objective>
Document and deterministically guard the Phase 62 cross-surface sync truth contract.

Purpose: OBS-01, OBS-02, D-09, D-10, D-11, D-12, and the repo verification contract require field-drift checks across Rust, TypeScript, docs, and default verification without public-network execution.
Output: Updated operator/architecture docs, a Bun contract checker, verify integration, and aggregate deterministic verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-02-SUMMARY.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-03-SUMMARY.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@scripts/check-phase61-resource-recovery-boundaries.ts
@scripts/verify.sh

<interfaces>
Required Phase 62 field strings for docs and checker:
```text
lifecycle
phase
configured_targets
attempt_counters
progress_signal
last_successful_progress_unix_seconds
latest_stop_reason
last_error
recovery_category
recovery_action
resource_pressure
peer health
header_height
downloaded_block_height
maybe_downloaded_block_hash
connected_block_height
maybe_connected_block_hash
messages_processed
headers_received
blocks_received
```

Public-network strings that must not appear in `scripts/verify.sh`:
```text
run-live-mainnet-smoke
--manual-peer
--restart-after-progress
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh operator and architecture truth-surface docs</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-01 through D-12 and deferred scope
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md` "Common Pitfalls"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` copywriting and terminal output contracts
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-02-SUMMARY.md`
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-03-SUMMARY.md`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
  </read_first>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md</files>
  <action>
    In `docs/operator/runtime-guide.md`, add a concise "Phase 62 sync truth fields" subsection near the existing durable sync status guidance. State that `open-bitcoin status`, `open-bitcoin dashboard`, `open-bitcoin sync status`, RPC sync status/warnings, bounded metrics, structured logs, and opt-in live-smoke snapshots read the shared status/durable sync truth projection per D-01. List the field order exactly as in the interface block and UI-SPEC: lifecycle, phase, configured targets, attempt counters, progress signal, last progress, latest stop reason, latest error, recovery category, recovery action, resource pressure, peer health, header height, downloaded block height/hash, connected block height/hash, and bounded counters.

    Keep copy-pasteable repo-local commands for operator review:
    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json`
    and
    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json`.
    Keep `bun run scripts/run-live-mainnet-smoke.ts --datadir=/tmp/open-bitcoin-mainnet` only under explicit opt-in UAT language, not default verification.

    In `docs/architecture/status-snapshot.md`, update the current prior-milestone wording to the current milestone wording and document the new `sync.configured_targets`, `sync.attempt_counters`, and `sync.latest_stop_reason` fields. Include the exact unavailable rule: `Unavailable: {reason}`. Preserve Phase 61 recovery category labels and resource-pressure field names without renaming per D-12.

    In `docs/architecture/operator-observability.md`, state that metrics remain bounded numeric samples for `header_height`, `downloaded_block_height`, `connected_block_height`, `sync_height`, and `peer_count`, while structured logs carry compact labels for `progress_signal`, `latest_stop_reason`, `recovery_category`, configured targets, and attempt counters. State that live-smoke Markdown/JSON uses compact snapshots and does not persist raw daemon tails. Do not add service supervision, support bundle expansion, hosted dashboard, GUI, broad production-node claim, or public-network default verification.
  </action>
  <acceptance_criteria>
    - `rg -n "Phase 62 sync truth fields|configured_targets|attempt_counters|latest_stop_reason|Unavailable: \\{reason\\}|open-bitcoin status|open-bitcoin dashboard|open-bitcoin sync status" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md` finds the documented contract.
    - `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet sync status --format json" docs/operator/runtime-guide.md` finds both repo-local commands.
    - `rg -n "metrics remain bounded numeric samples|does not persist raw daemon tails|public-network.*opt-in" docs/architecture/operator-observability.md docs/operator/runtime-guide.md` finds bounded evidence and opt-in wording.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 62 sync truth fields|configured_targets|attempt_counters|latest_stop_reason|Unavailable: \\{reason\\}|metrics remain bounded numeric samples|does not persist raw daemon tails" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md</automated>
  </verify>
  <done>Docs explain the Phase 62 truth contract, field order, bounded observability posture, and deterministic/opt-in verification split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic cross-surface contract checker</name>
  <read_first>
    - `scripts/check-phase61-resource-recovery-boundaries.ts`
    - `scripts/verify.sh`
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/runtime/support.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/node.rs`
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
  </read_first>
  <files>scripts/check-phase62-sync-truth-surfaces.ts, scripts/verify.sh</files>
  <behavior>
    - Test: checker fails if `configured_targets`, `attempt_counters`, or `latest_stop_reason` is missing from `status.rs`.
    - Test: checker fails if status/dashboard/sync-status/live-smoke source omits required Phase 62 labels.
    - Test: checker fails if docs omit any field listed in the interface block.
    - Test: checker fails if `scripts/verify.sh` contains `run-live-mainnet-smoke`, `--manual-peer`, or `--restart-after-progress`.
    - Test: checker passes after `scripts/verify.sh` invokes `bun run scripts/check-phase62-sync-truth-surfaces.ts`.
  </behavior>
  <action>
    Create `scripts/check-phase62-sync-truth-surfaces.ts` in the same style as `scripts/check-phase61-resource-recovery-boundaries.ts`: repo-root path resolution, `readText`, `requireContains`, `requireNotContains`, required string arrays, and final `console.log("validated Phase 62 sync truth surfaces")`.

    The checker must read:
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/runtime/support.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/node.rs`
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
    - `scripts/verify.sh`

    Require exact source strings for the Phase 62 fields: `SyncConfiguredTargets`, `SyncAttemptCounters`, `SyncStopReasonStatus`, `configured_targets`, `attempt_counters`, `latest_stop_reason`, `configuredTargets`, `attemptCounters`, `latestStopReason`, `Sync configured targets`, `Sync attempts`, `Sync latest stop reason`, `Configured targets`, `Attempt counters`, `Latest stop reason`, `progress_signal=`, `latest_stop_reason=`, `recovery_category=`, `target_outbound_peers=`, `target_header_height=`, `messages_processed`, `headers_received`, and `blocks_received`.

    Require docs strings from the interface block and require `scripts/test-run-live-mainnet-smoke.sh` as the deterministic live-smoke fixture path. Require `scripts/verify.sh` to include `bun run scripts/check-phase62-sync-truth-surfaces.ts`. Require `scripts/verify.sh` not to include `run-live-mainnet-smoke`, `--manual-peer`, or `--restart-after-progress` per D-09.

    Wire the checker into `scripts/verify.sh` immediately after the Phase 61 checker. Do not add public-network commands to `scripts/verify.sh`.
  </action>
  <acceptance_criteria>
    - `rg -n "validated Phase 62 sync truth surfaces|SyncConfiguredTargets|SyncAttemptCounters|SyncStopReasonStatus|configuredTargets|attemptCounters|latestStopReason|requireNotContains\\(verifyScript, \"run-live-mainnet-smoke\"" scripts/check-phase62-sync-truth-surfaces.ts` finds the checker contract.
    - `rg -n "bun run scripts/check-phase62-sync-truth-surfaces\\.ts" scripts/verify.sh` finds verify integration.
    - `bun run scripts/check-phase62-sync-truth-surfaces.ts` passes.
    - `bash -c 'if rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh; then exit 1; fi'` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase62-sync-truth-surfaces.ts</automated>
  </verify>
  <done>Default verification has a deterministic cross-surface contract guard for Phase 62 truth fields and public-network exclusion.</done>
</task>

<task type="auto">
  <name>Task 3: Run aggregate deterministic verification</name>
  <read_first>
    - `AGENTS.md` repo-local verification contract
    - `scripts/verify.sh`
    - `docs/metrics/lines-of-code.md`
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decision D-09
  </read_first>
  <files>docs/metrics/lines-of-code.md</files>
  <action>
    Run `bash scripts/verify.sh` from the repo root after the Phase 62 checker passes. Keep generated freshness changes to `docs/metrics/lines-of-code.md` if verification updates it. Do not add public-network live-smoke, manual peer, service-supervision, support-bundle expansion, hosted dashboard, GUI, broad production-node, or production service checks to default verification.
  </action>
  <acceptance_criteria>
    - `bash scripts/verify.sh` passes.
    - `bash -c 'if rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh; then exit 1; fi'` passes.
    - If `docs/metrics/lines-of-code.md` changes, `git diff -- docs/metrics/lines-of-code.md` shows only generated LOC freshness output.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Repo-native deterministic verification passes with Phase 62 contract checks and no default public-network execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/checker -> operator expectations | Documentation and deterministic checks define what operators and reviewers treat as truthful sync evidence. |
| default verification -> public network | Default verification must not depend on public-network availability. |
| generated LOC -> tracked docs | Verification may refresh an intentionally tracked generated artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04-01 | Repudiation | cross-surface field contract | mitigate | Add a deterministic checker that reads Rust, TypeScript, docs, and verify script sources for exact field names and labels. |
| T-62-04-02 | Denial of Service | default verification | mitigate | Require fixture checks only and assert `scripts/verify.sh` excludes public-network live-smoke/manual-peer/restart commands. |
| T-62-04-03 | Tampering | operator docs | mitigate | Guard exact docs strings for field order, unavailable semantics, bounded metrics/logs, and repo-local commands. |
| T-62-04-04 | Information Disclosure | docs/live-smoke guidance | mitigate | Document compact live-smoke summaries and no raw daemon tails; do not expand support bundles or hosted/report upload surfaces. |
</threat_model>

<verification>
Run docs/checker and aggregate deterministic verification:

```bash
rg -n "Phase 62 sync truth fields|configured_targets|attempt_counters|latest_stop_reason|Unavailable: \{reason\}|metrics remain bounded numeric samples|does not persist raw daemon tails" docs/operator/runtime-guide.md docs/architecture/status-snapshot.md docs/architecture/operator-observability.md
bun run scripts/check-phase62-sync-truth-surfaces.ts
bash scripts/verify.sh
```
</verification>

<success_criteria>
- Docs and checkers describe and guard the canonical Phase 62 truth projection.
- The deterministic checker spans Rust, TypeScript, docs, and default verification boundaries.
- Default verification remains public-network-free.
- Generated LOC freshness is preserved if verification updates it.
</success_criteria>

<output>
After completion, create `.planning/phases/62-long-run-sync-truth-surfaces/62-04-SUMMARY.md`.
</output>
