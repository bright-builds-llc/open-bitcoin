---
phase: 62-long-run-sync-truth-surfaces
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/runtime/support.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [OBS-01]
user_setup: []
must_haves:
  truths:
    - "`open-bitcoin status` renders the Phase 62 fields in the UI-SPEC order with unavailable reasons preserved."
    - "`open-bitcoin dashboard` derives rows from the same shared status fields without re-inferring progress, recovery, pressure, stop reason, or peer health."
    - "`open-bitcoin sync status` expands beyond lifecycle/phase to show configured targets, attempt counters, progress signal, stop reason, error, recovery, resource pressure, peer health, heights, hashes, and bounded counters."
    - "RPC-facing durable warnings and sync-status response tests prove the same typed fields are available to RPC consumers."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      provides: "Human and JSON status rendering of Phase 62 truth fields"
      contains: "Sync configured targets"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Dashboard rows for Phase 62 truth fields"
      contains: "Configured targets"
    - path: "packages/open-bitcoin-cli/src/operator/runtime/support.rs"
      provides: "`open-bitcoin sync status` compact operator output"
      contains: "Latest stop reason"
    - path: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      provides: "RPC durable warning integration for stop/progress/recovery"
      contains: "latest_stop_reason="
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "OpenBitcoinStatusSnapshot.sync typed fields"
      pattern: "snapshot.sync.configured_targets"
    - from: "packages/open-bitcoin-cli/src/operator/runtime/support.rs"
      to: "openbitcoinsyncstatus metadata"
      via: "RuntimeMetadata.maybe_sync_state"
      pattern: "metadata.maybe_sync_state"
    - from: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      to: "getblockchaininfo warnings"
      via: "durable_warnings"
      pattern: "latest_stop_reason="
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-06T19-46-48
generated_at: 2026-06-06T20:29:59Z
---

<objective>
Render the shared Phase 62 sync truth fields across Rust operator and RPC surfaces.

Purpose: OBS-01, D-01, D-02, D-03, D-10, D-11, D-12, and the UI-SPEC require status, dashboard, sync status, and RPC consumers to agree on field order and meaning.
Output: Updated status/dashboard/sync-status/RPC renderers and focused regression tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/runtime/support.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs

<interfaces>
Fields created by Plan 01:
```rust
pub configured_targets: FieldAvailability<SyncConfiguredTargets>
pub attempt_counters: FieldAvailability<SyncAttemptCounters>
pub latest_stop_reason: FieldAvailability<SyncStopReasonStatus>
```

Required human sync field order from `62-UI-SPEC.md`:
```text
Sync lifecycle / State
Sync phase / Phase
Configured targets
Attempt counters
Progress signal / Signal
Last progress
Latest stop reason
Latest error / Last error
Recovery category
Recovery action / Recovery
Resource pressure / Pressure
Peer health / Peers
Header height
Downloaded block
Connected block
Bounded counters
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render Phase 62 fields in status and dashboard order</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` "Terminal Output Contract"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-01, D-02, D-03, D-10, and D-12
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <behavior>
    - Test: human status contains `Sync configured targets: outbound_peers=4 target_header_height=840200`.
    - Test: human status contains `Sync attempts: attempted_peers=3 connected_peers=2 failed_peers=1 max_sync_rounds=8`.
    - Test: human status contains `Sync latest stop reason: target_header_reached`.
    - Test: dashboard `Sync and Peers` rows contain `Configured targets`, `Attempt counters`, and `Latest stop reason` in the UI-SPEC order.
    - Test: unavailable fields render as `Unavailable: {reason}` and do not fall back to zero, empty string, or `ok`.
  </behavior>
  <action>
    In `status/render.rs`, add helpers for `SyncConfiguredTargets`, `SyncAttemptCounters`, and `SyncStopReasonStatus`. The helpers must read `snapshot.sync.configured_targets`, `snapshot.sync.attempt_counters`, and `snapshot.sync.latest_stop_reason` directly per D-01. Render unavailable values as `Unavailable: {reason}` per D-03.

    Reorder the human status sync lines to follow the UI-SPEC sequence. Keep existing config/network/chain lines before sync truth if they already exist, then render sync truth rows in this order: `Sync state`, `Sync phase`, `Sync configured targets`, `Sync attempts`, `Sync signal`, `Sync last progress`, `Sync latest stop reason`, `Sync error`, `Sync recovery category`, `Sync recovery`, `Sync pressure`, `Peers`, `Peer detail`, `Sync`. The final `Sync` row may keep the compact header/downloaded/connected/counter summary, but it must not invert downloaded and connected evidence.

    In `dashboard/model.rs`, update the `Sync and Peers` section rows in the same order using labels `State`, `Phase`, `Configured targets`, `Attempt counters`, `Signal`, `Last progress`, `Latest stop reason`, `Last error`, `Recovery category`, `Recovery`, `Pressure`, `Peers`, and `Progress`. Use typed helpers; do not parse status text.

    Update the existing shared sync truth fixtures in `status/render.rs` and `dashboard/model/tests.rs` with configured targets, attempt counters, and latest stop reason values from Plan 01. Preserve Phase 61 `invalid_peer_data` category and resource pressure fields per D-12.
  </action>
  <acceptance_criteria>
    - `rg -n "Sync configured targets|Sync attempts|Sync latest stop reason|snapshot\\.sync\\.configured_targets|snapshot\\.sync\\.attempt_counters|snapshot\\.sync\\.latest_stop_reason" packages/open-bitcoin-cli/src/operator/status/render.rs` finds direct status rendering.
    - `rg -n "\"Configured targets\"|\"Attempt counters\"|\"Latest stop reason\"|snapshot\\.sync\\.configured_targets|snapshot\\.sync\\.attempt_counters|snapshot\\.sync\\.latest_stop_reason" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs` finds dashboard rows and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features</automated>
  </verify>
  <done>Status and dashboard surfaces render the Phase 62 typed fields in the UI-SPEC order with explicit unavailable reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand sync status and RPC truth checks</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-01, D-02, D-03, D-10, and D-12
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md` "Known implementation gaps"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` field order and copy rules
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md`
    - `packages/open-bitcoin-cli/src/operator/runtime/support.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/node.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/runtime/support.rs, packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <behavior>
    - Test: `open-bitcoin sync status` human output includes `Configured targets:`, `Attempt counters:`, `Progress signal:`, `Last progress:`, `Latest stop reason:`, `Latest error:`, `Recovery category:`, `Recovery action:`, `Resource pressure:`, `Peer health:`, `Header height:`, `Downloaded block:`, `Connected block:`, and `Bounded counters:`.
    - Test: the same output renders unavailable values as `Unavailable: {reason}`.
    - Test: RPC durable warnings include `latest_stop_reason=target_header_reached`, `progress_signal=header_progress` or the fixture signal, and `recovery_category=invalid_peer_data` when the durable state has those typed values.
    - Test: RPC sync status response still returns `RuntimeMetadata` with the new `metadata.maybe_sync_state.sync.configured_targets`, `attempt_counters`, and `latest_stop_reason` fields.
  </behavior>
  <action>
    In `operator/runtime/support.rs`, replace the narrow sync status formatter that currently prints only pause/lifecycle/phase/last-clean-shutdown/update. Add compact line-oriented rows in this exact order after `Sync paused`: `Sync lifecycle`, `Sync phase`, `Configured targets`, `Attempt counters`, `Progress signal`, `Last progress`, `Latest stop reason`, `Latest error`, `Recovery category`, `Recovery action`, `Resource pressure`, `Peer health`, `Header height`, `Downloaded block`, `Connected block`, `Bounded counters`, `Last clean shutdown`, `Last update`.

    Each row must read from `metadata.maybe_sync_state.sync` or `metadata.maybe_sync_state.peers`; do not parse previous human status text. Use `Unavailable: {reason}` for unavailable fields. For configured targets, print `outbound_peers={target_outbound_peers} target_header_height={value}` when present and `target_header_height=Unavailable: no target header configured` when the optional header target is `None`. For downloaded and connected block rows, print both height and hash, using `hash=Unavailable: no downloaded block hash recorded` or `hash=Unavailable: no connected block hash recorded` when the optional hash is `None`.

    In `dispatch/node.rs`, extend `durable_warnings` with compact labels derived from typed status fields: `progress_signal={label}`, `latest_stop_reason={label}`, and `recovery_category={label}`. Keep latest error first and recovery action after category. Do not add endpoint tables, raw peer arrays, credential paths, or public-network calls.

    Update RPC tests so durable sync fixtures set configured targets, attempt counters, latest stop reason, progress signal, recovery category, recovery action, resource pressure, peer counts, and progress counters. Add assertions for the warning labels and the `OpenBitcoinSyncControlResponse.metadata` JSON field names.
  </action>
  <acceptance_criteria>
    - `rg -n "Configured targets:|Attempt counters:|Progress signal:|Latest stop reason:|Resource pressure:|Bounded counters:" packages/open-bitcoin-cli/src/operator/runtime/support.rs` finds the expanded sync status output.
    - `rg -n "latest_stop_reason=|progress_signal=|recovery_category=" packages/open-bitcoin-rpc/src/dispatch/node.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds RPC warning labels and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli sync_status --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_sync_status --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli sync_status --all-features</automated>
  </verify>
  <done>`open-bitcoin sync status` and RPC consumers expose the same typed Phase 62 fields as status/dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared status -> terminal output | Typed sync truth becomes human-readable operator output. |
| durable status -> RPC response/warnings | Local RPC clients receive compact sync diagnosis labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-01 | Tampering | status/dashboard/sync-status renderers | mitigate | Render typed fields directly from `OpenBitcoinStatusSnapshot` or `RuntimeMetadata`; add tests for exact labels and unavailable reasons. |
| T-62-02-02 | Repudiation | RPC durable warnings | mitigate | Include stable `progress_signal=`, `latest_stop_reason=`, and `recovery_category=` labels in deterministic RPC tests. |
| T-62-02-03 | Information Disclosure | operator/RPC output | mitigate | Add only compact labels, counts, heights, hashes already in status, and unavailable reasons; do not expose credentials, raw logs, or unbounded peer tables. |
</threat_model>

<verification>
Run focused Rust surface checks:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli sync_status --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_sync_status --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_blockchain_info --all-features
```
</verification>

<success_criteria>
- Status, dashboard, sync status, and RPC consumers agree on lifecycle, phase, configured targets, attempts, progress signal, stop reason, latest error, recovery category/action, resource pressure, peer health, heights/hashes, and counters.
- Human output follows the UI-SPEC order and copy rules.
- Phase 61 recovery labels and resource-pressure fields are preserved without renaming.
</success_criteria>

<output>
After completion, create `.planning/phases/62-long-run-sync-truth-surfaces/62-02-SUMMARY.md`.
</output>
