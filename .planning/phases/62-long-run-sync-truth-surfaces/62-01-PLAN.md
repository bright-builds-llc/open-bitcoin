---
phase: 62-long-run-sync-truth-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/sync_state.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/runtime/support.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [OBS-01, OBS-02]
user_setup: []
must_haves:
  truths:
    - "The shared `SyncStatus` contract carries configured targets, attempt counters, and latest stop reason as typed fields with explicit unavailable reasons."
    - "Durable sync status projects configured target outbound peers, optional target header height, peer attempt counters, stop reason, progress signal, recovery category/action, resource pressure, peer health, and block/header counters from typed runtime state."
    - "Structured sync logs include compact machine-stable cycle facts that can be compared to status without growing unbounded retained arrays."
    - "Metrics remain bounded numeric series for header height, downloaded block height, connected block height, sync height, and peer count."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "Phase 62 shared sync truth contract fields"
      contains: "SyncConfiguredTargets"
    - path: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      provides: "Summary-to-status, metrics, and compact structured-log projection"
      contains: "latest_stop_reason"
    - path: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      provides: "Durable runtime configured target and stop-reason projection"
      contains: "maybe_target_header_height"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Deterministic runtime truth-projection regression coverage"
      contains: "phase62"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "DurableSyncRuntime builds SyncConfiguredTargets, SyncAttemptCounters, and SyncStopReasonStatus"
      pattern: "configured_targets"
    - from: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      to: "packages/open-bitcoin-node/src/metrics.rs"
      via: "SyncRunSummary::metric_samples keeps numeric bounded samples"
      pattern: "MetricKind::HeaderHeight"
    - from: "packages/open-bitcoin-node/src/sync/types/summary.rs"
      to: "structured sync logs"
      via: "SyncRunSummary::structured_log_records emits compact labels"
      pattern: "stop_reason="
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-06T19-46-48
generated_at: 2026-06-06T20:29:59Z
---

<objective>
Create the canonical Phase 62 sync truth contract in Rust and project it from durable sync state.

Purpose: OBS-01, OBS-02, D-01, D-02, D-03, D-04, D-05, D-06, and D-12 require one typed source of truth before renderer, RPC, live-smoke, and docs work can agree.
Output: Additive status fields, durable projection, compact structured-log facts, and deterministic Rust coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-VERIFICATION.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/sync/types.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/metrics.rs

<interfaces>
Existing availability wrapper:
```rust
#[serde(rename_all = "snake_case", tag = "state", content = "value")]
pub enum FieldAvailability<T> {
    Available(T),
    Unavailable { reason: String },
}
```

Add these status contract types in `packages/open-bitcoin-node/src/status.rs`; do not create a new Rust file for this plan:
```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SyncConfiguredTargets {
    pub target_outbound_peers: u32,
    pub maybe_target_header_height: Option<u64>,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SyncAttemptCounters {
    pub attempted_peers: u64,
    pub connected_peers: u64,
    pub failed_peers: u64,
    pub max_sync_rounds: u64,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SyncStopReasonStatus {
    pub label: String,
    pub message: String,
}
```

Add these fields to `SyncStatus` in this order:
```rust
pub lifecycle: FieldAvailability<SyncLifecycleState>,
pub phase: FieldAvailability<String>,
pub configured_targets: FieldAvailability<SyncConfiguredTargets>,
pub attempt_counters: FieldAvailability<SyncAttemptCounters>,
pub progress_signal: FieldAvailability<SyncProgressSignal>,
pub lag: FieldAvailability<SyncLagStatus>,
pub last_successful_progress_unix_seconds: FieldAvailability<u64>,
pub latest_stop_reason: FieldAvailability<SyncStopReasonStatus>,
pub last_error: FieldAvailability<String>,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 62 fields to the shared status contract</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-01, D-02, D-03, and D-12
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md` "Primary recommendation" and "Known implementation gaps"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` "Terminal Output Contract"
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/types.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
  </read_first>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/sync/types/summary.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-cli/src/operator/status/sync_state.rs, packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs, packages/open-bitcoin-cli/src/operator/runtime/support.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <behavior>
    - Test: serialized `SyncStatus` JSON contains `configured_targets`, `attempt_counters`, and `latest_stop_reason`.
    - Test: unavailable configured targets, attempt counters, and latest stop reason serialize through `FieldAvailability::Unavailable { reason }`, never as zero or empty strings.
    - Test: older persisted status JSON without the new fields deserializes with unavailable reasons, matching D-03.
    - Test: every current `SyncStatus {` constructor compiles with explicit values for the new fields.
  </behavior>
  <action>
    In `packages/open-bitcoin-node/src/status.rs`, add `SyncConfiguredTargets`, `SyncAttemptCounters`, and `SyncStopReasonStatus` exactly as shown in the interface block. Add `configured_targets`, `attempt_counters`, and `latest_stop_reason` to `SyncStatus` in the order shown in the interface block per D-02 and the UI-SPEC field order.

    Add serde default functions beside `no_recovery_category_recorded()`:
    `configured_targets_unavailable()`, `attempt_counters_unavailable()`, and `latest_stop_reason_unavailable()`. Use unavailable reasons exactly:
    - `configured targets unavailable`
    - `attempt counters unavailable`
    - `latest stop reason unavailable`
    Apply these defaults to the three new `SyncStatus` fields so older persisted `RuntimeMetadata` remains readable per D-03.

    In `SyncRunSummary::sync_status`, set `configured_targets` to `SyncConfiguredTargets { target_outbound_peers: self.target_outbound_peers as u32, maybe_target_header_height: None }`, set `attempt_counters` to `SyncAttemptCounters { attempted_peers: self.attempted_peers as u64, connected_peers: self.connected_peers as u64, failed_peers: self.failed_peers as u64, max_sync_rounds: 0 }`, and set `latest_stop_reason` from `self.maybe_stop_reason` as `{ label: stop_reason.label().to_string(), message: stop_reason.message() }` or unavailable `no stop reason recorded`.

    In `DurableSyncRuntime::durable_sync_state`, override `sync.configured_targets` with `target_outbound_peers: self.config.target_outbound_peers as u32` and `maybe_target_header_height: self.config.maybe_target_header_height`. Override `sync.attempt_counters.max_sync_rounds` with `self.config.max_rounds as u64` while preserving the summary attempted/connected/failed counts. Preserve Phase 61 `recovery_category`, `recovery_action`, and `resource_pressure` fields without renaming them per D-12.

    Update every `SyncStatus {` constructor found by `rg -n "SyncStatus \\{" packages/open-bitcoin-* -g '*.rs'` with explicit new-field values. Use available values in shared truth fixtures and unavailable values in fallback/RPC-only constructors. Do not create renderer-local guesses for configured targets, attempts, or stop reason.
  </action>
  <acceptance_criteria>
    - `rg -n "pub struct SyncConfiguredTargets|pub struct SyncAttemptCounters|pub struct SyncStopReasonStatus|configured_targets: FieldAvailability<SyncConfiguredTargets>|attempt_counters: FieldAvailability<SyncAttemptCounters>|latest_stop_reason: FieldAvailability<SyncStopReasonStatus>" packages/open-bitcoin-node/src/status.rs` finds all contract fields.
    - `rg -n "maybe_target_header_height|target_outbound_peers|attempted_peers|connected_peers|failed_peers|max_sync_rounds|latest_stop_reason" packages/open-bitcoin-node/src/sync/types/summary.rs packages/open-bitcoin-node/src/sync/runtime_state.rs` finds the projection paths.
    - `cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features` passes after all constructor updates.
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features</automated>
  </verify>
  <done>All Rust crates compile with the additive Phase 62 `SyncStatus` contract, and missing fields remain explicitly unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project compact cycle facts into status, metrics, and structured logs</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-04, D-05, and D-06
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-node/src/metrics.rs`
    - `.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-VERIFICATION.md` truths 11 through 13
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/types/summary.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs, packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Test: `SyncRunSummary::structured_log_records()` emits one compact summary record containing `target_outbound_peers=`, `target_header_height=`, `attempted_peers=`, `connected_peers=`, `failed_peers=`, `messages_processed=`, `headers_received=`, `blocks_received=`, `header=`, `downloaded=`, `connected=`, `signal=`, `last_progress=`, `stop_reason=`, and `recovery_category=`.
    - Test: no structured summary record exceeds the existing bounded message-length cap used by Phase 61; if the code truncates records, the stable labels above must still be present in the deterministic test fixture.
    - Test: `SyncRunSummary::metric_samples()` remains numeric-only and still returns exactly `HeaderHeight`, `DownloadedBlockHeight`, `ConnectedBlockHeight`, `SyncHeight`, and `PeerCount`.
    - Test: deterministic runtime status with `maybe_target_header_height` set exposes the same configured target and latest stop reason through durable status.
  </behavior>
  <action>
    Extend the compact sync summary structured-log record in `SyncRunSummary::structured_log_records()` to include the Phase 62 cycle fields listed in the behavior block. Use `unavailable` for `target_header_height`, `stop_reason`, and `last_progress` when no typed value exists. Keep the existing separate stop-reason log record only if current tests depend on it, but the primary summary record must contain `stop_reason={label_or_unavailable}` per D-06.

    Do not add string labels, peer tables, endpoint arrays, or status snapshots to `MetricSample`. Keep `metric_samples()` numeric-only with the existing five sync series per D-04 and D-05: header height, downloaded block height, connected block height, sync height, and peer count.

    Add or update deterministic Rust tests. Use names containing `phase62_sync_truth_contract` and `phase62_structured_logs_keep_bounded_cycle_facts` so focused acceptance commands execute real assertions. Tests must use Arrange/Act/Assert comments when setup is non-trivial.
  </action>
  <acceptance_criteria>
    - `rg -n "target_outbound_peers=|target_header_height=|attempted_peers=|connected_peers=|failed_peers=|stop_reason=|recovery_category=" packages/open-bitcoin-node/src/sync/types/summary.rs packages/open-bitcoin-node/src/sync/tests.rs` finds structured-log fields and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase62_sync_truth_contract --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase62_structured_logs_keep_bounded_cycle_facts --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_summary_projects_metric_samples --all-features` passes and still expects the five numeric sync metric samples only.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase62_sync_truth_contract --all-features</automated>
  </verify>
  <done>Shared status, compact structured logs, and numeric metrics expose the Phase 62 truth contract without unbounded retained evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable runtime -> shared status | Runtime config, peer outcomes, stop reasons, and storage metadata become operator-visible status facts. |
| shared status -> structured logs/metrics | Status truth is projected into retained observability stores with bounded retention. |
| older metadata -> new status contract | Older persisted `RuntimeMetadata` may omit Phase 62 fields and must deserialize safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-01 | Tampering | `SyncStatus` contract | mitigate | Add typed fields once in `status.rs`, require consumers to read those fields, and add serde defaults with explicit unavailable reasons. |
| T-62-01-02 | Repudiation | structured sync logs | mitigate | Emit compact stable labels for targets, attempts, counters, progress signal, stop reason, and recovery category so logs can be compared to status. |
| T-62-01-03 | Denial of Service | metrics/log retention | mitigate | Keep metrics numeric-only and bounded by existing retention; do not add retained status arrays, peer tables, or raw report history. |
| T-62-01-04 | Information Disclosure | status/log fields | mitigate | Expose only compact labels, counters, hashes already present in status, and unavailable reasons; do not add credentials, raw daemon tails, or endpoint arrays. |
</threat_model>

<verification>
Run focused Rust checks:

```bash
cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase62_sync_truth_contract --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase62_structured_logs_keep_bounded_cycle_facts --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_summary_projects_metric_samples --all-features
```
</verification>

<success_criteria>
- `SyncStatus` carries the complete Phase 62 shared truth contract for configured targets, attempts, stop reason, progress, errors, recovery, pressure, peers, heights, hashes, and counters.
- Durable status uses typed runtime/config/summary facts instead of renderer-local parsing.
- Metrics remain bounded numeric samples; structured logs carry compact machine-stable cycle labels.
- Phase 61 recovery labels and resource-pressure fields remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md`.
</output>
