---
phase: 62-long-run-sync-truth-surfaces
plan: 03
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
autonomous: true
requirements: [OBS-01, OBS-02]
user_setup: []
must_haves:
  truths:
    - "Live-smoke JSON snapshots use the same Phase 62 field names and semantics as shared status, with one TypeScript mapping layer for camelCase report fields."
    - "Live-smoke Markdown snapshot rows include progress signal, configured targets, recovery category/action, resource pressure, latest stop reason, latest error, heights, hashes, and peer attempt evidence."
    - "Live-smoke reports remain compact: no raw daemon tails, full endpoint tables beyond existing bounded preflight rows, or unbounded snapshot history are added."
    - "Deterministic fixture checks validate generated JSON and Markdown without public-network access."
  artifacts:
    - path: "scripts/run-live-mainnet-smoke.ts"
      provides: "Live-smoke Phase 62 snapshot mapping and Markdown rendering"
      contains: "configuredTargets"
    - path: "scripts/test-run-live-mainnet-smoke.sh"
      provides: "Deterministic live-smoke fixture coverage"
      contains: "progressSignal"
  key_links:
    - from: "scripts/run-live-mainnet-smoke.ts"
      to: "metadata.maybe_sync_state.sync"
      via: "finalStatusSummaryFromMetadata and sync status snapshot parsing"
      pattern: "configuredTargetsFromValue"
    - from: "scripts/test-run-live-mainnet-smoke.sh"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "local mock status JSON fixtures"
      pattern: "Latest Stop Reason"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-06T19-46-48
generated_at: 2026-06-06T20:29:59Z
---

<objective>
Update opt-in live-smoke JSON and Markdown snapshots to consume the Phase 62 truth contract compactly.

Purpose: OBS-01, OBS-02, D-01, D-02, D-03, D-07, D-08, D-09, D-10, and D-12 require live-smoke reports to agree with status while staying deterministic by default and compact for operator review.
Output: One TypeScript mapping layer, richer bounded snapshots, raw-tail removal from persisted reports, and fixture verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh

<interfaces>
Snake_case Rust status fields mapped to camelCase live-smoke report fields:
```text
sync.configured_targets -> configuredTargets
sync.attempt_counters -> attemptCounters
sync.progress_signal -> progressSignal
sync.latest_stop_reason -> latestStopReason
sync.recovery_category -> recoveryCategory
sync.recovery_action -> recoveryAction
sync.resource_pressure -> resourcePressure
sync.sync_progress.header_height -> headerHeight
sync.sync_progress.downloaded_block_height -> downloadedBlockHeight
sync.sync_progress.maybe_downloaded_block_hash -> maybeDownloadedBlockHash
sync.sync_progress.connected_block_height -> connectedBlockHeight
sync.sync_progress.maybe_connected_block_hash -> maybeConnectedBlockHash
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the live-smoke TypeScript truth mapping</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-01, D-02, D-03, D-07, D-08, D-09, and D-12
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` "Live-Smoke Snapshot Contract"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md`
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - Test: JSON `result.firstHeaderProgress.before.progressSignal` and `report.snapshots[0].progressSignal` are populated from `sync.progress_signal`.
    - Test: JSON `report.final_status.configuredTargets.targetOutboundPeers` and `maybeTargetHeaderHeight` are populated from `sync.configured_targets`.
    - Test: JSON `report.final_status.attemptCounters.attemptedPeers`, `connectedPeers`, `failedPeers`, and `maxSyncRounds` are populated from `sync.attempt_counters`.
    - Test: JSON `latestStopReason.label`, `recoveryCategory`, `recoveryAction`, and `resourcePressure.targetOutboundPeers` are populated from typed status fields.
    - Test: unavailable fields preserve explicit reason fields such as `maybeLatestStopReasonUnavailableReason` instead of silently becoming zero or empty strings.
  </behavior>
  <action>
    In `scripts/run-live-mainnet-smoke.ts`, extend `RuntimeMetadataJson` status shapes and the live-smoke report types with these compact summaries: `ConfiguredTargetsSummary`, `AttemptCountersSummary`, and `StopReasonSummary`. Add helper functions named exactly `configuredTargetsFromValue`, `attemptCountersFromValue`, `stopReasonFromValue`, and `unavailableReasonFromFieldAvailability`.

    Extend `SyncStatusSnapshot`, `RestartProgressSummary`, and `FinalStatusSummary` with `configuredTargets`, `attemptCounters`, `progressSignal`, `latestStopReason`, `maybeLatestStopReasonUnavailableReason`, `recoveryCategory`, `recoveryAction`, `maybeRecoveryActionUnavailableReason`, `resourcePressure`, and `maybeResourcePressureUnavailableReason` as applicable. Use camelCase only in this single mapping layer per D-08.

    Update `syncStatusSnapshotFromMetadata` and `finalStatusSummaryFromMetadata` so all fields read from `metadata.maybe_sync_state.sync` typed fields. Do not parse human CLI output, structured-log text, or Markdown. Preserve Phase 61 recovery labels exactly and reject unknown recovery categories by returning `null` plus the unavailable reason when present.

    Update the deterministic shell fixtures in `scripts/test-run-live-mainnet-smoke.sh` so mocked status JSON includes `configured_targets`, `attempt_counters`, `progress_signal`, `latest_stop_reason`, `recovery_category`, `recovery_action`, and `resource_pressure` in the same shape as `FieldAvailability`.
  </action>
  <acceptance_criteria>
    - `rg -n "ConfiguredTargetsSummary|AttemptCountersSummary|StopReasonSummary|configuredTargetsFromValue|attemptCountersFromValue|stopReasonFromValue|unavailableReasonFromFieldAvailability|progressSignal|latestStopReason" scripts/run-live-mainnet-smoke.ts` finds the single mapping layer and fields.
    - `rg -n "configured_targets|attempt_counters|latest_stop_reason|progress_signal|recovery_action|resource_pressure" scripts/test-run-live-mainnet-smoke.sh` finds deterministic fixtures for every field.
    - `bash scripts/test-run-live-mainnet-smoke.sh` passes without public-network access.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <done>Live-smoke JSON maps all Phase 62 status fields through one deterministic typed mapping layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render compact Phase 62 live-smoke Markdown snapshots</name>
  <read_first>
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-CONTEXT.md` decisions D-05, D-07, D-08, and D-09
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-UI-SPEC.md` "Live-Smoke Snapshot Contract"
    - `scripts/run-live-mainnet-smoke.ts`
    - `scripts/test-run-live-mainnet-smoke.sh`
  </read_first>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh</files>
  <behavior>
    - Test: Markdown `## Snapshots` table header includes `Signal`, `Configured Targets`, `Attempts`, `Recovery Category`, `Resource Pressure`, `Latest Stop Reason`, and `Latest Error`.
    - Test: Markdown `## Final Durable Status` includes lifecycle, phase, configured targets, attempt counters, progress signal, last progress, latest stop reason, latest error, recovery category, recovery action, resource pressure, peer health, header height, downloaded block height/hash, connected block height/hash, and bounded counters.
    - Test: when no snapshots exist, Markdown renders one row containing `Unavailable: no sync status snapshots captured`.
    - Test: generated JSON and Markdown no longer persist raw `stdoutTail` or `stderrTail` daemon tails.
  </behavior>
  <action>
    Update `markdownReport()` snapshot rows to follow the UI-SPEC minimum live-smoke table fields: captured timestamp, lifecycle, phase, progress signal, header height, downloaded block height/hash, connected block height/hash, outbound peers versus configured target, recovery category, resource pressure summary, latest stop reason, and latest error. Include attempt counters in the same row as `attempted/connected/failed/max_rounds`.

    Update the no-snapshot case to render one explicit row with `Unavailable: no sync status snapshots captured`; do not render a success-looking zero row.

    Update `## Final Durable Status` to list the same Phase 62 fields in compact bullet form. Use `Unavailable: {reason}` when the mapping carried an unavailable reason, and `Unavailable: no target header configured` when the optional header target is legitimately absent.

    Replace persisted raw daemon output fields with compact counts. Remove `stdoutTail` and `stderrTail` from the JSON report type and Markdown `Daemon Output Tail` fenced blocks. Preserve compact diagnosis fields: exit code, signal, stdout line count, stderr line count, and whether stderr/stdout were observed. This satisfies D-07 and does not expand support-bundle behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "Signal \\| Configured Targets \\| Attempts|Latest Stop Reason|Unavailable: no sync status snapshots captured|Final Durable Status|Daemon Output Summary" scripts/run-live-mainnet-smoke.ts` finds Markdown contract changes.
    - `bash scripts/test-run-live-mainnet-smoke.sh` passes.
    - `bash -c 'bash scripts/test-run-live-mainnet-smoke.sh >/tmp/phase62-live-smoke-test.log 2>&1; ! rg -n "stdoutTail|stderrTail|Daemon Output Tail" packages/target/live-mainnet-smoke-reports/open-bitcoin-live-mainnet-smoke.json packages/target/live-mainnet-smoke-reports/open-bitcoin-live-mainnet-smoke.md'` passes after the fixture run writes reports.
    - `bun --eval 'const report = await Bun.file("packages/target/live-mainnet-smoke-reports/open-bitcoin-live-mainnet-smoke.json").json(); if (!report.final_status?.configuredTargets || !report.snapshots?.[0]?.progressSignal) throw new Error("Phase 62 live-smoke truth fields missing");'` passes after the fixture run.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
  </verify>
  <done>Live-smoke Markdown and JSON preserve Phase 62 diagnosis evidence compactly without public-network default checks or raw daemon tails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon status JSON -> TypeScript report | Typed Rust status crosses into live-smoke JSON/Markdown reports. |
| live-smoke local artifacts -> operator review | Generated local reports may be shared for diagnosis. |
| deterministic fixtures -> default verification | Default verification must exercise fixtures only, not the public network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-01 | Tampering | TypeScript status mapping | mitigate | Use one mapping layer from typed status fields and deterministic fixture checks for every Phase 62 field. |
| T-62-03-02 | Information Disclosure | live-smoke JSON/Markdown | mitigate | Remove persisted raw daemon tails and keep compact summaries, unavailable reasons, and bounded snapshot rows. |
| T-62-03-03 | Denial of Service | live-smoke snapshots | mitigate | Do not add unbounded snapshot/report history; keep existing bounded snapshot collection and compact rows. |
| T-62-03-04 | Repudiation | public-network evidence boundary | mitigate | Verification uses `scripts/test-run-live-mainnet-smoke.sh` fixtures only; no public-network command is added to `scripts/verify.sh`. |
</threat_model>

<verification>
Run deterministic live-smoke fixture checks:

```bash
bash scripts/test-run-live-mainnet-smoke.sh
bash -c 'bash scripts/test-run-live-mainnet-smoke.sh >/tmp/phase62-live-smoke-test.log 2>&1; ! rg -n "stdoutTail|stderrTail|Daemon Output Tail" packages/target/live-mainnet-smoke-reports/open-bitcoin-live-mainnet-smoke.json packages/target/live-mainnet-smoke-reports/open-bitcoin-live-mainnet-smoke.md'
```
</verification>

<success_criteria>
- Live-smoke JSON and Markdown snapshots expose the same Phase 62 facts as status.
- TypeScript report casing differences are isolated to one mapping layer.
- Missing fields carry explicit unavailable reasons.
- Reports remain compact and fixture-verified without public-network default verification.
</success_criteria>

<output>
After completion, create `.planning/phases/62-long-run-sync-truth-surfaces/62-03-SUMMARY.md`.
</output>
