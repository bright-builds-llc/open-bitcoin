---
phase: 128-production-compact-announcement-transport
plan: "03"
type: execute
wave: 3
depends_on:
  - "128-01"
  - "128-02"
files_modified:
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/block_response.rs
  - packages/open-bitcoin-node/src/sync/block_reconcile.rs
  - packages/open-bitcoin-node/src/sync/session.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - CMP-04
  - CMP-05
  - OBS-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 128-2026-07-20T01-54-33
generated_at: 2026-07-20T02:30:00Z
must_haves:
  truths:
    - "A newly validated, durably available best tip triggers announcement planning exactly once; historical IBD, side branches, and failed persistence do not."
    - "Every selected compact/header/inventory announcement reaches the real session that owns its peer without an authority lock crossing storage or transport."
    - "Successful prefixes receive exactly-once receipts before a later batch failure, and metrics/logs sample the authoritative post-write snapshot."
  artifacts:
    - path: packages/open-bitcoin-node/src/sync/block_response.rs
      provides: "Typed post-durable best-tip announcement event"
    - path: packages/open-bitcoin-node/src/sync/session.rs
      provides: "Outbound transport drain and per-message receipt completion"
    - path: packages/open-bitcoin-rpc/src/inbound_listener.rs
      provides: "Inbound session drain and post-write receipt completion"
    - path: packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs
      provides: "End-to-end success, failure, fanout, and evidence regressions"
  key_links:
    - from: packages/open-bitcoin-node/src/sync/block_response.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "post-persistence best-tip event prepares peer emissions"
      pattern: "tip.*advance|prepare.*announcement"
    - from: packages/open-bitcoin-node/src/sync/session.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "successful write consumes matching receipt"
      pattern: "complete.*emission|receipt"
    - from: packages/open-bitcoin-rpc/src/inbound_listener.rs
      to: packages/open-bitcoin-node/src/network/announcement_transport.rs
      via: "owning inbound peer session drains bounded emissions"
      pattern: "PeerEmission|announcement"
---

<objective>
Wire post-durable block activation through real outbound and inbound peer transports.

Purpose: Close the production caller and transport gaps while preserving shared authority, bounded queues, fallback policy, and post-write evidence.

Output: Typed durable-tip trigger, session-owned emission drains, partial-batch acknowledgement, authoritative metric/log sampling, and end-to-end regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/128-production-compact-announcement-transport/128-CONTEXT.md
@.planning/phases/128-production-compact-announcement-transport/128-01-SUMMARY.md
@.planning/phases/128-production-compact-announcement-transport/128-02-SUMMARY.md

<interfaces>
`record_block_disposition`/block response persistence owns the direct validated-block durable boundary.
`block_reconcile.rs` owns multi-block active-chain reconciliation.
`DurableSyncRuntime::send_all` already acknowledges successful full-block write prefixes.
`WriteWireMessageOutcome::Written` is the inbound peer transport success boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit one post-durable best-tip announcement event</name>
  <files>packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/block_response.rs, packages/open-bitcoin-node/src/sync/block_reconcile.rs</files>
  <read-first>
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/block_response.rs
    packages/open-bitcoin-node/src/sync/block_reconcile.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/storage/fjall_store.rs
    packages/bitcoin-knots/src/validationinterface.cpp
    packages/bitcoin-knots/src/net_processing.cpp
  </read-first>
  <behavior>
    - A direct newly connected best-chain block emits after successful durable persistence.
    - Historical IBD connects, duplicate dispositions, side-branch blocks, invalid blocks, and failed persistence emit nothing.
    - Reconciliation emits only for the final newly activated durable tip.
    - Block loading/persistence and emission preparation never occur while the authority guard is retained across effects.
  </behavior>
  <action>
This task implements locked decisions D-05 and D-06.
Introduce a typed `DurableTipAdvanced` (or equivalently explicit) event carrying the final active block hash/body needed for announcement. Produce it only after validation, best-chain activation, and successful durable availability are true. Thread direct block response and reconciliation results through `DurableSyncRuntime`; for a multi-block reconciliation, collapse to the final newly active tip and suppress historical catch-up/side-branch/duplicate cases. Outside storage effects, ask the shared authority to prepare bounded emissions from live peer facts. Do not announce on receipt, pre-validation intent, cache presence alone, or every historical IBD connection.
  </action>
  <threat-model>
Mitigate false announcements and TOCTOU by requiring post-persistence typed state; mitigate amplification by deduplicating and collapsing reconciliation to one final tip; fail closed on validation/storage errors.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-node-durable-tip-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node durable_tip</automated>
  </verify>
  <acceptance-criteria>
    Production block response has a caller from successful durable best-tip activation to announcement preparation.
    Historical, duplicate, side-branch, invalid, and persistence-failure tests emit zero announcements.
    Reconciliation tests emit only the final newly active durable tip.
    Focused durable-tip tests exit 0.
  </acceptance-criteria>
  <done>Validated durable best-tip changes are the sole production announcement trigger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drain peer emissions through real session transports and complete receipts</name>
  <files>packages/open-bitcoin-node/src/sync/session.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</files>
  <read-first>
    packages/open-bitcoin-node/src/sync/session.rs
    packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    packages/open-bitcoin-node/src/network/announcement_transport.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/bitcoin-knots/src/net.cpp
  </read-first>
  <behavior>
    - Each live outbound or inbound session drains only emissions targeted to its PeerId.
    - Socket writes occur without the authority lock, and each successful write immediately consumes its receipt.
    - On a partial batch failure, successful prefix receipts commit once and the failed/unsent suffix commits none.
    - Disconnect and queue pressure clean up bounded volatile emissions without deleting chainstate or block data.
  </behavior>
  <action>
This task implements locked decisions D-07, D-08, D-09, D-10, D-11, D-12, and D-13.
Wire a bounded session registry/outbox into the existing production composition so the session owning each PeerId can drain `PeerEmission` values. Extend outbound `send_all` and inbound `WriteWireMessageOutcome::Written` paths to preserve the typed carrier until write success, then consume the receipt through `NetworkRuntimeAuthority`. Commit each successful prefix before returning a later error; never retry or credit the failed/unsent suffix implicitly. Add wakeup/cleanup behavior using the smallest existing session mechanism, with fixed caps and typed pressure/disconnect outcomes. Do not hold the network authority across socket writes, Fjall operations, `.await`, encoding, logging, metrics, or shutdown.
  </action>
  <threat-model>
Mitigate queue exhaustion and peer churn with bounded ownership/cleanup; mitigate deadlock by effect-free lock scope; mitigate replay/double-credit with consuming receipts and successful-prefix ordering.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-node-session-transport-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node announcement_transport</automated>
    <automated>bun run scripts/command-timings.ts run --key phase128-rpc-inbound-transport-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc announcement_transport</automated>
  </verify>
  <acceptance-criteria>
    Real outbound and inbound session write paths accept peer-targeted compact/header/inventory emissions.
    Every successful write consumes exactly one matching receipt outside the authority lock.
    Partial failure tests show credit for the successful prefix only.
    Queue-full and disconnect cleanup are bounded and volatile-only.
    Both command-timed transport test commands exit 0.
  </acceptance-criteria>
  <done>Selected announcements traverse their owning real peer transport and acknowledge achieved writes exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove end-to-end negotiation, fanout, fallback, and observability</name>
  <files>packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    packages/open-bitcoin-node/src/sync/tests.rs
    packages/open-bitcoin-node/src/sync/tests/runtime_projection_cases.rs
    packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs
    packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs
    packages/open-bitcoin-node/src/metrics/block_relay.rs
    packages/open-bitcoin-node/src/logging.rs
    packages/bitcoin-knots/test/functional/p2p_compactblocks.py
  </read-first>
  <behavior>
    - Enabled post-Verack high-bandwidth peer receives CompactBlock after a durable tip; low-bandwidth/ineligible peers receive the typed header/inventory fallback or suppression.
    - Tests use distinct live peer header facts and fail if constants replace them.
    - Failed compact writes leave compact-announced metrics/log/provenance unchanged; successful writes update fixed aggregates.
    - No raw peer endpoint, peer ID, block hash, permission, credential, or transaction payload reaches metric/log output.
  </behavior>
  <action>
This task implements locked decisions D-14 and D-15.
Create a focused end-to-end test module and register it from `sync/tests.rs`. Add the new test file's required pinned-Knots source breadcrumb block and `docs/parity/source-breadcrumbs.json` registration in this same atomic task. Exercise production-shaped handshake actions, remote high/low negotiation, authoritative durable tip activation, multiple live peers with different header ancestry/resource pressure, actual session writes, compact/header/inventory selection, write failure, partial batch success, disconnect cleanup, and authoritative metric/log snapshots. Assert exact stable outcomes and redaction. Keep fixtures deterministic, loopback/synthetic, caller-clocked, and public-network-free; do not use sleeps or service managers.
  </action>
  <threat-model>
Mitigate false-positive integration claims with production-shaped multi-peer tests; mitigate data disclosure with negative field assertions; mitigate resource bypass with pressure and disconnect cases.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-end-to-end-transport-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node production_announcement_transport_cases</automated>
  </verify>
  <acceptance-criteria>
    The end-to-end suite covers all four Phase 128 ROADMAP success criteria.
    A mutation from live peer header facts to constants causes the suite to fail.
    Failed writes produce no compact-announced metric/log/provenance credit.
    Output redaction assertions cover peer/block/permission/credential/transaction data.
    The command-timed end-to-end suite exits 0.
  </acceptance-criteria>
  <done>Production-shaped tests prove bilateral negotiation through post-write authoritative evidence.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| validation/persistence to announcement trigger | Pre-durable or duplicate announcement | Typed post-durable final-tip event |
| shared authority to concurrent sessions | Lock inversion, cross-peer delivery, queue exhaustion | Owned targeted values, short locks, bounded per-peer outboxes |
| socket write to observability | False credit or partial-batch drift | Immediate consuming receipt for each successful prefix |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-128-03-01 | Spoofing | mitigate | Bind emissions to the owning PeerId/session. |
| T-128-03-02 | Tampering | mitigate | Trigger only from validated durable active-tip state. |
| T-128-03-03 | Denial of Service | mitigate | Bound queues, clean on disconnect, preserve resource policy. |
| T-128-03-04 | Information Disclosure | mitigate | Test fixed aggregates and negative sensitive fields. |
</threat-model>

<verification>
Run all three focused command-timed suites, inspect lock scope and successful-prefix ordering, and confirm production composition uses the Phase 127 authority rather than a new network.
</verification>

<success-criteria>
A durable best-tip event fans out policy-selected announcements through real owning sessions, and only successful writes change authoritative compact-relay evidence, metrics, logs, or provenance.
</success-criteria>

<output>
After completion, create `.planning/phases/128-production-compact-announcement-transport/128-03-SUMMARY.md`.
</output>
