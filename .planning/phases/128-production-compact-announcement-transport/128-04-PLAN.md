---
phase: 128-production-compact-announcement-transport
plan: "04"
type: execute
wave: 4
depends_on:
  - "128-03"
files_modified:
  - scripts/check-phase128-production-compact-announcement-transport.ts
  - scripts/check-phase128-production-compact-announcement-transport.test.ts
  - scripts/verify.sh
  - docs/parity/source-breadcrumbs.json
  - docs/parity/index.json
  - docs/parity/catalog/p2p.md
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - CMP-04
  - CMP-05
  - OBS-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 128-2026-07-20T01-54-33
generated_at: 2026-07-20T02:30:00Z
must_haves:
  truths:
    - "Deterministic guards fail if local sendcmpct, live peer facts, production announcement callers, real transport writes, or post-write evidence disappear."
    - "Parity breadcrumbs and index/catalog evidence cite concrete pinned Knots negotiation, tip notification, compact announcement, and transport anchors."
    - "The full repo-native verifier passes without public network, service manager, wall-clock soak, or production deployment requirements."
  artifacts:
    - path: scripts/check-phase128-production-compact-announcement-transport.ts
      provides: "Static production-seam and scope guard"
    - path: scripts/check-phase128-production-compact-announcement-transport.test.ts
      provides: "Mutation coverage for every required production link"
    - path: docs/parity/index.json
      provides: "Machine-readable Phase 128 parity evidence"
  key_links:
    - from: scripts/verify.sh
      to: scripts/check-phase128-production-compact-announcement-transport.ts
      via: "default deterministic checker invocation"
      pattern: "check-phase128-production-compact-announcement-transport"
    - from: docs/parity/index.json
      to: packages/bitcoin-knots/src/net_processing.cpp
      via: "CMP-04/CMP-05/OBS-03 parity anchors"
      pattern: "sendcmpct|compact|announcement"
---

<objective>
Make the repaired production compact-announcement flow deterministic, parity-auditable, and regression-resistant.

Purpose: Prevent Phase 128 from passing on helper-only behavior or pre-write evidence and preserve the bounded v2.1 claim.

Output: Phase 128 checker with mutation tests, parity breadcrumbs/index/catalog evidence, verifier wiring, and a clean full repository verification run.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/phases/128-production-compact-announcement-transport/128-CONTEXT.md
@.planning/phases/128-production-compact-announcement-transport/128-03-SUMMARY.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
Substantial repo-owned guards use Bun/TypeScript and are invoked from `scripts/verify.sh`.
`docs/parity/source-breadcrumbs.json` registers first-party Rust source/test anchors.
`docs/parity/index.json` is the machine-readable v2.1 evidence root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a fail-closed Phase 128 production integration checker</name>
  <files>scripts/check-phase128-production-compact-announcement-transport.ts, scripts/check-phase128-production-compact-announcement-transport.test.ts, scripts/verify.sh</files>
  <read-first>
    scripts/check-phase127-authoritative-network-state-unification.ts
    scripts/check-phase127-authoritative-network-state-unification.test.ts
    scripts/check-phase126-compact-relay-residual-hardening.ts
    scripts/check-phase126-compact-relay-residual-hardening.test.ts
    scripts/verify.sh
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/v2.1-MILESTONE-AUDIT.md
  </read-first>
  <behavior>
    - Guard requires a production post-Verack local version-2 low-bandwidth SendCompact action.
    - Guard requires a post-durable active-tip caller, live per-peer header facts, bounded peer-targeted emissions, and both outbound/inbound real writes.
    - Guard requires consuming post-write completion and rejects pre-write compact metric/log/provenance mutation.
    - Guard rejects package/filter/public-default/public-network/production-readiness claim expansion.
  </behavior>
  <action>
This task implements locked decisions D-16 and D-17.
Create a focused Bun/TypeScript checker that parses the exact Phase 128 production files and fails closed when any ROADMAP link is absent. Require concrete patterns for `SendCompactMessage { announce: false, version: BIP152_COMPACT_BLOCKS_VERSION }`, post-Verack dispatch, remote preference retention, durable best-tip caller, live per-peer header facts, `PeerEmission`, bounded queue handling, outbound and inbound write-success seams, consuming receipt completion, authoritative fixed metric/log projection, and absence of pre-write compact credit. Add table-driven mutation tests that independently delete or replace each required pattern, including replacing live header facts with constants and moving evidence before write. Wire the checker and its test into `scripts/verify.sh` without adding public network or service dependencies.
  </action>
  <threat-model>
Mitigate verification bypass with independent mutations per production link; mitigate command injection by using Bun APIs and fixed repo paths; keep checker output free of secrets and raw runtime data.
  </threat-model>
  <verify>
    <automated>bun test scripts/check-phase128-production-compact-announcement-transport.test.ts</automated>
    <automated>bun run scripts/check-phase128-production-compact-announcement-transport.ts</automated>
  </verify>
  <acceptance-criteria>
    The checker exits 0 on the implemented production flow.
    Every named mutation exits non-zero for its intended missing link.
    Constant header facts and pre-write evidence mutations are independently detected.
    `scripts/verify.sh` invokes both the checker and its mutation tests.
    No checker path performs network, service-manager, or deployment effects.
  </acceptance-criteria>
  <done>Default verification rejects every Phase 128 production integration regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register parity evidence and source breadcrumbs</name>
  <files>docs/parity/source-breadcrumbs.json, docs/parity/index.json, docs/parity/catalog/p2p.md</files>
  <read-first>
    docs/parity/source-breadcrumbs.json
    docs/parity/index.json
    docs/parity/catalog/p2p.md
    packages/bitcoin-knots/src/net_processing.cpp
    packages/bitcoin-knots/src/net.cpp
    packages/bitcoin-knots/src/validationinterface.cpp
    packages/bitcoin-knots/test/functional/p2p_compactblocks.py
    .planning/phases/128-production-compact-announcement-transport/128-CONTEXT.md
  </read-first>
  <behavior>
    - Every new first-party Rust source/test file has a registered breadcrumb.
    - CMP-04, CMP-05, and OBS-03 evidence cites exact pinned Knots files and Open Bitcoin production artifacts/tests.
    - Human-readable catalog text remains bounded, default-off, and free of production/public-default claims.
  </behavior>
  <action>
This task implements locked decisions D-16 and D-17.
Audit the breadcrumb entries added atomically by Plans 02-03 for every Phase 128 Rust source/test file, using concrete `net_processing.cpp`, `net.cpp`, `validationinterface.cpp`, and `p2p_compactblocks.py` anchors, and repair only missing or inaccurate entries. Update the parity index evidence for CMP-04, CMP-05, and OBS-03 to name the production post-Verack offer, live-fact durable-tip announcement caller, real session writes, consuming receipts, end-to-end tests, and Phase 128 checker. Update the P2P catalog only where needed to state the bounded achieved behavior and retain explicit deferred package/filter/public-default/public-network/production boundaries.
  </action>
  <threat-model>
Mitigate false parity claims by tying each requirement to production code plus deterministic tests/checker; mitigate information disclosure by using repository paths and aggregate behavior only.
  </threat-model>
  <verify>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
    <automated>bun run scripts/check-phase128-production-compact-announcement-transport.ts</automated>
  </verify>
  <acceptance-criteria>
    `check-parity-breadcrumbs.ts` exits 0 with all new Rust files registered.
    `docs/parity/index.json` contains CMP-04, CMP-05, and OBS-03 production evidence.
    Catalog text explicitly preserves default-off and deferred public/production surfaces.
    Phase 128 checker exits 0 after parity updates.
  </acceptance-criteria>
  <done>Phase 128 behavior has auditable source and machine-readable parity evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete deterministic verification contract and review freshness</name>
  <files>docs/metrics/lines-of-code.md</files>
  <read-first>
    AGENTS.md
    scripts/verify.sh
    docs/metrics/lines-of-code.md
    README.md
    packages/open-bitcoin-node/README.md
    packages/open-bitcoin-network/README.md
  </read-first>
  <action>
This task implements locked decisions D-16 and D-17.
Run the relevant focused Cargo suites in the repository-required order through `bun run scripts/command-timings.ts run --key ... --`, then run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` against `packages/Cargo.toml` through stable timing keys. Run the Phase 128 checker/tests and parity breadcrumb checker. Finish with `bash scripts/verify.sh`; do not interrupt a quiet resumable session and poll liveness at least every 60 seconds. Accept hook/verifier regeneration of `docs/metrics/lines-of-code.md` as required freshness. Inspect the root/node/network READMEs and update them only if their contributor-facing description is now false; any README update must preserve bounded default-off/no-production claims and be added explicitly to this plan's files before editing.
  </action>
  <threat-model>
Mitigate verification race and stale evidence by using the cooperative timing wrapper and serial commands; mitigate claim expansion through explicit README review and the Phase 128 checker.
  </threat-model>
  <verify>
    <automated>bun test scripts/check-phase128-production-compact-announcement-transport.test.ts</automated>
    <automated>bun run scripts/check-phase128-production-compact-announcement-transport.ts</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance-criteria>
    Format, clippy with `-D warnings`, all-target/all-feature build, and all-feature tests exit 0.
    Phase 128 checker, mutation tests, and parity breadcrumb checker exit 0.
    `bash scripts/verify.sh` exits 0 after all implementation and evidence changes.
    `docs/metrics/lines-of-code.md` matches the verifier-generated checkout state.
    README review is recorded in the summary, with no unplanned broad documentation rewrite.
  </acceptance-criteria>
  <done>The full repo-native verification contract is clean and generated freshness artifacts are current.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| source integration to deterministic checker | Helper-only or stale-pattern false pass | Production-path assertions plus mutation tests |
| implementation to parity/release evidence | Overbroad or unsupported claim | Exact Knots anchors and bounded wording |
| local verification processes | Concurrent build races or false timeout | Cooperative timing lock and liveness polling |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-128-04-01 | Tampering | mitigate | Mutation-test every mandatory production link. |
| T-128-04-02 | Repudiation | mitigate | Register source breadcrumbs and machine-readable evidence. |
| T-128-04-03 | Information Disclosure | mitigate | Keep checker/docs aggregate and path-based. |
| T-128-04-04 | Denial of Service | mitigate | Deterministic local checks, cooperative lock, no public network. |
</threat-model>

<verification>
Run the Phase 128 checker and mutation tests, parity breadcrumb checker, required Rust format/lint/build/test sequence, and final `bash scripts/verify.sh`; review the final diff for scope and claim drift.
</verification>

<success-criteria>
Deterministic guards, parity evidence, and the full repository verifier prove CMP-04, CMP-05, and OBS-03 through the real production transport boundary.
</success-criteria>

<output>
After completion, create `.planning/phases/128-production-compact-announcement-transport/128-04-SUMMARY.md`.
</output>
