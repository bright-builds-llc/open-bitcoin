---
phase: 128-production-compact-announcement-transport
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/message_dispatch.rs
  - packages/open-bitcoin-network/src/peer/compact_relay.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
autonomous: true
requirements:
  - CMP-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 128-2026-07-20T01-54-33
generated_at: 2026-07-20T02:30:00Z
must_haves:
  truths:
    - "An activation-enabled production handshake emits exactly one version-2 low-bandwidth sendcmpct offer after Verack."
    - "Local offer state and remote high/low preference are directional typed state and cannot imply one another."
    - "Duplicate Verack, unsupported protocol versions, and disabled activation do not create false capability or extra offers."
  artifacts:
    - path: packages/open-bitcoin-network/src/peer/compact_relay.rs
      provides: "Directional local-offer and remote-negotiation state"
    - path: packages/open-bitcoin-network/src/peer/message_dispatch.rs
      provides: "Post-Verack SendCompact action emission"
    - path: packages/open-bitcoin-network/src/peer/tests.rs
      provides: "Handshake ordering, idempotence, and isolation regressions"
  key_links:
    - from: packages/open-bitcoin-network/src/peer/message_dispatch.rs
      to: packages/open-bitcoin-network/src/peer/compact_relay.rs
      via: "Verack transition asks typed policy whether to emit the local offer"
      pattern: "SendCompact|send_compact"
    - from: packages/open-bitcoin-network/src/peer.rs
      to: packages/open-bitcoin-network/src/peer/message_dispatch.rs
      via: "existing peer action flow transports the post-Verack offer"
      pattern: "PeerAction::Send|WireNetworkMessage::SendCompact"
---

<objective>
Complete bilateral compact negotiation at the production handshake boundary.

Purpose: Close the CMP-04 local-offer gap without conflating what Open Bitcoin advertised with what the remote peer requested.

Output: Directional typed compact-negotiation state, post-Verack version-2 low-bandwidth offer, and deterministic peer-policy regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/128-production-compact-announcement-transport/128-CONTEXT.md

<interfaces>
`WireNetworkMessage::SendCompact(SendCompactMessage)` is the existing BIP152 wire variant.
`BIP152_COMPACT_BLOCKS_VERSION` is the only positive in-scope version.
Incoming remote `sendcmpct(true|false)` already updates `CompactRelayPeerState`; this plan adds separate local offer state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model directional local-offer and remote compact state</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/compact_relay.rs</files>
  <read-first>
    packages/open-bitcoin-network/src/peer.rs
    packages/open-bitcoin-network/src/peer/compact_relay.rs
    packages/open-bitcoin-network/src/peer/message_dispatch.rs
    packages/bitcoin-knots/src/net_processing.cpp
    packages/bitcoin-knots/test/functional/p2p_compactblocks.py
  </read-first>
  <behavior>
    - Local offer status is unknown/not-offered until an eligible established handshake schedules it.
    - A scheduled or written local `announce: false` offer never sets remote high-bandwidth preference.
    - Remote version-2 `announce: true` and `announce: false` continue to set mutually exclusive remote preferences.
    - Unsupported remote versions remain decoded-but-ineligible without clearing a previous valid version-2 preference.
  </behavior>
  <action>
This task implements locked decisions D-01, D-02, D-03, and D-04.
Extend the compact-relay peer state with a typed directional local-offer state that is distinct from the existing remote-derived capability, version, high-bandwidth, and low-bandwidth fields. Add a pure policy function that returns `Some(SendCompactMessage { announce: false, version: BIP152_COMPACT_BLOCKS_VERSION })` only when compact relay is explicitly active, the handshake is established, the peer protocol version supports BIP152 messaging, and the offer has not already been scheduled. Make the state transition idempotent and retain incoming remote `sendcmpct` as the sole authority for outbound high-bandwidth compact eligibility. Do not consult transaction relay, package relay, permissions, filters, or public serving defaults.
  </action>
  <threat-model>
Mitigate negotiation-state spoofing by separating local and remote directions; mitigate replay/duplicate actions with an idempotent typed transition; fail closed for unsupported versions and disabled activation.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-network-negotiation-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compact_relay</automated>
  </verify>
  <acceptance-criteria>
    `compact_relay.rs` contains distinct local-offer and remote-preference state.
    The local message is exactly `announce: false` and `version: BIP152_COMPACT_BLOCKS_VERSION`.
    No local transition writes the remote high-bandwidth or low-bandwidth fields.
    Focused compact-relay tests exit 0.
  </acceptance-criteria>
  <done>Compact negotiation is directional and cannot infer a remote request from a local offer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the local offer after Verack through the existing action flow</name>
  <files>packages/open-bitcoin-network/src/peer/message_dispatch.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read-first>
    packages/open-bitcoin-network/src/peer/message_dispatch.rs
    packages/open-bitcoin-network/src/peer/tests.rs
    packages/open-bitcoin-network/src/peer.rs
    packages/open-bitcoin-network/src/peer/compact_relay.rs
    packages/bitcoin-knots/src/net_processing.cpp
  </read-first>
  <behavior>
    - The first valid Verack establishes the handshake, preserves existing actions, then schedules one local low-bandwidth SendCompact action in deterministic order.
    - Duplicate Verack does not emit a second SendCompact action.
    - Disabled compact relay and unsupported peer protocol versions emit no local SendCompact.
    - Incoming remote high/low preference remains retained after local offer emission.
  </behavior>
  <action>
This task implements locked decisions D-01, D-03, and D-04.
Extend the established-handshake/Verack branch to append the pure local compact offer to the existing peer action vector after the handshake transition and existing mandatory handshake actions. Reuse `PeerAction::Send` and `WireNetworkMessage::SendCompact`; do not add a transport side channel. Add Arrange/Act/Assert tests for action ordering, one-shot idempotence, activation disabled, unsupported protocol version, remote high-to-low/low-to-high updates after the local offer, and isolation from transaction/package/filter activation.
  </action>
  <threat-model>
Mitigate message-order interoperability faults by testing exact post-Verack ordering; mitigate action amplification with duplicate-Verack coverage; avoid privilege coupling by proving unrelated relay settings do not emit SendCompact.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-network-handshake-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network handshake</automated>
    <automated>bun run scripts/command-timings.ts run --key phase128-network-peer-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer::tests</automated>
  </verify>
  <acceptance-criteria>
    A valid enabled Verack action list contains exactly one version-2 `SendCompact` with `announce == false`.
    Duplicate Verack, disabled activation, and unsupported protocol tests contain no local SendCompact action.
    Existing sendheaders, wtxidrelay, and remote compact preference tests remain green.
    Both command-timed test commands exit 0.
  </acceptance-criteria>
  <done>Production-capable peer handshakes advertise local BIP152 support once and retain bilateral state.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| remote handshake messages to peer state | Spoofed or unsupported compact capability | Version-gated remote-derived state |
| local activation to outbound handshake action | Accidental public/default activation | Explicit activation gate and post-Verack policy |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-128-01-01 | Spoofing | mitigate | Keep local offer state separate from remote negotiation. |
| T-128-01-02 | Denial of Service | mitigate | Emit at most one local offer per established peer. |
| T-128-01-03 | Tampering | mitigate | Exact version/value and ordering tests fail closed. |
</threat-model>

<verification>
Run both focused network test commands through the timing wrapper and review the diff for accidental transaction/package/filter/public-default coupling.
</verification>

<success-criteria>
Production handshake policy emits one activation-gated post-Verack version-2 low-bandwidth offer while retaining independently negotiated remote state.
</success-criteria>

<output>
After completion, create `.planning/phases/128-production-compact-announcement-transport/128-01-SUMMARY.md`.
</output>
