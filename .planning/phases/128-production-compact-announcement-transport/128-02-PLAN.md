---
phase: 128-production-compact-announcement-transport
plan: "02"
type: execute
wave: 2
depends_on:
  - "128-01"
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network/announcement_transport.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - CMP-05
  - OBS-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 128-2026-07-20T01-54-33
generated_at: 2026-07-20T02:30:00Z
must_haves:
  truths:
    - "Announcement preparation uses live per-peer negotiation, header ancestry, block availability, and resource facts from the one runtime authority."
    - "An owned peer-targeted emission preserves typed message and evidence intent without holding the authority lock across effects."
    - "A consuming success receipt records compact/header/inventory outcomes exactly once and failed or unsent messages receive no achieved-effect credit."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/announcement_transport.rs
      provides: "Owned PeerEmission, consuming receipt, bounded queue outcomes"
    - path: packages/open-bitcoin-node/src/network/runtime_authority.rs
      provides: "Short-lock announcement snapshot and receipt completion commands"
    - path: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      provides: "Post-write-only aggregate and peer provenance transitions"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority.rs
      to: packages/open-bitcoin-node/src/network.rs
      via: "snapshot live peer facts and prepare owned emissions"
      pattern: "prepare.*announcement|PeerEmission"
    - from: packages/open-bitcoin-node/src/network/announcement_transport.rs
      to: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      via: "successful receipt commits matching achieved-effect evidence"
      pattern: "receipt|complete.*emission|acknowledge"
---

<objective>
Define the bounded announcement transport contract and post-write evidence core.

Purpose: Make compact/header/inventory intent peer-targeted and transportable without locks while making false or duplicate achieved-effect evidence difficult to represent.

Output: Live-fact announcement snapshot, owned `PeerEmission`, bounded queue outcomes, consuming receipt, and focused exactly-once regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/128-production-compact-announcement-transport/128-CONTEXT.md
@.planning/phases/128-production-compact-announcement-transport/128-01-SUMMARY.md

<interfaces>
`ManagedPeerNetwork::announce_block` and `announce_block_with_nonce` already decide and construct compact/header/inventory wire messages.
`NetworkRuntimeAuthority` is the single production network lock boundary.
`compact_announce_evidence_reason` already maps decision plus actual message, but current evidence occurs before a real transport write.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prepare owned peer emissions from live authoritative facts</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/announcement_transport.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-network/src/peer.rs
    packages/open-bitcoin-network/src/peer/compact_relay.rs
    packages/bitcoin-knots/src/net_processing.cpp
    packages/bitcoin-knots/src/net.cpp
  </read-first>
  <behavior>
    - Preparation snapshots each active peer's negotiated compact state, best-known/best-header-sent ancestry, eligibility, and queue pressure.
    - Constant header booleans and global-header proxies cannot enter the production preparation API.
    - A prepared emission owns PeerId, WireNetworkMessage, block identity, and a matching evidence intent after the lock is released.
    - Queue-full, disconnected, unavailable, construction-failure, and ineligible paths return typed fallback/suppression outcomes.
  </behavior>
  <action>
This task implements locked decisions D-05, D-07, D-08, D-09, and D-10.
Add a focused `announcement_transport.rs` module defining an owned `PeerEmission` and non-cloneable/consuming receipt intent. Add its required pinned-Knots source breadcrumb block and `docs/parity/source-breadcrumbs.json` registration in this same atomic task. Refactor announcement preparation so `NetworkRuntimeAuthority` snapshots live peer facts under one short synchronous critical section, calls the existing Phase 113/118 policy and nonce-consuming payload builder, and returns owned emissions plus typed suppress/fallback outcomes. Preserve the authoritative validated block as caller-owned input and never perform socket I/O, `.await`, Fjall access, encoding, logging, metrics, or serialization under the authority guard. Model bounded per-peer queue admission explicitly; do not introduce an actor, async mutex, general P2P emitter, retry loop, or dynamic peer label.
  </action>
  <threat-model>
Mitigate TOCTOU and forged evidence by binding peer/message/block/evidence intent in one owned value; mitigate queue exhaustion with explicit caps and fail-closed outcomes; mitigate deadlock by returning only owned data from the private guard.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-node-announcement-preparation-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node announcement_transport</automated>
  </verify>
  <acceptance-criteria>
    `PeerEmission` owns peer, typed message, block identity, and evidence intent.
    Production preparation accepts live typed peer facts rather than constant header flags.
    No public API returns a lock guard or performs effects under the authority lock.
    Queue pressure and lost eligibility produce stable fallback/suppression outcomes.
    Focused preparation tests exit 0.
  </acceptance-criteria>
  <done>Announcement intent leaves the authority as bounded owned peer emissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit exactly-once achieved-effect evidence from consuming receipts</name>
  <files>packages/open-bitcoin-node/src/network/block_relay_evidence.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-node/src/sync/tests/runtime_write_evidence_cases.rs
    packages/open-bitcoin-node/src/metrics/block_relay.rs
    packages/open-bitcoin-node/src/logging.rs
    packages/bitcoin-knots/src/net.cpp
  </read-first>
  <behavior>
    - Successful CompactBlock receipts increment compact-announced and update internal peer/block provenance once.
    - Successful Headers and Inv receipts increment only their corresponding fallback outcome/provenance.
    - Failed writes, suppressed decisions, dropped queues, and unsent suffixes do not increment achieved-effect counters.
    - A receipt cannot be cloned or applied twice, and mismatched peer/message acknowledgement is unrepresentable.
  </behavior>
  <action>
This task implements locked decisions D-10, D-11, D-12, D-13, D-14, and D-15.
Replace pre-write compact announcement mutation with a receipt completion method on `NetworkRuntimeAuthority`. Make the receipt consumed by value and derive the exact aggregate outcome from its bound typed message/evidence intent; keep peer IDs and block hashes internal. Preserve existing fixed `BlockRelayEvidenceStatus`, `MetricKind`, and structured-log vocabulary. Add a focused test module registered from `network/tests.rs` covering compact/header/inventory success, write failure/no completion, queue suppression, receipt ownership, partial successful prefix accounting, and unchanged availability/redaction semantics. Add the new test file's required pinned-Knots source breadcrumb block and `docs/parity/source-breadcrumbs.json` registration in this same atomic task. Remove or narrow generic acknowledgement APIs that could credit an unrelated or replayed message.
  </action>
  <threat-model>
Mitigate receipt replay and double-credit with consuming ownership; mitigate provenance tampering by binding fields at preparation; mitigate information disclosure by projecting only existing fixed aggregates.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase128-node-announcement-evidence-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node announcement_transport_cases</automated>
    <automated>bun run scripts/command-timings.ts run --key phase128-node-block-relay-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay_evidence</automated>
  </verify>
  <acceptance-criteria>
    `compact_announced_count` changes only through successful CompactBlock receipt completion.
    Header and inventory success use their existing fixed outcome labels.
    No failure, suppression, or unsent suffix test receives achieved-effect credit.
    Peer IDs and block hashes are absent from metric kinds and structured log fields.
    Both command-timed test commands exit 0.
  </acceptance-criteria>
  <done>Announcement evidence is a post-write, exactly-once, peer-bound achieved effect.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| authoritative peer state to transport | Stale or forged peer facts | Short-lock typed snapshot and bound emission |
| transport success to evidence | Replay, mismatch, or pre-write credit | Consuming receipt completed only after write |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-128-02-01 | Tampering | mitigate | Bind PeerId/message/block/evidence intent before transport. |
| T-128-02-02 | Denial of Service | mitigate | Bound outboxes and fail closed on pressure/disconnect. |
| T-128-02-03 | Information Disclosure | mitigate | Keep provenance internal and aggregate labels fixed. |
| T-128-02-04 | Elevation of Privilege | mitigate | Reuse existing activation/eligibility/resource policy without relay coupling. |
</threat-model>

<verification>
Run the focused preparation and evidence suites through the timing wrapper and inspect the API for cloneable receipts, generic unbound acknowledgements, dynamic labels, or effectful lock scope.
</verification>

<success-criteria>
Live policy produces bounded owned emissions, and only consuming successful-write receipts mutate compact/header/inventory evidence and provenance.
</success-criteria>

<output>
After completion, create `.planning/phases/128-production-compact-announcement-transport/128-02-SUMMARY.md`.
</output>
