---
phase: 127-authoritative-network-state-unification
plan: "03"
type: execute
wave: 3
depends_on:
  - "127-02"
files_modified:
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
autonomous: true
requirements:
  - OBS-02
  - OBS-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 127-2026-07-19T15-09-40
generated_at: 2026-07-19T16:00:00Z
must_haves:
  truths:
    - "RPC, CLI/dashboard, metrics/log, and support consumers derive from one owned authoritative aggregate snapshot."
    - "Existing RPC and operator schemas, copy, ordering, availability states, labels, and redaction remain unchanged."
    - "Authority failure renders unavailable/error rather than stale data."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/context/network.rs
      provides: "Owned authoritative operator snapshot adapter"
    - path: packages/open-bitcoin-rpc/src/dispatch/node.rs
      provides: "Unchanged RPC schema populated from shared truth"
    - path: packages/open-bitcoin-cli/src/operator/support/redaction.rs
      provides: "Regression proof for sensitive-field exclusion"
  key_links:
    - from: packages/open-bitcoin-rpc/src/context/network.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "one owned snapshot call"
      pattern: "snapshot"
    - from: packages/open-bitcoin-rpc/src/dispatch/node.rs
      to: packages/open-bitcoin-rpc/src/method/node.rs
      via: "existing response type without field changes"
      pattern: "block_relay"
---

<objective>
Project shared authoritative network truth through every operator surface without changing presentation or disclosure boundaries.

Purpose: Close the provenance gap while preserving the approved UI/RPC contract byte-for-byte or snapshot-equivalently.

Output: Owned aggregate snapshot adapter plus schema, dashboard, availability, and support-redaction regression tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/127-authoritative-network-state-unification/127-CONTEXT.md
@.planning/phases/127-authoritative-network-state-unification/127-UI-SPEC.md
@.planning/phases/127-authoritative-network-state-unification/127-02-SUMMARY.md

<interfaces>
`BlockRelayEvidenceStatus` and existing node RPC response types are frozen public contracts.
Dashboard `block_relay` model and support `redaction` consume those stable serialized shapes.
The Phase 127 handle must return an owned aggregate snapshot before JSON, metrics/log, dashboard, or bundle work starts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route RPC and runtime projections through one owned snapshot</name>
  <files>packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-node/src/sync.rs</files>
  <read-first>
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/method/node.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/status/block_relay_evidence.rs
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    .planning/phases/127-authoritative-network-state-unification/127-UI-SPEC.md
  </read-first>
  <behavior>
    - A sync mutation appears in RPC status, metrics/log projection, and inbound status from the same snapshot.
    - Serialization occurs after the authority guard is released.
    - Authority failure uses existing unavailable/error shapes.
    - Serialized field names, omission behavior, and labels do not change.
  </behavior>
  <action>
Per D-11, D-12, and D-14, replace remaining direct or duplicated network reads with one owned aggregate snapshot from the shared handle, then perform RPC serialization, metrics/log projection, and inbound-status adaptation after the critical section. Preserve `BlockRelayEvidenceStatus`, method response types, field names, availability wrappers, omission behavior, and fixed low-cardinality labels. Add before/after schema snapshots and provenance tests. Do not add fields, copy, labels, peer identifiers, or a mutable projection cache.
  </action>
  <threat-model>
    Mitigate stale-data spoofing by failing closed on authority error; mitigate polling denial of service by releasing the guard before serialization/persistence; mitigate schema tampering with exact snapshot tests.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-operator-rpc-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc authoritative_operator</automated>
  </verify>
  <acceptance-criteria>
    Every runtime projection starts from one owned authoritative snapshot.
    No serialization, logging, metric persistence, or await occurs under the guard.
    Existing RPC schema and availability behavior are unchanged.
    Failure returns unavailable/error and never last-known-good data.
  </acceptance-criteria>
  <done>RPC and runtime projections share one immutable authoritative provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock dashboard and support outputs to unchanged schemas and redaction</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs, packages/open-bitcoin-cli/src/operator/support/redaction.rs</files>
  <read-first>
    packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
    packages/open-bitcoin-cli/src/operator/support/redaction.rs
    packages/open-bitcoin-rpc/src/method/node.rs
    packages/open-bitcoin-node/src/status/block_relay_evidence.rs
    .planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-CONTEXT.md
    .planning/phases/127-authoritative-network-state-unification/127-UI-SPEC.md
  </read-first>
  <behavior>
    - Identical status input renders identical dashboard copy, ordering, and availability states.
    - Support output excludes endpoints, permissions, credentials, transactions, and dynamic peer labels.
    - Unified provenance does not introduce a new dashboard field or interaction.
  </behavior>
  <action>
Per D-11 and D-13 and the approved UI-SPEC, retain the dashboard and support implementations unless a narrow adapter update is necessary, but add regression assertions proving unchanged rendered/snapshot output and forbidden-field redaction from authoritative fixtures. Cover available and unavailable authority states. Do not add screens, panels, fields, shortcuts, styles, labels, or copy; do not weaken sanitization for debugging.
  </action>
  <threat-model>
    Mitigate information disclosure with adversarial fixtures containing raw endpoint, permission, credential, transaction, and dynamic-label material; mitigate repudiation with stable snapshot assertions.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-operator-cli-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli block_relay</automated>
    <automated>bun run scripts/command-timings.ts run --key phase127-support-redaction-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support</automated>
  </verify>
  <acceptance-criteria>
    Dashboard snapshots remain equivalent for identical inputs.
    Support fixtures contain none of the forbidden sensitive material after sanitization.
    Available/unavailable behavior uses existing wording and shapes.
    No operator-visible field, layout, label, or interaction changes.
  </acceptance-criteria>
  <done>CLI/dashboard and support outputs preserve their frozen contracts over authoritative data.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| authority snapshot to RPC/status | Stale or inconsistent projection | One owned aggregate and fail-closed errors |
| operator status to dashboard/support | Sensitive data exposure or schema churn | Frozen types, snapshots, adversarial redaction tests |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-127-03-01 | Spoofing | mitigate | Never present stale data after authority failure. |
| T-127-03-02 | Information Disclosure | mitigate | Preserve aggregate contracts and support redaction. |
| T-127-03-03 | Denial of Service | mitigate | Serialize/render only after releasing the guard. |
</threat-model>

<verification>
Run focused RPC, CLI/dashboard, and support tests through the timing wrapper and compare frozen schema/render snapshots.
</verification>

<success-criteria>
All operator consumers show the same authoritative truth with unchanged schemas, UX, labels, availability behavior, and redaction.
</success-criteria>

<output>
After completion, create `.planning/phases/127-authoritative-network-state-unification/127-03-SUMMARY.md`.
</output>
