---
phase: 127-authoritative-network-state-unification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - BSRV-03
  - BSRV-04
  - OBS-02
  - OBS-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 127-2026-07-19T15-09-40
generated_at: 2026-07-19T16:00:00Z
must_haves:
  truths:
    - "Production open-bitcoind constructs exactly one ManagedPeerNetwork authority shared by durable sync, inbound handling, and RPC."
    - "Callers receive typed mutation methods and owned snapshots, never a clone of ManagedPeerNetwork or an exposed lock guard."
    - "Authority failure is explicit and no critical section crosses await, socket, Fjall, logging, metrics, or serialization work."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/runtime_authority.rs
      provides: "Cloneable narrow authoritative-network handle and typed error"
    - path: packages/open-bitcoin-node/src/sync.rs
      provides: "DurableSyncRuntime backed by the shared authority"
    - path: packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
      provides: "Single production allocation and dependency wiring"
  key_links:
    - from: packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
      to: packages/open-bitcoin-node/src/sync.rs
      via: "durable runtime creates/returns the handle before RPC context construction"
      pattern: "network_handle|authoritative_network"
    - from: packages/open-bitcoin-rpc/src/context.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "ManagedRpcContext stores the shared handle"
      pattern: "ManagedNetworkHandle|AuthoritativeNetwork"
---

<objective>
Establish one production network authority and make durable sync, RPC, and inbound composition consume narrow handles to it.

Purpose: Close the split-brain construction defect while preserving the functional core and keeping synchronization out of public contracts.

Output: Node-owned shared handle, explicit authority errors, recovered durable runtime ownership, and one-allocation daemon composition.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/127-authoritative-network-state-unification/127-CONTEXT.md
@.planning/phases/127-authoritative-network-state-unification/127-RESEARCH.md
@.planning/phases/127-authoritative-network-state-unification/127-UI-SPEC.md

<interfaces>
Existing authority: `ManagedPeerNetwork<MemoryChainstateStore>` owns chainstate, peers, serving policy, cleanup, and block-relay evidence.
Existing durable owner: `DurableSyncRuntime { store: FjallNodeStore, network: ManagedPeerNetwork<MemoryChainstateStore>, ... }`.
Existing split consumer: `ManagedRpcContext { network: ManagedPeerNetwork<MemoryChainstateStore>, ... }`.
The new contract must replace both owned `network` fields with one cloneable handle that returns owned results.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the narrow shared authority and convert durable sync</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/lib.rs
    docs/parity/source-breadcrumbs.json
    packages/bitcoin-knots/src/node/context.h
  </read-first>
  <behavior>
    - Cloned handles mutate and snapshot the same allocation.
    - Snapshot values are owned and remain usable after the critical section ends.
    - Poisoned/unavailable authority returns a typed error and never panics or returns stale data.
  </behavior>
  <action>
Per D-01 through D-05, add a node-owned `Arc`-backed synchronous authority around exactly `ManagedPeerNetwork&lt;MemoryChainstateStore&gt;`. Keep its guard private; expose small closure/command methods for synchronous mutations and owned snapshots, with a typed poison/unavailable error and no `unwrap`. Per D-03, make the API incapable of retaining a guard across socket, await, Fjall, serialization, metrics, or log effects. Per D-04, do not add an actor/coordinator protocol, async mutex, queue, cancellation redesign, second projection network, or lock-bearing return type. Convert `DurableSyncRuntime` to create/recover this authority and use it for every network mutation and evidence snapshot; expose only a cloned handle. Add focused Arrange/Act/Assert tests in the new module and register the new Rust source in parity breadcrumbs with the Knots node-context anchor.
  </action>
  <threat-model>
    Mitigate deadlock/starvation by making guard lifetime private and synchronous; mitigate poisoned-lock spoofing by returning a typed error; mitigate split identity by testing mutation visibility through independently cloned handles.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-authority-node-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node runtime_authority</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance-criteria>
    Exactly one authority allocation backs all cloned handles.
    DurableSyncRuntime no longer owns an independent ManagedPeerNetwork value.
    No public method exposes a mutex guard or permits lock scope across effects.
    Focused identity, owned-snapshot, and failure tests pass.
  </acceptance-criteria>
  <done>The node crate owns a tested shared-authority contract and durable sync uses it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make daemon and RPC construction consume the one authority</name>
  <files>packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</files>
  <read-first>
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/bitcoin-knots/src/rpc/server_util.cpp
  </read-first>
  <behavior>
    - Production startup initializes durable recovery before constructing RPC/inbound consumers.
    - ManagedRpcContext receives the shared handle and cannot allocate a fresh production MemoryChainstateStore.
    - Sync-side mutations are immediately visible through the RPC-side handle.
  </behavior>
  <action>
Per D-01, D-02, and D-15, reorder `open-bitcoind` production composition so `FjallNodeStore` and `DurableSyncRuntime` establish the authority first, then pass its handle into `ManagedRpcContext` and inbound setup. Replace the production `ManagedRpcContext` network field and fresh `MemoryChainstateStore` construction with the handle while retaining explicit test-only constructors for focused fixtures. Map authority errors through existing RPC/runtime unavailable vocabulary. Add composition tests that fail if daemon/RPC paths allocate a second production network. Do not change RPC schemas, startup modes, serving defaults, or Phase 128 compact transport.
  </action>
  <threat-model>
    Mitigate duplicate-authority tampering by removing the production constructor path; mitigate denial of service by keeping startup recovery and serialization outside the guard; fail closed on authority errors.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-authority-rpc-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc authoritative_network</automated>
    <automated>rg -n "MemoryChainstateStore::default|ManagedPeerNetwork::new" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</automated>
  </verify>
  <acceptance-criteria>
    The production daemon constructs one network allocation.
    RPC and inbound consumers receive the DurableSyncRuntime handle.
    Fresh MemoryChainstateStore construction is confined to explicit tests or non-production fixtures.
    Mutation-visibility composition tests pass without schema changes.
  </acceptance-criteria>
  <done>Production composition has one authoritative network identity.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| daemon construction to runtime consumers | Duplicate mutable authority | One node-owned allocation and handle-only injection |
| shared handle to callers | Lock leakage or poisoned state | Private guard, owned return values, typed errors |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-127-01-01 | Tampering | mitigate | Remove independent production constructors and test identity. |
| T-127-01-02 | Denial of Service | mitigate | No effectful work or await under the authority guard. |
| T-127-01-03 | Information Disclosure | mitigate | Return existing aggregate snapshots, never internal peer state. |
</threat-model>

<verification>
Run both focused test commands through the timing wrapper, run the parity breadcrumb checker, and inspect production constructors for a second network allocation.
</verification>

<success-criteria>
Durable sync, inbound handling, and RPC share one tested authority through a narrow, failure-aware handle.
</success-criteria>

<output>
After completion, create `.planning/phases/127-authoritative-network-state-unification/127-01-SUMMARY.md`.
</output>
