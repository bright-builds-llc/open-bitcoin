---
phase: 127-authoritative-network-state-unification
plan: "02"
type: execute
wave: 2
depends_on:
  - "127-01"
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/block_serving.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/storage/fjall_store.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/context.rs
autonomous: true
requirements:
  - BSRV-03
  - BSRV-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 127-2026-07-19T15-09-40
generated_at: 2026-07-19T16:00:00Z
must_haves:
  truths:
    - "Validated eligible durable blocks remain serveable after cache loss or restart-shaped recovery."
    - "Block, witness-block, and compact-block requests share policy, caps, backpressure, cleanup, and failure vocabulary."
    - "Fjall reads and transport writes happen outside the authoritative-network critical section."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/block_serving.rs
      provides: "Typed gated serve intent and completion transition"
    - path: packages/open-bitcoin-rpc/src/inbound_listener.rs
      provides: "Lazy request-scoped durable lookup and post-write completion"
    - path: packages/open-bitcoin-node/src/storage/fjall_store.rs
      provides: "Narrow durable block-source implementation"
  key_links:
    - from: packages/open-bitcoin-rpc/src/inbound_listener.rs
      to: packages/open-bitcoin-node/src/storage/fjall_store.rs
      via: "serve intent triggers lazy load_block"
      pattern: "load_block"
    - from: packages/open-bitcoin-rpc/src/inbound_listener.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "completion/evidence is applied after lookup and write"
      pattern: "complete|acknowledge|record"
---

<objective>
Serve validated block bodies from durable storage through the shared authority without weakening existing request governance.

Purpose: Make persisted blocks authoritative for availability while retaining network-owned policy and achieved-effect ordering.

Output: Typed serve intent/completion seam, lazy Fjall block source, and restart/cache-loss regression coverage.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/127-authoritative-network-state-unification/127-CONTEXT.md
@.planning/phases/127-authoritative-network-state-unification/127-RESEARCH.md
@.planning/phases/127-authoritative-network-state-unification/127-01-SUMMARY.md

<interfaces>
`FjallNodeStore::load_block(block_hash) -> Result&lt;Option&lt;Block&gt;, StorageError&gt;` is the durable body source.
`serve_managed_block_request` currently gates policy and receives an in-memory lookup closure.
`ManagedPeerNetwork::serve_inventory` currently reads `blocks_by_hash`; production must instead emit an owned gated intent before durable I/O.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split policy decision from block-body lookup and completion</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/inventory.rs, packages/open-bitcoin-node/src/network/block_serving.rs, packages/open-bitcoin-node/src/network/action_translation.rs</files>
  <read-first>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/bitcoin-knots/src/net_processing.cpp
    packages/bitcoin-knots/src/validation.cpp
  </read-first>
  <behavior>
    - Ineligible or unavailable status denies storage reads.
    - Eligible block, witness-block, and compact-block requests yield an owned typed serve intent.
    - Missing/corrupt lookup completion maps to existing unavailable/suppressed evidence and preserves cleanup.
    - Served evidence advances only after successful transport acknowledgement.
  </behavior>
  <action>
Per D-06, D-09, and D-10, refactor serving into a pure network-owned gate that returns a typed owned intent containing only the request/hash/serialization mode and a short post-effect completion command. Preserve request caps, queue pressure, active/recent-valid checks, peer cleanup, compact request semantics, and existing evidence labels. Keep `blocks_by_hash` only as an explicit cache/test adapter. Add focused Arrange/Act/Assert tests for denied-read, all three inventory types, missing lookup, cap/backpressure, cleanup, and success-only evidence. Do not move validation policy into RPC or storage code.
  </action>
  <threat-model>
    Mitigate TOCTOU by carrying a typed decision and applying completion to the same authority; mitigate privilege bypass by making durable lookup impossible before the policy gate; mitigate false evidence by recording served effects only after acknowledgement.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-serving-policy-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_serving</automated>
  </verify>
  <acceptance-criteria>
    Storage access cannot occur for a denied request.
    Full, witness, and compact requests use one typed gate/completion boundary.
    Existing caps, backpressure, cleanup, and evidence labels remain intact.
    Network tests no longer treat cache presence as production durable authority.
  </acceptance-criteria>
  <done>Serving policy is effect-free and emits a bounded durable-read intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute lazy durable reads in the inbound shell</name>
  <files>packages/open-bitcoin-node/src/storage/fjall_store.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/context.rs</files>
  <read-first>
    packages/open-bitcoin-node/src/storage/fjall_store.rs
    packages/open-bitcoin-node/src/sync/block_response.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/bitcoin-knots/src/node/blockstorage.cpp
  </read-first>
  <behavior>
    - A persisted validated block is served when the in-memory cache is empty.
    - Missing, corrupt, and storage-error results fail closed without leaking storage details.
    - No Fjall load or socket write occurs while the network authority is locked.
  </behavior>
  <action>
Per D-07 through D-10, expose the smallest clonable durable block-source seam backed by `FjallNodeStore::load_block`, inject it beside the shared handle, and make inbound request handling execute: short authority gate, unlocked lazy durable load, unlocked serialization/write, short authority completion. Per D-08, lookup must remain lazy and request-scoped; never hydrate the durable corpus at startup. Reuse existing unavailable/suppressed responses and cleanup. Add deterministic same-datadir restart/cache-loss tests for block, witness block, and compact block plus missing/corrupt/store failure cases. Do not claim archive availability, contact the public network, or implement Phase 128 announcement transport.
  </action>
  <threat-model>
    Mitigate memory amplification with request-scoped loads; mitigate deadlock with explicit unlocked I/O phases; mitigate information disclosure by mapping storage failures to existing public vocabulary.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-durable-serving-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc durable_block_serving</automated>
    <automated>rg -n "load_block" packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-node/src/storage/fjall_store.rs</automated>
  </verify>
  <acceptance-criteria>
    Restart-shaped tests serve validated durable bodies with an empty cache.
    Lookup and transport are outside the shared-network critical section.
    Failure paths preserve caps, cleanup, evidence ordering, and redacted vocabulary.
    No eager hydration or archive-node claim is introduced.
  </acceptance-criteria>
  <done>Inbound serving uses durable truth through the shared authority boundary.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| untrusted getdata to policy | Cap/eligibility bypass | Typed gate before any storage read |
| serve intent to Fjall | TOCTOU, corruption, amplification | Request-scoped read and fail-closed mapping |
| body to peer transport | False achieved-effect evidence | Complete only after successful write |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-127-02-01 | Elevation of Privilege | mitigate | Existing validation and peer gates precede durable access. |
| T-127-02-02 | Denial of Service | mitigate | Preserve caps/backpressure and avoid eager hydration. |
| T-127-02-03 | Repudiation | mitigate | Completion records the real transport outcome. |
</threat-model>

<verification>
Run focused policy and durable-serving tests through the timing wrapper and confirm `load_block` is confined to the unlocked shell phase.
</verification>

<success-criteria>
Eligible durable blocks are served after restart/cache loss, while ineligible and failed reads remain bounded and fail closed.
</success-criteria>

<output>
After completion, create `.planning/phases/127-authoritative-network-state-unification/127-02-SUMMARY.md`.
</output>
