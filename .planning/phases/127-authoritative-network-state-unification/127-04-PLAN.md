---
phase: 127-authoritative-network-state-unification
plan: "04"
type: execute
wave: 4
depends_on:
  - "127-03"
files_modified:
  - packages/open-bitcoin-rpc/tests/black_box_parity.rs
  - scripts/check-phase127-authoritative-network-state-unification.ts
  - scripts/check-phase127-authoritative-network-state-unification.test.ts
  - scripts/verify.sh
  - docs/parity/index.json
  - docs/parity/catalog/p2p.md
  - docs/parity/source-breadcrumbs.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - BSRV-03
  - BSRV-04
  - OBS-02
  - OBS-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 127-2026-07-19T15-09-40
generated_at: 2026-07-19T16:00:00Z
must_haves:
  truths:
    - "A production-shaped test proves sync mutation, durable serving, RPC status, and support evidence share one authority."
    - "A deterministic mutation-tested checker rejects duplicate network construction, fresh RPC chainstate, cache-only serving, and non-authoritative projection."
    - "Parity evidence cites Knots authority, validation, peer-processing, and durable block-storage anchors without claiming Phase 128 or 129 scope."
    - "The final public-network-free repository verification contract passes and LOC is fresh."
  artifacts:
    - path: packages/open-bitcoin-rpc/tests/black_box_parity.rs
      provides: "Focused production-composition and restart-shaped integration proof"
    - path: scripts/check-phase127-authoritative-network-state-unification.ts
      provides: "Narrow structural Phase 127 guard"
    - path: docs/parity/catalog/p2p.md
      provides: "Auditable bounded parity claim"
    - path: scripts/verify.sh
      provides: "Default deterministic invocation of the Phase 127 guard"
  key_links:
    - from: scripts/verify.sh
      to: scripts/check-phase127-authoritative-network-state-unification.ts
      via: "default checker invocation"
      pattern: "check-phase127-authoritative-network-state-unification"
    - from: docs/parity/index.json
      to: docs/parity/catalog/p2p.md
      via: "registered Phase 127 parity evidence"
      pattern: "authoritative|block serving"
---

<objective>
Prove and guard the authoritative production composition, durable serving flow, and unchanged operator projections.

Purpose: Prevent regression to split state while leaving compact transport and milestone reconciliation to Phases 128 and 129.

Output: Production-shaped integration tests, mutation-tested checker, parity evidence, fresh LOC, and passing full verifier.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/127-authoritative-network-state-unification/127-CONTEXT.md
@.planning/phases/127-authoritative-network-state-unification/127-RESEARCH.md
@.planning/phases/127-authoritative-network-state-unification/127-UI-SPEC.md
@.planning/phases/127-authoritative-network-state-unification/127-01-SUMMARY.md
@.planning/phases/127-authoritative-network-state-unification/127-02-SUMMARY.md
@.planning/phases/127-authoritative-network-state-unification/127-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add production-path and restart-shaped parity integration proof</name>
  <files>packages/open-bitcoin-rpc/tests/black_box_parity.rs, docs/parity/index.json, docs/parity/catalog/p2p.md, docs/parity/source-breadcrumbs.json</files>
  <read-first>
    packages/open-bitcoin-rpc/tests/black_box_parity.rs
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    packages/open-bitcoin-rpc/src/inbound_listener.rs
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    docs/parity/index.json
    docs/parity/catalog/p2p.md
    docs/parity/source-breadcrumbs.json
    packages/bitcoin-knots/src/node/context.h
    packages/bitcoin-knots/src/rpc/server_util.cpp
    packages/bitcoin-knots/src/net_processing.cpp
    packages/bitcoin-knots/src/validation.cpp
    packages/bitcoin-knots/src/node/blockstorage.cpp
  </read-first>
  <behavior>
    - A durable sync mutation is visible through inbound serving and RPC status from one authority.
    - A validated block persisted in one runtime is served after restart-shaped recovery with an empty cache.
    - RPC/dashboard-compatible and support-redacted outputs remain schema-equivalent.
    - Tests are deterministic and make no public-network connection.
  </behavior>
  <action>
Per D-15, D-17, and D-18, add a focused production-shaped integration fixture using one temporary datadir and the real composition seams. Prove shared identity by mutating through durable sync and observing through inbound/RPC; prove durable block serving after cache loss/reopen; assert unchanged operator serialization and forbidden support material absence. Update parity index/catalog and all required source breadcrumbs with concrete Knots node-context, RPC manager resolution, validation, net-processing, and block-storage anchors. State the bounded claim and explicitly exclude compact negotiation/announcement transport (Phase 128) and aggregate reconciliation/promotion (Phase 129).
  </action>
  <threat-model>
    Mitigate false-positive integration evidence by using the production constructors and one datadir; mitigate data disclosure with redaction assertions; mitigate scope elevation with explicit deferred-boundary parity text.
  </threat-model>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase127-production-parity-tests -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --test black_box_parity phase127</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts</automated>
    <automated>bun run scripts/check-parity-drift.ts</automated>
  </verify>
  <acceptance-criteria>
    The integration test fails if sync, inbound, or RPC use different authorities.
    Restart/cache-loss serving succeeds only for validated eligible durable data.
    Schema and redaction assertions cover OBS-02 and OBS-04.
    Parity docs cite all five canonical Knots anchors and make no Phase 128/129 claim.
  </acceptance-criteria>
  <done>Production composition and bounded parity are proven end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install mutation-tested Phase 127 guard and run full verification</name>
  <files>scripts/check-phase127-authoritative-network-state-unification.ts, scripts/check-phase127-authoritative-network-state-unification.test.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read-first>
    scripts/check-phase126-compact-relay-residual-hardening.ts
    scripts/check-phase126-compact-relay-residual-hardening.test.ts
    scripts/check-active-milestone-verification-traceability.ts
    scripts/check-parity-breadcrumbs.ts
    scripts/verify.sh
    scripts/command-timings.ts
    docs/metrics/lines-of-code.md
    .planning/phases/127-authoritative-network-state-unification/127-CONTEXT.md
  </read-first>
  <behavior>
    - Checker passes the intended production composition.
    - Mutations for duplicate production allocation, fresh RPC MemoryChainstateStore, cache-only block serving, and non-authoritative projection each fail.
    - Checker rejects Phase 128 compact-transport and Phase 129 reconciliation ownership in Phase 127 artifacts.
    - Full verification refreshes and accepts LOC without public-network work.
  </behavior>
  <action>
Per D-16 through D-18, create a Bun/TypeScript structural checker following the Phase 126 precedent and a mutation suite that rewrites fixtures/source text in memory or temporary copies for each required regression. Check the real production construction path, handle injection, durable `load_block` seam, owned operator snapshot, frozen redaction/schema evidence, and phase-scope exclusions. Wire the checker and mutation test into `scripts/verify.sh` in deterministic order. Refresh `docs/metrics/lines-of-code.md` only through the repo generator/verification flow, run `git diff --check`, then run final `bash scripts/verify.sh`. For any ad hoc Cargo/Bazel rerun during diagnosis, use `bun run scripts/command-timings.ts run --key &lt;stable-key&gt; -- &lt;command&gt;`; do not overlap Cargo jobs.
  </action>
  <threat-model>
    Mitigate checker spoofing with mutation coverage; mitigate tampering with exact source-path assertions; mitigate denial of service by keeping the default guard deterministic/public-network-free; block any unresolved high-severity authority, lock, or redaction finding.
  </threat-model>
  <verify>
    <automated>bun test scripts/check-phase127-authoritative-network-state-unification.test.ts</automated>
    <automated>bun run scripts/check-phase127-authoritative-network-state-unification.ts</automated>
    <automated>git diff --check</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance-criteria>
    All four required structural mutations are rejected.
    scripts/verify.sh invokes the Phase 127 checker and remains deterministic/public-network-free.
    LOC freshness is generated and accepted as an intentional tracked change.
    Full repository verification and diff checks pass.
    No requirement promotion, milestone audit reconciliation, archive routing, compact negotiation, or announcement transport is added.
  </acceptance-criteria>
  <done>Phase 127 has deterministic regression guards and a passing full repository contract.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| production composition to integration test | Fixture bypasses real split | Real constructors, one datadir, mutation visibility |
| source tree to deterministic checker | Superficial text match | Positive structural assertions plus mutation suite |
| parity claim to later phases | Scope/requirement elevation | Explicit Phase 128/129 exclusions |

## STRIDE Register

| ID | Category | Disposition | Mitigation |
| --- | --- | --- | --- |
| T-127-04-01 | Spoofing | mitigate | Production-shaped identity and restart tests. |
| T-127-04-02 | Tampering | mitigate | Mutation-tested deterministic source checker. |
| T-127-04-03 | Information Disclosure | mitigate | Schema/redaction integration assertions. |
| T-127-04-04 | Elevation of Privilege | mitigate | Guard excludes later-phase and production-readiness claims. |
</threat-model>

<verification>
Run focused integration tests through the timing wrapper, parity/breadcrumb checks, mutation tests, Phase 127 checker, diff check, and final `bash scripts/verify.sh`.
</verification>

<success-criteria>
Production-path tests and deterministic guards fail on every known split-authority regression, parity evidence is auditable and bounded, LOC is fresh, and full verification passes.
</success-criteria>

<output>
After completion, create `.planning/phases/127-authoritative-network-state-unification/127-04-SUMMARY.md`.
</output>
