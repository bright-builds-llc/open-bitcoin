---
phase: 120-compact-download-timeout-and-misbehavior-runtime-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs
  - packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs
  - packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/parity/source-breadcrumbs.json
  - docs/parity/index.json
autonomous: true
requirements:
  - GOV-03
  - RCN-07
  - GOV-02
user_setup: []
must_haves:
  truths:
    - "Applying PeerAction::ReceivedBlock from compact completion clears matching volatile in_flight across all peers via on_compact_download_block_connected"
    - "Disconnect, timeout, and block-connected cleanup remove only volatile compact-relay state — never validated chainstate or durable block bytes"
    - "Parity breadcrumbs cover all new/touched first-party Rust sources for this phase; Phase 121 surfaces remain untouched"
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/action_translation.rs"
      provides: "ReceivedBlock → on_compact_download_block_connected wiring"
      contains: "on_compact_download_block_connected"
    - path: "packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs"
      provides: "GOV-03 volatile-only + ReceivedBlock multi-peer cleanup proofs"
      min_lines: 60
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "breadcrumb registry entries for Phase 120 files"
  key_links:
    - from: "process_actions PeerAction::ReceivedBlock"
      to: "PeerManager::on_compact_download_block_connected"
      via: "block_hash before/after connect_stored_block"
      pattern: "on_compact_download_block_connected"
    - from: "ReceivedBlock cleanup"
      to: "record_compact_cleanup\\(CompactDownloadCleanupCause::BlockConnected"
      via: "removed in_flight count when > 0"
      pattern: "BlockConnected"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 120-2026-07-13T20-01-34
generated_at: 2026-07-13T20:40:00.000Z
---

<objective>
Complete GOV-03 volatile cleanup by wiring `on_compact_download_block_connected` on the node `ReceivedBlock` path, prove disconnect/timeout/block-connected cleanup never touch durable chainstate, and close parity breadcrumb + Phase 121 isolation evidence for the phase.

Purpose: PeerManager `handle_block` already clears multi-peer compact slots, but compact completion emits `ReceivedBlock` via `process_actions` without that call — other peers' in-flight can linger (research Pitfall 5 / D-08).
Output: ReceivedBlock cleanup wiring, GOV-03 runtime proofs, breadcrumb registry updates, and explicit Phase 121 non-touch checks.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md

<interfaces>
From packages/open-bitcoin-node/src/network/action_translation.rs (current ReceivedBlock arm — missing multi-peer compact clear):
```rust
PeerAction::ReceivedBlock(block) => {
    maybe_block_disposition = Some(self.connect_stored_block(
        &block,
        self.next_chain_work(),
        timestamp,
        verify_flags,
        consensus_params,
    )?);
}
```

From packages/open-bitcoin-network/src/peer/compact_download_state.rs:
```rust
pub fn on_compact_download_block_connected(&mut self, block_hash: BlockHash) {
    for state in self.compact_download_states.values_mut() {
        let _ = cleanup_compact_download_on_block_connected(state, block_hash);
    }
}
```

From packages/open-bitcoin-network/src/peer/inventory_state.rs (already wired for wire Block path):
```rust
self.on_compact_download_block_connected(hash);
Ok(vec![PeerAction::ReceivedBlock(block)])
```

Locked decisions: D-07, D-08, D-09.3, D-09.4, D-10, D-11.
Cross-plan proofs may extend compact_timeout_cases / compact_misbehavior_cases from Plans 01–02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ReceivedBlock → on_compact_download_block_connected</name>
  <files>packages/open-bitcoin-node/src/network/action_translation.rs</files>
  <read_first>
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md (D-07, D-08)
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md (Pitfall 5)
    - packages/open-bitcoin-node/src/network/action_translation.rs (`ReceivedBlock` arm, `disconnect_peer_at` cleanup evidence pattern)
    - packages/open-bitcoin-network/src/peer/compact_download_state.rs (`on_compact_download_block_connected`)
    - packages/open-bitcoin-network/src/peer/inventory_state.rs (`handle_block` already calls on_compact_download_block_connected)
    - packages/open-bitcoin-node/src/network/block_relay_evidence.rs (`record_compact_cleanup`)
  </read_first>
  <behavior>
    - Before or after connect_stored_block on ReceivedBlock, call peer_manager.on_compact_download_block_connected(block_hash(&block.header))
    - If any peers had matching in_flight removed, record_compact_cleanup(BlockConnected, removed_count)
    - Does not delete chainstate entries or durable block blobs as part of compact cleanup
  </behavior>
  <action>
    1. In `process_actions` `PeerAction::ReceivedBlock(block)` arm (D-08):
       - Compute `block_hash` from the block header (use existing `block_hash` helper already used in this crate).
       - Count matching in_flight across peers for that hash (sum `compact_download_peer_state` in_flight contains_key, or count before/after).
       - Call `self.peer_manager.on_compact_download_block_connected(block_hash)`.
       - If removed_count &gt; 0, `self.record_compact_cleanup(CompactDownloadCleanupCause::BlockConnected, removed_count)`.
       - Then call existing `connect_stored_block(...)` as today (order: clear volatile first is preferred so connect failures still drop stale compact slots).
    2. Do not expand into mempool/package surfaces (Phase 119 closed). Do not touch `persist_metrics` / `block_relay_log_record`.
  </action>
  <verify>
    <automated>rg -n "on_compact_download_block_connected" packages/open-bitcoin-node/src/network/action_translation.rs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "on_compact_download_block_connected" packages/open-bitcoin-node/src/network/action_translation.rs` finds a call inside the ReceivedBlock arm
    - `rg -n "CompactDownloadCleanupCause::BlockConnected" packages/open-bitcoin-node/src/network/action_translation.rs` finds evidence recording
    - `rg -n "ReceivedBlock" -A15 packages/open-bitcoin-node/src/network/action_translation.rs` still calls `connect_stored_block`
  </acceptance_criteria>
  <done>Compact ReceivedBlock path clears multi-peer volatile in_flight and records BlockConnected cleanup when slots are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GOV-03 runtime proofs + parity breadcrumbs + Phase 121 isolation</name>
  <files>packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs, packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs, packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs, packages/open-bitcoin-node/src/network/tests.rs, docs/parity/source-breadcrumbs.json, docs/parity/index.json</files>
  <read_first>
    - packages/open-bitcoin-network/src/peer/tests.rs (`phase115_on_compact_download_block_connected_clears_matching_in_flight`)
    - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs (`phase119_package_filter_surfaces_untouched` negative pattern)
    - docs/parity/source-breadcrumbs.json (existing compact_download_state / action_translation entries)
    - scripts/check-parity-breadcrumbs.ts (contract expectations)
    - AGENTS.md Repo-Local Guidance (parity breadcrumb rules)
  </read_first>
  <behavior>
    - Two peers with in_flight for the same hash: completing compact ReceivedBlock on peer A clears peer B's matching slot via process_actions path
    - After disconnect/timeout/block-connected cleanup, MemoryChainstateStore (or equivalent) still holds previously connected blocks; only compact_download_states slots change
    - source-breadcrumbs.json lists every new Phase 120 Rust test/source file; breadcrumb check passes
    - Tests assert DurableSyncRuntime persist_metrics / block_relay_log_record files were not modified by this phase (git-based or explicit "do not call" negative — prefer asserting package/filter defaults untouched like phase119)
  </behavior>
  <action>
    1. Add `packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs` with Arrange/Act/Assert tests (D-09.3 / GOV-03):
       - Multi-peer: peer A and peer B both have compact in_flight for hash H; drive ReceivedBlock completion for H through ManagedPeerNetwork; assert both peers' in_flight lack H.
       - Disconnect cleanup: after `disconnect_peer`, peer's compact_download state is gone/empty; previously stored blocks remain queryable.
       - Timeout cleanup: after expire, in_flight empty; durable store unchanged (reinforce Plan 01 if needed).
       - Reorg/restart causes may reuse existing PeerManager cleanup helpers — assert they only clear `in_flight` maps (no store delete APIs invoked).
    2. Wire the module in `network/tests.rs`.
    3. Sweep parity breadcrumbs (D-10): ensure every new/touched first-party file under `packages/open-bitcoin-*/src` or `tests` from Plans 01–03 has file-header breadcrumbs and `docs/parity/source-breadcrumbs.json` entries anchored preferably to `net_processing.cpp` / `blockencodings.cpp` / `p2p_compactblocks.py`. Update `docs/parity/index.json` only if an intentional RCN-07/GOV-02/GOV-03 parity note is required for OB-vs-Knots Invalid→Disconnect alignment.
    4. Phase 121 isolation (D-09.4 / D-11): add an explicit test or acceptance check that package/filter/public-default surfaces remain default-off (mirror `phase119_package_filter_surfaces_untouched`), and confirm this plan does not edit `packages/open-bitcoin-node/src/sync.rs` persist_metrics / block_relay_log_record helpers. Run `bun scripts/check-parity-breadcrumbs.ts` (or repo-equivalent) and focused cargo tests.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_cleanup -- --nocapture; bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "on_compact_download_block_connected|in_flight" packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs` finds multi-peer cleanup assertions
    - `rg -n "compact_cleanup_cases" packages/open-bitcoin-node/src/network/tests.rs` finds module include
    - `rg -n "compact_timeout_cases|compact_misbehavior_cases|compact_cleanup_cases" docs/parity/source-breadcrumbs.json` finds breadcrumb registry entries
    - `rg -n "persist_metrics|block_relay_log_record" packages/open-bitcoin-node/src/network/tests/compact_cleanup_cases.rs` either absent or only appears in a negative/isolation assertion
    - `git diff --name-only` after implementation must not list Phase 121-owned metric projection edits under DurableSyncRuntime persist_metrics/block_relay_log_record (executor verifies)
  </acceptance_criteria>
  <done>GOV-03 ReceivedBlock multi-peer volatile clear is proven; breadcrumbs registered; Phase 121 / package / public-default isolation held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compact ReceivedBlock → chainstate connect | Validated block connect remains separate from volatile compact map clears |
| Cleanup cause → evidence counters | Low-cardinality Timeout/BlockConnected/PeerDisconnect counters only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-07 | Tampering | Cleanup APIs | mitigate | Only mutate compact_download_states; tests assert durable blocks remain |
| T-120-08 | Information Disclosure | Evidence counters | accept | Existing low-cardinality cleanup counters; no peer/tx payload logging in this phase |
| T-120-02 | Tampering | Multi-peer stale in_flight | mitigate | ReceivedBlock calls on_compact_download_block_connected for all peers |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_cleanup`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_timeout`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_misbehavior`
- `bun scripts/check-parity-breadcrumbs.ts`
</verification>

<success_criteria>
- GOV-03 complete: disconnect/timeout/block-connected cleanup volatile-only; ReceivedBlock clears all peers' matching slots
- RCN-07/GOV-02 proofs from prior plans still pass
- Parity breadcrumbs green; Phase 121 metrics/log projection untouched
</success_criteria>

<output>
After completion, create `.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-03-SUMMARY.md`
</output>
