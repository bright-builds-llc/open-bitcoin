---
phase: 120-compact-download-timeout-and-misbehavior-runtime-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer/compact_download_state.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - RCN-07
  - GOV-03
user_setup: []
must_haves:
  truths:
    - "ManagedPeerNetwork::expire_compact_download_timeouts is callable with an explicit now_unix_seconds clock and returns peer-targeted GetData(Block) wire messages for expired in-flight compact downloads"
    - "receive_message and receive_sync_message invoke compact-download expiry using the message timestamp so live traffic advances timeouts without a Tokio timer"
    - "Timeout expiry records CompactDownloadCleanupCause::Timeout evidence and clears only volatile compact in_flight state"
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/action_translation.rs"
      provides: "expire_compact_download_timeouts forwarder that keeps PeerAction::Send"
      contains: "expire_compact_download_timeouts"
    - path: "packages/open-bitcoin-node/src/network.rs"
      provides: "receive_* piggyback tick call-sites"
      contains: "expire_compact_download_timeouts"
    - path: "packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs"
      provides: "ManagedPeerNetwork live-path timeout→GetData proofs"
      min_lines: 80
  key_links:
    - from: "ManagedPeerNetwork::expire_compact_download_timeouts"
      to: "PeerManager::expire_compact_download_timeouts"
      via: "thin shell forwarder; no TransactionRelay-only filter_map"
      pattern: "expire_compact_download_timeouts"
    - from: "receive_message|receive_sync_message"
      to: "expire_compact_download_timeouts"
      via: "message timestamp as now_unix_seconds"
      pattern: "expire_compact_download_timeouts\\("
    - from: "expire_compact_download_timeouts"
      to: "record_compact_cleanup\\(CompactDownloadCleanupCause::Timeout"
      via: "expired in_flight count"
      pattern: "CompactDownloadCleanupCause::Timeout"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 120-2026-07-13T20-01-34
generated_at: 2026-07-13T20:40:00.000Z
---

<objective>
Wire the compact-download timeout tick from the node shell onto a live, caller-clocked path so expired in-flight reconstructions emit full-block `GetData` fallbacks (RCN-07) and record Timeout volatile cleanup evidence (GOV-03).

Purpose: Close the audit break where `PeerManager::expire_compact_download_timeouts` exists but `open-bitcoin-node` never calls it.
Output: Peer-scoped expire API, ManagedPeerNetwork forwarder, receive_* piggyback, Timeout evidence, and ManagedPeerNetwork timeout tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md
@.planning/v2.1-MILESTONE-AUDIT.md

<interfaces>
From packages/open-bitcoin-network/src/peer/compact_download_state.rs (current — must change return type):
```rust
pub fn expire_compact_download_timeouts(&mut self, now_unix_seconds: i64) -> Vec<PeerAction>
```

From packages/open-bitcoin-node/src/network/action_translation.rs (TX pattern to mirror API shape, NOT filter logic):
```rust
pub fn expire_transaction_requests(
    &mut self,
    now_unix_seconds: i64,
) -> ManagedResult<Vec<(PeerId, WireNetworkMessage)>> {
    Ok(self
        .peer_manager
        .expire_transaction_requests(now_unix_seconds)
        .into_iter()
        .filter_map(|(_peer_id, action)| match action {
            PeerAction::TransactionRelay(action) => process_transaction_relay_action(action),
            _ => None, // DO NOT copy this filter for compact — it would drop GetData
        })
        .collect())
}
```

From packages/open-bitcoin-network/src/peer/inventory_state.rs:
```rust
pub fn expire_transaction_requests(
    &mut self,
    now_unix_seconds: i64,
) -> Vec<(PeerId, PeerAction)>
```

From packages/open-bitcoin-network/src/compact_download.rs:
```rust
pub const COMPACT_BLOCK_DOWNLOAD_TIMEOUT_SECONDS: i64 = 60;
pub enum CompactDownloadCleanupCause { PeerDisconnect, Timeout, Reorg, RuntimeRestart, BlockConnected }
```

Locked decisions for this plan: D-01, D-02, D-03, D-07 (timeout volatile clear), D-09.1, partial D-09.3.
Out of scope: Phase 121 metrics/log projection, package/filter/public defaults, misbehavior escalation (Plan 02), ReceivedBlock multi-peer clear (Plan 03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Peer-scoped expire API + ManagedPeerNetwork forwarder</name>
  <files>packages/open-bitcoin-network/src/peer/compact_download_state.rs, packages/open-bitcoin-network/src/peer/tests.rs, packages/open-bitcoin-node/src/network/action_translation.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md (D-01..D-03, D-07)
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md (Pitfall 1 TX filter; Pattern 1; Open Question 3 peer-scoped pairs)
    - packages/open-bitcoin-network/src/peer/compact_download_state.rs (`expire_compact_download_timeouts`)
    - packages/open-bitcoin-node/src/network/action_translation.rs (`expire_transaction_requests`, `collect_outbound`, `record_compact_cleanup` usage in `disconnect_peer_at`)
    - packages/open-bitcoin-network/src/peer/tests.rs (`phase115_expire_compact_download_timeouts_requests_full_blocks`)
    - packages/open-bitcoin-node/src/network/block_relay_evidence.rs (`record_compact_cleanup`)
  </read_first>
  <behavior>
    - Changing PeerManager::expire_compact_download_timeouts to return Vec&lt;(PeerId, PeerAction)&gt; keeps existing GetData(Block) fallback semantics per peer whose in_flight expired
    - ManagedPeerNetwork::expire_compact_download_timeouts(now) returns Ok(Vec&lt;(PeerId, WireNetworkMessage)&gt;) containing only translated PeerAction::Send messages (never filter to TransactionRelay)
    - After expiry, expired peer in_flight maps are empty; chainstate/durable block stores are untouched
    - When N entries expire, record_compact_cleanup(CompactDownloadCleanupCause::Timeout, N) increments compact_download_timeout_count by N
  </behavior>
  <action>
    1. Change `PeerManager::expire_compact_download_timeouts` to return `Vec<(PeerId, PeerAction)>` by iterating `compact_download_states` with peer ids (same pattern as `expire_transaction_requests`). Keep using `expire_stale_compact_downloads` + `CompactDownloadAction::RequestFullBlock` → `PeerAction::Send(GetData)`. Do not change `COMPACT_BLOCK_DOWNLOAD_TIMEOUT_SECONDS` (per D-01/D-03 research).
    2. Update `phase115_expire_compact_download_timeouts_requests_full_blocks` (and any other callers) for the new return type: assert `(peer_id, PeerAction::Send(GetData(Block)))`.
    3. Add `ManagedPeerNetwork::expire_compact_download_timeouts(&amp;mut self, now_unix_seconds: i64) -&gt; ManagedResult&lt;Vec&lt;(PeerId, WireNetworkMessage)&gt;&gt;` in `action_translation.rs` per D-01:
       - Count total in_flight across peers before expire (or sum expired pair count after).
       - Call `self.peer_manager.expire_compact_download_timeouts(now_unix_seconds)`.
       - Map each `(peer_id, PeerAction::Send(msg))` → `(peer_id, msg)`; ignore non-Send arms if any appear.
       - Call `self.record_compact_cleanup(CompactDownloadCleanupCause::Timeout, expired_count)` when expired_count &gt; 0 (D-07 / GOV-03 Timeout evidence).
       - **CRITICAL:** Do not copy the TX `filter_map` that keeps only `PeerAction::TransactionRelay` — that would discard compact GetData (research Pitfall 1).
    4. If a new test module file is created under `packages/open-bitcoin-node/src/network/tests/`, add parity breadcrumbs + `docs/parity/source-breadcrumbs.json` entry anchored to `packages/bitcoin-knots/src/net_processing.cpp` (D-10 partial; Plan 03 completes remaining breadcrumb sweep).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase115_expire_compact_download_timeouts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn expire_compact_download_timeouts" packages/open-bitcoin-network/src/peer/compact_download_state.rs` shows return type `Vec<(PeerId, PeerAction)>`
    - `rg -n "fn expire_compact_download_timeouts" packages/open-bitcoin-node/src/network/action_translation.rs` finds the ManagedPeerNetwork method
    - `rg -n "TransactionRelay" packages/open-bitcoin-node/src/network/action_translation.rs` inside the new compact expire method body is absent (no TX filter reuse)
    - `rg -n "CompactDownloadCleanupCause::Timeout" packages/open-bitcoin-node/src/network/action_translation.rs` matches inside the new forwarder
  </acceptance_criteria>
  <done>PeerManager expire is peer-scoped; ManagedPeerNetwork forwarder returns GetData wire messages and records Timeout cleanup evidence without discarding Send actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: receive_* piggyback tick + live-path timeout tests</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs, packages/open-bitcoin-node/src/network/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs (`receive_message`, `receive_sync_message`)
    - packages/open-bitcoin-node/src/network/types.rs (`ManagedSyncMessageResult`)
    - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs (helpers: `compact_relay_enabled_managed_network`, handshake/sendcmpct patterns)
    - packages/open-bitcoin-network/src/lib.rs (`COMPACT_BLOCK_DOWNLOAD_TIMEOUT_SECONDS` re-export)
  </read_first>
  <behavior>
    - After processing a message, receive_message/receive_sync_message call expire_compact_download_timeouts(timestamp)
    - Same-peer expired GetData appears in outbound; other-peer expired GetData appears in receive_sync_message.targeted_outbound
    - Advancing timestamp by COMPACT_BLOCK_DOWNLOAD_TIMEOUT_SECONDS+1 after starting an in-flight compact download yields WireNetworkMessage::GetData with InventoryType::Block on the ManagedPeerNetwork path
    - block_relay_evidence_status compact_download_timeout_count increases after live expiry
  </behavior>
  <action>
    1. In `receive_message` and `receive_sync_message`, after the primary message → `process_actions` path completes successfully, call `self.expire_compact_download_timeouts(timestamp)?` (D-02). Merge results:
       - For `receive_sync_message`: push `(peer_id, msg)` into `outbound` when `peer_id` matches the receiving peer; otherwise push into `targeted_outbound`.
       - For `receive_message`: append same-peer messages onto the returned `outbound` Vec; push other-peer pairs into a path that does not drop them — prefer extending via the sync-result shape if you refactor the helper, or call expire and for other peers leave them only in the public API while still expiring all peers through the shared forwarder invoked here (document choice in SUMMARY). Prefer merging into `ManagedSyncMessageResult` when touching `receive_sync_message` (primary DurableSyncRuntime caller).
    2. Do not add a Tokio timer or DurableSyncRuntime-only metrics hook (D-01/D-02). Do not touch `persist_metrics` / `block_relay_log_record` (Phase 121).
    3. Create `packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs` and wire it from `network/tests.rs` mod list. Tests (Arrange/Act/Assert, one concern each) must prove D-09.1:
       - Public `expire_compact_download_timeouts` after starting compact in-flight returns GetData(Block) for that peer and clears in_flight.
       - `receive_sync_message` (or `receive_message`) with timestamp past timeout emits GetData on the live path and increments Timeout cleanup evidence.
       - Negative: durable chain tip / stored block count unchanged after timeout cleanup (volatile-only, D-07).
    4. Add parity breadcrumbs on the new test file + `docs/parity/source-breadcrumbs.json` entry (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_timeout -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "expire_compact_download_timeouts" packages/open-bitcoin-node/src/network.rs` shows call sites in both `receive_message` and `receive_sync_message`
    - `rg -n "compact_timeout_cases" packages/open-bitcoin-node/src/network/tests.rs` finds the module include
    - `rg -n "COMPACT_BLOCK_DOWNLOAD_TIMEOUT_SECONDS" packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs` finds clock-advance usage
    - `rg -n "compact_download_timeout_count|CompactDownloadCleanupCause::Timeout" packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs` finds Timeout evidence assertion
    - `rg -n "persist_metrics|block_relay_log_record" packages/open-bitcoin-node/src/network/tests/compact_timeout_cases.rs` returns no matches
  </acceptance_criteria>
  <done>Live receive path advances compact timeouts with the message clock; ManagedPeerNetwork tests prove GetData fallback + Timeout evidence without Phase 121 surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Peer wire → ManagedPeerNetwork receive_* | Untrusted peer messages supply timestamp used as expiry clock; expiry decisions stay in PeerManager pure core |
| Shell expire forwarder → outbound GetData | Effectful send of full-block fetch after volatile slot clear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Denial of Service | Stale compact in_flight pinning | mitigate | Deterministic expire clears volatile slots and requests full block via GetData |
| T-120-02 | Tampering | Timeout cleanup path | mitigate | Cleanup touches only compact_download_states in_flight; tests assert chainstate/durable blocks untouched |
| T-120-03 | Elevation of Privilege | Clock injection | accept | Caller-supplied now is intentional deterministic contract (same as TX expire); no ambient timer trust |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase115_expire_compact_download_timeouts`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_timeout`
- Confirm no edits under DurableSyncRuntime persist_metrics / block_relay_log_record
</verification>

<success_criteria>
- RCN-07 timeout→fallback is reachable on ManagedPeerNetwork live path
- GOV-03 Timeout cleanup evidence increments and only volatile compact state is cleared
- Public expire API mirrors TX clock injection without copying TX action filter
</success_criteria>

<output>
After completion, create `.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-01-SUMMARY.md`
</output>
