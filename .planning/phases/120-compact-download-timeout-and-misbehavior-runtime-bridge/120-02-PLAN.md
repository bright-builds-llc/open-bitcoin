---
phase: 120-compact-download-timeout-and-misbehavior-runtime-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-network/src/error.rs
  - packages/open-bitcoin-network/src/compact_download.rs
  - packages/open-bitcoin-network/src/peer/compact_download_state.rs
  - packages/open-bitcoin-network/src/compact_download/tests.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/peer_policy.rs
  - packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - GOV-02
user_setup: []
must_haves:
  truths:
    - "Typed CompactBlockTxnHandleOutcome::Misbehavior and UnexpectedBlockHash produce non-empty PeerAction::Disconnect (not empty Vec silence)"
    - "NoMatchingInFlight remains empty-action suppress matching Knots ignore of unexpected blocktxn"
    - "Invalid compact reconstruction at init escalates to disconnect/misbehavior instead of Fallback-only; ShortIdCollision/BucketOverload Failed stays Fallback GetData"
    - "ManagedPeerNetwork live path yields disconnect/score evidence for duplicate blocktxn, OOB indexes, and invalid compact headers"
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/compact_download_state.rs"
      provides: "escalating compact_block_txn_actions + init Invalid mapping"
      contains: "PeerAction::Disconnect"
    - path: "packages/open-bitcoin-network/src/compact_download.rs"
      provides: "Invalid init outcome no longer silent Fallback-only"
      contains: "CompactReconstructionOutcome::Invalid"
    - path: "packages/open-bitcoin-network/src/error.rs"
      provides: "compact-aligned DisconnectReason variant"
      contains: "DisconnectReason"
    - path: "packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs"
      provides: "GOV-02 runtime proofs beyond silent suppress"
      min_lines: 80
  key_links:
    - from: "compact_block_txn_actions"
      to: "PeerAction::Disconnect"
      via: "Misbehavior + UnexpectedBlockHash arms"
      pattern: "Misbehavior\\(_\\)|UnexpectedBlockHash"
    - from: "init_compact_block_download Invalid"
      to: "Disconnect or Misbehavior init outcome"
      via: "not Fallback"
      pattern: "CompactReconstructionOutcome::Invalid"
    - from: "NoMatchingInFlight"
      to: "Vec::new()"
      via: "benign suppress only"
      pattern: "NoMatchingInFlight =>"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 120-2026-07-13T20-01-34
generated_at: 2026-07-13T20:40:00.000Z
---

<objective>
Escalate typed compact misbehavior beyond silent empty-action suppress into Knots-aligned disconnect / peer-policy decisions (GOV-02), while keeping true NoMatchingInFlight suppressible and collision Failed as full-block Fallback.

Purpose: Close the audit break where `compact_block_txn_actions` maps Misbehavior to `Vec::new()`.
Output: DisconnectReason + init/txn action rewrites, peer-policy recording on compact disconnect, and runtime misbehavior proofs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md
@.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md

<interfaces>
From packages/open-bitcoin-network/src/peer/compact_download_state.rs (current gap):
```rust
fn compact_block_txn_actions(outcome: CompactBlockTxnHandleOutcome) -> Vec<PeerAction> {
    match outcome {
        CompactBlockTxnHandleOutcome::Progress { actions } => {
            compact_download_actions_to_peer_actions(actions)
        }
        CompactBlockTxnHandleOutcome::Misbehavior(_)
        | CompactBlockTxnHandleOutcome::UnexpectedBlockHash
        | CompactBlockTxnHandleOutcome::NoMatchingInFlight => Vec::new(),
    }
}
```

From packages/open-bitcoin-network/src/compact_download.rs (current Invalid arm — must change):
```rust
CompactReconstructionOutcome::Invalid(_) => {
    CompactBlockInitOutcome::Fallback(FullBlockFetch { block_hash })
}
```

From packages/open-bitcoin-network/src/compact_reconstruction.rs:
```rust
pub enum CompactBlockTxnMisbehavior {
    UnexpectedBlockHash,
    DuplicateResponse,
    OutOfBoundsIndex,
    TooManyTransactions,
    NotInitialized,
}
```

From packages/open-bitcoin-network/src/peer_policy.rs:
```rust
pub enum MisbehaviorKind { MalformedMessage, ..., HeaderViolation, ... }
pub enum MisbehaviorResponse { ObserveOnly, Disconnect, Discourage, Ban, ProtectedNoAction }
```

Recommended mapping table (discretion, Knots-aligned — implement exactly):
| Outcome | Action |
| NoMatchingInFlight | empty Vec |
| DuplicateResponse / OutOfBoundsIndex / TooManyTransactions / NotInitialized | Disconnect + MisbehaviorKind::MalformedMessage |
| UnexpectedBlockHash | Disconnect + MisbehaviorKind::MalformedMessage |
| Init Invalid(*) | Disconnect + MisbehaviorKind::HeaderViolation or MalformedMessage (NOT Fallback) |
| Init Failed(ShortIdCollision\|BucketOverload) | keep Fallback GetData |

Locked decisions: D-04, D-05, D-06, D-09.2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escalate compact_block_*_actions + Invalid init</name>
  <files>packages/open-bitcoin-network/src/error.rs, packages/open-bitcoin-network/src/compact_download.rs, packages/open-bitcoin-network/src/peer/compact_download_state.rs, packages/open-bitcoin-network/src/compact_download/tests.rs, packages/open-bitcoin-network/src/peer/tests.rs, packages/open-bitcoin-node/src/network/inventory.rs</files>
  <read_first>
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-CONTEXT.md (D-04..D-06)
    - .planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-RESEARCH.md (Pattern 2–3, Pitfalls 3–4/6, mapping table)
    - packages/open-bitcoin-network/src/peer/compact_download_state.rs (`compact_block_txn_actions`, `compact_block_init_actions`)
    - packages/open-bitcoin-network/src/compact_download.rs (`init_compact_block_download` Invalid/Failed arms, `CompactBlockInitOutcome`)
    - packages/open-bitcoin-network/src/error.rs (`DisconnectReason`)
    - packages/open-bitcoin-node/src/network/inventory.rs (`disconnect_network_error`)
    - packages/bitcoin-knots/src/net_processing.cpp (ProcessCompactBlockTxns ignore vs Misbehaving; InitData invalid)
  </read_first>
  <behavior>
    - compact_block_txn_actions(Misbehavior(_)) and UnexpectedBlockHash return vec![PeerAction::Disconnect(CompactBlockMisbehavior)] (name may vary) — never empty
    - compact_block_txn_actions(NoMatchingInFlight) returns empty Vec
    - init Invalid maps to disconnect/misbehavior outcome, not Fallback
    - init Failed(ShortIdCollision|BucketOverload) still Fallback GetData
    - Existing phase115 tests that assumed silent Misbehavior are updated to expect Disconnect
  </behavior>
  <action>
    1. Add `DisconnectReason::CompactBlockMisbehavior` (exact variant name OK if synonymous) in `error.rs` with Display text suitable for diagnostics. Do **not** overload `ResourceLimit` (research Pitfall 6). Add matching `NetworkError::CompactBlockMisbehavior(PeerId)` (or equivalent) and update `disconnect_network_error` in `inventory.rs` exhaustively.
    2. Extend `CompactBlockInitOutcome` with a misbehavior arm carrying `CompactReconstructionInvalidReason` (or a dedicated unit variant). Change `CompactReconstructionOutcome::Invalid(_) =>` from `Fallback` to that misbehavior arm (D-05 / GOV-02 malformed/invalid headers). Keep `Failed(ShortIdCollision|BucketOverload)` as `Fallback` (Knots READ_STATUS_FAILED → getdata).
    3. Rewrite `compact_block_init_actions` so the new Invalid/misbehavior arm emits `PeerAction::Disconnect(DisconnectReason::CompactBlockMisbehavior)` (and does not emit GetData for Invalid).
    4. Rewrite `compact_block_txn_actions` per D-04/D-06:
       - `Progress` → existing action translation
       - `Misbehavior(_)` → `Disconnect(CompactBlockMisbehavior)`
       - `UnexpectedBlockHash` → `Disconnect(CompactBlockMisbehavior)`
       - `NoMatchingInFlight` → `Vec::new()` only
    5. Update unit tests in `compact_download/tests.rs` and `peer/tests.rs` that currently expect silent empty or Invalid→Fallback. Add focused tests for DuplicateResponse, OutOfBoundsIndex, UnexpectedBlockHash → Disconnect; NoMatchingInFlight → empty; Invalid init → Disconnect; Failed collision → Fallback GetData.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compact_block -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "NoMatchingInFlight =>" -A2 packages/open-bitcoin-network/src/peer/compact_download_state.rs` shows empty Vec only for that arm
    - `rg -n "Misbehavior\(_\)|UnexpectedBlockHash" packages/open-bitcoin-network/src/peer/compact_download_state.rs` no longer share a single arm with `NoMatchingInFlight` that returns `Vec::new()`
    - `rg -n "CompactReconstructionOutcome::Invalid" -A3 packages/open-bitcoin-network/src/compact_download.rs` does not map Invalid to `Fallback`
    - `rg -n "CompactBlockMisbehavior" packages/open-bitcoin-network/src/error.rs packages/open-bitcoin-node/src/network/inventory.rs` finds DisconnectReason + NetworkError mapping
    - `rg -n "ShortIdCollision|ShortIdBucketOverload" packages/open-bitcoin-network/src/compact_download.rs` still maps Failed collisions to Fallback
  </acceptance_criteria>
  <done>Typed compact misbehavior and Invalid init escalate to Disconnect; NoMatchingInFlight stays silent; collision Failed stays Fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Peer-policy recording + ManagedPeerNetwork GOV-02 proofs</name>
  <files>packages/open-bitcoin-node/src/network/action_translation.rs, packages/open-bitcoin-node/src/network/peer_policy.rs, packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs, packages/open-bitcoin-node/src/network/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-node/src/network/action_translation.rs (`PeerAction::Disconnect` arm in `process_actions`)
    - packages/open-bitcoin-node/src/network/peer_policy.rs (`record_peer_policy_misbehavior`)
    - packages/open-bitcoin-node/src/network/tests.rs (existing MisbehaviorDecision recording test ~1675)
    - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs (live compact helpers)
    - packages/open-bitcoin-network/src/peer_policy.rs (`MisbehaviorKind`, `MisbehaviorPolicy`, defaults)
  </read_first>
  <behavior>
    - When process_actions applies Disconnect(CompactBlockMisbehavior), peer is removed and optional peer-policy MisbehaviorDecision is recorded with MalformedMessage or HeaderViolation (points ≥ discourage_threshold or explicit Disconnect response)
    - Live receive of duplicate blocktxn / OOB / invalid compact yields Err disconnect path — not Ok with empty outbound silence
    - Package/filter/public-default surfaces remain untouched (negative assertion OK)
  </behavior>
  <action>
    1. In `process_actions` Disconnect arm (or a small helper), when reason is `CompactBlockMisbehavior`, call `record_peer_policy_misbehavior` with a `MisbehaviorDecision` using `MisbehaviorKind::MalformedMessage` (blocktxn faults) or `HeaderViolation` (invalid header init) and `MisbehaviorResponse::Disconnect` or `Discourage` with points ≥ `discourage_threshold` (default 50) per research mapping / D-04 discretion. Keep SelfConnection special-case intact.
    2. Add `packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs` proving D-09.2 / GOV-02:
       - Duplicate `blocktxn` on live receive → disconnect error (non-empty escalation), not silent Ok.
       - Out-of-bounds indexes → disconnect.
       - Invalid compact header / NullHeader-style init → disconnect (not GetData Fallback).
       - NoMatchingInFlight stray blocktxn → Ok with no disconnect.
       - Short-id collision Failed path still returns GetData Fallback (regression guard).
    3. Wire module in `network/tests.rs`. Add parity breadcrumbs + `docs/parity/source-breadcrumbs.json` (D-10).
    4. Do not touch DurableSyncRuntime metrics/log projection or package/filter serving (D-11).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_misbehavior -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "CompactBlockMisbehavior" packages/open-bitcoin-node/src/network/action_translation.rs` finds Disconnect handling
    - `rg -n "record_peer_policy_misbehavior" packages/open-bitcoin-node/src/network/action_translation.rs` finds compact-path recording (or clearly documented equivalent bridge)
    - `rg -n "compact_misbehavior_cases" packages/open-bitcoin-node/src/network/tests.rs` finds module include
    - `rg -n "NoMatchingInFlight|stray|unexpected" packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs` finds benign-suppress test
    - `rg -n "persist_metrics|block_relay_log_record" packages/open-bitcoin-node/src/network/tests/compact_misbehavior_cases.rs` returns no matches
  </acceptance_criteria>
  <done>GOV-02 cases escalate on the ManagedPeerNetwork live path with peer-policy evidence; benign no-match stays suppressible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted compact/blocktxn payloads → PeerManager handlers | Malformed/duplicate/OOB classified in pure core |
| PeerAction::Disconnect → ManagedPeerNetwork | Shell applies disconnect + policy recording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-04 | Tampering / DoS | Malformed cmpctblock / blocktxn | mitigate | Escalate to Disconnect + MisbehaviorKind; clear volatile in_flight in handlers |
| T-120-05 | Denial of Service | Unexpected blocktxn spam | mitigate | Keep NoMatchingInFlight silent (Knots ignore) to avoid disconnect amplification |
| T-120-06 | Spoofing | Peer-policy scoring | accept | Existing MisbehaviorPolicy protected-peer rules apply; no new ban book |
</threat_model>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network compact_block`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node compact_misbehavior`
</verification>

<success_criteria>
- GOV-02 malformed/invalid/duplicate/unexpected/OOB paths produce disconnect or explicit suppression — not silence-only for typed Misbehavior
- NoMatchingInFlight remains suppressible
- Invalid init no longer Fallback-only; collision Failed remains Fallback
</success_criteria>

<output>
After completion, create `.planning/phases/120-compact-download-timeout-and-misbehavior-runtime-bridge/120-02-SUMMARY.md`
</output>
