---
phase: 139-coins-view-cache-contract-and-engine-apply
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-chainstate/src/coins.rs
  - packages/open-bitcoin-chainstate/src/coins/cache.rs
  - packages/open-bitcoin-chainstate/src/coins/memory.rs
  - packages/open-bitcoin-chainstate/src/coins/tests.rs
  - packages/open-bitcoin-chainstate/src/error.rs
  - packages/open-bitcoin-chainstate/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - CACHE-01
user_setup: []
must_haves:
  truths:
    - "A contributor can construct MemoryCoinsView parent truth and a resident CoinsCache overlay without cloning a live UTXO HashMap on Chainstate."
    - "Map occupancy, have_coin_in_cache, peek have_coin/get_coin, and parent-view truth are four distinguishable facts."
    - "A spent DIRTY tombstone is a cache hit that answers have_coin = false without consulting the parent."
    - "Illegal DIRTY/FRESH/spent combinations cannot be constructed as silent states."
    - "open-bitcoin-chainstate stays I/O-free: no Fjall, std::fs, Tokio, or wall clock in the new coins modules."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/coins.rs
      provides: "CoinsView trait, CoinsBatch, CoinsCacheEntry, five-state flags"
      contains: "pub trait CoinsView"
    - path: packages/open-bitcoin-chainstate/src/coins/cache.rs
      provides: "Resident CoinsCache overlay with peek/fetch lookup"
      contains: "pub struct CoinsCache"
    - path: packages/open-bitcoin-chainstate/src/coins/memory.rs
      provides: "In-memory parent truth"
      contains: "pub struct MemoryCoinsView"
    - path: packages/open-bitcoin-chainstate/src/coins/tests.rs
      provides: "Four-fact and five-state unit tests"
      contains: "spent_dirty_tombstone_is_occupancy_hit_with_have_coin_false"
  key_links:
    - from: packages/open-bitcoin-chainstate/src/coins/cache.rs
      to: packages/open-bitcoin-chainstate/src/coins.rs
      via: "CoinsCache implements peek-only CoinsView"
      pattern: "impl<V: CoinsView> CoinsView for CoinsCache<V>"
    - from: packages/open-bitcoin-chainstate/src/lib.rs
      to: packages/open-bitcoin-chainstate/src/coins.rs
      via: "pub mod coins plus re-exports"
      pattern: "pub use coins::"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 139-2026-08-30T15-42-10
generated_at: 2026-08-30T16:35:00.000Z
---

# Phase 139 Plan 01: CoinsView Contract and Lookup Facts

<objective>
Create the Knots-shaped coins view/cache types and prove the four lookup facts plus five valid DIRTY/FRESH states.

Purpose: CACHE-01 starts with a typed overlay. Later plans cannot retarget engine apply until occupancy, HaveCoin, in-cache unspent, and parent truth are distinct (D-01 through D-11).

Output: `coins.rs` + `coins/cache.rs` + `coins/memory.rs` + failing-then-green lookup tests, error variants, and parity breadcrumbs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-coins-view-cache-contract-and-engine-apply/139-CONTEXT.md
@.planning/phases/139-coins-view-cache-contract-and-engine-apply/139-RESEARCH.md
@packages/open-bitcoin-chainstate/src/lib.rs
@packages/open-bitcoin-chainstate/src/error.rs
@packages/open-bitcoin-chainstate/src/types.rs
@docs/parity/source-breadcrumbs.json
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-chainstate/src/types.rs — do not move spentness onto Coin (D-10):
```rust
pub struct Coin {
    pub output: TransactionOutput,
    pub is_coinbase: bool,
    pub created_height: u32,
    pub created_median_time_past: i64,
}
```

From packages/open-bitcoin-chainstate/src/error.rs — extend this enum; do not add CoinsError:
```rust
pub enum ChainstateError {
    MissingCoin { outpoint: OutPoint },
    OutputOverwrite { outpoint: OutPoint },
    RestoredCoinOverwrite { outpoint: OutPoint },
    // ...existing variants...
}
```

Target contracts this plan must create (executor writes these exact names):
```rust
pub trait CoinsView {
    fn get_coin(&self, outpoint: &OutPoint) -> Result<Option<Coin>, ChainstateError>;
    fn have_coin(&self, outpoint: &OutPoint) -> Result<bool, ChainstateError>;
    fn best_block(&self) -> Option<BlockHash>;
    fn batch_write(
        &mut self,
        writes: CoinsBatch,
        maybe_best_block: Option<BlockHash>,
    ) -> Result<(), ChainstateError>;
}

pub struct CoinsBatch {
    pub entries: HashMap<OutPoint, CoinsCacheEntry>,
}

pub enum CoinsCacheFlags {
    UnspentClean,
    UnspentDirty,
    UnspentFreshDirty,
    SpentDirty,
    SpentFresh,
}

pub struct CoinsCacheEntry { /* maybe_coin + flags; no Clone bound required */ }

pub struct MemoryCoinsView { /* HashMap<OutPoint, Coin> + maybe_best_block */ }

pub struct CoinsCache<V: CoinsView> { /* parent: V + overlay + maybe_best_block; NOT Clone */ }
```

Knots SanityCheck valid attr masks (comment + tests only; use the five-state enum, not bitflags crate) per D-11:
- 0 unspent clean
- 1 unspent DIRTY
- 3 unspent FRESH|DIRTY
- 5 spent DIRTY
- 6 spent FRESH-not-DIRTY
Invalid: 2 FRESH-only unspent, 4 spent clean, 7 spent FRESH|DIRTY.
</interfaces>
</context>

<feature>
  <name>Coins view lookup contract</name>
  <files>packages/open-bitcoin-chainstate/src/coins.rs, packages/open-bitcoin-chainstate/src/coins/cache.rs, packages/open-bitcoin-chainstate/src/coins/memory.rs, packages/open-bitcoin-chainstate/src/coins/tests.rs</files>
  <behavior>
    MemoryCoinsView stores only live unspent Coin values. CoinsCache overlays DIRTY/FRESH entries. Trait get_coin/have_coin never insert. fetch_coin inserts only into self. Spent DIRTY occupancy is a hit with have_coin false.
  </behavior>
  <implementation>
    Five-state enum for flags. Peek-only CoinsView on both MemoryCoinsView and CoinsCache. No add_coin/spend_coin/Flush yet — those are Plan 02.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing lookup, five-state, and MemoryCoinsView tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/coins/cache.rs
    packages/open-bitcoin-chainstate/src/coins/memory.rs
    packages/open-bitcoin-chainstate/src/coins/tests.rs
    packages/open-bitcoin-chainstate/src/error.rs
    packages/open-bitcoin-chainstate/src/lib.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    .planning/phases/139-coins-view-cache-contract-and-engine-apply/139-CONTEXT.md
    .planning/phases/139-coins-view-cache-contract-and-engine-apply/139-RESEARCH.md
    packages/open-bitcoin-chainstate/src/lib.rs
    packages/open-bitcoin-chainstate/src/error.rs
    packages/open-bitcoin-chainstate/src/types.rs
    packages/bitcoin-knots/src/coins.h
    packages/bitcoin-knots/src/coins.cpp
    docs/parity/source-breadcrumbs.json
    standards/core/testing.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    - Test `memory_coins_view_round_trips_unspent_coins_and_best_block`: MemoryCoinsView::from_coins inserts one Coin; get_coin returns it; have_coin is true; best_block matches; batch_write of a spent entry deletes the key; parent miss returns Ok(None), never MissingCoin.
    - Test `coins_cache_entry_constructs_exactly_the_five_valid_knots_states`: constructors succeed for UnspentClean, UnspentDirty, UnspentFreshDirty, SpentDirty, SpentFresh; spent entries expose maybe_coin() == None; unspent entries expose the Coin.
    - Test `coins_cache_entry_rejects_fresh_only_unspent_spent_clean_and_spent_fresh_dirty`: CoinsCacheEntry::try_from_flags returns ChainstateError::InvalidCacheEntry for (fresh=true, dirty=false, spent=false), (fresh=false, dirty=false, spent=true), and (fresh=true, dirty=true, spent=true).
    - Test `four_lookup_facts_are_distinct_for_spent_dirty_tombstone`: insert a SpentDirty entry via pub(crate) insert_entry_for_test; contains_in_cache is true; have_coin_in_cache is false; CoinsView::have_coin/get_coin on the cache are false/None without the parent map gaining the outpoint; parent.get_coin remains the pre-insert parent truth (Some or None as arranged).
    - Test `fetch_coin_populates_self_only_from_parent_peek`: parent holds an unspent Coin; cache overlay empty; fetch_coin returns the Coin and have_coin_in_cache becomes true; a second MemoryCoinsView used as a spy parent is not required — assert cache.contains_in_cache and parent map unchanged in key set.
    - Test `coins_view_get_coin_does_not_insert_on_parent_miss`: peek get_coin on empty overlay + empty parent returns Ok(None) and contains_in_cache stays false.
    - Test `coin_type_has_no_spent_flag`: grep-level in test by constructing Coin { output, is_coinbase, created_height, created_median_time_past } only — no is_spent field.
  </behavior>
  <action>
    Create the module skeleton so tests compile and fail on behavior (RED):

    1. Add `pub mod coins;` to `packages/open-bitcoin-chainstate/src/lib.rs` and re-export `CoinsView`, `CoinsCache`, `CoinsCacheEntry`, `CoinsCacheFlags`, `CoinsBatch`, `MemoryCoinsView`.
    2. Create `coins.rs` with `mod cache; mod memory; mod tests;` (tests behind `#[cfg(test)]`). Prefer `coins.rs` + `coins/` over `coins/mod.rs` (Rust standard). Copy this breadcrumb block onto every new first-party Rust source (`coins.rs`, `coins/cache.rs`, `coins/memory.rs`, `coins/tests.rs`):

    ```
    // Parity breadcrumbs:
    // - packages/bitcoin-knots/src/coins.h
    // - packages/bitcoin-knots/src/coins.cpp
    ```

    3. Append those four paths to the existing `chainstate-engine` `files` array in `docs/parity/source-breadcrumbs.json` immediately after `packages/open-bitcoin-chainstate/src/engine.rs`. Do not create a new breadcrumb group. Then run `bun run scripts/check-parity-breadcrumbs.ts --write` if the checker requires generated comment refresh.
    4. Add `ChainstateError::InvalidCacheEntry { dirty: bool, fresh: bool, spent: bool }` and `ChainstateError::FreshFlagMisapplied { outpoint: OutPoint }`. Extend Display and `display_messages_cover_every_error_variant` in `error.rs` tests. FreshFlagMisapplied is unused until Plan 02 — still add the variant now so Plan 02 does not reshape the error enum.
    5. Declare the trait, types, and stub methods named in `<interfaces>`. `CoinsCache` must not derive or impl `Clone` (D-04). Do not impl `CCoinsViewBacked`, linked-list sentinel, `CCoinsViewMemPool`, dual `CoinsViews`, assumeutxo types, or any production cursor trait method (D-02, D-06). Do not add `coins/flush.rs`, Fjall, `std::fs`, Tokio, `Instant`, or `std::time` (D-07).
    6. Write the tests in `coins/tests.rs` with Arrange / Act / Assert comments. Use a fixture OutPoint `{ txid: Txid::from_byte_array([1_u8; 32]), vout: 0 }` and a 50-sat P2SH-like `script_pubkey` of `[0x51]`.
    7. Provide `CoinsCache::insert_entry_for_test` as `pub(crate)` so lookup tests can install a SpentDirty tombstone without add_coin (Plan 02 owns add/spend).
    8. Do not change `engine.rs`, `Chainstate`, or node prepare in this plan. Do not add Cargo dependencies. Do not use `unwrap()` in production code.

    RED: the named tests must compile and fail (stubs return todo-equivalent `Err` or empty) until Task 2. If a stub `todo!()` is needed, keep it only in test-cfg-excluded production paths that Task 2 immediately replaces — prefer returning a dedicated placeholder `Err(ChainstateError::Serialization { context: "coins stub", reason: "not implemented".into() })` so `deny(clippy::unimplemented)` stays clean.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase139-01-coins-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-chainstate/src/lib.rs` contains `pub mod coins;` and `pub use coins::`
    - `packages/open-bitcoin-chainstate/src/coins.rs` contains `pub trait CoinsView` with `fn get_coin`, `fn have_coin`, `fn best_block`, `fn batch_write`
    - `packages/open-bitcoin-chainstate/src/coins.rs` does not contain `CCoinsViewBacked`, `CoinsViewMemPool`, `CoinsViews`, `Cursor`, `fjall`, `std::fs`, `tokio`, or `Instant`
    - `packages/open-bitcoin-chainstate/src/error.rs` contains `InvalidCacheEntry` and `FreshFlagMisapplied`
    - `docs/parity/source-breadcrumbs.json` `chainstate-engine` files list includes `packages/open-bitcoin-chainstate/src/coins.rs`, `coins/cache.rs`, `coins/memory.rs`, and `coins/tests.rs`
    - `packages/open-bitcoin-chainstate/src/coins/tests.rs` contains `spent_dirty_tombstone_is_occupancy_hit_with_have_coin_false` and `coins_cache_entry_rejects_fresh_only_unspent_spent_clean_and_spent_fresh_dirty`
    - Task 1 cargo test for `coins::` is RED (failures, not compile errors) before Task 2
  </acceptance_criteria>
  <done>Failing coins lookup/five-state tests exist against the named public types; breadcrumbs and error variants are registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MemoryCoinsView, CoinsCacheEntry, and peek/fetch lookup</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/coins/cache.rs
    packages/open-bitcoin-chainstate/src/coins/memory.rs
    packages/open-bitcoin-chainstate/src/error.rs
  </files>
  <read_first>
    packages/open-bitcoin-chainstate/src/coins.rs
    packages/open-bitcoin-chainstate/src/coins/tests.rs
    packages/open-bitcoin-chainstate/src/coins/cache.rs
    packages/open-bitcoin-chainstate/src/coins/memory.rs
    packages/bitcoin-knots/src/coins.cpp
    standards/core/architecture.md
    standards/core/code-shape.md
  </read_first>
  <behavior>
    Same tests as Task 1 must pass. Additional GREEN assertions already in those tests: parent miss is Ok(None); fetch inserts UnspentClean into self only; peek never inserts.
  </behavior>
  <action>
    Implement (GREEN) per D-01, D-03, D-08, D-09, D-10, D-11:

    **CoinsCacheFlags / CoinsCacheEntry (D-10, D-11):** Use the five-state enum (not a bitflags crate). `maybe_coin: Option<Coin>` is Some iff the variant is unspent. Infallible constructors: `unspent_clean`, `unspent_dirty`, `unspent_fresh_dirty`, `spent_dirty`, `spent_fresh`. `try_from_flags(maybe_coin, dirty, fresh)` maps the three invalid combos to `InvalidCacheEntry`. Do not store spentness on `Coin`.

    **MemoryCoinsView (D-03):** Fields `coins: HashMap<OutPoint, Coin>` and `maybe_best_block: Option<BlockHash>`. `from_coins(coins, maybe_best_block)`. Trait `get_coin` clones the HashMap value or returns None. `have_coin` is `contains_key` on this parent map (parent stores unspent only). `batch_write`: for each entry, Some(coin) inserts, None removes; if `maybe_best_block` argument is Some, store it. No DIRTY/FRESH flags on the parent.

    **CoinsCache&lt;V: CoinsView&gt; (D-01, D-04, D-08, D-09):** Fields `parent: V`, `overlay: HashMap<OutPoint, CoinsCacheEntry>`, `maybe_best_block: Option<BlockHash>`. `from_parent(parent: V)` starts with empty overlay and `maybe_best_block = parent.best_block()`. Do not derive/impl Clone. Implement:
    - `contains_in_cache(&self, outpoint) -> bool` — overlay occupancy including spent tombstones; no parent call.
    - `have_coin_in_cache(&self, outpoint) -> bool` — overlay unspent only; no parent call (Knots HaveCoinInCache).
    - `CoinsView::get_coin` / `have_coin` — if overlay occupancy: return `maybe_coin` / `maybe_coin.is_some()` and do not call parent (D-09). Else call `parent.get_coin` / `parent.have_coin`. Never insert.
    - `fetch_coin(&mut self, outpoint) -> Result<Option<&CoinsCacheEntry>, ChainstateError>` — if overlay occupancy, return existing. Else `parent.get_coin` (peek). On Some(coin), insert `unspent_clean(coin)` and return it. On None, do not leave a vacant occupancy; return None.
    - `insert_entry_for_test` for Task 1 tombstone fixtures.
    - `parent(&self) -> &V` for tests that assert parent truth is unchanged.
    - `best_block` prefers overlay `maybe_best_block` if set, else parent.

    Keep `add_coin`, `spend_coin`, `flush`, `sync`, and `CoinsOverlay` out of this plan (Plan 02). Do not retarget `Chainstate.utxos`. Do not use `unwrap()`. Keep each new production file under 628 physical lines.

    After GREEN: `rg -n "derive\\(.*, *Clone|impl Clone for CoinsCache" packages/open-bitcoin-chainstate/src/coins` must show no Clone on CoinsCache.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase139-01-coins-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p open-bitcoin-chainstate --lib coins::` exits 0
    - `packages/open-bitcoin-chainstate/src/coins/cache.rs` contains `pub struct CoinsCache` and does not contain `#[derive(` with `Clone` on CoinsCache
    - `packages/open-bitcoin-chainstate/src/coins/cache.rs` contains `fn contains_in_cache`, `fn have_coin_in_cache`, and `fn fetch_coin`
    - `packages/open-bitcoin-chainstate/src/coins/memory.rs` contains `pub struct MemoryCoinsView` and `impl CoinsView for MemoryCoinsView`
    - `packages/open-bitcoin-chainstate/src/types.rs` `struct Coin` still has exactly the four fields `output`, `is_coinbase`, `created_height`, `created_median_time_past`
    - `rg -n "fjall|std::fs|tokio|Instant" packages/open-bitcoin-chainstate/src/coins` prints nothing
    - `bash scripts/check-pure-core-deps.sh` exits 0
  </acceptance_criteria>
  <done>Lookup contract is green: four facts, five valid states, invalid construction errors, peek-only trait reads, fetch-into-self only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache overlay → parent view | Untrusted/stale occupancy must not resurrect a spent outpoint from parent truth. |
| Constructor → CoinsCacheEntry | Invalid DIRTY/FRESH/spent combos must not enter apply logic as silent states. |
| Pure-core crate → I/O | Fjall, filesystem, Tokio, or clocks in chainstate would break the functional-core boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Tampering | `CoinsCache::get_coin` / occupancy | mitigate | Spent overlay occupancy returns have_coin=false and must not call `parent.get_coin` (D-09). Unit test `spent_dirty_tombstone_is_occupancy_hit_with_have_coin_false`. |
| T-139-02 | Tampering | `CoinsCacheEntry::try_from_flags` | mitigate | Invalid combos return `InvalidCacheEntry`; five-state enum makes FRESH-only unspent / spent-clean / spent-FRESH\|DIRTY unrepresentable in infallible constructors (D-11). |
| T-139-03 | Elevation of Privilege | `open-bitcoin-chainstate` coins modules | mitigate | No Fjall/`std::fs`/Tokio/`Instant` imports (D-07). `check-pure-core-deps.sh` after edits. |
| T-139-04 | Information Disclosure | cache occupancy | accept | In-memory fixture coins only; no operator durability copy in this plan (D-23 owned by Plan 04). |
| T-139-05 | Spoofing | `CoinsView::get_coin` | mitigate | Trait methods never insert; fetch_coin is `&mut self` only. Test `coins_view_get_coin_does_not_insert_on_parent_miss`. |
</threat_model>

<verification>
`bun run scripts/command-timings.ts run --key phase139-01-coins-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --lib coins::`
`bash scripts/check-pure-core-deps.sh`
`bun run scripts/check-parity-breadcrumbs.ts`
Do not start a second Cargo job against the same target directory.
</verification>

<success_criteria>
- CoinsView / CoinsCache / MemoryCoinsView exist in `open-bitcoin-chainstate` with the four lookup facts.
- Five valid flag states construct; three invalid combos error.
- CoinsCache is not Clone. Coin is unchanged. No Fjall/fs/clock in the crate coins modules.
- Engine apply and node prepare are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/139-coins-view-cache-contract-and-engine-apply/139-01-SUMMARY.md`
</output>
