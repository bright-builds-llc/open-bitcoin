---
phase: 09-parity-harnesses-and-fuzzing
plan: 04
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - scripts/verify.sh
  - .github/workflows/ci.yml
  - docs/parity/catalog/README.md
  - docs/parity/catalog/verification-harnesses.md
  - docs/parity/index.json
  - .planning/phases/09-parity-harnesses-and-fuzzing/09-04-SUMMARY.md
  - .planning/phases/09-parity-harnesses-and-fuzzing/09-VERIFICATION.md
autonomous: true
requirements:
  - VER-03
  - VER-04
  - PAR-01
user_setup: []
must_haves:
  truths:
    - "`scripts/verify.sh` creates a default parity report directory for workspace tests."
    - "CI uploads generated parity reports as artifacts while `bash scripts/verify.sh` remains the blocking verification command."
    - "Parity docs describe the harness, optional Knots target, isolation model, and property coverage."
    - "Full repo-native verification passes."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-04-24T10-06-16
generated_at: 2026-04-24T10:09:15Z
---

<objective>
Surface parity, coverage, and harness results in CI.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wire parity reports into verify and CI</name>
  <read_first>
    - scripts/verify.sh
    - .github/workflows/ci.yml
  </read_first>
  <files>
    scripts/verify.sh
    .github/workflows/ci.yml
  </files>
  <acceptance_criteria>
    - `scripts/verify.sh` exports `OPEN_BITCOIN_PARITY_REPORT_DIR` with default `$PWD/packages/target/parity-reports`.
    - `scripts/verify.sh` includes `//:test_harness` in the Bazel smoke build.
    - `.github/workflows/ci.yml` has an `Upload parity reports` step using `actions/upload-artifact@v4`.
  </acceptance_criteria>
  <action>Keep `bash scripts/verify.sh` as the single blocking CI command, but make the report directory predictable and upload it after verification.</action>
  <verify>
    <automated>rg -n 'OPEN_BITCOIN_PARITY_REPORT_DIR' scripts/verify.sh .github/workflows/ci.yml</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update parity docs and run closeout verification</name>
  <read_first>
    - docs/parity/catalog/README.md
    - docs/parity/index.json
    - scripts/verify.sh
  </read_first>
  <files>
    docs/parity/catalog/README.md
    docs/parity/catalog/verification-harnesses.md
    docs/parity/index.json
    .planning/phases/09-parity-harnesses-and-fuzzing/09-04-SUMMARY.md
    .planning/phases/09-parity-harnesses-and-fuzzing/09-VERIFICATION.md
  </files>
  <acceptance_criteria>
    - `docs/parity/catalog/verification-harnesses.md` documents the harness, isolation model, property coverage, optional Knots env vars, and deferred full Knots process lifecycle.
    - `docs/parity/index.json` includes an entry with id `verification-harnesses`.
    - `bash scripts/verify.sh` passes.
    - `09-VERIFICATION.md` has `status: passed`.
  </acceptance_criteria>
  <action>Update audit-facing parity docs, run the full repo-native verification contract, and write the final Phase 9 verification report.</action>
  <verify>
    <automated>bash scripts/verify.sh
node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 9 --require-plans --require-verification --raw</automated>
  </verify>
</task>

</tasks>

<verification>
- `bash scripts/verify.sh`
- `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 9 --require-plans --require-verification --raw`
</verification>
