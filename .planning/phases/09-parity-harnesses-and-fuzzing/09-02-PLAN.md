---
phase: 09-parity-harnesses-and-fuzzing
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/open-bitcoin-test-harness/src/lib.rs
  - packages/open-bitcoin-test-harness/src/isolation.rs
  - packages/open-bitcoin-test-harness/src/report.rs
  - packages/open-bitcoin-rpc/tests/black_box_parity.rs
  - .planning/phases/09-parity-harnesses-and-fuzzing/09-02-SUMMARY.md
autonomous: true
requirements:
  - VER-03
  - VER-04
user_setup: []
must_haves:
  truths:
    - "Harness helpers allocate unique data directories and localhost ports for parallel-safe integration tests."
    - "Process guards clean up spawned child processes on drop."
    - "Parity suites can emit JSON and Markdown reports when `OPEN_BITCOIN_PARITY_REPORT_DIR` is set."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-04-24T10-06-16
generated_at: 2026-04-24T10:08:45Z
---

<objective>
Implement process, port, and data-dir isolation for parallel-safe integration runs.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sandbox, port, and process isolation helpers</name>
  <read_first>
    - packages/open-bitcoin-cli/tests/operator_flows.rs
    - packages/open-bitcoin-test-harness/src/lib.rs
  </read_first>
  <files>
    packages/open-bitcoin-test-harness/src/isolation.rs
    packages/open-bitcoin-test-harness/src/lib.rs
  </files>
  <acceptance_criteria>
    - `isolation.rs` defines `Sandbox`, `PortReservation`, and `ProcessGuard`.
    - Harness unit tests prove sibling sandbox paths are different.
    - Harness unit tests prove sibling port reservations are different.
  </acceptance_criteria>
  <action>Move the reusable integration-safety pattern into the harness crate. `Sandbox` creates a unique directory and removes it on drop. `PortReservation` binds `127.0.0.1:0` and exposes the reserved address. `ProcessGuard` owns a `Child` and kills/waits on drop.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-test-harness --all-features isolation::tests -- --nocapture</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add report helpers and wire the black-box suite to them</name>
  <read_first>
    - packages/open-bitcoin-test-harness/src/case.rs
    - packages/open-bitcoin-rpc/tests/black_box_parity.rs
  </read_first>
  <files>
    packages/open-bitcoin-test-harness/src/report.rs
    packages/open-bitcoin-test-harness/src/lib.rs
    packages/open-bitcoin-rpc/tests/black_box_parity.rs
  </files>
  <acceptance_criteria>
    - `report.rs` defines a helper that writes `{suite}-{target}.json` and `{suite}-{target}.md`.
    - `black_box_parity.rs` calls the report helper when `OPEN_BITCOIN_PARITY_REPORT_DIR` is set.
    - A report fixture test verifies Markdown contains `# Parity Suite Report` and JSON contains `"suite"`.
  </acceptance_criteria>
  <action>Add stable JSON/Markdown report output so local agents and CI can inspect parity outcomes without parsing raw test logs.</action>
  <verify>
    <automated>OPEN_BITCOIN_PARITY_REPORT_DIR=packages/target/parity-reports cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --test black_box_parity</automated>
  </verify>
</task>

</tasks>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-test-harness --all-features`
- `OPEN_BITCOIN_PARITY_REPORT_DIR=packages/target/parity-reports cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --test black_box_parity`
</verification>

