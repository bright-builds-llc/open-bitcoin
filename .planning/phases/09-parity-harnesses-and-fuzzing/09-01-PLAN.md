---
phase: 09-parity-harnesses-and-fuzzing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - BUILD.bazel
  - packages/open-bitcoin-test-harness/Cargo.toml
  - packages/open-bitcoin-test-harness/BUILD.bazel
  - packages/open-bitcoin-test-harness/src/lib.rs
  - packages/open-bitcoin-test-harness/src/case.rs
  - packages/open-bitcoin-test-harness/src/target.rs
  - packages/open-bitcoin-rpc/Cargo.toml
  - packages/open-bitcoin-rpc/tests/black_box_parity.rs
  - .planning/phases/09-parity-harnesses-and-fuzzing/09-01-SUMMARY.md
autonomous: true
requirements:
  - VER-03
user_setup: []
must_haves:
  truths:
    - "A reusable first-party Rust harness crate defines target-neutral `FunctionalCase` values and target adapters."
    - "The same case list runs against Open Bitcoin's JSON-RPC HTTP surface and can run against a Knots-compatible external RPC endpoint when configured."
    - "Open Bitcoin black-box parity tests run by default under workspace tests."
  artifacts:
    - path: packages/open-bitcoin-test-harness/src/case.rs
      provides: "Target-neutral functional case and suite execution model"
      contains: "FunctionalCase"
    - path: packages/open-bitcoin-test-harness/src/target.rs
      provides: "JSON-RPC target adapter contract and HTTP target"
      contains: "HarnessTarget"
    - path: packages/open-bitcoin-rpc/tests/black_box_parity.rs
      provides: "Default Open Bitcoin RPC black-box parity suite and optional Knots target"
      contains: "OPEN_BITCOIN_KNOTS_RPC_ADDR"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-04-24T10-06-16
generated_at: 2026-04-24T10:08:30Z
---

<objective>
Build the cross-implementation black-box functional test harness.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable harness crate and target contract</name>
  <read_first>
    - packages/Cargo.toml
    - BUILD.bazel
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/context.rs
    - packages/open-bitcoin-rpc/src/method.rs
  </read_first>
  <files>
    packages/open-bitcoin-test-harness/Cargo.toml
    packages/open-bitcoin-test-harness/BUILD.bazel
    packages/open-bitcoin-test-harness/src/lib.rs
    packages/open-bitcoin-test-harness/src/case.rs
    packages/open-bitcoin-test-harness/src/target.rs
    packages/Cargo.toml
    BUILD.bazel
  </files>
  <acceptance_criteria>
    - `packages/Cargo.toml` includes `"open-bitcoin-test-harness"` in `workspace.members`.
    - `packages/open-bitcoin-test-harness/src/case.rs` defines `FunctionalCase`, `ExpectedOutcome`, `CaseOutcome`, `SuiteReport`, and `run_suite`.
    - `packages/open-bitcoin-test-harness/src/target.rs` defines `HarnessTarget` and `RpcHttpTarget`.
    - `BUILD.bazel` exposes an `open_bitcoin_test_harness_lib` alias.
  </acceptance_criteria>
  <action>Create a first-party test harness crate using only standard library plus existing `serde_json` and `base64` dependencies. Implement a target-neutral RPC case model and a blocking JSON-RPC HTTP target adapter that sends authenticated POST requests to `127.0.0.1:<port>` or any configured compatible endpoint.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-test-harness --all-features</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Open Bitcoin and optional Knots parity suite</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/config.rs
    - packages/open-bitcoin-rpc/src/context.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
  </read_first>
  <files>
    packages/open-bitcoin-rpc/Cargo.toml
    packages/open-bitcoin-rpc/tests/black_box_parity.rs
  </files>
  <acceptance_criteria>
    - `black_box_parity.rs` contains one shared `functional_cases()` list used by both Open Bitcoin and optional Knots target tests.
    - The Open Bitcoin test starts the real RPC router and sends requests through HTTP Basic auth.
    - The optional Knots-compatible test reads `OPEN_BITCOIN_KNOTS_RPC_ADDR`, `OPEN_BITCOIN_KNOTS_RPC_USER`, and `OPEN_BITCOIN_KNOTS_RPC_PASSWORD`, and returns a visible skipped outcome when they are absent.
  </acceptance_criteria>
  <action>Add an RPC integration test that runs representative supported methods through the harness against Open Bitcoin by default. Add a second opt-in test path that runs the same cases against a configured external Knots-compatible RPC endpoint without requiring it in normal CI.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --test black_box_parity</automated>
  </verify>
</task>

</tasks>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-test-harness --all-features`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --test black_box_parity`
</verification>

<success_criteria>
- The harness and Open Bitcoin target test are compiled and run by the workspace.
- The same cases can run against an optional Knots-compatible target without code changes.
</success_criteria>

