---
phase: 09-parity-harnesses-and-fuzzing
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - packages/open-bitcoin-codec/tests/properties.rs
  - packages/open-bitcoin-network/tests/properties.rs
  - .planning/phases/09-parity-harnesses-and-fuzzing/09-03-SUMMARY.md
autonomous: true
requirements:
  - PAR-01
user_setup: []
must_haves:
  truths:
    - "Deterministic property-style tests cover transaction codec round trips."
    - "Deterministic property-style tests cover protocol wire-message encode/decode invariants."
    - "Generated malformed inputs return typed errors or successful parses without panics."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 9-2026-04-24T10-06-16
generated_at: 2026-04-24T10:09:00Z
---

<objective>
Add fuzz and property suites for parser, serialization, and protocol boundaries.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add codec property tests</name>
  <read_first>
    - packages/open-bitcoin-codec/src/transaction.rs
    - packages/open-bitcoin-codec/src/network.rs
    - packages/open-bitcoin-primitives/src/transaction.rs
  </read_first>
  <files>
    packages/open-bitcoin-codec/tests/properties.rs
  </files>
  <acceptance_criteria>
    - `properties.rs` includes a deterministic generator type.
    - At least 64 generated transactions round-trip through `encode_transaction` and `parse_transaction`.
    - At least 64 generated message headers round-trip through `encode_message_header` and `parse_message_header`.
  </acceptance_criteria>
  <action>Add bounded generated-case tests for transaction and message-header codec behavior under normal `cargo test`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-codec --all-features --test properties</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add network wire-message property tests</name>
  <read_first>
    - packages/open-bitcoin-network/src/message.rs
    - packages/open-bitcoin-network/tests/parity.rs
  </read_first>
  <files>
    packages/open-bitcoin-network/tests/properties.rs
  </files>
  <acceptance_criteria>
    - `properties.rs` includes generated ping, pong, version, inventory, and getheaders messages.
    - Each generated message round-trips through `encode_wire` and `ParsedNetworkMessage::decode_wire`.
    - Mutated checksum bytes produce `NetworkError::InvalidChecksum`.
  </acceptance_criteria>
  <action>Add deterministic property-style coverage for supported wire-message protocol boundaries.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features --test properties</automated>
  </verify>
</task>

</tasks>

<verification>
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-codec --all-features --test properties`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features --test properties`
</verification>
