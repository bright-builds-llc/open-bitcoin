---
phase: 107-runtime-relay-activation-and-download-eligibility-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 107-01
files_modified:
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - docs/parity/source-breadcrumbs.json
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - DL-01
  - DL-02
requirements_addressed:
  - ACT-01
  - ACT-02
  - DL-01
  - DL-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 107-2026-07-03T02-54-20
generated_at: 2026-07-03T03:18:27Z
must_haves:
  truths:
    - "Resolved `RuntimeConfig.relay` reaches `ManagedPeerNetwork` in production runtime and RPC context construction."
    - "Default construction remains default-off for compatibility tests and intentionally default relay behavior."
    - "Managed network download behavior changes when runtime relay activation is enabled versus disabled."
    - "Inbound serving eligibility uses resolved `RuntimeConfig.inbound.enabled` for this phase and remains deterministic."
    - "Service bits and public defaults remain unchanged."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/relay_serving.rs"
      provides: "ManagedPeerNetwork constructors that set PeerManager relay download policy"
      contains: "new_with_relay_activation"
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "RuntimeConfig relay propagation into ManagedPeerNetwork"
      contains: "config.relay"
    - path: "packages/open-bitcoin-rpc/src/context/tests.rs"
      provides: "Runtime propagation regression coverage"
    - path: "packages/open-bitcoin-node/src/network/tests.rs"
      provides: "Managed transaction download activation matrix"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/network/relay_serving.rs"
      via: "ManagedPeerNetwork::new_with_relay_activation"
      pattern: "config\\.relay"
    - from: "packages/open-bitcoin-node/src/network/relay_serving.rs"
      to: "packages/open-bitcoin-network/src/peer/relay_download.rs"
      via: "RelayDownloadPolicy"
      pattern: "RelayDownloadPolicy"
    - from: "packages/open-bitcoin-node/src/network/tests.rs"
      to: "WireNetworkMessage::GetData"
      via: "enabled eligible peer behavior"
      pattern: "GetData"
---

<objective>
Carry explicit runtime relay activation into managed network construction.

Purpose: fix the live wiring gap where resolved relay config exists but production runtime paths can still build the managed network with default-off activation.
Output: managed constructors and RPC context construction that pass activation/inbound-serving inputs into `PeerManager` transaction download eligibility.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/107-runtime-relay-activation-and-download-eligibility-integration/107-CONTEXT.md
@.planning/phases/107-runtime-relay-activation-and-download-eligibility-integration/107-RESEARCH.md
@.planning/phases/100-relay-activation-boundary-and-permission-semantics/100-02-SUMMARY.md
@.planning/phases/101-transaction-inventory-identity-and-download-scheduling/101-02-SUMMARY.md
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/network/relay_serving.rs

<interfaces>
From `packages/open-bitcoin-rpc/src/config.rs`:
```rust
pub struct RuntimeConfig {
    pub chain: AddressNetwork,
    pub maybe_data_dir: Option<PathBuf>,
    pub rpc_server: RpcServerConfig,
    pub rpc_client: RpcClientConfig,
    pub wallet: WalletRuntimeConfig,
    pub sync: DaemonSyncConfig,
    pub inbound: InboundListenerConfig,
    pub relay: RelayActivationConfig,
    pub inbound_permission_validation_failures: u32,
}
```

From `packages/open-bitcoin-node/src/network/relay_serving.rs`:
```rust
pub fn new_with_relay_activation(
    store: S,
    local_config: LocalPeerConfig,
    mempool_config: PolicyConfig,
    relay_activation: RelayActivationConfig,
    inbound_serving_enabled: bool,
) -> Self;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make managed constructors set relay download policy</name>
  <files>packages/open-bitcoin-node/src/network/relay_serving.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs, docs/parity/source-breadcrumbs.json, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-node/src/network/relay_serving.rs`
    - `packages/open-bitcoin-node/src/network/tests.rs`
    - `packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs`
    - `packages/open-bitcoin-network/src/peer/relay_download.rs`
    - `docs/parity/source-breadcrumbs.json`
  </read_first>
  <behavior>
    - `ManagedPeerNetwork::new` remains default-off and does not request transaction downloads by default.
    - `ManagedPeerNetwork::new_with_relay_activation(..., RelayActivationConfig { enabled: true }, inbound_serving_enabled)` passes `RelayDownloadPolicy { activation, inbound_serving_enabled }` into its `PeerManager`.
    - Managed outbound/manual eligible peers produce targeted `getdata` only when activation is enabled.
    - Ordinary inbound and protected-only inbound peers remain ineligible even when activation is enabled.
  </behavior>
  <action>
    Update `ManagedPeerNetwork::new_with_relay_activation` and `ManagedPeerNetwork::with_sync_limits_and_relay_activation` so their `PeerManager` is built with the Plan 107-01 `RelayDownloadPolicy { activation: relay_activation, inbound_serving_enabled }` instead of a default-off PeerManager. If `from_peer_manager` can receive an already-configured manager, keep the policy assignment in one place by calling `peer_manager.set_relay_download_policy(...)` before storing the manager. Keep `ManagedPeerNetwork::new` delegating to `RelayActivationConfig::default()` and `false` to preserve D-01 default-off compatibility. Update `network_info().relay` to reflect `self.relay_activation.enabled` while leaving `local_services_bits`, inbound listener defaults, compact block/filter behavior, and public defaults unchanged per D-09. Add managed network tests that use `RelayActivationConfig { enabled: false }` and `{ enabled: true }` and assert emitted `WireNetworkMessage::GetData` only for enabled eligible peers.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/network/relay_serving.rs` contains `RelayDownloadPolicy { activation: relay_activation, inbound_serving_enabled }` or an equivalent call to `set_relay_download_policy`.
    - `ManagedPeerNetwork::new` still passes `RelayActivationConfig::default()` and `false`.
    - Managed tests prove default-off suppresses transaction download and enabled outbound/manual scheduling emits `GetData`.
    - Managed tests prove enabled ordinary inbound and protected-only peers do not emit `GetData`.
    - No test or code changes service bits for relay activation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_network_transaction_relay -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib relay_serving_cases -- --nocapture</automated>
    <automated>rg -n "RelayDownloadPolicy|set_relay_download_policy|RelayActivationConfig::default\\(\\)" packages/open-bitcoin-node/src/network/relay_serving.rs</automated>
  </verify>
  <done>Managed network constructors carry activation and inbound-serving values into PeerManager download eligibility while preserving default-off constructor behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass RuntimeConfig relay settings through RPC context construction</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/context/tests.rs, packages/open-bitcoin-rpc/src/config/tests.rs, docs/metrics/lines-of-code.md</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/config.rs`
    - `packages/open-bitcoin-rpc/src/config/loader/open_bitcoin_runtime.rs`
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/context/tests.rs`
    - `packages/open-bitcoin-rpc/src/config/tests.rs`
    - `.planning/phases/107-runtime-relay-activation-and-download-eligibility-integration/107-CONTEXT.md`
  </read_first>
  <behavior>
    - `ManagedRpcContext::from_runtime_config_with_store` calls `ManagedPeerNetwork::new_with_relay_activation(..., config.relay, config.inbound.enabled)` per D-01.
    - Runtime config with `relay.enabled = true` produces a context whose managed network relay evidence differs from default-off config.
    - Existing config loader tests continue proving JSONC/CLI default-off and explicit `-openbitcoinrelay` precedence.
  </behavior>
  <action>
    Replace the production-like `ManagedPeerNetwork::new(MemoryChainstateStore::default(), local_config, PolicyConfig::default())` call in `ManagedRpcContext::from_runtime_config_with_store` with `ManagedPeerNetwork::new_with_relay_activation(MemoryChainstateStore::default(), local_config, PolicyConfig::default(), config.relay, config.inbound.enabled)`. Add or update tests in `packages/open-bitcoin-rpc/src/context/tests.rs` to construct `RuntimeConfig { relay: RelayActivationConfig { enabled: true }, inbound: InboundListenerConfig { enabled: true, .. }, ..RuntimeConfig::default() }` and assert activation is observable through `context.network_info().relay == true` or an existing test-only context helper. Add a default config assertion for `context.network_info().relay == false`. Keep baseline-compatible response shapes unchanged; do not add new `getnetworkinfo` fields per D-03.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/context/network.rs` contains `new_with_relay_activation` and `config.relay`.
    - `ManagedRpcContext::from_runtime_config_with_store` contains no production `ManagedPeerNetwork::new(` call after runtime config resolution.
    - Tests cover default-off and explicit enabled runtime relay propagation.
    - Existing `open_bitcoin_jsonc_defaults_relay_activation_to_disabled` and `open_bitcoin_jsonc_accepts_relay_activation_enabled` tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib managed_rpc_context_builds_from_runtime_config -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib open_bitcoin_jsonc_accepts_relay_activation_enabled -- --nocapture</automated>
    <automated>rg -n "new_with_relay_activation\\(|config\\.relay|network_info\\(\\)\\.relay" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/context/tests.rs</automated>
  </verify>
  <done>`RuntimeConfig.relay` and `RuntimeConfig.inbound.enabled` reach managed network construction from the RPC/daemon context path, with regression tests proving default-off and explicit enabled behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| config loader to runtime context | Operator-controlled JSONC/CLI relay settings enter runtime construction. |
| runtime context to managed network | Resolved activation/inbound-serving values become peer download eligibility inputs. |
| managed network to baseline RPC response | Existing status fields must report truth without adding ad hoc baseline fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-107-02-01 | Elevation of Privilege | `ManagedRpcContext::from_runtime_config_with_store` | mitigate | Pass `config.relay` into `ManagedPeerNetwork::new_with_relay_activation`; add tests that fail if the path falls back to `ManagedPeerNetwork::new`. |
| T-107-02-02 | Tampering | `ManagedPeerNetwork` constructor wiring | mitigate | Set `RelayDownloadPolicy` from constructor values exactly once and keep default constructor explicitly default-off. |
| T-107-02-03 | Information Disclosure | runtime/status projection | mitigate | Reuse existing fields and fixed booleans only; no peer ids, endpoints, permission strings, txids, wtxids, or raw transactions. |
| T-107-02-04 | Spoofing | public service-bit interpretation | mitigate | Keep `local_services_bits` and `LocalPeerConfig.services` unchanged; tests must assert relay activation does not imply new service bits. |
| T-107-02-05 | Repudiation | activation drift | mitigate | Add deterministic tests and later Phase 107 checker coverage for dropped `config.relay` propagation. |
</threat_model>

<verification>
```bash
cargo fmt --manifest-path packages/Cargo.toml --all --check
cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-targets --all-features -- -D warnings
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_network_transaction_relay -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib managed_rpc_context_builds_from_runtime_config -- --nocapture
```
</verification>

<success_criteria>
ACT-01 is closed at the runtime construction layer: explicit relay activation survives config loading and context construction, while public relay remains default-off and service-bit/public-default behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/107-runtime-relay-activation-and-download-eligibility-integration/107-02-SUMMARY.md`.
</output>
