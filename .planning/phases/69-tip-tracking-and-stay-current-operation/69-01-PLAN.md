---
phase: 69-tip-tracking-and-stay-current-operation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/sync/types.rs
  - packages/open-bitcoin-node/src/sync/types/summary.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - .planning/phases/69-tip-tracking-and-stay-current-operation/69-01-SUMMARY.md
autonomous: true
requirements: [TIP-01, TIP-02]
user_setup: []
must_haves:
  truths:
    - "Best-known tip evidence is a typed shared SyncStatus field, not renderer-local text."
    - "Stay-current state is a typed shared SyncStatus field that includes initial_catch_up, current_at_best_known_tip, stale_tip, recovering, and no_progress."
    - "New additive status fields have serde defaults so existing runtime metadata can decode without Phase 69 fields."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "Typed best-known tip and stay-current status contract"
      contains: "BestKnownTipStatus"
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "Typed stay-current state variants"
      contains: "CurrentAtBestKnownTip"
    - path: ".planning/phases/69-tip-tracking-and-stay-current-operation/69-01-SUMMARY.md"
      provides: "Plan 69-01 execution evidence"
      contains: "Self-Check: PASSED"
  key_links:
    - from: "packages/open-bitcoin-node/src/status.rs"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "Durable sync status projection fills additive Phase 69 fields"
      pattern: "best_known_tip"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 69-2026-06-11T15-13-14
generated_at: 2026-06-11T15:13:14.807Z
---

<objective>
Add the shared Phase 69 status contract for best-known tip evidence and
stay-current classification.

Purpose: TIP-01 and TIP-02 require all operator surfaces to consume the same
typed evidence instead of inferring tip/current/stale/no-progress state in
renderers.
Output: Additive status DTOs and summary projection stubs with deterministic
serialization/default coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-CONTEXT.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-RESEARCH.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/sync/types.rs
@packages/open-bitcoin-node/src/sync/types/summary.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync/tests.rs
</context>

<threat_model>
## Security And Correctness Notes

- This plan adds no new network listener, RPC control method, wallet mutation,
  service-manager action, or public-network default verification.
- The primary correctness risk is false current-at-tip credit from headers-only
  or downloaded-only progress.
- The primary operability risk is breaking existing persisted runtime metadata
  by adding strict non-defaulted fields to `SyncStatus`.
- Mitigations are typed status fields, explicit `FieldAvailability`
  unavailable defaults, and deterministic serialization/projection tests.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Phase 69 status DTOs</name>
  <read_first>
    - `packages/open-bitcoin-node/src/status.rs`
    - `.planning/phases/69-tip-tracking-and-stay-current-operation/69-CONTEXT.md`
    - `.planning/phases/69-tip-tracking-and-stay-current-operation/69-RESEARCH.md`
  </read_first>
  <files>packages/open-bitcoin-node/src/status.rs</files>
  <action>
    Add additive status types in `status.rs` near the existing sync DTOs:
    `BestKnownTipSource`, `TipFreshnessStatus`, `PeerTipAgreementStatus`,
    `PeerTipAgreement`, `BestKnownTipStatus`, and `StayCurrentStatus`.

    Required serialized labels:
    - `BestKnownTipSource`: `header_store`, `peer_observation`
    - `TipFreshnessStatus`: `fresh`, `stale`, `unknown`
    - `PeerTipAgreementStatus`: `agrees`, `behind`, `disagrees`, `no_evidence`
    - `StayCurrentStatus`: `initial_catch_up`,
      `current_at_best_known_tip`, `stale_tip`, `recovering`, `no_progress`

    `BestKnownTipStatus` must include:
    - `source: BestKnownTipSource`
    - `height: u64`
    - `block_hash: String`
    - `work: String`
    - `block_time_unix_seconds: u64`
    - `observed_at_unix_seconds: u64`
    - `freshness: TipFreshnessStatus`
    - `peer_agreement: Vec<PeerTipAgreement>`

    `PeerTipAgreement` must include bounded peer evidence:
    - `peer: String`
    - `maybe_resolved_endpoint: Option<String>`
    - `status: PeerTipAgreementStatus`
    - `maybe_height: Option<u64>`
    - `maybe_hash: Option<String>`
    - `maybe_work: Option<String>`
    - `maybe_last_activity_unix_seconds: Option<u64>`

    Add fields to `SyncStatus`:
    - `best_known_tip: FieldAvailability<BestKnownTipStatus>`
    - `stay_current: FieldAvailability<StayCurrentStatus>`

    Use serde defaults:
    - `#[serde(default = "best_known_tip_unavailable")]`
    - `#[serde(default = "stay_current_unavailable")]`
    with unavailable reasons `best-known tip evidence unavailable` and
    `stay-current state unavailable`.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/status.rs` contains `pub struct BestKnownTipStatus`.
    - `packages/open-bitcoin-node/src/status.rs` contains `pub enum StayCurrentStatus`.
    - `packages/open-bitcoin-node/src/status.rs` contains `CurrentAtBestKnownTip`.
    - `packages/open-bitcoin-node/src/status.rs` contains `best_known_tip: FieldAvailability<BestKnownTipStatus>`.
    - `packages/open-bitcoin-node/src/status.rs` contains `stay_current: FieldAvailability<StayCurrentStatus>`.
    - `packages/open-bitcoin-node/src/status.rs` contains `best-known tip evidence unavailable`.
    - `packages/open-bitcoin-node/src/status.rs` contains `stay-current state unavailable`.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all --check</automated>
  </verify>
  <done>Shared status DTOs expose typed Phase 69 fields with serde defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Wire summary and durable-state defaults</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/types.rs`
    - `packages/open-bitcoin-node/src/sync/types/summary.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/status.rs`
  </read_first>
  <files>
    packages/open-bitcoin-node/src/sync/types.rs,
    packages/open-bitcoin-node/src/sync/types/summary.rs,
    packages/open-bitcoin-node/src/sync/runtime_state.rs
  </files>
  <action>
    Keep this task additive. Update imports and `SyncRunSummary::sync_status`
    so newly constructed `SyncStatus` values compile with the new fields.
    For this plan, set:
    - `best_known_tip` to `FieldAvailability::unavailable("best-known tip evidence unavailable")`
      when no runtime projection has filled it yet.
    - `stay_current` to `FieldAvailability::unavailable("stay-current state unavailable")`
      when no runtime projection has filled it yet.

    Do not implement the final classification logic here; Plan 69-02 owns
    runtime derivation. This task should only make all constructors and existing
    tests compile with the additive contract.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/sync/types/summary.rs` constructs `best_known_tip`.
    - `packages/open-bitcoin-node/src/sync/types/summary.rs` constructs `stay_current`.
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs` compiles without renderer-specific stay-current strings.
    - Existing `SyncStatus` struct literals in `packages/open-bitcoin-node/src/sync/tests.rs` include or default the new fields.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_summary_status_projections_include_counters --all-features</automated>
  </verify>
  <done>Existing status construction works with additive Phase 69 fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add status serialization and default tests</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/storage/snapshot_codec.rs`
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <action>
    Add deterministic tests in `sync/tests.rs`:
    - `phase69_sync_status_defaults_tip_and_stay_current_fields`
    - `phase69_sync_status_serializes_tip_and_stay_current_fields`

    The default test must deserialize a JSON `SyncStatus` payload that omits
    `best_known_tip` and `stay_current`, then assert both fields are
    `FieldAvailability::Unavailable` with the exact default reasons.

    The serialization test must construct a `SyncStatus` with
    `BestKnownTipStatus { source: BestKnownTipSource::HeaderStore, height: 2,
    block_hash: "aa", work: "3", block_time_unix_seconds: 1_777_225_000,
    observed_at_unix_seconds: 1_777_225_010, freshness: TipFreshnessStatus::Fresh,
    peer_agreement: vec![PeerTipAgreement { peer: "127.0.0.1:18444",
    maybe_resolved_endpoint: Some("127.0.0.1:18444"), status:
    PeerTipAgreementStatus::Agrees, maybe_height: Some(2), maybe_hash:
    Some("aa"), maybe_work: Some("3"), maybe_last_activity_unix_seconds:
    Some(1_777_225_010) }] }` and
    `StayCurrentStatus::CurrentAtBestKnownTip`, then assert the JSON contains
    `best_known_tip`, `header_store`, `fresh`, `agrees`, and
    `current_at_best_known_tip`.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/sync/tests.rs` contains `phase69_sync_status_defaults_tip_and_stay_current_fields`.
    - `packages/open-bitcoin-node/src/sync/tests.rs` contains `phase69_sync_status_serializes_tip_and_stay_current_fields`.
    - The default test asserts `best-known tip evidence unavailable`.
    - The serialization test asserts `current_at_best_known_tip`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_sync_status --all-features</automated>
  </verify>
  <done>Phase 69 status fields have deterministic serde coverage.</done>
</task>

</tasks>

<verification>
Run:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_sync_status --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_summary_status_projections_include_counters --all-features
```
</verification>

<success_criteria>
- [ ] Best-known tip evidence is typed in shared `SyncStatus`.
- [ ] Stay-current state is typed in shared `SyncStatus`.
- [ ] New fields are additive and serde-defaulted.
- [ ] Deterministic tests cover omitted-field defaults and JSON labels.
</success_criteria>
