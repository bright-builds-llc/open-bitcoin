---
phase: 69-tip-tracking-and-stay-current-operation
plan: 03
type: execute
wave: 3
depends_on: [69-02]
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tip.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - .planning/phases/69-tip-tracking-and-stay-current-operation/69-03-SUMMARY.md
autonomous: true
requirements: [TIP-02, TIP-03]
user_setup: []
must_haves:
  truths:
    - "Fresh idle-at-tip daemon wakes report current_at_best_known_tip instead of generic no_progress."
    - "Stale-tip and no-progress status include bounded operator next-action context without absorbing Phase 70 recovery taxonomy."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "Shared stay-current next-action context"
      contains: "stay_current_next_action"
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Fresh idle-at-tip stay-current proof"
      contains: "phase69_fresh_idle_cycle_reports_current_at_best_known_tip"
    - path: ".planning/phases/69-tip-tracking-and-stay-current-operation/69-03-SUMMARY.md"
      provides: "Plan 69-03 execution evidence"
      contains: "Self-Check: PASSED"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/tip.rs"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "stay-current next-action helper used in durable status projection"
      pattern: "stay_current_next_action"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 69-2026-06-11T15-13-14
generated_at: 2026-06-11T15:13:14.807Z
---

<objective>
Implement bounded stay-current next-action context and fresh idle-at-tip
classification.

Purpose: TIP-02 and TIP-03 require a shared current/stale/no-progress state
that operators can act on without renderer-specific interpretation. A node that
is fresh and already connected to the best-known tip should report
`current_at_best_known_tip`, not generic no-progress.
Output: Shared next-action status field, runtime projection, and deterministic
fresh-idle/current tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-CONTEXT.md
@.planning/phases/69-tip-tracking-and-stay-current-operation/69-RESEARCH.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync/tip.rs
@packages/open-bitcoin-node/src/sync/tests.rs
</context>

<threat_model>
## Security And Correctness Notes

- This plan adds no inbound serving, relay, wallet mutation, service-manager
  behavior, external tip oracle, or public-network default verification.
- Correctness risk: stale-tip and no-progress operator action text could become
  Phase 70 recovery taxonomy. Mitigation: keep Phase 69 hints bounded and
  generic: refresh peers/evidence for stale tip, continue/retry sync for
  no-progress, inspect recovery category when already present.
- Correctness risk: reporting current while only headers advanced. Mitigation:
  this plan tests the fresh idle-at-tip case only when connected active-chain
  progress already matches best-known tip evidence.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded stay-current next-action context</name>
  <read_first>
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/tip.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `.planning/phases/69-tip-tracking-and-stay-current-operation/69-CONTEXT.md`
  </read_first>
  <files>
    packages/open-bitcoin-node/src/status.rs,
    packages/open-bitcoin-node/src/sync/tip.rs,
    packages/open-bitcoin-node/src/sync/runtime_state.rs
  </files>
  <action>
    Add a shared `stay_current_next_action: FieldAvailability<String>` field to
    `SyncStatus` with serde default function
    `stay_current_next_action_unavailable()` returning reason
    `stay-current next action unavailable`.

    In the Phase 69 tip helper, add `stay_current_next_action(status:
    StayCurrentStatus) -> Option<&'static str>` with exact messages:
    - `CurrentAtBestKnownTip`: `No action required; node is current at the best-known validated tip.`
    - `InitialCatchUp`: `Continue sync until connected active-chain progress reaches the best-known validated tip.`
    - `StaleTip`: `Refresh peers or wait for fresh peer tip evidence before treating the node as current.`
    - `NoProgress`: `Retry sync or inspect peer outcomes; no useful stay-current progress was observed.`
    - `Recovering`: return `None` so existing recovery_category/recovery_action remains the source of recovery guidance.

    In durable status projection, fill `sync.stay_current_next_action` from this
    helper when it returns a message. Leave existing `sync.recovery_action`
    behavior intact for recovery categories.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/status.rs` contains `stay_current_next_action`.
    - `packages/open-bitcoin-node/src/status.rs` contains `stay-current next action unavailable`.
    - `packages/open-bitcoin-node/src/sync/tip.rs` contains `Refresh peers or wait for fresh peer tip evidence`.
    - `packages/open-bitcoin-node/src/sync/tip.rs` contains `Retry sync or inspect peer outcomes`.
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs` assigns `sync.stay_current_next_action`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_stay_current_next_action --all-features</automated>
  </verify>
  <done>Stale-tip and no-progress carry bounded next-action context.</done>
</task>

<task type="auto">
  <name>Task 2: Classify fresh idle-at-tip as current</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/sync/tip.rs`
    - `packages/open-bitcoin-node/src/sync/tests.rs`
  </read_first>
  <files>
    packages/open-bitcoin-node/src/sync.rs,
    packages/open-bitcoin-node/src/sync/runtime_state.rs,
    packages/open-bitcoin-node/src/sync/tests.rs
  </files>
  <action>
    Adjust the idle-stop path so a fresh idle cycle at connected best-known tip
    persists `StayCurrentStatus::CurrentAtBestKnownTip` and an informational
    latest stop reason/status instead of treating the condition as generic
    `NoProgress`.

    Add test `phase69_fresh_idle_cycle_reports_current_at_best_known_tip`:
    - Arrange a runtime with validated headers and connected active-chain
      progress at height 1 with matching hash/work.
    - Run `sync_until_idle` with a fixed timestamp and a scripted peer that
      returns no new headers after the node is already current.
    - Assert durable status has `stay_current` =
      `CurrentAtBestKnownTip`, best-known tip freshness `Fresh`, and
      `stay_current_next_action` text `No action required; node is current at the best-known validated tip.`
    - Assert the test does not require public network access or sleeps.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/sync/tests.rs` contains `phase69_fresh_idle_cycle_reports_current_at_best_known_tip`.
    - The test asserts `CurrentAtBestKnownTip`.
    - The test asserts `No action required; node is current at the best-known validated tip.`
    - The test uses fixed numeric timestamps.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_fresh_idle_cycle_reports_current_at_best_known_tip --all-features</automated>
  </verify>
  <done>Fresh idle-at-tip cycles report current-at-best-known-tip.</done>
</task>

</tasks>

<verification>
Run:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_stay_current_next_action --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase69_fresh_idle_cycle_reports_current_at_best_known_tip --all-features
```
</verification>

<success_criteria>
- [ ] Fresh idle-at-tip cycles report current-at-best-known-tip.
- [ ] Stale-tip and no-progress include bounded next-action context.
- [ ] Existing recovery_action remains the source of recovery-specific guidance.
</success_criteria>
