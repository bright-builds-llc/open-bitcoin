---
phase: 52-operator-evidence-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - docs/operator/runtime-guide.md
  - docs/parity/release-readiness.md
  - .planning/v1.3-MILESTONE-AUDIT.md
  - .planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/52-operator-evidence-cleanup/52-01-SUMMARY.md
  - .planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - OBS-02
  - OBS-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-05-31T23-48-46
generated_at: 2026-06-01T00:32:53Z
must_haves:
  truths:
    - "Support evidence summarizes schema v2 live-smoke nested `result` fields for status, progress detection, typed no-progress cause, next action, header delta, and block delta."
    - "Support evidence preserves the old top-level allowlist fallback for older or hand-authored reports without letting it hide schema v2 nested `result` data."
    - "Support JSON and Markdown remain summary-only and do not embed raw live-smoke input, daemon tails, snapshots, options, or endpoint tables."
    - "`open-bitcoind` preflight wording describes durable-store preflight plus the explicit opt-in bounded mainnet sync worker while preserving non-claims about unattended production-node and packaged-service readiness."
    - "Operator/audit docs close D-02 and D-04 as deterministic cleanup without refreshing live public-network evidence or changing the Phase 53 boundary."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support.rs"
      provides: "Schema v2 nested-result live-smoke summary extraction with top-level fallback"
      contains: "LIVE_SMOKE_RESULT_SUMMARY_KEYS"
    - path: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      provides: "Named Live Smoke Markdown summary fields"
      contains: "Progress detected"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "Deterministic support-bundle tests for schema v2 summaries, redaction, fallback, and missing artifacts"
      contains: "open_bitcoin_support_bundle_summarizes_schema_v2_live_smoke_result"
    - path: "packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs"
      provides: "Deterministic daemon sync preflight message helper and test coverage"
      contains: "daemon_sync_preflight_message"
    - path: ".planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md"
      provides: "Phase 52 deterministic verification evidence"
      contains: "status: passed"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support.rs"
      to: "scripts/run-live-mainnet-smoke.ts"
      via: "schema v2 `result` field allowlist"
      pattern: "result.*status.*progressDetected.*maybeNoProgressCause.*nextAction.*headerDelta.*blockDelta"
    - from: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      to: "packages/open-bitcoin-cli/src/operator/support.rs"
      via: "LiveSmokeEvidence.summary JSON object"
      pattern: "live_smoke\\.summary"
    - from: "packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs"
      to: "open-bitcoind startup"
      via: "preflight -> report -> start_daemon_sync_worker"
      pattern: "preflight_daemon_sync.*report_daemon_sync_preflight.*start_daemon_sync_worker"
    - from: ".planning/v1.3-MILESTONE-AUDIT.md"
      to: ".planning/phases/52-operator-evidence-cleanup/52-01-SUMMARY.md"
      via: "D-02 and D-04 deterministic closure notes"
      pattern: "Phase 52"
---

<objective>
Close deterministic v1.3 audit tech debt D-02 and D-04 in operator-facing evidence without changing the public-network claim boundary.

Purpose: Reviewers should no longer have to reconcile a shallow support summary or stale daemon preflight message when auditing v1.3 evidence.
Output: Schema v2 support summaries, deterministic support/preflight tests, refreshed operator/audit wording, and Phase 52 verification artifacts.
</objective>

<scope_note>
This plan intentionally keeps the code, tests, docs, and traceability changes in
one wave because they close one tightly coupled audit finding pair: D-02 support
summary truth and D-04 daemon preflight truth. Splitting docs/bookkeeping into a
second plan would add handoff overhead without reducing implementation risk; the
tasks already sequence code/tests before traceability closeout.
</scope_note>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/52-operator-evidence-cleanup/52-CONTEXT.md
@.planning/phases/52-operator-evidence-cleanup/52-RESEARCH.md
@.planning/phases/48-support-evidence-and-operator-runbooks/48-CONTEXT.md
@.planning/phases/48-support-evidence-and-operator-runbooks/48-SUMMARY.md
@.planning/phases/51-live-smoke-fresh-status-integration/51-CONTEXT.md
@.planning/phases/51-live-smoke-fresh-status-integration/51-01-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
@packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
@docs/operator/runtime-guide.md
@docs/parity/release-readiness.md
@.planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md

Material repo rules read during planning: `AGENTS.md`, `AGENTS.bright-builds.md`,
`standards-overrides.md`, Bright Builds canonical `standards/index.md`,
`standards/core/architecture.md`, `standards/core/code-shape.md`,
`standards/core/verification.md`, `standards/core/testing.md`, and
`standards/languages/rust.md` at pinned commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`. No project-local skills were found
under `.claude/skills` or `.agents/skills`.

<interfaces>
Schema v2 live-smoke report result fields from `scripts/run-live-mainnet-smoke.ts`:

```typescript
result: {
  blockDelta: number;
  headerDelta: number;
  maybeNoProgressCause: NoProgressCause | null;
  message: string;
  nextAction: string;
  progressDetected: boolean;
  status: ReportStatus;
};
schema_version: 2;
```

Existing support extraction boundary in `packages/open-bitcoin-cli/src/operator/support.rs`:

```rust
fn collect_live_smoke_evidence(maybe_report_path: Option<&Path>) -> LiveSmokeEvidence;
fn live_smoke_summary(value: &Value) -> Option<Value>;
fn sanitize_json_value(value: &Value) -> Value;
fn redact_sensitive_text(text: &str) -> String;
```

Current top-level compatibility keys that must remain a fallback, not the schema v2 primary path:

```rust
const LIVE_SMOKE_SUMMARY_KEYS: &[&str] = &[
    "status",
    "maybeNoProgressCause",
    "maybe_no_progress_cause",
    "nextAction",
    "next_action",
    "reportPath",
    "report_path",
    "markdownPath",
    "markdown_path",
    "startedAtUnixSeconds",
    "started_at_unix_seconds",
    "finishedAtUnixSeconds",
    "finished_at_unix_seconds",
    "timeoutSeconds",
    "timeout_seconds",
    "pollSeconds",
    "poll_seconds",
    "manualPeers",
    "manual_peers",
    "generatedConfigPath",
    "generated_config_path",
];
```

Existing daemon startup order in `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs`:

```rust
if let Some(preflight) = preflight_daemon_sync(&runtime)? {
    report_daemon_sync_preflight(&preflight);
}
let maybe_sync_worker = start_daemon_sync_worker(&runtime)?;
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 52-01 | Task 1 | Full | Schema v2 nested `result` fields are primary. |
| D-02 | 52-01 | Task 1 | Full | Existing top-level allowlist remains fallback only. |
| D-03 | 52-01 | Task 1 | Full | Summary-only redaction and raw-field exclusion are tested. |
| D-04 | 52-01 | Task 1 | Full | Markdown renders named allowlisted summary values. |
| D-05 | 52-01 | Task 1 | Full | Schema v2 fixture includes nested result and raw secret-like data. |
| D-06 | 52-01 | Task 1 | Full | Missing report remains non-fatal unavailable evidence. |
| D-07 | 52-01 | Task 2 | Full | Preflight wording states durable store opened and enabled worker behavior. |
| D-08 | 52-01 | Task 2 | Full | Wording preserves production-node and packaged-service non-claims. |
| D-09 | 52-01 | Task 2 | Full | Unit test asserts exact preflight message contents. |
| D-10 | 52-01 | Task 3 | Full | Only stale operator/audit references are updated. |
| D-11 | 52-01 | Task 3 | Full | Phase 52 closes deterministic debt after code, docs, and verification; Phase 53 live refresh stays deferred. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Summarize schema v2 live-smoke result fields in support bundles</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <read_first>
    - .planning/phases/52-operator-evidence-cleanup/52-CONTEXT.md
    - .planning/phases/52-operator-evidence-cleanup/52-RESEARCH.md
    - .planning/v1.3-MILESTONE-AUDIT.md
    - .planning/phases/48-support-evidence-and-operator-runbooks/48-SUMMARY.md
    - scripts/run-live-mainnet-smoke.ts
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
  </read_first>
  <behavior>
    - Test 1: A schema v2 fixture with nested `result.status="no_progress"`, `result.progressDetected=false`, `result.maybeNoProgressCause="handshake_failure"`, `result.nextAction="Retry after rotating manual peers; rpcpassword=fixture-secret"`, `result.headerDelta=0`, and `result.blockDelta=0` produces `live_smoke.summary` from the nested `result` object first.
    - Test 2: The same fixture includes raw secret-like data under `daemon.stderrTail`, `options.rpcpassword`, `snapshots[].secret`, `preflight.checks[].message`, and `network_preflight.endpoint_outcomes[]`; JSON and Markdown output do not contain those raw strings, and the allowlisted secret-like `nextAction` is sanitized to `[redacted]`.
    - Test 3: An older top-level fixture with `status`, `maybeNoProgressCause`, and `nextAction` still summarizes through the compatibility fallback.
    - Test 4: A missing `--include-live-smoke-report` path remains `live_smoke.state="unavailable"` with a reason containing `does not exist` and does not fail support bundle generation.
  </behavior>
  <action>Implement D-01 through D-06. In `support.rs`, rename the existing `LIVE_SMOKE_SUMMARY_KEYS` to `LIVE_SMOKE_TOP_LEVEL_SUMMARY_KEYS` and add `LIVE_SMOKE_RESULT_SUMMARY_KEYS` with exactly these keys in this order: `status`, `progressDetected`, `maybeNoProgressCause`, `nextAction`, `headerDelta`, `blockDelta`. Add a helper named `live_smoke_summary_from_result(maybe_result: Option<&Value>) -> Option<Value>` that reads only those six keys from a nested object and applies `sanitize_json_value` to every copied value. Add a helper named `live_smoke_summary_from_top_level(object: &Map<String, Value>) -> Option<Value>` that preserves the old top-level allowlist fallback. Change `live_smoke_summary` so it checks `object.get("result")` with `live_smoke_summary_from_result` before calling `live_smoke_summary_from_top_level`; only return `{"status":"summary_fields_unavailable"}` when neither path finds any allowlisted summary fields. Do not copy `daemon`, `daemon.stderrTail`, `daemon.stdoutTail`, `options`, `snapshots`, `preflight`, `network_preflight.endpoint_outcomes`, manual peer endpoint tables, or raw report input into the summary. In `render.rs`, replace the opaque `- Summary: {json}` line with named Markdown rows when `summary` is an object: `- Status: ...`, `- Progress detected: ...`, `- No-progress cause: ...`, `- Next action: ...`, `- Header delta: ...`, and `- Block delta: ...`; omit missing fields for compatibility fallback rather than inventing values. Keep report path and unavailable reason lines unchanged. In `operator_binary.rs`, add or update tests with exact names `open_bitcoin_support_bundle_summarizes_schema_v2_live_smoke_result`, `open_bitcoin_support_bundle_preserves_top_level_live_smoke_fallback`, and `open_bitcoin_support_bundle_keeps_missing_live_smoke_report_unavailable`. Use explicit `// Arrange`, `// Act`, and `// Assert` comments in each new or substantially edited Rust test.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --test operator_binary</automated>
    <automated>rg -n "LIVE_SMOKE_RESULT_SUMMARY_KEYS|live_smoke_summary_from_result|live_smoke_summary_from_top_level" packages/open-bitcoin-cli/src/operator/support.rs</automated>
    <automated>rg -n "Progress detected|No-progress cause|Header delta|Block delta" packages/open-bitcoin-cli/src/operator/support/render.rs</automated>
    <automated>rg -n "open_bitcoin_support_bundle_summarizes_schema_v2_live_smoke_result|open_bitcoin_support_bundle_preserves_top_level_live_smoke_fallback|open_bitcoin_support_bundle_keeps_missing_live_smoke_report_unavailable" packages/open-bitcoin-cli/tests/operator_binary.rs</automated>
  </verify>
  <acceptance_criteria>
    - `live_smoke.summary.status` equals `no_progress` for the schema v2 fixture.
    - `live_smoke.summary.progressDetected` equals `false` for the schema v2 fixture.
    - `live_smoke.summary.maybeNoProgressCause` equals `handshake_failure` for the schema v2 fixture.
    - `live_smoke.summary.headerDelta` equals `0` and `live_smoke.summary.blockDelta` equals `0` for the schema v2 fixture.
    - `live_smoke.summary.nextAction` equals `[redacted]` when the allowlisted string contains `rpcpassword=fixture-secret`.
    - `support-evidence.md` contains `Progress detected`, `No-progress cause`, `Header delta`, and `Block delta`.
    - JSON and Markdown do not contain `live-smoke-secret`, `fixture-secret`, raw daemon tails, raw options, raw snapshots, or endpoint outcome table details from the fixture.
    - A top-level compatibility fixture still exposes top-level `status`, `maybeNoProgressCause`, and `nextAction`.
    - A missing report path leaves support generation successful with `live_smoke.state="unavailable"` and a reason containing `does not exist`.
  </acceptance_criteria>
  <done>Support bundles expose the schema v2 nested result summary values reviewers need while preserving fallback, redaction, and non-fatal missing-artifact behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh daemon sync preflight wording and guard it with a unit test</name>
  <files>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</files>
  <read_first>
    - .planning/phases/52-operator-evidence-cleanup/52-CONTEXT.md
    - .planning/phases/52-operator-evidence-cleanup/52-RESEARCH.md
    - .planning/v1.3-MILESTONE-AUDIT.md
    - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
    - docs/parity/release-readiness.md
    - docs/operator/runtime-guide.md
  </read_first>
  <behavior>
    - Test 1: `daemon_sync_preflight_message` for enabled mainnet sync contains `opened durable store`, `explicit opt-in bounded mainnet sync worker`, `not unattended production-node operation`, and `not a packaged-service guarantee`.
    - Test 2: The message includes `mode=mainnet-ibd`, the quoted datadir, `best_header_height=12`, and `best_block_height=3`.
    - Test 3: The message does not contain the stale phrase `peer transport and unattended full IBD are not started by this phase`.
  </behavior>
  <action>Implement D-07 through D-09. Add a pure helper named `daemon_sync_preflight_message(preflight: &DaemonSyncPreflight) -> String` next to `report_daemon_sync_preflight`. The exact message shape should be: `open-bitcoind mainnet sync preflight opened durable store: mode={mode}, datadir="{path}", best_header_height={best_header_height}, best_block_height={best_block_height}; enabled startup will run the explicit opt-in bounded mainnet sync worker. This is not unattended production-node operation and is not a packaged-service guarantee.` Change `report_daemon_sync_preflight` to print only `daemon_sync_preflight_message(preflight)`. Rename the existing test `enabled_sync_opens_durable_runtime_without_starting_transport` to `enabled_sync_preflight_opens_durable_runtime_before_worker_startup` so test names do not preserve stale wording. Add a unit test named `enabled_sync_preflight_message_describes_opt_in_worker_without_production_claim` that constructs a `DaemonSyncPreflight` with `mode: DaemonSyncConfig::mainnet_ibd().mode`, `data_dir: PathBuf::from("/tmp/open-bitcoin-mainnet")`, `best_header_height: 12`, and `best_block_height: 3`, then asserts the strings listed in the behavior block. Do not change worker activation, transport code, sync control, RPC binding, or the missing-datadir error.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind enabled_sync_preflight_message_describes_opt_in_worker_without_production_claim</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind enabled_sync_preflight</automated>
    <automated>rg -n "daemon_sync_preflight_message|opened durable store|explicit opt-in bounded mainnet sync worker|not unattended production-node operation|not a packaged-service guarantee" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</automated>
    <automated>bash -lc '! rg -n "peer transport and unattended full IBD are not started by this phase" packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs'</automated>
  </verify>
  <acceptance_criteria>
    - `daemon_sync_preflight_message` exists and is called by `report_daemon_sync_preflight`.
    - The unit test `enabled_sync_preflight_message_describes_opt_in_worker_without_production_claim` passes.
    - The message contains `opened durable store`, `explicit opt-in bounded mainnet sync worker`, `not unattended production-node operation`, and `not a packaged-service guarantee`.
    - The stale phrase `peer transport and unattended full IBD are not started by this phase` is absent from `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs`.
    - No public-network sync behavior is added to default verification or to `open-bitcoind` startup beyond the existing explicit opt-in worker path.
  </acceptance_criteria>
  <done>`open-bitcoind` now reports the current opt-in bounded worker truth without implying unattended production-node or packaged-service readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator/audit traceability and run deterministic verification</name>
  <files>docs/operator/runtime-guide.md, docs/parity/release-readiness.md, .planning/v1.3-MILESTONE-AUDIT.md, .planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/52-operator-evidence-cleanup/52-01-SUMMARY.md, .planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md, docs/metrics/lines-of-code.md</files>
  <read_first>
    - .planning/phases/52-operator-evidence-cleanup/52-CONTEXT.md
    - .planning/phases/52-operator-evidence-cleanup/52-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/v1.3-MILESTONE-AUDIT.md
    - .planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md
    - .planning/phases/51-live-smoke-fresh-status-integration/51-01-SUMMARY.md
    - docs/operator/runtime-guide.md
    - docs/parity/release-readiness.md
  </read_first>
  <action>Implement D-10 and D-11 after Tasks 1 and 2 pass. In `docs/operator/runtime-guide.md`, update the Support Evidence Bundles redaction boundaries to say the support bundle copies only schema v2 `result.status`, `result.progressDetected`, `result.maybeNoProgressCause`, `result.nextAction`, `result.headerDelta`, and `result.blockDelta` when a schema v2 live-smoke report is supplied, and keeps top-level report fields only as an older/hand-authored compatibility fallback. The same section must state that raw live-smoke input, daemon stdout/stderr tails, raw status snapshots, raw options, and endpoint tables are not embedded. Also update daemon sync wording in the runtime guide to use the phrase `explicit opt-in bounded mainnet sync worker` and preserve the phrase `not unattended production-node operation` or equivalent non-claim. In `docs/parity/release-readiness.md`, update the Redacted support evidence row and Phase 50/51/52 evidence closeout wording so reviewers know Phase 52 closes D-02 and D-04 deterministically; do not add a new successful live-mainnet progress claim. In `.planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md`, preserve the historical stale preflight quote and `summary_fields_unavailable` note, but add nearby sections named exactly `## Phase 52 Support Summary Amendment` and `## Phase 52 Preflight Wording Amendment` that point to Phase 52 deterministic cleanup for future support bundles and future `open-bitcoind` preflight output. In `.planning/v1.3-MILESTONE-AUDIT.md`, mark D-02 and D-04 as closed by Phase 52 while keeping D-01 and D-03 open for Phase 53. In `.planning/REQUIREMENTS.md`, change only the v1.3 tech-debt follow-up table statuses for D-02 and D-04 from `Pending` to `Complete`; leave D-01 and D-03 `Pending`. In `.planning/ROADMAP.md` and `.planning/STATE.md`, close Phase 52 only after the verification commands below pass. Create `52-01-SUMMARY.md` with implementation summary, files changed, verification commands, deviations, residual risk, and `## Self-Check: PASSED`. Create `52-VERIFICATION.md` with `status: passed`, requirement verdicts for OBS-02 and OBS-03, D-02/D-04 closure evidence, commands run, and residual risk. If `bash scripts/verify.sh` refreshes `docs/metrics/lines-of-code.md`, keep the generated freshness change. Do not run live public-network evidence, do not add `run-live-mainnet-smoke` to `bash scripts/verify.sh`, and do not close Phase 53 D-01/D-03 items.</action>
  <verify>
    <automated>rg -n "result\\.status|result\\.progressDetected|result\\.maybeNoProgressCause|result\\.nextAction|result\\.headerDelta|result\\.blockDelta|raw live-smoke input|daemon stdout/stderr tails|endpoint tables" docs/operator/runtime-guide.md docs/parity/release-readiness.md</automated>
    <automated>rg -n "Phase 52 Support Summary Amendment|Phase 52 Preflight Wording Amendment" .planning/phases/50-public-mainnet-progress-evidence-closeout/50-UAT.md</automated>
    <automated>rg -n "D-02.*Complete|D-04.*Complete|D-01.*Pending|D-03.*Pending" .planning/REQUIREMENTS.md</automated>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --test operator_binary</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind enabled_sync_preflight_message_describes_opt_in_worker_without_production_claim</automated>
    <automated>bun run scripts/check-v1.3-release-boundaries.ts</automated>
    <automated>bash -lc '! rg -n "run-live-mainnet-smoke" scripts/verify.sh'</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator/runtime-guide.md` names all six schema v2 `result.*` summary fields and excludes raw live-smoke input, daemon tails, snapshots, options, and endpoint tables.
    - `docs/parity/release-readiness.md` states Phase 52 closes support-summary/preflight wording debt without claiming new public-mainnet progress.
    - `50-UAT.md` contains `Phase 52 Support Summary Amendment` and `Phase 52 Preflight Wording Amendment`.
    - `.planning/REQUIREMENTS.md` shows D-02 and D-04 as `Complete`, while D-01 and D-03 remain `Pending`.
    - `.planning/v1.3-MILESTONE-AUDIT.md` records D-02 and D-04 closure and still leaves Phase 53 responsible for D-01 and D-03.
    - `.planning/phases/52-operator-evidence-cleanup/52-01-SUMMARY.md` contains `## Self-Check: PASSED`.
    - `.planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md` contains `status: passed` and requirement verdicts for `OBS-02` and `OBS-03`.
    - `bash scripts/verify.sh` passes and still does not contain `run-live-mainnet-smoke`.
  </acceptance_criteria>
  <done>Operator/audit traceability closes D-02 and D-04 deterministically, Phase 53 remains responsible for live evidence refresh, and repo-native verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator-supplied live-smoke report -> support evidence | Untrusted local JSON report content is parsed and summarized into shareable support JSON/Markdown. |
| daemon preflight state -> operator stderr | Runtime startup state is condensed into a human-readable preflight message that can influence operator expectations. |
| audit debt -> release claim docs | Deterministic cleanup wording can accidentally broaden or overclaim v1.3 public-mainnet readiness. |
| missing/malformed artifacts -> support command | Optional support inputs must not prevent operators from generating other evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-52-01-01 | Information Disclosure | `packages/open-bitcoin-cli/src/operator/support.rs` | mitigate | Copy only the six schema v2 `result` allowlist keys and existing top-level fallback keys; reuse `sanitize_json_value` and add fixture assertions proving raw report, daemon tails, options, snapshots, endpoint tables, and secret-like strings are absent from JSON/Markdown. |
| T-52-01-02 | Denial of Service | `collect_live_smoke_evidence` | mitigate | Preserve missing, unreadable, and malformed report handling as non-fatal `unavailable` evidence with a reason; add a deterministic missing-report assertion. |
| T-52-01-03 | Repudiation | `render_support_markdown` | mitigate | Render named audit fields for status, progress detection, typed no-progress cause, next action, header delta, and block delta instead of opaque or unavailable summaries. |
| T-52-01-04 | Spoofing / Repudiation | `open-bitcoind` preflight message | mitigate | Add `daemon_sync_preflight_message` and unit coverage asserting durable-store preflight, explicit opt-in bounded worker wording, and absence of the stale peer-transport phrase. |
| T-52-01-05 | Elevation of Privilege | operator/audit docs | mitigate | Preserve non-claims for unattended production-node operation, packaged service readiness, inbound serving, relay, hosted/public dashboard, GUI, migration apply mode, and production-funds wallet use. |
| T-52-01-06 | Denial of Service | default verification | mitigate | Keep live public-network checks outside `bash scripts/verify.sh`; assert the verify script does not contain `run-live-mainnet-smoke`. |
</threat_model>

<must_haves>
## Goal-Backward Truths

- Support bundle JSON and Markdown expose schema v2 result summary fields instead of `summary_fields_unavailable` for current live-smoke reports.
- Support summaries are allowlisted and redacted; raw live-smoke reports and high-volume/raw evidence remain out of the bundle.
- `open-bitcoind` preflight output truthfully describes the current explicit opt-in bounded mainnet sync worker without claiming unattended production-node or packaged-service readiness.
- Audit and operator docs close D-02 and D-04 without touching Phase 53 live evidence refresh scope.

## Required Artifacts

- `packages/open-bitcoin-cli/src/operator/support.rs` contains `LIVE_SMOKE_RESULT_SUMMARY_KEYS` and nested-result-first summary extraction.
- `packages/open-bitcoin-cli/src/operator/support/render.rs` contains named Live Smoke Markdown fields.
- `packages/open-bitcoin-cli/tests/operator_binary.rs` contains deterministic schema v2, fallback, redaction, and missing-report support tests.
- `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` contains `daemon_sync_preflight_message` and its unit test.
- `.planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md` records passed OBS-02/OBS-03 verification.

## Key Links

- Support extraction reads schema v2 fields from `scripts/run-live-mainnet-smoke.ts` report `result`.
- Markdown rendering uses `LiveSmokeEvidence.summary` from support extraction, not raw report text.
- Preflight message helper is called by `report_daemon_sync_preflight` before `start_daemon_sync_worker`.
- Audit and requirements traceability point D-02 and D-04 closure to Phase 52, leaving D-01 and D-03 for Phase 53.
</must_haves>

<verification>
Run after all tasks:

```bash
cargo fmt --manifest-path packages/Cargo.toml --all
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --test operator_binary
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind enabled_sync_preflight_message_describes_opt_in_worker_without_production_claim
bun run scripts/check-v1.3-release-boundaries.ts
bash -lc '! rg -n "run-live-mainnet-smoke" scripts/verify.sh'
bash scripts/verify.sh
```

Do not run live public-network smoke as part of Phase 52 default verification.
</verification>

<success_criteria>
- OBS-03: Support bundles summarize schema v2 nested live-smoke `result` fields, preserve top-level fallback, and keep raw report inputs redacted or omitted.
- OBS-02: `open-bitcoind` preflight wording matches current explicit opt-in bounded sync worker behavior and preserves production-node/package non-claims.
- D-02 and D-04 are marked complete in tech-debt traceability after deterministic code, docs, and verification pass.
- D-01 and D-03 remain pending for Phase 53.
- `bash scripts/verify.sh` passes and remains public-network-free.
</success_criteria>

<output>
After completion, create `.planning/phases/52-operator-evidence-cleanup/52-01-SUMMARY.md` and `.planning/phases/52-operator-evidence-cleanup/52-VERIFICATION.md`.
</output>
