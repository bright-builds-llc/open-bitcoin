---
phase: 125-compact-download-verification-traceability-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-active-milestone-verification-traceability.ts
  - scripts/check-active-milestone-verification-traceability.test.ts
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 125-2026-07-17T13-21-01
generated_at: 2026-07-17T14:26:30.175Z
must_haves:
  truths:
    - "An active requirement completed by summary evidence cannot remain absent from every lifecycle-valid active-phase verification artifact."
    - "Pending unsummarized, deferred, and archived requirements do not activate the orphan gate."
    - "Malformed ownership and invalid lifecycle metadata fail closed instead of masking a verification orphan."
  artifacts:
    - path: "scripts/check-active-milestone-verification-traceability.ts"
      provides: "Reusable active-milestone verification-orphan parser and checker"
      exports:
        - "checkActiveMilestoneVerificationTraceability"
    - path: "scripts/check-active-milestone-verification-traceability.test.ts"
      provides: "Temporary-filesystem mutation coverage and real-repository staged smoke test"
  key_links:
    - from: "scripts/check-active-milestone-verification-traceability.ts"
      to: ".planning/REQUIREMENTS.md"
      via: "active checklist and exact-once traceability parsing"
      pattern: "REQUIREMENTS\\.md"
    - from: "scripts/check-active-milestone-verification-traceability.ts"
      to: ".planning/phases/*/*-SUMMARY.md"
      via: "requirements-completed activation"
      pattern: "requirements-completed"
    - from: "scripts/check-active-milestone-verification-traceability.ts"
      to: ".planning/phases/*/*-VERIFICATION.md"
      via: "exact-token lifecycle-valid coverage"
      pattern: "VERIFICATION\\.md"
---

<objective>
Create the reusable deterministic guard that detects summary-complete active requirements missing from lifecycle-valid verification evidence.

Purpose: Prevent the exact traceability failure that orphaned RCN-04, RCN-05, and RCN-06 while allowing genuinely pending and archived work to remain outside the gate.

This plan is prerequisite infrastructure and intentionally owns no requirement promotion; Plan 04 is the sole completion/promotional owner for RCN-04, RCN-05, and RCN-06.

Output: A Bun/TypeScript checker and focused fixture suite. No Rust, runtime, or milestone-status mutation occurs in this plan.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/125-compact-download-verification-traceability-closure/125-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@scripts/check-phase98-traceability-reconciliation.ts
@scripts/check-phase98-traceability-reconciliation.test.ts
@scripts/check-phase124-milestone-closeout-reconciliation.ts
@scripts/check-phase124-milestone-closeout-reconciliation.test.ts
@scripts/check-phase117-parity-uat-release-boundary.ts
@scripts/check-phase117-parity-uat-release-boundary.test.ts

<interfaces>
The checker must export:

```typescript
type CheckActiveMilestoneVerificationTraceabilityOptions = {
  maybeRootDir?: string;
};

export function checkActiveMilestoneVerificationTraceability(
  maybeOptions?: CheckActiveMilestoneVerificationTraceabilityOptions,
): string[];
```

The CLI must print `Active milestone verification traceability checker passed.` on success and prefix every failure with `verification-traceability:` before exiting 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse the active milestone and enforce lifecycle-valid verification coverage</name>
  <read_first>
    scripts/check-phase98-traceability-reconciliation.ts
    scripts/check-phase124-milestone-closeout-reconciliation.ts
    scripts/check-phase117-parity-uat-release-boundary.ts
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/115-missing-transaction-round-trip-fallback-and-validation-handoff/115-01-SUMMARY.md
    .planning/phases/115-missing-transaction-round-trip-fallback-and-validation-handoff/115-02-SUMMARY.md
    .planning/phases/115-missing-transaction-round-trip-fallback-and-validation-handoff/115-03-SUMMARY.md
    .planning/phases/115-missing-transaction-round-trip-fallback-and-validation-handoff/115-VERIFICATION.md
    .planning/phases/125-compact-download-verification-traceability-closure/125-CONTEXT.md
  </read_first>
  <files>scripts/check-active-milestone-verification-traceability.ts, scripts/check-active-milestone-verification-traceability.test.ts</files>
  <behavior>
    - "A requirement activates only when it is in the non-deferred checklist, has exactly one traceability row, and an active-roadmap phase SUMMARY frontmatter lists the exact ID in requirements-completed."
    - "An activated requirement passes only when an exact token appears in a passed VERIFICATION artifact from a roadmap-listed active phase whose status, lifecycle_validated, lifecycle_mode, and phase_lifecycle_id match that phase's CONTEXT."
    - "Pending requirements without summary completion, deferred FUT IDs, archived milestone artifacts, and unsummarized requirements are ignored."
    - "Duplicate checklist IDs, duplicate/missing traceability rows, non-roadmap owners, malformed frontmatter, and invalid lifecycle masking return stable failures."
  </behavior>
  <action>
Create `scripts/check-active-milestone-verification-traceability.test.ts` first with the shared temporary-repository fixture builder and one test named `complete fixture succeeds` that constructs a fully covered active corpus and fails before the checker exists. Then create `scripts/check-active-milestone-verification-traceability.ts` as a filesystem-local Bun checker per D-05 through D-08 and make that initial test pass. Parse only the `## Active Milestone:` roadmap section and require each `- [x| ] **Phase N:` entry to have one unique numeric phase. Parse only `.planning/REQUIREMENTS.md` content between `## v2.1 Requirements` and `## Deferred Requirements`; accept checklist rows matching `- [x| ] **[A-Z]+-\d+**:` and traceability rows matching `| ID | Phase N | Complete|Pending |`, and require every active checklist ID to have exactly one traceability row owned by a roadmap-listed phase.

Discover summaries and verification files only beneath the active phase directories resolved by numeric prefix. Extract the first YAML frontmatter block; support inline and block-list `requirements-completed` values, but reject malformed or duplicate lifecycle fields. Activate an active requirement when any in-scope SUMMARY lists its exact ID, regardless of the checklist/traceability Pending marker; this is what makes RCN-04/05/06 visible before projection. For each verification artifact, require frontmatter `status: passed`, `lifecycle_validated: true`, and exact `lifecycle_mode` plus `phase_lifecycle_id` equality with that phase's CONTEXT before its body/frontmatter may cover an ID. Match IDs with token boundaries so `RCN-04` does not match `RCN-040`. Never scan `.planning/milestones/` or archived phase trees. If an invalid-lifecycle artifact contains an activated ID, report both the lifecycle error and the uncovered ID; it must never mask the orphan.

Keep parsing/decision helpers as pure data-in/data-out functions and the CLI as the thin filesystem shell. Export the exact interface above with `maybeRootDir` and `maybeOptions`; use `maybe...` names for all other nullable or optional internal values, early returns, no new dependency, no network access, and no Python. Preserve Phase 115 files byte-for-byte per D-02 and make no Rust change per D-16.
  </action>
  <verify>
    <automated>bun test scripts/check-active-milestone-verification-traceability.test.ts --test-name-pattern "complete fixture succeeds"</automated>
  </verify>
  <acceptance_criteria>
    `scripts/check-active-milestone-verification-traceability.ts` exports `checkActiveMilestoneVerificationTraceability`.
    `scripts/check-active-milestone-verification-traceability.test.ts` exists before implementation and contains the runnable `complete fixture succeeds` RED/GREEN test plus the reusable fixture builder that Task 2 expands.
    `rg -n "requirements-completed|lifecycle_validated|phase_lifecycle_id|Active Milestone|Deferred Requirements" scripts/check-active-milestone-verification-traceability.ts` finds all five parser boundaries.
    The CLI success string is exactly `Active milestone verification traceability checker passed.`.
    `git diff -- packages/open-bitcoin-network .planning/phases/115-missing-transaction-round-trip-fallback-and-validation-handoff` is empty.
  </acceptance_criteria>
  <done>The reusable parser enforces D-05 through D-08 and exposes the three current verification orphans without changing runtime or historical evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every activation, exclusion, ownership, and lifecycle branch</name>
  <read_first>
    scripts/check-active-milestone-verification-traceability.ts
    scripts/check-phase98-traceability-reconciliation.test.ts
    scripts/check-phase124-milestone-closeout-reconciliation.test.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/125-compact-download-verification-traceability-closure/125-CONTEXT.md
  </read_first>
  <files>scripts/check-active-milestone-verification-traceability.test.ts, docs/metrics/lines-of-code.md</files>
  <behavior>
    - "Complete synthetic corpus succeeds."
    - "Each activated missing ID fails independently."
    - "Pending unsummarized, deferred, and archived token collisions remain excluded."
    - "Duplicate ownership, malformed metadata, invalid-lifecycle masking, and near-token collisions fail."
    - "The real repository reports exactly RCN-04/05/06 before Phase 125 verification exists and zero failures after it exists."
  </behavior>
  <action>
Expand the temporary-repository fixture builder and initial `complete fixture succeeds` test created in Task 1, keeping explicit Arrange/Act/Assert comments. Add one focused test per remaining concern required by D-09: independent missing `RCN-04`, `RCN-05`, and `RCN-06`; pending-unsummarized exclusion; deferred FUT exclusion; archived verification collision exclusion; duplicate checklist and duplicate traceability ownership; owner phase missing from active roadmap; malformed CONTEXT/VERIFICATION lifecycle metadata; invalid-lifecycle verification containing the only token; exact-token rejection of `RCN-040`; and lifecycle-valid coverage success.

Add one real-repository smoke test. Before `125-VERIFICATION.md` exists, it must assert the only uncovered activated IDs are exactly `RCN-04`, `RCN-05`, and `RCN-06`; after that artifact exists, it must assert an empty failure list. The branch must use `existsSync` only to distinguish the documented Phase 125 stage and must still reject any extra failure, so it cannot mask unrelated drift. Keep each mutation test to one concern and clean all temporary roots in `afterEach`.

After the tracked TypeScript checker and test reach their final passing state, regenerate the intentionally tracked LOC artifact with the exact repo-owned command `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`, then require the same command with `--check` to pass. Do not hand-edit generated counts.
  </action>
  <verify>
    <automated>bun test scripts/check-active-milestone-verification-traceability.test.ts &amp;&amp; bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check</automated>
  </verify>
  <acceptance_criteria>
    `bun test scripts/check-active-milestone-verification-traceability.test.ts` exits 0 before Phase 125 evidence exists and after it exists.
    `rg -n "RCN-04|RCN-05|RCN-06|pending|deferred|archived|duplicate|malformed|invalid.lifecycle|RCN-040|real.repository" scripts/check-active-milestone-verification-traceability.test.ts` finds coverage for every required mutation class.
    The real-repository smoke assertion permits exactly three named orphan failures before closure and exactly zero failures after closure.
    `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check` reports the tracked LOC artifact is current after the new TypeScript files are visible to git.
  </acceptance_criteria>
  <done>D-09 is covered by focused synthetic mutations plus a strict staged real-repository smoke test.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Planning Markdown → checker parser | Repository text is untrusted input and may contain malformed ownership or spoofed tokens. |
| Historical artifacts → active milestone corpus | Archived prose must not authorize an active requirement. |
| Verification frontmatter → coverage decision | Lifecycle metadata must be validated before evidence is trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-125-01-01 | Spoofing | Verification token lookup | mitigate | Exact token boundaries plus active-roadmap directory restriction prevent archived/near-token masking. |
| T-125-01-02 | Tampering | Requirements ownership parser | mitigate | Fail on duplicate/malformed checklist and traceability rows and non-roadmap owners. |
| T-125-01-03 | Repudiation | Lifecycle provenance | mitigate | Require passed, lifecycle_validated, mode, and lifecycle ID equality with CONTEXT. |
| T-125-01-04 | Denial of Service | Local corpus scan | accept | Corpus is repository-local and bounded to active roadmap phase directories; no recursive archive scan or network I/O. |
| T-125-01-05 | Information Disclosure | Checker diagnostics | mitigate | Emit repository-relative artifact names and stable reason codes only; never print file contents or environment values. |
| T-125-01-06 | Elevation of Privilege | Evidence authorization | mitigate | Only lifecycle-valid verification owned by an active-roadmap phase can authorize coverage; summaries and archived prose cannot self-authorize. |
</threat-model>

<verification>
- `bun test scripts/check-active-milestone-verification-traceability.test.ts`
- `git diff --check`
- Confirm no Phase 115 or Rust path changed.
</verification>

<success-criteria>
- The checker dynamically derives active requirements, ownership, activation, and lifecycle-valid evidence without a v2.1 ID manifest.
- The fixture matrix covers every D-06 through D-09 branch with stable failure messages.
- The real repository exposes exactly the three intended pre-closure orphans.
- Phase 115 remains immutable and Phase 126 remains untouched.
</success-criteria>

<output>
After completion, create `.planning/phases/125-compact-download-verification-traceability-closure/125-01-SUMMARY.md`.
</output>
