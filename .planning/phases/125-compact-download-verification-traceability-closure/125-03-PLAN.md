---
phase: 125-compact-download-verification-traceability-closure
plan: "03"
type: execute
wave: 2
depends_on:
  - "125-01"
  - "125-02"
files_modified:
  - scripts/check-phase124-milestone-gap-closure.ts
  - scripts/check-phase124-milestone-gap-closure.test.ts
  - scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 125-2026-07-17T13-21-01
generated_at: 2026-07-17T14:26:30.175Z
must_haves:
  truths:
    - "The Phase 124 compatibility checker accepts every legal Phase 125 lifecycle state, including verification evidence written before requirement promotion and the promoted-pre-summary Phase 126 route."
    - "Mixed counts, artifact presence, lifecycle provenance, plan progress, and routing cannot be accepted as a legal stage."
    - "The live 30/39 repository passes the focused Phase 124 compatibility matrix as pre_verification before any Phase 125 verification or metadata promotion."
    - "The active verification-orphan CLI correctly remains failing and absent from scripts/verify.sh until Plan 04 creates lifecycle-valid Phase 125 evidence."
  artifacts:
    - path: "scripts/check-phase124-milestone-gap-closure.ts"
      provides: "Five-stage Phase 125 lifecycle parser and reconciliation rules"
      exports:
        - "verifyPhase124GapClosureStage"
    - path: "scripts/check-phase124-milestone-gap-closure.test.ts"
      provides: "Passing fixtures and one-concern mutations for all five stages"
    - path: "scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts"
      provides: "Stage-selectable Phase 125 planning corpus"
  key_links:
    - from: "scripts/check-phase124-milestone-gap-closure.ts"
      to: ".planning/phases/125-compact-download-verification-traceability-closure"
      via: "plan, summary, and lifecycle-valid verification artifact classification"
      pattern: "verification_written_pre_promotion"
    - from: "scripts/check-phase124-milestone-gap-closure.test.ts"
      to: "scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts"
      via: "stage-selectable temporary repository fixtures"
      pattern: "maybePhase125Stage"
    - from: "scripts/check-phase124-milestone-closeout-reconciliation.ts"
      to: "scripts/check-phase124-milestone-gap-closure.ts"
      via: "existing verifyPhase124GapClosureStage compatibility entrypoint"
      pattern: "verifyPhase124GapClosureStage"
    - from: "scripts/check-phase124-milestone-gap-closure.ts"
      to: ".planning/ROADMAP.md"
      via: "stage-specific primary-route validation"
      pattern: "/gsd-execute-phase (125|126)"
---

<objective>
Make the existing Phase 124 reconciliation guard understand the complete Phase 125 lifecycle and prove the current pre-verification projection.

Purpose: Remove the invalid four-stage jump that made `125-VERIFICATION.md` incompatible with the still-pending 30/39 projection, while leaving the active verification-orphan CLI correctly failing and unwired until evidence exists.

This plan is prerequisite compatibility infrastructure and intentionally owns no requirement promotion; Plan 04 is the sole completion/promotional owner for RCN-04, RCN-05, and RCN-06.

Output: A mutation-tested five-stage tagged union, stage-selectable fixtures, a focused `pre_verification` compatibility proof, and a fresh tracked LOC report after the TypeScript changes.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/125-compact-download-verification-traceability-closure/125-CONTEXT.md
@.planning/phases/125-compact-download-verification-traceability-closure/125-01-SUMMARY.md
@.planning/phases/125-compact-download-verification-traceability-closure/125-02-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v2.1-MILESTONE-AUDIT.md
@scripts/check-active-milestone-verification-traceability.ts
@scripts/check-active-milestone-verification-traceability.test.ts
@scripts/check-phase124-milestone-gap-closure.ts
@scripts/check-phase124-milestone-gap-closure.test.ts
@scripts/check-phase124-milestone-closeout-reconciliation.ts
@scripts/check-phase124-milestone-closeout-reconciliation.test.ts
@scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
@scripts/check-phase117-parity-uat-release-boundary.ts
@scripts/check-phase117-parity-uat-release-boundary.test.ts
@scripts/verify.sh
@docs/metrics/lines-of-code.md

<interfaces>
The stage parser must produce a discriminated union with exactly these tags:

```typescript
type Phase125LifecycleStage =
  | { kind: "planned"; planCount: 4; summaryCount: 0; verificationPresent: false }
  | {
      kind: "pre_verification";
      planCount: 4;
      summaryCount: 1 | 2 | 3;
      verificationPresent: false;
    }
  | {
      kind: "verification_written_pre_promotion";
      planCount: 4;
      summaryCount: 3;
      verificationPresent: true;
    }
  | {
      kind: "post_verification";
      planCount: 4;
      summaryCount: 3;
      verificationPresent: true;
    }
  | {
      kind: "post_summary";
      planCount: 4;
      summaryCount: 4;
      verificationPresent: true;
    };
```

`verifyPhase124GapClosureStage(...)` remains the compatibility entrypoint consumed by `scripts/check-phase124-milestone-closeout-reconciliation.ts`. New nullable or optional stage parsers must use names such as `maybeParsePhase125LifecycleStage`; new optional fixture fields must use names such as `maybePhase125Stage`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace count-driven branching with the five-stage lifecycle contract</name>
  <read_first>
    scripts/check-phase124-milestone-gap-closure.ts
    scripts/check-phase124-milestone-gap-closure.test.ts
    scripts/check-phase124-milestone-closeout-reconciliation.ts
    scripts/check-phase124-milestone-closeout-reconciliation.test.ts
    scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts
    scripts/check-active-milestone-verification-traceability.ts
    scripts/check-active-milestone-verification-traceability.test.ts
    scripts/check-phase117-parity-uat-release-boundary.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
    .planning/phases/125-compact-download-verification-traceability-closure/125-CONTEXT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/v2.1-MILESTONE-AUDIT.md
    AGENTS.md
    AGENTS.bright-builds.md
    standards/core/architecture.md
    standards/core/code-shape.md
    standards/core/testing.md
    standards/languages/typescript-javascript.md
  </read_first>
  <files>scripts/check-phase124-milestone-gap-closure.ts, scripts/check-phase124-milestone-gap-closure.test.ts, scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts, docs/metrics/lines-of-code.md</files>
  <behavior>
    - "Each of planned, pre_verification, verification_written_pre_promotion, post_verification, and post_summary has a passing Arrange/Act/Assert fixture."
    - "A lifecycle-valid 125-VERIFICATION.md with 30/39 requirements is accepted only as verification_written_pre_promotion."
    - "Every stage rejects wrong requirement counts, audit scores, phase counts, plan or summary cardinality, verification presence/provenance, Phase 126 status, or route, including premature Phase 126 routing before promotion and stale Phase 125 routing after promotion."
    - "Phase 117 remains the final visible and executable release-boundary/no-claim gate at every stage."
  </behavior>
  <action>
Replace the fixed 30-complete/9-pending branch in `scripts/check-phase124-milestone-gap-closure.ts` with a parser that derives a `Phase125LifecycleStage` from artifact presence and validated metadata before enforcing the projection. Use a pure parse/decision core with a thin filesystem shell. Parse exactly four Phase 125 plan files, Phase 125 summaries by exact `125-NN-SUMMARY.md` names, and lifecycle-valid `125-VERIFICATION.md`; never infer a stage from counts alone. Reject duplicate or malformed frontmatter and reject any summary whose plan number is not one of 01 through 04.

Encode these five and only these legal projections per D-10, D-13, D-14, and D-15:

| Stage | Plans / summaries | Verification | Requirements / traceability | Audit / phase projection | Phase 125 progress and primary route |
| --- | --- | --- | --- | --- | --- |
| `planned` | exactly 4 / exactly 0 | absent | 30 checked + Complete, 9 unchecked + Pending; RCN-04/05/06 Pending | `30/39`, `15/17`, `status: gaps_found` | unchecked, `**Plans:** 4 plans`, `/gsd-execute-phase 125` |
| `pre_verification` | exactly 4 / exactly 1, 2, or 3; no other summary count | absent | same 30/39 projection | same `30/39`, `15/17` audit | unchecked, summary-correlated `1/4`, `2/4`, or `3/4 plans executed`, `/gsd-execute-phase 125` |
| `verification_written_pre_promotion` | exactly 4 / exactly 3 | present and lifecycle-valid | still 30 checked + Complete and 9 Pending; RCN-04/05/06 remain Pending | still `30/39`, `15/17`, `status: gaps_found` | unchecked at `3/4 plans executed`; route remains `/gsd-execute-phase 125` because promotion is not authorized yet |
| `post_verification` | exactly 4 / exactly 3 | present and lifecycle-valid | 33 checked + Complete, 6 unchecked + Pending; only RCN-04/05/06 promote | `33/39`, `16/17`, `status: gaps_found` | lifecycle-valid promoted-pre-summary state: Phase 125 remains unchecked at `3/4 plans executed`, while ROADMAP, PROJECT, STATE, and audit already use `/gsd-execute-phase 126` as the sole primary route |
| `post_summary` | exactly 4 / exactly 4 | present and lifecycle-valid | same 33/39 projection with six Phase 126 IDs Pending | same `33/39`, `16/17`, `status: gaps_found` | Phase 125 becomes checked at `4/4 plans complete`; the canonical corpus retains the already-established sole `/gsd-execute-phase 126` route |

At every stage, require Phase 125 to own exactly `RCN-04`, `RCN-05`, and `RCN-06`; Phase 126 to own exactly `CMP-05`, `RCN-02`, `RCN-03`, `GOV-04`, `BOUND-01`, and `HARD-05`; and the canonical active-routing corpus to contain no `/gsd-complete-milestone v2.1`. Before promotion, reject `/gsd-execute-phase 126` as premature and require the sole Phase 125 route. At `post_verification` and `post_summary`, reject stale `/gsd-execute-phase 125` routing and require the sole Phase 126 route across ROADMAP, PROJECT, STATE, and the audit. Treat `124-VERIFICATION.md` as immutable superseded historical evidence rather than a current routing input. Keep the Phase 117 final ordering invariant.

Extend `scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts` with a `maybePhase125Stage?: Phase125LifecycleStage["kind"]` selector and the exact plan/summary/verification files needed by each stage. Every new optional or nullish internal fixture field, parameter, destructured binding, local, parser result, and parser function must use a `maybe` prefix per the repo TypeScript standard; do not rename stable external contracts solely for style. Make illegal fixture combinations unrepresentable after parsing.

Replace the prior single strict-gap test with one passing Arrange/Act/Assert test for each of the five stages. Add focused one-concern mutations covering each stage boundary: planned rejects a summary; pre_verification rejects a fourth summary without verification; verification_written_pre_promotion rejects promoted counts; every pre-promotion state rejects premature `/gsd-execute-phase 126`; post_verification rejects a missing verification, a premature checked phase, or stale `/gsd-execute-phase 125`; post_summary rejects 3/4 progress or stale Phase 125 routing while retaining the Phase 126 route. Retain and adapt the existing ownership/count mutations. Add independent mutations for wrong lifecycle ID/mode/status, wrong requirement and audit counts, wrong phase count, each Phase 126 ownership/status drift, `/gsd-complete-milestone v2.1`, and Phase 117 not last.

After all three tracked TypeScript files reach their final state, regenerate `docs/metrics/lines-of-code.md` with `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`; never hand-edit the generated counts. Require the same command with `--check` to pass after regeneration.
  </action>
  <verify>
    <automated>bun test scripts/check-phase124-milestone-gap-closure.test.ts &amp;&amp; bun test scripts/check-phase124-milestone-closeout-reconciliation.test.ts &amp;&amp; bun run scripts/check-phase124-milestone-closeout-reconciliation.ts &amp;&amp; bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check</automated>
  </verify>
  <acceptance_criteria>
    The source defines exactly five stage tags and includes `verification_written_pre_promotion`.
    The test suite has one passing fixture and at least one stage-specific mutation for every stage.
    The live repository is classified as `pre_verification` at 30/39 and 15/17 after Plans 01 and 02 have summaries.
    `rg -n "planned|pre_verification|verification_written_pre_promotion|post_verification|post_summary|maybePhase125Stage" scripts/check-phase124-milestone-gap-closure.ts scripts/check-phase124-milestone-gap-closure.test.ts scripts/check-phase124-milestone-closeout-reconciliation.fixtures.ts` finds the complete contract.
    Mutation coverage independently rejects premature Phase 126 routing before promotion and stale Phase 125 routing after promotion.
    Every new nullish or optional internal stage-fixture/parser name uses the `maybe` prefix.
    `docs/metrics/lines-of-code.md` is regenerated after the final TypeScript edits and the repo-owned `--check` command passes.
  </acceptance_criteria>
  <done>The Phase 124 compatibility layer represents every legal Phase 125 artifact/count transition without a verification-present 30/39 hole.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the pre-verification corpus without activating the orphan guard</name>
  <read_first>
    scripts/verify.sh
    scripts/check-active-milestone-verification-traceability.ts
    scripts/check-active-milestone-verification-traceability.test.ts
    scripts/check-phase124-milestone-gap-closure.ts
    scripts/check-phase124-milestone-gap-closure.test.ts
    scripts/check-phase124-milestone-closeout-reconciliation.ts
    scripts/check-phase124-milestone-closeout-reconciliation.test.ts
    scripts/check-phase117-parity-uat-release-boundary.ts
    scripts/check-phase117-parity-uat-release-boundary.test.ts
    AGENTS.md
  </read_first>
  <files>None — verification-only task; no additional files are modified.</files>
  <action>
Run the five-stage suite and the Phase 124 main test/live pair. Confirm that the live repository is accepted as `pre_verification` with exactly four plans, the extant one-to-three Phase 125 summaries, no `125-VERIFICATION.md`, 30/39 requirements, and 15/17 phases.

Run the active traceability test suite because its synthetic and real-repository cases are designed to pass while proving the live CLI's orphan result. Then invoke the live active traceability CLI under an explicit expected-failure assertion: the command must exit 1 and identify exactly `RCN-04`, `RCN-05`, and `RCN-06` as uncovered. A zero exit is a failure in this stage. Assert that neither the active traceability test command nor CLI command appears in the visible or executable regions of `scripts/verify.sh`; Plan 04 owns that wiring only after lifecycle-valid evidence exists per D-10 and D-13.

Run the Phase 117 test/live pair, lifecycle validation with plans required, the repo-owned LOC `--check`, and `git diff --check`. Do not run `bash scripts/verify.sh` as a Plan 03 acceptance gate because its permanent orphan guard is intentionally not yet installed. Do not create `125-VERIFICATION.md`, edit `scripts/verify.sh`, or change requirement, audit, roadmap completion, project, or state metadata.
  </action>
  <verify>
    <automated>bun test scripts/check-phase124-milestone-gap-closure.test.ts &amp;&amp; bun test scripts/check-phase124-milestone-closeout-reconciliation.test.ts &amp;&amp; bun run scripts/check-phase124-milestone-closeout-reconciliation.ts &amp;&amp; bun test scripts/check-active-milestone-verification-traceability.test.ts &amp;&amp; ! bun run scripts/check-active-milestone-verification-traceability.ts &amp;&amp; ! rg -n "check-active-milestone-verification-traceability" scripts/verify.sh &amp;&amp; bun test scripts/check-phase117-parity-uat-release-boundary.test.ts &amp;&amp; bun run scripts/check-phase117-parity-uat-release-boundary.ts &amp;&amp; node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 125 --require-plans --raw &amp;&amp; bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The Phase 124 live checker accepts the current `pre_verification` repository at 30/39 requirements, 15/17 phases, no verification artifact, and exactly the extant Phase 125 summaries.
    The active traceability test suite exits 0, while the live active traceability CLI exits 1 and reports exactly RCN-04, RCN-05, and RCN-06 as uncovered.
    `rg -n "check-active-milestone-verification-traceability" scripts/verify.sh` finds no visible or executable wiring.
    The focused compatibility matrix, Phase 117 pair, lifecycle validation, LOC freshness check, and `git diff --check` exit 0 without metadata promotion.
    No Phase 115, Phase 126, Rust, runtime, verification, requirements, project, state, roadmap, or audit file changes in this plan.
  </acceptance_criteria>
  <done>The five-stage compatibility layer proves `pre_verification`, while the active orphan CLI remains correctly failing and unwired for Plan 04.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Planning artifacts → lifecycle-stage parser | Repository Markdown and YAML are untrusted and may contain mixed counts, forged provenance, or ambiguous progress. |
| Fixture selector → synthetic repository | Optional stage configuration must not create combinations the production parser could never authorize. |
| Pre-verification corpus → checker commands | Compatibility checks must pass while the active orphan CLI independently and intentionally reports uncovered requirements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-125-03-01 | Spoofing | Phase 125 verification provenance | mitigate | Validate status, lifecycle flag, mode, and lifecycle ID against CONTEXT before selecting any verification-present stage. |
| T-125-03-02 | Tampering | Stage projection | mitigate | Parse artifacts first into exactly five tagged states, then enforce exact counts, ownership, plan progress, and route for that tag. |
| T-125-03-03 | Repudiation | Stage transitions | mitigate | One passing fixture and focused boundary mutations record why every accepted transition is legal. |
| T-125-03-04 | Information Disclosure | Checker diagnostics | mitigate | Emit repository-relative paths, stage names, and stable count/provenance failures only; never print file contents or environment values. |
| T-125-03-05 | Denial of Service | Focused pre-verification checks | accept | Deterministic local Bun commands scan bounded planning corpora with no network, clock wait, or service dependency. |
| T-125-03-06 | Elevation of Privilege | Archive and Phase 126 routing | mitigate | Reject milestone-completion routing throughout Phase 125, reject Phase 126 routing before promotion, and reject stale Phase 125 routing from the promoted-pre-summary state onward. |
</threat-model>

<verification>
- Five-stage Phase 124 gap-closure fixture suite and main Phase 124 test/live pair.
- Active-milestone traceability test passes while the live CLI is asserted to fail with exactly RCN-04/05/06.
- `scripts/verify.sh` remains free of active-milestone traceability commands until Plan 04.
- Final Phase 117 test/live pair remains last.
- Lifecycle validation with four plans required.
- Repo-owned LOC freshness check and `git diff --check` against the live pre-verification projection.
</verification>

<success-criteria>
- Exactly five lifecycle stages are representable and mutation-tested.
- `verification_written_pre_promotion` accepts lifecycle-valid Phase 125 evidence while counts remain 30/39 and 15/17.
- `post_verification` is the lifecycle-valid promoted-pre-summary state: Phase 125 remains at 3/4 while every canonical routing surface points solely to Phase 126.
- The live pre-verification repository passes focused Phase 124 compatibility verification before any promotion.
- The active orphan CLI remains correctly failing and unwired until Plan 04 creates verification evidence.
- Phase 115, runtime code, Phase 126, and canonical metadata remain unchanged.
</success-criteria>

<output>
After completion, create `.planning/phases/125-compact-download-verification-traceability-closure/125-03-SUMMARY.md`.
</output>
