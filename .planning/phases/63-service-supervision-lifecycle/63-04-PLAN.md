---
phase: 63-service-supervision-lifecycle
plan: 04
type: execute
wave: 4
depends_on: [63-03]
files_modified:
  - docs/operator/runtime-guide.md
  - scripts/check-phase63-service-lifecycle.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [SVC-01, SVC-02, SVC-04]
user_setup: []
must_haves:
  truths:
    - "Operator runbook shows preview, install, start, stop, restart, status, disable, and uninstall flows for launchd and systemd."
    - "Runbook includes log locations, config path review, safe shutdown, restart review, and recovery next-action guidance."
    - "Docs provide copy-pasteable repo-local Cargo and Bazel command forms for service workflows."
    - "Docs keep service checks as opt-in operator review and default verification deterministic."
    - "A deterministic Bun checker guards service labels, commands, daemon binary targeting, docs, and default verification boundaries."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "Phase 63 launchd/systemd service lifecycle runbook"
      contains: "Service lifecycle labels"
    - path: "scripts/check-phase63-service-lifecycle.ts"
      provides: "Deterministic source/docs/default-verification service lifecycle guard"
      contains: "validated Phase 63 service lifecycle"
    - path: "scripts/verify.sh"
      provides: "Default deterministic checker integration"
      contains: "bun run scripts/check-phase63-service-lifecycle.ts"
    - path: "docs/metrics/lines-of-code.md"
      provides: "Tracked generated LOC freshness after verification"
      contains: "check-phase63-service-lifecycle"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase63-service-lifecycle.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase63-service-lifecycle.ts"
    - from: "scripts/check-phase63-service-lifecycle.ts"
      to: "packages/open-bitcoin-cli/src/operator/service.rs"
      via: "exact string assertions for service commands and lifecycle labels"
      pattern: "ServiceCommand::Restart"
    - from: "docs/operator/runtime-guide.md"
      to: "repo-local operator workflows"
      via: "Cargo and Bazel command examples"
      pattern: "bazel run //packages/open-bitcoin-cli:open_bitcoin"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 63-2026-06-07T14-20-10
generated_at: 2026-06-07T14:34:55Z
---

<objective>
Document and deterministically guard the Phase 63 service supervision lifecycle workflow.

Purpose: SVC-04 plus SVC-01/SVC-02 closeout require runbook-quality operator commands, exact lifecycle labels, safe service-manager boundaries, and default verification that does not start live services or public-network checks.
Output: Updated runtime guide, Phase 63 Bun checker, verify integration, and generated LOC freshness.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md
@.planning/phases/63-service-supervision-lifecycle/63-RESEARCH.md
@.planning/phases/63-service-supervision-lifecycle/63-01-SUMMARY.md
@.planning/phases/63-service-supervision-lifecycle/63-02-SUMMARY.md
@.planning/phases/63-service-supervision-lifecycle/63-03-SUMMARY.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-04-SUMMARY.md
@docs/operator/runtime-guide.md
@scripts/check-phase61-resource-recovery-boundaries.ts
@scripts/check-phase62-sync-truth-surfaces.ts
@scripts/verify.sh

<interfaces>
Required service lifecycle labels:
```text
unmanaged
installed-stopped
running
failed
disabled
unavailable-manager
```

Required repo-local command prefixes:
```text
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --
bazel run //packages/open-bitcoin-cli:open_bitcoin --
```

Default verification must not contain live service or public-network command invocations:
```text
systemctl --user start
systemctl --user stop
systemctl --user restart
launchctl bootstrap
launchctl bootout
launchctl kickstart
run-live-mainnet-smoke
--manual-peer
--restart-after-progress
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update launchd and systemd operator runbook</name>
  <read_first>
    - `.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md` decisions D-01 through D-14
    - `.planning/phases/63-service-supervision-lifecycle/63-RESEARCH.md` "Common Pitfalls" and "Security Domain"
    - `.planning/phases/62-long-run-sync-truth-surfaces/62-04-SUMMARY.md`
    - `AGENTS.md` repo-local command guidance
    - `docs/operator/runtime-guide.md`
  </read_first>
  <files>docs/operator/runtime-guide.md</files>
  <action>
    Replace the existing "Service Lifecycle" section in `docs/operator/runtime-guide.md` with a Phase 63 runbook that documents these commands in this order:
    `service preview`, `service install`, `service install --apply`, `service start`, `service status`, `service restart`, `service stop`, `service disable`, `service uninstall`, and `service uninstall --apply`.

    For each operator command, provide both repo-local forms:
    - `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service <command>`
    - `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service <command>`
    Include `--config=/tmp/open-bitcoin-mainnet/open-bitcoin.jsonc` examples for preview/install so operators can review selected config path handling.

    State these exact facts:
    - `service preview` is always side-effect-free.
    - `service install` and `service uninstall` are previews unless `--apply` is supplied.
    - Generated launchd plist and systemd unit definitions supervise `open-bitcoind`, not the `open-bitcoin` operator wrapper.
    - launchd remains user-level under `~/Library/LaunchAgents/org.open-bitcoin.node.plist`.
    - systemd remains user-level under `~/.config/systemd/user/open-bitcoin-node.service`.
    - the service log path is `<log_dir>/open-bitcoin.log` when the operator log directory is configured; otherwise status reports an explicit unavailable reason.
    - service lifecycle labels are exactly `unmanaged`, `installed-stopped`, `running`, `failed`, `disabled`, and `unavailable-manager`.
    - status/dashboard JSON and human output preserve Phase 62 sync lifecycle, phase, configured targets, attempt counters, latest stop reason, recovery category/action, resource pressure, peer health, and downloaded/connected block evidence beside service state.

    Add safe operation notes for log inspection, config path review, safe shutdown, restart review, and recovery next actions. Keep wording bounded to opt-in extended operator review. Do not use the phrases `production service`, `production full node`, `packaged service guarantee`, or `unattended production-node replacement`.

    Keep public-network and live service checks under explicit optional UAT wording. Do not add any command that makes `bash scripts/verify.sh` start a service, stop a service, restart a service, call public peers, or require network access.
  </action>
  <acceptance_criteria>
    - `rg -n "service preview|service install --apply|service start|service stop|service restart|service status|service disable|service uninstall --apply|open-bitcoind|~/Library/LaunchAgents/org.open-bitcoin.node.plist|~/.config/systemd/user/open-bitcoin-node.service|Service lifecycle labels|unavailable-manager" docs/operator/runtime-guide.md` finds the runbook content.
    - `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service preview|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service preview|cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service restart|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service restart" docs/operator/runtime-guide.md` finds repo-local command forms.
    - `bash -c 'if rg -n "production service|production full node|packaged service guarantee|unattended production-node replacement" docs/operator/runtime-guide.md; then exit 1; fi'` passes.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "service preview|service start|service stop|service restart|Service lifecycle labels|open-bitcoind|unavailable-manager" docs/operator/runtime-guide.md</automated>
  </verify>
  <done>Operator docs explain complete launchd/systemd lifecycle management with repo-local commands, safe boundaries, and exact status labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic Phase 63 service lifecycle checker</name>
  <read_first>
    - `scripts/check-phase61-resource-recovery-boundaries.ts`
    - `scripts/check-phase62-sync-truth-surfaces.ts`
    - `scripts/verify.sh`
    - `packages/open-bitcoin-cli/src/operator.rs`
    - `packages/open-bitcoin-cli/src/operator/runtime.rs`
    - `packages/open-bitcoin-cli/src/operator/service.rs`
    - `packages/open-bitcoin-cli/src/operator/service/launchd.rs`
    - `packages/open-bitcoin-cli/src/operator/service/systemd.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/action.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-node/src/status.rs`
    - `docs/operator/runtime-guide.md`
  </read_first>
  <files>scripts/check-phase63-service-lifecycle.ts, scripts/verify.sh</files>
  <behavior>
    - Test: checker fails if any lifecycle label is missing from Rust source or docs.
    - Test: checker fails if `ServiceCommand::Preview`, `Start`, `Stop`, or `Restart` is missing.
    - Test: checker fails if daemon resolver or docs omit `open-bitcoind`.
    - Test: checker fails if launchd/systemd command strings omit user-scope manager commands.
    - Test: checker fails if `scripts/verify.sh` contains live service manager or public-network command invocations.
    - Test: checker passes only when `scripts/verify.sh` runs `bun run scripts/check-phase63-service-lifecycle.ts`.
  </behavior>
  <action>
    Create `scripts/check-phase63-service-lifecycle.ts` in the same style as the Phase 61 and Phase 62 checkers: repo-root path resolution, `readText`, `requireContains`, `requireNotContains`, required string arrays, and final `console.log("validated Phase 63 service lifecycle")`.

    The checker must read:
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-cli/src/operator.rs`
    - `packages/open-bitcoin-cli/src/operator/runtime.rs`
    - `packages/open-bitcoin-cli/src/operator/service.rs`
    - `packages/open-bitcoin-cli/src/operator/service/launchd.rs`
    - `packages/open-bitcoin-cli/src/operator/service/systemd.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/action.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `docs/operator/runtime-guide.md`
    - `scripts/verify.sh`

    Require exact strings:
    - `ServiceLifecycleStatus`
    - `unmanaged`
    - `installed-stopped`
    - `running`
    - `failed`
    - `disabled`
    - `unavailable-manager`
    - `ServiceCommand::Preview`
    - `ServiceCommand::Start`
    - `ServiceCommand::Stop`
    - `ServiceCommand::Restart`
    - `resolve_service_daemon_binary`
    - `open-bitcoind`
    - `systemctl --user start open-bitcoin-node.service`
    - `systemctl --user stop open-bitcoin-node.service`
    - `systemctl --user restart open-bitcoin-node.service`
    - `launchctl bootstrap`
    - `launchctl bootout`
    - `launchctl kickstart -k`
    - `t start service`
    - `o stop service`
    - `x restart service`
    - `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service preview`
    - `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=/tmp/open-bitcoin-mainnet service preview`

    Require `scripts/verify.sh` to include `bun run scripts/check-phase63-service-lifecycle.ts`. Require `scripts/verify.sh` not to contain the live service/public-network strings from the interface block. Require `docs/operator/runtime-guide.md` not to contain `production service`, `production full node`, `packaged service guarantee`, or `unattended production-node replacement`.

    Wire the checker into `scripts/verify.sh` immediately after `bun run scripts/check-phase62-sync-truth-surfaces.ts`. Do not add live service-manager commands or network checks to default verification.
  </action>
  <acceptance_criteria>
    - `rg -n "validated Phase 63 service lifecycle|ServiceLifecycleStatus|ServiceCommand::Preview|ServiceCommand::Start|ServiceCommand::Stop|ServiceCommand::Restart|resolve_service_daemon_binary|requireNotContains\\(verifyScript, \"systemctl --user start\"" scripts/check-phase63-service-lifecycle.ts` finds the checker contract.
    - `rg -n "bun run scripts/check-phase63-service-lifecycle\\.ts" scripts/verify.sh` finds verify integration.
    - `bun run scripts/check-phase63-service-lifecycle.ts` passes.
    - `bash -c 'if rg -n "systemctl --user start|systemctl --user stop|systemctl --user restart|launchctl bootstrap|launchctl bootout|launchctl kickstart|run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh; then exit 1; fi'` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase63-service-lifecycle.ts</automated>
  </verify>
  <done>Default verification has a deterministic Phase 63 service lifecycle guard with no live service-manager or public-network execution.</done>
</task>

<task type="auto">
  <name>Task 3: Run aggregate deterministic verification</name>
  <read_first>
    - `AGENTS.md` repo-local verification contract
    - `scripts/verify.sh`
    - `scripts/check-phase63-service-lifecycle.ts`
    - `docs/metrics/lines-of-code.md`
  </read_first>
  <files>docs/metrics/lines-of-code.md</files>
  <action>
    Run the Phase 63 checker, then run the repo-native verification contract `bash scripts/verify.sh`. Preserve any `docs/metrics/lines-of-code.md` freshness update produced by the verification hooks. Review `scripts/verify.sh` after verification and confirm it invokes deterministic checkers only; it must not invoke service start/stop/restart or public-network smoke commands.
  </action>
  <acceptance_criteria>
    - `bun run scripts/check-phase63-service-lifecycle.ts` passes.
    - `bash scripts/verify.sh` passes.
    - `rg -n "check-phase63-service-lifecycle" docs/metrics/lines-of-code.md scripts/verify.sh` finds generated LOC/checker references after verification.
    - `git diff --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Phase 63 docs and checker are integrated into deterministic verification, with generated LOC freshness preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator action | Runbook commands can cause service-manager side effects if copied by an operator. |
| checker -> default verification | A deterministic script enforces source/docs boundaries without starting services. |
| service lifecycle wording -> operator expectations | Docs must describe opt-in review boundaries without unsupported service claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-01 | Elevation of Privilege | runbook commands | mitigate | Document only repo-local `cargo run` and `bazel run` operator forms plus user-scope launchd/systemd locations; no sudo or machine-wide paths. |
| T-63-04-02 | Tampering | copied service workflows | mitigate | State preview/install/uninstall dry-run boundaries and `open-bitcoind` target explicitly; no source Core/Knots service or source datadir mutation commands. |
| T-63-04-03 | Denial of Service | default verification | mitigate | Checker asserts `scripts/verify.sh` excludes live service-manager commands and public-network smoke flags. |
| T-63-04-04 | Repudiation | lifecycle docs | mitigate | Checker requires the six exact labels and service command strings in source and docs. |
| T-63-04-05 | Information Disclosure | log/config path docs | accept | Runbook intentionally teaches operators to inspect selected config paths and service log paths; it must not include credential or cookie contents. |
</threat_model>

<verification>
Run the docs/checker assertions and aggregate repo verification:

```bash
rg -n "service preview|service start|service stop|service restart|Service lifecycle labels|open-bitcoind|unavailable-manager" docs/operator/runtime-guide.md
bun run scripts/check-phase63-service-lifecycle.ts
bash scripts/verify.sh
git diff --check
```
</verification>

<success_criteria>
Plan 63-04 is complete when the operator runbook covers launchd/systemd lifecycle commands, log/config/recovery guidance, exact lifecycle labels, repo-local Cargo/Bazel forms, and default deterministic verification includes the Phase 63 checker without live service-manager or public-network execution.
</success_criteria>

<output>
After completion, create `.planning/phases/63-service-supervision-lifecycle/63-04-SUMMARY.md`.
</output>
