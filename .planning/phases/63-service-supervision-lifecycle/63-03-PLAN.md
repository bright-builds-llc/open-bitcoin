---
phase: 63-service-supervision-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [63-02]
files_modified:
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - packages/open-bitcoin-cli/src/operator/service.rs
  - packages/open-bitcoin-cli/src/operator/service/tests.rs
autonomous: true
requirements: [SVC-02]
user_setup: []
must_haves:
  truths:
    - "Service lifecycle status uses exactly `unmanaged`, `installed-stopped`, `running`, `failed`, `disabled`, and `unavailable-manager` labels."
    - "`open-bitcoin service status`, `open-bitcoin status`, JSON status, and dashboard service rows agree on lifecycle, manager, installed, enabled, running, service file path, log path, diagnostics, and unavailable reasons."
    - "Status inspection succeeds with typed unavailable-manager state when the platform manager is unsupported, missing, or fails status inspection."
    - "Service lifecycle status complements the Phase 62 sync fields and does not reinterpret sync lifecycle, progress, stop reason, recovery category, configured targets, or block evidence."
    - "Missing service evidence stays explicit through `Unavailable: {reason}` instead of false success, empty strings, or zero values."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status.rs"
      provides: "Shared service lifecycle status contract"
      contains: "ServiceLifecycleStatus"
    - path: "packages/open-bitcoin-cli/src/operator/status.rs"
      provides: "Service manager snapshot to shared status projection"
      contains: "service_lifecycle_from_snapshot"
    - path: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      provides: "Human status service lifecycle rendering"
      contains: "lifecycle="
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      provides: "Dashboard service lifecycle rows"
      contains: "Lifecycle"
    - path: "packages/open-bitcoin-cli/src/operator/service.rs"
      provides: "Direct service status lifecycle labels"
      contains: "installed-stopped"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "collect_service_status builds shared ServiceStatus with ServiceLifecycleStatus"
      pattern: "ServiceLifecycleStatus"
    - from: "packages/open-bitcoin-cli/src/operator/status/render.rs"
      to: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs"
      via: "both render the same shared ServiceStatus fields"
      pattern: "service.lifecycle"
    - from: "packages/open-bitcoin-cli/src/operator/status/tests.rs"
      to: "Phase 62 sync truth fields"
      via: "status tests assert service unavailable-manager does not remove sync configured targets or stop reason"
      pattern: "configured_targets"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 63-2026-06-07T14-20-10
generated_at: 2026-06-07T14:34:55Z
---

<objective>
Create and render the shared Phase 63 service lifecycle status contract across service status, status JSON, human status, and dashboard rows.

Purpose: SVC-02 and decisions D-05, D-06, D-07, D-08, D-13, and D-14 require one typed service lifecycle vocabulary and explicit unavailable reasons while preserving Phase 62 sync truth.
Output: Shared lifecycle enum, richer ServiceStatus fields, status projection helpers, renderer/dashboard updates, and deterministic Rust coverage for all six labels.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md
@.planning/phases/63-service-supervision-lifecycle/63-RESEARCH.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-01-SUMMARY.md
@.planning/phases/62-long-run-sync-truth-surfaces/62-02-SUMMARY.md
@.planning/phases/61-resource-bounds-and-recovery-taxonomy/61-05-SUMMARY.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/service.rs

<interfaces>
Add this shared enum in `packages/open-bitcoin-node/src/status.rs`:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "kebab-case")]
pub enum ServiceLifecycleStatus {
    Unmanaged,
    InstalledStopped,
    Running,
    Failed,
    Disabled,
    UnavailableManager,
}
```

Extend `ServiceStatus` to this shape:
```rust
pub struct ServiceStatus {
    pub manager: FieldAvailability<String>,
    pub lifecycle: FieldAvailability<ServiceLifecycleStatus>,
    pub installed: FieldAvailability<bool>,
    pub enabled: FieldAvailability<bool>,
    pub running: FieldAvailability<bool>,
    pub service_file_path: FieldAvailability<String>,
    pub log_path: FieldAvailability<String>,
    pub diagnostics: FieldAvailability<String>,
}
```

Lifecycle labels required everywhere:
```text
unmanaged
installed-stopped
running
failed
disabled
unavailable-manager
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared service lifecycle status contract</name>
  <read_first>
    - `.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md` decisions D-05, D-06, D-07, and D-08
    - `.planning/phases/63-service-supervision-lifecycle/63-RESEARCH.md` "Pattern 1: One Shared Service Lifecycle Contract"
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/status/tests.rs`
    - `packages/open-bitcoin-node/src/lib.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests.rs, packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <behavior>
    - Test: `ServiceLifecycleStatus::InstalledStopped` serializes as JSON string `installed-stopped`.
    - Test: `ServiceLifecycleStatus::UnavailableManager` serializes as JSON string `unavailable-manager`.
    - Test: deserializing older `ServiceStatus` JSON without `lifecycle`, `service_file_path`, `log_path`, or `diagnostics` yields explicit unavailable reasons.
    - Test: all `OpenBitcoinStatusSnapshot` test constructors compile with the richer `ServiceStatus` contract.
  </behavior>
  <action>
    Add `ServiceLifecycleStatus` to `packages/open-bitcoin-node/src/status.rs` exactly as shown in the interface block. Add an inherent `as_str(self) -> &'static str` helper returning the six exact labels from the interface block.

    Extend `ServiceStatus` with `lifecycle`, `service_file_path`, `log_path`, and `diagnostics`. Add serde default functions with exact unavailable reasons:
    - lifecycle: `service lifecycle unavailable`
    - service_file_path: `service file path unavailable`
    - log_path: `service log path unavailable`
    - diagnostics: `service diagnostics unavailable`
    Apply these defaults to the new fields so older JSON remains readable.

    Re-export `ServiceLifecycleStatus` from `packages/open-bitcoin-node/src/lib.rs` beside the other status types.

    Update all Rust `ServiceStatus {` constructors found by `rg -n "ServiceStatus \\{" packages/open-bitcoin-* -g '*.rs'`. In test fixtures where the service is running, use lifecycle `FieldAvailability::available(ServiceLifecycleStatus::Running)`, service file path `/tmp/open-bitcoin-node.service` or existing fixture path, log path `/tmp/logs/open-bitcoin.log` when a log path exists, and diagnostics `FieldAvailability::unavailable("service diagnostics unavailable")` when no specific diagnostic is part of the test.
  </action>
  <acceptance_criteria>
    - `rg -n "pub enum ServiceLifecycleStatus|InstalledStopped|UnavailableManager|installed-stopped|unavailable-manager|service_file_path|log_path|diagnostics" packages/open-bitcoin-node/src/status.rs packages/open-bitcoin-node/src/status/tests.rs packages/open-bitcoin-node/src/lib.rs` finds the contract and tests.
    - `rg -n "ServiceStatus \\{" packages/open-bitcoin-* -g '*.rs'` shows no constructor lacking `lifecycle`, `service_file_path`, `log_path`, or `diagnostics`.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase63_service_lifecycle_status_contract --all-features` passes and runs nonzero tests.
    - `cargo check --manifest-path packages/Cargo.toml --workspace --all-targets --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase63_service_lifecycle_status_contract --all-features</automated>
  </verify>
  <done>The shared status contract can represent every Phase 63 service lifecycle label and preserve explicit unavailable reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project service manager snapshots into lifecycle status</name>
  <read_first>
    - `.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md` decisions D-05, D-06, D-07, and D-08
    - `.planning/phases/63-service-supervision-lifecycle/63-RESEARCH.md` "Pattern 2: Pure Mapping From Adapter Snapshot To Operator Contract"
    - `packages/open-bitcoin-cli/src/operator/status.rs`
    - `packages/open-bitcoin-cli/src/operator/status/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/service.rs`
    - `packages/open-bitcoin-cli/src/operator/service/fake.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <behavior>
    - Test: `ServiceLifecycleState::Unmanaged` maps to `ServiceLifecycleStatus::Unmanaged`.
    - Test: `ServiceLifecycleState::Running` maps to `Running` even when `maybe_enabled == Some(false)`.
    - Test: `ServiceLifecycleState::Failed` maps to `Failed`.
    - Test: installed service with `maybe_enabled == Some(false)` maps to `Disabled`.
    - Test: installed/enabled/stopped service maps to `InstalledStopped`.
    - Test: manager status errors map to available lifecycle `UnavailableManager` while manager, booleans, paths, and diagnostics preserve explicit unavailable reasons.
    - Test: service-manager failures do not remove Phase 62 sync fields such as `configured_targets`, `attempt_counters`, and `latest_stop_reason` from the returned snapshot.
  </behavior>
  <action>
    In `packages/open-bitcoin-cli/src/operator/status.rs`, import `ServiceLifecycleStatus`. Add a pure helper named `service_lifecycle_from_snapshot(snapshot: &ServiceStateSnapshot) -> ServiceLifecycleStatus`.

    The helper must map in this precedence:
    - `ServiceLifecycleState::Unmanaged` -> `ServiceLifecycleStatus::Unmanaged`
    - `ServiceLifecycleState::Running` -> `ServiceLifecycleStatus::Running`
    - `ServiceLifecycleState::Failed` -> `ServiceLifecycleStatus::Failed`
    - any installed state with `snapshot.maybe_enabled == Some(false)` -> `ServiceLifecycleStatus::Disabled`
    - all remaining installed, enabled, or stopped states -> `ServiceLifecycleStatus::InstalledStopped`

    Add small helpers for path and diagnostics availability:
    - service file path available when `maybe_service_file_path` exists, otherwise unavailable reason `service file path unavailable`
    - log path available when `maybe_log_path` exists, otherwise use `maybe_log_path_unavailable_reason` or `service log path unavailable`
    - diagnostics available when `maybe_manager_diagnostics` is non-empty after trim, otherwise unavailable reason `service diagnostics unavailable`

    Update `collect_service_status` so a successful manager snapshot returns manager name, lifecycle, installed, enabled, running, service_file_path, log_path, and diagnostics. Preserve `enabled` from `snapshot.maybe_enabled` when present; if absent, use unavailable reason `service manager did not report enablement` instead of inferring a false value. Preserve `running` as available true only for `ServiceLifecycleState::Running`.

    When `manager.status()` returns `ServiceError`, return `ServiceStatus` with `lifecycle: FieldAvailability::available(ServiceLifecycleStatus::UnavailableManager)`, manager unavailable reason `service manager unavailable: {error}`, installed/enabled/running unavailable reason `service manager unavailable: {error}`, service_file_path/log_path unavailable reason `service manager unavailable: {error}`, and diagnostics available with `error.to_string()`. Do not fail the whole status snapshot.

    In detection-only service status, set lifecycle to `UnavailableManager` and keep service file path available from `ServiceCandidate.path` only when a candidate is present. When no manager and no candidate are available, set lifecycle `UnavailableManager` and all other fields unavailable with reason `service manager not inspected`.
  </action>
  <acceptance_criteria>
    - `rg -n "service_lifecycle_from_snapshot|ServiceLifecycleStatus::Unmanaged|ServiceLifecycleStatus::InstalledStopped|ServiceLifecycleStatus::Running|ServiceLifecycleStatus::Failed|ServiceLifecycleStatus::Disabled|ServiceLifecycleStatus::UnavailableManager|service manager unavailable" packages/open-bitcoin-cli/src/operator/status.rs packages/open-bitcoin-cli/src/operator/status/tests.rs` finds mapping logic and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_projection --all-features` passes and runs nonzero tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli collect_status_snapshot_with_error_manager --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features` passes, proving Phase 62 sync fields still render with service status changes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_projection --all-features</automated>
  </verify>
  <done>Status collection returns truthful Phase 63 service lifecycle state without losing Phase 62 sync truth fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render lifecycle labels in service status, status, JSON, and dashboard</name>
  <read_first>
    - `.planning/phases/63-service-supervision-lifecycle/63-CONTEXT.md` decisions D-05, D-06, D-07, D-08, and D-13
    - `packages/open-bitcoin-cli/src/operator/service.rs`
    - `packages/open-bitcoin-cli/src/operator/service/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/status/render.rs`
    - `packages/open-bitcoin-cli/src/operator/status/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model.rs`
    - `packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/service.rs, packages/open-bitcoin-cli/src/operator/service/tests.rs, packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <behavior>
    - Test: `open-bitcoin service status` renders `service: installed-stopped`, `service: disabled`, and `service: unavailable-manager` for matching inputs.
    - Test: human `open-bitcoin status` service line contains `lifecycle=running manager=launchd installed=true enabled=true running=true file=... logs=... diagnostics=...`.
    - Test: JSON status exposes `service.lifecycle.value` as `running`, `disabled`, `installed-stopped`, or `unavailable-manager`.
    - Test: dashboard service rows include labels exactly `Lifecycle`, `Manager`, `Installed`, `Enabled`, `Running`, `Service file`, `Logs`, and `Diagnostics`.
    - Test: unavailable values render as `Unavailable: {reason}` and never as empty strings or zeros.
  </behavior>
  <action>
    In `service.rs`, update `render_service_state_snapshot` to use Phase 63 lifecycle labels for direct `open-bitcoin service status`. Add a local helper that maps `ServiceStateSnapshot` to the exact labels in the interface block. The direct service status first line must be `service: {label}`. Use `disabled` when an installed service has `maybe_enabled == Some(false)`, `installed-stopped` for installed/enabled/stopped but not running or failed, and keep `unmanaged`, `running`, and `failed` labels exact.

    In `status/render.rs`, update `service_text` to include these keys in order: `lifecycle`, `manager`, `installed`, `enabled`, `running`, `file`, `logs`, `diagnostics`. Use `ServiceLifecycleStatus::as_str()` for lifecycle values and existing availability helpers for unavailable fields.

    In `dashboard/model.rs`, update `service_rows` to return rows in exact order: `Lifecycle`, `Manager`, `Installed`, `Enabled`, `Running`, `Service file`, `Logs`, `Diagnostics`. Use lifecycle labels from `ServiceLifecycleStatus::as_str()` and `Unavailable: {reason}` for missing fields.

    Update status, dashboard, and direct service tests. Use existing Phase 62 shared sync truth fixtures and add assertions that sync configured targets, attempt counters, latest stop reason, recovery category, and connected/downloaded block evidence still render after service lifecycle fields are added. Do not add a new interpretation of sync state under service rendering.
  </action>
  <acceptance_criteria>
    - `rg -n "service: installed-stopped|service: disabled|service: unavailable-manager|lifecycle=|Service file|Diagnostics|Unavailable: \\{reason\\}" packages/open-bitcoin-cli/src/operator/service.rs packages/open-bitcoin-cli/src/operator/service/tests.rs packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/status/tests.rs packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs` finds rendering and tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_rendering --all-features` passes and runs nonzero tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_service_lifecycle --all-features` passes and runs nonzero tests.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_rendering --all-features</automated>
  </verify>
  <done>All operator-facing status surfaces use the same Phase 63 service lifecycle labels and preserve Phase 62 sync truth fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service manager output -> status snapshot | Platform status evidence and errors are normalized into shared JSON and human/dashboard output. |
| status snapshot -> terminal/dashboard/JSON renderers | Shared service lifecycle fields are rendered for operators and scripts. |
| manager error -> status success | Unsupported or missing manager evidence must not collapse the whole status command or erase sync truth fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03-01 | Repudiation | lifecycle mapping | mitigate | Add a shared enum with exact labels and tests for all six states; avoid renderer-local strings that can drift. |
| T-63-03-02 | Information Disclosure | service diagnostics | mitigate | Surface manager diagnostics only as manager/status output already available locally; do not add credentials, cookie contents, or raw source datadir material. |
| T-63-03-03 | Denial of Service | status collection | mitigate | Convert manager errors to `unavailable-manager` service state while returning the rest of the status snapshot, including Phase 62 sync truth fields. |
| T-63-03-04 | Spoofing | missing manager evidence | mitigate | Use `FieldAvailability::Unavailable { reason }` for manager, booleans, paths, and diagnostics when not inspected; do not render false success or empty values. |
| T-63-03-05 | Tampering | source Bitcoin Core/Knots services/datadirs | mitigate | Status projection reads Open Bitcoin service manager snapshots and detection evidence only; tasks do not add source service disable/uninstall or source datadir mutation. |
</threat_model>

<verification>
Run focused service lifecycle contract, projection, renderer, and sync-preservation tests, then the Rust gate:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase63_service_lifecycle_status_contract --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_projection --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli phase63_service_lifecycle_rendering --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_service_lifecycle --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status_render_includes_sync_progress_and_peer_evidence --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_sections_surface_sync_progress_and_peer_counts --all-features
cargo fmt --manifest-path packages/Cargo.toml --all --check
cargo clippy --manifest-path packages/Cargo.toml --workspace --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features
cargo test --manifest-path packages/Cargo.toml --workspace --all-features
```
</verification>

<success_criteria>
Plan 63-03 is complete when all service status surfaces agree on the six lifecycle labels, richer service evidence fields stay explicit, manager failures remain inspectable as unavailable-manager, and Phase 62 sync truth rendering still passes unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/63-service-supervision-lifecycle/63-03-SUMMARY.md`.
</output>
