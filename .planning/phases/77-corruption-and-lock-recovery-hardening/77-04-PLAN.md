---
phase: 77-corruption-and-lock-recovery-hardening
plan: 04
type: execute
wave: 4
depends_on: [77-03-PLAN.md]
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/evidence.rs
  - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - scripts/run-live-mainnet-smoke.ts
  - scripts/test-run-live-mainnet-smoke.sh
autonomous: true
requirements: [REC-05, REC-06, REC-07, REC-08]
requirements_addressed: [REC-05, REC-06, REC-07, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Support bundles and dashboard projections consume status.recovery_evidence instead of reclassifying strings."
    - "Live-smoke summaries preserve the same recovery evidence fields instead of exposing only legacy recovery category/action strings."
    - "Support evidence remains compact and does not open Fjall stores directly."
    - "Missing recovery evidence is represented as explicit unavailable evidence."
  artifacts:
    - { path: "packages/open-bitcoin-cli/src/operator/support/evidence.rs", provides: "Full-sync evidence recovery summary from top-level recovery evidence" }
    - { path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs", provides: "Allowlisted live-smoke recovery evidence summary projection" }
    - { path: "scripts/run-live-mainnet-smoke.ts", provides: "Live-smoke report recovery evidence fields" }
    - { path: "packages/open-bitcoin-cli/src/operator/support/render.rs", provides: "Support Markdown recovery evidence section" }
    - { path: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs", provides: "Dashboard recovery evidence rows" }
  key_links:
    - { from: "packages/open-bitcoin-cli/src/operator/support/evidence.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "status.recovery_evidence", pattern: "recovery_evidence" }
    - { from: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs", to: "scripts/run-live-mainnet-smoke.ts", via: "allowlisted summary keys", pattern: "recoveryEvidence" }
    - { from: "packages/open-bitcoin-cli/src/operator/dashboard/model.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "shared status snapshot", pattern: "Recovery evidence" }
---

<objective>
Project recovery evidence into support, live-smoke, and dashboard surfaces from the shared status snapshot.

Purpose: D-12 requires support evidence and dashboard summaries to use the same recovery evidence fields as status, with unavailable reasons preserved.
Output: compact support JSON/Markdown recovery evidence, live-smoke recovery evidence summaries, and dashboard rows sourced from `OpenBitcoinStatusSnapshot.recovery_evidence`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-03-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/evidence.rs
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status snapshot -> support bundle | Local status evidence becomes shareable support JSON/Markdown. |
| live-smoke report -> support bundle | Opt-in public-network report data is reduced to an allowlisted local summary. |
| status snapshot -> dashboard model | Machine status is converted to terminal dashboard rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-10 | Information Disclosure | support bundle recovery evidence | mitigate | Include only category, cause, action class, basis labels, namespace/path metadata, and next action. |
| T-77-11 | Tampering | support collection | mitigate | Remove direct Fjall store opens from support bundle inspection; consume already-collected status evidence only. |
| T-77-12 | Repudiation | dashboard/support projection | mitigate | Reuse `status.recovery_evidence`; do not parse `sync.recovery_action` prose or duplicate category mapping. |
| T-77-12A | Information Disclosure | live-smoke summary | mitigate | Allowlist recovery evidence fields and keep raw logs, peer tables, RPC credentials, wallet material, and backend dumps out of support summaries. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact support recovery evidence</name>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/evidence.rs, packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/src/operator/support/evidence.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-cli/src/operator/support/tests.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: support JSON includes `recovery_evidence` with `state`, `category`, `cause`, `action_class`, `evidence_basis`, `next_action`, and `maybe_unavailable_reason`.
    - Test 2: support Markdown includes `## Recovery Evidence` and renders `Action class`, `Cause`, `Category`, and `Next action`.
    - Test 3: support bundle collection does not call `FjallNodeStore::open` for runtime metadata or metrics history.
    - Test 4: unavailable status recovery evidence is preserved as `state=unavailable` with the original reason.
  </behavior>
  <action>
    Add a compact support projection for `OpenBitcoinStatusSnapshot.recovery_evidence`. Prefer a small `RecoverySupportEvidence` struct in `support.rs` or a sibling helper matching the existing `resource_bounds` projection style. The JSON shape must include exactly these public field names: `state`, `category`, `cause`, `action_class`, `evidence_basis`, `affected_namespace`, `affected_path`, `next_action`, `compatibility_action`, `maybe_unavailable_reason`, and `source`.

    Update `derive_full_sync_evidence` in `support/evidence.rs` so the `FullSyncEvidence.recovery` summary prefers `status.recovery_evidence` and formats available evidence as `category=<category> cause=<cause> action_class=<action_class> next_action=<next_action>`. Fall back to existing `sync.recovery_category` and `sync.recovery_action` only when top-level evidence is unavailable.

    Update support bundle Markdown rendering with a `## Recovery Evidence` section. Render available fields from the compact support projection and render unavailable as `- Status: Unavailable: <reason>`.

    Remove direct `FjallNodeStore::open` calls from `packages/open-bitcoin-cli/src/operator/support.rs` for support-bundle runtime metadata and metrics history collection. Derive runtime metadata evidence from `status.service.restart_resume` and metrics evidence from `status.metrics`; when status lacks the data, use unavailable reasons `runtime metadata unavailable: probe-only support bundle does not open Fjall stores` and `metrics history unavailable: probe-only support bundle does not open Fjall stores`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib support_recovery_evidence_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "RecoverySupportEvidence|recovery_evidence|## Recovery Evidence|Action class|Cause|Next action" packages/open-bitcoin-cli/src/operator/support.rs packages/open-bitcoin-cli/src/operator/support/render.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds support JSON/Markdown projection code and tests.
    - `rg -n "category=.*cause=.*action_class=.*next_action=" packages/open-bitcoin-cli/src/operator/support/evidence.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds full-sync recovery summary formatting.
    - `rg -n "FjallNodeStore::open" packages/open-bitcoin-cli/src/operator/support.rs` finds no matches.
    - `rg -n "probe-only support bundle does not open Fjall stores" packages/open-bitcoin-cli/src/operator/support.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds explicit unavailable reasons.
  </acceptance_criteria>
  <done>Support bundles include compact shared recovery evidence and no longer open Fjall stores during support inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add live-smoke recovery evidence projection</name>
  <files>scripts/run-live-mainnet-smoke.ts, scripts/test-run-live-mainnet-smoke.sh, packages/open-bitcoin-cli/src/operator/support/live_smoke.rs</files>
  <read_first>
    - scripts/run-live-mainnet-smoke.ts
    - scripts/test-run-live-mainnet-smoke.sh
    - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: live-smoke JSON fixtures carrying top-level `recovery_evidence` produce final-status fields for recovery category, cause, action class, next action, and unavailable reason.
    - Test 2: live-smoke Markdown includes `Recovery action class`, `Recovery cause`, and `Recovery next action` when evidence is available.
    - Test 3: support live-smoke summaries include only allowlisted recovery evidence fields and redact raw logs, peer tables, RPC credentials, wallet material, and backend detail strings.
    - Test 4: missing live-smoke recovery evidence remains explicit as an unavailable reason rather than being silently omitted.
  </behavior>
  <action>
    Implement the D-12 live-smoke recovery evidence projection without adding live-smoke to default verification. In `scripts/run-live-mainnet-smoke.ts`, add typed report fields that mirror the shared Phase 77 status contract:
    - `recoveryEvidence: RecoveryEvidenceSummary | null`
    - `maybeRecoveryEvidenceUnavailableReason: string | null`
    - `recoveryActionClass: string | null`
    - `recoveryCause: string | null`
    - `recoveryNextAction: string | null`

    Populate those fields from `OpenBitcoinStatusSnapshot.recovery_evidence` when the status JSON contains it. If the live-smoke status command only exposes legacy sync metadata, preserve the existing `recoveryCategory` and `recoveryAction` compatibility fields and set `maybeRecoveryEvidenceUnavailableReason` from the missing field reason, defaulting to `recovery evidence unavailable`. Render the new fields in JSON and Markdown near the existing recovery category/action lines with exact Markdown labels `Recovery action class`, `Recovery cause`, and `Recovery next action`.

    Update `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` so the allowlisted summary accepts both camelCase and snake_case recovery evidence keys from live-smoke reports: `recoveryEvidence`, `recovery_evidence`, `recoveryActionClass`, `recovery_action_class`, `recoveryCause`, `recovery_cause`, `recoveryNextAction`, `recovery_next_action`, `maybeRecoveryEvidenceUnavailableReason`, and `maybe_recovery_evidence_unavailable_reason`. Summarize only `state`, `category`, `cause`, `action_class`, `actionClass`, `evidence_basis`, `evidenceBasis`, `affected_namespace`, `affectedNamespace`, `affected_path`, `affectedPath`, `next_action`, `nextAction`, `compatibility_action`, `compatibilityAction`, `maybe_unavailable_reason`, and `maybeUnavailableReason`.

    Update `scripts/test-run-live-mainnet-smoke.sh` deterministic fixtures to include an available recovery evidence object with `category=storage_lock_contention`, `cause=stale_lock_evidence`, `action_class=read_only_inspection`, and `next_action=Inspect the datadir read-only and avoid deleting lock artifacts automatically.` Add shell assertions that the generated JSON/Markdown contain those fields and still do not require public network, service managers, long sleeps, or default `scripts/verify.sh` wiring.
  </action>
  <verify>
    <automated>bash scripts/test-run-live-mainnet-smoke.sh</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib live_smoke_recovery_evidence_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Recovery action class|Recovery cause|Recovery next action|recoveryActionClass|recoveryCause|recoveryNextAction|maybeRecoveryEvidenceUnavailableReason" scripts/run-live-mainnet-smoke.ts scripts/test-run-live-mainnet-smoke.sh` finds live-smoke report fields and deterministic shell assertions.
    - `rg -n "phase77_live_smoke_summary_preserves_recovery_evidence|recoveryEvidence|recovery_action_class|stale_lock_evidence|read_only_inspection" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` finds allowlisted summary and test anchors.
    - `rg -n "rawPeerTable|daemonStdout|daemonStderr|rawLogTail|rpcpassword|rpcauth|__cookie__|walletMaterial" packages/open-bitcoin-cli/src/operator/support/live_smoke.rs` still finds redaction/exclusion tests.
    - `rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress" scripts/verify.sh` finds no matches; live-smoke remains opt-in.
  </acceptance_criteria>
  <done>Live-smoke reports and support summaries project shared recovery evidence fields with deterministic tests while keeping live-smoke outside default verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dashboard recovery evidence rows</name>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    - packages/open-bitcoin-node/src/recovery.rs
  </read_first>
  <behavior>
    - Test 1: dashboard status section includes `Recovery evidence` sourced from top-level status evidence.
    - Test 2: available evidence renders `category=... cause=... action_class=...`.
    - Test 3: unavailable evidence renders `Unavailable: recovery evidence unavailable`.
  </behavior>
  <action>
    Update the dashboard model projection so the status/details section includes a row labeled exactly `Recovery evidence`. The row must read from `snapshot.recovery_evidence`, not from `snapshot.sync.recovery_action` prose. Render available evidence as `category=<category> cause=<cause> action_class=<action_class>`, appending `next_action=<next_action>` if space is already available in the local row format. Preserve existing `Recovery category` and `Recovery` rows for compatibility.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib dashboard_recovery_evidence_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Recovery evidence|recovery_evidence|action_class" packages/open-bitcoin-cli/src/operator/dashboard/model.rs packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs` finds dashboard projection and tests.
    - `rg -n "sync\\.recovery_action.*cause|sync\\.recovery_action.*action_class" packages/open-bitcoin-cli/src/operator/dashboard/model.rs` finds no renderer-local parsing of recovery prose.
    - Focused dashboard tests exit 0.
  </acceptance_criteria>
  <done>Dashboard summaries expose the same recovery evidence as status/support without duplicating classification logic.</done>
</task>

</tasks>

<verification>
Run focused support and dashboard tests. Review support output for compact allowlisted fields only and confirm no direct store open remains in support bundle collection.
</verification>

<success_criteria>
- Support JSON and Markdown include compact recovery evidence with action class and next action.
- Dashboard rows consume top-level recovery evidence.
- Support bundle collection does not mutate or open damaged datadirs while gathering recovery evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-04-SUMMARY.md`.
</output>
