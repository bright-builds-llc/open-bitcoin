---
phase: 77-corruption-and-lock-recovery-hardening
plan: 02
type: execute
wave: 2
depends_on: [77-01-PLAN.md]
files_modified:
  - packages/open-bitcoin-node/src/storage.rs
  - packages/open-bitcoin-node/src/storage/lock_probe.rs
  - packages/open-bitcoin-node/src/storage/fjall_store.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [REC-05, REC-06, REC-08]
requirements_addressed: [REC-05, REC-06, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Lock contention and stale-lock evidence are collected without opening or mutating the Fjall store."
    - "Real Fjall lock/open failures still map through StorageError and storage_lock_contention."
    - "Deterministic storage fixtures cover lock contention, stale lock evidence, schema mismatch, corruption marker, partial write, and backend open failure."
  artifacts:
    - { path: "packages/open-bitcoin-node/src/storage/lock_probe.rs", provides: "Probe-only Fjall lock evidence collector" }
    - { path: "packages/open-bitcoin-node/src/storage/fjall_store.rs", provides: "Typed Fjall backend lock/open error mapping" }
    - { path: "packages/open-bitcoin-node/src/storage/fjall_store/tests.rs", provides: "Real temp-store recovery fixtures" }
  key_links:
    - { from: "packages/open-bitcoin-node/src/storage.rs", to: "packages/open-bitcoin-node/src/storage/lock_probe.rs", via: "pub use lock_probe", pattern: "probe_fjall_lock" }
    - { from: "packages/open-bitcoin-node/src/storage/fjall_store.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "StorageError backend mapping consumed by classifier", pattern: "fjall::Error::Locked" }
---

<objective>
Add non-mutating Fjall lock evidence and strengthen real storage-open error mapping.

Purpose: REC-05 requires safe lock diagnosis with no hidden source datadir mutation, while D-03 requires backend open failures to keep flowing through `StorageError`.
Output: A probe-only lock collector, typed Fjall lock mapping, and deterministic Fjall recovery fixtures.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-RESEARCH.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-01-SUMMARY.md
@packages/open-bitcoin-node/src/recovery.rs
@packages/open-bitcoin-node/src/storage.rs
@packages/open-bitcoin-node/src/storage/fjall_store.rs
@packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
@/Users/peterryszkiewicz/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fjall-3.1.4/src/error.rs
@/Users/peterryszkiewicz/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fjall-3.1.4/src/locked_file.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> lock evidence | Local datadir and lock-file state becomes operator evidence. |
| Fjall backend -> StorageError | Backend-specific errors become stable Open Bitcoin recovery categories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Tampering | `probe_fjall_lock` | mitigate | Use `std::fs::File::open` and advisory `try_lock` only; do not create, delete, clear, repair, or open the database. |
| T-77-05 | Denial of Service | lock-holder tests | mitigate | Keep lock-holder fixtures local, deterministic, and explicitly dropped before cleanup. |
| T-77-06 | Repudiation | backend error mapping | mitigate | Match `fjall::Error::Locked` explicitly and test resulting `StorageError::recovery_category()`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add probe-only Fjall lock evidence</name>
  <files>packages/open-bitcoin-node/src/storage.rs, packages/open-bitcoin-node/src/storage/lock_probe.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    - /Users/peterryszkiewicz/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fjall-3.1.4/src/locked_file.rs
    - /Users/peterryszkiewicz/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fjall-3.1.4/src/file.rs
    - docs/parity/source-breadcrumbs.json
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A missing datadir returns unavailable evidence with reason `lock probe unavailable: datadir does not exist`.
    - Test 2: A datadir without a `lock` file returns `LockEvidenceKind::NoLockArtifact` and does not create the lock file.
    - Test 3: A present-but-unheld `lock` file returns `LockEvidenceKind::StaleLockEvidence` and leaves the file in place.
    - Test 4: A held Fjall store returns `LockEvidenceKind::ActiveContention` without calling `FjallNodeStore::open` from the probe.
  </behavior>
  <action>
    Add `packages/open-bitcoin-node/src/storage/lock_probe.rs` and re-export it from `storage.rs` with `mod lock_probe; pub use lock_probe::probe_fjall_lock;`.

    Implement `pub const FJALL_LOCK_FILE_NAME: &str = "lock";` and `pub fn probe_fjall_lock(datadir: impl AsRef<Path>) -> FieldAvailability<LockEvidence>`.

    The probe must use only filesystem metadata and the lock file itself:
    - if `datadir` does not exist or is not a directory, return `FieldAvailability::unavailable("lock probe unavailable: datadir does not exist")`
    - if `<datadir>/lock` does not exist, return `LockEvidence { kind: LockEvidenceKind::NoLockArtifact, lock_path: "<path>", detail: "no Fjall lock artifact found" }`
    - if `<datadir>/lock` opens and `try_lock()` succeeds, unlock/drop the file and return `LockEvidenceKind::StaleLockEvidence` with detail `Fjall lock artifact is present but not currently held`
    - if `try_lock()` returns `WouldBlock`, return `LockEvidenceKind::ActiveContention` with detail `Fjall lock is currently held by another opener`
    - for other open or lock errors, return `LockEvidenceKind::ProbeUnavailable` with a bounded detail string prefixed `lock probe unavailable:`

    Do not call `FjallNodeStore::open`, `Database::builder`, `create_dir_all`, `File::create`, `remove_file`, `remove_dir_all`, repair, clear, compact, reindex, or marker-writing APIs in `probe_fjall_lock`. Add the new Rust file to `docs/parity/source-breadcrumbs.json` using the `none` breadcrumb style.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib lock_probe_ --all-features</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "FJALL_LOCK_FILE_NAME|pub fn probe_fjall_lock|LockEvidenceKind::NoLockArtifact|LockEvidenceKind::ActiveContention|LockEvidenceKind::StaleLockEvidence|LockEvidenceKind::ProbeUnavailable" packages/open-bitcoin-node/src/storage/lock_probe.rs` finds all probe labels.
    - `rg -n "FjallNodeStore::open|Database::builder|create_dir_all|File::create|remove_file|remove_dir_all|clear_recovery_marker|mark_interrupted_write" packages/open-bitcoin-node/src/storage/lock_probe.rs` finds no matches.
    - `rg -n "lock probe unavailable: datadir does not exist|no Fjall lock artifact found|Fjall lock artifact is present but not currently held|Fjall lock is currently held by another opener" packages/open-bitcoin-node/src/storage/lock_probe.rs packages/open-bitcoin-node/src/storage/fjall_store/tests.rs` finds concrete evidence strings.
    - The lock probe tests use temp datadirs only and do not require public network, service managers, large disk allocation, or non-portable process scans.
  </acceptance_criteria>
  <done>Lock evidence can be collected safely without hidden datadir mutation or platform process scanning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map real Fjall recovery failures through typed evidence</name>
  <files>packages/open-bitcoin-node/src/storage/fjall_store.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-node/src/storage/fjall_store.rs
    - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - /Users/peterryszkiewicz/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fjall-3.1.4/src/error.rs
  </read_first>
  <behavior>
    - Test 1: Opening a store while another `FjallNodeStore` holds it returns `StorageError::BackendFailure` whose `recovery_category()` is `SyncRecoveryCategory::StorageLockContention`.
    - Test 2: Opening a path that is a regular file returns `StorageError::BackendFailure` and classifier output `category=storage_backend_failure`, `cause=backend_open_failure`, `action_class=stop_and_escalate`.
    - Test 3: Existing schema mismatch, malformed snapshot, and recovery marker tests additionally assert classifier causes `schema_mismatch`, `corruption_marker`, and `partial_write`.
  </behavior>
  <action>
    Update `backend_failure(namespace, error: fjall::Error) -> StorageError` in `fjall_store.rs` to match `fjall::Error::Locked` explicitly. For `Locked`, set `message` to `database locked by another process` and `action` to `StorageRecoveryAction::Restart`. Preserve `StorageRecoveryAction::for_backend_message(&message)` for other backend errors so Phase 76 storage-pressure mapping remains intact.

    Extend `fjall_store/tests.rs` with deterministic recovery tests named with the `fjall_recovery_evidence_` prefix. Use existing `temp_store_path` and `remove_dir_if_exists` helpers. Keep one `FjallNodeStore` guard alive while attempting a second open to prove lock contention. Use a path-as-file fixture for storage-open failure. Reuse existing schema mismatch, malformed snapshot, and recovery marker fixtures to call `classify_recovery` and assert the concrete `RecoveryCause` and `RecoveryActionClass` values from Plan 77-01.

    Do not add sleep-based timing, public-network setup, service-manager setup, OS process scanning, or automatic destructive repair.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib fjall_recovery_evidence_ --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage_recovery_category_maps_schema_corruption_lock_and_backend_states --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fjall::Error::Locked|database locked by another process" packages/open-bitcoin-node/src/storage/fjall_store.rs` finds explicit typed lock mapping.
    - `rg -n "fjall_recovery_evidence_lock_contention|fjall_recovery_evidence_path_as_file|fjall_recovery_evidence_schema_mismatch|fjall_recovery_evidence_corruption_marker|fjall_recovery_evidence_partial_write" packages/open-bitcoin-node/src/storage/fjall_store/tests.rs` finds deterministic fixture names.
    - `rg -n "RecoveryCause::SchemaMismatch|RecoveryCause::CorruptionMarker|RecoveryCause::PartialWrite|RecoveryCause::BackendOpenFailure|RecoveryActionClass::StopAndEscalate" packages/open-bitcoin-node/src/storage/fjall_store/tests.rs` finds classifier assertions.
    - Focused tests exit 0 without leaked child processes, public peers, service managers, or large disk allocations.
  </acceptance_criteria>
  <done>Real Fjall lock and storage-open failures preserve typed storage mapping and feed the shared recovery classifier with deterministic tests.</done>
</task>

</tasks>

<verification>
Run both focused Cargo test commands and the parity breadcrumb checker. Review the diff to confirm `probe_fjall_lock` never opens or creates the store and no destructive repair/cleanup behavior was added.
</verification>

<success_criteria>
- REC-05 lock contention and stale-lock evidence have a probe-only implementation.
- REC-06 storage failures keep typed categories for schema, corruption, partial write, and backend open failure.
- REC-08 deterministic Fjall fixtures prove lock, stale lock, schema mismatch, corruption marker, partial write, and open-failure paths.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-02-SUMMARY.md`.
</output>
