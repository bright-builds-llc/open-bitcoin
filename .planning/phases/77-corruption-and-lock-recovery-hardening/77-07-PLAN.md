---
phase: 77-corruption-and-lock-recovery-hardening
plan: 07
type: execute
wave: 7
depends_on: [77-01-PLAN.md, 77-02-PLAN.md, 77-03-PLAN.md, 77-04-PLAN.md, 77-05-PLAN.md, 77-06-PLAN.md]
files_modified:
  - scripts/check-phase77-corruption-lock-recovery.ts
  - scripts/check-phase77-corruption-lock-recovery.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/77-corruption-and-lock-recovery-hardening/77-VERIFICATION.md
autonomous: true
requirements: [REC-05, REC-06, REC-07, REC-08]
requirements_addressed: [REC-05, REC-06, REC-07, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Default verification remains deterministic and does not require public peers, service managers, large disk allocation, or multi-day waits."
    - "The Phase 77 checker validates requirements, source anchors, tests, docs, parity roots, and verifier wiring."
    - "The checker validates live-smoke recovery evidence projection anchors while keeping opt-in live-smoke commands out of default verification."
    - "Phase verification records evidence for lock contention, stale locks, corruption markers, schema mismatch, partial writes, and storage-open failures."
  artifacts:
    - { path: "scripts/check-phase77-corruption-lock-recovery.ts", provides: "Focused deterministic Phase 77 checker" }
    - { path: "scripts/verify.sh", provides: "Repo-native verifier wiring after Phase 76 checker" }
    - { path: "scripts/run-live-mainnet-smoke.ts", provides: "Checked opt-in live-smoke recovery evidence report anchors" }
    - { path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs", provides: "Checked allowlisted live-smoke recovery summary anchors" }
    - { path: ".planning/phases/77-corruption-and-lock-recovery-hardening/77-VERIFICATION.md", provides: "Phase 77 completion evidence" }
  key_links:
    - { from: "scripts/verify.sh", to: "scripts/check-phase77-corruption-lock-recovery.ts", via: "Bun checker invocation", pattern: "check-phase77-corruption-lock-recovery" }
    - { from: "scripts/check-phase77-corruption-lock-recovery.ts", to: "packages/open-bitcoin-node/src/recovery.rs", via: "source anchors", pattern: "RecoveryEvidenceSnapshot" }
    - { from: "scripts/check-phase77-corruption-lock-recovery.ts", to: "scripts/run-live-mainnet-smoke.ts", via: "live-smoke recovery evidence anchors", pattern: "recoveryActionClass" }
---

<objective>
Wire deterministic Phase 77 verification into the repo-native verifier and record completion evidence.

Purpose: REC-08 requires deterministic recovery tests, and the phase checker must guard that implementation, docs, parity roots, and default-verification boundaries stay intact.
Output: Bun checker, checker tests, `scripts/verify.sh` wiring, LOC freshness, and Phase 77 verification report.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-RESEARCH.md
@scripts/verify.sh
@scripts/check-phase76-resource-bounds.ts
@scripts/check-phase76-resource-bounds.test.ts
@scripts/run-live-mainnet-smoke.ts
@scripts/test-run-live-mainnet-smoke.sh
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
@docs/metrics/lines-of-code.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker -> source tree | Automation asserts that recovery safety anchors exist. |
| verifier -> local environment | Repo-native verification runs local commands and updates tracked generated LOC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-19 | Repudiation | phase checker | mitigate | Check concrete source, tests, docs, parity, plan requirements, and verify wiring together. |
| T-77-20 | Denial of Service | default verification | mitigate | Keep checker hermetic; reject public-network, service-manager, multi-day, and large-disk gates in `scripts/verify.sh`. |
| T-77-21 | Tampering | verification report | mitigate | Record commands, pass/fail evidence, and residual risks only after checks pass. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic Phase 77 checker</name>
  <files>scripts/check-phase77-corruption-lock-recovery.ts, scripts/check-phase77-corruption-lock-recovery.test.ts, scripts/verify.sh</files>
  <read_first>
    - scripts/check-phase76-resource-bounds.ts
    - scripts/check-phase76-resource-bounds.test.ts
    - scripts/verify.sh
    - standards/languages/typescript-javascript.md
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: checker fails when any REC-05 through REC-08 requirement is absent from Phase 77 plan frontmatter.
    - Test 2: checker fails when `recovery_evidence`, action-class labels, or cause labels are missing from source anchors.
    - Test 3: checker fails when status/support files contain direct `FjallNodeStore::open` inspection calls.
    - Test 4: checker fails when live-smoke report or support-summary recovery evidence anchors are missing.
    - Test 5: checker fails when `scripts/verify.sh` omits the checker test/checker or wires it before Phase 76.
  </behavior>
  <action>
    Add `scripts/check-phase77-corruption-lock-recovery.ts` using the Phase 76 checker style: repo-root-relative file reads, `requireContains`, `requireNotContains`, a `frontmatterFor` helper, an accumulated `failures: string[]`, and a final success line `validated Phase 77 corruption and lock recovery hardening boundaries`.

    The checker must validate:
    - all Phase 77 plan frontmatters cover `REC-05`, `REC-06`, `REC-07`, and `REC-08`
    - source anchors contain `RecoveryEvidenceSnapshot`, `RecoveryActionClass`, `RecoveryCause`, `RecoveryEvidenceBasis`, `LockEvidenceKind`, `probe_fjall_lock`, `FJALL_LOCK_FILE_NAME`, `fjall::Error::Locked`, and `database locked by another process`
    - operator status/support anchors contain `recovery_evidence`, `Recovery evidence:`, `## Recovery Evidence`, `stale_lock_evidence`, `concurrent_datadir_use`, and `probe-only status does not open Fjall stores`
    - live-smoke anchors contain `recoveryEvidence`, `recoveryActionClass`, `recoveryCause`, `recoveryNextAction`, `maybeRecoveryEvidenceUnavailableReason`, `Recovery action class`, `Recovery cause`, `Recovery next action`, `phase77_live_smoke_summary_preserves_recovery_evidence`, and `bash scripts/test-run-live-mainnet-smoke.sh`
    - soak anchors contain `maybe_recovery_action_class_label`, `maybe_recovery_cause_label`, `Recovery action class`, `Recovery cause`, and `Recovery next action`
    - docs/parity anchors contain `phase77-corruption-and-lock-recovery-hardening`, all four REC IDs, and the exact no-mutation boundary sentence from Plan 77-06
    - deterministic test anchors include `lock_probe_`, `fjall_recovery_evidence_`, `status_recovery_evidence_`, `support_recovery_evidence_`, `live_smoke_recovery_evidence_`, `dashboard_recovery_evidence_`, and `soak_recovery_evidence_`
    - `scripts/verify.sh` runs `bun test scripts/check-phase77-corruption-lock-recovery.test.ts` and `bun run scripts/check-phase77-corruption-lock-recovery.ts` after `bun run scripts/check-phase76-resource-bounds.ts`
    - `scripts/verify.sh` does not contain forbidden default-verification strings: `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl`, `launchctl`, `openbitcoinsync=mainnet-ibd`, `sleep 86400`, `lsof`, and `/proc/`

    Add `scripts/check-phase77-corruption-lock-recovery.test.ts` with fixture-root tests for the checker helpers and failure cases. Wire the checker test and checker into `scripts/verify.sh` immediately after the Phase 76 checker command.
  </action>
  <verify>
    <automated>bun test scripts/check-phase77-corruption-lock-recovery.test.ts</automated>
    <automated>bun run scripts/check-phase77-corruption-lock-recovery.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validated Phase 77 corruption and lock recovery hardening boundaries|REC-05|REC-06|REC-07|REC-08|RecoveryEvidenceSnapshot|probe_fjall_lock|check-phase77-corruption-lock-recovery" scripts/check-phase77-corruption-lock-recovery.ts` finds checker anchors.
    - `rg -n "recoveryEvidence|recoveryActionClass|recoveryCause|recoveryNextAction|maybeRecoveryEvidenceUnavailableReason|phase77_live_smoke_summary_preserves_recovery_evidence|live_smoke_recovery_evidence_" scripts/check-phase77-corruption-lock-recovery.ts` finds live-smoke checker anchors.
    - `rg -n "bun test scripts/check-phase77-corruption-lock-recovery.test.ts|bun run scripts/check-phase77-corruption-lock-recovery.ts" scripts/verify.sh` finds verify wiring.
    - `rg -n "run-live-mainnet-smoke|--manual-peer|--restart-after-progress|systemctl|launchctl|openbitcoinsync=mainnet-ibd|sleep 86400|lsof|/proc/" scripts/check-phase77-corruption-lock-recovery.ts` finds forbidden-string checks.
    - Checker tests and checker both exit 0.
  </acceptance_criteria>
  <done>The Phase 77 checker is hermetic, tested, and wired into the repo-native verifier after Phase 76.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full verification and record Phase 77 evidence</name>
  <files>docs/metrics/lines-of-code.md, .planning/phases/77-corruption-and-lock-recovery-hardening/77-VERIFICATION.md</files>
  <read_first>
    - scripts/verify.sh
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Run `bash scripts/verify.sh`. Review the diff for unintended side effects. Record `.planning/phases/77-corruption-and-lock-recovery-hardening/77-VERIFICATION.md` with status `passed` only after full verification succeeds. The report must list focused commands for recovery classifier, lock probe, Fjall recovery evidence, status recovery evidence, support/dashboard recovery evidence, soak recovery evidence, checker tests, checker, and full `bash scripts/verify.sh`.

    Include residual risks that public-network soak, real service-manager behavior, and destructive repair remain outside default verification and scoped out of Phase 77.
  </action>
  <verify>
    <automated>bash scripts/verify.sh</automated>
    <automated>node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 77 --require-plans --require-verification --raw</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify.sh` exits 0.
    - `77-VERIFICATION.md` contains `status: passed`.
    - `77-VERIFICATION.md` contains `lock contention`, `stale lock`, `corruption marker`, `schema mismatch`, `partial write`, and `storage-open failure`.
    - `docs/metrics/lines-of-code.md` is fresh if verification updates it.
  </acceptance_criteria>
  <done>Full repo verification passes and Phase 77 verification evidence records commands, coverage, and residual risks.</done>
</task>

</tasks>

<verification>
Run checker tests, checker, full repo verifier, and lifecycle validation. Do not mark the phase passed if any command fails.
</verification>

<success_criteria>
- Phase 77 checker and checker tests are wired into `scripts/verify.sh`.
- Full `bash scripts/verify.sh` passes.
- Phase 77 verification report records deterministic coverage for all REC-08 paths.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-07-SUMMARY.md`.
</output>
