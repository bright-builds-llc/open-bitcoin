---
phase: 77-corruption-and-lock-recovery-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/recovery.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [REC-06, REC-07, REC-08]
requirements_addressed: [REC-06, REC-07, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Recovery evidence is a shared typed status contract, not renderer-local prose."
    - "Stable recovery category labels remain unchanged while richer cause and action-class fields are added beside them."
    - "The pure classifier emits safe_retry, read_only_inspection, backup_then_rebuild, or stop_and_escalate for every storage, marker, lock, and resource input."
  artifacts:
    - { path: "packages/open-bitcoin-node/src/recovery.rs", provides: "Pure recovery evidence contracts and classifier" }
    - { path: "packages/open-bitcoin-node/src/status.rs", provides: "Top-level recovery_evidence status snapshot field" }
    - { path: "docs/parity/source-breadcrumbs.json", provides: "Breadcrumb coverage for the new Rust recovery module" }
  key_links:
    - { from: "packages/open-bitcoin-node/src/status.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "FieldAvailability<RecoveryEvidenceSnapshot>", pattern: "recovery_evidence" }
    - { from: "packages/open-bitcoin-node/src/lib.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "public recovery evidence exports", pattern: "RecoveryEvidenceSnapshot" }
---

<objective>
Create the shared Phase 77 recovery evidence contract and pure classifier that downstream status, support, dashboard, and soak surfaces consume.

Purpose: REC-06 and REC-07 require typed storage recovery causes and explicit safety guidance without changing the stable `SyncRecoveryCategory` labels.
Output: `RecoveryEvidenceSnapshot`, action-class and cause enums, classifier tests, and a top-level status field.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-RESEARCH.md
@packages/open-bitcoin-node/src/lib.rs
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/status/recovery.rs
@packages/open-bitcoin-node/src/status/tests.rs
@packages/open-bitcoin-node/src/storage.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
Existing stable category labels from `packages/open-bitcoin-node/src/status/recovery.rs`:
```rust
pub enum SyncRecoveryCategory {
    CleanShutdown,
    UncleanShutdown,
    IncompatibleSchema,
    StoreCorruption,
    StorageLockContention,
    StorageBackendFailure,
    ResourceExhaustion,
    InvalidPeerData,
    PublicNetworkUnreachable,
    OperatorCancellation,
}
```

Existing storage inputs from `packages/open-bitcoin-node/src/storage.rs`:
```rust
pub enum StorageError {
    InvalidSchemaVersion { version: u32 },
    SchemaMismatch { expected: SchemaVersion, actual: SchemaVersion },
    Corruption { namespace: StorageNamespace, detail: String, action: StorageRecoveryAction },
    UnavailableNamespace { namespace: StorageNamespace },
    InterruptedWrite { namespace: StorageNamespace, action: StorageRecoveryAction },
    BackendFailure { namespace: StorageNamespace, message: String, action: StorageRecoveryAction },
}

pub struct RecoveryMarker {
    pub namespace: StorageNamespace,
    pub action: StorageRecoveryAction,
    pub detail: String,
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage error -> status JSON | Backend and codec errors cross into operator-visible output. |
| recovery marker -> support evidence | Local durable recovery details cross into shareable support bundles. |
| classifier -> renderers | Safety guidance becomes operator-facing text and must not imply mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | `classify_recovery` | mitigate | Model recovery guidance as typed action classes and never emit automatic repair/delete/clear guidance. |
| T-77-02 | Repudiation | `RecoveryEvidenceSnapshot` | mitigate | Preserve stable `SyncRecoveryCategory` while recording cause, basis, namespace/path, and next action in one shared DTO. |
| T-77-03 | Information Disclosure | support-bound recovery details | mitigate | Keep detail fields short, typed, and allowlisted; do not add raw logs, peer tables, wallet data, credentials, or unbounded backend dumps. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recovery evidence contracts and status field</name>
  <files>packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-node/src/recovery.rs, packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-node/src/lib.rs
    - packages/open-bitcoin-node/src/status.rs
    - packages/open-bitcoin-node/src/status/recovery.rs
    - packages/open-bitcoin-node/src/status/tests.rs
    - packages/open-bitcoin-node/src/storage.rs
    - docs/parity/source-breadcrumbs.json
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `RecoveryActionClass` serializes as `safe_retry`, `read_only_inspection`, `backup_then_rebuild`, and `stop_and_escalate`.
    - Test 2: `RecoveryCause` serializes as `schema_mismatch`, `corruption_marker`, `partial_write`, `unreadable_namespace`, `backend_open_failure`, `active_lock`, `stale_lock_evidence`, `concurrent_datadir_use`, and `resource_pressure`.
    - Test 3: `LockEvidence` serializes as `{"kind":"stale_lock_evidence","lock_path":"...","detail":"..."}` and round-trips with the exact `kind`, `lock_path`, and `detail` fields consumed by Plan 77-02.
    - Test 4: Legacy `OpenBitcoinStatusSnapshot` JSON without `recovery_evidence` decodes with `FieldAvailability::Unavailable { reason: "recovery evidence unavailable" }`.
  </behavior>
  <action>
    Add `packages/open-bitcoin-node/src/recovery.rs` as a pure module and expose it from `packages/open-bitcoin-node/src/lib.rs` with `pub mod recovery;` plus public exports for `RecoveryActionClass`, `RecoveryCause`, `RecoveryEvidenceBasis`, `RecoveryEvidenceSnapshot`, `RecoveryClassifierInput`, `LockEvidence`, `LockEvidenceKind`, and `classify_recovery`.

    Define these serde snake_case enums exactly:
    - `RecoveryActionClass`: `SafeRetry`, `ReadOnlyInspection`, `BackupThenRebuild`, `StopAndEscalate`
    - `RecoveryCause`: `SchemaMismatch`, `CorruptionMarker`, `PartialWrite`, `UnreadableNamespace`, `BackendOpenFailure`, `ActiveLock`, `StaleLockEvidence`, `ConcurrentDatadirUse`, `ResourcePressure`
    - `RecoveryEvidenceBasis`: `StorageError`, `RecoveryMarker`, `LockProbe`, `ServiceStatus`, `LiveRpc`, `ResourceBounds`, `UnavailableReason`
    - `LockEvidenceKind`: `NoLockArtifact`, `ActiveContention`, `StaleLockEvidence`, `ProbeUnavailable`

    Define `LockEvidence` in this plan before Plan 77-02 depends on it. The exact struct shape is:

    ```rust
    #[derive(Clone, Debug, Eq, PartialEq, Serialize, Deserialize)]
    pub struct LockEvidence {
        pub kind: LockEvidenceKind,
        pub lock_path: String,
        pub detail: String,
    }
    ```

    Add a contract test that constructs each `LockEvidenceKind` through this struct, verifies serde JSON contains `kind`, `lock_path`, and `detail`, and verifies the `kind` values use the `LockEvidenceKind` snake_case labels. Plan 77-02 must populate this struct exactly rather than introducing variant-specific structs or alternative field names.

    Define `RecoveryEvidenceSnapshot` with fields `category: SyncRecoveryCategory`, `action_class: RecoveryActionClass`, `cause: RecoveryCause`, `evidence_basis: Vec<RecoveryEvidenceBasis>`, `maybe_affected_namespace: Option<String>`, `maybe_affected_path: Option<String>`, `next_action: String`, and `compatibility_action: FieldAvailability<String>`. Use `maybe_` prefixes for optional fields per the Rust standard.

    Add `pub const RECOVERY_EVIDENCE_UNAVAILABLE_REASON: &str = "recovery evidence unavailable";` and implement `Default for FieldAvailability<RecoveryEvidenceSnapshot>` returning that unavailable reason.

    Add `#[serde(default)] pub recovery_evidence: FieldAvailability<RecoveryEvidenceSnapshot>` to `OpenBitcoinStatusSnapshot` immediately before `resource_bounds`, preserving existing `sync.recovery_category` and `sync.recovery_action`. Add focused status tests for legacy JSON defaulting and JSON field presence. Add the new Rust file to `docs/parity/source-breadcrumbs.json` using the existing Open Bitcoin-only `none` breadcrumb style.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib recovery_evidence_contract_ --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib status_recovery_evidence_ --all-features</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub enum RecoveryActionClass|SafeRetry|ReadOnlyInspection|BackupThenRebuild|StopAndEscalate" packages/open-bitcoin-node/src/recovery.rs` finds all action classes.
    - `rg -n "pub enum RecoveryCause|SchemaMismatch|CorruptionMarker|PartialWrite|UnreadableNamespace|BackendOpenFailure|ActiveLock|StaleLockEvidence|ConcurrentDatadirUse|ResourcePressure" packages/open-bitcoin-node/src/recovery.rs` finds all cause variants.
    - `rg -n "pub struct LockEvidence|pub kind: LockEvidenceKind|pub lock_path: String|pub detail: String|stale_lock_evidence" packages/open-bitcoin-node/src/recovery.rs packages/open-bitcoin-node/src/status/tests.rs` finds the shared lock evidence struct and serialization coverage.
    - `rg -n "recovery_evidence: FieldAvailability<RecoveryEvidenceSnapshot>" packages/open-bitcoin-node/src/status.rs` finds the top-level status field.
    - `rg -n "RECOVERY_EVIDENCE_UNAVAILABLE_REASON|recovery evidence unavailable" packages/open-bitcoin-node/src/recovery.rs packages/open-bitcoin-node/src/status/tests.rs` finds the default unavailable reason and test coverage.
    - `rg -n "packages/open-bitcoin-node/src/recovery.rs" docs/parity/source-breadcrumbs.json` finds the parity breadcrumb entry.
  </acceptance_criteria>
  <done>Recovery evidence has a shared typed contract, stable JSON labels, status snapshot defaulting, and parity breadcrumb coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure recovery classification matrix</name>
  <files>packages/open-bitcoin-node/src/recovery.rs, packages/open-bitcoin-node/src/status/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-node/src/recovery.rs
    - packages/open-bitcoin-node/src/storage.rs
    - packages/open-bitcoin-node/src/status/recovery.rs
    - packages/open-bitcoin-node/src/status/resource_bounds.rs
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: schema mismatch maps to `category=incompatible_schema`, `cause=schema_mismatch`, `action_class=backup_then_rebuild`.
    - Test 2: recovery marker or interrupted write maps to `cause=partial_write`, includes `RecoveryEvidenceBasis::RecoveryMarker`, and emits `backup_then_rebuild`.
    - Test 3: active lock maps to `storage_lock_contention` and `safe_retry`; stale lock maps to `storage_lock_contention` and `read_only_inspection`; concurrent datadir use outranks stale-lock evidence.
    - Test 4: stop-required resource bounds map to `resource_exhaustion` with `safe_retry`; generic backend open failure maps to `storage_backend_failure` with `stop_and_escalate`.
  </behavior>
  <action>
    Implement `RecoveryClassifierInput` as an owned input struct with these fields: `maybe_storage_error: Option<StorageError>`, `maybe_recovery_marker: Option<RecoveryMarker>`, `lock_evidence: FieldAvailability<LockEvidence>`, `service_same_datadir: FieldAvailability<bool>`, `live_rpc_available: FieldAvailability<bool>`, `resource_bounds: FieldAvailability<ResourceBoundSnapshot>`, and `unavailable_reason: String`.

    Implement `classify_recovery(input: RecoveryClassifierInput) -> FieldAvailability<RecoveryEvidenceSnapshot>` with this exact precedence:
    1. `resource_bounds` with `overall_level == ResourcePressureState::StopRequired` -> category `ResourceExhaustion`, cause `ResourcePressure`, action class `SafeRetry`, basis `ResourceBounds`, next action `Free disk space or reduce the reported resource pressure before retrying the operation.`
    2. live RPC available with same-datadir service evidence true -> category `StorageLockContention`, cause `ConcurrentDatadirUse`, action class `ReadOnlyInspection`, basis `LockProbe`, `ServiceStatus`, `LiveRpc`, next action `Use the running daemon or stop it cleanly before any operation that needs exclusive datadir access.`
    3. active lock contention -> category `StorageLockContention`, cause `ActiveLock`, action class `SafeRetry`, basis `LockProbe`, next action `Retry after the process holding the datadir lock exits or after confirming the running daemon owns the datadir.`
    4. stale lock evidence -> category `StorageLockContention`, cause `StaleLockEvidence`, action class `ReadOnlyInspection`, basis `LockProbe`, next action `Inspect the datadir read-only and avoid deleting lock artifacts automatically.`
    5. `StorageError::SchemaMismatch` or `InvalidSchemaVersion` -> category `IncompatibleSchema`, cause `SchemaMismatch`, action class `BackupThenRebuild`.
    6. `StorageError::Corruption` -> category `StoreCorruption`, cause `CorruptionMarker`, action class `BackupThenRebuild`.
    7. `RecoveryMarker` or `StorageError::InterruptedWrite` -> category from the marker/action compatibility mapping, cause `PartialWrite`, action class `BackupThenRebuild`.
    8. `StorageError::UnavailableNamespace` -> category `StorageBackendFailure`, cause `UnreadableNamespace`, action class `StopAndEscalate`.
    9. `StorageError::BackendFailure` -> if `error.recovery_category()` is `StorageLockContention`, cause `ActiveLock` and action class `SafeRetry`; otherwise category from `error.recovery_category()`, cause `BackendOpenFailure`, action class `StopAndEscalate`.
    10. no signal -> unavailable with `input.unavailable_reason`.

    For schema, corruption, marker, namespace, and backend cases, set `maybe_affected_namespace` to `Some(namespace.as_str().to_string())` when a namespace exists. Set `compatibility_action` to the existing `StorageRecoveryAction::operator_message()` when one exists; otherwise use `FieldAvailability::unavailable("no compatibility recovery action recorded")`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib recovery_classifier_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct RecoveryClassifierInput|pub fn classify_recovery" packages/open-bitcoin-node/src/recovery.rs` finds the classifier contract.
    - `rg -n "ConcurrentDatadirUse|Use the running daemon or stop it cleanly|avoid deleting lock artifacts automatically|no compatibility recovery action recorded" packages/open-bitcoin-node/src/recovery.rs` finds the concrete locked-decision guidance.
    - `rg -n "StorageError::SchemaMismatch|StorageError::InterruptedWrite|StorageError::UnavailableNamespace|StorageError::BackendFailure" packages/open-bitcoin-node/src/recovery.rs` finds storage-error mapping branches.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib recovery_classifier_ --all-features` exits 0.
  </acceptance_criteria>
  <done>The pure classifier deterministically maps lock, schema, corruption, partial-write, unreadable-store, backend, and resource signals to typed categories and action classes.</done>
</task>

</tasks>

<verification>
Run the automated commands listed in each task. Before closing the plan, review the diff and confirm no existing `SyncRecoveryCategory` variant was renamed and no task adds automatic repair, lock cleanup, reindex, deletion, or datadir relocation behavior.
</verification>

<success_criteria>
- `RecoveryEvidenceSnapshot` is available as a public node crate status contract.
- `OpenBitcoinStatusSnapshot` includes top-level `recovery_evidence` with backward-compatible unavailable defaulting.
- Classifier tests cover REC-06 and REC-07 cause/action mappings and keep REC-08 deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-01-SUMMARY.md`.
</output>
