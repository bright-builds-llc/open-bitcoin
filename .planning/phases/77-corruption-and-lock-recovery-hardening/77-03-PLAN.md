---
phase: 77-corruption-and-lock-recovery-hardening
plan: 03
type: execute
wave: 3
depends_on: [77-01-PLAN.md, 77-02-PLAN.md]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs
  - packages/open-bitcoin-cli/src/operator/status/sync_state.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [REC-05, REC-06, REC-07, REC-08]
requirements_addressed: [REC-05, REC-06, REC-07, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Operator status JSON exposes top-level recovery_evidence even when sync state is unavailable."
    - "Status/support inspection does not call the normal Fjall store-open path to collect recovery evidence."
    - "Concurrent datadir use is classified from bounded service/RPC/same-datadir evidence, not process scans."
  artifacts:
    - { path: "packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs", provides: "CLI status recovery evidence collector" }
    - { path: "packages/open-bitcoin-cli/src/operator/status.rs", provides: "Status snapshot wiring for recovery_evidence" }
    - { path: "packages/open-bitcoin-cli/src/operator/status/render.rs", provides: "Human status recovery evidence rendering" }
  key_links:
    - { from: "packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs", to: "packages/open-bitcoin-node/src/storage/lock_probe.rs", via: "probe_fjall_lock", pattern: "probe_fjall_lock" }
    - { from: "packages/open-bitcoin-cli/src/operator/status.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "status.recovery_evidence", pattern: "recovery_evidence" }
---

<objective>
Wire shared recovery evidence into operator status while keeping status inspection probe-only for recovery and lock diagnosis.

Purpose: REC-05 through REC-07 must be visible from operator status without hidden source datadir mutation, and D-10 requires a top-level status field.
Output: status recovery collector, JSON/human rendering, binary status proof, and tests for active/stale/concurrent cases.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-01-SUMMARY.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-02-SUMMARY.md
@packages/open-bitcoin-node/src/recovery.rs
@packages/open-bitcoin-node/src/storage/lock_probe.rs
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/sync_state.rs
@packages/open-bitcoin-cli/src/operator/status/service_status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/status/tests.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
@docs/parity/source-breadcrumbs.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator datadir -> status | User-selected local path is inspected for recovery evidence. |
| live RPC/service evidence -> recovery classifier | Running-daemon signals help classify concurrent datadir use. |
| status JSON -> downstream support/soak/dashboard | Status becomes the source of truth for recovery evidence projections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-07 | Tampering | status collection | mitigate | Do not call `FjallNodeStore::open` from status inspection paths for recovery/durable status loading. |
| T-77-08 | Denial of Service | concurrent datadir diagnosis | mitigate | Combine lock probe, same-datadir service evidence, and live RPC availability; do not depend on `lsof`, `/proc`, or process scans. |
| T-77-09 | Information Disclosure | human/JSON status rendering | mitigate | Render typed labels, bounded paths, and next actions only; do not include credentials, raw logs, or backend dumps. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status recovery evidence collector</name>
  <files>packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs, packages/open-bitcoin-cli/src/operator/status/sync_state.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/status.rs
    - packages/open-bitcoin-cli/src/operator/status/sync_state.rs
    - packages/open-bitcoin-cli/src/operator/status/service_status.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - packages/open-bitcoin-node/src/storage/lock_probe.rs
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: stopped status for an empty datadir does not create `schema_version`, `CURRENT`, `MANIFEST`, `vlog`, or any Fjall store files.
    - Test 2: a present unheld `lock` file yields `recovery_evidence.value.cause == "stale_lock_evidence"` and action class `read_only_inspection`.
    - Test 3: a held store with same-datadir running service and reachable live RPC yields `cause == "concurrent_datadir_use"` and category `storage_lock_contention`.
    - Test 4: missing recovery evidence remains explicit unavailable JSON, not omitted.
  </behavior>
  <action>
    Add `packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs` with `collect_status_recovery_evidence(input: &StatusCollectorInput, service: &ServiceStatus, live_rpc_available: FieldAvailability<bool>) -> FieldAvailability<RecoveryEvidenceSnapshot>`. The function must:
    - use `input.config_resolution.maybe_data_dir` as the selected datadir path
    - call `probe_fjall_lock(datadir)` when a datadir exists
    - derive `service_same_datadir` from `service.restart_resume.value.same_datadir` when available
    - pass `live_rpc_available` through to `classify_recovery`
    - set classifier `unavailable_reason` to `recovery evidence unavailable: no storage, lock, service, or RPC signal`
    - never call `FjallNodeStore::open`, `WalletRegistry::load`, `Database::builder`, repair, clear, delete, compact, reindex, or marker-writing APIs

    In `status.rs`, add `mod recovery_evidence;` and call `collect_status_recovery_evidence` in both `collect_live_status_snapshot` and `stopped_status_snapshot`. Bind `let service = collect_service_status(input);` once and reuse it in the snapshot. Set `live_rpc_available` to `FieldAvailability::available(true)` only after `get_network_info`, `get_blockchain_info`, and `get_mempool_info` all succeed. Set it to `FieldAvailability::unavailable(reason.clone())` for stopped or unreachable snapshots.

    Remove store-backed inspection from status collection: `collect_status_snapshot`, `durable_sync_state` usage in status, `metrics_status`, and `resolve_status_wallet_rpc_access` must not call `FjallNodeStore::open` when serving `open-bitcoin status` or support-bundle status collection. Replace store-derived sync state with existing RPC status when live RPC succeeds, otherwise `unavailable_sync_status(reason)`. Replace metrics store inspection with `MetricsStatus::unavailable(MetricRetentionPolicy::default(), "metrics history unavailable: probe-only status does not open Fjall stores")` when no already-collected metrics are available. Change `resolve_status_wallet_rpc_access` to avoid store inspection and return `StatusWalletRpcAccess::Root` unless a live RPC response later reports a wallet-scope error.

    Add the new CLI Rust file to `docs/parity/source-breadcrumbs.json`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib status_recovery_evidence_ --all-features</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "collect_status_recovery_evidence|probe_fjall_lock|classify_recovery|recovery evidence unavailable: no storage, lock, service, or RPC signal" packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs` finds the collector and concrete unavailable reason.
    - `rg -n "FjallNodeStore::open|WalletRegistry::load|Database::builder|mark_interrupted_write|clear_recovery_marker|remove_file|remove_dir_all" packages/open-bitcoin-cli/src/operator/status.rs packages/open-bitcoin-cli/src/operator/status/sync_state.rs packages/open-bitcoin-cli/src/operator/status/recovery_evidence.rs` finds no matches.
    - `rg -n "metrics history unavailable: probe-only status does not open Fjall stores" packages/open-bitcoin-cli/src/operator/status.rs packages/open-bitcoin-cli/src/operator/status/tests.rs` finds the explicit metrics unavailable reason.
    - `rg -n "stale_lock_evidence|concurrent_datadir_use|storage_lock_contention|read_only_inspection" packages/open-bitcoin-cli/src/operator/status/tests.rs` finds JSON assertions for stale and concurrent lock cases.
  </acceptance_criteria>
  <done>Status collection exposes recovery evidence and avoids normal Fjall store opens for inspection-only status paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render recovery evidence in human and binary status</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/status/render.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
    - packages/open-bitcoin-node/src/recovery.rs
  </read_first>
  <behavior>
    - Test 1: human status includes a `Recovery evidence:` line after `Sync recovery:`.
    - Test 2: available evidence renders `category=storage_lock_contention cause=stale_lock_evidence action_class=read_only_inspection`.
    - Test 3: unavailable evidence renders `Recovery evidence: Unavailable: recovery evidence unavailable`.
    - Test 4: the operator binary stopped-node JSON includes top-level `recovery_evidence`.
  </behavior>
  <action>
    Update human status rendering in `render.rs` with a new line exactly labeled `Recovery evidence:` immediately after the existing `Sync recovery:` line. Render available evidence as `category=<category> cause=<cause> action_class=<action_class> next_action=<next_action>`, using the serde/stable labels from `as_str()` helpers or JSON serialization helpers. Render unavailable evidence as `Unavailable: <reason>`.

    Keep JSON status rendering as `serde_json::to_string_pretty(snapshot)` so top-level `recovery_evidence` is emitted from the shared snapshot contract. Add unit tests for available and unavailable human rendering. Update `packages/open-bitcoin-cli/tests/operator_binary.rs` so `open_bitcoin_status_json_succeeds_for_stopped_node` asserts the top-level `recovery_evidence` field exists and is unavailable or available with typed labels, depending on the fixture.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib status_recovery_evidence_render_ --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary open_bitcoin_status_json_succeeds_for_stopped_node --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Recovery evidence:" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/status/tests.rs` finds the human status label and tests.
    - `rg -n "category=.*cause=.*action_class=.*next_action=" packages/open-bitcoin-cli/src/operator/status/render.rs packages/open-bitcoin-cli/src/operator/status/tests.rs` finds the concrete rendering format.
    - `rg -n "recovery_evidence" packages/open-bitcoin-cli/tests/operator_binary.rs` finds binary JSON coverage.
    - Focused status tests pass without creating Fjall files in an empty status datadir.
  </acceptance_criteria>
  <done>Human and JSON status expose shared recovery evidence with stable labels and no renderer-local classification.</done>
</task>

</tasks>

<verification>
Run the focused status tests and operator binary test. Review the diff for direct `FjallNodeStore::open` use in status inspection paths and for accidental raw credential/log/backend detail rendering.
</verification>

<success_criteria>
- `open-bitcoin status --format json` includes top-level `recovery_evidence`.
- Active lock, stale lock evidence, and concurrent datadir use are distinguished through typed evidence.
- Status collection does not mutate empty or damaged datadirs through normal Fjall store opens.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-03-SUMMARY.md`.
</output>
