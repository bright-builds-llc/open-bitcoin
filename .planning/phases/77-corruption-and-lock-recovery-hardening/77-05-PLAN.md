---
phase: 77-corruption-and-lock-recovery-hardening
plan: 05
type: execute
wave: 5
depends_on: [77-04-PLAN.md]
files_modified:
  - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
  - packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
  - packages/open-bitcoin-cli/src/operator/soak/report.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs
autonomous: true
requirements: [REC-06, REC-07, REC-08]
requirements_addressed: [REC-06, REC-07, REC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 77-2026-06-15T18-33-03
generated_at: "2026-06-15T20:05:15Z"
must_haves:
  truths:
    - "Soak checkpoints preserve recovery cause, action class, and next action from the shared status evidence."
    - "Storage, schema, corruption, lock, and backend categories still produce recovery_stop while resource exhaustion remains resource_stop."
    - "Soak reports expose richer recovery evidence without changing Phase 75 outcome labels."
  artifacts:
    - { path: "packages/open-bitcoin-cli/src/operator/soak/ledger.rs", provides: "Recovery evidence fields in checkpoint status" }
    - { path: "packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs", provides: "Checkpoint and outcome projection from recovery_evidence" }
    - { path: "packages/open-bitcoin-cli/src/operator/soak/report.rs", provides: "Report rendering for recovery cause/action class" }
  key_links:
    - { from: "packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs", to: "packages/open-bitcoin-node/src/recovery.rs", via: "snapshot.recovery_evidence", pattern: "maybe_recovery_action_class_label" }
    - { from: "packages/open-bitcoin-cli/src/operator/soak/outcome.rs", to: "packages/open-bitcoin-node/src/status/recovery.rs", via: "stable recovery_stop mapping", pattern: "StorageBackendFailure" }
---

<objective>
Carry Phase 77 recovery evidence through soak checkpoints and reports while preserving existing soak outcome semantics.

Purpose: D-13 requires schema, corruption, lock, and backend storage categories to remain `recovery_stop`, with Phase 77 evidence making the stop more precise.
Output: richer soak checkpoint/report recovery fields and deterministic tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-01-SUMMARY.md
@.planning/phases/76-disk-and-resource-bound-enforcement/76-01-SUMMARY.md
@.planning/phases/77-corruption-and-lock-recovery-hardening/77-04-SUMMARY.md
@packages/open-bitcoin-cli/src/operator/soak/ledger.rs
@packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
@packages/open-bitcoin-cli/src/operator/soak/outcome.rs
@packages/open-bitcoin-cli/src/operator/soak/report.rs
@packages/open-bitcoin-cli/src/operator/soak/tests.rs
@packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status snapshot -> soak ledger | Recovery evidence is serialized into datadir-owned JSONL checkpoints. |
| soak ledger -> report | Checkpoint evidence becomes operator-readable report output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-13 | Repudiation | soak reports | mitigate | Preserve existing outcome labels and add cause/action fields from status evidence, not local reclassification. |
| T-77-14 | Information Disclosure | soak ledger/report recovery fields | mitigate | Store only stable labels and next action, not raw backend dumps, logs, credentials, or wallet material. |
| T-77-15 | Tampering | soak recovery stop mapping | mitigate | Keep `resource_exhaustion` mapped to `resource_stop` and storage categories mapped to `recovery_stop`; do not add repair actions. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend soak checkpoint recovery fields</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/ledger.rs, packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs, packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
    - packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs
    - packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs
    - packages/open-bitcoin-node/src/recovery.rs
    - .planning/phases/77-corruption-and-lock-recovery-hardening/77-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: checkpoint status from available top-level recovery evidence records action class, cause, and next action labels.
    - Test 2: checkpoint status from unavailable evidence leaves the new optional fields as `None`.
    - Test 3: recovery outcome uses `snapshot.recovery_evidence.value.category` when available and falls back to `snapshot.sync.recovery_category` for compatibility.
  </behavior>
  <action>
    Add these optional fields to `SoakCheckpointStatus` in `ledger.rs`: `maybe_recovery_action_class_label: Option<String>`, `maybe_recovery_cause_label: Option<String>`, and `maybe_recovery_next_action: Option<String>`.

    Update `checkpoint_status_from_snapshot` in `runtime/helpers.rs` to populate:
    - `maybe_recovery_category_label` from `snapshot.recovery_evidence.value.category.as_str()` when available, else existing `snapshot.sync.recovery_category`
    - `maybe_recovery_action_class_label` from the recovery evidence action class stable label
    - `maybe_recovery_cause_label` from the recovery evidence cause stable label
    - `maybe_recovery_next_action` from `snapshot.recovery_evidence.value.next_action`

    Update `outcome_for_snapshot` so available `snapshot.recovery_evidence.value.category` feeds `SoakOutcomeEvidence.maybe_recovery_category`. Preserve the existing Phase 75 outcome labels: `StorageLockContention`, `IncompatibleSchema`, `StoreCorruption`, and `StorageBackendFailure` classify as `RecoveryStop`; `ResourceExhaustion` classifies as `ResourceStop`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_recovery_evidence_checkpoint_ --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_outcome_classifies_recovery_and_resource_evidence --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_recovery_action_class_label|maybe_recovery_cause_label|maybe_recovery_next_action" packages/open-bitcoin-cli/src/operator/soak/ledger.rs packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs` finds checkpoint fields and tests.
    - `rg -n "snapshot\\.recovery_evidence|maybe_recovery_category_label" packages/open-bitcoin-cli/src/operator/soak/runtime/helpers.rs` finds top-level evidence projection.
    - `rg -n "ResourceExhaustion.*ResourceStop|StorageBackendFailure.*RecoveryStop|StorageLockContention.*RecoveryStop" packages/open-bitcoin-cli/src/operator/soak/tests.rs packages/open-bitcoin-cli/src/operator/soak/tests/runtime.rs` finds outcome assertions preserving D-13.
  </acceptance_criteria>
  <done>Soak checkpoint evidence carries Phase 77 cause/action details while preserving recovery_stop/resource_stop semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render recovery evidence in soak reports</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/report.rs, packages/open-bitcoin-cli/src/operator/soak/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/soak/report.rs
    - packages/open-bitcoin-cli/src/operator/soak/tests.rs
    - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
  </read_first>
  <behavior>
    - Test 1: JSON report includes `recovery_action_class`, `recovery_cause`, and `recovery_next_action` in the latest checkpoint payload.
    - Test 2: Markdown report includes `Recovery action class`, `Recovery cause`, and `Recovery next action` lines when present.
    - Test 3: report rendering omits raw backend detail strings and credentials.
  </behavior>
  <action>
    Update report projection/rendering to expose the new checkpoint fields without changing `SoakOutcomeLabel`. In Markdown, render these exact labels near the existing `Recovery category` line: `Recovery action class`, `Recovery cause`, and `Recovery next action`. In JSON, preserve the serialized `SoakCheckpointStatus` field names from Task 1.

    Add focused report tests that build a checkpoint with `maybe_recovery_action_class_label = Some("backup_then_rebuild")`, `maybe_recovery_cause_label = Some("partial_write")`, and `maybe_recovery_next_action = Some("Back up the selected datadir, then rebuild affected storage before normal operation.")`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_recovery_evidence_report_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Recovery action class|Recovery cause|Recovery next action" packages/open-bitcoin-cli/src/operator/soak/report.rs packages/open-bitcoin-cli/src/operator/soak/tests.rs` finds Markdown labels and tests.
    - `rg -n "backup_then_rebuild|partial_write|Back up the selected datadir" packages/open-bitcoin-cli/src/operator/soak/tests.rs` finds deterministic report fixture assertions.
    - `rg -n "raw backend|rpcpassword|Authorization|wallet material" packages/open-bitcoin-cli/src/operator/soak/tests.rs` finds redaction/exclusion assertions.
  </acceptance_criteria>
  <done>Soak reports expose Phase 77 recovery evidence details without changing outcome vocabulary or adding raw local artifacts.</done>
</task>

</tasks>

<verification>
Run the focused soak checkpoint/outcome/report tests. Review serialized checkpoint/report output to confirm recovery evidence is compact and the existing outcome taxonomy remains unchanged.
</verification>

<success_criteria>
- Soak checkpoint JSON records recovery category, action class, cause, and next action when available.
- Soak report Markdown displays recovery evidence details.
- Resource exhaustion remains `resource_stop`; schema/corruption/lock/backend categories remain `recovery_stop`.
</success_criteria>

<output>
After completion, create `.planning/phases/77-corruption-and-lock-recovery-hardening/77-05-SUMMARY.md`.
</output>
