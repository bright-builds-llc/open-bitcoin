---
phase: 142-manager-flush-lifecycle-and-restart
plan: "06"
type: tdd
wave: 5
depends_on:
  - "03"
  - "04"
  - "05"
files_modified:
  - packages/open-bitcoin-node/src/sync.rs
  - packages/open-bitcoin-node/src/sync/runtime_state.rs
  - packages/open-bitcoin-node/src/sync/tests/restart_chainstate.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/chainstate.rs
  - packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - MGR-02
user_setup: []
must_haves:
  truths:
    - "DurableSyncRuntime::open_with_runtime_activation attaches FjallCoinsView plus an empty cache after initialize/replay. It does not hydrate leftover snapshot UTXOs into MemoryCoinsView (D-13, D-17, MGR-02)."
    - "After same-datadir restart, tip and UTXO lookups come from coins B / FjallCoinsView, not a leftover snapshot blob (MGR-02)."
    - "persist_progress writes headers and runtime metadata only. It does not call save_chainstate_snapshot or seed_coins_from_snapshot (D-17)."
    - "Progress credit after persist_progress requires coins best-block B to equal the claimed tip hash; unflushed in-memory connects are not credited (D-17)."
    - "Leftover snapshot files may remain on disk unread. Production persist paths do not write them (D-17 discretion: leave unread)."
    - "Phase 140/141 persist_progress leftover-write guards are flipped (D-18)."
  artifacts:
    - path: packages/open-bitcoin-node/src/sync.rs
      provides: "open uses FlushLifecycle::initialize and Fjall parent"
      contains: "FlushLifecycle"
    - path: packages/open-bitcoin-node/src/sync/runtime_state.rs
      provides: "headers/runtime persist plus B-gated credit"
      contains: "fn persist_progress"
    - path: packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
      provides: "Flipped persist_progress leftover-write guard"
      contains: "durable_sync_persist_progress_does_not_write_snapshot"
  key_links:
    - from: packages/open-bitcoin-node/src/sync.rs
      to: packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
      via: "open_with_runtime_activation calls initialize"
      pattern: "initialize("
    - from: packages/open-bitcoin-node/src/sync/runtime_state.rs
      to: packages/open-bitcoin-node/src/storage/coins_view.rs
      via: "credit compares coins best_block to claimed tip"
      pattern: "best_block"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 142-2026-09-06T16-23-52
generated_at: 2026-09-06T17:20:00.000Z
---

# Phase 142 Plan 06: Restart From Coins Best-Block and Persist-Progress Cutover

<objective>
Cut production open and `persist_progress` off leftover snapshot writes so same-datadir restart and progress credit follow durable coins `B`.

Purpose: MGR-02 / D-13 / D-17 / D-18. Plan 05 already removed `ManagedChainstate::persist` leftover writes. This plan finishes write-site cutover and production Fjall parent attach.

Output: `open_with_runtime_activation` uses `FlushLifecycle::initialize` + `Chainstate::from_parent(FjallCoinsView)`. `persist_progress` is headers/runtime + B-gated credit. Leftover files left unread. Flipped persist_progress guards. `seed_coins_from_snapshot` remains a test helper only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-manager-flush-lifecycle-and-restart/142-CONTEXT.md
@.planning/phases/142-manager-flush-lifecycle-and-restart/142-RESEARCH.md
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/sync/tests/restart_chainstate.rs
@packages/open-bitcoin-node/src/network/relay_serving.rs
@packages/open-bitcoin-node/src/chainstate.rs
@packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
@packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
`DurableSyncRuntime::open_with_runtime_activation` today:
```rust
let memory_store = match store.hydrate_chainstate_for_open()? {
    Some(snapshot) => MemoryChainstateStore::from_snapshot(snapshot),
    None => MemoryChainstateStore::default(),
};
let mut network = ManagedPeerNetwork::with_sync_limits_and_block_relay_activation(memory_store, ...);
```

Target (D-11, D-13, MGR-02):
```rust
let (lifecycle, view, cache) = initialize(&store, now, next_write, 0, false, disk_free)?;
let active_chain = /* load chain_meta via existing load_hydrate_chain_meta or a new pub(crate) loader that does NOT copy leftover utxos */;
let undo_by_block = /* load_all_undo_records or empty */;
let chainstate = Chainstate::from_parent(view, active_chain, undo_by_block, counts);
let managed = ManagedChainstate::from_chainstate(FjallChainstateStore::from_store(store.clone()), chainstate);
let mut network = ManagedPeerNetwork::from_initialized_chainstate(managed, ...);
```

Add these constructors (do not make every test call site name `V`):

```rust
// chainstate.rs
impl<S: ChainstateStore> ManagedChainstate<S> {
    pub fn from_chainstate(store: S, chainstate: Chainstate<impl CoinsView>) -> Self;
}
```

If adding `V` to `ManagedChainstate` / `ManagedPeerNetwork` is required, use `V: CoinsView = MemoryCoinsView` so existing `ManagedPeerNetwork<MemoryChainstateStore>` tests compile. `Clone` for `ManagedChainstate` must stay `impl<S: Clone> Clone for ManagedChainstate<S, MemoryCoinsView>` only — production Fjall parents must not clone through `from_snapshot` (research pitfall).

`FjallChainstateStore` (new type in `chainstate.rs` or `flush_lifecycle.rs`):
- `load_snapshot()` → `None` (leftover unread)
- `save_snapshot()` → no-op
- coins methods forward to `FjallCoinsView::from_store`
- `load_undo` / `save_undo` forward to `FjallNodeStore`

`persist_progress` target (D-17):
```rust
self.store.save_header_entries(&header_entries, self.config.persist_mode)?;
let mut metadata = self.load_runtime_metadata()?;
metadata.last_clean_shutdown = false;
self.store.save_runtime_metadata(&metadata, self.config.persist_mode)?;
// no seed_coins_from_snapshot
// no save_chainstate_snapshot
```

Credit: when building progress after persist, treat durable tip as `store.coins_view().best_block()?`. `made_validated_durable_progress` / `connected_block` may keep in-memory tip as a separate fact, but `progress_credit` Available requires `best_block == Some(claimed_hash)`. If B is missing or differs, credit stays unavailable for that connect (allowed crash-loss window).

Flip guards (D-18):
- `durable_sync_persist_progress_still_writes_snapshot_and_does_not_call_decide_flush` → `durable_sync_persist_progress_does_not_write_snapshot`. Assert `!runtime_state.contains("save_chainstate_snapshot")` and `!runtime_state.contains("seed_coins_from_snapshot")`. `FlushMode` may appear only if you add a comment; do **not** call `decide_flush` from `persist_progress` (IfNeeded stays on commit — D-02).
- `persist_progress_source_still_writes_leftover_snapshot` in `coins_migration.rs` → assert the same negatives and that `chainstate.rs` no longer contains `save_snapshot(self.chainstate.snapshot())`.
- `restart_chainstate.rs` `persist_progress_seeds_coins_before_leftover_and_survives_missing_leftover`: replace with a test that persist_progress does not write leftover and reopen ignores leftover UTXOs.
- `open` source test that requires `MemoryChainstateStore::from_snapshot(snapshot)` and `hydrate_chainstate_for_open` must flip to `initialize` / `from_parent` / `FjallCoinsView`.

`hydrate_chainstate_for_open` may remain for tests/export. Production open must not use it for live UTXOs. `seed_coins_from_snapshot` / `seed_coins_from_leftover_for_reopen` stay as test helpers; production persist must not call them.

Do not delete leftover `"snapshot"` blobs (discretion: leave unread). Do not rewrite wallet-rescan product surfaces. Do not add Phase 143 availability labels or Phase 144 operator fields.
</interfaces>
</context>

<feature>
  <name>Restart from coins B and persist_progress cutover</name>
  <files>packages/open-bitcoin-node/src/sync.rs, packages/open-bitcoin-node/src/sync/runtime_state.rs</files>
  <behavior>
    Open attaches Fjall parent. Reopen tip equals coins B. persist_progress has no leftover write. Credit requires B match. Leftover blob on disk is ignored as UTXO truth.
  </behavior>
  <implementation>
    initialize + from_parent + FjallChainstateStore. Shrink persist_progress in place (already 625 lines).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing restart-from-B and leftover-cutover tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
    packages/open-bitcoin-node/src/sync/tests/restart_chainstate.rs
  </files>
  <read_first>
    .planning/phases/142-manager-flush-lifecycle-and-restart/142-CONTEXT.md
    .planning/phases/142-manager-flush-lifecycle-and-restart/142-RESEARCH.md
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/sync/tests/restart_chainstate.rs
    packages/open-bitcoin-chainstate/src/coins/tests/flush.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
    packages/open-bitcoin-node/src/network/relay_serving.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `durable_sync_persist_progress_does_not_write_snapshot`: `runtime_state.rs` source contains `fn persist_progress` and does not contain `save_chainstate_snapshot` or `seed_coins_from_snapshot` (D-18).
    - Test `persist_progress_source_does_not_write_leftover_snapshot`: replace `persist_progress_source_still_writes_leftover_snapshot`. Same negatives. `chainstate.rs` must not contain `save_snapshot(self.chainstate.snapshot())`.
    - Test `open_runtime_does_not_hydrate_leftover_utxos`: `sync.rs` source contains `initialize` and `FjallCoinsView` (or `from_parent`) and does not contain `hydrate_chainstate_for_open` inside `open_with_runtime_activation` (MGR-02).
    - Test `same_datadir_reopen_tip_matches_coins_best_block`: persist a flushed coins tip `B = hash_n` plus a leftover snapshot whose tip/utxos differ. Reopen via `DurableSyncRuntime::open_with_runtime_activation`. In-memory tip hash equals `coins_view.best_block()`. A leftover-only outpoint is `get_coin == Ok(None)` (MGR-02, leftover unread).
    - Test `persist_progress_does_not_credit_when_coins_b_lags_memory_tip`: after a Memory-only connect that did not Flush (IfNeeded None), `persist_progress` plus status credit must not report Available for the unflushed hash. After an Always/Flush that advances B, credit may become Available for that hash (D-17).
  </behavior>
  <action>
    RED. Per D-13, D-17, D-18, MGR-02:

    1. Flip both persist_progress leftover-write source tests as specified.
    2. Update `restart_chainstate.rs` hydrate/open assertions that require `MemoryChainstateStore::from_snapshot(snapshot)` or `hydrate_chainstate_for_open` in `open_with_runtime_activation`.
    3. Add `same_datadir_reopen_tip_matches_coins_best_block` using existing Fjall temp-dir + leftover plant helpers.
    4. Keep `seed_coins_from_snapshot` available for the leftover plant; production source under test must not call it.
    5. Do not add Phase 143 `durable_availability` or Phase 144 metrics.
    6. Combined RED+GREEN in one hook-passing feat commit is allowed.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase142-06-cutover-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib sync::tests::restart_chainstate -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `flush.rs` contains `durable_sync_persist_progress_does_not_write_snapshot` and does not contain `durable_sync_persist_progress_still_writes_snapshot_and_does_not_call_decide_flush`
    - `coins_migration.rs` contains `persist_progress_source_does_not_write_leftover_snapshot`
    - `restart_chainstate.rs` contains `same_datadir_reopen_tip_matches_coins_best_block`
    - `sync.rs` still contains `hydrate_chainstate_for_open` only if it is outside `open_with_runtime_activation` (allowed as unused helper call from tests)
  </acceptance_criteria>
  <done>Failing cutover and restart-from-B tests exist; leftover write guards now expect the new durability path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach Fjall parent on open and cut persist_progress</name>
  <files>
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/relay_serving.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    packages/open-bitcoin-node/src/sync.rs
    packages/open-bitcoin-node/src/sync/runtime_state.rs
    packages/open-bitcoin-node/src/chainstate.rs
    packages/open-bitcoin-node/src/chainstate/flush_lifecycle.rs
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/relay_serving.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    standards/core/architecture.md
    standards/core/code-shape.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    Task 1 tests pass. Existing restart tests that still require leftover writes are updated to seed coins via Fjall BatchWrite or `seed_coins_from_snapshot` as a test-only setup helper, not via persist_progress. `runtime_state.rs` and `sync.rs` stay ≤ 628 lines. Extract `open_with_runtime_activation` body to `sync/open_runtime.rs` if `sync.rs` would exceed 628.
  </behavior>
  <action>
    GREEN per D-13, D-17, D-18, MGR-02:

    1. Implement `FjallChainstateStore`, `ManagedChainstate::from_chainstate`, and `ManagedPeerNetwork::from_initialized_chainstate` (or extend `from_peer_manager` to accept a prebuilt `ManagedChainstate`). Default type parameters keep Memory tests compiling.
    2. Replace hydrate-into-Memory in `open_with_runtime_activation` with `initialize` + `Chainstate::from_parent`. Load `active_chain` from `chain_meta` only — add `pub(crate) fn load_chain_meta_for_open(&self) -> Result<(Vec<ChainPosition>, Option<HashMap<Txid, u32>>), StorageError>` on `FjallNodeStore` that does **not** fall back to leftover snapshot UTXOs. If `chain_meta` is missing, use `Vec::new()` and `Some(HashMap::new())` rather than leftover utxos.
    3. Rewrite `persist_progress` as headers + runtime only. Keep the function in `runtime_state.rs` and delete the seed/leftover lines so the file shrinks (currently 625 lines).
    4. Gate progress credit on `store.coins_view().best_block()` matching the claimed tip. Do not credit from `connected_block()` alone.
    5. If `sync.rs` would exceed 628 lines, create `sync/open_runtime.rs` with the same breadcrumbs as `sync.rs` and add it to the existing sync breadcrumb group in `docs/parity/source-breadcrumbs.json`.
    6. Update wallet-rescan or restart tests that fail solely because leftover is no longer written: seed coins in the test with `seed_coins_from_snapshot` (test helper) or `FjallCoinsView::batch_write`. Do not restore leftover writes on the production persist path.
    7. Do not delete leftover blobs. Do not call `decide_flush` from `persist_progress`. Do not add prune/assumeutxo/availability product surfaces.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase142-06-cutover-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib sync::tests::restart_chainstate -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `open_with_runtime_activation` does not contain `hydrate_chainstate_for_open`
    - `runtime_state.rs` `fn persist_progress` does not contain `save_chainstate_snapshot` or `seed_coins_from_snapshot`
    - `runtime_state.rs` persist_progress path contains `save_header_entries` and `save_runtime_metadata`
    - Production open path contains `from_parent` or `FjallCoinsView`
    - `same_datadir_reopen_tip_matches_coins_best_block` passes
    - Flipped leftover-write guards pass
    - `wc -l packages/open-bitcoin-node/src/sync.rs packages/open-bitcoin-node/src/sync/runtime_state.rs` both ≤ 628
    - `scripts/check-pure-core-deps.sh` still passes (no Fjall in chainstate)
  </acceptance_criteria>
  <done>Restart tip and UTXO view come from coins B; persist_progress no longer writes leftover snapshots; leftover files are unread; progress credit is B-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Leftover snapshot blob → reopen | Attacker- or crash-written leftover JSON must not become UTXO truth |
| In-memory tip → progress credit | Crediting unflushed connects lies about durability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-21 | Tampering | `open_with_runtime_activation` | mitigate | Ignore leftover UTXOs; attach `FjallCoinsView`; tip from coins B (MGR-02) |
| T-142-22 | Tampering | `persist_progress` | mitigate | Stop `save_chainstate_snapshot` / `seed_coins_from_snapshot` (D-17) |
| T-142-23 | Spoofing | progress credit | mitigate | Credit only when coins `B` equals the claimed tip |
| T-142-24 | Tampering | leftover file left on disk | accept | Unread leftover is non-authoritative; deletion is optional (discretion) |
| T-142-25 | Spoofing | serve from coins B | accept | Phase 143 owns payload-present availability; do not set serve labels from B |
</threat_model>

<verification>
Restart-from-B test passes. Both leftover-write guard families are flipped and pass. `persist_progress` has no leftover write. Core crate remains I/O-free. No Phase 143/144/FUT surfaces added.
</verification>

<success_criteria>
Same-datadir restart follows coins B. Leftover snapshot is not live UTXO truth. Progress credit is B-gated. D-18 guards match the new durability path.
</success_criteria>

<output>
After completion, create `.planning/phases/142-manager-flush-lifecycle-and-restart/142-06-SUMMARY.md`
</output>
