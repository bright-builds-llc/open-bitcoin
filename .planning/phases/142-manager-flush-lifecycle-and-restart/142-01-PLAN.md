---
phase: 142-manager-flush-lifecycle-and-restart
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-chainstate/src/error.rs
  - packages/open-bitcoin-node/src/storage/coins_view.rs
  - packages/open-bitcoin-node/src/storage/coins_view/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
autonomous: true
requirements:
  - FLUSH-02
user_setup: []
must_haves:
  truths:
    - "Two-element H plus missing B is an observable interrupted marker: head_blocks returns Ok([new, old]) and FjallNodeStore::open succeeds."
    - "Schema-2 leftover plus two-element H without B is interrupted, never leftover-empty RestoreFromBackup (WR-02 / D-10)."
    - "Interrupted-write remap matches ChainstateError::InterruptedWrite, not Display text containing interrupted write (IN-01 / D-10)."
    - "Empty H plus present B stays consistent; unexpected H counts and decode failures stay fail-closed Corruption (D-09)."
    - "hydrate_chainstate_for_open still returns InterruptedWrite for two-element H without B so leftover scanners do not invent a tip."
  artifacts:
    - path: packages/open-bitcoin-chainstate/src/error.rs
      provides: "Typed InterruptedWrite distinct from CoinsStorage Display flattening"
      contains: "InterruptedWrite"
    - path: packages/open-bitcoin-node/src/storage/coins_view.rs
      provides: "MarkerState::Interrupted and Ok two-hash head_blocks"
      contains: "Interrupted"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
      provides: "ensure_schema_two skips leftover-empty when H is two-element; typed map_heads_error"
      contains: "InterruptedTwoHeads"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
      to: packages/open-bitcoin-node/src/storage/coins_view.rs
      via: "ensure_schema_two classifies head_blocks before leftover-empty"
      pattern: "decide_recovery"
    - from: packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
      to: packages/open-bitcoin-chainstate/src/error.rs
      via: "map_heads_error matches InterruptedWrite variant"
      pattern: "ChainstateError::InterruptedWrite"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 142-2026-09-06T16-23-52
generated_at: 2026-09-06T17:20:00.000Z
---

# Phase 142 Plan 01: Observable Interrupted H and Typed Remap

<objective>
Make two-element `H` plus missing `B` an observable interrupted-flush marker that store-open can surface, and remap interrupted writes by typed error instead of Display text.

Purpose: FLUSH-02 cannot replay if `FjallNodeStore::open` still fail-closes before a manager exists, or if leftover-empty shadows interrupted `H` (141-REVIEW WR-02). IN-01 / D-10 requires typed remap.

Output: `MarkerState::Interrupted { new, old }`, `head_blocks() -> Ok([new, old])`, store open succeeds, leftover-empty skipped when `decide_recovery(2)`, `ChainstateError::InterruptedWrite`, no `detail.contains("interrupted write")`. No ReplayBlocks, no persist cutover, no CanFlush.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-manager-flush-lifecycle-and-restart/142-CONTEXT.md
@.planning/phases/142-manager-flush-lifecycle-and-restart/142-RESEARCH.md
@packages/open-bitcoin-chainstate/src/error.rs
@packages/open-bitcoin-chainstate/src/coins/flush.rs
@packages/open-bitcoin-node/src/storage/coins_view.rs
@packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
@packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/storage/coins_view.rs today:
```rust
enum MarkerState {
    Empty,
    Consistent { best_block: BlockHash },
}
// classify_markers: (2, None) => Err(interrupted_write())
// head_blocks: Empty | Consistent => Ok(Vec::new())
```

Target after this plan (D-09, D-10):
```rust
enum MarkerState {
    Empty,
    Consistent { best_block: BlockHash },
    Interrupted { new: BlockHash, old: BlockHash },
}
// (2, None) => Ok(Interrupted { new: heads[0], old: heads[1] })
// head_blocks(Interrupted) => Ok(vec![new, old])
// best_block(Interrupted) => Ok(None)
```

From packages/open-bitcoin-chainstate/src/error.rs — add exactly:
```rust
InterruptedWrite { heads: Vec<BlockHash> },
```
Display: `interrupted coins write`. Add the variant to `display_messages_cover_every_error_variant`.

From packages/open-bitcoin-chainstate/src/coins/flush.rs — do not edit. `decide_recovery(2) == RecoveryDecision::InterruptedTwoHeads`. Node code may call it.

From packages/open-bitcoin-node/src/storage/fjall_store/coins.rs today:
```rust
fn map_heads_error(error: ChainstateError) -> StorageError {
    let ChainstateError::CoinsStorage { detail } = error else { /* Corruption */ };
    if detail.contains("interrupted write") { return interrupted_coins_write(); }
    StorageError::Corruption { .. }
}
```
Target: match `ChainstateError::InterruptedWrite { .. }` first. Delete the Display substring branch.

Do not edit `ManagedChainstate::persist`, `persist_progress`, `flush.rs` leftover-write guards, or implement ReplayBlocks.
</interfaces>
</context>

<feature>
  <name>Observable interrupted H and typed InterruptedWrite remap</name>
  <files>packages/open-bitcoin-node/src/storage/coins_view.rs, packages/open-bitcoin-node/src/storage/fjall_store/coins.rs, packages/open-bitcoin-chainstate/src/error.rs</files>
  <behavior>
    Two-element H without B opens the store and returns two hashes from head_blocks. Leftover snapshot plus that H is not leftover-empty. map_heads_error never inspects Display text. hydrate_chainstate_for_open still returns StorageError::InterruptedWrite for two-element H without B.
  </behavior>
  <implementation>
    classify_markers returns Interrupted instead of Err. ensure_schema_two and hydrate classify interrupted H before leftover-empty. map_storage / map_heads_error carry ChainstateError::InterruptedWrite.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing interrupted-marker and typed-remap tests</name>
  <files>
    packages/open-bitcoin-chainstate/src/error.rs
    packages/open-bitcoin-node/src/storage/coins_view/tests.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
  </files>
  <read_first>
    .planning/phases/142-manager-flush-lifecycle-and-restart/142-CONTEXT.md
    .planning/phases/142-manager-flush-lifecycle-and-restart/142-RESEARCH.md
    packages/open-bitcoin-node/src/storage/coins_view.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/coins_migration.rs
    packages/open-bitcoin-node/src/storage/coins_view/tests.rs
    packages/open-bitcoin-chainstate/src/error.rs
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `head_blocks_two_element_h_without_b_returns_both_hashes`: plant H=`[BlockHash([0x44;32]), BlockHash([0x55;32])]` with no B. `FjallNodeStore::open` returns `Ok`. `FjallCoinsView::head_blocks()` equals `Ok(vec![hash_44, hash_55])`. `best_block()` equals `Ok(None)`. `decide_recovery(heads.len())` equals `RecoveryDecision::InterruptedTwoHeads` (D-07, D-09).
    - Test `leftover_plus_two_element_h_without_b_opens_as_interrupted_not_leftover_empty`: replace `leftover_plus_two_element_h_without_b_is_interrupted_write`. Open must be `Ok` (not leftover-empty `RestoreFromBackup`). After open, `head_blocks().len() == 2`. Do not expect `FjallNodeStore::open` to return `InterruptedWrite` (D-10 / WR-02).
    - Test `hydrate_two_element_h_without_b_fails_closed` stays `StorageError::InterruptedWrite { namespace: Coins, action: Reindex }` so leftover scanners do not invent a tip (D-08 hydrate boundary).
    - Test `map_heads_error_source_does_not_match_display_text`: `include_str` of `fjall_store/coins.rs` must not contain `detail.contains("interrupted write")` and must contain `ChainstateError::InterruptedWrite` (IN-01 / D-10).
    - Test `interrupted_write_display_covers_variant`: `ChainstateError::InterruptedWrite { heads: vec![BlockHash::from_byte_array([1;32])] }.to_string()` is non-empty and is not `missing coin`.
  </behavior>
  <action>
    RED only. Per D-09, D-10, WR-02, IN-01:

    1. Add `ChainstateError::InterruptedWrite { heads: Vec&lt;BlockHash&gt; }` to `error.rs` with Display `interrupted coins write`. Add it to `display_messages_cover_every_error_variant`. Do not implement coins_view classify yet so new head_blocks tests fail on behavior.
    2. In `coins_view/tests.rs` add `head_blocks_two_element_h_without_b_returns_both_hashes` using the existing raw-byte plant helpers (`write_raw_bytes`, `encode_head_blocks_value`, `encode_head_blocks_key`).
    3. Rewrite `leftover_plus_two_element_h_without_b_is_interrupted_write` in `coins_migration.rs` to the new name and assertions above. Keep the leftover snapshot plant plus H-without-B plant.
    4. Add `map_heads_error_source_does_not_match_display_text` in `coins_migration.rs`.
    5. Keep `hydrate_two_element_h_without_b_fails_closed` assertions unchanged.
    6. Do not implement ReplayBlocks. Do not flip Phase 140 leftover-write guards. Do not edit `persist` or `persist_progress`.
    7. Combined RED+GREEN in one hook-passing feat commit is allowed because pre-commit runs tests, matching Phase 140/141.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase142-01-interrupted-marker-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-chainstate/src/error.rs` contains `InterruptedWrite { heads: Vec<BlockHash> }`
    - `coins_view/tests.rs` contains `head_blocks_two_element_h_without_b_returns_both_hashes`
    - `coins_migration.rs` contains `leftover_plus_two_element_h_without_b_opens_as_interrupted_not_leftover_empty`
    - `coins_migration.rs` contains `map_heads_error_source_does_not_match_display_text`
    - `hydrate_two_element_h_without_b_fails_closed` still expects `StorageError::InterruptedWrite`
    - New tests are RED on behavior (not missing-type compile errors) before Task 2 unless RED+GREEN are combined
  </acceptance_criteria>
  <done>Failing tests exist for observable two-hash H, leftover-empty skip, typed remap, and hydrate still fail-closed on interrupted H.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Interrupted marker, leftover-empty skip, and typed remap</name>
  <files>
    packages/open-bitcoin-node/src/storage/coins_view.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-chainstate/src/error.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/storage/coins_view.rs
    packages/open-bitcoin-node/src/storage/fjall_store/coins.rs
    packages/open-bitcoin-chainstate/src/error.rs
    packages/open-bitcoin-chainstate/src/coins/flush.rs
    .planning/phases/142-manager-flush-lifecycle-and-restart/142-RESEARCH.md
    standards/core/architecture.md
    standards/core/code-shape.md
  </read_first>
  <behavior>
    Same tests as Task 1 pass. classify_markers (2, None) is Ok(Interrupted). (2, Some(_)) stays Corruption. (0, Some) Consistent. (0, None) Empty. Other counts stay Corruption. map_storage maps StorageError::InterruptedWrite to ChainstateError::InterruptedWrite. map_heads_error maps ChainstateError::InterruptedWrite to StorageError::InterruptedWrite. ensure_schema_two and hydrate call decide_recovery(heads.len()) and skip leftover-empty when InterruptedTwoHeads. hydrate still returns interrupted_coins_write() for that case.
  </behavior>
  <action>
    GREEN per D-09, D-10:

    1. In `coins_view.rs` add `MarkerState::Interrupted { new: BlockHash, old: BlockHash }`. Change `(2, None)` to `Ok(Interrupted { new: heads[0], old: heads[1] })`. `best_block` on Interrupted returns `Ok(None)`. `head_blocks` on Interrupted returns `Ok(vec![new, old])`. Empty/Consistent still return `Ok(Vec::new())`.
    2. Change `map_storage` to:
    ```rust
    match error {
        StorageError::InterruptedWrite { .. } => ChainstateError::InterruptedWrite { heads: Vec::new() },
        other => ChainstateError::CoinsStorage { detail: other.to_string() },
    }
    ```
    3. In `fjall_store/coins.rs` `ensure_schema_two`:
    ```rust
    let heads = FjallCoinsView::from_store(self).head_blocks().map_err(map_heads_error)?;
    if matches!(decide_recovery(heads.len()), RecoveryDecision::InterruptedTwoHeads) {
        return Ok(());
    }
    // existing leftover_present && coins_empty leftover-empty error
    ```
    Import `decide_recovery` / `RecoveryDecision` from `open_bitcoin_core::chainstate`.
    4. In `hydrate_chainstate_for_open`, after `head_blocks()`, if `decide_recovery(heads.len())` is `InterruptedTwoHeads` return `Err(interrupted_coins_write())` **before** leftover-empty. Leftover-empty remains only when H is not interrupted.
    5. Replace `map_heads_error` with a typed match. Delete `if detail.contains("interrupted write")`. `ChainstateError::InterruptedWrite` → `interrupted_coins_write()`. `CoinsStorage { detail }` → `Corruption { namespace: Coins, detail, action: Repair }`. Other variants → Corruption with `error.to_string()`, never a substring search for interrupted.
    6. Do not auto-reindex. `StorageRecoveryAction::Reindex` stays diagnostic (FUT-23 out of scope).
    7. Do not grow `coins_view.rs` or `coins.rs` past 628 physical lines. Do not add Fjall to `open-bitcoin-chainstate`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase142-01-interrupted-marker-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `coins_view.rs` contains `Interrupted { new` and `(2, None)` does not call `interrupted_write()`
    - `head_blocks` match arm returns two hashes for Interrupted
    - `fjall_store/coins.rs` contains `decide_recovery` and `InterruptedTwoHeads` before leftover-empty
    - `fjall_store/coins.rs` does not contain `detail.contains("interrupted write")`
    - `fjall_store/coins.rs` contains `ChainstateError::InterruptedWrite`
    - `error.rs` contains `InterruptedWrite`
    - Task 1 tests pass
    - `wc -l packages/open-bitcoin-node/src/storage/coins_view.rs packages/open-bitcoin-node/src/storage/fjall_store/coins.rs` both ≤ 628
  </acceptance_criteria>
  <done>Interrupted H is observable at head_blocks, store open succeeds, leftover-empty is skipped, remap is typed, hydrate still fail-closes without inventing a tip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk markers → store open | Untrusted `H`/`B` bytes and leftover snapshot blob cross into typed recovery |
| ChainstateError → StorageError | Flattening a typed interrupt into Display text can hide crash state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | `classify_markers` / `ensure_schema_two` | mitigate | Classify two-element H before leftover-empty; never treat interrupted H as consistent leftover-empty tip (WR-02) |
| T-142-02 | Information disclosure | `map_heads_error` | mitigate | Match `ChainstateError::InterruptedWrite`; delete Display substring remap (IN-01) |
| T-142-03 | Tampering | `hydrate_chainstate_for_open` | mitigate | Two-element H without B returns `InterruptedWrite`; do not scan C into a snapshot tip |
| T-142-04 | Denial of service | `StorageRecoveryAction::Reindex` | accept | Diagnostic only; FUT-23 automatic reindex stays out of scope |
</threat_model>

<verification>
`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::` passes. `flush.rs` still has no `ReplayBlocks` / `BlockHash`. Leftover-write guards in Phase 140/141 tests stay unflipped.
</verification>

<success_criteria>
Store open plus `head_blocks` observe interrupted H. Leftover-empty cannot shadow it. Remap is typed. Hydrate still fail-closes. Replay, CanFlush, and persist cutover are not implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/142-manager-flush-lifecycle-and-restart/142-01-SUMMARY.md`
</output>
