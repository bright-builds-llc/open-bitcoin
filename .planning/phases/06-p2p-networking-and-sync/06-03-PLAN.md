---
phase: 06-p2p-networking-and-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/open-bitcoin-node/Cargo.toml
  - packages/open-bitcoin-node/BUILD.bazel
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-network/src/peer.rs
autonomous: true
requirements:
  - P2P-01
  - P2P-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 06-2026-04-15T00-28-26
generated_at: 2026-04-15T00:28:26Z
must_haves:
  truths:
    - The node shell exposes a thin managed networking adapter over the pure
      networking core, managed chainstate, and managed mempool.
    - Block data received from peers applies through managed chainstate rather
      than bypassing the existing core.
    - Transaction relay preserves txid or wtxid negotiation and submits inbound
      transactions through the managed mempool.
  artifacts:
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/lib.rs
  key_links:
    - open-bitcoin-node depends on open-bitcoin-network as its protocol core.
---

<objective>
Implement the thin node-side networking adapter and transaction or block relay
integration.

Purpose: connect the pure-core peer manager to managed chainstate, managed
mempool, and local block or transaction stores without moving protocol rules
into the shell.
Output: a managed network wrapper that can handshake, serve or request data,
apply blocks, and admit relayed transactions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p2p-networking-and-sync/06-CONTEXT.md
@.planning/phases/06-p2p-networking-and-sync/06-RESEARCH.md
@.planning/phases/06-p2p-networking-and-sync/06-01-PLAN.md
@.planning/phases/06-p2p-networking-and-sync/06-02-PLAN.md
@packages/open-bitcoin-node/src/chainstate.rs
@packages/open-bitcoin-node/src/mempool.rs
@packages/open-bitcoin-mempool/src/lib.rs
@packages/bitcoin-knots/test/functional/p2p_tx_download.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire open-bitcoin-node to the new networking core</name>
  <files>packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/BUILD.bazel, packages/open-bitcoin-node/src/lib.rs</files>
  <read_first>packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/BUILD.bazel, packages/open-bitcoin-node/src/lib.rs</read_first>
  <action>Add the networking crate dependency to the node shell and expose the managed wrapper from the node crate's public API.</action>
  <verify>`cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-node`</verify>
  <acceptance_criteria>
    - open-bitcoin-node depends on open-bitcoin-network
    - the managed network wrapper is exported from `open-bitcoin-node`
  </acceptance_criteria>
  <done>The runtime shell can construct the networking adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the managed networking adapter and relay integration</name>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-network/src/peer.rs</files>
  <read_first>packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-node/src/mempool.rs, packages/bitcoin-knots/test/functional/p2p_tx_download.py</read_first>
  <action>Implement a thin managed wrapper that serves `getheaders` or `getdata` from local block or transaction stores, applies received blocks through managed chainstate, and submits received transactions through the managed mempool while preserving txid or wtxid relay negotiation.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib managed_network_requests_transactions_using_wtxidrelay_when_negotiated`</verify>
  <acceptance_criteria>
    - Received blocks are applied through managed chainstate
    - Received transactions go through managed mempool
    - Relay requests switch between txid and wtxid inventory types based on negotiation
  </acceptance_criteria>
  <done>The shell integrates networking with the existing managed adapters without owning protocol rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The node shell drives block sync and tx relay through the new networking core

</success_criteria>

<output>
After completion, create `.planning/phases/06-p2p-networking-and-sync/06-03-SUMMARY.md`
</output>
