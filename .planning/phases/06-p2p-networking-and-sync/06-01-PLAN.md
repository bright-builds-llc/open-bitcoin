---
phase: 06-p2p-networking-and-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/BUILD.bazel
  - BUILD.bazel
  - packages/README.md
  - scripts/pure-core-crates.txt
  - packages/open-bitcoin-core/Cargo.toml
  - packages/open-bitcoin-core/BUILD.bazel
  - packages/open-bitcoin-core/src/lib.rs
  - packages/open-bitcoin-network/Cargo.toml
  - packages/open-bitcoin-network/BUILD.bazel
  - packages/open-bitcoin-network/src/lib.rs
  - packages/open-bitcoin-network/src/error.rs
  - packages/open-bitcoin-network/src/message.rs
  - packages/open-bitcoin-network/src/peer.rs
autonomous: true
requirements:
  - P2P-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 06-2026-04-15T00-28-26
generated_at: 2026-04-15T00:28:26Z
must_haves:
  truths:
    - The workspace exposes a dedicated pure-core networking crate instead of
      burying peer state in the runtime shell.
    - The pure-core crate owns strict wire-message handling for the Phase 6
      command subset and explicit peer lifecycle state.
    - open-bitcoin-core re-exports the networking crate for downstream callers.
  artifacts:
    - packages/open-bitcoin-network/Cargo.toml
    - packages/open-bitcoin-network/src/lib.rs
    - packages/open-bitcoin-network/src/message.rs
    - packages/open-bitcoin-network/src/peer.rs
  key_links:
    - BUILD.bazel exposes a public networking alias.
---

<objective>
Implement the new pure-core networking crate and the first peer-lifecycle
surface.

Purpose: establish the Phase 6 crate boundary, wire-message subset, and peer
state foundation that later sync and relay plans can build on.
Output: a new `open-bitcoin-network` crate wired into the workspace with typed
messages, strict envelope encoding or decoding, service flags, and handshake
state management.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-p2p-networking-and-sync/06-CONTEXT.md
@.planning/phases/06-p2p-networking-and-sync/06-RESEARCH.md
@packages/open-bitcoin-primitives/src/network.rs
@packages/open-bitcoin-codec/src/network.rs
@packages/open-bitcoin-consensus/src/crypto.rs
@packages/bitcoin-knots/src/protocol.h
@packages/bitcoin-knots/test/functional/test_framework/messages.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the pure-core networking crate into the workspace</name>
  <files>packages/Cargo.toml, packages/BUILD.bazel, BUILD.bazel, packages/README.md, scripts/pure-core-crates.txt, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-core/src/lib.rs, packages/open-bitcoin-network/Cargo.toml, packages/open-bitcoin-network/BUILD.bazel, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>packages/Cargo.toml, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-mempool/Cargo.toml</read_first>
  <action>Create `packages/open-bitcoin-network` as a new pure-core crate, add it to Cargo and Bazel, list it in `scripts/pure-core-crates.txt`, describe it in `packages/README.md`, and re-export it from `open-bitcoin-core`. Keep dependencies limited to first-party pure-core crates only.</action>
  <verify>`cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-network`</verify>
  <acceptance_criteria>
    - `packages/Cargo.toml` includes `open-bitcoin-network`
    - `BUILD.bazel` exposes a public `network` alias
    - `open-bitcoin-core` re-exports `open_bitcoin_network`
  </acceptance_criteria>
  <done>The workspace builds the new crate and downstream crates can import it.</done>
</task>

<task type="auto">
  <name>Task 2: Define message types, protocol constants, and wire envelope handling</name>
  <files>packages/open-bitcoin-network/src/error.rs, packages/open-bitcoin-network/src/lib.rs, packages/open-bitcoin-network/src/message.rs</files>
  <read_first>packages/open-bitcoin-primitives/src/network.rs, packages/open-bitcoin-codec/src/network.rs, packages/open-bitcoin-consensus/src/crypto.rs, packages/bitcoin-knots/src/protocol.h, packages/bitcoin-knots/test/functional/test_framework/messages.py</read_first>
  <action>Implement the typed message subset for Phase 6, service-flag helpers, and strict envelope encode or decode logic with payload checksums. Reuse existing primitives and codec surfaces instead of duplicating low-level Bitcoin serializers.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib wire_message_round_trips_version_and_inventory_payloads`</verify>
  <acceptance_criteria>
    - Message types exist for the Phase 6 command subset
    - Envelope parsing validates command names, payload length, and checksum
    - Empty payload commands encode or decode exactly as empty payloads
  </acceptance_criteria>
  <done>The new crate can encode and decode the first networking payload set through Bitcoin message envelopes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement peer lifecycle and handshake state</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>packages/bitcoin-knots/test/functional/p2p_handshake.py, packages/bitcoin-knots/src/protocol.h, packages/open-bitcoin-primitives/src/network.rs</read_first>
  <action>Implement peer registration, version negotiation, `verack`, `sendheaders`, `wtxidrelay`, ping or pong handling, and explicit peer actions for outbound messages, disconnects, and sync start decisions.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib outbound_handshake_negotiates_verack_sendheaders_and_wtxidrelay`</verify>
  <acceptance_criteria>
    - Peer state tracks lifecycle progress explicitly
    - Handshake negotiation emits the expected follow-up messages
    - Ping or pong logic preserves peer nonce handling
  </acceptance_criteria>
  <done>The pure-core networking crate exposes explicit lifecycle state and the Phase 6 handshake surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- [ ] `cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The pure-core networking crate exists and covers the first wire or handshake surface

</success_criteria>

<output>
After completion, create `.planning/phases/06-p2p-networking-and-sync/06-01-SUMMARY.md`
</output>
