---
phase: 06-p2p-networking-and-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/open-bitcoin-network/src/header_store.rs
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/lib.rs
autonomous: true
requirements:
  - P2P-01
  - P2P-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 06-2026-04-15T00-28-26
generated_at: 2026-04-15T00:28:26Z
must_haves:
  truths:
    - The pure-core networking crate maintains a deterministic best-header view
      and derives block locators from it.
    - Unknown block announcements trigger `getheaders`, and accepted headers
      trigger explicit `getdata` requests for missing blocks.
    - Header and block sync decisions stay pure data transformations rather
      than hidden node-side mutation.
  artifacts:
    - packages/open-bitcoin-network/src/header_store.rs
    - packages/open-bitcoin-network/src/peer.rs
  key_links:
    - Peer actions can express block-download requests after header sync.
---

<objective>
Implement deterministic header sync and block-download orchestration in the
pure-core networking crate.

Purpose: establish the header tree, locator-building logic, and block-request
behavior that fulfills the roadmap's sync requirements before node-side data
application begins.
Output: explicit header-store and peer-manager logic for `getheaders`,
`headers`, block announcements, and `getdata` follow-up requests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p2p-networking-and-sync/06-CONTEXT.md
@.planning/phases/06-p2p-networking-and-sync/06-RESEARCH.md
@.planning/phases/06-p2p-networking-and-sync/06-01-PLAN.md
@packages/open-bitcoin-chainstate/src/types.rs
@packages/open-bitcoin-consensus/src/crypto.rs
@packages/bitcoin-knots/src/headerssync.h
@packages/bitcoin-knots/src/headerssync.cpp
@packages/bitcoin-knots/src/sync.cpp
@packages/bitcoin-knots/test/functional/p2p_initial_headers_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deterministic header-store for locator and best-chain tracking</name>
  <files>packages/open-bitcoin-network/src/header_store.rs, packages/open-bitcoin-network/src/lib.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/types.rs, packages/bitcoin-knots/src/headerssync.h, packages/bitcoin-knots/src/headerssync.cpp</read_first>
  <action>Implement a pure header store that can seed from the local active chain, track parent relationships and deterministic work, choose the best chain, and derive block locators suitable for `getheaders` queries and `headers` responses.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib header_store_builds_exponential_locators`</verify>
  <acceptance_criteria>
    - Header state can seed from an existing chain view
    - Best-chain selection is explicit and deterministic
    - Locator construction uses a Bitcoin-style stepped walkback
  </acceptance_criteria>
  <done>The networking core can reason about its best known chain without node-owned globals.</done>
</task>

<task type="auto">
  <name>Task 2: Teach peer state to sync headers and request blocks</name>
  <files>packages/open-bitcoin-network/src/peer.rs</files>
  <read_first>packages/bitcoin-knots/src/sync.cpp, packages/bitcoin-knots/test/functional/p2p_initial_headers_sync.py</read_first>
  <action>Implement sync-start behavior after handshake, block-announcement handling that issues `getheaders`, header-batch acceptance, and block-request actions through `getdata` for newly connected headers.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib block_inventory_triggers_getheaders_then_getdata_for_missing_blocks`</verify>
  <acceptance_criteria>
    - Handshake completion can trigger initial `getheaders`
    - Unknown block inventory triggers header-first sync, not direct block fetch
    - Accepted headers produce deterministic block-download requests
  </acceptance_criteria>
  <done>The pure-core networking crate owns the Phase 6 sync-decision surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Header sync produces explicit block requests through peer actions

</success_criteria>

<output>
After completion, create `.planning/phases/06-p2p-networking-and-sync/06-02-SUMMARY.md`
</output>
