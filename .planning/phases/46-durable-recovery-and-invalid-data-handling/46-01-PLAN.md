---
phase: 46
phase_name: Durable Recovery and Invalid Data Handling
phase_lifecycle_id: 46-2026-05-26T17-16-33
lifecycle_mode: yolo
generated_at: 2026-05-26T17:16:33Z
generated_by: gsd-yolo-discuss-plan-execute-commit-and-push
requirements:
  - NODE-02
  - NODE-03
  - NODE-05
status: complete
---

# Phase 46 Plan

## Tasks

- [x] Add explicit durable progress separation.
  - Add downloaded and connected block heights to `SyncProgress`.
  - Track downloaded best-chain height in `SyncRunSummary`.
  - Refresh summary heights from runtime state after reconciliation and peer outcomes.
- [x] Persist peer failure error and recovery guidance.
  - Keep storage recovery metadata as the highest-priority guidance.
  - Derive peer/network guidance from `PeerFailureReason` when the latest peer outcome failed or waited.
  - Persist latest active-run peer error in durable sync state without forcing the run lifecycle to failed.
- [x] Classify invalid block bodies as invalid peer data.
  - Map sync-time chainstate validation failures to `SyncRuntimeError::InvalidData`.
  - Ensure invalid blocks are not saved to the durable block store after failed validation.
- [x] Add deterministic tests.
  - Restart after partial durable block download reports separated downloaded and connected heights.
  - Invalid block body is peer-attributed, uncredited, unsaved, and does not advance chainstate.
  - Active durable state records peer failure error and recovery guidance after invalid data.
- [x] Update operator-facing docs and generated metrics.
  - Explain validated header, downloaded block, connected block, last error, and recovery action fields.
  - Include repo-local Cargo and Bazel command examples for status checks.
- [x] Verify.
  - Run focused Rust tests for sync/status/RPC/CLI fixture changes.
  - Run `cargo fmt --manifest-path packages/Cargo.toml --all`.
  - Run `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`.
  - Run `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`.
  - Run `cargo test --manifest-path packages/Cargo.toml --all-features`.
  - Run `bash scripts/verify.sh`.
  - Run GSD lifecycle validation for Phase 46.

## Elegance Pass

The final implementation keeps status changes additive, preserves `block_height` as a compatibility alias, reuses the existing durable block store/header reconciliation path, and adds small shared helpers for summary refresh and peer recovery guidance instead of introducing a new queue or status DTO.
