---
phase: 116-operator-evidence-metrics-logs-and-support-boundary
plan: 03
type: execute
wave: 2
depends_on:
  - "116-01"
files_modified:
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/metrics/tests.rs
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/tests.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - docs/parity/source-breadcrumbs.json
requirements:
  - OBS-03
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 116-2026-07-06T03-46-36
generated_at: 2026-07-06T04:30:00Z
user_setup: []
must_haves:
  truths:
    - "Metrics expose fixed low-cardinality block/compact outcome counters derived from shared status — not log parsing."
    - "Structured logs use stable source name and fixed cause/outcome/label fields for block-relay events."
    - "Runtime adapters increment shared evidence counters at effect sites; metrics/log writers read projected status."
    - "No dynamic string labels, peer ids, block hashes, or transaction payloads appear in metrics or log records."
  artifacts:
    - path: "packages/open-bitcoin-node/src/metrics.rs"
      provides: "block_relay_metric_samples and MetricKind block/compact counters"
      contains: "BlockServedCount|compact_announced"
    - path: "packages/open-bitcoin-node/src/logging.rs"
      provides: "BLOCK_RELAY_LOG_SOURCE and block_relay_log_record"
      contains: "block_relay"
    - path: "packages/open-bitcoin-node/src/metrics/tests.rs"
      provides: "Low-cardinality and projection tests for block-relay metrics"
      contains: "block_relay_metric"
  key_links:
    - from: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      to: "packages/open-bitcoin-node/src/metrics.rs"
      via: "block_relay_metric_samples"
      pattern: "block_served_count|compact_announced_count"
    - from: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      to: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      via: "counter increments at effect sites"
      pattern: "increment|count"
    - from: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      to: "packages/open-bitcoin-node/src/logging.rs"
      via: "block_relay_log_record reads projected counters"
      pattern: "block_relay_log_record"
---

# Phase 116 Plan 03: Block-Relay Metrics And Structured Logs

<objective>
Add fixed low-cardinality metrics and sanitized structured logs for block-serving and compact-relay outcomes, wired to shared evidence counters at runtime effect sites.

Purpose: Operators need bounded telemetry matching Phase 105 relay patterns (D-11, D-12, D-13). This plan mirrors Phase 105-02 metrics/logs tasks only — no CLI or dashboard work.

Output: `MetricKind` block/compact variants, `block_relay_metric_samples`, `BLOCK_RELAY_LOG_SOURCE`, `block_relay_log_record`, runtime increment hooks, focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-CONTEXT.md
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-RESEARCH.md
@.planning/phases/105-operator-rpc-metrics-logs-and-support-evidence/105-02-PLAN.md

<interfaces>
From Phase 105 metrics pattern (`packages/open-bitcoin-node/src/metrics.rs`):

```rust
pub fn relay_metric_samples(
    relay: &RelayEvidenceStatus,
    timestamp_unix_seconds: u64,
) -> Vec<MetricSample> { /* fixed MetricKind mapping */ }
```

From Phase 105 logging pattern:

```rust
pub const RELAY_MEMPOOL_LOG_SOURCE: &str = "relay_mempool";
pub fn relay_mempool_log_record(/* ... */) -> /* sanitized record */;
```

Phase 110–115 label strings to reuse (D-11): `block_serving_eligible`, `block_serving_suppressed`, `compact_announced`, `compact_reconstruction_failed`, `compact_download_timeout`, `compact_download_peer_disconnect`, etc. (see 116-RESEARCH.md Label Inventory).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed block-relay MetricKind counters and projection</name>
  <files>
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/metrics/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: `block_relay_metric_samples` emits all OBS-03 counter kinds from populated status.
    - Test: Metric names are fixed and end in `_count` — no dynamic labels or peer/block dimensions.
    - Test: Sample output absent: endpoints, txids, block hashes, permission strings, reject reasons.
  </behavior>
  <action>
    Add `MetricKind` variants and `block_relay_metric_samples()` mirroring `relay_metric_samples()` (D-11, D-12). Fixed counter kinds (names at planner discretion, must cover):

    - `block_served_count` (full block served)
    - `block_serving_suppressed_count`
    - `compact_announced_count`
    - `compact_reconstructed_count`
    - `compact_missing_tx_requested_count`
    - `compact_fallback_count`
    - `compact_malformed_count`
    - `compact_timeout_count`
    - `compact_cleanup_count`

    Map from `BlockRelayEvidenceStatus` counter fields — do not parse logs. No peer ids, endpoints, block hashes, or dynamic labels in metric names or dimensions (D-12).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay_metric -- --nocapture</automated>
  </verify>
  <done>Block-relay metrics project fixed `_count` samples from shared status; tests prove low cardinality and sensitive-string absence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sanitized block-relay structured log records</name>
  <files>
    packages/open-bitcoin-node/src/logging.rs
    packages/open-bitcoin-node/src/logging/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: `block_relay_log_record` emits stable source `block_relay` with fixed `cause`/`outcome`/`label` fields.
    - Test: Log records reject or omit: raw transaction data, block hashes, peer ids, socket addresses, permission strings, credentials, cookies, secrets, suspicious hex, dynamic labels.
    - Test: Labels reuse Phase 110–115 fixed strings where applicable (e.g., `compact_announced`, `compact_download_timeout`).
  </behavior>
  <action>
    Add `BLOCK_RELAY_LOG_SOURCE` (stable source name, e.g., `"block_relay"`) and `block_relay_log_record()` mirroring `relay_mempool_log_record()` (D-12). Log only fixed outcome names, typed states, and counts from projected status — not raw wire payloads.

    Reuse existing Phase 110–115 `as_str()` label strings for `label`/`cause` fields. Reject the full sensitive-material set in tests: tx hex, block hashes, endpoints, peer identifiers, permission strings, credentials, dynamic metric labels (D-12).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay_log -- --nocapture</automated>
  </verify>
  <done>Structured block-relay logs use fixed fields and Phase 110–115 labels; tests cover sensitive-material rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire runtime counter increments and metrics/log readers</name>
  <files>
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/metrics.rs
    packages/open-bitcoin-node/src/logging.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: Effect-site increments update shared evidence counters consumed by `block_relay_metric_samples` and `block_relay_log_record`.
    - Test: Metrics and log writers read projected `BlockRelayEvidenceStatus` — no log text parsing (D-13).
    - Test: Counter increments at serve, suppress, announce, reconstruct, missing-tx request, fallback, malformed, timeout, cleanup paths use fixed label vocabulary.
  </behavior>
  <action>
    Complete runtime increment hooks at block/compact effect sites started in 116-01 (D-13): inventory serve, compact announce, reconstruction, getblocktxn schedule, fallback, cleanup. Ensure metrics projection and log record builders read `block_relay_evidence_status()` output rather than parsing log text or inventing local counts.

    Align increment labels with Phase 110–115 enums; add `as_str()` helpers where missing (e.g., `CompactRelayNegotiationReason`) per RESEARCH.md discretion note A3.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay -- --nocapture</automated>
  </verify>
  <done>Runtime increments feed shared counters; metrics and logs consume projected status with effect-site tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Effect sites → counters | Internal wire data must not leak into metric/log dimensions |
| Metrics/logs → retention | Fixed cardinality only — no operator-supplied strings as labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03-01 | Denial of service | Dynamic metric labels | mitigate | Fixed `MetricKind` enum only (D-12); tests reject dynamic strings |
| T-116-03-02 | Information disclosure | Structured logs | mitigate | Counts and fixed labels only; full sensitive-material test matrix |
| T-116-03-03 | Tampering | Log-scraping metrics | mitigate | Increment at effect sites; readers use projected status (D-13) |
</threat_model>

<verification>
```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node metrics -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node logging -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay -- --nocapture
```
</verification>

<success_criteria>
Plan 116-03 is complete when block-relay metrics and structured logs use fixed labels, runtime increments feed shared counters, and tests prove OBS-03 low-cardinality and sanitization requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-03-SUMMARY.md`
</output>
