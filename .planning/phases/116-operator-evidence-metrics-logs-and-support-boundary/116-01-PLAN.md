---
phase: 116-operator-evidence-metrics-logs-and-support-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status/block_serving.rs
  - packages/open-bitcoin-node/src/status/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - docs/parity/source-breadcrumbs.json
requirements:
  - OBS-01
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 116-2026-07-06T03-46-36
generated_at: 2026-07-06T04:30:00Z
user_setup: []
must_haves:
  truths:
    - "Block-serving and compact-relay evidence share one typed contract consumed by all downstream operator surfaces."
    - "Activation uses FieldAvailability with explicit unavailable reasons until runtime projects evidence; aggregate counters default to zeroed available shapes where safe."
    - "In-flight compact download evidence exposes only aggregate counts — never block hashes, tx indexes, or peer ids."
    - "Open Bitcoin-specific RPC exposes block_relay evidence without changing baseline-compatible RPC method shapes."
  artifacts:
    - path: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      provides: "BlockRelayEvidenceStatus composing block serving plus compact-relay counter groups"
      contains: "BlockRelayEvidenceStatus"
    - path: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      provides: "ManagedPeerNetwork runtime projection for block-relay evidence"
      contains: "block_relay_evidence_status"
    - path: "packages/open-bitcoin-rpc/src/method/node.rs"
      provides: "OpenBitcoinNetworkStatusResponse.block_relay field"
      contains: "block_relay"
    - path: "packages/open-bitcoin-rpc/src/dispatch/tests.rs"
      provides: "RPC dispatch coverage for block-relay evidence projection"
      contains: "open_bitcoin_network_status"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/block_relay_evidence.rs"
      to: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      via: "runtime counter aggregation"
      pattern: "block_relay_evidence_status"
    - from: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      to: "packages/open-bitcoin-rpc/src/method/node.rs"
      via: "OpenBitcoinNetworkStatusResponse serialization"
      pattern: "block_relay"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      via: "ManagedRpcContext accessor"
      pattern: "block_relay_evidence_status"
---

# Phase 116 Plan 01: Shared Block-Relay Status Contract And RPC Projection

<objective>
Define `BlockRelayEvidenceStatus` as the single typed contract for block-serving and compact-relay operator evidence, wire runtime projection from `ManagedPeerNetwork`, and expose it through Open Bitcoin-specific RPC without changing baseline-compatible RPC shapes.

Purpose: Phases 110–115 produce block/compact policy outcomes but no operator surface consumes them yet. This plan creates the shared source of truth first so CLI, dashboard, metrics, logs, and support cannot drift (D-01, D-02, D-05, D-06, D-07).

Output: `BlockRelayEvidenceStatus`, runtime projection helper, RPC `block_relay` field, dispatch tests, parity breadcrumbs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-CONTEXT.md
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-RESEARCH.md
@.planning/phases/105-operator-rpc-metrics-logs-and-support-evidence/105-01-PLAN.md

<interfaces>
From `packages/open-bitcoin-node/src/status/block_serving.rs`:

```rust
pub struct BlockServingEvidenceStatus {
    pub activation: FieldAvailability<BlockServingActivationEvidence>,
    pub eligibility: FieldAvailability<BlockServingEligibilityCounters>,
    pub status: FieldAvailability<BlockServingStatusCounters>,
}
```

From `packages/open-bitcoin-rpc/src/method/node.rs` (extend similarly to Phase 105 `relay`):

```rust
pub struct OpenBitcoinNetworkStatusResponse {
    pub inbound: FieldAvailability<InboundPeerServingStatus>,
    pub relay: RelayEvidenceStatus,
    pub metrics: MetricsStatus,
    // Phase 116: pub block_relay: BlockRelayEvidenceStatus,
}
```

From `packages/open-bitcoin-rpc/src/context/network.rs` (mirror `relay_evidence_status`):

```rust
pub fn relay_evidence_status(&self) -> RelayEvidenceStatus {
    self.network.relay_evidence_status()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define BlockRelayEvidenceStatus shared contract</name>
  <files>
    packages/open-bitcoin-node/src/status/block_relay_evidence.rs
    packages/open-bitcoin-node/src/status/block_serving.rs
    packages/open-bitcoin-node/src/status.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: `BlockRelayEvidenceStatus::default_unavailable()` reports activation unavailable with `BLOCK_SERVING_EVIDENCE_UNAVAILABLE_REASON` (per D-03).
    - Test: Composed status includes `BlockServingEvidenceStatus` plus compact counter groups: negotiation, announcement, reconstruction, missing_transaction, fallback, in_flight (aggregate-only: `in_flight_count`, `getblocktxn_in_flight_count`, `peers_with_in_flight_count` per D-04), cleanup.
    - Test: Serialized JSON contains no peer endpoints, block hashes, tx indexes, permission strings, credentials, or dynamic labels.
    - Test: Eligibility and status counter structs default to zeroed available aggregates when activation is unavailable.
  </behavior>
  <action>
    Create `block_relay_evidence.rs` under `packages/open-bitcoin-node/src/status/` per D-01. Compose existing `BlockServingEvidenceStatus` with new compact-relay counter structs grouped by: negotiation, announcement, reconstruction, missing-transaction, fallback, in-flight aggregates, cleanup. Use `FieldAvailability` semantics from Phase 110/72 (D-03) — activation unavailable until runtime projection; zeroed aggregate counters where safe.

    Export types from `status.rs`. Add parity breadcrumbs for any new Rust source files (D-20). Do not add peer ids, block hashes, tx indexes, raw payloads, or dynamic string labels to the contract.

    Prefer reusing Phase 110–115 label vocabulary in counter field names (e.g., `compact_announced_count`, `compact_reconstructed_count`, `compact_fallback_count`) aligned with RESEARCH.md OBS-03 mapping.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay_evidence -- --nocapture</automated>
  </verify>
  <done>Shared contract exists with composed block-serving and compact-relay counter groups, FieldAvailability semantics, aggregate-only in-flight fields, and tests proving default-unavailable sanitized behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project runtime block-relay evidence from ManagedPeerNetwork</name>
  <files>
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-node/src/network.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: `block_relay_evidence_status()` returns deterministic fixed counters after simulated block serve, compact announce, reconstruction, fallback, and cleanup effects.
    - Test: Before first projection, activation is unavailable with stable reason; counters remain zeroed aggregates.
    - Test: In-flight evidence exposes only the three aggregate count fields — no block hash or peer id strings in output.
  </behavior>
  <action>
    Add `ManagedPeerNetwork::block_relay_evidence_status()` mirroring `relay_evidence_status()` (D-02). Create `network/block_relay_evidence.rs` with counter storage on managed network state. Increment counters at existing effect sites: inventory full-block serve, compact announce, reconstruction success/failure, getblocktxn schedule, fallback, cleanup (D-13 hook points — minimal increments here; full metrics wiring lands in 116-03).

    Collapse internal peer/block identifiers to counts before crossing into the shared contract. Map unsupported behavior to explicit unavailable states with stable non-sensitive reasons.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay_evidence_status -- --nocapture</automated>
  </verify>
  <done>Managed network projects live block-relay evidence with deterministic counters and aggregate-only in-flight fields; tests cover projection and unavailable-default behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose block_relay through Open Bitcoin-specific RPC</name>
  <files>
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/method/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: `open_bitcoin_network_status()` includes `block_relay` serialized from managed context on live runtime.
    - Test: When runtime unavailable, RPC returns same default-unavailable contract as CLI offline snapshot (D-06).
    - Test: `getnetworkinfo`, `getmempoolinfo`, `sendrawtransaction` response shapes unchanged (D-07).
  </behavior>
  <action>
    Add `block_relay: BlockRelayEvidenceStatus` to `OpenBitcoinNetworkStatusResponse` (D-05). Add `ManagedRpcContext::block_relay_evidence_status()` accessor. Wire field in `open_bitcoin_network_status()` dispatch — thin projection only, no dispatch-layer label translation.

    Preserve baseline-compatible RPC methods unchanged. Open Bitcoin-specific `openbitcoinnetworkstatus` is the truth surface for block/compact evidence.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture</automated>
  </verify>
  <done>RPC exposes shared block_relay evidence; baseline RPC compatibility tests pass; offline/unavailable path matches CLI default contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Managed network → status contract | Internal peer/block state must collapse to aggregate counts before serialization |
| Status contract → RPC JSON | Untrusted consumers (CLI, support) receive only allowlisted fixed fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01-01 | Information disclosure | `BlockRelayEvidenceStatus` | mitigate | Aggregate-only in-flight fields (D-04); no block hashes, peer ids, or tx payloads in contract |
| T-116-01-02 | Spoofing | Baseline RPC methods | mitigate | Keep block/compact evidence on `openbitcoinnetworkstatus` only (D-07); dispatch tests guard shape |
| T-116-01-03 | Tampering | Downstream surfaces | mitigate | Single shared contract (D-02); downstream plans consume RPC/status fields only |
</threat_model>

<verification>
```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node block_relay -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc getnetworkinfo -- --nocapture
bun run scripts/check-parity-breadcrumbs.ts
```
</verification>

<success_criteria>
Plan 116-01 is complete when `BlockRelayEvidenceStatus` exists, managed network projects it, RPC exposes `block_relay`, baseline RPC shapes are unchanged, and focused node plus RPC tests prove truthful sanitized block-relay evidence per OBS-01.
</success_criteria>

<output>
After completion, create `.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-01-SUMMARY.md`
</output>
