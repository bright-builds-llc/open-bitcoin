---
phase: 116-operator-evidence-metrics-logs-and-support-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - "116-01"
files_modified:
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
  - docs/parity/source-breadcrumbs.json
requirements:
  - OBS-02
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 116-2026-07-06T03-46-36
generated_at: 2026-07-06T04:30:00Z
user_setup: []
must_haves:
  truths:
    - "CLI human and JSON status render block-relay evidence from the shared contract — not local heuristics."
    - "Dashboard model rows mirror CLI JSON fields for activation, eligibility, compact negotiation, reconstruction/fallback, in-flight, and cleanup."
    - "Unavailable activation renders explicit reason text; zeroed counters still display where safe."
    - "No raw peer addresses, permission tokens, credentials, transaction hex, or compact block payloads appear in CLI or dashboard output."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs"
      provides: "Human and JSON block-relay status rendering"
      contains: "Block relay evidence"
    - path: "packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs"
      provides: "Terminal dashboard block-relay rows"
      contains: "block_relay"
    - path: "packages/open-bitcoin-cli/src/operator/status/tests.rs"
      provides: "CLI block-relay render and redaction coverage"
      contains: "block_relay"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/method/node.rs"
      to: "packages/open-bitcoin-cli/src/operator/status.rs"
      via: "collect_open_bitcoin_network_status maps block_relay"
      pattern: "block_relay"
    - from: "packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs"
      to: "packages/open-bitcoin-node/src/status/block_relay_evidence.rs"
      via: "shared contract fields only"
      pattern: "BlockRelayEvidenceStatus"
    - from: "packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs"
      to: "packages/open-bitcoin-cli/src/operator/status.rs"
      via: "same snapshot block_relay field"
      pattern: "block_relay"
---

# Phase 116 Plan 02: CLI And Dashboard Block-Relay Rendering

<objective>
Render block-serving and compact-relay evidence through operator CLI (human and JSON) and terminal dashboard from the Phase 116-01 shared contract without leaking sensitive material.

Purpose: Operators need the same block/compact truth on CLI and dashboard that RPC exposes (D-08, D-09, D-10). This plan mirrors Phase 105 relay render modules only — no metrics or logs here.

Output: `render/block_relay.rs`, `dashboard/model/block_relay.rs`, collection wiring, focused CLI tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-CONTEXT.md
@.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-RESEARCH.md
@.planning/phases/105-operator-rpc-metrics-logs-and-support-evidence/105-02-PLAN.md
@packages/open-bitcoin-cli/src/operator/status/render/relay.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs

<interfaces>
From Phase 116-01 (executor must read 116-01-SUMMARY if field names differ):

```rust
pub struct BlockRelayEvidenceStatus {
    pub block_serving: BlockServingEvidenceStatus,
    // compact counter groups: negotiation, announcement, reconstruction,
    // missing_transaction, fallback, in_flight aggregates, cleanup
}
```

From Phase 105 CLI pattern (`packages/open-bitcoin-cli/src/operator/status/render/relay.rs`):
- Human section header: `Relay evidence`
- Unavailable/deferred copy: `Unavailable: {reason}`
- Fixed counter lines from shared contract only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collect block_relay in operator status snapshot</name>
  <files>
    packages/open-bitcoin-cli/src/operator/status.rs
    packages/open-bitcoin-cli/src/operator/status/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: Status collection maps RPC `block_relay` into operator snapshot when daemon responds.
    - Test: On RPC failure, offline fallback uses `BlockRelayEvidenceStatus::default_unavailable()` matching RPC unavailable path (D-06).
    - Test: Snapshot JSON includes block_relay with same shape as RPC response — no CLI-only schema.
  </behavior>
  <action>
    Extend `collect_open_bitcoin_network_status` (or equivalent) to carry `block_relay` from `OpenBitcoinNetworkStatusResponse`. On RPC failure, use the same default-unavailable contract as managed runtime offline snapshots (D-02, D-06). Do not re-derive block/compact truth from local heuristics or config flags alone.

    Add parity breadcrumbs for touched files (D-20).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator_status -- --nocapture</automated>
  </verify>
  <done>Operator status snapshot carries block_relay from RPC or default-unavailable fallback with tests proving shape parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render block-relay human and JSON status lines</name>
  <files>
    packages/open-bitcoin-cli/src/operator/status/render.rs
    packages/open-bitcoin-cli/src/operator/status/render/block_relay.rs
    packages/open-bitcoin-cli/src/operator/status/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: Human output includes section `Block relay evidence` with activation, eligibility/status summaries, compact negotiation, reconstruction/fallback, in-flight aggregates, cleanup counters.
    - Test: Unavailable activation renders `Unavailable: block serving evidence unavailable` (or contract reason) — not omitted section.
    - Test: Output absent: peer endpoints, permission strings, cookies, credentials, transaction hex, cmpctblock payloads, block hashes, dynamic labels (D-10).
  </behavior>
  <action>
    Create `render/block_relay.rs` mirroring `render/relay.rs` layout (D-08). Wire into `render.rs`. Human lines use concise copy matching transaction relay evidence style:

    - `Block relay evidence`
    - Activation: block serving / compact relay enabled flags when available
    - Eligibility and status counter summaries
    - Compact negotiation, reconstruction, fallback, in-flight, cleanup counter groups
    - `Unavailable: {reason}` when activation unavailable

    JSON mode exposes the shared serialized shape unchanged. Never print raw peer addresses, permission tokens, credentials, transaction hex, or compact block payloads (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli block_relay -- --nocapture</automated>
  </verify>
  <done>CLI human and JSON render block-relay evidence from shared contract; tests assert sensitive needles absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dashboard block-relay model rows</name>
  <files>
    packages/open-bitcoin-cli/src/operator/dashboard/model.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/block_relay.rs
    packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Test: Dashboard sections surface block-relay rows from shared snapshot fields (D-09).
    - Test: Unavailable activation renders explicit unavailable row — not hidden section.
    - Test: Dashboard output absent: endpoints, peer ids, permission strings, credentials, tx hex, block hashes.
  </behavior>
  <action>
    Create `dashboard/model/block_relay.rs` mirroring `dashboard/model/relay.rs`. Add compact scan-friendly rows for activation, eligibility, status, compact negotiation, reconstruction/fallback, in-flight aggregates, cleanup. Use same shared contract fields as CLI JSON (D-09). Treat unavailable activation as first-class rendered state.

    Do not display peer endpoints, transaction identifiers, permission strings, credentials, or free-form labels (D-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_model -- --nocapture</automated>
  </verify>
  <done>Dashboard model renders block-relay rows from shared contract with unavailable state and sensitive-value absence tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC JSON → CLI render | CLI must not enrich status with sensitive local data |
| Terminal output → operator | Human/dashboard output is support-adjacent; treat as public-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-02-01 | Information disclosure | CLI human render | mitigate | Fixed counter fields only; forbid endpoints, payloads, hashes (D-10) |
| T-116-02-02 | Spoofing | CLI offline fallback | mitigate | Same default-unavailable contract as RPC (D-06); tests compare shapes |
| T-116-02-03 | Repudiation | Dashboard drift from CLI | mitigate | Both consume same snapshot `block_relay` field (D-02); cross-surface tests in 116-04 |
</threat_model>

<verification>
```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator_status -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli dashboard_model -- --nocapture
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli block_relay -- --nocapture
```
</verification>

<success_criteria>
Plan 116-02 is complete when CLI human/JSON and dashboard render block-relay evidence from the shared contract, unavailable states are explicit, and tests prove OBS-02 sensitive-material absence.
</success_criteria>

<output>
After completion, create `.planning/phases/116-operator-evidence-metrics-logs-and-support-boundary/116-02-SUMMARY.md`
</output>
