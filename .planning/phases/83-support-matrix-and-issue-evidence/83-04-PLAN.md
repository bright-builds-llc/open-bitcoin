---
phase: 83-support-matrix-and-issue-evidence
plan: 04
type: execute
wave: 3
depends_on: ["83-01", "83-02", "83-03"]
files_modified:
  - scripts/check-phase83-support-matrix-issue-evidence.ts
  - scripts/check-phase83-support-matrix-issue-evidence.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
autonomous: true
requirements: [SUP-01, SUP-02, SUP-03, SUP-04]
user_setup: []
must_haves:
  truths:
    - "Default verification validates the support matrix, issue checklist, residual-risk rows, canonical links, and exact support terms."
    - "The checker is deterministic, short-running, public-network-free, real-service-manager-free, multi-day-free, and upload-free."
    - "The checker does not implement the Phase 88 broad all-doc production-claim scanner."
  artifacts:
    - path: "scripts/check-phase83-support-matrix-issue-evidence.ts"
      provides: "Narrow Phase 83 deterministic checker"
      contains: "v1-8-support-matrix-issue-evidence"
    - path: "scripts/check-phase83-support-matrix-issue-evidence.test.ts"
      provides: "Fixture tests for Phase 83 checker"
      contains: "fails_when"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring after Phase 82 checker"
      contains: "check Phase 83 support matrix issue evidence"
    - path: "docs/metrics/lines-of-code.md"
      provides: "Tracked generated LOC freshness after verifier/checker changes"
      contains: "Total"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase83-support-matrix-issue-evidence.ts"
      via: "run_step command"
      pattern: "bun run scripts/check-phase83-support-matrix-issue-evidence.ts"
    - from: "scripts/check-phase83-support-matrix-issue-evidence.ts"
      to: "docs/parity/support-matrix.md"
      via: "targeted file list"
      pattern: "docs/parity/support-matrix.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 83-2026-06-21T16-02-39
generated_at: 2026-06-21T16:30:00Z
---

<objective>
Add the narrow deterministic Phase 83 checker and wire it into repo-native verification.

Purpose: Phase 83 needs guardrails against support-term drift, incomplete issue evidence, missing residual-risk rows, missing canonical links, and parity-root drift, while leaving broad production-claim scanning to Phase 88.
Output: Bun checker, fixture test, `scripts/verify.sh` wiring, and LOC artifact refresh if verification regenerates it.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-support-matrix-and-issue-evidence/83-CONTEXT.md
@.planning/phases/83-support-matrix-and-issue-evidence/83-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@docs/parity/support-matrix.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@docs/parity/release-readiness.md
@docs/parity/deviations-and-unknowns.md
@docs/operator/runtime-guide.md
@README.md
@docs/parity/catalog/operator-runtime-release-hardening.md
@docs/parity/catalog/p2p.md
@docs/parity/catalog/chainstate.md
@docs/parity/catalog/wallet.md
@docs/parity/catalog/drop-in-audit-and-migration.md
@scripts/check-phase82-production-claim-boundary.ts
@scripts/check-phase82-production-claim-boundary.test.ts
@scripts/verify.sh

<interfaces>
Checker constants to implement:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE83_REPO_ROOT";
const SURFACE_ID = "v1-8-support-matrix-issue-evidence";
const PHASE83_REQUIREMENTS = ["SUP-01", "SUP-02", "SUP-03", "SUP-04"] as const;
const SUPPORT_TERMS = ["supported", "preview", "opt-in UAT", "unsupported", "deferred"] as const;
const FORBIDDEN_MATURITY_LABELS = ["best-effort", "beta", "production-grade", "production-ish", "partial production", "community-supported", "GA", "certified", "validated", "fully supported"] as const;
const MATRIX_COLUMNS = [
  "Environment family",
  "Support term",
  "Evidence basis",
  "Default verification",
  "Opt-in UAT / manual validation",
  "Residual risk",
  "Next gate",
] as const;
```

Verifier commands to add after Phase 82:
```bash
bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts
bun run scripts/check-phase83-support-matrix-issue-evidence.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fixture tests for support-matrix checker</name>
  <files>scripts/check-phase83-support-matrix-issue-evidence.test.ts</files>
  <read_first>
    - scripts/check-phase83-support-matrix-issue-evidence.test.ts
    - scripts/check-phase82-production-claim-boundary.test.ts
    - docs/parity/support-matrix.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - scripts/verify.sh
    - .planning/phases/83-support-matrix-and-issue-evidence/83-CONTEXT.md
    - .planning/phases/83-support-matrix-and-issue-evidence/83-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: A complete fixture containing the Phase 83 support matrix, parity roots, pointer docs, and executable verifier commands returns no failures.
    - Test 2: Replacing support term `supported` with a forbidden maturity label such as `best-effort`, `beta`, `partial production`, or `community-supported` fails with a message containing `unsupported support term`.
    - Test 3: Removing matrix column `Opt-in UAT / manual validation` fails with a message containing `support matrix columns`.
    - Test 4: Removing environment family `automatic support-bundle upload`, `inbound serving`, `Windows service integration`, or `broad production-node readiness` fails with a message containing `missing environment family`.
    - Test 5: Removing `Unavailable: <reason>` or adding forbidden evidence `rpcpassword` as requested support evidence fails with a message containing `issue evidence`.
    - Test 6: Removing `dashboard pseudoterminal/raw-input repaint and input behavior` fails with a message containing `residual risk`.
    - Test 7: Removing SUP-04 or `docs/parity/support-matrix.md` from `docs/parity/index.json` fails with a message containing `parity root`.
    - Test 8: Putting Phase 83 verifier commands only inside the `VERIFY_COMMAND_ORDER` heredoc fails with a message containing `verifier-order`.
  </behavior>
  <action>Create `scripts/check-phase83-support-matrix-issue-evidence.test.ts` by following the Phase 82 fixture pattern: import `afterEach`, `expect`, and `test` from `bun:test`; create temp roots with `mkdtemp`; clean them in `afterEach`; create minimal fixture files for README, runtime guide, support matrix, parity README, release readiness, deviations, checklist, index JSON, catalog files, and `scripts/verify.sh`; expose mutation helpers for omission, replacement, append, custom parity index text, and custom verify script text. Include explicit Arrange, Act, Assert comments in each test per Bright Builds testing standards. The tests must import `checkPhase83SupportMatrixIssueEvidence` from `./check-phase83-support-matrix-issue-evidence`. Keep tests focused on one checker behavior each. Do not add network calls, service-manager calls, long sleeps, disk-heavy fixtures, raw datadir fixtures, or upload behavior, per D-14.</action>
  <acceptance_criteria>
    - `scripts/check-phase83-support-matrix-issue-evidence.test.ts` exists.
    - The test file imports `checkPhase83SupportMatrixIssueEvidence`.
    - The test file contains `OPEN_BITCOIN_PHASE83_REPO_ROOT`, `v1-8-support-matrix-issue-evidence`, `SUP-01`, `SUP-02`, `SUP-03`, and `SUP-04`.
    - The test file contains forbidden maturity label fixtures for `best-effort`, `beta`, `partial production`, and `community-supported`.
    - The test file contains deferred-surface fixtures for `inbound serving`, `address relay`, `block serving`, `transaction relay`, `compact block relay`, `Windows service integration`, and `broad production-node readiness`.
    - The test file contains tests named `passes_when_phase83_fixture_contains_support_matrix_roots_and_verify_order`, `fails_when_support_matrix_uses_non_phase82_support_term`, `fails_when_support_matrix_required_column_is_missing`, `fails_when_required_environment_family_is_missing`, `fails_when_issue_evidence_redaction_boundary_drifts`, `fails_when_residual_risk_row_is_missing`, `fails_when_parity_roots_omit_requirement_or_support_matrix_evidence`, and `fails_when_phase83_verify_commands_exist_only_in_legacy_heredoc`.
    - Each test contains `// Arrange`, `// Act`, and `// Assert`.
    - The test file does not contain `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, or `automatic upload`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "checkPhase83SupportMatrixIssueEvidence|OPEN_BITCOIN_PHASE83_REPO_ROOT|v1-8-support-matrix-issue-evidence|SUP-01|SUP-02|SUP-03|SUP-04" scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
    <automated>rg -n "best-effort|beta|partial production|community-supported|inbound serving|address relay|block serving|transaction relay|compact block relay|Windows service integration|broad production-node readiness" scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
    <automated>rg -n "passes_when_phase83_fixture_contains_support_matrix_roots_and_verify_order|fails_when_support_matrix_uses_non_phase82_support_term|fails_when_support_matrix_required_column_is_missing|fails_when_required_environment_family_is_missing|fails_when_issue_evidence_redaction_boundary_drifts|fails_when_residual_risk_row_is_missing|fails_when_parity_roots_omit_requirement_or_support_matrix_evidence|fails_when_phase83_verify_commands_exist_only_in_legacy_heredoc" scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
    <automated>rg -n "// Arrange|// Act|// Assert" scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
    <automated>! rg -n "run-live-mainnet-smoke|systemctl|launchctl|sleep 259200|automatic upload" scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
  </verify>
  <done>The checker test file defines focused fixture regressions that will fail until the Phase 83 checker is implemented and pass after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checker and verifier wiring</name>
  <files>scripts/check-phase83-support-matrix-issue-evidence.ts, scripts/verify.sh, docs/metrics/lines-of-code.md</files>
  <read_first>
    - scripts/check-phase83-support-matrix-issue-evidence.ts
    - scripts/check-phase83-support-matrix-issue-evidence.test.ts
    - scripts/check-phase82-production-claim-boundary.ts
    - scripts/check-phase82-production-claim-boundary.test.ts
    - scripts/verify.sh
    - docs/parity/support-matrix.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/README.md
    - docs/parity/release-readiness.md
    - docs/parity/deviations-and-unknowns.md
    - docs/operator/runtime-guide.md
    - README.md
    - docs/parity/catalog/operator-runtime-release-hardening.md
    - docs/parity/catalog/p2p.md
    - docs/parity/catalog/chainstate.md
    - docs/parity/catalog/wallet.md
    - docs/parity/catalog/drop-in-audit-and-migration.md
    - docs/metrics/lines-of-code.md
  </read_first>
  <action>Create `scripts/check-phase83-support-matrix-issue-evidence.ts` as a narrow Bun checker following the Phase 82 script style. Export `checkPhase83SupportMatrixIssueEvidence(maybeRepoRoot = process.env.OPEN_BITCOIN_PHASE83_REPO_ROOT): string[]`, read only the known Phase 83 files listed in `read_first`, and exit non-zero with each failure printed when run as a CLI. Use constants from the `<interfaces>` block, including `FORBIDDEN_MATURITY_LABELS`. Parse the support matrix table under `## Support Matrix` and require exact `MATRIX_COLUMNS`, no blank cells, no support terms outside `SUPPORT_TERMS`, and required environment families from Plan 83-01: `source-built install and repo verification`, `repo-local operator command forms through Cargo and Bazel`, `local deterministic runtime, status, config, RPC, and support-bundle surfaces`, `operator dashboard and shipped operator convenience surfaces`, `public-network mainnet activation`, `storage/datadir resource-bound evidence and recovery diagnosis`, `live storage pressure and long-run resource behavior`, `launchd/systemd service-supervision previews`, `real launchd/systemd service-manager lifecycle`, `migration dry-run`, `migration apply, source service mutation, and source datadir rewrite`, `support bundle and support forensics`, `wallet current non-production slice`, `production-funds wallet use and safety`, `inbound serving`, `address relay`, `block serving`, `transaction relay`, `compact block relay`, `signed packaging or package-manager distribution`, `Windows service integration`, `hosted dashboards and GUI parity`, `automatic support-bundle upload`, `destructive repair`, `public-network default checks, public-network CI, and release-blocking live sync`, and `broad production-node readiness`. Validate `## Issue Evidence Checklist`, `Unavailable: <reason>`, both Cargo and Bazel support-bundle command forms, and `### Do Not Attach` containing wallet private material, raw wallet files, RPC cookies, `rpcpassword`, `rpcauth`, raw datadirs, unredacted logs, raw unbounded logs, full peer tables with sensitive local data, and automatic support-bundle upload. Validate the residual-risk table contains all rows named in Plan 83-01 Task 3. Validate parity roots: top-level `surfaces`, `checklist.surfaces`, and `audit.v1_8_support_matrix_issue_evidence` include `v1-8-support-matrix-issue-evidence`, status `done`, exact requirements `SUP-01` through `SUP-04`, and evidence path `docs/parity/support-matrix.md`. Validate human roots and entrypoint docs contain `support-matrix.md` pointers and do not contain the full support matrix header outside `docs/parity/support-matrix.md`. Validate forbidden near-synonyms `best-effort`, `beta`, `production-grade`, `production-ish`, `partial production`, `community-supported`, `GA`, `certified`, `validated`, and `fully supported` are absent from Phase 83 target docs unless they appear inside test files. Validate `scripts/verify.sh` has executed run_step commands, after the executed Phase 82 checker, for `bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts` and `bun run scripts/check-phase83-support-matrix-issue-evidence.ts`; strip or ignore the `VERIFY_COMMAND_ORDER` heredoc before checking executed order, matching the Phase 82 false-positive fix. Update the visible `VERIFY_COMMAND_ORDER` heredoc and executed `run_step` section in `scripts/verify.sh` by adding the Phase 83 test and checker immediately after Phase 82. Do not scan every repository doc for production claims; that broad scanner is Phase 88 scope per D-13. Do not add public-network access, real service-manager mutation, multi-day sleeps, large disk allocation, source datadir mutation, or upload commands to default verification per D-14. If `bash scripts/verify.sh` regenerates `docs/metrics/lines-of-code.md`, keep that tracked artifact fresh.</action>
  <acceptance_criteria>
    - `scripts/check-phase83-support-matrix-issue-evidence.ts` exists and exports `checkPhase83SupportMatrixIssueEvidence`.
    - The checker contains `OPEN_BITCOIN_PHASE83_REPO_ROOT`, `v1-8-support-matrix-issue-evidence`, `PHASE83_REQUIREMENTS`, `SUPPORT_TERMS`, `FORBIDDEN_MATURITY_LABELS`, and `MATRIX_COLUMNS`.
    - The checker reads only known Phase 83 target files and does not perform an all-doc repository scan.
    - The checker validates exact support terms, forbidden maturity labels, matrix columns, required environment families including `inbound serving`, `address relay`, `block serving`, `transaction relay`, `compact block relay`, `Windows service integration`, and `broad production-node readiness`, issue-evidence checklist, forbidden sensitive evidence, residual-risk rows, parity roots, human roots, entrypoint pointers, and executable verifier order.
    - `scripts/verify.sh` contains `bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts` and `bun run scripts/check-phase83-support-matrix-issue-evidence.ts` in both `VERIFY_COMMAND_ORDER` and executed `run_step` sections after Phase 82.
    - `scripts/verify.sh` does not add public-network, real service-manager, multi-day, large-disk, source-datadir mutation, destructive repair, or upload commands for Phase 83.
    - `bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts`, `bun --check scripts/check-phase83-support-matrix-issue-evidence.ts`, `bun run scripts/check-phase83-support-matrix-issue-evidence.ts`, and `bash scripts/verify.sh` pass.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "export function checkPhase83SupportMatrixIssueEvidence|OPEN_BITCOIN_PHASE83_REPO_ROOT|v1-8-support-matrix-issue-evidence|PHASE83_REQUIREMENTS|SUPPORT_TERMS|FORBIDDEN_MATURITY_LABELS|MATRIX_COLUMNS" scripts/check-phase83-support-matrix-issue-evidence.ts</automated>
    <automated>rg -n "inbound serving|address relay|block serving|transaction relay|compact block relay|Windows service integration|broad production-node readiness|best-effort|beta|partial production|community-supported" scripts/check-phase83-support-matrix-issue-evidence.ts</automated>
    <automated>rg -n "bun test scripts/check-phase83-support-matrix-issue-evidence\\.test\\.ts|bun run scripts/check-phase83-support-matrix-issue-evidence\\.ts|check Phase 83 support matrix issue evidence" scripts/verify.sh</automated>
    <automated>! rg -n "run-live-mainnet-smoke|systemctl|launchctl|sleep 259200|automatic support-bundle upload --|destructive repair --" scripts/check-phase83-support-matrix-issue-evidence.ts scripts/verify.sh</automated>
    <automated>bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts</automated>
    <automated>bun --check scripts/check-phase83-support-matrix-issue-evidence.ts</automated>
    <automated>bun run scripts/check-phase83-support-matrix-issue-evidence.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>The Phase 83 checker and default verifier wiring pass, remain narrow and deterministic, and preserve the Phase 88 boundary for broad production-claim scanning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown/json docs -> checker | Local docs and parity JSON are parsed as input to deterministic validation. |
| checker -> default verifier | Checker runtime becomes part of `bash scripts/verify.sh`; failures block local verification. |
| Phase 83 checker -> Phase 88 scope | Narrow checker must not become a broad all-doc production-claim scanner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04-01 | Tampering | markdown table parsing | mitigate | Parse only the canonical support matrix table, require exact columns, reject blank cells and unknown support terms. |
| T-83-04-02 | Information Disclosure | issue-evidence checklist validation | mitigate | Assert forbidden sensitive evidence language and local-only support-bundle boundaries are present. |
| T-83-04-03 | Denial of Service | `scripts/verify.sh` Phase 83 wiring | mitigate | Add only short Bun test/checker commands; no public-network, real service-manager, multi-day, large-disk, source-datadir mutation, or upload commands. |
| T-83-04-04 | Elevation of Privilege | support-term drift | mitigate | Fail on `best-effort`, `beta`, `production-grade`, `production-ish`, `partial production`, `community-supported`, `GA`, `certified`, `validated`, or `fully supported` in Phase 83 target docs. |
| T-83-04-05 | Tampering | verifier-order detection | mitigate | Strip/ignore `VERIFY_COMMAND_ORDER` heredoc before checking executed `run_step` order, matching the Phase 82 checker fix. |
</threat_model>

<verification>
Run `bun test scripts/check-phase83-support-matrix-issue-evidence.test.ts`, `bun --check scripts/check-phase83-support-matrix-issue-evidence.ts`, `bun run scripts/check-phase83-support-matrix-issue-evidence.ts`, and final `bash scripts/verify.sh`. Preserve any verifier-generated `docs/metrics/lines-of-code.md` update.
</verification>

<success_criteria>
Default verification enforces the Phase 83 support matrix, issue-evidence checklist, residual-risk rows, canonical links, parity roots, exact Phase 82 support terms, forbidden maturity-label checks, and Phase 82 deferred-surface rows including inbound serving, relay sub-surfaces, Windows service integration, and broad production-node readiness while remaining deterministic and narrow. Phase 88 broad claim scanning remains unimplemented in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/83-support-matrix-and-issue-evidence/83-04-SUMMARY.md`.
</output>
