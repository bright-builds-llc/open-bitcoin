---
phase: 143-honest-stored-block-availability
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network/block_serving.rs
  - packages/open-bitcoin-node/src/network/block_serving/tests.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/tests/block_serving.rs
  - packages/open-bitcoin-network/src/block_serving.rs
  - scripts/check-phase111-full-block-serving-request-path.ts
  - scripts/check-phase111-full-block-serving-request-path.test.ts
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - HAVL-02
  - HAVL-03
user_setup: []
must_haves:
  truths:
    - "ManagedBlockServeDecision exposes distinguishable payload_present, index_known, and validated_on_active_chain facts (D-08, D-09, HAVL-03)."
    - "Production fact assembly never injects BlockServingDataAvailability::Pruned; missing payload is Unavailable (D-05, D-07, HAVL-02)."
    - "Available is authorized only by payload_present; index_known and validated_on_active_chain never set data_availability Available (D-01, D-09)."
    - "classify_block_serving_status stays I/O-free; Pruned remains a reserved classifier mapping (D-03, D-06)."
    - "The Phase 111 missing-body test is named phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound and the Phase 111 checker requires that name."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/block_serving.rs
      provides: "BlockServingPresenceFacts on Input and Decision"
      contains: "struct BlockServingPresenceFacts"
    - path: packages/open-bitcoin-node/src/network/inventory.rs
      provides: "Honest payload_present assembly without Pruned"
      contains: "durable_payload_present"
    - path: packages/open-bitcoin-node/src/network/tests/block_serving.rs
      provides: "Flipped missing-payload Unavailable NotFound test"
      contains: "phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound"
  key_links:
    - from: packages/open-bitcoin-node/src/network/inventory.rs
      to: packages/open-bitcoin-network/src/block_serving.rs
      via: "data_availability Available only when payload_present"
      pattern: "BlockServingDataAvailability::Available"
    - from: packages/open-bitcoin-node/src/network/block_serving.rs
      to: packages/open-bitcoin-node/src/network/inventory.rs
      via: "presence facts copied onto ManagedBlockServeDecision"
      pattern: "presence:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 143-2026-09-17T01-47-45
generated_at: 2026-09-17T02:16:12.005Z
---

# Phase 143 Plan 01: Presence Facts and Reserved Pruned

<objective>
Assemble typed `payload_present`, `index_known`, and `validated_on_active_chain` on the shell report seam, stop injecting `Pruned` for missing payload, and keep the classifier I/O-free with `Pruned` reserved.

Purpose: HAVL-02 / HAVL-03 / D-01 / D-03 / D-05 / D-06 / D-07 / D-08 / D-09. The classifier already maps injected `Pruned` to `block_status_pruned`. The lie is the shell match arm that invents `Pruned` for active-non-tip cache misses.

Output: `BlockServingPresenceFacts` on `ManagedBlockServeInput` and `ManagedBlockServeDecision`. Production `data_availability` is `Available` only when `payload_present` is true, otherwise `Unavailable`. Phase 111 missing-body test and checker use the `unavailable_notfound` name. No `has_block` probe yet (Plan 02). No `LookupUnavailable` status rewrite yet (Plan 03). No new operator UI or `getblock` (D-10, D-11).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
@.planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
@packages/open-bitcoin-node/src/network/inventory.rs
@packages/open-bitcoin-node/src/network/block_serving.rs
@packages/open-bitcoin-network/src/block_serving.rs
@packages/open-bitcoin-node/src/network/tests/block_serving.rs
@scripts/check-phase111-full-block-serving-request-path.ts
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/network/block_serving.rs today:
```rust
pub(super) struct ManagedBlockServeInput {
    // ... existing fields ...
    pub data_availability: BlockServingDataAvailability,
    pub suppressed: bool,
}

pub(super) struct ManagedBlockServeDecision {
    pub label: BlockServingOutcomeLabel,
    pub status_label: BlockServingStatusLabel,
    pub eligibility_reason: BlockServingEligibilityReason,
    pub maybe_block: Option<Block>,
    pub missing_inventory: bool,
}
```

Target (D-08, sit facts beside BlockServingStatusFacts — do not add them to the I/O-free classifier input):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(super) struct BlockServingPresenceFacts {
    pub payload_present: bool,
    pub index_known: bool,
    pub validated_on_active_chain: bool,
}
```
Add `pub presence: BlockServingPresenceFacts` to both Input and Decision.

From packages/open-bitcoin-node/src/network/inventory.rs today (dishonest):
```rust
pub(super) fn managed_block_serve_input(
    ...,
    durable_availability: bool,
) -> ManagedBlockServeInput {
    let has_local_data = self.blocks_by_hash.contains_key(&block_hash);
    let data_availability = match (is_active, is_tip, has_local_data || durable_availability) {
        (true, _, true) => BlockServingDataAvailability::Available,
        (true, false, false) => BlockServingDataAvailability::Pruned,
        _ => BlockServingDataAvailability::Unavailable,
    };
}
```

Target (D-01, D-02 name, D-05):
```rust
pub(super) fn managed_block_serve_input(
    ...,
    durable_payload_present: bool,
) -> ManagedBlockServeInput {
    let payload_present = self.blocks_by_hash.contains_key(&block_hash)
        || durable_payload_present;
    let index_known = self.peer_manager.header_store().contains(&block_hash);
    let validated_on_active_chain = maybe_active_index.is_some();
    let data_availability = if payload_present {
        BlockServingDataAvailability::Available
    } else {
        BlockServingDataAvailability::Unavailable
    };
}
```

Do not call `best_block()` or coins tip. Do not move Fjall or `blocks_by_hash` into `open-bitcoin-network`. `gate_inventory_for_durable_serving` may still pass `true` this plan; Plan 02 replaces that override.

From packages/open-bitcoin-network/src/block_serving.rs — keep `Pruned` variants and `as_str()` values `pruned` / `block_status_pruned`. Add rustdoc only. Do not rename the enum. Classifier tests that inject `BlockServingDataAvailability::Pruned` stay.

`node block_serving.rs` is 624 lines. Move the existing `#[cfg(test)] mod tests { ... }` body to `packages/open-bitcoin-node/src/network/block_serving/tests.rs` and leave `#[cfg(test)] mod tests;` in the parent so the production file stays ≤628 after adding `presence`.
</interfaces>
</context>

<feature>
  <name>Typed presence facts and reserved Pruned on the shell report seam</name>
  <files>packages/open-bitcoin-node/src/network/block_serving.rs, packages/open-bitcoin-node/src/network/inventory.rs, packages/open-bitcoin-node/src/network/tests/block_serving.rs</files>
  <behavior>
    Missing cache plus durable_payload_present=false classifies Unavailable, not Pruned. Decision.presence exposes the three bools. index_known true without payload_present is still Unavailable. Classifier remains a pure function of BlockServingStatusFacts.
  </behavior>
  <implementation>
    Add BlockServingPresenceFacts. Rewrite managed_block_serve_input availability. Copy presence onto every ManagedBlockServeDecision. Flip and rename the Phase 111 missing-body test. Update the Phase 111 checker required test name.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing presence-fact and Unavailable-not-Pruned tests</name>
  <files>
    packages/open-bitcoin-node/src/network/tests/block_serving.rs
    packages/open-bitcoin-node/src/network/block_serving/tests.rs
    scripts/check-phase111-full-block-serving-request-path.ts
    scripts/check-phase111-full-block-serving-request-path.test.ts
  </files>
  <read_first>
    .planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
    .planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/tests/block_serving.rs
    scripts/check-phase111-full-block-serving-request-path.ts
    scripts/check-phase111-full-block-serving-request-path.test.ts
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound` replaces `..._returns_pruned_notfound`. Same fixture: connect genesis+child, remove genesis from `blocks_by_hash`, GetData genesis. Outbound is `vec![WireNetworkMessage::NotFound(inventory)]`. After a public or `pub(super)` inspect helper (or by recording the last decision in the existing evidence path), `status_label` equals `BlockServingStatusLabel::Unavailable` and `presence.payload_present` is false. Do not expect `Pruned` or `block_status_pruned` (D-07).
    - Test `managed_serve_input_exposes_presence_facts_without_authorizing_available_from_index`: build a managed network, connect genesis, remove it from `blocks_by_hash`, keep the header in `peer_manager.header_store()`. Call `managed_block_serve_input(peer, request, genesis_hash, false, false)`. Assert `presence.payload_present == false`, `presence.index_known == true`, `presence.validated_on_active_chain == true`, `data_availability == BlockServingDataAvailability::Unavailable` (D-08, D-09).
    - Test `managed_serve_input_cache_hit_sets_payload_present_true`: same network with genesis still in `blocks_by_hash`. `durable_payload_present=false`. Assert `presence.payload_present == true` and `data_availability == Available` (D-01).
    - Test `production_inventory_source_does_not_inject_pruned`: `include_str` of `inventory.rs` must not contain `BlockServingDataAvailability::Pruned` (D-05).
    - Test `classify_block_serving_status_has_no_fjall_or_cache_reads`: `include_str` of `packages/open-bitcoin-network/src/block_serving.rs` must not contain `fjall`, `blocks_by_hash`, or `has_block` (D-03).
    - Keep `packages/open-bitcoin-network/src/block_serving/tests/status_cases.rs` Pruned-injection case. That is the reserved-variant classifier test, not a production emit (D-06).
  </behavior>
  <action>
    RED only. Per D-05, D-07, D-08, D-09, D-15:

    1. Rename `phase111_active_chain_non_tip_missing_local_block_returns_pruned_notfound` in `packages/open-bitcoin-node/src/network/tests/block_serving.rs` to `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound`. Change the `.expect("pruned non-tip getdata")` string to `unavailable non-tip getdata`. Add assertions that the refused decision is Unavailable, not Pruned. If the current test only checks the wire NotFound, add a `pub(super)` last-decision or evidence inspection that is already used by `record_block_serving_evidence`; do not invent a new RPC/status field (D-10).
    2. Add the two presence-fact tests and the two `include_str` guards in `packages/open-bitcoin-node/src/network/tests/block_serving.rs`.
    3. In `scripts/check-phase111-full-block-serving-request-path.ts`, replace the REQUIRED_TESTS entry `phase111_active_chain_non_tip_missing_local_block_returns_pruned_notfound` with `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound`. Keep `block_status_pruned` in REQUIRED_TERMS (reserved vocabulary).
    4. In `scripts/check-phase111-full-block-serving-request-path.test.ts` `nodeTestsText()`, make the same rename. Keep the `block_status_pruned` fixture string in other tests that assert reserved-term presence.
    5. Do not implement `has_block`. Do not change `gate_inventory_for_durable_serving(..., true)` yet. Do not invent `getblock`. Do not edit operator JSON/CLI/dashboard/metrics/logs (D-10, D-11).
    6. Combined RED+GREEN in one hook-passing feat commit is allowed because pre-commit runs `verify.sh`, matching Phase 140–142.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-01-presence-facts-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::tests::block_serving -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/network/tests/block_serving.rs` contains `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound`
    - `packages/open-bitcoin-node/src/network/tests/block_serving.rs` does not contain `phase111_active_chain_non_tip_missing_local_block_returns_pruned_notfound`
    - `packages/open-bitcoin-node/src/network/tests/block_serving.rs` contains `managed_serve_input_exposes_presence_facts_without_authorizing_available_from_index`
    - `packages/open-bitcoin-node/src/network/tests/block_serving.rs` contains `production_inventory_source_does_not_inject_pruned`
    - `scripts/check-phase111-full-block-serving-request-path.ts` contains `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound`
    - `scripts/check-phase111-full-block-serving-request-path.ts` still contains `"block_status_pruned"`
    - New tests are RED on behavior (not missing-type compile errors) before Task 2 unless RED+GREEN are combined
  </acceptance_criteria>
  <done>Failing tests exist for three presence facts, Unavailable-not-Pruned missing payload, I/O-free classifier, and the Phase 111 checker name flip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement presence facts and stop injecting Pruned</name>
  <files>
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/block_serving/tests.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-network/src/block_serving.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-network/src/block_serving.rs
    packages/open-bitcoin-network/src/header_store.rs
    docs/parity/source-breadcrumbs.json
    standards/core/architecture.md
    standards/core/code-shape.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    Same tests as Task 1 pass. managed_block_serve_input never matches Pruned. presence fields are copied onto every Decision constructed by gate_managed_block_request and missing(). enabled_input in moved unit tests sets presence to payload_present=true, index_known=true, validated_on_active_chain=true so existing eligible tests stay green. Pruned rustdoc says reserved for a future prune-mode delete, not that this node is in prune mode.
  </behavior>
  <action>
    GREEN per D-01, D-03, D-05, D-06, D-08, D-09, D-15:

    1. Move the existing inline `#[cfg(test)] mod tests { ... }` from `block_serving.rs` to `packages/open-bitcoin-node/src/network/block_serving/tests.rs`. Parent keeps `#[cfg(test)] mod tests;`. Copy the existing parity breadcrumb block into the new test file. Add the new path to the `node-network-block-serving-adapter` files array in `docs/parity/source-breadcrumbs.json`.
    2. Add `BlockServingPresenceFacts` and `presence` fields exactly as in `<interfaces>`. Update `missing()`, `eligible_decision`, and `enabled_input()` to set `presence`. Copy `input.presence` onto every Decision.
    3. In `inventory.rs`, rename the last parameter to `durable_payload_present`. Compute:
       - `payload_present = blocks_by_hash.contains_key(&block_hash) || durable_payload_present`
       - `index_known = self.peer_manager.header_store().contains(&block_hash)`
       - `validated_on_active_chain = maybe_active_index.is_some()`
       - `data_availability = if payload_present { Available } else { Unavailable }`
       Delete the `(is_active, is_tip, has_local_data || durable_availability)` match, including the `Pruned` arm. Do not use `is_tip` for availability. Do not call coins `best_block()`.
    4. Callers of `managed_block_serve_input` keep their current bools: serve/compact pass `false`; `gate_inventory_for_durable_serving` may still pass `true` this plan (Plan 02 replaces that override per D-02).
    5. On `BlockServingDataAvailability::Pruned` and `BlockServingStatusLabel::Pruned` in `open-bitcoin-network/src/block_serving.rs`, add rustdoc: `Reserved for a future prune-mode delete when prune mode actually deleted files. Missing payload is Unavailable; this variant is not a prune-mode product signal.` Do not change `as_str()` (`pruned` / `block_status_pruned`). Do not add Fjall or cache reads to `classify_block_serving_status`.
    6. Do not add `getblock`. Do not add status/RPC/CLI/dashboard/metrics/log fields. Do not grow `block_serving.rs` or `inventory.rs` past 628 physical lines. No `unwrap()`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-01-presence-facts-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `block_serving.rs` contains `struct BlockServingPresenceFacts`
    - `block_serving.rs` contains `payload_present` and `validated_on_active_chain`
    - `inventory.rs` contains `durable_payload_present`
    - `inventory.rs` does not contain `BlockServingDataAvailability::Pruned`
    - `inventory.rs` does not contain `best_block()`
    - `open-bitcoin-network/src/block_serving.rs` still contains `BlockServingDataAvailability::Pruned => BlockServingStatusLabel::Pruned`
    - `open-bitcoin-network/src/block_serving.rs` rustdoc near `Pruned` contains `Reserved for a future prune-mode delete`
    - `docs/parity/source-breadcrumbs.json` contains `packages/open-bitcoin-node/src/network/block_serving/tests.rs`
    - Task 1 tests pass
    - `wc -l packages/open-bitcoin-node/src/network/block_serving.rs packages/open-bitcoin-node/src/network/inventory.rs` both ≤ 628
  </acceptance_criteria>
  <done>Presence facts are on the report seam, production missing payload is Unavailable, Pruned stays reserved and unused on the inventory path, and the Phase 111 checker accepts the renamed test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell facts → I/O-free classifier | Untrusted header-index / active-chain membership must not become Available |
| Status label → operator evidence | A `Pruned` emit can be read as prune-mode product behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Spoofing | `managed_block_serve_input` | mitigate | Set `data_availability` Available only when `payload_present`; `index_known` and `validated_on_active_chain` are report-only (D-01, D-09) |
| T-143-02 | Tampering | `inventory.rs` availability match | mitigate | Delete the `Pruned` production arm; missing payload is Unavailable (D-05) |
| T-143-03 | Information disclosure | `BlockServingStatusLabel::Pruned` docs | mitigate | Rustdoc states reserved-for-future-prune-delete, not "this node pruned historical blocks" (D-06) |
| T-143-04 | Elevation of privilege | `classify_block_serving_status` | mitigate | Keep the function I/O-free; do not add cache or Fjall reads (D-03) |
</threat_model>

<verification>
`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::` passes. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib block_serving::` still covers injected `Pruned`. `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` passes. No `getblock` method is added. `gate_inventory_for_durable_serving` may still pass `true` until Plan 02.
</verification>

<success_criteria>
The three HAVL-03 facts are distinguishable on `ManagedBlockServeDecision`. Production missing payload is Unavailable, not Pruned. The classifier stays I/O-free. The Phase 111 checker requires the renamed Unavailable test.
</success_criteria>

<output>
After completion, create `.planning/phases/143-honest-stored-block-availability/143-01-SUMMARY.md`
</output>
