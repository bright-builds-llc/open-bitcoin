---
phase: 143-honest-stored-block-availability
plan: "03"
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-node/src/network/block_serving.rs
  - packages/open-bitcoin-node/src/network/block_serving/tests.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
autonomous: true
requirements:
  - HAVL-01
  - HAVL-02
user_setup: []
must_haves:
  truths:
    - "LookupUnavailable rewrites status_label to Unavailable and payload_present to false; complete_block_serve increments unavailable_count, not available_count (D-04, HAVL-01)."
    - "Inventory, durable-gate deny, compact announcement, and compact-txn missing-payload paths refuse with WireNetworkMessage::NotFound or the existing compact suppress path plus Unavailable — never Pruned (D-12, D-05)."
    - "A successful classify followed by a failed byte read does not serve a fabricated Block body (D-04)."
    - "No new wire error type is added (D-12)."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/block_serving.rs
      provides: "LookupUnavailable completion uses Unavailable status_label"
      contains: "BlockServingStatusLabel::Unavailable"
    - path: packages/open-bitcoin-node/src/network/block_serving/tests.rs
      provides: "LookupUnavailable status_label and missing_inventory assertions"
      contains: "lookup_unavailable_completion_reports_unavailable_not_available"
  key_links:
    - from: packages/open-bitcoin-node/src/network/block_serving.rs
      to: packages/open-bitcoin-node/src/network/block_relay_evidence.rs
      via: "complete_block_serve records completion.decision().status_label"
      pattern: "LookupUnavailable"
    - from: packages/open-bitcoin-rpc/src/context/inbound_wire.rs
      to: packages/open-bitcoin-node/src/network/block_serving.rs
      via: "resolve_block_intent still uses WireNetworkMessage::NotFound on load miss"
      pattern: "WireNetworkMessage::NotFound"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 143-2026-09-17T01-47-45
generated_at: 2026-09-17T02:16:12.005Z
---

# Phase 143 Plan 03: Missing-Payload Refuse and LookupUnavailable Honesty

<objective>
Make every missing-payload refuse path report `Unavailable` + existing NotFound machinery, including post-gate read failure.

Purpose: HAVL-01 / HAVL-02 / D-04 / D-12. Plan 01 stopped injecting `Pruned`. Plan 02 probes before the durable gate. This plan fixes the leftover lie: `ManagedBlockServeIntent::completion(LookupUnavailable)` still copies `status_label: Available` into evidence.

Output: `LookupUnavailable` sets `status_label = Unavailable`, `presence.payload_present = false`, `missing_inventory = true`. Compact announcement and compact-txn inherit the Plan 01 fact-assembly rule. No new wire error. No `Pruned` emit. No `getblock`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
@.planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
@.planning/phases/143-honest-stored-block-availability/143-01-PLAN.md
@packages/open-bitcoin-node/src/network/block_serving.rs
@packages/open-bitcoin-rpc/src/context/inbound_wire.rs
@packages/open-bitcoin-node/src/network/announcement_transport.rs
@packages/open-bitcoin-node/src/network/action_translation.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/network/block_serving.rs today (dishonest status copy):
```rust
ManagedBlockServeCompletionOutcome::LookupUnavailable => missing(
    BlockServingOutcomeLabel::BlockStatusUnavailable,
    self.eligible_decision.status_label, // stays Available after a successful gate
    self.eligible_decision.eligibility_reason,
),
```

Target (D-04, D-05):
```rust
ManagedBlockServeCompletionOutcome::LookupUnavailable => {
    let mut decision = missing(
        BlockServingOutcomeLabel::BlockStatusUnavailable,
        BlockServingStatusLabel::Unavailable,
        self.eligible_decision.eligibility_reason,
    );
    decision.presence = BlockServingPresenceFacts {
        payload_present: false,
        index_known: self.eligible_decision.presence.index_known,
        validated_on_active_chain: self.eligible_decision.presence.validated_on_active_chain,
    };
    decision
}
```
Leave `eligibility_reason` unchanged unless a compiling test requires `StatusUnavailable`. Do not emit `Pruned`. `TransportFailed` and `Written` still clone `eligible_decision`.

`serve_managed_block_request` already returns `intent.completion(LookupUnavailable).decision` when `lookup_block` yields `None`. It inherits this rewrite.

From packages/open-bitcoin-rpc/src/context/inbound_wire.rs — keep:
```rust
Some(Ok(None)) | Some(Err(_)) | None => {
    intent.completion(ManagedBlockServeCompletionOutcome::LookupUnavailable);
    WireNetworkMessage::NotFound(...)
}
```
Do not invent a new wire message. Do not put store error strings on the wire (existing inbound tests already assert bytes omit `b"private"`).

Compact inheritance (D-10 discretion): `announcement_transport.rs` and `action_translation.rs` already call `managed_block_serve_input(..., false)`. After Plan 01 that is cache-only `payload_present`. Do not add a second probe implementation. Compact-txn stays cache-first.

Do not grow `block_serving.rs` past 628 lines; add new tests in `block_serving/tests.rs` only.
</interfaces>
</context>

<feature>
  <name>Honest LookupUnavailable and missing-payload NotFound</name>
  <files>packages/open-bitcoin-node/src/network/block_serving.rs, packages/open-bitcoin-node/src/network/block_serving/tests.rs</files>
  <behavior>
    Post-gate missing bytes report Unavailable, increment unavailable_count, and send NotFound. Compact cache-miss is Unavailable, not Pruned. No fabricated block body.
  </behavior>
  <implementation>
    Rewrite LookupUnavailable completion. Add unit and inheritance tests. Keep inbound redaction tests as the wire contract.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing LookupUnavailable and compact-inheritance tests</name>
  <files>
    packages/open-bitcoin-node/src/network/block_serving/tests.rs
  </files>
  <read_first>
    .planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
    .planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-node/src/network/block_serving/tests.rs
    packages/open-bitcoin-node/src/network/block_relay_evidence.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `lookup_unavailable_completion_reports_unavailable_not_available`: `gate_managed_block_request(enabled_input())` yields `Serve(intent)`. `intent.completion(LookupUnavailable).decision()` has `label == BlockStatusUnavailable`, `status_label == Unavailable`, `presence.payload_present == false`, `missing_inventory == true`. `records_served_effect()` is false (D-04).
    - Test `lookup_unavailable_does_not_keep_available_status_label`: `include_str` of `block_serving.rs` `completion` function must contain `BlockServingStatusLabel::Unavailable` and must not pass `self.eligible_decision.status_label` into the LookupUnavailable `missing(` call (D-04, HAVL-01).
    - Test `serve_managed_block_request_lookup_none_is_unavailable`: `serve_managed_block_request(enabled_input(), |_| None)` returns `status_label == Unavailable`, `maybe_block == None`, `missing_inventory == true`. The lookup closure was invoked (gate passed). No `Block` is fabricated (D-04).
    - Test `compact_paths_reuse_managed_block_serve_input_without_second_probe`: `include_str` of `announcement_transport.rs` and `action_translation.rs` both contain `managed_block_serve_input`. Neither file contains `has_block` or a second durable probe. Compact stays cache-first via the shared Plan 01 `durable_payload_present=false` call (D-10).
    - Existing `durable_block_serving_missing_body_returns_notfound_without_served_credit`, `durable_block_serving_corruption_is_redacted_as_notfound`, and `durable_block_serving_store_error_is_redacted_as_notfound` stay the inbound wire contract. Do not weaken them.
  </behavior>
  <action>
    RED only. Per D-04, D-05, D-12:

    1. Add the three adapter tests to `packages/open-bitcoin-node/src/network/block_serving/tests.rs` next to `phase127_completion_preserves_unavailable_and_success_only_effects`. Extend that existing test to also assert `unavailable.decision().status_label == BlockServingStatusLabel::Unavailable` if that file still only checks the outcome label.
    2. Add `compact_paths_reuse_managed_block_serve_input_without_second_probe` to `packages/open-bitcoin-node/src/network/block_serving/tests.rs`.
    3. Do not add a new `WireNetworkMessage` variant. Do not emit `Pruned`. Do not invent `getblock`. Do not add Phase 144 operator surfaces.
    4. Combined RED+GREEN in one hook-passing feat commit is allowed because pre-commit runs `verify.sh`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-03-refuse-honesty-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::block_serving:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `block_serving/tests.rs` contains `lookup_unavailable_completion_reports_unavailable_not_available`
    - `block_serving/tests.rs` contains `serve_managed_block_request_lookup_none_is_unavailable`
    - `block_serving/tests.rs` contains `compact_paths_reuse_managed_block_serve_input_without_second_probe`
    - New tests are RED on behavior before Task 2 unless RED+GREEN are combined
  </acceptance_criteria>
  <done>Failing tests exist for LookupUnavailable status rewrite, no fabricated body, and compact inheritance of Unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite LookupUnavailable completion and keep NotFound refuse</name>
  <files>
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
  </files>
  <read_first>
    packages/open-bitcoin-node/src/network/block_serving.rs
    packages/open-bitcoin-rpc/src/context/inbound_wire.rs
    packages/open-bitcoin-node/src/network/announcement_transport.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    standards/core/architecture.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    Same tests as Task 1 pass. inbound missing-body and redaction tests still pass. Production completion never copies Available onto a LookupUnavailable decision. inventory.rs still has no Pruned inject.
  </behavior>
  <action>
    GREEN per D-04, D-05, D-12:

    1. Change `ManagedBlockServeIntent::completion` LookupUnavailable arm to the target snippet in `<interfaces>`. `TransportFailed` and `Written` stay `self.eligible_decision.clone()`.
    2. Do not change `resolve_block_intent` control flow. Keep `WireNetworkMessage::NotFound` and `LookupUnavailable` for `Ok(None)`, `Err(_)`, and missing source.
    3. Do not add a durable-store probe to compact-txn or compact announcement. They inherit Plan 01 cache-only `payload_present`.
    4. Do not emit `BlockServingDataAvailability::Pruned` or increment `pruned_count` on these paths. Do not add a new wire error.
    5. If `block_serving.rs` would exceed 628 lines, move helpers rather than inlining more tests. New tests stay in `block_serving/tests.rs`.
    6. No `unwrap()`. No `getblock`. No operator UI fields.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-03-refuse-honesty-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::block_serving:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `block_serving.rs` `completion` LookupUnavailable arm contains `BlockServingStatusLabel::Unavailable`
    - `block_serving.rs` LookupUnavailable arm contains `payload_present: false`
    - `block_serving.rs` does not pass `self.eligible_decision.status_label` into LookupUnavailable `missing(`
    - `inbound_wire.rs` still contains `WireNetworkMessage::NotFound` and `LookupUnavailable`
    - Task 1 tests pass
    - `wc -l packages/open-bitcoin-node/src/network/block_serving.rs` is ≤ 628
  </acceptance_criteria>
  <done>Post-gate read failure reports Unavailable and NotFound without serving a body or incrementing available_count. Compact paths inherit the same missing-payload rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate Available → byte read | A later miss must not keep the Available report |
| Completion → evidence counters | available_count must not rise on NotFound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-09 | Spoofing | `ManagedBlockServeIntent::completion` | mitigate | LookupUnavailable forces `status_label=Unavailable` and `payload_present=false` (D-04, HAVL-01) |
| T-143-10 | Tampering | `serve_managed_block_request` | mitigate | `lookup_block` `None` returns no `Block` body; missing_inventory stays true |
| T-143-11 | Information disclosure | `resolve_block_intent` | mitigate | Keep store errors as NotFound; do not encode error details (existing inbound redaction tests) |
| T-143-12 | Tampering | compact announcement / compact-txn | mitigate | Reuse `managed_block_serve_input`; missing cache is Unavailable, not Pruned (D-05, D-12) |
</threat_model>

<verification>
Node block_serving unit tests pass. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib inbound_listener::tests::block_serving -- --nocapture` still expects NotFound and `served_count == 0` on missing/corrupt bodies. No new `WireNetworkMessage` variant is introduced.
</verification>

<success_criteria>
LookupUnavailable cannot report Available. Missing payload refuses with NotFound + Unavailable on inventory, durable gate, compact inheritance, and post-gate read fail.
</success_criteria>

<output>
After completion, create `.planning/phases/143-honest-stored-block-availability/143-03-SUMMARY.md`
</output>
