---
phase: 143-honest-stored-block-availability
plan: "02"
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/open-bitcoin-node/src/storage/fjall_store.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/block_presence.rs
  - packages/open-bitcoin-rpc/src/context/inbound_wire.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - packages/open-bitcoin-node/src/network/tests/block_serving.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - HAVL-01
user_setup: []
must_haves:
  truths:
    - "FjallNodeStore::has_block is a contains_key probe on block:<64-hex> and does not decode the body (D-01)."
    - "DurableBlockSource::has_block is the durable probe; gate_inventory_for_durable_serving uses the probe result, never literal true (D-02)."
    - "Cache hit still counts as payload_present; empty cache plus missing store key is not Available (D-01, D-02)."
    - "Store probe errors are treated as payload absent, not Available, and are not copied onto the wire (D-01)."
    - "classify_block_serving_status still has no Fjall or cache reads (D-03)."
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
      provides: "FjallNodeStore::has_block contains_key probe"
      contains: "pub fn has_block"
    - path: packages/open-bitcoin-rpc/src/context/inbound_wire.rs
      provides: "DurableBlockSource::has_block"
      contains: "fn has_block"
    - path: packages/open-bitcoin-node/src/network/inventory.rs
      provides: "gate uses durable_payload_present(block_hash)"
      contains: "durable_payload_present(block_hash)"
  key_links:
    - from: packages/open-bitcoin-rpc/src/context.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "prepare_inbound_wire_message passes has_block probe into receive_message_for_durable_serving"
      pattern: "has_block"
    - from: packages/open-bitcoin-node/src/network/inventory.rs
      to: packages/open-bitcoin-node/src/network/block_serving.rs
      via: "gate_inventory_for_durable_serving feeds probe result as durable_payload_present"
      pattern: "durable_payload_present(block_hash)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 143-2026-09-17T01-47-45
generated_at: 2026-09-17T02:16:12.005Z
---

# Phase 143 Plan 02: Payload-Byte Probe Replaces Override

<objective>
Replace the caller override `durable_availability: true` with a cache-or-store `contains_key` probe so deferred durable serving classifies Available only when payload bytes are present.

Purpose: HAVL-01 / D-01 / D-02 / D-03. Plan 01 made `durable_payload_present` a fact. This plan makes the durable inbound gate obtain that fact from `has_block`, not from a literal `true`.

Output: `FjallNodeStore::has_block`, `DurableBlockSource::has_block`, and a `Fn(BlockHash) -> bool` threaded through `receive_message_for_durable_serving` / `gate_inventory_for_durable_serving`. No body decode during classify. No `getblock`. No Phase 144 UI. The file list exceeds the 10-file warning because the probe signature must compile atomically across store, trait, gate, and callers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
@.planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
@.planning/phases/143-honest-stored-block-availability/143-01-PLAN.md
@packages/open-bitcoin-node/src/storage/fjall_store.rs
@packages/open-bitcoin-rpc/src/context/inbound_wire.rs
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-node/src/network/inventory.rs
@packages/open-bitcoin-node/src/network/action_translation.rs
@packages/open-bitcoin-node/src/network/runtime_authority.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-node/src/storage/fjall_store.rs — reuse the existing key helper:
```rust
fn block_key(block_hash: BlockHash) -> String { /* "block:" + 64 hex */ }
pub fn save_block(&self, block: &Block, mode: PersistMode) -> Result<BlockHash, StorageError>;
pub fn load_block(&self, block_hash: BlockHash) -> Result<Option<Block>, StorageError>;
```
`fjall_store.rs` is 623 lines. Add `mod blocks;` only. Put `has_block` in `fjall_store/blocks.rs`. Child module may use `self.block_index` and `super::block_key`.

Target:
```rust
// fjall_store/blocks.rs
impl FjallNodeStore {
    pub fn has_block(&self, block_hash: BlockHash) -> Result<bool, StorageError> {
        self.block_index
            .contains_key(super::block_key(block_hash))
            .map_err(|error| super::backend_failure(StorageNamespace::BlockIndex, error))
    }
}
```
Do not call `load_block` or `parse_block` inside `has_block`.

From packages/open-bitcoin-rpc/src/context/inbound_wire.rs today:
```rust
pub(crate) trait DurableBlockSource: Send + Sync {
    fn load_block(&self, block_hash: BlockHash) -> Result<Option<Block>, StorageError>;
}
```
Add:
```rust
fn has_block(&self, block_hash: BlockHash) -> Result<bool, StorageError>;
```
`impl DurableBlockSource for FjallNodeStore` delegates `has_block` to `FjallNodeStore::has_block`. Keep `load_block` exactly as Phase 127 requires (`FjallNodeStore::load_block(self, block_hash)`). Do not change `resolve_block_intent`'s `maybe_block` initializer.

From packages/open-bitcoin-node/src/network/action_translation.rs and runtime_authority.rs:
```rust
pub fn receive_message_for_durable_serving(
    ...,
    durable_payload_present: impl Fn(BlockHash) -> bool,
) -> Result<ManagedSyncMessageResult, ...>;
```
`gate_inventory_for_durable_serving` takes the same `Fn(BlockHash) -> bool` and calls:
```rust
self.managed_block_serve_input(peer_id, &request, block_hash, false, durable_payload_present(block_hash))
```
Do not pass literal `true`.

From packages/open-bitcoin-rpc/src/context.rs `prepare_inbound_wire_message`:
```rust
let maybe_source = self.maybe_block_source.clone();
self.network.receive_message_for_durable_serving(
    peer_id, message, timestamp, self.verify_flags, self.consensus_params,
    |hash| match maybe_source.as_ref() {
        Some(source) => source.has_block(hash).unwrap_or(false),
        None => false,
    },
)?;
```
`Result::unwrap_or(false)` is the fail-closed probe (not `unwrap()`). Store errors are payload-absent, not Available, and must not be formatted onto the wire.

Memory/non-durable `receive_message` / `serve_inventory` keep `durable_payload_present=false`. Compact announcement and compact-txn keep `false` (cache-first; D-10 / research A2).

`ScriptedDurableBlockSource` in `inbound_listener/tests.rs` must implement `has_block` as `Ok(true)` so Plan 03 / existing D-04 load-failure tests still reach `load_block`. Real missing-key Fjall returns `Ok(false)`.
</interfaces>
</context>

<feature>
  <name>Cache-or-store payload-byte probe</name>
  <files>packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs, packages/open-bitcoin-rpc/src/context/inbound_wire.rs, packages/open-bitcoin-node/src/network/inventory.rs</files>
  <behavior>
    Saved block key returns has_block true without decode. Missing key returns false. Durable getdata with persist_block=false classifies Unavailable at the gate. Literal true override is gone.
  </behavior>
  <implementation>
    Add has_block. Thread Fn(BlockHash) -> bool into the durable receive/gate APIs. RPC context probes DurableBlockSource before classification.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing has_block and probe-not-override tests</name>
  <files>
    packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests/block_presence.rs
    packages/open-bitcoin-node/src/network/tests/block_serving.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
  </files>
  <read_first>
    .planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
    .planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
    packages/open-bitcoin-node/src/storage/fjall_store.rs
    packages/open-bitcoin-rpc/src/context/inbound_wire.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
    standards/core/testing.md
  </read_first>
  <behavior>
    - Test `has_block_is_true_after_save_block_without_calling_load_block`: open a temp `FjallNodeStore`, `save_block` a fixture block with `PersistMode::Sync`. `has_block(hash)` is `Ok(true)`. `has_block(other_hash)` is `Ok(false)`. `include_str` of `fjall_store/blocks.rs` contains `contains_key` and does not contain `load_block` or `parse_block` (D-01).
    - Test `gate_inventory_source_does_not_pass_literal_true_durable_override`: `include_str` of `inventory.rs` must not contain `block_hash, false, true` and must contain `durable_payload_present(block_hash)` (D-02).
    - Test `durable_getdata_without_store_body_is_unavailable_at_gate`: reuse `durable_block_serving_context(false)` from `inbound_listener/tests.rs`. After `prepare_inbound_wire_message` (before or without treating resolve as the only honesty check), the inbound plan has no `ManagedInboundResponsePlanItem::DurableBlock` intent for the missing body and the gated decision `status_label` is `Unavailable`. Resolve still returns one `WireNetworkMessage::NotFound` and `block_served_write_count() == 0`. Do not treat post-gate NotFound alone as sufficient (D-02). Keep existing `durable_block_serving_missing_body_returns_notfound_without_served_credit` green (D-12).
    - Test `durable_cache_miss_store_hit_is_payload_present`: in a node unit test, call `managed_block_serve_input(..., durable_payload_present=true)` with empty `blocks_by_hash` for an active hash. `presence.payload_present` is true and `data_availability` is Available. This pins cache-or-store OR semantics without requiring Fjall inside the node unit test (D-01, D-02).
  </behavior>
  <action>
    RED only. Per D-01, D-02, D-15:

    1. Add `packages/open-bitcoin-node/src/storage/fjall_store/tests/block_presence.rs` with the `has_block` tests. Register `mod block_presence;` in `fjall_store/tests.rs`.
    2. Add the inventory source-scan and cache-miss-store-hit tests to `packages/open-bitcoin-node/src/network/tests/block_serving.rs`.
    3. Add `durable_getdata_without_store_body_is_unavailable_at_gate` to `packages/open-bitcoin-rpc/src/inbound_listener/tests/block_serving.rs`. Assert gate `Unavailable` and no `DurableBlock` intent for `durable_block_serving_context(false)`, plus NotFound / `served_count == 0` after resolve. Do not treat post-gate NotFound alone as enough. Do not rewrite the corruption/backend redaction tests; those stay the D-04 / info-disclosure contract.
    4. Do not implement `has_block` yet. Do not invent `getblock`. Do not add operator UI fields.
    5. Combined RED+GREEN in one hook-passing feat commit is allowed because pre-commit runs `verify.sh`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-red -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::fjall_store::tests::block_presence -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-red-network -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::tests::block_serving -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-red-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib inbound_listener::tests::block_serving -- --nocapture -- durable_getdata_without_store_body_is_unavailable_at_gate</automated>
  </verify>
  <acceptance_criteria>
    - `fjall_store/tests.rs` contains `mod block_presence`
    - `fjall_store/tests/block_presence.rs` contains `has_block_is_true_after_save_block_without_calling_load_block`
    - `network/tests/block_serving.rs` contains `gate_inventory_source_does_not_pass_literal_true_durable_override`
    - `network/tests/block_serving.rs` contains `durable_cache_miss_store_hit_is_payload_present`
    - `inbound_listener/tests/block_serving.rs` contains `durable_getdata_without_store_body_is_unavailable_at_gate`
    - New tests are RED on behavior before Task 2 unless RED+GREEN are combined
  </acceptance_criteria>
  <done>Failing tests exist for contains_key has_block, no literal true override, cache-or-store OR semantics, and gate Unavailable when the durable body is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement has_block and thread the probe into the durable gate</name>
  <files>
    packages/open-bitcoin-node/src/storage/fjall_store.rs
    packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs
    packages/open-bitcoin-rpc/src/context/inbound_wire.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <read_first>
    packages/open-bitcoin-node/src/storage/fjall_store.rs
    packages/open-bitcoin-rpc/src/context/inbound_wire.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-node/src/network/inventory.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    scripts/check-phase127-authoritative-network-state-unification.ts
    standards/core/architecture.md
    standards/languages/rust.md
  </read_first>
  <behavior>
    Same tests as Task 1 pass. receive_message_for_durable_serving requires the probe callback. relay_serving_cases GetData-tx call passes |_| false. ScriptedDurableBlockSource.has_block returns Ok(true). Fjall missing key is Ok(false). classify_block_serving_status still has no I/O.
  </behavior>
  <action>
    GREEN per D-01, D-02, D-03, D-15:

    1. Add `mod blocks;` to `fjall_store.rs`. Create `fjall_store/blocks.rs` with `FjallNodeStore::has_block` exactly as in `<interfaces>`. Do not add `has_block` to the 623-line parent body.
    2. Add `has_block` to `DurableBlockSource` and the `FjallNodeStore` impl. Do not change `resolve_block_intent`'s `load_block` initializer string; Phase 127 checker pins it.
    3. Change `gate_inventory_for_durable_serving` to take `durable_payload_present: impl Fn(BlockHash) -> bool` and pass `durable_payload_present(block_hash)` into `managed_block_serve_input`. Delete the `, true` override.
    4. Change `ManagedPeerNetwork::receive_message_for_durable_serving` and `ManagedNetworkHandle::receive_message_for_durable_serving` to take the same `impl Fn(BlockHash) -> bool` and forward it to the gate. Update `relay_serving_cases.rs` to pass `|_| false` (tx GetData does not need a block probe).
    5. In `prepare_inbound_wire_message`, pass the fail-closed `has_block` closure from `<interfaces>`.
    6. Implement `ScriptedDurableBlockSource::has_block` as `Ok(true)` so existing corruption/backend tests still exercise `load_block` after a successful classify (D-04 remains Plan 03). Any other `DurableBlockSource` impls in this crate must compile.
    7. Breadcrumb `fjall_store/blocks.rs` and `fjall_store/tests/block_presence.rs` under `node-storage-contract` in `docs/parity/source-breadcrumbs.json`. File comments: Knots `blockstorage.cpp` `HaveBlockData` / `CheckBlockDataAvailability` for `has_block`; `none` only if a checker rejects that anchor — prefer the Knots anchor.
    8. Do not put Fjall types in `open-bitcoin-network`. Do not decode during classify. Do not invent `getblock`. `wc -l` of `fjall_store.rs`, `inbound_wire.rs`, and `inventory.rs` stay ≤628.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-green -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib storage::fjall_store::tests::block_presence -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-green-network -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --lib network::tests::block_serving -- --nocapture</automated>
    <automated>bun run scripts/command-timings.ts run --key phase143-02-payload-probe-green-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib inbound_listener::tests::block_serving -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `fjall_store/blocks.rs` contains `pub fn has_block` and `contains_key`
    - `fjall_store/blocks.rs` does not contain `load_block` or `parse_block`
    - `inbound_wire.rs` contains `fn has_block`
    - `inventory.rs` contains `durable_payload_present(block_hash)`
    - `inventory.rs` does not contain `block_hash, false, true`
    - `context.rs` contains `has_block`
    - `relay_serving_cases.rs` `receive_message_for_durable_serving` call has a probe argument
    - `docs/parity/source-breadcrumbs.json` contains `packages/open-bitcoin-node/src/storage/fjall_store/blocks.rs`
    - Task 1 tests pass, including `durable_getdata_without_store_body_is_unavailable_at_gate`
    - `wc -l packages/open-bitcoin-node/src/storage/fjall_store.rs` is ≤ 628
  </acceptance_criteria>
  <done>Durable serving classifies from a real cache-or-store probe. The unconditional true override is gone. Classifier I/O boundary is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable store → classify | A missing or unreadable key must not become Available |
| Store error → peer wire | Backend/corruption strings must not leave the probe closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-05 | Spoofing | `gate_inventory_for_durable_serving` | mitigate | Replace literal `true` with `durable_payload_present(block_hash)` from `has_block` (D-02) |
| T-143-06 | Tampering | `FjallNodeStore::has_block` | mitigate | Presence is `contains_key` on `block:<hex>` only; do not treat coins tip or header snapshot as payload (D-01) |
| T-143-07 | Information disclosure | `prepare_inbound_wire_message` probe | mitigate | Map `has_block` `Err` to `false`; do not format `StorageError` into inventory or RPC responses |
| T-143-08 | Denial of service | classify-time probe | mitigate | Use `contains_key`, not `load_block`/`parse_block`, on every GetData classify |
</threat_model>

<verification>
Node `block_presence` and `network::tests::block_serving` tests pass. `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib inbound_listener::tests::block_serving -- --nocapture` covers `durable_getdata_without_store_body_is_unavailable_at_gate` plus missing-body NotFound and redacted store errors. `bun run scripts/check-phase127-authoritative-network-state-unification.ts` still accepts the pinned `load_block` resolve path. No `getblock` method is added.
</verification>

<success_criteria>
Available on the durable gate requires a successful cache-or-store payload-byte probe. Literal `true` is gone. Probe errors fail closed as absent. Classifier stays I/O-free.
</success_criteria>

<output>
After completion, create `.planning/phases/143-honest-stored-block-availability/143-02-SUMMARY.md`
</output>
