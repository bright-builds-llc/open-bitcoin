---
phase: 143-honest-stored-block-availability
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - docs/architecture/status-snapshot.md
  - docs/operator/runtime-guide.md
  - docs/parity/catalog/p2p.md
  - docs/parity/index.json
  - scripts/check-phase111-full-block-serving-request-path.ts
  - scripts/check-phase111-full-block-serving-request-path.test.ts
autonomous: true
requirements:
  - HAVL-02
user_setup: []
must_haves:
  truths:
    - "Claim-scanned Phase 111 docs no longer say this node pruned historical files; missing payload is Unavailable and block_status_pruned is reserved (D-06, D-07)."
    - "Doc edits do not add a getblock product or a Phase 144 operator UI rollout (D-10, D-11)."
    - "Doc edits do not claim archive-node honesty or a public-default serving change (D-13)."
    - "Verification remains bash scripts/verify.sh, deterministic, and public-network-free (D-14)."
  artifacts:
    - path: docs/architecture/status-snapshot.md
      provides: "Reserved Pruned copy; missing payload is Unavailable"
      contains: "reserved for a future prune-mode delete"
    - path: docs/operator/runtime-guide.md
      provides: "Same reserved-Pruned wording plus repo-local Cargo/Bazel review commands"
      contains: "block_status_unavailable"
    - path: docs/parity/catalog/p2p.md
      provides: "P2P catalog no longer reads pruned active non-tip as prune-mode"
      contains: "reserved for a future prune-mode delete"
    - path: docs/parity/index.json
      provides: "v2-1-full-block-serving-request-path rationale updated"
      contains: "reserved for a future prune-mode delete"
  key_links:
    - from: docs/parity/index.json
      to: scripts/check-phase111-full-block-serving-request-path.ts
      via: "rationale still includes required terms WireNetworkMessage::NotFound and block_status_pruned"
      pattern: "block_status_pruned"
    - from: docs/operator/runtime-guide.md
      to: scripts/check-phase111-full-block-serving-request-path.ts
      via: "REQUIRED_RUNTIME_COMMANDS remain verbatim"
      pattern: "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ..."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 143-2026-09-17T01-47-45
generated_at: 2026-09-17T02:16:12.005Z
---

# Phase 143 Plan 04: Reserved-Pruned Docs and Phase 111 Claim Copy

<objective>
Rewrite claim-scanned Phase 111 copy so reserved `Pruned` cannot be read as prune-mode, without adding getblock, archive-node, or public-default serving claims.

Purpose: HAVL-02 / D-06 / D-10 / D-11 / D-13 / D-14. Plan 01 renamed the missing-body test and checker entry. This plan updates the four claim-scanned docs that still say "pruned active non-tip blocks" as if this node deleted historical files.

Output: Updated `status-snapshot.md`, `runtime-guide.md`, `p2p.md`, and the `v2-1-full-block-serving-request-path` rationale in `index.json`. Phase 111 checker still finds required terms including `block_status_pruned`. No new operator UI. No `getblock`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
@.planning/phases/143-honest-stored-block-availability/143-RESEARCH.md
@docs/architecture/status-snapshot.md
@docs/operator/runtime-guide.md
@docs/parity/catalog/p2p.md
@docs/parity/index.json
@scripts/check-phase111-full-block-serving-request-path.ts
@standards/core/architecture.md

<interfaces>
Phase 111 checker scans these files and requires these exact strings still to appear somewhere in the combined corpus:
- `block_status_pruned`
- `block_status_unavailable`
- `block_request_cap_reached`
- `WireNetworkMessage::Block`
- `WireNetworkMessage::NotFound`
- `ManagedBlockServeInput`
- `serve_managed_block_request`
- `lookup_block`
- `InventoryType::Block`, `InventoryType::WitnessBlock`, `InventoryType::CompactBlock`
- `BSRV-04`, `GOV-01`, `GOV-05`
- `v2-1-full-block-serving-request-path`

Runtime guide must keep these command lines verbatim (D-14 / repo-local UAT):
```
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...
bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...
bash scripts/verify.sh
```

Forbidden positive claims (do not introduce as enabled/supported/added). Existing "does not add ... archive-node behavior / public block serving by default" sentences must stay:
- archive-node behavior
- public block serving by default
- compact filter serving
- production full-node readiness
- production service operation
- production-funds wallet use

Do not add `getblock` as a method or product sentence (D-11). Do not add status/RPC/CLI/dashboard/metrics/log field tables (D-10 / Phase 144).

Replace this exact phrase wherever it appears in the four files:
`pruned active non-tip blocks`

Replacement sentence to use (keep required terms):
`active-chain blocks whose payload bytes are missing, including non-tip hashes, return WireNetworkMessage::NotFound with block_status_unavailable. The label block_status_pruned remains reserved for a future prune-mode delete when prune mode actually deleted files and is not emitted on production paths.`

Do not write "this node is in prune mode" or "historical blocks were pruned."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pruned-as-prune-mode copy in the four claim-scanned docs</name>
  <files>
    docs/architecture/status-snapshot.md
    docs/operator/runtime-guide.md
    docs/parity/catalog/p2p.md
    docs/parity/index.json
  </files>
  <read_first>
    .planning/phases/143-honest-stored-block-availability/143-CONTEXT.md
    docs/architecture/status-snapshot.md
    docs/operator/runtime-guide.md
    docs/parity/catalog/p2p.md
    docs/parity/index.json
    scripts/check-phase111-full-block-serving-request-path.ts
  </read_first>
  <action>
    Per D-06, D-10, D-11, D-13:

    1. In `docs/architecture/status-snapshot.md` Phase 111 section, replace `pruned active non-tip blocks` with the replacement sentence from `<interfaces>`. Keep `block_status_pruned` as reserved vocabulary. Keep the existing "Phase 111 does not add ... archive-node behavior ... public block serving by default ..." paragraph.
    2. In `docs/operator/runtime-guide.md` Phase 111 section, make the same replacement. Keep the three repo-local command lines verbatim. Keep the "does not add" deferral paragraph.
    3. In `docs/parity/catalog/p2p.md` request-path paragraph, make the same replacement. Keep Knots anchors `net_processing.cpp`, `blockstorage.cpp`, `validation.cpp`, and `p2p_getdata.py`.
    4. In `docs/parity/index.json` `v2-1-full-block-serving-request-path` `rationale`, replace `pruned active non-tip blocks` with the same reserved-Pruned wording. Keep `WireNetworkMessage::NotFound`, `block_status_pruned`, `block_status_unavailable`, and `block_request_cap_reached` in that rationale string so REQUIRED_TERMS still match. Do not change `status: done` or the evidence/upstream arrays.
    5. Do not edit `docs/parity/checklist.md` unless a phrase there still says this node pruned historical files. Do not add a `getblock` surface. Do not add CSOBS operator-field tables.
    6. Do not claim the missing-payload refuse is archive-node honesty or a public-default serving change.
  </action>
  <verify>
    <automated>bun test scripts/check-phase111-full-block-serving-request-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/status-snapshot.md` contains `reserved for a future prune-mode delete`
    - `docs/operator/runtime-guide.md` contains `reserved for a future prune-mode delete`
    - `docs/parity/catalog/p2p.md` contains `reserved for a future prune-mode delete`
    - `docs/parity/index.json` contains `reserved for a future prune-mode delete`
    - none of those four files contains `pruned active non-tip blocks`
    - `docs/operator/runtime-guide.md` still contains `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...`
    - `docs/operator/runtime-guide.md` still contains `bazel run //packages/open-bitcoin-cli:open_bitcoin -- ...`
    - `docs/operator/runtime-guide.md` still contains `bash scripts/verify.sh`
    - `docs/architecture/status-snapshot.md` still contains `does not add` and `archive-node behavior`
    - none of the four files contains `getblock` as a new product method
  </acceptance_criteria>
  <done>Claim-scanned docs describe missing payload as Unavailable and reserved Pruned as future prune-mode delete only.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm Phase 111 checker and no-claim scan stay green</name>
  <files>
    scripts/check-phase111-full-block-serving-request-path.ts
    scripts/check-phase111-full-block-serving-request-path.test.ts
  </files>
  <read_first>
    scripts/check-phase111-full-block-serving-request-path.ts
    scripts/check-phase111-full-block-serving-request-path.test.ts
    docs/architecture/status-snapshot.md
    docs/operator/runtime-guide.md
    docs/parity/catalog/p2p.md
    docs/parity/index.json
  </read_first>
  <action>
    Per D-06, D-13, D-14:

    1. Run `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` and `bun run scripts/check-phase111-full-block-serving-request-path.ts`. If either fails because a required term vanished, restore the exact term in the same paragraph as the reserved-Pruned sentence. Do not drop `block_status_pruned` from REQUIRED_TERMS.
    2. If the checker test fixture `nodeTestsText()` or claim-scan fixtures still contain `pruned active non-tip blocks` as a live-doc expectation, update those fixtures to the reserved wording while keeping `block_status_pruned` present.
    3. Do not add public-network, live-mainnet, or wall-clock gates to `scripts/verify.sh`. Historical-serving review stays opt-in UAT guidance only.
    4. Do not invent `getblock`. Do not add Phase 144 surfaces.
  </action>
  <verify>
    <automated>bun run scripts/check-phase111-full-block-serving-request-path.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase111-full-block-serving-request-path.ts` still contains `"block_status_pruned"`
    - `scripts/check-phase111-full-block-serving-request-path.ts` contains `phase111_active_chain_non_tip_missing_local_block_returns_unavailable_notfound`
    - `bun test scripts/check-phase111-full-block-serving-request-path.test.ts` exits 0
    - `bun run scripts/check-phase111-full-block-serving-request-path.ts` exits 0
    - `scripts/verify.sh` is not edited to add public-network or live-mainnet gates
  </acceptance_criteria>
  <done>Phase 111 checker and no-claim scan accept the reserved-Pruned docs. verify.sh stays deterministic and public-network-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator reading | Help text can be mistaken for prune-mode or archive-node product claims |
| Checker → release evidence | Dropping reserved terms or adding forbidden positive claims fails the Phase 111 scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-13 | Spoofing | Phase 111 rationale / runtime guide | mitigate | Replace `pruned active non-tip blocks` with reserved-for-future-prune-delete wording (D-06) |
| T-143-14 | Information disclosure | claim-scanned docs | mitigate | Do not document payload contents, peer identifiers, or store error strings |
| T-143-15 | Repudiation | archive / public-default sentences | mitigate | Keep existing "does not add archive-node behavior / public block serving by default" no-claim lines (D-13) |
| T-143-16 | Tampering | `getblock` product copy | mitigate | Do not add a getblock method or surface (D-11) |
</threat_model>

<verification>
`bun test scripts/check-phase111-full-block-serving-request-path.test.ts` and `bun run scripts/check-phase111-full-block-serving-request-path.ts` pass. The four docs contain `reserved for a future prune-mode delete` and do not contain `pruned active non-tip blocks`. Runtime guide still has the three repo-local commands.
</verification>

<success_criteria>
Operators and the Phase 111 checker can still see `block_status_pruned` as reserved vocabulary, and cannot read current missing-payload behavior as prune-mode, archive-node honesty, public-default serving, or a new getblock product.
</success_criteria>

<output>
After completion, create `.planning/phases/143-honest-stored-block-availability/143-04-SUMMARY.md`
</output>
