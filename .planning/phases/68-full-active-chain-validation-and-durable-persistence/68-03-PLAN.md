---
phase: 68-full-active-chain-validation-and-durable-persistence
plan: 03
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - docs/architecture/status-snapshot.md
  - docs/operator/runtime-guide.md
  - scripts/check-phase68-active-chain-persistence.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/68-full-active-chain-validation-and-durable-persistence/68-03-SUMMARY.md
  - .planning/phases/68-full-active-chain-validation-and-durable-persistence/68-VERIFICATION.md
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04]
user_setup: []
must_haves:
  truths:
    - "Operator docs explain that validated active-chain progress is connected chainstate progress, not header-only or downloaded-only progress."
    - "Default verification remains deterministic and excludes public-network live smoke and real service-manager operations."
    - "A deterministic checker fails if Phase 68 docs or status fields stop naming active-chain persistence evidence."
  artifacts:
    - path: "docs/architecture/status-snapshot.md"
      provides: "Status field contract for active-chain evidence"
      contains: "validated_active_chain_height"
    - path: "docs/operator/runtime-guide.md"
      provides: "Operator-facing active-chain progress explanation"
      contains: "maybe_validated_active_chain_work"
    - path: "scripts/check-phase68-active-chain-persistence.ts"
      provides: "Deterministic Phase 68 drift guard"
      contains: "validated Phase 68 active-chain persistence evidence"
    - path: "scripts/verify.sh"
      provides: "Repo-native verification wiring"
      contains: "bun run scripts/check-phase68-active-chain-persistence.ts"
    - path: ".planning/phases/68-full-active-chain-validation-and-durable-persistence/68-VERIFICATION.md"
      provides: "Phase 68 lifecycle verification evidence"
      contains: "status: passed"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase68-active-chain-persistence.ts"
      via: "Bun deterministic checker"
      pattern: "bun run scripts/check-phase68-active-chain-persistence.ts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 68-2026-06-11T11-56-49
generated_at: 2026-06-11T11:56:49.713Z
---

<objective>
Align docs and deterministic verification with the Phase 68 active-chain
persistence contract.

Purpose: SYNC-01 through SYNC-04 need auditable evidence that status, docs, and
default verification all preserve the same validated-progress boundary.
Output: Narrow docs updates, a deterministic Bun checker, verify wiring, and
Phase 68 verification artifact.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-CONTEXT.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-RESEARCH.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-PLAN.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-02-PLAN.md
@docs/architecture/status-snapshot.md
@docs/operator/runtime-guide.md
@scripts/verify.sh
@scripts/check-v1.5-release-boundaries.ts
</context>

<threat_model>
## Security And Claim-Boundary Notes

- This plan changes documentation and deterministic checks only; it must not
  add public-network or service-manager execution to default verification.
- The main user-facing risk is overstating Phase 68 as production full-node
  readiness. Docs must preserve the explicit opt-in sync-to-tip and deferred
  Phase 69-74 boundaries.
- The checker should assert required evidence strings and verify wiring, not
  run a live mainnet daemon.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update active-chain status and operator docs</name>
  <read_first>
    - `docs/architecture/status-snapshot.md`
    - `docs/operator/runtime-guide.md`
    - `.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-CONTEXT.md`
  </read_first>
  <files>docs/architecture/status-snapshot.md, docs/operator/runtime-guide.md</files>
  <behavior>
    - Docs describe `validated_active_chain_height`, `maybe_validated_active_chain_hash`, and `maybe_validated_active_chain_work`.
    - Docs continue to describe `downloaded_block_height`, `connected_block_height`, and `block_height` compatibility behavior.
    - Docs state active-chain progress is credited only after consensus validation, active-chain connection, and durable persistence.
    - Docs preserve opt-in UAT wording and do not claim inbound serving, relay, production funds, migration apply, packaging, GUI, or broad production-node readiness.
  </behavior>
  <action>
    Make the smallest doc changes needed near existing sync progress field
    descriptions. Avoid rewriting unrelated operator workflow sections.
  </action>
  <acceptance_criteria>
    - Both docs contain `validated_active_chain_height`, `maybe_validated_active_chain_hash`, and `maybe_validated_active_chain_work`.
    - Both docs distinguish downloaded block bodies from connected active-chain progress.
    - Runtime guide keeps repo-local copy-paste Cargo and Bazel command examples.
  </acceptance_criteria>
  <verify>
    <automated>rg "validated_active_chain_height|maybe_validated_active_chain_hash|maybe_validated_active_chain_work|downloaded-only|durable" docs/architecture/status-snapshot.md docs/operator/runtime-guide.md</automated>
  </verify>
  <done>Docs state the active-chain persistence boundary clearly and narrowly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic Phase 68 checker and verification wiring</name>
  <read_first>
    - `scripts/check-v1.5-release-boundaries.ts`
    - `scripts/verify.sh`
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
  </read_first>
  <files>scripts/check-phase68-active-chain-persistence.ts, scripts/verify.sh</files>
  <behavior>
    - Add `scripts/check-phase68-active-chain-persistence.ts`.
    - The checker validates status fields, runtime projection strings, docs strings, Phase 68 plan artifacts, and verify wiring.
    - The checker rejects public-network or service-manager operations in the Phase 68 checker itself.
    - `scripts/verify.sh` runs the checker under Bun.
  </behavior>
  <action>
    Mirror existing TypeScript checker style. Keep checks deterministic, local,
    and text/JSON based. Wire the checker into `scripts/verify.sh` near other
    phase-level deterministic docs/status guards.
  </action>
  <acceptance_criteria>
    - `scripts/check-phase68-active-chain-persistence.ts` contains `validated Phase 68 active-chain persistence evidence`.
    - `scripts/verify.sh` contains `bun run scripts/check-phase68-active-chain-persistence.ts`.
    - `bun run scripts/check-phase68-active-chain-persistence.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase68-active-chain-persistence.ts</automated>
  </verify>
  <done>Default verification guards the Phase 68 active-chain persistence contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full verification and write lifecycle evidence</name>
  <read_first>
    - `bash scripts/verify.sh`
    - `.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-SUMMARY.md`
    - `.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-02-SUMMARY.md`
    - `.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-03-SUMMARY.md`
  </read_first>
  <files>.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-VERIFICATION.md, docs/metrics/lines-of-code.md</files>
  <behavior>
    - Run focused Rust tests for Phase 68 status/sync changes.
    - Run `bun run scripts/check-phase68-active-chain-persistence.ts`.
    - Run `bash scripts/verify.sh`.
    - Refresh generated line-count metrics if the repo-native verification does so.
    - Write `68-VERIFICATION.md` with `status: passed` only after verification passes.
  </behavior>
  <action>
    Use the final repo-native verification contract as the gate. If any check
    fails, fix the root cause before writing a passing verification artifact.
  </action>
  <acceptance_criteria>
    - Focused Rust tests pass.
    - Phase 68 checker passes.
    - `bash scripts/verify.sh` passes.
    - `68-VERIFICATION.md` contains `status: passed`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node connected_active_chain_progress_survives_runtime_reopen sync_progress --all-features</automated>
    <automated>bun run scripts/check-phase68-active-chain-persistence.ts</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Phase 68 has passing local verification and lifecycle evidence.</done>
</task>

</tasks>

<verification>
Run:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node connected_active_chain_progress_survives_runtime_reopen --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync_progress --all-features
bun run scripts/check-phase68-active-chain-persistence.ts
bash scripts/verify.sh
```
</verification>

<success_criteria>
- [ ] Docs and status fields describe validated active-chain progress explicitly.
- [ ] Deterministic checker is wired into repo-native verification.
- [ ] Full repo verification passes.
- [ ] Phase 68 verification artifact records `status: passed`.
</success_criteria>
