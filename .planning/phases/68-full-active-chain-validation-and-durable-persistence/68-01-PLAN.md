---
phase: 68-full-active-chain-validation-and-durable-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/sync/tests.rs
  - .planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-SUMMARY.md
autonomous: true
requirements: [SYNC-01, SYNC-03, SYNC-04]
user_setup: []
must_haves:
  truths:
    - "A requested best-chain block is credited only after consensus validation, active-chain connection, and durable persistence."
    - "Reopening `DurableSyncRuntime` on the same datadir recovers the connected active-chain tip, downloaded body evidence, and chain work needed to resume validation."
    - "Downloaded-only or no-credit block responses do not advance connected active-chain progress."
  artifacts:
    - path: "packages/open-bitcoin-node/src/sync/tests.rs"
      provides: "Durable active-chain reopen proof"
      contains: "connected_active_chain_progress_survives_runtime_reopen"
    - path: ".planning/phases/68-full-active-chain-validation-and-durable-persistence/68-01-SUMMARY.md"
      provides: "Plan 68-01 execution evidence"
      contains: "Self-Check: PASSED"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/tests.rs"
      to: "packages/open-bitcoin-node/src/sync/runtime_state.rs"
      via: "status projection after reopening the durable store"
      pattern: "validated_active_chain_height"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 68-2026-06-11T11-56-49
generated_at: 2026-06-11T11:56:49.713Z
---

<objective>
Prove the Phase 68 active-chain persistence contract at the runtime boundary.

Purpose: SYNC-01, SYNC-03, and SYNC-04 require connected progress to survive a
same-datadir restart and remain distinct from downloaded-only progress.
Output: Focused sync runtime tests for connected progress, reopen recovery, and
no-credit boundaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-CONTEXT.md
@.planning/phases/68-full-active-chain-validation-and-durable-persistence/68-RESEARCH.md
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/block_response.rs
@packages/open-bitcoin-node/src/sync/runtime_state.rs
@packages/open-bitcoin-node/src/chainstate.rs
@packages/open-bitcoin-node/src/storage/fjall_store.rs
</context>

<threat_model>
## Security And Correctness Notes

- This plan adds no network listener, wallet mutation, service-manager action,
  or public-network default verification.
- The primary correctness risk is false credit: treating stored headers or
  downloaded block bodies as validated active-chain progress.
- Mitigation is a same-store reopen test that checks connected active-chain
  height/hash/work after `DurableSyncRuntime` reloads persisted state.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add same-datadir active-chain reopen proof</name>
  <read_first>
    - `packages/open-bitcoin-node/src/sync/tests.rs`
    - `packages/open-bitcoin-node/src/sync.rs`
    - `packages/open-bitcoin-node/src/sync/runtime_state.rs`
    - `packages/open-bitcoin-node/src/chainstate.rs`
  </read_first>
  <files>packages/open-bitcoin-node/src/sync/tests.rs</files>
  <behavior>
    - Add a deterministic sync test named `connected_active_chain_progress_survives_runtime_reopen`.
    - The test builds a genesis + child best chain, requests the child block, and runs one sync cycle.
    - The first runtime reports downloaded height 1, connected height 1, connected child hash, and active-chain work for the connected tip.
    - A second `DurableSyncRuntime` opened on the same store reports the same connected height/hash/work before requesting more blocks.
    - The reopened store still has the child block body available for recovery.
  </behavior>
  <action>
    Reuse existing scripted transport and store helpers in `sync/tests.rs`.
    Prefer assertions against public runtime summary/status projections where
    possible, and inspect the chainstate snapshot only for durable proof that is
    not otherwise surfaced.
  </action>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/sync/tests.rs` contains `connected_active_chain_progress_survives_runtime_reopen`.
    - The test asserts connected height, connected hash, downloaded body evidence, and connected chain work before and after reopening.
    - The test does not use public network access or a real datadir outside the temporary test store.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node connected_active_chain_progress_survives_runtime_reopen --all-features</automated>
  </verify>
  <done>Connected active-chain progress has a same-datadir restart proof.</done>
</task>

</tasks>

<verification>
Run:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node connected_active_chain_progress_survives_runtime_reopen --all-features
```
</verification>

<success_criteria>
- [ ] Same-datadir reopen preserves connected active-chain height.
- [ ] Same-datadir reopen preserves connected active-chain hash and chain work.
- [ ] Downloaded body evidence remains available after reopen.
- [ ] Focused open-bitcoin-node sync test passes.
</success_criteria>
