---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "04"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/status/mempool_groups.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests/mempool_groups.rs
  - packages/open-bitcoin-node/src/status/tests.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests/network_status.rs
autonomous: true
requirements: [MPOBS-02]
must_haves:
  truths:
    - "MempoolStatus carries distinct resources and fee_floors groups with Open Bitcoin field names, not Knots aliases."
    - "virtual_size, accounted_usage, and accounted_capacity are separate integers; the four fee floors are separate integers with effective = max(static, rolling) in fixtures where static != rolling."
    - "openbitcoinnetworkstatus.mempool publishes those groups so a later collector can copy them."
  artifacts:
    - path: packages/open-bitcoin-node/src/status/mempool_groups.rs
      provides: "Resources and fee-floor group types"
      contains: "MempoolResourcesGroup"
    - path: packages/open-bitcoin-rpc/src/method/node.rs
      provides: "Status RPC mempool field"
      contains: "pub mempool"
  key_links:
    - from: packages/open-bitcoin-rpc/src/dispatch/node.rs
      to: packages/open-bitcoin-node/src/status.rs
      via: "OpenBitcoinNetworkStatusResponse.mempool"
      pattern: "mempool:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Extend the shared snapshot mempool object with identifier-free `resources` and `fee_floors` groups and publish them on `openbitcoinnetworkstatus`.

Purpose: Status currently keeps only `size` and `relay` (D-10, D-11, D-12). Operators need Phase 130 roles on the shared model before dashboard/metrics copy them.
Output: Typed groups on `MempoolStatus` plus daemon publication. No dashboard rows yet (Plan 07).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@packages/open-bitcoin-node/src/status.rs
@packages/open-bitcoin-node/src/network/types.rs
@packages/open-bitcoin-rpc/src/method/node.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@docs/architecture/status-snapshot.md
@standards/core/code-shape.md
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-node/src/status.rs (extend, do not replace `relay`):

```rust
pub struct MempoolStatus {
    pub transactions: FieldAvailability&lt;u64&gt;,
    pub relay: relay_evidence::RelayEvidenceStatus,
    // add resources + fee_floors
}
```

From packages/open-bitcoin-node/src/network/types.rs:

```rust
pub struct ManagedMempoolInfo {
    pub transaction_count: usize,
    pub total_virtual_size: usize,
    pub accounted_memory: usize,
    pub mempool_capacity: usize,
    pub static_relay_fee_rate_sats_per_kvb: i64,
    pub incremental_relay_fee_rate_sats_per_kvb: i64,
    pub rolling_mempool_fee_rate_sats_per_kvb: i64,
    pub effective_admission_fee_rate_sats_per_kvb: i64,
    // ...
}
```

From packages/open-bitcoin-rpc/src/method/node.rs:

```rust
pub struct OpenBitcoinNetworkStatusResponse {
    pub inbound: FieldAvailability&lt;InboundPeerServingStatus&gt;,
    pub relay: RelayEvidenceStatus,
    pub block_relay: BlockRelayEvidenceStatus,
    pub metrics: MetricsStatus,
    // add mempool: MempoolStatus with serde(default)
}
```

`status.rs` is 594 lines. Add `mod mempool_groups` and keep new types out of the root file (628-line gate).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MempoolResourcesGroup and MempoolFeeFloorsGroup</name>
  <read-first>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>packages/open-bitcoin-node/src/status.rs</file>
    <file>packages/open-bitcoin-node/src/network/types.rs</file>
    <file>packages/open-bitcoin-node/src/status/tests.rs</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/status/mempool_groups.rs, packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests/mempool_groups.rs, packages/open-bitcoin-node/src/status/tests.rs</files>
  <behavior>
    - `MempoolResourcesGroup` serializes snake_case fields `virtual_size`, `accounted_usage`, `accounted_capacity`, `transaction_count` as numbers. JSON must not contain `bytes`, `usage`, `maxmempool`, or `mempoolminfee` (D-12).
    - `MempoolFeeFloorsGroup` serializes `static_relay_floor`, `rolling_mempool_floor`, `effective_admission_floor`, `incremental_relay_fee` as i64 sat/kvB. A fixture with static=1000, rolling=2500, incremental=500 must serialize `effective_admission_floor=2500` and keep incremental=500 (D-11).
    - `MempoolStatus` default/serde-missing groups deserialize as `FieldAvailability::Unavailable` with a fixed reason function, and `relay` remains the Phase 105 type.
    - `from_managed_mempool_info(&amp;ManagedMempoolInfo)` maps `total_virtual_size` → `virtual_size`, `accounted_memory` → `accounted_usage`, `mempool_capacity` → `accounted_capacity`, `transaction_count` → `transaction_count`, and the four fee fields 1:1 without swapping minrelay and incremental.
    - No field is a `String` hex identity. No `PackageReport` is stored on the groups.
  </behavior>
  <action>
    Per D-10, D-11, and D-12, create `status/mempool_groups.rs` using `foo.rs` + `foo/` if tests need a sibling. `mod mempool_groups;` in `status.rs` and `pub use mempool_groups::{MempoolFeeFloorsGroup, MempoolResourcesGroup};`.

    Add to `MempoolStatus` with `#[serde(default = "...")]` helpers returning `FieldAvailability::unavailable("mempool group unavailable")` so old snapshots still deserialize.

    ```rust
    pub struct MempoolResourcesGroup {
        pub virtual_size: u64,
        pub accounted_usage: u64,
        pub accounted_capacity: u64,
        pub transaction_count: u64,
    }
    pub struct MempoolFeeFloorsGroup {
        pub static_relay_floor: i64,
        pub rolling_mempool_floor: i64,
        pub effective_admission_floor: i64,
        pub incremental_relay_fee: i64,
    }
    pub fn resources_from_managed_info(info: &amp;ManagedMempoolInfo) -&gt; MempoolResourcesGroup;
    pub fn fee_floors_from_managed_info(info: &amp;ManagedMempoolInfo) -&gt; MempoolFeeFloorsGroup;
    ```

    Do not change `GetMempoolInfoResponse` aliases (D-12 RPC-only). Do not add pressure/retry groups (Plan 05).

    Write failing tests named exactly:
    - `resources_group_json_uses_open_bitcoin_names_not_knots_aliases`
    - `fee_floors_keep_four_distinct_values_when_rolling_exceeds_static`
    - `mempool_status_deserializes_missing_groups_as_unavailable`
    - `resources_from_managed_info_does_not_swap_vsize_and_accounted_usage`

    Update existing `MempoolStatus { transactions, relay }` struct literals across the node crate that this compile break touches **only** if they are in files already listed. Prefer `#[serde(default)]` plus `Default` on the new fields so existing literals keep compiling via `..Default` only if you add `Default` for the new fields and a local helper `MempoolStatus::from_transactions_and_relay`. Do not rewrite CLI files in this plan.

    If compile requires CLI/status fixture updates, stop and add a thin `impl Default for MempoolStatus` that keeps `relay: RelayEvidenceStatus::default()` and new groups unavailable so existing literals that set only `transactions` and `relay` still compile.

    File-header breadcrumb: `none` (Open Bitcoin snapshot). Do not edit `source-breadcrumbs.json`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-04-groups -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features resources_group_json -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub struct MempoolResourcesGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'pub struct MempoolFeeFloorsGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'bytes|maxmempool|mempoolminfee' packages/open-bitcoin-node/src/status/mempool_groups.rs` returns no matches.</criterion>
    <criterion>`rg -n 'pub resources:|pub fee_floors:' packages/open-bitcoin-node/src/status.rs` matches.</criterion>
    <criterion>The named Cargo filter reports the resources JSON test passing.</criterion>
  </acceptance-criteria>
  <done>Shared MempoolStatus has distinct resource and fee-floor groups with Open Bitcoin names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish resources and fee floors on openbitcoinnetworkstatus.mempool</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/method/node.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/node.rs</file>
    <file>packages/open-bitcoin-node/src/status/mempool_groups.rs</file>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/tests.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/method/node.rs, packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs, packages/open-bitcoin-rpc/src/dispatch/tests/network_status.rs</files>
  <behavior>
    - `OpenBitcoinNetworkStatusResponse` serializes `mempool` as `MempoolStatus`.
    - `open_bitcoin_network_status` sets `mempool.transactions` from `snapshot.mempool().transaction_count`, `mempool.relay` from `snapshot.relay()`, `mempool.resources` and `mempool.fee_floors` from the Plan 04 mappers, and leaves pressure/eviction/checkpoint/recovery/retry/admission unavailable until Plan 05.
    - Response JSON under `mempool.resources` uses `virtual_size` not `bytes`, and under `mempool.fee_floors` uses `effective_admission_floor` not `mempoolminfee`.
    - `getmempoolinfo` still serializes `bytes`, `usage`, `maxmempool`, `mempoolminfee` unchanged (D-09, D-12).
  </behavior>
  <action>
    Per D-07 and D-10, add `#[serde(default)] pub mempool: MempoolStatus` to `OpenBitcoinNetworkStatusResponse`. Import `MempoolStatus` from `open_bitcoin_node::status`. Fill it in `open_bitcoin_network_status` from `authoritative_operator_snapshot()`.

    If `dispatch/node.rs` would exceed 500 lines, move the mempool fill into `dispatch/node/mempool_status.rs` using `foo.rs` + `foo/`.

    Write failing tests in the existing RPC dispatch test module (or `dispatch/tests/network_status.rs` if you must split), named exactly:
    - `openbitcoinnetworkstatus_mempool_resources_use_open_bitcoin_names`
    - `openbitcoinnetworkstatus_fee_floors_are_distinct_from_getmempoolinfo_aliases`
    - `getmempoolinfo_aliases_remain_bytes_usage_maxmempool_mempoolminfee`

    Do not update the CLI collector (Plan 07). Do not add dashboard rows.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-04-status-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features openbitcoinnetworkstatus_mempool -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub mempool: MempoolStatus' packages/open-bitcoin-rpc/src/method/node.rs` matches.</criterion>
    <criterion>`rg -n 'resources_from_managed_info|fee_floors_from_managed_info' packages/open-bitcoin-rpc/src/dispatch/node.rs` matches (or the split child file).</criterion>
    <criterion>`rg -n 'getrawmempool' packages/open-bitcoin-rpc/src/method.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the two openbitcoinnetworkstatus tests passing.</criterion>
  </acceptance-criteria>
  <done>Daemon status RPC publishes identifier-free resource and fee-floor groups beside existing relay evidence.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authority snapshot → status RPC | Authenticated but not an originating package response (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04-01 | Information Disclosure | MempoolStatus groups | mitigate | High: counts and fee integers only. Tests fail if hex/txid/fingerprint fields exist. |
| T-137-04-02 | Information Disclosure | Knots alias leak | mitigate | Dashboard/status JSON uses Open Bitcoin names. Knots aliases stay on `getmempoolinfo` only. |
| T-137-04-03 | Spoofing | effective floor | mitigate | Mapper copies `effective_admission_fee_rate_sats_per_kvb` from ManagedMempoolInfo; test uses unequal static/rolling. |
| T-137-04-04 | Tampering | last-package echo | mitigate | Do not add package member tables to `OpenBitcoinNetworkStatusResponse`. |
</threat_model>

<verification>
Run the timed group tests and status-RPC tests, format `open-bitcoin-node` and `open-bitcoin-rpc` through `scripts/command-timings.ts`, and `git diff --check`.
</verification>

<success_criteria>
- D-10 resources and fee_floors exist on the shared snapshot type.
- D-11 four fee roles and three resource roles stay distinct.
- D-12 Knots aliases remain getmempoolinfo-only.
- D-07 no identifiers on the new groups.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-04-SUMMARY.md`
</output>
