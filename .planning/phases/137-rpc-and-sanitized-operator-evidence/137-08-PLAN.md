---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "08"
type: tdd
wave: 3
depends_on: ["05"]
files_modified:
  - packages/open-bitcoin-node/src/metrics.rs
  - packages/open-bitcoin-node/src/metrics/tests/contracts.rs
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/sync/metrics.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/inbound_metrics.rs
  - packages/open-bitcoin-node/src/logging.rs
  - packages/open-bitcoin-node/src/logging/tests/redaction.rs
autonomous: true
requirements: [MPOBS-02]
must_haves:
  truths:
    - "Fixed MetricKind values exist for virtual size, accounted usage, accounted capacity, the four fee floors, pressure removals, checkpoint overdue, recovery recovered count, and retry eligible/cleared."
    - "Structured logs use a new allowlisted source and snapshot-matching keys with no dynamic labels or 64-hex identities."
    - "New metric kinds are not bound to a ninth dashboard sparkline."
  artifacts:
    - path: packages/open-bitcoin-node/src/metrics.rs
      provides: "Low-cardinality MetricKind additions"
      contains: "MempoolVirtualSize"
    - path: packages/open-bitcoin-node/src/logging.rs
      provides: "Allowlisted mempool policy log source"
      contains: "MEMPOOL_POLICY_LOG_SOURCE"
  key_links:
    - from: packages/open-bitcoin-node/src/metrics.rs
      to: packages/open-bitcoin-node/src/status/mempool_groups.rs
      via: "as_str names match snapshot field stems"
      pattern: "mempool_virtual_size"
    - from: packages/open-bitcoin-rpc/src/bin/open_bitcoind/inbound_metrics.rs
      to: packages/open-bitcoin-node/src/metrics/mempool_policy.rs
      via: "persist_inbound_metrics_once extends samples"
      pattern: "mempool_policy_metric_samples"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Add fixed low-cardinality metrics and structured-log keys that match the snapshot group labels.

Purpose: MPOBS-02 requires the same vocabulary on metrics/logs as status, without dynamic labels or Knots aliases (D-07, D-12).
Output: `MetricKind` variants plus an allowlisted `mempool_policy` log record builder. No dashboard chart binding.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@packages/open-bitcoin-node/src/metrics.rs
@packages/open-bitcoin-node/src/metrics/tests/contracts.rs
@packages/open-bitcoin-node/src/logging.rs
@packages/open-bitcoin-node/src/logging/tests/redaction.rs
@packages/open-bitcoin-node/src/status/mempool_groups.rs
@docs/architecture/operator-observability.md
@standards/core/code-shape.md
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-node/src/metrics.rs:

```rust
pub enum MetricKind { /* ALL is [Self; 60] today — update length when adding */ }
impl MetricKind {
    pub const ALL: [Self; 60];
    pub const fn as_str(self) -> &amp;'static str;
}
```

From packages/open-bitcoin-node/src/logging.rs:

```rust
pub const RELAY_MEMPOOL_LOG_SOURCE: &amp;str = "relay_mempool";
pub fn relay_mempool_log_record(...) -> StructuredLogRecord;
```

`metrics.rs` is 599 lines and `logging.rs` is near the 628 gate. Split new kinds/keys into `metrics/mempool_policy.rs` and `logging/mempool_policy.rs` using `foo.rs` + `foo/` before adding.

Do not add `bytes`, `mempoolminfee`, `propagated`, or `public_relay` kinds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed MetricKind variants matching snapshot labels</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/metrics.rs</file>
    <file>packages/open-bitcoin-node/src/metrics/tests/contracts.rs</file>
    <file>packages/open-bitcoin-node/src/status/mempool_groups.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs</file>
    <file>standards/core/code-shape.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/metrics.rs, packages/open-bitcoin-node/src/metrics/mempool_policy.rs, packages/open-bitcoin-node/src/metrics/tests/contracts.rs, packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-node/src/sync/metrics.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/inbound_metrics.rs</files>
  <behavior>
    - New `MetricKind` variants exist with `as_str()` exactly: `mempool_virtual_size`, `mempool_accounted_usage`, `mempool_accounted_capacity`, `mempool_static_relay_floor`, `mempool_rolling_mempool_floor`, `mempool_effective_admission_floor`, `mempool_incremental_relay_fee`, `mempool_pressure_removal_count`, `mempool_checkpoint_overdue`, `mempool_recovery_recovered_count`, `mempool_retry_eligible`, `mempool_retry_cleared`, `mempool_admission_accepted`, `mempool_admission_still_present`.
    - `MetricKind::ALL` includes every new variant and its array length equals the new variant count.
    - None of `as_str()` values are `bytes`, `usage`, `maxmempool`, `mempoolminfee`, `broadcast`, or `propagated`.
    - Sampling maps from `MempoolStatus` Available groups; Unavailable groups omit the sample (do not record 0 as if it were a measurement).
    - `persist_inbound_metrics_once` and `DurableSyncRuntime::persist_metrics` invoke `mempool_policy_metric_samples` (no new collector).
    - Dashboard `MAX_DASHBOARD_CHARTS` is still 8 and no new kind is added to the sparkline substitution list.
  </behavior>
  <action>
    Per D-11 and D-12, add the variants and keep Phase 105 `Relay*` kinds unchanged (D-13). If `ALL` length updates break tests in `metrics/tests/contracts.rs`, update those expected name lists with the new snake_case strings.

    Add a pure `mempool_policy_metric_samples(mempool: &amp;MempoolStatus) -&gt; BTreeMap&lt;MetricKind, u64&gt;` in `metrics/mempool_policy.rs` that reads only Available groups.

    Wire that function into the existing metrics-collection call sites that already `samples.extend(...)` sibling families. Do not create a second collector:
    1. `persist_inbound_metrics_once` in `packages/open-bitcoin-rpc/src/bin/open_bitcoind/inbound_metrics.rs` — after `samples.extend(relay_metric_samples(&amp;relay, timestamp))`, extend with `mempool_policy_metric_samples` from `snapshot.mempool()` (Plan 04/05 `MempoolStatus` on `authoritative_operator_snapshot()`). Convert each `(MetricKind, u64)` to `MetricSample` with the same timestamp used for inbound/relay.
    2. `DurableSyncRuntime::persist_metrics` in `packages/open-bitcoin-node/src/sync/metrics.rs` — after the inbound/block_relay extends, if `self.network.operator_snapshot()` yields an available mempool, extend `samples` the same way.

    Re-export `mempool_policy_metric_samples` from `metrics.rs` via `pub use` and add it to the `open_bitcoin_node` `pub use metrics::{...}` list in `lib.rs` next to `relay_metric_samples`.

    Write failing tests named exactly:
    - `mempool_policy_metric_kinds_use_open_bitcoin_names`
    - `mempool_policy_metric_all_includes_new_kinds`
    - `mempool_policy_samples_skip_unavailable_groups`
    - `mempool_policy_kinds_are_not_dashboard_chart_slots`

    The last test can read `MAX_DASHBOARD_CHARTS` from the CLI crate only if that does not create a new production dependency; otherwise assert in rustdoc and a node-side comment plus `rg` in acceptance that `dashboard/model/metrics.rs` still has `= 8` and does not mention `MempoolVirtualSize`. Prefer a CLI test in this plan only if you add `packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs` to files_modified — do **not** add it; keep the chart assertion as `rg` in acceptance plus a node test that `as_str` names are not in a hardcoded chart-kind list copied from the existing eight names.

    Do not record identifiers as metric labels.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-08-metrics -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features mempool_policy_metric -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'MempoolVirtualSize' packages/open-bitcoin-node/src/metrics.rs` matches.</criterion>
    <criterion>`rg -n 'mempool_effective_admission_floor' packages/open-bitcoin-node/src/metrics.rs` matches.</criterion>
    <criterion>`rg -n 'mempoolminfee|"bytes"' packages/open-bitcoin-node/src/metrics.rs` returns no new kind strings.</criterion>
    <criterion>`rg -n 'MAX_DASHBOARD_CHARTS: usize = 8' packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs` matches.</criterion>
    <criterion>`rg -n 'mempool_policy_metric_samples' packages/open-bitcoin-rpc/src/bin/open_bitcoind/inbound_metrics.rs` matches.</criterion>
    <criterion>`rg -n 'mempool_policy_metric_samples' packages/open-bitcoin-node/src/sync/metrics.rs` matches.</criterion>
    <criterion>The named Cargo filter reports the metric-kind tests passing.</criterion>
  </acceptance-criteria>
  <done>Fixed MetricKind values match snapshot labels and stay off the eight-chart strip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add allowlisted mempool_policy structured log records</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/logging.rs</file>
    <file>packages/open-bitcoin-node/src/logging/tests/redaction.rs</file>
    <file>packages/open-bitcoin-node/src/metrics/mempool_policy.rs</file>
    <file>docs/architecture/operator-observability.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/logging.rs, packages/open-bitcoin-node/src/logging/mempool_policy.rs, packages/open-bitcoin-node/src/logging/tests/redaction.rs</files>
  <behavior>
    - `MEMPOOL_POLICY_LOG_SOURCE` equals `mempool_policy`.
    - `mempool_policy_log_record` builds a `StructuredLogRecord` whose `message` is a space-separated `key=value` list using only: `virtual_size`, `accounted_usage`, `accounted_capacity`, `static_relay_floor`, `rolling_mempool_floor`, `effective_admission_floor`, `incremental_relay_fee`, `pressure_removal_count`, `decay`, `recovered_count`, `retry_eligible`, `retry_cleared`, `admission_accepted`, `admission_still_present`, `admission_cleared`, `relay_disabled`.
    - `decay` values are only `half_life_12h` / `half_life_6h` / `half_life_3h` / `not_decaying`.
    - Rendering a record from a snapshot that includes a fake txid in a recovery record (if passed) still produces a message with no 64-hex substring because the function accepts groups/counts only, not `MempoolRecoveryRecord`.
    - Message must not contain `propagated`, `broadcast`, `mempoolminfee`, or `rebroadcast_deferred`.
  </behavior>
  <action>
    Per D-07 and D-12, add `logging/mempool_policy.rs` and a thin wrapper in `logging.rs`. Follow `relay_mempool_log_record` for level/timestamp shape. Do not log free-text reject reasons.

    Write failing tests named exactly:
    - `mempool_policy_log_source_is_fixed`
    - `mempool_policy_log_message_uses_snapshot_keys`
    - `mempool_policy_log_message_has_no_txid_hex`
    - `mempool_policy_log_forbids_propagation_and_knots_alias_keys`

    Wire sampling into the existing metrics/log status projection path only if a single obvious collector already emits `relay_mempool` logs from `operator_snapshot`. If no such hook is one function, export the builder and call it from the same site that already writes `RELAY_MEMPOOL_LOG_SOURCE` records — grep `relay_mempool_log_record` and add a sibling call. Do not create a second log filesystem.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-08-logs -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features mempool_policy_log -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub const MEMPOOL_POLICY_LOG_SOURCE' packages/open-bitcoin-node/src/logging.rs` matches or the child module is re-exported from logging.rs.</criterion>
    <criterion>`rg -n 'fn mempool_policy_log_record' packages/open-bitcoin-node/src/logging` matches.</criterion>
    <criterion>`rg -n 'rebroadcast_deferred' packages/open-bitcoin-node/src/logging/mempool_policy.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the log tests passing.</criterion>
  </acceptance-criteria>
  <done>Structured logs emit allowlisted snapshot keys with no identities or propagation words.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Snapshot groups → metrics/logs | Shared evidence that may be scraped or shipped off-box. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-08-01 | Information Disclosure | dynamic metric labels | mitigate | High: fixed `MetricKind` only. No txid/wtxid labels. |
| T-137-08-02 | Information Disclosure | log message | mitigate | Allowlisted keys; test rejects 64-hex and reject-reason strings. |
| T-137-08-03 | Spoofing | Knots alias metrics | mitigate | No `mempoolminfee` / `bytes` kind names. |
| T-137-08-04 | Tampering | retry vs rebroadcast | mitigate | Do not add a metric that aliases `RelayRebroadcastDeferredCount`. |
</threat_model>

<verification>
Run the timed metrics and log suites. Format `open-bitcoin-node` through `scripts/command-timings.ts`. `git diff --check`.
</verification>

<success_criteria>
- D-07/D-12 metrics and logs use Open Bitcoin snapshot names.
- No ninth chart binding.
- No identifier cardinality.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-08-SUMMARY.md`
</output>
