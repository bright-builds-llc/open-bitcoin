---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "05"
type: tdd
wave: 2
depends_on: ["04"]
files_modified:
  - packages/open-bitcoin-node/src/status/mempool_groups.rs
  - packages/open-bitcoin-node/src/status.rs
  - packages/open-bitcoin-node/src/status/tests/mempool_groups.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
autonomous: true
requirements: [MPOBS-02, MPOBS-03]
must_haves:
  truths:
    - "MempoolStatus has distinct pressure, eviction, checkpoint, recovery, retry, and admission groups that are identifier-free."
    - "Retry counters are not folded into rebroadcast_deferred_count; pressure_removal_count is not evicted_count."
    - "Recovery group copies ManagedMempoolRecoverySummary counts only and never serializes MempoolRecoveryRecord.txid."
  artifacts:
    - path: packages/open-bitcoin-node/src/status/mempool_groups.rs
      provides: "Remaining D-10 groups plus admission aggregates"
      contains: "MempoolRetryGroup"
    - path: packages/open-bitcoin-node/src/network/types.rs
      provides: "Operator snapshot fields for unpublished evidence"
      contains: "checkpoint"
  key_links:
    - from: packages/open-bitcoin-node/src/status/mempool_groups.rs
      to: packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
      via: "count-only recovery mapping"
      pattern: "recovered_count"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Add the remaining identifier-free snapshot groups: pressure, eviction, checkpoint, recovery, retry, and admission aggregates.

Purpose: MPOBS-02/03 need distinct pressure vs relay-eviction vs retry vs recovery, without publishing recovery txids (D-08, D-13, D-15).
Output: Groups on `MempoolStatus` filled from authority-owned evidence through `ManagedNetworkOperatorSnapshot`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-04-PLAN.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@packages/open-bitcoin-node/src/status/mempool_groups.rs
@packages/open-bitcoin-node/src/network/lifecycle_projection.rs
@packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
@packages/open-bitcoin-node/src/network/recovery.rs
@packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/status/relay_evidence.rs
@standards/core/testing.md

<interfaces>
Plan 04 already added `resources` and `fee_floors`. Extend `mempool_groups.rs` only.

From packages/open-bitcoin-node/src/network/lifecycle_projection.rs:

```rust
pub(super) struct LifecycleEvidenceSnapshot {
    pub(super) pressure_removals: u64,
    pub(super) retry_clears: u64,
    // plus other removal counters
}
```

From packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs:

```rust
pub struct CheckpointEvidenceSnapshot {
    pub outcome: CheckpointOutcome, // NeverAttempted, Pending, Succeeded, Failed
    pub overdue: bool,
    pub maybe_persistence_strength: Option&lt;CheckpointPersistenceStrength&gt;, // Sync
    pub checkpoint_age_seconds: Option&lt;u64&gt;,
    pub maybe_loss_bound_seconds: Option&lt;u64&gt;,
    pub maybe_dirty_generation: Option&lt;u64&gt;,
}
```

From packages/open-bitcoin-node/src/network/recovery.rs:

```rust
pub struct ManagedMempoolRecoverySummary {
    pub recovered_count: u64,
    pub dropped_confirmed_count: u64,
    pub dropped_duplicate_count: u64,
    pub dropped_missing_parent_count: u64,
    pub dropped_policy_incompatible_count: u64,
    pub dropped_expired_count: u64,
    pub dropped_evicted_count: u64,
    pub records: Vec&lt;MempoolRecoveryRecord&gt;, // DO NOT serialize
}
```

From packages/open-bitcoin-node/src/status/relay_evidence.rs:

```rust
pub struct RelayEvidenceCounters { pub evicted_count: u64, pub rebroadcast_deferred_count: u64, /* ... */ }
```

Decay labels (UI-SPEC / research A1): `half_life_12h`, `half_life_6h`, `half_life_3h`, `not_decaying`.
Occupancy rule from `fee/rolling.rs`: usage &lt; capacity/4 → 3h; usage &lt; capacity/2 → 6h; else 12h; if decay gate closed → `not_decaying`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pressure, eviction, checkpoint, and recovery group types</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/status/mempool_groups.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/recovery.rs</file>
    <file>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs</file>
    <file>packages/open-bitcoin-mempool/src/fee/rolling.rs</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/status/mempool_groups.rs, packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/status/tests/mempool_groups.rs</files>
  <behavior>
    - `MempoolPressureGroup` has `pressure_removal_count: u64` and `decay_half_life_label` which is only one of `half_life_12h` / `half_life_6h` / `half_life_3h` / `not_decaying`. No ETA field.
    - `MempoolEvictionGroup` has `pressure_removal_count` as its own field AND documents that relay `evicted_count` is read from `relay.outcome_counters` (D-13). The group must not copy `rebroadcast_deferred_count`.
    - `MempoolCheckpointGroup` has `outcome` (`never_attempted` / `pending` / `succeeded` / `failed`), `overdue: bool`, `persistence_strength` (`sync` or `unavailable`), `age_seconds: Option&lt;u64&gt;`, `loss_bound_seconds: Option&lt;u64&gt;`, `dirty_generation_present: bool`. No generation integer is published (identifier-adjacent). `dirty_generation_present` is `maybe_dirty_generation.is_some()`.
    - `MempoolRecoveryGroup` has the seven count fields from `ManagedMempoolRecoverySummary` and no `records`, `txid`, or `wtxid` field. Serde of the group JSON string must not match `[0-9a-f]{64}`.
    - `recovery_group_from_summary` ignores `.records` even when records contain txids.
  </behavior>
  <action>
    Per D-10, D-13, and D-08, add the four structs and mappers on `mempool_groups.rs`. Add matching `FieldAvailability` fields on `MempoolStatus` with the same unavailable default as Plan 04.

    ```rust
    pub fn decay_half_life_label(usage: usize, capacity: usize, decay_gate_open: bool) -&gt; &amp;'static str;
    pub fn recovery_group_from_summary(summary: &amp;ManagedMempoolRecoverySummary) -&gt; MempoolRecoveryGroup;
    pub fn checkpoint_group_from_evidence(evidence: &amp;CheckpointEvidenceSnapshot) -&gt; MempoolCheckpointGroup;
    ```

    `CheckpointEvidenceSnapshot` is currently authority-visible. If it is not crate-public, add a crate-public identifier-free DTO in `mempool_groups.rs` and map from a new public snapshot accessor in Task 2 rather than leaking `LifecycleEvidenceSnapshot` out of `pub(super)`.

    Write failing tests named exactly:
    - `decay_label_is_not_decaying_when_gate_closed`
    - `decay_label_is_3h_when_usage_below_quarter_capacity`
    - `recovery_group_from_summary_drops_txid_records`
    - `checkpoint_group_exposes_dirty_generation_present_not_generation_id`
    - `pressure_group_json_has_no_evicted_count_or_rebroadcast_deferred`

    Do not add retry/admission yet (Task 2). Do not edit dashboard.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-05-pressure -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features recovery_group_from_summary -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub struct MempoolPressureGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'pub struct MempoolCheckpointGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'pub struct MempoolRecoveryGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'records|txid' packages/open-bitcoin-node/src/status/mempool_groups.rs` returns no field names (comments forbidding txids are allowed).</criterion>
    <criterion>The named Cargo filter reports the recovery mapping test passing.</criterion>
  </acceptance-criteria>
  <done>Pressure, eviction, checkpoint, and recovery groups are count/label-only and drop recovery txids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add retry and admission aggregates and publish all new groups</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/status/mempool_groups.rs</file>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-node/src/network/types.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/node.rs</file>
    <file>packages/open-bitcoin-node/src/status/relay_evidence.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/status/mempool_groups.rs, packages/open-bitcoin-node/src/status.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/types.rs, packages/open-bitcoin-rpc/src/dispatch/node.rs, packages/open-bitcoin-node/src/status/tests/mempool_groups.rs</files>
  <behavior>
    - `MempoolRetryGroup` has u64 fields `eligible`, `queued`, `attempted`, `emitted`, `requested`, `served`, `suppressed`, `relay_disabled`, `cleared`.
    - `MempoolAdmissionGroup` has u64 fields `accepted`, `still_present`, `cleared` (D-15).
    - `eligible` is `unbroadcast_members.len()` as u64. `cleared` is `LifecycleEvidenceSnapshot.retry_clears`. `requested` / `served` / `suppressed` copy the existing relay outcome counters of those names. `relay_disabled` is 1 when `ManagedNetworkInfo.relay == false` else 0, or a count of locally accepted members while relay is disabled if that count is already available — prefer the boolean-as-0/1 plus still-present local unbroadcast when relay is off, documented in rustdoc.
    - `queued` / `attempted` / `emitted`: inventory live fanout fields first (`queued_transactions`, leftover unattempted, TX INV drain counts). Map existing counters; do not invent a second authority. If a counter does not exist, publish `0` and rustdoc `not_yet_counted` is forbidden — use `0` with rustdoc "no separate live counter; reported as zero until a later phase owns it" only if grep confirms absence. Prefer leftover_unattempted → not queued. If `relay_fanout_info().queued_transactions` exists, that is `queued`.
    - Publishing these groups must not increment `rebroadcast_deferred_count` (D-13).
    - `ManagedNetworkOperatorSnapshot` gains identifier-free copies of checkpoint, recovery counts, lifecycle pressure/retry_clears, unbroadcast len, and admission aggregates. `operator_snapshot()` fills them under the existing read guard. Do not store `MempoolRecoveryRecord` on the snapshot.
    - `open_bitcoin_network_status` copies the new groups onto `response.mempool`.
    - JSON for retry/admission contains none of `propagated`, `broadcast`, `public_relay` (D-18).
  </behavior>
  <action>
    Per D-10, D-13, D-15, and D-17, add `MempoolRetryGroup` and `MempoolAdmissionGroup`. Extend `ManagedNetworkOperatorSnapshot` in `types.rs` with new `pub(super)` identifier-free fields and getters. Fill them in `ManagedPeerNetwork::operator_snapshot`. If checkpoint/recovery/lifecycle evidence is not reachable from `self` today, add the smallest public-in-crate accessors on the authority state that already owns `CheckpointEvidenceSnapshot` and the last `ManagedMempoolRecoverySummary` (default empty counts if never recovered).

    `EligibleServe` must not increment `cleared` (D-17). `cleared` is retry_clears / TransportWritten+LifecycleRemoval evidence only.

    Write failing tests named exactly:
    - `retry_group_does_not_use_rebroadcast_deferred_count`
    - `admission_group_has_accepted_still_present_cleared_only`
    - `recovery_snapshot_field_has_no_txid_hex`
    - `operator_snapshot_retry_eligible_equals_unbroadcast_len`
    - `openbitcoinnetworkstatus_includes_retry_and_admission_groups`

    The last test may live in the rpc crate if it needs dispatch; if so add `packages/open-bitcoin-rpc/src/dispatch/tests/network_status.rs` to this plan's files and keep it identifier-free.

    If `types.rs` or `network.rs` would exceed 500/628 lines, split snapshot assembly into `network/operator_snapshot.rs` using `foo.rs` + `foo/`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-05-retry -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features retry_group_does_not_use -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub struct MempoolRetryGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'pub struct MempoolAdmissionGroup' packages/open-bitcoin-node/src/status/mempool_groups.rs` matches.</criterion>
    <criterion>`rg -n 'rebroadcast_deferred' packages/open-bitcoin-node/src/status/mempool_groups.rs` returns no matches.</criterion>
    <criterion>`rg -n 'propagated|public_relay' packages/open-bitcoin-node/src/status/mempool_groups.rs` returns no matches.</criterion>
    <criterion>`rg -n 'MempoolRecoveryRecord' packages/open-bitcoin-node/src/network/types.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the retry-group test passing.</criterion>
  </acceptance-criteria>
  <done>All seven D-10 groups plus admission aggregates are published as identifier-free counts; retry is distinct from rebroadcast_deferred.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Recovery records → snapshot | Records contain txids and must not cross into shared evidence. |
| Lifecycle evidence → status RPC | Authenticated status is still not an originating package response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-05-01 | Information Disclosure | recovery mapping | mitigate | High: `recovery_group_from_summary` copies counts only. Test includes records with txids and asserts JSON has no 64-hex string. |
| T-137-05-02 | Information Disclosure | checkpoint generation | mitigate | Publish `dirty_generation_present` bool, not the generation integer. |
| T-137-05-03 | Tampering | counter overload | mitigate | D-13: retry ≠ `rebroadcast_deferred_count`; pressure ≠ `evicted_count`. |
| T-137-05-04 | Spoofing | EligibleServe as clear | mitigate | D-17: `cleared` maps `retry_clears` only. |
| T-137-05-05 | Spoofing | propagation fields | mitigate | D-18: no `propagated` / `broadcast` / `public_relay` on new groups. |
</threat_model>

<verification>
Run the timed recovery and retry suites, then `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features decay_label -- --test-threads=1` through `scripts/command-timings.ts`. Format node and rpc crates. `git diff --check`.
</verification>

<success_criteria>
- D-10 remaining groups exist.
- D-13 retry and pressure stay distinct from Phase 105 counters.
- D-08 recovery txids never appear.
- D-15 admission aggregates exist as counts.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-05-SUMMARY.md`
</output>
