---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "06"
type: tdd
wave: 3
depends_on: ["03"]
files_modified:
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/package.rs
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/package.rs
  - packages/open-bitcoin-rpc/src/package_projection.rs
  - packages/open-bitcoin-rpc/src/package_projection/tests.rs
  - packages/open-bitcoin-cli/src/operator.rs
  - packages/open-bitcoin-cli/src/operator/package.rs
  - packages/open-bitcoin-cli/src/operator/runtime.rs
  - packages/open-bitcoin-cli/src/operator/tests/routing.rs
autonomous: true
requirements: [MPOBS-01, MPOBS-03]
must_haves:
  truths:
    - "openbitcoinpackage is MethodOrigin::OpenBitcoinExtension with required mode dry-run or submit and deny_unknown_fields."
    - "open-bitcoin package dry-run|submit calls openbitcoinpackage and prints input-ordered members, fingerprint, and dual-state tokens without claiming public relay."
    - "open-bitcoin-cli continues to forward testmempoolaccept and submitpackage after Plan 03 registration; it does not grow an Open Bitcoin package parser."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/method/package.rs
      provides: "Extension request/response types"
      contains: "OpenBitcoinPackageRequest"
    - path: packages/open-bitcoin-cli/src/operator/package.rs
      provides: "clap package dry-run/submit"
      contains: "PackageCommand"
  key_links:
    - from: packages/open-bitcoin-cli/src/operator/package.rs
      to: packages/open-bitcoin-rpc/src/method/package.rs
      via: "HTTP JSON-RPC method openbitcoinpackage"
      pattern: "openbitcoinpackage"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Expose the typed Open Bitcoin package report on `openbitcoinpackage` and `open-bitcoin package {dry-run,submit}`.

Purpose: D-04 keeps Knots objects clean while operators get fingerprint, fee groups, and dual-state on an explicit extension path.
Output: One extension method with `mode`, projector for the typed DTO, and clap/runtime wiring. Dual-state tokens are present; relay-disabled proof is completed in Plan 10.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-03-PLAN.md
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-cli/src/operator.rs
@packages/open-bitcoin-cli/src/operator/runtime.rs
@packages/open-bitcoin-cli/src/operator/wallet.rs
@packages/open-bitcoin-rpc/src/package_projection.rs
@standards/core/testing.md

<interfaces>
Plan 03 already registered BaselineParity methods. Add `OpenBitcoinPackage` to the extension arm of `origin()`.

Discretion (A1): one method `openbitcoinpackage` with required `mode` = `dry-run` | `submit`.

UI-SPEC originating copy:
- dry-run heading `Package dry-run` plus `Dry-run does not change mempool, relay, persistence, or evidence state.`
- submit heading `Package submit` plus `Local admission only. This is not public or default relay and not network-wide propagation.`
- Admission tokens: `accepted`, `still-present`, `cleared`
- Relay tokens: `eligible`, `queued`, `attempted`, `emitted`, `requested`, `served`, `suppressed`, `relay_disabled`

From packages/open-bitcoin-cli/src/operator.rs:

```rust
pub enum OperatorCommand { /* add Package(PackageArgs) */ }
```

Do not add dashboard keys. Do not change sendrawtransaction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add openbitcoinpackage method, typed projector, and dispatch</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/method.rs</file>
    <file>packages/open-bitcoin-rpc/src/method/package.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/package.rs</file>
    <file>packages/open-bitcoin-rpc/src/package_projection.rs</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/method.rs, packages/open-bitcoin-rpc/src/method/package.rs, packages/open-bitcoin-rpc/src/dispatch.rs, packages/open-bitcoin-rpc/src/dispatch/package.rs, packages/open-bitcoin-rpc/src/package_projection.rs, packages/open-bitcoin-rpc/src/package_projection/tests.rs</files>
  <behavior>
    - `SupportedMethod::from_name("openbitcoinpackage")` is `Some` with `origin() == OpenBitcoinExtension` and `scope() == Node`.
    - `OpenBitcoinPackageRequest` has `mode: OpenBitcoinPackageMode` (`dry-run` / `submit`), `rawtxs: Vec&lt;String&gt;`, `deny_unknown_fields`. Missing `mode` fails deserialize.
    - `mode=dry-run` calls `dry_run_local_package` and does not change `getmempoolinfo.size`.
    - `mode=submit` calls `submit_local_package`.
    - `project_open_bitcoin_package` returns a JSON object with `fingerprint` (hex), `status` (`complete` / `partial` / `failed`), input-ordered `members` (each with `txid`, `wtxid`, `result` variant name, `admission`, `relay`), and `effective_fee_groups`. It is a different function from the Knots projectors (D-05 family, not D-02 pollution).
    - Dual-state on each member uses exactly the UI-SPEC tokens. Admission mapping is the same on dry-run and submit, from `PackageMemberResult` only: `FinallyPresent` → `accepted`, `AlreadyPresent` → `still-present`, rejected/absent (`HardRejected`, `Reconsiderable`, `SameTxidDifferentWitness`, `PostTrimAbsent`) → `cleared`.
    - Dry-run applies no lifecycle or fanout. Dry-run `relay` is `relay_disabled` when `RelayIntent::NotRequested` or `ManagedNetworkInfo.relay` is false, else `eligible` if the member is `FinallyPresent` or `AlreadyPresent`, else `suppressed`.
    - Submit `relay` is mapped from `MempoolLifecycleDelta` plus current unbroadcast/fanout/serving receipts after submit (D-15, D-18), not only `RelayIntent` / `network.relay`. See Task 1 action for the token order.
    - Typed JSON must not contain keys `propagated`, `broadcast`, or `public_relay`.
  </behavior>
  <action>
    Per D-04, D-05, and D-15, register `OpenBitcoinPackage` in every `SupportedMethod` match (`all`, `name`, `origin`, `MethodCall`, `normalize`). Normalize names: `["mode", "rawtxs"]`.

    Add:

    ```rust
    pub fn project_open_bitcoin_package(
        report: &amp;PackageReport,
        member_facts: &amp;[PackageMemberProjectionFacts],
        member_dual_state: &amp;[PackageMemberDualState],
    ) -&gt; Result&lt;serde_json::Value, PackageProjectionError&gt;;

    pub struct PackageMemberDualState {
        pub admission: PackageAdmissionState, // Accepted, StillPresent, Cleared — serde rename still-present
        pub relay: PackageRelayState, // Eligible, Queued, Attempted, Emitted, Requested, Served, Suppressed, RelayDisabled
    }
    ```

    Serde tokens must be `accepted`, `still-present`, `cleared`, `eligible`, `queued`, `attempted`, `emitted`, `requested`, `served`, `suppressed`, `relay_disabled`.

    Dispatch builds dual-state from the report plus, on submit, `MempoolLifecycleDelta` and current unbroadcast/fanout/serving facts. `RelayIntent` / `snapshot.network().relay` only decide `relay_disabled` versus the transport ladder. Do not echo this object from `openbitcoinnetworkstatus` (D-07).

    Submit `relay` token order (first match wins; inventory the same live fields Plan 05 uses — do not invent a second fanout authority):
    1. `RelayIntent::NotRequested` or `ManagedNetworkInfo.relay == false` → `relay_disabled`
    2. member identity is in `relay_fanout` queue (`queued_relay_ids_for_peer` / `TxFanoutQueue` / `relay_fanout_info().queued_transactions` membership) → `queued`
    3. leftover-unattempted or attempt started with no TX INV drain for this member → `attempted`
    4. this submit's TX INV drain / announced receipt for the member → `emitted`
    5. getdata/request receipt for the member → `requested`
    6. `EligibleServe` classification or served receipt for the member → `served` (D-17: never rewrite admission to `cleared`)
    7. fanout suppression for the member → `suppressed`
    8. else `FinallyPresent` / `AlreadyPresent` / `delta.admitted` / current unbroadcast membership → `eligible`
    9. else → `suppressed`

    `TransportWritten` or `LifecycleRemoval` on `delta.retry_clears` / `delta.removed` may accompany admission `cleared` only when the report variant is rejected/absent. They must not rewrite `AlreadyPresent` → `cleared`.

    Write failing tests named exactly:
    - `openbitcoinpackage_is_open_bitcoin_extension`
    - `openbitcoinpackage_dry_run_does_not_change_mempool_size`
    - `openbitcoinpackage_dry_run_already_present_is_still_present`
    - `project_open_bitcoin_package_includes_fingerprint_and_dual_state`
    - `project_open_bitcoin_package_omits_propagation_keys`
    - `openbitcoinpackage_unknown_field_is_rejected`

    Reuse Plan 03 package dispatch fixtures.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-06-extension -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features openbitcoinpackage -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'OpenBitcoinPackage' packages/open-bitcoin-rpc/src/method.rs` matches.</criterion>
    <criterion>`rg -n 'OpenBitcoinExtension' packages/open-bitcoin-rpc/src/method.rs` matches a match-arm that includes `OpenBitcoinPackage`.</criterion>
    <criterion>`rg -n 'pub fn project_open_bitcoin_package' packages/open-bitcoin-rpc/src/package_projection.rs` matches.</criterion>
    <criterion>`rg -n 'still-present|relay_disabled' packages/open-bitcoin-rpc/src/package_projection.rs` matches.</criterion>
    <criterion>`rg -n 'openbitcoinpackage_dry_run_already_present_is_still_present' packages/open-bitcoin-rpc/src` matches.</criterion>
    <criterion>`rg -n 'propagated|public_relay' packages/open-bitcoin-rpc/src/package_projection.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the extension tests passing.</criterion>
  </acceptance-criteria>
  <done>Typed extension RPC projects fingerprint, ordered members, and dual-state from the same PackageReport family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add open-bitcoin package dry-run and submit CLI</name>
  <read-first>
    <file>packages/open-bitcoin-cli/src/operator.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/runtime.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/wallet.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/http.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/tests/routing.rs</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-cli/src/operator.rs, packages/open-bitcoin-cli/src/operator/package.rs, packages/open-bitcoin-cli/src/operator/runtime.rs, packages/open-bitcoin-cli/src/operator/tests/routing.rs</files>
  <behavior>
    - `route_cli_invocation("open-bitcoin", ["package", "dry-run", "--hex", "00"])` parses `OperatorCommand::Package` with mode dry-run.
    - `package submit --hex &lt;a&gt; --hex &lt;b&gt;` parses submit with two hex strings.
    - Human render for dry-run starts with `Package dry-run` and includes `Dry-run does not change mempool, relay, persistence, or evidence state.`
    - Human render for submit starts with `Package submit` and includes `Local admission only. This is not public or default relay and not network-wide propagation.`
    - Human/JSON output may include fingerprint and member txids (originating response). JSON is the extension RPC result, not a second schema.
    - Help/text must not contain `propagated`, `broadcast`, `guaranteed`, or `public relay ready`.
    - `open-bitcoin-cli` path is unchanged: `validate_supported_method` already forwards `testmempoolaccept` / `submitpackage` once they are in `SupportedMethod`. Do not add a package subcommand to `open-bitcoin-cli`.
  </behavior>
  <action>
    Per D-04 and UI-SPEC, add `pub mod package;` and `Package(PackageArgs)` on `OperatorCommand`. Clap shape:

    ```rust
    pub struct PackageArgs { pub command: PackageCommand }
    pub enum PackageCommand { DryRun(PackageHexArgs), Submit(PackageHexArgs) }
    pub struct PackageHexArgs { pub hex: Vec&lt;String&gt; } // clap --hex, required 1..=25
    ```

    Execute via the existing authenticated HTTP client pattern in `operator/wallet.rs` / `HttpStatusRpcClient`, calling method `openbitcoinpackage` with `mode` and `rawtxs`. Add `execute_package_command` in `package.rs` and a match arm in `runtime.rs` plus the detect-skip list that currently enumerates every `OperatorCommand`.

    Write failing tests in `operator/tests/routing.rs` and a new `operator/package/tests.rs` if needed, named exactly:
    - `package_dry_run_parses_hex_flags`
    - `package_submit_parses_multiple_hex_flags`
    - `package_human_dry_run_includes_non_mutating_disclaimer`
    - `package_human_submit_includes_local_admission_disclaimer`
    - `package_copy_forbids_propagation_words`

    Render tests can call a pure `render_package_human(mode, value: &amp;Value) -&gt; String` so they do not need a live node.

    File-header breadcrumb on `operator/package.rs`: Knots `bitcoin-cli.cpp` + `rpc/mempool.cpp`. Do not edit `source-breadcrumbs.json`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-06-cli -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features package_ -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'Package\\(PackageArgs\\)|enum PackageCommand' packages/open-bitcoin-cli/src/operator.rs` matches.</criterion>
    <criterion>`rg -n 'openbitcoinpackage' packages/open-bitcoin-cli/src/operator/package.rs` matches.</criterion>
    <criterion>`rg -n 'Dry-run does not change mempool' packages/open-bitcoin-cli/src/operator/package.rs` matches.</criterion>
    <criterion>`rg -n 'Local admission only' packages/open-bitcoin-cli/src/operator/package.rs` matches.</criterion>
    <criterion>`rg -n 'propagated|guaranteed propagation|public relay ready' packages/open-bitcoin-cli/src/operator/package.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the package CLI tests passing.</criterion>
  </acceptance-criteria>
  <done>open-bitcoin package dry-run/submit is the extension workflow; baseline names stay on open-bitcoin-cli.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Operator CLI → extension RPC | Authenticated cookie/Basic already required; hex is untrusted input. |
| Typed report → human stdout | Identifiers allowed only on this originating command (D-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-06-01 | Tampering | dry-run mode | mitigate | High: `mode=dry-run` must call `dry_run_local_package`. Test asserts mempool size unchanged. |
| T-137-06-02 | Information Disclosure | status confusion | mitigate | Do not write the typed report onto `OpenBitcoinNetworkStatusResponse`. |
| T-137-06-03 | Spoofing | success copy | mitigate | UI-SPEC disclaimers required; forbid propagation words. |
| T-137-06-04 | Denial of Service | `--hex` arity | mitigate | Clap / RPC count 1..=25 before decode. |
| T-137-06-05 | Elevation of Privilege | unknown fields | mitigate | `deny_unknown_fields` on the extension request. |
</threat_model>

<verification>
Run the timed rpc extension suite and the timed CLI package suite. Format `open-bitcoin-rpc` and `open-bitcoin-cli` through `scripts/command-timings.ts`. `git diff --check`.
</verification>

<success_criteria>
- D-04 extension RPC and `open-bitcoin package` exist.
- D-05 typed projection shares the PackageReport family.
- D-01 baseline names remain Knots-only on `open-bitcoin-cli`.
- D-18 originating copy has no propagation claim.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-06-SUMMARY.md`
</output>
