---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "09"
type: tdd
wave: 3
depends_on: ["05"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/support/redaction.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/render/relay.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs
autonomous: true
requirements: [MPOBS-02, MPOBS-03]
must_haves:
  truths:
    - "Support bundles remain shareable after recursive redaction and never copy txids, wtxids, package hashes, raw hex, peer ids, endpoints, or credentials from the new groups."
    - "Markdown includes count-only policy bullets and the locked next-action sentence that local admission is not public or default relay."
    - "Counts need no redaction; any free-text group field sanitizes to redacted_relay_mempool_evidence."
  artifacts:
    - path: packages/open-bitcoin-cli/src/operator/support/redaction.rs
      provides: "Redaction of new mempool groups"
      contains: "redact_mempool_policy_groups"
    - path: packages/open-bitcoin-cli/src/operator/support/render/relay.rs
      provides: "Support Markdown policy bullets"
      contains: "Next action: Treat package"
  key_links:
    - from: packages/open-bitcoin-cli/src/operator/support/redaction.rs
      to: packages/open-bitcoin-node/src/status.rs
      via: "support_status_for_bundle mutates snapshot.mempool groups"
      pattern: "redact_mempool_policy_groups"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Project the new snapshot groups into redacted support bundles as count-only Markdown bullets.

Purpose: D-08 shareable bundles must not grow identifier leaks when recovery/retry groups are added.
Output: Extended `support_status_for_bundle` plus UI-SPEC support copy. No dashboard work.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@packages/open-bitcoin-cli/src/operator/support/redaction.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/support/render/relay.rs
@packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs
@docs/architecture/operator-observability.md
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-cli/src/operator/support/redaction.rs:

```rust
pub(crate) fn support_status_for_bundle(mut status: OpenBitcoinStatusSnapshot) -> OpenBitcoinStatusSnapshot {
    redact_relay_mempool_evidence(&amp;mut status.mempool.relay);
    // add redact_mempool_policy_groups(&amp;mut status.mempool);
    status
}
```

From packages/open-bitcoin-cli/src/operator/support/render/relay.rs:

```rust
// after Mempool bullet, before Relay evidence
```

UI-SPEC support next action (exact):

`Next action: Treat package, pressure, checkpoint, recovery, and retry evidence as bounded local operator status. Successful local admission is not public or default relay and is not network-wide propagation.`

Omitted list already mentions raw transaction hex, txids, wtxids. Append `package fingerprints` to that omitted string if it is not already covered.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redact new mempool groups on the support snapshot</name>
  <read-first>
    <file>packages/open-bitcoin-cli/src/operator/support/redaction.rs</file>
    <file>packages/open-bitcoin-node/src/status/mempool_groups.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/support/tests.rs</file>
    <file>docs/architecture/operator-observability.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-cli/src/operator/support/redaction.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <behavior>
    - `support_status_for_bundle` calls `redact_mempool_policy_groups`.
    - Count fields are unchanged after redaction.
    - If a group `Unavailable` / free-text `reason` contains a 64-hex string or a peer endpoint, the reason becomes `redacted_relay_mempool_evidence` (reuse `REDACTED_RELAY_MEMPOOL_LABEL`).
    - Recursive redaction that already drops 64-hex from the serialized bundle still drops them after groups are added. A fixture snapshot whose recovery unavailable reason is a txid hex must not appear in `serde_json::to_string` of the redacted snapshot.
    - `redaction_summary().omitted` includes the substring `package fingerprints`.
  </behavior>
  <action>
    Per D-08, add `redact_mempool_policy_groups(mempool: &amp;mut MempoolStatus)` in `redaction.rs`. Walk `FieldAvailability::Unavailable { reason }` on resources, fee_floors, pressure, eviction, checkpoint, recovery, retry, and admission. Leave `Available` count structs untouched.

    Write failing tests named exactly:
    - `support_bundle_redaction_keeps_retry_counts`
    - `support_bundle_redaction_strips_txid_from_group_unavailable_reason`
    - `support_bundle_omitted_list_mentions_package_fingerprints`
    - `support_bundle_json_has_no_64_hex_after_poisoned_recovery_reason`

    Do not add Markdown bullets in this task.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-09-redact -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features support_bundle_redaction -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn redact_mempool_policy_groups' packages/open-bitcoin-cli/src/operator/support/redaction.rs` matches.</criterion>
    <criterion>`rg -n 'package fingerprints' packages/open-bitcoin-cli/src/operator/support/redaction.rs` matches.</criterion>
    <criterion>`rg -n 'redact_mempool_policy_groups' packages/open-bitcoin-cli/src/operator/support/redaction.rs` matches inside `support_status_for_bundle`.</criterion>
    <criterion>The named Cargo filter reports the redaction tests passing.</criterion>
  </acceptance-criteria>
  <done>Support snapshot redaction keeps counts and strips identifier-bearing free text from new groups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add count-only support Markdown policy bullets</name>
  <read-first>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>packages/open-bitcoin-cli/src/operator/support/render.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/support/render/relay.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/src/operator/support/render/relay.rs, packages/open-bitcoin-cli/src/operator/support/tests/forensics_recovery_relay.rs</files>
  <behavior>
    - Markdown inserts bullets after the existing `Mempool:` bullet and before `Relay evidence:` using Open Bitcoin labels (`Virtual size`, `Accounted usage`, … `Admission states`, `Relay states`) and the same value formats as UI-SPEC.
    - The exact next-action sentence from UI-SPEC appears once in the Markdown body.
    - Markdown does not contain `propagated`, `guaranteed`, `mempoolminfee`, or a 64-hex string when groups are Available counts.
    - Existing `Mempool: transactions=` and `Relay evidence:` bullets remain.
    - `Recovery` (mempool recovery counts) is distinct from `Relay recovery`.
  </behavior>
  <action>
    Per D-08, D-12, and UI-SPEC, add `mempool_policy_support_bullets` in `support/render/relay.rs` or a new `support/render/mempool_policy.rs` (`foo.rs` + `foo/`). Call it from `render_support_markdown` / the relay render helper in the required order.

    Update `forensics_recovery_relay.rs` fixtures that assert exact Markdown line lists so they include the new bullets and next-action line.

    Write failing tests named exactly:
    - `support_markdown_inserts_virtual_size_before_relay_evidence`
    - `support_markdown_includes_locked_next_action`
    - `support_markdown_forbids_propagation_and_knots_alias_labels`

    No volatile tables. No pasted JSON-RPC bodies.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-09-markdown -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features support_markdown_inserts_virtual_size -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'Next action: Treat package, pressure, checkpoint, recovery, and retry evidence' packages/open-bitcoin-cli/src/operator/support` matches.</criterion>
    <criterion>`rg -n 'Virtual size' packages/open-bitcoin-cli/src/operator/support/render` matches.</criterion>
    <criterion>`rg -n 'mempoolminfee' packages/open-bitcoin-cli/src/operator/support/render` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the Markdown order test passing.</criterion>
  </acceptance-criteria>
  <done>Support Markdown is identifier-free, count-only, and states that local admission is not public relay.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Operator snapshot → shareable zip/dir | Bundles leave the machine. Identifiers and secrets must not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-09-01 | Information Disclosure | recovery/retry groups | mitigate | High: counts only; poisoned-reason test forbids 64-hex in bundle JSON. |
| T-137-09-02 | Information Disclosure | credentials | mitigate | Existing omitted list stays; do not copy cookie/auth. |
| T-137-09-03 | Spoofing | support next action | mitigate | Locked sentence forbids public/default relay and propagation claims. |
| T-137-09-04 | Information Disclosure | package fingerprints | mitigate | Add to omitted list; groups must not carry fingerprint fields. |
</threat_model>

<verification>
Run the timed redaction and Markdown suites. Format `open-bitcoin-cli` through `scripts/command-timings.ts`. `git diff --check`.
</verification>

<success_criteria>
- D-08 bundles stay shareable and identifier-free.
- D-12 Open Bitcoin labels on support Markdown.
- D-18 next-action copy is present.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-09-SUMMARY.md`
</output>
