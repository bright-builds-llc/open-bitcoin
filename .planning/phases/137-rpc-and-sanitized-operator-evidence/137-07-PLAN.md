---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "07"
type: tdd
wave: 3
depends_on: ["05"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status.rs
  - packages/open-bitcoin-cli/src/operator/status/render.rs
  - packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs
autonomous: true
requirements: [MPOBS-02, MPOBS-03]
must_haves:
  truths:
    - "CLI collector copies openbitcoinnetworkstatus.mempool groups instead of discarding fee/resource fields."
    - "Human status and dashboard Mempool and Wallet rows use the locked UI-SPEC labels after Mempool and before Relay evidence."
    - "MAX_DASHBOARD_CHARTS stays 8; no new chart kind is added for the seven groups."
  artifacts:
    - path: packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs
      provides: "Human policy lines"
      contains: "Virtual size"
    - path: packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs
      provides: "Dashboard rows from snapshot groups"
      contains: "Admission states"
  key_links:
    - from: packages/open-bitcoin-cli/src/operator/status.rs
      to: packages/open-bitcoin-rpc/src/method/node.rs
      via: "network_status.mempool copy"
      pattern: "network_status.mempool"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Render the shared snapshot groups on human/JSON status and the Ratatui dashboard using the Phase 137 UI-SPEC.

Purpose: Operators must see Open Bitcoin labels, not Knots aliases, without a ninth chart or last-package table (D-12, D-14, D-07).
Output: Collector copy + locked rows/lines. No metrics/logs/support in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md
@packages/open-bitcoin-cli/src/operator/status.rs
@packages/open-bitcoin-cli/src/operator/status/render.rs
@packages/open-bitcoin-cli/src/operator/status/render/relay.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs
@packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs
@docs/architecture/status-snapshot.md
@standards/core/frontend-ui.md
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-cli/src/operator/status.rs (today):

```rust
mempool: MempoolStatus {
    transactions: FieldAvailability::available(saturating_usize_to_u64(mempool_info.size)),
    relay,
}
```

Replace live collection with `network_status.mempool` when that object has Available groups; if `mempool.resources` is unavailable, keep `transactions` from `getmempoolinfo.size` and `relay` from `network_status.relay` so Phase 105 still works.

From packages/open-bitcoin-cli/src/operator/status/render.rs:

```rust
// after Mempool: line, before relay_evidence_lines
```

From packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs:

```rust
row("Mempool", ...),
// INSERT UI-SPEC rows here
row("Relay evidence", ...),
```

Locked labels and formats are in 137-UI-SPEC.md "Locked dashboard and human-status labels". Use those strings exactly.

Stopped-node: every new line is `Unavailable: {reason}` using the same reason as `mempool.transactions`.

`MAX_DASHBOARD_CHARTS = 8` in `dashboard/model/metrics.rs` — do not change it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy snapshot groups in the collector and render human policy lines</name>
  <read-first>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>packages/open-bitcoin-cli/src/operator/status.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/render.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/render/relay.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/tests.rs</file>
    <file>packages/open-bitcoin-node/src/status.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-cli/src/operator/status.rs, packages/open-bitcoin-cli/src/operator/status/render.rs, packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <behavior>
    - Live collector sets `snapshot.mempool` from `network_status.mempool` (including resources, fee_floors, pressure, eviction, checkpoint, recovery, retry, admission) and still keeps `relay` from that object.
    - Stopped collector sets every new group to `FieldAvailability::Unavailable` with the same `reason` string used for `mempool.transactions`.
    - `render_human_status` inserts lines after `Mempool:` and before the first `Relay evidence:` line, using exact labels: `Virtual size`, `Accounted usage`, `Accounted capacity`, `Static relay floor`, `Rolling mempool floor`, `Effective admission floor`, `Incremental relay fee`, `Pressure removals`, `Eviction (relay)`, `Checkpoint`, `Recovery`, `Retry`, `Admission states`, `Relay states`.
    - Available resource lines use `{n} vbytes` / `{n} accounted bytes`. Fee lines use `{n} sat/kvB`. No line uses `bytes`, `maxmempool`, or `mempoolminfee` as a label.
    - JSON status is `serde_json::to_string_pretty(snapshot)` unchanged except that the snapshot now contains the groups (no last-package member table).
    - Existing Phase 105 labels `Relay evidence`, `Rebroadcast: deferred`, `Public relay` remain later in the file via `relay_evidence_lines`.
  </behavior>
  <action>
    Per D-10, D-12, D-14, and UI-SPEC, split human policy rendering into `status/render/mempool_policy.rs` so `render.rs` stays under 628 lines. Insert `lines.extend(mempool_policy_lines(&amp;snapshot.mempool));` immediately after the `Mempool:` push.

    Collector: if `OpenBitcoinNetworkStatusResponse.mempool` is present (Plan 04/05), assign it. Preserve `getmempoolinfo` only as fallback for `transactions` when the status mempool transactions field is unavailable.

    Write failing tests named exactly:
    - `human_status_inserts_virtual_size_before_relay_evidence`
    - `human_status_uses_accounted_usage_not_knots_bytes_label`
    - `stopped_status_policy_lines_share_transactions_unavailable_reason`
    - `json_status_has_mempool_fee_floors_and_no_last_package_members`
    - `human_status_forbids_mempoolminfee_label`

    Do not add dashboard rows in this task.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-07-human -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features human_status_inserts_virtual_size -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'mempool_policy_lines' packages/open-bitcoin-cli/src/operator/status/render.rs` matches.</criterion>
    <criterion>`rg -n 'Virtual size' packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs` matches.</criterion>
    <criterion>`rg -n 'network_status.mempool' packages/open-bitcoin-cli/src/operator/status.rs` matches.</criterion>
    <criterion>`rg -n 'mempoolminfee' packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the insert-order test passing.</criterion>
  </acceptance-criteria>
  <done>Human/JSON status shows locked policy lines from the shared snapshot and copies groups from the status RPC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert dashboard rows in Mempool and Wallet without a ninth chart</name>
  <read-first>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/dashboard/model.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs</file>
    <file>standards/core/frontend-ui.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-cli/src/operator/dashboard/model/relay.rs, packages/open-bitcoin-cli/src/operator/dashboard/model.rs, packages/open-bitcoin-cli/src/operator/dashboard/model/tests.rs</files>
  <behavior>
    - `mempool_and_wallet_rows` inserts the same locked labels as human status after `Mempool` and before `Relay evidence`, then keeps existing relay/wallet/`block_relay` rows.
    - Row values match UI-SPEC formats (`key=value` pairs for pressure/checkpoint/recovery/retry/admission/relay states).
    - `MAX_DASHBOARD_CHARTS` remains `8`. No new `MetricKind` is bound to a sparkline in this plan.
    - Unavailable groups render `Unavailable: {reason}` in the value cell, not a blank row drop.
    - Empty mempool (0 transactions) still shows policy rows with zero counts when groups are Available with zeros.
    - No new dashboard keybinding is added. Colors of new rows stay default body (not Cyan/Green).
  </behavior>
  <action>
    Per D-14 and UI-SPEC, reuse `mempool_policy` formatters from the CLI status module if that avoids duplicating label strings — export `pub(crate)` format helpers from `mempool_policy.rs` and call them from `dashboard/model/relay.rs`. Do not create a second DTO.

    Write failing tests named exactly:
    - `dashboard_rows_place_virtual_size_after_mempool_before_relay_evidence`
    - `dashboard_rows_include_admission_states_and_relay_states`
    - `dashboard_does_not_add_a_ninth_chart_kind`
    - `dashboard_row_labels_are_open_bitcoin_not_knots_aliases`

    The chart test must `assert_eq!` `MAX_DASHBOARD_CHARTS` is 8 and that no new sparkline kind name for `fee_floors` / `retry` exists in `dashboard/model/metrics.rs`.

    If `relay.rs` would exceed 500 lines, split policy rows into `dashboard/model/mempool_policy.rs` using `foo.rs` + `foo/`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-07-dashboard -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features dashboard_rows_place_virtual_size -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'Virtual size' packages/open-bitcoin-cli/src/operator/dashboard/model` matches.</criterion>
    <criterion>`rg -n 'Admission states' packages/open-bitcoin-cli/src/operator/dashboard/model` matches.</criterion>
    <criterion>`rg -n 'MAX_DASHBOARD_CHARTS' packages/open-bitcoin-cli/src/operator/dashboard/model/metrics.rs` matches a `= 8` assignment.</criterion>
    <criterion>`rg -n 'mempoolminfee' packages/open-bitcoin-cli/src/operator/dashboard/model` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the row-order test passing.</criterion>
  </acceptance-criteria>
  <done>Dashboard shows locked policy rows inside Mempool and Wallet; eight charts stay unchanged.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Status RPC → CLI snapshot | Authenticated collector must not treat status as an originating package response. |
| Snapshot → terminal | Human/dashboard output is shareable over a shoulder; identifiers forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-07-01 | Information Disclosure | last-package echo | mitigate | High: JSON test asserts no member table / fingerprint field on snapshot.mempool. |
| T-137-07-02 | Information Disclosure | Knots alias confusion | mitigate | Labels are UI-SPEC Open Bitcoin names only. |
| T-137-07-03 | Spoofing | color-as-propagation | mitigate | No new Green/Cyan meaning for accepted/retry rows. |
| T-137-07-04 | Tampering | renderer-local summary | mitigate | Formatters read snapshot groups only; collector copies daemon groups. |
</threat_model>

<verification>
Run the timed human and dashboard suites. Format `open-bitcoin-cli` through `scripts/command-timings.ts`. `git diff --check`.
</verification>

<success_criteria>
- D-12/D-14 UI-SPEC rows exist; 8 charts unchanged.
- D-07 no last-package table on status/dashboard.
- Collector no longer drops fee/resource groups.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-07-SUMMARY.md`
</output>
