---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "03"
type: tdd
wave: 2
depends_on: ["01", "02"]
files_modified:
  - packages/open-bitcoin-rpc/src/error.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/package.rs
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/package.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests/package_methods.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
autonomous: true
requirements: [MPOBS-01]
must_haves:
  truths:
    - "testmempoolaccept and submitpackage are SupportedMethod BaselineParity node methods with Knots 29.3 parameter names."
    - "RPC-level errors are only empty/count (code -8), hex/decode (code -22), and submitpackage topology (code -25); member policy stays in the result body."
    - "Dispatch calls dry_run_local_package for testmempoolaccept and submit_local_package for submitpackage, then Plan 01 projectors."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/method/package.rs
      provides: "Knots request types"
      contains: "TestMempoolAcceptRequest"
    - path: packages/open-bitcoin-rpc/src/dispatch/package.rs
      provides: "Decode, count, topology, project, dispatch"
      contains: "test_mempool_accept"
  key_links:
    - from: packages/open-bitcoin-rpc/src/dispatch/package.rs
      to: packages/open-bitcoin-rpc/src/package_projection.rs
      via: "project_testmempoolaccept / project_submitpackage"
      pattern: "project_testmempoolaccept"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Register and dispatch Knots-named `testmempoolaccept` and `submitpackage` as BaselineParity methods.

Purpose: Existing scripts keep working (D-01) while policy outcomes stay in the result body and dry-run cannot mutate (D-03).
Output: Method enum entries, Knots error codes -8/-22/-25, and dispatch that uses Plan 01 projectors plus Plan 02 handle methods.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-01-PLAN.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-02-PLAN.md
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-rpc/src/error.rs
@packages/open-bitcoin-rpc/src/dispatch.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/open-bitcoin-rpc/src/package_projection.rs
@packages/bitcoin-knots/src/rpc/mempool.cpp
@packages/bitcoin-knots/src/rpc/protocol.h
@standards/core/testing.md

<interfaces>
Plan 01 (must call, not reimplement):

```rust
pub fn project_testmempoolaccept(report: &amp;PackageReport, member_facts: &amp;[PackageMemberProjectionFacts]) -> Result&lt;Value, PackageProjectionError&gt;;
pub fn project_submitpackage(report: &amp;PackageReport, member_facts: &amp;[PackageMemberProjectionFacts], replaced_txids: &amp;[Txid]) -> Result&lt;Value, PackageProjectionError&gt;;
```

Plan 02 (must call, not reimplement):

```rust
impl ManagedNetworkHandle {
    pub fn dry_run_local_package(...) -> Result&lt;PackageReport, ManagedNetworkAuthorityError&gt;;
    pub fn submit_local_package(...) -> Result&lt;SubmittedPackageResult, ManagedNetworkAuthorityError&gt;;
}
```

From packages/open-bitcoin-rpc/src/error.rs (extend):

```rust
pub enum RpcErrorCode { /* add InvalidParameter = -8, DeserializationError = -22, VerifyError = -25 */ }
impl RpcFailure {
    pub fn verify_rejected(message: impl Into&lt;String&gt;) -> Self; // existing -26
}
```

Do not add `getrawmempool` (D-09). Do not add Open Bitcoin keys to Knots result objects (D-02). Do not change `SendRawTransactionResponse` (D-16, Plan 10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Knots error codes and BaselineParity method registration</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/error.rs</file>
    <file>packages/open-bitcoin-rpc/src/method.rs</file>
    <file>packages/open-bitcoin-rpc/src/method/node.rs</file>
    <file>packages/open-bitcoin-rpc/src/method/tests.rs</file>
    <file>packages/bitcoin-knots/src/rpc/protocol.h</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/node.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/error.rs, packages/open-bitcoin-rpc/src/method.rs, packages/open-bitcoin-rpc/src/method/package.rs, packages/open-bitcoin-rpc/src/method/tests.rs</files>
  <behavior>
    - `RpcErrorCode::InvalidParameter.as_i32() == -8`, `DeserializationError == -22`, `VerifyError == -25`. `TryFrom&lt;i32&gt;` round-trips those three values.
    - `RpcFailure::invalid_parameter(msg)` uses kind `InvalidParams` and detail code `-8`.
    - `RpcFailure::deserialization_error(msg)` uses kind `InvalidParams` and detail code `-22`.
    - `RpcFailure::verify_error(msg)` uses kind `InvalidParams` and detail code `-25`.
    - `SupportedMethod::from_name("testmempoolaccept")` is `Some(TestMempoolAccept)` with `origin() == BaselineParity`, `scope() == Node`, `name() == "testmempoolaccept"`.
    - `SupportedMethod::from_name("submitpackage")` is `Some(SubmitPackage)` with the same origin/scope and `name() == "submitpackage"`.
    - `SupportedMethod::all()` includes both new variants. Existing `method/tests.rs` name-list tests still pass after updating expected lists.
    - `TestMempoolAcceptRequest` deserializes Knots names `rawtxs`, optional `maxfeerate`, optional `maxburnamount`, optional `ignore_rejects` with `deny_unknown_fields`.
    - `SubmitPackageRequest` deserializes Knots names `package`, optional `maxfeerate`, optional `maxburnamount`, optional `ignore_rejects` with `deny_unknown_fields`.
  </behavior>
  <action>
    Per D-01, add `method/package.rs` and `mod package; pub use package::*;` from `method.rs` beside `mod node`. Add enum variants `TestMempoolAccept` / `SubmitPackage` with serde renames `testmempoolaccept` / `submitpackage`. Extend `all()`, `name()`, `MethodCall`, `MethodCall::scope`, and `normalize_method_call`.

    Positional names for normalize:
    - testmempoolaccept: `["rawtxs", "maxfeerate", "maxburnamount", "ignore_rejects"]`
    - submitpackage: `["package", "maxfeerate", "maxburnamount", "ignore_rejects"]`

    Add `RpcFailure` constructors above. Update `as_i32` and `TryFrom&lt;i32&gt;`.

    Write failing tests in `method/tests.rs` (or a new `method/package/tests.rs` if that file would exceed 500 lines), named exactly:
    - `rpc_error_codes_include_knots_minus_eight_twenty_two_twenty_five`
    - `testmempoolaccept_is_baseline_parity_node_method`
    - `submitpackage_is_baseline_parity_node_method`
    - `package_requests_deny_unknown_fields`

    Do not implement dispatch in this task beyond `normalize_method_call` mapping to `MethodCall`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-03-register -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features rpc_error_codes_include_knots -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'TestMempoolAccept' packages/open-bitcoin-rpc/src/method.rs` matches.</criterion>
    <criterion>`rg -n 'SubmitPackage' packages/open-bitcoin-rpc/src/method.rs` matches.</criterion>
    <criterion>`rg -n 'InvalidParameter' packages/open-bitcoin-rpc/src/error.rs` matches.</criterion>
    <criterion>`rg -n 'DeserializationError' packages/open-bitcoin-rpc/src/error.rs` matches.</criterion>
    <criterion>`rg -n 'VerifyError' packages/open-bitcoin-rpc/src/error.rs` matches.</criterion>
    <criterion>`rg -n 'getrawmempool' packages/open-bitcoin-rpc/src/method.rs` returns no matches.</criterion>
  </acceptance-criteria>
  <done>Knots method names and -8/-22/-25 codes exist; getrawmempool is not added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch decode, count, topology, project, and handle calls</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/dispatch.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/node.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/decode.rs</file>
    <file>packages/open-bitcoin-rpc/src/package_projection.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/tests/transaction_methods.rs</file>
    <file>packages/bitcoin-knots/src/rpc/mempool.cpp</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/dispatch.rs, packages/open-bitcoin-rpc/src/dispatch/package.rs, packages/open-bitcoin-rpc/src/dispatch/tests/package_methods.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <behavior>
    - Empty `rawtxs` / `package` or length `> 25` returns RPC error code `-8` and message containing `Array must contain between 1 and 25 transactions`.
    - Invalid hex or `parse_transaction` failure returns code `-22`.
    - Explicit `Some(maxfeerate)`, `Some(maxburnamount)`, or non-empty `ignore_rejects` returns `-8` (or existing `InvalidParams` `-32602` only if you reuse the sendrawtransaction fail-closed helpers) with a stable message that those options are unsupported. Omitted / default-empty is compatible.
    - `submitpackage` with a well-formed hex list that is not child-with-unconfirmed-parents (`SubmissionPackage::try_from_package` / `PackageShape` topology) returns code `-25`.
    - `testmempoolaccept` calls `dry_run_local_package` then `project_testmempoolaccept`. After a successful dry-run of a valid tx, `getmempoolinfo.size` is unchanged.
    - `submitpackage` calls `submit_local_package` then `project_submitpackage`. Success JSON is an object with `package_msg` and `tx-results` and no Open Bitcoin keys.
    - Member policy rejection is HTTP 200 JSON-RPC success with `allowed=false` / `error` in the body, not `-26` / `-25`.
  </behavior>
  <action>
    Per D-01, D-03, and Pitfall 9, add `dispatch/package.rs` and match arms in `dispatch.rs` only (do not grow `dispatch/node.rs`). Reuse `decode::decode_hex` and `parse_transaction` from the sendrawtransaction path. Sample time with existing `current_timestamp_unix_seconds`. Resolve `RelayIntent` the same way local sendrawtransaction does (activation-enabled → `Requested`, else `NotRequested`).

    Build `PackageMemberProjectionFacts` from the decoded transactions (txid/wtxid via `transaction_txid` / `transaction_wtxid`, virtual size from the transaction, base fee from the admission report fee group or from input minus output values if the report group is absent for AlreadyPresent). Fail closed with internal error if facts cannot be built for a FinallyPresent member.

    `replaced_txids` for submitpackage come from `delta` replacement removals only (txid list), not from pressure evictions.

    Write failing tests in `dispatch/tests/package_methods.rs` using the same `empty_context` / spendable-chain fixtures as `transaction_methods.rs`, named exactly:
    - `testmempoolaccept_empty_array_is_invalid_parameter_minus_eight`
    - `testmempoolaccept_twenty_six_is_invalid_parameter_minus_eight`
    - `testmempoolaccept_bad_hex_is_deserialization_minus_twenty_two`
    - `testmempoolaccept_dry_run_does_not_change_getmempoolinfo_size`
    - `submitpackage_non_child_with_parents_is_verify_error_minus_twenty_five`
    - `submitpackage_success_is_knots_object_without_open_bitcoin_keys`
    - `testmempoolaccept_policy_reject_is_result_body_not_rpc_error`
    - `package_methods_fail_closed_on_explicit_maxfeerate`

    Do not add `openbitcoinpackage` (Plan 06). Do not touch `send_raw_transaction`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-03-dispatch -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features testmempoolaccept_ -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn test_mempool_accept|fn submit_package' packages/open-bitcoin-rpc/src/dispatch/package.rs` matches both handlers.</criterion>
    <criterion>`rg -n 'project_testmempoolaccept|project_submitpackage' packages/open-bitcoin-rpc/src/dispatch/package.rs` matches.</criterion>
    <criterion>`rg -n 'dry_run_local_package|submit_local_package' packages/open-bitcoin-rpc/src/dispatch/package.rs` matches.</criterion>
    <criterion>`rg -n 'MethodCall::TestMempoolAccept|MethodCall::SubmitPackage' packages/open-bitcoin-rpc/src/dispatch.rs` matches.</criterion>
    <criterion>`rg -n 'getrawmempool' packages/open-bitcoin-rpc/src` returns no matches in method.rs or dispatch.rs.</criterion>
    <criterion>The named Cargo filter reports the Task 2 testmempoolaccept tests passing.</criterion>
  </acceptance-criteria>
  <done>BaselineParity package RPCs decode, fail closed on count/decode/topology, project Knots JSON, and keep dry-run non-mutating.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON-RPC client → package methods | Untrusted hex arrays and optional Knots fee args. |
| Dispatch → mempool | Only after count/decode/shape gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03-01 | Denial of Service | rawtxs/package arrays | mitigate | High: reject empty and `> 25` with `-8` before decode of a 26th tx. Weight/shape still enforced by `WellFormedPackage`. |
| T-137-03-02 | Tampering | dry-run dispatch | mitigate | High: testmempoolaccept must call `dry_run_local_package` only. Test asserts getmempoolinfo.size unchanged. |
| T-137-03-03 | Information Disclosure | Knots result identifiers | accept | D-06: originating authenticated response may include wtxid keys. Do not copy onto status RPC. |
| T-137-03-04 | Elevation of Privilege | ignore_rejects / max fee | mitigate | Fail closed on explicit non-default Knots options (Pitfall 9). |
| T-137-03-05 | Spoofing | topology vs policy | mitigate | Topology is `-25`; member policy stays in-body so clients cannot confuse RPC failure with accept. |
</threat_model>

<verification>
Run the timed register test, then the timed dispatch testmempoolaccept filter plus `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features submitpackage_ -- --test-threads=1` through `scripts/command-timings.ts`. Format the rpc crate and `git diff --check`.
</verification>

<success_criteria>
- D-01 BaselineParity methods exist with Knots names and error boundaries.
- D-02 no extra keys on Knots objects.
- D-03 dry-run does not mutate.
- D-09 getrawmempool is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-03-SUMMARY.md`
</output>
