---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "11"
type: execute
wave: 5
depends_on: ["07", "08", "09", "10"]
files_modified:
  - docs/parity/source-breadcrumbs.json
  - docs/parity/catalog/rpc-cli-config.md
  - docs/parity/service-operation-expectations.md
  - docs/architecture/operator-observability.md
  - docs/architecture/status-snapshot.md
  - docs/operator/runtime-guide.md
  - docs/parity/index.json
  - README.md
autonomous: true
requirements: [MPOBS-01, MPOBS-02, MPOBS-03]
must_haves:
  truths:
    - "Parity catalog and operator docs describe Knots testmempoolaccept/submitpackage plus openbitcoinpackage without public/default relay or production-readiness claims."
    - "UAT examples include both cargo run --manifest-path packages/Cargo.toml and bazel run //packages/... command forms."
    - "Every new first-party Rust source from Plans 01-10 is registered in source-breadcrumbs.json."
  artifacts:
    - path: docs/parity/catalog/rpc-cli-config.md
      provides: "Package RPC/CLI catalog rows"
      contains: "testmempoolaccept"
    - path: docs/parity/source-breadcrumbs.json
      provides: "Breadcrumb groups for new Phase 137 sources"
      contains: "package_projection.rs"
  key_links:
    - from: docs/parity/catalog/rpc-cli-config.md
      to: packages/open-bitcoin-rpc/src/dispatch/package.rs
      via: "documented BaselineParity vs extension split"
      pattern: "openbitcoinpackage"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Register Phase 137 operator surfaces in parity docs, breadcrumbs, and UAT command forms without broadening v2.2 claims.

Purpose: Contributors need auditable Knots anchors and copy-pasteable Cargo/Bazel commands; Phase 138 owns adversarial/claim-guardrail closeout.
Output: Catalog, observability, status-snapshot, runtime-guide, breadcrumb, and README freshness updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-RESEARCH.md
@docs/parity/catalog/rpc-cli-config.md
@docs/parity/service-operation-expectations.md
@docs/architecture/operator-observability.md
@docs/architecture/status-snapshot.md
@docs/operator/runtime-guide.md
@docs/parity/source-breadcrumbs.json
@docs/parity/index.json
@README.md
@packages/open-bitcoin-cli/BUILD.bazel
@standards/core/verification.md

<interfaces>
Verified Bazel labels (do not invent):

```text
//packages/open-bitcoin-rpc:open_bitcoind
//packages/open-bitcoin-cli:open_bitcoin
//packages/open-bitcoin-cli:open_bitcoin_cli
```

Cargo forms (AGENTS.md):

```text
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind -- ...
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin-cli -- ...
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- ...
```

Intentional difference to catalog (research A3): Open Bitcoin `submitpackage` does not throw a Knots `BroadcastTransaction` failure after accept. Fanout stays local transport facts.

Do not add `getrawmempool`. Do not claim public/default/production relay, guaranteed propagation, or production-readiness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register breadcrumbs and catalog the package RPC split</name>
  <read-first>
    <file>docs/parity/source-breadcrumbs.json</file>
    <file>docs/parity/catalog/rpc-cli-config.md</file>
    <file>docs/parity/index.json</file>
    <file>docs/parity/service-operation-expectations.md</file>
    <file>scripts/check-parity-breadcrumbs.ts</file>
    <file>packages/open-bitcoin-cli/BUILD.bazel</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-RESEARCH.md</file>
  </read-first>
  <files>docs/parity/source-breadcrumbs.json, docs/parity/catalog/rpc-cli-config.md, docs/parity/index.json, docs/parity/service-operation-expectations.md</files>
  <action>
    Per D-01, D-04, and repo breadcrumb rules, append groups to `source-breadcrumbs.json` for every new first-party Rust source created in this phase. Confirm the live tree with `rg --files packages/open-bitcoin-rpc/src/package_projection packages/open-bitcoin-rpc/src/method/package.rs packages/open-bitcoin-rpc/src/dispatch/package.rs packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs packages/open-bitcoin-node/src/status/mempool_groups.rs packages/open-bitcoin-cli/src/operator/package.rs packages/open-bitcoin-cli/src/operator/status/render/mempool_policy.rs` (include any `foo/` children executors actually added).

    Knots-anchored groups:
    - RPC projector/dispatch/method package files → `packages/bitcoin-knots/src/rpc/mempool.cpp`, `packages/bitcoin-knots/src/rpc/protocol.h`
    - `local_package.rs` → `mempool.cpp` plus `packages/bitcoin-knots/src/validation.cpp`
    - `operator/package.rs` → `packages/bitcoin-knots/src/bitcoin-cli.cpp` plus `mempool.cpp`

    `none` groups (use repo `noneReason` text):
    - `status/mempool_groups.rs`, CLI `mempool_policy` renderers, metrics/logging mempool_policy modules, support changes that are Open Bitcoin-only

    In `docs/parity/catalog/rpc-cli-config.md` add rows that state:
    1. `testmempoolaccept` / `submitpackage` are BaselineParity Knots 29.3 names and result shapes
    2. `openbitcoinpackage` / `open-bitcoin package` is the typed extension
    3. `getmempoolinfo` aliases stay RPC-only
    4. `getrawmempool` is out of this phase
    5. submitpackage does not throw Knots broadcast errors (intentional difference)
    6. no public/default relay claim

    Add UAT command blocks to `service-operation-expectations.md` using **both** Cargo and Bazel forms for:
    - `open-bitcoind` with `-server=1`
    - `open-bitcoin-cli ... testmempoolaccept '["&lt;hex&gt;"]'`
    - `open-bitcoin-cli ... submitpackage '["&lt;hex&gt;"]'`
    - `open-bitcoin ... package dry-run --hex '&lt;hex&gt;'`
    - `open-bitcoin ... package submit --hex '&lt;hex&gt;'`
    - `open-bitcoin ... status --format json`

    Update `docs/parity/index.json` only if that file already lists RPC catalog entries for v2.2 — add a Phase 137 in_progress pointer, do not mark MPVFY complete.

    Do not run `bash scripts/verify.sh`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-11-breadcrumbs -- bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'package_projection.rs' docs/parity/source-breadcrumbs.json` matches.</criterion>
    <criterion>`rg -n 'testmempoolaccept' docs/parity/catalog/rpc-cli-config.md` matches.</criterion>
    <criterion>`rg -n 'openbitcoinpackage' docs/parity/catalog/rpc-cli-config.md` matches.</criterion>
    <criterion>`rg -n 'bazel run //packages/open-bitcoin-cli:open_bitcoin_cli' docs/parity/service-operation-expectations.md` matches.</criterion>
    <criterion>`rg -n 'cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --' docs/parity/service-operation-expectations.md` matches a `package dry-run` example.</criterion>
    <criterion>`rg -n 'getrawmempool' docs/parity/catalog/rpc-cli-config.md` matches only a sentence that it is out of Phase 137.</criterion>
    <criterion>`rg -n 'production-ready|guaranteed propagation|public/default relay is enabled' docs/parity/catalog/rpc-cli-config.md` returns no matches that assert those claims as true.</criterion>
  </acceptance-criteria>
  <done>Breadcrumbs and RPC catalog describe the dual surfaces and UAT command forms without claim inflation.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh operator docs and contributor README wording</name>
  <read-first>
    <file>docs/architecture/operator-observability.md</file>
    <file>docs/architecture/status-snapshot.md</file>
    <file>docs/operator/runtime-guide.md</file>
    <file>README.md</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>standards/core/verification.md</file>
  </read-first>
  <files>docs/architecture/operator-observability.md, docs/architecture/status-snapshot.md, docs/operator/runtime-guide.md, README.md</files>
  <action>
    Per D-10, D-12, D-15, and D-18, update:
    - `status-snapshot.md` mempool field ownership to list `resources`, `fee_floors`, `pressure`, `eviction`, `checkpoint`, `recovery`, `retry`, `admission` beside existing `transactions` / `relay`. State that identifiers stay off this model.
    - `operator-observability.md` with the new `MetricKind` / `mempool_policy` log source and the support next-action sentence. Repeat that status RPC is not an originating package response.
    - `runtime-guide.md` with `open-bitcoin package dry-run|submit` and baseline `open-bitcoin-cli` method names, using Cargo and Bazel forms. Include the dry-run non-mutating sentence and the submit local-admission disclaimer from UI-SPEC.

    README: only refresh sentences that would otherwise say package RPC is absent or that operator evidence is still Phase 105-only. Do not announce production relay or Phase 138 closeout. If README has no package-RPC sentence, add one short status line under the existing operator/RPC section.

    Forbidden words in the new paragraphs: `propagated`, `guaranteed propagation`, `production node ready`, `public relay ready`.

    Do not add a hosted dashboard. Do not run `bash scripts/verify.sh`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-11-docs -- rg -n "openbitcoinpackage|mempool.fee_floors|package dry-run" docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/operator/runtime-guide.md README.md</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fee_floors|MempoolFeeFloorsGroup|resources' docs/architecture/status-snapshot.md` matches.</criterion>
    <criterion>`rg -n 'mempool_policy' docs/architecture/operator-observability.md` matches.</criterion>
    <criterion>`rg -n 'package dry-run' docs/operator/runtime-guide.md` matches.</criterion>
    <criterion>`rg -n 'bazel run //packages/open-bitcoin-cli:open_bitcoin' docs/operator/runtime-guide.md` matches.</criterion>
    <criterion>`rg -n 'propagated|guaranteed propagation|production node ready' docs/architecture/status-snapshot.md docs/architecture/operator-observability.md docs/operator/runtime-guide.md README.md` returns no matches in newly added Phase 137 paragraphs (existing historical mentions of the phrase as a forbidden claim may remain only if they are explicitly "must not claim").</criterion>
  </acceptance-criteria>
  <done>Operator docs and README describe redacted snapshot groups and package workflows with Cargo/Bazel UAT forms and bounded claim wording.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Docs → operators | Wording is treated as product truth. Claim inflation is a spoofing risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-11-01 | Spoofing | catalog/README claims | mitigate | High: forbid public/default/production relay and guaranteed-propagation as achieved facts. Catalog the Knots broadcast-throw difference as intentional. |
| T-137-11-02 | Information Disclosure | UAT examples | mitigate | Use placeholder `&lt;hex&gt;` not live mainnet txids or credentials. |
| T-137-11-03 | Tampering | breadcrumb drift | mitigate | Register every new Rust source; run `check-parity-breadcrumbs.ts`. |
| T-137-11-04 | Elevation of Privilege | Phase 138 scope | accept | No claim-guardrail mutation tests here; Phase 138 owns MPVFY-04. |
</threat_model>

<verification>
Run the timed breadcrumb checker and the docs rg verification. Do not run `bash scripts/verify.sh`. `git diff --check` on the edited docs.
</verification>

<success_criteria>
- D-01/D-04 surfaces are documented with both CLI binaries.
- D-12/D-18 claim wording is bounded.
- New Rust sources have breadcrumb registry entries.
- UAT commands include Cargo and Bazel forms.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-11-SUMMARY.md`
</output>
