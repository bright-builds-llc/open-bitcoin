---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "10"
type: tdd
wave: 4
depends_on: ["05", "06", "07"]
files_modified:
  - packages/open-bitcoin-rpc/src/dispatch/tests/transaction_methods.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests/package_methods.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests/dual_state.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
autonomous: true
requirements: [MPOBS-03]
must_haves:
  truths:
    - "sendrawtransaction success JSON stays the existing {txid_hex, replaced_txids, evicted_txids} object with no admission or relay dual-state fields."
    - "A relay-disabled local package submit can be accepted and still-present while member relay is relay_disabled, and shared aggregates increment those counts without a propagated field."
    - "openbitcoinnetworkstatus and operator JSON status do not contain last-package member tables or fingerprints."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/dispatch/tests/dual_state.rs
      provides: "Relay-disabled dual-state and status-echo proofs"
      contains: "relay_disabled"
    - path: packages/open-bitcoin-rpc/src/method/node.rs
      provides: "Unchanged SendRawTransactionResponse"
      contains: "txid_hex"
  key_links:
    - from: packages/open-bitcoin-rpc/src/dispatch/tests/dual_state.rs
      to: packages/open-bitcoin-rpc/src/package_projection.rs
      via: "openbitcoinpackage typed report"
      pattern: "openbitcoinpackage"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Prove the two independent dual-state axes and freeze BaselineParity `sendrawtransaction` as a non-propagation object.

Purpose: D-15 through D-18 are easy to break by decorating Knots success JSON or echoing the last package on status.
Output: Regression tests. Production types change only if Plan 06 dual-state wiring is incomplete — prefer tests over new fields.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-06-PLAN.md
@packages/open-bitcoin-rpc/src/method/node.rs
@packages/open-bitcoin-rpc/src/dispatch/node.rs
@packages/open-bitcoin-rpc/src/dispatch/tests/transaction_methods.rs
@packages/open-bitcoin-rpc/src/method/package.rs
@standards/core/testing.md

<interfaces>
From packages/open-bitcoin-rpc/src/method/node.rs (must stay this shape):

```rust
pub struct SendRawTransactionResponse {
    pub txid_hex: String,
    pub replaced_txids: Vec&lt;String&gt;,
    pub evicted_txids: Vec&lt;String&gt;,
}
```

D-16: do not add dual-state or propagation fields. Do not rewrite this into a Knots hex string in this phase (research A2).

D-17: `EligibleServe` is not membership-clear evidence. Assert `cleared` does not increase when a serve classification happens without `TransportWritten` / `LifecycleRemoval` if such a fixture exists; otherwise assert the typed report never labels `EligibleServe` as `cleared`.

D-18: no `propagated`, `broadcast`, `public_relay` keys on extension JSON, status JSON, or sendrawtransaction JSON.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze sendrawtransaction and prove no propagation keys</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/method/node.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/node.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/tests/transaction_methods.rs</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/dispatch/tests/transaction_methods.rs, packages/open-bitcoin-rpc/src/method/node.rs</files>
  <behavior>
    - Existing `sendrawtransaction_returns_txid_and_maps_rejections` still passes.
    - New test deserializes success JSON as a map and asserts keys are exactly `txid_hex`, `replaced_txids`, `evicted_txids` (or a superset that does **not** include `admission`, `relay`, `fingerprint`, `propagated`, `broadcast`, `public_relay`, `allowed`).
    - `SendRawTransactionResponse` struct fields remain those three names. Do not add fields.
  </behavior>
  <action>
    Per D-16 and D-18, add tests only unless a prior plan accidentally added fields — if fields were added, delete them.

    Write failing tests named exactly:
    - `sendrawtransaction_success_keys_are_txid_replaced_and_evicted_only`
    - `sendrawtransaction_struct_has_no_dual_state_fields`

    The struct test can use `serde_json::to_value(SendRawTransactionResponse { ... })` and assert key set.

    Do not change Knots object-vs-hex-string (Phase 138).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-10-sendraw -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features sendrawtransaction_success_keys -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub struct SendRawTransactionResponse' -A 8 packages/open-bitcoin-rpc/src/method/node.rs` shows only `txid_hex`, `replaced_txids`, and `evicted_txids` fields.</criterion>
    <criterion>`rg -n 'admission|relay_disabled|propagated' packages/open-bitcoin-rpc/src/method/node.rs` returns no matches on that struct.</criterion>
    <criterion>The named Cargo filter reports the key-set test passing.</criterion>
  </acceptance-criteria>
  <done>sendrawtransaction remains a txid-centered object with no dual-state or propagation claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove relay-disabled accept and no last-package echo</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/dispatch/package.rs</file>
    <file>packages/open-bitcoin-rpc/src/dispatch/tests/package_methods.rs</file>
    <file>packages/open-bitcoin-rpc/src/method/node.rs</file>
    <file>packages/open-bitcoin-cli/src/operator/status/tests.rs</file>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-UI-SPEC.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/dispatch/tests/dual_state.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <behavior>
    - `openbitcoinpackage` mode=submit with `RelayIntent::NotRequested` (or a context whose `ManagedNetworkInfo.relay == false`) returns a typed member with `admission` in {`accepted`, `still-present`} and `relay` = `relay_disabled`.
    - After that submit, `openbitcoinnetworkstatus` JSON `mempool.admission` / `mempool.retry` counts change, but the status object has no `members`, `fingerprint`, `txid`, or `wtxid` key under `mempool`.
    - Operator JSON status fixture (CLI) with those groups has no last-package table.
    - Typed report and status JSON contain none of the strings `propagated`, `broadcast`, `public_relay`.
    - A member that is `Eligible` / `served` is never labeled `cleared` solely because it was served (D-17).
  </behavior>
  <action>
    Per D-15, D-07, D-17, and D-18, add `dispatch/tests/dual_state.rs` and `mod dual_state;` from `dispatch/tests.rs`. Reuse Plan 03 fixtures. Force relay-off by constructing the RPC context the same way existing tests disable relay if such a helper exists; otherwise call `submit_local_package` / `openbitcoinpackage` with `RelayIntent::NotRequested` and assert the projector token.

    If Plan 06 already covers relay_disabled tokens but not the status-echo case, this task owns the status-echo test.

    CLI test named exactly `json_status_has_no_last_package_members` may already exist from Plan 07 — if so, extend it rather than duplicating, still listed here so this plan re-runs it.

    Write failing tests named exactly:
    - `openbitcoinpackage_relay_disabled_accept_is_still_present`
    - `openbitcoinnetworkstatus_has_no_package_member_table`
    - `dual_state_json_forbids_propagation_keys`
    - `served_is_not_cleared`

    Do not add `getrawmempool`. Do not add production `propagated` fields to "make a test pass".
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-10-dual -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features openbitcoinpackage_relay_disabled -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'relay_disabled' packages/open-bitcoin-rpc/src/dispatch/tests/dual_state.rs` matches.</criterion>
    <criterion>`rg -n 'fingerprint|tx-results' packages/open-bitcoin-rpc/src/method/node.rs` returns no matches on `OpenBitcoinNetworkStatusResponse`.</criterion>
    <criterion>`rg -n 'getrawmempool' packages/open-bitcoin-rpc/src/method.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the relay-disabled test passing.</criterion>
  </acceptance-criteria>
  <done>Relay-disabled local accept is visible as dual-state; shared status stays identifier-free; sendrawtransaction is unchanged.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Originating package RPC → status RPC | Both authenticated; only the originating call may carry identifiers. |
| Local accept → operator wording | Success must not be read as public delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-10-01 | Information Disclosure | status RPC confused deputy | mitigate | High: D-07 test forbids member tables on `openbitcoinnetworkstatus`. |
| T-137-10-02 | Spoofing | sendrawtransaction as broadcast | mitigate | High: key-set freeze; no dual-state on that object. |
| T-137-10-03 | Spoofing | EligibleServe as clear/delivery | mitigate | D-17 test: served ≠ cleared. |
| T-137-10-04 | Elevation of Privilege | claim fields | mitigate | D-18: reject `propagated` / `broadcast` / `public_relay` keys. |
</threat_model>

<verification>
Run the timed sendrawtransaction and dual-state suites, then `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features json_status_has_no_last_package -- --test-threads=1` through `scripts/command-timings.ts`. Format rpc and cli crates. `git diff --check`.
</verification>

<success_criteria>
- D-15 dual-state axes are demonstrated on the extension path and aggregates.
- D-16 sendrawtransaction is unchanged.
- D-17 served is not cleared.
- D-18 no propagation fields.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-10-SUMMARY.md`
</output>
