---
phase: 137-rpc-and-sanitized-operator-evidence
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-node/src/network/tests/local_package_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
autonomous: true
requirements: [MPOBS-01]
must_haves:
  truths:
    - "Local dry-run calls DryRunPackageCommand only and leaves mempool count, rolling fee, relay counters, dirty generation, and lifecycle evidence unchanged."
    - "Local submit routes SubmitPackageCommand then LifecycleCommand::PackageAdmission with AdmissionProjectionSource::Local and never reuses the peer 1P1C bridge."
    - "Handle methods require shell-sampled PolicyTime and RelayIntent; they do not call SystemTime or getrandom."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs
      provides: "Local dry-run and submit on ManagedPeerNetwork"
      contains: "dry_run_local_package"
    - path: packages/open-bitcoin-node/src/network/runtime_authority.rs
      provides: "ManagedNetworkHandle facades"
      contains: "submit_local_package"
  key_links:
    - from: packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs
      to: packages/open-bitcoin-mempool/src/package.rs
      via: "DryRunPackageCommand vs SubmitPackageCommand"
      pattern: "DryRunPackageCommand"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 137-2026-08-19T21-52-42
generated_at: 2026-08-19T22:45:00.000Z
---

<objective>
Add local package dry-run and submit on `ManagedNetworkHandle` through Phase 132 commands and Local admission projection.

Purpose: RPC/CLI must not loop `sendrawtransaction` or reuse the peer 1P1C bridge (D-03).
Output: `dry_run_local_package` and `submit_local_package` with a before/after dry-run snapshot test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md
@.planning/phases/137-rpc-and-sanitized-operator-evidence/137-RESEARCH.md
@packages/open-bitcoin-mempool/src/package.rs
@packages/open-bitcoin-node/src/mempool.rs
@packages/open-bitcoin-node/src/network/admission_bridge/package.rs
@packages/open-bitcoin-node/src/network/runtime_authority.rs
@packages/open-bitcoin-node/src/network/lifecycle_projection.rs
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
From packages/open-bitcoin-mempool/src/package.rs (do not modify):

```rust
pub struct DryRunPackageCommand { pub package: WellFormedPackage, pub context: AdmissionContext }
pub struct DryRunPackageResult { pub report: PackageReport }
pub struct SubmitPackageCommand { pub package: SubmissionPackage, pub context: AdmissionContext }
pub struct SubmittedPackageResult { pub report: PackageReport, pub delta: MempoolLifecycleDelta }
```

From packages/open-bitcoin-mempool/src/context.rs:

```rust
impl AdmissionContext {
    pub const fn local(accepted_at: PolicyTime, relay_intent: RelayIntent) -> Self;
}
```

From packages/open-bitcoin-node/src/network/lifecycle_projection.rs:

```rust
pub(super) enum AdmissionProjectionSource { None, Local, Peer(BTreeMap<MempoolMemberIdentity, PeerId>) }
```

From packages/open-bitcoin-node/src/mempool.rs:

```rust
impl ManagedMempool {
    pub fn submit_package(...) -> Result<SubmittedPackageResult, MempoolError>;
    // dry_run_package is missing — add it
}
```

Peer package admission in `admission_bridge/package.rs` is the wrong entry point. Do not call `submit_same_peer_candidate`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ManagedMempool::dry_run_package and handle dry_run_local_package</name>
  <read-first>
    <file>.planning/phases/137-rpc-and-sanitized-operator-evidence/137-CONTEXT.md</file>
    <file>packages/open-bitcoin-mempool/src/package.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/package_admission.rs</file>
    <file>packages/open-bitcoin-node/src/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/admission_bridge.rs</file>
    <file>packages/open-bitcoin-node/src/network/admission_bridge/package.rs</file>
    <file>standards/core/testing.md</file>
    <file>standards/languages/rust.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/mempool.rs, packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs, packages/open-bitcoin-node/src/network/admission_bridge.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/tests/local_package_cases.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <behavior>
    - `ManagedMempool::dry_run_package` forwards to `self.mempool.dry_run_package` and does not increment `PACKAGE_SUBMIT_COUNT` (the existing submit probe).
    - `ManagedNetworkHandle::dry_run_local_package(transactions, verify_flags, consensus_params, now_unix_seconds, relay_intent)` builds `WellFormedPackage::try_from(transactions)`, `AdmissionContext::local(PolicyTime::from_unix_seconds(now_unix_seconds), relay_intent)`, and `DryRunPackageCommand`.
    - After a successful dry-run of a valid one-member package, `mempool_info().transaction_count`, `rolling_mempool_fee_rate_sats_per_kvb`, `operator_snapshot().relay` outcome counters, and checkpoint dirty generation are unchanged versus a cloned before-snapshot.
    - Shape errors (`empty`, `> MAX_PACKAGE_COUNT`) return `ManagedNetworkError::PackageShape` and still mutate nothing.
    - The method does not contain `SystemTime` or `getrandom`.
  </behavior>
  <action>
    Per D-03, add `local_package.rs` beside `package.rs` under `admission_bridge/` and `mod local_package;` in `admission_bridge.rs`. Implement `ManagedPeerNetwork` methods there. Add handle facades on `ManagedNetworkHandle` next to `submit_local_transaction_outcome_at` using `try_read` / `read` for dry-run (not `try_mutate`). If the handle has no `try_read`, use the existing `read` helper used by `operator_snapshot` / `mempool_info`.

    Exact handle signature:

    ```rust
    pub fn dry_run_local_package(
        &amp;self,
        transactions: Vec&lt;Transaction&gt;,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
        now_unix_seconds: i64,
        relay_intent: RelayIntent,
    ) -&gt; Result&lt;PackageReport, ManagedNetworkAuthorityError&gt;;
    ```

    Do not apply `LifecycleCommand`. Do not enqueue fanout. Do not call peer package helpers.

    Write failing tests first in `network/tests/local_package_cases.rs` and `mod local_package_cases;` from `network/tests.rs`, named exactly:
    - `dry_run_local_package_returns_report_without_changing_mempool_count`
    - `dry_run_local_package_does_not_change_rolling_fee_or_relay_counters`
    - `dry_run_local_package_does_not_dirty_checkpoint_generation`
    - `dry_run_local_package_rejects_empty_without_mutation`

    Reuse existing network test fixtures that already connect a spendable chain and build a standard transaction. Compare before/after `mempool_info` and `operator_snapshot().relay`.

    Add the file-header breadcrumb listing `packages/bitcoin-knots/src/rpc/mempool.cpp` and `packages/bitcoin-knots/src/validation.cpp`. Do not edit `source-breadcrumbs.json`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-02-dry-run -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features dry_run_local_package -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub fn dry_run_package' packages/open-bitcoin-node/src/mempool.rs` matches.</criterion>
    <criterion>`rg -n 'pub fn dry_run_local_package' packages/open-bitcoin-node/src/network/runtime_authority.rs` matches.</criterion>
    <criterion>`rg -n 'DryRunPackageCommand' packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs` matches.</criterion>
    <criterion>`rg -n 'LifecycleCommand|submit_same_peer_candidate|SystemTime' packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs` returns no matches in the dry-run function body (submit may use LifecycleCommand in Task 2).</criterion>
    <criterion>The named Cargo filter reports the four Task 1 tests passing.</criterion>
  </acceptance-criteria>
  <done>Local dry-run is a non-mutating handle read that returns PackageReport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add submit_local_package through PackageAdmission Local</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs</file>
    <file>packages/open-bitcoin-node/src/network/admission_bridge/package.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority.rs</file>
    <file>packages/open-bitcoin-node/src/mempool.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/tests/local_package_cases.rs</files>
  <behavior>
    - `submit_local_package` builds `SubmissionPackage::try_from_package` / `SubmitPackageCommand` with `AdmissionContext::local`, then `ManagedMempool::submit_package`, then `LifecycleCommand::PackageAdmission` with `AdmissionProjectionSource::Local`.
    - A valid one-member local submit increases `mempool_info().transaction_count` by 1 and returns `SubmittedPackageResult` whose `report` members include `FinallyPresent` or `AlreadyPresent`.
    - The path does not call `submit_same_peer_candidate` or `AdmissionProjectionSource::peer`.
    - `RelayIntent::NotRequested` still admits locally when policy allows; it must not be rewritten to `Requested` inside the handle.
    - Failed `WellFormedPackage` / `SubmissionPackage` construction returns `ManagedNetworkError::PackageShape` and does not apply a lifecycle command.
  </behavior>
  <action>
    Per D-03, add:

    ```rust
    pub fn submit_local_package(
        &amp;self,
        transactions: Vec&lt;Transaction&gt;,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
        now_unix_seconds: i64,
        relay_intent: RelayIntent,
    ) -&gt; Result&lt;SubmittedPackageResult, ManagedNetworkAuthorityError&gt;;
    ```

    Use `try_mutate` like `submit_local_transaction_outcome_at`. Follow the peer package file only for how `apply_lifecycle_command(..., LifecycleCommand::PackageAdmission(plan))` is built — change the source to `AdmissionProjectionSource::Local` and do not attach peer ids.

    If `local_package.rs` would exceed 500 lines, split submit into `admission_bridge/local_package/submit.rs` using `foo.rs` + `foo/`.

    Write failing tests named exactly:
    - `submit_local_package_admits_one_member_through_local_source`
    - `submit_local_package_does_not_call_peer_bridge`
    - `submit_local_package_not_requested_still_accepts_locally`
    - `submit_local_package_shape_error_does_not_apply_lifecycle`

    The peer-bridge test can be a source-level / behavior test: after submit, no orphan/1P1C candidate is created for a local RPC package (assert peer package admission types are unused by checking no `ManagedPeerPackageAdmission` and mempool membership matches the report).

    Do not add RPC methods. Do not project Knots JSON here.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase137-02-submit -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features submit_local_package -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'pub fn submit_local_package' packages/open-bitcoin-node/src/network/runtime_authority.rs` matches.</criterion>
    <criterion>`rg -n 'AdmissionProjectionSource::Local' packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs` matches.</criterion>
    <criterion>`rg -n 'submit_same_peer_candidate' packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs` returns no matches.</criterion>
    <criterion>`rg -n 'LifecycleCommand::PackageAdmission' packages/open-bitcoin-node/src/network/admission_bridge/local_package.rs` matches.</criterion>
    <criterion>The named Cargo filter reports the four Task 2 tests passing.</criterion>
  </acceptance-criteria>
  <done>Local submit commits through PackageAdmission Local and returns the authoritative SubmittedPackageResult.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| RPC shell → handle | Hex-decoded transactions and sampled time cross into authority. |
| Dry-run → authority | Dry-run must not take the mutate lock in a way that applies lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-02-01 | Tampering | dry-run path | mitigate | High: dry-run uses read + `DryRunPackageCommand` only. Tests compare mempool count, rolling fee, relay counters, and dirty generation. Block if dry-run mutates. |
| T-137-02-02 | Elevation of Privilege | peer bridge reuse | mitigate | Local source only; no `submit_same_peer_candidate`. Peer origin must not be forged for RPC hex. |
| T-137-02-03 | Spoofing | RelayIntent | mitigate | Handle takes caller-supplied `RelayIntent` from the RPC shell (activation-resolved). Do not force `Requested`. |
| T-137-02-04 | Denial of Service | package size | mitigate | `WellFormedPackage` / `MAX_PACKAGE_COUNT` fail closed before submit. |
| T-137-02-05 | Information Disclosure | handle return | accept | `PackageReport` contains identifiers. That is correct for originating RPC (D-06); do not copy it onto `operator_snapshot`. |
</threat_model>

<verification>
Run the timed dry-run suite, then the timed submit suite, then `cargo fmt --manifest-path packages/Cargo.toml -p open-bitcoin-node` through `scripts/command-timings.ts` and `git diff --check`.
</verification>

<success_criteria>
- D-03 dry-run is non-mutating and submit matches authoritative membership.
- Peer 1P1C bridge is unused.
- No RPC, CLI, or snapshot group work is included.
</success_criteria>

<output>
After completion, create `.planning/phases/137-rpc-and-sanitized-operator-evidence/137-02-SUMMARY.md`
</output>
