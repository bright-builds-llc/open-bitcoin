---
phase: 85-operator-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/operator-runbooks.md
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 85-2026-06-22T11-57-13
generated_at: 2026-06-22T14:28:37Z
must_haves:
  truths:
    - "Operator can complete a production-boundary preflight before long-running source-built operation."
    - "Operator can monitor and diagnose no-progress using field-level status, recovery, resource, structured logs, metrics, support-bundle summaries, checkpoint timeline, opt-in soak/live-smoke reports, and support evidence."
    - "Operator can choose safe recovery, stop, or escalation actions without hidden mutation guidance."
    - "Operator can collect a redacted support-bundle timeline sufficient for support triage."
  artifacts:
    - path: "docs/parity/operator-runbooks.md"
      provides: "Canonical Phase 85 operator runbook"
      contains: "Surface id: `v1-8-operator-runbooks`"
  key_links:
    - from: "docs/parity/operator-runbooks.md"
      to: "docs/parity/production-claim-boundary.md"
      via: "production-boundary preflight link"
      pattern: "production-claim-boundary\\.md"
    - from: "docs/parity/operator-runbooks.md"
      to: "docs/parity/support-matrix.md"
      via: "support and issue-evidence link"
      pattern: "support-matrix\\.md"
    - from: "docs/parity/operator-runbooks.md"
      to: "docs/parity/upgrade-and-rollback-policy.md"
      via: "upgrade and rollback boundary link"
      pattern: "upgrade-and-rollback-policy\\.md"
---

<objective>
Create the canonical Phase 85 operator runbook at `docs/parity/operator-runbooks.md`.

Purpose: Give operators one durable source for preflight, long-run monitoring, no-progress diagnosis, recovery, support-bundle timeline, and escalation procedures without widening the v1.8 production-readiness boundary.
Output: A complete docs/parity runbook satisfying RUN-01, RUN-02, and RUN-03.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/code-shape.md
@standards/core/verification.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-operator-runbooks/85-CONTEXT.md
@.planning/phases/85-operator-runbooks/85-RESEARCH.md
@docs/parity/production-claim-boundary.md
@docs/parity/support-matrix.md
@docs/parity/upgrade-and-rollback-policy.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/architecture/storage-decision.md
@docs/parity/catalog/operator-runtime-release-hardening.md

<interfaces>
Phase 82 support terms that must be reused exactly:
```text
supported
preview
opt-in UAT
unsupported
deferred
```

Recovery action classes that must be reused exactly:
```text
safe_retry
read_only_inspection
backup_then_rebuild
stop_and_escalate
```

Required repo-local operator command prefixes:
```bash
cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin --
bazel run //packages/open-bitcoin-cli:open_bitcoin --
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runbook shell and production-boundary preflight</name>
  <files>docs/parity/operator-runbooks.md</files>
  <read_first>
    <file>.planning/phases/85-operator-runbooks/85-CONTEXT.md</file>
    <file>.planning/phases/85-operator-runbooks/85-RESEARCH.md</file>
    <file>docs/parity/production-claim-boundary.md</file>
    <file>docs/parity/support-matrix.md</file>
    <file>docs/parity/upgrade-and-rollback-policy.md</file>
    <file>docs/operator/runtime-guide.md</file>
  </read_first>
  <action>Create `docs/parity/operator-runbooks.md` with title `# Operator Runbooks` and `Surface id: `v1-8-operator-runbooks``. Add `## Scope And Non-Claims` and `## Production-Boundary Preflight`. Per D-01, the first procedural section must send operators through `production-claim-boundary.md`, `support-matrix.md`, and `upgrade-and-rollback-policy.md` before long-running operation. Per D-02, add a checklist table with columns `Evidence to record`, `How to collect it`, `Mutation status`, and `Escalation use`, and include rows for selected datadir, source revision, repo-local verification status, Cargo or Bazel command form, config paths, current status evidence, resource/disk review, service state or unavailable reason, wallet scope, and support-bundle availability. Per D-03, include both exact status command forms: `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; status --format json` and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; status --format json`. Per D-04, every preflight row must be `review-only evidence`; add explicit prose that status, support bundles, config summaries, service state, and local report paths are acceptable evidence, while source datadir, wallet, service, and config mutation is outside Phase 85.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test -f docs/parity/operator-runbooks.md; for needle in "Surface id:" "v1-8-operator-runbooks" "## Scope And Non-Claims" "## Production-Boundary Preflight" "production-claim-boundary.md" "support-matrix.md" "upgrade-and-rollback-policy.md" "selected datadir" "source revision" "repo-local verification status" "Cargo or Bazel command form" "config paths" "current status evidence" "resource/disk review" "service state or unavailable reason" "wallet scope" "support-bundle availability" "review-only evidence" "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; status --format json" "bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; status --format json"; do rg -F -- "$needle" docs/parity/operator-runbooks.md >/dev/null; done; git diff --check -- docs/parity/operator-runbooks.md'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/operator-runbooks.md` exists.
    - The file contains `Surface id: `v1-8-operator-runbooks``.
    - The file contains `## Production-Boundary Preflight`.
    - The preflight table contains every D-02 evidence item exactly: `selected datadir`, `source revision`, `repo-local verification status`, `Cargo or Bazel command form`, `config paths`, `current status evidence`, `resource/disk review`, `service state or unavailable reason`, `wallet scope`, and `support-bundle availability`.
    - The file contains both exact repo-local status command forms from the action.
    - The file contains `review-only evidence` and does not describe preflight as permission for mutation.
  </acceptance_criteria>
  <done>RUN-01 preflight is complete and evidence-based without hidden mutation guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Add long-run monitoring, no-progress diagnosis, and recovery decisions</name>
  <files>docs/parity/operator-runbooks.md</files>
  <read_first>
    <file>docs/parity/operator-runbooks.md</file>
    <file>docs/architecture/status-snapshot.md</file>
    <file>docs/architecture/operator-observability.md</file>
    <file>docs/architecture/storage-decision.md</file>
    <file>docs/operator/runtime-guide.md</file>
  </read_first>
  <action>Add `## Long-Run Monitoring`, `## No-Progress Diagnosis`, `## Recovery And Stop Decisions`, and `## Escalation Evidence Thresholds`. Per D-05 and D-06, organize monitoring and diagnosis around field-level evidence and explicitly state that elapsed time, daemon startup, peer reachability, raw log tail, report existence, and support bundle existence are not sufficient proof. Require these exact field names where relevant: `progress_credit`, `last_useful_work`, `last_peer_contribution`, `expected_progress_window`, `no_progress_threshold`, `stall_diagnosis`, `sync.no_progress_diagnosis`, `sync.no_progress_next_action`, `latest_stop_reason`, `resource_bounds`, `sync.resource_pressure`, `recovery_evidence`, and `support_forensics`. Also require the exact D-05/D-07 evidence vocabulary `structured logs`, `metrics`, `support-bundle summaries`, `soak reports`, `live-smoke reports`, `checkpoint timeline`, `stalled subsystem`, `public-network opt-in`, `stay-current opt-in`, and `multi-day soak opt-in`; make clear that opt-in reports are useful evidence only when the operator explicitly chose those non-default workflows. Per D-07 and D-09, add a recovery decision table using only `safe_retry`, `read_only_inspection`, `backup_then_rebuild`, and `stop_and_escalate`, with columns for required evidence, allowed action, forbidden action, and escalation bundle content. Per D-08, include this exact sentence: `Default bash scripts/verify.sh remains deterministic, public-network-free, service-manager-free, and multi-day-free.` Per D-11, include escalation thresholds for repeated no-progress with typed cause, unavailable critical fields, recovery class requiring stop/escalate, resource pressure crossing documented bounds, inconsistent status/support evidence, and failure to collect the minimum redacted support-bundle timeline. Per D-10 and D-12, forbid destructive repair, source datadir mutation, external wallet mutation, service-manager mutation, config rewrite, automatic rebuild, response timelines, hosted support upload, and production service ownership.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; for needle in "## Long-Run Monitoring" "## No-Progress Diagnosis" "## Recovery And Stop Decisions" "## Escalation Evidence Thresholds" "progress_credit" "last_useful_work" "last_peer_contribution" "expected_progress_window" "no_progress_threshold" "stall_diagnosis" "sync.no_progress_diagnosis" "sync.no_progress_next_action" "latest_stop_reason" "resource_bounds" "sync.resource_pressure" "recovery_evidence" "support_forensics" "structured logs" "metrics" "support-bundle summaries" "soak reports" "live-smoke reports" "checkpoint timeline" "stalled subsystem" "public-network opt-in" "stay-current opt-in" "multi-day soak opt-in" "elapsed time" "daemon startup" "peer reachability" "raw log tail" "report existence" "support bundle existence" "safe_retry" "read_only_inspection" "backup_then_rebuild" "stop_and_escalate" "Default bash scripts/verify.sh remains deterministic, public-network-free, service-manager-free, and multi-day-free." "repeated no-progress with typed cause" "unavailable critical fields" "recovery class requiring stop/escalate" "resource pressure crossing documented bounds" "inconsistent status/support evidence" "failure to collect the minimum redacted support-bundle timeline" "destructive repair" "source datadir mutation" "external wallet mutation" "service-manager mutation" "config rewrite" "automatic rebuild"; do rg -F -- "$needle" docs/parity/operator-runbooks.md >/dev/null; done; git diff --check -- docs/parity/operator-runbooks.md'</automated>
  </verify>
  <acceptance_criteria>
    - The runbook contains the four headings named in the action.
    - The runbook contains every exact field name listed in the action.
    - The runbook contains every exact D-05/D-07 evidence term listed in the action, including structured logs, metrics, support-bundle summaries, soak/live-smoke opt-in reports, checkpoint timeline, and stalled subsystem.
    - The runbook contains every exact recovery action class listed in the action.
    - The runbook contains the exact default verifier boundary sentence from the action.
    - The runbook contains every exact escalation threshold listed in the action.
    - The runbook states that elapsed time, daemon startup, peer reachability, raw log tail, report existence, and support bundle existence are not sufficient proof.
    - The runbook forbids destructive repair, source datadir mutation, external wallet mutation, service-manager mutation, config rewrite, and automatic rebuild as Phase 85 actions.
  </acceptance_criteria>
  <done>RUN-02 monitoring, no-progress diagnosis, recovery, and escalation decisions are complete using existing evidence vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3: Add support-bundle timeline and privacy boundaries</name>
  <files>docs/parity/operator-runbooks.md</files>
  <read_first>
    <file>docs/parity/operator-runbooks.md</file>
    <file>docs/parity/support-matrix.md</file>
    <file>docs/operator/runtime-guide.md</file>
    <file>docs/architecture/operator-observability.md</file>
  </read_first>
  <action>Add `## Support-Bundle Timeline` and `## Privacy And Safety Boundaries`. Per D-13, define a redacted timeline shape with the exact timeline event labels `preflight evidence`, `command start`, `status snapshots`, `progress or no-progress events`, `resource/recovery events`, `support-bundle collection`, `operator action taken`, `final status`, and `escalation decision`. Per D-14, define the minimum useful bundle with exact items: redacted `support-evidence.json`, redacted `support-evidence.md`, exact command output, bounded log summary, config summary, service state or unavailable reason, resource evidence, recovery/progress evidence, sync evidence, version/toolchain context, platform details, exact repo-local reproduction command, and `Unavailable: &lt;reason&gt;`. Include both support-bundle command forms: `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support --format json` and `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support --format json`. Per D-15, explicitly forbid wallet private material, raw wallet files, RPC cookies, `rpcpassword`, `rpcauth`, raw datadirs, unredacted logs, raw unbounded logs, full peer tables with sensitive local data, and automatic support-bundle upload.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "## Support-Bundle Timeline|## Privacy And Safety Boundaries|preflight evidence|command start|status snapshots|progress or no-progress events|resource/recovery events|support-bundle collection|operator action taken|final status|escalation decision|support-evidence.json|support-evidence.md|exact command output|bounded log summary|config summary|service state or unavailable reason|resource evidence|recovery/progress evidence|sync evidence|version/toolchain context|platform details|exact repo-local reproduction command|Unavailable: &lt;reason&gt;|cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support --format json|bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir=&lt;path&gt; support bundle --output-dir=&lt;path&gt;/support --format json|wallet private material|raw wallet files|RPC cookies|rpcpassword|rpcauth|raw datadirs|unredacted logs|raw unbounded logs|full peer tables with sensitive local data|automatic support-bundle upload" docs/parity/operator-runbooks.md; git diff --check -- docs/parity/operator-runbooks.md'</automated>
  </verify>
  <acceptance_criteria>
    - The runbook contains `## Support-Bundle Timeline` and `## Privacy And Safety Boundaries`.
    - The runbook contains every exact timeline event label listed in the action.
    - The runbook contains every exact minimum useful bundle item listed in the action.
    - The runbook contains both exact support-bundle command forms from the action.
    - The runbook contains every exact forbidden evidence item listed in the action.
  </acceptance_criteria>
  <done>RUN-03 support-bundle timeline, evidence sufficiency, and privacy boundaries are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator docs to operator action | Documentation can influence local datadir, wallet, config, and service decisions. |
| Operator evidence to support triage | Redacted local evidence can include sensitive runtime, peer, config, and wallet-adjacent data if boundaries are unclear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Spoofing | `docs/parity/operator-runbooks.md` support terms | mitigate | Reuse only `supported`, `preview`, `opt-in UAT`, `unsupported`, and `deferred`; acceptance checks require those terms and Phase 82 links. |
| T-85-02 | Tampering | Preflight and recovery prose | mitigate | Mark preflight as `review-only evidence`; forbid destructive repair, source datadir mutation, external wallet mutation, service-manager mutation, config rewrite, and automatic rebuild. |
| T-85-03 | Repudiation | No-progress and support-bundle proof | mitigate | Require field-level status/support evidence and `Unavailable: &lt;reason&gt;`; reject elapsed time, startup, peer reachability, raw logs, report existence, and bundle existence as proof by themselves. |
| T-85-04 | Information Disclosure | Support-bundle timeline | mitigate | Require smallest useful redacted evidence and forbid wallet private material, raw wallet files, RPC cookies, `rpcpassword`, `rpcauth`, raw datadirs, unredacted logs, raw unbounded logs, sensitive peer tables, and automatic upload. |
| T-85-05 | Denial of Service | Default verification boundary | mitigate | State that `bash scripts/verify.sh` remains deterministic, public-network-free, service-manager-free, and multi-day-free. |
</threat_model>

<verification>
Overall plan verification is the union of task-level `rg` checks plus:

```bash
git diff --check -- docs/parity/operator-runbooks.md
```
</verification>

<success_criteria>
- `docs/parity/operator-runbooks.md` exists with surface id `v1-8-operator-runbooks`.
- RUN-01, RUN-02, and RUN-03 content is complete in the canonical runbook.
- Every locked decision D-01 through D-15 is implemented in the runbook without reduced scope.
- No public-network, real service-manager, multi-day, destructive repair, hidden mutation, production-funds wallet, migration apply, production service ownership, or automatic upload support is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/85-operator-runbooks/85-01-SUMMARY.md`.
</output>
