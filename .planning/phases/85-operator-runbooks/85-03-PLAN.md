---
phase: 85-operator-runbooks
plan: 03
type: execute
wave: 3
depends_on:
  - 85-01
  - 85-02
files_modified:
  - scripts/check-phase85-operator-runbooks.test.ts
  - scripts/check-phase85-operator-runbooks.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 85-2026-06-22T11-57-13
generated_at: 2026-06-22T14:28:37Z
must_haves:
  truths:
    - "Contributors can run a deterministic Bun checker for Phase 85 runbook drift."
    - "The checker validates runbook sections, exact support terms, repo-local command forms, structured monitoring evidence, opt-in report boundaries, timeline fields, forbidden claims, canonical links, parity roots, and verifier wiring."
    - "Default verification runs the Phase 85 checker after Phase 84 without public-network, real service-manager, or multi-day actions."
  artifacts:
    - path: "scripts/check-phase85-operator-runbooks.test.ts"
      provides: "Bun fixture tests for Phase 85 checker behavior"
      contains: "passes_when_phase85_fixture_contains_runbook_roots_and_verify_order"
    - path: "scripts/check-phase85-operator-runbooks.ts"
      provides: "Deterministic Phase 85 checker"
      exports:
        - "checkPhase85OperatorRunbooks"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring for Phase 85"
      contains: "bun run scripts/check-phase85-operator-runbooks.ts"
  key_links:
    - from: "scripts/check-phase85-operator-runbooks.ts"
      to: "docs/parity/operator-runbooks.md"
      via: "fixed target-file read"
      pattern: "docs/parity/operator-runbooks\\.md"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase85-operator-runbooks.ts"
      via: "run_step after Phase 84"
      pattern: "check Phase 85 operator runbooks"
---

<objective>
Add a narrow deterministic Bun checker for Phase 85 and wire it into the repo-native verifier.

Purpose: Guard RUN-01 through RUN-03 documentation drift without becoming the broad Phase 88 production-claim scanner.
Output: Phase 85 checker tests, checker implementation, and `scripts/verify.sh` test/check commands after Phase 84.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-operator-runbooks/85-CONTEXT.md
@.planning/phases/85-operator-runbooks/85-RESEARCH.md
@.planning/phases/85-operator-runbooks/85-01-SUMMARY.md
@.planning/phases/85-operator-runbooks/85-02-SUMMARY.md
@scripts/check-phase84-upgrade-rollback-policy.ts
@scripts/check-phase84-upgrade-rollback-policy.test.ts
@scripts/verify.sh
@docs/parity/operator-runbooks.md
@docs/parity/index.json
@docs/parity/checklist.md

<interfaces>
Create this exported checker contract:
```typescript
export function checkPhase85OperatorRunbooks(
  maybeRepoRoot = process.env.OPEN_BITCOIN_PHASE85_REPO_ROOT,
): string[];
```

Use these constants:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE85_REPO_ROOT";
const SURFACE_ID = "v1-8-operator-runbooks";
const PHASE85_REQUIREMENTS = ["RUN-01", "RUN-02", "RUN-03"] as const;
const RUNBOOK_PATH = "docs/parity/operator-runbooks.md";
const PHASE84_CHECKER_COMMAND =
  "bun run scripts/check-phase84-upgrade-rollback-policy.ts";
const PHASE85_TEST_COMMAND =
  "bun test scripts/check-phase85-operator-runbooks.test.ts";
const PHASE85_CHECKER_COMMAND =
  "bun run scripts/check-phase85-operator-runbooks.ts";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 85 checker fixture tests</name>
  <files>scripts/check-phase85-operator-runbooks.test.ts</files>
  <read_first>
    <file>scripts/check-phase84-upgrade-rollback-policy.test.ts</file>
    <file>scripts/check-phase84-upgrade-rollback-policy.ts</file>
    <file>docs/parity/operator-runbooks.md</file>
    <file>docs/parity/index.json</file>
    <file>docs/parity/checklist.md</file>
    <file>scripts/verify.sh</file>
  </read_first>
  <behavior>
    - Test valid fixture roots and executed verifier order pass.
    - Test missing preflight evidence fails with a preflight-specific message.
    - Test missing structured monitoring, checkpoint, stalled-subsystem, or opt-in report evidence fails.
    - Test treating artifact existence, elapsed time, daemon startup, peer reachability, raw logs, or report existence as proof fails.
    - Test hidden mutation, destructive repair, service-manager mutation, config rewrite, automatic rebuild, or automatic upload permission fails.
    - Test missing support timeline fields or redaction boundaries fails.
    - Test parity roots missing RUN-01/RUN-02/RUN-03 or `docs/parity/operator-runbooks.md` fail.
    - Test Phase 85 commands only in the `VERIFY_COMMAND_ORDER` heredoc fail.
    - Test executable verifier text containing public-network, service-manager, or multi-day default command drift fails.
  </behavior>
  <action>Following the Phase 84 test pattern, create `scripts/check-phase85-operator-runbooks.test.ts`. Use Bun's `test`, `expect`, `afterEach`, temporary fixture roots, and Arrange/Act/Assert comments in every test. Define fixture text containing the final Phase 85 headings, terms, command forms, parity roots, human pointers, structured logs, metrics, support-bundle summaries, soak reports, live-smoke reports, checkpoint timeline, stalled subsystem, public-network opt-in, stay-current opt-in, multi-day soak opt-in, and verifier order. Include test names exactly: `passes_when_phase85_fixture_contains_runbook_roots_and_verify_order`, `fails_when_runbook_omits_preflight_evidence`, `fails_when_runbook_omits_structured_monitoring_or_opt_in_evidence`, `fails_when_runbook_treats_artifact_existence_or_elapsed_time_as_proof`, `fails_when_runbook_allows_hidden_mutation_or_automatic_upload`, `fails_when_support_timeline_omits_required_field_or_redaction_boundary`, `fails_when_parity_roots_omit_runbook_requirement_or_evidence`, `fails_when_phase85_verify_commands_exist_only_in_legacy_heredoc`, and `fails_when_default_verify_contains_public_network_service_manager_or_multiday_drift`. Per D-17 and D-18, tests must stay fixed-file and deterministic; do not execute operator commands, public-network commands, service-manager commands, or sleeps.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "OPEN_BITCOIN_PHASE85_REPO_ROOT|passes_when_phase85_fixture_contains_runbook_roots_and_verify_order|fails_when_runbook_omits_preflight_evidence|fails_when_runbook_omits_structured_monitoring_or_opt_in_evidence|fails_when_runbook_treats_artifact_existence_or_elapsed_time_as_proof|fails_when_runbook_allows_hidden_mutation_or_automatic_upload|fails_when_support_timeline_omits_required_field_or_redaction_boundary|fails_when_parity_roots_omit_runbook_requirement_or_evidence|fails_when_phase85_verify_commands_exist_only_in_legacy_heredoc|fails_when_default_verify_contains_public_network_service_manager_or_multiday_drift|structured logs|metrics|support-bundle summaries|soak reports|live-smoke reports|checkpoint timeline|stalled subsystem|public-network opt-in|stay-current opt-in|multi-day soak opt-in|// Arrange|// Act|// Assert" scripts/check-phase85-operator-runbooks.test.ts; set +e; bun test scripts/check-phase85-operator-runbooks.test.ts; status="$?"; set -e; if [[ "$status" -eq 0 ]]; then echo "expected RED failure before checker implementation" >&2; exit 1; fi; git diff --check -- scripts/check-phase85-operator-runbooks.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase85-operator-runbooks.test.ts` exists.
    - The file imports `checkPhase85OperatorRunbooks` from `./check-phase85-operator-runbooks`.
    - The file contains all nine exact test names listed in the action.
    - The file contains `// Arrange`, `// Act`, and `// Assert`.
    - The initial RED run fails only because the checker module does not exist or is not implemented yet; after Task 2, the same command passes.
  </acceptance_criteria>
  <done>Phase 85 checker behavior is specified with focused Bun fixture tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement narrow Phase 85 checker</name>
  <files>scripts/check-phase85-operator-runbooks.ts</files>
  <read_first>
    <file>scripts/check-phase85-operator-runbooks.test.ts</file>
    <file>scripts/check-phase84-upgrade-rollback-policy.ts</file>
    <file>docs/parity/operator-runbooks.md</file>
    <file>docs/parity/index.json</file>
    <file>docs/parity/checklist.md</file>
    <file>README.md</file>
    <file>docs/operator/runtime-guide.md</file>
    <file>docs/parity/catalog/operator-runtime-release-hardening.md</file>
  </read_first>
  <behavior>
    - `checkPhase85OperatorRunbooks()` returns `[]` when all required Phase 85 roots, links, fields, and verifier commands exist after Task 3 wires `scripts/verify.sh`.
    - The checker returns clear failure strings for missing runbook headings, terms, command forms, preflight fields, no-progress evidence fields, recovery action classes, support timeline fields, redaction boundaries, parity roots, human pointers, and verifier order.
    - The checker rejects forbidden Phase 85 claims without scanning unrelated repository files.
  </behavior>
  <action>Create `scripts/check-phase85-operator-runbooks.ts` following the Phase 84 checker structure: fixed target-file list, `readText`, pure validation helpers, exported `checkPhase85OperatorRunbooks`, CLI failure reporting, and `import.meta.main`. Per D-17, validate only Phase 85 targets: `docs/parity/operator-runbooks.md`, `docs/parity/index.json`, `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/production-claim-boundary.md`, `docs/parity/support-matrix.md`, `docs/parity/upgrade-and-rollback-policy.md`, `README.md`, `docs/operator/runtime-guide.md`, `docs/parity/catalog/operator-runtime-release-hardening.md`, and `scripts/verify.sh`. Validate required headings, exact support terms, the exact Cargo/Bazel status and support-bundle command prefixes, D-02 preflight fields, D-05/D-06 evidence fields, exact D-05/D-07 monitoring terms `structured logs`, `metrics`, `support-bundle summaries`, `soak reports`, `live-smoke reports`, `checkpoint timeline`, `stalled subsystem`, `public-network opt-in`, `stay-current opt-in`, and `multi-day soak opt-in`, D-07/D-09 recovery classes, D-13 timeline labels, D-14 minimum bundle fields, D-15 forbidden evidence strings, `v1-8-operator-runbooks`, `v1_8_operator_runbooks`, exact RUN requirements, required evidence roots, human pointer links, and non-duplication of the runbook procedural table outside `docs/parity/operator-runbooks.md`. Per D-18, keep it deterministic and public-network-free; the checker must not run operator commands. Include verifier-order validation that strips the `VERIFY_COMMAND_ORDER` heredoc before confirming executed Phase 85 test and checker commands occur after `bun run scripts/check-phase84-upgrade-rollback-policy.ts`; Task 2 fixture tests may validate this behavior before Task 3 wires the real verifier, but the real-repository CLI run belongs to Task 3. Reject forbidden executable verifier strings `run-live-mainnet-smoke`, `systemctl`, `launchctl`, and `sleep 259200`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; bun test scripts/check-phase85-operator-runbooks.test.ts; bun --check scripts/check-phase85-operator-runbooks.ts; rg -n "OPEN_BITCOIN_PHASE85_REPO_ROOT|v1-8-operator-runbooks|v1_8_operator_runbooks|RUN-01|RUN-02|RUN-03|docs/parity/operator-runbooks.md|checkPhase85OperatorRunbooks|VERIFY_COMMAND_ORDER|structured logs|metrics|support-bundle summaries|soak reports|live-smoke reports|checkpoint timeline|stalled subsystem|public-network opt-in|stay-current opt-in|multi-day soak opt-in|run-live-mainnet-smoke|systemctl|launchctl|sleep 259200" scripts/check-phase85-operator-runbooks.ts; git diff --check -- scripts/check-phase85-operator-runbooks.ts scripts/check-phase85-operator-runbooks.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-phase85-operator-runbooks.ts` exists and exports `checkPhase85OperatorRunbooks`.
    - `bun test scripts/check-phase85-operator-runbooks.test.ts` passes.
    - `bun --check scripts/check-phase85-operator-runbooks.ts` passes.
    - `bun run scripts/check-phase85-operator-runbooks.ts` is deferred to Task 3 after `scripts/verify.sh` has real Phase 85 wiring.
    - The checker target-file list does not include broad repository globs.
    - The checker does not execute any operator command from documentation.
  </acceptance_criteria>
  <done>Phase 85 deterministic checker passes against the real docs and fixture drift tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 85 checker into scripts/verify.sh</name>
  <files>scripts/verify.sh</files>
  <read_first>
    <file>scripts/verify.sh</file>
    <file>scripts/check-phase85-operator-runbooks.ts</file>
    <file>scripts/check-phase85-operator-runbooks.test.ts</file>
    <file>scripts/check-phase84-upgrade-rollback-policy.ts</file>
  </read_first>
  <action>Per D-18, wire the Phase 85 test and checker immediately after Phase 84 in both the visible `VERIFY_COMMAND_ORDER` block and the executed `run_step` sequence. Add exactly these command lines to the visible order after `bun run scripts/check-phase84-upgrade-rollback-policy.ts`: `bun test scripts/check-phase85-operator-runbooks.test.ts` and `bun run scripts/check-phase85-operator-runbooks.ts`. Add exactly these executed steps after `run_step "check Phase 84 upgrade rollback policy" bun run scripts/check-phase84-upgrade-rollback-policy.ts`: `run_step "test Phase 85 operator runbooks checker" bun test scripts/check-phase85-operator-runbooks.test.ts` and `run_step "check Phase 85 operator runbooks" bun run scripts/check-phase85-operator-runbooks.ts`. Do not add public-network, real service-manager, multi-day, or operator runtime command execution to `scripts/verify.sh`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "bun test scripts/check-phase85-operator-runbooks.test.ts|bun run scripts/check-phase85-operator-runbooks.ts|test Phase 85 operator runbooks checker|check Phase 85 operator runbooks" scripts/verify.sh; bun test scripts/check-phase85-operator-runbooks.test.ts; bun run scripts/check-phase85-operator-runbooks.ts; git diff --check -- scripts/verify.sh scripts/check-phase85-operator-runbooks.ts scripts/check-phase85-operator-runbooks.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify.sh` visible command-order block contains the Phase 85 test and checker after Phase 84.
    - `scripts/verify.sh` executed `run_step` sequence contains the Phase 85 test and checker after Phase 84.
    - `bun run scripts/check-phase85-operator-runbooks.ts` validates the executed order, not only the heredoc order.
    - `scripts/verify.sh` does not add `run-live-mainnet-smoke`, `systemctl`, `launchctl`, or `sleep 259200` as executable default verification actions.
  </acceptance_criteria>
  <done>Default verification includes the narrow Phase 85 checker while preserving deterministic verifier boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files to checker | Markdown, JSON, and shell text are parsed as fixed local inputs. |
| Checker to verifier | Checker failures control whether `bash scripts/verify.sh` passes. |
| Verifier to local environment | Default verifier must not start public-network, real service-manager, or multi-day actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-11 | Tampering | `docs/parity/index.json` and human roots | mitigate | Checker validates exact surface id, audit key, RUN requirements, evidence roots, and human pointer links. |
| T-85-12 | Repudiation | Runbook evidence sufficiency | mitigate | Checker requires field-level evidence fields and rejects artifact-existence proof language. |
| T-85-13 | Information Disclosure | Support timeline docs | mitigate | Checker requires redaction boundaries and forbidden sensitive evidence strings. |
| T-85-14 | Denial of Service | `scripts/verify.sh` | mitigate | Checker validates executed verifier order and rejects public-network, service-manager, and multi-day default command strings. |
| T-85-15 | Elevation of Privilege | Checker scope | mitigate | Fixed target-file list avoids a broad Phase 88 scanner and avoids executing operator commands from documentation. |
</threat_model>

<verification>
Run focused verification:

```bash
bun test scripts/check-phase85-operator-runbooks.test.ts
bun --check scripts/check-phase85-operator-runbooks.ts
bun run scripts/check-phase85-operator-runbooks.ts
git diff --check -- scripts/check-phase85-operator-runbooks.ts scripts/check-phase85-operator-runbooks.test.ts scripts/verify.sh
```
</verification>

<success_criteria>
- Phase 85 checker tests pass.
- Phase 85 checker passes against the real repository state.
- `scripts/verify.sh` executes Phase 85 after Phase 84.
- The checker remains narrow to Phase 85 and does not become the Phase 88 broad claim scanner.
- Default verification remains deterministic, public-network-free, real-service-manager-free, and multi-day-free.
</success_criteria>

<output>
After completion, create `.planning/phases/85-operator-runbooks/85-03-SUMMARY.md`.
</output>
