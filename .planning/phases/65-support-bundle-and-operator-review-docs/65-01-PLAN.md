---
phase: 65-support-bundle-and-operator-review-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: [OBS-03]
user_setup: []
must_haves:
  truths:
    - "Support bundles remain local JSON/Markdown evidence, not raw report archives."
    - "Support bundle JSON and Markdown include compact v1.5 long-run/service/restart fields needed for unattended operation review."
    - "Credential contents, raw live-smoke reports, raw daemon tails, endpoint tables, wallet material, and unbounded logs are excluded from support evidence."
    - "Unavailable local evidence renders with explicit unavailable reasons."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      provides: "Allowlisted v1.5 live-smoke/support summary projection"
      contains: "restartResumeEvidence"
    - path: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      provides: "Markdown rendering for compact support evidence"
      contains: "Restart/resume evidence"
    - path: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      provides: "Support bundle redaction and v1.5 field regression tests"
      contains: "open_bitcoin_support_bundle"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support/live_smoke.rs"
      to: "packages/open-bitcoin-cli/tests/operator_binary.rs"
      via: "Fixture proves only allowlisted summary fields survive"
      pattern: "assert_absent"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-06-08T14-45-59
generated_at: 2026-06-08T14:58:00.000Z
---

<objective>
Extend support bundle evidence for v1.5 unattended operation review without
broadening redaction or default-verification boundaries.

Purpose: OBS-03 requires a redacted support bundle that summarizes long-run sync
cycles, service state, restart/recovery evidence, peer outcomes, progress
counters, stop reasons, metrics, logs, and config sources.
Output: Support bundle JSON/Markdown fields and focused redaction tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-support-bundle-and-operator-review-docs/65-CONTEXT.md
@.planning/phases/65-support-bundle-and-operator-review-docs/65-RESEARCH.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/live_smoke.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/tests/operator_binary.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the v1.5 support bundle fixture</name>
  <read_first>
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-CONTEXT.md` decisions D-01 through D-07
    - `packages/open-bitcoin-cli/tests/operator_binary.rs`
    - `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <behavior>
    - Add or extend a support bundle fixture that includes compact v1.5 evidence for `restartResumeEvidence`, `finalStatus`, `resourcePressure`, metrics availability, service state, and unavailable reasons.
    - The test asserts JSON contains `live_smoke.summary.restartResumeEvidence`, `live_smoke.summary.finalStatus`, `live_smoke.summary.finalStatus.resourcePressure`, `status.service`, `status.metrics`, and `status.logs`.
    - The test asserts Markdown contains `Restart/resume evidence`, `Final status`, `Resource pressure`, `Metrics samples`, and `Store Health`.
    - The test asserts forbidden raw markers remain absent: `stdoutTail`, `stderrTail`, `endpoint_outcomes`, `manualPeers`, `rpcpassword`, `raw daemon stdout tail`, `raw daemon stderr tail`, `snapshots`, `192.0.2.10:8333`, `__cookie__:live-smoke-secret`, and `seed phrase live-smoke-secret`.
  </behavior>
  <action>
    First make the fixture fail if any required v1.5 support field is missing.
    Reuse the existing `open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence`
    test when possible. Keep the test one-concern focused: compact support
    summary and redaction for v1.5 support review. Do not add public-network
    execution or real service-manager setup.
  </action>
  <acceptance_criteria>
    - `rg -n "restartResumeEvidence|Final status|Resource pressure|stdoutTail|endpoint_outcomes|manualPeers|seed phrase live-smoke-secret" packages/open-bitcoin-cli/tests/operator_binary.rs` finds the fixture assertions and forbidden marker checks.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence --all-features` exits 0 after implementation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle_summarizes_phase61_resource_recovery_evidence --all-features</automated>
  </verify>
  <done>Support bundle regression test pins v1.5 compact evidence and redaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend support summary projection and Markdown rendering</name>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support.rs`
    - `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/tests/operator_binary.rs`
  </read_first>
  <files>packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/live_smoke.rs, packages/open-bitcoin-cli/src/operator/support/render.rs</files>
  <behavior>
    - Support bundle JSON keeps `config`, `status`, `store_health`, `live_smoke`, and `redaction` as top-level evidence.
    - Live-smoke summary allowlists compact fields only; raw report arrays, endpoint tables, daemon tails, and option maps are not copied.
    - Markdown renders v1.5 fields that are present: restart/resume evidence, recovery diagnosis, final status, recovery category, resource pressure, metrics samples, and unavailable live-smoke reasons.
    - Missing evidence remains explicit through current unavailable `reason` fields.
  </behavior>
  <action>
    Update support summary helpers only where the Task 1 fixture proves a missing
    compact field or renderer label. Prefer small pure helpers in
    `support/live_smoke.rs` and `support/render.rs`. Keep `SupportEvidenceBundle`
    as the single support DTO unless a new bounded sub-structure is necessary.
    Do not ingest raw logs, raw reports, raw status snapshots, or raw endpoint
    tables.
  </action>
  <acceptance_criteria>
    - `rg -n "restartResumeEvidence|finalStatus|resourcePressure|Restart/resume evidence|Final status|Resource pressure" packages/open-bitcoin-cli/src/operator/support packages/open-bitcoin-cli/tests/operator_binary.rs` finds implementation and test coverage.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --all-features` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --all-features</automated>
  </verify>
  <done>Support bundle JSON and Markdown expose compact v1.5 evidence while preserving redaction boundaries.</done>
</task>

</tasks>

<verification>
Run:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --all-features
```

Do not run public-network live smoke or real service-manager commands in this
plan.
</verification>

<success_criteria>
- [ ] Support bundle tests cover compact v1.5 evidence fields.
- [ ] Support bundle tests prove raw live-smoke/report/service material remains absent.
- [ ] Support bundle Markdown renders the fields operators need for OBS-03.
- [ ] Focused `open-bitcoin-cli` support bundle tests pass.
</success_criteria>

<threat_model>
| Threat | Surface | Mitigation |
| --- | --- | --- |
| Credential or wallet data leakage | Support bundle JSON/Markdown | Keep metadata-only credential evidence, allowlist summary fields, and forbidden marker tests. |
| Raw endpoint or daemon log leakage | Live-smoke report ingestion | Do not copy raw report sections; test `stdoutTail`, `stderrTail`, endpoint tables, manual peers, and raw endpoints are absent. |
| Misleading support success signal | Operator support artifact | Preserve unavailable reasons and keep docs interpretation in Plan 65-02. |
</threat_model>
