---
phase: 65-support-bundle-and-operator-review-docs
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/parity/catalog/p2p.md
  - scripts/check-phase65-support-review.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/65-support-bundle-and-operator-review-docs/65-VERIFICATION.md
autonomous: true
requirements: [OBS-03, OBS-04]
user_setup: []
must_haves:
  truths:
    - "Operator docs present a v1.5 review sequence: deterministic checks first, opt-in long-run review, opt-in service review, support bundle collection, and field-based interpretation."
    - "Docs include repo-local Cargo and Bazel command forms for status, service status/restart, sync status, and support bundle collection."
    - "Docs and parity wording state that public-network long-run and real service-manager checks are opt-in UAT evidence outside default verification."
    - "Default verification runs a deterministic Phase 65 checker and still excludes public-network and real service-manager commands."
  artifacts:
    - path: "docs/operator/runtime-guide.md"
      provides: "v1.5 operator review and support bundle runbook"
      contains: "v1.5 operator review"
    - path: "scripts/check-phase65-support-review.ts"
      provides: "deterministic docs/support/default-verification boundary checker"
      contains: "check-phase65-support-review"
    - path: "scripts/verify.sh"
      provides: "default verification integration"
      contains: "check-phase65-support-review"
    - path: ".planning/phases/65-support-bundle-and-operator-review-docs/65-VERIFICATION.md"
      provides: "OBS-03/OBS-04 verification evidence"
      contains: "status: passed"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase65-support-review.ts"
      via: "Bun checker enforces docs and default-verification boundaries"
      pattern: "check-phase65-support-review"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-06-08T14-45-59
generated_at: 2026-06-08T14:58:00.000Z
---

<objective>
Document and deterministically guard v1.5 support bundle and operator review
workflows.

Purpose: OBS-04 requires copy-pasteable repo-local deterministic and opt-in
review commands, while OBS-03 requires support bundle field interpretation and
redaction boundaries.
Output: Runtime/architecture/parity docs, deterministic checker, verify
integration, and Phase 65 verification artifact.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-support-bundle-and-operator-review-docs/65-CONTEXT.md
@.planning/phases/65-support-bundle-and-operator-review-docs/65-RESEARCH.md
@.planning/phases/65-support-bundle-and-operator-review-docs/65-01-PLAN.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/parity/catalog/p2p.md
@scripts/check-phase64-service-restart-resume.ts
@scripts/check-phase62-sync-truth-surfaces.ts
@scripts/verify.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic Phase 65 support review checker</name>
  <read_first>
    - `scripts/check-phase64-service-restart-resume.ts`
    - `scripts/check-phase62-sync-truth-surfaces.ts`
    - `scripts/verify.sh`
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
    - `packages/open-bitcoin-cli/src/operator/support/live_smoke.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/tests/operator_binary.rs`
  </read_first>
  <files>scripts/check-phase65-support-review.ts, scripts/verify.sh</files>
  <behavior>
    - Checker requires support code/tests to mention compact v1.5 fields: `restartResumeEvidence`, `finalStatus`, `resourcePressure`, `Recovery diagnosis`, and `assert_absent`.
    - Checker requires runtime docs to contain `v1.5 operator review`, `bash scripts/verify.sh`, `bash scripts/test-run-live-mainnet-smoke.sh`, `support bundle --output-dir=/tmp/open-bitcoin-support`, Cargo support bundle command, Bazel support bundle command, Cargo status JSON command, Bazel status JSON command, Cargo service status command, Bazel service status command, and `opt-in UAT`.
    - Checker requires architecture docs to mention support bundles preserve `Unavailable` reasons and bounded support evidence.
    - Checker requires `scripts/verify.sh` to run `bun run scripts/check-phase65-support-review.ts`.
    - Checker fails if `scripts/verify.sh` contains `run-live-mainnet-smoke`, `--manual-peer`, `--restart-after-progress`, `systemctl --user`, or `launchctl`.
  </behavior>
  <action>
    Create `scripts/check-phase65-support-review.ts` by following the existing
    Bun checker style: `#!/usr/bin/env bun`, `readFileSync`, `requireContains`,
    `requireNotContains`, `requireAllContains`, and a clear success message
    `Phase 65 support review checks passed.` Add it to `scripts/verify.sh`
    immediately after the Phase 64 checker.
  </action>
  <acceptance_criteria>
    - `scripts/check-phase65-support-review.ts` contains `restartResumeEvidence`, `support bundle --output-dir=/tmp/open-bitcoin-support`, `bun run scripts/check-phase65-support-review.ts`, `run-live-mainnet-smoke`, `systemctl --user`, and `launchctl`.
    - `scripts/verify.sh` contains `bun run scripts/check-phase65-support-review.ts`.
    - `bun run scripts/check-phase65-support-review.ts` exits 0 after docs updates in Task 2.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase65-support-review.ts</automated>
  </verify>
  <done>Deterministic checker guards Phase 65 source/docs/default-verification boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document v1.5 operator review and support interpretation</name>
  <read_first>
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-CONTEXT.md` decisions D-08 through D-10
    - `docs/operator/runtime-guide.md`
    - `docs/architecture/status-snapshot.md`
    - `docs/architecture/operator-observability.md`
  </read_first>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md</files>
  <behavior>
    - Runtime guide includes a `v1.5 operator review` section.
    - Runtime guide lists deterministic commands: `bash scripts/verify.sh` and `bash scripts/test-run-live-mainnet-smoke.sh`.
    - Runtime guide lists repo-local Cargo and Bazel forms for `sync status --format json`, `status --format json`, `service status`, `service restart`, and `support bundle --output-dir=/tmp/open-bitcoin-support`.
    - Runtime guide states public-network long-run review, manual peers, restart-after-progress, and real service-manager review are `opt-in UAT` outside default verification.
    - Runtime guide explains support bundle pass/fail interpretation using fields: `support-evidence.json`, `support-evidence.md`, `live_smoke.summary.finalStatus`, `restartResumeEvidence`, `recoveryCategory`, `resourcePressure`, `status.service.restart_resume`, `status.metrics`, `status.logs`, and unavailable reasons.
    - Architecture docs explain support bundles preserve shared snapshot `Unavailable` reasons and bounded support evidence rather than raw reports/logs.
  </behavior>
  <action>
    Add concise operator-facing documentation, not marketing copy. Keep commands
    copy-pasteable from repo root. Do not say Open Bitcoin is a production full
    node, production service, packaged service guarantee, hosted dashboard, or
    broad drop-in replacement.
  </action>
  <acceptance_criteria>
    - `rg -n "v1.5 operator review|support-evidence.json|live_smoke.summary.finalStatus|restartResumeEvidence|status.service.restart_resume|opt-in UAT|support bundle --output-dir=/tmp/open-bitcoin-support" docs/operator/runtime-guide.md` finds the review section and interpretation fields.
    - `rg -n "Support bundles embed this same snapshot|Unavailable|bounded support evidence|raw daemon log|raw peer table" docs/architecture/status-snapshot.md docs/architecture/operator-observability.md` finds the architecture boundary text.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "v1.5 operator review|support-evidence.json|restartResumeEvidence|opt-in UAT" docs/operator/runtime-guide.md</automated>
  </verify>
  <done>Operator docs explain deterministic and opt-in review commands plus support bundle interpretation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Preserve parity/release boundaries and write verification</name>
  <read_first>
    - `docs/parity/catalog/p2p.md`
    - `docs/parity/release-readiness.md`
    - `docs/parity/index.json`
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-CONTEXT.md`
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-01-SUMMARY.md` if present
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-02-SUMMARY.md` if present
  </read_first>
  <files>docs/parity/catalog/p2p.md, .planning/phases/65-support-bundle-and-operator-review-docs/65-VERIFICATION.md, docs/metrics/lines-of-code.md</files>
  <behavior>
    - Parity docs mention v1.5 support bundle/operator review only as local redacted evidence for extended operator review.
    - Parity docs do not broaden production-node, inbound serving, relay, production wallet, migration apply, packaging, hosted dashboard, or GUI claims.
    - `65-VERIFICATION.md` frontmatter includes `status: passed` only after required checks pass.
    - Verification maps OBS-03 and OBS-04 to the support bundle tests, checker, docs, and `bash scripts/verify.sh` results.
  </behavior>
  <action>
    Update parity wording only if needed to make Phase 65 evidence discoverable
    without creating release-boundary drift. Then run focused checks and
    `bash scripts/verify.sh`. If verification refreshes
    `docs/metrics/lines-of-code.md`, keep the generated freshness change.
    Write `65-VERIFICATION.md` with status `passed` and command evidence only
    after commands pass.
  </action>
  <acceptance_criteria>
    - `.planning/phases/65-support-bundle-and-operator-review-docs/65-VERIFICATION.md` contains `status: passed`, `OBS-03`, `OBS-04`, `bun run scripts/check-phase65-support-review.ts`, and `bash scripts/verify.sh`.
    - `bash scripts/verify.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/check-phase65-support-review.ts</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --all-features</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <done>Phase 65 verification passes with OBS-03/OBS-04 evidence and no default-verification drift.</done>
</task>

</tasks>

<verification>
Run:

```bash
bun run scripts/check-phase65-support-review.ts
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli open_bitcoin_support_bundle --all-features
bash scripts/verify.sh
```
</verification>

<success_criteria>
- [ ] Runtime guide contains v1.5 deterministic and opt-in operator review commands.
- [ ] Architecture docs preserve support bundle shared-snapshot and bounded-evidence semantics.
- [ ] Phase 65 checker is wired into default deterministic verification.
- [ ] Default verification excludes public-network and real service-manager commands.
- [ ] `65-VERIFICATION.md` records passed OBS-03/OBS-04 evidence.
</success_criteria>

<threat_model>
| Threat | Surface | Mitigation |
| --- | --- | --- |
| Public-network checks enter default verification | `scripts/verify.sh` | Phase 65 checker asserts forbidden live-smoke/manual-peer/restart flags are absent. |
| Real service-manager operations enter default verification | `scripts/verify.sh` | Phase 65 checker asserts `systemctl --user` and `launchctl` are absent. |
| Release claim drift | Runtime/parity docs | Keep wording scoped to local redacted support evidence and opt-in extended operator review. |
| Operator misreads bundle existence as pass evidence | Runtime guide | Document exact fields and pass/fail interpretation. |
</threat_model>
