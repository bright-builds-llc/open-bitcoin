---
phase: 36
phase_name: "Mainnet Peer Discovery and Outbound Lifecycle"
plan_id: "36-03"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: "36-2026-05-01T22-57-33"
generated_at: "2026-05-01T23:05:08.840Z"
status: pending
---

# Plan 36-03: Peer Telemetry And Status Truthfulness

## Objective

Project Phase 36 peer lifecycle truth into the shared sync/status/log contracts so later phases can expose the same evidence without reverse-engineering socket or DNS details.

## Requirements

- PEERMAIN-04: Record peer source, negotiated network, capabilities, contribution, failure reason, and last activity without leaking low-level socket details into pure-core crates.
- RESUME-02: Surface typed runtime failures as operator-visible health signals.
- VERMAIN-02: Deterministic tests cover status and telemetry truthfulness.

## Files

- `packages/open-bitcoin-node/src/status.rs`
- `packages/open-bitcoin-node/src/sync/types.rs`
- `packages/open-bitcoin-node/src/sync.rs`
- `packages/open-bitcoin-node/src/sync/tests.rs`
- `packages/open-bitcoin-cli/src/operator/status/tests.rs`
- `packages/open-bitcoin-cli/tests/operator_binary.rs`

## Tasks

<task id="T36-03-01" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-node/src/status.rs</file>
    <file>packages/open-bitcoin-node/src/sync/types.rs</file>
  </read_first>
  <action>Extend the shared peer-status and sync-summary domain to carry Phase 36 lifecycle telemetry: source, endpoint label, negotiated capability summary, attempts, contribution counters, last activity, and typed terminal reason. Preserve compatibility for existing peer-count consumers by appending optional truth rather than replacing existing fields.</action>
  <acceptance_criteria>
    <item>`PeerStatus` still exposes peer counts.</item>
    <item>New telemetry fields avoid raw socket objects or direct DNS internals.</item>
    <item>Typed failure reasons remain machine-readable.</item>
  </acceptance_criteria>
</task>

<task id="T36-03-02" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-node/src/sync.rs</file>
    <file>packages/open-bitcoin-node/src/sync/tests.rs</file>
  </read_first>
  <action>Project the richer lifecycle state into `SyncRunSummary`, structured log records, and health signals, including concise peer-rotation/failure evidence that later phases can consume. Keep messages operator-actionable without leaking absolute paths, credentials, or low-level socket dumps.</action>
  <acceptance_criteria>
    <item>Summary/status/log records distinguish resolver failures, transport failures, stalls, and invalid-data disconnects.</item>
    <item>Contribution counters and last-activity timestamps stay truthful in tests.</item>
    <item>Phase 16-style metrics/log behavior remains deterministic.</item>
  </acceptance_criteria>
</task>

<task id="T36-03-03" owner="inline">
  <read_first>
    <file>packages/open-bitcoin-cli/src/operator/status/tests.rs</file>
    <file>packages/open-bitcoin-cli/tests/operator_binary.rs</file>
  </read_first>
  <action>Add status-surface regression tests so CLI JSON/human status consumers stay compatible when richer peer telemetry is present or unavailable.</action>
  <acceptance_criteria>
    <item>Existing status snapshots remain valid for stopped or unavailable states.</item>
    <item>JSON serialization for new peer telemetry fields is covered.</item>
    <item>No current CLI tests need live network access.</item>
  </acceptance_criteria>
</task>

## Threat Model

| Threat | Severity | Mitigation |
| --- | --- | --- |
| Status surfaces become incompatible with existing consumers | High | Preserve peer counts and use additive availability-wrapped fields. |
| Telemetry leaks low-level or sensitive transport details | Medium | Project only shell-safe labels, counters, and typed reasons. |
| Richer logs/status become non-deterministic | Medium | Use explicit timestamps/counters in tests and keep live-network behavior opt-in only. |

## Verification

- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node sync::`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli operator::status::`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
