---
phase: 136-receive-independent-maintenance-and-transport-receipts
plan: "06"
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/recovery.rs
  - packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/retry.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [IBR-01, IBR-02, IBR-03, IBR-04]
must_haves:
  truths:
    - "A shutdown-aware open-bitcoind timer wakes with no inbound messages and issues one maintenance_tick(RetryDecisionContext) per due cycle."
    - "Each tick walks only unbroadcast_members with inspect=256 / prepare=32, remints a fresh 10-to-15-minute due time, and does not persist the cursor or due time."
    - "Local submit plus drain_tx_fanout_emissions prepares first-hop TX INV without waiting 10 minutes and without the inbound receive loop."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs
      provides: "Handle-owned maintenance tick and TX INV drain"
      contains: "maintenance_tick"
    - path: packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs
      provides: "Checkpoint-style shutdown-aware retry worker"
      contains: "start_initial_broadcast_retry_worker"
  key_links:
    - from: packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority.rs
      via: "maintenance_tick with shell-sampled now and jitter"
      pattern: "maintenance_tick"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 136-2026-08-15T21-24-17
generated_at: 2026-08-15T22:20:00.000Z
---

<objective>
Wake receive-independent maintenance from open-bitcoind and drain first-hop plus retry INV through generalized TX emissions.

Purpose: Idle nodes retry; first hop still happens without inbound messages; clocks and jitter stay in the shell.
Output: `maintenance_tick`, `drain_tx_fanout_emissions`, and a checkpoint-style retry worker that is not a public-default relay loop.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md
@packages/open-bitcoin-rpc/src/bin/open_bitcoind/checkpoint.rs
@packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
@packages/open-bitcoin-node/src/network/runtime_authority.rs
@packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
@packages/open-bitcoin-node/src/network/relay_fanout.rs

<interfaces>
Plan 01 (must call, not reimplement):

```rust
pub fn next_retry_due_unix_seconds(context: RetryDecisionContext) -> Option<i64>;
pub fn retry_cycle_is_due(observed_at_unix_seconds: i64, due_at_unix_seconds: i64) -> bool;
pub fn select_maintenance_identities<T: Copy + Ord>(
    members: &BTreeSet<T>,
    maybe_after: Option<T>,
    inspect: MaintenanceInspectBudget,
    prepare: MaintenancePrepareBudget,
) -> MaintenanceIdentitySelection<T>;
pub const MAINTENANCE_INSPECT_BUDGET: usize = 256;
pub const MAINTENANCE_PREPARE_BUDGET: usize = 32;
```

Plan 05:

```rust
pub(in crate::network) fn enqueue_retry_admissions(
    &mut self,
    identities: &[MempoolMemberIdentity],
) -> Vec<TxFanoutAction>;
```

Plan 03:

```rust
PeerEmission::try_new_tx_inventory(peer_id, WireNetworkMessage::Inv(...), member, capability)
```

Checkpoint worker to copy (do not couple to DurableSyncRuntime):

```rust
// packages/open-bitcoin-rpc/src/bin/open_bitcoind/checkpoint.rs
pub(super) fn start_mempool_checkpoint_worker(...) -> Option<MempoolCheckpointWorker>;
pub(super) fn checkpoint_worker_loop<Wait, Now>(...)
```

`LifecycleCommand::Maintenance` is the expiry/pressure projector. Do not overload it with retry scheduling. Add `ManagedNetworkHandle::maintenance_tick`.

In-memory only on `ManagedPeerNetwork` (never snapshot fields):
- `maybe_retry_due_at_unix_seconds: Option<i64>`
- `maybe_unbroadcast_walk_cursor: Option<MempoolMemberIdentity>`

Recovery install and handle construction must set both to `None` (D-06 remint).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add maintenance_tick and first-hop TX INV drain on the handle</name>
  <read-first>
    <file>.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority.rs</file>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs</file>
    <file>packages/open-bitcoin-node/src/network/relay_fanout.rs</file>
    <file>packages/open-bitcoin-node/src/network/announcement_transport.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/recovery.rs</file>
    <file>packages/open-bitcoin-rpc/src/context/network.rs</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/recovery.rs, packages/open-bitcoin-node/src/network/tests/maintenance_tick_cases.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-rpc/src/context/network.rs</files>
  <behavior>
    - `maintenance_tick(RetryDecisionContext)` under the authority lock: if `maybe_retry_due_at_unix_seconds` is `None` or `retry_cycle_is_due`, walk `unbroadcast_members` only via `select_maintenance_identities` with `MaintenanceInspectBudget::production()` and `MaintenancePrepareBudget::production()`, call `enqueue_retry_admissions(&selection.prepare)`, store `maybe_next_after` as the cursor, remint `maybe_retry_due_at_unix_seconds` from `next_retry_due_unix_seconds(context)`, then release the lock before any socket I/O.
    - If `next_retry_due_unix_seconds` returns `None`, return a typed error `MaintenanceTickError::DueTimeOverflow` and do not walk.
    - If the tick is not due, return empty prepare/drain work and leave the cursor unchanged.
    - `drain_tx_fanout_emissions(now_unix_seconds)` calls existing `drain_relay_fanout`, then for each `(peer_id, WireNetworkMessage::Inv(_))` builds `try_new_tx_inventory` after `PrepareRelay`. It does not use `receive_message` / `receive_message_for_durable_serving`.
    - `submit_local_transaction_outcome_at` stays enqueue-only. `submit_local_transaction_with_relay_evidence_at` in `context/network.rs` must call `drain_tx_fanout_emissions(now_unix_seconds)` after a successful accept so first-hop INV is prepared without a 10-minute wait (D-07).
    - Recovery install and `ManagedNetworkHandle` construction set `maybe_retry_due_at_unix_seconds = None` and `maybe_unbroadcast_walk_cursor = None`.
    - Authority is not held across encode/write. Returned `PeerEmission` values are owned drain work.
  </behavior>
  <action>
    Per D-05, D-06, D-07, D-16, D-17, and D-19, add `packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs` and `mod maintenance;` from `runtime_authority.rs` (foo.rs + foo/ already exists for runtime_authority). Do not add Tokio to `open-bitcoin-node`. Do not sample `SystemTime` or `getrandom` in this crate.

    Add these exact public handle methods:

    ```rust
    pub fn maintenance_tick(
        &amp;self,
        context: RetryDecisionContext,
    ) -&gt; Result&lt;MaintenanceTickOutcome, ManagedNetworkAuthorityError&gt;;

    pub fn drain_tx_fanout_emissions(
        &amp;self,
        now_unix_seconds: i64,
    ) -&gt; Result&lt;Vec&lt;PeerEmission&gt;, ManagedNetworkAuthorityError&gt;;
    ```

    `MaintenanceTickOutcome` must include:
    - `inspected_count: usize`
    - `prepared_count: usize`
    - `leftover_unattempted_count: usize`
    - `maybe_next_due_unix_seconds: Option&lt;i64&gt;`
    - `emissions: Vec&lt;PeerEmission&gt;` (result of `drain_tx_fanout_emissions` after enqueue, so the caller need not remember to drain)

    Walk only `network.unbroadcast_members` (IBR-01). Use production budgets 256/32. Do not record queue-cap/rate-limit/suppress as attempts for identities outside `selection.prepare` (D-11).

    Wire `submit_local_transaction_with_relay_evidence_at` to drain after accept. Do not drain inside the mempool mutation itself. Existing node tests that call `submit_local_transaction_outcome_at` directly and assert `queued_transactions == 1` must keep passing.

    Recovery: in the `InstallRecovery` apply path in `lifecycle_projection/recovery.rs` (or the field-assign site that sets `unbroadcast_members`), set the two timer fields to `None`. Do not persist them in snapshots.

    Register `mod maintenance_tick_cases;` and add tests named exactly:
    - `maintenance_tick_walks_only_unbroadcast_members`
    - `maintenance_tick_respects_inspect_256_and_prepare_32_with_leftover_cursor`
    - `maintenance_tick_not_due_is_a_noop`
    - `maintenance_tick_remints_due_time_from_injected_context`
    - `recovery_install_clears_due_time_and_cursor`
    - `local_accept_plus_drain_prepares_first_hop_inv_without_advancing_ten_minutes`
    - `maintenance_tick_does_not_call_receive_message`

    For the inspect/prepare test, insert 40 local unbroadcast members (or the smallest N &gt; 32 that the fixture can build), call `maintenance_tick` with `observed_at_unix_seconds` due, and assert `prepared_count == 32`, `inspected_count == 40.min(256)`, `leftover_unattempted_count &gt;= 8`, and a second tick with a later due time prepares a previously leftover identity (cursor progress). If building 40 real transactions is too expensive, use a `#[cfg(test)]` helper that inserts identities into `unbroadcast_members` plus matching fanout metadata — but the walk source must still be `unbroadcast_members`, not `mempool.entries()`.

    The first-hop test: local accept, `relay_activation` enabled, one eligible peer, `now` unchanged from admission, call `drain_tx_fanout_emissions` (or the RPC wrapper), assert at least one `PeerEmission` whose message is `Inv` and `is_transaction_inventory()`. Assert the test never sleeps and never calls `receive_message`.

    rustdoc on `maintenance_tick` must say: this is initial-broadcast retry for the local unbroadcast set; it is not public/default relay and does not guarantee propagation (D-04, D-19).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-06-tick -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features maintenance_tick -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn maintenance_tick' packages/open-bitcoin-node/src/network/runtime_authority.rs packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs` matches.</criterion>
    <criterion>`rg -n 'unbroadcast_members' packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs` matches the walk source.</criterion>
    <criterion>`rg -n 'mempool\\.entries' packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs` returns no matches.</criterion>
    <criterion>`rg -n 'DurableSyncRuntime|receive_message_for_durable_serving' packages/open-bitcoin-node/src/network/runtime_authority/maintenance.rs` returns no matches.</criterion>
    <criterion>`rg -n 'maybe_retry_due_at_unix_seconds' packages/open-bitcoin-node/src/storage` returns no matches (due time is not durable).</criterion>
    <criterion>The named Cargo filter reports the seven new tests passing.</criterion>
  </acceptance-criteria>
  <done>Handle-owned ticks walk only unbroadcast members, remint injected due times, and drain first-hop INV without the receive loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the open-bitcoind shutdown-aware retry worker</name>
  <read-first>
    <file>packages/open-bitcoin-rpc/src/bin/open_bitcoind/checkpoint.rs</file>
    <file>packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs</file>
    <file>packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/checkpoint.rs</file>
    <file>packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs</file>
    <file>packages/open-bitcoin-rpc/src/bin/open_bitcoind/runtime_control.rs</file>
    <file>docs/parity/source-breadcrumbs.json</file>
    <file>standards/core/architecture.md</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs, packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests.rs, packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/retry.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - `start_initial_broadcast_retry_worker(handle)` always starts (no store required), using `mpsc` + `thread` + `recv_timeout` like the checkpoint worker.
    - Production `Now` uses `current_timestamp_unix_seconds` / `SystemTime`. Production jitter uses `getrandom::fill` into `RetryJitterSeconds::new`. If `getrandom` fails or jitter is out of range, the worker returns `RetryWait::JitterUnavailable` and waits the previous interval again; it must not substitute a silent `0` or `300`.
    - Tests inject `wait`, `now`, and `jitter` with no `thread::sleep` and no wall-clock gate.
    - Shutdown via the worker's sender joins the thread. `open-bitcoind.rs` starts the worker next to `start_mempool_checkpoint_worker` and shuts it down on process exit. It is not started from `DurableSyncRuntime` or IBD.
    - The worker is not a public-default relay loop: rustdoc states relay activation and peer eligibility still gate emissions, and a successful tick does not claim guaranteed propagation.
  </behavior>
  <action>
    Per D-16, D-17, and D-18, create `packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs` by copying the injectible `wait` / `now` shape from `checkpoint.rs` without importing checkpoint types into the retry policy.

    Exact names:
    - `enum RetryWait { Elapsed, Shutdown, JitterUnavailable }`
    - `fn start_initial_broadcast_retry_worker(handle: ManagedNetworkHandle) -> InitialBroadcastRetryWorker`
    - `fn retry_worker_loop&lt;Wait, Now, Jitter&gt;(handle, wait, now, jitter) -> Result&lt;(), DaemonRetryError&gt;`
    - `struct InitialBroadcastRetryWorker { shutdown_sender, join_handle }` with `shutdown(self) -> Result&lt;(), DaemonRetryError&gt;`

    Loop:
    1. Sample `now` and `jitter`. On jitter failure, `wait` the last interval (or `Duration::from_secs(600)` only as the initial wait before any successful sample — document this as `RetryWait::JitterUnavailable`, not as a public cadence claim) and continue.
    2. Build `RetryDecisionContext::new(now, jitter)`.
    3. Call `handle.maintenance_tick(context)`.
    4. Compute the next wait from `maybe_next_due_unix_seconds` minus `now`, minimum `Duration::from_secs(1)`, never a negative duration.
    5. `wait(next)` → `Elapsed` continues, `Shutdown` returns.

    In `open-bitcoind.rs` add `#[path = "open_bitcoind/retry.rs"] mod retry;` and start/shutdown beside the checkpoint worker. Do not start the worker inside `start_daemon_sync_worker` or `DurableSyncRuntime::open`.

    Add `mod retry;` in `open_bitcoind/tests.rs` and create `tests/retry.rs` named exactly:
    - `retry_worker_fires_maintenance_tick_on_elapsed_without_sleep`
    - `retry_worker_shutdown_stops_before_next_tick`
    - `retry_worker_jitter_unavailable_does_not_use_silent_constant_jitter`
    - `retry_worker_is_not_started_from_durable_sync_runtime`

    Mirror the checkpoint test style (`Arc&lt;Mutex&lt;Vec&lt;Duration&gt;&gt;&gt;` wait observations, injected now/jitter). The DurableSyncRuntime test asserts `DurableSyncRuntime::open` source does not call `start_initial_broadcast_retry_worker` (`rg` in the test file's comment is not enough — assert by reading that `start_daemon_sync_worker` / `DurableSyncRuntime` modules do not reference the retry worker, or by starting a runtime in the existing checkpoint fixture and asserting no extra retry thread is required for sync). Prefer a source-level unit test in the retry test module that the retry start symbol is invoked only from `open-bitcoind.rs` main after checkpoint start: a simple `include_str!` assertion that `durable` / `DurableSyncRuntime` files do not contain `start_initial_broadcast_retry_worker`.

    Register new first-party Rust sources in `docs/parity/source-breadcrumbs.json` under a new group `node-initial-broadcast-retry` with breadcrumbs:
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/bitcoin-knots/src/bitcoind.cpp`
    - `packages/bitcoin-knots/test/functional/mempool_unbroadcast.py`

    Files to register: `runtime_authority/maintenance.rs`, `open_bitcoind/retry.rs`, `tests/maintenance_tick_cases.rs`, `open_bitcoind/tests/retry.rs`. Do not claim public/default/production relay or guaranteed propagation in the group label or comments.

    Do not add RPC/dashboard retry counters (Phase 137). Do not add claim-guardrail closeout (Phase 138) beyond the rustdoc sentence that this worker is not public-default relay and does not guarantee propagation.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-06-timer -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --bin open-bitcoind --all-features retry_worker -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn start_initial_broadcast_retry_worker' packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs` matches.</criterion>
    <criterion>`rg -n 'start_initial_broadcast_retry_worker' packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` matches beside `start_mempool_checkpoint_worker`.</criterion>
    <criterion>`rg -n 'start_initial_broadcast_retry_worker' packages/open-bitcoin-node/src/sync packages/open-bitcoin-rpc/src/bin/open_bitcoind/sync_seed.rs` returns no matches.</criterion>
    <criterion>`rg -n 'getrandom' packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs` matches the production jitter path, and `rg -n 'RetryJitterSeconds::new\\(0\\)|RetryJitterSeconds::new\\(300\\)' packages/open-bitcoin-rpc/src/bin/open_bitcoind/retry.rs` returns no silent fallback.</criterion>
    <criterion>`rg -n 'thread::sleep' packages/open-bitcoin-rpc/src/bin/open_bitcoind/tests/retry.rs` returns no matches.</criterion>
    <criterion>`rg -n 'node-initial-broadcast-retry' docs/parity/source-breadcrumbs.json` matches.</criterion>
    <criterion>The named Cargo filter reports the four worker tests passing.</criterion>
  </acceptance-criteria>
  <done>Idle daemons remint a fresh injected cycle without receive-loop or IBD coupling, and default verification stays hermetic.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Shell timer → handle | `now` and jitter are untrusted injected facts; policy must validate jitter via `RetryJitterSeconds::new`. |
| Authority lock → sockets | Emissions are prepared under lock and written after release. |
| Worker → public network | Starting a timer must not imply default-on public relay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-136-06-01 | Denial of Service | timer / lock-across-I/O | mitigate | `maintenance_tick` prepares at most 32 identities, then returns owned `PeerEmission` values. Encode/write happens after `try_mutate` returns. Inspect 256 bounds the lock-hold walk. |
| T-136-06-02 | Denial of Service | retry amplification | mitigate | One process-global cycle; leftover identities wait for the next reminted 10-to-15-minute window. PHASE104 drain 16 still applies. |
| T-136-06-03 | Information Disclosure | unbroadcast fingerprinting | mitigate | Walk `unbroadcast_members` only (5,000-cap local set). Fresh `0..=300` jitter every remint; `getrandom` failure is `JitterUnavailable`, not a fixed public cadence. |
| T-136-06-04 | Tampering | stale receipt after drain | mitigate | TX INV uses `try_new_tx_inventory`; Plan 04 still requires `TransactionResponse` + `Applied` to clear. |
| T-136-06-05 | Elevation of Privilege | claim boundary | mitigate | rustdoc and breadcrumbs forbid public/default/production relay and guaranteed-propagation wording. Relay-disabled operation may admit locally and emit no fanout. |
| T-136-06-06 | Tampering | receive-loop piggyback | mitigate | Worker is started from `open-bitcoind.rs` only; `DurableSyncRuntime` and inbound receive paths must not own the timer. |
</threat_model>

<verification>
Run the timed node maintenance-tick suite, then the timed open-bitcoind retry-worker suite. Do not overlap Cargo jobs against the same target directory. Run `bun run scripts/check-parity-breadcrumbs.ts` after registering new sources.
</verification>

<success_criteria>
- D-05 through D-08 wakeup/remint and D-16 through D-19 shell ownership are implemented.
- IBR-02 has a receive-independent timer; IBR-01 walk set is `unbroadcast_members`; IBR-03 drain reuses TX INV PeerEmission; IBR-04 claim wording does not promise propagation.
- Default verification has no wall-clock or public-network gate.
</success_criteria>

<output>
After completion, create `.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-06-SUMMARY.md`
</output>
