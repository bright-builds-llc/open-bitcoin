---
phase: 136-receive-independent-maintenance-and-transport-receipts
plan: "04"
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - packages/open-bitcoin-node/src/network/tests/getdata_tx_receipt_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
autonomous: true
requirements: [IBR-04]
must_haves:
  truths:
    - "Eligible GETDATA TX classify (EligibleServe) does not remove unbroadcast membership before the TX bytes are written."
    - "A fresh acknowledge_write of WireNetworkMessage::Tx applies MempoolRetryClearCause::TransportWritten and removes the member."
    - "Failed encode, write abort, AlreadyApplied, and AchievedButStale leave the member retry-eligible."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/inventory.rs
      provides: "GETDATA TX promoted to PreparedTxServe(PeerEmission)"
      contains: "PreparedTxServe"
    - path: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      provides: "Applied-only TransportWritten membership mutation"
      contains: "TransportWritten"
  key_links:
    - from: packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      via: "complete_peer_emission after successful TX write"
      pattern: "complete_peer_emission"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 136-2026-08-15T21-24-17
generated_at: 2026-08-15T22:20:00.000Z
---

<objective>
Promote eligible GETDATA TX serves to a TX-response PeerEmission and clear unbroadcast only after a fresh successful write.

Purpose: Match Knots "not INV" acknowledgement while keeping Open Bitcoin's stronger successful-write receipt (D-02).
Output: PreparedTxServe plan items, Applied-only TransportWritten, and no pre-write EligibleServe clears.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md
@packages/open-bitcoin-node/src/network/inventory.rs
@packages/open-bitcoin-node/src/network/types.rs
@packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
@packages/open-bitcoin-mempool/src/pool/lifecycle.rs

<interfaces>
Today GETDATA TX is Immediate and has no receipt:

```rust
// inventory.rs gate_inventory_for_durable_serving
if decision.label != TxServeOutcomeLabel::Served { missing; continue; }
response_plan.push(ManagedInboundResponsePlanItem::Immediate(
    WireNetworkMessage::Tx(transaction.clone()),
));

// types.rs
pub enum ManagedInboundResponsePlanItem {
    Immediate(WireNetworkMessage),
    DurableBlock(ManagedBlockServeIntent),
}

// context.rs InboundWireResponsePlan::resolve
Immediate(message) => resolved.push_encoded(message, None, magic);

// connection_runtime.rs acknowledge_inbound_response_write
acknowledge_wire_message_written(&response.message) // not complete_peer_emission
```

Plan 03 constructors to call:

```rust
PeerEmission::try_new_tx_response(peer_id, WireNetworkMessage::Tx(tx), member, capability)
```

`complete_peer_effect` already returns `EffectCompletion::{Applied, AchievedButStale, AlreadyApplied}` and records compact evidence only when `is_fresh`. Apply `TransportWritten` only on `Applied` + `write_kind.is_transaction_response()`.

Precedence stays `LifecycleRemoval > TransportWritten > EligibleServe`. Do not write `EligibleServe` into `retry_clears` (D-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote eligible GETDATA TX to PreparedTxServe</name>
  <read-first>
    <file>.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md</file>
    <file>packages/open-bitcoin-node/src/network/inventory.rs</file>
    <file>packages/open-bitcoin-node/src/network/types.rs</file>
    <file>packages/open-bitcoin-node/src/network/announcement_transport.rs</file>
    <file>packages/open-bitcoin-rpc/src/context.rs</file>
    <file>packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/inventory.rs, packages/open-bitcoin-node/src/network/types.rs, packages/open-bitcoin-rpc/src/context.rs, packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs, packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs</files>
  <behavior>
    - When `classify_request` returns `TxServeOutcomeLabel::Served` with a transaction, `gate_inventory_for_durable_serving` pushes `ManagedInboundResponsePlanItem::PreparedTxServe(PeerEmission)` built by `try_new_tx_response` after `PrepareRelay`, not `Immediate(Tx)`.
    - Classify-only helpers such as `serve_inventory` may still return `WireNetworkMessage::Tx` for existing tests, but they must not remove unbroadcast membership.
    - `InboundWireResponsePlan::resolve` encodes the TX bytes and carries `PeerEmissionWriteCapability` on `EncodedWireResponse`.
    - Encode failure or constructor `None` aborts the reserved peer effect and does not push a successful encoded TX.
    - `acknowledge_inbound_response_write` on `WriteWireMessageOutcome::Written` calls `complete_peer_emission(capability.acknowledge_write())`. On write failure it calls `AbortPeerEffect` / the existing abort path and does not complete.
  </behavior>
  <action>
    Per D-02 and D-03, add this exact variant to `ManagedInboundResponsePlanItem`:

    ```rust
    PreparedTxServe(Box&lt;PeerEmission&gt;),
    ```

    `PeerEmission` is not `Clone`. Remove `Clone` from `ManagedInboundResponsePlanItem` if it cannot be derived; update any compile break that required Clone (prefer moving the value, not cloning). Keep `Immediate` for non-TX messages that today use it.

    In `gate_inventory_for_durable_serving`, for `InventoryType::Transaction | WitnessTransaction` when `decision.label == TxServeOutcomeLabel::Served` and `maybe_transaction` is `Some`:
    1. Resolve `MempoolMemberIdentity { txid, wtxid }` from the served transaction via the crate's canonical `transaction_txid` / `transaction_wtxid` (same functions inventory.rs already imports under `cfg(test)` — promote those imports to production in this file).
    2. Call `prepare_peer_emission` is compact-only today; do not use it. Instead `apply_lifecycle_command(LifecycleCommand::PrepareRelay(PeerRelayPreparationRequest::new(peer_id)))` then `PeerEmission::try_new_tx_response(...)`.
    3. Push `PreparedTxServe(Box::new(emission))`.
    4. If prepare or constructor fails, push nothing for that request (treat as missing) and abort any reserved capability. Do not push `Immediate(Tx)` as a fallback that would skip the receipt.

    Extend `EncodedWireResponse` with `maybe_tx_write_capability: Option&lt;PeerEmissionWriteCapability&gt;`. In `resolve`, match `PreparedTxServe`, encode `emission.message()`, and store the capability. If encode fails, abort the capability and set `failed` the same way durable-block lookup failure does.

    Update `acknowledge_inbound_response_write` / `acknowledge_encoded_wire_response`:
    - If `maybe_tx_write_capability` is `Some` and `was_written`, `complete_peer_emission(capability.acknowledge_write())`.
    - If `maybe_tx_write_capability` is `Some` and not written, abort via `AbortPeerEffect` using `capability.into_effect_capability()` (already `pub(in crate::network)` — add a handle abort wrapper on `ManagedNetworkHandle` if RPC cannot see `pub(in crate::network)`, named `abort_peer_effect(capability: PeerEffectCapability)` if one does not already exist; reuse `AbortPeerEffect` if a public abort already exists).
    - Do not call `acknowledge_wire_message_written` as the TX-clear path.

    Update `relay_serving_cases.rs` tests that currently assert `outbound == vec![WireNetworkMessage::Tx(...)]` after classify/serve so they still pass against `serve_inventory` (classify/serve bytes) and do not assert unbroadcast empty. Add a comment on any classify test: EligibleServe is semantic and does not clear membership (D-03).

    Do not apply `TransportWritten` in this task; Task 2 owns the membership mutation inside `complete_peer_effect`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-04-prepare -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features relay_serving -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'PreparedTxServe' packages/open-bitcoin-node/src/network/types.rs` matches.</criterion>
    <criterion>`rg -n 'Immediate\\(\\s*WireNetworkMessage::Tx' packages/open-bitcoin-node/src/network/inventory.rs` returns no matches inside `gate_inventory_for_durable_serving`.</criterion>
    <criterion>`rg -n 'try_new_tx_response' packages/open-bitcoin-node/src/network/inventory.rs` matches.</criterion>
    <criterion>`rg -n 'complete_peer_emission' packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs` matches the successful TX-write path.</criterion>
    <criterion>`rg -n 'EligibleServe' packages/open-bitcoin-node/src/network/inventory.rs` returns no `retry_clears` / `record_retry_clear` emission.</criterion>
  </acceptance-criteria>
  <done>Eligible GETDATA TX is a receipt-bearing write. Classify-time Served does not clear unbroadcast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply TransportWritten only on fresh TX-response completion</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/announcement_transport.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/tests/getdata_tx_receipt_cases.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <behavior>
    - `complete_peer_effect` on `EffectCompletion::Applied` with `write_kind.is_transaction_response()` removes `evidence.maybe_member()` from `unbroadcast_members` when present.
    - `AlreadyApplied` and `AchievedButStale` do not remove the member (stale receipt must not clear newer intent).
    - `is_transaction_inventory()` completions do not remove the member (D-01).
    - Failed/aborted writes never reach this Applied branch; membership stays.
    - After a successful TX-response clear, a later child-package admission must not re-insert the parent (Plan 02 insert gate; assert here as an integration test).
  </behavior>
  <action>
    Per D-02 and Phase 134 Applied-only receipts, extend `complete_peer_effect` in `runtime_authority/lifecycle.rs` after the existing `is_fresh` evidence recording and after `complete_exact` succeeds:

    ```rust
    if is_fresh
        &amp;&amp; let Some(evidence) = maybe_evidence
        &amp;&amp; evidence.write_kind().is_transaction_response()
        &amp;&amp; let Some(member) = evidence.maybe_member()
    {
        network.unbroadcast_members.remove(&amp;member);
    }
    ```

    Do this only when the function will return `EffectCompletion::Applied`. Do not remove on `AchievedButStale` or `AlreadyApplied`. Do not construct a `MempoolRetryClearCause::EligibleServe` row. Do not claim network-wide propagation in comments; say the member is no longer retry-eligible for initial broadcast (D-04).

    Register `mod getdata_tx_receipt_cases;` in `network/tests.rs`. Create `getdata_tx_receipt_cases.rs` with tests named exactly:
    - `eligible_serve_classify_does_not_clear_unbroadcast`
    - `fresh_tx_response_write_clears_unbroadcast_as_transport_written`
    - `stale_tx_response_receipt_does_not_clear_newer_unbroadcast`
    - `tx_inventory_write_does_not_clear_unbroadcast`
    - `aborted_tx_response_leaves_unbroadcast_member`

    Use the existing local-admission fixtures (`relay_enabled_network` / `admitted_network` patterns). For the stale case: admit locally, start a TX-response emission under the current generation, then apply a newer lifecycle mutation that remints unbroadcast (re-admit or bump generation), then complete the old receipt and assert the new unbroadcast member remains.

    Add a parity breadcrumb for the new test file anchored to `packages/bitcoin-knots/src/net_processing.cpp` (GETDATA `RemoveUnbroadcastTx` ~2457) and `packages/bitcoin-knots/test/functional/mempool_unbroadcast.py`. Document the intentional difference: Open Bitcoin clears after successful write, not at send-buffer push.

    Do not add RPC/dashboard presentation. Do not start a timer.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-04-clear -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features getdata_tx_receipt -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'is_transaction_response' packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs` matches inside `complete_peer_effect`.</criterion>
    <criterion>`rg -n 'unbroadcast_members.remove' packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs` matches the Applied TX-response branch.</criterion>
    <criterion>`rg -n 'EligibleServe' packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs` returns no `retry_clears` insert.</criterion>
    <criterion>`rg -n 'fresh_tx_response_write_clears_unbroadcast_as_transport_written' packages/open-bitcoin-node/src/network/tests/getdata_tx_receipt_cases.rs` matches.</criterion>
    <criterion>The named Cargo filter reports all five new tests passing.</criterion>
  </acceptance-criteria>
  <done>Unbroadcast clears only on a fresh successful TX-response write or on lifecycle removal already implemented by Plan 02 teardown.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GETDATA classify → write | Served classification is not a receipt; untrusted peers can GETDATA. |
| Write capability → membership | Only `acknowledge_write` of the exact affine capability may mint a receipt. |
| Stale receipt → current unbroadcast | Older generation/session receipts must not clear newer local intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-136-04-01 | Tampering | receipt spoofing / stale-clear | mitigate | Apply membership removal only on `EffectCompletion::Applied` for current authority epoch, lifecycle generation, and peer session. `AlreadyApplied` / `AchievedButStale` are no-ops. |
| T-136-04-02 | Tampering | INV write | mitigate | Filter `is_transaction_response()`; `TransactionInventory` never clears (D-01). |
| T-136-04-03 | Tampering | EligibleServe pre-write | mitigate | Classify `Served` does not emit `retry_clears`. Clear happens only after successful write completion. |
| T-136-04-04 | Denial of Service | failed encode/write | mitigate | Abort the reserved effect; member stays retry-eligible (D-02). |
| T-136-04-05 | Elevation of Privilege | claim boundary | mitigate | Comments and tests must say a TX write is first-hop delivery evidence, not guaranteed propagation or public relay. |
| T-136-04-06 | Information Disclosure | unbroadcast fingerprinting | accept | GETDATA serve of a local unbroadcast member is the Knots acknowledgement path; set membership remains the 5,000-cap local set from Plan 02. |
</threat_model>

<verification>
Run the timed relay-serving compile/regression suite, then the timed GETDATA receipt suite. Do not overlap Cargo jobs.
</verification>

<success_criteria>
- D-01, D-02, D-03, and D-04 receipt rules are implemented and tested.
- IBR-04 applying clear is `TransportWritten` on a fresh `WireNetworkMessage::Tx` write.
- No EligibleServe membership-clearing row is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-04-SUMMARY.md`
</output>
