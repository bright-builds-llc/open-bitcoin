---
phase: 136-receive-independent-maintenance-and-transport-receipts
plan: "05"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/open-bitcoin-node/src/network/relay_fanout.rs
  - packages/open-bitcoin-node/src/network/relay_fanout/lifecycle.rs
  - packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs
  - packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
autonomous: true
requirements: [PPKG-04, IBR-03]
must_haves:
  truths:
    - "Co-admitted still-present package members enqueue parent then child on the existing TxFanoutQueue FIFO."
    - "An independently admitted child does not re-enqueue an already-present parent."
    - "Retry prepare calls enqueue_admission on TxFanoutQueue and does not add rebroadcast.rs or a second queue."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/relay_fanout.rs
      provides: "Retry identity enqueue through existing admission path"
      contains: "enqueue_retry_admissions"
    - path: packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs
      provides: "Parent-before-child and AlreadyPresent-parent proofs"
      contains: "parent_before_child"
  key_links:
    - from: packages/open-bitcoin-node/src/network/relay_fanout.rs
      to: packages/open-bitcoin-network/src/peer/transaction_relay/fanout.rs
      via: "TxFanoutQueue::enqueue_admission push_back"
      pattern: "enqueue_admission"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 136-2026-08-15T21-24-17
generated_at: 2026-08-15T22:20:00.000Z
---

<objective>
Prove parent-before-child package FIFO and route retry identities through the existing Phase 104 fanout path.

Purpose: Deliver PPKG-04 and IBR-03 without a parallel announcer.
Output: `enqueue_retry_admissions` plus package FIFO tests. No `rebroadcast.rs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md
@packages/open-bitcoin-node/src/network/relay_fanout.rs
@packages/open-bitcoin-node/src/network/relay_fanout/lifecycle.rs
@packages/open-bitcoin-network/src/peer/transaction_relay/fanout.rs
@packages/open-bitcoin-mempool/src/pool/package_admission/finalization.rs
@packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs

<interfaces>
Existing FIFO and enqueue (do not replace):

```rust
pub const PHASE104_MAX_TX_FANOUT_QUEUE_PER_PEER: usize = 1024;
pub const PHASE104_MAX_TX_FANOUT_DRAIN_PER_PEER: usize = 16;

impl TxFanoutQueue {
    pub fn enqueue_admission(&mut self, admission: TxFanoutAdmission, peers: &[TxFanoutPeerInput]) -> Vec<TxFanoutAction>;
    // peer_queue.queued.push_back(relay_id);
    pub fn drain_peer(&mut self, peer_id: PeerId, now_unix_seconds: i64) -> Vec<TxFanoutAction>;
    // pop_front up to max_drain_per_peer
}

impl ManagedRelayFanoutState {
    fn record_prepared_admission(&mut self, admission: TxFanoutAdmission, peers: &[TxFanoutPeerInput]) -> Vec<TxFanoutAction> {
        self.queue.enqueue_admission(admission, peers)
    }
}
```

`prepare_fanout_projection` already iterates `facts.final_present()` (admitted topological order) and calls `record_prepared_admission`. `AlreadyPresent` parents are not in `final_present()` / `delta().admitted`. Keep that. Independently admitted children must not walk unrelated ancestors (D-14, D-15).

`rebroadcast_deferred` remains a truthful label for "retry not yet due / waiting for the next cycle / relay-disabled may emit no public fanout". Do not imply Phase 104 added a timer. Do not create `rebroadcast.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove co-admitted parent-before-child FIFO and no parent re-announce</name>
  <read-first>
    <file>.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md</file>
    <file>packages/open-bitcoin-node/src/network/relay_fanout/lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/transaction_relay/fanout.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/package_admission/finalization.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs, packages/open-bitcoin-node/src/network/relay_fanout/lifecycle.rs</files>
  <behavior>
    - Submitting one accepted parent+child package with two `FinallyPresent` members enqueues parent then child (`push_back` order) on each eligible peer queue.
    - `drain_peer` / `drain_relay_fanout` emits parent inventory before child inventory for that peer on the first drain that has room for both, subject to `PHASE104_MAX_TX_FANOUT_DRAIN_PER_PEER` (`16`).
    - Admitting a child whose parent is `AlreadyPresent` does not call `enqueue_admission` for the parent and does not increase the parent's queued count.
    - Independently admitted parent then independently admitted child uses ordinary single-transaction enqueue; the second admission does not re-enqueue the parent (D-14).
    - No `package` / `getpkgtxns` wire message is introduced.
  </behavior>
  <action>
    Per D-13, D-14, and D-15, do not add a second fanout path. If `prepare_fanout_projection` already enqueues `facts.final_present()` in order via `enqueue_admission`, keep that loop and do not sort, reverse, or walk `mempool` ancestors.

    Do not edit `packages/open-bitcoin-node/src/network/tests.rs` or `docs/parity/source-breadcrumbs.json` (Plan 06 owns those shared registrations). Create `package_fanout_cases.rs` with tests named exactly:
    - `co_admitted_package_enqueues_parent_before_child`
    - `already_present_parent_is_not_reenqueued_when_child_is_accepted`
    - `independently_admitted_child_does_not_reannounce_present_parent`
    - `package_fanout_uses_txid_or_wtxid_from_existing_peer_mode`

    Use the existing package-admission fixtures from `package_bridge_cases` / lifecycle package tests (read those files first if helper names differ; reuse them rather than inventing a new package evaluator). Connect at least one relay-eligible peer with a known `TxRelayPeerMode`. After package admission, inspect `relay_fanout.queue` / `drain_relay_fanout` output and assert the parent `TxRelayId` appears before the child `TxRelayId` for that peer.

    For the AlreadyPresent case: admit the parent first as a singleton, snapshot queued identities, admit the child package, and assert the parent identity was not appended again (`queued` does not gain a second parent entry).

    If `prepare_fanout_projection` currently re-enqueues every `final_present()` member including an already-present parent that leaked into `final_present()`, change the loop to iterate `facts.delta().admitted` (or `facts.admitted_order()`) paired with the matching `final_present()` body so `AlreadyPresent` parents are not enqueued. That is the required target state, not "align with package finalization."

    Do not require emit/write order across peers or across ticks (D-15). One test may show drain `16` splitting a pair; that is allowed and must not fail the test.

    Do not claim a package wire protocol. Do not add the `docs/parity/source-breadcrumbs.json` entry here — Plan 06 registers `package_fanout_cases.rs` under `node-initial-broadcast-retry` with `net_processing.cpp` and `mempool_unbroadcast.py`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-05-fifo -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features package_fanout -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'co_admitted_package_enqueues_parent_before_child' packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs` matches.</criterion>
    <criterion>`rg -n 'already_present_parent_is_not_reenqueued_when_child_is_accepted' packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs` matches.</criterion>
    <criterion>`rg -n 'rebroadcast\\.rs|mod rebroadcast' packages/open-bitcoin-node/src/network packages/open-bitcoin-network/src` returns no new announcer module.</criterion>
    <criterion>`rg -n 'getpkgtxns|NetMsgType::PKG' packages/open-bitcoin-node/src/network/relay_fanout packages/open-bitcoin-node/src/network/tests/package_fanout_cases.rs` returns no matches.</criterion>
    <criterion>`rg -n 'mod package_fanout_cases' packages/open-bitcoin-node/src/network/tests.rs` returns no match yet — Plan 06 registers this module and re-runs the `package_fanout` filter.</criterion>
    <criterion>The named Cargo filter reports zero or more matching tests; the four new tests become live after Plan 06 registers the module. Production fanout loop changes must still compile with the existing `relay_fanout` suite.</criterion>
  </acceptance-criteria>
  <done>Co-admitted package members leave the existing FIFO parent-before-child; already-present parents are not re-announced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add enqueue_retry_admissions on the existing Phase 104 path</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/relay_fanout.rs</file>
    <file>packages/open-bitcoin-node/src/network/relay_fanout/lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/relay_fanout.rs, packages/open-bitcoin-node/src/network/tests/relay_fanout_cases.rs, packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs</files>
  <behavior>
    - `enqueue_retry_admissions(identities: &[MempoolMemberIdentity])` calls `record_prepared_admission` / `TxFanoutQueue::enqueue_admission` once per identity in the given order and returns the existing `TxFanoutAction` list (announce/suppress/queue_cap/rate_limit).
    - Identities the caller did not pass are not enqueued (Plan 01 leftover/unattempted stay unattempted; D-11).
    - Relay-disabled or ineligible peers produce the existing `TxFanoutSuppressionReason::RelayDisabled` / `NotRelayEligible` actions and no new queue.
    - `rebroadcast_deferred` remains on local first-hop admission evidence; comments say it means retry is not yet due, not that no timer exists.
  </behavior>
  <action>
    Per D-12, D-19, and IBR-03, add this exact method on `ManagedPeerNetwork` (or `ManagedRelayFanoutState` plus a thin network wrapper) in `relay_fanout.rs`:

    ```rust
    pub(in crate::network) fn enqueue_retry_admissions(
        &amp;mut self,
        identities: &amp;[open_bitcoin_mempool::MempoolMemberIdentity],
    ) -&gt; Vec&lt;TxFanoutAction&gt;;
    ```

    Implementation must:
    1. For each identity in slice order, build `TxFanoutAdmission { txid, wtxid, outcome: TxFanoutAdmissionOutcome::Accepted }`.
    2. Build peer inputs via the existing `relay_fanout_peer_inputs(None, Some(admission))`.
    3. Call `self.relay_fanout.record_prepared_admission(admission, &amp;peer_inputs)` (make `record_prepared_admission` `pub(in crate::network)` if it is currently private to `lifecycle.rs`).
    4. Not construct a new `VecDeque`, not call `mempool.entries()`, and not import a new announcer module.

    Add tests in `relay_fanout_cases.rs` named exactly:
    - `enqueue_retry_admissions_uses_existing_queue_and_preserves_order`
    - `enqueue_retry_admissions_does_not_mark_unpassed_identities_as_attempted`
    - `enqueue_retry_admissions_honors_relay_disabled_suppression`

    Update `local_submission_records_rebroadcast_deferred_without_timer` comments (keep the test name and assertions) so the rustdoc/comment says `rebroadcast_deferred` means the first hop is queued and the receive-independent retry cycle has not yet run — not that Phase 104 shipped a timer and not that retry was cancelled.

    Do not start the shell timer (Plan 06). Do not drain to sockets here. Do not clear unbroadcast on enqueue (D-01).
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-05-retry-enqueue -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features enqueue_retry_admissions -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn enqueue_retry_admissions' packages/open-bitcoin-node/src/network/relay_fanout.rs` matches.</criterion>
    <criterion>`rg -n 'enqueue_admission' packages/open-bitcoin-node/src/network/relay_fanout.rs` matches inside `enqueue_retry_admissions` or `record_prepared_admission`.</criterion>
    <criterion>`rg -n 'mod rebroadcast|rebroadcast\\.rs' packages/open-bitcoin-node/src/network packages/open-bitcoin-network/src` returns no new module.</criterion>
    <criterion>`rg -n 'without_timer' packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs` still exists and does not claim that no retry timer will exist in Phase 136.</criterion>
    <criterion>The named Cargo filter reports the three new enqueue tests passing.</criterion>
  </acceptance-criteria>
  <done>Retry identities enter the existing PHASE104 queue/rate/identity/activation path in caller-supplied order.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Package admission → fanout queue | Only `FinallyPresent` / `delta().admitted` members may enqueue. |
| Retry identities → fanout queue | Caller (Plan 06) must pass unbroadcast members only; this helper does not walk the mempool. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-136-05-01 | Information Disclosure | whole-mempool rebroadcast | mitigate | `enqueue_retry_admissions` iterates the supplied slice only; no `mempool.entries()` walk. |
| T-136-05-02 | Information Disclosure | package wire / ancestor walk | mitigate | Ordinary INV/TX only; no ancestor walk beyond co-admitted members (D-15). |
| T-136-05-03 | Denial of Service | retry amplification | mitigate | Existing PHASE104 queue 1024 / drain 16 / 1s interval still apply after enqueue. Plan 01 prepare-32 is the tick cap. |
| T-136-05-04 | Tampering | INV as ack | mitigate | Enqueue is not a receipt; D-01 remains. No unbroadcast mutation in this plan. |
| T-136-05-05 | Elevation of Privilege | claim boundary | mitigate | Tests and comments must not claim public/default relay or guaranteed propagation. Relay-disabled suppression stays. |
</threat_model>

<verification>
Run the timed package FIFO suite, then the timed retry-enqueue suite. Do not overlap Cargo jobs.
</verification>

<success_criteria>
- PPKG-04 parent-before-child FIFO is tested on the existing queue.
- IBR-03 retry enqueue reuses `TxFanoutQueue::enqueue_admission`.
- D-13, D-14, D-15, D-19 are implemented without a second announcer.
</success_criteria>

<output>
After completion, create `.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-05-SUMMARY.md`
</output>
