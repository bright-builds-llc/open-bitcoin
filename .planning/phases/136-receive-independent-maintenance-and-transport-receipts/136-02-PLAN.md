---
phase: 136-receive-independent-maintenance-and-transport-receipts
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs
  - packages/open-bitcoin-node/src/network/tests/unbroadcast_projection_cases.rs
  - packages/open-bitcoin-node/src/network/tests.rs
autonomous: true
requirements: [IBR-01, IBR-04]
must_haves:
  truths:
    - "Newly admitted local+requested members enter unbroadcast; a later still-present projection does not re-insert a TransportWritten-cleared parent."
    - "LifecycleRemoval still removes unbroadcast membership immediately."
    - "Reconciliation treats unbroadcast as a subset of retry-eligible canonical members and does not require every retry-eligible member to remain in the set."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      provides: "Insert-only-on-new-admission unbroadcast projection"
      contains: "delta().admitted"
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
      provides: "Subset oracle for unbroadcast after TX-write clears"
      contains: "expected_unbroadcast_members"
  key_links:
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      to: packages/open-bitcoin-mempool/src/context.rs
      via: "is_retry_eligible used only as the insert gate"
      pattern: "is_retry_eligible"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 136-2026-08-15T21-24-17
generated_at: 2026-08-15T22:20:00.000Z
---

<objective>
Stop re-inserting still-present cleared unbroadcast members and rewrite the reconciliation oracle.

Purpose: A successful TX write (Plan 04) cannot survive if the next package or maintenance projection puts the parent back because it is still local + requested + present.
Output: Insert-on-new-admission-only projection and a subset reconciliation contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md
@.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-RESEARCH.md
@packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
@packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
@packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs
@packages/open-bitcoin-mempool/src/context.rs
@packages/open-bitcoin-mempool/src/pool/lifecycle.rs

<interfaces>
Current insert loop to replace in `prepare_unbroadcast_projection` (authority.rs ~335-339):

```rust
for member in facts.final_present() {
    if member.metadata.is_retry_eligible(true) {
        replacement.insert(member.member);
    }
}
```

Required replacement (D-04 insert gate; IBR-01):

```rust
for member in facts.final_present() {
    if facts.delta().admitted.contains(&member.member)
        && member.metadata.is_retry_eligible(true)
    {
        replacement.insert(member.member);
    }
}
```

Keep the existing teardown_order and `facts.delta().retry_clears` removal loops and the `MAX_UNBROADCAST_MEMBERS` (5_000) capacity check.

`is_retry_eligible` remains:

```rust
is_current_member
    && matches!(self.origin, MempoolOrigin::Local)
    && matches!(self.relay_intent, RelayIntent::Requested)
```

Use it only as the insert gate. After `TransportWritten`, a member may stay eligible by metadata and still be absent from unbroadcast.

Do not emit `MempoolRetryClearCause::EligibleServe` rows in this plan (D-03). Recovery install already restores snapshot membership in `lifecycle_projection/recovery.rs` and must not be routed through this insert loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert only newly admitted retry-eligible members</name>
  <read-first>
    <file>.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-CONTEXT.md</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-mempool/src/context.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/lifecycle.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs, packages/open-bitcoin-node/src/network/tests/unbroadcast_projection_cases.rs, packages/open-bitcoin-node/src/network/tests.rs</files>
  <behavior>
    - Local requested singleton admission inserts the member into `unbroadcast_members`.
    - Peer / `RelayIntent::NotRequested` admission does not insert.
    - After the member is removed from `unbroadcast_members` to simulate `TransportWritten`, a later child-only package admission whose `delta().admitted` does not contain the parent must leave the parent absent.
    - A `LifecycleRemoval` teardown still removes the member even when it was present.
    - An expiry/maintenance facts object with empty `delta().admitted` and retry-eligible `final_present` members must not re-insert a previously cleared identity.
    - No `MempoolRetryClearCause::EligibleServe` is written into `retry_clears` by this plan.
  </behavior>
  <action>
    Per D-04 and IBR-01, change only the insert loop in `prepare_unbroadcast_projection` so a member is inserted when `facts.delta().admitted.contains(&member.member) && member.metadata.is_retry_eligible(true)`. Keep teardown removals and `retry_clears` removals. Do not derive membership from metadata alone after the first insert.

    Register `mod unbroadcast_projection_cases;` in `packages/open-bitcoin-node/src/network/tests.rs` next to `lifecycle_projection_cases`.

    Create `packages/open-bitcoin-node/src/network/tests/unbroadcast_projection_cases.rs` with Arrange/Act/Assert tests named exactly:
    - `local_requested_admission_inserts_unbroadcast_member`
    - `not_requested_or_peer_admission_does_not_insert_unbroadcast_member`
    - `cleared_still_present_parent_is_not_reinserted_by_child_package_admission`
    - `lifecycle_removal_removes_unbroadcast_member`
    - `empty_admitted_maintenance_facts_do_not_reinsert_cleared_member`

    Reuse the existing `admitted_network` / `apply_prepared` / `network_with_spendable_coinbase` helpers from `lifecycle_projection_cases` (import via the same test-root pattern those files use). For the child-package case: admit parent locally with `RelayIntent::Requested`, remove parent from `unbroadcast_members` to simulate a prior `TransportWritten` clear, then admit a child package whose `AlreadyPresent` parent is not in `delta().admitted`. Assert parent remains absent and child is inserted if the child is local+requested and newly admitted.

    Do not add GETDATA, PeerEmission, or timer code. Do not persist due times. Add a parity breadcrumb block on the new test file via `docs/parity/source-breadcrumbs.json` using the existing `network-initial-broadcast-retry-inputs` group or a new `node-unbroadcast-projection` group anchored to `packages/bitcoin-knots/src/txmempool.cpp` and `packages/bitcoin-knots/test/functional/mempool_unbroadcast.py`.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-02-insert -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features unbroadcast_projection -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'facts\\.delta\\(\\)\\.admitted.contains' packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs` matches the insert loop.</criterion>
    <criterion>`rg -n 'for member in facts.final_present\\(\\) \\{[\\s\\S]*is_retry_eligible\\(true\\) \\{[\\s\\S]*replacement.insert' packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs` no longer matches an insert that skips the admitted check. After the change, every `replacement.insert` in `prepare_unbroadcast_projection` is guarded by `delta().admitted.contains`.</criterion>
    <criterion>`rg -n 'EligibleServe' packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs` returns no new retry-clear emission (existing mention of the enum in comments is allowed only if it says it must not be emitted here).</criterion>
    <criterion>The named Cargo filter reports the five new tests passing.</criterion>
  </acceptance-criteria>
  <done>Unbroadcast insert is a one-time admission gate; still-present cleared members stay cleared until a new local+requested admission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite expected unbroadcast to a subset oracle</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>standards/core/testing.md</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs</files>
  <behavior>
    - `expected_unbroadcast_members` equals `unbroadcast_members ∩ retry-eligible canonical members` (illegal extras only).
    - A foreign extra unbroadcast identity still counts as one mismatch.
    - A still-present retry-eligible canonical member missing from unbroadcast (TransportWritten-cleared) counts as zero mismatches.
    - A swap (remove legal member, insert foreign extra) counts as one mismatch (the extra), not two.
    - Clean admission membership still reports count `0`.
  </behavior>
  <action>
    Per IBR-04, replace the Phase 134 equality oracle in `expected_unbroadcast_members`. Stop filtering canonical members by `is_retry_eligible(true)` and treating that set as required membership.

    New contract:
    1. Every unbroadcast member that is missing from canonical or not `is_retry_eligible(true)` is a mismatch (foreign / stale / ineligible).
    2. Retry-eligible canonical members absent from unbroadcast are not mismatches.

    Implement `expected_unbroadcast_members` as the intersection of `self.unbroadcast_members` with retry-eligible canonical identities. `unbroadcast_mismatch_count` / exact test identities then report only illegal extras.

    Update these existing tests in `lifecycle_projection_cases/reconciliation.rs` to the new contract (keep the function names so callers stay stable):
    - `unbroadcast_reconciliation_counts_a_missing_member_once` → rewrite body and rename to `unbroadcast_reconciliation_cleared_still_present_member_is_not_a_mismatch`. After `unbroadcast_members.remove(&member)` on an admitted local requested member, `counts()[4] == 0` and `exact.unbroadcast` is empty.
    - `unbroadcast_reconciliation_counts_equal_cardinality_swap_twice` → rewrite so removing the legal member and inserting `extra` yields `counts()[4] == 1` and `exact.unbroadcast == BTreeSet::from([extra])`. Rename to `unbroadcast_reconciliation_counts_only_the_illegal_extra_after_clear_and_swap`.
    - Keep `unbroadcast_reconciliation_counts_an_extra_member_once` and `unbroadcast_reconciliation_reports_clean_membership` passing.

    Add one new test `unbroadcast_reconciliation_ineligible_canonical_member_in_set_is_a_mismatch` if an ineligible (peer-origin) identity can be inserted into `unbroadcast_members` in the fixture: it must count as a mismatch.

    Do not add RPC/dashboard fields. Do not claim guaranteed propagation in comments.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase136-02-reconciliation -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features unbroadcast_reconciliation -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`rg -n 'fn expected_unbroadcast_members' -A 25 packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs` shows intersection with `self.unbroadcast_members`, not a filter of all canonical retry-eligible members as the required set.</criterion>
    <criterion>`rg -n 'unbroadcast_reconciliation_cleared_still_present_member_is_not_a_mismatch' packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs` matches.</criterion>
    <criterion>`rg -n 'unbroadcast_reconciliation_counts_a_missing_member_once' packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs` returns no matches.</criterion>
    <criterion>The named Cargo filter reports the updated unbroadcast reconciliation tests passing.</criterion>
  </acceptance-criteria>
  <done>Reconciliation accepts TransportWritten-cleared still-present members and still flags foreign or ineligible unbroadcast extras.</done>
</task>

</tasks>

<threat_model>
Security gate: OWASP ASVS Level 1; unresolved High severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Lifecycle facts → unbroadcast set | Admission metadata must not re-create retry intent after a successful TX write or lifecycle removal. |
| Reconciliation oracle → operator evidence | Mismatch counts are low-cardinality; identities stay test-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-136-02-01 | Tampering | `prepare_unbroadcast_projection` | mitigate | Insert only `delta().admitted` ∩ retry-eligible; teardown and `retry_clears` remain the only exits. Prevents stale re-insert of a cleared parent. |
| T-136-02-02 | Information Disclosure | unbroadcast walk set | mitigate | Insert gate still requires local + requested + current; peer and NotRequested admissions never enter the 5,000-cap set (IBR-01). |
| T-136-02-03 | Tampering | reconciliation oracle | mitigate | Subset oracle cannot be used to force re-insertion of cleared members. Illegal extras still fail closed. |
| T-136-02-04 | Elevation of Privilege | claim boundary | mitigate | Comments and tests must not say a remaining unbroadcast member is guaranteed to propagate. |
| T-136-02-05 | Repudiation | EligibleServe | mitigate | This plan emits no `EligibleServe` retry-clear rows; classify-time serve is not a membership mutation. |
</threat_model>

<verification>
Run the timed insert suite, then the timed reconciliation suite, then format the touched Rust files through `scripts/command-timings.ts` and `git diff --check`.
</verification>

<success_criteria>
- D-04 insert/clear projection is implemented without EligibleServe clears.
- IBR-01 insert gate and IBR-04 still-present-clear survival are tested.
- Phase 134 "unbroadcast == all retry-eligible" equality is gone.
</success_criteria>

<output>
After completion, create `.planning/phases/136-receive-independent-maintenance-and-transport-receipts/136-02-SUMMARY.md`
</output>
