---
phase: 47
phase_name: Operator Sync Truth Surfaces
phase_lifecycle_id: 47-2026-05-26T21-36-05
lifecycle_mode: yolo
generated_at: 2026-05-26T21:36:05.164Z
generated_by: gsd-yolo-discuss-plan-execute-commit-and-push
requirements:
  - OBS-01
  - OBS-02
status: complete
---

# Phase 47 Plan

## Tasks

- [x] Extend the shared sync status contract.
  - Add `SyncProgressSignal` to `packages/open-bitcoin-node/src/status.rs`
    with stable snake_case JSON values.
  - Add `progress_signal: FieldAvailability<SyncProgressSignal>` and
    `last_successful_progress_unix_seconds: FieldAvailability<u64>` to
    `SyncStatus`.
  - Update every `SyncStatus` constructor and serialization test to keep
    unavailable fields explicit when no durable sync truth exists.

- [x] Derive progress signal and last-progress time from sync runtime truth.
  - Add `SyncRunSummary` helpers for progress signal and latest successful
    progress timestamp based on accepted header or block contribution.
  - In `DurableSyncRuntime::durable_sync_state_from_summary`, preserve the
    prior durable last-progress timestamp when the current run has no new
    successful contribution.
  - Keep `lag` as estimated lag and keep `block_height` as connected-height
    compatibility alias.

- [x] Align metrics and structured logs with status progress dimensions.
  - Add `downloaded_block_height` and `connected_block_height` metric kinds.
  - Emit header, downloaded, connected, and compatibility sync-height samples
    from `SyncRunSummary::metric_samples`.
  - Include header, downloaded, connected, progress signal, and last progress
    timestamp in structured sync summary logs.

- [x] Align operator renderers and RPC fallback status.
  - Update human status with progress signal and last successful progress rows.
  - Update dashboard sync rows to show headers/downloaded/connected, progress
    signal, last progress, recovery guidance, and last error.
  - Add header/downloaded/connected charts to dashboard metric kinds.
  - Update `rpc_sync_status`, `unavailable_sync_status`, and
    `getblockchaininfo` tests so RPC remains conservative about connected
    height.

- [x] Update docs and generated metrics.
  - Refresh `docs/architecture/status-snapshot.md` and
    `docs/operator/runtime-guide.md` for progress signal, last progress,
    estimated lag, and metrics/log semantics.
  - Regenerate `docs/metrics/lines-of-code.md` after source changes.

- [x] Verify.
  - Run focused tests for sync runtime projection, status serialization, CLI
    status/dashboard rendering, RPC blockchain info, and metrics.
  - Run `cargo fmt --manifest-path packages/Cargo.toml --all`.
  - Run `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`.
  - Run `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`.
  - Run `cargo test --manifest-path packages/Cargo.toml --all-features`.
  - Run `bash scripts/verify.sh`.
  - Run GSD lifecycle validation for Phase 47.

## Threat Model

- Public peer data must not be treated as validated progress until existing
  validation paths accept it. This plan only changes projection fields and keeps
  peer validation, block persistence, and connected-chain semantics unchanged.
- Operator status must not leak credentials or peer secrets. New fields are
  counters, enum values, and timestamps already derivable from local runtime
  metadata.

## Elegance Pass

The intended implementation is additive and centered on the existing shared
status contract. It avoids a renderer-local status model, avoids duplicate lag
fields, and preserves existing compatibility aliases while making the missing
OBS-01 truth machine-readable.
