---
phase: 05-mempool-and-node-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/types.rs
autonomous: true
requirements:
  - MEM-01
  - MEM-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-04-13T23-15-14
generated_at: 2026-04-13T23:15:14.250Z
must_haves:
  truths:
    - Ancestor or descendant accounting is reproducible and queryable through
      explicit entry metrics.
    - Entry limits and size-limit eviction are enforced deterministically.
    - Correctness wins over clever caching in the first mempool slice.
  artifacts:
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/types.rs
  key_links:
    - The mempool entry surface exposes ancestor or descendant and conflict
      state that later networking work can inspect.
---

<objective>
Implement fee, ancestor, descendant, and eviction policy accounting.

Purpose: extend the admission engine into a reproducible policy state machine
with explicit relationship metrics and mempool-size trimming.
Output: ancestor or descendant bookkeeping, deterministic limit checks, and
size-limit eviction behavior over the pure-core mempool API.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mempool-and-node-policy/05-CONTEXT.md
@.planning/phases/05-mempool-and-node-policy/05-RESEARCH.md
@.planning/phases/05-mempool-and-node-policy/05-01-PLAN.md
@packages/bitcoin-knots/src/txmempool.h
@packages/bitcoin-knots/src/txmempool.cpp
@packages/bitcoin-knots/src/test/txpackage_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic parent or child and ancestor or descendant accounting</name>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/types.rs</files>
  <read_first>packages/open-bitcoin-mempool/src/pool.rs, packages/bitcoin-knots/src/txmempool.h, packages/bitcoin-knots/src/txmempool.cpp</read_first>
  <action>Model direct parents and children plus aggregate ancestor or descendant counts, fees, and virtual sizes for each entry. Recompute the relationship graph after each mutation so the resulting metrics are deterministic and easy to assert in targeted fixtures.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool tracks_parent_child_and_ancestor_descendant_metrics`</verify>
  <acceptance_criteria>
    - Entries expose direct parents and children explicitly
    - Ancestor or descendant metrics are queryable from the public entry state
  </acceptance_criteria>
  <done>The mempool surface can report stable relationship accounting for accepted transactions.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce ancestor or descendant count and size limits</name>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/lib.rs</files>
  <read_first>packages/bitcoin-knots/src/txmempool.cpp, packages/bitcoin-knots/src/test/txpackage_tests.cpp</read_first>
  <action>Reject admissions that would exceed configured ancestor or descendant count or size limits. Use the deterministic relationship view from Task 1 rather than hidden mutable caches, and return typed limit errors that say which policy bound failed.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool rejects_entries_that_exceed_ancestor_limits`</verify>
  <acceptance_criteria>
    - Count and size bounds are enforced for both ancestor and descendant directions
    - Errors differentiate count overflow from size overflow
  </acceptance_criteria>
  <done>The mempool reproduces the roadmap's ancestor or descendant policy outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement size-limit eviction using descendant-aware ordering</name>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/lib.rs</files>
  <read_first>packages/bitcoin-knots/src/txmempool.h, packages/bitcoin-knots/src/txmempool.cpp</read_first>
  <action>When the configured mempool-size cap is exceeded, evict the weakest package according to descendant-aware fee scoring and remove descendants with it. Surface the evicted txids in the admission result so later shell code can react explicitly.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool evicts_lowest_descendant_score_package_when_size_limit_is_exceeded`</verify>
  <acceptance_criteria>
    - Size-limit trimming is deterministic
    - Eviction removes a tx plus any dependent descendants
    - Admission results report the evicted txids
  </acceptance_criteria>
  <done>The pure-core mempool can enforce fee-related size pressure without hidden node state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Ancestor or descendant policy outcomes and eviction behavior are deterministic and testable

  </success_criteria>

<output>
After completion, create `.planning/phases/05-mempool-and-node-policy/05-02-SUMMARY.md`
</output>
