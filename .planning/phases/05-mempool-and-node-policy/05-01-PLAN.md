---
phase: 05-mempool-and-node-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/BUILD.bazel
  - BUILD.bazel
  - packages/README.md
  - scripts/pure-core-crates.txt
  - packages/open-bitcoin-core/Cargo.toml
  - packages/open-bitcoin-core/BUILD.bazel
  - packages/open-bitcoin-core/src/lib.rs
  - packages/open-bitcoin-mempool/Cargo.toml
  - packages/open-bitcoin-mempool/BUILD.bazel
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/error.rs
  - packages/open-bitcoin-mempool/src/policy.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool.rs
autonomous: true
requirements:
  - MEM-01
  - MEM-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-04-13T23-15-14
generated_at: 2026-04-13T23:15:14.250Z
must_haves:
  truths:
    - The workspace exposes a pure-core mempool crate instead of burying policy
      logic in the runtime shell.
    - Admission uses existing chainstate and consensus metadata rather than
      hidden global state or duplicate validation code.
    - Policy configuration and typed rejection reasons are explicit Rust types.
  artifacts:
    - packages/open-bitcoin-mempool/Cargo.toml
    - packages/open-bitcoin-mempool/src/lib.rs
    - packages/open-bitcoin-mempool/src/policy.rs
    - packages/open-bitcoin-mempool/src/pool.rs
  key_links:
    - open-bitcoin-core re-exports open-bitcoin-mempool for downstream callers.
---

<objective>
Implement mempool admission and replacement rules in a new pure-core crate.

Purpose: establish the policy engine boundary and the first mempool behavior
slice that later accounting, eviction, and node orchestration can build on.
Output: a new `open-bitcoin-mempool` crate wired into the workspace with
explicit policy config, typed errors, standardness helpers, and public APIs for
transaction admission and replacement.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mempool-and-node-policy/05-CONTEXT.md
@.planning/phases/05-mempool-and-node-policy/05-RESEARCH.md
@packages/open-bitcoin-chainstate/src/types.rs
@packages/open-bitcoin-consensus/src/context.rs
@packages/open-bitcoin-consensus/src/transaction.rs
@packages/open-bitcoin-consensus/src/classify.rs
@packages/bitcoin-knots/src/policy/policy.h
@packages/bitcoin-knots/src/policy/rbf.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the new pure-core mempool crate into the workspace</name>
  <files>packages/Cargo.toml, packages/BUILD.bazel, BUILD.bazel, packages/README.md, scripts/pure-core-crates.txt, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-core/src/lib.rs, packages/open-bitcoin-mempool/Cargo.toml, packages/open-bitcoin-mempool/BUILD.bazel, packages/open-bitcoin-mempool/src/lib.rs</files>
  <read_first>packages/Cargo.toml, packages/open-bitcoin-core/Cargo.toml, packages/open-bitcoin-core/BUILD.bazel, packages/open-bitcoin-chainstate/Cargo.toml</read_first>
  <action>Create `packages/open-bitcoin-mempool` as a new pure-core crate, add it to the Cargo workspace and Bazel aliases, list it in `scripts/pure-core-crates.txt`, describe it in `packages/README.md`, and re-export it from `open-bitcoin-core`. Keep dependencies one-way from mempool into first-party pure-core crates only.</action>
  <verify>`cargo check --manifest-path packages/Cargo.toml -p open-bitcoin-mempool`</verify>
  <acceptance_criteria>
    - `packages/Cargo.toml` includes `open-bitcoin-mempool`
    - `BUILD.bazel` exposes a public `mempool` alias
    - `open-bitcoin-core` re-exports `open_bitcoin_mempool`
  </acceptance_criteria>
  <done>The workspace builds the new crate and downstream crates can import it.</done>
</task>

<task type="auto">
  <name>Task 2: Define policy config, entry types, and typed rejection reasons</name>
  <files>packages/open-bitcoin-mempool/src/error.rs, packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/policy.rs, packages/open-bitcoin-mempool/src/types.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/classify.rs, packages/open-bitcoin-consensus/src/script.rs, packages/bitcoin-knots/src/policy/policy.h</read_first>
  <action>Implement explicit policy config and fee-rate types, entry or aggregate state for txid or wtxid identity and ancestor or descendant accounting, and typed mempool errors that distinguish missing inputs, non-standard transactions, relay-fee failures, and replacement failures. Keep the policy constants visible in Rust types rather than scattering magic numbers through the engine.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib`</verify>
  <acceptance_criteria>
    - `types.rs` defines entry, aggregate, and config structs
    - `error.rs` defines policy-appropriate rejection reasons
    - `policy.rs` centralizes standardness, weight, and feerate helpers
  </acceptance_criteria>
  <done>The crate exposes the typed data model needed for policy evaluation.</done>
</task>

<task type="auto">
  <name>Task 3: Implement transaction admission and targeted replacement rules</name>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/lib.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/types.rs, packages/open-bitcoin-consensus/src/context.rs, packages/open-bitcoin-consensus/src/transaction.rs, packages/bitcoin-knots/src/policy/rbf.h</read_first>
  <action>Implement admission that derives prevout context from chainstate or in-mempool parents, runs the existing consensus validator, applies standardness and relay-fee checks, detects direct conflicts, and performs targeted replacement only when the configured RBF rules are satisfied. Make accepted, replaced, or evicted txids explicit return values rather than hidden side effects.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool accepts_standard_confirmed_spend`</verify>
  <acceptance_criteria>
    - Admission derives context without hidden globals
    - Non-standard or too-cheap transactions fail with typed mempool errors
    - Replacement rejects equal-or-worse conflicts and removes valid conflict sets deterministically
  </acceptance_criteria>
  <done>The pure-core mempool can admit and replace transactions through an explicit public API.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib`
- [ ] `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-targets --all-features -- -D warnings`
- [ ] `cargo build --manifest-path packages/Cargo.toml --workspace --all-targets --all-features`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The pure-core mempool crate exists and exposes admission plus replacement APIs

  </success_criteria>

<output>
After completion, create `.planning/phases/05-mempool-and-node-policy/05-01-SUMMARY.md`
</output>
