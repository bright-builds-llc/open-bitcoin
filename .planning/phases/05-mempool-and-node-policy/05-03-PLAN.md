---
phase: 05-mempool-and-node-policy
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - packages/open-bitcoin-node/Cargo.toml
  - packages/open-bitcoin-node/BUILD.bazel
  - packages/open-bitcoin-node/src/lib.rs
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-mempool/tests/parity.rs
  - docs/parity/catalog/mempool-policy.md
  - docs/parity/index.json
autonomous: true
requirements:
  - MEM-01
  - MEM-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-04-13T23-15-14
generated_at: 2026-04-13T23:15:14.250Z
must_haves:
  truths:
    - The runtime shell exposes a thin mempool wrapper without leaking policy
      state into adapters.
    - Targeted parity fixtures cover admission, replacement, ancestor or
      descendant accounting, and eviction truths.
    - The parity ledger can move `mempool-policy` from planned to done without
      hiding deferred policy surfaces.
  artifacts:
    - packages/open-bitcoin-node/src/mempool.rs
    - packages/open-bitcoin-mempool/tests/parity.rs
    - docs/parity/catalog/mempool-policy.md
    - docs/parity/index.json
  key_links:
    - The node wrapper feeds chainstate snapshots into the pure-core mempool
      engine rather than owning the policy implementation itself.
---

<objective>
Add node-side orchestration, targeted policy parity tests, and deviation tracking.

Purpose: close Phase 5 with an auditable shell boundary, deterministic parity
fixtures, and explicit parity bookkeeping.
Output: a thin node-side mempool wrapper, repo-owned integration tests for the
Phase 5 truths, and parity docs that mark the mempool surface complete while
spelling out deferred behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mempool-and-node-policy/05-CONTEXT.md
@.planning/phases/05-mempool-and-node-policy/05-RESEARCH.md
@.planning/phases/05-mempool-and-node-policy/05-02-PLAN.md
@docs/parity/index.json
@docs/parity/catalog/chainstate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a node-side managed mempool wrapper</name>
  <files>packages/open-bitcoin-node/Cargo.toml, packages/open-bitcoin-node/BUILD.bazel, packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-node/src/mempool.rs</files>
  <read_first>packages/open-bitcoin-node/src/lib.rs, packages/open-bitcoin-node/src/chainstate.rs, packages/open-bitcoin-mempool/src/lib.rs</read_first>
  <action>Add a thin `open-bitcoin-node` wrapper that owns runtime-facing mempool orchestration, accepts chainstate snapshots or managed chainstate state, and delegates the actual policy decision to the pure-core mempool crate.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node`</verify>
  <acceptance_criteria>
    - `open-bitcoin-node` depends on `open-bitcoin-mempool`
    - Runtime orchestration stays thin and explicit
  </acceptance_criteria>
  <done>The shell crate can submit transactions through the pure-core mempool API without re-implementing policy logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted Phase 5 parity fixtures</name>
  <files>packages/open-bitcoin-mempool/tests/parity.rs</files>
  <read_first>packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-chainstate/tests/parity.rs, packages/bitcoin-knots/src/test/rbf_tests.cpp, packages/bitcoin-knots/src/test/txpackage_tests.cpp</read_first>
  <action>Create repo-owned parity tests that cover the documented Phase 5 truths: standard admission, non-standard rejection, fee-bump replacement, ancestor or descendant limit handling, and size-limit eviction. Use deterministic Rust fixtures rather than a network harness.</action>
  <verify>`cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --test parity`</verify>
  <acceptance_criteria>
    - `tests/parity.rs` exists and exercises the Phase 5 policy truths
    - The fixtures assert explicit replacement or eviction outcomes, not just boolean success
  </acceptance_criteria>
  <done>The phase has dedicated parity fixtures instead of relying only on unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update parity documentation for the mempool-policy surface</name>
  <files>docs/parity/catalog/mempool-policy.md, docs/parity/index.json</files>
  <read_first>docs/parity/catalog/chainstate.md, docs/parity/index.json</read_first>
  <action>Add a mempool-policy catalog entry that documents the implemented first-party Rust surface, the Knots sources mirrored in this phase, the targeted truths now covered, and the deferred policy areas that remain out of scope. Then update `docs/parity/index.json` so the `mempool-policy` surface moves from `planned` to `done` once verification passes.</action>
  <verify>`rg -n '\"mempool-policy\"' docs/parity/index.json && test -f docs/parity/catalog/mempool-policy.md`</verify>
  <acceptance_criteria>
    - Parity docs describe the implemented policy slice and the deferred surfaces honestly
    - `docs/parity/index.json` reflects the completed mempool-policy surface
  </acceptance_criteria>
  <done>Parity status is auditable from the repo docs instead of implicit in code only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --test parity`
- [ ] `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node`
- [ ] `bash scripts/verify.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The mempool-policy surface is documented and auditable

  </success_criteria>

<output>
After completion, create `.planning/phases/05-mempool-and-node-policy/05-03-SUMMARY.md`
</output>
