# Phase 07.4: Sweep the codebase for let-else opportunities - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution
> agents. Decisions are captured in CONTEXT.md; this log preserves the
> alternatives considered.

**Date:** 2026-04-19T10:33:26.510Z
**Phase:** 07.4-sweep-the-codebase-for-let-else-opportunities
**Mode:** Yolo
**Areas discussed:** Sweep boundary, Let-else heuristics, Verification and stopping rule

---

## Sweep boundary

| Option | Description | Selected |
|--------|-------------|----------|
| High-value production guard candidates first | Start with first-party Rust production files whose guard-style extraction still uses `if let`, `= if let`, or split extraction patterns. Skip vendored and test-only code. | ✓ |
| Broad repo-wide rewrite | Try to convert most remaining `if let`, `match`, or optional branches across the repository. | |
| Only revisit 07.3 hotspots | Restrict the sweep to files already touched by the early-return phase. | |

**User's choice:** High-value production guard candidates first
**Notes:** Yolo mode selected the narrow maintainability-first default so
Phase 07.4 stays consistent with the 07.1-07.3 insertion sequence and does not
expand into a blanket style rewrite.

---

## Let-else heuristics

| Option | Description | Selected |
|--------|-------------|----------|
| Use let...else only for early-exit extraction | Reserve `let...else` for focused `Option`, `Result`, or pattern extractions whose `else` path exits immediately. | ✓ |
| Prefer mechanical syntax conversion | Convert most eligible `if let` chains even when the current shape is already readable. | |
| Prefer helper extraction instead of let...else | Use helpers for most readability problems and keep `let...else` rare. | |

**User's choice:** Use let...else only for early-exit extraction
**Notes:** This keeps `let...else` aligned with the Bright Builds code-shape
guidance and leaves recognizer ladders or optional side-effect blocks alone
unless the rewritten form is obviously clearer.

---

## Verification and stopping rule

| Option | Description | Selected |
|--------|-------------|----------|
| Behavior-neutral sweep with search-backed stop rule | Rank candidates, keep public surfaces stable, document skipped non-improving sites, and finish with `bash scripts/verify.sh`. | ✓ |
| Allow opportunistic cleanup | Fold in nearby panic, magic-number, or Phase 8 cleanup while already touching files. | |
| Exhaust every remaining candidate | Push toward near-total conversion coverage before leaving the phase. | |

**User's choice:** Behavior-neutral sweep with search-backed stop rule
**Notes:** Yolo mode kept the phase narrow, behavior-neutral, and verification
driven. The planner should inventory likely candidates first and stop once
readability gains flatten out instead of chasing full conversion coverage.

---

## the agent's Discretion

- Exact candidate ranking and task grouping by crate
- Helper names or small local reshapes used when a readability problem is not
  best solved by `let...else`
- Leaving an existing `if let` or `match` shape untouched when it is already
  the clearest representation of the code

## Deferred Ideas

- AI-agent-friendly CLI surface belongs to Phase 8 operator interfaces.
- Sweep panics and illegal states remains a separate correctness-hardening
  phase.
- Repo-wide mechanical conversion of every remaining `if let` site is out of
  scope for 07.4.
