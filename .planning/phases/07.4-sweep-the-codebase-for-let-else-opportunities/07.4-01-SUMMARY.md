---
phase: 07.4-sweep-the-codebase-for-let-else-opportunities
plan: 01
subsystem: consensus
tags: [rust, consensus, let-else, verification]
requires:
  - phase: 07.3-reduce-nesting-with-early-returns
    provides: guard-first readability baselines and stable module surfaces for the narrowed follow-on sweep
provides:
  - targeted `let...else` rewrites in consensus witness-commitment and public-key parsing guards
  - explicit skip reporting for the remaining D-01 shortlist files
  - repo-native verification evidence for the narrowed sweep
affects: [phase-08, readability, consensus]
tech-stack:
  added: []
  patterns:
    - use `let...else` only for true diverging guard paths
    - add direct regression assertions when coverage exposes a newly uncovered guard branch
key-files:
  created:
    - .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md
  modified:
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-consensus/src/signature.rs
key-decisions:
  - "Keep the D-01 shortlist unchanged outside `block.rs` and `signature.rs` when the current flow is an ordered ladder, optional side effect, or normal success path."
  - "Add a direct missing-commitment regression assertion when repo-native coverage exposes an uncovered guard branch."
patterns-established:
  - "Consensus guard rewrites stay behavior-neutral and preserve reject reasons byte-for-byte."
  - "Shortlist sweeps stay auditable by recording explicit skip reasons instead of forcing mechanical conversions."
requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.4-2026-04-19T10-33-26
generated_at: 2026-04-20T02:15:15Z
duration: 7 min
completed: 2026-04-20
---

# Phase 07.4 Plan 01: Sweep the codebase for let-else opportunities Summary

**Consensus witness-commitment and public-key guard extraction narrowed to two let-else rewrites with explicit skip reporting for the rest of the shortlist**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-20T02:08:54Z
- **Completed:** 2026-04-20T02:15:15Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Rewrote `check_witness_commitment` in `packages/open-bitcoin-consensus/src/block.rs` to use a direct `let Some(...) = ... else` guard while preserving `unexpected-witness`, `bad-witness-nonce-size`, and `bad-witness-merkle-match`.
- Rewrote the final `PublicKey::from_slice(bytes)` split in `packages/open-bitcoin-consensus/src/signature.rs` to `let Ok(public_key) = ... else`, preserving the `InvalidPublicKey` versus `IncorrectSignature` mapping.
- Re-ran the narrowed shortlist audit, kept the non-improving candidates unchanged, and verified the final tree with targeted consensus checks plus `bash scripts/verify.sh`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Reconfirm the shortlist and apply only the two proven consensus let-else wins** - `45f9917` (refactor)
2. **Task 2: Close the narrowed sweep with repo-native verification and explicit skip reporting** - this summary is part of the task commit created after the final verifier pass

## Files Created/Modified
- `packages/open-bitcoin-consensus/src/block.rs` - narrows witness-commitment lookup into a direct diverging guard.
- `packages/open-bitcoin-consensus/src/block/tests.rs` - adds the missing witness-commitment regression assertion that exercises the new guard branch.
- `packages/open-bitcoin-consensus/src/signature.rs` - narrows the final public-key parse success path into a direct `let...else`.
- `.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md` - records the narrowed sweep outcome, verification evidence, and explicit skip reasons.

## Skipped Shortlist

- `packages/open-bitcoin-network/src/header_store.rs` keeps a normal genesis-versus-parent success path.
- `packages/open-bitcoin-wallet/src/wallet/sign.rs` keeps an optional redeem-script side effect.
- `packages/open-bitcoin-wallet/src/wallet/build.rs` keeps an optional change-output success path.
- `packages/open-bitcoin-wallet/src/address.rs` keeps optional payload and merkle-root appends.
- `packages/open-bitcoin-wallet/src/descriptor.rs` keeps an ordered recognizer ladder.
- `packages/open-bitcoin-consensus/src/classify.rs` keeps an ordered recognizer ladder.
- `packages/open-bitcoin-consensus/src/sighash.rs` keeps an optional annex append.
- `packages/open-bitcoin-consensus/src/script/sigops.rs` already expresses the guard flow clearly.
- `packages/open-bitcoin-mempool/src/error.rs` keeps an optional formatting branch.

## Decisions Made

- Kept the phase scope aligned with D-01, D-03, and D-04 by converting only the two consensus guard sites that clearly improved the left-margin flow.
- Treated the repo-native coverage failure as a blocking task-local issue and fixed it with the smallest direct regression assertion instead of widening the sweep.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing witness-commitment coverage for the new guard branch**
- **Found during:** Task 1 (Reconfirm the shortlist and apply only the two proven consensus let-else wins)
- **Issue:** `bash scripts/verify.sh` failed because the new `let...else` branch in `check_witness_commitment` that rejects witness-bearing blocks without a commitment output was no longer covered.
- **Fix:** Extended `witness_commitment_and_coinbase_height_paths_are_exercised` to remove the commitment output from an otherwise valid witness block and assert the unchanged `unexpected-witness` reject reason.
- **Files modified:** `packages/open-bitcoin-consensus/src/block/tests.rs`
- **Verification:** `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features witness_commitment_and_coinbase_height_paths_are_exercised` and `bash scripts/verify.sh`
- **Committed in:** `45f9917` (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix stayed within the planned files, preserved behavior, and avoided widening the sweep beyond the two intended guard rewrites.

## Issues Encountered

- Repo-native coverage caught the only regression risk in the narrowed rewrite and was resolved inline before Task 1 was committed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 8 can assume the remaining D-01 shortlist files were deliberately skipped for readability reasons, not missed accidentally.
- The consensus guard sites touched here are covered by targeted assertions and a passing repo-native verifier.

## Self-Check

- PASSED
- Verified `.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md` exists on disk.
- Verified task commit `45f9917` exists in `git log --oneline --all`.
- Verified the summary contains every required skipped-shortlist file path from the plan acceptance criteria.
- Stub scan across the touched files found no placeholder or TODO-style content that affects this phase goal.

---
*Phase: 07.4-sweep-the-codebase-for-let-else-opportunities*
*Completed: 2026-04-20*
