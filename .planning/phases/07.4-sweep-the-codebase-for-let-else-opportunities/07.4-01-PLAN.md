---
phase: 07.4-sweep-the-codebase-for-let-else-opportunities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-consensus/src/signature.rs
  - packages/open-bitcoin-consensus/src/signature/tests.rs
  - .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.4-2026-04-19T10-33-26
generated_at: 2026-04-19T10:53:10Z
must_haves:
  truths:
    - The phase applies `let ... else` only to true diverging guard paths, not to ordered recognizer ladders or optional side-effect branches.
    - Consensus witness-commitment and public-key parsing behavior stays unchanged while the selected guard paths move closer to the left margin.
    - The repo-native verification contract still passes after the narrowed sweep.
  artifacts:
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/signature.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-consensus/src/signature/tests.rs
    - .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md
  key_links:
    - packages/open-bitcoin-consensus/src/block.rs still emits `unexpected-witness`, `bad-witness-nonce-size`, and `bad-witness-merkle-match` through `BlockValidationError`.
    - packages/open-bitcoin-consensus/src/signature.rs still maps strict public-key parse failures to `SignatureError::InvalidPublicKey` and non-strict parse failures to `SignatureError::IncorrectSignature`.
    - .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md lists each D-01 shortlist file left unchanged and the concrete skip reason.
---

<objective>
Audit the Phase 07.4 shortlist, convert only the true guard-shaped `let ... else` wins, and close the maintainability follow-on with full repo-native verification.

Purpose: finish the residual guard-style readability cleanup before Phase 8 without turning the phase into a mechanical rewrite or a broader correctness sweep.
Output: narrowed consensus guard rewrites in `block.rs` and `signature.rs`, only the smallest test adjustments needed to preserve evidence or coverage, and a phase summary that records deliberate skips for the rest of the shortlist.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-CONTEXT.md
@.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-RESEARCH.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@scripts/verify.sh
@rust-toolchain.toml

<interfaces>
From packages/open-bitcoin-consensus/src/block.rs:
```rust
pub fn validate_block(
    block: &Block,
    spent_outputs: &[Vec<SpentOutput>],
) -> Result<(), BlockValidationError>;

pub fn validate_block_with_context(
    block: &Block,
    transaction_contexts: &[TransactionValidationContext],
    block_context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;

fn check_witness_commitment(
    block: &Block,
    context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;
```

From packages/open-bitcoin-consensus/src/signature.rs:
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum SignatureError {
    EmptySignature,
    InvalidDer,
    InvalidHashType(u32),
    InvalidPublicKey,
    NonCompressedPublicKey,
    NonLowS,
    IncorrectSignature,
    UnsupportedSigVersion,
}

fn parse_public_key_for_verification(
    bytes: &[u8],
    require_compressed: bool,
    flags: ScriptVerifyFlags,
) -> Result<PublicKey, SignatureError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconfirm the shortlist and apply only the two proven consensus let-else wins</name>
  <files>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, packages/open-bitcoin-network/src/header_store.rs, packages/open-bitcoin-wallet/src/wallet/sign.rs, packages/open-bitcoin-wallet/src/wallet/build.rs, packages/open-bitcoin-wallet/src/address.rs, packages/open-bitcoin-wallet/src/descriptor.rs, packages/open-bitcoin-consensus/src/classify.rs, packages/open-bitcoin-consensus/src/sighash.rs, packages/open-bitcoin-consensus/src/script/sigops.rs, packages/open-bitcoin-mempool/src/error.rs, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-CONTEXT.md, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-RESEARCH.md, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/index.md, ../coding-and-architecture-requirements/standards/core/code-shape.md, ../coding-and-architecture-requirements/standards/languages/rust.md</read_first>
  <behavior>
    - `check_witness_commitment` keeps the current `unexpected-witness`, `bad-witness-nonce-size`, and `bad-witness-merkle-match` outcomes while the commitment lookup becomes a direct diverging guard.
    - `parse_public_key_for_verification` still accepts normalized hybrid keys only outside `STRICTENC`, still returns `InvalidPublicKey` for strict failures, and still returns `IncorrectSignature` for non-strict parse failures.
    - The D-01 shortlist files outside `block.rs` and `signature.rs` stay unchanged when their current flow is an ordered ladder, a normal success path, or an optional side effect.
  </behavior>
  <action>Run `rg -n "if let|is_none\\(|is_some\\(|expect\\(" packages/open-bitcoin-network/src/header_store.rs packages/open-bitcoin-wallet/src/wallet/sign.rs packages/open-bitcoin-wallet/src/wallet/build.rs packages/open-bitcoin-wallet/src/address.rs packages/open-bitcoin-wallet/src/descriptor.rs packages/open-bitcoin-consensus/src/classify.rs packages/open-bitcoin-consensus/src/sighash.rs packages/open-bitcoin-consensus/src/script/sigops.rs packages/open-bitcoin-mempool/src/error.rs packages/open-bitcoin-consensus/src/block.rs packages/open-bitcoin-consensus/src/signature.rs` to reconfirm the candidate inventory per D-01 and D-07. Keep `header_store.rs`, `wallet/sign.rs`, `wallet/build.rs`, `address.rs`, `descriptor.rs`, `classify.rs`, `sighash.rs`, `script/sigops.rs`, and `mempool/error.rs` unchanged unless the reread reveals a new immediate-divergence guard that is plainly clearer than the current ordered or optional flow; the expected outcome is skip for those files. In `packages/open-bitcoin-consensus/src/block.rs`, rewrite `check_witness_commitment` so `witness_commitment_index(block)` is bound with `let Some(commitment_index) = witness_commitment_index(block) else { ... };` after computing `witness_present`, preserving the exact reject reasons, `coinbase_input` lookup, reserved-value size check, and commitment-byte comparison. In `packages/open-bitcoin-consensus/src/signature.rs`, rewrite the final `PublicKey::from_slice(bytes)` success-or-error split in `parse_public_key_for_verification` as `let Ok(public_key) = PublicKey::from_slice(bytes) else { ... };`, preserving `STRICTENC`, hybrid normalization, compressed-key enforcement, and the existing `InvalidPublicKey` versus `IncorrectSignature` mapping. Touch `packages/open-bitcoin-consensus/src/block/tests.rs` or `packages/open-bitcoin-consensus/src/signature/tests.rs` only if a direct regression assertion or coverage fix is required to keep the targeted crate green; do not widen into new feature coverage or rewrite other shortlist files just to increase the let-else count.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "let Some\\(commitment_index\\) = witness_commitment_index\\(block\\) else" packages/open-bitcoin-consensus/src/block.rs` returns exactly one match.
    - `rg -n "let Ok\\(public_key\\) = PublicKey::from_slice\\(bytes\\) else" packages/open-bitcoin-consensus/src/signature.rs` returns exactly one match.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features` exits with status 0.
  </acceptance_criteria>
  <done>The shortlist has been re-audited, only the two real guard-shaped consensus opportunities were rewritten, and the targeted consensus crate still behaves the same.</done>
</task>

<task type="auto">
  <name>Task 2: Close the narrowed sweep with repo-native verification and explicit skip reporting</name>
  <files>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md</files>
  <read_first>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, packages/open-bitcoin-consensus/src/signature.rs, packages/open-bitcoin-consensus/src/signature/tests.rs, scripts/verify.sh, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-CONTEXT.md, .planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-RESEARCH.md, AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, ../coding-and-architecture-requirements/standards/index.md</read_first>
  <acceptance_criteria>
    - `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings` exits with status 0.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features` exits with status 0.
    - `bash scripts/verify.sh` exits with status 0.
    - `.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md` contains the exact file names `packages/open-bitcoin-network/src/header_store.rs`, `packages/open-bitcoin-wallet/src/wallet/sign.rs`, `packages/open-bitcoin-wallet/src/wallet/build.rs`, `packages/open-bitcoin-wallet/src/address.rs`, `packages/open-bitcoin-wallet/src/descriptor.rs`, `packages/open-bitcoin-consensus/src/classify.rs`, `packages/open-bitcoin-consensus/src/sighash.rs`, `packages/open-bitcoin-consensus/src/script/sigops.rs`, and `packages/open-bitcoin-mempool/src/error.rs`.
  </acceptance_criteria>
  <action>Run `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings`, then `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features`, then `bash scripts/verify.sh` exactly as the completion gate in D-08 and repo-local guidance require. If verification fails only because the let-else rewrite needs a direct regression or coverage guard in `block/tests.rs` or `signature/tests.rs`, add the smallest targeted assertion and rerun the full sequence; do not widen into unrelated consensus cleanup. In the required summary file, explicitly list every D-01 shortlist file left unchanged and the concrete reason it was skipped: `header_store.rs` keeps a normal genesis-versus-parent success path, `wallet/sign.rs` keeps an optional redeem-script side effect, `wallet/build.rs` keeps an optional change-output success path, `address.rs` keeps optional payload and merkle-root appends, `descriptor.rs` and `classify.rs` keep ordered recognizer ladders, `sighash.rs` keeps an optional annex append, `script/sigops.rs` already expresses the guard flow clearly, and `mempool/error.rs` keeps an optional formatting branch.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features && bash scripts/verify.sh</automated>
  </verify>
  <done>The narrowed sweep ends on a passing repo-native verifier and the summary records the deliberate skips so the phase stays auditably non-mechanical.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if a rewrite changes a consensus reject reason, public function signature, or strict-versus-non-strict signature-verification outcome, stop and leave that site unchanged.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consensus block validation -> reject reasons | Readability rewrites must not change block acceptance or witness-commitment failure semantics |
| signature parsing -> script-verification outcomes | Guard rewrites must not change when invalid public keys become `InvalidPublicKey` versus `IncorrectSignature` |
| shortlist audit -> phase scope | Inventory work must not spill into ladder, optional-side-effect, CLI, RPC, or correctness-hardening rewrites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-01 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Keep the let-else rewrite confined to `check_witness_commitment`, preserve the existing reject reasons byte-for-byte, and verify with targeted consensus checks plus `bash scripts/verify.sh`. |
| T-07.4-02 | T | `packages/open-bitcoin-consensus/src/signature.rs` | mitigate | Preserve `STRICTENC`, hybrid normalization, and the current `InvalidPublicKey` versus `IncorrectSignature` split, then prove it with targeted consensus tests and the repo-native verifier. |
| T-07.4-03 | E | Phase 07.4 shortlist scope | mitigate | Re-run the exact shortlist search from D-01, keep the known ladder or side-effect files unchanged unless a new diverging guard is clearly better, and record each skip reason in the summary so scope creep is visible. |
</threat_model>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- `packages/open-bitcoin-consensus/src/block.rs` uses `let ... else` for the witness-commitment lookup without changing the existing reject reasons or commitment validation flow.
- `packages/open-bitcoin-consensus/src/signature.rs` uses `let ... else` for the final `PublicKey::from_slice(bytes)` guard without changing strict or non-strict verification outcomes.
- No other D-01 shortlist file is rewritten unless the executor finds a clearly superior diverging guard during the mandated reread.
- `.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md` names every skipped D-01 shortlist file and the concrete reason it stayed unchanged.
- `bash scripts/verify.sh` passes in the final state.
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-sweep-the-codebase-for-let-else-opportunities/07.4-01-SUMMARY.md`
</output>
