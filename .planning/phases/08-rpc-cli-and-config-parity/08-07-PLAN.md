---
phase: 08-rpc-cli-and-config-parity
plan: 07
type: execute
wave: 6
depends_on:
  - 08-05
files_modified:
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/loader.rs
  - packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-cli/src/startup.rs
  - packages/open-bitcoin-cli/src/startup/tests.rs
  - packages/open-bitcoin-cli/src/args.rs
  - packages/open-bitcoin-cli/src/args/tests.rs
  - packages/open-bitcoin-cli/src/main.rs
  - packages/open-bitcoin-cli/tests/operator_flows.rs
  - .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - CLI-01
  - CLI-02
user_setup: []
must_haves:
  truths:
    - "Client config accepts baseline-shaped `-rpcconnect=localhost` and other hostname inputs while preserving explicit `-rpcport` over embedded-port over chain-default precedence."
    - "Duplicate CLI named parameters are preserved through parsing and rejected by the shared method normalizer before any HTTP transport attempt."
    - "CLI startup reads stdin only when `-stdin` or `-stdinrpcpass` is enabled, and a no-stdin-flag invocation reaches config or transport without waiting for EOF."
    - "The real CLI binary has an automated open-stdin regression that catches the terminal-style blocking path missed by `Command::output()`."
    - "Plan 08-07 execution produces a lifecycle-provenance summary for the CLI/config gap closure."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs
      provides: "Hostname-preserving RPC client endpoint parsing"
      contains: "parse_rpc_client"
    - path: packages/open-bitcoin-rpc/src/config.rs
      provides: "Client endpoint type that can preserve hostnames without DNS lookup"
      contains: "RpcClient"
    - path: packages/open-bitcoin-rpc/src/method.rs
      provides: "Shared named-parameter normalizer that rejects duplicate keys"
      contains: "normalize_method_call"
    - path: packages/open-bitcoin-cli/src/args.rs
      provides: "Stdin-gate detection and duplicate named-parameter preservation"
      contains: "parse_named_parameters"
    - path: packages/open-bitcoin-cli/src/main.rs
      provides: "Real CLI binary entrypoint that does not drain stdin unless a stdin flag requires it"
      contains: "read_to_string"
    - path: packages/open-bitcoin-cli/tests/operator_flows.rs
      provides: "Automated open-stdin subprocess regression for the normal terminal path"
      contains: "Stdio::piped"
    - path: .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
      provides: "CLI/config gap-closure lifecycle provenance"
      contains: "phase_lifecycle_id: 08-2026-04-24T02-23-19"
  key_links:
    - from: packages/open-bitcoin-rpc/src/config/loader.rs
      to: packages/open-bitcoin-cli/src/startup.rs
      via: "Shared config loading must return a hostname-preserving client endpoint that `CliStartupConfig` can pass into the HTTP client."
      pattern: "RpcClientConfig"
    - from: packages/open-bitcoin-cli/src/args.rs
      to: packages/open-bitcoin-rpc/src/method.rs
      via: "CLI `-named` parsing must preserve duplicate `(name, value)` entries so `normalize_method_call()` can return invalid params before transport."
      pattern: "normalize_method_call"
    - from: packages/open-bitcoin-cli/src/main.rs
      to: packages/open-bitcoin-cli/tests/operator_flows.rs
      via: "The real CLI binary must proceed with an open stdin pipe when no stdin flags are present."
      pattern: "Stdio::piped"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: 2026-04-24T02:37:29Z
---

<objective>
Close the Phase 8 CLI/config/stdin verification gaps without touching the RPC dispatcher or parity docs owned by adjacent plans.

Purpose: Make the client path accept baseline-shaped hostname endpoints, reject duplicate named parameters before transport, and prove normal CLI invocations do not block on open stdin unless stdin flags request that behavior.
Output: Focused config/startup/args/main regressions, the open-stdin subprocess test, and an 08-07 lifecycle summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-04-SUMMARY.md
@.planning/phases/08-rpc-cli-and-config-parity/08-05-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/bitcoin-cli.cpp
@packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/config/loader.rs
@packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-cli/src/args.rs
@packages/open-bitcoin-cli/src/startup.rs
@packages/open-bitcoin-cli/src/main.rs
@packages/open-bitcoin-cli/tests/operator_flows.rs

<interfaces>
Current client config issue from `packages/open-bitcoin-rpc/src/config.rs`:
```rust
pub struct RpcClientConfig {
    pub connect_address: SocketAddr,
    pub auth: RpcAuthConfig,
}
```

Target client endpoint contract for this plan:
```rust
pub struct RpcClientEndpoint {
    pub host: String,
    pub port: u16,
}

pub struct RpcClientConfig {
    pub endpoint: RpcClientEndpoint,
    pub auth: RpcAuthConfig,
}
```
</interfaces>
</context>

<requirements_coverage>
| Requirement | Task | Coverage |
|-------------|------|----------|
| CLI-01 | Task 1, Task 2 | Full: hostname client endpoint parsing, port precedence, and duplicate named-argument rejection match the supported baseline-shaped surface. |
| CLI-02 | Task 3 | Full: stdin gating and the open-stdin subprocess regression prove normal CLI use reaches startup or transport without GUI or hidden stdin blocking. |
</requirements_coverage>

<gap_coverage>
| Verification gap | Plan | Task | Coverage |
|------------------|------|------|----------|
| CLI stdin gating | 08-07 | Task 3 | Full |
| `-rpcconnect=localhost` and hostname endpoint parsing | 08-07 | Task 1 | Full |
| Duplicate named parameter rejection | 08-07 | Task 2 | Full |
| Open-stdin regression | 08-07 | Task 3 | Full |
| 08-07 summary lifecycle provenance | 08-07 | Task 3 | Full |
</gap_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve hostname RPC client endpoints and port precedence</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/config.rs
    - packages/open-bitcoin-rpc/src/config/loader.rs
    - packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs
    - packages/open-bitcoin-rpc/src/config/tests.rs
    - packages/open-bitcoin-cli/src/startup.rs
    - packages/open-bitcoin-cli/src/startup/tests.rs
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/loader.rs
    packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
    packages/open-bitcoin-cli/src/startup.rs
    packages/open-bitcoin-cli/src/startup/tests.rs
  </files>
  <behavior>
    - Test 1: `load_runtime_config_for_args(["-rpcconnect=localhost"], default_data_dir)` returns a client endpoint with host `localhost` and the active chain default RPC port.
    - Test 2: Client endpoint precedence remains `-rpcport` over embedded `-rpcconnect` port over chain default, including hostname inputs such as `localhost:18443`.
    - Test 3: IPv4 and bracketed IPv6 client endpoints continue to parse without DNS lookup.
    - Test 4: Server bind addresses still require IP-shaped socket inputs and are not converted into hostname-capable client parsing.
  </behavior>
  <acceptance_criteria>
    - Replace `RpcClientConfig.connect_address: SocketAddr` with a hostname-preserving client endpoint type such as `RpcClientEndpoint { host: String, port: u16 }`; keep `RpcServerConfig.bind_address: SocketAddr` unchanged so server bind addresses still require IP-shaped inputs.
    - `parse_rpc_client_address()` or its replacement accepts hostname-only, hostname-with-port, IPv4, IPv4-with-port, bracketed IPv6, and bracketed IPv6-with-port inputs without resolving DNS during config loading.
    - Explicit `-rpcport=<port>` overrides an embedded port in `-rpcconnect=<host>:<port>`, and an embedded port overrides `default_rpc_port(chain)`.
    - `CliStartupConfig.rpc.host` or the equivalent startup output is populated from the preserved endpoint host string, so `localhost` remains `localhost` and is not forced through `SocketAddr::ip()`.
  </acceptance_criteria>
  <action>Per CLI-01, D-07, D-08, and the hostname gap in `08-GAPS.md`, fix the client boundary rather than adding test-only wrapper behavior. In `open-bitcoin-rpc` config, introduce a hostname-preserving client endpoint type and make `rpc_address.rs` parse `localhost`, `localhost:18443`, `127.0.0.1`, `127.0.0.1:18443`, `[::1]`, and `[::1]:18443` into `{host, port}` without DNS lookup. Keep the server bind parser IP-based. Update `startup.rs` so the CLI endpoint uses the preserved host and port. Preserve explicit `-rpcport` over embedded `-rpcconnect` port over chain default precedence.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::rpcconnect_accepts_hostnames_and_preserves_port_precedence -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features startup::tests::client_startup_preserves_rpcconnect_hostnames -- --exact</automated>
  </verify>
  <done>The client config path accepts baseline-shaped hostnames and preserves the expected endpoint precedence without weakening server bind validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve duplicate named arguments and reject them before transport</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/open-bitcoin-rpc/src/method/tests.rs
    - packages/open-bitcoin-cli/src/args.rs
    - packages/open-bitcoin-cli/src/args/tests.rs
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/method.rs
    packages/open-bitcoin-rpc/src/method/tests.rs
    packages/open-bitcoin-cli/src/args.rs
    packages/open-bitcoin-cli/src/args/tests.rs
  </files>
  <behavior>
    - Test 1: `parse_cli_args(["-named", "deriveaddresses", "descriptor=\"a\"", "descriptor=\"b\""], "")` preserves both `descriptor` entries before normalization.
    - Test 2: The shared method normalizer rejects duplicate named keys with RPC invalid params before any HTTP request is built.
    - Test 3: Positional/name collisions remain rejected with their existing deterministic invalid-params behavior.
    - Test 4: Duplicate named-key and positional/name collision errors are distinct enough for a verifier to tell which path failed.
  </behavior>
  <acceptance_criteria>
    - `parse_named_parameters()` preserves repeated named entries in `RequestParameters::Named(Vec<(String, Value)>)` or an equivalent ordered representation instead of overwriting them.
    - `normalize_method_call()` distinguishes duplicate named keys from positional/name collisions and returns invalid params before HTTP request building.
    - Existing named-argument collision regressions continue to pass.
    - No method handler receives raw duplicate named parameters; rejection stays in the shared normalizer per D-02 and D-09.
  </acceptance_criteria>
  <action>Per CLI-01, D-02, D-09, D-11, and the duplicate named-parameter gap in `08-GAPS.md`, remove overwrite semantics from `args.rs`. Keep repeated named entries through CLI parsing and rely on the shared RPC normalizer to return invalid params before `client.rs` builds an HTTP request. Update `method.rs` if needed so duplicate named keys and positional/name collisions produce deterministic, distinct messages through the same normalized request path used by HTTP and CLI.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::named_params_distinguish_duplicate_keys_from_positional_collisions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::named_arguments_reject_duplicate_keys_before_transport -- --exact</automated>
  </verify>
  <done>Duplicate named CLI arguments can no longer be silently overwritten or transported as a mutated request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate stdin reads and add the open-stdin CLI regression</name>
  <read_first>
    - packages/open-bitcoin-cli/src/args.rs
    - packages/open-bitcoin-cli/src/args/tests.rs
    - packages/open-bitcoin-cli/src/main.rs
    - packages/open-bitcoin-cli/tests/operator_flows.rs
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/args.rs
    packages/open-bitcoin-cli/src/args/tests.rs
    packages/open-bitcoin-cli/src/main.rs
    packages/open-bitcoin-cli/tests/operator_flows.rs
    .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
  </files>
  <behavior>
    - Test 1: `stdin_required_for_args()` or the equivalent parser-owned helper returns true only when `-stdin` or `-stdinrpcpass` is enabled.
    - Test 2: `main.rs` does not call `read_to_string()` unless stdin is required by one of those flags.
    - Test 3: The real CLI binary exits to config or transport promptly with an open stdin pipe and no stdin flags.
    - Test 4: Existing `-stdin` and `-stdinrpcpass` behavior remains covered and still consumes stdin in the expected order.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/main.rs` gathers `env::args_os()` first, reads stdin only when a parser-owned helper such as `stdin_required_for_args(&cli_args)` returns true, and passes an empty stdin string into `client::run_cli()` otherwise.
    - `packages/open-bitcoin-cli/tests/operator_flows.rs` includes an automated open-stdin subprocess regression that keeps the child's stdin pipe open, waits up to 2 seconds, and fails the test if the no-stdin-flag CLI process is still running.
    - The no-stdin-flag command reaches a deterministic config or connection failure instead of hanging.
    - `.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md` is created with `generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 08-2026-04-24T02-23-19`, `generated_at`, and `requirements-completed: [CLI-01, CLI-02]`.
  </acceptance_criteria>
  <action>Per CLI-02, D-10, D-11, D-13, D-14, and the stdin/open-stdin gaps in `08-GAPS.md`, prevent the real binary from draining stdin before parsing. Add a lightweight parser-owned stdin requirement scan over raw args, then call `read_to_string()` only for enabled `-stdin` or `-stdinrpcpass`. Add the open-stdin subprocess regression in `operator_flows.rs` using `Command::new(env!("CARGO_BIN_EXE_open-bitcoin-cli")).stdin(Stdio::piped()).spawn()`, keep the stdin handle open, and assert the no-stdin-flag command reaches a connection or config failure instead of hanging. After implementation, create the 08-07 summary with lifecycle provenance and targeted proof commands; do not modify 08-06 or 08-08 summaries here.</action>
  <verify>
    <automated>set -euo pipefail
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::stdin_requirement_detection_matches_stdin_flags -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows normal_cli_without_stdin_flags_does_not_wait_for_open_stdin -- --exact
summary=.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
rg -n '^generated_by: gsd-execute-plan$' "$summary"
rg -n '^lifecycle_mode: yolo$' "$summary"
rg -n '^phase_lifecycle_id: 08-2026-04-24T02-23-19$' "$summary"
rg -n '^generated_at:' "$summary"
rg -nF 'requirements-completed: [CLI-01, CLI-02]' "$summary"
rg -nF 'normal_cli_without_stdin_flags_does_not_wait_for_open_stdin' "$summary"
rg -nF 'stdin_requirement_detection_matches_stdin_flags' "$summary"</automated>
  </verify>
  <done>The CLI/config path accepts baseline-shaped hostnames, rejects duplicate named parameters before transport, and no longer blocks on normal terminal-style invocations without stdin flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config files and CLI flags -> client endpoint | Hostname and port parsing determines which RPC endpoint the CLI contacts. |
| CLI args/stdin -> startup and request building | Local operator input controls auth, endpoint, stdin reads, and RPC parameter encoding. |
| CLI binary -> operator terminal | The binary can deny service by blocking before config or transport when stdin remains open. |
| Execution summary -> GSD lifecycle state | Missing lifecycle fields make the CLI/config gap-closure artifact harder to trace to the approved Phase 8 context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | S | `packages/open-bitcoin-rpc/src/config/loader/rpc_address.rs` | mitigate | Preserve hostname endpoint strings without resolving DNS at config load, and keep explicit port precedence deterministic. |
| T-08-07-02 | T | `packages/open-bitcoin-cli/src/args.rs` and `packages/open-bitcoin-rpc/src/method.rs` | mitigate | Preserve duplicate named parameters through CLI parsing and reject them in the shared normalizer before HTTP transport. |
| T-08-07-03 | D | `packages/open-bitcoin-cli/src/main.rs` | mitigate | Gate stdin reads on enabled stdin flags and add an open-pipe subprocess regression so normal terminal use cannot block before startup. |
| T-08-07-04 | R | `.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md` | mitigate | Require lifecycle provenance in the new 08-07 summary and record targeted proof commands. |
</threat_model>

<verification>
- Targeted config/startup tests prove hostname `rpcconnect` and explicit port precedence.
- Targeted method/args tests prove duplicate named-parameter rejection before transport.
- Targeted stdin and operator-flow tests prove normal CLI invocations do not block on open stdin.
- `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`, and `cargo test --manifest-path packages/Cargo.toml --all-features` pass before committing this plan's implementation.
- 08-08 owns parity docs, final full verification, and human verification notes.
</verification>

<success_criteria>
- `-rpcconnect=localhost` and hostname forms are accepted on the client path with correct port precedence.
- Duplicate named arguments are rejected by shared method normalization before HTTP transport.
- `open-bitcoin-cli` reads stdin only when `-stdin` or `-stdinrpcpass` is enabled.
- The open-stdin subprocess regression catches the real terminal-style path.
- `.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md` carries lifecycle provenance for the CLI/config gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md` with frontmatter containing:

```yaml
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: <execution timestamp>
requirements-completed: [CLI-01, CLI-02]
```
</output>
