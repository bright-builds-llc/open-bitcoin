---
phase: 08-rpc-cli-and-config-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - packages/open-bitcoin-cli/BUILD.bazel
  - packages/open-bitcoin-cli/src/lib.rs
  - packages/open-bitcoin-cli/src/args.rs
  - packages/open-bitcoin-cli/src/args/tests.rs
  - packages/open-bitcoin-cli/src/client.rs
  - packages/open-bitcoin-cli/src/client/tests.rs
  - packages/open-bitcoin-cli/src/getinfo.rs
  - packages/open-bitcoin-cli/src/getinfo/tests.rs
  - packages/open-bitcoin-cli/src/main.rs
  - packages/open-bitcoin-cli/src/output.rs
  - packages/open-bitcoin-cli/tests/operator_flows.rs
  - docs/parity/index.json
  - docs/parity/catalog/rpc-cli-config.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "Operators can invoke the supported Phase 8 surface through `bitcoin-cli`-style flags and method calls with honest auth and precedence behavior."
    - "A hermetic headless workflow completes through CLI and RPC only: descriptor import, rescan, balance or UTXO inspection, deterministic build or sign, and raw transaction submission."
    - "Parity docs make the supported baseline methods, Open Bitcoin extension methods, and deferred surfaces explicit."
  artifacts:
    - path: packages/open-bitcoin-cli/src/args.rs
      provides: "Knots-like CLI flag parsing and precedence rules"
      contains: "stdinrpcpass"
    - path: packages/open-bitcoin-cli/src/client.rs
      provides: "HTTP RPC client over the Phase 8 transport"
      contains: "ureq"
    - path: packages/open-bitcoin-cli/tests/operator_flows.rs
      provides: "Hermetic end-to-end operator flow coverage"
      contains: "buildandsigntransaction"
    - path: docs/parity/catalog/rpc-cli-config.md
      provides: "Supported versus deferred Phase 8 interface ledger"
      contains: "sendtoaddress"
  key_links:
    - from: packages/open-bitcoin-cli/src/getinfo.rs
      to: packages/open-bitcoin-rpc/src/dispatch.rs
      via: "`-getinfo` aggregates `getnetworkinfo`, `getblockchaininfo`, `getwalletinfo`, and `getbalances` as real RPC calls."
      pattern: "getnetworkinfo|getblockchaininfo|getwalletinfo|getbalances"
    - from: packages/open-bitcoin-cli/src/client.rs
      to: packages/open-bitcoin-rpc/src/http.rs
      via: "The CLI uses the authenticated POST JSON-RPC transport instead of duplicating business logic."
      pattern: "POST|Authorization"
    - from: packages/open-bitcoin-cli/tests/operator_flows.rs
      to: docs/parity/catalog/rpc-cli-config.md
      via: "The documented supported and deferred surface matches the operator flows proven by tests."
      pattern: "buildandsigntransaction|sendtoaddress"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T02:12:24Z
---

<objective>
Implement the client-side CLI, prove the headless operator workflows end to end, and document the exact supported versus deferred Phase 8 interface surface.

Purpose: Close D-05 through D-09 and satisfy CLI-01 plus CLI-02 by shipping a `bitcoin-cli`-style interface with correct precedence and auth behavior, then proving the full operator slice works through CLI and RPC only.
Output: Working CLI binary and library, hermetic end-to-end operator flow tests, and updated parity docs for RPC, CLI, and config support.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-01-PLAN.md
@.planning/phases/08-rpc-cli-and-config-parity/08-02-PLAN.md
@packages/bitcoin-knots/src/bitcoin-cli.cpp
@packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
@packages/bitcoin-knots/test/functional/rpc_getgeneralinfo.py
@packages/bitcoin-knots/test/functional/rpc_deriveaddresses.py
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-rpc/src/config.rs
@packages/open-bitcoin-rpc/src/http.rs
@packages/open-bitcoin-rpc/src/dispatch.rs
@docs/parity/index.json
@docs/parity/catalog/wallet.md
@docs/parity/catalog/p2p.md

<interfaces>
Expected from Plan `08-02` in `packages/open-bitcoin-rpc/src/dispatch.rs`:
```rust
pub fn dispatch(
    method: RpcMethod,
    context: &mut ManagedRpcContext,
) -> Result<serde_json::Value, RpcError>;
```

Expected from Plan `08-02` in `packages/open-bitcoin-rpc/src/config.rs`:
```rust
pub fn load_runtime_config(cli: CliConfigArgs) -> Result<RuntimeConfig, ConfigError>;
```

Expected from Plan `08-02` in `packages/open-bitcoin-rpc/src/http.rs`:
```rust
pub fn router(context: ManagedRpcContext, config: RpcServerConfig) -> Router;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Knots-like CLI argument parsing, precedence, and `-getinfo` batching</name>
  <read_first>
    - packages/open-bitcoin-cli/src/lib.rs
    - packages/open-bitcoin-cli/src/args.rs
    - packages/open-bitcoin-cli/src/getinfo.rs
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
    - packages/bitcoin-knots/test/functional/rpc_getgeneralinfo.py
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/open-bitcoin-rpc/src/config.rs
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/lib.rs
    packages/open-bitcoin-cli/src/args.rs
    packages/open-bitcoin-cli/src/args/tests.rs
    packages/open-bitcoin-cli/src/getinfo.rs
    packages/open-bitcoin-cli/src/getinfo/tests.rs
  </files>
  <behavior>
    - Test 1: `-stdinrpcpass` consumes the first stdin line before `-stdin` appends later positional arguments.
    - Test 2: Invalid ports in `-rpcconnect` and `-rpcport` fail before any network call.
    - Test 3: Named arguments reject positional collisions and repeated `args=` payloads the same way the upstream CLI tests expect.
    - Test 4: `-getinfo` rejects extra arguments and builds a four-call batch over `getnetworkinfo`, `getblockchaininfo`, `getwalletinfo`, and `getbalances`.
  </behavior>
  <acceptance_criteria>
    - The CLI follows the baseline mental model from D-05: a method-oriented client binary, not a custom nested app-specific subcommand tree.
    - Flag precedence mirrors the supported upstream rules: `-rpcport` overrides the port in `-rpcconnect`, the port embedded in `-rpcconnect` overrides the chain default, and cookie auth is preferred when `-rpcpassword` is empty per D-06.
    - The parser supports `-named`, `-stdinrpcpass`, `-stdin`, `-getinfo`, and `-color` for the supported slice, while explicitly rejecting `-netinfo` and `-rpcwallet` as deferred surfaces.
    - The `-getinfo` helper stays a thin batch builder over real RPC methods and gains a machine-readable JSON mode for automation per D-07.
  </acceptance_criteria>
  <action>Per D-05, D-06, and D-07, implement `packages/open-bitcoin-cli/src/args.rs` with `clap` derive so the binary behaves like `bitcoin-cli` for the supported slice. Support method-style invocation plus the specific flags `-named`, `-stdinrpcpass`, `-stdin`, `-rpcconnect`, `-rpcport`, `-rpcuser`, `-rpcpassword`, `-rpccookiefile`, `-getinfo`, and `-color`; reject `-netinfo` and `-rpcwallet` with actionable deferred-surface errors. In `getinfo.rs`, keep `-getinfo` as a strict four-request batch over `getnetworkinfo`, `getblockchaininfo`, `getwalletinfo`, and `getbalances`, reject extra arguments, and add a deterministic JSON rendering mode for automation instead of only the human dashboard format.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::stdinrpcpass_is_consumed_before_stdin_arguments -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::invalid_rpc_ports_fail_before_request_dispatch -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::named_arguments_reject_positional_collisions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features getinfo::tests::getinfo_builds_expected_four_call_batch_and_rejects_extra_args -- --exact</automated>
  </verify>
  <done>The CLI can parse the supported `bitcoin-cli`-style surface correctly and build the exact request batches or auth config the transport layer expects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the HTTP client, CLI execution path, and hermetic operator-flow tests</name>
  <read_first>
    - packages/open-bitcoin-cli/src/args.rs
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
    - packages/bitcoin-knots/test/functional/rpc_deriveaddresses.py
  </read_first>
  <files>
    packages/open-bitcoin-cli/BUILD.bazel
    packages/open-bitcoin-cli/src/client.rs
    packages/open-bitcoin-cli/src/client/tests.rs
    packages/open-bitcoin-cli/src/output.rs
    packages/open-bitcoin-cli/src/main.rs
    packages/open-bitcoin-cli/tests/operator_flows.rs
  </files>
  <behavior>
    - Test 1: The CLI uses HTTP POST with Basic auth and surfaces RPC errors as exit code `1` with actionable stderr.
    - Test 2: An end-to-end hermetic flow completes through CLI and RPC only: `importdescriptors` → `rescanblockchain` → `getbalances` → `listunspent` → `buildandsigntransaction` → `sendrawtransaction`.
    - Test 3: `-getinfo --json` returns stable machine-readable output, while plain `-getinfo` still renders the human dashboard.
    - Test 4: Deferred commands such as `sendtoaddress`, `-netinfo`, and `-rpcwallet` fail explicitly instead of silently degrading.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/client.rs` uses `ureq::Agent` and the shared runtime-config or method metadata from `open-bitcoin-rpc`; it does not duplicate method schemas or auth parsing.
    - The CLI executes the supported baseline-backed methods plus the documented Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`.
    - The operator-flow tests start the Phase 8 RPC server in-process with in-memory runtime fixtures, without external daemons or GUI steps, satisfying D-08.
    - Machine-readable output is stable for info and wallet-inspection commands where automation materially benefits, satisfying D-07.
  </acceptance_criteria>
  <action>Per D-05, D-07, and D-08, implement `packages/open-bitcoin-cli/src/client.rs` as a thin `ureq::Agent` JSON-RPC client over the Phase 8 transport, with centralized request building, Basic auth headers, and exit-code-aware error rendering in `output.rs` plus `main.rs`. Then add `packages/open-bitcoin-cli/tests/operator_flows.rs` that boots the `open-bitcoind` router in-process with in-memory managed runtime fixtures and proves the full headless workflow through CLI and RPC only: import a descriptor, rescan, inspect balances and UTXOs, call `buildandsigntransaction`, and submit the resulting hex through `sendrawtransaction`. The same suite must assert explicit deferred-surface failures for `sendtoaddress`, `-netinfo`, and `-rpcwallet`.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features client::tests::rpc_errors_surface_exit_code_one_with_actionable_stderr -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features client::tests::getinfo_json_mode_is_stable_for_automation -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows descriptor_rescan_balance_build_sign_and_send_roundtrip -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows deferred_surfaces_fail_explicitly -- --exact</automated>
  </verify>
  <done>A real local operator can drive the supported Phase 8 node and wallet slice through CLI and RPC alone, and the full workflow is pinned by hermetic tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update the parity ledger for the supported and deferred Phase 8 interface surface</name>
  <read_first>
    - docs/parity/index.json
    - docs/parity/catalog/wallet.md
    - docs/parity/catalog/p2p.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
  </read_first>
  <files>
    docs/parity/index.json
    docs/parity/catalog/rpc-cli-config.md
  </files>
  <acceptance_criteria>
    - `docs/parity/catalog/rpc-cli-config.md` lists the exact baseline-backed methods implemented in Phase 8 and the Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`.
    - The parity doc explicitly marks `sendtoaddress`, `rpcauth`, `rpcwhitelist`, `/wallet/<name>`, `getpeerinfo`, `-netinfo`, richer wallet admin flows, and multiwallet persistence as deferred instead of silently omitted, satisfying D-09.
    - `docs/parity/index.json` includes a new catalog document entry for the Phase 8 operator-interface surface.
  </acceptance_criteria>
  <action>Per D-09, add `docs/parity/catalog/rpc-cli-config.md` and update `docs/parity/index.json` so the operator-interface surface is auditable. Document the exact supported baseline-backed RPC or CLI methods, the Open Bitcoin extension methods used for deterministic build or sign flows, the supported single-wallet and local-operator auth plus config slice, and every deferred surface that Phase 8 intentionally leaves out: `sendtoaddress`, `rpcauth`, whitelist controls, `/wallet/<name>`, `getpeerinfo`, `-netinfo`, external signers, richer wallet admin flows, multiwallet persistence, and GUI work.</action>
  <verify>
    <automated>rg -n 'buildtransaction|buildandsigntransaction|sendtoaddress|rpcauth|rpcwhitelist|rpcwallet|getpeerinfo|-netinfo' docs/parity/catalog/rpc-cli-config.md
rg -n 'rpc-cli-config' docs/parity/index.json</automated>
  </verify>
  <done>The parity ledger tells future phases and reviewers exactly what Phase 8 supports, what it extends, and what remains deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args and stdin → CLI request builder | Untrusted local input crosses into auth, port selection, named-argument normalization, and command execution. |
| CLI HTTP client → RPC transport | Client output and error handling must preserve auth, POST-only semantics, and explicit failures. |
| documented parity surface → future operator expectations | Missing or vague parity docs can mislead users about deferred wallet, auth, or multiwallet behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | T | `packages/open-bitcoin-cli/src/args.rs` | mitigate | Normalize named and positional arguments in one place, reject duplicates or collisions, and preserve the upstream `-stdinrpcpass` and port-precedence rules. |
| T-08-03-02 | S | `packages/open-bitcoin-cli/src/client.rs` | mitigate | Reuse the shared auth config to build Basic auth headers, prefer cookie auth when no password is set, and keep HTTP execution POST-only through the typed client layer. |
| T-08-03-03 | E | `packages/open-bitcoin-cli/tests/operator_flows.rs` | mitigate | Prove the supported headless workflow with hermetic in-memory fixtures and assert explicit failures for deferred mutating or multiwallet surfaces. |
| T-08-03-04 | R | `docs/parity/catalog/rpc-cli-config.md` | mitigate | Document baseline-backed methods, Open Bitcoin extensions, and deferred interfaces explicitly so operators cannot mistake omitted behavior for parity. |
</threat_model>

<verification>
- The targeted CLI parsing, client, and operator-flow tests named above pass exactly.
- The new parity catalog document and root index entry mention the supported and deferred Phase 8 surfaces explicitly.
- `bash scripts/verify.sh` is expected to pass on the final Phase 8 tree once all plans are complete.
</verification>

<success_criteria>
- The CLI behaves like the supported `bitcoin-cli` slice for auth, precedence, named args, and `-getinfo`.
- A descriptor-import through raw-transaction-submission workflow runs through CLI and RPC only in hermetic tests.
- Parity docs make the supported baseline methods, extension methods, and deferred surfaces explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-03-SUMMARY.md`
</output>
