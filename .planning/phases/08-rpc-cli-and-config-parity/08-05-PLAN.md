---
phase: 08-rpc-cli-and-config-parity
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - packages/open-bitcoin-cli/BUILD.bazel
  - packages/open-bitcoin-cli/src/client.rs
  - packages/open-bitcoin-cli/src/client/tests.rs
  - packages/open-bitcoin-cli/src/output.rs
  - packages/open-bitcoin-cli/src/main.rs
  - packages/open-bitcoin-cli/tests/operator_flows.rs
  - docs/parity/index.json
  - docs/parity/catalog/rpc-cli-config.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "Operators can execute the supported Phase 8 RPC surface through the CLI with explicit auth, actionable non-zero failures, and stable machine-readable output where promised."
    - "A hermetic headless workflow completes through CLI and RPC only: descriptor import, rescan, balance or UTXO inspection, deterministic build or sign, and raw transaction submission."
    - "Parity docs state the supported baseline methods, Open Bitcoin extension methods, and deferred surfaces explicitly."
  artifacts:
    - path: packages/open-bitcoin-cli/src/client.rs
      provides: "Thin HTTP RPC client over the Phase 8 transport"
      contains: "ureq"
    - path: packages/open-bitcoin-cli/src/main.rs
      provides: "CLI execution path over explicit startup and helper contracts"
      contains: "main"
    - path: packages/open-bitcoin-cli/tests/operator_flows.rs
      provides: "Hermetic end-to-end operator flow coverage"
      contains: "buildandsigntransaction"
    - path: docs/parity/catalog/rpc-cli-config.md
      provides: "Supported versus deferred Phase 8 interface ledger"
      contains: "sendtoaddress"
  key_links:
    - from: packages/open-bitcoin-cli/src/main.rs
      to: packages/open-bitcoin-cli/src/startup.rs
      via: "The CLI execution path consumes the explicit startup config from Plan 08-04 before issuing RPCs or helper batches."
      pattern: "CliStartupConfig|resolve_startup"
    - from: packages/open-bitcoin-cli/tests/operator_flows.rs
      to: packages/open-bitcoin-rpc/src/http.rs
      via: "Hermetic operator-flow tests boot the real Phase 8 router in-process and drive it only through CLI requests."
      pattern: "router|buildandsigntransaction|sendrawtransaction"
    - from: docs/parity/catalog/rpc-cli-config.md
      to: docs/parity/index.json
      via: "The parity ledger indexes the supported and deferred Phase 8 operator interface surface."
      pattern: "rpc-cli-config"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: 2026-04-23T03:12:35Z
lifecycle_repair_note: "Adopted into refreshed Phase 08 gap-closure lifecycle on 2026-04-24; original generated_at retained."
---

<objective>
Implement the CLI HTTP client and execution path, prove the headless operator workflows end to end, and document the exact supported versus deferred Phase 8 interface surface.

Purpose: Close D-05 through D-09 and satisfy CLI-01 plus CLI-02 by turning the frozen startup and helper contracts from Plan 08-04 into a working `bitcoin-cli`-style client, then proving the supported operator slice works through CLI and RPC only.
Output: Working CLI binary and HTTP client, hermetic end-to-end operator-flow tests, and updated parity docs for RPC, CLI, and config support.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-03-PLAN.md
@.planning/phases/08-rpc-cli-and-config-parity/08-04-PLAN.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/bitcoin-cli.cpp
@packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
@packages/bitcoin-knots/test/functional/rpc_deriveaddresses.py
@packages/open-bitcoin-rpc/src/http.rs
@packages/open-bitcoin-rpc/src/dispatch.rs
@packages/open-bitcoin-rpc/src/method.rs
@docs/parity/index.json
@docs/parity/catalog/wallet.md
@docs/parity/catalog/p2p.md

<interfaces>
Expected from Plan `08-04` in `packages/open-bitcoin-cli/src/startup.rs`:
```rust
pub struct CliStartupConfig {
    pub rpc_client: RpcClientConfig,
}

pub fn resolve_startup(parsed: ParsedCliArgs) -> Result<CliStartupConfig, CliStartupError>;
```

Expected from Plan `08-04` in `packages/open-bitcoin-cli/src/getinfo.rs`:
```rust
pub fn build_getinfo_batch() -> Vec<JsonRpcRequestEnvelope>;
```

Expected from Plan `08-03` in `packages/open-bitcoin-rpc/src/http.rs`:
```rust
pub fn router(context: ManagedRpcContext, config: RpcServerConfig) -> Router;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the HTTP client, CLI execution path, and operator-facing output handling</name>
  <read_first>
    - packages/open-bitcoin-cli/src/startup.rs
    - packages/open-bitcoin-cli/src/getinfo.rs
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
  </read_first>
  <files>
    packages/open-bitcoin-cli/BUILD.bazel
    packages/open-bitcoin-cli/src/client.rs
    packages/open-bitcoin-cli/src/client/tests.rs
    packages/open-bitcoin-cli/src/output.rs
    packages/open-bitcoin-cli/src/main.rs
  </files>
  <behavior>
    - Test 1: The CLI uses HTTP POST with Basic auth and surfaces RPC errors as exit code `1` with actionable stderr.
    - Test 2: `-getinfo --json` returns stable machine-readable output, while plain `-getinfo` still renders the human dashboard.
    - Test 3: Supported baseline-backed methods and the documented Open Bitcoin extension methods execute through the same request builder instead of bespoke per-command clients.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-cli/src/client.rs` uses `ureq::Agent` and the shared runtime-config or method metadata from `open-bitcoin-rpc`; it does not duplicate method schemas or auth parsing.
    - `packages/open-bitcoin-cli/src/main.rs` consumes the explicit startup contract from Plan 08-04 and executes either method-style RPC calls or the `-getinfo` helper path without re-deciding precedence rules.
    - `packages/open-bitcoin-cli/src/output.rs` gives stable machine-readable output where promised and actionable stderr with explicit exit code `1` for RPC failures, satisfying D-07.
    - This task stays focused on client execution and output handling; hermetic operator-flow coverage and parity docs stay in the following task.
  </acceptance_criteria>
  <action>Per D-05 and D-07, implement `packages/open-bitcoin-cli/src/client.rs` as a thin `ureq::Agent` JSON-RPC client over the Phase 8 transport, with centralized request building, Basic auth headers, and no duplicate config or method parsing. Then wire `packages/open-bitcoin-cli/src/main.rs` plus `output.rs` so the CLI consumes `CliStartupConfig` from Plan 08-04, executes supported baseline-backed methods plus the documented Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`, and renders deterministic JSON or actionable stderr with explicit exit codes. Keep this task limited to executable client behavior; do not add parity docs or the full end-to-end operator-flow fixtures here.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features client::tests::rpc_errors_surface_exit_code_one_with_actionable_stderr -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features client::tests::getinfo_json_mode_is_stable_for_automation -- --exact</automated>
  </verify>
  <done>The CLI can issue authenticated RPC requests over the supported Phase 8 surface and render deterministic operator-facing output without reopening startup or helper-flag decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the supported headless operator flow and update the parity ledger</name>
  <read_first>
    - packages/open-bitcoin-cli/src/main.rs
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
    - packages/bitcoin-knots/test/functional/rpc_deriveaddresses.py
    - docs/parity/index.json
    - docs/parity/catalog/wallet.md
    - docs/parity/catalog/p2p.md
  </read_first>
  <files>
    packages/open-bitcoin-cli/tests/operator_flows.rs
    docs/parity/index.json
    docs/parity/catalog/rpc-cli-config.md
  </files>
  <behavior>
    - Test 1: An end-to-end hermetic flow completes through CLI and RPC only: `importdescriptors` → `rescanblockchain` → `getbalances` → `listunspent` → `buildandsigntransaction` → `sendrawtransaction`.
    - Test 2: Deferred commands such as `sendtoaddress`, `-netinfo`, and `-rpcwallet` fail explicitly instead of silently degrading.
    - Test 3: The parity ledger lists the exact supported baseline-backed methods, Open Bitcoin extension methods, and deferred surfaces for the Phase 8 operator interface.
  </behavior>
  <acceptance_criteria>
    - The operator-flow tests start the Phase 8 RPC server in-process with in-memory runtime fixtures, without external daemons or GUI steps, satisfying D-08.
    - The suite proves the supported headless workflow and asserts explicit deferred-surface failures for `sendtoaddress`, `-netinfo`, and `-rpcwallet`.
    - `docs/parity/catalog/rpc-cli-config.md` lists the exact baseline-backed methods implemented in Phase 8, the Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`, and the deferred surfaces required by D-09.
    - `docs/parity/index.json` includes a new catalog entry for the Phase 8 operator-interface surface.
  </acceptance_criteria>
  <action>Per D-08 and D-09, add `packages/open-bitcoin-cli/tests/operator_flows.rs` that boots the `open-bitcoind` router in-process with in-memory managed runtime fixtures and proves the full supported headless workflow through CLI and RPC only: import a descriptor, rescan, inspect balances and UTXOs, call `buildandsigntransaction`, and submit the resulting hex through `sendrawtransaction`. The same suite must assert explicit deferred-surface failures for `sendtoaddress`, `-netinfo`, and `-rpcwallet`. Then add `docs/parity/catalog/rpc-cli-config.md` and update `docs/parity/index.json` so the operator-interface surface is auditable, explicitly documenting the supported baseline-backed RPC or CLI methods, the Open Bitcoin extension methods used for deterministic build or sign flows, the supported single-wallet and local-operator auth plus config slice, and every deferred surface Phase 8 intentionally leaves out.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows descriptor_rescan_balance_build_sign_and_send_roundtrip -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows deferred_surfaces_fail_explicitly -- --exact
rg -n 'buildtransaction|buildandsigntransaction|sendtoaddress|rpcauth|rpcwhitelist|rpcwallet|getpeerinfo|-netinfo' docs/parity/catalog/rpc-cli-config.md
rg -n 'rpc-cli-config' docs/parity/index.json</automated>
  </verify>
  <done>A real local operator can drive the supported Phase 8 node and wallet slice through CLI and RPC alone, and the parity ledger states exactly what is supported, extended, and deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI HTTP client → RPC transport | Client output and error handling must preserve auth, POST-only semantics, and explicit failures. |
| hermetic operator fixtures → supported headless workflow | The proof suite must exercise the real router and CLI surface instead of a fake or bypassed path. |
| documented parity surface → future operator expectations | Missing or vague parity docs can mislead users about deferred wallet, auth, or multiwallet behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | S | `packages/open-bitcoin-cli/src/client.rs` | mitigate | Reuse the shared auth config to build Basic auth headers, prefer cookie auth when no password is set, and keep HTTP execution POST-only through the typed client layer. |
| T-08-05-02 | E | `packages/open-bitcoin-cli/tests/operator_flows.rs` | mitigate | Prove the supported headless workflow with hermetic in-memory fixtures and assert explicit failures for deferred mutating or multiwallet surfaces. |
| T-08-05-03 | R | `docs/parity/catalog/rpc-cli-config.md` | mitigate | Document baseline-backed methods, Open Bitcoin extensions, and deferred interfaces explicitly so operators cannot mistake omitted behavior for parity. |
</threat_model>

<verification>
- The targeted client, operator-flow, and parity-doc checks named above pass exactly.
- The supported headless operator workflow runs through CLI and RPC only in hermetic tests.
- `bash scripts/verify.sh` is expected to pass on the final Phase 8 tree once all plans are complete.
</verification>

<success_criteria>
- The CLI can execute the supported Phase 8 surface with authenticated HTTP requests, deterministic automation output, and explicit non-zero failure behavior.
- A descriptor-import through raw-transaction-submission workflow runs through CLI and RPC only in hermetic tests.
- Parity docs make the supported baseline methods, extension methods, and deferred surfaces explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-05-SUMMARY.md`
</output>
