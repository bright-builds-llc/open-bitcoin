---
phase: 08-rpc-cli-and-config-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
files_modified:
  - packages/open-bitcoin-cli/src/lib.rs
  - packages/open-bitcoin-cli/src/args.rs
  - packages/open-bitcoin-cli/src/args/tests.rs
  - packages/open-bitcoin-cli/src/startup.rs
  - packages/open-bitcoin-cli/src/startup/tests.rs
  - packages/open-bitcoin-cli/src/getinfo.rs
  - packages/open-bitcoin-cli/src/getinfo/tests.rs
autonomous: true
requirements:
  - CLI-01
must_haves:
  truths:
    - "Operators can invoke the supported Phase 8 CLI surface with baseline-shaped flags, stdin handling, and precedence rules before any HTTP call is attempted."
    - "`bitcoin-cli` startup resolves config location, datadir, endpoint, and auth on the client path itself instead of inheriting server-only startup behavior."
    - "`-getinfo` stays a thin, deterministic helper over real RPC methods and deferred CLI flags fail explicitly."
  artifacts:
    - path: packages/open-bitcoin-cli/src/args.rs
      provides: "Baseline-shaped CLI argument parsing for the supported Phase 8 flags and helper modes"
      contains: "stdinrpcpass"
    - path: packages/open-bitcoin-cli/src/startup.rs
      provides: "Explicit client-side config and auth precedence resolution"
      contains: "CliStartupConfig"
    - path: packages/open-bitcoin-cli/src/getinfo.rs
      provides: "Thin `-getinfo` batch builder and deterministic rendering helper"
      contains: "getnetworkinfo"
  key_links:
    - from: packages/open-bitcoin-cli/src/startup.rs
      to: packages/open-bitcoin-rpc/src/config.rs
      via: "Client startup resolves config and auth through the shared runtime-config loader instead of duplicating precedence logic."
      pattern: "load_runtime_config|RpcClientConfig"
    - from: packages/open-bitcoin-cli/src/args.rs
      to: packages/open-bitcoin-cli/src/startup.rs
      via: "Parsed CLI flags and stdin-derived password state feed one explicit startup-resolution path before request dispatch."
      pattern: "stdinrpcpass|rpcconnect|rpcport"
    - from: packages/open-bitcoin-cli/src/getinfo.rs
      to: packages/open-bitcoin-rpc/src/method.rs
      via: "`-getinfo` aggregates the four supported RPC calls as real method invocations instead of bespoke local status logic."
      pattern: "getnetworkinfo|getblockchaininfo|getwalletinfo|getbalances"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T03:12:35Z
---

<objective>
Implement the explicit `bitcoin-cli` startup path, argument parsing, and `-getinfo` helper behavior before client execution and operator-flow proof work.

Purpose: Close the D-05 through D-07 startup and parsing slice cleanly by giving Phase 8 a baseline-shaped `bitcoin-cli` entry path with explicit config or auth precedence, helper-flag handling, and deterministic `-getinfo` batching before client transport and end-to-end tests land.
Output: CLI library scaffolding for argument parsing, client-side startup resolution, and `-getinfo` request batching with focused precedence regressions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-02-PLAN.md
@.planning/phases/08-rpc-cli-and-config-parity/08-03-PLAN.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/bitcoin-knots/src/bitcoin-cli.cpp
@packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
@packages/bitcoin-knots/test/functional/rpc_getgeneralinfo.py
@packages/open-bitcoin-rpc/src/method.rs
@packages/open-bitcoin-rpc/src/config.rs

<interfaces>
Expected from Plan `08-03` in `packages/open-bitcoin-rpc/src/config.rs`:
```rust
pub fn load_runtime_config(cli: CliConfigArgs) -> Result<RuntimeConfig, ConfigError>;

pub struct RpcClientConfig {
    pub host: String,
    pub port: u16,
    pub auth: RpcAuthConfig,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit `bitcoin-cli` startup, precedence, and helper-flag parsing</name>
  <read_first>
    - packages/open-bitcoin-cli/src/lib.rs
    - packages/open-bitcoin-cli/src/args.rs
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
    - packages/bitcoin-knots/test/functional/rpc_getgeneralinfo.py
    - packages/open-bitcoin-rpc/src/config.rs
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/lib.rs
    packages/open-bitcoin-cli/src/args.rs
    packages/open-bitcoin-cli/src/args/tests.rs
    packages/open-bitcoin-cli/src/startup.rs
    packages/open-bitcoin-cli/src/startup/tests.rs
  </files>
  <behavior>
    - Test 1: `-stdinrpcpass` consumes the first stdin line before `-stdin` appends later positional arguments.
    - Test 2: Invalid ports in `-rpcconnect` and `-rpcport` fail before any network call.
    - Test 3: The client startup path resolves `-conf`, `-datadir`, `-rpcconnect`, `-rpcport`, cookie auth, and explicit credentials in upstream precedence order instead of relying on server startup behavior.
    - Test 4: Named arguments reject positional collisions and repeated `args=` payloads the same way the upstream CLI tests expect.
    - Test 5: Deferred CLI surfaces `-netinfo` and `-rpcwallet` fail with actionable errors instead of silently degrading.
  </behavior>
  <acceptance_criteria>
    - The CLI follows the baseline mental model from D-05: a method-oriented client binary, not a custom nested app-specific subcommand tree.
    - A dedicated startup module such as `CliStartupConfig` or equivalent resolves config-file and CLI precedence on the `bitcoin-cli` path itself per D-06.
    - Flag precedence mirrors the supported upstream rules: `-rpcport` overrides the port in `-rpcconnect`, the port embedded in `-rpcconnect` overrides the chain default, and cookie auth is preferred when `-rpcpassword` is empty per D-06.
    - CLI `-named` input routes through the same shared method-metadata normalization path introduced in Plan 08-03 instead of defining CLI-only named-parameter semantics.
    - The parser supports `-named`, `-stdinrpcpass`, `-stdin`, `-rpcconnect`, `-rpcport`, `-rpcuser`, `-rpcpassword`, `-rpccookiefile`, `-getinfo`, and `-color` for the supported slice, while explicitly rejecting `-netinfo` and `-rpcwallet` as deferred surfaces.
  </acceptance_criteria>
  <action>Per D-05 and D-06, implement an explicit `bitcoin-cli` startup path instead of leaving config precedence implicit in server startup only. Add `packages/open-bitcoin-cli/src/startup.rs` to load and merge config-file plus CLI inputs through the shared `open-bitcoin-rpc` config loader, then hand the resolved client endpoint and auth settings to the later CLI execution path. In `args.rs`, support method-style invocation plus the specific flags `-named`, `-stdinrpcpass`, `-stdin`, `-rpcconnect`, `-rpcport`, `-rpcuser`, `-rpcpassword`, `-rpccookiefile`, `-getinfo`, and `-color`; reject `-netinfo` and `-rpcwallet` with actionable deferred-surface errors. Route CLI `-named` requests through the same shared method-metadata normalization introduced in Plan 08-03 so duplicate-key rejection and positional/name collision behavior stays identical between HTTP and CLI. Keep this task limited to parsing, normalization, and startup resolution; do not add HTTP execution, output rendering, or operator-flow coverage here.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::stdinrpcpass_is_consumed_before_stdin_arguments -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::invalid_rpc_ports_fail_before_request_dispatch -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features startup::tests::client_startup_resolves_conf_datadir_and_auth_precedence -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::named_arguments_reject_positional_collisions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::deferred_cli_surfaces_fail_with_actionable_errors -- --exact</automated>
  </verify>
  <done>The CLI can resolve its own startup config honestly, parse the supported `bitcoin-cli`-style surface correctly, and hand later transport code one normalized startup contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the `-getinfo` helper batch and deterministic rendering contract</name>
  <read_first>
    - packages/open-bitcoin-cli/src/getinfo.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/bitcoin-knots/test/functional/rpc_getgeneralinfo.py
    - packages/bitcoin-knots/test/functional/interface_bitcoin_cli.py
  </read_first>
  <files>
    packages/open-bitcoin-cli/src/getinfo.rs
    packages/open-bitcoin-cli/src/getinfo/tests.rs
  </files>
  <behavior>
    - Test 1: `-getinfo` rejects extra arguments and builds a four-call batch over `getnetworkinfo`, `getblockchaininfo`, `getwalletinfo`, and `getbalances`.
    - Test 2: `-getinfo --json` returns stable machine-readable output, while plain `-getinfo` still renders the human dashboard.
  </behavior>
  <acceptance_criteria>
    - The `-getinfo` helper stays a thin batch builder over real RPC methods rather than a bespoke local status path, satisfying D-05.
    - The helper rejects extra arguments and keeps a deterministic JSON rendering mode for automation, satisfying D-07.
    - This task does not add HTTP client transport, `main.rs`, or end-to-end operator tests; those stay for the follow-on execution plan created by this revision.
  </acceptance_criteria>
  <action>Per D-05 and D-07, implement `packages/open-bitcoin-cli/src/getinfo.rs` as the thin helper layer over the supported RPC surface. Keep `-getinfo` as a strict four-request batch over `getnetworkinfo`, `getblockchaininfo`, `getwalletinfo`, and `getbalances`, reject extra arguments, and add a deterministic JSON rendering mode for automation instead of only the human dashboard format. Do not add HTTP client transport, `main.rs`, or operator-flow fixtures in this task; the goal here is to freeze the helper contract and precedence behavior before transport execution work begins.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features getinfo::tests::getinfo_builds_expected_four_call_batch_and_rejects_extra_args -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features getinfo::tests::getinfo_json_mode_is_stable_for_automation -- --exact</automated>
  </verify>
  <done>The CLI helper surface is frozen as a real RPC batch contract with deterministic automation output before transport execution work lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args and stdin → CLI request builder | Untrusted local input crosses into auth, port selection, named-argument normalization, and command execution. |
| CLI startup path → shared config loader | Client-side config precedence must be explicit and correct, not inferred indirectly from server startup behavior. |
| `-getinfo` helper → shared RPC method surface | Helper-mode batching must stay aligned with real RPC methods instead of drifting into local-only status behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T | `packages/open-bitcoin-cli/src/args.rs` plus `startup.rs` | mitigate | Normalize named and positional arguments in one place, reject duplicates or collisions, and preserve the upstream `-stdinrpcpass`, config, and port-precedence rules on the client path itself. |
| T-08-04-02 | S | `packages/open-bitcoin-cli/src/startup.rs` | mitigate | Reuse the shared auth config to prefer cookie auth when no password is set and resolve endpoint precedence before any later HTTP call is attempted. |
| T-08-04-03 | T | `packages/open-bitcoin-cli/src/getinfo.rs` | mitigate | Keep helper-mode batching bound to the shared RPC method names and reject extra arguments so helper behavior cannot drift from the real interface surface. |
</threat_model>

<verification>
- The targeted CLI parsing, startup, and `-getinfo` tests named above pass exactly.
- The client-side startup path resolves config and auth precedence explicitly before any transport work begins.
- `bash scripts/verify.sh` remains the expected full-phase closeout command once all Phase 8 plans are complete.
</verification>

<success_criteria>
- The CLI behaves like the supported `bitcoin-cli` slice for startup precedence, auth, named args, and `-getinfo`.
- The client path no longer relies on server-only startup behavior to resolve config or auth.
- Later transport and operator-flow work can build on a frozen parsing and helper contract without reopening CLI precedence decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-04-SUMMARY.md`
</output>
