---
phase: 08-rpc-cli-and-config-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/Cargo.lock
  - BUILD.bazel
  - scripts/verify.sh
  - packages/open-bitcoin-rpc/Cargo.toml
  - packages/open-bitcoin-rpc/BUILD.bazel
  - packages/open-bitcoin-rpc/src/lib.rs
  - packages/open-bitcoin-cli/Cargo.toml
  - packages/open-bitcoin-cli/BUILD.bazel
  - packages/open-bitcoin-cli/src/lib.rs
autonomous: true
requirements:
  - RPC-01
  - CLI-01
must_haves:
  truths:
    - "The workspace exposes first-party RPC and CLI shell crates through both Cargo and Bazel so later interface work can verify cleanly."
    - "Repo-native verification sees the new shell targets from day one instead of discovering missing package wiring late."
    - "Both new crates compile as thin shell entrypoints only; typed contracts, adapter seams, transport, and CLI behavior stay for later plans."
  artifacts:
    - path: packages/Cargo.toml
      provides: "Workspace membership for the new Phase 8 shell crates"
      contains: "open-bitcoin-rpc"
    - path: packages/open-bitcoin-rpc/Cargo.toml
      provides: "RPC shell package manifest with the Phase 8 dependency surface"
      contains: "open-bitcoin-node"
    - path: packages/open-bitcoin-cli/src/lib.rs
      provides: "CLI package shell for later argument parsing and HTTP client work"
      contains: "crate_ready"
  key_links:
    - from: packages/Cargo.toml
      to: BUILD.bazel
      via: "New Phase 8 crates are first-class workspace members and Bazel aliases from day one."
      pattern: "open-bitcoin-rpc|open-bitcoin-cli"
    - from: scripts/verify.sh
      to: BUILD.bazel
      via: "Repo-native verification builds the new shell targets through root aliases."
      pattern: "bazel build"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T02:36:48Z
---

<objective>
Create the Phase 8 shell-package and verification foundation without mixing it with contract, adapter, or transport work.

Purpose: Satisfy D-01, D-05, and research pitfall 6 by making `open-bitcoin-rpc` and `open-bitcoin-cli` first-class packages before later plans add the adapter seam, typed contracts, server transport, or CLI behavior.
Output: New `open-bitcoin-rpc` and `open-bitcoin-cli` package shells, updated Cargo/Bazel wiring, and repo-native verification coverage for the new root targets.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@BUILD.bazel
@packages/Cargo.toml
@scripts/verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add first-class RPC and CLI packages to Cargo and Bazel</name>
  <read_first>
    - packages/Cargo.toml
    - BUILD.bazel
    - .planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
  </read_first>
  <files>
    packages/Cargo.toml
    packages/Cargo.lock
    BUILD.bazel
    packages/open-bitcoin-rpc/Cargo.toml
    packages/open-bitcoin-rpc/BUILD.bazel
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-cli/Cargo.toml
    packages/open-bitcoin-cli/BUILD.bazel
    packages/open-bitcoin-cli/src/lib.rs
  </files>
  <acceptance_criteria>
    - `packages/Cargo.toml` lists `open-bitcoin-rpc` and `open-bitcoin-cli` as workspace members per D-01 and D-05.
    - `packages/open-bitcoin-rpc/Cargo.toml` and `packages/open-bitcoin-cli/Cargo.toml` declare the Phase 8 shell dependencies without introducing third-party Rust Bitcoin libraries per repo constraints.
    - `BUILD.bazel` exports root aliases named `rpc` and `cli` so Phase 8 shell targets exist before behavior work starts.
    - Both new package `BUILD.bazel` files define `rust_library` targets, leaving later transport binaries as thin wrappers instead of collapsing everything into `main.rs`.
    - Both crate roots compile with thin placeholder exports only; no dispatcher, transport, or CLI behavior is implemented in this plan.
  </acceptance_criteria>
  <action>Per D-01, D-05, and pitfall 6 from the research, create `packages/open-bitcoin-rpc` and `packages/open-bitcoin-cli` as first-class workspace members with thin `lib.rs` crate roots, wire them into `packages/Cargo.toml` plus `packages/Cargo.lock`, and add root aliases in `BUILD.bazel`. Declare the shell-layer dependencies that later plans will need, but keep the code surface intentionally minimal here so this plan only solves package and build-system ownership. Do not add `/wallet/<name>` support, `rpcauth`, whitelist controls, or any dependency that would widen the deferred scope.</action>
  <verify>
    <automated>cargo metadata --manifest-path packages/Cargo.toml --format-version 1 > /tmp/open-bitcoin-phase08-metadata.json
rg -n '"name":"open-bitcoin-rpc"|"name":"open-bitcoin-cli"' /tmp/open-bitcoin-phase08-metadata.json
bazel query //:rpc
bazel query //:cli
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib</automated>
  </verify>
  <done>The repo treats the new shell crates like any other first-party package, and later plans can add contracts or behavior without reopening workspace wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire repo-native verification to the new shell targets</name>
  <read_first>
    - scripts/verify.sh
    - BUILD.bazel
    - packages/open-bitcoin-rpc/BUILD.bazel
    - packages/open-bitcoin-cli/BUILD.bazel
  </read_first>
  <files>
    scripts/verify.sh
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-cli/src/lib.rs
  </files>
  <acceptance_criteria>
    - `scripts/verify.sh` builds or checks the new `//:rpc` and `//:cli` aliases as part of the repo-native smoke flow.
    - `packages/open-bitcoin-rpc/src/lib.rs` and `packages/open-bitcoin-cli/src/lib.rs` each expose a deliberately thin crate-root marker such as `crate_ready`, `pub mod`, or placeholder exports that later plans can replace incrementally.
    - The verification wiring does not pretend Phase 8 behavior exists yet; it only ensures missing Bazel or Cargo wiring fails early.
  </acceptance_criteria>
  <action>Per the repo-local verification contract and Bright Builds verification guidance, extend `scripts/verify.sh` so the new root aliases fail fast during Phase 8 work instead of surfacing missing target ownership late in Plan 03 or Plan 04. Keep the RPC and CLI crate roots intentionally skeletal here, and do not leak method contracts, transport logic, or CLI execution behavior into this plan.</action>
  <verify>
    <automated>rg -n '//:rpc|//:cli' scripts/verify.sh
bash scripts/verify.sh</automated>
  </verify>
  <done>Repo-native verification covers the new shell targets, and the package scaffolding is complete enough that later plans only need to add behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifests and build graph → Phase 8 shell packages | Incorrect package ownership or alias wiring can block later interface work before runtime behavior exists. |
| repo-native verify entrypoint → new shell targets | Missing verification wiring can let broken Bazel or Cargo ownership survive until much later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | `packages/Cargo.toml` plus `BUILD.bazel` | mitigate | Make the new shell packages first-class Cargo and Bazel members immediately so later plans cannot drift into partially owned build surfaces. |
| T-08-01-02 | D | `scripts/verify.sh` | mitigate | Extend the repo-native smoke entrypoint to cover `//:rpc` and `//:cli` so missing shell targets fail early during Phase 8 work. |
| T-08-01-03 | T | package manifests | mitigate | Keep the scaffolding limited to shell crates and disallow deferred-scope dependencies such as wallet-path routing or multi-user auth libraries. |
</threat_model>

<verification>
- `cargo metadata`, `cargo build`, and `bazel query` all recognize the new shell crates and root aliases.
- `rg` plus `bash scripts/verify.sh` prove the repo-native verification path and the root aliases cover the new shell targets.
- Later Phase 8 plans can modify source behavior without reopening package or verification wiring.
</verification>

<success_criteria>
- The workspace and Bazel graph both contain first-party `open-bitcoin-rpc` and `open-bitcoin-cli` packages.
- The repo-native verification entrypoint sees those new shell targets from day one.
- No transport, dispatcher, or CLI behavior has leaked into this scaffolding-only plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-01-SUMMARY.md`
</output>
