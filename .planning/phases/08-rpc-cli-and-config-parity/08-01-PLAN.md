---
phase: 08-rpc-cli-and-config-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Cargo.toml
  - packages/Cargo.lock
  - BUILD.bazel
  - scripts/verify.sh
  - packages/open-bitcoin-rpc/Cargo.toml
  - packages/open-bitcoin-rpc/BUILD.bazel
  - packages/open-bitcoin-rpc/src/lib.rs
  - packages/open-bitcoin-rpc/src/error.rs
  - packages/open-bitcoin-rpc/src/envelope.rs
  - packages/open-bitcoin-rpc/src/envelope/tests.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
  - packages/open-bitcoin-cli/Cargo.toml
  - packages/open-bitcoin-cli/BUILD.bazel
  - packages/open-bitcoin-cli/src/lib.rs
autonomous: true
requirements:
  - RPC-01
  - CLI-01
must_haves:
  truths:
    - "The workspace exposes first-party RPC and CLI shell crates through both Cargo and Bazel so later interface work can verify cleanly."
    - "One shared typed contract defines the exact Phase 8 method surface, runtime config, and auth model instead of scattering free-form JSON and config state."
    - "The new interface layer composes `ManagedPeerNetwork` and `ManagedWallet` through a shell-owned runtime context before any transport code is added."
  artifacts:
    - path: packages/Cargo.toml
      provides: "Workspace membership for the new Phase 8 shell crates"
      contains: "open-bitcoin-rpc"
    - path: packages/open-bitcoin-rpc/src/method.rs
      provides: "Typed method registry and metadata for the supported Phase 8 surface"
      contains: "buildandsigntransaction"
    - path: packages/open-bitcoin-rpc/src/config.rs
      provides: "Typed runtime and auth config contracts"
      contains: "RpcAuthConfig"
    - path: packages/open-bitcoin-rpc/src/context.rs
      provides: "Managed runtime context layered over existing node and wallet facades"
      contains: "ManagedRpcContext"
    - path: packages/open-bitcoin-cli/src/lib.rs
      provides: "CLI package shell for later argument parsing and HTTP client work"
      contains: "crate_ready"
  key_links:
    - from: packages/open-bitcoin-node/src/lib.rs
      to: packages/open-bitcoin-rpc/src/context.rs
      via: "`ManagedRpcContext` composes the existing managed shell facades instead of reaching into pure-core crates."
      pattern: "ManagedPeerNetwork|ManagedWallet"
    - from: packages/Cargo.toml
      to: BUILD.bazel
      via: "New Phase 8 crates are first-class workspace members and Bazel aliases from day one."
      pattern: "open-bitcoin-rpc|open-bitcoin-cli"
    - from: scripts/verify.sh
      to: BUILD.bazel
      via: "Repo-native verification builds the new shell targets through root aliases."
      pattern: "bazel build"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T02:12:24Z
---

<objective>
Create the Phase 8 shell-package and contract foundation that later RPC and CLI implementation can build against without re-opening workspace wiring.

Purpose: Satisfy D-01, D-02, D-05, D-06, and research pitfall 6 by adding first-class interface crates, repo-native verification hooks, and a single typed contract layer over the existing managed adapters.
Output: New `open-bitcoin-rpc` and `open-bitcoin-cli` packages, updated Cargo/Bazel wiring, and typed method/config/context contracts with focused contract tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@BUILD.bazel
@packages/Cargo.toml
@packages/open-bitcoin-node/src/lib.rs
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/wallet.rs
@packages/open-bitcoin-wallet/src/wallet.rs
@packages/bitcoin-knots/src/bitcoin-cli.cpp
@packages/bitcoin-knots/src/rpc/request.cpp

<interfaces>
From `packages/open-bitcoin-node/src/lib.rs`:
```rust
pub use chainstate::{ChainstateStore, ManagedChainstate, MemoryChainstateStore};
pub use mempool::ManagedMempool;
pub use network::{ManagedNetworkError, ManagedPeerNetwork};
pub use wallet::{ManagedWallet, MemoryWalletStore, WalletStore};
```

From `packages/open-bitcoin-node/src/network.rs`:
```rust
impl<S: ChainstateStore> ManagedPeerNetwork<S> {
    pub fn new(store: S, local_config: LocalPeerConfig, mempool_config: PolicyConfig) -> Self;
    pub fn chainstate(&self) -> &ManagedChainstate<S>;
    pub fn mempool(&self) -> &ManagedMempool;
    pub fn peer_manager(&self) -> &PeerManager;
    pub fn submit_local_transaction(
        &mut self,
        transaction: Transaction,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<AdmissionResult, ManagedNetworkError>;
}
```

From `packages/open-bitcoin-node/src/wallet.rs`:
```rust
impl<S: WalletStore> ManagedWallet<S> {
    pub fn import_descriptor(
        &mut self,
        label: impl Into<String>,
        role: DescriptorRole,
        descriptor_text: &str,
    ) -> Result<u32, WalletError>;
    pub fn rescan_chainstate(
        &mut self,
        snapshot: &ChainstateSnapshot,
    ) -> Result<(), WalletError>;
    pub fn build_and_sign(
        &self,
        request: &BuildRequest,
        coinbase_maturity: u32,
    ) -> Result<BuiltTransaction, WalletError>;
}
```

From `packages/open-bitcoin-wallet/src/wallet.rs`:
```rust
pub struct WalletBalance {
    pub total: Amount,
    pub spendable: Amount,
    pub immature: Amount,
}

pub struct WalletUtxo {
    pub descriptor_id: u32,
    pub outpoint: OutPoint,
    pub output: TransactionOutput,
    pub created_height: u32,
    pub created_median_time_past: i64,
    pub is_coinbase: bool,
}

pub struct BuildRequest {
    pub recipients: Vec<Recipient>,
    pub fee_rate: FeeRate,
    pub maybe_change_descriptor_id: Option<u32>,
    pub maybe_lock_time: Option<u32>,
    pub enable_rbf: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add first-class RPC and CLI packages to Cargo, Bazel, and repo-native verification</name>
  <read_first>
    - packages/Cargo.toml
    - packages/open-bitcoin-node/Cargo.toml
    - BUILD.bazel
    - packages/open-bitcoin-node/BUILD.bazel
    - scripts/verify.sh
    - .planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
  </read_first>
  <files>
    packages/Cargo.toml
    packages/Cargo.lock
    BUILD.bazel
    scripts/verify.sh
    packages/open-bitcoin-rpc/Cargo.toml
    packages/open-bitcoin-rpc/BUILD.bazel
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-cli/Cargo.toml
    packages/open-bitcoin-cli/BUILD.bazel
    packages/open-bitcoin-cli/src/lib.rs
  </files>
  <acceptance_criteria>
    - `packages/Cargo.toml` lists `open-bitcoin-rpc` and `open-bitcoin-cli` as workspace members per D-01 and D-05.
    - `packages/open-bitcoin-rpc/Cargo.toml` depends on `open-bitcoin-node`, `serde`, `serde_json`, `base64`, `axum`, and `tokio`, and does not add third-party Rust Bitcoin libraries per repo constraints.
    - `packages/open-bitcoin-cli/Cargo.toml` depends on `open-bitcoin-rpc`, `clap`, `ureq`, and `serde_json`, and does not add `percent-encoding` because `/wallet/<name>` stays deferred.
    - `BUILD.bazel` exports root aliases named `rpc` and `cli`, and `scripts/verify.sh` builds those aliases alongside the existing smoke targets so Bazel integration fails early.
    - Both new package `BUILD.bazel` files define `rust_library` targets, leaving later transport binaries as thin wrappers instead of collapsing everything into `main.rs`.
  </acceptance_criteria>
  <action>Per D-01, D-05, and pitfall 6 from the research, create `packages/open-bitcoin-rpc` and `packages/open-bitcoin-cli` as first-class workspace members with `foo.rs` plus `foo/`-style module entry points, wire them into `packages/Cargo.toml` and `packages/Cargo.lock`, add root aliases in `BUILD.bazel`, and extend `scripts/verify.sh` so the repo-native Bazel smoke build covers the new shell libraries. Keep the crate skeletons minimal but compiling, and do not add `/wallet/<name>` support, `rpcauth`, whitelist controls, or any dependency that would widen the deferred scope.</action>
  <verify>
    <automated>cargo metadata --manifest-path packages/Cargo.toml --format-version 1 > /tmp/open-bitcoin-phase08-metadata.json
rg -n '"name":"open-bitcoin-rpc"|"name":"open-bitcoin-cli"' /tmp/open-bitcoin-phase08-metadata.json
bazel query //:rpc
bazel query //:cli</automated>
  </verify>
  <done>The repo treats the new interface crates like any other first-party package, and later plans can implement them without reopening workspace or verification wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the shared method, envelope, config, and managed-runtime contracts</name>
  <read_first>
    - packages/open-bitcoin-node/src/lib.rs
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - packages/bitcoin-knots/src/rpc/request.cpp
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-rpc/src/error.rs
    packages/open-bitcoin-rpc/src/envelope.rs
    packages/open-bitcoin-rpc/src/envelope/tests.rs
    packages/open-bitcoin-rpc/src/method.rs
    packages/open-bitcoin-rpc/src/method/tests.rs
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/tests.rs
  </files>
  <behavior>
    - Test 1: The method registry exposes exactly the supported Phase 8 HTTP methods `getblockchaininfo`, `getmempoolinfo`, `getnetworkinfo`, `sendrawtransaction`, `deriveaddresses`, `getwalletinfo`, `getbalances`, `listunspent`, `importdescriptors`, and `rescanblockchain`.
    - Test 2: The contract also exposes Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`, and flags them as extensions instead of baseline parity methods.
    - Test 3: Runtime config defaults bind RPC to `127.0.0.1`, prefer cookie or explicit `rpcuser` plus `rpcpassword` auth only, and keep a single active wallet slice.
    - Test 4: `ManagedRpcContext` composes `ManagedPeerNetwork<MemoryChainstateStore>` and `ManagedWallet<MemoryWalletStore>` without importing pure-core internals directly into future transport code.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/method.rs` defines typed request/response structs and metadata for every supported Phase 8 method per D-02 and D-03.
    - The extension build/sign methods use machine-friendly integer fields such as `amount_sats` and `fee_rate_sat_per_kvb` so D-07 is satisfied without inventing `sendtoaddress` parity.
    - `packages/open-bitcoin-rpc/src/config.rs` defines `RuntimeConfig`, `RpcServerConfig`, `RpcClientConfig`, and `RpcAuthConfig`, and explicitly excludes `rpcauth`, `rpcwhitelist`, and multiwallet path routing per D-04 and the yolo defaults.
    - `packages/open-bitcoin-rpc/src/error.rs` centralizes JSON-RPC code mapping for parse errors, invalid requests, method not found, invalid params, wallet-required conditions, and auth failures.
    - `packages/open-bitcoin-rpc/src/context.rs` exposes one shell-owned runtime context built on the existing managed facades so later handlers do not bypass `open-bitcoin-node`.
  </acceptance_criteria>
  <action>Per D-01, D-02, D-03, D-06, and D-07, turn `open-bitcoin-rpc` into the single typed contract layer for Phase 8. Add `envelope.rs` for JSON-RPC request or response types, `method.rs` for the supported method registry and exact typed request or result structs, `config.rs` for frozen runtime and auth config contracts, `context.rs` for a `ManagedRpcContext` that composes `ManagedPeerNetwork<MemoryChainstateStore>` plus `ManagedWallet<MemoryWalletStore>`, and `error.rs` for centralized RPC error mapping. The baseline-backed HTTP methods must be the exact list from Test 1, while deterministic wallet construction must surface through extension methods `buildtransaction` and `buildandsigntransaction` with integer satoshi fields instead of claiming `sendtoaddress` parity. Keep `/wallet/<name>`, `rpcauth`, whitelist rules, and full HD or multiwallet semantics out of the contract entirely.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::supported_http_methods_match_phase_8_surface -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::build_transaction_methods_are_marked_as_open_bitcoin_extensions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::runtime_config_defaults_to_local_single_wallet_auth -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features context::tests::managed_rpc_context_composes_network_and_wallet_facades -- --exact</automated>
  </verify>
  <done>The Phase 8 interface surface is defined in one auditable contract layer that later plans can implement without re-deciding scope or method shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config/raw request intent → typed Phase 8 contracts | Untrusted operator input must be constrained by typed method, auth, and runtime-config models before any transport or handler code exists. |
| managed shell facades → future transport code | Future handlers must cross through `ManagedPeerNetwork` and `ManagedWallet` instead of reaching into pure-core state directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | `packages/open-bitcoin-rpc/src/method.rs` | mitigate | Define one typed method registry and exact request or response structs so later plans cannot smuggle free-form JSON through ad hoc handlers per D-02. |
| T-08-01-02 | E | `packages/open-bitcoin-rpc/src/config.rs` | mitigate | Restrict the initial auth contract to cookie or explicit `rpcuser` plus `rpcpassword`, bind locally by default, and omit `rpcauth` and whitelist scope from the Phase 8 contract per D-04 and the yolo defaults. |
| T-08-01-03 | T | `packages/open-bitcoin-rpc/src/context.rs` | mitigate | Compose only the existing managed shell facades inside `ManagedRpcContext` so later RPC and CLI code cannot bypass shell boundaries into pure-core crates. |
</threat_model>

<verification>
- The targeted `open-bitcoin-rpc` contract tests named above pass exactly.
- `cargo metadata` and `bazel query` both recognize the new shell crates and root aliases.
- `bash scripts/verify.sh` is prepared to build the new shell library aliases on later implementation plans.
</verification>

<success_criteria>
- The workspace and Bazel graph both contain first-party `open-bitcoin-rpc` and `open-bitcoin-cli` packages.
- A single typed contract layer defines the supported Phase 8 method surface, runtime config, and auth model.
- The contract layer composes existing managed node and wallet facades without widening pure-core boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-01-SUMMARY.md`
</output>
