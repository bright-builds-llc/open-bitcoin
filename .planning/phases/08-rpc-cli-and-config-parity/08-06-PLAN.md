---
phase: 08-rpc-cli-and-config-parity
plan: 06
type: execute
wave: 6
depends_on:
  - 08-05
files_modified:
  - packages/open-bitcoin-rpc/Cargo.toml
  - packages/open-bitcoin-rpc/BUILD.bazel
  - packages/Cargo.lock
  - MODULE.bazel.lock
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/http.rs
  - packages/open-bitcoin-rpc/src/http/tests.rs
  - .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - RPC-01
user_setup: []
must_haves:
  truths:
    - "RPC `rescanblockchain` accepts only the full active snapshot range that the Phase 8 wallet seam executes truthfully, and every partial or out-of-bounds range fails with invalid params before wallet mutation."
    - "RPC `sendrawtransaction` rejects explicit `maxfeerate` and `maxburnamount` values before decoding or mempool submission because Phase 8 does not enforce those limits."
    - "Cookie-auth RPC startup creates strong local credentials and owner-only cookie files on Unix."
    - "Plan 08-06 execution produces a lifecycle-provenance summary for the RPC/cookie gap closure."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/dispatch.rs
      provides: "RPC semantic guards for unsupported rescan ranges and unenforced transaction safety-limit parameters"
      contains: "rescan_blockchain"
    - path: packages/open-bitcoin-rpc/src/dispatch/tests.rs
      provides: "Regression coverage proving rejected RPC parameters do not mutate wallet or mempool state"
      contains: "sendrawtransaction_rejects_unenforced_fee_limits"
    - path: packages/open-bitcoin-rpc/src/http.rs
      provides: "Strong cookie-auth secret creation and owner-only cookie-file handling"
      contains: "read_or_create_cookie_password"
    - path: packages/open-bitcoin-rpc/src/http/tests.rs
      provides: "Cookie-auth randomness and Unix permission regression coverage"
      contains: "cookie_auth_creates_owner_only_file"
    - path: .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
      provides: "RPC gap-closure lifecycle provenance"
      contains: "phase_lifecycle_id: 08-2026-04-24T02-23-19"
  key_links:
    - from: packages/open-bitcoin-rpc/src/dispatch.rs
      to: packages/open-bitcoin-rpc/src/context.rs
      via: "`rescanblockchain` must call `ManagedRpcContext::rescan_wallet(&snapshot)` only for the full active snapshot that the current seam can execute truthfully."
      pattern: "rescan_wallet"
    - from: packages/open-bitcoin-rpc/src/dispatch.rs
      to: packages/open-bitcoin-rpc/src/context.rs
      via: "`sendrawtransaction` must reject explicit safety-limit parameters before `ManagedRpcContext::submit_local_transaction` can mutate mempool state."
      pattern: "submit_local_transaction"
    - from: packages/open-bitcoin-rpc/src/http.rs
      to: packages/open-bitcoin-rpc/Cargo.toml
      via: "Cookie-auth generation must use a direct randomness dependency owned by the shell crate, not a predictable fallback."
      pattern: "getrandom"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: 2026-04-24T02:37:29Z
---

<objective>
Close only the Phase 8 RPC dispatcher semantics and cookie-auth hardening gaps from `08-GAPS.md`.

Purpose: Preserve the completed 08-01 through 08-05 implementation shape while making unsupported RPC parameters fail explicitly and removing weak local cookie-auth credential creation.
Output: Focused RPC dispatcher regressions, hardened cookie-auth creation, generated lock/build metadata as needed, and an 08-06 lifecycle summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-03-SUMMARY.md
@.planning/phases/08-rpc-cli-and-config-parity/08-05-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/open-bitcoin-rpc/src/dispatch.rs
@packages/open-bitcoin-rpc/src/dispatch/tests.rs
@packages/open-bitcoin-rpc/src/http.rs
@packages/open-bitcoin-rpc/src/http/tests.rs
@packages/open-bitcoin-rpc/src/context.rs
@packages/open-bitcoin-rpc/src/method.rs

<interfaces>
Current RPC context seam from `packages/open-bitcoin-rpc/src/context.rs`:
```rust
pub fn blockchain_snapshot(&self) -> ChainstateSnapshot;
pub fn rescan_wallet(&mut self, snapshot: &ChainstateSnapshot) -> Result<(), WalletError>;
pub fn submit_local_transaction(
    &mut self,
    transaction: Transaction,
) -> Result<AdmissionResult, ManagedNetworkError>;
```

Current request contracts from `packages/open-bitcoin-rpc/src/method.rs`:
```rust
pub struct SendRawTransactionRequest {
    pub transaction_hex: String,
    pub maybe_max_fee_rate_sat_per_kvb: Option<i64>,
    pub maybe_max_burn_amount_sats: Option<i64>,
    pub ignore_rejects: Vec<String>,
}

pub struct RescanBlockchainRequest {
    pub maybe_start_height: Option<u32>,
    pub maybe_stop_height: Option<u32>,
}
```
</interfaces>
</context>

<requirements_coverage>
| Requirement | Task | Coverage |
|-------------|------|----------|
| RPC-01 | Task 1, Task 2, Task 3 | Full: unsupported `rescanblockchain` ranges and explicit `sendrawtransaction` safety-limit parameters are rejected, and cookie-auth credential handling is hardened at the RPC shell boundary. |
</requirements_coverage>

<gap_coverage>
| Verification gap | Plan | Task | Coverage |
|------------------|------|------|----------|
| `rescanblockchain` range handling | 08-06 | Task 1 | Full |
| `sendrawtransaction` `maxfeerate` / `maxburnamount` handling | 08-06 | Task 1 | Full |
| Cookie auth hardening | 08-06 | Task 2, Task 3 | Full |
| 08-06 summary lifecycle provenance | 08-06 | Task 3 | Full |
</gap_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject unsupported RPC rescan and transaction safety semantics</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-rpc/src/context.rs
    - packages/open-bitcoin-rpc/src/method.rs
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/dispatch.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
  </files>
  <behavior>
    - Test 1: `rescanblockchain` with omitted heights or an explicit full range `start_height=0, stop_height=<tip_height>` calls `ManagedRpcContext::rescan_wallet(&snapshot)` and returns `{start_height: 0, stop_height: <tip_height>}`.
    - Test 2: `rescanblockchain` with `start_height > 0`, `stop_height < tip_height`, `start_height > tip_height`, `stop_height > tip_height`, or `stop_height < start_height` returns RPC invalid params code `-32602` and does not mutate wallet UTXOs.
    - Test 3: `sendrawtransaction` with any explicit non-null `maxfeerate` returns RPC invalid params code `-32602` before transaction decoding or mempool submission.
    - Test 4: `sendrawtransaction` with any explicit non-null `maxburnamount` returns RPC invalid params code `-32602` before transaction decoding or mempool submission.
    - Test 5: The same transaction submission path succeeds when `maxfeerate` and `maxburnamount` are omitted or JSON-null.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/dispatch.rs` rejects unsupported partial rescan windows with the exact message `rescanblockchain height ranges are not supported in Phase 8; omit start_height and stop_height to rescan the full active snapshot`.
    - `rescan_blockchain()` calls `context.rescan_wallet(&snapshot)` only after validating that the effective requested range is the full active snapshot: `start_height == 0` and `stop_height == tip_height`.
    - `send_raw_transaction()` rejects `maybe_max_fee_rate_sat_per_kvb.is_some()` with the exact message `sendrawtransaction maxfeerate enforcement is not supported in Phase 8; omit maxfeerate`.
    - `send_raw_transaction()` rejects `maybe_max_burn_amount_sats.is_some()` with the exact message `sendrawtransaction maxburnamount enforcement is not supported in Phase 8; omit maxburnamount`.
    - The successful `sendrawtransaction` path still accepts omitted or JSON-null `maxfeerate` and `maxburnamount`, preserving existing no-limit Phase 8 behavior without accepting ignored explicit limits.
    - No pure-core crate imports filesystem, randomness, HTTP, or config dependencies; all changes remain in shell crates per D-01.
  </acceptance_criteria>
  <action>Per RPC-01, D-02, D-03, D-04, D-05, D-11, and the `08-GAPS.md` RPC gaps, make `dispatch.rs` stop accepting semantics the current seams cannot execute truthfully. The wallet seam takes only `rescan_wallet(&ChainstateSnapshot)`, which rebuilds wallet UTXOs from the full snapshot; do not fake a scoped rescan by filtering and replacing the full wallet view. Accept only omitted heights or the explicit full snapshot range `0..tip_height`, return the validated start and stop heights, and reject every partial or out-of-bounds range before calling `context.rescan_wallet(&snapshot)`. In `send_raw_transaction()`, reject any explicit non-null `maxfeerate` or `maxburnamount` before decoding or submitting the transaction because Phase 8 has no typed fee-rate or burn-amount evaluator at the dispatcher boundary. Add focused dispatch tests that prove rejection occurs before wallet or mempool mutation.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::rescanblockchain_rejects_partial_height_ranges_without_rescanning -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::sendrawtransaction_rejects_unenforced_fee_limits_before_mempool_submission -- --exact</automated>
  </verify>
  <done>RPC inputs that cannot be enforced no longer reach wallet or mempool mutation paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add direct randomness dependency for RPC cookie auth</name>
  <read_first>
    - packages/open-bitcoin-rpc/Cargo.toml
    - packages/open-bitcoin-rpc/BUILD.bazel
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/Cargo.toml
    packages/open-bitcoin-rpc/BUILD.bazel
    packages/Cargo.lock
    MODULE.bazel.lock
  </files>
  <acceptance_criteria>
    - `open-bitcoin-rpc` uses a direct `getrandom` dependency for 32 random bytes and updates Cargo/Bazel lock metadata as needed.
    - `packages/open-bitcoin-rpc/BUILD.bazel`, `packages/Cargo.lock`, and `MODULE.bazel.lock` are updated consistently with the direct dependency.
    - No Rust source behavior is changed in this task; cookie-auth implementation and tests stay in Task 3.
  </acceptance_criteria>
  <action>Per RPC-01, D-01, D-06, and the cookie-auth warning in `08-GAPS.md`, add the direct randomness dependency needed by the RPC shell before changing credential creation code. Update `packages/open-bitcoin-rpc/Cargo.toml`, `packages/open-bitcoin-rpc/BUILD.bazel`, `packages/Cargo.lock`, and `MODULE.bazel.lock` so Task 3 can use `getrandom` without mixing dependency metadata, implementation, and summary work in one step. Keep the dependency in `open-bitcoin-rpc`; do not introduce randomness into pure-core crates.</action>
  <verify>
    <automated>cargo metadata --manifest-path packages/Cargo.toml --format-version 1 > /tmp/open-bitcoin-phase08-rpc-cookie-metadata.json
rg -n '"name":"getrandom"|getrandom' /tmp/open-bitcoin-phase08-rpc-cookie-metadata.json packages/open-bitcoin-rpc/Cargo.toml packages/open-bitcoin-rpc/BUILD.bazel packages/Cargo.lock MODULE.bazel.lock</automated>
  </verify>
  <done>The RPC shell crate has a direct, build-system-consistent randomness dependency for cookie-auth credential creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden cookie auth and write the 08-06 lifecycle summary</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/http.rs
    - packages/open-bitcoin-rpc/src/http/tests.rs
    - packages/open-bitcoin-rpc/Cargo.toml
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-05-SUMMARY.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/http.rs
    packages/open-bitcoin-rpc/src/http/tests.rs
    .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
  </files>
  <behavior>
    - Test 1: Creating a missing cookie auth file writes content shaped as `__cookie__:<64 lowercase hex chars>`.
    - Test 2: Cookie auth secret bytes come from `getrandom` and the predictable PID/time fallback is removed.
    - Test 3: On Unix, newly created cookie files have mode `0600`.
    - Test 4: Existing cookie files are read without overwriting their contents.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/http.rs` removes the predictable PID/time fallback from `random_hex_secret()`.
    - Missing cookie files are created with owner-read/write permissions on Unix via `std::os::unix::fs::OpenOptionsExt::mode(0o600)`.
    - Existing-cookie reads preserve current behavior and do not regenerate credentials.
    - Cookie-auth changes remain in the RPC shell crate and do not leak randomness or filesystem concerns into pure-core crates per D-01.
    - `.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md` is created with `generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 08-2026-04-24T02-23-19`, `generated_at`, and `requirements-completed: [RPC-01]`.
    - The summary records the targeted test commands for rescan range rejection, `sendrawtransaction` safety-limit rejection, and cookie-auth hardening.
    - The summary states that CLI/config/stdin and parity-doc gaps are delegated to 08-07 and 08-08.
  </acceptance_criteria>
  <action>Per RPC-01, D-01, D-06, D-13, and the cookie-auth warning in `08-GAPS.md`, treat weak local credentials as an Elevation of Privilege risk under the Phase 8 threat model. Use the direct `getrandom` dependency from Task 2 to remove predictable fallback material from cookie secret generation, and create new cookie files with `0600` mode on Unix while preserving existing-cookie reads. Keep the hardening inside `open-bitcoin-rpc` because auth, filesystem, and randomness are adapter concerns under D-01. Then create the plan completion summary for this RPC-only gap-closure slice. Do not retrofit 08-03 through 08-05 summaries per D-13. Use the lifecycle fields from `08-CONTEXT.md`, include the exact proof commands run during execution, and make the handoff explicit: CLI stdin, hostname endpoint parsing, duplicate named args, docs, full verification, and human verification notes belong to 08-07 and 08-08.</action>
  <verify>
    <automated>set -euo pipefail
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::cookie_auth_creates_owner_only_file_with_random_secret -- --exact
summary=.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
rg -n '^generated_by: gsd-execute-plan$' "$summary"
rg -n '^lifecycle_mode: yolo$' "$summary"
rg -n '^phase_lifecycle_id: 08-2026-04-24T02-23-19$' "$summary"
rg -n '^generated_at:' "$summary"
rg -nF 'requirements-completed: [RPC-01]' "$summary"
rg -nF 'rescanblockchain_rejects_partial_height_ranges_without_rescanning' "$summary"
rg -nF 'sendrawtransaction_rejects_unenforced_fee_limits_before_mempool_submission' "$summary"
rg -nF 'cookie_auth_creates_owner_only_file_with_random_secret' "$summary"</automated>
  </verify>
  <done>Local cookie auth no longer relies on predictable fallback credentials or permissive default file creation, and the RPC gap-closure summary exists with lifecycle provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC parameters -> wallet rescan and mempool submission | Untrusted operator parameters can otherwise claim constraints that the current wallet or mempool seam does not enforce. |
| RPC cookie file -> local authentication | Local credential generation and permissions control whether same-host users can authenticate to mutating RPC methods. |
| Execution summary -> GSD lifecycle state | Missing lifecycle fields make the gap-closure artifact harder to trace to the approved Phase 8 context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | T | `packages/open-bitcoin-rpc/src/dispatch.rs::rescan_blockchain` | mitigate | Reject every partial or out-of-bounds height range with invalid params because the current wallet seam only executes full-snapshot rescans truthfully. |
| T-08-06-02 | T | `packages/open-bitcoin-rpc/src/dispatch.rs::send_raw_transaction` | mitigate | Reject explicit `maxfeerate` and `maxburnamount` values before transaction decoding or mempool submission so unenforced safety limits cannot be smuggled through. |
| T-08-06-03 | E | `packages/open-bitcoin-rpc/src/http.rs::read_or_create_cookie_password` | mitigate | Generate cookie secrets with `getrandom`, remove predictable fallback material, and create new Unix cookie files with mode `0600`. |
| T-08-06-04 | R | `.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md` | mitigate | Require lifecycle provenance in the new 08-06 summary and explicitly hand off remaining gap categories to dependent plans. |
</threat_model>

<verification>
- Targeted RPC tests prove rescan range rejection, transaction safety-limit rejection before mempool submission, and hardened cookie auth.
- `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`, and `cargo test --manifest-path packages/Cargo.toml --all-features` pass before committing this plan's implementation.
- 08-07 owns the CLI/config/stdin gaps; 08-08 owns parity docs, final full verification, and human verification notes.
</verification>

<success_criteria>
- Unsupported `rescanblockchain` height ranges fail with invalid params and do not mutate wallet state.
- Explicit `sendrawtransaction` `maxfeerate` and `maxburnamount` values fail with invalid params and do not reach mempool submission.
- Cookie-auth creation uses strong local randomness and owner-only file permissions on Unix.
- `.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md` carries lifecycle provenance for the RPC/cookie gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md` with frontmatter containing:

```yaml
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: <execution timestamp>
requirements-completed: [RPC-01]
```
</output>
