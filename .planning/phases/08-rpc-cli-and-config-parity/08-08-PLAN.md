---
phase: 08-rpc-cli-and-config-parity
plan: 08
type: execute
wave: 7
depends_on:
  - 08-06
  - 08-07
files_modified:
  - docs/parity/catalog/rpc-cli-config.md
  - .planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - RPC-01
  - CLI-01
  - CLI-02
user_setup: []
must_haves:
  truths:
    - "The Phase 8 parity catalog states the closed RPC semantics for full-snapshot rescans, rejected rescan height ranges, and rejected explicit `sendrawtransaction` safety-limit parameters."
    - "The Phase 8 parity catalog states the closed CLI/config semantics for hostname `rpcconnect`, stdin-gated CLI reads, duplicate named-parameter rejection, and the open-stdin regression."
    - "The final closeout runs targeted gap tests plus the repo-native full verification contract."
    - "Plans 08-06, 08-07, and 08-08 each have lifecycle-provenance summaries tied to `phase_lifecycle_id: 08-2026-04-24T02-23-19`."
    - "Human verification notes for terminal stdin behavior and cookie-auth file exposure are recorded without blocking autonomous execution."
  artifacts:
    - path: docs/parity/catalog/rpc-cli-config.md
      provides: "Auditable record of supported and explicitly rejected Phase 8 RPC/CLI/config semantics"
      contains: "maxfeerate"
    - path: .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
      provides: "RPC/cookie gap-closure lifecycle provenance from dependent plan execution"
      contains: "phase_lifecycle_id: 08-2026-04-24T02-23-19"
    - path: .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
      provides: "CLI/config/stdin gap-closure lifecycle provenance from dependent plan execution"
      contains: "phase_lifecycle_id: 08-2026-04-24T02-23-19"
    - path: .planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
      provides: "Final Phase 8 gap-closure verification and human verification notes"
      contains: "requirements-completed: [RPC-01, CLI-01, CLI-02]"
  key_links:
    - from: docs/parity/catalog/rpc-cli-config.md
      to: .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
      via: "The parity catalog must reflect every verifier gap closed by 08-06 and 08-07."
      pattern: "rescanblockchain|maxfeerate|maxburnamount|rpcconnect=localhost|-stdinrpcpass|duplicate named"
    - from: .planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
      to: scripts/verify.sh
      via: "The final gap-closure summary must record the repo-native verification evidence."
      pattern: "bash scripts/verify.sh"
    - from: .planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
      to: .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
      via: "Final lifecycle closeout must verify dependent gap-closure summaries carry provenance."
      pattern: "08-06-SUMMARY"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: 2026-04-24T02:37:29Z
---

<objective>
Close the Phase 8 gap-closure documentation and final verification slice after 08-06 and 08-07 have implemented the behavior fixes.

Purpose: Make the parity ledger and lifecycle summaries auditable, run the full repo verification contract, and preserve human verification notes for the two environment-sensitive checks identified by the verifier.
Output: Updated Phase 8 parity catalog, final full verification evidence, and an 08-08 lifecycle summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
@.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@docs/parity/catalog/rpc-cli-config.md
@docs/parity/index.json
@scripts/verify.sh
</context>

<requirements_coverage>
| Requirement | Task | Coverage |
|-------------|------|----------|
| RPC-01 | Task 1, Task 2 | Full: parity docs and final verification cover RPC rescan, transaction safety-limit, and cookie-auth gap closures from 08-06. |
| CLI-01 | Task 1, Task 2 | Full: parity docs and final verification cover hostname endpoint parsing and duplicate named-parameter rejection from 08-07. |
| CLI-02 | Task 1, Task 2 | Full: parity docs, final verification, and human notes cover stdin gating and open-stdin operator readiness from 08-07. |
</requirements_coverage>

<gap_coverage>
| Verification gap | Plan | Task | Coverage |
|------------------|------|------|----------|
| Rescan range handling docs | 08-08 | Task 1 | Full |
| `sendrawtransaction` `maxfeerate` / `maxburnamount` docs | 08-08 | Task 1 | Full |
| CLI stdin gating docs | 08-08 | Task 1 | Full |
| `-rpcconnect=localhost` docs | 08-08 | Task 1 | Full |
| Duplicate named parameter docs | 08-08 | Task 1 | Full |
| Open-stdin regression evidence | 08-08 | Task 2 | Full |
| Cookie auth hardening evidence and human note | 08-08 | Task 2 | Full |
| Parity docs | 08-08 | Task 1 | Full |
| 08-06/08-07/08-08 summary lifecycle provenance | 08-08 | Task 2 | Full |
</gap_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Phase 8 parity docs for the closed gap semantics</name>
  <read_first>
    - docs/parity/catalog/rpc-cli-config.md
    - docs/parity/index.json
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
  </read_first>
  <files>
    docs/parity/catalog/rpc-cli-config.md
  </files>
  <acceptance_criteria>
    - `docs/parity/catalog/rpc-cli-config.md` has an anchored bullet or table row for `rescanblockchain` that says Phase 8 supports full active-snapshot rescans and rejects partial height ranges with invalid params.
    - `docs/parity/catalog/rpc-cli-config.md` has an anchored bullet or table row for `sendrawtransaction` that says explicit `maxfeerate` and `maxburnamount` values are rejected because those safety limits are not enforced by the supported dispatcher surface.
    - `docs/parity/catalog/rpc-cli-config.md` has an anchored bullet or table row for `-rpcconnect=localhost` that says hostname endpoints are supported on the client path and preserves explicit `-rpcport` over embedded-port over chain-default precedence.
    - `docs/parity/catalog/rpc-cli-config.md` has an anchored bullet or table row for stdin behavior that says `-stdin` and `-stdinrpcpass` are the only CLI flags that trigger stdin reads.
    - `docs/parity/catalog/rpc-cli-config.md` has an anchored bullet or table row for duplicate named parameters that says they are rejected before transport.
    - `docs/parity/catalog/rpc-cli-config.md` has anchored deferred-surface lines for `sendtoaddress`, `rpcauth`, `rpcwhitelist`, `rpcwallet`, `getpeerinfo`, and `-netinfo`; those checks must not pass solely because the words appear elsewhere in the document.
  </acceptance_criteria>
  <action>Per D-12, RPC-01, CLI-01, CLI-02, and the parity-doc gap in `08-GAPS.md`, update only the Phase 8 parity catalog entry. Make the supported-versus-rejected semantics explicit for full-snapshot rescans, unsupported rescan height ranges, explicit `sendrawtransaction` safety-limit parameters, hostname `rpcconnect` inputs, stdin flags, duplicate named-parameter rejection, and the open-stdin regression. Do not retrofit earlier summaries and do not add support claims for deferred surfaces such as `sendtoaddress`, `rpcauth`, `rpcwhitelist`, `rpcwallet`, `getpeerinfo`, or `-netinfo`.</action>
  <verify>
    <automated>set -euo pipefail
doc=docs/parity/catalog/rpc-cli-config.md
rg -n '^[[:space:]]*[-|].*`rescanblockchain`.*full active-snapshot rescans.*partial height ranges.*invalid params' "$doc"
rg -n '^[[:space:]]*[-|].*`sendrawtransaction`.*`maxfeerate`.*`maxburnamount`.*rejected.*not enforced' "$doc"
rg -n '^[[:space:]]*[-|].*`-rpcconnect=localhost`.*hostname endpoints.*`-rpcport`.*embedded.*chain-default' "$doc"
rg -n '^[[:space:]]*[-|].*`-stdin`.*`-stdinrpcpass`.*only CLI flags.*stdin reads' "$doc"
rg -n '^[[:space:]]*[-|].*duplicate named parameters.*rejected before transport' "$doc"
rg -n '^[[:space:]]*[-|].*open-stdin regression.*normal CLI.*does not wait' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`sendtoaddress`' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`rpcauth`' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`rpcwhitelist`' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`rpcwallet`' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`getpeerinfo`' "$doc"
rg -n '^[[:space:]]*[-|].*deferred.*`-netinfo`' "$doc"</automated>
  </verify>
  <done>The parity catalog states every closed RPC, CLI, config, and stdin gap without widening the Phase 8 interface surface.</done>
</task>

<task type="auto">
  <name>Task 2: Run full closeout verification and write the final lifecycle summary</name>
  <read_first>
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-GAPS.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
    - .planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
    - scripts/verify.sh
  </read_first>
  <files>
    .planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
  </files>
  <acceptance_criteria>
    - The targeted 08-06 and 08-07 commands are rerun or their same exact tests are included in the final evidence block.
    - `cargo fmt --manifest-path packages/Cargo.toml --all` passes.
    - `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings` passes.
    - `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features` passes.
    - `cargo test --manifest-path packages/Cargo.toml --all-features` passes.
    - `bash scripts/verify.sh` passes.
    - `.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md`, `08-07-SUMMARY.md`, and `08-08-SUMMARY.md` all include `generated_by`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 08-2026-04-24T02-23-19`, `generated_at`, and the appropriate `requirements-completed` list.
    - `08-08-SUMMARY.md` includes human verification notes for a real terminal no-stdin invocation and cookie-auth file inspection, with expected outcomes but no blocking checkpoint.
    - `08-08-SUMMARY.md` records that older 08-03 through 08-05 summaries were not retrofitted, and that lifecycle provenance for the new gap-closure slice is carried by 08-06, 08-07, and 08-08.
  </acceptance_criteria>
  <action>After 08-06 and 08-07 are complete, run the targeted commands from their summaries plus the full Rust and repo-native verification sequence. Create `.planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md` with lifecycle fields copied from `08-CONTEXT.md` per D-13, `requirements-completed: [RPC-01, CLI-01, CLI-02]`, targeted proof commands, full verification evidence, and human verification notes per D-14. State that old 08-03 through 08-05 summaries were not retrofitted, while the new gap-closure slice carries lifecycle provenance through 08-06, 08-07, and 08-08. The human notes must include: from a real terminal, run `packages/target/debug/open-bitcoin-cli -rpcconnect=127.0.0.1:9 -rpcuser=alice -rpcpassword=secret getnetworkinfo` and expect a prompt connection failure without waiting for EOF; start cookie-auth RPC mode and inspect the cookie file, expecting content shape `__cookie__:<64 lowercase hex chars>` and Unix mode `0600`.</action>
  <verify>
    <automated>set -euo pipefail
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::rescanblockchain_rejects_partial_height_ranges_without_rescanning -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::sendrawtransaction_rejects_unenforced_fee_limits_before_mempool_submission -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::cookie_auth_creates_owner_only_file_with_random_secret -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::rpcconnect_accepts_hostnames_and_preserves_port_precedence -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::named_params_distinguish_duplicate_keys_from_positional_collisions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features startup::tests::client_startup_preserves_rpcconnect_hostnames -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::named_arguments_reject_duplicate_keys_before_transport -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features args::tests::stdin_requirement_detection_matches_stdin_flags -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-features --test operator_flows normal_cli_without_stdin_flags_does_not_wait_for_open_stdin -- --exact
cargo fmt --manifest-path packages/Cargo.toml --all
cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings
cargo build --manifest-path packages/Cargo.toml --all-targets --all-features
cargo test --manifest-path packages/Cargo.toml --all-features
bash scripts/verify.sh
summary=.planning/phases/08-rpc-cli-and-config-parity/08-06-SUMMARY.md
rg -n '^generated_by: gsd-execute-plan$' "$summary"
rg -n '^lifecycle_mode: yolo$' "$summary"
rg -n '^phase_lifecycle_id: 08-2026-04-24T02-23-19$' "$summary"
rg -n '^generated_at:' "$summary"
rg -nF 'requirements-completed: [RPC-01]' "$summary"
summary=.planning/phases/08-rpc-cli-and-config-parity/08-07-SUMMARY.md
rg -n '^generated_by: gsd-execute-plan$' "$summary"
rg -n '^lifecycle_mode: yolo$' "$summary"
rg -n '^phase_lifecycle_id: 08-2026-04-24T02-23-19$' "$summary"
rg -n '^generated_at:' "$summary"
rg -nF 'requirements-completed: [CLI-01, CLI-02]' "$summary"
summary=.planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md
rg -n '^generated_by: gsd-execute-plan$' "$summary"
rg -n '^lifecycle_mode: yolo$' "$summary"
rg -n '^phase_lifecycle_id: 08-2026-04-24T02-23-19$' "$summary"
rg -n '^generated_at:' "$summary"
rg -nF 'requirements-completed: [RPC-01, CLI-01, CLI-02]' "$summary"
rg -nF 'real terminal' "$summary"
rg -nF 'open stdin' "$summary"
rg -nF 'cookie file' "$summary"
rg -nF '0600' "$summary"
rg -nF 'bash scripts/verify.sh' "$summary"
rg -nF '08-03' "$summary"
rg -nF '08-04' "$summary"
rg -nF '08-05' "$summary"
rg -nF 'not retrofitted' "$summary"</automated>
  </verify>
  <done>The gap-closure behavior is documented, the repo-native verification contract is green, and all three new summaries carry lifecycle provenance plus closeout evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implementation fixes -> parity documentation | Missing or vague docs can mislead operators about which baseline behaviors are supported, rejected, or deferred. |
| Targeted tests -> final verification | Green targeted tests can miss regressions outside the immediate gap files unless the repo-native verification contract is run. |
| Execution summaries -> GSD lifecycle state | Missing lifecycle fields make the gap-closure artifact harder to trace to the approved Phase 8 context. |
| Environment-sensitive behaviors -> human verification notes | Terminal TTY behavior and OS cookie-file exposure need clear manual confirmation notes even with automated regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | R | `docs/parity/catalog/rpc-cli-config.md` | mitigate | Document supported, rejected, and deferred RPC/CLI/config semantics explicitly so parity claims remain auditable. |
| T-08-08-02 | D | full verification closeout | mitigate | Require targeted gap tests plus Cargo fmt, clippy, build, full tests, and `bash scripts/verify.sh` before the final summary is complete. |
| T-08-08-03 | R | `.planning/phases/08-rpc-cli-and-config-parity/*-SUMMARY.md` | mitigate | Verify 08-06, 08-07, and 08-08 summaries all carry lifecycle provenance and requirement completion frontmatter. |
| T-08-08-04 | I | human verification notes | accept | Terminal behavior and cookie-file inspection notes do not expose secrets; they record commands, expected shapes, and permission expectations only. |
</threat_model>

<verification>
- Parity docs contain the closed rescan, transaction safety-limit, hostname endpoint, stdin, duplicate named-parameter, open-stdin, cookie-auth, and deferred-surface semantics.
- Targeted gap tests from 08-06 and 08-07 pass.
- `cargo fmt --manifest-path packages/Cargo.toml --all`, `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`, `cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`, `cargo test --manifest-path packages/Cargo.toml --all-features`, and `bash scripts/verify.sh` pass.
- 08-06, 08-07, and 08-08 summaries carry lifecycle provenance and requirement completion lists.
</verification>

<success_criteria>
- The Phase 8 parity catalog reflects every closed gap from `08-GAPS.md`.
- The final gap-closure summary records targeted proof, full repo verification, and human verification notes.
- `RPC-01`, `CLI-01`, and `CLI-02` are all covered by the split gap-closure plan set.
- No plan in the split exceeds the file-count guardrail that triggered the checker blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-08-SUMMARY.md` with frontmatter containing:

```yaml
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-24T02-23-19
generated_at: <execution timestamp>
requirements-completed: [RPC-01, CLI-01, CLI-02]
```
</output>
