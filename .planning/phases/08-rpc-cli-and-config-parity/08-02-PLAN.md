---
phase: 08-rpc-cli-and-config-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/wallet.rs
  - packages/open-bitcoin-rpc/src/lib.rs
  - packages/open-bitcoin-rpc/src/error.rs
  - packages/open-bitcoin-rpc/src/envelope.rs
  - packages/open-bitcoin-rpc/src/envelope/tests.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
autonomous: true
requirements:
  - RPC-01
  - CLI-01
must_haves:
  truths:
    - "RPC handlers have a shell-owned adapter seam with projection and command helpers, so they do not need to reach through raw `wallet()` or `peer_manager()` internals."
    - "One shared typed contract defines the exact supported Phase 8 method, envelope, auth, and runtime-config surface instead of scattering free-form JSON and config state."
    - "The runtime context composes one managed peer-network instance and one managed wallet slice for the local-operator, single-wallet Phase 8 scope."
  artifacts:
    - path: packages/open-bitcoin-node/src/network.rs
      provides: "Managed network projection and command helpers for RPC use"
      contains: "submit_local_transaction"
    - path: packages/open-bitcoin-node/src/wallet.rs
      provides: "Managed wallet projection and command helpers for RPC use"
      contains: "build_and_sign"
    - path: packages/open-bitcoin-rpc/src/method.rs
      provides: "Typed method registry and metadata for the supported Phase 8 surface"
      contains: "buildandsigntransaction"
    - path: packages/open-bitcoin-rpc/src/config.rs
      provides: "Typed runtime and auth config contracts"
      contains: "RpcAuthConfig"
    - path: packages/open-bitcoin-rpc/src/context.rs
      provides: "Managed runtime context layered over the adapter seam"
      contains: "ManagedRpcContext"
  key_links:
    - from: packages/open-bitcoin-rpc/src/context.rs
      to: packages/open-bitcoin-node/src/network.rs
      via: "`ManagedRpcContext` depends on adapter-owned helper methods instead of calling raw `peer_manager()` or chainstate internals from transport code."
      pattern: "ManagedPeerNetwork"
    - from: packages/open-bitcoin-rpc/src/context.rs
      to: packages/open-bitcoin-node/src/wallet.rs
      via: "Wallet RPC handlers depend on managed wallet projections and commands instead of raw `.wallet()` access."
      pattern: "ManagedWallet"
    - from: packages/open-bitcoin-rpc/src/method.rs
      to: packages/open-bitcoin-rpc/src/config.rs
      via: "The supported method registry and auth or runtime contracts live in one auditable shell contract layer."
      pattern: "RpcAuthConfig|buildandsigntransaction"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T02:36:48Z
---

<objective>
Add the missing adapter seam and the shared typed RPC or config contracts before any dispatcher or transport work starts.

Purpose: Satisfy D-01 through D-04 and close the checker blocker by giving Phase 8 a shell-owned RPC seam over `ManagedPeerNetwork` and `ManagedWallet`, plus one typed contract layer that later server and CLI plans can share.
Output: Adapter-owned projection or command helpers in `open-bitcoin-node`, plus typed envelope, method, config, error, and runtime-context contracts in `open-bitcoin-rpc`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-01-PLAN.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/wallet.rs
@packages/open-bitcoin-wallet/src/wallet.rs
@packages/bitcoin-knots/src/rpc/request.cpp
@packages/bitcoin-knots/src/bitcoin-cli.cpp

<interfaces>
From `packages/open-bitcoin-node/src/network.rs`:
```rust
impl<S: ChainstateStore> ManagedPeerNetwork<S> {
    pub fn chainstate(&self) -> &ManagedChainstate<S>;
    pub fn mempool(&self) -> &ManagedMempool;
    pub fn peer_manager(&self) -> &PeerManager;
    pub fn submit_local_transaction(
        &mut self,
        transaction: Transaction,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<AdmissionResult, ManagedNetworkError>;
}
```

From `packages/open-bitcoin-node/src/wallet.rs`:
```rust
impl<S: WalletStore> ManagedWallet<S> {
    pub fn wallet(&self) -> &Wallet;
    pub fn import_descriptor(
        &mut self,
        label: impl Into<String>,
        role: DescriptorRole,
        descriptor_text: &str,
    ) -> Result<u32, WalletError>;
    pub fn rescan_chainstate(
        &mut self,
        snapshot: &ChainstateSnapshot,
    ) -> Result<(), WalletError>;
    pub fn build_and_sign(
        &self,
        request: &BuildRequest,
        coinbase_maturity: u32,
    ) -> Result<BuiltTransaction, WalletError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapter-owned RPC projection and command helpers before dispatcher work</name>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/open-bitcoin-wallet/src/wallet.rs
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-node/src/wallet.rs
  </files>
  <behavior>
    - Test 1: Managed network helpers expose the chainstate, mempool, and network-info projections needed for `getblockchaininfo`, `getmempoolinfo`, and `getnetworkinfo` without future RPC code reaching through raw `peer_manager()` or mempool internals.
    - Test 2: Managed wallet helpers expose balance, UTXO, build, sign, build-and-sign, import-descriptor, and rescan commands without future RPC code reaching through raw `.wallet()` internals.
    - Test 3: The helper surface stays honest about the single-wallet, local-operator Phase 8 slice and does not add multiwallet or keypool semantics.
  </behavior>
  <acceptance_criteria>
    - `ManagedPeerNetwork` grows narrow projection helpers for chain tip or snapshot, mempool totals, network metadata, and raw transaction submission that later handlers can call directly per D-01.
    - `ManagedWallet` grows narrow helpers for balance, UTXO inspection, deterministic build or sign flows, descriptor import, and rescan so later RPC or CLI code does not need `.wallet()` internals per D-01 and D-03.
    - The helper surface does not add `sendtoaddress`, external-signer, keypool, or multiwallet semantics per D-04 and the resolved research questions.
    - Tests prove the new seam is sufficient for the supported method set before any dispatcher or transport code is planned against it.
  </acceptance_criteria>
  <action>Per D-01, D-03, and the checker blocker, add an explicit adapter seam on top of the existing managed node and wallet facades before any dispatcher work begins. Extend `ManagedPeerNetwork` with the narrow projection or command helpers needed for `getblockchaininfo`, `getmempoolinfo`, `getnetworkinfo`, and `sendrawtransaction`, and extend `ManagedWallet` with the narrow projection or command helpers needed for `getwalletinfo`, `getbalances`, `listunspent`, `importdescriptors`, `rescanblockchain`, `buildtransaction`, and `buildandsigntransaction`. Future `open-bitcoin-rpc` code in Plans 03 and 04 must target these helpers rather than calling `.wallet()` or `.peer_manager()` from transport-layer code.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node network::tests::managed_network_exposes_rpc_projection_helpers -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node wallet::tests::managed_wallet_exposes_rpc_projection_and_build_helpers -- --exact</automated>
  </verify>
  <done>The Phase 8 server and CLI have an explicit shell-owned adapter seam to build against before any dispatcher or transport code is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the shared typed envelope, method, config, error, and runtime-context contracts</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/lib.rs
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/bitcoin-knots/src/rpc/request.cpp
    - packages/bitcoin-knots/src/bitcoin-cli.cpp
    - .planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-rpc/src/error.rs
    packages/open-bitcoin-rpc/src/envelope.rs
    packages/open-bitcoin-rpc/src/envelope/tests.rs
    packages/open-bitcoin-rpc/src/method.rs
    packages/open-bitcoin-rpc/src/method/tests.rs
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/tests.rs
  </files>
  <behavior>
    - Test 1: The method registry exposes exactly the supported Phase 8 HTTP methods `getblockchaininfo`, `getmempoolinfo`, `getnetworkinfo`, `sendrawtransaction`, `deriveaddresses`, `getwalletinfo`, `getbalances`, `listunspent`, `importdescriptors`, and `rescanblockchain`.
    - Test 2: The contract also exposes Open Bitcoin extension methods `buildtransaction` and `buildandsigntransaction`, and flags them as extensions instead of baseline parity methods.
    - Test 3: Runtime config defaults bind RPC to `127.0.0.1`, prefer cookie or explicit `rpcuser` plus `rpcpassword` auth only, and keep a single active wallet slice.
    - Test 4: `ManagedRpcContext` composes `ManagedPeerNetwork<MemoryChainstateStore>` and `ManagedWallet<MemoryWalletStore>` through the new adapter helpers rather than planning direct raw-inner-object access.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/method.rs` defines typed request or response structs and metadata for every supported Phase 8 method per D-02 and D-03.
    - The extension build or sign methods use machine-friendly integer fields such as `amount_sats` and `fee_rate_sat_per_kvb` so D-07 is satisfied without inventing `sendtoaddress` parity.
    - `packages/open-bitcoin-rpc/src/config.rs` defines `RuntimeConfig`, `RpcServerConfig`, `RpcClientConfig`, and `RpcAuthConfig`, and explicitly excludes `rpcauth`, `rpcwhitelist`, `rpcwallet`, and wallet-path routing per D-04 and the resolved research questions.
    - `packages/open-bitcoin-rpc/src/error.rs` centralizes JSON-RPC code mapping for parse errors, invalid requests, method not found, invalid params, wallet-required conditions, and auth failures.
    - `packages/open-bitcoin-rpc/src/context.rs` exposes one shell-owned runtime context built on the managed adapter seam so later handlers do not bypass `open-bitcoin-node`.
  </acceptance_criteria>
  <action>Per D-01 through D-04, turn `open-bitcoin-rpc` into the single typed contract layer for Phase 8. Add `envelope.rs` for JSON-RPC request or response types, `method.rs` for the supported method registry and exact typed request or result structs, `config.rs` for frozen runtime and auth config contracts, `context.rs` for a `ManagedRpcContext` that composes `ManagedPeerNetwork<MemoryChainstateStore>` plus `ManagedWallet<MemoryWalletStore>` over the new adapter seam, and `error.rs` for centralized RPC error mapping. The baseline-backed HTTP methods must be the exact list from Test 1, while deterministic wallet construction must surface through extension methods `buildtransaction` and `buildandsigntransaction` with integer satoshi fields instead of claiming `sendtoaddress` parity. Keep `/wallet/<name>`, `rpcauth`, whitelist rules, and full HD or multiwallet semantics out of the contract entirely.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::supported_http_methods_match_phase_8_surface -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::build_transaction_methods_are_marked_as_open_bitcoin_extensions -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::runtime_config_defaults_to_local_single_wallet_auth -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features context::tests::managed_rpc_context_composes_network_and_wallet_facades -- --exact</automated>
  </verify>
  <done>The Phase 8 interface surface is defined in one auditable adapter-backed contract layer that later server and CLI plans can implement without re-deciding scope or seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| managed adapter internals → Phase 8 RPC seam | Raw chainstate, mempool, peer-manager, and wallet objects must not leak directly into future transport code. |
| raw JSON-RPC or CLI intent → typed contracts | Untrusted operator input must be constrained by typed method, auth, and runtime-config models before server or CLI behavior is added. |
| `ManagedRpcContext` → later dispatch and transport code | Future server and CLI plans must consume the seam-backed context instead of bypassing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T | `packages/open-bitcoin-node/src/network.rs` | mitigate | Add narrow RPC projection or command helpers so later handlers do not couple themselves to raw peer-manager, mempool, or chainstate internals. |
| T-08-02-02 | T | `packages/open-bitcoin-node/src/wallet.rs` | mitigate | Add narrow wallet projection or command helpers so later handlers do not couple themselves to raw wallet internals or accidentally widen scope. |
| T-08-02-03 | E | `packages/open-bitcoin-rpc/src/config.rs` | mitigate | Restrict the initial auth contract to cookie or explicit `rpcuser` plus `rpcpassword`, bind locally by default, and omit `rpcauth`, whitelist scope, and wallet-path routing. |
| T-08-02-04 | T | `packages/open-bitcoin-rpc/src/method.rs` | mitigate | Define one typed method registry and exact request or response structs so later plans cannot smuggle free-form JSON through ad hoc handlers. |
</threat_model>

<verification>
- The targeted adapter-seam and contract tests named above pass exactly.
- `ManagedRpcContext` is planned against seam-backed helper methods before dispatcher work begins.
- Later Phase 8 plans can implement server and CLI behavior without revisiting method scope or adapter ownership.
</verification>

<success_criteria>
- `open-bitcoin-node` exposes a dedicated RPC-facing helper surface over the existing managed adapters.
- `open-bitcoin-rpc` owns one typed, auditable contract layer for methods, config, auth, error mapping, and runtime context.
- Dispatcher and transport work can proceed without reaching through raw adapter internals.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-02-SUMMARY.md`
</output>
