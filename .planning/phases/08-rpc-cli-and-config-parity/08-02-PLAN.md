---
phase: 08-rpc-cli-and-config-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - packages/open-bitcoin-rpc/BUILD.bazel
  - packages/open-bitcoin-rpc/src/lib.rs
  - packages/open-bitcoin-rpc/src/error.rs
  - packages/open-bitcoin-rpc/src/config.rs
  - packages/open-bitcoin-rpc/src/config/tests.rs
  - packages/open-bitcoin-rpc/src/context.rs
  - packages/open-bitcoin-rpc/src/context/tests.rs
  - packages/open-bitcoin-rpc/src/method.rs
  - packages/open-bitcoin-rpc/src/method/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/http.rs
  - packages/open-bitcoin-rpc/src/http/tests.rs
  - packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
autonomous: true
requirements:
  - RPC-01
must_haves:
  truths:
    - "Operators can POST authenticated JSON-RPC requests to a local server and get the correct `200`, `204`, `404`, or `500` semantics for the supported Phase 8 surface."
    - "Supported node, mempool, wallet, descriptor, and raw-transaction methods execute through managed shell facades with typed request and response mapping."
    - "Unsupported baseline methods and deferred surfaces fail explicitly instead of exposing fake parity."
  artifacts:
    - path: packages/open-bitcoin-rpc/src/config.rs
      provides: "Repo-owned Bitcoin config parsing and runtime loading"
      contains: "load_runtime_config"
    - path: packages/open-bitcoin-rpc/src/dispatch.rs
      provides: "Typed RPC dispatch over the managed runtime context"
      contains: "buildandsigntransaction"
    - path: packages/open-bitcoin-rpc/src/http.rs
      provides: "Authenticated POST-only JSON-RPC transport with batch and notification handling"
      contains: "StatusCode::NO_CONTENT"
    - path: packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
      provides: "Node/server entrypoint for the Phase 8 RPC shell"
      contains: "main"
  key_links:
    - from: packages/open-bitcoin-rpc/src/config.rs
      to: packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
      via: "`load_runtime_config()` freezes CLI and config-file precedence before the server starts."
      pattern: "load_runtime_config"
    - from: packages/open-bitcoin-rpc/src/http.rs
      to: packages/open-bitcoin-rpc/src/dispatch.rs
      via: "The HTTP route parses JSON-RPC envelopes once and forwards typed methods into the shared dispatcher."
      pattern: "dispatch"
    - from: packages/open-bitcoin-rpc/src/dispatch.rs
      to: packages/open-bitcoin-node/src/network.rs
      via: "Raw transaction submission and node info methods stay on the managed adapter seam."
      pattern: "ManagedPeerNetwork|submit_local_transaction"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-04-23T01-44-19
generated_at: 2026-04-23T02:12:24Z
---

<objective>
Implement the authenticated RPC server, repo-owned config loader, and the exact supported Phase 8 method handlers over the shared contract layer.

Purpose: Deliver D-01 through D-06 on the server side by turning the Phase 8 contracts into a running JSON-RPC surface with honest method coverage, local-operator auth, and Knots-compatible HTTP semantics for the supported slice.
Output: Working `open-bitcoind` transport and dispatcher, repo-owned config parsing with correct precedence, and focused RPC transport or handler regressions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
@.planning/phases/08-rpc-cli-and-config-parity/08-RESEARCH.md
@.planning/phases/08-rpc-cli-and-config-parity/08-01-PLAN.md
@packages/bitcoin-knots/src/common/config.cpp
@packages/bitcoin-knots/src/rpc/request.cpp
@packages/bitcoin-knots/src/rpc/server.cpp
@packages/bitcoin-knots/src/rpc/blockchain.cpp
@packages/bitcoin-knots/src/rpc/mempool.cpp
@packages/bitcoin-knots/src/rpc/net.cpp
@packages/bitcoin-knots/src/wallet/rpc/wallet.cpp
@packages/bitcoin-knots/src/wallet/rpc/transactions.cpp
@packages/bitcoin-knots/test/functional/interface_rpc.py
@packages/bitcoin-knots/test/functional/feature_config_args.py
@packages/open-bitcoin-node/src/network.rs
@packages/open-bitcoin-node/src/wallet.rs
@packages/open-bitcoin-mempool/src/pool.rs
@packages/open-bitcoin-network/src/message.rs
@packages/open-bitcoin-network/src/peer.rs

<interfaces>
Expected from Plan `08-01` in `packages/open-bitcoin-rpc/src/method.rs`:
```rust
pub enum RpcMethod {
    GetBlockchainInfo,
    GetMempoolInfo,
    GetNetworkInfo,
    SendRawTransaction(SendRawTransactionRequest),
    DeriveAddresses(DeriveAddressesRequest),
    GetWalletInfo,
    GetBalances,
    ListUnspent(ListUnspentRequest),
    ImportDescriptors(ImportDescriptorsRequest),
    RescanBlockchain(RescanBlockchainRequest),
    BuildTransaction(BuildTransactionRequest),
    BuildAndSignTransaction(BuildTransactionRequest),
}
```

Expected from Plan `08-01` in `packages/open-bitcoin-rpc/src/config.rs`:
```rust
pub struct RuntimeConfig {
    pub chain: AddressNetwork,
    pub maybe_data_dir: Option<PathBuf>,
    pub rpc_server: RpcServerConfig,
    pub rpc_client: RpcClientConfig,
    pub wallet: WalletRuntimeConfig,
}

pub enum RpcAuthConfig {
    Cookie { maybe_cookie_file: Option<PathBuf> },
    UserPassword { username: String, password: String },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the repo-owned config parser and local-operator auth resolution</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/config.rs
    - packages/open-bitcoin-rpc/src/config/tests.rs
    - packages/bitcoin-knots/src/common/config.cpp
    - packages/bitcoin-knots/test/functional/feature_config_args.py
    - .planning/phases/08-rpc-cli-and-config-parity/08-CONTEXT.md
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/config.rs
    packages/open-bitcoin-rpc/src/config/tests.rs
    packages/open-bitcoin-rpc/src/context.rs
    packages/open-bitcoin-rpc/src/context/tests.rs
  </files>
  <behavior>
    - Test 1: Config loading rejects `conf=` inside a config file or included config file with the upstream-style parse error.
    - Test 2: Config loading rejects `rpcpassword` values containing `#` with the upstream-style ambiguity error.
    - Test 3: A config file can relocate the effective datadir, but an explicit CLI `-datadir` still wins over config-file values.
    - Test 4: Auth resolution prefers cookie auth when `rpcpassword` is empty and falls back to explicit `rpcuser` plus `rpcpassword` only when both are provided.
  </behavior>
  <acceptance_criteria>
    - `load_runtime_config()` parses `bitcoin.conf`, `includeconf`, and chain sections for the supported slice per D-06.
    - The supported config keys are limited to the honest Phase 8 slice: `server`, `rpcbind`, `rpcport`, `rpcconnect`, `rpcuser`, `rpcpassword`, `rpccookiefile`, `conf`, `datadir`, and the selected chain.
    - The parser does not accept or silently ignore `rpcauth`, `rpcwhitelist`, `/wallet/<name>`, or other deferred multiwallet surfaces per D-04 and the yolo defaults.
    - `ManagedRpcContext` can be built from the frozen runtime config with one active wallet and one managed peer-network instance, keeping config and runtime concerns in shell code.
  </acceptance_criteria>
  <action>Per D-01, D-04, and D-06, turn `packages/open-bitcoin-rpc/src/config.rs` into the repo-owned parser and precedence engine for the supported Phase 8 slice. Port the important `feature_config_args.py` semantics directly: reject `conf=` inside config files, reject ambiguous `rpcpassword` lines containing `#`, support `includeconf`, honor chain-scoped sections only for the active chain, let config-file `datadir` relocate the effective data-root when no CLI `-datadir` is present, and let explicit CLI flags override config-file values. Resolve auth as cookie-file auth when `rpcpassword` is empty or unset, or as explicit `rpcuser` plus `rpcpassword` when both are supplied; do not implement `rpcauth`, method whitelists, or wallet-path routing in this phase.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::conf_cannot_be_set_in_configuration_files -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::rpcpassword_with_hash_is_rejected -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::cli_datadir_overrides_config_datadir -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features config::tests::auth_resolution_prefers_cookie_when_password_is_empty -- --exact</automated>
  </verify>
  <done>The Phase 8 server can load and freeze its runtime configuration honestly from CLI plus config files before any transport or handler logic runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the typed dispatcher and the exact supported method handlers</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/method.rs
    - packages/open-bitcoin-rpc/src/error.rs
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/wallet.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/bitcoin-knots/src/rpc/blockchain.cpp
    - packages/bitcoin-knots/src/rpc/mempool.cpp
    - packages/bitcoin-knots/src/rpc/net.cpp
    - packages/bitcoin-knots/src/wallet/rpc/wallet.cpp
    - packages/bitcoin-knots/src/wallet/rpc/transactions.cpp
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/error.rs
    packages/open-bitcoin-rpc/src/method.rs
    packages/open-bitcoin-rpc/src/method/tests.rs
    packages/open-bitcoin-rpc/src/dispatch.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
    packages/open-bitcoin-rpc/src/context.rs
  </files>
  <behavior>
    - Test 1: `getnetworkinfo`, `getblockchaininfo`, and `getmempoolinfo` return the documented Phase 8 field subsets from managed runtime state.
    - Test 2: `importdescriptors`, `rescanblockchain`, `getbalances`, and `listunspent` round-trip through the managed wallet using one active wallet slice.
    - Test 3: `sendrawtransaction` returns the txid on success and maps mempool rejection outcomes through centralized RPC error codes.
    - Test 4: `buildtransaction` and `buildandsigntransaction` return deterministic fee or hex results as Open Bitcoin extension methods without claiming `sendtoaddress` parity.
    - Test 5: Unsupported or deferred methods still return method-not-found, and ranged descriptor requests still fail explicitly instead of pretending HD support.
  </behavior>
  <acceptance_criteria>
    - All handlers operate through `ManagedRpcContext`, `ManagedPeerNetwork`, or `ManagedWallet` per D-01; no transport code reaches directly into pure-core wallet, chainstate, mempool, or network internals.
    - The HTTP-exposed baseline methods are exactly `getnetworkinfo`, `getblockchaininfo`, `getmempoolinfo`, `sendrawtransaction`, `deriveaddresses`, `getwalletinfo`, `getbalances`, `listunspent`, `importdescriptors`, and `rescanblockchain`.
    - The extension methods `buildtransaction` and `buildandsigntransaction` use integer satoshi request or response fields and are marked as Open Bitcoin extensions in method metadata per D-03 and D-09.
    - `deriveaddresses` requires a checksum and rejects ranged descriptors, `sendtoaddress`, `getpeerinfo`, mining RPCs, external signer RPCs, and multiwallet-only calls stay unsupported per D-04.
    - `getwalletinfo` and `getbalances` return only the field subset the current runtime can back honestly and do not fabricate keypool, scanning-progress, external-signer, or multiwallet fields.
  </acceptance_criteria>
  <action>Per D-02, D-03, and D-04, implement `packages/open-bitcoin-rpc/src/dispatch.rs` as the only handler registry over the managed runtime. The baseline-backed HTTP methods must be the exact list in the acceptance criteria. Shape the result payloads around the honest field subsets the current runtime can back now: `getnetworkinfo` should include version, subversion, protocolversion, localservices, localrelay, connection counts, relayfee, incrementalfee, and warnings; `getblockchaininfo` should include chain, blocks, headers, bestblockhash, mediantime, verificationprogress, initialblockdownload, and warnings; `getmempoolinfo` should include size, bytes, usage, total_fee_sats, maxmempool, mempoolminfee, minrelaytxfee, and loaded; `getbalances` and `listunspent` should project current managed-wallet state; `importdescriptors` should support the honest single-key descriptor subset with explicit errors for ranged or keypool fields; `rescanblockchain` should run synchronously against the managed chainstate snapshot and return start or stop heights; `buildtransaction` and `buildandsigntransaction` should call the existing deterministic wallet builder and signer with integer satoshi fields so the send surface stays honest without widening to `sendtoaddress` parity. Centralize error mapping in `error.rs`, including method-not-found, invalid params, parse errors, and mempool rejection mapping.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::node_info_methods_return_documented_phase_8_fields -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::wallet_descriptor_and_rescan_methods_update_wallet_views -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::sendrawtransaction_returns_txid_and_maps_rejections -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features dispatch::tests::buildandsigntransaction_returns_deterministic_hex_and_fee -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features method::tests::ranged_descriptors_and_deferred_methods_fail_explicitly -- --exact</automated>
  </verify>
  <done>The Phase 8 server owns one honest, typed method surface over the existing managed adapters, including the documented Open Bitcoin build or sign extension methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the authenticated axum transport and `open-bitcoind` entrypoint</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/envelope.rs
    - packages/open-bitcoin-rpc/src/error.rs
    - packages/open-bitcoin-rpc/src/dispatch.rs
    - packages/open-bitcoin-rpc/src/config.rs
    - packages/bitcoin-knots/src/rpc/request.cpp
    - packages/bitcoin-knots/src/rpc/server.cpp
    - packages/bitcoin-knots/test/functional/interface_rpc.py
  </read_first>
  <files>
    packages/open-bitcoin-rpc/BUILD.bazel
    packages/open-bitcoin-rpc/src/lib.rs
    packages/open-bitcoin-rpc/src/http.rs
    packages/open-bitcoin-rpc/src/http/tests.rs
    packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs
  </files>
  <behavior>
    - Test 1: JSON-RPC 2.0 request errors still return HTTP `200` with an error object, while legacy 1.0 or 1.1 errors map to HTTP `404` or `500`.
    - Test 2: JSON-RPC 2.0 notifications return HTTP `204 No Content` and still execute valid commands.
    - Test 3: Mixed-version batch requests are accepted for compatibility, and all-notification batches return no body.
    - Test 4: The transport is POST-only and rejects unauthenticated requests unless the runtime config explicitly resolved local credentials.
  </behavior>
  <acceptance_criteria>
    - `packages/open-bitcoin-rpc/src/http.rs` owns envelope parsing, notification detection, batch execution, and HTTP-status mapping in one place per the research pitfall warning.
    - The server uses HTTP Basic auth backed by either cookie-file or explicit user or password credentials, and defaults to binding locally per D-06 and the security guidance.
    - `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` loads the runtime config once, constructs the managed runtime context once, and starts the axum router without leaking async concerns into the pure-core crates.
    - The transport does not expose `/wallet/<name>`, `rpcauth`, `rpcwhitelist`, or GET-based RPC handling in this phase.
  </acceptance_criteria>
  <action>Per D-01, D-02, D-06, and the Phase 8 security guidance, add an `axum` transport in `packages/open-bitcoin-rpc/src/http.rs` that is POST-only, authenticates with HTTP Basic auth using the resolved cookie or explicit credentials, parses single or batch JSON-RPC envelopes once, executes them through the shared dispatcher, and mirrors the `interface_rpc.py` semantics for legacy versus 2.0 HTTP status codes and notifications. Add a thin `packages/open-bitcoin-rpc/src/bin/open-bitcoind.rs` entrypoint that loads config exactly once through `load_runtime_config()`, constructs `ManagedRpcContext`, and starts the router on the configured local bind address and port. Keep the binary thin; do not move handler logic, config precedence, or auth parsing into the binary.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::legacy_and_json_rpc_v2_status_mapping_matches_phase_8_contract -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::json_rpc_v2_notifications_return_no_content_and_execute -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::mixed_version_batches_are_accepted -- --exact
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features http::tests::post_only_transport_rejects_unauthenticated_requests -- --exact
cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --bin open-bitcoind</automated>
  </verify>
  <done>A local operator can start a real authenticated JSON-RPC server for the supported Phase 8 surface, and the tricky HTTP semantics are pinned by hermetic tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config files and CLI flags → `RuntimeConfig` | Untrusted local operator input crosses into process binding, auth, datadir, and chain selection. |
| HTTP client → RPC transport | Untrusted network requests cross into JSON-RPC parsing, auth checks, and handler dispatch. |
| RPC handler input → managed shell facades | Untrusted RPC params cross into node, mempool, and wallet mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | E | `packages/open-bitcoin-rpc/src/http.rs` auth gate | mitigate | Require POST plus Basic auth, bind locally by default, and limit the auth model to cookie or explicit user or password credentials for the supported single-operator slice. |
| T-08-02-02 | D | `packages/open-bitcoin-rpc/src/http.rs` envelope parser | mitigate | Centralize batch parsing, notification detection, and HTTP-status mapping in one transport module with direct `interface_rpc.py`-style tests. |
| T-08-02-03 | T | `packages/open-bitcoin-rpc/src/config.rs` | mitigate | Canonicalize `conf`, `datadir`, and `includeconf` handling in one parser, reject `conf=` inside config files, and keep unsupported auth or wallet-path features out of scope. |
| T-08-02-04 | T | `packages/open-bitcoin-rpc/src/dispatch.rs` | mitigate | Normalize params through typed request structs only once, reject unsupported ranges and deferred methods explicitly, and route all mutations through `ManagedRpcContext`. |
</threat_model>

<verification>
- The targeted config, dispatch, and HTTP tests named above pass exactly.
- `cargo build --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-features --bin open-bitcoind` succeeds.
- `bash scripts/verify.sh` is expected to pass after the final tree is in place.
</verification>

<success_criteria>
- `open-bitcoind` starts from typed runtime config and exposes POST-only authenticated JSON-RPC for the supported slice.
- The supported method set is implemented through managed shell facades and returns honest field subsets and error mappings.
- Notification, batch, and legacy-versus-2.0 HTTP semantics are pinned by hermetic tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rpc-cli-and-config-parity/08-02-SUMMARY.md`
</output>
