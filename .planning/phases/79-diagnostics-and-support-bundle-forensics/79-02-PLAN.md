---
phase: 79-diagnostics-and-support-bundle-forensics
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 79-2026-06-17T13-53-04
generated_at: 2026-06-17T15:15:24Z
must_haves:
  truths:
    - "Support bundle Markdown renders the same support_forensics verdict, timeline, checkpoint-chain, and source facts as JSON."
    - "Renderers format typed diagnostics without reclassifying causes, verdicts, resource states, recovery actions, or stalled subsystems."
    - "Rust fixtures prove redaction, timeline ordering, size/projection facts, and cross-surface consistency through deterministic inputs."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      provides: "Markdown sections for Forensic Timeline, Checkpoint Chain, and Failure Narrative"
      contains: "## Forensic Timeline"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "Phase 79 Markdown, redaction, and cross-surface agreement coverage"
      contains: "phase79_support_forensics_markdown"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/support/render.rs"
      to: "SupportEvidenceBundle.support_forensics"
      via: "push_support_forensics renders typed sidecar fields"
      pattern: "bundle.support_forensics"
    - from: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      to: "packages/open-bitcoin-node/src/status.rs"
      via: "fixtures seed shared resource/recovery/progress status fields and assert bundle projection agrees"
      pattern: "resource_bound_evidence|recovery_evidence|progress_guarantee"
---

<objective>
Render Phase 79 support forensics in Markdown and prove JSON/Markdown consistency with deterministic Rust fixtures.

Purpose: Operators need a concise human-readable "what happened" story, while DIAG-02 and DIAG-04 require the rendered support bundle to stay aligned with the typed diagnostic contract and redaction boundaries.
Output: Markdown sections and focused Rust tests for forensic timeline, checkpoint chain, failure narrative, source/redaction facts, and cross-surface consistency.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/79-diagnostics-and-support-bundle-forensics/79-CONTEXT.md
@.planning/phases/79-diagnostics-and-support-bundle-forensics/79-RESEARCH.md
@.planning/phases/79-diagnostics-and-support-bundle-forensics/79-01-SUMMARY.md
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/forensics.rs
@packages/open-bitcoin-cli/src/operator/support/tests.rs

<interfaces>
From Plan 79-01 output:
```rust
struct SupportEvidenceBundle {
    // existing fields...
    soak_evidence: SoakSupportEvidence,
    support_forensics: SupportForensicsEvidence,
    resource_bound_evidence: ResourceBoundSupportEvidence,
}

struct SupportForensicsEvidence {
    pub(super) state: EvidenceState,
    pub(super) timeline: Vec<ForensicTimelineEntry>,
    pub(super) checkpoint_chain: CheckpointChainEvidence,
    pub(super) narrative: ForensicNarrative,
    pub(super) source: ForensicSourceEvidence,
    pub(super) redaction: ForensicRedactionEvidence,
    pub(super) maybe_unavailable_reason: Option<String>,
}
```

From `packages/open-bitcoin-cli/src/operator/support/render.rs`:
```rust
pub(super) fn render_support_markdown(bundle: &SupportEvidenceBundle) -> String {
    // existing sections:
    // ## Redaction, ## Config, ## Status Snapshot, ## Recovery Evidence,
    // ## Resource Bound Evidence, ## Store Health, ## Full Sync Evidence,
    // ## Soak Evidence, ## Live Smoke
}
```
</interfaces>
</context>

<must_haves>
- Markdown contains exact headings `## Forensic Timeline`, `## Checkpoint Chain`, and `## Failure Narrative`.
- Markdown renders `Verdict`, `Likely cause`, `Evidence basis`, `Next action`, and `Confidence` from `bundle.support_forensics.narrative`.
- Markdown renders `Algorithm`, `Event count`, `First sequence`, `Latest sequence`, `Latest hash`, `Ordered`, `Missing sequence count`, and `Truncated` from `bundle.support_forensics.checkpoint_chain`.
- Tests prove support output does not include seeded sensitive material while still exposing bounded redaction and source/projection facts.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render typed support-forensics Markdown sections</name>
  <files>packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/src/operator/support.rs`
    - `packages/open-bitcoin-cli/src/operator/support/forensics.rs`
    - `packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `.planning/phases/79-diagnostics-and-support-bundle-forensics/79-01-SUMMARY.md`
  </read_first>
  <behavior>
    - RED: Add failing test `phase79_support_forensics_markdown_renders_timeline_chain_and_failure_narrative`.
    - Markdown must contain exact headings `## Forensic Timeline`, `## Checkpoint Chain`, and `## Failure Narrative`.
    - Markdown must include `Verdict:`, `Likely cause:`, `Evidence basis:`, `Next action:`, `Confidence:`, `Algorithm: sha256-json-v1`, `Missing sequence count:`, and `Truncated:`.
  </behavior>
  <action>
    Update `packages/open-bitcoin-cli/src/operator/support/render.rs` per D-07, D-10, and D-12. Add a private `push_support_forensics(output: &mut String, evidence: &super::forensics::SupportForensicsEvidence)` helper or equivalent module-local helper. Call it from `render_support_markdown` immediately after `## Soak Evidence` and before `## Live Smoke`. Read the Plan 79-01 implementation first and use the `pub(super)` field contract for `SupportForensicsEvidence`, `ForensicTimelineEntry`, `CheckpointChainEvidence`, `ForensicNarrative`, `ForensicSourceEvidence`, and `ForensicRedactionEvidence`; do not bypass module privacy with serialization round-trips or string parsing.

    The renderer must format only typed sidecar fields. It must not inspect raw ledger JSON, raw report Markdown, raw daemon logs, raw live-smoke input, raw options, endpoint tables, or freeform renderer text. It must not map strings back into `ForensicVerdict`, `ForensicConfidence`, recovery categories, resource states, or stalled subsystems. Missing values must render as `unavailable`.

    For timeline entries, render one bounded line per entry in this exact shape:
    `- Sequence {sequence}: kind={kind} recorded_at={recorded_at_unix_seconds} summary={summary} basis={basis_csv} next_action={next_action_or_unavailable}`.
    Do not render more than the collected typed entries; do not append raw logs, tails, or tables.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase79_support_forensics_markdown_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "## Forensic Timeline|## Checkpoint Chain|## Failure Narrative|push_support_forensics" packages/open-bitcoin-cli/src/operator/support/render.rs` finds all rendering anchors.
    - `rg -n "support_forensics\\.narrative|support_forensics\\.checkpoint_chain|support_forensics\\.timeline|pub\\(super\\)" packages/open-bitcoin-cli/src/operator/support/render.rs packages/open-bitcoin-cli/src/operator/support/forensics.rs` confirms Plan 02 uses the Plan 01 field-visibility contract.
    - `rg -n "phase79_support_forensics_markdown_renders_timeline_chain_and_failure_narrative|Algorithm: sha256-json-v1|Likely cause:|Next action:|Confidence:" packages/open-bitcoin-cli/src/operator/support/tests.rs` finds the Markdown assertions.
    - `rg -n "raw ledger|raw report|raw daemon|raw live-smoke|endpoint table|rpcpassword|rpcauth" packages/open-bitcoin-cli/src/operator/support/render.rs` returns no matches except existing redaction headings or comments that explicitly exclude those inputs.
  </acceptance_criteria>
  <done>Support Markdown exposes the Phase 79 story from typed sidecar data without renderer-local classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove redaction and cross-surface diagnostic consistency</name>
  <files>packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-cli/src/operator/support/tests.rs`
    - `packages/open-bitcoin-cli/src/operator/support/forensics.rs`
    - `packages/open-bitcoin-cli/src/operator/support/render.rs`
    - `packages/open-bitcoin-cli/src/operator/support/resource_bounds.rs`
    - `packages/open-bitcoin-cli/src/operator/support/progress_guarantee.rs`
    - `packages/open-bitcoin-cli/src/operator/support/evidence.rs`
    - `packages/open-bitcoin-node/src/status.rs`
    - `packages/open-bitcoin-node/src/status/progress_guarantee.rs`
    - `packages/open-bitcoin-node/src/status/resource_bounds.rs`
    - `packages/open-bitcoin-node/src/status/recovery.rs`
  </read_first>
  <behavior>
    - RED: Add failing tests named `phase79_support_forensics_cross_surface_agreement_uses_shared_status_contract` and `phase79_support_forensics_markdown_and_json_exclude_forbidden_sensitive_material`.
    - Cross-surface test must seed shared `OpenBitcoinStatusSnapshot` recovery, resource-bound, progress-credit, last-peer-contribution, and stall-diagnosis fields, then assert JSON and Markdown support forensics use the same labels instead of renderer-derived prose.
    - Redaction test must seed forbidden material in ignored trailing bytes, raw report-like strings, raw live-smoke-like strings, and credential-looking values, then assert JSON and Markdown exclude those exact literals.
  </behavior>
  <action>
    Extend `packages/open-bitcoin-cli/src/operator/support/tests.rs` per D-05, D-07, D-08, D-14, and D-15.

    The cross-surface fixture must prove these exact anchors:
    - `support_forensics.narrative.evidence_basis` includes typed labels that already exist in the shared status or soak checkpoint fields, such as `recovery=`, `resource_bound=`, `stall=`, `progress_credit=`, or `last_peer_contribution=`.
    - `support_forensics.source.event_count` equals the number of complete typed ledger events read.
    - `support_forensics.redaction.omitted` contains the existing support redaction topics, including `RPC cookie contents`, `RPC password and RPC auth values`, `wallet private material and raw wallet files`, and `raw unbounded log contents`.
    - `resource_bound_evidence.maybe_projected_bundle_size_bytes` remains the support-bundle size/projection source; do not duplicate support-bundle byte accounting in the forensics sidecar.

    The forbidden-sensitive-material test must include these exact seeded strings in test fixtures only: `rpcpassword=phase79-secret`, `rpcauth=phase79-secret`, `phase79 wallet seed phrase`, `raw daemon stdout phase79-secret`, `raw daemon stderr phase79-secret`, `raw live-smoke input phase79-secret`, `raw options phase79-secret`, and `endpoint table phase79-secret`. Assert none of these appear in serialized `support-evidence.json` or rendered Markdown.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase79_support_forensics_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase79_support_forensics_cross_surface_agreement_uses_shared_status_contract|phase79_support_forensics_markdown_and_json_exclude_forbidden_sensitive_material" packages/open-bitcoin-cli/src/operator/support/tests.rs` finds both tests.
    - `rg -n "maybe_projected_bundle_size_bytes|resource_bound_evidence|recovery_evidence|stall_diagnosis|last_peer_contribution|progress_credit" packages/open-bitcoin-cli/src/operator/support/tests.rs` confirms cross-surface assertions.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase79_support_forensics_ --all-features` passes.
  </acceptance_criteria>
  <done>Deterministic Rust fixtures prove Phase 79 support-forensics redaction and shared-contract consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed support_forensics JSON -> Markdown renderer | Renderer could accidentally reclassify evidence or expose fields not intended for human output. |
| test fixtures with seeded secrets -> output assertions | Negative fixtures intentionally contain sensitive-looking strings that must not leak into support artifacts. |
| shared status contract -> support narrative | Cross-surface labels can drift if support rendering invents its own wording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | I | `render_support_markdown` | mitigate | Render only typed `support_forensics` fields; add negative tests for `rpcpassword`, `rpcauth`, wallet material, raw daemon stdout/stderr, raw live-smoke input, raw options, and endpoint tables. |
| T-79-02-02 | R | `push_support_forensics` | mitigate | Renderer must not derive verdicts or likely causes; tests assert narrative fields match JSON sidecar fields. |
| T-79-02-03 | I | source and path rendering | mitigate | Render only source ledger/report/status paths already present in typed sidecar; do not render cookie contents, RPC credentials, raw options, or endpoint tables. |
| T-79-02-04 | D | default verification | accept | Plan 02 verification is local Rust fixture execution only; no public-network, service-manager, or multi-day commands. |
</threat_model>

<verification>
- `cargo fmt --manifest-path packages/Cargo.toml --all`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase79_support_forensics_ --all-features`
</verification>

<success_criteria>
- Markdown and JSON expose the same typed Phase 79 verdict, timeline, checkpoint-chain, source, redaction, and unavailable-evidence facts.
- Renderer code contains no root-cause classification logic and no raw support input parsing.
- Deterministic Rust tests cover redaction, timeline ordering/truncation, narrative fields, support-bundle size/projection facts, and cross-surface consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/79-diagnostics-and-support-bundle-forensics/79-02-SUMMARY.md`.
</output>
