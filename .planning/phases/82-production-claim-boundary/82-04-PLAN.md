---
phase: 82-production-claim-boundary
plan: 04
type: execute
wave: 3
depends_on: ["82-01", "82-02", "82-03"]
files_modified:
  - scripts/check-phase82-production-claim-boundary.ts
  - scripts/check-phase82-production-claim-boundary.test.ts
  - scripts/verify.sh
  - docs/metrics/lines-of-code.md
  - .planning/phases/82-production-claim-boundary/82-VERIFICATION.md
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04]
user_setup: []
must_haves:
  truths:
    - "Contributor can run a narrow deterministic Phase 82 traceability checker without public-network, service-manager, multi-day, or live-sync work."
    - "Default verification runs the Phase 82 checker after the existing Phase 80 boundary checker."
    - "Phase 82 verification evidence names PROD-01 through PROD-04 and records the final `bash scripts/verify.sh` result."
  artifacts:
    - path: "scripts/check-phase82-production-claim-boundary.ts"
      provides: "Narrow Bun checker for Phase 82 docs/parity anchors"
      exports: ["checkPhase82ProductionClaimBoundary"]
    - path: "scripts/check-phase82-production-claim-boundary.test.ts"
      provides: "Fixture tests proving checker failures for missing terms, omitted deferred surfaces, missing parity roots, and exact overclaim strings"
      contains: "OPEN_BITCOIN_PHASE82_REPO_ROOT"
    - path: "scripts/verify.sh"
      provides: "Default verifier wiring for the Phase 82 test and checker"
      contains: "check Phase 82 production claim boundary"
    - path: ".planning/phases/82-production-claim-boundary/82-VERIFICATION.md"
      provides: "Phase 82 closeout verification report"
      contains: "PROD-01"
  key_links:
    - from: "scripts/check-phase82-production-claim-boundary.ts"
      to: "docs/parity/production-claim-boundary.md"
      via: "targeted required anchor check"
      pattern: "production-claim-boundary\\.md"
    - from: "scripts/verify.sh"
      to: "scripts/check-phase82-production-claim-boundary.ts"
      via: "run_step after Phase 80"
      pattern: "check-phase82-production-claim-boundary\\.ts"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 82-2026-06-21T12-38-13
generated_at: 2026-06-21T12:58:11Z
---

<objective>
Add narrow deterministic Phase 82 traceability and closeout verification.

Purpose: The docs and parity metadata need automated proof that the Phase 82 boundary remains internally consistent. This is not the Phase 88 broad production-claim scanner.
Output: A focused Bun checker/test pair, verifier wiring, refreshed LOC metrics if verification regenerates them, and `82-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/82-production-claim-boundary/82-CONTEXT.md
@.planning/phases/82-production-claim-boundary/82-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-01-SUMMARY.md
@.planning/phases/82-production-claim-boundary/82-02-SUMMARY.md
@.planning/phases/82-production-claim-boundary/82-03-SUMMARY.md
@scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts
@scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts
@scripts/verify.sh
@docs/parity/production-claim-boundary.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/release-readiness.md
@README.md
@docs/operator/runtime-guide.md

<interfaces>
Use the Phase 80 checker pattern:
```typescript
export function checkPhase80OptInSoakUatReleaseBoundaries(
  maybeRepoRoot = process.env[REPO_ROOT_OVERRIDE_ENV],
): string[] {
  const repoRoot =
    maybeRepoRoot === undefined ? DEFAULT_REPO_ROOT : path.resolve(maybeRepoRoot);
  const failures: string[] = [];
  return failures;
}
```

Required Phase 82 checker constants:
```typescript
const REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE82_REPO_ROOT";
const SURFACE_ID = "v1-8-production-claim-boundary";
const PHASE82_REQUIREMENTS = ["PROD-01", "PROD-02", "PROD-03", "PROD-04"] as const;
const SUPPORT_TERMS = ["supported", "preview", "opt-in UAT", "unsupported", "deferred"] as const;
```

Targeted claim files only:
```text
README.md
docs/operator/runtime-guide.md
docs/parity/production-claim-boundary.md
docs/parity/release-readiness.md
docs/parity/checklist.md
docs/parity/README.md
docs/parity/deviations-and-unknowns.md
docs/parity/catalog/operator-runtime-release-hardening.md
docs/parity/catalog/p2p.md
docs/parity/catalog/chainstate.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Add fixture tests for the Phase 82 checker</name>
  <files>scripts/check-phase82-production-claim-boundary.test.ts</files>
  <read_first>
    - scripts/check-phase80-opt-in-soak-uat-release-boundaries.test.ts
    - scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts
    - docs/parity/production-claim-boundary.md
    - docs/parity/index.json
    - scripts/verify.sh
  </read_first>
  <behavior>
    - Test 1: complete fixture with all support terms, matrix columns, D-09 deferred surfaces, parity roots, and verify wiring returns no failures.
    - Test 2: fixture missing `opt-in UAT` or containing `production-grade` fails with a support vocabulary message.
    - Test 3: fixture missing `automatic support-bundle upload`, `destructive repair`, or `broad production-node readiness` fails with a deferred inventory message.
    - Test 4: fixture missing `PROD-04` or `docs/parity/production-claim-boundary.md` from `docs/parity/index.json` fails with a parity root message.
    - Test 5: fixture containing exact overclaim text `Open Bitcoin is production full-node ready.` or `v1.8 proves production full-node readiness.` in a targeted claim file fails.
    - Test 6: fixture missing Phase 82 commands from `scripts/verify.sh`, or placing them before the Phase 80 commands, fails with a verifier-order message.
  </behavior>
  <action>Create `scripts/check-phase82-production-claim-boundary.test.ts` using Bun test style from Phase 80, including `// Arrange`, `// Act`, and `// Assert` comments. Build fixture roots with the target files listed in `<interfaces>`, not the whole repository. Import `checkPhase82ProductionClaimBoundary` from `./check-phase82-production-claim-boundary`. Use `maybe...` names for nullable fixture options per the TypeScript standard. The initial run should fail because `scripts/check-phase82-production-claim-boundary.ts` does not exist or is not implemented yet.</action>
  <acceptance_criteria>
    - Test file contains `OPEN_BITCOIN_PHASE82_REPO_ROOT`, `v1-8-production-claim-boundary`, and `PHASE82_REQUIREMENTS`.
    - Test file includes the exact strings `Open Bitcoin is production full-node ready.` and `v1.8 proves production full-node readiness.` as negative fixture inputs only.
    - Test file includes `automatic support-bundle upload`, `destructive repair`, and `broad production-node readiness`.
    - Test file uses `// Arrange`, `// Act`, and `// Assert` comments in non-trivial tests.
    - `bun test scripts/check-phase82-production-claim-boundary.test.ts` fails before implementation for the expected missing checker/import or failing assertions.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase82-production-claim-boundary.test.ts</automated>
  </verify>
  <done>RED tests describe the narrow Phase 82 checker behavior and fail before the checker implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement the narrow Phase 82 checker</name>
  <files>scripts/check-phase82-production-claim-boundary.ts, scripts/check-phase82-production-claim-boundary.test.ts</files>
  <read_first>
    - scripts/check-phase82-production-claim-boundary.test.ts
    - scripts/check-phase80-opt-in-soak-uat-release-boundaries.ts
    - docs/parity/production-claim-boundary.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/deviations-and-unknowns.md
  </read_first>
  <behavior>
    - The exported function returns an array of failure lines and performs no process exit itself.
    - The CLI wrapper prints failures to stderr and sets `process.exitCode = 1`; success prints `validated Phase 82 production claim boundary`.
    - The checker parses `docs/parity/index.json` with `JSON.parse`, validates exact Phase 82 ids/evidence, and checks only targeted claim files.
    - The checker rejects near-synonyms `production-grade`, `production-ish`, `beta-supported`, and `ready enough`.
    - The checker rejects exact Phase 82 overclaim sentences, not every generic use of `production`.
  </behavior>
  <action>Implement `scripts/check-phase82-production-claim-boundary.ts` with the Bun/TypeScript pattern from Phase 80. Constants must include `REPO_ROOT_OVERRIDE_ENV = "OPEN_BITCOIN_PHASE82_REPO_ROOT"`, `SURFACE_ID = "v1-8-production-claim-boundary"`, `PHASE82_REQUIREMENTS = ["PROD-01", "PROD-02", "PROD-03", "PROD-04"] as const`, `SUPPORT_TERMS = ["supported", "preview", "opt-in UAT", "unsupported", "deferred"] as const`, matrix columns `Statement`, `Support term`, `Current status`, `Evidence sources`, `Verification command`, `UAT status`, `Residual risk`, `Next required gate`, D-09 deferred surfaces, and targeted claim files from `<interfaces>`. Validate the canonical doc, release-readiness section, deviations register, parity index, checklist, parity README, README/runtime-guide/catalog links, absence of near-synonyms, absence of exact overclaim sentences, absence of new v1.8 evidence manifest files, and Phase 82 checker order in `scripts/verify.sh`. Do not scan every Markdown file for generic `production`, `ready`, or `supported`; that broad guardrail belongs to Phase 88 per D-08. Keep the checker deterministic, public-network-free, service-manager-free, timing-stable, and short-running per D-17.</action>
  <acceptance_criteria>
    - `scripts/check-phase82-production-claim-boundary.ts` exports `checkPhase82ProductionClaimBoundary`.
    - The file contains `OPEN_BITCOIN_PHASE82_REPO_ROOT`, `v1-8-production-claim-boundary`, and all four PROD requirements.
    - The file contains D-09 surfaces including `automatic support-bundle upload`, `destructive repair`, and `broad production-node readiness`.
    - The file contains targeted claim files from `<interfaces>` and does not glob all Markdown files.
    - The file contains `production-grade`, `production-ish`, `beta-supported`, and `ready enough` only as forbidden near-synonym checks.
    - `bun test`, `bun --check`, and `bun run` commands for the Phase 82 checker all pass.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase82-production-claim-boundary.test.ts</automated>
    <automated>bun --check scripts/check-phase82-production-claim-boundary.ts</automated>
    <automated>bun run scripts/check-phase82-production-claim-boundary.ts</automated>
  </verify>
  <done>The narrow checker passes its fixture tests and real-worktree validation without duplicating Phase 88 broad claim scanning.</done>
</task>

<task type="auto">
  <name>Task 3: Wire verification and record Phase 82 closeout evidence</name>
  <files>scripts/verify.sh, docs/metrics/lines-of-code.md, .planning/phases/82-production-claim-boundary/82-VERIFICATION.md</files>
  <read_first>
    - scripts/verify.sh
    - scripts/check-phase82-production-claim-boundary.ts
    - scripts/check-phase82-production-claim-boundary.test.ts
    - docs/metrics/lines-of-code.md
    - .planning/phases/82-production-claim-boundary/82-CONTEXT.md
    - .planning/phases/82-production-claim-boundary/82-RESEARCH.md
  </read_first>
  <action>Wire `scripts/verify.sh` to run `bun test scripts/check-phase82-production-claim-boundary.test.ts` and `bun run scripts/check-phase82-production-claim-boundary.ts` immediately after the existing Phase 80 checker/test commands. Do not add public-network, service-manager, multi-day wall-clock, current-tip timing, live sync, large disk allocation, signed packaging, support upload, or destructive repair commands. Run focused checker commands, then run `bash scripts/verify.sh` per D-16. If verification refreshes `docs/metrics/lines-of-code.md`, keep that tracked generated artifact. Create `.planning/phases/82-production-claim-boundary/82-VERIFICATION.md` with frontmatter `phase: 82-production-claim-boundary`, `status: passed`, `requirements: [PROD-01, PROD-02, PROD-03, PROD-04]`, and a coverage table mapping each PROD requirement to docs/parity artifacts and checker evidence. Include the final `bash scripts/verify.sh` result and residual risks: Phase 82 defines gates only; production full-node readiness, inbound serving, relay, production-funds wallet use/safety, migration apply mode, signed packaging, Windows service integration, hosted dashboards, GUI parity, public-network CI/default checks, release-blocking live sync, automatic support-bundle upload, destructive repair, and broad production-node readiness remain deferred.</action>
  <acceptance_criteria>
    - `scripts/verify.sh` contains `bun test scripts/check-phase82-production-claim-boundary.test.ts`.
    - `scripts/verify.sh` contains `bun run scripts/check-phase82-production-claim-boundary.ts`.
    - The Phase 82 verifier commands appear after `check-phase80-opt-in-soak-uat-release-boundaries.ts`.
    - `scripts/verify.sh` does not add `run-live-mainnet-smoke`, `systemctl`, `launchctl`, `sleep 259200`, `release-blocking live sync`, `automatic support-bundle upload`, or destructive repair commands for Phase 82.
    - `.planning/phases/82-production-claim-boundary/82-VERIFICATION.md` contains `status: passed`, `PROD-01`, `PROD-02`, `PROD-03`, `PROD-04`, `bash scripts/verify.sh`, and `v1-8-production-claim-boundary`.
    - Final `bash scripts/verify.sh` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun test scripts/check-phase82-production-claim-boundary.test.ts</automated>
    <automated>bun run scripts/check-phase82-production-claim-boundary.ts</automated>
    <automated>bash scripts/verify.sh</automated>
    <automated>rg -n "status: passed|PROD-01|PROD-02|PROD-03|PROD-04|bash scripts/verify\\.sh|v1-8-production-claim-boundary" .planning/phases/82-production-claim-boundary/82-VERIFICATION.md</automated>
  </verify>
  <done>Default verification runs the narrow Phase 82 checker, full repo verification passes, and Phase 82 verification evidence maps PROD-01 through PROD-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker -> repository docs | The checker interprets docs and JSON metadata as release-control evidence. |
| default verifier -> contributors | Contributors may treat `bash scripts/verify.sh` as proof of Phase 82 consistency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04-01 | Denial of Service | `scripts/check-phase82-production-claim-boundary.ts` | mitigate | Keep checker targeted to Phase 82 files and short-running; no public-network, service-manager, or wall-clock gates. |
| T-82-04-02 | Repudiation | checker scope | mitigate | Validate exact support terms, matrix columns, PROD requirements, D-09 surfaces, and parity root ids. |
| T-82-04-03 | Spoofing | exact overclaim checks | mitigate | Reject exact Phase 82 overclaim sentences while leaving broad all-doc scanning to Phase 88. |
| T-82-04-04 | Information Disclosure | support-upload wording | mitigate | Checker and verification report keep automatic support-bundle upload deferred and do not add upload commands. |
| T-82-04-05 | Tampering | `scripts/verify.sh` ordering | mitigate | Fixture tests and checker verify Phase 82 commands run after Phase 80 and do not introduce forbidden default-verification operations. |
</threat_model>

<verification>
Run `bun test scripts/check-phase82-production-claim-boundary.test.ts`, `bun --check scripts/check-phase82-production-claim-boundary.ts`, `bun run scripts/check-phase82-production-claim-boundary.ts`, and final `bash scripts/verify.sh`.
</verification>

<success_criteria>
Phase 82 has deterministic traceability for the canonical boundary, parity roots, entrypoint links, deferred surfaces, verifier wiring, and passed full repo verification without implementing Phase 88's broad production-claim scanner.
</success_criteria>

<output>
After completion, create `.planning/phases/82-production-claim-boundary/82-04-SUMMARY.md`.
</output>
