---
phase: 82-production-claim-boundary
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - docs/parity/deviations-and-unknowns.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/README.md
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04]
user_setup: []
must_haves:
  truths:
    - "Release reviewer can find the v1.8 production claim boundary from machine-readable and human-readable parity roots."
    - "Contributor can inspect one deferred-surface register that preserves every PROD-04 surface and its future gate."
    - "Parity README points to v1.8 as the current boundary-setting root while preserving v1.7 as historical evidence."
  artifacts:
    - path: "docs/parity/deviations-and-unknowns.md"
      provides: "Durable v1.8 deferred-surface register"
      contains: "v1.8 Production Claim Boundary"
    - path: "docs/parity/index.json"
      provides: "Machine-readable `v1-8-production-claim-boundary` surface and `v1_8_production_claim_boundary` audit root"
      contains: "v1-8-production-claim-boundary"
    - path: "docs/parity/checklist.md"
      provides: "Human-readable checklist row for PROD-01 through PROD-04"
      contains: "v1-8-production-claim-boundary"
    - path: "docs/parity/README.md"
      provides: "Parity entrypoint pointer to current v1.8 boundary"
      contains: "production-claim-boundary.md"
  key_links:
    - from: "docs/parity/index.json"
      to: "docs/parity/production-claim-boundary.md"
      via: "audit evidence path"
      pattern: "docs/parity/production-claim-boundary\\.md"
    - from: "docs/parity/checklist.md"
      to: "docs/parity/production-claim-boundary.md"
      via: "human checklist evidence link"
      pattern: "production-claim-boundary\\.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 82-2026-06-21T12-38-13
generated_at: 2026-06-21T12:58:11Z
---

<objective>
Update the parity roots and deferred-surface register so the canonical Phase 82 boundary is discoverable from existing reviewer entrypoints.

Purpose: PROD-02 and PROD-04 require traceability, not a new evidence registry. This plan uses the existing parity index, checklist, README, and deviations register instead of inventing a manifest.
Output: v1.8 machine/human parity roots and a durable deferred-surface register.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/82-production-claim-boundary/82-CONTEXT.md
@.planning/phases/82-production-claim-boundary/82-RESEARCH.md
@.planning/phases/82-production-claim-boundary/82-01-SUMMARY.md
@docs/parity/production-claim-boundary.md
@docs/parity/deviations-and-unknowns.md
@docs/parity/index.json
@docs/parity/checklist.md
@docs/parity/README.md
@docs/parity/release-readiness.md
@.planning/phases/80-opt-in-soak-uat-and-release-boundaries/80-02-SUMMARY.md

<interfaces>
From `docs/parity/index.json`:
```json
{
  "surfaces": [{ "name": "surface-name", "status": "done" }],
  "checklist": {
    "surfaces": [
      {
        "id": "surface-id",
        "status": "done",
        "requirements": ["REQ-01"],
        "evidence": ["docs/path.md"],
        "known_gaps": ["gap text"],
        "suspected_unknowns": ["unknown text"]
      }
    ]
  },
  "audit": {
    "audit_key": {
      "path": "release-readiness.md",
      "status": "done",
      "requirements": ["REQ-01"],
      "evidence": ["docs/path.md"]
    }
  }
}
```

Required new identifiers:
```text
surface id/name: v1-8-production-claim-boundary
audit key: v1_8_production_claim_boundary
requirements: PROD-01, PROD-02, PROD-03, PROD-04
canonical doc: docs/parity/production-claim-boundary.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the deviations register with the v1.8 deferred inventory</name>
  <files>docs/parity/deviations-and-unknowns.md</files>
  <read_first>
    - docs/parity/deviations-and-unknowns.md
    - docs/parity/production-claim-boundary.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/82-production-claim-boundary/82-CONTEXT.md
  </read_first>
  <action>Add a new section titled `### v1.8 Production Claim Boundary` under `## Deferred Surfaces`. State that Phase 82 supports PROD-01, PROD-02, PROD-03, and PROD-04 by defining claim gates and preserving deferred status, not by expanding support. Add a compact table with columns `Surface`, `Support term`, `Why deferred`, and `Required future gate`. Use support term `deferred` for every D-09 surface: inbound serving; address relay; block serving; transaction relay; compact block relay; production-funds wallet use; production-funds wallet safety; migration apply mode; signed packaging or package-manager distribution; Windows service integration; hosted dashboards; GUI parity; public-network default checks; public-network CI; release-blocking live sync; automatic support-bundle upload; destructive repair; broad production-node readiness. Include concrete future gates such as scoped P2P production milestone, wallet-production threat model and audit, migration apply safety design, release-engineering signing/provenance gates, hosted-operations design, GUI milestone, public-network CI policy decision, support-upload privacy and consent design, destructive-repair policy, and future production-readiness milestone. Do not mark any deferred surface as `supported`, `preview`, `opt-in UAT`, or `unsupported` in this v1.8 table.</action>
  <acceptance_criteria>
    - `docs/parity/deviations-and-unknowns.md` contains `### v1.8 Production Claim Boundary`.
    - The section contains `PROD-01`, `PROD-02`, `PROD-03`, and `PROD-04`.
    - The section contains all D-09 surfaces named in the action.
    - Each row in the v1.8 table uses `deferred` as the support term.
    - The section contains `automatic support-bundle upload`, `destructive repair`, and `broad production-node readiness`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "### v1\\.8 Production Claim Boundary|PROD-01|PROD-02|PROD-03|PROD-04|automatic support-bundle upload|destructive repair|broad production-node readiness" docs/parity/deviations-and-unknowns.md</automated>
    <automated>rg -n "inbound serving|address relay|block serving|transaction relay|compact block relay|production-funds wallet use|production-funds wallet safety|migration apply mode|signed packaging or package-manager distribution|Windows service integration|hosted dashboards|GUI parity|public-network default checks|public-network CI|release-blocking live sync|automatic support-bundle upload|destructive repair|broad production-node readiness" docs/parity/deviations-and-unknowns.md</automated>
  </verify>
  <done>The durable deviations register preserves every v1.8 deferred production-adjacent surface and future gate without upgrading support status.</done>
</task>

<task type="auto">
  <name>Task 2: Add the v1.8 machine-readable parity root</name>
  <files>docs/parity/index.json</files>
  <read_first>
    - docs/parity/index.json
    - docs/parity/production-claim-boundary.md
    - docs/parity/release-readiness.md
    - docs/parity/deviations-and-unknowns.md
    - .planning/phases/82-production-claim-boundary/82-CONTEXT.md
  </read_first>
  <action>Update `docs/parity/index.json` using structured JSON editing, not ad hoc string appends. Add top-level `surfaces` entry `{ "name": "v1-8-production-claim-boundary", "status": "done" }` if the array uses `name` entries. Add a `checklist.surfaces` object with `id` `v1-8-production-claim-boundary`, `title` `v1.8 Production Claim Boundary`, `status` `done`, `requirements` exactly `["PROD-01", "PROD-02", "PROD-03", "PROD-04"]`, evidence paths `docs/parity/production-claim-boundary.md`, `docs/parity/release-readiness.md`, `docs/parity/deviations-and-unknowns.md`, `docs/parity/checklist.md`, `docs/parity/README.md`, `README.md`, `docs/operator/runtime-guide.md`, and `scripts/verify.sh`, plus known gaps stating that v1.8 defines gates only and does not claim production full-node readiness or deferred surfaces as supported. Add `audit.v1_8_production_claim_boundary` with `path` `production-claim-boundary.md`, `status` `done`, the same four requirements, and the same evidence paths. Do not create a new evidence manifest JSON file.</action>
  <acceptance_criteria>
    - `jq -e '.surfaces[] | select(.name=="v1-8-production-claim-boundary" and .status=="done")' docs/parity/index.json` succeeds.
    - `jq -e '.checklist.surfaces[] | select(.id=="v1-8-production-claim-boundary" and .status=="done" and (.requirements == ["PROD-01","PROD-02","PROD-03","PROD-04"]))' docs/parity/index.json` succeeds.
    - `jq -e '.audit.v1_8_production_claim_boundary | select(.path=="production-claim-boundary.md" and .status=="done" and (.requirements == ["PROD-01","PROD-02","PROD-03","PROD-04"]))' docs/parity/index.json` succeeds.
    - The v1.8 evidence list contains `docs/parity/production-claim-boundary.md`, `README.md`, `docs/operator/runtime-guide.md`, and `scripts/verify.sh`.
    - No new `docs/parity/*manifest*v1.8*.json`, `docs/parity/*evidence*v1.8*.json`, or similar manifest file is created.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.surfaces[] | select(.name=="v1-8-production-claim-boundary" and .status=="done")' docs/parity/index.json</automated>
    <automated>jq -e '.checklist.surfaces[] | select(.id=="v1-8-production-claim-boundary" and .status=="done" and (.requirements == ["PROD-01","PROD-02","PROD-03","PROD-04"]))' docs/parity/index.json</automated>
    <automated>jq -e '.audit.v1_8_production_claim_boundary | select(.path=="production-claim-boundary.md" and .status=="done" and (.requirements == ["PROD-01","PROD-02","PROD-03","PROD-04"]))' docs/parity/index.json</automated>
  </verify>
  <done>The machine-readable parity root exposes the Phase 82 boundary through existing index/checklist/audit structures and no new manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Update the human parity checklist and parity README</name>
  <files>docs/parity/checklist.md, docs/parity/README.md</files>
  <read_first>
    - docs/parity/checklist.md
    - docs/parity/README.md
    - docs/parity/index.json
    - docs/parity/production-claim-boundary.md
    - docs/parity/deviations-and-unknowns.md
  </read_first>
  <action>Add a `v1-8-production-claim-boundary` row to `docs/parity/checklist.md` that mirrors `checklist.surfaces` from `docs/parity/index.json`: status `done`, requirements `PROD-01`, `PROD-02`, `PROD-03`, `PROD-04`, evidence links to `production-claim-boundary.md`, `release-readiness.md`, `deviations-and-unknowns.md`, `README.md`, `docs/operator/runtime-guide.md`, and `scripts/verify.sh`, known gaps saying Phase 82 defines gates only, and suspected unknowns saying future production-node readiness, inbound serving, relay, production-wallet, migration-apply, signed packaging, Windows service, GUI, hosted-dashboard, public-network CI, support-upload, destructive-repair, and Phase 88 guardrails need separate scoped phases. Update `docs/parity/README.md` so the current root is v1.8 production claim boundary with a link to `production-claim-boundary.md`, while v1.7 remains historical source-built opt-in soak/recovery evidence. Do not duplicate the full claim-to-evidence matrix in the README.</action>
  <acceptance_criteria>
    - `docs/parity/checklist.md` contains a row for `v1-8-production-claim-boundary`.
    - That row contains `PROD-01`, `PROD-02`, `PROD-03`, `PROD-04`, and a link to `production-claim-boundary.md`.
    - `docs/parity/README.md` contains `v1.8 production claim boundary` and `production-claim-boundary.md`.
    - `docs/parity/README.md` still contains `v1.7` and `historical evidence`.
    - `docs/parity/README.md` does not contain the full matrix header `Statement | Support term | Current status | Evidence sources | Verification command | UAT status | Residual risk | Next required gate`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "v1-8-production-claim-boundary|PROD-01|PROD-02|PROD-03|PROD-04|production-claim-boundary\\.md" docs/parity/checklist.md</automated>
    <automated>rg -n "v1\\.8 production claim boundary|production-claim-boundary\\.md|v1\\.7|historical evidence" docs/parity/README.md</automated>
    <automated>! rg -n "Statement \\| Support term \\| Current status \\| Evidence sources \\| Verification command \\| UAT status \\| Residual risk \\| Next required gate" docs/parity/README.md</automated>
  </verify>
  <done>The human parity roots expose the v1.8 boundary without duplicating the canonical matrix or rewriting historical v1.7 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity JSON -> release tooling/reviewers | Structured metadata may be consumed by scripts or reviewers as the machine-readable source of claim evidence. |
| deviations register -> contributors | Contributors use deferred-surface entries to decide whether a support term can change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Tampering / Repudiation | `docs/parity/index.json` | mitigate | Validate JSON with `jq` and require exact `v1-8-production-claim-boundary` requirements and evidence paths. |
| T-82-02-02 | Repudiation | `docs/parity/deviations-and-unknowns.md` | mitigate | Add every D-09 deferred surface with `deferred` support term and future gate. |
| T-82-02-03 | Spoofing | human checklist row | mitigate | Checklist must state Phase 82 defines gates only and does not claim production full-node readiness. |
| T-82-02-04 | Information Disclosure | automatic support upload wording | mitigate | Keep automatic support-bundle upload deferred pending privacy, consent, retention, and upload design. |
</threat_model>

<verification>
Run the task-level `rg` and `jq` checks. After all plans complete, Phase 82 closeout must run `bash scripts/verify.sh`.
</verification>

<success_criteria>
The v1.8 production claim boundary is discoverable through the parity index, checklist, parity README, and deviations register, with every PROD requirement and D-09 deferred surface preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/82-production-claim-boundary/82-02-SUMMARY.md`.
</output>
