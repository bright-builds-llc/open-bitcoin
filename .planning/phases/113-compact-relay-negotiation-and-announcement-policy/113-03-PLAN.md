---
phase: 113-compact-relay-negotiation-and-announcement-policy
plan: 03
type: execute
wave: 3
depends_on:
  - 113-01
  - 113-02
files_modified:
  - packages/open-bitcoin-network/src/peer/compact_relay.rs
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - docs/metrics/lines-of-code.md
autonomous: true
requirements:
  - CMP-05
  - CMP-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 113-2026-07-04T22-53-48
generated_at: 2026-07-04T23:05:00Z
must_haves:
  truths:
    - "When compact gates fail, peers receive explicit headers, inventory, or suppress decisions instead of implicit behavior."
    - "Low-bandwidth compact preference remains capability state and does not allow direct compact announcements."
    - "Unsupported sendcmpct versions remain evidence-only when a supported version 2 preference already exists."
    - "Transaction relay, package relay, bloom/filter permissions, compact filters, public defaults, and production-readiness surfaces do not activate compact announcements."
    - "Compact-block getdata serving remains suppressed and does not start reconstruction or missing-transaction state."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Fallback and isolation tests for low-bandwidth, unsupported versions, default-disabled, relay, permission, and public-default guardrails."
    - path: "packages/open-bitcoin-node/src/network/tests.rs"
      provides: "Node-shell regression proving compact getdata remains suppressed and separate from direct announcement policy."
    - path: "docs/metrics/lines-of-code.md"
      provides: "Tracked generated LOC freshness if repo verification requires regeneration."
  key_links:
    - from: "decide_compact_announcement"
      to: "headers/inventory/suppress outcomes"
      via: "CompactAnnouncementAction"
      pattern: "AnnounceHeaders|AnnounceInventory|Suppress"
    - from: "transaction relay setup"
      to: "compact announcement decision"
      via: "scope-isolation tests"
      pattern: "phase113_.*transaction.*does_not.*compact"
    - from: "ManagedPeerNetwork::serve_inventory"
      to: "compact getdata suppression"
      via: "node network regression"
      pattern: "InventoryType::CompactBlock"
---

<objective>
Close Phase 113 by pinning header/inventory fallback behavior and preventing accidental scope activation through adjacent relay, permission, filter, public-default, or production surfaces.

Purpose: Compact relay negotiation and announcement policy must be useful to later phases without making broader relay or production claims. Failed compact gates need explicit typed outcomes, and neighboring surfaces must stay unable to turn compact relay on by implication.
Output: Guardrail tests and verification that preserve fallback behavior, compact getdata suppression, and deterministic local verification boundaries.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-CONTEXT.md
@.planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-RESEARCH.md
@.planning/phases/111-full-block-serving-request-path/111-01-SUMMARY.md
@.planning/phases/111-full-block-serving-request-path/111-02-SUMMARY.md
@.planning/phases/112-bip152-wire-codec-and-message-semantics/112-03-SUMMARY.md
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/compact_relay.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-node/src/network/inventory.rs
@packages/open-bitcoin-node/src/network/tests.rs
@scripts/verify.sh

<interfaces>
Plan 113-02 provides:

```rust
pub enum CompactAnnouncementAction {
    AnnounceCompactBlock,
    AnnounceHeaders,
    AnnounceInventory,
    Suppress,
}

pub fn decide_compact_announcement(input: CompactAnnouncementInput) -> CompactAnnouncementDecision;
```

Existing node-shell compact getdata boundary from `packages/open-bitcoin-node/src/network/inventory.rs`:

```rust
InventoryType::CompactBlock => {
    let block_hash = BlockHash::from(request.object_hash);
    let input = self.managed_block_serve_input(peer_id, &request, block_hash, true);
    let _decision = serve_managed_block_request(input, |hash| {
        self.blocks_by_hash.get(&hash).cloned()
    });
    missing.push(request);
}
```

Keep that compact getdata behavior suppressed in Phase 113. Do not add compact reconstruction, short-ID matching, missing transaction requests, `blocktxn` response matching, validation/connect handoff, broad operator/RPC/CLI/dashboard/metrics/log/support rollout, package relay, bloom/filter serving, compact filter serving, public serving defaults, public-network CI, archive-node claims, production service operation, production full-node readiness, or production-funds wallet claims.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin headers, inventory, and suppress fallback decisions</name>
  <files>packages/open-bitcoin-network/src/peer/compact_relay.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-network/src/peer/compact_relay.rs
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-network/src/peer/tests.rs
    - packages/open-bitcoin-network/src/block_serving.rs
    - .planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-CONTEXT.md
    - .planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-RESEARCH.md
  </read_first>
  <behavior>
    - Low-bandwidth `sendcmpct(false, 2)` records compact capability but yields non-compact fallback for direct compact announcement decisions, including after an earlier high-bandwidth `sendcmpct(true, 2)`.
    - High -> low -> high supported `sendcmpct` toggles refresh stored `CompactAnnouncementEligibility` only after each announcement decision is recorded: `Eligible`, then `HighBandwidthNotRequested` or equivalent non-eligible state, then `Eligible` again.
    - Unsupported versions without prior supported version 2 preference yield non-compact fallback and retain the exact failed-gate reason; unsupported versions after supported high-bandwidth version 2 preserve the high-bandwidth version 2 preference while retaining unsupported-version evidence.
    - Peers that already have the current header yield headers fallback, not compact announcement.
    - Unavailable block status and resource pressure yield suppress decisions.
  </behavior>
  <action>
    Add or extend tests in `packages/open-bitcoin-network/src/peer/tests.rs` with these names:
    - `phase113_low_bandwidth_compact_peer_uses_headers_fallback_for_direct_announcement`
    - `phase113_low_bandwidth_compact_peer_uses_inventory_fallback_without_sendheaders`
    - `phase113_high_to_low_toggle_never_announces_compact`
    - `phase113_low_to_high_toggle_all_gates_allow_compact_block`
    - `phase113_high_low_high_toggle_refreshes_recorded_eligibility`
    - `phase113_unsupported_compact_version_without_supported_preference_uses_inventory_fallback_without_disconnect`
    - `phase113_unsupported_compact_version_after_supported_high_bandwidth_still_announces_compact`
    - `phase113_peer_already_has_current_header_uses_headers_fallback`
    - `phase113_missing_header_or_unavailable_block_never_announces_compact`

    The tests must use `decide_compact_announcement_for_peer` and the real stored peer state from `PeerManager`. For low-bandwidth tests, first process `WireNetworkMessage::SendCompact(SendCompactMessage { announce: false, version: 2 })`; assert action is `AnnounceHeaders` when `SendHeaders` was processed and `AnnounceInventory` otherwise. For the high-to-low toggle test, process `announce: true, version: 2` followed by `announce: false, version: 2`, set every other compact announcement gate to the positive path, and assert the decision is not `AnnounceCompactBlock` with reason `"compact_high_bandwidth_not_requested"`. For the low-to-high toggle test, process `announce: false, version: 2` followed by `announce: true, version: 2`, set every other compact announcement gate to the positive path, and assert `AnnounceCompactBlock` with reason `"compact_announced"`. For the high-low-high eligibility-refresh test, call the decision entrypoint after each supported toggle and assert stored eligibility becomes `Eligible`, then `Ineligible { reason: CompactAnnouncementEligibilityReason::HighBandwidthNotRequested }` or an equivalent non-eligible high-bandwidth-not-requested state, then `Eligible` again. For unsupported-only tests, process `version: 1` or `version: 3`; assert no disconnect action and reason `"compact_unsupported_version"`. For unsupported-after-supported tests, process `announce: true, version: 2`, then unsupported `version: 1` or `version: 3`, set every other compact announcement gate to the positive path, and assert `AnnounceCompactBlock` with reason `"compact_announced"` while unsupported-version evidence remains available. For already-current-header and missing-header tests, set the corresponding `peer_has_current_header` and `peer_has_previous_header` facts explicitly and assert non-compact action.

    If any reason label from Plan 113-02 is too coarse to distinguish these outcomes, refine the enum while preserving the exact strings already listed in Plan 113-02. Do not introduce raw block hashes, peer IDs, transaction IDs, permission strings, or dynamic resource values into reason strings.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_low_bandwidth -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_high_to_low_toggle_never_announces_compact -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_low_to_high_toggle_all_gates_allow_compact_block -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_unsupported_compact_version -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_missing_header_or_unavailable_block_never_announces_compact -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Low-bandwidth tests assert `CompactRelayPreference::Requested` for low-bandwidth and no `AnnounceCompactBlock` action.
    - High-to-low toggle test asserts final high-bandwidth preference is `CompactRelayPreference::NotRequested`, final low-bandwidth preference is `CompactRelayPreference::Requested`, action is not `AnnounceCompactBlock`, and reason string is `"compact_high_bandwidth_not_requested"`.
    - Low-to-high toggle test asserts final low-bandwidth preference is `CompactRelayPreference::NotRequested`, final high-bandwidth preference is `CompactRelayPreference::Requested`, action is `AnnounceCompactBlock`, and reason string is `"compact_announced"`.
    - High-low-high eligibility-refresh test asserts recorded eligibility transitions `Eligible` -> `HighBandwidthNotRequested` or equivalent non-eligible state -> `Eligible` only after decision recording.
    - Unsupported-only test asserts reason string `"compact_unsupported_version"` and no disconnect action.
    - Unsupported-after-supported test asserts supported high-bandwidth version 2 followed by unsupported version 1 or 3 still uses high-bandwidth version 2 preference, returns `AnnounceCompactBlock` with reason `"compact_announced"` when all other gates pass, and keeps unsupported-version evidence available.
    - Already-current-header test asserts reason string `"compact_peer_already_has_header"` and action `AnnounceHeaders`.
    - Missing/unavailable test asserts no `CompactAnnouncementAction::AnnounceCompactBlock`.
  </acceptance_criteria>
  <done>Fallback outcomes are explicit and deterministic for low-bandwidth, unsupported, already-known, missing-header, unavailable-block, and resource-limited paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove adjacent relay and permission surfaces cannot activate compact announcements</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-network/src/peer/tests.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay.rs
    - packages/open-bitcoin-network/src/block_serving.rs
    - packages/open-bitcoin-network/src/relay.rs
    - .planning/REQUIREMENTS.md
    - .planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-CONTEXT.md
  </read_first>
  <behavior>
    - `WtxidRelay`, transaction relay activation, inbound serving, protected admission, and download permission inputs do not create compact capability.
    - `BlockRelayActivationPolicy::default()` keeps compact relay disabled.
    - Enabling block serving without compact relay does not allow direct compact announcement.
    - No package relay, bloom-filter, compact-filter, public default, or production-readiness symbol becomes part of compact relay policy.
  </behavior>
  <action>
    Add tests in `packages/open-bitcoin-network/src/peer/tests.rs` with these names:
    - `phase113_wtxidrelay_does_not_activate_compact_announcement`
    - `phase113_block_serving_enabled_without_compact_relay_does_not_announce_compact`
    - `phase113_download_permission_does_not_grant_compact_announcement`
    - `phase113_protected_inbound_permission_does_not_grant_compact_announcement`
    - `phase113_default_activation_policy_suppresses_compact_announcement`

    The positive compact path must still require `SendCompactMessage { announce: true, version: 2 }` and `activation.compact_relay.enabled = true`. Use permission and inbound helper patterns already present in `peer/tests.rs`; do not create a new permission vocabulary. The tests must assert the action is not `AnnounceCompactBlock` and the reason is the first failed compact gate, usually `"compact_relay_disabled"` or `"compact_peer_not_negotiated"`.

    Add an acceptance-visible guard in `peer/compact_relay.rs` comments or tests only if needed: "transaction relay is not a compact relay activator". Do not add package relay, bloom/filter serving, compact filter serving, public-default, service-manager, production-readiness, public-network CI, or production-funds wallet behavior.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_wtxidrelay_does_not_activate_compact_announcement -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_block_serving_enabled_without_compact_relay_does_not_announce_compact -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_download_permission_does_not_grant_compact_announcement -- --nocapture</automated>
    <automated>bash -lc '! rg -n "package[_ -]?relay|bloom|compact[_ -]?filter|public[_ -]?default|production[_ -]?readiness|public-network" packages/open-bitcoin-network/src/peer/compact_relay.rs packages/open-bitcoin-network/src/peer.rs'</automated>
  </verify>
  <acceptance_criteria>
    - Each listed scope-isolation test exists and asserts no `CompactAnnouncementAction::AnnounceCompactBlock`.
    - `BlockRelayActivationPolicy::default()` is covered by a Phase 113 compact announcement test.
    - The negative `rg` command finds no package/filter/public/production scope terms in compact relay production code, except if already present in comments added solely to state "not an activator"; if such a comment is added, adjust the guard to inspect code tokens instead of prose.
  </acceptance_criteria>
  <done>Compact announcement policy cannot be activated by transaction relay, block serving alone, peer permissions, inbound protection, package/filter surfaces, public defaults, or production-readiness wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve compact getdata suppression and run phase verification</name>
  <files>packages/open-bitcoin-node/src/network/tests.rs, docs/metrics/lines-of-code.md</files>
  <read_first>
    - packages/open-bitcoin-node/src/network/inventory.rs
    - packages/open-bitcoin-node/src/network/block_serving.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-network/src/peer/compact_relay.rs
    - .planning/phases/111-full-block-serving-request-path/111-02-SUMMARY.md
    - .planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-CONTEXT.md
    - AGENTS.md
    - scripts/verify.sh
  </read_first>
  <behavior>
    - `InventoryType::CompactBlock` getdata remains missing/suppressed in the node shell.
    - Enabling compact announcement policy does not make getdata compact serving emit `WireNetworkMessage::CompactBlock`.
    - Full repo verification remains deterministic and local.
  </behavior>
  <action>
    Add a managed-network regression in `packages/open-bitcoin-node/src/network/tests.rs` named `phase113_compact_getdata_remains_suppressed_after_negotiation_policy`. The test must set up the existing managed network block-serving fixture, request `InventoryType::CompactBlock`, and assert:
    - returned messages do not include `WireNetworkMessage::CompactBlock`
    - the compact inventory request remains in the missing/notfound path
    - no `GetBlockTxn`, `BlockTxn`, reconstruction, mempool lookup, or validation/connect handoff is triggered

    Run targeted network and node checks, then run `bash scripts/verify.sh` as the phase completion gate. If `bash scripts/verify.sh` reports `docs/metrics/lines-of-code.md` is stale, run `bun run scripts/generate-loc-report.ts --source=worktree --output=docs/metrics/lines-of-code.md`, then rerun `bash scripts/verify.sh`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase113_compact_getdata_remains_suppressed_after_negotiation_policy -- --nocapture</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_ -- --nocapture</automated>
    <automated>cargo fmt --manifest-path packages/Cargo.toml --all -- --check</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network -p open-bitcoin-node --all-targets --all-features -- -D warnings</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `packages/open-bitcoin-node/src/network/tests.rs` contains `phase113_compact_getdata_remains_suppressed_after_negotiation_policy`.
    - The node test asserts no `WireNetworkMessage::CompactBlock` is emitted for compact getdata.
    - The node test does not require public-network access, wall-clock soak, service-manager setup, production deployment, or external credentials.
    - Final `bash scripts/verify.sh` passes, with `docs/metrics/lines-of-code.md` refreshed only if the verifier requires it.
  </acceptance_criteria>
  <done>Compact getdata remains suppressed, Phase 113 guardrails pass targeted tests, and repo-native deterministic verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adjacent relay state -> compact policy | Transaction relay, inbound permissions, and block-serving activation are nearby state surfaces that must not grant compact announcements by implication. |
| Compact announcement policy -> getdata serving | Direct announcement decisions must not activate compact-block request serving, reconstruction, or missing transaction state. |
| Verification artifacts -> release evidence | Test names, reason labels, and generated metrics can leak misleading production or public-default claims if not bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03-01 | Elevation of Privilege | Scope-isolation tests | mitigate | Assert transaction relay, block serving alone, download permission, protected inbound, and default activation do not yield `AnnounceCompactBlock`. |
| T-113-03-02 | Denial of Service | `InventoryType::CompactBlock` getdata path | mitigate | Keep compact getdata suppressed/missing and verify no compact payload, reconstruction, or missing transaction request is emitted. |
| T-113-03-03 | Information Disclosure | Reason labels and tests | mitigate | Keep fixed labels only and avoid raw peer, permission, transaction, block, public-network, or production evidence values. |
| T-113-03-04 | Tampering | Header/inventory fallback behavior | mitigate | Tests pin low-bandwidth, unsupported-version, already-current-header, missing-header, unavailable-block, and resource-limited outcomes. |
| T-113-03-05 | Repudiation | Phase verification evidence | mitigate | Run `bash scripts/verify.sh` and record command outcomes in `113-03-SUMMARY.md` so deterministic local evidence is auditable. |
</threat_model>

<must_haves>
- Header and inventory fallback are explicit typed outcomes with stable reasons.
- Low-bandwidth compact preference never authorizes direct compact announcements.
- High-to-low and low-to-high supported toggles refresh recorded announcement eligibility only after fresh announcement decisions.
- Unsupported-version evidence never overrides an existing supported version 2 preference.
- Adjacent relay, permission, package/filter, public-default, and production wording cannot activate compact announcements.
- Compact getdata remains suppressed and separate from direct announcement policy.
- The final phase verifier is deterministic and local.
</must_haves>

<verification>
Run targeted and full verification:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network phase113_ -- --nocapture`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase113_compact_getdata_remains_suppressed_after_negotiation_policy -- --nocapture`
- `cargo fmt --manifest-path packages/Cargo.toml --all -- --check`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network -p open-bitcoin-node --all-targets --all-features -- -D warnings`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
CMP-05 is closed with explicit fallback and suppression outcomes for failed compact gates. CMP-06 is closed with deterministic guardrails proving compact relay negotiation remains independent from transaction relay, package relay, bloom/filter permissions, compact filters, public serving defaults, public-network CI, production service operation, production full-node readiness, and production-funds wallet claims.
</success_criteria>

<output>
After completion, create `.planning/phases/113-compact-relay-negotiation-and-announcement-policy/113-03-SUMMARY.md`.
</output>
