---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "19"
type: execute
wave: 15
depends_on: ["134-18"]
files_modified:
  - packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
  - packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs
  - packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/bounded_packages.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
gap_closure: true
requirements: [MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "Raw accepted-package input at the named lifecycle-command cap can prepare, while unique and duplicate-only cap+1 inputs fail before preprocessing with unchanged manager state."
    - "Accepted package fingerprints are bounded by total unique entries as well as member count."
    - "Repeated admission of one package fingerprint is idempotent and does not consume capacity."
    - "Same-transition retirements occur before final prospective-state capacity validation."
  artifacts:
    - path: packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
      provides: "Raw-command-bounded, deduplicated, retirement-aware accepted-package projection"
    - path: packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/bounded_packages.rs
      provides: "Raw-command cap, stored-state cap, duplicate, replacement, and atomic-failure regressions"
    - path: docs/parity/source-breadcrumbs.json
      provides: "Auditable Knots anchors for the new Rust regression module"
  key_links:
    - from: packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
      to: "accepted package state"
      via: "reject raw command overflow first; then retire, deduplicate, and enforce final stored-state caps"
      pattern: "accepted_packages\\.len\\(\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Close the accepted-package fixed-memory and ordering gaps.

Purpose: Enforce D-09, D-15, and D-16 by validating the final unique peer-cache state after same-transition retirements.
Output: An early raw lifecycle-command package bound, idempotent duplicate handling, retirement-aware stored-state capacity checks, and adversarial regression coverage with parity breadcrumbs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-18-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound raw accepted-package work and deduplicate stored fingerprints</name>
  <read-first>
    <file>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs</file>
    <file>docs/parity/source-breadcrumbs.json</file>
    <file>scripts/check-parity-breadcrumbs.ts</file>
    <file>.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md</file>
    <file>.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md</file>
  </read-first>
  <files>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/bounded_packages.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - "Repeated admission of the same fingerprint and members leaves accepted-package state unchanged."
    - "A raw accepted-package input at the lifecycle-command cap succeeds."
    - "A raw accepted-package input at cap+1 fails atomically before package iteration, deduplication, or allocation."
    - "A duplicate-only raw cap+1 input is rejected even though deduplication would produce one stored fingerprint, and the complete manager state remains unchanged."
    - "The exact stored unique-package cap succeeds and stored cap+1 fails atomically."
    - "Total member/memory caps remain enforced independently of unique fingerprint count."
  </behavior>
  <action>
    Define `const MAX_ACCEPTED_PACKAGES_PER_LIFECYCLE_COMMAND: usize =
    PHASE102_MAX_ORPHAN_TRANSACTIONS;` and add
    `PeerTransactionLifecyclePreparationError::AcceptedPackageCountLimit { count,
    maximum }`. Make the raw
    `input.accepted_packages.len() > MAX_ACCEPTED_PACKAGES_PER_LIFECYCLE_COMMAND`
    rejection the first branch executed by `validate_identity_work_bounds`, which
    remains the first call in `prepare_transaction_lifecycle`. This branch must run
    before the admissions/teardowns count loop, any accepted-package/member iteration,
    alias validation, `BTreeMap`/`BTreeSet` collection, prospective-state clone,
    fingerprint deduplication, or prepared-operation allocation. Return the count-limit
    error without mutating `PeerManager`; in particular, do not deduplicate an oversized
    raw command into compliance.

    After that raw command has passed the named lifecycle-command bound, enforce
    `const MAX_STORED_ACCEPTED_PACKAGE_FINGERPRINTS: usize =
    PHASE102_MAX_ORPHAN_TRANSACTIONS;` against the prospective final map and construct
    stored admissions through deterministic fingerprint-keyed deduplication. A bounded
    duplicate must neither append a prepared operation nor consume stored count/member
    budget. Preserve complete fingerprint identity per D-09 and existing bounded
    member accounting. Create `bounded_packages.rs` with inline Knots breadcrumbs,
    register it in `docs/parity/source-breadcrumbs.json`, and add focused
    Arrange/Act/Assert regressions named
    `accepts_raw_accepted_package_command_at_exact_cap`,
    `rejects_raw_unique_accepted_package_command_at_cap_plus_one_without_mutation`,
    `rejects_raw_duplicate_only_accepted_package_command_at_cap_plus_one_before_deduplication`,
    plus focused bounded-duplicate idempotency, exact stored-fingerprint cap, stored
    fingerprint cap+1, and mixed member-count tests. Each cap+1 test must compare a
    complete pre-state snapshot with the post-error manager state; the duplicate-only
    cap+1 test must also assert `AcceptedPackageCountLimit { count: cap + 1, maximum:
    cap }` to prove rejection occurred before deduplication.
  </action>
  <acceptance-criteria>
    <criterion>`transaction_lifecycle.rs` defines `MAX_ACCEPTED_PACKAGES_PER_LIFECYCLE_COMMAND` and an `AcceptedPackageCountLimit { count, maximum }` error.</criterion>
    <criterion>The raw `input.accepted_packages.len()` cap check executes before every accepted-package iteration, deduplication, prospective-state allocation, and prepared-operation allocation.</criterion>
    <criterion>An input containing exactly `MAX_ACCEPTED_PACKAGES_PER_LIFECYCLE_COMMAND` accepted packages prepares successfully when all other bounds are satisfied.</criterion>
    <criterion>A unique cap+1 input returns `AcceptedPackageCountLimit` and preserves a complete pre-state snapshot.</criterion>
    <criterion>A duplicate-only cap+1 input returns `AcceptedPackageCountLimit { count: cap + 1, maximum: cap }` before deduplication and preserves a complete pre-state snapshot.</criterion>
    <criterion>`transaction_lifecycle.rs` defines `MAX_STORED_ACCEPTED_PACKAGE_FINGERPRINTS` and enforces it only after raw input passes the command cap and bounded stored admissions are deduplicated.</criterion>
    <criterion>Bounded duplicates are idempotent in prepared operations and final state.</criterion>
    <criterion>Rejected stored-state cap cases preserve a complete pre-state snapshot.</criterion>
    <criterion>The new Rust path has matching inline and registry parity breadcrumbs.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-19-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-19-01-raw-stored-package-bounds -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features bounded_packages -- --nocapture &amp;&amp; bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <done>Raw accepted-package work is rejected at cap+1 before preprocessing, stored fingerprints and members are independently fixed-memory bounded, and bounded duplicate input is idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate capacity against the post-retirement final state</name>
  <read-first>
    <file>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/bounded_packages.rs</file>
  </read-first>
  <files>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/bounded_packages.rs</files>
  <behavior>
    - "A full cache can retire old package identities and admit equal-size replacements in one transition."
    - "Capacity validation uses the final unique state after teardown/retirement and admission."
    - "A final over-cap state fails before apply and leaves the manager unchanged."
  </behavior>
  <action>
    Reorder pure preparation so teardown and package retirement are applied to the
    prospective clone before deduplicated admissions and all final capacity checks.
    Validate the complete final state, not the pre-retirement intermediate state. Keep
    apply infallible and graph ordering unchanged. Extend the focused tests with
    full-cap retire-and-replace, partial retirement still over cap, duplicate
    replacement, and failure atomicity.
  </action>
  <acceptance-criteria>
    <criterion>Retirement operations precede final count/member capacity validation.</criterion>
    <criterion>A within-cap retire-and-replace transition succeeds.</criterion>
    <criterion>Every over-cap final state fails during preparation with no mutation.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-19-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-19-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-19-02-retirement-capacity -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features bounded_packages -- --nocapture</automated>
  </verify>
  <done>Preparation accepts exactly the transitions whose post-retirement final peer-cache state fits all bounds.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Package lifecycle input to peer caches | Duplicate or replacement-heavy input controls bounded identity state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-19-01 | Denial of Service | Accepted package lifecycle input and stored fingerprints | mitigate | Check raw `input.accepted_packages.len()` against `MAX_ACCEPTED_PACKAGES_PER_LIFECYCLE_COMMAND` before iteration, deduplication, cloning, or allocation; only bounded input proceeds to deduplicated final-state unique-entry/member caps. |
| T-134-19-02 | Tampering | Prospective peer lifecycle | mitigate | Apply retirements before admissions and reject over-cap final state before mutation. |
| T-134-19-03 | Repudiation | Capacity failure | mitigate | Assert exact-cap success plus complete unchanged before/after snapshots for unique and duplicate-only raw cap+1 commands and stored-state over-cap cases. |
</threat-model>

<verification>
Run focused raw-command and stored-package bound tests plus breadcrumb validation, then the required Rust pre-commit sequence in order and Bright Builds checks.
</verification>

<success-criteria>
- Unique accepted packages and total members are independently bounded.
- Raw accepted-package input at the named lifecycle-command cap succeeds, while unique and duplicate-only cap+1 inputs fail before preprocessing with unchanged state.
- Bounded duplicate package admissions are idempotent.
- Same-transition retirements are accounted before final-state capacity validation.
- MPLIFE-02 and MPLIFE-03 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-19-SUMMARY.md`.
</output>
