---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "18"
type: execute
wave: 14
depends_on: ["134-13"]
files_modified:
  - packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
  - packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs
  - packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/reconciliation.rs
  - packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/identity_aliases.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
gap_closure: true
requirements: [MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "Removing an orphan by txid also removes every cursor keyed by the orphan's actual wtxid, including txid/wtxid alias cases."
    - "Peer reconciliation compares expected and actual txid and wtxid identity symmetrically."
    - "Unbroadcast reconciliation reports both unexpected actual members and missing expected members."
  artifacts:
    - path: packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
      provides: "Identity-complete orphan removal and peer reconciliation"
    - path: packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/identity_aliases.rs
      provides: "Same-txid/different-wtxid cleanup regressions"
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
      provides: "Symmetric expected-versus-actual unbroadcast audit"
    - path: docs/parity/source-breadcrumbs.json
      provides: "Auditable Knots anchors for the new Rust regression module"
  key_links:
    - from: packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
      to: "orphan removals"
      via: "removed orphan records carry their stored wtxid into cursor teardown"
      pattern: "(removed|orphan).*wtxid"
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs
      to: "canonical mempool membership"
      via: "symmetric set difference against expected unbroadcast members"
      pattern: "difference"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Close identity-alias cleanup and one-way reconciliation gaps.

Purpose: Enforce D-06, D-09, D-10, and D-16 so txid/wtxid aliases cannot leave stale peer cursor state and audit reports detect omissions as well as extras.
Output: Identity-complete teardown facts, symmetric peer/unbroadcast reconciliation, and adversarial alias regressions with parity breadcrumbs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-03-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-05-SUMMARY.md

<interfaces>
`PeerTransactionIdentity` remains the canonical txid+wtxid pair. Reconciliation remains
read-only and bounded: it audits authoritative state and never becomes a normal-path
rebuild or mutation mechanism per D-10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry actual orphan identities through cursor teardown</name>
  <read-first>
    <file>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs</file>
    <file>packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/reconciliation.rs</file>
    <file>docs/parity/source-breadcrumbs.json</file>
    <file>scripts/check-parity-breadcrumbs.ts</file>
  </read-first>
  <files>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/reconciliation.rs, packages/open-bitcoin-network/src/peer/tests/transaction_lifecycle_cases/identity_aliases.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - "When teardown names a txid with a wtxid alias different from the stored orphan, removal uses the stored orphan's actual wtxid for cursor cleanup."
    - "All orphan, request-tracker, and inflight cursor entries for both canonical and alias identity are absent after apply."
    - "Peer reconciliation reports either a stale txid record or stale wtxid cursor and is clean after exact teardown."
  </behavior>
  <action>
    During preparation, resolve each removed orphan to its stored complete
    `PeerTransactionIdentity` before constructing cursor teardown operations. Carry
    those actual removed wtxids in the prepared plan; do not reconstruct them from the
    incoming canonical teardown list. Make reconciliation compare expected and actual
    identity domains in both txid and wtxid directions, including request and inflight
    cursors. Add `identity_aliases.rs` with the required inline parity breadcrumb block,
    register it in `docs/parity/source-breadcrumbs.json`, and wire it from the existing
    test module. Cover same txid/different wtxid, multiple aliases, and clean
    reconciliation using explicit Arrange/Act/Assert tests.
  </action>
  <acceptance-criteria>
    <criterion>No orphan cursor cleanup derives its only wtxid set from canonical teardown inputs.</criterion>
    <criterion>Alias regressions prove the stored orphan wtxid and every related cursor are removed.</criterion>
    <criterion>The new Rust path has matching inline and registry parity breadcrumbs before verification.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-18-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-18-01-peer-identity -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features transaction_lifecycle_cases -- --nocapture &amp;&amp; bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <done>Peer lifecycle teardown and reconciliation use actual complete orphan identity, and alias cases leave no stale cursor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make unbroadcast reconciliation symmetric</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection/reconciliation.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/reconciliation.rs</files>
  <behavior>
    - "An unexpected actual unbroadcast member increments mismatch count."
    - "A retry-eligible canonical member missing from actual unbroadcast state increments mismatch count."
    - "One-for-one replacement with equal cardinality but different identities reports two directional mismatches."
    - "A clean projection reports zero mismatches."
  </behavior>
  <action>
    Derive the expected unbroadcast identity set from canonical member metadata, then
    count the symmetric difference between expected and actual sets. Keep this audit
    deterministic, bounded, and read-only per D-10; do not repair or rebuild
    `unbroadcast_members` in reconciliation. Add exact test-only corruption helpers only
    where already permitted, and regress missing-only, extra-only, swapped/equal-count,
    and clean states.
  </action>
  <acceptance-criteria>
    <criterion>Unbroadcast mismatch accounting examines `expected - actual` and `actual - expected`.</criterion>
    <criterion>Missing-only and equal-cardinality swapped corruption fail reconciliation.</criterion>
    <criterion>Normal lifecycle application remains the only production mutation path.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-18-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-18-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-18-02-reconciliation -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features lifecycle_projection_cases::reconciliation -- --nocapture &amp;&amp; bun scripts/check-parity-breadcrumbs.ts</automated>
  </verify>
  <done>Unbroadcast and peer identity audits detect both missing and unexpected state without mutating normal runtime state.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Canonical lifecycle delta to peer caches | One transaction may be addressed by txid while cursor state is keyed by a distinct wtxid. |
| Authoritative mempool to reconciliation views | Audit compares separately maintained bounded caches with canonical membership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-18-01 | Tampering | Orphan/request cursors | mitigate | Carry the stored complete identity into teardown and test txid/wtxid alias removal. |
| T-134-18-02 | Information Disclosure | Peer cursor retention | mitigate | Remove stale request/inflight metadata for the actual wtxid when the orphan leaves lifecycle state. |
| T-134-18-03 | Repudiation | Reconciliation report | mitigate | Count both directional set differences and cover equal-cardinality swaps. |
| T-134-18-04 | Elevation of Privilege | Reconciliation helper | mitigate | Keep the production reconciler read-only; mutation helpers remain test-scoped. |
</threat-model>

<verification>
Run focused peer lifecycle and node reconciliation suites, validate source breadcrumbs, then execute the required Rust pre-commit sequence and Bright Builds checks.
</verification>

<success-criteria>
- Alias teardown leaves no txid- or wtxid-keyed peer residue.
- Peer and unbroadcast reconciliation are symmetric and read-only.
- Every new Rust source/test path has an auditable parity breadcrumb.
- MPLIFE-02 and MPLIFE-03 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-18-SUMMARY.md`.
</output>
