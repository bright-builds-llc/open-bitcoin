---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "12"
type: execute
wave: 12
depends_on: ["134-11"]
files_modified:
  - scripts/check-phase134-apply-boundaries.ts
  - scripts/check-phase134-authoritative-lifecycle.ts
  - scripts/check-phase134-authoritative-lifecycle.test.ts
  - scripts/verify.sh
autonomous: true
requirements: [MPLIFE-01, MPLIFE-02, MPLIFE-03, MPLIFE-04]
must_haves:
  truths:
    - "Default verification rejects authority bypass, target omission, second owners, I/O under lock, replay/stale bugs, and deferred claims"
    - "Each checker contract is independently mutated in a fresh fixture and fails for its exact diagnostic"
    - "The apply-boundary checker is mutation-tested and remains body-scoped"
  artifacts:
    - path: scripts/check-phase134-authoritative-lifecycle.ts
      provides: "Fail-closed Phase 134 lifecycle checker"
      contains: MPLIFE-04
    - path: scripts/check-phase134-authoritative-lifecycle.test.ts
      provides: "Independent fresh-fixture mutations"
      contains: io_under_lock
  key_links:
    - from: scripts/verify.sh
      to: scripts/check-phase134-authoritative-lifecycle.test.ts
      via: "mutation suite runs before live checker"
      pattern: check-phase134-authoritative-lifecycle
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Enforce every Phase 134 authority, projection, effect, scenario, and scope contract in default verification.

Purpose: Make architectural drift fail closed.
Output: Body-scoped apply guard, independently mutation-tested lifecycle checker, and verifier wiring.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-11-SUMMARY.md
@scripts/check-phase133-package-aware-download-orphan-bridge.ts
@scripts/check-phase133-package-aware-download-orphan-bridge.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and independently mutation-test the fail-closed checker</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-11-SUMMARY.md`
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards/core/code-shape.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `standards/languages/typescript-javascript.md`
    - `scripts/check-phase133-package-aware-download-orphan-bridge.ts`
    - `scripts/check-phase133-package-aware-download-orphan-bridge.test.ts`
    - `packages/open-bitcoin-node/src/network/runtime_authority.rs`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `scripts/check-phase134-apply-boundaries.ts`
    - Confirm this new path does not yet exist: `scripts/check-phase134-authoritative-lifecycle.ts`
    - Confirm this new path does not yet exist: `scripts/check-phase134-authoritative-lifecycle.test.ts`
  </read-first>
  <files>scripts/check-phase134-apply-boundaries.ts, scripts/check-phase134-authoritative-lifecycle.ts, scripts/check-phase134-authoritative-lifecycle.test.ts</files>
  <behavior>
    - Checker requires sole authority/shared dispatcher, all closed targets, final membership/order, bounded receipts, three outcomes, successful prefixes, all scenarios, and deferred scope
    - Fresh fixture mutations independently remove/bypass each contract and assert exact diagnostic
    - Apply-body mutation proves `?`/derivation in apply is rejected while fallible preparation is allowed
  </behavior>
  <action>
    Implement D-17 and D-18 enforcement. Parse exact owned files and stable diagnostic codes. Independently mutate: direct adapter mutation; dispatcher bypass; each target omitted from construction/apply/reconcile/assertions; second mempool/unbroadcast/generation owner; socket/Fjall/write/await under authority; Clone receipt; missing epoch/generation/effect/session; unbounded ledger; stale-clear; successful-prefix loss; each missing scenario; normal-path reconciliation; high-cardinality evidence; apply-body derivation; and each Phase 135–138/broad relay claim. One fresh fixture and one intended diagnostic per test.
  </action>
  <pre-commit>
    MANDATORY after task-specific TypeScript verification and before its task commit:
    1. `bun test scripts/check-phase134-authoritative-lifecycle.test.ts`
    2. `bun run scripts/check-phase134-authoritative-lifecycle.ts`
    3. `bun run scripts/check-phase134-apply-boundaries.ts`
    4. `bun run scripts/command-timings.ts run --key phase134-12-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    5. `bun run scripts/command-timings.ts run --key phase134-12-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    6. `bun run scripts/command-timings.ts run --key phase134-12-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    7. `bun run scripts/command-timings.ts run --key phase134-12-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    8. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun test scripts/check-phase134-authoritative-lifecycle.test.ts &amp;&amp; bun run scripts/check-phase134-authoritative-lifecycle.ts &amp;&amp; bun run scripts/check-phase134-apply-boundaries.ts</automated>
  </verify>
  <acceptance-criteria>
    - The live checker emits stable diagnostics for sole authority/dispatcher, every closed target, final-membership/order, bounded receipt identities/outcomes, successful-prefix truth, all eleven scenarios, and D-18 deferred claims.
    - Each listed mutation runs in a fresh fixture and fails for its one intended diagnostic; no test combines mutations or passes because an earlier unrelated invariant failed.
    - Apply-boundary mutations prove each allowlisted target apply rejects `Result`/`?`/derivation/I/O while `validate_prepared_lifecycle` may be fallible and aggregate `apply_prepared_lifecycle` remains unit/no-`?`; the checker never mutates live source.
  </acceptance-criteria>
  <done>The checker demonstrably rejects every prohibited architecture/scope mutation it claims to enforce.</done>
</task>

<task type="auto">
  <name>Task 2: Wire checker tests and live checks into default verification</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-11-SUMMARY.md`
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards/core/verification.md`
    - `scripts/verify.sh`
    - `scripts/check-phase133-package-aware-download-orphan-bridge.ts`
    - `scripts/check-phase133-package-aware-download-orphan-bridge.test.ts`
    - `scripts/check-phase134-apply-boundaries.ts`
    - `scripts/check-phase134-authoritative-lifecycle.ts`
    - `scripts/check-phase134-authoritative-lifecycle.test.ts`
  </read-first>
  <files>scripts/verify.sh</files>
  <action>
    Add the apply-boundary checker, then the Phase 134 mutation suite, then the live Phase 134 checker immediately after Phase 133 in both `VERIFY_COMMAND_ORDER` and executable order. Keep Bash thin and fail-fast; do not suppress errors.
  </action>
  <pre-commit>
    MANDATORY after task-specific shell/checker verification and before its task commit:
    1. `bash -n scripts/verify.sh`
    2. `bun test scripts/check-phase134-authoritative-lifecycle.test.ts`
    3. `bun run scripts/check-phase134-authoritative-lifecycle.ts`
    4. `bun run scripts/check-phase134-apply-boundaries.ts`
    5. `bun run scripts/command-timings.ts run --key phase134-12-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    6. `bun run scripts/command-timings.ts run --key phase134-12-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    7. `bun run scripts/command-timings.ts run --key phase134-12-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    8. `bun run scripts/command-timings.ts run --key phase134-12-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    9. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun test scripts/check-phase134-authoritative-lifecycle.test.ts &amp;&amp; bun run scripts/check-phase134-authoritative-lifecycle.ts &amp;&amp; bash -n scripts/verify.sh</automated>
  </verify>
  <acceptance-criteria>
    - `VERIFY_COMMAND_ORDER` and executable flow both place apply-boundary, Phase 134 mutation suite, and live Phase 134 checker immediately after Phase 133 in the same order.
    - Removing or reordering any one of the three Phase 134 entries makes the verifier-order assertion fail; `bash -n scripts/verify.sh` succeeds.
    - The shell remains thin/fail-fast with no `|| true`, swallowed status, inline foreign-language program, or duplicate implementation of checker logic.
  </acceptance-criteria>
  <done>Default verification cannot silently lose the Phase 134 enforcement surface.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Mutable source/docs → checker | Drift must produce stable failure |
| Checker → default verifier | Wiring removal must be visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-12-A | Elevation of Privilege | authority bypass | mitigate | Independent direct/bypass/second-owner mutations |
| T-134-12-B | Tampering | checker false confidence | mitigate | One fresh mutation per contract and exact diagnostic |
| T-134-12-C | Denial of Service | checker | mitigate | Filesystem-only bounded fixture parsing |
</threat-model>

<verification>
Run mutation/live/apply checks, Bright checker, shell syntax, and `git diff --check`.
</verification>

<success-criteria>
- D-17 has independent mutation evidence.
- D-18 deferred boundaries fail closed.
- Default verifier runs all Phase 134 enforcement.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-12-SUMMARY.md`.
</output>
