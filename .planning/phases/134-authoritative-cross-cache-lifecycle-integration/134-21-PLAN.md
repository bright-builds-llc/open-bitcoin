---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "21"
type: execute
wave: 18
depends_on: ["134-17", "134-20"]
files_modified:
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/admission.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-01, MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "The aggregate's only fallible commit step occurs before compact, serving, fanout, peer, unbroadcast, persistence, or evidence mutation."
    - "A stale core revision leaves all eight authoritative/core and dependent target snapshots unchanged."
    - "After a successful core commit, all seven dependent prepared applies remain infallible and complete."
    - "The live node migrates to `Mempool::commit_prepared_mempool_transition`, and the temporary `ValidatedMempoolTransition` type plus validate/apply methods are removed in that same task and green commit."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      provides: "Core-first fallible aggregate commit followed by closed infallible target applies"
    - path: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      provides: "Typed stale error propagation through the single lifecycle dispatcher"
    - path: packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/admission.rs
      provides: "Stale aggregate no-mutation and successful complete-projection regressions"
    - path: packages/open-bitcoin-mempool/src/lib.rs
      provides: "Public exports with the obsolete validated transition surface removed only after node migration"
  key_links:
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      to: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      via: "atomic core commit is the first operation in aggregate apply"
      pattern: "commit_prepared_mempool_transition"
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs
      via: "dispatcher propagates stale before returning a command result"
      pattern: "apply_prepared_lifecycle"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Integrate atomic mempool commit into the sole lifecycle authority.

Purpose: Close WR-04 across the aggregate boundary while preserving D-01 through D-05, mandatory workspace gates, and the closed seven-target projection.
Output: A live node migrated to the atomic core commit, compatibility API removal in the same green task, typed dispatcher propagation, and complete cross-cache no-mutation/success regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-VERIFICATION.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-17-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-20-SUMMARY.md

<interfaces>
Plan 20 provides
`Mempool::commit_prepared_mempool_transition(&amp;mut self,
PreparedMempoolTransition) -&gt; Result&lt;MempoolLifecycleDelta,
MempoolError&gt;` while temporarily retaining `ValidatedMempoolTransition`,
`validate_prepared_mempool_transition`, and
`apply_validated_mempool_transition` for the live node. `LifecycleProjectionPlan`
continues to own prepared core facts and all seven prepared dependent projections. The
handle remains the sole `LifecycleCommand` authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the live aggregate and remove compatibility in one green commit</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-mempool/src/lib.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs</file>
    <file>.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-20-SUMMARY.md</file>
  </read-first>
  <files>packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs, packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</files>
  <behavior>
    - "Authority epoch and all target preparation checks finish before aggregate apply."
    - "Aggregate apply calls atomic core commit first; stale returns an error before any target apply."
    - "After core success, compact, serving, fanout, peer, unbroadcast, persistence, and evidence apply exactly once without failure."
    - "The lifecycle dispatcher returns the typed stale error through the existing handle facade."
    - "After the node consumer is migrated, `ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, and `apply_validated_mempool_transition` are absent from crate exports and production source."
  </behavior>
  <action>
    Store `PreparedMempoolTransition` rather than `ValidatedMempoolTransition` in the
    sealed projection. Keep authority epoch and every dependent capacity/fallibility
    check in preparation. At aggregate apply, call the exact Plan 20 method
    `Mempool::commit_prepared_mempool_transition` as the first mutating operation and
    return its stale error immediately; only after success call each of the seven
    target applies in the existing mandatory order, with no rollback or further
    fallibility. Update `LifecycleProjectionError` and the single dispatcher path to
    propagate the stale error. Once every node import and call site uses the atomic
    method, remove `ValidatedMempoolTransition`,
    `validate_prepared_mempool_transition`, and
    `apply_validated_mempool_transition` from `prepared_lifecycle.rs`, `pool.rs`, and
    `lib.rs` in this same task. Run the full ordered workspace gates before committing
    so there is no intermediate green commit or plan state where the old API is absent
    while the live consumer still depends on it. Do not introduce a second authority
    or expose a direct aggregate mutation entry point.
  </action>
  <acceptance-criteria>
    <criterion>The core atomic commit syntactically and behaviorally precedes every dependent target apply.</criterion>
    <criterion>No dependent apply returns `Result` or participates in rollback.</criterion>
    <criterion>The sole lifecycle dispatcher propagates stale without direct facade mutation.</criterion>
    <criterion>The same task/commit that migrates `open-bitcoin-node` removes all production/export occurrences of `ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, and `apply_validated_mempool_transition`, and all four ordered workspace gates pass.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-21-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-21-01-authority-commit -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool -p open-bitcoin-node --all-features lifecycle_projection_cases -- --nocapture</automated>
  </verify>
  <done>The node uses the exact atomic commit, the obsolete validated API is removed in the same green task, and the aggregate has one fallible core-first boundary followed by complete infallible projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove stale aggregate failure is cross-cache atomic</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/admission.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/admission.rs</files>
  <behavior>
    - "Prepare lifecycle plan N, commit a newer mempool transition N+1, then apply N: stale error and full aggregate snapshot unchanged from N+1."
    - "The unchanged snapshot covers core, compact, serving, fanout, peer, unbroadcast, persistence, and evidence state."
    - "A fresh plan still applies all targets and reconciles cleanly."
  </behavior>
  <action>
    Add an adversarial test hook that advances only the core revision after a sealed
    plan is prepared, then dispatch that stale plan through the same production
    authority path. Compare a complete eight-domain snapshot before and after the
    expected error. Add the paired fresh success control and assert complete projection
    plus clean reconciliation. Keep test mutation helpers test-scoped and do not use
    reconciliation to repair state.
  </action>
  <acceptance-criteria>
    <criterion>Stale aggregate apply has complete no-mutation assertions across all eight domains.</criterion>
    <criterion>Fresh aggregate apply still projects every closed target exactly once.</criterion>
    <criterion>Reconciliation is used only as a read-only postcondition oracle.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-21-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-21-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-21-02-stale-aggregate -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features stale -- --nocapture</automated>
  </verify>
  <done>Stale aggregate work is rejected before any core or cache mutation, and current work still projects all closed targets.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Core mempool to dependent lifecycle targets | Stale core failure must not leave partial projection. |
| Lifecycle facade to serialized aggregate | Error propagation must stay on the sole command authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-21-01 | Tampering | Aggregate projection | mitigate | Commit core first and return stale before every dependent mutation. |
| T-134-21-02 | Elevation of Privilege | Lifecycle dispatcher | mitigate | Keep the handle command path sole; add no direct mutation facade. |
| T-134-21-03 | Repudiation | Partial-state failure | mitigate | Compare all eight domains and require clean fresh reconciliation. |
| T-134-21-04 | Denial of Service | Workspace compatibility | mitigate | Add atomic API in Plan 20, then migrate the live consumer and remove compatibility together under all mandatory workspace gates. |
</threat-model>

<verification>
Run focused aggregate lifecycle tests, then the required Rust pre-commit sequence in order and Bright Builds checks.
</verification>

<success-criteria>
- Stale core work fails before all mutation.
- Successful work applies every dependent target infallibly.
- The live node migration and obsolete validated API removal land in the same green task/commit.
- The handle remains the sole lifecycle dispatcher.
- MPLIFE-01 through MPLIFE-03 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-21-SUMMARY.md`.
</output>
