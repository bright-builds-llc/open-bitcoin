---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "04"
type: execute
wave: 4
depends_on: ["134-03"]
files_modified:
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/inventory.rs
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/network/relay_fanout.rs
  - packages/open-bitcoin-node/src/network/compact_receive_candidates.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_target_cases.rs
  - scripts/check-phase134-apply-boundaries.ts
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "Compact, serving, inventory, fanout, and peer targets prepare all fallible work before mutation"
    - "Only final-present members enter accepted state and every final-absent member removes both aliases"
    - "Focused structural inspection proves only the exact allowlisted target-apply bodies are free of derivation, decoding, and Result propagation"
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      provides: "Prepared target construction from authoritative final membership"
      contains: PreparedServingProjection
    - path: scripts/check-phase134-apply-boundaries.ts
      provides: "Brace-balanced inspection limited to exact infallible target-apply bodies"
      contains: apply_prepared
  key_links:
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      to: packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs
      via: "closed plan contains the prepared peer operation from Plan 03"
      pattern: PreparedPeerTransactionLifecycle
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Implement the compact, serving, inventory, fanout, and peer target preparations as infallible apply operations.

Purpose: Prevent partial cross-cache state after the core transition commits.
Output: Checked target constructors, unit-returning applies, focused behavior tests, and a body-scoped structural guard.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-02-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-03-SUMMARY.md

<interfaces>
```rust
struct LifecycleProjectionPlan {
    authority_epoch: AuthorityEpoch,
    core: PreparedMempoolTransition,
    compact: PreparedCompactProjection,
    serving: PreparedServingProjection,
    fanout: PreparedFanoutProjection,
    peers: PreparedPeerLifecycleProjection,
    unbroadcast: PreparedUnbroadcastProjection,
    persistence: PreparedPersistenceProjection,
    evidence: PreparedLifecycleEvidence,
}
fn apply_prepared_compact(&mut self, prepared: PreparedCompactProjection);
fn apply_prepared_serving(&mut self, prepared: PreparedServingProjection);
fn apply_prepared_fanout(&mut self, prepared: PreparedFanoutProjection);
fn apply_prepared_peer_lifecycle(&mut self, prepared: PreparedPeerLifecycleProjection);
// Plan 05 extends the checker allowlist with its infallible target applies:
fn apply_prepared_unbroadcast(&mut self, prepared: PreparedUnbroadcastProjection);
fn apply_prepared_persistence(&mut self, prepared: PreparedPersistenceProjection);
fn apply_prepared_evidence(&mut self, prepared: PreparedLifecycleEvidence);
```
The Plan 04 checker initially allowlists only the target-specific infallible apply functions implemented in this plan. Plan 05 extends that exact allowlist with its unbroadcast, persistence, and evidence target applies. Plan 05 separately validates authority epoch plus core revision into a `ValidatedLifecycleProjection`; its aggregate `apply_prepared_lifecycle` consumes that capability and is not discovered by the target-function matcher.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prepare and apply compact, serving, inventory, fanout, and peer targets</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-02-SUMMARY.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-03-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/network/inventory.rs`
    - `packages/open-bitcoin-node/src/network/relay_serving.rs`
    - `packages/open-bitcoin-node/src/network/relay_fanout.rs`
    - `packages/open-bitcoin-node/src/network/compact_receive_candidates.rs`
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/inventory.rs, packages/open-bitcoin-node/src/network/relay_serving.rs, packages/open-bitcoin-node/src/network/relay_fanout.rs, packages/open-bitcoin-node/src/network/compact_receive_candidates.rs</files>
  <behavior>
    - Identity/body derivation, final-membership filtering, and cap checks occur in preparation
    - Replacement bodies enter bounded compact extras before accepted serving retirement
    - Apply methods return unit and consume only owned validated operations
  </behavior>
  <action>
    Implement D-05 through D-09 for these targets. Build from `PreparedLifecycleFacts` and `final_membership`, never attempt-level outcomes. Preserve parent-first relay work and descendant-first identity-complete teardown. Include Plan 03's peer capability as the mandatory `peers` field. Application may allocate only from already bounded owned vectors and must not derive identifiers, decode bodies, perform I/O, or return an error.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-04-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-04-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-04-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-04-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-target-operations -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_target_cases</automated>
  </verify>
  <acceptance-criteria>
    - Preparation produces mandatory compact, serving, fanout, and peer operations from `PreparedLifecycleFacts::final_membership`, with parent-first admits and descendant-first dual-identity removals.
    - Exact target functions `apply_prepared_compact`, `apply_prepared_serving`, `apply_prepared_fanout`, and `apply_prepared_peer_lifecycle` consume bounded owned values and return unit; Plan 05 invokes them in that order after the core transition.
    - Target apply bodies contain no `Result`, `?`, identifier/body derivation, encode/decode, allocation from unbounded input, I/O, or aggregate authority guard; replacement compact retention occurs before serving retirement.
  </acceptance-criteria>
  <done>All cache targets accept complete prevalidated plans and cannot partially fail after core commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove apply bodies are structurally infallible without false positives</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-03-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/code-shape.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `packages/open-bitcoin-node/src/network/tests.rs`
    - `packages/open-bitcoin-node/src/network/inventory.rs`
    - `packages/open-bitcoin-node/src/network/relay_serving.rs`
    - `packages/open-bitcoin-node/src/network/relay_fanout.rs`
    - `packages/open-bitcoin-node/src/network/compact_receive_candidates.rs`
    - `scripts/check-phase133-package-aware-download-orphan-bridge.ts`
    - `docs/parity/source-breadcrumbs.json`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_target_cases.rs`
    - Confirm this new path does not yet exist: `scripts/check-phase134-apply-boundaries.ts`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_target_cases.rs, scripts/check-phase134-apply-boundaries.ts, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Focused tests prove no state changes on each preparation error
    - The checker extracts only an exact allowlist of brace-balanced target apply bodies
    - `?`, txid/wtxid derivation, encode, decode, and `Result` remain allowed in preparation/validation but rejected inside allowlisted target apply bodies
    - The matcher does not classify Plan 05 `validate_prepared_lifecycle` or aggregate `apply_prepared_lifecycle` as a target apply
  </behavior>
  <action>
    Replace the former broad file-level negative `rg` with a Bun checker that parses exact target files and matches only the explicit target-function allowlist from Task 1. Extract each balanced body and reject `?`, txid/wtxid derivation, `encode`, `decode`, I/O, or `Result` returns only within those target bodies. Explicitly exclude the Plan 05 fallible `validate_prepared_lifecycle` guard and the aggregate unit-returning `apply_prepared_lifecycle` from target discovery so validation semantics cannot conflict with the target rule. Add behavior tests for final-present filtering, alias removal, replacement ordering, and unchanged state on identity/body/final-membership/bound errors. Plan 12 will mutation-test this guard. Register the new Rust test breadcrumb.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-04-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-04-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-04-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-04-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/check-phase134-apply-boundaries.ts &amp;&amp; bun run scripts/command-timings.ts run --key phase134-target-atomicity -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_target_cases</automated>
  </verify>
  <acceptance-criteria>
    - The checker fails independently when any allowlisted target apply gains `Result`, `?`, txid/wtxid derivation, encode/decode, or I/O and passes equivalent fallible work inside `validate_prepared_lifecycle`.
    - Checker discovery output names exactly the four Plan 04 target apply functions (and, after Plan 05, the three explicit Plan 05 target applies) and does not include `validate_prepared_lifecycle` or aggregate `apply_prepared_lifecycle`.
    - Focused cases prove all preparation failures preserve the complete target snapshot; the new Rust test has a Knots breadcrumb and the TypeScript checker adds no source mutation or broad file-level regex.
  </acceptance-criteria>
  <done>Infallible application is proven without scanning intentionally fallible preparation code.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Prepared target → cache | Only validated bounded data may cross into mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-04-A | Tampering | partial projection | mitigate | Every fallible operation occurs in preparation and apply returns unit |
| T-134-04-B | Tampering | post-trim admission | mitigate | Final membership is the only admission/removal selector |
| T-134-04-C | Denial of Service | body-scoped checker | mitigate | Exact files/functions and balanced parsing avoid broad false positives |
</threat-model>

<verification>
Run focused Rust cases, body-scoped checker, mandatory Rust gates, breadcrumbs, and `git diff --check`.
</verification>

<success-criteria>
- D-05 through D-09 target operations are complete and infallible.
- The negative structural check inspects only apply bodies.
- The copied closed-plan interface includes `authority_epoch`.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-04-SUMMARY.md`.
</output>
