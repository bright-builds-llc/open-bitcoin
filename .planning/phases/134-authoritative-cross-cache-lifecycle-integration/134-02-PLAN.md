---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "02"
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPLIFE-01, MPLIFE-02]
must_haves:
  truths:
    - "One LifecycleCommand family names every lifecycle mutation, effect preparation, relay preparation, and receipt completion path"
    - "LifecycleProjectionPlan has mandatory authority_epoch plus every closed projection target"
    - "The contract preserves the sole Arc<Mutex<ManagedPeerNetwork> authority and contains no I/O"
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      provides: "Typed lifecycle command, closed plan, checked epochs/generations, and private constructors"
      contains: LifecycleCommand
  key_links:
    - from: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      to: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      via: "closed plan owns the core capability and reads validated facts"
      pattern: PreparedMempoolTransition
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Define the single typed command and closed projection contract that every later lifecycle and effect path must use.

Purpose: Make omissions and alternate mutation paths difficult to represent before cache implementations are changed.
Output: Authority/generation newtypes, exhaustive LifecycleCommand, closed plan fields, and checked constructors.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-01-SUMMARY.md
@packages/open-bitcoin-node/src/network/runtime_authority.rs

<interfaces>
```rust
struct LifecycleProjectionPlan {
    authority_epoch: AuthorityEpoch,
    core: PreparedMempoolTransition,
    compact: PreparedCompactProjection,
    serving: PreparedServingProjection,
    fanout: PreparedFanoutProjection,
    peers: PreparedPeerLifecycleProjection,
    unbroadcast: PreparedUnbroadcastProjection,
    persistence: PreparedPersistenceProjection,
    evidence: PreparedLifecycleEvidence,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exhaustive lifecycle and effect command variants</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-01-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/network/runtime_authority.rs`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-mempool/src/pool.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection.rs</files>
  <behavior>
    - Variants cover singleton/package admission, pressure/expiry, connected block, reorg step, maintenance, snapshot preparation, relay preparation, peer completion, and snapshot completion
    - AuthorityEpoch and LifecycleGeneration are non-interchangeable checked newtypes
    - Lifecycle facts, prepared projection, and owned effects remain distinct types
  </behavior>
  <action>
    Implement D-01 and D-04. Define the exhaustive `LifecycleCommand` family now, including typed effect prepare/complete variants that Plan 08 will implement through the same dispatcher. Do not create separate effect mutation entrypoints. Use private payload constructors, no callbacks, no generic heterogeneous batch, and no I/O imports.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-02-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-02-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-02-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-02-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-command-contract -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection::command_contract</automated>
  </verify>
  <acceptance-criteria>
    - `LifecycleCommand` contains distinct variants for singleton/package admission, pressure, expiry, block, reorg step, maintenance, snapshot/relay preparation, and peer/snapshot completion.
    - `AuthorityEpoch` and `LifecycleGeneration` are non-interchangeable newtypes, and lifecycle facts, projection plans, owned effects, and receipts remain distinct concrete types.
    - The new module contains no `std::fs`, Fjall, socket, async runtime, callback, actor/mailbox, or heterogeneous effect-batch dependency.
  </acceptance-criteria>
  <done>Every D-01 path has one typed variant in the shared command family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the closed authority-bound projection plan</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-01-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-node/src/mempool.rs`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-node/src/network/runtime_authority.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `docs/parity/source-breadcrumbs.json`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/mempool.rs, packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - The plan requires authority_epoch and all seven named fields covering the eight target families
    - No target is Option, callback-driven, or externally forgeable
    - Existing Arc/Mutex ownership and narrow caller APIs remain intact
  </behavior>
  <action>
    Implement D-02, D-03, D-05, and D-06 as compile-time contracts. Add checked private constructors and preparation errors, register the module and breadcrumbs, and expose thin `ManagedMempool` preparation accessors. Add construction/privacy tests. Do not route production callers yet, mutate caches, add actors, or perform effects.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-02-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-02-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-02-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-02-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-closed-plan -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection::closed_plan</automated>
  </verify>
  <acceptance-criteria>
    - Construction tests require `authority_epoch`, core, compact, serving, fanout, peers, unbroadcast, persistence, and evidence fields with no `Option` target or externally public constructor.
    - `ManagedMempool` exposes only thin preparation accessors; `ManagedNetworkHandle` retains the existing `Arc&lt;Mutex&lt;ManagedPeerNetwork&gt;&gt;` ownership and no production caller is routed in this task.
    - The module is registered and breadcrumbed; no target mutation, effect execution, second dispatcher, actor, or I/O appears.
  </acceptance-criteria>
  <done>The authoritative plan cannot omit a target or bypass the shared typed command vocabulary.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Typed command → authority | All canonical, projection, and completion mutations must enter one dispatcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-02-A | Elevation of Privilege | alternate mutation path | mitigate | Exhaustive private command family and no public aggregate mutation |
| T-134-02-B | Tampering | omitted target | mitigate | Concrete mandatory plan fields include authority_epoch and every projection family |
| T-134-02-C | Denial of Service | lock boundary | mitigate | Contract imports no socket, Fjall, async, or actor APIs |
</threat-model>

<verification>
Run focused contract tests, both mandatory Rust commit gates, breadcrumbs, and `git diff --check`.
</verification>

<success-criteria>
- D-01 through D-06 are represented in types.
- `authority_epoch` is part of every copied downstream plan interface.
- Plans 03 onward extend this contract rather than introducing another dispatcher.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-02-SUMMARY.md`.
</output>
