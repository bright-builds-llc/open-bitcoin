---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "20"
type: execute
wave: 14
depends_on: ["134-13"]
files_modified:
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs
  - packages/open-bitcoin-mempool/src/pool/admission.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/expiry.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "The new `Mempool::commit_prepared_mempool_transition` API rechecks revision and performs core mutation in one consuming mutable call."
    - "A prepared transition committed through the new API cannot overwrite a newer mempool revision."
    - "Existing `ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, and `apply_validated_mempool_transition` remain temporarily available so the still-unmigrated live node consumer and mandatory workspace gates stay green."
    - "All mempool-owned compatibility facades use the new atomic commit before the live node consumer migration."
  artifacts:
    - path: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      provides: "Atomic revision-check-and-commit contract"
    - path: packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs
      provides: "Stale commit and no-mutation regressions"
    - path: packages/open-bitcoin-mempool/src/lib.rs
      provides: "Temporary compatibility exports plus the new atomic commit surface"
  key_links:
    - from: packages/open-bitcoin-mempool/src/pool/admission.rs
      to: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      via: "facades consume prepared work through the atomic commit API"
      pattern: "commit_prepared_mempool_transition"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Add the atomic mempool commit contract without breaking its live node consumer.

Purpose: Establish the D-05-safe `Mempool::commit_prepared_mempool_transition` contract and migrate mempool-owned facades while keeping the legacy validated transition API as a short-lived compatibility bridge until Plan 21 migrates the node and removes it in one green commit.
Output: One atomic prepared-transition commit contract, green compatibility exports, migrated mempool facades, and no-mutation stale regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-VERIFICATION.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-01-SUMMARY.md

<interfaces>
`PreparedMempoolTransition` continues to expose immutable `PreparedLifecycleFacts`.
The new `Mempool::commit_prepared_mempool_transition` operation owns the prepared
transition and one `&amp;mut Mempool`; it returns stale before mutation or commits
without yielding control. Plan 20 MUST retain the existing validated type and methods
because `open-bitcoin-node` still imports them until Plan 21.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add atomic prepared commit beside the compatibility API</name>
  <read-first>
    <file>packages/open-bitcoin-mempool/src/lib.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs</file>
  </read-first>
  <files>packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs</files>
  <behavior>
    - "Prepare N, commit another transition N+1, then commit N: N returns `StalePreparedTransition`."
    - "After stale N, revision, entries, indexes, fees, resource ledger, and lifecycle facts exactly equal the N+1 snapshot."
    - "A current prepared transition commits once and returns its exact lifecycle delta."
    - "The workspace still compiles with the node's existing `ValidatedMempoolTransition` consumer."
  </behavior>
  <action>
    Add the exact public method
    `Mempool::commit_prepared_mempool_transition(&amp;mut self, prepared:
    PreparedMempoolTransition) -&gt; Result&lt;MempoolLifecycleDelta,
    MempoolError&gt;`. It checks the prepared base revision using the same mutable
    borrow that applies the patch; mismatch returns `StalePreparedTransition` before
    any field changes, and a match keeps post-check core apply infallible and returns
    the exact committed delta. Do NOT remove or change the visibility of
    `ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, or
    `apply_validated_mempool_transition` in this plan: the live node consumer still
    requires them and every task's mandatory workspace gates must pass. Mark the
    compatibility surface for removal by Plan 21 only if an existing local convention
    supports deprecation without warnings; do not introduce warnings. Extend the
    prepared lifecycle suite with complete-state snapshots for stale-after-newer-
    commit, current commit, no-op, and replay ownership behavior.
  </action>
  <acceptance-criteria>
    <criterion>`prepared_lifecycle.rs` contains the exact `commit_prepared_mempool_transition` method with one mutable revision-check-and-apply boundary.</criterion>
    <criterion>`ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, and `apply_validated_mempool_transition` remain exported and the full workspace gates pass in Plan 20.</criterion>
    <criterion>Revision comparison and mutation share one `&amp;mut Mempool` call.</criterion>
    <criterion>Stale tests compare the complete observable mempool state before and after failure.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-20-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-20-01-atomic-mempool-commit -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-features prepared_lifecycle_cases -- --nocapture</automated>
  </verify>
  <done>The atomic commit API rejects stale work before mutation while the temporary validated compatibility surface keeps the unmigrated node and workspace green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate every mempool facade to the atomic commit contract</name>
  <read-first>
    <file>packages/open-bitcoin-mempool/src/pool/admission.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/package_admission.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/expiry.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/lifecycle.rs</file>
  </read-first>
  <files>packages/open-bitcoin-mempool/src/pool/admission.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/expiry.rs, packages/open-bitcoin-mempool/src/pool/lifecycle.rs</files>
  <behavior>
    - "Singleton admission, package admission, expiry, pressure, block, and reorg paths commit through the same atomic operation."
    - "Facade result/delta vocabulary is unchanged for current prepared work."
    - "Stale errors propagate rather than being swallowed or converted to a partial success."
  </behavior>
  <action>
    Replace each internal validate/apply pair with the new consuming atomic commit.
    Preserve existing result projection and lifecycle delta semantics, including empty
    noncommitting attempts. Propagate stale errors using the existing `MempoolError`;
    do not use `unwrap`, rollback, or duplicate patch-apply logic in the facades.
    Update nearby behavioral tests only where signatures require it and keep all
    production mutation routed through the single contract.
  </action>
  <acceptance-criteria>
    <criterion>No mempool facade calls a removed validate/apply pair.</criterion>
    <criterion>Every lifecycle family propagates atomic commit failure before mutation.</criterion>
    <criterion>Existing admission, package, expiry, pressure, block, and reorg behavior tests pass unchanged in meaning.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-20-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-20-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-20-02-mempool-facades -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-features</automated>
  </verify>
  <done>All mempool lifecycle families use the atomic revision-check-and-commit path and preserve their externally observable results.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Prepared facts to mutable mempool | The mempool revision may advance after preparation and before commit. |
| Public crate API to callers | An exported owned capability could otherwise be held across newer mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-20-01 | Tampering | Prepared transition commit | mitigate | Recheck revision and apply under one mutable borrow; stale input exits before mutation. |
| T-134-20-02 | Elevation of Privilege | Temporary public validated capability | mitigate | Keep compatibility only until Plan 21 migrates the live node and removes the old surface in the same green commit. |
| T-134-20-03 | Repudiation | Stale failure state | mitigate | Compare complete observable snapshots and exact stale diagnostics in tests. |
</threat-model>

<verification>
Run focused prepared lifecycle and full mempool crate tests, then the required Rust pre-commit sequence in order and Bright Builds checks.
</verification>

<success-criteria>
- Stale prepared work cannot overwrite newer state.
- The exact atomic API exists and all mempool-owned lifecycle facades share it.
- The legacy validated API remains available only to keep the unmigrated node and Plan 20 workspace gates green; Plan 21 owns migration plus removal.
- MPLIFE-02 and MPLIFE-03 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-20-SUMMARY.md`.
</output>
