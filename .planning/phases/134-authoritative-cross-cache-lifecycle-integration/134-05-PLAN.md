---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "05"
type: execute
wave: 5
depends_on: ["134-04"]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs
  - scripts/check-phase134-apply-boundaries.ts
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPLIFE-01, MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "ManagedPeerNetwork alone owns authority epoch, lifecycle/dirty generation, unbroadcast membership, and bounded evidence"
    - "apply_lifecycle_command is the sole handle mutex dispatcher; validate_prepared_lifecycle checks epoch/revision before mutation and apply_prepared_lifecycle consumes only its validated capability"
    - "Reconciliation reports each closed target but is audit/startup/test-only"
  artifacts:
    - path: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      provides: "Single typed lifecycle dispatcher"
      contains: apply_lifecycle_command
    - path: packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs
      provides: "Shared complete projection assertion root"
      contains: assert_complete_projection
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      via: "every LifecycleCommand prepares/applies through one mutex closure"
      pattern: apply_prepared_lifecycle
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Own aggregate generations/evidence and implement the sole authority dispatcher plus audit reconciliation.

Purpose: Complete the one-authority in-memory reducer before routing production callers.
Output: Aggregate owner fields, infallible all-target apply, typed dispatcher, and bounded reconciliation/report fixtures.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-04-SUMMARY.md

<interfaces>
```rust
fn apply_lifecycle_command(&self, command: LifecycleCommand) -> Result<LifecycleCommandResult, LifecycleProjectionError>;
fn validate_prepared_lifecycle(
    &self,
    plan: LifecycleProjectionPlan,
) -> Result<ValidatedLifecycleProjection, LifecycleProjectionError>;
fn apply_prepared_lifecycle(
    &mut self,
    validated: ValidatedLifecycleProjection,
);
struct LifecycleProjectionPlan {
    authority_epoch: AuthorityEpoch,
    core: PreparedMempoolTransition,
    compact: PreparedCompactProjection,
    serving: PreparedServingProjection,
    fanout: PreparedFanoutProjection,
    peers: PreparedPeerLifecycleProjection,
    unbroadcast: PreparedUnbroadcastProjection,
    persistence: PreparedPersistenceProjection,
    evidence: PreparedLifecycleEvidence,
}
```
`validate_prepared_lifecycle` is the only fallible authority-epoch/core-revision guard and returns a non-forgeable validated capability. Aggregate `apply_prepared_lifecycle` returns unit, contains no `?`, and delegates only to the allowlisted infallible target applies from Plan 04.
Its exact aggregate order is core transition, compact, serving, fanout, peers, unbroadcast, persistence, then evidence. Compact precedes serving so replacement bodies are retained before accepted-serving retirement can discard their final authoritative copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement aggregate ownership and sole command application</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-04-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-node/src/network/runtime_authority.rs`
    - `packages/open-bitcoin-mempool/src/pool.rs`
    - `scripts/check-phase134-apply-boundaries.ts`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/bitcoin-knots/src/txmempool.cpp`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, scripts/check-phase134-apply-boundaries.ts</files>
  <behavior>
    - Non-empty committed delta advances lifecycle and dirty generation once; empty delta advances neither
    - Unbroadcast admits only final-present Local + Requested entries and clears exact typed identities
    - `validate_prepared_lifecycle` checks authority epoch and core revision and returns a non-forgeable validated capability before mutation
    - `apply_prepared_lifecycle` consumes only that capability, returns unit, contains no `?`, and applies core transition, compact, serving, fanout, peers, unbroadcast, persistence, then evidence
  </behavior>
  <action>
    Implement D-01, D-02, D-03, D-05, D-06, and D-07. Put authority epoch, generations, bounded unbroadcast, and fixed low-cardinality evidence on `ManagedPeerNetwork`. Implement the sole private `apply_lifecycle_command` mutex closure. Split its lifecycle path into fallible `validate_prepared_lifecycle`, which checks authority epoch and mempool revision, invokes the core revision validator, and returns a private non-forgeable `ValidatedLifecycleProjection` containing `ValidatedMempoolTransition`, followed by unit-returning aggregate `apply_prepared_lifecycle`. The aggregate body must contain no `Result` or `?` and must call `apply_validated_mempool_transition` then delegate only to explicit infallible target applies in this exact order: compact, serving, fanout, peers, unbroadcast, persistence, evidence. Compact must retain replacement bodies before serving retirement so a removed transaction body needed for replacement compact reconstruction cannot be lost. Add the last three exact target names to the Plan 04 checker allowlist without broadening its matcher; continue to exclude validation and aggregate apply. Retain effect variants as typed dispatch arms for Plan 08; do not add a separate effects mutation method.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-05-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-05-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-05-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-05-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/check-phase134-apply-boundaries.ts &amp;&amp; bun run scripts/command-timings.ts run --key phase134-authority-apply -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_cases::authority</automated>
  </verify>
  <acceptance-criteria>
    - `validate_prepared_lifecycle` returns `Result&lt;ValidatedLifecycleProjection, LifecycleProjectionError&gt;` and tests prove stale authority epoch or mempool revision leaves canonical and every dependent target unchanged.
    - `apply_prepared_lifecycle` accepts only `ValidatedLifecycleProjection`, returns `()`, contains no `?`/`Result`, calls `apply_validated_mempool_transition`, then calls `apply_prepared_compact`, `apply_prepared_serving`, `apply_prepared_fanout`, `apply_prepared_peer_lifecycle`, `apply_prepared_unbroadcast`, `apply_prepared_persistence`, and `apply_prepared_evidence` exactly once in that order.
    - Replacement compact retention is observable before serving retirement, so a removed transaction body needed for replacement reconstruction remains available.
    - Non-empty delta advances lifecycle/dirty generation once; empty delta advances neither; unbroadcast admits only final-present Local/Requested members and no adapter-visible second authority/effect mutation entrypoint exists.
  </acceptance-criteria>
  <done>All mutable lifecycle truth has one owner and one typed apply dispatcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded reconciliation and complete assertion fixtures</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-04-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `packages/open-bitcoin-node/src/network/tests.rs`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `docs/parity/source-breadcrumbs.json`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/tests.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Reconciliation names serving, fanout, peer, compact, unbroadcast, persistence, and evidence mismatches
    - Production reports only fixed target labels and bounded counts; exact IDs are test-only
    - Normal apply never invokes reconciliation or rebuilds a cache
  </behavior>
  <action>
    Implement D-10. Add explicit audit/startup/test reconciliation and a shared `ExpectedProjection`/`assert_complete_projection` requiring all targets and generation fields. Add authority/no-op baselines and exact cfg(test) mismatch inspection. Register the new Rust test path. Prohibit calls from admission, pressure, expiry, block, reorg, or effect completion.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-05-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-05-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-05-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-05-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-reconciliation-foundation -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_cases</automated>
  </verify>
  <acceptance-criteria>
    - `ExpectedProjection` and `assert_complete_projection` require canonical membership plus serving, fanout, peer, compact, unbroadcast, persistence, evidence, authority epoch, and lifecycle/dirty generation.
    - Reconciliation reports fixed target labels/counts in production and exact IDs only under `cfg(test)`; deliberate per-target divergence is observable.
    - `rg "reconcile_lifecycle_projection" packages/open-bitcoin-node/src/network` finds only audit/startup/test entrypoints and no admission, pressure, expiry, block, reorg, aggregate apply, or effect-completion call.
  </acceptance-criteria>
  <done>The audit oracle detects every target divergence without becoming a normal-path repair.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| LifecycleCommand → Arc/Mutex authority | Typed input reaches the only mutable aggregate |
| Canonical mempool → audit report | Read-only comparison must not become repair |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-05-A | Elevation of Privilege | aggregate authority | mitigate | Private sole dispatcher and no adapter mutable access |
| T-134-05-B | Tampering | generation/unbroadcast | mitigate | Exact final-membership and non-empty-delta rules are in closed plan |
| T-134-05-C | Information Disclosure | reconciliation/evidence | mitigate | Production emits fixed labels/counts; identities are cfg(test) |
</threat-model>

<verification>
Run authority/reconciliation tests, mandatory Rust gates, breadcrumbs, body-scoped apply checker, and `git diff --check`.
</verification>

<success-criteria>
- D-01, D-02, D-03, D-06, D-07, and D-10 are implemented.
- The effect command variants remain in the same dispatcher for Plan 08.
- Normal paths remain incremental and I/O-free under lock.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-05-SUMMARY.md`.
</output>
