---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "09"
type: execute
wave: 9
depends_on: ["134-08"]
files_modified:
  - packages/open-bitcoin-node/src/network/announcement_transport.rs
  - packages/open-bitcoin-node/src/network/block_relay_evidence.rs
  - packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/sync/session.rs
  - packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
autonomous: true
requirements: [MPLIFE-04]
must_haves:
  truths:
    - "Peer receipt exists only after its exact encode/write succeeds"
    - "Node sync and RPC inbound executors complete every successful prefix before reporting a later failure"
    - "RPC verification covers injected successful-prefix failures, not only node transport tests"
  artifacts:
    - path: packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
      provides: "Outside-lock RPC peer executor with immediate per-success completion"
      contains: acknowledge_write
  key_links:
    - from: packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      via: "each successful write yields and completes one receipt"
      pattern: complete_peer_emission
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Execute peer effects outside authority and preserve successful-prefix truth in node and RPC shells.

Purpose: Credit only achieved writes and prove both production executor surfaces.
Output: Owned emission write capability, node/sync executor updates, RPC executor updates, and injected prefix-failure tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/operability.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind peer emissions to exact achieved writes</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/operability.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/network/announcement_transport.rs`
    - `packages/open-bitcoin-node/src/network/block_relay_evidence.rs`
    - `packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/announcement_transport.rs, packages/open-bitcoin-node/src/network/block_relay_evidence.rs, packages/open-bitcoin-node/src/network/tests/announcement_transport_cases.rs</files>
  <behavior>
    - Prepared emission owns wire message and non-Clone capability, never a receipt
    - Encode/send succeeds before acknowledge and immediate completion
    - Failure/skip/disconnect gives no credit to failed or unsent suffix
  </behavior>
  <action>
    Implement the D-11 through D-14 peer capability boundary. Bind capability to epoch, lifecycle generation, effect ID, peer ID, and peer-session generation. Only consuming `acknowledge_write` after success may create a receipt; update block-relay completion and focused capability/current-stale-duplicate tests.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-09-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-09-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-09-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-09-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-peer-node-executor -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node announcement_transport_cases -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    - Prepared peer emission owns its wire payload and non-`Clone` success capability but no receipt; only consuming `acknowledge_write` after exact encode/write success creates the typed receipt.
    - Capability and receipt carry epoch, lifecycle generation, effect ID, peer ID, and peer-session generation; current/stale/duplicate tests return the exact completion outcome.
    - Encode/send/skip/disconnect failure creates no receipt or achieved credit for that command or unsent suffix, and block-relay evidence advances only after successful completion.
  </acceptance-criteria>
  <done>Peer emissions can prove only their exact achieved write and cannot pre-create receipts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve successful prefixes in the node sync shell</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/operability.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-node/src/sync/session.rs`
    - `packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs`
    - `packages/open-bitcoin-node/src/network/announcement_transport.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/sync/session.rs, packages/open-bitcoin-node/src/sync/tests/production_announcement_transport_cases.rs</files>
  <behavior>
    - Node sync executes, acknowledges, and completes one emission before the next
    - A later encode/send/disconnect failure leaves earlier completions intact
    - Failed and unsent suffix commands receive no achieved credit
  </behavior>
  <action>
    Apply D-14 to the production sync session loop. Execute ordered commands outside authority; after each successful send acknowledge and dispatch completion before advancing. Add encode, queue, socket, disconnect/reconnect stale, duplicate, and 3-command second-failure cases in the sync/effect suites.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-09-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-09-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-09-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-09-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-peer-sync-executor -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node production_announcement_transport_cases -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    - Production sync processes each command in prepare-outside-lock write-acknowledge-complete order before advancing to the next command.
    - A deterministic three-command second-failure case proves command 1 completes once, command 2 gets no credit, command 3 remains unsent, and the returned failure remains exact.
    - Encode, queue, socket, disconnect/reconnect-stale, and duplicate branches preserve earlier successful completions; no socket write or await occurs inside the authority closure.
  </acceptance-criteria>
  <done>The node sync shell credits exactly the achieved peer-write prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update and independently verify the RPC inbound executor</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/operability.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-node/src/network/announcement_transport.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
  </read-first>
  <files>packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <behavior>
    - RPC executor uses the same write-acknowledge-complete order
    - Injected second-command failure completes command 1 once and commands 2-3 never
    - Encoding, rejection, disconnect, and write failure branches retain successful prefixes
  </behavior>
  <action>
    Apply the D-14 executor contract to `connection_runtime.rs` and add focused inbound-listener tests with an injected writer/encoder failure at each boundary. Verify outcomes and completion calls directly; do not rely on the open-bitcoin-node announcement suite as coverage for RPC.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-09-03-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-09-03-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-09-03-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-09-03-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-rpc-successful-prefix -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    - RPC inbound execution uses the same per-command write-acknowledge-complete sequence and invokes only the shared authority completion facade.
    - Injected failure at command 2 proves command 1 completes exactly once and commands 2-3 receive no achieved credit; separate encode, rejection, disconnect, and write failures retain any earlier prefix.
    - Coverage lives in `open-bitcoin-rpc` inbound-listener tests rather than relying on node tests, and source contains no lock-held write/await or alternate completion mutation.
  </acceptance-criteria>
  <done>The RPC production executor has its own timed successful-prefix regression proof.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Owned emission → socket/session | External effect may partially succeed |
| Write success → receipt completion | Achieved truth must be recorded exactly once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-09-A | Tampering | successful prefix | mitigate | Immediate per-success completion before later failure return |
| T-134-09-B | Spoofing | reconnect receipt | mitigate | Peer-session generation makes older capability stale |
| T-134-09-C | Denial of Service | I/O under lock | mitigate | Both node and RPC executors run after owned commands leave authority |
</threat-model>

<verification>
Run timed node and RPC suites sequentially, both mandatory Rust gates, and `git diff --check`.
</verification>

<success-criteria>
- D-11 through D-14 peer behavior is proven in node and RPC.
- RPC successful-prefix regression is directly tested.
- No failed or unsent command receives achieved credit.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-09-SUMMARY.md`.
</output>
