---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "14"
type: execute
wave: 14
depends_on: ["134-13"]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/relay_serving.rs
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-04]
must_haves:
  truths:
    - "Every live ManagedNetworkHandle has a distinct non-default authority incarnation, so an effect receipt from another handle cannot match locally."
    - "Pending peer and snapshot effects are keyed and consumed by their complete family-specific binding rather than by a reusable numeric ID."
    - "A foreign or binding-mismatched receipt leaves pending and completed ledgers plus every lifecycle target unchanged."
    - "An exact achieved receipt atomically leaves pending accounting, enters completed accounting once, and returns Applied or AchievedButStale according to current freshness without clearing newer state."
    - "An exact replay returns AlreadyApplied and performs no second lifecycle mutation."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      provides: "Exact peer/snapshot effect keys and ledgers with atomic validate-and-take semantics"
    - path: packages/open-bitcoin-node/src/network.rs
      provides: "Per-handle authority incarnation allocation"
    - path: packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
      provides: "Two-handle and exact-binding regression coverage"
  key_links:
    - from: packages/open-bitcoin-node/src/network/relay_serving.rs
      to: packages/open-bitcoin-node/src/network.rs
      via: "handle construction installs a unique authority epoch"
      pattern: "authority_epoch"
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      via: "completion atomically moves an exact achieved binding from pending to completed before classifying current freshness"
      pattern: "(complete|take|record).*pending"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Close the receipt-authority and exact-consumption portion of the Phase 134 effect gap.

Purpose: Preserve D-11 through D-15 by making an affine effect capability valid only for the exact authority incarnation, lifecycle generation, family identity, session or persistence generation, and effect ID that reserved it.
Output: Unique handle incarnations, exact family-specific pending keys, atomic completion primitives, and adversarial two-handle regression tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md

<interfaces>
Use the existing family-specific `PeerEffectCapability`/`PeerEffectReceipt` and
`SnapshotEffectCapability`/`SnapshotEffectReceipt` fields as the complete binding.
Do not replace them with a generic receipt or a second authority. The public handle
continues to serialize commands through the existing `Arc<Mutex<...>>` authority
per D-01 through D-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make pending effect identity authority-incarnation complete</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-node/src/network/relay_serving.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/relay_serving.rs, packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "Two independently constructed handles reserve receipts with different non-initial authority incarnations even when their local numeric effect IDs match."
    - "Peer pending identity includes authority epoch, lifecycle generation, effect ID, peer ID, and the peer-session generation."
    - "Snapshot pending identity includes authority epoch, lifecycle generation, effect ID, persistence generation, and snapshot identity."
    - "A ledger can validate and consume only an exact pending binding; foreign and binding-mismatched receipts leave pending and completed accounting unchanged."
    - "Completed accounting retains an exact achieved binding so replay can return `AlreadyApplied` without relying on a raw numeric ID."
  </behavior>
  <action>
    Replace the constant `AuthorityEpoch::INITIAL` construction path with a process-local,
    monotonic, overflow-checked authority-incarnation allocator owned by the handle
    construction boundary. Define family-specific exact pending key types derived from
    the full receipt binding and key both pending and completed accounting by those
    types. Provide ledger operations that atomically take an exact pending key and
    record the same exact key in bounded completed accounting; do not expose a raw-ID
    removal sequence. Preserve the family split,
    bounded ledgers, affine capability conversion, and explicit targets per D-06,
    D-11, D-12, and D-15. Do not add persistence, a generic effect bus, or a second
    state authority. Add Arrange/Act/Assert tests for same numeric IDs across two
    handles, cross-handle peer and snapshot receipt rejection, mismatched binding
    rejection, one exact pending-to-completed move, and duplicate classification.
  </action>
  <acceptance-criteria>
    <criterion>No production constructor assigns `AuthorityEpoch::INITIAL` to a live handle.</criterion>
    <criterion>No pending or completed ledger mutation is keyed solely by `PeerEffectId` or `SnapshotEffectId`.</criterion>
    <criterion>Foreign or binding-mismatched receipt tests assert all relevant pending, completed, peer, evidence, unbroadcast, and dirty-state snapshots are unchanged.</criterion>
    <criterion>An exact achieved binding can move from pending to completed exactly once even when later freshness checks classify it stale.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-14-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-14-01-node-effect-contracts -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Each family has exact, incarnation-bound pending identity and adversarial ledger tests prove that raw ID collisions cannot consume another reservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record exact achieved truth before freshness-sensitive lifecycle mutation</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "Completion validates the entire receipt against the pending reservation before removing anything."
    - "A foreign authority incarnation or immutable binding mismatch cannot remove pending work, enter completed accounting, or mutate any lifecycle target."
    - "An exact first achieved receipt atomically leaves pending and enters completed accounting before freshness is classified."
    - "An exact current receipt returns `Applied`; an exact receipt made stale by a newer lifecycle, persistence, unbroadcast, or target-peer session returns `AchievedButStale` while preserving that newer state."
    - "An exact replay is classified `AlreadyApplied` from completed accounting without reapplying state."
  </behavior>
  <action>
    Refactor the authority completion path into two explicit classifications. First,
    derive the complete family binding and reject a foreign authority incarnation or
    any receipt whose immutable binding does not match an exact pending or completed
    key; these inputs leave both ledgers and all targets unchanged. Second, for an
    exact pending achieved receipt, atomically remove the pending key and record that
    exact key in bounded completed accounting. Only after achieved truth is recorded,
    compare the receipt's lifecycle/persistence generation and target-peer session with
    current state: return `Applied` and perform the current consequence when fresh, or
    return `AchievedButStale` without clearing or overwriting newer dirty,
    unbroadcast, peer-session, provenance, or evidence state. An exact completed replay
    returns `AlreadyApplied`. Keep the D-13 vocabulary and do not classify a foreign or
    malformed binding as achieved. Add separate Arrange/Act/Assert regressions for
    foreign authority, every immutable binding mismatch, exact-current, exact-stale
    after each freshness dimension advances, and exact replay.
  </action>
  <acceptance-criteria>
    <criterion>Authority completion contains no raw effect-ID removal and no ledger mutation before full immutable binding validation.</criterion>
    <criterion>An exact achieved receipt transitions pending to completed once before returning `Applied` or `AchievedButStale`.</criterion>
    <criterion>Exact-stale tests prove pending decreases, completed increases once, and newer dirty/unbroadcast/peer-session/provenance/evidence state is unchanged.</criterion>
    <criterion>Foreign and immutable-mismatch tests prove pending, completed, and every lifecycle target are unchanged.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-14-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-14-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-14-02-node-effects -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Foreign bindings are no-ops, exact achieved bindings are accounted once, freshness controls only the current-state consequence, and stale completion cannot clear newer work.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Effect executor to lifecycle authority | An asynchronously returned receipt crosses back into serialized authoritative state. |
| One handle incarnation to another | Process-local handles may reuse local counters but must never share receipt authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-14-01 | Spoofing | Peer/snapshot receipt completion | mitigate | Bind exact pending keys to a unique authority incarnation and all family fields before atomic consumption. |
| T-134-14-02 | Tampering | Snapshot dirty state | mitigate | Permit clearing only after consuming the exact snapshot reservation; assert rejected receipts preserve state. |
| T-134-14-03 | Repudiation | Completed-effect accounting | mitigate | Atomically move an exact achieved key from pending to completed before freshness classification; never credit foreign or mismatched bindings. |
| T-134-14-04 | Denial of Service | Bounded pending/completed ledgers | mitigate | Preserve configured bounds and checked incarnation/effect counters; fail visibly instead of wrapping identifiers. |
</threat-model>

<verification>
Run the focused effect contract suite, then the required Rust pre-commit sequence in order and the Bright Builds checks. Review the diff for any second authority, generic effect family, raw-ID pending removal, or deferred durable outbox.
</verification>

<success-criteria>
- Two live handles cannot accept each other's receipts even when local effect IDs collide.
- Every exact achieved completion moves the exact family-specific key from pending to completed atomically, whether fresh or stale.
- Foreign and binding-mismatched receipts preserve pending, completed, peer, evidence, unbroadcast, provenance, and snapshot dirty state.
- Exact stale completions preserve newer state while returning `AchievedButStale`; exact replays return `AlreadyApplied`.
- MPLIFE-04 remains pending in `.planning/REQUIREMENTS.md` until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-14-SUMMARY.md`.
</output>
