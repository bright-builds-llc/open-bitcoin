---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "08"
type: execute
wave: 8
depends_on: ["134-07"]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/lifecycle_projection.rs
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPLIFE-01, MPLIFE-04]
must_haves:
  truths:
    - "Peer relay and snapshot prepare/complete are variants of the same LifecycleCommand and enter apply_lifecycle_command"
    - "Family receipts are non-Clone and bind epoch, generation, effect ID, and peer-session or snapshot identity"
    - "Completion is Applied, AchievedButStale, or AlreadyApplied with fixed family caps and bounded ledgers"
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      provides: "Family-specific commands, capabilities, receipts, IDs, caps, and ledgers"
      contains: EffectCompletion
    - path: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      provides: "Effect command arms in the shared authoritative dispatcher"
      contains: PrepareSnapshot
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_projection.rs
      via: "thin public facades construct LifecycleCommand and call apply_lifecycle_command"
      pattern: apply_lifecycle_command
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Implement both effect-family contracts through the same typed authoritative lifecycle dispatcher.

Purpose: Satisfy D-01 and D-11 through D-15 before any socket or Fjall executor changes.
Output: Exact caps, owned commands, non-replayable receipts, bounded ledgers, and shared command prepare/complete arms.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/operability.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-07-SUMMARY.md

<interfaces>
```rust
pub enum EffectCompletion { Applied, AchievedButStale, AlreadyApplied }
```
Peer caps: prepared/completed `PHASE94_MAX_PEER_QUEUED_MESSAGES` (128).
Snapshot caps: one pending write, two completed effect IDs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define family identities, exact caps, capabilities, receipts, and ledgers</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-07-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/operability.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/network.rs`
    - `packages/open-bitcoin-node/src/network/announcement_transport.rs`
    - `packages/open-bitcoin-network/src/resource.rs`
    - `packages/open-bitcoin-node/src/sync/session.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/bitcoin-knots/src/node/mempool_persist.cpp`
    - `docs/parity/source-breadcrumbs.json`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/lifecycle_effects.rs`
    - `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Peer and snapshot types cannot cross-complete
    - Receipts are created only from consuming success capabilities
    - Oldest completed effect evicts deterministically at cap+1
  </behavior>
  <action>
    Implement D-11 through D-15 contracts. Add distinct IDs/session/snapshot types, non-Clone capabilities and receipts, private deque+set ledgers, and exact resolved constants: peer batch/completion 128, pending snapshots 1, completed snapshots 2. Store the bounded ledgers and peer-session generation on `ManagedPeerNetwork`; no journal, outbox, trait-object bus, or durable history.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-08-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-08-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-08-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-08-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-effect-types -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_cases::effects::contracts</automated>
  </verify>
  <acceptance-criteria>
    - Peer and snapshot effect IDs/capabilities/receipts are distinct non-`Clone` types binding authority epoch, lifecycle/persistence generation, effect ID, and peer-session or snapshot identity.
    - Tests prove peer prepared/completed caps are 128, snapshot pending cap is 1, snapshot completion ledger cap is 2, and cap+1 evicts the oldest completed ID deterministically.
    - `ManagedPeerNetwork` owns only bounded deque+set family ledgers; source contains no generic effect bus, durable journal/outbox/history, schema change, or cross-family completion conversion.
  </acceptance-criteria>
  <done>Both families have bounded, identity-complete, non-replayable effect capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route effect preparation and completion through apply_lifecycle_command</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-07-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/operability.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-node/src/network/runtime_authority.rs`
    - `packages/open-bitcoin-node/src/network/announcement_transport.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/bitcoin-knots/src/node/mempool_persist.cpp`
    - `packages/open-bitcoin-node/src/network/lifecycle_projection.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-node/src/network/runtime_authority/effects.rs`
    - `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_projection.rs, packages/open-bitcoin-node/src/network/runtime_authority.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</files>
  <behavior>
    - Relay/snapshot prepare and peer/snapshot complete variants all use the sole dispatcher
    - Duplicate checks precede freshness checks; stale success clears no newer state
    - Public effect methods are thin facades, not independent mutation closures
  </behavior>
  <action>
    Extend the Plan 02 `LifecycleCommand`/Plan 05 dispatcher with complete peer relay preparation, snapshot preparation, peer completion, and snapshot completion arms. `runtime_authority/effects.rs` may expose ergonomic methods only by constructing a command and calling `apply_lifecycle_command`; it must not call `mutate`/`try_mutate` itself. Record achieved effect before freshness checks and preserve newer dirty, unbroadcast, and peer provenance on stale completion. Test current, stale, duplicate, wrong-family, cap, and ledger eviction cases.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-08-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-08-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-08-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-08-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-effect-dispatch -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_cases::effects::completion</automated>
  </verify>
  <acceptance-criteria>
    - Relay/snapshot prepare and peer/snapshot complete facades each construct a `LifecycleCommand` and call `apply_lifecycle_command`; `runtime_authority/effects.rs` contains no direct `mutate`/`try_mutate` call.
    - Completion checks duplicate before freshness and returns exact `Applied`, `AchievedButStale`, or `AlreadyApplied`; stale success records achieved truth but preserves newer dirty/unbroadcast/peer provenance.
    - Current, stale epoch/generation/session, duplicate, wrong-family, cap, and ledger-eviction tests pass; no I/O, generic batch, or unbounded ledger appears under authority.
  </acceptance-criteria>
  <done>Snapshot preparation, relay preparation, and both receipt completions use the same authoritative command path.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authority → owned effect | Bounded command leaves lock without external I/O |
| Successful effect → completion command | Receipt may be stale or duplicated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-08-A | Spoofing | stale/cross-family receipt | mitigate | Family newtypes bind epoch, generation, effect, and session/snapshot identity |
| T-134-08-B | Tampering | alternate completion path | mitigate | All prepare/complete facades call the sole lifecycle dispatcher |
| T-134-08-C | Denial of Service | effects/ledger | mitigate | Exact 128/1/2 caps and deterministic bounded eviction |
</threat-model>

<verification>
Run contract/completion suites, mandatory Rust gates, breadcrumbs, apply-boundary checker, and `git diff --check`.
</verification>

<success-criteria>
- D-01 and D-11 through D-15 are represented and tested.
- `runtime_authority/effects.rs` contains no independent aggregate mutation method.
- No I/O occurs under the authority lock.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`.
</output>
