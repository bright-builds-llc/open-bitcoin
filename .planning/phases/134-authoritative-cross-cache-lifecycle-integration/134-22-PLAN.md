---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "22"
type: execute
wave: 19
depends_on: ["134-17", "134-19", "134-21"]
files_modified:
  - scripts/check-phase134-apply-boundaries.ts
  - scripts/check-phase134-authoritative-lifecycle.test.ts
  - scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts
autonomous: true
gap_closure: true
requirements: [MPLIFE-01, MPLIFE-02, MPLIFE-03, MPLIFE-04]
must_haves:
  truths:
    - "The apply-boundary guard follows repo-owned helper calls or rejects unclassified helpers, so forbidden mutation or I/O cannot hide one call below a checked apply function."
    - "The only fallible aggregate commit exception is the exact `Mempool::commit_prepared_mempool_transition` call introduced by Plans 20-21, and it must occur before all seven dependent applies."
    - "All other accepted callees are named in explicit pure or infallible-apply allowlists; unknown repo-owned helpers fail closed."
  artifacts:
    - path: scripts/check-phase134-apply-boundaries.ts
      provides: "Fail-closed transitive apply-helper analysis"
    - path: scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts
      provides: "Indirect helper-bypass mutation tests"
  key_links:
    - from: scripts/check-phase134-apply-boundaries.ts
      to: scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts
      via: "independent nested-helper, unknown-helper, and cycle mutations"
      pattern: "apply-helpers"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Turn the Phase 134 apply checker into a fail-closed transitive boundary.

Purpose: Close WR-09 and enforce D-05 and D-17 after the production repairs converge.
Output: Transitive helper analysis, exact atomic-core and explicit pure/infallible-call allowlists, and independently mutated guard tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-17-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-19-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-20-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-21-SUMMARY.md

<interfaces>
The guard remains deterministic and filesystem-only. It may use balanced lexical parsing
and a repo-owned call graph, but it must not depend on network access, generated build
artifacts, or compiler internals. The aggregate's fallible atomic core commit introduced
by Plans 20-21 is exactly `Mempool::commit_prepared_mempool_transition` and is
permitted only as the first mutating operation before all dependent target applies;
helper traversal must still prove that target applies contain no hidden I/O or
fallible mutation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make apply-boundary checking transitive and fail closed</name>
  <read-first>
    <file>scripts/check-phase134-apply-boundaries.ts</file>
    <file>scripts/check-phase134-authoritative-lifecycle.test.ts</file>
    <file>scripts/source-corpus.ts</file>
    <file>packages/open-bitcoin-node/src/network/lifecycle_projection/authority.rs</file>
    <file>packages/open-bitcoin-node/src/network/compact_receive_candidates.rs</file>
    <file>packages/open-bitcoin-node/src/network/inventory.rs</file>
    <file>packages/open-bitcoin-node/src/network/relay_fanout.rs</file>
    <file>packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/transaction_lifecycle.rs</file>
    <file>packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs</file>
  </read-first>
  <files>scripts/check-phase134-apply-boundaries.ts, scripts/check-phase134-authoritative-lifecycle.test.ts, scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts</files>
  <behavior>
    - "A forbidden direct mutation moved into a one-level or multi-level repo-owned helper still fails with the apply-boundary diagnostic."
    - "Filesystem/network I/O hidden in a helper called by apply fails."
    - "An unknown repo-owned helper reachable from apply fails closed unless classified."
    - "The exact first call to `Mempool::commit_prepared_mempool_transition` passes; the removed validated API names and any other fallible call fail."
    - "Known pure helpers and infallible apply callees pass only when listed by exact symbol in their respective allowlists."
    - "Recursive helper cycles terminate deterministically without suppressing violations."
  </behavior>
  <action>
    Extend balanced function extraction into a deterministic call graph over the node
    target files plus the reachable mempool and peer lifecycle source corpus. Starting
    at every required apply boundary, recursively inspect reachable repo-owned helpers,
    track visited fully qualified symbols, and reject forbidden I/O, `Result`/`?` or
    other fallibility after mutation begins, direct authority bypass, unresolved
    overloads, or unclassified repo-owned calls. Define three explicit exact-symbol
    classifications: (1) `ATOMIC_CORE_COMMIT` contains only
    `Mempool::commit_prepared_mempool_transition` and is legal exactly once as the
    first mutating statement of `ManagedPeerNetwork::apply_prepared_lifecycle`; (2)
    `INFALLIBLE_APPLY_CALLEES` contains the seven named
    `ManagedPeerNetwork::apply_prepared_{compact,serving,fanout,peer_lifecycle,unbroadcast,persistence,evidence}`
    methods plus `PeerManager::apply_prepared_transaction_lifecycle` and the exact
    peer/orphan apply helpers reached beneath it; each remains transitively inspected;
    (3) `PURE_CALL_ALLOWLIST` contains only exact standard/container operations and
    exact pure repo helpers such as `PeerTransactionIdentity::relay_ids` that the
    inspected bodies actually require. Do not broadly allow by suffix, receiver-free
    method name, or module prefix. Explicitly reject
    `ValidatedMempoolTransition`, `validate_prepared_mempool_transition`, and
    `apply_validated_mempool_transition` after Plan 21. Split helper-specific fixture
    mutations into `apply-helpers.ts`. Independently mutate a helper call plus hidden
    `std::fs::write`, forbidden target mutation, nested helper, unknown helper, removed
    validated API, atomic commit in a later position, second atomic commit, and a
    recursive cycle; include exact pure-helper and legitimate core-first controls.
  </action>
  <acceptance-criteria>
    <criterion>The checker follows every reachable repo-owned helper from all required apply targets or fails it as unclassified.</criterion>
    <criterion>`ATOMIC_CORE_COMMIT`, `INFALLIBLE_APPLY_CALLEES`, and `PURE_CALL_ALLOWLIST` contain exact fully qualified symbols; no suffix or broad prefix allowance exists.</criterion>
    <criterion>The only fallible allowance is one first-position `Mempool::commit_prepared_mempool_transition`; all removed validated API symbols fail.</criterion>
    <criterion>Independent helper-bypass mutations produce the exact apply-boundary diagnostic.</criterion>
    <criterion>The legitimate atomic core-first and infallible dependent apply sequence passes.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-22-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-22-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-22-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-22-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun test scripts/check-phase134-authoritative-lifecycle.test.ts scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts</step>
    <step>bun scripts/check-phase134-apply-boundaries.ts</step>
    <step>git diff --check</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun test scripts/check-phase134-authoritative-lifecycle.test.ts scripts/check-phase134-authoritative-lifecycle.test/apply-helpers.ts &amp;&amp; bun scripts/check-phase134-apply-boundaries.ts</automated>
  </verify>
  <done>Phase 134 apply safety cannot be bypassed by moving prohibited behavior into a reachable helper.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source code to lexical guard | Prohibited behavior may be hidden behind helper indirection or syntax variants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-22-01 | Tampering | Apply-boundary checker | mitigate | Traverse repo-owned helper calls, reject unclassified helpers, and independently mutate nested/cyclic cases. |
| T-134-22-02 | Elevation of Privilege | Atomic-core exception | mitigate | Permit only the exact Plan 20/21 commit symbol once in first position and reject removed validated APIs. |
| T-134-22-03 | Spoofing | Pure-call classification | mitigate | Use exact fully qualified pure/infallible allowlists and fail closed on unresolved or unknown repo helpers. |
| T-134-22-04 | Tampering | Transitive helper corpus | mitigate | Keep positive, negative, cycle, and unknown-helper mutations independent so fail-closed traversal cannot silently regress. |
</threat-model>

<verification>
Run the ordered Rust gates, focused helper mutations, live apply checker, diff validation, and Bright Builds checks.
</verification>

<success-criteria>
- Helper indirection cannot bypass the apply-boundary guard.
- The exact Plan 20/21 atomic core commit is the sole first-position fallible exception.
- Explicit pure and infallible-apply allowlists fail closed on every unclassified repo helper.
- MPLIFE-01 through MPLIFE-04 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-22-SUMMARY.md`.
</output>
