---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "17"
type: execute
wave: 17
depends_on: ["134-16"]
files_modified:
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-01, MPLIFE-04]
must_haves:
  truths:
    - "An exact pre-achievement snapshot abort releases its own pending reservation even when lifecycle or persistence freshness advanced after preparation."
    - "A foreign or immutable binding-mismatched snapshot abort leaves pending, completed, dirty, and persisted state unchanged."
    - "Snapshot encode or Fjall save failure terminates through exact abort, preserves dirty state and prior durable bytes, restores capacity, and permits retry."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      provides: "Exact snapshot pre-achievement abort contract"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Complete-or-abort snapshot execution shell"
    - path: packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
      provides: "Encode/save failure, freshness-advance, capacity recovery, and retry regressions"
  key_links:
    - from: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      via: "every consumed snapshot capability exits through exact completion or exact pre-achievement abort"
      pattern: "(complete|abort).*snapshot"
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      via: "snapshot abort consumes its immutable exact pending binding without requiring current freshness"
      pattern: "AbortSnapshot"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Close the snapshot reserved-effect leak as a focused terminal-capability slice.

Purpose: Guarantee that a consumed snapshot capability terminates exactly once through completion after durable achievement or abort after known pre-achievement failure, without conflating binding validity with current freshness.
Output: Exact snapshot abort commands, Fjall encode/save failure routing, capacity recovery, dirty-state preservation, and retry regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/operability.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-GAPS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-14-SUMMARY.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-16-SUMMARY.md

<interfaces>
Plan 14 defines the complete immutable snapshot binding and exact achieved
pending-to-completed move. Plan 16 establishes the pre-achievement abort rule: an
exact capability can release its own reservation after current freshness advances,
while foreign and immutable binding mismatches cannot mutate either ledger.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an exact pre-achievement snapshot abort command</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "An exact snapshot abort removes its pending reservation and enters neither completed nor persistence-success accounting."
    - "The exact abort succeeds after lifecycle generation, dirty generation, or another current freshness field advances because no external effect was achieved."
    - "Foreign authority incarnation, snapshot identity, persistence generation, or effect binding mismatch leaves pending and completed accounting unchanged."
    - "Abort never clears dirty state; replay is a typed no-op."
  </behavior>
  <action>
    Add a family-specific `AbortSnapshotEffect` lifecycle command and thin handle
    facade. Validate the complete immutable reservation binding from Plan 14, but do
    not require current lifecycle generation, dirty generation, or persistence
    freshness to match the captured values: a pre-achievement abort releases only its
    own reservation even when newer state exists. Remove that exact pending key,
    restore bounded capacity, and return a typed abort classification without adding a
    completed key, persistence evidence, or clean-state transition. Foreign authority
    or immutable binding mismatch leaves both ledgers and every snapshot/lifecycle
    target unchanged. Add Arrange/Act/Assert tests for exact-current,
    exact-after-each-freshness-advance, foreign, each immutable mismatch, replay, and
    capacity recovery.
  </action>
  <acceptance-criteria>
    <criterion>`AbortSnapshotEffect` is routed through the sole lifecycle dispatcher and consumes no raw effect ID.</criterion>
    <criterion>An exact pre-achievement abort releases its own pending key after lifecycle/dirty freshness advances and never enters completed accounting.</criterion>
    <criterion>Foreign and immutable binding mismatch tests prove pending, completed, dirty, evidence, and persisted-state snapshots are unchanged.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-17-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-17-01-snapshot-abort -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Snapshot pre-achievement abort releases only its exact reservation after any freshness advance and cannot create success or clear dirty state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminate failed Fjall snapshot execution through exact abort</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs</file>
    <file>packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - "Snapshot encoding failure aborts the exact reservation and retains dirty state."
    - "Fjall save failure aborts the exact reservation and retains the previously persisted snapshot."
    - "After either failure, pending capacity returns to baseline and a later prepare/save/complete succeeds."
    - "A successful durable save creates and completes a receipt; it never runs the abort path."
  </behavior>
  <action>
    Make the Fjall prepared snapshot executor retain ownership of the capability across
    encoding and save. Convert it to a receipt only after durable save succeeds and
    complete that achieved receipt through Plan 14's exact completion contract. On
    encode or save error, consume the still-unachieved capability through the exact
    snapshot abort facade from Task 1. Preserve the original storage error while
    propagating any abort error visibly, and never clear dirty state on abort. Extend
    injected encode/save tests to assert prior persisted bytes, pending/completed
    accounting, dirty generation, capacity restoration, and a successful later retry.
  </action>
  <acceptance-criteria>
    <criterion>Every snapshot executor exit invokes exactly one complete-after-achievement or abort-before-achievement terminal path.</criterion>
    <criterion>Encoding and save failures retain dirty state, completed accounting, and prior durable contents while releasing pending capacity.</criterion>
    <criterion>A subsequent exact snapshot write can prepare, persist, and complete after either failure.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-17-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-17-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-17-02-snapshot-failure -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features snapshot_persistence -- --nocapture</automated>
  </verify>
  <done>Failed snapshot execution aborts without false achievement, preserves dirty/durable truth, restores capacity, and permits retry.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Snapshot encoder/storage to authority | A capability leaves authority and must return through exactly one truthful terminal path. |
| Older snapshot reservation to newer dirty state | Exact ownership permits capacity release but never permits clearing newer dirty state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-17-01 | Denial of Service | Snapshot pending ledger | mitigate | Exact abort restores the sole pending slot on every known pre-achievement failure. |
| T-134-17-02 | Tampering | Dirty generation | mitigate | Abort never clears dirty state; exact stale achieved completion records truth without overwriting newer dirty state. |
| T-134-17-03 | Spoofing | Snapshot abort binding | mitigate | Validate the complete immutable binding and leave ledgers unchanged for foreign/mismatched inputs. |
| T-134-17-04 | Repudiation | Durable achievement | mitigate | Create receipts only after save success and test prior bytes plus pending/completed accounting on every failure. |
</threat-model>

<verification>
Run focused effect and snapshot-persistence tests, then the required ordered Rust gates and Bright Builds checks. Review every snapshot capability branch for exactly one terminal operation.
</verification>

<success-criteria>
- No encode/save failure leaks the snapshot pending slot.
- Exact pre-achievement abort releases its own reservation after freshness advances without entering completed accounting.
- Foreign/mismatched aborts are no-ops and snapshot failure never clears dirty state or prior durable bytes.
- MPLIFE-01 and MPLIFE-04 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-17-SUMMARY.md`.
</output>
