---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "10"
type: execute
wave: 10
depends_on: ["134-09"]
files_modified:
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs
autonomous: true
requirements: [MPLIFE-04]
must_haves:
  truths:
    - "Snapshot preparation/completion already enters the shared LifecycleCommand dispatcher from Plan 08"
    - "Fjall execution lives beside the existing save primitive and occurs after authority release"
    - "Receipt exists only after current-schema encoding/save succeeds and stale success cannot clear newer dirty state"
  artifacts:
    - path: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      provides: "Outside-lock execute_prepared_mempool_snapshot_write beside save_mempool_snapshot"
      contains: execute_prepared_mempool_snapshot_write
  key_links:
    - from: packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs
      to: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      via: "executor consumes PreparedSnapshotWrite and acknowledges only after save succeeds"
      pattern: SnapshotWriteReceipt
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Add the current-schema Fjall snapshot executor outside authority and prove truthful completion.

Purpose: Complete the snapshot effect family without implementing Phase 135 schema, cadence, coordinator, or recovery.
Output: Owned current-schema executor and success/stale/duplicate/failure persistence tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md
@packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs

<interfaces>
Plan 08 supplies `PreparedSnapshotWrite`, `SnapshotWriteReceipt`, and shared-command `prepare_mempool_snapshot_write` / `complete_snapshot_write`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the prepared current-schema snapshot beside the Fjall save primitive</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-node/src/storage/mempool_snapshot.rs`
    - `packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs`
    - `packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs`
    - `packages/bitcoin-knots/src/node/mempool_persist.cpp`
  </read-first>
  <files>packages/open-bitcoin-node/src/storage/mempool_snapshot.rs, packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - Executor consumes one prepared snapshot and uses existing codec/save path
    - Encoding/save occurs outside authority and receipt is created only after requested PersistMode succeeds
    - Encoding or storage failure produces no receipt
  </behavior>
  <action>
    Implement D-11 and D-14 snapshot execution exactly at the resolved location: `FjallNodeStore::execute_prepared_mempool_snapshot_write` in `storage/fjall_store/mempool.rs`, beside `save_mempool_snapshot`. Consume the Plan 08 command, encode/save the existing `MempoolSnapshot`, and acknowledge only after success. Do not add schema fields, timer cadence, shutdown policy, recovery, crash claims, outbox, or journal.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-10-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-10-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-10-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-10-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-snapshot-executor -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node snapshot_persistence -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    - `FjallNodeStore::execute_prepared_mempool_snapshot_write` consumes one owned Plan 08 command and uses the existing current-schema `MempoolSnapshot` encode/save path beside `save_mempool_snapshot`.
    - Receipt construction occurs only after the requested `PersistMode` succeeds; deterministic encode and storage failures return no receipt.
    - Executor source has no authority lock access, schema-field change, timer/shutdown/recovery coordinator, durable outbox/journal, or crash-durability claim.
  </acceptance-criteria>
  <done>Current-schema persistence executes outside the lock and proves only achieved saves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove generation-safe snapshot completion and failures</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-08-SUMMARY.md`
    - `AGENTS.md`
    - `standards/core/testing.md`
    - `standards/core/verification.md`
    - `packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs`
    - `packages/open-bitcoin-node/src/storage/fjall_store/mempool.rs`
    - `packages/bitcoin-knots/src/node/mempool_persist.cpp`
    - `packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs`
  </read-first>
  <files>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/storage/fjall_store/tests/snapshot_persistence.rs</files>
  <behavior>
    - Exact current receipt clears only its pending/dirty generation
    - Older successful receipt is AchievedButStale and newer dirty state survives
    - Duplicate is AlreadyApplied; cap, encode, and write failure keep state truthful
  </behavior>
  <action>
    Add deterministic temp-store tests for success, stale generation, duplicate completion, one-pending cap rejection, encoding failure, write failure, and newer dirty state surviving older success. Exercise Plan 08 public facades so prepare and completion continue to route through `LifecycleCommand` / `apply_lifecycle_command`.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-10-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-10-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-10-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-10-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-snapshot-completion-store -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node snapshot_persistence -- --test-threads=1 &amp;&amp; bun run scripts/command-timings.ts run --key phase134-snapshot-completion-authority -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node lifecycle_projection_cases::effects -- --test-threads=1</automated>
  </verify>
  <acceptance-criteria>
    - Exact current receipt returns `Applied` and clears only its matching pending/dirty generation; older successful receipt returns `AchievedButStale` and preserves newer dirty state.
    - Duplicate returns `AlreadyApplied` without mutation; one-pending cap, encode failure, and write failure preserve exact authority/persistence snapshots and create no achieved credit.
    - Tests call Plan 08 public prepare/complete facades and therefore the shared `LifecycleCommand` dispatcher; no direct authority mutation or Phase 135 behavior is added.
  </acceptance-criteria>
  <done>Snapshot completion remains truthful across current, stale, duplicate, and failed effects.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Prepared snapshot → Fjall | Owned data crosses from authority to durable I/O |
| Successful save → completion | Durable truth may be stale relative to current generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-10-A | Tampering | stale dirty clear | mitigate | Exact epoch/generation/snapshot ID guard preserves newer state |
| T-134-10-B | Denial of Service | pending writes | mitigate | One pending snapshot and two completed IDs |
| T-134-10-C | Elevation of Privilege | storage mutation | mitigate | Fjall executor cannot mutate authority; completion uses shared dispatcher |
</threat-model>

<verification>
Run timed snapshot/effect suites sequentially, both mandatory Rust gates, and `git diff --check`.
</verification>

<success-criteria>
- D-11 through D-15 snapshot behavior is complete.
- Executor location and caps match resolved research.
- Phase 135 behavior remains absent.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-10-SUMMARY.md`.
</output>
