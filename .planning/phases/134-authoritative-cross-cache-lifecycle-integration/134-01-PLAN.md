---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/admission.rs
  - packages/open-bitcoin-mempool/src/pool/package_admission.rs
  - packages/open-bitcoin-mempool/src/pool/expiry.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [MPLIFE-02, MPLIFE-03]
must_haves:
  truths:
    - "Every mempool transition family can prepare without mutation and consume one non-Clone revision-bound capability"
    - "Prepared facts expose canonical identities, bodies, final membership, admission order, and graph-derived teardown order without exposing MempoolPatch"
    - "Stale/no-op preparations fail or complete before mutation with exact revision truth"
  artifacts:
    - path: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      provides: "Sealed PreparedMempoolTransition and immutable PreparedLifecycleFacts"
      contains: PreparedMempoolTransition
    - path: packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs
      provides: "Preparation, stale, no-op, partial, and ordering proof"
      contains: sequential_reorg
  key_links:
    - from: packages/open-bitcoin-mempool/src/pool/package_admission.rs
      to: packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs
      via: "package evaluation returns the sealed prepared capability"
      pattern: "prepare_.*package"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T02:50:10Z
---

<objective>
Create the sealed, fully preflighted mempool transition boundary used by the authoritative node projector.

Purpose: Move every possible core preparation failure ahead of canonical or dependent mutation.
Output: One opaque prepared capability for admission, pressure, expiry, connected-block, and sequential reorg steps.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md
@packages/open-bitcoin-mempool/src/pool.rs
@packages/open-bitcoin-mempool/src/pool/lifecycle.rs

<interfaces>
```rust
pub struct PreparedMempoolTransition { /* private patch and validated facts */ }
impl PreparedMempoolTransition {
    pub fn facts(&self) -> &PreparedLifecycleFacts;
}
pub struct ValidatedMempoolTransition { /* private validated revision capability */ }
pub fn validate_prepared_mempool_transition(
    &self,
    prepared: PreparedMempoolTransition,
) -> Result<ValidatedMempoolTransition, MempoolError>;
pub fn apply_validated_mempool_transition(
    &mut self,
    validated: ValidatedMempoolTransition,
) -> MempoolLifecycleDelta;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose prepared singleton and package transitions</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
    - `packages/open-bitcoin-mempool/src/lib.rs`
    - `packages/open-bitcoin-mempool/src/pool.rs`
    - `packages/open-bitcoin-mempool/src/pool/patch.rs`
    - `packages/open-bitcoin-mempool/src/pool/lifecycle.rs`
    - `packages/bitcoin-knots/src/validation.cpp`
    - `docs/parity/source-breadcrumbs.json`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs`
    - Confirm this new path does not yet exist: `packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs`
  </read-first>
  <files>packages/open-bitcoin-mempool/src/lib.rs, packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs, docs/parity/source-breadcrumbs.json</files>
  <behavior>
    - Preparation changes neither revision nor full mempool state
    - Facts contain exact delta, txid/wtxid pairs, final-present bodies, removal bodies, and admitted order
    - Stale revision is rejected before mutation and no-op capability advances no revision
  </behavior>
  <action>
    Implement the D-04/D-05 capability contract. Keep `MempoolPatch` private; expose only immutable validated facts and a non-Clone consuming capability. Preflight identity/body/package/final-membership consistency, represent empty deltas explicitly, and add focused Arrange/Act/Assert tests and exact Knots breadcrumbs. Do not add rollback, a second mempool, or node cache behavior.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run these commands sequentially and do not commit unless all pass:
    1. `bun run scripts/command-timings.ts run --key phase134-01-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-01-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-01-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-01-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-prepared-admission -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prepared_lifecycle_cases</automated>
  </verify>
  <acceptance-criteria>
    - `rg "pub struct PreparedMempoolTransition|pub struct PreparedLifecycleFacts" packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs` finds sealed exported capability/fact symbols, while `rg "pub .*MempoolPatch" packages/open-bitcoin-mempool/src` finds no public patch exposure.
    - Focused tests prove preparation preserves byte-for-byte state/revision, rejects stale revision before mutation, and exposes exact txid/wtxid, body, membership, and order facts.
    - `docs/parity/source-breadcrumbs.json` contains entries for both new Rust paths anchored to `packages/bitcoin-knots/src/validation.cpp`; no node cache, rollback, second mempool, or I/O implementation is added.
  </acceptance-criteria>
  <done>Singleton and package transitions cross the crate boundary as sealed, single-use, fully validated capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route singleton and package facades through the capability</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-mempool/src/pool.rs`
    - `packages/open-bitcoin-mempool/src/pool/admission.rs`
    - `packages/open-bitcoin-mempool/src/pool/package_admission.rs`
    - `packages/open-bitcoin-mempool/src/pool/lifecycle.rs`
    - `packages/bitcoin-knots/src/validation.cpp`
    - `packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs`
    - `packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs`
  </read-first>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs, packages/open-bitcoin-mempool/src/pool/admission.rs, packages/open-bitcoin-mempool/src/pool/package_admission.rs, packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs</files>
  <behavior>
    - Singleton and package preparation changes neither revision nor mempool state
    - Compatibility submit methods prepare then consume once
    - Post-trim absence and partial-package survivor order are exact
  </behavior>
  <action>
    Complete D-04, D-05, and D-07 for admission. Split singleton/package methods into prepare plus compatibility facades and expose exact outward report/outcome facts without letting those attempt-level values drive later projection. Test stale consume, partial ordering, post-trim absence, and empty-delta no-op.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run sequentially:
    1. `bun run scripts/command-timings.ts run --key phase134-01-02-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-01-02-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-01-02-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-01-02-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-prepared-admission-facades -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prepared_lifecycle_cases</automated>
  </verify>
  <acceptance-criteria>
    - Singleton and package compatibility methods each call their prepare path and consume exactly one non-`Clone` capability; focused tests reject a second consume at compile/type boundary or by ownership.
    - Partial acceptance and post-trim cases assert that only `final_membership` survivors appear, in admitted topological order, while attempt-level `MempoolOutcome`/`SubmittedPackageResult` never selects projection facts.
    - Empty-delta and stale-consume tests assert the complete mempool snapshot and revision are unchanged; no cache projection, effect, actor, or alternate mempool owner appears in these files.
  </acceptance-criteria>
  <done>Singleton and package compatibility APIs use the same sealed prepare/consume boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend preparation to expiry, pressure, block, and reorg steps</name>
  <read-first>
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md`
    - `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-RESEARCH.md`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `packages/open-bitcoin-mempool/src/pool.rs`
    - `packages/open-bitcoin-mempool/src/pool/expiry.rs`
    - `packages/open-bitcoin-mempool/src/pool/lifecycle.rs`
    - `packages/open-bitcoin-mempool/src/pool/patch.rs`
    - `packages/bitcoin-knots/src/txmempool.cpp`
    - `packages/bitcoin-knots/src/validation.cpp`
    - `packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs`
    - `packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs`
  </read-first>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/prepared_lifecycle.rs, packages/open-bitcoin-mempool/src/pool/expiry.rs, packages/open-bitcoin-mempool/src/pool/lifecycle.rs, packages/open-bitcoin-mempool/src/pool/tests/prepared_lifecycle_cases.rs</files>
  <behavior>
    - Expiry, pressure, and block removals prepare without mutation
    - Final-present order is parent-first and teardown is graph-derived descendant-first
    - Reorg steps must be consumed sequentially and cannot be coalesced
    - Normal preparation/apply never calls a full-state oracle
  </behavior>
  <action>
    Implement D-08 and D-10 on the Task 1 capability. Add prepare/compatibility pairs for expiry and connected-block changes, carry explicit graph order independent of cause/role, and require transition N to consume before N+1 prepares. Preserve `MempoolLifecycleDelta` as the fact vocabulary and keep reconciliation test-only/audit-only.
  </action>
  <pre-commit>
    MANDATORY after this Rust-changing task and before its task commit: run these commands sequentially and do not commit unless all pass:
    1. `bun run scripts/command-timings.ts run --key phase134-01-03-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml`
    2. `bun run scripts/command-timings.ts run --key phase134-01-03-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings`
    3. `bun run scripts/command-timings.ts run --key phase134-01-03-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features`
    4. `bun run scripts/command-timings.ts run --key phase134-01-03-test -- cargo test --manifest-path packages/Cargo.toml --all-features`
    5. `bun scripts/bright-builds-check.ts all`; any failure blocks the task commit.
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-prepared-maintenance -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool prepared_lifecycle_cases</automated>
  </verify>
  <acceptance-criteria>
    - Focused cases cover expiry, pressure, connected-block confirmation/conflict, and reorg preparation without mutation, with parent-first survivor and descendant-first teardown vectors.
    - A sequence-sensitive reorg test proves step N must apply before N+1 prepares and rejects any cross-transition absent-wins coalescing.
    - `rg "reconcile|rebuild" packages/open-bitcoin-mempool/src/pool/{expiry.rs,lifecycle.rs,prepared_lifecycle.rs}` shows no normal-path full-state repair call; no snapshot schema, scheduler, or public relay scope is added.
  </acceptance-criteria>
  <done>All core lifecycle families share one ordered non-mutating preparation and consuming apply boundary.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Raw transaction/package input → prepared facts | Untrusted identities, bodies, membership, and bounds become validated domain facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-01-A | Tampering | prepared transition | mitigate | Revision, identity, membership, order, and bounds validate before mutation |
| T-134-01-B | Denial of Service | package/removal preparation | mitigate | Existing package/orphan/resource caps bound all owned vectors |
| T-134-01-C | Elevation of Privilege | patch internals | mitigate | MempoolPatch remains private and only the consuming capability crosses crates |
</threat-model>

<verification>
Run both focused suites, the mandatory per-task Rust gates, breadcrumb validation, and `git diff --check`.
</verification>

<success-criteria>
- D-04, D-05, D-07, D-08, and D-10 are compiled and tested.
- No fallible identity/body/order work occurs after core mutation.
- No later-phase actor, snapshot schema, scheduler, or public relay scope appears.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-01-SUMMARY.md`.
</output>
