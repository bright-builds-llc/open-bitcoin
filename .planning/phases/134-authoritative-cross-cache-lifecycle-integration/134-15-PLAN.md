---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "15"
type: execute
wave: 15
depends_on: ["134-14"]
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-01, MPLIFE-04]
must_haves:
  truths:
    - "Peer receipt freshness is scoped to the target peer, so churn on peer B does not stale an outstanding receipt for peer A."
    - "Reconnect or replacement of peer A makes peer A's exact achieved prior receipt `AchievedButStale`: it leaves pending, enters completed accounting, and cannot mutate the replacement session."
    - "Peer completion and its evidence update occur in one lifecycle command while the authority lock is held once."
  artifacts:
    - path: packages/open-bitcoin-node/src/network.rs
      provides: "Per-peer session-generation ownership"
    - path: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      provides: "Atomic peer completion-plus-evidence command"
    - path: packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
      provides: "Per-peer freshness and one-command evidence regressions"
  key_links:
    - from: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      via: "facade dispatches one complete-peer-emission command"
      pattern: "CompletePeer"
    - from: packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
      to: packages/open-bitcoin-node/src/network.rs
      via: "receipt freshness looks up the target peer generation"
      pattern: "peer.*session.*generation"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Make peer-effect completion atomic and peer-local.

Purpose: Enforce D-01, D-02, and D-11 through D-14 without allowing unrelated peer churn or a second lock acquisition to alter completion classification.
Output: Per-peer session versions, one authoritative completion/evidence command, and focused concurrency-independent regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-VERIFICATION.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-14-SUMMARY.md

<interfaces>
The facade remains a thin entry into the existing `LifecycleCommand` projector. The
aggregate stays behind the existing `Arc<Mutex<...>>`; no peer actor, second mutex, or
second authority is permitted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope peer-session freshness to the receipt target</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "Connect/add/disconnect of peer B leaves peer A's session generation unchanged."
    - "Disconnect and reconnect of peer A advances peer A's generation; an exact achieved pre-reconnect receipt returns `AchievedButStale`, moves from pending to completed once, and creates no replacement-session evidence."
    - "A foreign or target-binding-mismatched receipt for a removed peer leaves both ledgers and the replacement session unchanged."
  </behavior>
  <action>
    Replace the aggregate-wide `peer_session_generation` freshness oracle with bounded
    per-peer session versions owned by the aggregate. Advance only the target peer's
    version on connect/add/disconnect/replacement and retain enough generation history
    to classify a delayed exact receipt after removal/reconnect. Receipt preparation
    captures the target version; completion first performs Plan 14's exact achieved
    pending-to-completed move and then compares the captured version against that
    target only. A same-peer mismatch in current freshness returns
    `AchievedButStale`; it is not conflated with a foreign or malformed binding and
    cannot append evidence or alter the replacement session. Preserve explicit peer
    targets and bounded state per D-06 and D-15. Add two-peer tests for unrelated
    churn, same-peer replacement, exact-stale completed accounting, and foreign
    bindings, including snapshots proving no newer lifecycle target changes.
  </action>
  <acceptance-criteria>
    <criterion>No global peer-session counter participates in peer receipt freshness.</criterion>
    <criterion>Peer B churn does not change peer A completion from `Applied` to stale.</criterion>
    <criterion>Peer A reconnect makes an exact pre-reconnect achieved receipt return `AchievedButStale`, reduces pending by one, increases completed by one, and leaves replacement-session evidence and state unchanged.</criterion>
    <criterion>A foreign or immutable target-binding mismatch leaves pending and completed accounting unchanged.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-15-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-15-01-peer-freshness -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Freshness follows the exact peer session named by the receipt, not unrelated aggregate churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete peer effect and evidence in one authority command</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "The facade acquires the authority once for peer completion and evidence recording."
    - "Only an exact current receipt records evidence and returns `Applied`."
    - "An exact stale achieved receipt enters completed accounting and returns `AchievedButStale` but records no evidence; foreign/mismatched inputs enter neither ledger and exact replay returns `AlreadyApplied`."
    - "The completion classification and evidence snapshot cannot observe an intervening peer mutation."
  </behavior>
  <action>
    Introduce a single family-specific `LifecycleCommand` variant that owns the peer
    receipt plus its evidence payload and performs immutable-binding validation, the
    exact achieved pending-to-completed move, per-peer freshness classification, and
    evidence mutation under one dispatcher invocation. Exact current returns
    `Applied` and records evidence; exact stale returns `AchievedButStale` without
    overwriting newer peer, unbroadcast, provenance, or evidence state; exact replay
    returns `AlreadyApplied`; foreign or immutable mismatches are no-ops. Make
    `complete_peer_emission` a thin facade that dispatches that command once; remove
    the post-completion `try_mutate` pass. Keep fact
    derivation/projection/effects distinct per D-04 and do not perform network I/O
    under the authority lock. Add tests that inspect the before/after aggregate and
    prove exact-stale, foreign, mismatch, and replay outcomes cannot append evidence.
  </action>
  <acceptance-criteria>
    <criterion>`complete_peer_emission` has one authority dispatch and no follow-up mutation lock.</criterion>
    <criterion>Exact achieved accounting, freshness classification, and any permitted evidence recording are one indivisible command result.</criterion>
    <criterion>Exact stale completion changes only pending/completed accounting; foreign, mismatch, and replay cases leave all current evidence exactly unchanged.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-15-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-15-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-15-02-atomic-peer-completion -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Peer completion classification and evidence mutation share one command and one authority critical section.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Peer transport result to authority | Delayed external write results return after arbitrary peer churn. |
| Facade to serialized aggregate | The facade must not open a second mutation interval around one logical completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-15-01 | Spoofing | Peer-session receipt | mitigate | Validate immutable binding first, then compare current freshness with the target peer's generation rather than a process-wide counter. |
| T-134-15-02 | Tampering | Peer completion evidence | mitigate | Consume receipt and append evidence in one lifecycle command under one lock. |
| T-134-15-03 | Repudiation | Completion result classification | mitigate | Test current, same-peer stale, unrelated-peer churn, and replay as distinct deterministic outcomes. |
| T-134-15-04 | Elevation of Privilege | Effects facade | mitigate | Keep the facade thin and prohibit direct aggregate mutation outside `LifecycleCommand`. |
</threat-model>

<verification>
Run the focused effect tests, required Rust pre-commit sequence, and Bright Builds checks. Inspect the facade to prove one authority acquisition and no I/O under the lock.
</verification>

<success-criteria>
- Per-peer generations classify delayed exact same-peer receipts `AchievedButStale` without penalizing unrelated peers or losing achieved accounting.
- Completion and evidence form one authoritative transition.
- D-01 through D-04 and D-11 through D-15 remain intact.
- MPLIFE-01 and MPLIFE-04 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-15-SUMMARY.md`.
</output>
