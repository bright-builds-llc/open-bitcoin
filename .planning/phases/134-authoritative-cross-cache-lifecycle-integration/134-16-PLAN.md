---
phase: 134-authoritative-cross-cache-lifecycle-integration
plan: "16"
type: execute
wave: 16
depends_on: ["134-15"]
files_modified:
  - packages/open-bitcoin-node/src/network/lifecycle_effects.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs
  - packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs
  - packages/open-bitcoin-node/src/sync/session.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests/announcement_successful_prefix.rs
autonomous: true
gap_closure: true
requirements: [MPLIFE-01, MPLIFE-04]
must_haves:
  truths:
    - "A failed or deliberately unsent peer effect is explicitly aborted, releasing its own exact bounded pending slot without pretending it completed."
    - "An exact pre-achievement peer abort releases its reservation even when lifecycle or target-peer freshness advanced after preparation."
    - "A foreign or immutable binding-mismatched abort leaves pending and completed accounting unchanged."
    - "Peer fanout retains evidence only for the successful prefix and aborts the failed item plus every reserved but unsent suffix item."
  artifacts:
    - path: packages/open-bitcoin-node/src/network/lifecycle_effects.rs
      provides: "Exact peer pre-achievement abort primitive"
    - path: packages/open-bitcoin-node/src/sync/session.rs
      provides: "Failure and unsent-suffix cleanup at the peer write shell"
  key_links:
    - from: packages/open-bitcoin-node/src/sync/session.rs
      to: packages/open-bitcoin-node/src/network/runtime_authority/effects.rs
      via: "every reserved peer capability exits through complete or exact abort"
      pattern: "(complete|abort).*peer"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 134-2026-07-28T01-41-12
generated_at: 2026-07-28T23:02:54Z
---

<objective>
Close the peer reserved-effect leak and successful-prefix gap.

Purpose: Guarantee that every consumed peer capability reaches exactly one terminal authority path—completion or abort—while preserving D-13 and D-14 outcome semantics.
Output: Exact peer abort commands, node/RPC shell routing for all failures and unsent suffixes, bounded-capacity recovery, and injected-failure regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/operability.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-CONTEXT.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-REVIEW.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-VERIFICATION.md
@.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-15-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an exact pre-achievement peer abort command</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/network/lifecycle_effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs</file>
    <file>packages/open-bitcoin-node/src/network/runtime_authority/effects.rs</file>
    <file>packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/network/lifecycle_effects.rs, packages/open-bitcoin-node/src/network/runtime_authority/lifecycle.rs, packages/open-bitcoin-node/src/network/runtime_authority/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects.rs, packages/open-bitcoin-node/src/network/tests/lifecycle_projection_cases/effects/contracts.rs</files>
  <behavior>
    - "An exact peer abort consumes its pending reservation without recording completion or evidence."
    - "The exact peer abort still releases its own reservation after lifecycle or target-peer freshness advances because abort proves pre-achievement termination, not current freshness."
    - "Foreign and immutable binding-mismatched aborts do not consume another reservation or enter completed accounting."
    - "Pending peer-ledger capacity returns to its pre-prepare level after an exact abort; replay is a typed no-op."
  </behavior>
  <action>
    Add an explicit peer abort command that consumes the complete immutable binding
    defined by Plan 14 and route it through the single lifecycle dispatcher. Validate
    authority incarnation and the reservation's peer/effect/session binding, but do not
    require current lifecycle generation or current peer-session freshness to equal the
    captured values: an exact capability is entitled to release its own reservation
    even after state advances because no external achievement occurred. Return a typed
    abort classification rather than silently ignoring mismatch. Exact abort releases
    bounded pending capacity and never enters completed/evidence ledgers; foreign or
    immutable binding mismatch changes neither ledger. Keep reconciliation read-only
    per D-10. Add exact-after-freshness-advance, foreign, immutable-mismatch, replay,
    and capacity-recovery tests.
  </action>
  <acceptance-criteria>
    <criterion>The peer effect family has an authority-routed terminal abort operation.</criterion>
    <criterion>Exact abort releases only its own pending key even after current lifecycle or target-peer session advances, and cannot create completed or evidence accounting.</criterion>
    <criterion>Foreign or immutable binding mismatch leaves pending and completed accounting unchanged.</criterion>
    <criterion>Peer capacity recovery is covered by deterministic tests.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-16-01-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-01-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-01-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-01-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-16-01-effect-abort -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node --all-features network::tests::effects -- --nocapture</automated>
  </verify>
  <done>Every consumed pre-achievement peer capability has a typed exact-abort terminal path that restores its pending capacity without false success, including after freshness advances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route peer fanout failures and unsent suffixes through abort</name>
  <read-first>
    <file>packages/open-bitcoin-node/src/sync/session.rs</file>
    <file>packages/open-bitcoin-node/src/sync/tests.rs</file>
    <file>packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs</file>
    <file>packages/open-bitcoin-rpc/src/inbound_listener/tests/announcement_successful_prefix.rs</file>
  </read-first>
  <files>packages/open-bitcoin-node/src/sync/session.rs, packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-rpc/src/inbound_listener/connection_runtime.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests/announcement_successful_prefix.rs</files>
  <behavior>
    - "A peer target mismatch or write failure aborts the current capability and all reserved unsent suffix capabilities."
    - "Successful-prefix receipts remain completed and evidenced; suffix abort does not roll them back."
    - "RPC and node fanout share the same complete-or-abort terminal contract."
  </behavior>
  <action>
    Audit every node and RPC executor that consumes a prepared peer capability. Structure
    ownership so the successful path converts the capability to a receipt and completes
    it, while every known pre-achievement error/early-return path consumes it through
    the exact peer abort.
    For ordered fanout, abort the failed current item and every reserved but unsent
    suffix item while retaining the successful prefix exactly per D-14. Propagate abort
    errors visibly; do not use drop as cleanup and do not introduce a durable outbox.
    Add failure injection at first, middle, last, and target-mismatch positions and
    assert pending capacity returns to baseline.
  </action>
  <acceptance-criteria>
    <criterion>Every executor exit after capability consumption calls exactly one complete or abort terminal path.</criterion>
    <criterion>Middle-failure tests retain only successful-prefix evidence and leave no pending suffix reservations.</criterion>
  </acceptance-criteria>
  <pre-commit>
    <step>bun run scripts/command-timings.ts run --key phase134-16-fmt -- cargo fmt --all --manifest-path packages/Cargo.toml</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-clippy -- cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-build -- cargo build --manifest-path packages/Cargo.toml --all-targets --all-features</step>
    <step>bun run scripts/command-timings.ts run --key phase134-16-test -- cargo test --manifest-path packages/Cargo.toml --all-features</step>
    <step>bun scripts/bright-builds-check.ts all</step>
  </pre-commit>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase134-16-02-failure-terminals -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node -p open-bitcoin-rpc --all-features announcement_successful_prefix -- --nocapture</automated>
  </verify>
  <done>Node and RPC peer executors terminate every reservation explicitly, preserve the successful prefix, and recover bounded capacity on all tested failures.</done>
</task>

</tasks>

<threat-model>
Security gate: OWASP ASVS Level 1; any unresolved High severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authority to external executor | A prepared capability leaves the lock and may encounter I/O failure. |
| Ordered fanout to peer transports | A failure splits completed prefix from failed and unsent suffix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-134-16-01 | Denial of Service | Pending effect ledger | mitigate | Exact abort releases every failed/unsent reservation and tests restore capacity to baseline. |
| T-134-16-02 | Tampering | Successful-prefix evidence | mitigate | Complete only actual successes; abort current/suffix without rolling back prefix evidence. |
| T-134-16-03 | Spoofing | Abort capability binding | mitigate | Validate and consume the same exact peer key as completion; reject foreign/mismatched aborts while allowing an exact owner to release after freshness advances. |
</threat-model>

<verification>
Run focused effect, sync, and RPC successful-prefix tests. Then run the required Rust pre-commit sequence in order and Bright Builds checks. Review every peer-capability-consuming branch for an explicit terminal call.
</verification>

<success-criteria>
- No tested I/O failure or early return leaks a pending reservation.
- Successful-prefix behavior remains exact.
- Exact peer abort releases its own reservation after freshness advances; foreign/mismatched aborts are no-ops.
- MPLIFE-01 and MPLIFE-04 remain pending until phase re-verification.
</success-criteria>

<output>
After completion, create `.planning/phases/134-authoritative-cross-cache-lifecycle-integration/134-16-SUMMARY.md`.
</output>
