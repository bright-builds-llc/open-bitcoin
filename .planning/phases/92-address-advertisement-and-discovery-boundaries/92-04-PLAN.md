---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 04
type: execute
wave: 4
depends_on: ["92-03"]
files_modified:
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
autonomous: true
requirements: [ADDR-01, ADDR-02, ADDR-03]
must_haves:
  truths:
    - "Inbound `addr` messages update learned-address evidence through parser and policy boundaries only."
    - "Inbound `getaddr` requests can produce at most one direct `addr` response per served state and only when permission evidence allows address responses."
    - "No unsolicited address relay, periodic self-announcement, fanout queue, or relay state is created."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer.rs"
      provides: "PeerManager address-boundary state, addr intake, getaddr response handling, and version sender integration"
    - path: "packages/open-bitcoin-network/src/peer/tests.rs"
      provides: "Synthetic inbound addr/getaddr, permission, repeated request, and no-relay tests"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer.rs"
      to: "packages/open-bitcoin-network/src/address/response.rs"
      via: "getaddr response selection"
      pattern: "select_getaddr_response"
    - from: "packages/open-bitcoin-network/src/peer.rs"
      to: "packages/open-bitcoin-network/src/address/book.rs"
      via: "learned address batch intake"
      pattern: "LearnedAddressBook"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:23:41Z
---

<objective>
Integrate bounded address handling into the pure peer manager.

Purpose: Let the existing `PeerManager::handle_message` flow process scoped `addr` and `getaddr` behavior using the pure policies from Plans 92-01 through 92-03, while preserving the no-full-relay boundary.
Output: Peer state, evidence accessors, address intake, direct getaddr responses, sender-address gating, and focused peer tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-01-SUMMARY.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-02-SUMMARY.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-03-SUMMARY.md
@packages/open-bitcoin-network/src/peer.rs
@packages/open-bitcoin-network/src/peer/tests.rs
@packages/open-bitcoin-network/src/address.rs
@packages/open-bitcoin-network/src/inbound/permissions.rs
@packages/bitcoin-knots/src/net_processing.cpp
@packages/bitcoin-knots/test/functional/p2p_getaddr_caching.py
@packages/bitcoin-knots/test/functional/p2p_addr_relay.py

<interfaces>
Existing peer action path:
```rust
pub enum PeerAction {
    Send(WireNetworkMessage),
    ServeInventory(Vec<InventoryVector>),
    ReceivedTransaction(Transaction),
    ReceivedBlock(Block),
    Disconnect(DisconnectReason),
}
```

Existing inbound record permission seam:
```rust
pub struct InboundPeerRecord {
    pub permission_decision: InboundPermissionDecision,
    pub connection_class: PeerConnectionClass,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handle inbound `addr` learned-address intake</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/address.rs`
    - `packages/open-bitcoin-network/src/address/book.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/bitcoin-knots/src/addrman.cpp`
    - `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md`
  </read_first>
  <behavior>
    - Test inbound `WireNetworkMessage::Addr(AddressList)` learns valid public entries as `learned_accepted`.
    - Test duplicate, stale/future, invalid port, loopback, private, documentation, and over-cap batches record `learned_rejected` reasons without disconnecting the peer.
    - Test outbound peers can parse `addr` without producing address response or relay actions unless policy explicitly accepts learned entries.
  </behavior>
  <action>
    Add address-boundary storage to `PeerManager`: `learned_addresses: LearnedAddressBook`, `local_address_decisions: Vec<LocalAdvertisementDecision>`, and a bounded evidence snapshot exposed as `address_boundary_evidence(&self) -> PeerAddressBoundaryEvidence`. Add `PeerAddressBoundaryEvidence` fields for `local_advertisement_candidates`, `suppressed_advertisements`, `getaddr_responses_served`, `getaddr_requests_suppressed`, `learned_address_entries`, `learned_address_rejections`, and `maybe_latest_address_decision`. In `handle_message`, add a `WireNetworkMessage::Addr(addresses)` arm that calls the learned-address batch policy with `AddressSourceKind::InboundAddr` (stable label `source_inbound_addr` from Plan 92-01), the provided timestamp, and the current local advertisement decisions for self/local leakage checks. Return `Ok(Vec::new())` after recording evidence. Do not disconnect for ordinary address rejections in this phase and do not add eviction, ban, discourage, or misbehavior behavior. This implements D-11, D-12, D-13, D-14, D-15, D-19, and D-20.
  </action>
  <acceptance_criteria>
    - `rg -n "LearnedAddressBook|PeerAddressBoundaryEvidence|address_boundary_evidence|learned_address_entries|learned_address_rejections|maybe_latest_address_decision" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/tests.rs` finds the peer evidence contract and tests.
    - `rg -n "WireNetworkMessage::Addr|learned_accepted|learned_rejected|stale_or_future|duplicate_address|invalid_port|not_publicly_routable" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/tests.rs` finds intake behavior and tests.
    - `! rg -n "Ban|Discourage|Misbehavior|Evict|addrman\\.dat|peers\\.dat" packages/open-bitcoin-network/src/peer.rs` proves Phase 93 and full persistence behavior were not added.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast</automated>
  </verify>
  <done>Inbound `addr` messages update typed learned-address evidence through bounded policy and do not create ban, eviction, persistence, or relay side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle bounded permission-aware `getaddr` responses</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/peer.rs`
    - `packages/open-bitcoin-network/src/peer/tests.rs`
    - `packages/open-bitcoin-network/src/address/response.rs`
    - `packages/open-bitcoin-network/src/address/advertisement.rs`
    - `packages/open-bitcoin-network/src/inbound/permissions.rs`
    - `packages/bitcoin-knots/src/net_processing.cpp`
    - `packages/bitcoin-knots/test/functional/p2p_getaddr_caching.py`
  </read_first>
  <behavior>
    - Test inbound peers with `AddressResponsePolicyInput` receive exactly one `WireNetworkMessage::Addr` capped by `PHASE92_GETADDR_RESPONSE_LIMIT`.
    - Test repeated `getaddr` from the same peer records `getaddr_suppressed` with `already_served` and sends no message.
    - Test ordinary inbound peers without the `addr` permission effect and outbound peers record suppression reasons and send no message.
    - Test inbound version response uses `version_message_with_sender_policy` and remains zero sender when local advertisement decisions are suppressed.
    - Test no unsolicited `Addr` appears during version/verack/ping/inv/headers flows.
  </behavior>
  <action>
    Add per-peer `GetAddrRequestState` to `PeerState` and initialize it in `PeerState::new`. Add `PeerManager::set_local_address_decisions(decisions: Vec<LocalAdvertisementDecision>)` and use `maybe_version_sender_address(&self.local_address_decisions)` when sending a local `Version` response to inbound peers. Add a `WireNetworkMessage::GetAddr` arm in `handle_message` that builds `GetAddrPeerEligibility` from `peer.role == ConnectionRole::Inbound` and the peer's `InboundPermissionDecision.active_effects()` containing `PermissionEffectLabel::AddressResponsePolicyInput`. Call `select_getaddr_response` with local candidates, learned-address book entries, per-peer state, and timestamp. On `getaddr_served`, return `PeerAction::Send(WireNetworkMessage::Addr(list))`; on suppression, return no action and record bounded evidence. Do not add timers, periodic self-announcement, address fanout, peer relay selection, randomized caches, bloom filters, or gossip state. This implements D-05, D-06, D-07, D-08, D-09, D-10, D-15, D-19, and D-20.
  </action>
  <acceptance_criteria>
    - `rg -n "GetAddrRequestState|set_local_address_decisions|maybe_version_sender_address|WireNetworkMessage::GetAddr|WireNetworkMessage::Addr" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/tests.rs` finds getaddr integration.
    - `rg -n "getaddr_served|getaddr_suppressed|already_served|permission_policy_denied|not_inbound|PHASE92_GETADDR_RESPONSE_LIMIT" packages/open-bitcoin-network/src/peer.rs packages/open-bitcoin-network/src/peer/tests.rs` finds response labels and cap tests.
    - `! rg -n "timer|periodic|fanout|trickle|rebroadcast|PushAddress|MaybeSendAddr|known filter|bloom" packages/open-bitcoin-network/src/peer.rs` proves relay machinery was not added.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>`getaddr` handling is direct, bounded, permission-aware, served once, and free of relay/fanout side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer message -> PeerManager | Untrusted inbound `addr` and `getaddr` messages enter pure peer state. |
| permission evidence -> address response | Phase 91 permission labels determine whether a peer may receive bounded address responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-01 | Tampering | `WireNetworkMessage::Addr` handling | mitigate | Route all address entries through `LearnedAddressBook` policy and record rejection reasons for poisoned entries. |
| T-92-04-02 | Information Disclosure | `WireNetworkMessage::GetAddr` handling | mitigate | Require inbound role plus `AddressResponsePolicyInput`, cap responses, and suppress repeated requests. |
| T-92-04-03 | Denial of Service | repeated getaddr | mitigate | Per-peer served state returns `already_served` without rebuilding unbounded response work. |
| T-92-04-04 | Elevation of Privilege | permission integration | mitigate | Use typed `PermissionEffectLabel::AddressResponsePolicyInput`; raw class names and raw config strings are not consulted. |
| T-92-04-05 | Repudiation | no-relay boundary | mitigate | Tests and grep checks prohibit relay timers, fanout, trickle, rebroadcast, and known-filter machinery. |
</threat_model>

<verification>
Run focused peer verification:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network peer --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
ADDR-02 and ADDR-03 work through the existing pure peer message flow. Address-response policy uses Phase 91 permission evidence, and no full relay/discovery behavior is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-04-SUMMARY.md`.
</output>
