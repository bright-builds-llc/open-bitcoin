---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 06
type: execute
wave: 6
depends_on: ["92-04", "92-05"]
files_modified:
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/inbound_listener.rs
  - packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements: [ADDR-01, ADDR-02]
must_haves:
  truths:
    - "Runtime listener evidence feeds local advertisement decisions without interface discovery or external IP inference."
    - "`openbitcoinnetworkstatus` exposes shared address-boundary fields while baseline `getnetworkinfo` stays free of address-boundary details."
    - "Loopback listener UAT evidence remains suppressed for public advertisement with `not_publicly_routable`."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "ManagedRpcContext projection of address-boundary status and listener evidence"
    - path: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      provides: "Runtime listener evidence handoff into pure local advertisement policy"
    - path: "packages/open-bitcoin-rpc/src/dispatch/tests.rs"
      provides: "RPC regression coverage for Open Bitcoin extension status and baseline getnetworkinfo non-drift"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/inbound_listener.rs"
      to: "packages/open-bitcoin-network/src/address/advertisement.rs"
      via: "LocalAdvertisementInput from listener evidence"
      pattern: "LocalAdvertisementInput"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "current_inbound_status projection"
      pattern: "latest_address_decision"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:23:41Z
---

<objective>
Wire address-boundary evidence through RPC runtime context and listener activation.

Purpose: Connect pure address decisions to the existing opt-in listener/runtime path while keeping default verification local, baseline RPC shapes stable, and local/private address leakage suppressed.
Output: Runtime listener evidence handoff, current inbound status projection, Open Bitcoin RPC extension tests, and getnetworkinfo non-drift tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-04-SUMMARY.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-05-SUMMARY.md
@packages/open-bitcoin-rpc/src/context/network.rs
@packages/open-bitcoin-rpc/src/inbound_listener.rs
@packages/open-bitcoin-rpc/src/inbound_listener/tests.rs
@packages/open-bitcoin-rpc/src/dispatch/tests.rs
@packages/open-bitcoin-rpc/src/config/open_bitcoin.rs
@packages/open-bitcoin-network/src/address.rs
@packages/open-bitcoin-network/src/inbound.rs

<interfaces>
Existing listener evidence:
```rust
pub struct InboundListenerEvidence {
    pub listener_state: String,
    pub preflight_reason: String,
    pub bound_endpoints: Vec<String>,
    pub admitted_inbound_peers: usize,
    pub rejected_inbound_peers: usize,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed listener evidence into local advertisement policy</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/inbound_listener.rs, packages/open-bitcoin-rpc/src/inbound_listener/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener.rs`
    - `packages/open-bitcoin-rpc/src/inbound_listener/tests.rs`
    - `packages/open-bitcoin-rpc/src/config/open_bitcoin.rs`
    - `packages/open-bitcoin-network/src/address/advertisement.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
  </read_first>
  <behavior>
    - Test `127.0.0.1:18444` listener evidence produces `advertise_suppressed` with `not_publicly_routable`.
    - Test a public literal listener with `inbound.allow_public=true` can produce one `advertise_candidate` decision from runtime-bound evidence.
    - Test no arbitrary interface, DNS, UPnP/NAT-PMP, outbound peer, `-externalip`, or `-discover` input is used.
  </behavior>
  <action>
    In `ManagedRpcContext::set_inbound_listener_evidence`, convert `InboundListenerEvidence.bound_endpoints` and the runtime `RuntimeConfig.inbound.listen_addresses`/`allow_public` values into `LocalAdvertisementInput` values. Call the concrete `ManagedPeerNetwork::set_local_address_decisions(decisions)` created by Plan 92-05, which forwards to `PeerManager::set_local_address_decisions`. If a bound endpoint cannot be parsed as `SocketAddr`, record an `advertise_suppressed` decision with `invalid_port` or `unsupported_address_network` instead of panicking. Keep `LocalPeerConfig.address` default zero in `from_runtime_config`; only the policy output can affect sender address through the PeerManager helper from Plan 92-04. This implements D-01, D-02, D-03, D-04, D-05, D-15, and D-19.
  </action>
  <acceptance_criteria>
    - `rg -n "LocalAdvertisementInput|select_local_advertisement_candidates|set_local_address_decisions|allow_public|bound_endpoints" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/inbound_listener.rs packages/open-bitcoin-rpc/src/inbound_listener/tests.rs` finds the handoff and tests.
    - `rg -n "not_publicly_routable|advertise_suppressed|advertise_candidate" packages/open-bitcoin-rpc/src/inbound_listener/tests.rs packages/open-bitcoin-rpc/src/context/network.rs` finds runtime evidence labels.
    - `! rg -n "externalip|discover|interface|UPnP|NAT|dns|outbound peer" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/inbound_listener.rs` proves forbidden discovery inputs are not used.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast</automated>
  </verify>
  <done>Runtime listener evidence feeds pure local advertisement decisions and loopback evidence remains suppressed from public advertisement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project address evidence through Open Bitcoin RPC status</name>
  <files>packages/open-bitcoin-rpc/src/context/network.rs, packages/open-bitcoin-rpc/src/dispatch/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-rpc/src/context/network.rs`
    - `packages/open-bitcoin-rpc/src/dispatch/tests.rs`
    - `packages/open-bitcoin-node/src/status/inbound.rs`
    - `packages/open-bitcoin-node/src/network/inbound.rs`
    - `docs/architecture/status-snapshot.md`
  </read_first>
  <behavior>
    - Test `openbitcoinnetworkstatus` JSON contains `local_advertisement_candidates`, `suppressed_advertisements`, `getaddr_responses_served`, `getaddr_requests_suppressed`, `learned_address_entries`, `learned_address_rejections`, and `latest_address_decision`.
    - Test `getnetworkinfo` JSON does not contain Phase 92 address-boundary fields.
    - Test raw permission class names, raw config strings, peer IDs, and raw address bytes are absent from RPC address evidence.
  </behavior>
  <action>
    Update `ManagedRpcContext::current_inbound_status` so it reads `self.network.address_boundary_info()` and populates the Phase 92 fields on `InboundPeerServingStatus`. Add dispatch tests for `openbitcoinnetworkstatus` with local advertisement suppression and learned/getaddr evidence. Extend the existing baseline `getnetworkinfo` regression to reject `local_advertisement_candidates`, `suppressed_advertisements`, `getaddr_responses_served`, `getaddr_requests_suppressed`, `learned_address_entries`, `learned_address_rejections`, and `latest_address_decision`. This implements D-09, D-14, D-15, D-17, and D-19.
  </action>
  <acceptance_criteria>
    - `rg -n "address_boundary_info|local_advertisement_candidates|suppressed_advertisements|getaddr_responses_served|getaddr_requests_suppressed|learned_address_entries|learned_address_rejections|latest_address_decision" packages/open-bitcoin-rpc/src/context/network.rs packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds RPC projection and tests.
    - `rg -n "getnetworkinfo.*local_advertisement_candidates|getnetworkinfo.*latest_address_decision|latest_address_decision.*getnetworkinfo" packages/open-bitcoin-rpc/src/dispatch/tests.rs` finds non-drift assertions.
    - `! rg -n "peer_id|address_bytes|raw_permission|raw_config|class_name" packages/open-bitcoin-rpc/src/context/network.rs` proves raw/high-cardinality evidence was not added.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>Open Bitcoin RPC status exposes bounded Phase 92 address evidence, while baseline `getnetworkinfo` remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime listener evidence -> pure address policy | Bound socket evidence crosses from Tokio listener adapter into pure candidate selection. |
| RPC extension status -> local operator | Address-boundary evidence is exposed through Open Bitcoin-owned RPC status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06-01 | Information Disclosure | listener evidence handoff | mitigate | Classify bound endpoints through pure policy and suppress loopback/private/unroutable addresses before version sender or getaddr use. |
| T-92-06-02 | Spoofing | advertisement inputs | mitigate | Use only configured `inbound.listen_addresses` and runtime-bound listener evidence; do not infer from interfaces, DNS, external IP probes, or peers. |
| T-92-06-03 | Information Disclosure | RPC status | mitigate | Expose bounded labels/counts and no raw address bytes, peer IDs, raw permission class names, or raw config strings. |
| T-92-06-04 | Repudiation | `getnetworkinfo` baseline | mitigate | Regression tests prove baseline RPC output does not grow Phase 92 fields or imply full discovery/relay support. |
</threat_model>

<verification>
Run focused RPC/runtime checks:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc inbound_listener --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc open_bitcoin_network_status --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
Runtime listener evidence drives address-boundary decisions, Open Bitcoin RPC exposes bounded evidence, baseline RPC remains stable, and no public discovery or relay claim appears.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-06-SUMMARY.md`.
</output>
