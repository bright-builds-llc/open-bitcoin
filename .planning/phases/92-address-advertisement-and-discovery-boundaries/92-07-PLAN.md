---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 07
type: execute
wave: 6
depends_on: ["92-05"]
files_modified:
  - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
  - packages/open-bitcoin-cli/src/operator/status/tests.rs
  - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
autonomous: true
requirements: [ADDR-04]
must_haves:
  truths:
    - "Operator status names local advertisement candidates, suppressed advertisements, bounded getaddr responses, and learned address counts without implying full address relay."
    - "Support evidence carries the same bounded Phase 92 address-boundary labels without exposing raw endpoints, peer IDs, address bytes, raw permission class strings, or raw config strings."
    - "CLI output distinguishes direct getaddr support from peer discovery, unsolicited address relay, and public-network readiness."
  artifacts:
    - path: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      provides: "human status rendering for Phase 92 inbound address evidence"
      contains: "local advertisement candidates"
    - path: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      provides: "support bundle rendering for Phase 92 inbound address evidence"
      contains: "bounded getaddr"
    - path: "packages/open-bitcoin-cli/src/operator/support/tests.rs"
      provides: "redaction and no-claim regression tests"
      contains: "full address relay"
  key_links:
    - from: "packages/open-bitcoin-cli/src/operator/status/render/inbound.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "InboundPeerServingStatus fields from Plan 05"
      pattern: "local_advertisement_candidates"
    - from: "packages/open-bitcoin-cli/src/operator/support/render/inbound.rs"
      to: "packages/open-bitcoin-node/src/status/inbound.rs"
      via: "shared support evidence projection"
      pattern: "learned_address_entries"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:34:07Z
---

<objective>
Render Phase 92 address-boundary evidence in operator status and support output.

Purpose: ADDR-04 requires shared operator-visible evidence that cleanly separates local advertisement, bounded direct getaddr, learned-address storage, and out-of-scope relay/discovery claims.
Output: CLI status/support renderers and tests that consume Plan 05 shared status fields without recomputing policy locally.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-05-SUMMARY.md
@packages/open-bitcoin-node/src/status/inbound.rs
@packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
@packages/open-bitcoin-cli/src/operator/support/render/inbound.rs

<interfaces>
Plan 05 creates these `InboundPeerServingStatus` address-boundary fields for this plan to render:
```rust
pub local_advertisement_candidates: Vec<InboundAddressEvidenceEntry>;
pub suppressed_advertisements: Vec<InboundAddressDecisionEvent>;
pub getaddr_responses_served: u32;
pub getaddr_requests_suppressed: u32;
pub learned_address_entries: u32;
pub learned_address_rejections: u32;
pub latest_address_decision: FieldAvailability<InboundAddressDecisionEvent>;
```

Plan 05 evidence entries intentionally expose labels/counts only:
```rust
pub struct InboundAddressEvidenceEntry {
    pub source: String,
    pub network_kind: String,
    pub routability: String,
    pub freshness: String,
    pub services_bits: u64,
    pub port: u16,
    pub persistence_eligible: bool,
}

pub struct InboundAddressDecisionEvent {
    pub outcome: String,
    pub reason: String,
    pub label: String,
    pub source: String,
    pub message: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render Phase 92 address evidence in status output</name>
  <files>packages/open-bitcoin-cli/src/operator/status/render/inbound.rs, packages/open-bitcoin-cli/src/operator/status/render/tests.rs, packages/open-bitcoin-cli/src/operator/status/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/status/render/inbound.rs
    - packages/open-bitcoin-cli/src/operator/status/render/tests.rs
    - packages/open-bitcoin-cli/src/operator/status/tests.rs
    - packages/open-bitcoin-node/src/status/inbound.rs
    - .planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: human inbound status with one public candidate renders `local advertisement candidates: 1` and the candidate labels `source=runtime_bound_listener`, `routability=publicly_routable`, and `persistence_eligible=true`.
    - Test 2: suppressed loopback/private advertisements render `suppressed advertisements: N` with stable decision labels such as `not_publicly_routable`, not raw socket addresses.
    - Test 3: JSON status includes the exact shared field names `local_advertisement_candidates`, `suppressed_advertisements`, `getaddr_responses_served`, `getaddr_requests_suppressed`, `learned_address_entries`, `learned_address_rejections`, and `latest_address_decision`.
    - Test 4: status output contains `bounded getaddr` and does not contain `full address relay`, `peer discovery support`, `public inbound by default`, `address_bytes`, `peer_id=`, or raw permission class strings.
  </behavior>
  <action>
    Add Phase 92 status rendering to `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs` using only the Plan 05 shared fields. Render these concrete labels: `local advertisement candidates`, `suppressed advertisements`, `bounded getaddr responses served`, `bounded getaddr requests suppressed`, `learned address entries`, `learned address rejections`, and `latest address decision`. Map unavailable `latest_address_decision` to the shared unavailable reason string instead of a positive support claim. Do not render raw endpoint strings beyond existing Phase 90 listener evidence, do not render raw IPv6 address bytes, and do not derive permission state from raw class/config strings. This implements D-15, D-16, D-17, and D-19.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "local advertisement candidates|bounded getaddr responses served|learned address entries|latest address decision" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs` finds matches.
    - `rg -n "full address relay|peer discovery support|public inbound by default|address_bytes|peer_id=" packages/open-bitcoin-cli/src/operator/status/render/inbound.rs packages/open-bitcoin-cli/src/operator/status/render/tests.rs` has no positive status-output claim.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status --no-fail-fast` passes.
  </acceptance_criteria>
  <done>Status output consumes shared Phase 92 evidence and names bounded behavior without claiming full relay, public discovery, or raw address exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Phase 92 evidence in support bundles with redaction guards</name>
  <files>packages/open-bitcoin-cli/src/operator/support/render/inbound.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/support/render/inbound.rs
    - packages/open-bitcoin-cli/src/operator/support/tests.rs
    - packages/open-bitcoin-cli/src/operator/support/redaction.rs
    - packages/open-bitcoin-node/src/status/inbound.rs
    - .planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: support output includes a Phase 92 inbound address section with the strings `local advertisement candidates`, `bounded getaddr`, and `learned address entries`.
    - Test 2: support output for suppressed loopback/private/local candidates includes `not_publicly_routable` and `local evidence only`.
    - Test 3: support output never includes `127.0.0.1:`, `0.0.0.0:`, `::1`, `address_bytes`, `peer_id=`, `operator_loopback`, `raw_permission`, `full address relay support`, or `peer discovery support`.
    - Test 4: next-action text says direct getaddr is bounded and does not imply unsolicited fanout, addr gossip, DNS seed discovery, UPnP/NAT-PMP discovery, or public-network readiness.
  </behavior>
  <action>
    Extend `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs` with a `## Inbound Address Boundary Evidence` section. Use these exact line prefixes: `Local advertisement candidates:`, `Suppressed advertisements:`, `Bounded getaddr responses served:`, `Bounded getaddr requests suppressed:`, `Learned address entries:`, `Learned address rejections:`, and `Latest address decision:`. Add next-action text: `Next action: Treat Phase 92 as bounded local advertisement and direct getaddr evidence only; peer discovery, unsolicited address relay, DNS seed discovery, UPnP/NAT-PMP discovery, and public-network readiness remain outside this surface.` Ensure tests cover redaction of raw endpoint bytes, raw peer IDs, and raw permission/config strings. This implements D-15, D-16, D-17, and D-19.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Inbound Address Boundary Evidence|Local advertisement candidates:|Bounded getaddr responses served:|Learned address entries:" packages/open-bitcoin-cli/src/operator/support/render/inbound.rs packages/open-bitcoin-cli/src/operator/support/tests.rs` finds matches.
    - `rg -n "full address relay support|peer discovery support|DNS seed discovery|UPnP/NAT-PMP discovery|public-network readiness" packages/open-bitcoin-cli/src/operator/support/tests.rs` finds negative-boundary assertions.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast` passes.
  </acceptance_criteria>
  <done>Support bundles expose bounded Phase 92 evidence with deterministic redaction and explicit no-claim language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node status -> CLI status | Serialized local address-boundary evidence is rendered for operators. |
| node status -> support bundle | Local evidence becomes a shareable artifact and must not leak raw peer/address details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-07-01 | Information Disclosure | status renderer | mitigate | Render stable labels/counts only; tests reject raw address bytes, peer IDs, raw permission strings, and raw config strings. |
| T-92-07-02 | Information Disclosure | support renderer | mitigate | Support tests reject loopback/private socket strings and raw address internals. |
| T-92-07-03 | Repudiation | CLI no-claim boundary | mitigate | Status/support tests require bounded getaddr wording and reject full relay, discovery, and public-network support claims. |
| T-92-07-04 | Tampering | renderer-local derivation | mitigate | Renderers consume shared Plan 05 fields only and do not recompute policy from raw inputs. |
</threat_model>

<verification>
Run focused CLI checks:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli status --no-fail-fast`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli support --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-cli --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
ADDR-04 evidence is visible in status and support outputs, the output is redaction-safe, and operators cannot confuse Phase 92 bounded getaddr/local-advertisement evidence with full discovery or relay support.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-07-SUMMARY.md`.
</output>
