---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 09
type: execute
wave: 8
depends_on: ["92-08"]
files_modified:
  - scripts/check-phase92-address-boundaries.ts
  - scripts/check-phase92-address-boundaries.test.ts
  - scripts/verify.sh
autonomous: true
requirements: [ADDR-01, ADDR-02, ADDR-03, ADDR-04]
must_haves:
  truths:
    - "Default verification deterministically checks Phase 92 evidence without public-network access."
    - "The checker fails if docs or metadata claim full address relay, peer discovery, public inbound defaults, public-network readiness, or production full-node readiness."
    - "The checker enforces ADDR-01 through ADDR-04 coverage, source breadcrumbs, UAT command fragments, and verifier ordering."
  artifacts:
    - path: "scripts/check-phase92-address-boundaries.ts"
      provides: "deterministic Phase 92 evidence checker"
      contains: "checkPhase92AddressBoundaries"
    - path: "scripts/check-phase92-address-boundaries.test.ts"
      provides: "checker regression tests"
      contains: "v1-9-address-advertisement-discovery-boundaries"
    - path: "scripts/verify.sh"
      provides: "default verification wiring"
      contains: "check Phase 92 address boundaries"
  key_links:
    - from: "scripts/verify.sh"
      to: "scripts/check-phase92-address-boundaries.ts"
      via: "bun run verifier step"
      pattern: "check-phase92-address-boundaries"
    - from: "scripts/check-phase92-address-boundaries.ts"
      to: "docs/parity/index.json"
      via: "surface and requirement validation"
      pattern: "ADDR-04"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:34:07Z
---

<objective>
Add deterministic Phase 92 checker coverage and wire it into repo verification.

Purpose: ADDR-01 through ADDR-04 need durable evidence that local address advertisement, bounded direct getaddr, learned address policy, and docs/status/support boundaries stay in sync without expanding public-network or relay claims.
Output: Bun checker, checker tests, and `scripts/verify.sh` wiring after Phase 91.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-08-SUMMARY.md
@scripts/check-phase91-peer-permissions.ts
@scripts/check-phase91-peer-permissions.test.ts
@scripts/verify.sh

<interfaces>
Phase checker scripts in this repo export a callable check function and a CLI entrypoint. Follow the Phase 91 shape:
```ts
export function checkPhase91PeerPermissions(options: CheckOptions = {}): string[] {
  // returns failure messages
}
```

`scripts/verify.sh` currently runs the Phase 91 checker before pure-core checks:
```bash
run_step "test Phase 91 peer permissions checker" bun test scripts/check-phase91-peer-permissions.test.ts
run_step "check Phase 91 peer permissions" bun run scripts/check-phase91-peer-permissions.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 92 checker fixture tests</name>
  <files>scripts/check-phase92-address-boundaries.test.ts, scripts/check-phase92-address-boundaries.ts</files>
  <read_first>
    - scripts/check-phase91-peer-permissions.test.ts
    - scripts/check-phase91-peer-permissions.ts
    - scripts/check-phase92-address-boundaries.test.ts
    - scripts/check-phase92-address-boundaries.ts
    - .planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a complete synthetic corpus containing `v1-9-address-advertisement-discovery-boundaries`, `ADDR-01`, `ADDR-02`, `ADDR-03`, `ADDR-04`, Phase 92 UAT command fragments, and source breadcrumbs produces zero failures.
    - Test 2: missing any one ADDR requirement ID produces a failure containing `Phase 92 requirement coverage`.
    - Test 3: a corpus containing scoped positive claims such as `Phase 92 supports full address relay`, `Phase 92 includes peer discovery support`, `public inbound by default is enabled`, `public-network readiness is achieved`, `production full-node readiness is achieved`, `unsolicited address relay is supported`, `addr gossip relay is supported`, `DNS seed discovery support is enabled`, or `UPnP/NAT-PMP discovery support is enabled` produces a failure containing `Phase 92 no-claim boundary`.
    - Test 4: a corpus containing required negative/no-claim wording such as `peer discovery, unsolicited address relay, DNS seed discovery, UPnP/NAT-PMP discovery, and public-network readiness remain outside this surface` produces zero `Phase 92 no-claim boundary` failures.
    - Test 5: support/status corpus containing `127.0.0.1:`, `0.0.0.0:`, `::1`, `address_bytes`, `peer_id=`, `raw_permission`, `operator_loopback`, or `inbound.allow_public=true` produces a failure containing `Phase 92 raw evidence boundary`.
    - Test 6: `scripts/verify.sh` ordering must require Phase 92 checker test and checker lines after Phase 91 checker lines and before pure-core checks.
  </behavior>
  <action>
    Create `scripts/check-phase92-address-boundaries.test.ts` in the same style as `scripts/check-phase91-peer-permissions.test.ts`. Define constants `SURFACE_ID = "v1-9-address-advertisement-discovery-boundaries"`, `PHASE92_REQUIREMENTS = ["ADDR-01", "ADDR-02", "ADDR-03", "ADDR-04"]`, `PHASE92_TEST_COMMAND = "bun test scripts/check-phase92-address-boundaries.test.ts"`, and `PHASE92_CHECKER_COMMAND = "bun run scripts/check-phase92-address-boundaries.ts"`. Write failing tests first for requirement coverage, required docs/status labels, UAT command fragments, source breadcrumb coverage, verifier order, raw-evidence rejection, default verifier public-network-command rejection, scoped positive no-claim rejection, and explicit allowance for required negative/no-claim sentences such as `remain outside this surface`. Add a skeletal exported `checkPhase92AddressBoundaries` only if needed for the red step; do not implement passing logic until Task 2. This implements D-17, D-18, D-19, D-20, and D-21.
  </action>
  <verify>
    <automated>bun test scripts/check-phase92-address-boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "checkPhase92AddressBoundaries|v1-9-address-advertisement-discovery-boundaries|ADDR-01|ADDR-02|ADDR-03|ADDR-04|Phase 92 no-claim boundary|Phase 92 raw evidence boundary|verifier-order" scripts/check-phase92-address-boundaries.test.ts` finds matches.
    - The initial red run fails before Task 2 because `scripts/check-phase92-address-boundaries.ts` does not yet satisfy all fixture expectations.
    - After Task 2, `bun test scripts/check-phase92-address-boundaries.test.ts` passes.
  </acceptance_criteria>
  <done>Checker tests fully specify the Phase 92 deterministic evidence and no-claim boundaries before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 92 checker and default verifier wiring</name>
  <files>scripts/check-phase92-address-boundaries.ts, scripts/verify.sh</files>
  <read_first>
    - scripts/check-phase92-address-boundaries.ts
    - scripts/check-phase92-address-boundaries.test.ts
    - scripts/check-phase91-peer-permissions.ts
    - scripts/verify.sh
    - docs/operator/runtime-guide.md
    - docs/architecture/status-snapshot.md
    - docs/architecture/operator-observability.md
    - docs/parity/catalog/p2p.md
    - docs/parity/checklist.md
    - docs/parity/index.json
    - docs/parity/source-breadcrumbs.json
  </read_first>
  <behavior>
    - Test 1: checker returns no failures on the real repo corpus after Plans 01-08 are complete.
    - Test 2: checker requires status/support labels `local_advertisement_candidates`, `suppressed_advertisements`, `not_publicly_routable`, `bounded getaddr`, `learned_address_entries`, `learned_address_rejections`, `latest_address_decision`, and `full_relay_deferred`.
    - Test 3: checker requires source breadcrumbs for `packages/open-bitcoin-network/src/address.rs`, `address/advertisement.rs`, `address/book.rs`, `address/response.rs`, and `address/tests.rs`.
    - Test 4: checker rejects public-network default verifier fragments `curl `, `nc `, `systemctl`, `launchctl`, `dig `, `nslookup`, `--public-network`, and `multi-day`.
    - Test 5: checker passes only when `scripts/verify.sh` includes Phase 92 test/check lines immediately after Phase 91 check lines in both printed command help and executed `run_step` order.
  </behavior>
  <action>
    Implement `scripts/check-phase92-address-boundaries.ts` with exported `checkPhase92AddressBoundaries(options?: { rootDir?: string }): string[]` and a CLI entrypoint that prints `validated Phase 92 address advertisement and discovery boundary evidence` on success and exits nonzero with one failure per line on error. Build a deterministic corpus from exactly these files: `docs/operator/runtime-guide.md`, `docs/architecture/status-snapshot.md`, `docs/architecture/operator-observability.md`, `docs/parity/catalog/p2p.md`, `docs/parity/checklist.md`, `docs/parity/index.json`, `docs/parity/source-breadcrumbs.json`, `packages/open-bitcoin-cli/src/operator/status/render/inbound.rs`, `packages/open-bitcoin-cli/src/operator/support/render/inbound.rs`, and `scripts/verify.sh`. Require surface ID `v1-9-address-advertisement-discovery-boundaries`, requirement IDs `ADDR-01` through `ADDR-04`, docs/status/support labels, repo-local Cargo/Bazel command fragments from Plan 08, source breadcrumb mappings, and verifier order. Reject only scoped positive claims for full address relay, peer discovery support, public inbound by default, public-network readiness, production full-node readiness, unsolicited address relay, addr gossip relay, DNS seed discovery support, UPnP/NAT-PMP discovery support, `-discover` parity, and `-externalip` parity. Explicitly allow required negative/no-claim sentences such as `remain outside this surface`, `does not imply`, and `without claiming full address relay` so Plans 07 and 08 can document boundaries. Reject raw status/support leakage strings `address_bytes`, `peer_id=`, `raw_permission`, `operator_loopback`, `inbound.allow_public=true`, and loopback/private socket strings in support-rendered evidence tests. Wire `scripts/verify.sh` with `bun test scripts/check-phase92-address-boundaries.test.ts` and `bun run scripts/check-phase92-address-boundaries.ts` immediately after the Phase 91 checker in both the printed command list and executable `run_step` list. This implements D-17, D-18, D-19, D-20, and D-21.
  </action>
  <verify>
    <automated>bun test scripts/check-phase92-address-boundaries.test.ts</automated>
    <automated>bun run scripts/check-phase92-address-boundaries.ts</automated>
    <automated>bash scripts/verify.sh --fast</automated>
    <automated>bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validated Phase 92 address advertisement and discovery boundary evidence|PHASE92_REQUIREMENTS|v1-9-address-advertisement-discovery-boundaries|ADDR-04" scripts/check-phase92-address-boundaries.ts` finds matches.
    - `rg -n "test Phase 92 address boundaries checker|check Phase 92 address boundaries|check-phase92-address-boundaries" scripts/verify.sh` finds exactly the printed command and `run_step` entries.
    - `bun test scripts/check-phase92-address-boundaries.test.ts` passes.
    - `bun run scripts/check-phase92-address-boundaries.ts` passes.
    - `bash scripts/verify.sh --fast` passes as local iteration evidence.
    - `bash scripts/verify.sh` passes as the required repo-native final verification contract.
  </acceptance_criteria>
  <done>Default verification enforces Phase 92 evidence, order, requirement coverage, source breadcrumbs, redaction, and no false relay/public-network claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/code -> deterministic checker | Human-maintained evidence is converted into machine-enforced release boundaries. |
| checker -> default verifier | A local checker becomes part of the repo-wide verification contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-09-01 | Tampering | checker corpus | mitigate | Use a fixed file list and explicit required strings rather than globbing arbitrary docs. |
| T-92-09-02 | Repudiation | no-claim enforcement | mitigate | Reject scoped positive claims for full relay, peer discovery, public inbound defaults, public-network readiness, and production full-node readiness while allowing required negative boundary wording. |
| T-92-09-03 | Information Disclosure | support/status evidence checks | mitigate | Reject raw endpoint, address byte, peer ID, raw permission, and raw config leakage strings in CLI evidence files. |
| T-92-09-04 | Denial of Service | default verifier | mitigate | Keep checker deterministic and local-only; reject curl/nc/systemctl/launchctl/DNS/public-network/multi-day command fragments in `scripts/verify.sh`. |
| T-92-09-05 | Spoofing | verifier order | mitigate | Require Phase 92 checker lines after Phase 91 and before pure-core checks in both help text and executed steps. |
</threat_model>

<verification>
Run checker verification:
- `bun test scripts/check-phase92-address-boundaries.test.ts`
- `bun run scripts/check-phase92-address-boundaries.ts`
- `bash scripts/verify.sh --fast` for local iteration evidence
- `bash scripts/verify.sh` for the required repo-native final verification contract
</verification>

<success_criteria>
ADDR-01 through ADDR-04 are protected by deterministic verification, the default verifier stays local-only, and Phase 92 cannot silently drift into full relay/discovery/public-network claims.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-09-SUMMARY.md`.
</output>
