---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/address.rs
  - packages/open-bitcoin-network/src/address/advertisement.rs
  - packages/open-bitcoin-network/src/address/tests.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [ADDR-01, ADDR-03]
must_haves:
  truths:
    - "Local listener advertisement candidates are derived only from configured listener endpoints and runtime listener evidence."
    - "Loopback, private, unspecified, multicast, documentation, and privacy-network addresses are suppressed with stable reasons."
    - "Version-message sender address selection remains zero unless the same typed advertisement policy returns an advertised candidate."
  artifacts:
    - path: "packages/open-bitcoin-network/src/address.rs"
      provides: "Address boundary domain types and exports"
    - path: "packages/open-bitcoin-network/src/address/advertisement.rs"
      provides: "Pure local listener advertisement candidate selection"
    - path: "packages/open-bitcoin-network/src/address/tests.rs"
      provides: "Deterministic routability, privacy-boundary, and sender-address gating tests"
    - path: "docs/parity/source-breadcrumbs.json"
      provides: "Breadcrumb registration for new address policy Rust files"
  key_links:
    - from: "packages/open-bitcoin-network/src/address/advertisement.rs"
      to: "packages/open-bitcoin-network/src/inbound.rs"
      via: "InboundListenerEndpoint inputs"
      pattern: "InboundListenerEndpoint"
    - from: "packages/open-bitcoin-network/src/lib.rs"
      to: "packages/open-bitcoin-network/src/address.rs"
      via: "crate re-export"
      pattern: "pub use address::"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:23:41Z
---

<objective>
Create the pure Phase 92 local address advertisement boundary.

Purpose: Keep address advertisement as a typed, data-in/data-out decision over Open Bitcoin listener config and runtime listener evidence, so no runtime code infers public addresses from interfaces, DNS, UPnP/NAT-PMP, outbound peers, or external IP probes.
Output: New `open-bitcoin-network` address policy module, tests, exports, and breadcrumb registration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@packages/open-bitcoin-network/src/inbound.rs
@packages/open-bitcoin-network/src/message.rs
@packages/open-bitcoin-network/src/lib.rs
@docs/parity/source-breadcrumbs.json
@packages/bitcoin-knots/src/netaddress.h
@packages/bitcoin-knots/src/netaddress.cpp
@packages/bitcoin-knots/src/net.cpp

<interfaces>
Existing listener endpoint contract:
```rust
pub struct InboundListenerEndpoint {
    pub raw: String,
    pub normalized: String,
    pub address: SocketAddr,
}
```

Existing wire address contract:
```rust
pub struct NetworkAddress {
    pub services: u64,
    pub address_bytes: [u8; 16],
    pub port: u16,
}
```
</interfaces>

<parity_breadcrumbs>
Exact source breadcrumb group for Phase 92 address-boundary Rust files:
```json
{
  "label": "network-address-boundaries",
  "files": [
    "packages/open-bitcoin-network/src/address.rs",
    "packages/open-bitcoin-network/src/address/advertisement.rs",
    "packages/open-bitcoin-network/src/address/book.rs",
    "packages/open-bitcoin-network/src/address/response.rs",
    "packages/open-bitcoin-network/src/address/tests.rs"
  ],
  "breadcrumbs": [
    "packages/bitcoin-knots/src/protocol.h",
    "packages/bitcoin-knots/src/protocol.cpp",
    "packages/bitcoin-knots/src/netaddress.h",
    "packages/bitcoin-knots/src/netaddress.cpp",
    "packages/bitcoin-knots/src/net.h",
    "packages/bitcoin-knots/src/net.cpp",
    "packages/bitcoin-knots/src/net_processing.cpp",
    "packages/bitcoin-knots/src/addrman.h",
    "packages/bitcoin-knots/src/addrman.cpp",
    "packages/bitcoin-knots/src/addrdb.h",
    "packages/bitcoin-knots/src/addrdb.cpp"
  ]
}
```

Exact top-of-file block produced by `bun run scripts/check-parity-breadcrumbs.ts --write` for each file in that group:
```rust
// Parity breadcrumbs:
// - packages/bitcoin-knots/src/protocol.h
// - packages/bitcoin-knots/src/protocol.cpp
// - packages/bitcoin-knots/src/netaddress.h
// - packages/bitcoin-knots/src/netaddress.cpp
// - packages/bitcoin-knots/src/net.h
// - packages/bitcoin-knots/src/net.cpp
// - packages/bitcoin-knots/src/net_processing.cpp
// - packages/bitcoin-knots/src/addrman.h
// - packages/bitcoin-knots/src/addrman.cpp
// - packages/bitcoin-knots/src/addrdb.h
// - packages/bitcoin-knots/src/addrdb.cpp
```
</parity_breadcrumbs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed address boundary contracts</name>
  <files>packages/open-bitcoin-network/src/address.rs, packages/open-bitcoin-network/src/address/tests.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-network/src/lib.rs`
    - `docs/parity/source-breadcrumbs.json`
    - `packages/bitcoin-knots/src/netaddress.h`
    - `packages/bitcoin-knots/src/netaddress.cpp`
    - `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md`
  </read_first>
  <behavior>
    - Test IPv4 and IPv6 public candidates classify as `publicly_routable`.
    - Test loopback, private, unspecified, multicast, documentation, and link-local inputs classify as `not_publicly_routable`.
    - Test unsupported privacy-network inputs classify as `privacy_network_deferred` without address bytes in an advertised candidate.
  </behavior>
  <action>
    Create `packages/open-bitcoin-network/src/address.rs` and `packages/open-bitcoin-network/src/address/tests.rs`. Add or update `docs/parity/source-breadcrumbs.json` with the exact `network-address-boundaries` group entry shown in `<parity_breadcrumbs>` before applying breadcrumb comments. Run `git add -N packages/open-bitcoin-network/src/address.rs packages/open-bitcoin-network/src/address/tests.rs`, then `bun run scripts/check-parity-breadcrumbs.ts --write`, and ensure both new files receive the exact top-of-file `// Parity breadcrumbs:` block shown in `<parity_breadcrumbs>` before `--check`; do not leave this source breadcrumb coverage to Plan 92-08. Define the single Phase 92 address evidence vocabulary that all later peer, status, CLI, docs, and checker plans must preserve without renaming. The closed domain types must expose stable `as_str()` labels: `AddressNetworkKind` (`ipv4`, `ipv6`, `unsupported_privacy_network`, `unsupported_future_network`), `RoutabilityClass` (`publicly_routable`, `not_publicly_routable`, `invalid`, `privacy_network_deferred`), `AddressSourceKind` variants `LocalListener` -> `source_local_listener` and `InboundAddr` -> `source_inbound_addr`, `AddressDecisionLabel` (`advertise_candidate`, `advertise_suppressed`, `learned_accepted`, `learned_rejected`, `getaddr_served`, `getaddr_suppressed`, `full_relay_deferred`), and `AddressDecisionReason` (`policy_accepted`, `not_publicly_routable`, `privacy_network_deferred`, `unsupported_address_network`, `invalid_port`, `stale_or_future`, `duplicate_address`, `over_cap_batch`, `not_inbound`, `permission_policy_denied`, `already_served`, `empty_response_cache`). Export the new address module from `packages/open-bitcoin-network/src/lib.rs`. This implements D-02, D-03, D-04, D-11, and D-21.
  </action>
  <acceptance_criteria>
    - `rg -n "pub enum AddressNetworkKind|pub enum RoutabilityClass|pub enum AddressSourceKind|pub enum AddressDecisionLabel|pub enum AddressDecisionReason" packages/open-bitcoin-network/src/address.rs` finds all enums.
    - `rg -n "publicly_routable|not_publicly_routable|privacy_network_deferred|source_local_listener|source_inbound_addr|full_relay_deferred|not_inbound|permission_policy_denied|already_served|empty_response_cache" packages/open-bitcoin-network/src/address.rs packages/open-bitcoin-network/src/address/tests.rs` finds stable labels and tests.
    - `rg -n "pub use address::|mod address" packages/open-bitcoin-network/src/lib.rs` finds crate exports.
    - `rg -n "network-address-boundaries|packages/open-bitcoin-network/src/address.rs|packages/open-bitcoin-network/src/address/tests.rs|packages/bitcoin-knots/src/protocol.cpp|packages/bitcoin-knots/src/netaddress.cpp|packages/bitcoin-knots/src/addrdb.cpp" docs/parity/source-breadcrumbs.json` finds the exact breadcrumb mapping paths.
    - `rg -n "^// Parity breadcrumbs:$|packages/bitcoin-knots/src/protocol.h|packages/bitcoin-knots/src/protocol.cpp|packages/bitcoin-knots/src/netaddress.h|packages/bitcoin-knots/src/netaddress.cpp|packages/bitcoin-knots/src/net.h|packages/bitcoin-knots/src/net.cpp|packages/bitcoin-knots/src/net_processing.cpp|packages/bitcoin-knots/src/addrman.h|packages/bitcoin-knots/src/addrman.cpp|packages/bitcoin-knots/src/addrdb.h|packages/bitcoin-knots/src/addrdb.cpp" packages/open-bitcoin-network/src/address.rs packages/open-bitcoin-network/src/address/tests.rs` finds the applied top-of-file breadcrumb blocks.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network address --no-fail-fast</automated>
    <automated>git add -N packages/open-bitcoin-network/src/address.rs packages/open-bitcoin-network/src/address/tests.rs</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --write</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <done>The address boundary vocabulary is typed, exported, tested, and breadcrumb-registered; unsupported privacy-network values cannot be advertised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement local listener advertisement selection</name>
  <files>packages/open-bitcoin-network/src/address.rs, packages/open-bitcoin-network/src/address/advertisement.rs, packages/open-bitcoin-network/src/address/tests.rs, packages/open-bitcoin-network/src/lib.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - `packages/open-bitcoin-network/src/address.rs`
    - `packages/open-bitcoin-network/src/inbound.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `docs/parity/source-breadcrumbs.json`
    - `scripts/check-parity-breadcrumbs.ts`
    - `packages/bitcoin-knots/src/net.cpp`
    - `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md`
  </read_first>
  <behavior>
    - Test selection starts only from `InboundListenerEndpoint` and runtime-bound `SocketAddr` evidence.
    - Test `127.0.0.1`, `10.0.0.1`, `172.16.0.1`, `192.168.0.1`, `0.0.0.0`, `224.0.0.1`, `192.0.2.1`, `203.0.113.1`, `::1`, `::`, `ff02::1`, `fc00::1`, `fe80::1`, and `2001:db8::1` are `advertise_suppressed` with `not_publicly_routable`.
    - Test a routable IPv4 listener with `allow_public=true` becomes `advertise_candidate`, carries services bits and port, and can be converted to a version sender address.
    - Test version sender gating returns zero address when no candidate passes.
  </behavior>
  <action>
    Create `packages/open-bitcoin-network/src/address/advertisement.rs` and expose it through `address.rs`. Ensure the exact `network-address-boundaries` group entry shown in `<parity_breadcrumbs>` includes `packages/open-bitcoin-network/src/address/advertisement.rs` before applying breadcrumb comments. Run `git add -N packages/open-bitcoin-network/src/address/advertisement.rs`, then `bun run scripts/check-parity-breadcrumbs.ts --write`, and ensure `address/advertisement.rs` receives the exact top-of-file `// Parity breadcrumbs:` block shown in `<parity_breadcrumbs>` before `--check`; do not leave this source breadcrumb coverage to Plan 92-08. Define `LocalAdvertisementInput { listener_endpoint: InboundListenerEndpoint, maybe_bound_addr: Option<SocketAddr>, services: ServiceFlags, allow_public: bool }`, `LocalAdvertisementDecision { label: AddressDecisionLabel, reason: AddressDecisionReason, source: AddressSourceKind, network_kind: AddressNetworkKind, routability: RoutabilityClass, services_bits: u64, port: u16, maybe_wire_address: Option<NetworkAddress> }`, `select_local_advertisement_candidates(inputs: &[LocalAdvertisementInput]) -> Vec<LocalAdvertisementDecision>`, and `maybe_version_sender_address(decisions: &[LocalAdvertisementDecision]) -> Option<NetworkAddress>`. Use only `inbound.listen_addresses`, `InboundListenerEndpoint`, and runtime-bound listener evidence per D-01. Do not inspect interfaces, outbound peers, DNS, UPnP/NAT-PMP, NAT-PMP, external IP probes, `-externalip`, or `-discover`. This implements D-01, D-02, D-03, D-04, D-05, and D-21.
  </action>
  <acceptance_criteria>
    - `rg -n "struct LocalAdvertisementInput|struct LocalAdvertisementDecision|select_local_advertisement_candidates|maybe_version_sender_address" packages/open-bitcoin-network/src/address/advertisement.rs packages/open-bitcoin-network/src/address.rs` finds the API.
    - `rg -n "advertise_candidate|advertise_suppressed|not_publicly_routable|privacy_network_deferred" packages/open-bitcoin-network/src/address/advertisement.rs packages/open-bitcoin-network/src/address/tests.rs` finds policy labels and tests.
    - `rg -n "packages/open-bitcoin-network/src/address/advertisement.rs|packages/bitcoin-knots/src/net.h|packages/bitcoin-knots/src/net.cpp" docs/parity/source-breadcrumbs.json packages/open-bitcoin-network/src/address/advertisement.rs` finds the exact breadcrumb mapping and applied block.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
    - `! rg -n "externalip|discover|interface|UPnP|NAT|dns|outbound peer" packages/open-bitcoin-network/src/address/advertisement.rs` proves forbidden discovery inputs were not introduced.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network address --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
    <automated>git add -N packages/open-bitcoin-network/src/address/advertisement.rs</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --write</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <done>Local address advertisement is a pure bounded policy over typed listener evidence, and sender-address gating stays conservative unless the same policy returns a candidate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listener config/runtime evidence -> address policy | Configured listener endpoints and runtime-bound addresses become candidate advertisement decisions. |
| address policy -> wire/version sender | Only accepted candidates may become sender addresses or later getaddr response entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Information Disclosure | `select_local_advertisement_candidates` | mitigate | Suppress loopback, private, unspecified, multicast, documentation, link-local, and privacy-network addresses with stable reasons before wire conversion. |
| T-92-01-02 | Spoofing | local advertisement inputs | mitigate | Accept only `InboundListenerEndpoint` and runtime-bound listener evidence; no external IP probes, DNS, UPnP/NAT-PMP, outbound peer hints, `-externalip`, or `-discover`. |
| T-92-01-03 | Information Disclosure | `maybe_version_sender_address` | mitigate | Return `None` unless a policy-approved candidate exists; callers keep zero-address sender behavior otherwise. |
| T-92-01-04 | Repudiation | status/doc evidence labels | mitigate | Use closed stable labels (`advertise_candidate`, `advertise_suppressed`, `not_publicly_routable`, `privacy_network_deferred`) that later checkers can verify. |
</threat_model>

<verification>
Run focused pure-network verification:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network address --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
- `bun run scripts/check-parity-breadcrumbs.ts --write`
- `bun run scripts/check-parity-breadcrumbs.ts --check`
</verification>

<success_criteria>
ADDR-01 has a pure, typed local advertisement foundation. ADDR-03 has the shared address vocabulary needed by learned-address work. No full address relay, peer discovery, DNS seed, public-network default, or production-readiness claim is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-01-SUMMARY.md`.
</output>
