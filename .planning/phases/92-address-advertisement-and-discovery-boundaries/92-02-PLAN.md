---
phase: 92-address-advertisement-and-discovery-boundaries
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - packages/open-bitcoin-network/src/address.rs
  - packages/open-bitcoin-network/src/message.rs
  - packages/open-bitcoin-network/src/message/tests.rs
autonomous: true
requirements: [ADDR-01, ADDR-02, ADDR-03]
must_haves:
  truths:
    - "`getaddr` and legacy `addr` are wire-supported only for bounded direct request/response and learned-address intake."
    - "`addrv2` and `sendaddrv2` remain unknown or deferred surfaces."
    - "Version-message sender address behavior keeps zero-address output unless a policy-approved local candidate is supplied."
  artifacts:
    - path: "packages/open-bitcoin-network/src/message.rs"
      provides: "WireNetworkMessage::GetAddr, WireNetworkMessage::Addr, payload codecs, and sender-address gating helper"
    - path: "packages/open-bitcoin-network/src/message/tests.rs"
      provides: "Round-trip, cap, empty-payload, unknown addrv2, and sender-gating tests"
    - path: "packages/open-bitcoin-network/src/address.rs"
      provides: "AddressAnnouncement and AddressList wire-facing contract"
  key_links:
    - from: "packages/open-bitcoin-network/src/message.rs"
      to: "packages/open-bitcoin-codec/src/network.rs"
      via: "encode_network_address/parse_network_address"
      pattern: "encode_network_address|parse_network_address"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 92-2026-06-26T03-52-33
generated_at: 2026-06-26T04:23:41Z
---

<objective>
Add bounded legacy `getaddr` and `addr` wire support plus conservative version sender gating.

Purpose: Let later peer policy answer direct inbound address requests and parse learned address announcements without adding gossip relay, addr rebroadcast, addrv2 relay, DNS seeds, or public discovery behavior.
Output: Wire message variants, address payload types, codec tests, and a sender-address helper that defaults to zero-address behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-RESEARCH.md
@.planning/phases/92-address-advertisement-and-discovery-boundaries/92-01-SUMMARY.md
@packages/open-bitcoin-network/src/address.rs
@packages/open-bitcoin-network/src/message.rs
@packages/open-bitcoin-network/src/message/tests.rs
@packages/open-bitcoin-codec/src/network.rs
@packages/bitcoin-knots/src/protocol.h
@packages/bitcoin-knots/src/net_processing.cpp

<interfaces>
Existing message extension point:
```rust
pub enum WireNetworkMessage {
    Version(VersionMessage),
    Verack,
    // ...
}

impl WireNetworkMessage {
    pub fn command_name(&self) -> &'static str;
    pub fn encode_payload(&self) -> Result<Vec<u8>, NetworkError>;
    pub fn decode_payload(command: &MessageCommand, payload: &[u8]) -> Result<Self, NetworkError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded `getaddr` and legacy `addr` payload support</name>
  <files>packages/open-bitcoin-network/src/address.rs, packages/open-bitcoin-network/src/message.rs, packages/open-bitcoin-network/src/message/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/address.rs`
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-network/src/message/tests.rs`
    - `packages/open-bitcoin-codec/src/network.rs`
    - `packages/bitcoin-knots/src/protocol.h`
    - `packages/bitcoin-knots/src/net_processing.cpp`
  </read_first>
  <behavior>
    - Test `WireNetworkMessage::GetAddr` encodes and decodes with command `getaddr` and empty payload.
    - Test `getaddr` with any non-empty payload fails with trailing data.
    - Test `WireNetworkMessage::Addr(AddressList)` round-trips timestamped legacy address records with compact-size count, u32 little-endian time, and the existing 26-byte network address codec.
    - Test `addr` with 65 records fails when `PHASE92_ADDR_BATCH_LIMIT` is 64.
    - Test `addrv2` and `sendaddrv2` remain unknown commands.
  </behavior>
  <action>
    In `packages/open-bitcoin-network/src/address.rs`, add `pub const PHASE92_ADDR_BATCH_LIMIT: usize = 64`, `AddressAnnouncement { pub time_unix_seconds: u32, pub address: NetworkAddress }`, and `AddressList { pub addresses: Vec<AddressAnnouncement> }`. In `packages/open-bitcoin-network/src/message.rs`, add `WireNetworkMessage::GetAddr` and `WireNetworkMessage::Addr(AddressList)`, command names `getaddr` and `addr`, empty-payload validation for `getaddr`, and bounded legacy `addr` payload encode/decode. Each `addr` record must encode as `time_unix_seconds.to_le_bytes()` plus `encode_network_address(&announcement.address)`. Do not add `addrv2`, `sendaddrv2`, unsolicited relay, rebroadcast scheduling, trickle relay, bloom known-address filters, or full relay peer selection. This implements D-06, D-08, D-09, D-12, and D-19.
  </action>
  <acceptance_criteria>
    - `rg -n "PHASE92_ADDR_BATCH_LIMIT|struct AddressAnnouncement|struct AddressList" packages/open-bitcoin-network/src/address.rs` finds the wire address contract.
    - `rg -n "GetAddr|Addr\\(AddressList\\)|\"getaddr\"|\"addr\"" packages/open-bitcoin-network/src/message.rs packages/open-bitcoin-network/src/message/tests.rs` finds variants, commands, and tests.
    - `rg -n "addrv2|sendaddrv2" packages/open-bitcoin-network/src/message/tests.rs` finds unknown-command coverage.
    - `! rg -n "MaybeSendAddr|PushAddress|trickle|fanout|rebroadcast|addrv2|sendaddrv2" packages/open-bitcoin-network/src/message.rs` proves relay and addrv2 behavior were not introduced.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network message --no-fail-fast</automated>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>`getaddr` and legacy `addr` have bounded wire support; `addrv2` remains deferred; payload caps and empty-payload validation are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate version-message sender address</name>
  <files>packages/open-bitcoin-network/src/message.rs, packages/open-bitcoin-network/src/message/tests.rs</files>
  <read_first>
    - `packages/open-bitcoin-network/src/message.rs`
    - `packages/open-bitcoin-network/src/message/tests.rs`
    - `packages/open-bitcoin-network/src/address/advertisement.rs`
    - `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-CONTEXT.md`
  </read_first>
  <behavior>
    - Test `LocalPeerConfig::version_message` preserves existing receiver and sender behavior for current callers.
    - Test the new sender-gated helper returns zero sender when `maybe_sender` is `None`.
    - Test the new sender-gated helper uses the supplied sender only when a policy caller provides `Some(NetworkAddress)`.
  </behavior>
  <action>
    Add `LocalPeerConfig::version_message_with_sender_policy(&self, timestamp: i64, start_height: i32, maybe_sender: Option<NetworkAddress>) -> VersionMessage`. It must call the existing `version_message` shape for all fields, then set `sender` to `maybe_sender.unwrap_or_else(zero_address)`. Keep the existing `version_message` behavior unchanged for current callers. Do not make runtime code choose a sender here; this helper only consumes the pure policy output from Plan 92-01. This implements D-05.
  </action>
  <acceptance_criteria>
    - `rg -n "version_message_with_sender_policy|maybe_sender|zero_address" packages/open-bitcoin-network/src/message.rs packages/open-bitcoin-network/src/message/tests.rs` finds the helper and tests.
    - `rg -n "local_peer_config_builds_expected_version_message" packages/open-bitcoin-network/src/message/tests.rs` still finds the existing compatibility test.
    - `! rg -n "externalip|discover|interface|dns|UPnP|NAT" packages/open-bitcoin-network/src/message.rs` proves sender selection does not infer addresses.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network message --no-fail-fast</automated>
  </verify>
  <done>Version sender behavior remains conservative and can only use an address already accepted by the pure advertisement policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer wire bytes -> typed message | Untrusted `getaddr` and `addr` bytes are parsed into typed messages. |
| local address policy -> version sender | A policy-approved sender address may cross into the `version` payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Denial of Service | `addr` payload decoder | mitigate | Enforce `PHASE92_ADDR_BATCH_LIMIT = 64` before allocation-heavy decode work. |
| T-92-02-02 | Information Disclosure | version sender helper | mitigate | Default to zero address unless caller supplies a policy-approved sender. |
| T-92-02-03 | Tampering | wire parser | mitigate | Reject non-empty `getaddr` payloads and malformed/trailing `addr` payloads through existing typed codec errors. |
| T-92-02-04 | Repudiation | `addrv2`/relay boundary | mitigate | Tests prove `addrv2` and `sendaddrv2` remain unknown; no relay scheduling strings are introduced. |
</threat_model>

<verification>
Run focused network message verification:
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network message --no-fail-fast`
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>
ADDR-02 has bounded wire-level `getaddr` and `addr` support. ADDR-01 sender-address gating is available without changing existing behavior. ADDR-03 has the timestamped address announcement shape required by learned-address policy.
</success_criteria>

<output>
After completion, create `.planning/phases/92-address-advertisement-and-discovery-boundaries/92-02-SUMMARY.md`.
</output>
