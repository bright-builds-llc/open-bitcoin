---
phase: 07.3-reduce-nesting-with-early-returns
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-network/src/peer.rs
  - packages/open-bitcoin-network/src/peer/tests.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
autonomous: true
requirements:
  - TBD
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.3-2026-04-19T01-44-09
generated_at: 2026-04-19T01:44:09.175Z
must_haves:
  truths:
    - Peer message handling keeps the same wire-message actions and unknown-peer errors while repeated lookup and request-cleanup branches stop obscuring the main protocol dispatch.
    - Mempool admission and replacement keep the same fee, feerate, and unconfirmed-input decisions while the replacement gate reads as a sequence of clear policy guards.
  artifacts:
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-mempool/src/pool.rs
  key_links:
    - packages/open-bitcoin-network/src/peer.rs still dispatches through the single `handle_message` `match` and returns `PeerAction` values.
    - packages/open-bitcoin-mempool/src/pool.rs still validates transactions, computes the replacement set, recomputes state, and trims size in the same order.
---

<objective>
Flatten the highest-confidence networking and mempool control-flow hotspots without rewriting protocol or policy algorithms.

Purpose: make the peer and mempool crates easier to scan before Phase 8 uses them from CLI and RPC adapters.
Output: shallower `peer.rs` and `pool.rs` functions plus only the narrow test adjustments needed to preserve behavior and coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-network/src/peer.rs:
```rust
pub fn handle_message(
    &mut self,
    peer_id: PeerId,
    message: WireNetworkMessage,
    timestamp: i64,
) -> Result<Vec<PeerAction>, NetworkError>;
```

From packages/open-bitcoin-mempool/src/pool.rs:
```rust
pub fn accept_transaction(
    &mut self,
    transaction: Transaction,
    chainstate: &ChainstateSnapshot,
    verify_flags: ScriptVerifyFlags,
    consensus_params: ConsensusParams,
) -> Result<AdmissionResult, MempoolError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flatten repeated peer lookup and request cleanup without rewriting dispatch</name>
  <files>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs</files>
  <read_first>packages/open-bitcoin-network/src/peer.rs, packages/open-bitcoin-network/src/peer/tests.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md</read_first>
  <behavior>
    - `handle_message` still dispatches every `WireNetworkMessage` variant from the existing top-level `match`.
    - Unknown-peer handling, requested block or transaction cleanup, and emitted `PeerAction` values remain unchanged for `NotFound`, `Headers`, `Tx`, and `Block`.
  </behavior>
  <action>In `packages/open-bitcoin-network/src/peer.rs`, refactor the audited hotspots per D-01, D-03, D-04, and D-05. Add a single private peer-lookup helper and, if it materially clarifies the file, one small request-cleanup helper so `handle_message`, `handle_inventory`, `handle_headers`, `handle_transaction`, and `handle_block` stop repeating the same `UnknownPeer` and requested-item removal scaffolding. Keep the protocol-shaped `match` in `handle_message` visible, keep `handle_inventory` and `handle_headers` as the message-specific algorithms, and do not flatten them into generic tables or mechanically rewrite state-machine branches. Update `packages/open-bitcoin-network/src/peer/tests.rs` only when D-06 requires a narrow regression or coverage assertion for the refactored guard paths.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features</automated>
  </verify>
  <acceptance_criteria>
    - File-read verification shows `packages/open-bitcoin-network/src/peer.rs` centralizes mutable peer lookup instead of repeating `self.peers.get_mut(&peer_id)` in every audited handler.
    - File-read verification shows `handle_message` still contains the single `WireNetworkMessage` dispatch `match`.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --all-features` passes.
  </acceptance_criteria>
  <done>`packages/open-bitcoin-network/src/peer.rs` keeps the same peer actions and error behavior while repeated guard scaffolding is flattened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flatten replacement-policy guards in the audited mempool hotspot</name>
  <files>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/tests.rs</files>
  <read_first>packages/open-bitcoin-mempool/src/pool.rs, packages/open-bitcoin-mempool/src/pool/tests.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md</read_first>
  <behavior>
    - `accept_transaction` still computes input contexts, policy checks, consensus validation, replacement set, prospective state, and trim-to-size in the current order.
    - `validate_replacement` still rejects the same RBF-policy, fee, feerate, fee-bump, and new-unconfirmed-input cases.
  </behavior>
  <action>In `packages/open-bitcoin-mempool/src/pool.rs`, refactor only `accept_transaction` and `validate_replacement` per D-01, D-03, D-04, and D-06. Keep the current admission algorithm and replacement math, but rewrite the replacement gate so `RbfPolicy::Never`, non-signaled `RbfPolicy::OptIn`, insufficient absolute fee, insufficient feerate, insufficient fee bump, and new unconfirmed inputs exit early instead of being buried inside broader branches. If helper extraction is needed, keep it private and scoped to the policy gate or conflicting-input collection; do not rewrite ancestor or descendant traversal, eviction ordering, or the surrounding prospective-state flow. Update `packages/open-bitcoin-mempool/src/pool/tests.rs` only when a narrow regression or coverage guard is required.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-features</automated>
  </verify>
  <acceptance_criteria>
    - File-read verification shows `packages/open-bitcoin-mempool/src/pool.rs` still exposes the same `accept_transaction` and `validate_replacement` signatures.
    - File-read verification shows `validate_replacement` is organized as sequential guard exits for the audited rejection cases instead of a larger nested policy branch.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --all-features` passes.
  </acceptance_criteria>
  <done>`packages/open-bitcoin-mempool/src/pool.rs` preserves the current policy behavior while the replacement path is flatter and easier to audit.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if flattening would hide the protocol dispatch, change emitted `PeerAction` values, or alter replacement-policy outcomes, stop and keep the current state-machine shape.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer message dispatch -> peer actions | Control-flow cleanup must not change the network state machine or unknown-peer handling |
| mempool replacement guards -> admission decisions | Readability edits must not change replacement acceptance or rejection outcomes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-03 | T | `packages/open-bitcoin-network/src/peer.rs` | mitigate | Keep the top-level message `match`, reuse a narrow peer-lookup helper instead of rewriting dispatch, and verify with the networking crate tests plus full repo verification. |
| T-07.3-04 | D | `packages/open-bitcoin-mempool/src/pool.rs` | mitigate | Limit edits to `accept_transaction` and `validate_replacement`, preserve policy ordering and error text, and stop if flattening would require altering ancestor, descendant, or eviction algorithms. |
</threat_model>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-network -p open-bitcoin-mempool --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network -p open-bitcoin-mempool --all-features`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The audited `peer.rs` and `pool.rs` hotspots are flatter to scan without hiding the underlying protocol or policy flow.
- Any test edits stay limited to the existing sibling test modules and only cover refactored guard paths.
- `bash scripts/verify.sh` passes after the refactor slice.
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-02-SUMMARY.md`
</output>
