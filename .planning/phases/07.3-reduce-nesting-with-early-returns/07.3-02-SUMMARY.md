---
phase: 07.3-reduce-nesting-with-early-returns
plan: 02
subsystem: networking-mempool
tags: [rust, networking, mempool, maintainability, early-returns]
requires:
  - phase: 07.2
    provides: protocol-clarity cleanup that left the peer and mempool hotspots narrow enough for a behavior-neutral early-return pass
provides:
  - centralized mutable peer lookup and request cleanup in the network peer manager
  - ordered replacement-policy guards in mempool admission
  - regression assertions that pin peer request cleanup and replacement rejection reasons
affects: [phase-08-interface-parity, networking, mempool, policy]
tech-stack:
  added: []
  patterns:
    - Private helper extraction that keeps protocol dispatch and admission order visible
    - Sequential guard exits for parity-sensitive replacement-policy checks
key-files:
  created: []
  modified:
    - packages/open-bitcoin-network/src/peer.rs
    - packages/open-bitcoin-network/src/peer/tests.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
key-decisions:
  - Peer message handling should keep the top-level wire-message match visible while sharing only the repeated mutable peer lookup and request cleanup scaffolding.
  - Mempool admission should keep its existing validation and prospective-state order, with replacement selection extracted as a narrow private step before state recomputation.
  - Replacement-policy tests should assert guard-specific rejection reasons so future refactors cannot silently reorder absolute-fee, feerate, fee-bump, or new-unconfirmed-input checks.
patterns-established:
  - "In peer manager handlers, centralize UnknownPeer mutable lookup through a helper that borrows only the peer map so the surrounding protocol state remains readable."
  - "In mempool replacement policy, express each rejection case as its own left-margin guard and pin the reason text in focused tests when guard ordering matters."
requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.3-2026-04-19T01-44-09
generated_at: 2026-04-19T02:36:59Z
duration: 9 min
completed: 2026-04-19
---

# Phase 07.3 Plan 02: Reduce Nesting With Early Returns Summary

**Peer message handling now shares one mutable peer-lookup path and request-forget helper, while mempool admission keeps the same replacement policy through ordered early-exit guards and guard-specific regression assertions.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-19T02:28:00Z
- **Completed:** 2026-04-19T02:36:59Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Reused one private helper in [packages/open-bitcoin-network/src/peer.rs](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-network/src/peer.rs) so the audited handlers stop repeating `UnknownPeer` lookup logic without hiding the top-level `WireNetworkMessage` dispatch match.
- Shared request-cleanup behavior for `NotFound`, `Tx`, and `Block` paths in the peer manager while keeping emitted `PeerAction` values and requested-item bookkeeping unchanged.
- Extracted replacement-set selection in [packages/open-bitcoin-mempool/src/pool.rs](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-mempool/src/pool.rs) so `accept_transaction` stays in the same admission order but reads as a straighter sequence.
- Rewrote `validate_replacement` as sequential policy guards and added direct reason assertions in the sibling tests so absolute-fee, feerate, fee-bump, and new-unconfirmed-input rejections remain auditable.

## Task Commits

Each task was committed atomically:

1. **Task 1: Flatten repeated peer lookup and request cleanup without rewriting dispatch** - `df6b81e` (`refactor`)
2. **Task 2: Flatten replacement-policy guards in the audited mempool hotspot** - `5819d5b` (`refactor`)

## Files Created/Modified

- `packages/open-bitcoin-network/src/peer.rs` - centralizes mutable peer lookup and requested-inventory cleanup while preserving the visible message dispatch and peer actions.
- `packages/open-bitcoin-network/src/peer/tests.rs` - adds direct coverage that received transactions and blocks clear outstanding requested inventory.
- `packages/open-bitcoin-mempool/src/pool.rs` - extracts replacement-set selection and flattens replacement-policy rejections into ordered guard exits.
- `packages/open-bitcoin-mempool/src/pool/tests.rs` - pins absolute-fee, feerate, and fee-bump rejection reasons so guard ordering stays explicit.

## Decisions Made

- Kept the peer refactor limited to shared scaffolding helpers and left the protocol-shaped `handle_message` match intact rather than replacing it with generic dispatch indirection.
- Kept `accept_transaction` in its existing policy and state-transition order, extracting only the replacement-set decision instead of widening into ancestor, descendant, or eviction rewrites.
- Used the repo’s AGENTS.md verification contract and Bright Builds code-shape, verification, testing, and Rust guidance to keep the phase narrow, behavior-neutral, and fully verified before each commit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - AGENTS precedence] Treated TDD-marked refactor tasks as green-only regression work**
- **Found during:** Task 1 and Task 2
- **Issue:** Both plan tasks were behavior-preserving refactors of already-correct code, while the repo’s Rust commit contract forbids failing test commits.
- **Fix:** Added narrow regression assertions for the refactored guard paths and used targeted crate checks plus the full repo verification contract before each commit instead of manufacturing failing red commits.
- **Files modified:** `packages/open-bitcoin-network/src/peer/tests.rs`, `packages/open-bitcoin-mempool/src/pool/tests.rs`
- **Verification:** Targeted crate `cargo clippy` and `cargo test` passed for both crates, and `bash scripts/verify.sh` passed after each task and for the final combined state.

---

**Total deviations:** 1 auto-fixed (1 AGENTS-precedence adjustment)
**Impact on plan:** The adjustment kept the work behavior-neutral and inside the planned hotspot scope while honoring the repo’s stricter Rust verification and commit rules.

## Issues Encountered

None.

## User Setup Required

None - no external services, configuration changes, or manual migration steps are required.

## Next Phase Readiness

- The networking and mempool hotspots targeted by `07.3-02` are flatter to scan and preserve the same peer-action and replacement-policy outcomes expected by later interface work.
- Phase `07.3-03` can focus on the remaining maintainability slice without reopening these peer or mempool seams.
- No new blockers were introduced.

## Self-Check: PASSED

- Verified `.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-02-SUMMARY.md` exists on disk.
- Verified task commits `df6b81e` and `5819d5b` exist in git history.
