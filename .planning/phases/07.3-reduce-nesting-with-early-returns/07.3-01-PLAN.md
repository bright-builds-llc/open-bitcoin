---
phase: 07.3-reduce-nesting-with-early-returns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-consensus/src/block.rs
  - packages/open-bitcoin-consensus/src/block/tests.rs
  - packages/open-bitcoin-chainstate/src/engine.rs
  - packages/open-bitcoin-chainstate/src/engine/tests.rs
autonomous: true
requirements:
  - TBD
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.3-2026-04-19T01-44-09
generated_at: 2026-04-19T01:44:09.175Z
must_haves:
  truths:
    - Block validation keeps the current reject reasons and debug-message shape while the witness-commitment and transaction-validation paths become shallower to read.
    - Chainstate connect and disconnect keep the current UTXO, undo, and mismatch behavior while the coinbase-versus-non-coinbase handling stops driving deep loop nesting.
  artifacts:
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
  key_links:
    - packages/open-bitcoin-consensus/src/block.rs still routes transaction failures through `BlockValidationError` with unchanged reject reasons.
    - packages/open-bitcoin-chainstate/src/engine.rs still builds `TransactionValidationContext` before `validate_transaction_with_context` and records `BlockUndo` before the new tip is pushed.
---

<objective>
Flatten the highest-confidence consensus and chainstate nesting hotspots without changing behavior or public crate surfaces.

Purpose: make the core validation and state-transition code easier to audit before Phase 8 builds new interfaces on top of it.
Output: a shallower `block.rs` and `engine.rs` plus only the narrow sibling-test adjustments needed to preserve behavior and coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-consensus/src/block.rs:
```rust
pub fn validate_block(
    block: &Block,
    spent_outputs: &[Vec<SpentOutput>],
) -> Result<(), BlockValidationError>;

pub fn validate_block_with_context(
    block: &Block,
    transaction_contexts: &[TransactionValidationContext],
    block_context: &BlockValidationContext,
) -> Result<(), BlockValidationError>;
```

From packages/open-bitcoin-chainstate/src/engine.rs:
```rust
impl Chainstate {
    pub fn connect_block(
        &mut self,
        block: &Block,
        chain_work: u128,
        verify_flags: ScriptVerifyFlags,
        consensus_params: ConsensusParams,
    ) -> Result<ChainPosition, ChainstateError>;

    pub fn disconnect_tip(
        &mut self,
        block: &Block,
    ) -> Result<ChainPosition, ChainstateError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flatten block validation guard paths in the audited consensus hotspot</name>
  <files>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/block.rs, packages/open-bitcoin-consensus/src/block/tests.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md</read_first>
  <behavior>
    - Existing block validation tests keep the same reject reasons for missing prevouts, bad witness nonce size, unexpected witness data, and witness commitment mismatch.
    - `validate_block` and `validate_block_with_context` still attach the same txid-based debug-message text when transaction validation fails.
  </behavior>
  <action>In `packages/open-bitcoin-consensus/src/block.rs`, refactor only `validate_block`, `validate_block_with_context`, and `check_witness_commitment` per D-01, D-03, and D-04. Extract a single private helper that maps transaction validation failures into the existing `BlockValidationError` shape so the two public validation entrypoints stop duplicating the same txid/debug-message formatting. Rewrite `check_witness_commitment` around left-margin guard exits: handle the no-commitment and unexpected-witness path first, then keep the enforced segwit commitment path as one straight-line block that validates the reserved value size, computes the expected commitment, and compares the exact commitment bytes. Preserve every public function signature, reject reason, debug message, and sigop-counting path; do not mechanically rewrite merkle logic or unrelated `match` branches. Touch `packages/open-bitcoin-consensus/src/block/tests.rs` only if a narrow existing-test adjustment or one direct regression assertion is required by D-06 to keep coverage or reject-reason evidence intact.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features</automated>
  </verify>
  <acceptance_criteria>
    - File-read verification shows `packages/open-bitcoin-consensus/src/block.rs` now contains one private helper reused by both `validate_block` and `validate_block_with_context` for transaction-validation error mapping.
    - File-read verification shows `check_witness_commitment` no longer nests the segwit-commitment happy path under a combined `if context.consensus_params.enforce_segwit && let Some(...)` branch.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features` passes.
  </acceptance_criteria>
  <done>`packages/open-bitcoin-consensus/src/block.rs` is shallower to scan while preserving the current block-validation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flatten connect and disconnect transaction handling in chainstate</name>
  <files>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs</files>
  <read_first>packages/open-bitcoin-chainstate/src/engine.rs, packages/open-bitcoin-chainstate/src/engine/tests.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md</read_first>
  <behavior>
    - `connect_block` still validates non-coinbase transactions before removing spent inputs and still records `BlockUndo` in the same order.
    - `disconnect_tip` still removes outputs before restoring non-coinbase inputs and still returns the same `ChainstateError` variants for missing undo, mismatched counts, and restored-coin overwrite.
  </behavior>
  <action>In `packages/open-bitcoin-chainstate/src/engine.rs`, refactor only `connect_block` and `disconnect_tip` per D-01, D-03, D-04, and D-05. Pull the non-coinbase spend-and-undo work out of `connect_block` into one private helper so the outer loop reads as coinbase guard, non-coinbase apply, then output insertion. Do the same for the non-coinbase input-restore branch inside `disconnect_tip` so the reverse loop reads as remove outputs, optionally restore inputs, then continue. Preserve the existing loop order, `expect(...)` messages, `ChainstateError` variants, `build_transaction_context` usage, and tip-push/pop behavior. Do not flatten `reorg`, change module boundaries, or widen this into a panic or illegal-state sweep. Update `packages/open-bitcoin-chainstate/src/engine/tests.rs` only if D-06 requires a narrow regression or coverage guard.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features</automated>
  </verify>
  <acceptance_criteria>
    - File-read verification shows `packages/open-bitcoin-chainstate/src/engine.rs` extracts the non-coinbase work from `connect_block` and `disconnect_tip` into private helpers instead of keeping those branches nested inline.
    - File-read verification shows the public signatures of `connect_block` and `disconnect_tip` are unchanged.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-chainstate --all-features` passes.
  </acceptance_criteria>
  <done>`packages/open-bitcoin-chainstate/src/engine.rs` keeps the same state-transition semantics with shallower loop bodies and unchanged public entrypoints.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if flattening requires changing reject reasons, public signatures, or transaction-application order, stop and keep the current shape instead of forcing an early-return rewrite.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consensus validators -> reject reasons | Readability edits must not change block acceptance or rejection semantics |
| chainstate helpers -> UTXO and undo state | Helper extraction must not reorder spend, restore, or tip updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-01 | T | `packages/open-bitcoin-consensus/src/block.rs` | mitigate | Keep reject reasons and debug-message formatting unchanged, limit edits to the named functions, and verify with targeted consensus tests plus full repo verification. |
| T-07.3-02 | D | `packages/open-bitcoin-chainstate/src/engine.rs` | mitigate | Preserve connect/disconnect ordering, reuse the current error variants, and stop if a refactor would alter the state-machine flow rather than just extracting guard helpers. |
</threat_model>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-chainstate --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus -p open-bitcoin-chainstate --all-features`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The audited `block.rs` and `engine.rs` hotspots are flatter to scan, with unchanged public signatures and error semantics.
- Any test edits stay narrow and exist only to preserve existing behavior evidence or coverage.
- `bash scripts/verify.sh` passes after the refactor slice.
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-01-SUMMARY.md`
</output>
