---
phase: 07.3-reduce-nesting-with-early-returns
plan: 03
type: execute
wave: 2
depends_on:
  - "07.3-01"
  - "07.3-02"
files_modified:
  - packages/open-bitcoin-consensus/src/script/legacy.rs
  - packages/open-bitcoin-consensus/src/script/tests.rs
autonomous: true
requirements:
  - TBD
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07.3-2026-04-19T01-44-09
generated_at: 2026-04-19T01:44:09.175Z
must_haves:
  truths:
    - The optional script follow-on touches only the exact low-risk `legacy.rs` functions identified in research, not the broader opcode or state-machine dispatch.
    - Phase 07.3 ends with `bash scripts/verify.sh` passing after the narrowed hotspot sweep.
  artifacts:
    - packages/open-bitcoin-consensus/src/script/legacy.rs
    - packages/open-bitcoin-consensus/src/script/tests.rs
  key_links:
    - `packages/open-bitcoin-consensus/src/script.rs` remains the stable public module root while `legacy.rs` stays a private child implementation file.
    - `packages/open-bitcoin-consensus/src/script/tests.rs` continues to exercise the existing legacy execution behavior when any narrow regression guard is needed.
---

<objective>
Apply the only allowed `legacy.rs` follow-on cleanup and close the phase on the repo-native verification gate.

Purpose: finish the readability sweep with one narrow consensus-script pass only where it is clearly lower risk than leaving the nesting in place.
Output: a modest cleanup in `legacy.rs`, optional narrow test adjustments, and final verification evidence for the whole phase.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md
@.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md
@.planning/phases/07.1-codebase-maintainability-refactor-wave/07.1-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@scripts/verify.sh

<interfaces>
From packages/open-bitcoin-consensus/src/script/legacy.rs:
```rust
pub(super) fn execute_checksig(
    stack: &mut Vec<Vec<u8>>,
    script: &ScriptBuf,
    maybe_context: Option<&LegacyExecutionContext<'_>>,
    maybe_execution_data: Option<&mut ScriptExecutionData>,
    verify: bool,
) -> Result<(), ScriptError>;

pub(super) fn execute_checkmultisig(
    stack: &mut Vec<Vec<u8>>,
    script: &ScriptBuf,
    maybe_context: Option<&LegacyExecutionContext<'_>>,
    _maybe_execution_data: Option<&mut ScriptExecutionData>,
    op_count: &mut usize,
    verify: bool,
) -> Result<(), ScriptError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the optional low-risk early-return cleanup in legacy script execution</name>
  <files>packages/open-bitcoin-consensus/src/script/legacy.rs, packages/open-bitcoin-consensus/src/script/tests.rs</files>
  <read_first>packages/open-bitcoin-consensus/src/script/legacy.rs, packages/open-bitcoin-consensus/src/script/tests.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-RESEARCH.md</read_first>
  <behavior>
    - `execute_checksig` still rejects missing context, verify-failed tapscript data, and NULLFAIL cases exactly as it does today.
    - `execute_checkmultisig` still preserves the current signature or pubkey matching loop, dummy handling, NULLDUMMY or NULLFAIL checks, and verify-after-push behavior.
  </behavior>
  <action>Touch only `execute_checksig` and `execute_checkmultisig` in `packages/open-bitcoin-consensus/src/script/legacy.rs` per D-01, D-03, and D-04. Flatten only the missing-context, unsupported-opcode, verify-after-push, and related guard branches that are clearly simpler with `let...else`, early returns, or one tiny private helper. Keep the `match context.sig_version` structure in `execute_checksig`, keep the `remaining_pubkeys.by_ref()` matching loop in `execute_checkmultisig`, and keep `eval_script_internal` plus the wider opcode dispatch untouched. Stop immediately if further flattening would require rewriting the protocol-shaped control flow, altering stack mutation order, or widening the change into `packages/open-bitcoin-wallet/src/address.rs` or the deferred panic sweep. Touch `packages/open-bitcoin-consensus/src/script/tests.rs` only if a narrow existing-test adjustment or one direct regression assertion is required by D-06.</action>
  <verify>
    <automated>cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings && cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features</automated>
  </verify>
  <acceptance_criteria>
    - File-read verification shows only `execute_checksig` and `execute_checkmultisig` are materially refactored inside `packages/open-bitcoin-consensus/src/script/legacy.rs`.
    - File-read verification shows the public `script.rs` module root and the wider opcode dispatch in `eval_script_internal` remain structurally unchanged.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features` passes.
  </acceptance_criteria>
  <done>`packages/open-bitcoin-consensus/src/script/legacy.rs` gets the one allowed low-risk cleanup without turning into a broader protocol rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Close the narrowed sweep with repo-native verification only</name>
  <files>packages/open-bitcoin-consensus/src/script/legacy.rs, packages/open-bitcoin-consensus/src/script/tests.rs</files>
  <read_first>scripts/verify.sh, packages/open-bitcoin-consensus/src/script/legacy.rs, .planning/phases/07.3-reduce-nesting-with-early-returns/07.3-CONTEXT.md</read_first>
  <action>Run the final phase verification per D-07: first rerun the targeted consensus checks, then run `bash scripts/verify.sh`. If verification surfaces a blocker directly caused by the `legacy.rs` cleanup, apply the smallest fix in `packages/open-bitcoin-consensus/src/script/legacy.rs` or `packages/open-bitcoin-consensus/src/script/tests.rs` only. Do not widen the final 07.3 edit list into `packages/open-bitcoin-wallet/src/address.rs`, vendored Knots code, new module boundaries, or the deferred panic or illegal-state sweep.</action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features && bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify.sh` passes.
    - No files outside the Phase 07.3 touched hotspot list are modified by the final cleanup.
    - The final edit list still excludes `packages/open-bitcoin-wallet/src/address.rs`.
  </acceptance_criteria>
  <done>The phase ends on the repo-native verification contract with the sweep still narrow and behavior-neutral.</done>
</task>

</tasks>

<threat_model>
High-severity stop condition: if the optional `legacy.rs` cleanup would change opcode dispatch, signature-matching semantics, or require widening the phase to excluded files, stop and leave the current nesting in place.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy script helpers -> consensus-script behavior | Readability cleanup must not change stack operations, signature checks, or opcode outcomes |
| phase-touched files -> repo-native verification | The final sweep must end with the full repository contract passing, not only targeted crate checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-05 | T | `packages/open-bitcoin-consensus/src/script/legacy.rs` | mitigate | Restrict edits to the two named functions, preserve the protocol-shaped `match` and matching loop, and stop if a wider rewrite becomes necessary. |
| T-07.3-06 | D | final phase verification | mitigate | Require targeted consensus checks plus `bash scripts/verify.sh`, and forbid widening the cleanup into excluded files such as `packages/open-bitcoin-wallet/src/address.rs`. |
</threat_model>

<verification>
- `cargo clippy --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-targets --all-features -- -D warnings`
- `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-consensus --all-features`
- `bash scripts/verify.sh`
</verification>

<success_criteria>
- The only `legacy.rs` edits are the explicit low-risk follow-on cleanup named in this plan.
- The phase still excludes `packages/open-bitcoin-wallet/src/address.rs` and the deferred panic sweep.
- `bash scripts/verify.sh` passes after the whole Phase 07.3 hotspot sweep.
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-03-SUMMARY.md`
</output>
