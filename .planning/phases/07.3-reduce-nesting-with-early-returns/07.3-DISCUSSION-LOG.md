# Phase 07.3: Reduce nesting with early returns - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 07.3-reduce-nesting-with-early-returns
**Mode:** Yolo
**Areas discussed:** scope and targeting, flattening strategy, verification and regression guards

---

## Scope and Targeting

| Option | Description | Selected |
|--------|-------------|----------|
| Repo-wide sweep | Flatten nesting across first-party crates, tests, and helper modules in one pass | |
| Targeted production hotspot sweep | Focus on the highest-value first-party Rust production files with meaningful nested control-flow pressure before Phase 8 | ✓ |
| Single-subsystem cleanup | Limit the phase to one subsystem such as consensus only | |

**User's choice:** Yolo-selected recommended option: targeted production hotspot sweep.  
**Notes:** A quick scout surfaced the clearest candidates in `script/legacy.rs`, `block.rs`, `pool.rs`, `peer.rs`, `engine.rs`, and `address.rs`. The phase stays narrow rather than becoming a repo-wide style rewrite.

---

## Flattening Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Mechanical rewrite | Convert most nested conditionals into guard clauses even when the current shape is intentional | |
| Guard-clause-first refactors | Use early returns and `let...else` where they clarify the happy path, and extract helpers only when nesting reflects mixed responsibilities | ✓ |
| Model rewrite | Use the phase to redesign illegal states and data models wherever nesting exists | |

**User's choice:** Yolo-selected recommended option: guard-clause-first refactors.  
**Notes:** Preserve meaningful protocol and state-machine `match` branches. Prefer Rust-native guard constructs when extraction is the real problem, and only extract helpers when they make the workflow easier to read.

---

## Verification and Regression Guards

| Option | Description | Selected |
|--------|-------------|----------|
| No new tests | Rely on existing coverage while rewriting control flow | |
| Targeted guard coverage + repo verify | Add narrow tests where reordered guards or extracted helpers could hide behavior changes, and require repo-native verification before completion | ✓ |
| Broad regression expansion | Add large new regression suites across every touched crate as part of the readability pass | |

**User's choice:** Yolo-selected recommended option: targeted guard coverage + repo verify.  
**Notes:** This phase should lean on existing parity and crate tests, use small additional unit coverage only where control-flow changes need a regression guard, and still finish on `bash scripts/verify.sh`.

## the agent's Discretion

- Exact hotspot order, helper naming, and where to stop flattening once clarity stops improving
- Leaving a nested `match` in place when it is clearer than a forced guard-clause rewrite

## Deferred Ideas

- AI-agent-friendly CLI surface — belongs in Phase 8
- Sweep panics and illegal states — separate correctness-hardening pass
- Repo-wide early-return rewrites outside the highest-value production hotspots
