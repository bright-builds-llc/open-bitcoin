---
phase: 07.3-reduce-nesting-with-early-returns
plan: 01
subsystem: consensus-chainstate
tags: [rust, consensus, chainstate, maintainability, early-returns]
requires:
  - phase: 07.2
    provides: protocol-clarity cleanup that left the remaining consensus and chainstate hotspots narrow enough for a behavior-neutral early-return pass
provides:
  - shared transaction-validation error mapping in consensus block validation
  - guard-style witness commitment validation in block.rs
  - extracted non-coinbase connect and disconnect helpers in chainstate
affects: [phase-08-interface-parity, consensus, chainstate, validation]
tech-stack:
  added: []
  patterns:
    - Shared private helpers for parity-safe error mapping and non-coinbase state mutation
    - Guard-first early returns for consensus and chainstate validation hotspots
key-files:
  created: []
  modified:
    - packages/open-bitcoin-consensus/src/block.rs
    - packages/open-bitcoin-consensus/src/block/tests.rs
    - packages/open-bitcoin-chainstate/src/engine.rs
    - packages/open-bitcoin-chainstate/src/engine/tests.rs
key-decisions:
  - Block transaction validation failures should map through one private helper so both public entrypoints preserve the same txid-based debug text.
  - Chainstate connect and disconnect should keep their existing transaction order while isolating non-coinbase mutation in focused private helpers.
  - TDD red phases should stay local in this repo because the Rust pre-commit contract forbids commits that fail fmt, clippy, build, or tests.
patterns-established:
  - "When two consensus entrypoints wrap the same transaction error shape, centralize that mapping in one private helper and pin the exact debug text with direct tests."
  - "In chainstate loops, keep coinbase handling as the guard path and move non-coinbase spend or restore work into narrow helpers instead of nesting mutation logic inline."
requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 07.3-2026-04-19T01-44-09
generated_at: 2026-04-19T02:25:28Z
duration: 8 min
completed: 2026-04-19
---

# Phase 07.3 Plan 01: Reduce Nesting With Early Returns Summary

**Consensus block validation now shares one txid-aware error mapper and guard-first witness handling, while chainstate connect or disconnect keep the same mutation order through extracted non-coinbase helpers.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-19T02:17:56Z
- **Completed:** 2026-04-19T02:25:28Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Reused one private mapper in [`packages/open-bitcoin-consensus/src/block.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-consensus/src/block.rs) so `validate_block` and `validate_block_with_context` preserve the exact txid-based debug-message shape without duplicating formatting logic.
- Rewrote `check_witness_commitment` around early exits so the unexpected-witness path is handled up front and the enforced commitment path reads as one straight-line validation block.
- Extracted `apply_non_coinbase_transaction` and `restore_non_coinbase_inputs` in [`packages/open-bitcoin-chainstate/src/engine.rs`](/Users/peterryszkiewicz/Repos/open-bitcoin/packages/open-bitcoin-chainstate/src/engine.rs), keeping connect/disconnect ordering and error variants intact while making the outer loops shallower.
- Added direct helper-focused regressions in the sibling test modules so the extracted private seams are covered without widening the public behavior suite.

## Task Commits

Each task was committed atomically:

1. **Task 1: Flatten block validation guard paths in the audited consensus hotspot** - `9dbfa86` (`refactor`)
2. **Task 2: Flatten connect and disconnect transaction handling in chainstate** - `d10388c` (`refactor`)

## Files Created/Modified

- `packages/open-bitcoin-consensus/src/block.rs` - shares transaction-validation error mapping and flattens witness-commitment guard flow.
- `packages/open-bitcoin-consensus/src/block/tests.rs` - adds direct regression coverage for the shared error-mapping helper.
- `packages/open-bitcoin-chainstate/src/engine.rs` - extracts non-coinbase connect/disconnect helpers while preserving state-transition order.
- `packages/open-bitcoin-chainstate/src/engine/tests.rs` - adds direct helper coverage for input consumption, undo recording, and undo-shape mismatch handling.

## Decisions Made

- Kept the consensus refactor limited to the two public validation entrypoints and `check_witness_commitment` instead of widening into unrelated merkle or sigop cleanup.
- Kept the chainstate outer loops intact and moved only the non-coinbase work into helpers so connect/disconnect ordering stays obvious and auditable.
- Used the repo’s stricter Rust pre-commit contract instead of making failing red-phase commits, while still running and confirming local red test failures before each implementation step.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - AGENTS precedence] Kept TDD red phases local instead of committing failing tests**
- **Found during:** Task 1 and Task 2
- **Issue:** The plan requested TDD tasks, but repo-local Rust rules require `cargo fmt`, `cargo clippy`, `cargo build`, and `cargo test` to pass before any commit.
- **Fix:** Ran local red phases to confirm the missing-helper failures, then committed only the green refactor states after the required verification sequence passed.
- **Files modified:** `packages/open-bitcoin-consensus/src/block/tests.rs`, `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** Both red runs failed on the expected missing-helper imports before implementation; all required Rust checks and `bash scripts/verify.sh` passed before each task commit.

**2. [Rule 3 - Blocking] Reduced `apply_non_coinbase_transaction` arguments after clippy rejected the first extraction**
- **Found during:** Task 2
- **Issue:** `cargo clippy --all-targets --all-features -- -D warnings` failed on `clippy::too_many_arguments` for the initial helper signature.
- **Fix:** Threaded `BlockValidationContext` through the helper instead of passing separate height, median-time-past, and consensus parameters.
- **Files modified:** `packages/open-bitcoin-chainstate/src/engine.rs`, `packages/open-bitcoin-chainstate/src/engine/tests.rs`
- **Verification:** `cargo clippy --manifest-path packages/Cargo.toml --all-targets --all-features -- -D warnings` passed after the signature change.

---

**Total deviations:** 2 auto-fixed (1 AGENTS-precedence adjustment, 1 blocking lint fix)
**Impact on plan:** Both deviations kept the work inside the planned hotspot scope and preserved the intended behavior-neutral outcome.

## Issues Encountered

- The first `engine.rs` helper extraction tripped `clippy::too_many_arguments`; collapsing the helper inputs through `BlockValidationContext` resolved it without changing loop order or visibility.

## User Setup Required

None - no external services, configuration changes, or manual migration steps are required.

## Next Phase Readiness

- The consensus and chainstate hotspots targeted by plan `07.3-01` are flatter to scan and retain the same reject reasons, debug text, and mutation order expected by later interface work.
- Phase `07.3-02` can now focus on the remaining networking and mempool guard-path hotspots without reopening these consensus or chainstate seams.
- No new blockers were introduced.

## Self-Check: PASSED

- Verified `.planning/phases/07.3-reduce-nesting-with-early-returns/07.3-01-SUMMARY.md` exists on disk.
- Verified task commits `9dbfa86` and `d10388c` exist in git history.
