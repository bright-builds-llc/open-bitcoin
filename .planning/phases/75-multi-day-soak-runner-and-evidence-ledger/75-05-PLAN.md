---
phase: 75-multi-day-soak-runner-and-evidence-ledger
plan: 05
type: execute
wave: 3
depends_on: [75-02, 75-03]
files_modified:
  - docs/operator/runtime-guide.md
  - docs/architecture/status-snapshot.md
  - docs/architecture/operator-observability.md
  - docs/parity/catalog/p2p.md
  - docs/parity/catalog/chainstate.md
  - docs/parity/catalog/operator-runtime-release-hardening.md
  - docs/parity/index.json
  - docs/parity/checklist.md
  - docs/parity/README.md
  - README.md
autonomous: true
requirements: [SOAK-01, SOAK-02, SOAK-03, SOAK-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 75-2026-06-14T22-59-23
generated_at: "2026-06-15T00:46:59Z"
must_haves:
  truths:
    - "Operator docs provide repo-local Cargo and Bazel soak command forms and explicit proof/non-proof boundaries."
    - "Architecture docs describe the soak ledger event kinds and final outcome vocabulary without overloading lower-level sync terms."
    - "Parity roots and README wording describe scoped opt-in soak evidence without production-node claims."
    - "Docs keep public-network and multi-day soak execution opt-in and outside default verification."
  artifacts:
    - { path: "docs/operator/runtime-guide.md", provides: "Operator soak command matrix and evidence interpretation" }
    - { path: "docs/architecture/status-snapshot.md", provides: "Shared status wording for soak checkpoint and outcome evidence" }
    - { path: "docs/architecture/operator-observability.md", provides: "Soak evidence source-of-truth and support projection boundaries" }
    - { path: "docs/parity/index.json", provides: "Auditable parity root for Phase 75 soak evidence" }
    - { path: "README.md", provides: "Scoped operator-surface wording without broad production-node claims" }
  key_links:
    - { from: "docs/operator/runtime-guide.md", to: "open-bitcoin soak", via: "repo-local Cargo and Bazel commands", pattern: "soak start --elapsed-time-seconds 259200" }
    - { from: "docs/parity/index.json", to: "docs/parity/checklist.md", via: "phase75-multi-day-soak-runner-evidence-ledger surface id", pattern: "phase75-multi-day-soak-runner-evidence-ledger" }
    - { from: "README.md", to: "docs/operator/runtime-guide.md", via: "scoped proof-boundary wording", pattern: "bounded opt-in full-sync soak behavior" }
---

<objective>
Document the Phase 75 operator soak workflow, evidence semantics, and parity surface.

Purpose: Operators and reviewers need exact commands and scoped claims so soak evidence is useful without implying production-node readiness.
Output: Operator docs, architecture docs, parity roots, and README wording aligned to SOAK-01 through SOAK-04.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-RESEARCH.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-02-SUMMARY.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-03-SUMMARY.md
@.planning/phases/73-opt-in-uat-and-deterministic-verification/73-04-SUMMARY.md
@.planning/phases/74-release-boundaries-parity-and-documentation/74-01-SUMMARY.md
@AGENTS.md
@docs/operator/runtime-guide.md
@docs/architecture/status-snapshot.md
@docs/architecture/operator-observability.md
@docs/parity/index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document operator soak commands, evidence semantics, and parity roots</name>
  <files>docs/operator/runtime-guide.md, docs/architecture/status-snapshot.md, docs/architecture/operator-observability.md, docs/parity/catalog/p2p.md, docs/parity/catalog/chainstate.md, docs/parity/catalog/operator-runtime-release-hardening.md, docs/parity/index.json, docs/parity/checklist.md, docs/parity/README.md, README.md</files>
  <read_first>
    - docs/operator/runtime-guide.md
    - docs/architecture/status-snapshot.md
    - docs/architecture/operator-observability.md
    - docs/parity/catalog/p2p.md
    - docs/parity/catalog/chainstate.md
    - docs/parity/catalog/operator-runtime-release-hardening.md
    - docs/parity/index.json
    - docs/parity/checklist.md
    - docs/parity/README.md
    - README.md
    - AGENTS.md
    - .planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Operator docs include exact repo-local Cargo and Bazel `soak start` commands with elapsed-time, checkpoint interval, target height, peer policy, disk budget, and stop-condition flags.
    - Test 2: Operator docs include `soak resume`, `soak stop`, and `soak report` command forms plus the exact durable source-of-truth wording for `run-index.json` and `events.jsonl`.
    - Test 3: Docs and README state the exact proof/non-proof boundary for bounded opt-in full-sync soak behavior without broad production-node, inbound serving, relay, production-wallet, migration-apply, packaging, GUI, or hosted-dashboard claims.
    - Test 4: Architecture docs name the soak ledger event kinds and final outcome vocabulary without redefining lower-level sync stop or recovery enums.
    - Test 5: Parity index, checklist, README, and relevant catalog pages reference the `phase75-multi-day-soak-runner-evidence-ledger` surface id.
  </behavior>
  <action>
    Add operator and parity documentation for the Phase 75 soak runner. In `docs/operator/runtime-guide.md`, add heading `### Phase 75 multi-day soak runner`. Include these exact repo-local command forms:

    `cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir /path/to/open-bitcoin --network mainnet soak start --elapsed-time-seconds 259200 --checkpoint-interval-seconds 300 --target-height <target-height> --peer-policy daemon-configured --disk-budget-bytes 107374182400 --stop-condition elapsed-time`

    `bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir /path/to/open-bitcoin --network mainnet soak start --elapsed-time-seconds 259200 --checkpoint-interval-seconds 300 --target-height <target-height> --peer-policy daemon-configured --disk-budget-bytes 107374182400 --stop-condition elapsed-time`

    Also document resume, stop, and report command forms with `soak resume --run-id <run-id> --checkpoint-interval-seconds 300`, `soak stop --run-id <run-id> --reason operator-stop`, and `soak report --run-id <run-id>`.

    Add exact evidence wording: `The durable source of truth is <datadir>/soak/run-index.json plus <datadir>/soak/runs/<run_id>/events.jsonl.` Add exact proof boundary wording: `A soak run can prove bounded opt-in full-sync soak behavior, durable resume evidence, or diagnosed blocker evidence; it does not prove inbound serving, relay, production-funds wallet safety, migration apply mode, signed packages, GUI readiness, hosted dashboards, or broad production-node readiness.`

    Update `docs/architecture/status-snapshot.md` and `docs/architecture/operator-observability.md` to name the soak event kinds `started`, `checkpoint`, `resume`, `stop`, and `verdict`, and final outcomes `clean_completion`, `diagnosed_blocker`, `operator_stop`, `resource_stop`, `recovery_stop`, and `unexpected_termination`.

    Add a Phase 75 parity surface named `phase75-multi-day-soak-runner-evidence-ledger` to `docs/parity/index.json`, plus matching references in `docs/parity/checklist.md`, `docs/parity/README.md`, `docs/parity/catalog/p2p.md`, `docs/parity/catalog/chainstate.md`, and `docs/parity/catalog/operator-runtime-release-hardening.md`. Update `README.md` only with scoped operator-surface wording; keep production-node, inbound-serving, relay, production-wallet, migration-apply, packaging, GUI, and hosted-dashboard claims out.
  </action>
  <verify>
    <automated>rg -n "Phase 75 multi-day soak runner|soak start --elapsed-time-seconds 259200|durable source of truth is|phase75-multi-day-soak-runner-evidence-ledger" docs/operator/runtime-guide.md docs/parity/index.json README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 75 multi-day soak runner" docs/operator/runtime-guide.md` finds the new section.
    - `rg -n "cargo run --manifest-path packages/Cargo.toml -p open-bitcoin-cli --bin open-bitcoin -- --datadir /path/to/open-bitcoin --network mainnet soak start --elapsed-time-seconds 259200" docs/operator/runtime-guide.md` finds the Cargo command.
    - `rg -n "bazel run //packages/open-bitcoin-cli:open_bitcoin -- --datadir /path/to/open-bitcoin --network mainnet soak start --elapsed-time-seconds 259200" docs/operator/runtime-guide.md` finds the Bazel command.
    - `rg -n "soak resume --run-id <run-id> --checkpoint-interval-seconds 300|soak stop --run-id <run-id> --reason operator-stop|soak report --run-id <run-id>" docs/operator/runtime-guide.md` finds resume, stop, and report commands.
    - `rg -n "The durable source of truth is <datadir>/soak/run-index.json plus <datadir>/soak/runs/<run_id>/events.jsonl" docs/operator/runtime-guide.md docs/architecture/operator-observability.md` finds source-of-truth wording.
    - `rg -n "bounded opt-in full-sync soak behavior, durable resume evidence, or diagnosed blocker evidence" docs/operator/runtime-guide.md README.md docs/parity/catalog/operator-runtime-release-hardening.md` finds proof-boundary wording.
    - `rg -n "phase75-multi-day-soak-runner-evidence-ledger" docs/parity/index.json docs/parity/checklist.md docs/parity/README.md` finds parity root references.
  </acceptance_criteria>
  <done>Operator docs and parity roots expose the exact soak workflow, evidence semantics, and non-claim boundaries with repo-local Cargo and Bazel command forms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/parity roots -> operator interpretation | Documentation controls what operators and reviewers believe the evidence proves. |
| README -> external claim surface | Public project wording can accidentally broaden Phase 75 claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05-01 | Spoofing | parity/README claim wording | mitigate | Require scoped proof wording and keep production-node, inbound-serving, relay, production-wallet, migration-apply, packaging, GUI, and hosted-dashboard claims out. |
| T-75-05-02 | Repudiation | operator docs | mitigate | Docs must name the datadir run index and JSONL ledger as source of truth, not reports or support bundles. |
| T-75-05-03 | Denial of Service | operator command docs | mitigate | Default docs must preserve explicit opt-in command forms and keep public-network/multi-day soak execution outside `bash scripts/verify.sh`. |
</threat_model>

<verification>
Run documentation anchor checks:

```bash
rg -n "Phase 75 multi-day soak runner|soak start --elapsed-time-seconds 259200|durable source of truth is|phase75-multi-day-soak-runner-evidence-ledger" docs/operator/runtime-guide.md docs/parity/index.json README.md
```
</verification>

<success_criteria>
- Operator docs include exact Cargo and Bazel soak commands plus resume, stop, and report commands.
- Architecture docs describe the Phase 75 ledger event and outcome vocabulary.
- Parity roots include `phase75-multi-day-soak-runner-evidence-ledger`.
- README and docs preserve scoped opt-in soak claims and do not imply broad production-node readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-05-SUMMARY.md`.
</output>
