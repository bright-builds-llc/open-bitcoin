---
phase: 75-multi-day-soak-runner-and-evidence-ledger
plan: 04
type: execute
wave: 3
depends_on: [75-02]
files_modified:
  - packages/open-bitcoin-cli/src/operator/soak/report.rs
  - packages/open-bitcoin-cli/src/operator/support.rs
  - packages/open-bitcoin-cli/src/operator/support/render.rs
  - packages/open-bitcoin-cli/src/operator/support/tests.rs
  - packages/open-bitcoin-cli/tests/operator_binary.rs
autonomous: true
requirements: [SOAK-01, SOAK-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 75-2026-06-14T22-59-23
generated_at: "2026-06-15T00:46:59Z"
must_haves:
  truths:
    - "Support bundles may include a compact redacted soak summary, but the ledger remains the source of truth."
    - "Support JSON reports available soak evidence with run id, final outcome, latest sequence, and ledger/report paths."
    - "Support Markdown exposes a compact soak section without raw ledger lines, daemon logs, reports, wallet material, credentials, or peer tables."
    - "Missing soak evidence is reported as unavailable instead of being inferred from stale reports."
  artifacts:
    - { path: "packages/open-bitcoin-cli/src/operator/support.rs", provides: "Compact soak summary added to local support evidence" }
    - { path: "packages/open-bitcoin-cli/src/operator/support/render.rs", provides: "Markdown rendering for compact soak evidence" }
    - { path: "packages/open-bitcoin-cli/src/operator/support/tests.rs", provides: "Unit coverage for available, unavailable, and redacted soak evidence" }
    - { path: "packages/open-bitcoin-cli/tests/operator_binary.rs", provides: "Operator binary coverage for support bundle soak summary output" }
  key_links:
    - { from: "packages/open-bitcoin-cli/src/operator/support.rs", to: "packages/open-bitcoin-cli/src/operator/soak/report.rs", via: "compact summary derived from latest ledger projection", pattern: "soak_evidence|SoakSupportEvidence" }
    - { from: "packages/open-bitcoin-cli/src/operator/support/render.rs", to: "packages/open-bitcoin-cli/src/operator/support.rs", via: "SupportEvidenceBundle renders soak_evidence", pattern: "## Soak Evidence" }
---

<objective>
Add the compact support-bundle soak summary projection.

Purpose: D-07 allows support bundles to include redacted soak summaries, but only as projections from the ledger/report source of truth.
Output: Support JSON and Markdown can show available or unavailable soak evidence without exposing raw local evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-RESEARCH.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-01-SUMMARY.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-02-SUMMARY.md
@.planning/phases/72-operator-observability-and-support-evidence/72-04-SUMMARY.md
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@packages/open-bitcoin-cli/src/operator/support.rs
@packages/open-bitcoin-cli/src/operator/support/render.rs
@packages/open-bitcoin-cli/src/operator/soak/report.rs

<interfaces>
From `packages/open-bitcoin-cli/src/operator/support.rs`:
```rust
struct SupportEvidenceBundle {
    generated_at_unix_seconds: u64,
    generated_by: String,
    output: SupportEvidenceOutput,
    redaction: RedactionSummary,
    config: ConfigEvidence,
    status: OpenBitcoinStatusSnapshot,
    store_health: StoreHealthEvidence,
    live_smoke: LiveSmokeEvidence,
    full_sync_evidence: FullSyncEvidence,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact soak summary to support bundles</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/report.rs, packages/open-bitcoin-cli/src/operator/support.rs, packages/open-bitcoin-cli/src/operator/support/render.rs, packages/open-bitcoin-cli/src/operator/support/tests.rs, packages/open-bitcoin-cli/tests/operator_binary.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/soak/report.rs
    - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
    - packages/open-bitcoin-cli/src/operator/support.rs
    - packages/open-bitcoin-cli/src/operator/support/render.rs
    - packages/open-bitcoin-cli/src/operator/support/tests.rs
    - packages/open-bitcoin-cli/tests/operator_binary.rs
    - .planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
    - .planning/phases/72-operator-observability-and-support-evidence/72-04-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Support JSON includes `soak_evidence.state = "available"` plus run id, final outcome, latest sequence, source ledger path, JSON report path, and Markdown report path when a latest soak run exists.
    - Test 2: Support Markdown includes `## Soak Evidence`, `Final outcome:`, `Source ledger:`, and `Latest sequence:`.
    - Test 3: Support JSON and Markdown exclude raw ledger lines, raw daemon logs, raw reports, wallet material, RPC credentials, and unbounded peer tables.
    - Test 4: Missing soak ledger produces `soak_evidence.state = "unavailable"` with reason `soak ledger unavailable`.
  </behavior>
  <action>
    Extend support bundle generation so support evidence can include a compact soak summary projection per D-07. In `packages/open-bitcoin-cli/src/operator/support.rs`, add a `soak_evidence: SoakSupportEvidence` field to `SupportEvidenceBundle`. Implement `collect_soak_support_evidence(config_resolution: &OperatorConfigResolution) -> SoakSupportEvidence` that reads only the selected datadir's `<datadir>/soak/run-index.json`, resolves the latest indexed run, reads that run's `events.jsonl`, derives a report projection, and returns compact fields.

    Define `SoakSupportEvidence` with `state`, `maybe_run_id`, `maybe_final_outcome`, `maybe_latest_sequence`, `maybe_source_ledger_path`, `maybe_json_report_path`, `maybe_markdown_report_path`, and `maybe_unavailable_reason`. Use `available` only when the latest run's ledger can be read. Use unavailable reason `soak ledger unavailable` when no datadir, no run index, no latest run, or unreadable ledger exists.

    In `packages/open-bitcoin-cli/src/operator/support/render.rs`, add Markdown section `## Soak Evidence` with exact labels `State:`, `Run:`, `Final outcome:`, `Source ledger:`, `JSON report:`, `Markdown report:`, and `Latest sequence:`. Do not embed raw ledger event lines or raw report bodies. Add unit and binary tests to prove redaction and unavailable behavior.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase75_soak_support_ --all-features</automated>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary open_bitcoin_support_bundle_includes_phase75_soak_summary --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "soak_evidence|SoakSupportEvidence|collect_soak_support_evidence|soak ledger unavailable" packages/open-bitcoin-cli/src/operator/support.rs` finds support summary code.
    - `rg -n "## Soak Evidence|Final outcome:|Source ledger:|Latest sequence:" packages/open-bitcoin-cli/src/operator/support/render.rs` finds Markdown labels.
    - `rg -n "phase75_soak_support_|open_bitcoin_support_bundle_includes_phase75_soak_summary" packages/open-bitcoin-cli/src/operator/support/tests.rs packages/open-bitcoin-cli/tests/operator_binary.rs` finds support tests.
    - `rg -n "raw ledger|raw daemon logs|raw reports|wallet material|RPC credentials|unbounded peer tables" packages/open-bitcoin-cli/src/operator/support/tests.rs packages/open-bitcoin-cli/tests/operator_binary.rs` finds redaction assertions.
    - Focused Cargo support tests exit 0.
  </acceptance_criteria>
  <done>Support bundles include a compact redacted soak summary when available, while preserving the ledger/report source-of-truth boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soak ledger -> support bundle | Local durable evidence is summarized into shareable support artifacts. |
| Support renderer -> operator/shareable output | Compact evidence is projected into JSON and Markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Information Disclosure | support soak summary | mitigate | Include only allowlisted run id, outcome, sequence, ledger/report paths, and unavailable reason; tests reject raw ledger lines, logs, credentials, wallet material, and peer tables. |
| T-75-04-02 | Repudiation | support soak summary | mitigate | Support output must identify the datadir ledger as source of truth and expose latest sequence so stale reports are not treated as state. |
| T-75-04-03 | Tampering | support ledger lookup | mitigate | Read only the selected datadir-owned run index and latest indexed run; do not accept arbitrary report paths as current state. |
</threat_model>

<verification>
Run focused support checks:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib phase75_soak_support_ --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --test operator_binary open_bitcoin_support_bundle_includes_phase75_soak_summary --all-features
```
</verification>

<success_criteria>
- Support bundle JSON and Markdown include compact soak evidence when available and unavailable evidence when absent.
- Support output is a projection from the datadir ledger, not a source of truth.
- Support output excludes raw ledger lines, raw daemon logs, raw reports, wallet material, RPC credentials, and unbounded peer tables.
</success_criteria>

<output>
After completion, create `.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-04-SUMMARY.md`.
</output>
