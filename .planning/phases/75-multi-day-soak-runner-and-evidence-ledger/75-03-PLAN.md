---
phase: 75-multi-day-soak-runner-and-evidence-ledger
plan: 03
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - packages/open-bitcoin-node/src/sync/tests.rs
  - packages/open-bitcoin-node/src/sync/tests/soak.rs
  - packages/open-bitcoin-cli/src/operator/soak/tests.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements: [SOAK-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 75-2026-06-14T22-59-23
generated_at: "2026-06-15T00:16:14.918Z"
must_haves:
  truths:
    - "Contributor can replay long-run soak lifecycle behavior with deterministic Rust tests."
    - "Synthetic soak coverage uses scripted transports, explicit timestamps, local temp datadirs, and no public-network access."
    - "Interrupted and resumed evidence is tested from durable ledger/runtime reopen paths without multi-day waits."
    - "Resource and no-progress stops use existing shared status evidence, not soak-local reclassification."
  artifacts:
    - { path: "packages/open-bitcoin-node/src/sync/tests/soak.rs", provides: "Synthetic long-run sync and reopen tests for soak control flow" }
    - { path: "packages/open-bitcoin-node/src/sync/tests.rs", provides: "Registration for the soak child test module" }
    - { path: "packages/open-bitcoin-cli/src/operator/soak/tests.rs", provides: "Ledger replay tests for interrupted/resumed soak evidence" }
    - { path: "docs/parity/source-breadcrumbs.json", provides: "Breadcrumb coverage for new Rust test module" }
  key_links:
    - { from: "packages/open-bitcoin-node/src/sync/tests.rs", to: "packages/open-bitcoin-node/src/sync/tests/soak.rs", via: "child module registration", pattern: "mod soak;" }
    - { from: "packages/open-bitcoin-node/src/sync/tests/soak.rs", to: "DurableSyncRuntime", via: "scripted transport and real store reopen", pattern: "DurableSyncRuntime::open" }
    - { from: "packages/open-bitcoin-cli/src/operator/soak/tests.rs", to: "packages/open-bitcoin-cli/src/operator/soak/ledger.rs", via: "ledger replay over explicit timestamps", pattern: "SoakLedgerEvent" }
---

<objective>
Add deterministic synthetic coverage for Phase 75 long-run soak behavior without public peers or wall-clock multi-day waits.

Purpose: SOAK-04 requires replayable synthetic proof. This plan builds that proof from existing `DurableSyncRuntime`, scripted transport/resolver fixtures, explicit timestamps, same-datadir reopen, and Plan 01 ledger replay.
Output: Focused Rust tests named `phase75_synthetic_soak_*` and `soak_synthetic_*`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-RESEARCH.md
@.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-01-SUMMARY.md
@.planning/phases/71-resource-bounds-and-durable-restart-resume/71-02-SUMMARY.md
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@packages/open-bitcoin-node/src/sync.rs
@packages/open-bitcoin-node/src/sync/tests.rs
@packages/open-bitcoin-cli/src/operator/soak/ledger.rs
@packages/open-bitcoin-cli/src/operator/soak/tests.rs
@docs/parity/source-breadcrumbs.json

<interfaces>
From `packages/open-bitcoin-node/src/sync/tests.rs`:
```rust
fn temp_store_path(test_name: &str) -> PathBuf;
fn build_block(previous_block_hash: BlockHash, height: u32) -> Block;
fn block_hash_hex(block_hash: BlockHash) -> String;
fn save_chain_headers_snapshot_and_blocks(
    path: &Path,
    best_chain: &[(&Block, u32)],
    active_chain: &[(&Block, u32)],
    stored_blocks: &[(&Block, u32)],
);

struct ScriptedTransport;
struct ScriptedResolver;
```

From `packages/open-bitcoin-node/src/sync/types.rs`:
```rust
pub struct SyncRuntimeConfig {
    pub manual_peers: Vec<SyncPeerAddress>,
    pub dns_seeds: Vec<String>,
    pub target_outbound_peers: usize,
    pub maybe_target_header_height: Option<u64>,
    pub max_messages_per_peer: usize,
    pub max_rounds: usize,
    pub max_blocks_in_flight_total: usize,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add synthetic long-run sync soak tests</name>
  <files>packages/open-bitcoin-node/src/sync/tests.rs, packages/open-bitcoin-node/src/sync/tests/soak.rs, docs/parity/source-breadcrumbs.json</files>
  <read_first>
    - packages/open-bitcoin-node/src/sync.rs
    - packages/open-bitcoin-node/src/sync/tests.rs
    - packages/open-bitcoin-node/src/sync/types.rs
    - packages/open-bitcoin-node/src/sync/runtime_state.rs
    - .planning/phases/71-resource-bounds-and-durable-restart-resume/71-02-SUMMARY.md
    - docs/parity/source-breadcrumbs.json
    - standards/core/testing.md
  </read_first>
  <behavior>
    - Test 1: A 96-block synthetic local chain reaches a target height with `dns_seeds: Vec::new()`, manual loopback peers, explicit timestamps, and `maybe_target_header_height: Some(95)`.
    - Test 2: Reopening the same temp datadir after partial progress preserves connected active-chain height/hash/work and does not request an already connected block again.
    - Test 3: A synthetic storage/resource blocked state projects `SyncRecoveryCategory::ResourceExhaustion` and `NoProgressDiagnosis::StorageOrResourceBlocked` into shared status evidence.
  </behavior>
  <action>
    Create `packages/open-bitcoin-node/src/sync/tests/soak.rs` and register it from `packages/open-bitcoin-node/src/sync/tests.rs` with `mod soak;`. Use `super::*` so the child module can reuse `DurableSyncRuntime`, `FjallNodeStore`, `ScriptedTransport`, `ScriptedResolver`, `build_block`, `headers_script`, `version_verack_script`, `sync_config`, `temp_store_path`, `remove_dir_if_exists`, `getdata_block_hashes`, and `block_hash_hex`.

    Add tests named `phase75_synthetic_soak_long_run_reaches_target_height_without_public_network`, `phase75_synthetic_soak_reopen_preserves_resume_progress_without_duplicate_getdata`, and `phase75_synthetic_soak_resource_stop_uses_shared_status_evidence`. Use `const SYNTHETIC_SOAK_BLOCKS: usize = 96`. The config in the long-run test must set `manual_peers` to two loopback peers, `dns_seeds: Vec::new()`, `target_outbound_peers: 2`, `maybe_target_header_height: Some(95)`, `max_blocks_in_flight_total: 4`, `max_messages_per_peer: 12`, and `max_rounds: 64`.

    Use explicit block header times for timestamps. Do not use `sleep`, public DNS seeds, public peers, service-manager commands, current-tip timing, or `scripts/run-live-mainnet-smoke.ts`. Update `docs/parity/source-breadcrumbs.json` for `packages/open-bitcoin-node/src/sync/tests/soak.rs` with the existing Knots sync anchors already used by `packages/open-bitcoin-node/src/sync/tests.rs`.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase75_synthetic_soak_ --all-features</automated>
    <automated>bun run scripts/check-parity-breadcrumbs.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mod soak;" packages/open-bitcoin-node/src/sync/tests.rs` finds the child module registration.
    - `rg -n "SYNTHETIC_SOAK_BLOCKS: usize = 96|dns_seeds: Vec::new\\(\\)|maybe_target_header_height: Some\\(95\\)|max_rounds: 64" packages/open-bitcoin-node/src/sync/tests/soak.rs` finds deterministic long-run bounds.
    - `rg -n "phase75_synthetic_soak_long_run_reaches_target_height_without_public_network|phase75_synthetic_soak_reopen_preserves_resume_progress_without_duplicate_getdata|phase75_synthetic_soak_resource_stop_uses_shared_status_evidence" packages/open-bitcoin-node/src/sync/tests/soak.rs` finds all tests.
    - `rg -n "sleep|run-live-mainnet-smoke|manual-peer|systemctl|launchctl" packages/open-bitcoin-node/src/sync/tests/soak.rs` returns no matches.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase75_synthetic_soak_ --all-features` exits 0.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` exits 0.
  </acceptance_criteria>
  <done>Deterministic Rust sync tests prove long-run target-height, same-datadir resume, and resource-stop evidence without public-network or multi-day timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add synthetic ledger replay tests for interruption and resume evidence</name>
  <files>packages/open-bitcoin-cli/src/operator/soak/tests.rs</files>
  <read_first>
    - packages/open-bitcoin-cli/src/operator/soak/tests.rs
    - packages/open-bitcoin-cli/src/operator/soak/ledger.rs
    - packages/open-bitcoin-cli/src/operator/soak/outcome.rs
    - packages/open-bitcoin-cli/src/operator/soak/report.rs
    - .planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A ledger with `Started` and `Checkpoint` but no `Stop` or `Verdict` is replayed as interrupted prior run evidence when `Resume` is appended.
    - Test 2: A ledger with a `clean_completion` verdict refuses same-run resume in pure ledger logic.
    - Test 3: A resource-stop ledger replay preserves `resource_stop` in both the latest verdict and Markdown report.
  </behavior>
  <action>
    Extend `packages/open-bitcoin-cli/src/operator/soak/tests.rs` with deterministic ledger replay tests named `soak_synthetic_interrupted_run_replays_as_unexpected_termination_resume`, `soak_synthetic_clean_completion_refuses_same_run_resume`, and `soak_synthetic_resource_stop_report_preserves_final_outcome`.

    Use explicit timestamps `1_777_300_000`, `1_777_300_060`, and `1_777_300_120`. Do not call system time in these tests. Use temp directories under the repo's existing test-sandbox pattern. Build event envelopes through the same public crate-local constructors used by production code, not hand-written JSON strings.
  </action>
  <verify>
    <automated>cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_synthetic_ --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "soak_synthetic_interrupted_run_replays_as_unexpected_termination_resume|soak_synthetic_clean_completion_refuses_same_run_resume|soak_synthetic_resource_stop_report_preserves_final_outcome" packages/open-bitcoin-cli/src/operator/soak/tests.rs` finds all tests.
    - `rg -n "1_777_300_000|1_777_300_060|1_777_300_120|unexpected_termination|clean_completion|resource_stop" packages/open-bitcoin-cli/src/operator/soak/tests.rs` finds explicit timestamps and outcomes.
    - `cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_synthetic_ --all-features` exits 0.
  </acceptance_criteria>
  <done>Ledger replay tests cover interrupted recovery, clean-completion resume refusal, and resource-stop report projection with explicit timestamps and no wall-clock dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixtures -> sync runtime | Local scripted peer messages exercise runtime behavior without public peers. |
| Durable store reopen -> status evidence | Reopened temp datadirs prove resume behavior from persisted data. |
| Ledger replay -> report projection | Synthetic event histories prove interruption/resume semantics without operator timing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Spoofing | synthetic long-run tests | mitigate | Assert `dns_seeds: Vec::new()` and loopback manual peers so tests cannot masquerade public-network evidence as deterministic proof. |
| T-75-03-02 | Tampering | ledger replay fixtures | mitigate | Build replay histories through typed event constructors instead of raw JSON strings. |
| T-75-03-03 | Repudiation | same-datadir reopen tests | mitigate | Assert connected active-chain height/hash/work before and after reopen and verify no duplicate getdata for already connected blocks. |
| T-75-03-04 | Denial of Service | test execution | mitigate | Use 96 synthetic blocks, explicit timestamps, no sleeps, no DNS, and no service manager calls. |
| T-75-03-05 | Information Disclosure | test fixtures | accept | Fixtures use local temp datadirs and synthetic blocks only; no wallet, credential, or raw operator material is created. |
</threat_model>

<verification>
Run deterministic coverage:

```bash
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node phase75_synthetic_soak_ --all-features
cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-cli --lib soak_synthetic_ --all-features
bun run scripts/check-parity-breadcrumbs.ts --check
```
</verification>

<success_criteria>
- Synthetic long-run tests use `DurableSyncRuntime`, scripted transports, explicit timestamps, and local temp datadirs.
- Tests do not use public peers, DNS seeds, service managers, long sleeps, or live-smoke scripts.
- Ledger replay tests cover interrupted resume, clean-completion resume refusal, and resource-stop projection.
- New Rust test module is registered in parity breadcrumbs.
</success_criteria>

<output>
After completion, create `.planning/phases/75-multi-day-soak-runner-and-evidence-ledger/75-03-SUMMARY.md`.
</output>
