---
phase: 130-resource-time-and-fee-primitives
plan: "08"
type: execute
wave: 8
depends_on:
  - 130-07
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/types.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
  - packages/open-bitcoin-rpc/src/method/node.rs
  - docs/parity/catalog/mempool-policy.md
  - docs/parity/checklist.md
  - docs/parity/index.json
  - docs/parity/source-breadcrumbs.json
  - scripts/check-phase130-resource-time-fee-primitives.ts
  - scripts/check-phase130-resource-time-fee-primitives.test.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - FEEP-01
  - FEEP-02
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Operator evidence maps `bytes` to total transaction vsize, `usage` to accounted memory, and `maxmempool` to configured accounted-memory capacity."
    - "Operator evidence distinguishes static, incremental, raw rolling, and derived effective admission fee roles; `mempoolminfee` remains the effective max(static, rolling)."
    - "Evidence states that Phase 130 still enforces the explicit legacy vsize trim seam rather than falsely claiming accounted-capacity enforcement."
    - "Shared evidence uses fixed aggregate/enum-derived fields and contains no transaction or peer identities."
    - "A deterministic checker guards every FEEP contract, locked decision boundary, legacy compatibility rule, and deferred-scope exclusion."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/method/node.rs"
      provides: "Truthful aggregate getmempoolinfo resource and fee fields"
      contains: "incrementalrelayfee"
    - path: "docs/parity/catalog/mempool-policy.md"
      provides: "Phase 130 formula, Knots mappings, intentional Rust-accounting difference, and later-phase boundaries"
      contains: "Phase 130 resource, time, and fee primitives"
    - path: "scripts/check-phase130-resource-time-fee-primitives.ts"
      provides: "Deterministic FEEP/decision/parity/no-claim guard"
      contains: "checkPhase130ResourceTimeFeePrimitives"
    - path: "scripts/check-phase130-resource-time-fee-primitives.test.ts"
      provides: "Independent mutation failures for all five requirements and high-risk boundaries"
      contains: "FEEP-01"
  key_links:
    - from: "packages/open-bitcoin-node/src/network.rs"
      to: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      via: "One authoritative ManagedMempoolInfo snapshot projects typed resources and fees"
      pattern: "mempool_info"
    - from: "scripts/check-phase130-resource-time-fee-primitives.ts"
      to: "scripts/verify.sh"
      via: "Checker test and live check run after Phase 129 and before final Phase 117 gate"
      pattern: "check-phase130-resource-time-fee-primitives"
---

<objective>
Expose truthful aggregate resource/fee evidence and install deterministic Phase 130 guardrails.

Purpose: Complete D-04 and D-15 operator truth, document all D-01 through D-16 boundaries, and make the full Phase 130 contract auditable before Phase 131 consumes it.
Output: Corrected authoritative mempool info/RPC fields, unequal-value tests, parity documentation/registration, a mutation-tested checker, verifier wiring, and full repository verification.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-07-SUMMARY.md
@.planning/phases/103-mempool-chainstate-lifecycle-and-durable-recovery/103-04-SUMMARY.md
@.planning/phases/129-integration-guardrails-and-milestone-reconciliation/129-01-SUMMARY.md
@packages/bitcoin-knots/src/rpc/mempool.cpp
@packages/bitcoin-knots/src/txmempool.cpp
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md

<interfaces>
Required baseline-compatible mappings:
- `size`: transaction count
- `bytes`: `TransactionVirtualSize`
- `usage`: `AccountedMempoolMemory`
- `maxmempool`: `MempoolCapacity`
- `minrelaytxfee`: `StaticRelayFeeRate`
- `incrementalrelayfee`: `IncrementalRelayFeeRate`
- `mempoolminfee`: `EffectiveAdmissionFeeRate == max(static, rolling)`

Open Bitcoin extension fields may expose raw `rollingmempoolfee`, duplicate the explicit `effectiveadmissionfee`, and identify `capacityenforcement` as fixed `legacy_vsize` in Phase 130. Do not redefine a Knots field to carry the extension.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project authoritative resource and fee evidence</name>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/network/types.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-rpc/src/method/node.rs
    - packages/bitcoin-knots/src/rpc/mempool.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/types.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
    packages/open-bitcoin-rpc/src/method/node.rs
  </files>
  <behavior>
    - A witness/script fixture produces exact `bytes != usage` and both differ from `maxmempool`.
    - `mempoolminfee` and `effectiveadmissionfee` equal max(static, rolling); `minrelaytxfee`, `incrementalrelayfee`, and `rollingmempoolfee` remain separate exact values.
    - `incrementalrelayfee` does not influence `mempoolminfee`.
    - `capacityenforcement` serializes exactly `legacy_vsize` during Phase 130.
    - Serialized shared mempool info contains no txid, wtxid, raw transaction, peer id, endpoint, or dynamic label.
  </behavior>
  <action>
    Extend `ManagedMempoolInfo` with typed `total_virtual_size`, `accounted_memory`, `mempool_capacity`, all four fee roles, and a fixed enum-derived `MempoolCapacityEnforcement::LegacyVsize` label. Build it from the authoritative mempool resource ledger/config/fee state under the existing single operator snapshot read. Capacity status may describe accounted usage versus capacity, but the separate enforcement field must remain truthful that Phase 130 trimming is legacy vsize.

    Correct `GetMempoolInfoResponse` and projection per D-04: `bytes=vsize`, `usage=accounted`, `maxmempool=accounted capacity`, `mempoolminfee=effective`, `minrelaytxfee=static`, and add `incrementalrelayfee`. Add Open Bitcoin extension fields `rollingmempoolfee`, `effectiveadmissionfee`, and `capacityenforcement` without changing baseline field meanings. Preserve existing `total_fee_sats` and `loaded`.

    Add an integration fixture with a nontrivial witness/script transaction and non-default capacity/static/incremental/rolling values so the test proves unequal mappings and incremental exclusion, not merely field presence. Keep this aggregate response identity-free.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-rpc-mempool-evidence -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_mempool_info -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "usage: info\\.accounted_memory|maxmempool: info\\.mempool_capacity|incrementalrelayfee|rollingmempoolfee|effectiveadmissionfee|capacityenforcement" packages/open-bitcoin-rpc/src/{dispatch/node.rs,method/node.rs}` finds every mapping/field.
    - Focused RPC tests assert exact unequal `bytes`, `usage`, and `maxmempool`.
    - Focused tests assert `mempoolminfee == effectiveadmissionfee == max(minrelaytxfee, rollingmempoolfee)` and is independent of incremental fee.
    - Serialized response/status fixture contains none of the forbidden identity/material markers.
  </acceptance_criteria>
  <done>Authoritative node and RPC projections expose exact distinct resource/fee meanings with truthful enforcement and no shared identity leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Register parity evidence and install a mutation-tested phase guard</name>
  <read_first>
    - docs/parity/catalog/mempool-policy.md
    - docs/parity/checklist.md
    - docs/parity/index.json
    - docs/parity/source-breadcrumbs.json
    - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
    - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts
    - scripts/verify.sh
    - .planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
    - .planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
  </read_first>
  <files>
    docs/parity/catalog/mempool-policy.md
    docs/parity/checklist.md
    docs/parity/index.json
    docs/parity/source-breadcrumbs.json
    scripts/check-phase130-resource-time-fee-primitives.ts
    scripts/check-phase130-resource-time-fee-primitives.test.ts
    scripts/verify.sh
  </files>
  <action>
    Register parity surface `v2-2-resource-time-fee-primitives` with unique ownership of FEEP-01 through FEEP-05 and exact Knots sources for resources, RPC mapping, admission metadata, persistence, removal causes, and retry timing. Update `mempool-policy.md` with the version-1 Rust-owned accounting formula and explicit intentional difference from C++ allocator estimates; all four fee roles; metadata/recovery compatibility; explicit contexts; lifecycle delta invariants; and the exact `bytes`/`usage`/`maxmempool` mappings.

    State exact deferred boundaries without weakening Phase 130: Phase 131 owns accounted enforcement/rolling mechanics; Phase 132 owns package execution and pinned exceptions; Phase 134 owns complete cross-cache projection; Phase 135 owns full snapshot/checkpoint/recovery schema; Phase 136 owns retry scheduling/receipts. Do not claim general package wire relay, whole-mempool rebroadcast, public/default relay, guaranteed propagation, public-network default CI, production readiness, or production-funds use.

    Add a deterministic Bun checker exporting `checkPhase130ResourceTimeFeePrimitives(maybeRepoRoot?)`. It must verify every FEEP requirement and D-01..D-16 evidence family: resource types/formula/cache/oracle/legacy enforcement seam; fee role types/effective derivation/package static boundary; metadata/contexts/no hidden effects/no zero-time production calls; lifecycle delta/cause-role/final membership/fixed labels; legacy all-or-none snapshot compatibility; exact RPC mappings; parity ownership/anchors; and deferred/no-claim text. It must use only local file reads.

    Add independent mutation tests that each remove or corrupt one required anchor: one row per FEEP ID plus overflow checking, incremental exclusion, peer/local origin, hidden clock/RNG token, cause-role split, legacy partial-metadata rejection, RPC usage mapping, Phase 131/134 boundary, identity leak, and forbidden broad claim. Wire the test and live checker after Phase 129 and before the final Phase 117 gate in both `VERIFY_COMMAND_ORDER` and executable `run_step` order. Preserve Phase 117 as the last phase-numbered release gate.
  </action>
  <verify>
    <automated>bun test scripts/check-phase130-resource-time-fee-primitives.test.ts &amp;&amp; bun run scripts/check-phase130-resource-time-fee-primitives.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/index.json` parses and contains one surface with requirements exactly `FEEP-01` through `FEEP-05`.
    - Checker mutation tests fail independently for every listed FEEP/security/boundary mutation and pass the complete corpus.
    - `rg -n "check-phase(129|130|117)" scripts/verify.sh` shows Phase 129, then Phase 130 test/check, then Phase 117 in both visible and executable sequences.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` passes.
    - `bash scripts/verify.sh` passes before the plan is committed.
  </acceptance_criteria>
  <done>Parity evidence uniquely owns all FEEP requirements and the mutation-tested local guard passes inside the full repository verifier.</done>
</task>

</tasks>

<threat_model>
## Assets
- Truthful operator capacity/fee evidence.
- Shared evidence confidentiality and bounded cardinality.
- Auditable parity ownership and release scope.
- Deterministic verification contract.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Authoritative mempool → RPC serialization | Internal typed values become externally visible aggregate fields. |
| Lifecycle/metadata → shared evidence | Detailed identities must not cross the aggregate boundary. |
| Repository corpus → deterministic checker | Missing or misleading implementation/docs must fail closed. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-29 | Tampering / Repudiation | High | resource and fee RPC mapping | mitigate | Exact unequal-value and fee-equation tests plus checker mutations are blocking. |
| T-130-30 | Information Disclosure / Denial of Service | High | shared evidence | mitigate | Aggregate fixed fields only; forbidden identifier/material serialization and mutation tests block. |
| T-130-31 | Tampering | High | parity/no-claim guard | mitigate | Unique FEEP ownership, exact anchors, deferred boundaries, and forbidden-claim mutations run in default verification. |
| T-130-32 | Denial of Service | Medium | checker execution | mitigate | Local bounded file reads only; no network, subprocess spawning, wall-clock, or public-network dependency. |

## Security Verification
- Every high-severity threat has a concrete blocking test/checker mutation.
- Full `bash scripts/verify.sh` is mandatory and retains the final Phase 117 release boundary.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan08-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node mempool_info -- --nocapture`
2. `bun run scripts/command-timings.ts run --key phase130-plan08-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc get_mempool_info -- --nocapture`
3. `bun test scripts/check-phase130-resource-time-fee-primitives.test.ts`
4. `bun run scripts/check-phase130-resource-time-fee-primitives.ts`
5. `bun run scripts/check-parity-breadcrumbs.ts --check`
6. `bash scripts/verify.sh`
7. `git diff --check`
</verification>

<success_criteria>
- `getmempoolinfo` resource and fee meanings match the pinned contract and are proven with unequal values.
- Shared evidence is fixed, aggregate, and identity-free.
- Every FEEP requirement and D-01 through D-16 decision is represented by implementation evidence or an exact later-phase boundary.
- Legacy snapshot compatibility is guarded.
- Default verification remains deterministic, hermetic, and release-boundary-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-08-SUMMARY.md`.
</output>
