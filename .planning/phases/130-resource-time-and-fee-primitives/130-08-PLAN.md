---
phase: 130-resource-time-and-fee-primitives
plan: "08"
type: execute
wave: 8
depends_on:
  - 130-07
files_modified:
  - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
  - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
  - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
  - packages/open-bitcoin-node/src/network/recovery.rs
  - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
autonomous: true
requirements:
  - FEEP-03
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "New snapshots preserve known acceptance time, origin, and relay intent exactly."
    - "Existing current-schema snapshots with no metadata still decode and replay as explicit legacy-unknown, non-local, non-requested entries."
    - "Partially present or invalid metadata is classified as mempool snapshot corruption rather than guessed."
    - "Legacy recovery does not bump the repository-wide schema and never substitutes restart time."
  artifacts:
    - path: "packages/open-bitcoin-node/src/storage/mempool_snapshot.rs"
      provides: "Snapshot records carrying canonical entry metadata and context-aware recovery replay"
      contains: "pub metadata: MempoolEntryMetadata"
    - path: "packages/open-bitcoin-node/src/storage/snapshot_codec.rs"
      provides: "Backward-compatible optional wire fields and all-or-none metadata decoding"
      contains: "maybe_accepted_at_unix_seconds"
    - path: "packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs"
      provides: "Known round-trip, legacy decode, partial corruption, and schema-stability tests"
      contains: "legacy_mempool_snapshot_decodes_to_fail_closed_metadata"
  key_links:
    - from: "packages/open-bitcoin-node/src/storage/snapshot_codec.rs"
      to: "packages/open-bitcoin-node/src/storage/mempool_snapshot.rs"
      via: "DTO all-or-none metadata maps to known or legacy-unknown domain metadata"
      pattern: "MempoolEntryMetadata"
    - from: "packages/open-bitcoin-node/src/storage/mempool_snapshot.rs"
      to: "packages/open-bitcoin-mempool/src/context.rs"
      via: "Recovery replay uses record metadata as AdmissionContext without substitution"
      pattern: "AdmissionContext"
---

<objective>
Resolve legacy snapshot compatibility while preserving truthful recovery metadata.

Purpose: Complete D-10 for the already-existing snapshot path without stealing Phase 135's schema/checkpoint/replay scope or invalidating unrelated durable namespaces.
Output: Metadata-bearing snapshot records, backward-compatible wire decoding, fail-closed legacy classification, context-aware replay, and exact compatibility tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-07-SUMMARY.md
@.planning/phases/103-mempool-chainstate-lifecycle-and-durable-recovery/103-03-SUMMARY.md
@.planning/phases/108-durable-mempool-relay-state-recovery/108-01-SUMMARY.md
@packages/bitcoin-knots/src/node/mempool_persist.cpp
@packages/bitcoin-knots/test/functional/mempool_persist.py
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Concrete compatibility decision:
- Keep the existing repository `SchemaVersion::CURRENT` unchanged.
- Add three optional mempool-record DTO fields with serde defaults and stable external names: `accepted_at_unix_seconds`, `origin`, and `relay_requested`.
- Internally name optional fields with `maybe_`.
- All three present decodes to known typed metadata.
- All three absent decodes to `LegacyUnknown` + `RecoveryUnknown` + `NotRequested`.
- Any partial combination, invalid origin label, or out-of-range value is `StorageError::Corruption { namespace: Mempool, ... }`.
- Encoding a known record emits all three fields. No code writes a partial set.

This is the final Phase-130 answer to the research open question. Phase 135 may add mempool-local versioning/checkpoint semantics, but it must preserve this truthful legacy classification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve metadata through snapshot capture and replay</name>
  <read_first>
    - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
    - packages/open-bitcoin-node/src/network/recovery.rs
    - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/bitcoin-knots/src/node/mempool_persist.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
    packages/open-bitcoin-node/src/network/recovery.rs
    packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  </files>
  <behavior>
    - `MempoolSnapshot::from_mempool` copies each entry's exact metadata.
    - Known local requested metadata at time 90 replays as time 90/local/requested and remains retry-eligible only while present.
    - Known peer/reorg metadata remains peer/reorg and non-local after replay.
    - Legacy metadata replays as unknown/non-local/not-requested, not restart time/local.
    - Recovery duplicate/failure does not rewrite existing canonical metadata.
  </behavior>
  <action>
    Add `metadata: MempoolEntryMetadata` to `MempoolSnapshotRecord` and copy it in `from_mempool`. Change replay to call the context-aware transition API with `AdmissionContext::recovery(record.metadata.clone())`. Known metadata must pass through unchanged. Legacy metadata must stay `LegacyUnknown`, `RecoveryUnknown`, and `NotRequested`; do not sample a clock or infer local origin from snapshot presence.

    Update managed recovery to consume each transition delta for current state restoration while retaining existing typed recovered/dropped classifications and no-socket fanout seeding. Add exact tests for local, peer, reorg, legacy, and duplicate metadata. Do not add topological replay, unbroadcast membership persistence, checkpoint cadence, dirty generations, I/O scheduling, or rolling-fee persistence; Phase 135 owns them.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-recovery-metadata -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovery_metadata -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub metadata: MempoolEntryMetadata|AdmissionContext::recovery" packages/open-bitcoin-node/src/{storage/mempool_snapshot.rs,network/recovery.rs}` finds capture and replay wiring.
    - Focused tests prove exact known metadata and fail-closed legacy metadata after replay.
    - `rg -n "SystemTime|UNIX_EPOCH|MempoolOrigin::Local" packages/open-bitcoin-node/src/storage/mempool_snapshot.rs` returns no recovery-time/origin fabrication.
    - Existing recovery classification and no-socket fanout tests remain green.
  </acceptance_criteria>
  <done>Snapshot capture/replay preserves known metadata exactly and keeps legacy metadata unknown and retry-ineligible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add all-or-none backward-compatible metadata decoding</name>
  <read_first>
    - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
    - packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    - packages/open-bitcoin-node/src/storage/mempool_snapshot.rs
    - packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-node/src/storage.rs
    - packages/bitcoin-knots/src/node/mempool_persist.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs
    packages/open-bitcoin-node/src/storage/fjall_store/tests.rs
    packages/open-bitcoin-node/src/network/tests/recovery_cases.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  </files>
  <behavior>
    - New known-metadata snapshots round-trip all three fields.
    - A literal pre-Phase-130 current-schema JSON record lacking all metadata fields decodes successfully to fail-closed legacy metadata.
    - Each of the three partial-field combinations fails as mempool corruption.
    - Unknown origin strings and malformed timestamp/relay values fail as mempool corruption.
    - Encoded schema version remains exactly the pre-Phase-130 `SchemaVersion::CURRENT`.
  </behavior>
  <action>
    Extend `MempoolSnapshotRecordDto` with `maybe_accepted_at_unix_seconds`, `maybe_origin`, and `maybe_relay_requested`, using serde `default`, stable external field names, and omission only for the all-legacy case. Add a closed serde DTO enum for `local`, `peer`, `reorg`, and `recovery_unknown`.

    Implement explicit all-or-none decoding: `(Some, Some, Some)` builds known typed metadata after validation; `(None, None, None)` builds fail-closed legacy metadata; every other tuple returns `StorageError::Corruption` in `StorageNamespace::Mempool`. Keep `SchemaVersion::CURRENT` unchanged and add a literal old-format fixture proving backward decode at that same version. Update every `MempoolSnapshotRecord` fixture in snapshot codec, Fjall, recovery, and lifecycle tests with explicit metadata.

    Do not use `#[serde(default)]` directly on a domain metadata struct in a way that could silently guess local/requested values.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-snapshot-compat -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node mempool_snapshot -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybe_accepted_at_unix_seconds|maybe_origin|maybe_relay_requested" packages/open-bitcoin-node/src/storage/snapshot_codec.rs` finds all optional internal DTO fields.
    - Tests named for known round-trip, legacy fail-closed decode, partial metadata corruption, invalid origin, and unchanged schema all pass.
    - `rg -n "SchemaVersion::CURRENT|get\\(\\)" packages/open-bitcoin-node/src/storage/snapshot_codec/tests.rs` includes an assertion that the encoded version did not change.
    - No unrelated chainstate/wallet/runtime schema fixture changes are required.
  </acceptance_criteria>
  <done>The codec implements the all-or-none compatibility decision, rejects malformed metadata, and retains the existing global schema version.</done>
</task>

</tasks>

<threat_model>
## Assets
- Original acceptance age.
- Local-origin and relay-intent privacy truth across restart.
- Durable namespace compatibility.
- Recovery availability for existing snapshots.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Fjall bytes → snapshot DTO | Corrupt or crafted JSON controls optional metadata fields. |
| Legacy DTO → canonical entry | Missing facts must remain unknown and retry-ineligible. |
| Canonical entry → new snapshot | Known metadata must be serialized atomically as one logical set. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-25 | Spoofing / Information Disclosure | High | legacy origin/relay decode | mitigate | All-absent maps to recovery-unknown/not-requested; exact replay tests are blocking. |
| T-130-26 | Tampering | High | partial metadata DTO | mitigate | All-or-none decode rejects every partial combination as typed corruption. |
| T-130-27 | Repudiation | High | acceptance timestamp | mitigate | Known time round-trips exactly; missing time remains `LegacyUnknown`; restart time is never substituted. |
| T-130-28 | Denial of Service | Medium | global schema compatibility | mitigate | No global schema bump; literal old-format and unrelated namespace tests remain green. |

## Security Verification
- High-severity spoofing/tampering/time threats block on exact codec and replay tests.
- Crafted partial/invalid metadata must fail before mempool mutation.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan08-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node mempool_snapshot -- --nocapture`
2. `bun run scripts/command-timings.ts run --key phase130-plan08-recovery -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node recovery -- --nocapture`
3. `git diff --check`
</verification>

<success_criteria>
- The legacy snapshot compatibility decision is concrete, implemented, and tested without a global schema bump.
- New recovery preserves known metadata; legacy recovery remains unknown and retry-ineligible.
- FEEP-03 and recovery-related FEEP-05 are truthful across current durable snapshots.
- Phase 135 retains complete snapshot schema, topological replay, checkpointing, and crash-window ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-08-SUMMARY.md`.
</output>
