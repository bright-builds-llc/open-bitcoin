---
phase: 130-resource-time-and-fee-primitives
plan: "04"
type: execute
wave: 4
depends_on:
  - 130-03
files_modified:
  - packages/open-bitcoin-mempool/src/outcome.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
  - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Committed mempool consequences are represented by one cache-agnostic `MempoolLifecycleDelta`, separate from `MempoolOutcome` attempt results."
    - "Removal cause and direct-versus-descendant role are independent typed facts."
    - "Affected identities have deterministic admitted/removal/final-membership/retry-clear ordering and deduplication."
    - "Shared labels come only from fixed enums; transaction identities remain internal or direct-response data."
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      provides: "Lifecycle delta, removal cause/role, final membership, retry clear, and deterministic builder"
      contains: "pub struct MempoolLifecycleDelta"
    - path: "packages/open-bitcoin-mempool/src/pool.rs"
      provides: "Committed admission/removal transitions that return semantic deltas"
      contains: "MempoolTransition"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs"
      provides: "Ordering, deduplication, cause/role, and final-membership regressions"
      contains: "lifecycle_delta_deduplicates_and_orders_affected_members"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool.rs"
      to: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      via: "Committed paths return MempoolTransition containing one MempoolLifecycleDelta"
      pattern: "MempoolTransition"
    - from: "packages/open-bitcoin-mempool/src/pool/admission_outcome.rs"
      to: "packages/open-bitcoin-mempool/src/outcome.rs"
      via: "Attempt result remains MempoolOutcome while committed facts are built separately"
      pattern: "MempoolOutcome"
---

<objective>
Create the single semantic lifecycle fact set that later authority and cache projection consume.

Purpose: Implement D-13 through D-16 at the pure-core seam, eliminating cause/role conflation and node-side reclassification without expanding Phase 130 into Phase 134's complete cross-cache projection.
Output: `MempoolLifecycleDelta`, stable lifecycle enums/labels, deterministic invariants, transition-returning committed paths, and focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-03-SUMMARY.md
@packages/bitcoin-knots/src/kernel/mempool_removal_reason.h
@packages/bitcoin-knots/src/txmempool.cpp
@packages/bitcoin-knots/src/validation.h
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
`MempoolOutcome` remains validation/admission-attempt vocabulary. Production adapters must migrate to:
```rust
pub struct MempoolTransition {
    pub outcome: MempoolOutcome,
    pub delta: MempoolLifecycleDelta,
}
```

The delta contains admitted identities, typed removals, final membership for every affected identity, and retry-clear facts. It is not a cache command and must not name serving, fanout, persistence, compact reconstruction, RPC, metrics, or logs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lifecycle facts, labels, and invariants</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/lib.rs
    - packages/open-bitcoin-mempool/src/outcome.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
    - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/bitcoin-knots/src/kernel/mempool_removal_reason.h
    - packages/bitcoin-knots/src/txmempool.cpp
    - docs/parity/source-breadcrumbs.json
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/lib.rs
    packages/open-bitcoin-mempool/src/outcome.rs
    packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    packages/open-bitcoin-mempool/src/pool/tests.rs
    packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
    packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Removal labels are exactly `replacement`, `expiry`, `pressure`, `block_confirmation`, `block_conflict`, and `reorg`.
    - Removal roles are exactly `direct` and `descendant`; a direct fact upgrades a duplicate descendant fact without replacing its cause.
    - Admitted members preserve supplied transition/topological order while duplicate identities appear once.
    - Removals, final membership, and retry clears are deduplicated by identity and emitted in deterministic txid/wtxid order.
    - Every affected identity has exactly one final `present` or `absent` membership fact.
  </behavior>
  <action>
    Write tests first. Replace `MempoolLifecycleRemovalReason` with independent `MempoolRemovalCause::{Replacement, Expiry, Pressure, BlockConfirmation, BlockConflict, Reorg}` and `MempoolRemovalRole::{Direct, Descendant}`. Add `as_str()` fixed labels for both. Add `MempoolMemberIdentity { txid, wtxid }`, `FinalMempoolMembership::{Present, Absent}`, `MempoolMemberState`, `MempoolLifecycleRemoval`, `MempoolRetryClearCause::{LifecycleRemoval, EligibleServe, TransportWritten}`, and `MempoolRetryClear`.

    Define `MempoolLifecycleDelta` with admitted, removed, final-membership, and retry-clear collections plus a builder that enforces D-14/D-15 invariants: admitted order preserved with first identity winning; the other collections ordered by identity; one removal per identity using a documented stable cause precedence and direct-over-descendant role; one final state per identity; one retry-clear fact per identity/cause. Return typed invariant errors for contradictory duplicate facts rather than panicking.

    Keep identities in the semantic delta but expose no identity-derived metric/log/support label. Register the new test file in `mempool-lifecycle` breadcrumbs. Do not add cache-specific effects; complete projection remains Phase 134 per D-16.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-lifecycle-delta-types -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool lifecycle_delta -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub (struct|enum) (MempoolLifecycleDelta|MempoolRemovalCause|MempoolRemovalRole|FinalMempoolMembership|MempoolRetryClearCause|MempoolTransition)" packages/open-bitcoin-mempool/src/{pool.rs,pool/lifecycle.rs}` finds all semantic contracts.
    - `rg -n "Descendant" packages/open-bitcoin-mempool/src/pool/lifecycle.rs` shows it only as `MempoolRemovalRole::Descendant`, never a cause.
    - Focused tests prove exact labels, deterministic ordering, deduplication, direct-role precedence, final membership, and typed contradiction errors.
    - No field in `MempoolLifecycleDelta` names a node/network/RPC cache.
  </acceptance_criteria>
  <done>Lifecycle facts, labels, ordering, deduplication, and contradiction handling satisfy the cache-agnostic contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce lifecycle deltas from committed mempool transitions</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/outcome.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
    - packages/open-bitcoin-mempool/src/pool/topology.rs
    - packages/bitcoin-knots/src/validation.cpp
    - packages/bitcoin-knots/src/txmempool.cpp
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/outcome.rs
    packages/open-bitcoin-mempool/src/pool.rs
    packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
    packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    packages/open-bitcoin-mempool/src/pool/tests/lifecycle_cases.rs
    packages/open-bitcoin-mempool/src/pool/tests/lifecycle_delta_cases.rs
  </files>
  <behavior>
    - Accepted admission yields one admitted member and final `present`.
    - Replacement yields admitted replacement `present`, direct victim `replacement/absent`, and descendant victims `replacement/descendant/absent`.
    - Legacy vsize trim yields `pressure` removals and truthful final membership without implementing accounted-pressure policy or a rolling bump.
    - Block confirmation and conflict use distinct causes; conflict descendants retain `block_conflict` with descendant role.
    - Rejected, orphaned, and duplicate attempts return an empty committed delta and do not rewrite existing member metadata.
  </behavior>
  <action>
    Add `MempoolTransition { outcome, delta }` and context-aware transition entry points for admission. Build the delta from the prospective/committed state, not by parsing `MempoolOutcome` strings or allowing node consumers to infer causes. Capture victim wtxids before removal. Mark legacy trim removals with semantic `Pressure` while keeping the Phase-130 enforcement seam on legacy vsize and leaving rolling-floor changes absent.

    Add transition-returning block cleanup methods that classify the confirmed transaction as `BlockConfirmation/Direct`, direct conflicts as `BlockConflict/Direct`, and their descendants as `BlockConflict/Descendant`. Keep compatibility summary/outcome wrappers only as projections from the transition so this plan compiles independently; Plans 130-05 and 130-06 migrate all production node consumers to the delta.

    Define reorg and retry-clear variants/builders now, but do not invent reorg removal behavior or receive-independent retry clearing. Those facts are emitted only when a real committed transition exists in Phases 134/136.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-lifecycle-delta-production -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool lifecycle -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Admission, replacement, legacy trim, block confirmation, block conflict/descendant, and empty-attempt delta tests pass.
    - `rg -n "MempoolLifecycleRemovalReason|reason:" packages/open-bitcoin-mempool/src/pool` returns no old overloaded removal-reason type/field.
    - `rg -n "outcome\\.replaced\\(\\)|outcome\\.evicted\\(\\)" packages/open-bitcoin-mempool/src` finds no lifecycle-delta construction by outcome reclassification.
    - `RollingMempoolFeeRate` remains unchanged by the Phase-130 legacy trim path.
  </acceptance_criteria>
  <done>Every existing committed mempool transition emits a truthful semantic delta while deferred variants remain contracts only.</done>
</task>

</tasks>

<threat_model>
## Assets
- Authoritative committed membership facts.
- Removal cause and descendant attribution.
- Stable low-cardinality evidence vocabulary.
- Future cache-cleanup completeness.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Admission/block/reorg event → committed mempool state | Hostile graph/conflict shapes may affect many identities. |
| Pure transition → node authority | Node consumers must not reinterpret outcome vectors into different causes. |
| Lifecycle enums → shared evidence | Dynamic identifiers/reasons must not become labels. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-13 | Tampering | High | lifecycle dedup/cause-role mapping | mitigate | Deterministic builder, typed contradiction errors, and replacement/block descendant tests are blocking. |
| T-130-14 | Tampering / Denial of Service | High | final membership | mitigate | Every affected identity receives exactly one final state; committed-transition tests compare the final pool. |
| T-130-15 | Information Disclosure / Denial of Service | High | shared labels | mitigate | Labels derive only from fixed enums; no identifier-to-label API exists; Plan 08 checker enforces this boundary. |
| T-130-16 | Tampering | Medium | cross-cache projection | transfer | Phase 134 owns complete `ManagedNetworkHandle` projection; Phase 130 provides and migrates the semantic seam only. |

## Security Verification
- High-severity lifecycle threats block on ordering/dedup/final-membership and fixed-label tests.
- No plan may claim all dependent caches are projected; that claim remains Phase 134.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan04-mempool -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool lifecycle -- --nocapture`
2. `bun run scripts/check-parity-breadcrumbs.ts --check`
3. `git diff --check`
</verification>

<success_criteria>
- D-13, D-14, and D-15 are executable pure-core contracts.
- D-16 is honored: semantic facts are complete, current consumers can migrate, and Phase 134 retains full cache projection.
- FEEP-05 has stable typed outcomes for every named cause even when later phases own the behavior that emits some variants.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-04-SUMMARY.md`.
</output>
