---
phase: 130-resource-time-and-fee-primitives
plan: "13"
type: execute
wave: 11
depends_on:
  - 130-12
files_modified:
  - scripts/check-phase130-resource-time-fee-primitives.ts
  - scripts/check-phase130-resource-time-fee-primitives.test.ts
  - scripts/verify.sh
autonomous: true
requirements:
  - FEEP-01
  - FEEP-02
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:44:00Z
must_haves:
  truths:
    - "A deterministic checker guards every FEEP contract, D-01 through D-16 evidence family, compatibility rule, and deferred-scope exclusion."
    - "Independent mutations prove the checker fails when each high-risk implementation, parity, privacy, or no-claim anchor is removed."
    - "README freshness is validated independently for `README.md`, `packages/README.md`, and `docs/parity/README.md`, with one exact stale-wording mutation and expected failure message per file."
    - "Default repository verification runs Phase 130 after Phase 129 and before the final Phase 117 release gate."
  artifacts:
    - path: "scripts/check-phase130-resource-time-fee-primitives.ts"
      provides: "Deterministic FEEP/decision/parity/no-claim guard"
      contains: "checkPhase130ResourceTimeFeePrimitives"
    - path: "scripts/check-phase130-resource-time-fee-primitives.test.ts"
      provides: "Independent mutation failures for all requirements and high-risk boundaries"
      contains: "FEEP-01"
    - path: "scripts/verify.sh"
      provides: "Default Phase 130 checker integration"
      contains: "check-phase130-resource-time-fee-primitives"
  key_links:
    - from: "scripts/check-phase130-resource-time-fee-primitives.test.ts"
      to: "scripts/check-phase130-resource-time-fee-primitives.ts"
      via: "Mutation fixtures invoke the exported checker against local corpus copies"
      pattern: "checkPhase130ResourceTimeFeePrimitives"
    - from: "scripts/check-phase130-resource-time-fee-primitives.ts"
      to: "scripts/verify.sh"
      via: "Checker test and live check run after Phase 129 and before final Phase 117 gate"
      pattern: "check-phase130-resource-time-fee-primitives"
---

<objective>
Freeze the complete Phase 130 contract in deterministic repository guardrails.

Purpose: Make the implementation, parity, README, compatibility, privacy, and deferred-scope evidence produced by Plans 130-01 through 130-12 fail closed under the default verification contract.
Output: A local-file-only Bun checker, independent mutation tests, verifier ordering, and a passing full repository gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-12-SUMMARY.md
@scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
@scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts
@scripts/verify.sh
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md

<interfaces>
The checker must export:
```typescript
export function checkPhase130ResourceTimeFeePrimitives(
  maybeRepoRoot?: string,
): string[];
```
This intentionally reuses the established Phase 129 `string[]` failure-list contract; do not introduce a new result alias or structure. Follow the Phase 129 checker/mutation-fixture pattern and use only bounded local file reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install mutation-tested Phase 130 verification guardrails</name>
  <read_first>
    - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.ts
    - scripts/check-phase129-integration-guardrails-and-milestone-reconciliation.test.ts
    - scripts/verify.sh
    - .planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
    - .planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
    - packages/open-bitcoin-node/src/storage/snapshot_codec.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - docs/parity/catalog/mempool-policy.md
    - docs/parity/index.json
    - docs/parity/source-breadcrumbs.json
    - README.md
    - packages/README.md
    - docs/parity/README.md
  </read_first>
  <files>
    scripts/check-phase130-resource-time-fee-primitives.ts
    scripts/check-phase130-resource-time-fee-primitives.test.ts
    scripts/verify.sh
  </files>
  <behavior>
    - The complete repository corpus passes one live Phase 130 check.
    - Removing or corrupting any one required FEEP/security/compatibility/privacy/no-claim anchor fails the corresponding mutation independently.
    - Reintroducing stale active-milestone wording in each README file independently produces that file's exact dedicated failure message.
    - The checker performs bounded local file reads only and does not invoke subprocesses, networks, clocks, or public nodes.
    - `scripts/verify.sh` retains Phase 117 as the last phase-numbered release gate.
  </behavior>
  <action>
    Write mutation tests first, then add a deterministic Bun checker exporting `checkPhase130ResourceTimeFeePrimitives(maybeRepoRoot?: string): string[]`, matching the Phase 129 checker contract exactly. Do not define a separate result alias. It must verify every FEEP requirement and D-01..D-16 evidence family: resource types/formula/cache/oracle/legacy enforcement seam; fee role types/effective derivation/package static boundary; metadata/contexts/no hidden effects/no zero-time production calls; lifecycle delta/cause-role/final membership/fixed labels; retry input bounds; legacy all-or-none snapshot compatibility; exact RPC mappings; parity ownership and production/test breadcrumbs; README freshness; and deferred/no-claim text. Use only bounded local file reads.

    Implement README checks as three independent `readTarget` calls and validations, never as one concatenated README corpus and never by reading only the root README:
    - `README.md` must reject the exact stale wording `> Status: Open Bitcoin v2.1` with failure `P130 README root freshness: README.md still advertises v2.1 active status`.
    - `packages/README.md` must reject the exact stale wording `current v2.1 milestone` with failure `P130 README packages freshness: packages/README.md still describes current v2.1 milestone`.
    - `docs/parity/README.md` must reject the exact stale wording `The current v2.1 claim is intentionally narrow:` with failure `P130 README parity freshness: docs/parity/README.md still presents v2.1 as the current claim`.

    Add independent mutation tests that each remove or corrupt one required anchor: one row per FEEP ID plus overflow checking, incremental exclusion, peer/local origin, hidden clock/RNG token, cause-role split, legacy partial-metadata rejection, retry jitter bound, RPC usage mapping, Phase 131/134 boundary, missing test-file breadcrumb, identity leak, and forbidden broad claim. Add exactly one dedicated stale-wording mutation row for each README path above; each mutation changes only that file and asserts its exact dedicated failure message.

    Wire the test and live checker after Phase 129 and before the final Phase 117 gate in both `VERIFY_COMMAND_ORDER` and executable `run_step` order. Preserve Phase 117 as the last phase-numbered release gate. Keep Bash as thin orchestration and all substantial checker logic in TypeScript.
  </action>
  <verify>
    <automated>bun test scripts/check-phase130-resource-time-fee-primitives.test.ts &amp;&amp; bun run scripts/check-phase130-resource-time-fee-primitives.ts &amp;&amp; bash scripts/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - Checker mutation tests fail independently for every listed FEEP/security/compatibility/privacy/README/boundary mutation and pass the complete corpus.
    - The test fixture includes all three README paths; each exact stale-wording mutation fails only through its dedicated expected message, proving the checker reads and validates that file independently.
    - The exported checker signature ends in `string[]` and no separate result alias exists.
    - `rg -n "check-phase(129|130|117)" scripts/verify.sh` shows Phase 129, then Phase 130 test/check, then Phase 117 in both visible and executable sequences.
    - The checker contains no subprocess, network, wall-clock, random, or public-node dependency.
    - `bash scripts/verify.sh` passes before the plan is committed.
  </acceptance_criteria>
  <done>The mutation-tested local guard protects the complete Phase 130 contract inside the full repository verifier.</done>
</task>

</tasks>

<threat_model>
## Assets
- Deterministic verification contract.
- Complete Phase 130 requirement coverage.
- Release/no-claim boundary.
- Checker availability and hermeticity.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Repository corpus → deterministic checker | Missing or misleading implementation/docs must fail closed. |
| Checker → default verifier | Ordering mistakes could bypass Phase 130 or displace the final release gate. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-31 | Tampering / Repudiation | High | FEEP/parity/no-claim guard | mitigate | Independent mutations cover requirements, implementation anchors, breadcrumbs, README truth, deferrals, and forbidden claims. |
| T-130-32 | Denial of Service | Medium | checker execution | mitigate | Local bounded file reads only; no network, subprocess spawning, wall-clock, randomness, or public-network dependency. |
| T-130-40 | Tampering | High | verifier ordering | mitigate | Exact Phase 129 → 130 → 117 assertions in both declared and executable order. |

## Security Verification
- Every high-severity checker/order threat has a concrete blocking mutation or ordering assertion.
- Full `bash scripts/verify.sh` is mandatory and retains the final Phase 117 release boundary.
</threat_model>

<verification>
Run serially:
1. `bun test scripts/check-phase130-resource-time-fee-primitives.test.ts`
2. `bun run scripts/check-phase130-resource-time-fee-primitives.ts`
3. `bun run scripts/check-parity-breadcrumbs.ts --check`
4. `bash scripts/verify.sh`
5. `git diff --check`
</verification>

<success_criteria>
- Every FEEP requirement and D-01 through D-16 decision is guarded by deterministic implementation/documentation evidence or an exact later-phase boundary.
- Legacy snapshot compatibility, test-module registration, production/test breadcrumbs, README freshness, and shared-evidence privacy cannot silently regress.
- Default verification remains deterministic, hermetic, and release-boundary-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-13-SUMMARY.md`.
</output>
