---
phase: 130-resource-time-and-fee-primitives
plan: "03"
type: execute
wave: 3
depends_on:
  - 130-02
files_modified:
  - packages/open-bitcoin-mempool/src/context.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/context_cases.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FEEP-03
  - FEEP-04
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Every accepted mempool entry carries typed acceptance time, origin, and relay intent."
    - "Admission, pressure, block, reorg, and retry decisions receive operation-specific immutable inputs."
    - "Legacy-unknown recovery metadata is explicit and never qualifies as local initial-broadcast retry."
    - "Pure mempool and network policy modules acquire neither wall-clock time nor randomness."
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/context.rs"
      provides: "Entry metadata plus admission, pressure, block, and reorg contexts"
      contains: "pub struct MempoolEntryMetadata"
    - path: "packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs"
      provides: "Injected retry observation and bounded jitter vocabulary"
      contains: "pub struct RetryDecisionContext"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/context_cases.rs"
      provides: "Metadata preservation and fail-closed retry regressions"
      contains: "legacy_unknown_metadata_is_not_retry_eligible"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool.rs"
      to: "packages/open-bitcoin-mempool/src/context.rs"
      via: "Context-aware admission copies metadata into canonical MempoolEntry state"
      pattern: "AdmissionContext"
    - from: "packages/open-bitcoin-network/src/peer/transaction_relay.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs"
      via: "Public re-export of pure retry input types"
      pattern: "RetryDecisionContext"
---

<objective>
Define canonical entry metadata and explicit operation inputs before shell and lifecycle consumers migrate.

Purpose: Implement D-09 through D-12 in the pure crates so expiry, recovery, reorg, pressure, and retry behavior cannot read hidden time/randomness or infer privacy-sensitive origin.
Output: Typed time/origin/intent metadata, narrow contexts, context-aware admission, retry jitter vocabulary, and deterministic tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-02-SUMMARY.md
@packages/bitcoin-knots/src/kernel/mempool_entry.h
@packages/bitcoin-knots/src/node/mempool_persist.cpp
@packages/bitcoin-knots/src/net_processing.cpp
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Required semantic shapes:
```rust
pub struct PolicyTime(i64);

pub enum MempoolAcceptanceTime {
    Known(PolicyTime),
    LegacyUnknown,
}

pub enum MempoolOrigin {
    Local,
    Peer,
    Reorg,
    RecoveryUnknown,
}

pub enum RelayIntent {
    Requested,
    NotRequested,
}

pub struct MempoolEntryMetadata {
    pub accepted_at: MempoolAcceptanceTime,
    pub origin: MempoolOrigin,
    pub relay_intent: RelayIntent,
}
```
`LegacyUnknown` and `RecoveryUnknown` are fail-closed compatibility facts, never guessed values. Plan 130-07 maps them to the durable wire format.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical metadata and narrow mempool contexts</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/lib.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
    - docs/parity/source-breadcrumbs.json
    - packages/bitcoin-knots/src/kernel/mempool_entry.h
    - packages/bitcoin-knots/src/node/mempool_persist.cpp
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/context.rs
    packages/open-bitcoin-mempool/src/lib.rs
    packages/open-bitcoin-mempool/src/types.rs
    packages/open-bitcoin-mempool/src/pool.rs
    packages/open-bitcoin-mempool/src/pool/admission_outcome.rs
    packages/open-bitcoin-mempool/src/pool/tests.rs
    packages/open-bitcoin-mempool/src/pool/tests/context_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Local, peer, and reorg admission retain the exact supplied known time and origin.
    - A duplicate attempt does not rewrite canonical metadata.
    - Known recovery preserves original metadata; legacy recovery is `LegacyUnknown`, `RecoveryUnknown`, and `NotRequested`.
    - Retry eligibility is true only for local + requested + currently present.
  </behavior>
  <action>
    Write the focused tests first. Add `PolicyTime`, `MempoolAcceptanceTime::{Known, LegacyUnknown}`, `MempoolOrigin::{Local, Peer, Reorg, RecoveryUnknown}`, `RelayIntent::{Requested, NotRequested}`, and `MempoolEntryMetadata` (D-09/D-10). Add `MempoolEntryMetadata::is_retry_eligible(is_current_member)` implementing local AND requested AND current membership, with every unknown/peer/reorg state failing closed.

    Add immutable `AdmissionContext { metadata }`, `PressureDecisionContext { observed_at, usage, capacity }`, `BlockLifecycleContext { connected_at, height }`, and `ReorgLifecycleContext { occurred_at }` (D-11). Constructors must require every relevant fact and expose no ambient clock/default-current-time path. Add `MempoolEntry.metadata`.

    Introduce `accept_transaction_with_context` and `accept_transaction_outcome_with_context`; copy metadata only on successful first admission. Keep existing no-context methods solely as fail-closed migration adapters that use legacy-unknown metadata so this plan remains independently compilable. Mark them deprecated with an explicit Plan-130 migration note. Plan 130-05 removes production use, and Plan 130-08's checker rejects remaining production callers. Never fabricate local origin or timestamp.

    Register `context.rs` under mempool policy breadcrumbs. Keep tests Arrange/Act/Assert and one concern each.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-mempool-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool context_cases -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub (struct|enum) (PolicyTime|MempoolAcceptanceTime|MempoolOrigin|RelayIntent|MempoolEntryMetadata|AdmissionContext|PressureDecisionContext|BlockLifecycleContext|ReorgLifecycleContext)" packages/open-bitcoin-mempool/src/context.rs` finds every contract.
    - Context tests prove exact local/peer/reorg metadata, duplicate immutability, known recovery preservation, and fail-closed legacy retry eligibility.
    - `rg -n "SystemTime|UNIX_EPOCH|getrandom|RandomState|thread_rng|rand::" packages/open-bitcoin-mempool/src` returns no matches.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` accepts `context.rs`.
  </acceptance_criteria>
  <done>Canonical metadata and all narrow mempool contexts exist, are tested, and acquire no hidden effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define injected retry time and jitter inputs</name>
  <read_first>
    - packages/open-bitcoin-network/src/lib.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay/fanout.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs
    - packages/bitcoin-knots/src/net_processing.cpp
    - docs/parity/source-breadcrumbs.json
  </read_first>
  <files>
    packages/open-bitcoin-network/src/peer/transaction_relay.rs
    packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
    packages/open-bitcoin-network/src/lib.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - `RetryJitterSeconds` accepts 0 and 300 and rejects values above 300.
    - `RetryDecisionContext` retains the exact observed Unix seconds and injected jitter.
    - Constructing and comparing retry contexts performs no sleeping, clock reads, or random sampling.
  </behavior>
  <action>
    Add `retry.rs` as a pure input-contract module. Define `RetryJitterSeconds` with a fallible constructor bounded to 0..=300 seconds, a typed range error with a fixed label, and `RetryDecisionContext { observed_at_unix_seconds: i64, jitter: RetryJitterSeconds }`. This represents the injected variable portion of the pinned 10-to-15-minute cycle without implementing scheduling, candidate scans, fanout, or transport receipts; Phase 136 owns those behaviors.

    Re-export the types through `peer::transaction_relay` and the crate root. Add focused inline tests and parity breadcrumbs to `net_processing.cpp`. Do not add RNG state, Tokio timers, `SystemTime`, `RandomState`, or a fixed schedule decision.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-retry-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network retry -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct (RetryJitterSeconds|RetryDecisionContext)" packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs` finds both types.
    - Focused tests prove 0/300 acceptance, 301 rejection, and exact context retention.
    - `rg -n "SystemTime|UNIX_EPOCH|getrandom|RandomState|thread_rng|rand::|tokio::time" packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs` returns no matches.
    - No retry scheduler, fanout emission, or unbroadcast mutation is added.
  </acceptance_criteria>
  <done>The retry module exposes only validated injected inputs and remains free of scheduling, randomness, clocks, and transport effects.</done>
</task>

</tasks>

<threat_model>
## Assets
- Canonical acceptance age.
- Privacy-sensitive local-origin and relay-intent truth.
- Deterministic policy replay.
- Future retry scheduling entropy boundary.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Peer/RPC/recovery adapter → `AdmissionContext` | External transactions and stored records acquire trusted origin/time classification. |
| Stored legacy record → canonical metadata | Missing metadata must not be upgraded to local or newly accepted. |
| Shell clock/RNG → pure contexts | Effectful samples enter deterministic policy as immutable values. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-09 | Spoofing / Information Disclosure | High | origin and retry eligibility | mitigate | Typed origin/intent plus fail-closed eligibility; legacy/peer/reorg tests must pass. |
| T-130-10 | Tampering / Repudiation | High | time and jitter inputs | mitigate | Operation contexts require explicit values and pure-module source scans forbid clock/RNG acquisition. |
| T-130-11 | Tampering | Medium | duplicate admission | mitigate | Duplicate regression proves canonical acceptance metadata is immutable. |
| T-130-12 | Denial of Service | Medium | retry jitter input | mitigate | Fallible 0..=300 bound; scheduling/work caps remain Phase 136. |

## Security Verification
- High-severity origin spoofing and hidden-effect threats block on focused tests and exact source scans.
- No shared evidence or durable codec is changed here; those boundaries are verified in Plans 130-07 and 130-08.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan03-mempool -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool context -- --nocapture`
2. `bun run scripts/command-timings.ts run --key phase130-plan03-network -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network retry -- --nocapture`
3. `bun run scripts/check-parity-breadcrumbs.ts --check`
4. `git diff --check`
</verification>

<success_criteria>
- D-09 through D-12 have complete pure-domain representations and tests.
- FEEP-03 has canonical metadata and fail-closed retry eligibility.
- FEEP-04 has explicit operation inputs with no hidden time or randomness.
- Scheduling, persistence schema completion, and transport clearing remain delegated to Phases 135 and 136.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-03-SUMMARY.md`.
</output>
