---
phase: 130-resource-time-and-fee-primitives
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/open-bitcoin-mempool/src/resource.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FEEP-01
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Transaction virtual size, accounted mempool memory, and configured mempool capacity are compile-time-distinct values."
    - "The mempool caches virtual size and accounted memory while an independent oracle can reproduce both totals from canonical entries and spent-outpoint state."
    - "Phase 130 reports an accounted-memory contract without changing the legacy vsize trimming decision owned by Phase 131."
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/resource.rs"
      provides: "Resource newtypes, versioned accounting formula, checked ledger, and recomputation oracle"
      contains: "pub struct MempoolResourceLedger"
    - path: "packages/open-bitcoin-mempool/src/pool.rs"
      provides: "Canonical mempool ownership of the cached resource ledger"
      contains: "resource_ledger"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs"
      provides: "Deterministic accounting and cache-versus-oracle regressions"
      contains: "cached_resource_ledger_matches_recomputation_oracle"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool.rs"
      to: "packages/open-bitcoin-mempool/src/resource.rs"
      via: "Mempool state transitions update MempoolResourceLedger and compare it with the independent oracle in tests"
      pattern: "MempoolResourceLedger"
    - from: "packages/open-bitcoin-mempool/src/types.rs"
      to: "packages/open-bitcoin-mempool/src/pool.rs"
      via: "PolicyConfig carries distinct accounted capacity and explicitly legacy vsize trim values"
      pattern: "mempool_capacity|legacy_vsize_trim_limit"
    - from: "packages/open-bitcoin-mempool/src/pool/tests.rs"
      to: "packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs"
      via: "The test module root registers `mod resource_cases;`, making focused resource tests discoverable"
      pattern: "mod resource_cases;"
---

<objective>
Establish the deterministic resource-accounting contract that every later pressure and operator surface consumes.

Purpose: Implement D-01 and D-02 fully while preserving D-03's boundary: Phase 130 defines truthful typed accounting, and Phase 131—not this plan—switches capacity enforcement, eviction, tolerance, and performance policy to accounted memory.
Output: Distinct resource newtypes, a documented version-1 Rust-owned accounting formula, a cached ledger, an independent recomputation oracle, and focused invariant tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md

Local guidance materially applied: functional core/imperative shell, typed invariants, no `unwrap()` in production, Arrange/Act/Assert tests, parity breadcrumbs for every new Rust file, command-timing wrappers for Cargo/Bazel, and `bash scripts/verify.sh` as the final contract.

<interfaces>
Current `PolicyConfig` uses one `max_mempool_virtual_size: usize` field for both trimming and RPC capacity reporting. Current `Mempool` and `MempoolState` cache only `total_virtual_size: usize`. This plan must remove that semantic overload without changing trimming behavior.

Pinned external meanings:
- Knots `getmempoolinfo.bytes` = total transaction virtual size.
- Knots `getmempoolinfo.usage` = accounted/dynamic memory estimate.
- Knots `getmempoolinfo.maxmempool` = configured accounted-memory capacity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define resource values and the versioned accounting formula</name>
  <read_first>
    - .planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
    - .planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
    - packages/open-bitcoin-mempool/src/lib.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
    - docs/parity/source-breadcrumbs.json
    - packages/bitcoin-knots/src/txmempool.h
    - packages/bitcoin-knots/src/txmempool.cpp
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/resource.rs
    packages/open-bitcoin-mempool/src/lib.rs
    packages/open-bitcoin-mempool/src/types.rs
    packages/open-bitcoin-mempool/src/pool/tests.rs
    packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Empty canonical state produces `TransactionVirtualSize::ZERO` and `AccountedMempoolMemory::ZERO`.
    - A witness-bearing transaction has accounted memory greater than its virtual size because fixed entry/index storage, scripts, witness payloads, relation identities, and spent-outpoint ownership are counted separately.
    - Parent/child relation identities and each spent-outpoint key/value contribute deterministic bytes.
    - Every component addition and aggregate addition returns `ResourceAccountingError::Overflow { component }` instead of saturating, wrapping, or panicking.
  </behavior>
  <action>
    Write the focused tests first. Add public newtypes `TransactionVirtualSize`, `AccountedMempoolMemory`, and `MempoolCapacity` over `usize`, each with `ZERO` where meaningful, explicit constructors/accessors, checked addition, and succinct API docs. Do not add cross-type arithmetic or `From` implementations that make capacity, usage, and vsize interchangeable (D-01).

    In `resource.rs`, define `MEMPOOL_RESOURCE_ACCOUNTING_VERSION: u32 = 1`, `ResourceAccountingError::Overflow { component: &'static str }`, and a documented formula that counts only Rust-owned logical state (D-02): entry-map `Txid` key plus fixed `MempoolEntry`; input/output element storage; scriptSig, scriptPubKey, witness payload and witness-item header storage; parent and child `Txid` payloads; and spent-outpoint index `OutPoint` key plus `Txid` value. Use `size_of` only for first-party fixed values and collection lengths for dynamic payloads. Exclude allocator capacity/slack, hash buckets, C++ pointer estimates, and network/node caches. Keep every sum checked.

    Move `FeeRate` only if later plan ownership requires it; this task owns no fee-role behavior. Add `mod resource_cases;` to `pool/tests.rs` so the new test file is compiled and discovered. Register both new Rust files in `docs/parity/source-breadcrumbs.json`: `resource.rs` under the existing `mempool-policy` group with `txmempool.h`, `txmempool.cpp`, and `rpc/mempool.cpp` anchors, and `pool/tests/resource_cases.rs` with the corresponding Knots mempool test/source anchors. The breadcrumb check must cover production and test files, not only `resource.rs`.
  </action>
  <verify>
    <automated>test "$(bun run scripts/command-timings.ts run --key phase130-resource-list -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib -- --list | rg -c '^pool::tests::resource_cases::.*: test$')" -ge 5 &amp;&amp; bun run scripts/command-timings.ts run --key phase130-resource-types -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib resource_cases -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct (TransactionVirtualSize|AccountedMempoolMemory|MempoolCapacity|MempoolResourceLedger)|MEMPOOL_RESOURCE_ACCOUNTING_VERSION" packages/open-bitcoin-mempool/src/resource.rs` finds all four types and version `1`.
    - `rg -n "saturating_add|wrapping_add|unwrap\\(" packages/open-bitcoin-mempool/src/resource.rs` returns no matches.
    - `pool/tests.rs` contains `mod resource_cases;`; the list command finds at least five named `pool::tests::resource_cases::*` tests before the focused command executes them, preventing zero-match success.
    - The focused test command executes and passes cases for empty state, witness/script payloads, parent/child relations, spent outpoints, and checked overflow.
    - `docs/parity/source-breadcrumbs.json` registers both `resource.rs` and `pool/tests/resource_cases.rs`, and `bun run scripts/check-parity-breadcrumbs.ts --check` accepts both.
  </acceptance_criteria>
  <done>Resource types, formula, overflow behavior, tests, exports, and breadcrumbs satisfy every acceptance criterion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate the cached ledger and independent oracle</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
    - packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
    - packages/open-bitcoin-mempool/src/pool/topology.rs
    - packages/bitcoin-knots/src/txmempool.cpp
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/types.rs
    packages/open-bitcoin-mempool/src/pool.rs
    packages/open-bitcoin-mempool/src/pool/tests/resource_cases.rs
  </files>
  <behavior>
    - Add, parent/child add, replacement, legacy trim removal, and connected-block removal leave the cached ledger equal to a fresh oracle result.
    - `PolicyConfig::default()` exposes `mempool_capacity == 300_000_000` accounted bytes and a separate `legacy_vsize_trim_limit == 300_000_000` vbytes.
    - Phase 130 trimming compares only `TransactionVirtualSize` to `legacy_vsize_trim_limit`; it does not compare accounted memory to `mempool_capacity`.
    - The recomputation oracle traverses canonical entries and spent outpoints independently of cached-ledger update methods.
  </behavior>
  <action>
    Replace `MempoolState.total_virtual_size` and `Mempool.total_virtual_size` with a cached `MempoolResourceLedger` containing both typed totals. Change `MempoolEntry.virtual_size` and `AggregateStats.virtual_size` to `TransactionVirtualSize`, and update fee/limit call sites through explicit accessors.

    Replace `PolicyConfig.max_mempool_virtual_size` with two non-overloaded fields: `mempool_capacity: MempoolCapacity` and `legacy_vsize_trim_limit: TransactionVirtualSize`. Preserve the current trim algorithm against `legacy_vsize_trim_limit` only and document the field as the Phase-130 transition seam that Phase 131 removes when accounted-memory enforcement lands (D-03). Do not add accounted-capacity rejection, eviction changes, rolling-fee bump/decay, tolerance, or benchmarks.

    Implement `MempoolResourceLedger` update/build methods for the committed/prospective state path and a public/testable `recompute_resource_ledger(entries, spent_outpoints)` oracle whose traversal does not call the cache mutation methods. Make state recomputation and admission propagate `ResourceAccountingError` as a typed `MempoolError::InternalInvariant` boundary without panic. Add `Mempool::resource_ledger()` and typed total accessors.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-resource-ledger -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool cached_resource_ledger -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "max_mempool_virtual_size" packages/open-bitcoin-mempool/src` returns no production matches.
    - `rg -n "mempool_capacity|legacy_vsize_trim_limit|resource_ledger|recompute_resource_ledger" packages/open-bitcoin-mempool/src/{types.rs,pool.rs,resource.rs}` finds the typed capacity, legacy trim seam, cache, and oracle.
    - The focused test proves cache/oracle equality after add, replacement, trim, and block removal.
    - A source assertion or focused test proves `trim_to_size` reads `legacy_vsize_trim_limit` and does not read `mempool_capacity`.
  </acceptance_criteria>
  <done>The committed mempool maintains typed cached totals equal to the oracle while legacy vsize trimming remains the only Phase-130 enforcement path.</done>
</task>

</tasks>

<threat_model>
## Assets
- Correct accounted-memory totals used by future capacity enforcement.
- Mempool availability under hostile transaction shapes.
- Operator truth separating vsize, usage, and capacity.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Serialized transaction → pure mempool | Attacker-controlled input/output/script/witness lengths and graph relations enter accounting. |
| Canonical mempool state → cached ledger | Mutation paths may omit or double-count owned structures. |
| Cached ledger → future policy/operator adapters | Incorrect totals could permit exhaustion or false capacity evidence. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-01 | Tampering / Denial of Service | High | `resource.rs` arithmetic | mitigate | Checked component and aggregate math returns `ResourceAccountingError::Overflow`; Task 1 overflow tests must pass before commit. |
| T-130-02 | Tampering | High | `MempoolResourceLedger` cache | mitigate | Independent full-state oracle is compared after add/replace/remove transitions; Task 2 cache/oracle tests are blocking. |
| T-130-03 | Repudiation | Medium | accounting formula | mitigate | Version constant and rustdoc enumerate included/excluded components; parity docs close the evidence link in Plan 08. |
| T-130-04 | Denial of Service | Medium | Phase 130 enforcement seam | accept | Accounted enforcement is intentionally not active until Phase 131; legacy vsize trimming remains explicit and no stronger claim is allowed. |

## Security Verification
- The high-severity overflow and cache-drift threats are blocked by the two automated task commands.
- Source checks must find no saturating/wrapping accounting and no capacity comparison in the Phase 130 trim loop.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan01-mempool -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib`
2. `bun run scripts/check-parity-breadcrumbs.ts --check`
3. `git diff --check`
</verification>

<success_criteria>
- D-01 and D-02 are implemented with compile-time-distinct values, checked accounting, one cached ledger, and an independent oracle.
- D-03 is honored: accounted-capacity enforcement and pressure behavior remain absent and explicitly delegated to Phase 131.
- FEEP-01 has pure-domain evidence proving vsize, usage, and capacity can be unequal and cannot be accidentally interchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-01-SUMMARY.md`.
</output>
