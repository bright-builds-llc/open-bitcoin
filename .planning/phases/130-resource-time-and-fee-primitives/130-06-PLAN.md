---
phase: 130-resource-time-and-fee-primitives
plan: "06"
type: execute
wave: 6
depends_on:
  - 130-05
files_modified:
  - packages/open-bitcoin-node/src/network.rs
  - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  - packages/open-bitcoin-node/src/sync/block_reconcile.rs
  - packages/open-bitcoin-node/src/sync/tests.rs
autonomous: true
requirements:
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Block-connected mempool transitions receive explicit block lifecycle time and height."
    - "A genuine reorg reacceptance receives the sync event's explicit current time and stores reorg origin."
    - "Block and reorg cache cleanup consume semantic delta cause/role/final-membership facts without renaming them."
    - "No block/reorg policy reads wall-clock time or randomness."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/mempool_lifecycle.rs"
      provides: "Typed block/reorg context handoff and delta consumption"
      contains: "ReorgLifecycleContext"
    - path: "packages/open-bitcoin-node/src/sync/block_reconcile.rs"
      provides: "Forwarding of existing explicit sync timestamp into reorg lifecycle"
      contains: "ReorgLifecycleContext"
    - path: "packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs"
      provides: "Exact block/reorg metadata and cause/role integration tests"
      contains: "managed_reorg_reacceptance_uses_explicit_event_time"
  key_links:
    - from: "packages/open-bitcoin-node/src/sync/block_reconcile.rs"
      to: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      via: "Reconcile timestamp becomes ReorgLifecycleContext under the sole authority"
      pattern: "reorg_to_branch"
    - from: "packages/open-bitcoin-node/src/network/mempool_lifecycle.rs"
      to: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      via: "Block/reorg bridge consumes MempoolLifecycleDelta facts"
      pattern: "MempoolLifecycleDelta"
---

<objective>
Route explicit block and reorg facts through the authoritative lifecycle bridge.

Purpose: Complete D-10 through D-16 for current block/reorg behavior while leaving rolling-fee mechanics to Phase 131 and complete cross-cache projection to Phase 134.
Output: Explicit block/reorg contexts, correct reorg acceptance metadata, delta-driven current cleanup, and deterministic integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-05-SUMMARY.md
@.planning/phases/127-authoritative-network-state-unification/127-01-SUMMARY.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/kernel/disconnected_transactions.cpp
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
`connect_stored_block` already receives `timestamp: i64`; use that exact value in `BlockLifecycleContext`.
`reconcile_best_chain_inner` already receives `timestamp: i64`; forward it unchanged into `ReorgLifecycleContext`.
For direct/local block-connect test adapters that do not have an outer receive time, use the explicit block-header time to construct the context; never read the system clock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply block lifecycle through explicit context and delta</name>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
    - packages/open-bitcoin-node/src/network/runtime_authority.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/bitcoin-knots/src/txmempool.cpp
    - packages/bitcoin-knots/src/validation.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  </files>
  <behavior>
    - Stored block connection at timestamp 70 and height 2 passes exactly 70/2 to the pure block lifecycle transition.
    - Direct local block connection constructs context from the explicit block header time and connected height, with no clock call.
    - Confirmed removal consumes `BlockConfirmation/Direct`; conflict roots and descendants consume `BlockConflict/Direct|Descendant`.
    - Current mempool, txid/wtxid, serving, fanout, and compact-partial cleanup uses delta identities and final membership.
  </behavior>
  <action>
    Extend `apply_connected_block_mempool_lifecycle` to require `BlockLifecycleContext` and call the transition-returning pure API. `connect_stored_block` must construct it from the method's existing explicit timestamp and the connected `ChainPosition.height`. `connect_local_block` must use the block header's explicit timestamp and the connected height; do not add `SystemTime`.

    Apply current cleanup from delta removals. Map current serving status from the typed cause without converting descendants into a separate cause. Use final membership to avoid deleting a surviving/reaccepted identity. Preserve the existing short authority mutation; no storage/network I/O is introduced.

    Add assertions for exact context values and cause/role handling. Do not add rolling-floor block gating or decay; Phase 131 owns behavior behind this context.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-block-lifecycle-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node managed_block_connect -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BlockLifecycleContext" packages/open-bitcoin-node/src/network{.rs,/mempool_lifecycle.rs}` finds construction and consumption.
    - Block integration tests assert confirmation versus conflict causes and descendant roles.
    - `rg -n "SystemTime|UNIX_EPOCH|getrandom|RandomState" packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` returns no matches.
    - No rolling-fee state mutation is added.
  </acceptance_criteria>
  <done>Block lifecycle transitions receive explicit context and drive current cleanup from typed delta cause, role, and membership facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reaccept disconnected transactions with explicit reorg facts</name>
  <read_first>
    - packages/open-bitcoin-node/src/network.rs
    - packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
    - packages/open-bitcoin-node/src/network/runtime_authority.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-node/src/sync/block_reconcile.rs
    - packages/open-bitcoin-node/src/sync/tests.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/bitcoin-knots/src/kernel/disconnected_transactions.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network.rs
    packages/open-bitcoin-node/src/network/mempool_lifecycle.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    packages/open-bitcoin-node/src/sync/block_reconcile.rs
    packages/open-bitcoin-node/src/sync/tests.rs
  </files>
  <behavior>
    - Reorg at timestamp 80 stores `Known(80)`, `Reorg`, and `NotRequested` on each newly reaccepted disconnected transaction.
    - Replacement-branch block cleanup receives the same explicit reorg operation time plus each connected height.
    - Rejected/duplicate/orphaned reorg attempts return typed outcomes but do not fabricate local relay intent.
    - Current cache effects consume each transition delta.
  </behavior>
  <action>
    Change `ManagedNetworkHandle::reorg_to_branch`, `ManagedPeerNetwork::reorg_to_branch`, and `apply_reorg_mempool_lifecycle` to accept `ReorgLifecycleContext`. In `sync/block_reconcile.rs`, construct it from the existing `timestamp` argument and forward it unchanged. Use `AdmissionContext::reorg(context.occurred_at)` for each disconnected non-coinbase transaction; never use restart time, block time, zero, or local origin.

    Route every reacceptance through `MempoolTransition` and apply its delta through the current bridge. Keep ordered typed attempt outcomes for the existing reorg result. Add node and sync tests proving the exact event time reaches metadata and remains deterministic under repeated fixture runs.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-reorg-lifecycle-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node reorg -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ReorgLifecycleContext" packages/open-bitcoin-node/src/{network.rs,network/mempool_lifecycle.rs,network/runtime_authority.rs,sync/block_reconcile.rs}` finds end-to-end forwarding.
    - `rg -n "submit_transaction_outcome\\(" packages/open-bitcoin-node/src/network/mempool_lifecycle.rs` returns no direct old outcome-only call.
    - Reorg tests assert `Known(80)`, `Reorg`, and `NotRequested`.
    - No reorg path constructs `MempoolOrigin::Local` or `RelayIntent::Requested`.
  </acceptance_criteria>
  <done>Reorg reconciliation forwards the exact event time and preserves reorg/non-requested metadata through delta-driven transitions.</done>
</task>

</tasks>

<threat_model>
## Assets
- Correct block-confirmation/conflict attribution.
- Reorg transaction age and origin.
- Current cache membership consistency.
- Deterministic replay inputs.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Validated block/replacement branch → mempool lifecycle | Block transactions determine removals and conflict descendants. |
| Sync shell timestamp → reorg policy | Event time controls future age-sensitive decisions. |
| Pure delta → current managed caches | Incorrect cause/role/final state could leave stale or remove live entries. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-21 | Tampering | High | block conflict/descendant cleanup | mitigate | Typed cause/role delta tests and current-cache absence assertions are blocking. |
| T-130-22 | Spoofing / Information Disclosure | High | reorg reacceptance origin | mitigate | Explicit `ReorgLifecycleContext`; tests reject local/requested metadata. |
| T-130-23 | Repudiation | High | reorg event time | mitigate | Existing sync timestamp is forwarded unchanged and asserted exactly. |
| T-130-24 | Tampering | Medium | complete cross-cache state | transfer | Phase 134 owns exhaustive projection; this plan migrates only existing consumers and states the boundary. |

## Security Verification
- Block/reorg high threats are blocked by exact cause, role, final-membership, time, and origin tests.
- Source scans ensure no hidden clock and no old outcome-only reacceptance path.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan06-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node mempool_lifecycle -- --nocapture`
2. `git diff --check`
</verification>

<success_criteria>
- Block/reorg policy receives explicit immutable inputs.
- Genuine reorg reacceptance has exact event time and reorg origin.
- Current lifecycle consumers preserve delta cause/role semantics.
- FEEP-03, FEEP-04, and block/reorg portions of FEEP-05 are integrated without implementing Phase 131 or Phase 134 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-06-SUMMARY.md`.
</output>
