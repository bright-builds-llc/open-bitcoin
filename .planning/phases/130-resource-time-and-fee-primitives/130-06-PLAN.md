---
phase: 130-resource-time-and-fee-primitives
plan: "06"
type: execute
wave: 6
depends_on:
  - 130-05
files_modified:
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
autonomous: true
requirements:
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T16:10:00Z
must_haves:
  truths:
    - "Every node-owned production and test caller supplies an explicit acceptance timestamp and relay intent to local admission."
    - "No node-owned caller injects timestamp zero or guesses local origin; the deprecated compatibility API remains only for the still-unmigrated `open-bitcoin-rpc` caller."
    - "All migrated admission, serving, compact-receive, and lifecycle fixtures compile against the explicit authority contract and retain deterministic metadata."
    - "The whole Cargo workspace compiles at this commit boundary while the RPC caller still uses the fail-closed compatibility method."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Explicit-time node admission plus the temporary fail-closed RPC compatibility adapter"
      contains: "submit_local_transaction_outcome_at"
    - path: "packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs"
      provides: "Deterministic explicit-time local relay fixtures"
      contains: "submit_local_transaction_outcome_at"
  key_links:
    - from: "packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs"
      to: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      via: "Local submission fixtures pass exact timestamps and relay intent through the sole authority"
      pattern: "submit_local_transaction_outcome_at"
    - from: "packages/open-bitcoin-node/src/network/action_translation.rs"
      to: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      via: "Translated peer/local actions use typed admission results without outcome reclassification"
      pattern: "ManagedAdmissionBridgeResult|MempoolTransition"
---

<objective>
Migrate every node-owned managed caller to the explicit-time admission authority while retaining the compatibility adapters required by the unmigrated RPC crate.

Purpose: Complete the node-owned caller/test half of D-09 through D-13 after Plan 130-05 lands an independently compilable production contract, ensuring no node production or fixture path retains old zero-time behavior without breaking `open-bitcoin-rpc` between commits. Plan 130-11 owns the final RPC migration and compatibility-method removal.
Output: Explicit-time node production/test callers, intentionally retained fail-closed RPC compatibility adapters, deterministic caller coverage, and a workspace-compilable commit.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-05-SUMMARY.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 130-05 provides `submit_local_transaction_outcome_at(transaction, ..., now_unix_seconds, relay_intent)` through `ManagedNetworkHandle` plus deprecated fail-closed adapters solely for independent compilation.

This plan must migrate every caller under `packages/open-bitcoin-node/src` except the two compatibility method definitions in `network/admission_bridge.rs` and `network/runtime_authority.rs`. It MUST NOT delete those definitions: `packages/open-bitcoin-rpc/src/context/network.rs` continues to call `submit_local_transaction_outcome` until Plan 130-11. Test timestamps are deterministic fixture facts. Production timestamps must come from an already-explicit event input; the RPC shell samples live time in Plan 130-11.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate node callers and preserve RPC compatibility</name>
  <read_first>
    - packages/open-bitcoin-node/src/network/admission_bridge.rs
    - packages/open-bitcoin-node/src/network/action_translation.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
    - packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
    - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network/admission_bridge.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
    packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
    packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  </files>
  <behavior>
    - Local relay-requested fixtures pass exact nonzero timestamps and `RelayIntent::Requested`; relay-disabled fixtures pass `NotRequested`.
    - Peer and orphan-reconsideration fixtures continue to retain their exact receive/reconsideration timestamp and peer origin.
    - Admission, serving, compact-receive, and lifecycle fixtures compile and preserve their existing outcome assertions after the API migration.
    - No node source file calls `submit_local_transaction_outcome` without the `_at` suffix outside the two compatibility method definitions.
    - `packages/open-bitcoin-rpc/src/context/network.rs` still calls the compatibility method, and `cargo check --workspace` succeeds at this commit boundary.
  </behavior>
  <action>
    Update every node-owned production and test caller identified by the source scan to call `submit_local_transaction_outcome_at` with an explicit deterministic timestamp and the correct typed relay intent. Use existing event timestamps in production action translation. Use stable fixture timestamps in tests and assert stored metadata where the fixture observes the accepted entry. Do not use zero, restart time, block time, or inferred local origin as a substitute for a missing live-admission timestamp (D-09/D-10/D-12).

    Retain the deprecated no-time compatibility methods in `ManagedNetworkHandle` and the admission bridge unchanged and fail closed, because the RPC crate still calls them at this commit boundary. Do not edit `runtime_authority.rs` in this plan. Keep detailed outcomes available to direct authenticated consumers, do not reintroduce `outcome.replaced()`/`outcome.evicted()` cache classification, and preserve every existing serving, compact-receive, local-relay, and lifecycle assertion while changing only the node caller seam. Plan 130-11 must migrate the RPC caller first and remove both compatibility definitions in the same commit.

    Per ASVS-130-V1/V4/V8/V11/V13, run a workspace-wide caller inventory for both old and `_at` authority names after migration, covering node/RPC libraries, binaries, benchmarks, integration tests, and doctests. Every node-owned caller must migrate in this task. The sole permitted old caller is `open-bitcoin-rpc/src/context/network.rs`, backed by the deprecated fail-closed methods whose rustdoc names Plan 130-11 as final migration/removal owner. Do not commit unless the exact all-target workspace check proves that intermediate compatibility boundary.
  </action>
  <verify>
    <automated>test -z "$(rg -n 'submit_local_transaction_outcome\\(' packages/open-bitcoin-node/src --glob '*.rs' --glob '!network/admission_bridge.rs' --glob '!network/runtime_authority.rs')" &amp;&amp; rg -q 'submit_local_transaction_outcome\\(' packages/open-bitcoin-rpc/src/context/network.rs &amp;&amp; test -z "$(rg -n 'submit_local_transaction_outcome_at\\([^\\n]*, 0\\)|AdmissionContext::.*\\(.*0' packages/open-bitcoin-node/src --glob '*.rs')" &amp;&amp; bun run scripts/command-timings.ts run --key phase130-node-admission-callers -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge -- --nocapture &amp;&amp; bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "submit_local_transaction_outcome\\(" packages/open-bitcoin-node/src --glob '*.rs' --glob '!network/admission_bridge.rs' --glob '!network/runtime_authority.rs'` returns no node-owned call; the only node matches are the two retained compatibility definitions.
    - `packages/open-bitcoin-rpc/src/context/network.rs` still contains its compatibility call, proving removal has been correctly deferred to Plan 130-11.
    - `rg -n "submit_local_transaction_outcome_at\\([^\\n]*, 0\\)|AdmissionContext::.*\\(.*0" packages/open-bitcoin-node/src --glob '*.rs'` returns no zero-time injection.
    - All affected test modules compile and focused admission tests pass with exact peer/local/reconsidered metadata and duplicate immutability; the plan cannot commit unless `bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets` proves the untouched RPC caller, binaries, benchmarks, integration tests, and doctests still compile.
    - `rg -n "outcome\\.replaced\\(\\)|outcome\\.evicted\\(\\)" packages/open-bitcoin-node/src/network/{admission_bridge.rs,action_translation.rs}` returns no matches.
  </acceptance_criteria>
  <done>Every node-owned caller uses explicit time and relay intent, the fail-closed RPC compatibility adapters remain until Plan 130-11, and the full workspace compiles.</done>
</task>

</tasks>

<threat_model>
## Assets
- Accurate local acceptance time.
- Trusted local-versus-peer origin and relay intent.
- Node caller compatibility and cache consistency.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Production event/RPC shell → managed authority | External transactions acquire trusted time, origin, and relay-intent facts. |
| Test fixtures → authority API | Deterministic inputs must exercise the same typed contract without sentinel zero. |
| Lifecycle delta → caller-owned caches | Caller migration must not restore outcome-based reclassification. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-41 | Spoofing / Repudiation | High | local admission callers | mitigate | Source scans reject node-owned old calls and zero-time injection; retained adapters stay fail closed until Plan 130-11 migrates RPC and removes them. |
| T-130-42 | Tampering | High | migrated cache callers | mitigate | Preserve delta-driven behavior and reject outcome-vector reclassification in source scans. |
| T-130-43 | Denial of Service | Medium | broad caller migration | mitigate | Keep the production contract separately compilable in Plan 130-05, then require the complete affected node test corpus to compile here. |

## Security Verification
- ASVS matrix ownership: ASVS-130-V1, ASVS-130-V4, ASVS-130-V8, ASVS-130-V11, and ASVS-130-V13.
- Node-owned old calls and zero sentinels are blocked by exact negative source assertions.
- Focused integration tests and a workspace check prove the explicit node metadata contract while the RPC compatibility boundary remains compilable.
</threat_model>

<verification>
Run serially:
1. `test -z "$(rg -n 'submit_local_transaction_outcome\\(' packages/open-bitcoin-node/src --glob '*.rs' --glob '!network/admission_bridge.rs' --glob '!network/runtime_authority.rs')"`
2. `rg -q 'submit_local_transaction_outcome\\(' packages/open-bitcoin-rpc/src/context/network.rs`
3. `test -z "$(rg -n 'submit_local_transaction_outcome_at\\([^\\n]*, 0\\)|AdmissionContext::.*\\(.*0' packages/open-bitcoin-node/src --glob '*.rs')"`
4. `bun run scripts/command-timings.ts run --key phase130-plan06-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge -- --nocapture`
5. `bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets`
6. `git diff --check`
</verification>

<success_criteria>
- Node-owned production/test callers have no implicit or zero-time local submission path.
- The compatibility API remains fail closed only for the pending RPC caller, and the whole workspace compiles at this boundary.
- FEEP-03, FEEP-04, and the admission portion of FEEP-05 are integrated without broadening Phase 134/136 scope.
- The task cannot commit unless the exact timed all-target workspace check exits 0 with the sole old RPC caller protected by the Plan-130-11-owned fail-closed shim.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-06-SUMMARY.md`.
</output>
