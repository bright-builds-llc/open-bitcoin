---
phase: 130-resource-time-and-fee-primitives
plan: "02"
type: execute
wave: 2
depends_on:
  - 130-01
files_modified:
  - packages/open-bitcoin-mempool/src/fee.rs
  - packages/open-bitcoin-mempool/src/lib.rs
  - packages/open-bitcoin-mempool/src/types.rs
  - packages/open-bitcoin-mempool/src/pool.rs
  - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
  - packages/open-bitcoin-mempool/src/pool/tests.rs
  - packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FEEP-02
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Static relay, incremental relay, rolling mempool, and effective admission fee rates are different Rust types."
    - "Effective individual admission is derived as max(static relay, rolling mempool) and is never independent mutable state."
    - "Incremental relay fee remains a replacement/pressure-bump input and cannot become an ordinary admission threshold."
    - "A future eligible package aggregate may satisfy the rolling floor, but every ordinary member still has an independently testable static-floor obligation."
  artifacts:
    - path: "packages/open-bitcoin-mempool/src/fee.rs"
      provides: "Fee arithmetic representation, semantic role wrappers, derivation, and package-floor contract"
      contains: "pub struct EffectiveAdmissionFeeRate"
    - path: "packages/open-bitcoin-mempool/src/pool.rs"
      provides: "Ordinary admission and replacement wired to role-correct fee inputs"
      contains: "effective_admission_fee_rate"
    - path: "packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs"
      provides: "Boundary tests for static, rolling, incremental, and package-floor roles"
      contains: "incremental_relay_fee_is_not_an_admission_floor"
  key_links:
    - from: "packages/open-bitcoin-mempool/src/pool.rs"
      to: "packages/open-bitcoin-mempool/src/fee.rs"
      via: "Ordinary admission derives the effective floor from static and rolling wrappers"
      pattern: "effective_admission_fee_rate"
    - from: "packages/open-bitcoin-mempool/src/pool.rs"
      to: "packages/open-bitcoin-mempool/src/types.rs"
      via: "Replacement consumes IncrementalRelayFeeRate from PolicyConfig"
      pattern: "incremental_relay_fee_rate"
---

<objective>
Make every fee-floor role explicit and impossible to interchange accidentally.

Purpose: Implement D-05 through D-08 before rolling policy and package admission exist, so Phase 131 and Phase 132 consume one role-correct contract instead of inventing temporary thresholds.
Output: Semantic fee-rate wrappers, pure effective-floor derivation, explicit package member/aggregate obligations, migrated ordinary admission/replacement call sites, and focused boundary tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-01-SUMMARY.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/rpc/mempool.cpp
@packages/bitcoin-knots/doc/policy/packages.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 130-01 provides typed `TransactionVirtualSize`; fee arithmetic must accept it explicitly rather than raw `usize`.

Required public contracts:
```rust
pub struct StaticRelayFeeRate(FeeRate);
pub struct IncrementalRelayFeeRate(FeeRate);
pub struct RollingMempoolFeeRate(FeeRate);
pub struct EffectiveAdmissionFeeRate(FeeRate);

pub fn effective_admission_fee_rate(
    static_floor: StaticRelayFeeRate,
    rolling_floor: RollingMempoolFeeRate,
) -> EffectiveAdmissionFeeRate;
```

The exact package API name is discretionary, but it must expose separate member-static and aggregate-rolling checks and must not encode a broad TRUC exception. Phase 132 owns explicitly pinned package exceptions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define semantic fee roles and floor derivation</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/lib.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-mempool/src/pool/tests.rs
    - docs/parity/source-breadcrumbs.json
    - packages/bitcoin-knots/src/validation.cpp
    - packages/bitcoin-knots/doc/policy/packages.md
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/fee.rs
    packages/open-bitcoin-mempool/src/lib.rs
    packages/open-bitcoin-mempool/src/types.rs
    packages/open-bitcoin-mempool/src/pool/tests.rs
    packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - Rolling below static, equal to static, and above static derive static, static, and rolling respectively.
    - Zero rolling baseline derives the configured static relay floor.
    - An incremental rate greater than both static and rolling does not affect effective admission.
    - Package member evaluation fails when a member is below static even if aggregate feerate exceeds rolling; aggregate evaluation may satisfy rolling independently.
  </behavior>
  <action>
    Write tests first, then move or retain `FeeRate` as the arithmetic representation while adding transparent semantic wrappers `StaticRelayFeeRate`, `IncrementalRelayFeeRate`, `RollingMempoolFeeRate`, and `EffectiveAdmissionFeeRate` (D-05). Give wrappers explicit constructors/accessors; do not implement cross-role `From` conversions.

    Implement `effective_admission_fee_rate(static, rolling)` as a pure max and never expose a constructor that accepts incremental fee (D-06/D-08). Add a focused package-floor contract that evaluates an ordinary member against `StaticRelayFeeRate` and an eligible aggregate against `RollingMempoolFeeRate` as two separate decisions (D-07). Do not add package parsing/admission, package RBF, TRUC, ephemeral dust, or an exception boolean; Phase 132 must add any pinned exception as an explicit typed policy branch.

    Change `PolicyConfig` fields to `static_relay_fee_rate` and `incremental_relay_fee_rate`. Add `RollingMempoolFeeRate::ZERO` as the Phase-130 baseline contract; do not implement bump or decay. Register `fee.rs` in the `mempool-policy` breadcrumb group.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-fee-types -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool fee_cases -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct (StaticRelayFeeRate|IncrementalRelayFeeRate|RollingMempoolFeeRate|EffectiveAdmissionFeeRate)|pub fn effective_admission_fee_rate" packages/open-bitcoin-mempool/src/fee.rs` finds every role and the derivation.
    - `rg -n "impl From<.*FeeRate.*> for (StaticRelayFeeRate|IncrementalRelayFeeRate|RollingMempoolFeeRate|EffectiveAdmissionFeeRate)" packages/open-bitcoin-mempool/src/fee.rs` returns no matches.
    - Focused tests pass for rolling below/equal/above static, zero rolling, incremental exclusion, and the separate package member/aggregate obligations.
    - `bun run scripts/check-parity-breadcrumbs.ts --check` accepts `fee.rs`.
  </acceptance_criteria>
  <done>All semantic fee roles, derivation, package-floor contracts, tests, exports, and breadcrumbs satisfy the specified checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply fee roles to admission, replacement, and pressure evidence</name>
  <read_first>
    - packages/open-bitcoin-mempool/src/fee.rs
    - packages/open-bitcoin-mempool/src/resource.rs
    - packages/open-bitcoin-mempool/src/types.rs
    - packages/open-bitcoin-mempool/src/pool.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs
    - packages/bitcoin-knots/src/validation.cpp
    - packages/bitcoin-knots/src/rpc/mempool.cpp
  </read_first>
  <files>
    packages/open-bitcoin-mempool/src/types.rs
    packages/open-bitcoin-mempool/src/pool.rs
    packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    packages/open-bitcoin-mempool/src/pool/tests/fee_cases.rs
  </files>
  <behavior>
    - Ordinary admission rejects below max(static, rolling) and accepts at that exact effective floor.
    - Replacement bump calculation reads only `IncrementalRelayFeeRate`.
    - The pressure summary exposes static, incremental, raw rolling, and derived effective rates as separate typed values.
    - No mutable `effective_*` field exists in `Mempool`, `PolicyConfig`, or `MempoolPressureSummary`.
  </behavior>
  <action>
    Add `rolling_mempool_fee_rate: RollingMempoolFeeRate` to `Mempool`, initialized to zero and exposed read-only for Phase 130. Change ordinary admission to derive and enforce `EffectiveAdmissionFeeRate` from the configured static floor and current rolling floor. Keep replacement fee bump calculation on `IncrementalRelayFeeRate` only. Update `FeeRate` methods to consume `TransactionVirtualSize` explicitly.

    Replace raw fee integers in `MempoolPressureSummary` with `static_relay_fee_rate`, `incremental_relay_fee_rate`, `rolling_mempool_fee_rate`, and a derived `effective_admission_fee_rate` computed when the summary is built. Do not store effective fee in canonical mutable state. Keep `RollingFeeParityStatus::Deferred` until Phase 131 implements behavior.

    Update all in-crate tests and constructors to use the semantic wrappers. Preserve exact rejection-category behavior and the existing legacy trimming boundary.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-fee-policy -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool fee -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "min_relay_feerate|incremental_relay_feerate" packages/open-bitcoin-mempool/src` returns no production field-name matches.
    - `rg -n "effective_admission_fee_rate" packages/open-bitcoin-mempool/src/{types.rs,pool.rs,pool/lifecycle.rs}` shows derivation/use but no `pub effective_admission_fee_rate:` field in `PolicyConfig` or `Mempool`.
    - The focused command proves ordinary admission uses max(static, rolling) and replacement uses incremental only.
    - Existing replacement and relay-fee rejection tests remain green.
  </acceptance_criteria>
  <done>Admission, replacement, and pressure evidence consume role-correct fee types with no mutable effective-floor state.</done>
</task>

</tasks>

<threat_model>
## Assets
- Static anti-free-relay boundary.
- Rolling pressure boundary consumed by future package admission.
- Replacement fee-bump integrity.
- Stable, truthful operator fee vocabulary.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Transaction/package fee facts → mempool policy | Attacker-selected fees and virtual sizes are compared with policy roles. |
| Configuration → pure policy | Static and incremental settings must not be swapped. |
| Pure fee state → later RPC/package adapters | A mislabeled role could bypass static relay policy or over-reject CPFP. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-05 | Tampering / Denial of Service | High | ordinary/package fee checks | mitigate | Incompatible wrapper types plus member-static/aggregate-rolling tests; Task 1 and Task 2 focused suites are blocking. |
| T-130-06 | Tampering | High | effective floor derivation | mitigate | Pure two-argument max excludes incremental fee; source and boundary tests must pass. |
| T-130-07 | Repudiation | Medium | operator fee labels | mitigate | Pressure summary carries all four role names with enum/newtype-derived values; RPC mapping is completed in Plan 08. |
| T-130-08 | Elevation of Privilege | Medium | package exceptions | transfer | Phase 132 owns explicitly pinned TRUC/package exceptions; this phase deliberately exposes no generic bypass switch. |

## Security Verification
- High-severity fee-bypass threats are blocked on exact below/equal/above and package member/aggregate tests.
- Source checks reject an incremental argument in effective-floor derivation and reject mutable effective state.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan02-mempool -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-mempool --lib`
2. `bun run scripts/check-parity-breadcrumbs.ts --check`
3. `git diff --check`
</verification>

<success_criteria>
- D-05 through D-08 are represented in type signatures and executable tests.
- FEEP-02 cannot regress through accidental role interchange, incremental-floor admission, or package aggregate bypass of the static member floor.
- Rolling mechanics and package exceptions remain delegated intact to Phases 131 and 132.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-02-SUMMARY.md`.
</output>
