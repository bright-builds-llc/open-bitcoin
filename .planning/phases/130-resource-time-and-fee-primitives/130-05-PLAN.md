---
phase: 130-resource-time-and-fee-primitives
plan: "05"
type: execute
wave: 5
depends_on:
  - 130-04
files_modified:
  - packages/open-bitcoin-node/src/mempool.rs
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/action_translation.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-node/src/network/tests.rs
  - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
  - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
  - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
  - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements:
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:20:23Z
must_haves:
  truths:
    - "Peer admission uses the receive timestamp and peer origin; local RPC admission samples time in the shell and records explicit relay intent."
    - "No production local-admission path injects timestamp zero or guesses origin."
    - "Current admission-side cache consequences consume `MempoolLifecycleDelta` rather than reclassifying `MempoolOutcome` replacement/eviction vectors."
    - "Authenticated direct responses retain detailed outcomes while shared state receives only semantic fixed facts."
  artifacts:
    - path: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      provides: "Typed peer/local admission contexts and delta consumption"
      contains: "apply_admitted_transition"
    - path: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      provides: "Explicit-time local admission through the sole network authority"
      contains: "submit_local_transaction_outcome_at"
    - path: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      provides: "Effectful current-time sampling at the local RPC shell"
      contains: "current_timestamp_unix_seconds"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      to: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      via: "sendrawtransaction samples Unix seconds then invokes explicit-time authority admission"
      pattern: "submit_local_transaction_with_relay_evidence_at"
    - from: "packages/open-bitcoin-node/src/network/admission_bridge.rs"
      to: "packages/open-bitcoin-mempool/src/pool/lifecycle.rs"
      via: "Node bridge applies typed delta removals and admissions without outcome-vector reclassification"
      pattern: "MempoolLifecycleDelta|MempoolTransition"
---

<objective>
Carry canonical metadata and lifecycle deltas through live peer and local admission shells.

Purpose: Complete the live portions of D-09 through D-13 while preserving the one `ManagedNetworkHandle` authority and existing authenticated response behavior.
Output: Explicit-time peer/local admission, shell-only clock sampling, semantic delta consumption for current caches, and deterministic integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-04-SUMMARY.md
@.planning/phases/127-authoritative-network-state-unification/127-01-SUMMARY.md
@packages/bitcoin-knots/src/validation.cpp
@packages/bitcoin-knots/src/net_processing.cpp
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 130-04 provides `MempoolTransition { outcome, delta }`. The node may return `outcome` to authenticated RPC callers, but all current cache consequences must consume `delta`.

Metadata mapping:
- peer transaction/reconsidered orphan: `Known(receive_or_reconsideration_time)`, `Peer`, `NotRequested`;
- local `sendrawtransaction`: `Known(shell_sampled_time)`, `Local`, relay intent derived from the existing resolved relay request/activation input;
- duplicate attempts: no metadata mutation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate managed admission to typed contexts and deltas</name>
  <read_first>
    - packages/open-bitcoin-node/src/mempool.rs
    - packages/open-bitcoin-node/src/network/admission_bridge.rs
    - packages/open-bitcoin-node/src/network/action_translation.rs
    - packages/open-bitcoin-node/src/network/runtime_authority.rs
    - packages/open-bitcoin-node/src/network/tests.rs
    - packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
    - packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
    - packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    - packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
    - packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
    - packages/open-bitcoin-mempool/src/context.rs
    - packages/open-bitcoin-mempool/src/pool/lifecycle.rs
    - packages/bitcoin-knots/src/net_processing.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/mempool.rs
    packages/open-bitcoin-node/src/network/admission_bridge.rs
    packages/open-bitcoin-node/src/network/action_translation.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-node/src/network/tests.rs
    packages/open-bitcoin-node/src/network/tests/admission_bridge_cases.rs
    packages/open-bitcoin-node/src/network/tests/relay_local_submission_cases.rs
    packages/open-bitcoin-node/src/network/tests/relay_serving_cases.rs
    packages/open-bitcoin-node/src/network/tests/compact_receive_cases.rs
    packages/open-bitcoin-node/src/network/tests/mempool_lifecycle_cases.rs
  </files>
  <behavior>
    - Peer admission at timestamp 42 stores known time 42, `Peer`, and `NotRequested`.
    - Reconsidered orphan accepted at timestamp 43 stores 43 and preserves peer origin.
    - Local relay-requested admission at timestamp 50 stores 50, `Local`, and `Requested`; relay-disabled/not-requested local admission stores `NotRequested`.
    - Duplicate local or peer submission does not change the first accepted metadata.
    - Replacement and legacy trim cleanup use delta cause/role/final-membership facts, not `outcome.replaced()` or `outcome.evicted()`.
  </behavior>
  <action>
    Write/extend the node tests first. Change `ManagedMempool` to expose transition-returning context-aware methods. In `process_peer_transaction_admission` and orphan reconsideration, create `AdmissionContext::peer(PolicyTime::from_unix_seconds(timestamp))`; never use peer id as shared metadata and never infer local origin.

    Replace `submit_local_transaction_outcome` with an explicit `submit_local_transaction_outcome_at(transaction, ..., now_unix_seconds, relay_intent)` path through `ManagedNetworkHandle`. Remove the zero-time convenience implementation and update every node production/test caller to pass a deterministic explicit timestamp. If the existing relay activation/request decision is false, use `RelayIntent::NotRequested` rather than fabricating requested intent.

    Change `ManagedAdmissionBridgeResult` and bridge helpers so current admitted/removal consequences are driven by `MempoolTransition.delta`. Replace `apply_admitted_outcome` with `apply_admitted_transition`; derive current `TxServingRecordStatus` from typed removal causes, use delta identities for compact-extra and peer cleanup, and honor final membership before storing the admitted body. Do not implement Phase 134's complete serving/fanout/peer/orphan/compact/retry/persistence/evidence projection; only migrate the caches this bridge already owns, without reclassification.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-node-admission-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Node tests assert exact peer/local/reconsidered metadata and duplicate immutability.
    - `rg -n "submit_local_transaction_outcome\\(" packages/open-bitcoin-node/src --glob '*.rs'` returns no old no-time production method/call.
    - `rg -n "submit_local_transaction_outcome_at\\([^\\n]*, 0\\)|AdmissionContext::.*\\(.*0" packages/open-bitcoin-node/src --glob '*.rs'` returns no production zero-time injection.
    - `rg -n "outcome\\.replaced\\(\\)|outcome\\.evicted\\(\\)" packages/open-bitcoin-node/src/network/admission_bridge.rs` returns no matches.
  </acceptance_criteria>
  <done>Managed peer/local admission preserves exact metadata and applies current consequences only from semantic transition deltas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sample local acceptance time in the RPC shell</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/context/network.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-rpc/src/context/resource_governance.rs
    - packages/open-bitcoin-node/src/network/runtime_authority.rs
    - packages/open-bitcoin-node/src/network/admission_bridge.rs
    - packages/bitcoin-knots/src/node/transaction.cpp
  </read_first>
  <files>
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
  </files>
  <behavior>
    - A deterministic `_at` RPC-context test injects timestamp 60 and the accepted entry stores 60/local/requested.
    - `sendrawtransaction` samples current time only in the RPC adapter and passes it through the authority.
    - Clock-before-epoch failure returns a typed shell error or a fail-closed non-local/non-requested result; it never silently stores zero as a known local acceptance time.
  </behavior>
  <action>
    Add `ManagedRpcContext::submit_local_transaction_with_relay_evidence_at(transaction, now_unix_seconds)` for deterministic tests and internal use. In `dispatch/node.rs`, sample `SystemTime::now()` at the shell boundary, convert to `i64` with checked conversion, and pass the result to the `_at` method. Map clock acquisition/conversion failure to existing stable internal RPC failure vocabulary; do not use `unwrap_or(0)`.

    Keep detailed txid/replacement/eviction data confined to the direct authenticated `sendrawtransaction` response. Add an integration assertion that the authoritative mempool entry contains the sampled metadata without exposing it in shared status. Update dispatch tests to use explicit times where they bypass the public shell.
  </action>
  <verify>
    <automated>bun run scripts/command-timings.ts run --key phase130-rpc-admission-time -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc sendrawtransaction -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "submit_local_transaction_with_relay_evidence_at|current_timestamp_unix_seconds" packages/open-bitcoin-rpc/src/{context/network.rs,dispatch/node.rs}` finds the deterministic seam and shell sampler.
    - `rg -n "unwrap_or\\(0\\)|submit_local_transaction_outcome\\(" packages/open-bitcoin-rpc/src/{context/network.rs,dispatch/node.rs}` returns no matches.
    - Focused RPC tests pass and prove local metadata while shared serialized status omits txid/wtxid and metadata details.
  </acceptance_criteria>
  <done>The RPC shell samples time safely, forwards it explicitly, and preserves the direct-response/shared-evidence boundary.</done>
</task>

</tasks>

<threat_model>
## Assets
- Trusted local-versus-peer origin classification.
- Accurate acceptance time.
- Relay-request privacy state.
- Current node cache consistency.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Peer receive path → admission context | Peer-controlled transaction receives trusted peer classification and shell timestamp. |
| Authenticated RPC → local admission | Caller transaction acquires local origin and relay intent. |
| Pure lifecycle delta → managed caches | Semantic facts trigger removal/storage consequences. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-17 | Spoofing / Information Disclosure | High | local origin/relay intent | mitigate | Only the local RPC shell constructs local metadata; exact peer/local tests and no-zero source scans are blocking. |
| T-130-18 | Tampering / Repudiation | High | acceptance time | mitigate | Checked shell sampling and explicit-time authority APIs; clock failure cannot become known time zero. |
| T-130-19 | Tampering | High | admission cache cleanup | mitigate | Current bridge consumes typed delta facts; source scan rejects outcome-vector reclassification. |
| T-130-20 | Information Disclosure | Medium | direct versus shared evidence | mitigate | Detailed identities remain in authenticated direct response; shared status serialization regression excludes them. |

## Security Verification
- All high-severity threats have blocking node/RPC tests plus exact source scans.
- Full cross-cache completeness is not claimed; Phase 134 remains the owner.
</threat_model>

<verification>
Run serially:
1. `bun run scripts/command-timings.ts run --key phase130-plan05-node -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-node admission_bridge -- --nocapture`
2. `bun run scripts/command-timings.ts run --key phase130-plan05-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc sendrawtransaction -- --nocapture`
3. `git diff --check`
</verification>

<success_criteria>
- Live peer and local admissions preserve exact canonical metadata.
- Production local submission has no implicit/zero-time path.
- Current admission consequences consume `MempoolLifecycleDelta`.
- FEEP-03, FEEP-04, and the admission portion of FEEP-05 are integrated without broadening Phase 134/136 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-05-SUMMARY.md`.
</output>
