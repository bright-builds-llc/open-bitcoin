---
phase: 130-resource-time-and-fee-primitives
plan: "11"
type: execute
wave: 8
depends_on:
  - 130-07
files_modified:
  - packages/open-bitcoin-node/src/network/admission_bridge.rs
  - packages/open-bitcoin-node/src/network/runtime_authority.rs
  - packages/open-bitcoin-rpc/src/context/network.rs
  - packages/open-bitcoin-rpc/src/dispatch/node.rs
  - packages/open-bitcoin-rpc/src/dispatch/tests.rs
autonomous: true
requirements:
  - FEEP-03
  - FEEP-04
  - FEEP-05
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:44:00Z
must_haves:
  truths:
    - "Local RPC admission samples current time only in the RPC shell and passes it explicitly through the managed authority."
    - "Clock acquisition or conversion failure never becomes known local acceptance time zero."
    - "The RPC caller migrates to the explicit-time method before the no-time compatibility methods are removed, so the workspace compiles at the commit boundary."
    - "Authenticated direct responses retain detailed outcomes while shared status omits transaction metadata and identities."
  artifacts:
    - path: "packages/open-bitcoin-rpc/src/context/network.rs"
      provides: "Deterministic explicit-time local submission seam"
      contains: "submit_local_transaction_with_relay_evidence_at"
    - path: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      provides: "Effectful current-time sampling at the local RPC shell"
      contains: "current_timestamp_unix_seconds"
    - path: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      provides: "Final authority surface with the no-time local admission method removed"
      contains: "submit_local_transaction_outcome_at"
  key_links:
    - from: "packages/open-bitcoin-rpc/src/dispatch/node.rs"
      to: "packages/open-bitcoin-rpc/src/context/network.rs"
      via: "sendrawtransaction samples checked Unix seconds then calls the deterministic `_at` seam"
      pattern: "submit_local_transaction_with_relay_evidence_at"
    - from: "packages/open-bitcoin-rpc/src/context/network.rs"
      to: "packages/open-bitcoin-node/src/network/runtime_authority.rs"
      via: "RPC context forwards exact time and relay intent through the sole managed authority"
      pattern: "submit_local_transaction_outcome_at"
---

<objective>
Complete local RPC admission timing and privacy wiring at the imperative-shell boundary.

Purpose: Finish the RPC portion of D-09 through D-13 after Plan 130-06 migrates node-owned callers and Plan 130-07 completes its shared `runtime_authority.rs` lifecycle work. Migrate the final RPC compatibility caller before removing the no-time methods in the same commit.
Output: Checked shell time sampling, deterministic `_at` submission, removed no-time compatibility methods, typed error handling, focused RPC integration tests, and a workspace-compilable final authority API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-07-SUMMARY.md
@packages/bitcoin-knots/src/node/transaction.cpp
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 130-06 migrates all node-owned callers but intentionally leaves the deprecated no-time methods because `packages/open-bitcoin-rpc/src/context/network.rs` still calls one. Plan 130-07 then completes block/reorg changes in the shared `runtime_authority.rs`. This plan owns both authority files, migrates that final RPC caller to `submit_local_transaction_outcome_at(..., now_unix_seconds, relay_intent)`, and removes the no-time methods only after the caller is updated. This plan is the only place where local RPC admission acquires wall-clock time.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sample local acceptance time in the RPC shell</name>
  <read_first>
    - packages/open-bitcoin-rpc/src/context/network.rs
    - packages/open-bitcoin-rpc/src/dispatch/node.rs
    - packages/open-bitcoin-rpc/src/dispatch/tests.rs
    - packages/open-bitcoin-rpc/src/context/resource_governance.rs
    - packages/open-bitcoin-node/src/network/runtime_authority.rs
    - packages/open-bitcoin-node/src/network/admission_bridge.rs
    - packages/bitcoin-knots/src/node/transaction.cpp
  </read_first>
  <files>
    packages/open-bitcoin-node/src/network/admission_bridge.rs
    packages/open-bitcoin-node/src/network/runtime_authority.rs
    packages/open-bitcoin-rpc/src/context/network.rs
    packages/open-bitcoin-rpc/src/dispatch/node.rs
    packages/open-bitcoin-rpc/src/dispatch/tests.rs
  </files>
  <behavior>
    - A deterministic `_at` RPC-context test injects timestamp 60 and the accepted entry stores 60/local/requested.
    - `sendrawtransaction` samples current time only in the RPC adapter and passes it through the authority.
    - Clock-before-epoch failure returns a typed shell error; it never silently stores zero as a known local acceptance time.
    - After the RPC caller is migrated, no production source defines or calls `submit_local_transaction_outcome` without the `_at` suffix.
  </behavior>
  <action>
    Add `ManagedRpcContext::submit_local_transaction_with_relay_evidence_at(transaction, now_unix_seconds)` for deterministic tests and internal use. Make it call `ManagedNetworkHandle::submit_local_transaction_outcome_at` with the exact timestamp and resolved typed relay intent. In `dispatch/node.rs`, sample `SystemTime::now()` at the shell boundary, convert to `i64` with checked conversion, and pass the result to the `_at` method. Map clock acquisition/conversion failure to existing stable internal RPC failure vocabulary; do not use `unwrap_or(0)`.

    Once `packages/open-bitcoin-rpc/src/context/network.rs` no longer calls the no-time method, remove `submit_local_transaction_outcome` from both `packages/open-bitcoin-node/src/network/runtime_authority.rs` and `packages/open-bitcoin-node/src/network/admission_bridge.rs`. Do the caller migration and removals in this task/commit; never create an intermediate commit that deletes the method while RPC still calls it.

    Keep detailed txid/replacement/eviction data confined to the direct authenticated `sendrawtransaction` response. Add an integration assertion that the authoritative mempool entry contains the sampled metadata without exposing it in shared status. Update dispatch tests to use explicit times where they bypass the public shell, then run a workspace check to prove the final API compiles across node and RPC crates.
  </action>
  <verify>
    <automated>test "$(bun run scripts/command-timings.ts run --key phase130-rpc-time-list -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib -- --list | rg -c 'sendrawtransaction.*: test$')" -ge 2 &amp;&amp; bun run scripts/command-timings.ts run --key phase130-rpc-admission-time -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib sendrawtransaction -- --nocapture &amp;&amp; test -z "$(rg -n 'submit_local_transaction_outcome\\(' packages/open-bitcoin-node/src packages/open-bitcoin-rpc/src --glob '*.rs')" &amp;&amp; bun run scripts/command-timings.ts run --key phase130-plan11-workspace-check -- cargo check --manifest-path packages/Cargo.toml --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "submit_local_transaction_with_relay_evidence_at|current_timestamp_unix_seconds" packages/open-bitcoin-rpc/src/{context/network.rs,dispatch/node.rs}` finds the deterministic seam and shell sampler.
    - `rg -n "unwrap_or\\(0\\)|submit_local_transaction_outcome\\(" packages/open-bitcoin-rpc/src/{context/network.rs,dispatch/node.rs}` returns no matches.
    - `rg -n "submit_local_transaction_outcome\\(" packages/open-bitcoin-node/src packages/open-bitcoin-rpc/src --glob '*.rs'` returns no method definition or caller after the RPC migration.
    - The list command finds at least two named `sendrawtransaction` tests before the focused command executes them, preventing zero-match success.
    - Focused RPC tests prove exact local metadata and typed clock failure while shared serialized status omits txid/wtxid and metadata details; `cargo check --workspace` proves the final authority surface compiles.
  </acceptance_criteria>
  <done>The RPC shell samples time safely, forwards it explicitly, and preserves the direct-response/shared-evidence boundary.</done>
</task>

</tasks>

<threat_model>
## Assets
- Trusted local-origin classification.
- Accurate acceptance time.
- Relay-request privacy state.
- Authenticated direct-response boundary.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Authenticated RPC → local admission | Caller transaction acquires local origin and relay intent. |
| RPC shell clock → managed authority | Effectful wall-clock time becomes canonical entry metadata. |
| Detailed outcome → shared evidence | Transaction identities must remain confined to direct response. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-34 | Spoofing / Information Disclosure | High | local origin/relay intent | mitigate | Only the authenticated local RPC shell invokes the local explicit-time authority path; focused metadata tests block. |
| T-130-35 | Tampering / Repudiation | High | acceptance time and compatibility removal | mitigate | Checked shell sampling and conversion; migrate the final RPC caller before deleting both compatibility definitions; source scans reject zero fallback and old no-time calls; workspace check blocks the commit. |
| T-130-36 | Information Disclosure | Medium | direct versus shared evidence | mitigate | Integration serialization test excludes txid/wtxid and metadata from shared status. |

## Security Verification
- High-severity origin/time threats block on exact integration tests and source scans.
- Shared evidence remains aggregate and identity-free.
</threat_model>

<verification>
Run serially:
1. `test "$(bun run scripts/command-timings.ts run --key phase130-plan11-list -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib -- --list | rg -c 'sendrawtransaction.*: test$')" -ge 2`
2. `bun run scripts/command-timings.ts run --key phase130-plan11-rpc -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-rpc --lib sendrawtransaction -- --nocapture`
3. `test -z "$(rg -n 'submit_local_transaction_outcome\\(' packages/open-bitcoin-node/src packages/open-bitcoin-rpc/src --glob '*.rs')"`
4. `bun run scripts/command-timings.ts run --key phase130-plan11-workspace-check -- cargo check --manifest-path packages/Cargo.toml --workspace`
5. `git diff --check`
</verification>

<success_criteria>
- Live local RPC admission preserves exact canonical time/origin/intent metadata.
- Production local submission has no implicit or zero-time path, and the no-time compatibility API is removed only after the RPC caller migration.
- FEEP-03/FEEP-04 shell integration and FEEP-05 direct/shared evidence boundaries are executable and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-11-SUMMARY.md`.
</output>
