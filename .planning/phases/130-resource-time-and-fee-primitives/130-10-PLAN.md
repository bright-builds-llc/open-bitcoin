---
phase: 130-resource-time-and-fee-primitives
plan: "10"
type: execute
wave: 5
depends_on:
  - 130-03
  - 130-04
files_modified:
  - packages/open-bitcoin-network/src/peer/transaction_relay.rs
  - packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
  - packages/open-bitcoin-network/src/lib.rs
  - docs/parity/source-breadcrumbs.json
autonomous: true
requirements:
  - FEEP-04
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 130-2026-07-23T14-26-46
generated_at: 2026-07-23T15:44:00Z
must_haves:
  truths:
    - "Retry policy receives explicit observation time and bounded injected jitter."
    - "Pure network retry vocabulary reads no clock, randomness, timer, transport, or scheduler state."
    - "Phase 130 defines retry inputs without implementing Phase 136 scheduling or clearing behavior."
  artifacts:
    - path: "packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs"
      provides: "Injected retry observation and bounded jitter vocabulary"
      contains: "pub struct RetryDecisionContext"
  key_links:
    - from: "packages/open-bitcoin-network/src/peer/transaction_relay.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs"
      via: "Public module registration and re-export of pure retry input types"
      pattern: "mod retry|RetryDecisionContext"
    - from: "packages/open-bitcoin-network/src/lib.rs"
      to: "packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs"
      via: "Crate-root re-export for later shell consumers"
      pattern: "RetryDecisionContext|RetryJitterSeconds"
---

<objective>
Define the injected network retry time and jitter contract in a focused crate-boundary plan.

Purpose: Complete the retry portion of D-11/D-12 and FEEP-04 without coupling the mempool context plan to network files or implementing deferred Phase 136 behavior.
Output: A pure retry input module, validated jitter value, public re-exports, inline unit tests, and parity breadcrumbs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-resource-time-and-fee-primitives/130-CONTEXT.md
@.planning/phases/130-resource-time-and-fee-primitives/130-RESEARCH.md
@.planning/phases/130-resource-time-and-fee-primitives/130-03-SUMMARY.md
@packages/bitcoin-knots/src/net_processing.cpp
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md

<interfaces>
Plan 130-03 provides canonical mempool metadata and operation-specific context patterns. This plan applies the same explicit-input boundary to network retry without importing mempool policy or adding effect acquisition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define injected retry time and jitter inputs</name>
  <read_first>
    - packages/open-bitcoin-network/src/lib.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay/fanout.rs
    - packages/open-bitcoin-network/src/peer/transaction_relay/scheduler.rs
    - packages/bitcoin-knots/src/net_processing.cpp
    - docs/parity/source-breadcrumbs.json
  </read_first>
  <files>
    packages/open-bitcoin-network/src/peer/transaction_relay.rs
    packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs
    packages/open-bitcoin-network/src/lib.rs
    docs/parity/source-breadcrumbs.json
  </files>
  <behavior>
    - `RetryJitterSeconds` accepts 0 and 300 and rejects values above 300.
    - `RetryDecisionContext` retains the exact observed Unix seconds and injected jitter.
    - Constructing and comparing retry contexts performs no sleeping, clock reads, or random sampling.
  </behavior>
  <action>
    Add `retry.rs` as a pure input-contract module. Define `RetryJitterSeconds` with a fallible constructor bounded to 0..=300 seconds, a typed range error with a fixed label, and `RetryDecisionContext { observed_at_unix_seconds: i64, jitter: RetryJitterSeconds }`. This represents the injected variable portion of the pinned 10-to-15-minute cycle without implementing scheduling, candidate scans, fanout, or transport receipts; Phase 136 owns those behaviors.

    Register the module and re-export the types through `peer::transaction_relay` and the crate root. Add focused inline Arrange/Act/Assert tests. Register `peer/transaction_relay/retry.rs` in `docs/parity/source-breadcrumbs.json` with `net_processing.cpp`; because tests are inline, this one registration covers the new production/test-bearing Rust file. Do not add RNG state, Tokio timers, `SystemTime`, `RandomState`, or a fixed schedule decision.

    Per ASVS-130-V1/V5/V9-boundary/V10-boundary/V11/V13, inventory all workspace imports and same-named retry/jitter/context contracts before adding the public re-exports, including node binaries, network benchmarks, integration tests, and doctests. This is additive and should require no compatibility shim; if an existing symbol collides, migrate the caller or choose a non-conflicting explicit name in this task rather than masking it. The task cannot commit unless all workspace targets compile.
  </action>
  <verify>
    <automated>test "$(bun run scripts/command-timings.ts run --key phase130-retry-list -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib -- --list | rg -c 'retry::.*: test$')" -ge 3 &amp;&amp; bun run scripts/command-timings.ts run --key phase130-retry-context -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib retry -- --nocapture &amp;&amp; bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct (RetryJitterSeconds|RetryDecisionContext)" packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs` finds both types.
    - The list command finds at least three named retry tests before the focused command executes them, preventing zero-match success.
    - Focused tests prove 0/300 acceptance, 301 rejection, and exact context retention.
    - `docs/parity/source-breadcrumbs.json` registers `retry.rs`, and `bun run scripts/check-parity-breadcrumbs.ts --check` accepts it.
    - `rg -n "SystemTime|UNIX_EPOCH|getrandom|RandomState|thread_rng|rand::|tokio::time" packages/open-bitcoin-network/src/peer/transaction_relay/retry.rs` returns no matches.
    - No retry scheduler, fanout emission, or unbroadcast mutation is added.
    - The workspace export/import audit finds no collision or unmigrated caller; the plan cannot commit unless `bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets` exits 0.
  </acceptance_criteria>
  <done>The retry module exposes only validated injected inputs and remains free of scheduling, randomness, clocks, and transport effects.</done>
</task>

</tasks>

<threat_model>
## Assets
- Deterministic retry-policy inputs.
- Future retry scheduling entropy boundary.
- Bounded jitter vocabulary.

## Trust Boundaries
| Boundary | Untrusted input crossing it |
| --- | --- |
| Shell clock/RNG → pure retry context | Effectful samples enter deterministic policy as immutable values. |
| Retry context → future scheduler | Invalid jitter could widen or destabilize the retry cycle. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation and blocking verification |
| --- | --- | --- | --- | --- | --- |
| T-130-12 | Tampering / Repudiation | High | retry time and jitter inputs | mitigate | Explicit immutable context and source scans forbid clock/RNG acquisition in the pure module. |
| T-130-33 | Denial of Service | Medium | retry jitter input | mitigate | Fallible 0..=300 bound; scheduling/work caps remain Phase 136. |

## Security Verification
- ASVS matrix ownership: ASVS-130-V1, ASVS-130-V5, ASVS-130-V9 boundary, ASVS-130-V10 boundary, ASVS-130-V11, and ASVS-130-V13.
- The explicit-input and bounded-jitter threats block on named unit tests and source scans.
- No deferred scheduler, receipt, or unbroadcast behavior is introduced.
</threat_model>

<verification>
Run serially:
1. `test "$(bun run scripts/command-timings.ts run --key phase130-plan10-list -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib -- --list | rg -c 'retry::.*: test$')" -ge 3`
2. `bun run scripts/command-timings.ts run --key phase130-plan10-network -- cargo test --manifest-path packages/Cargo.toml -p open-bitcoin-network --lib retry -- --nocapture`
3. `bun run scripts/check-parity-breadcrumbs.ts --check`
4. `bun run scripts/command-timings.ts run --key cargo-check-workspace-all-targets -- cargo check --manifest-path packages/Cargo.toml --workspace --all-targets`
5. `git diff --check`
</verification>

<success_criteria>
- D-11 and D-12 include an explicit pure-network retry input contract.
- FEEP-04 covers bounded injected time/jitter with no hidden effects.
- Scheduling, candidate scans, transport receipts, and retry clearing remain delegated to Phase 136.
- The Rust commit is blocked unless the exact timed all-target workspace check exits 0 after the public export/import audit.
</success_criteria>

<output>
After completion, create `.planning/phases/130-resource-time-and-fee-primitives/130-10-SUMMARY.md`.
</output>
