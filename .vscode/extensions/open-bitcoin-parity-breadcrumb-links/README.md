# Open Bitcoin Parity Breadcrumb Links

This local VS Code-compatible extension makes source comments such as
`packages/bitcoin-knots/src/script/interpreter.cpp` command-clickable from Rust
files.

VS Code and Cursor should detect local workspace extensions under
`.vscode/extensions/` and prompt to enable them for the workspace. Reload the
window after enabling the extension.
